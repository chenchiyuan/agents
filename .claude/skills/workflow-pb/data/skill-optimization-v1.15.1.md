# SKILL.md v1.15.1 优化记录（对应规范 workflow-pb v0.13.1）

**触发**：迭代 `0029-hub-client-session-and-duplex` 的阶段 6 迭代级终验（`docs/iterations/0029-hub-client-session-and-duplex/clarifications/verify-20260917-101326-iteration-final.md`）三轮抓出主 agent 自身的**过程记账不一致**（PR 子状态行与"全部合并"自相矛盾 / 派发台账口径与表体实数不符 / 新增脚注与阶段状态表一格矛盾），其修复建议包含"阶段推进核查加三向机械比对"；同时用户要求"整理并提交代码"，把该迭代期间的自建核查脚本沉淀为仓库工具。

**根因**：规范要求"每次推进前对照推进条件逐项核查"，但阶段 4 的七项结构性检查（七字段 / 覆盖 / 文件范围不重叠 / 依赖无环 / 无悬挂依赖 / 关键路径 ≤3 / 并发可行性）此前**无任何机械手段**，只能人工通读 8 个 PR 文件与 21 条文件范围——既易漏检，也不可复现（0029 的三处记账问题正是"人工维护的账"与"git 事实"脱节的同类）。

**具体改动**：
- Gate 阶段 4 → 5 的「验证内容」新增一条：可用 `tools/check-pr-gates.py docs/iterations/{迭代ID}` 机械复核七项，**并明确其为辅助**（不替代逐项判断；未过项仍须回到执行角色补充）。覆盖项为提示——豁免（如 0029 的 F04 系 D-16 已裁决出范围 = 19/20）属人的裁决，不计入工具退出码。
- 版本引用三处同步：front matter description、`**版本**` 行（1.15.0 → 1.15.1）、Purpose 段的规范版本引用；`**变更历史**` 指针改指本文件。

**未采纳的方案**：把「验收证据」段的机械核查也一并工具化（`check-acceptance-evidence.py` 已在草稿中写好）——用 0029 **最终态** PR 文件回跑时误报率极高：① 命令自身引用 `/tmp` 检测模式（`grep -nE '/tmp/|<[a-z_]+>' <file>`，其存在恰是"已清理"的证据）；② API 表格中的路径占位符（`GET /api/chats/<chat_id>`）。⇒ **纯文本启发式不足以判定证据合格**，作为门禁会训练人忽略告警；该检查器**未入库**，结论回填 `docs/hub-issues-and-fixes-2026-09-17.md` HB-10：证据格式只能靠 `tasks` 冻结模板解决，机械核查至多作提示。

**验证**：`tools/check-pr-gates.py docs/iterations/0029-hub-client-session-and-duplex` 用**最终态**回跑，输出与阶段 4 当时结论一致——8 个 PR / 21 条文件范围 / 19-20 卡片覆盖（F04 未覆盖 = D-16 出范围）/ 依赖无环 / 无悬挂依赖 / 关键路径 3 / 每个 PR 均有互不可达伙伴，退出码 0。工具泛化过程中实跑发现并修掉一处缺陷：`pr-*.md` 通配把 planner 的 `*-tasks.md` 也计入（16 个），已加排除（阶段 4 核查时 tasks 尚未产出，故原始脚本无此过滤）。

**范围**：纯增量的核查辅助与指针，不改变阶段推进条件与任何流程语义，改完立即生效；不追溯已完成迭代。
