# 工作流进度

**工作流**: workflow-pb v0.8.0
**迭代**: 0016-api-reflection-docs
**当前阶段**: PR 实现（阶段 5）
**迭代分支**: iteration/0016-api-reflection-docs
**状态**: 进行中
**history**: 开启

## 阶段状态

| # | 阶段 | 完成 | 已验证 | 备注 |
|---|---|---|---|---|
| 1 | 需求收敛 | ✅ | ⬜ | demand.md（W1~W7 做什么 / N1~N12 不做 / E1~E10 效果）；六提案全部经用户确认；demand 主动补 W5 元数据必填锁 + W6 写操作防护 |
| 2 | 功能规格 | ✅ | ⬜ | 8 卡 F01~F08（路由表登记 / 既有行为零变化 / 文档页 / 调试台 / llms.txt / 漂移锁三条 / 写操作防护 / 互补互链）；MI-01~MI-07 用户已确认（含判定界"不需改实现、重载后生效"） |
| 3 | 技术架构 | ✅ | ⬜ | architecture.md（943 行，AR-01~AR-09 共 35 子项全填）；**两条 L1 已确认**（新增 `GET /api/docs`；静态白名单加 `/API.md`、`/README.md`）；**事实纠错入档**（§4.5：`?archived=` 空值实测 200 而非 400，更正上游报告口径） |
| 4 | PR 规划 | ✅ | ⏸ | 2 PR（pr-001 路由登记与派生 / pr-002 文档页与调试台）；依赖 pr-002→pr-001（三条代码耦合边有证据）；文件范围互斥；**llms.txt 断言归属已裁决归 pr-001** |
| 5 | PR 实现 | ⏸ | ⬜ | 串行派发：pr-001 先行 |
| 6 | 独立验证 | — | — | 按需触发，不计入线性进度 |

## PR 实现子状态（阶段 5 展开）

| PR 文件 | depends_on | 状态 | worktree 分支 | 已合并 | 槛位状态 |
|---|---|---|---|---|---|
| pr-001-route-registry-and-derivations.md | （无） | ✅ | (已清理) | ✅ | 已释放 |
| pr-002-docs-and-debug-pages.md | pr-001 | ⏸ | feat/0016-pr-002-pages | ⬜ | 占用 |

## 并发配置（阶段 5）

**起始并发数**: 3
**硬上限**: 5
**当前有效上限**: 3
**累计槛位释放次数**: 1
**已派发总数**: 2

> 本迭代 2 个 PR 为真实代码依赖（三条耦合边）→ 无并发分支，阶段 6 并发三项按工作流记"不适用"。

## 待确认项

- [x] **L1-01 新增 `GET /api/docs`**（接口面 10 → 11）→ 2026-09-12 用户确认采纳（见「用户确认记录」区块与 history 的 Gate 确认行）
- [x] **L1-02 静态白名单新增 `/API.md`、`/README.md`**（text/plain；把 web/ 之外的仓库文件变为 HTTP 可读面）→ 2026-09-12 用户确认采纳
- [x] 两点已知局限（API.md 链接不自动滚动到章节；两个新页面不加返回控制台导航）→ 用户接受
- [x] 六项产品提案（API.md 路径级硬锁 / 写操作一次确认 / 文档页字段级 / llms.txt 仅 HTTP 面 / 顶栏 2 入口 / llms.txt 走静态面）→ 用户确认

（原「待确认项」占位段保留于下方，历史留痕不删除。）

- [x] 需求澄清（用户 2026-09-12 决策）：① 调试平台 = **可交互**（按反射元数据生成表单、可发送请求、看真实响应）② 单一真源 = **路由表反射**（把 web.js 的 if 链重构为声明式路由表，文档随表自动同步）③ llms.txt = **HTTP 路由 + 仓库根文件** ④ 与 API.md = **互补共存**（反射管结构与字段，API.md 管叙述与示例）⑤ 入口 = **顶栏入口 + 独立 URL**（/docs、/debug）

## 更新日志

- 2026-09-12: 迭代启动（在线接口文档 + 调试平台 + llms.txt）。前置：0015-hub-orchestration-open-api 已合并 main；测试清理已提交（239 用例）。
- 2026-09-12 13:52:24: pr-001 验收 PASS（保真复核 80 例输入、既有面 73/73 逐字节一致）并合并；pr-002（文档页 + 调试台）已派发。
