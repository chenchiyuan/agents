# verify-20260912-132718-stage234.md — 0016-api-reflection-docs 阶段 2/3/4 独立验证

**验证者身份**：技术规格评审者（能判断「需求 → 功能卡」的覆盖与污染）+ 架构评审者（能判断硬契约是否与既有代码等价、是否避免过度设计）+ 冲刺计划审查者（能识别 PR 依赖是否真实、验收标准是否可独立判定）；事实纠错部分以**独立复现者**身份自测，不采信上游任何"已实测"结论。

**产出物**（校验时点 md5，验证期间未再变动）：

| 产出物 | 路径 | md5 |
|---|---|---|
| 阶段 2 索引 | `docs/iterations/0016-api-reflection-docs/prd.md` | `ec0345745b8f1233929802da8c81f5a6` |
| 阶段 2 卡片 | `.../prd/F01~F08*.md` | F01 `673aaa30…`、F02 `e5243e27…`、F03 `0ebc0fce…`、F04 `2afbe42b…`、F05 `0626c123…`、F06 `3704a7e2…`、F07 `ede1d4b4…`、F08 `27b12489…` |
| 阶段 3 | `.../architecture.md`（944 行） | `cca3ab71e6e1e4eeb8b0a5db121548c6` |
| 阶段 4 | `.../prs/pr-001-route-registry-and-derivations.md`、`pr-002-docs-and-debug-pages.md` | `788a460b…`、`d9c51be4…` |
| 对照基准 | `.../demand.md`（v1.1.0）、`.../status.md`、`.../history.md`；代码现状 `oamp/src/web.js`（770 行 / `8f1fca24…`）、`oamp/src/persist.js`、`oamp/API.md`、`oamp/web/{index.html,style.css,app.js}`、`oamp/test/web.test.js`、`oamp/package.json` | — |

**验证标准来源**：主 agent 委托（context 的 9 条标准）。**未接收任何执行过程上下文**；执行角色在产物中的自述（"已实测""已确认"）一律不作为证据，全部重新取证。

**验证日期**：2026-09-12

**独立实测环境（标准 5/6 的证据来源）**：`/tmp` 临时目录内 `OAMP_DB=<tmp>/data/sql.db` + `OAMP_SOCKET=<tmp>/router.sock` + 随机端口（21000–55000）分别起 `oamp router start` 与 `oamp web start` 子进程，探测后 SIGKILL 回收。**未触碰主仓库运行集群**（未使用 7788、`oamp/.runtime/`、`oamp/data/sql.db`）。探测脚本：`/tmp/v0016-probe{,2,3,4}.mjs`、`/tmp/rx.mjs`。

---

## 逐项判定

### 标准 1 · prd 覆盖性 —— **pass**

- **W1~W7 全覆盖、零新增功能点**：`prd.md`「需求追溯」表逐条映射 W1(前半)→F01、W1(后半)→F02、W2→F03、W3→F04、W4→F05、W5→F06、W6→F07、W7→F08（+F03 验收 8）；8 卡的「**来源**」行均逐条回指 `demand.md` 的 W/D/M/E/N/C 编号（如 F01 来源行含 `W1（前半）；决策 D-2；澄清 M-1 / M-2；效果 E1 / E9…`）。`demand.md` 第二段 §1 只有 W1~W7，无第 8 项能力被发明。MI-01~MI-07 是判定面/边界推导（非新能力），且已全部 `[user_confirmed]`。
- **四要素齐备**：F01~F08 每卡含 `**功能 ID**`、`## 用户价值`、`## 验收标准`（F01 5 条 / F02 8 条 / F03 8 条 / F04 7 条 / F05 7 条 / F06 4 条 / F07 6 条 / F08 6 条）、`## 边界（不包含）`。
- **`[model_inferred]` 零残留**：全目录检索 `\[model_inferred\]` 与 `[架构待填]` 于 `prd/` 下 **0 命中**；`prd.md` 内仅出现在「model_inferred 列表」标题/说明/来源标注行（`| \`model_inferred\` | **无** |`），属历史留档而非活跃标注。
- N1~N12 逐条落入卡边界或全局边界（`prd.md`「N 条款」表）、E1~E10 逐条映射为可判定验收（「E 条款」表）、M/C 条款均有落点（「M 澄清结论与 C 约束」表）——**无悬空条款**。

### 标准 2 · 无技术污染（阶段 2 产品维度）—— **pass**

- 8 卡的产品维度（用户价值 / 验收标准 / 边界）通读：无函数名、模块名、变量名、DOM id、类名、语句写法；无数据模型与接口设计。
- 出现的"技术形态词"全部属 `prd.md` 明示的两类例外且为 demand 已确认的产品可见面：① HTTP 方法与路径（文档页/调试台展示对象本身，W2/W3/D-2）；② 交付物标识与既有命令 `API.md`、`llms.txt`、`npm test`（D-4/D-3/E2）。F05 验收 2 的响应类型 `text/plain; charset=utf-8` 同属用户可见可观测面（D-11 / C-6 / E10 已确认取值）。
- 技术内容全部被隔离在卡末「架构落定（阶段 3）」段与 `prd.md`「架构落定索引」；`prd.md`「术语口径」段显式声明了追溯表/架构表的锚点属**引用**而非本阶段决策。
- 边界外的技术性描述（F02 验收 4 的"前缀 / 后缀 / 精确串"）是**路由可观察行为**的产品化表述（demand C-5② 原始措辞），不构成实现选型。

### 标准 3 · architecture 补全度与 prd 改动范围 —— **partial**（AR 补全与残留项 pass；"改动仅限架构维度" fail）

- **AR-01~AR-09 及其子项逐条可定位：pass**。`architecture.md` §11 恰有 **35** 行 `**AR-xx-y**` 对照行（脚本计数 = 35，覆盖 AR-01-a~e / 02-a~c / 03-a~f / 04-a~f / 05-a~d / 06-a~c / 07-a~d / 08-a~c / AR-09），每行含落定结论 + 详见锚点；8 卡末段逐条给出同一批 AR 的卡内摘要（F01 §AR-01-a~e、F02 §AR-02-a~c、F03 §AR-03-a~f、F04 §AR-04-a~f、F05 §AR-05-a~d、F06 §AR-06-a~c、F07 §AR-07-a~d、F08 §AR-08-a~c），AR-09 全局项落在 `prd.md` 索引 + §11 + §13 R-6。`status.md:16` 记"AR-01~AR-09 共 35 子项全填"，与计数一致。
- **`[架构待填]` 残留：pass**。全文 `\[架构待填\]` 仅 **1** 处（`architecture.md:938`），是越界声明中"本次只回填各卡的 `[架构待填]` 段"的**描述性引用**，非未填占位；`prd/` 下 0 命中。
- **"架构阶段对 prd 的改动仅限架构维度"：fail（可定位）**。`git diff 0ec04e3 d0b15b9 -- docs/iterations/0016-api-reflection-docs/prd*` 显示阶段 3 提交同时改写了**产品维度文本**：
  - 验收标准行的 MI 标注被重写：`F01:18` MI-06 由"服务需重新加载新登记"扩写为"服务重载 + 刷新 / 重新生成即出现；**不要求运行期热更新**"；`F03:19` MI-06 由裸标记扩写为同义长句；`F05:21` MI-05 由"与**取得服务的方式**"改为"与**启动方式**"。
  - MI 列表内容被改写：`prd.md` MI-04 行"本阶段不选边"→"用户已确认取本粒度"、MI-05"取得服务的方式"→"启动方式"、MI-06 增补"索引快照需重新生成…不要求运行期热更新"。
  - 「疑问与越界」第 1、3 条整体改写。
  - 上述内容与 `prd.md:190`「阶段 3 未改动任何产品维度（验收标准 / 边界 / 追溯 / MI 列表 / 本节的 1~5 条）」及 `architecture.md:938`「一字未改」**自相矛盾**（虽有 `prd.md:188` 的"第二轮仅将 7 项推导的标注由 model_inferred 改为 user_confirmed"作为部分说明）。
  - 归因说明（不改变判定）：这批改写是阶段 2→3 门禁上的 MI 确认回合产物（`history.md:44`「MI-01~MI-07 用户已确认」），与架构产物同批提交，因此 `git log -p` 无法区分；但按委托标准"仅限架构维度"的字面判据，本项不通过。
- **架构内部一致性缺陷（同属标准 3，locatable）**：路由表项数 **12 vs 11** 自相矛盾。写"12 项/12 个表项"处：`architecture.md:87`（组件图）、`:134`、`:217`（"`createApiRoutes({})` 返回 12 项"）、`:645`（锁①检查对象"得到 12 个表项"）、`:697`、`:708`、`:837`；`prd.md:170`、`prd/F01:40`、`history.md:52`；写 11 处：`architecture.md:462`（"第 11 项"）、`:681`（"本轮 = 11 条接口"）、`:793`（"第 11 个表项"）、`:795`（"接口面口径 = 11 条"）；`prs/pr-001:25` 写"返回 12 个表项"而 `:29/:31` 写 11 条/11 条签名。§3.3 对照表实际只有 **11 行**（10 既有 + `GET /api/docs`），静态面明确"不入表"（`:332`），⇒ 权威值应为 **11**，"12 项"系沿用旧 `if` 链"12 处 `req.method ===`"计数的残留。此为硬契约（§3/§5.1/§7 均引 12）中的错误数字，同时污染 pr-001 验收 1。

### 标准 4 · L1 判定与留痕 —— **partial**

- **L1 标注正确性：pass**。`architecture.md` §10 明列两条 L1：L1-01 新增登记接口 `GET /api/docs`（接口面 10→11）、L1-02 静态面新增 `/API.md`、`/README.md`；二者确实是**系统边界变更**（新增对外的接口面 / 新增两个仓库文件的 HTTP 可读面）。§13 R-1/R-2 同步登记，取值已写成实现契约（L1-01 五条、L1-02 四条）。
- **"新增静态 URL 暴露两个仓库文件"是否构成系统边界变更：构成，且已正确标注为 L1-02**。它不是"只是多两条路由"——它把 `web/` 之外的仓库文件变成 HTTP 可读资源，属对外可观察面扩张，标注为 L1 并请用户确认是正确的处理。附带被 L1-02 第 1 条披露的 `serveStatic` 前缀守卫基准由 `WEB_ROOT` 改为包根，经代码核实**未扩大可达集**：分发层只按 `STATIC_FILES[p]` **精确键匹配**后才调用 `serveStatic`（`oamp/src/web.js:733-739` 现状 + §3.7 映射表设计），键集合是封闭白名单，`path.join` 归一后 `..` 也无法从键值进入，故不存在未标注的额外边界变更。**未发现其他未标注的 L1**（`/docs`、`/debug`、`/docs.js`、`/debug.js`、`/api-pages.css`、`/llms.txt` 均为同一静态白名单机制内的新增资产，属用户要求的交付物本身）。
- **用户确认留痕：partial（可定位）**。确认痕迹只有：`status.md:16` 阶段表**备注列**（"**两条 L1 已确认**（新增 `GET /api/docs`；静态白名单加 `/API.md`、`/README.md`）"）、`history.md:53`（architect 报告"均经用户确认采纳"）、`history.md:58-60`（调度决策"推进条件满足（L1 两条已确认…）"）。**缺口**：`status.md:38-40`「待确认项」区块没有 L1 条目（该区块只有阶段 1 的需求澄清 5 项，用 `- [x] …（用户 2026-09-12 决策）` 体例）；`history.md` 内没有独立的"用户确认 / Gate"事件行——全部痕迹都源自**执行角色的自报**（architect 报告）与据此作出的调度决策，缺一条由主 agent 记录的、带时刻的用户裁决记录。
- **未标注的决策偏离（并入标准 4 证据）**：`llms.txt` 快照落点与用户已确认决策不一致——demand `D-3`/`D-11`/`W4`/`C-6`/`M-3`/§3「已知受影响文件」与 `status.md:40` 均写"**仓库根 `oamp/llms.txt`**"，而 `architecture.md:346/513/523/535/858`、`prd/F05:43`、`prs/pr-001:19/30` 一律落 **`oamp/web/llms.txt`**；`prd.md:10` 的术语口径仍写交付物标识为 `oamp/llms.txt`。该偏离**未被标注、未被 L1 化、未被用户确认**，且并非静态面机制所迫（L1-02 已把映射基准改成包根并暴露 `API.md`/`README.md`，`/llms.txt → llms.txt` 同样可行）。

### 标准 5 · 事实纠错核实（独立实测）—— **pass**（附 1 条口径不精确的偏差）

**独立实测结果（隔离实例，随机端口，自己的观测）**：

| 请求 | 实测 | §4.5 声称 | 判定 |
|---|---|---|---|
| `GET /api/chats?archived=`（空） | **200** `{"chats":[],"total":0,"limit":50,"offset":0}` | 200（等价不传） | ✔ 一致 |
| `GET /api/chats?state=`（空） | **200** 同上 | 200 | ✔ 一致 |
| `GET /api/chats?q=` / `?limit=` | **200** 同上 | 200 | ✔ 一致 |
| `GET /api/agents?state=`（空） | **200** `{"agents":[]}` | 200（显式判空） | ✔ 一致 |

⇒ **architecture §4.5 对上游报告的更正是成立的**：`demand.md` F-12 / C-5⑥「`?archived=` 空值**走 400**」与实现不符；实测**不存在任何"空值 → 400"路径**，"两处实现不同、结果同为无参/默认值"的表述正确（`web.js:473-476` 的 `num` 空串→`undefined`；`web.js:482-486` 显式判空；`persist.js:105-113` `readArchived(undefined)===0`；`persist.js:122-129` `readOptionalString('')===null`；`persist.js:280` 默认参数 `archived = 0`）。代码证据行号逐一核对无误（`web.js:470-476 / 482-491 / 507-510`、`persist.js:91-103 / 105-113 / 284-287` 均落在所称位置）。

**"真正 400 清单"逐条实测**（8 条全部复现，文案一致）：`?limit=0`→400、`?limit=201`→400（`LIMIT_MAX=200`）、`?limit=abc`→400、`?offset=-1`→400、`?archived=2`→400、`?state=bogus`→400、`?from=abc`→400、`?from=100&to=50`→400。⇒ 清单内**无一条错误**。

**唯一不精确（偏差 D-6，不影响本节判定）**：该清单以"**真正产生 400 的输入**（更正后的正确清单）"陈述，字面读作穷举，实际只覆盖 `/api/chats` 列表查询参数。实测清单外仍有 400/413 来源：`GET /api/agents?state=bogus`→**400**（正是同段讨论的端点）、`GET /api/stream`（缺 `chat_id`）→**400**、`POST /api/messages` 畸形 JSON→**400**、`POST /api/messages` 空 `text`→**400**、70KB 请求体→**413**。（§4.1③ 与 §4.2 Q-8 分别覆盖了 body 类与 stream 缺参，故全篇信息不缺，仅该清单的标题未限域。）

### 标准 6 · 行为保真约束（C-5 七条）可执行性 —— **fail**（② 与现有代码语义不等价，③ 受同一处牵连）

逐条对照 `oamp/src/web.js` 现状 + 隔离实例实测：

| # | 约束 | 判定 | 证据 |
|---|---|---|---|
| ① | 不引入 405 | **pass** | `web.js`/`API.md` 全仓 grep `405` **0 命中**；实测 `PUT /api/agents`→404 `not found: PUT /api/agents`、`DELETE /api/chats/archive`→404 `not found: DELETE /api/chats/archive`、`PATCH /api/chats`→404、`GET /api/messages`→404 |
| ② | 匹配优先级/处理顺序与今日同序，含 `GET /api/chats/archive` 怪癖 | **fail** | 顺序本身同序（§3.3 对照表 11 行与 `web.js:478/494/514/524/545/574/594/635/644/650` 逐条一致；`GET /api/chats/archive`→404 `chat 不存在: archive` 实测一致）。**但 §3.3 C1/C2 声称 `(.*)` + `^…$`「精确复刻今日 `p.slice(...)` 语义」是错的**：今日三条后缀路由的条件是 `p.startsWith('/api/chats/') && p.endsWith('/close'\|'/activate'\|'/rename')` + `slice(prefix.len, -suffix.len)`，后缀可与前缀尾斜杠重叠；编译成 `^/api/chats/(.*)/close$` 等形式后**要求字面 `/` 分隔**，对"剩余段短于后缀"的输入不命中。独立实测（今日真值）：`POST /api/chats/close`→404 `chat 不存在: `、`POST /api/chats/activate`→404 `chat 不存在: `、`POST /api/chats/rename`→404 `chat 不存在: `，且 `POST /api/chats/rename` 带畸形 JSON 体→**400**（读体在 handler 内先于 404）。按 §3.3 规则编译出的正则对这三条路径 `exec()` 返回 **null**（`/tmp/rx.mjs` 输出：rx#4 vs `/api/chats/close` = MISS、rx#6 vs `/api/chats/activate` = null、rx#7 vs `/api/chats/rename` = null），表驱动下将落到 404 `not found: POST /api/chats/…`（且不读体 ⇒ 状态码 400→404）。⇒ F02 验收 4 的"匹配结果逐项一致"与验收 2 的"字节等价"在这 3 个输入上不成立；§4.2 怪癖清单 Q-1~Q-8 **未覆盖**该类输入，L1 既有 39 条用例亦不覆盖（grep：测试只出现 `POST /api/chats/archive` 与带真实 id 的 `/close\|/activate\|/rename`） |
| ③ | `readBody` 留 handler 内、`/rename` 顺序逐字 | **pass（附注）** | 分发层从不读体（`web.js:596-605`/`651-661` 均在 handler 内；§3.4 分发循环只 `await` + `return`）；实测正常路径 `POST /api/chats/unknown-id/rename` + 畸形体 → **400**（读体先于 404），顺序保持。**附注**：②的正则缺口会让空捕获形态（`/api/chats/rename`）连体都不读，状态码由 400 变 404——属 ② 的同一根因 |
| ④ | SSE 独占 `res`，分发层不包装返回值 | **pass** | `web.js:644-649` `transport.handle(req,res,{chatId:null})` 后直接 `return`；实测 `GET /api/events`→**200** `content-type: text/event-stream; charset=utf-8`、首帧 `retry: 1000`（未被包装/未被二次序列化）；`GET /api/stream`（缺参）→400 `需要 chat_id（不做全局订阅）` 与 Q-8 一致 |
| ⑤ | 单 try/catch → 502 覆盖面不缩小 | **pass** | 实测匹配期抛错落 502：`GET /api/chats/%E0%A4%A`→**502** `router 不可达或请求失败: URI malformed`；`GET /api/chats/%E0%A4%A/close`、`POST /api/chats/%E0%A4%A/rename` 同；`web.js:477/742` 单 `try` 包住匹配 + 调用，404 文案 `not found: ${req.method} ${p}`（:741）逐字一致 |
| ⑥ | 既有参数空值语义原样保留 | **pass** | 实测空值一律 200（见标准 5），`num` 函数体与 `qs` 调用点、`persist` 校验零改动是可行契约（§4.2 Q-6 已按更正口径给出期望值，探针期望值以"改造前实测固化"为准） |
| ⑦ | 零第三方依赖 | **pass** | `oamp/package.json:14` `"dependencies": {}`；方案只用 `node:` 内置与既有模块 |

⇒ 7 条中 **6 条可执行且与现状一致，② 不可执行**（其"结构性保证"与今日语义不等价），故本条标准判 **fail**。

### 标准 7 · prs 格式与依赖正确性 —— **pass**

- **七字段齐备**：脚本校验两文件均含 `## 上下文摘要`/`## 涉及功能点`/`## 文件范围`/`## 验收标准`/`## 参考资料`/`## depends_on`/`## batch`（各 10 条 `- [ ]` 验收项；文件范围 5 + 9 项）。
- **`depends_on` 证据真实（三条耦合边逐条核实）**：pr-002 → pr-001，理由给出的三条均为**代码/文件级**耦合，非叙述顺序 —— ① `docs.js`/`debug.js` 以 `fetch('/api/docs')` 取数并消费 `projectRoutes` 投影字段（接口/共享符号，由 pr-001 新建）；② `index.html` 新增锚点目标 `/docs`、`/debug` 与 `api-pages.css` 的可达性来自 pr-001 的 `STATIC_FILES` 新增项（文件/登记面，证据行 `oamp/src/web.js:239-253` 的静态白名单现状经核对存在）；③ `test/api-pages.test.js` 断言的三个 URL 可达性同源。pr-001 无依赖并写 `（无）`。
- **文件范围无重叠**：pr-001 = {`oamp/src/web.js`、`oamp/scripts/gen-llms-txt.mjs`、`oamp/web/llms.txt`、`oamp/API.md`、`oamp/test/api-routes.test.js`}；pr-002 = {`web/docs.html`、`web/docs.js`、`web/debug.html`、`web/debug.js`、`web/api-pages.css`、`web/index.html`、`web/style.css`、`oamp/README.md`、`oamp/test/api-pages.test.js`} —— **交集为空**，并集覆盖 `architecture.md` §2.2 列出的全部 14 个受影响文件（无遗漏）。
- **依赖图无环**：pr-001（根）→ pr-002，单向，无环。
- **F01~F08 全覆盖**：pr-001 引 F01/F02/F05/F06/F08，pr-002 引 F03/F04/F07/F08 ⇒ 8/8 被引用，无功能点漏挂。

### 标准 8 · 验收标准可独立判断 —— **partial**

- **可判定性：pass（总体）**。两 PR 共 20 条验收项逐条检视，均为可执行判定（起实例打请求 / 跑 `node --test` / `git diff` 为空 / 集合求差 / 依赖清单读取 / 浏览器人工核对），不依赖第三个 PR；浏览器侧项（页面渲染、确认对话框、订阅面板）由 `architecture.md` §13 R-10 显式承接为阶段 5 人工/浏览器实测，不构成"不可判定"。
- **F08 验收 3 的跨 PR 拆分：合理**。pr-001 先在 `API.md` 落顶部链接并**显式声明**"链接'能打开页面'的完整判定在 pr-002 的验收标准内"（pr-001 验收 10），pr-002 验收 9 承接完整判定；拆分由"pr-001 独占 `API.md` 写权、pr-002 不碰该文件"的结构决定，两个 PR 各自仍有可独立判定的条目，属合法拆分（`depends_on` 已声明）。**F08 验收 4 无跨 PR 问题**（`docLink` 由 pr-001 投影、渲染由 pr-002 完成，pr-002 验收 9 一并判定）。
- **互斥冲突：fail（可定位）**。pr-001 的验收项**无法同时满足**：验收 1「`createApiRoutes({})` 返回 **12 个表项**且不抛错」（`pr-001:25`）与验收 5「`routes` 为 **11 条**（含本接口自身）」（`pr-001:29`）、验收 7「登记侧 **11 条**签名集合」（`pr-001:31`）直接冲突；且"12"与 `architecture.md` §3.3 的 11 行对照表、§5.1「第 11 项」、§10 L1-01「第 11 个表项 / 接口面 11 条」相互矛盾（见标准 3 缺陷）。最坏后果：实现者按验收 1 写"12"会与验收 5/7 及漂移锁③的登记侧集合同时红。

### 标准 9 · 三条漂移锁的可测性 —— **pass**

- **锁① 元数据必填**：检查对象 = `createApiRoutes({})` 的**真实返回值**（§7.1、`prd/F06:37`）——不读源码文本，落地为 `import { createApiRoutes } from '../src/web.js'` 进程内调用（纯构造、空 dep 不抛错；`architecture.md:217/§3.2` 已把"纯构造"写成契约且可自测）。可执行。
- **锁② 索引快照逐字节**：检查对象 = `renderLlmsTxt(projectRoutes(createApiRoutes({})))` 的**真实输出** vs 文件**真实字节**（Buffer 比对 + 首处差异 line/col/byte + 修复命令）；生成函数被约束为纯函数（不读盘/不看时间/env/端口），逐字节确定性成立。可执行。
- **锁③ 契约文档路径级双向覆盖**：检查对象 = **两个集合求差**（登记侧遍历表项 `method + shape(path)`；文档侧从 `oamp/API.md` 抽反引号签名后同一 `shape()` 归一）。**这是对"被锁对象"（markdown 文档）本身做文本抽取，不是对被锁代码读源码正则**，属正确的检查对象选择；识别形态由 §6.7 / `prd/F08:41` 钉死（反引号包裹 `METHOD /api/<path>`，反例不得用该形态）。
  **独立复现该规则**：以 `\`(GET|POST) (/api/[^\`\s]*)\`` 抽取当前 `API.md` → 35 处命中、**12 种拼写变体**（`<chat_id>`/`<id>`/`?state=online`/`?chat_id=<id>` 等），归一后集合**恰好等于既有 10 条路由**，`R\D = ∅` 且 `D\R = ∅` ⇒ 现状零假阳性、零假阴性，本轮只需补 `GET /api/docs`。可执行。
- **偏差（不影响判定）**：`architecture.md:689` 的 S-10 写"**13 处签名**含 3 种拼写变体"，按其所声明的抽取规则实测为 **35 处 / 12 种拼写**（归一后 10 条）——结论正确、计数不准确。

---

## 汇总

- **pass：5 项**（标准 1、2、5、7、9）
- **fail：1 项**（标准 6 —— C-5② 的正则等价性不成立）
- **partial：3 项**（标准 3、4、8）
- **blocked：0 项**

## 偏差记录

> 实现/架构与现有规格或文档不一致之处。不影响"标准条目"的通过与否，但应在下一迭代同步文档或评估调整。

| # | 规格/文档描述 | 实现实际行为（实测/核实） | 建议处理 |
|---|---|---|---|
| D-1 | `demand.md` F-12 / C-5⑥ 与 `prd/F02:28`（验收 7）：「`?archived=` 空值**走 400**」「空值参数一处视为无参、一处**走参数错误**」 | 实测 `?archived=` 空 → **200**；`?state=` 空 → **200**；`/api/agents?state=` 空 → **200**；**不存在任何"空值 → 400"**（真正 400 来自 `limit`/`offset`/`archived`/`state` 的**非法值**，非空值） | F02 卡产品维度已冻结（阶段 3 未改，其 AR 段 ⑥ 已注明"阶段 3 实测修正"）；`architecture.md` §4.5 亦已入档。**建议**：按工作流把该需求层事实错误写入 `deferred-demand-changes.md`（本轮该文件不存在），或下一迭代修正 F02 验收 7 的描述性从句（不改其判定方式） |
| D-2 | `demand.md` D-3 / D-11 / W4 / C-6 / M-3 与 `status.md:40`：「llms.txt 快照 = **仓库根 `oamp/llms.txt`**」；`prd.md:10` 交付物标识仍写 `oamp/llms.txt` | `architecture.md:346/513/523/535/858`、`prd/F05:43`、`prs/pr-001:19/30` 一律落 **`oamp/web/llms.txt`**（HTTP 与快照同一文件）；全目录无任何"落点变更"的说明或用户确认 | 二选一：① 改回仓库根 `oamp/llms.txt`（L1-02 已把映射基准设为包根，`/llms.txt → llms.txt` 同样可行）；② 保留 `web/llms.txt` 但作为**已确认偏离**记录（需用户拍板）。当前状态为"用户已确认的决策被静默改址"，建议优先处理 |
| D-3 | 路由表项数：`architecture.md:87/134/217/328/645/697/708/837`、`prd.md:170`、`prd/F01:40`、`history.md:52`、`prs/pr-001:25` 写 **12 项 / 12 个表项**（`:328` 为"12 个新增表项包装行"） | §3.3 对照表只有 **11** 行（10 既有 API + `GET /api/docs`），§3.7 明确静态面"不入表"；`architecture.md:462/681/793/795` 与 `prs/pr-001:29/31` 写 **11**。权威值 = 11（12 系旧 `if` 链"12 处 `req.method ===`"计数残留） | 统一为 **11**（含 pr-001 验收 1 的"返回 12 个表项"必须改为 11，否则与验收 5/7 及锁③集合冲突） |
| D-4 | `prd.md:190`「阶段 3 未改动任何产品维度（验收标准 / 边界 / 追溯 / MI 列表 / 本节的 1~5 条）」与 `architecture.md:938`「验收标准、边界、用户价值、需求追溯、MI 列表、疑问与越界章节**一字未改**」 | `git diff 0ec04e3 d0b15b9` 显示阶段 3 提交实际改写了 `F01:18`、`F03:19`、`F05:21` 的验收标准行内 MI 标注、`prd.md` MI 表 MI-04/05/06 内容、`prd.md` 疑问与越界 1/3 条、各卡「来源」行 | 把表述改为"**产品结论未改**；MI 标注按用户确认（2026-09-12）由 `model_inferred` 转正并同步措辞"，避免留下"改过却声明未改"的记录 |
| D-5 | `architecture.md:689` S-10「`API.md` 中反引号签名共 **13 处**…含 3 种拼写变体」 | 按其声明规则实测 = **35 处命中 / 12 种拼写变体**（归一后恰为 10 条路由，集合等式仍成立） | 校正计数（结论无需改） |
| D-6 | `architecture.md:426`「**真正产生 400 的输入**（更正后的正确清单）」字面穷举 | 实为"`/api/chats` 列表查询参数"范围；清单外另有 400/413：`/api/agents?state=bogus`→400、`/api/stream` 缺参→400、`POST /api/messages` 畸形 JSON→400 / 空 text→400 / 70KB 体→413 | 加限定语（"列表查询参数的全部 400 来源"）或补全；该节为硬契约，限域不清会误导实现/验证 |
| D-7 | `architecture.md:325`（§3.6）「仅这一行（`:515/:525/:575/:595` 共 **5 处**）」与 `:328`「5 行 `chatId` 取值行」、`prd/F01:44`「5 处 `decodeURIComponent(p.slice(…))` 改为 `params.chat_id`」 | `oamp/src/web.js` 实际 **4 处** `decodeURIComponent`（:515 / :525 / :575 / :595，各自单行；grep 计数 = 4） | 改为 **4 处**（数量写错会让"handler 体零改写"的 diff 核验口径不一致） |

## 下一迭代候选

- **C-1｜L2 探针集盲区**：`architecture.md` §4.2 的 Q-1~Q-8 未覆盖"后缀路由空捕获"形态（`POST /api/chats/close|activate|rename`），而这正是本轮改动新引入且**既有 39 条用例完全不覆盖**的回归面。建议下一迭代把该类输入补进特征化探针（并与标准 6 的 fail 一并处理）。
- **C-2｜`deferred-demand-changes.md` 机制本轮未启用**：阶段 3 发现了上游需求依据（`demand.md` F-12）的事实错误，选择只写进 `architecture.md` §4.5 而未按工作流"需求层问题 → 直写 `deferred-demand-changes.md`"留痕，导致下一迭代/阶段 6 缺一个显式入口（本报告 D-1 代为记录）。
- **C-3｜静态面 404 形态的中间态**：pr-001 单独合并后 `/docs`、`/debug` 等已登记 URL 会由 JSON 404 变为**纯文本 404**（`serveStatic` 读失败路径，`web.js:241-243`），`prs` 已写明该中间态。属既有事实（F-7），但值得在下一迭代评估"未交付资产的 404 形态"是否需要一致化。
- **C-4｜`API.md` 以 `text/plain` 提供导致锚点不滚动**（§13 R-3，用户已接受）：下一迭代可评估零依赖的替代（如静态 HTML 化的说明页），当前按"接受现状"处理。
- **C-5｜`llms.txt` 落点与用户决策不一致**（D-2）：需产品侧裁决后再动，不由实现层自行决定。

## 结论

**FAIL**（存在 fail 条目）

- 需返工的根因只有一处，但影响硬契约与 pr-001 验收：**§3.3 的 `(.*) + ^…$` 不是今日 `startsWith + endsWith + 双侧 slice` 的等价复刻**（3 个可实测的具体输入会从 `404 chat 不存在: ` 变为 `404 not found: …`；其中 `/api/chats/rename` 带畸形体还会从 400 变 404），而 §4.2 怪癖清单与 L1 既有用例都不覆盖该类输入（标准 6 fail → 标准 6 的②）。
- 两个 partial 的实质：**标准 3/8** 由"12 vs 11"这一处计数错误与阶段 3 提交内含产品维度文本改写共同造成（均附行号，可一次性修正）；**标准 4** 是 L1 用户确认缺一条主 agent 侧的独立记录，外加 `llms.txt` 落点对已确认决策的静默偏离。
- 事实纠错部分**经我独立实测确认无误**：架构 §4.5 对上游"`?archived=` 空 → 400"的更正是正确的（实测 200），所列 400 清单内逐条准确；偏差点仅在"清单未限域"。
- 阶段 2/4 的覆盖性、无污染、格式、依赖证据、锁的可测性均通过，无需返工。

---

### 附：报告自检

- 全文未使用执行过程上下文（未采信"已确认/已实测/设计意图"类自述作为证据；所有结论均有文件行号或隔离实例观测）。
- 每条 fail/partial 均可定位（文件:行 / 实测命令输出 / git diff 范围）。
- 未修改任何被验证产出物；本次唯一写入 = 本报告。
