# verify-20260912-141646-stage6 — 0016-api-reflection-docs 迭代最终产物独立验收

## 身份与交付元信息

| 项 | 值 |
|---|---|
| **验证者身份（反射结果）** | **验收型发布评审者（release gate reviewer）**——长期做"迭代关闸"的人：既看单 PR 的验收证据链是否闭合，也看**迭代作为一个整体**是否自洽（阶段产物与代码产物是否互证、协议承诺是否被工程手段（而非人工约定）兜住、状态记录是否忠实于 git 事实）。判据优先级 = **可复现的实测证据 > git 一手记录 > 报告自述**；对"文档说锁住了"一律要求看到锁真的会红。 |
| **产出物（验证范围）** | `iteration/0016-api-reflection-docs` 分支全量：`docs/iterations/0016-api-reflection-docs/`（demand.md / prd.md + prd/F01~F08 / architecture.md / prs/pr-001+pr-002（+tasks）/ status.md / history.md / clarifications/verify-*×3）+ `oamp/`（`src/web.js`、`llms.txt`、`scripts/gen-llms-txt.mjs`、`API.md`、`web/{docs,debug}.{html,js}`、`web/api-pages.css`、`web/{index.html,style.css}`、`README.md`、`test/{api-routes,api-pages}.test.js`） |
| **验证标准来源** | 主 agent 委托的 A~J 十条验证标准（原样内联于 brief）+ 为判定 A/C/D 而引用的 `workflow-pb`（v0.8.0）§阶段定义 / §PR 文件格式规范 / §验证目标（阶段 6 独立验证读）+ 两张 PR 卡与 architecture.md（作为 A 的判定对象） |
| **验证日期** | 2026-09-12 |
| **验证方式** | 纯只读主仓库（验证前后 `git status --porcelain` 仅剩 3 个与 0016 无关的既有未提交改动，见偏差 D-5）+ 所有篡改/对照实验在 `/tmp/v0016-stage6/` 副本中进行（已删除）+ 隔离实例（临时 `OAMP_SOCKET` / `OAMP_DB`、端口 46151、已 stop）跑真实 HTTP 与真实无头 Chromium + 主仓库 `oamp/` 内串行跑全量测试 |
| **未使用的信息** | 未接收、未使用任何执行过程叙述（"为什么这么设计 / 当时是怎么做的"）；全部判定只基于产出物、规格文本、git 记录与实测输出 |

**隔离性声明**：主仓库运行中的集群（router PID 15766 / web PID 15790，`127.0.0.1:7788`）验证前后 PID 与监听状态不变，未被请求、未被重启；我的隔离实例（router/web 于 `/tmp` 副本，端口 46151）已停止，`/tmp/v0016-stage6/` 已 `rm -rf`。

---

## 逐项判定

### A. 阶段 5 推进条件（逐 PR 核实）— **pass**

三个子项分别核实：

**A-1 两个 PR 的验收标准全部通过（交叉引用 verify 报告）— pass**

| PR | 交叉引用的独立验收 | 结论 | 本轮一手复核（不采信报告结论、只看产出物） |
|---|---|---|---|
| pr-001 | `clarifications/verify-20260912-135131-pr001.md` | **PASS**（10/10 标准 pass、0 fail/0 partial、3 偏差） | 见 E①②③：11 条登记/投影形状/llms.txt 逐字节/锁转红全部由我独立复现 |
| pr-002 | `clarifications/verify-20260912-140856-pr002.md` | **PASS**（10/10 标准 + PR 卡 A1~A10 全 pass、4 偏差） | 见 H：文档页渲染与投影一致、真实发送 `200 · 6 ms`、POST 确认 1 次且 fetch 0 次、SSE 订阅可停、顶栏 2 入口可跳转，全部由我在隔离实例 + 真实浏览器复现 |

**A-2 无简报外改动 — pass**

按提交逐个比对 PR 卡的「文件范围」：

- `c3a68e4`（PR-001 实现）6 个文件 = 卡面 5 个（`src/web.js`、`scripts/gen-llms-txt.mjs`、`llms.txt`、`API.md`、`test/api-routes.test.js`）+ 阶段 5 强制产物 `prs/pr-001-…-tasks.md`（workflow-pb「阶段 5」输出列明）。
- `93687dd`（PR-002 实现）10 个文件 = 卡面 9 个（5 个 web 资产 + `index.html` + `style.css` + `README.md` + `test/api-pages.test.js`）+ `prs/pr-002-…-tasks.md`。
- `oamp/` 内两 PR 文件集合**零重叠、零遗漏**；其余提交（`bdceb77`/`d637a84`/`9d6e49e`/`73ccc8e` 等）只改 `docs/iterations/0016-…/`（status/history/verifier 报告归档），不触碰 `oamp/`。
- 附加核实：`oamp/test/web.test.js`、`oamp/web/app.js`、`oamp/package.json` 相对 base numstat **无输出（零改动）**。

**A-3 PR 文件齐备 — pass**

两张卡各含七字段（标题行号）：`## 上下文摘要` / `## 涉及功能点` / `## 文件范围` / `## 验收标准` / `## 参考资料` / `## depends_on` / `## batch`——pr-001 于 :3/7/15/23/36/45/49，pr-002 于 :3/7/14/26/39/48/52；`depends_on` 均存在（pr-001 为「（无）」，pr-002 引用 pr-001）且 `batch` 有值。功能点覆盖：pr-001 = F01/F02/F05/F06/F08，pr-002 = F03/F04/F07/F08，并集 = F01~F08 无缺口。

> 时序瑕疵（不改变本项判定，已记偏差 D-2）：pr-001 卡在实现提交（13:43:13）之后被修改（13:52:24，`d637a84`）。

### B. 并发调度真实执行证据 — **pass（三项均判「不适用」，附证据）**

**前置事实（触发条件不成立）**：`pr-002` 的 `depends_on` 只有 `pr-001`，且声明了三条代码级耦合边（`fetch('/api/docs')` 投影来源、`STATIC_FILES` 的 `/docs`/`/debug`/`/api-pages.css` 映射、页面用例断言的 MIME 依赖 `STATIC_TYPES`）——两条 PR 是**真实串行依赖**，`prs/` 依赖图不存在并发分支 ⇒ workflow-pb「并发调度真实执行证据」的触发条件（≥2 个可同时进入阶段 5 的 PR）**不成立**。

**三项核查的判定与证据**：

| 协议核查项 | 判定 | 证据 |
|---|---|---|
| ① worktree 时间窗口重叠 | **不适用** | 无并发分支：`git log -1 --format=%P 93687dd` = `1d073fc…`（PR-002 提交的父提交 = **PR-001 合并进迭代分支的那次 merge**）⇒ PR-002 的 worktree 只能从"已合入 PR-001 的迭代分支"拉出，两个 worktree 不可能同时存活。`git worktree list` 现存 worktree 仅主仓库 + `agents-0004-model-dispatch`（0016 的两个 feat 分支已清理，与 `status.md` 标注 `(已清理)` 一致） |
| ② 并发配置区块真实初始化与更新 | **不适用（区块本身已初始化且被更新）** | `git show bd9736e:…/status.md`（阶段 4→5 入口初始化）：起始并发数 3 / 硬上限 5 / 当前有效上限 3 / 累计槛位释放次数 0 / 已派发总数 1（五字段均有值）；`git show 73ccc8e:…/status.md`（阶段 5 结束）：累计槛位释放次数 **0→2**、已派发总数 **1→2** ⇒ 区块确实被后续释放事件更新过，非"初始化后再未变化" |
| ③ 爬升公式真实触发 | **不适用** | 本迭代 PR 数（2）从未产生"已解锁且排队中"的 PR，未触发爬升计算前提（协议明示：未触发爬升条件时本项记"不适用"）。**但落盘字段与公式不一致**：`累计槛位释放次数 = 2` 时公式给出 `min(3 + 2×3, 5) = 5`，而 `status.md` 记 `当前有效上限 = 3`（未重算）——记偏差 **D-1**（0015 阶段 6 出现过同类问题并已按公式修正为 5，本次复发） |

### C. 依赖正确性（`depends_on` 与已合并事实一致）— **pass**

- **git 事实**：`93687dd`（PR-002 实现）的父提交 = `1d073fc`（`merge: pr-001 … into iteration/0016`）⇒ PR-002 的工作分支 base = **已合入 PR-001 的迭代分支**，与卡面声明完全一致；PR-001 的合并（`1d073fc`）在 PR-002 提交之前，`history.md` 的「槛位释放 → 立即派发 pr-002」记录与之一致。
- **代码级证据（三条耦合边逐条落地）**：① `oamp/web/docs.js` 与 `oamp/web/debug.js` 均以 `fetch('/api/docs')` 取数并消费投影字段（`route.method/path/summary/params/response/errors/kind/danger/docLink`）；② `oamp/src/web.js:261-274` 的 `STATIC_FILES` 提供 `/docs`、`/docs.js`、`/debug`、`/debug.js`、`/api-pages.css` 五个键（我实测这五个 URL 均 200 且 MIME 正确，见 H）；③ `oamp/test/api-pages.test.js` 断言的正是这五个 URL 的可达性与响应类型 ⇒ 若 pr-001 未合并，pr-002 的三条耦合边都无来源。
- **无反向依赖**：PR-001 的提交不含任何 pr-002 资产引用，其验收标准也显式把"链接能打开页面"的完整判定交给 pr-002（`pr-001-…md` 验收 10 末句）。
- **依赖图无环**：两条 PR 单向 pr-002 → pr-001。

### D. PR 粒度复核（逻辑原子性 / 可审查性 / 独立性）— **pass**

| PR | 逻辑原子性 | 可审查性 | 独立性 | 判定 |
|---|---|---|---|---|
| pr-001（路由表 + 三个派生面 + API.md 同步） | 一句话可描述：**"把 if 链变成有序登记表，并让三个派生面从同一份投影生成"**。三个派生面（`/api/docs` 投影、`llms.txt` 快照、三条漂移锁）都是**同一机制的下游**，无其一则其余无意义（无表则无投影、无投影则锁②③无可比对对象）；回滚它不会牵连无关功能（既有 10 条 API 的行为保真已被 39 条既有用例 + 80 例隔离双服务 diff 证明） | 只有 1 个既有源文件被改（`src/web.js`）+ 4 个新增文件，reviewer 全程在一个心智模型内（表项 → 匹配 → 投影）；无跨模块跳跃 | 18 条验收标准中 17 条可在 PR-002 未合并时判定；唯一跨 PR 的"链接目标可打开"已在卡面显式划归 pr-002 | pass |
| pr-002（文档页 + 调试台 + 顶栏入口 + 资产同步） | 一句话可描述："新增两个消费 `/api/docs` 投影的页面 + 控制台入口"，不再夹带任何服务端逻辑 | 5 个新资产 + 3 个既有文件的小改（`index.html` +2 行、`style.css` +3 行、`README` 同步）；与 PR-001 文件范围零重叠 | 验收标准全部依赖 PR-001 已合并（`/api/docs` 存在、5 个静态映射生效），卡面已明示 | pass |

未发现"行数驱动"的粒度信号（本迭代为自动生成的登记/投影代码，行数不作粒度依据）；PR 划分未出现"把顺序偏好当依赖"（`depends_on` 三条边均有代码证据）。

### E. 最终产物一致性（三条硬契约抽查）— **pass**

**① 匹配等价形态（`exact` / `prefixSuffix`，无正则，含重叠边界）— pass**

- 实现层面（`oamp/src/web.js:289-306`）：仅两种判据——无参数段 `pathname === pathPattern`；单参数段 `startsWith(prefix) && endsWith(suffix)` + `slice(prefix.length, len - suffix.length)`。全文件 `grep "new RegExp|RegExp("` = **零命中**，matcher 体内无正则字面量。
- 行为层面（我在进程内直接 import `createApiRoutes`/`matchRoute` 的独立探针）：

| 输入 | 命中 | 参数 |
|---|---|---|
| `POST /api/chats/close` | `POST /api/chats/:chat_id/close` | `chat_id=""`（**前后缀重叠 → 空串，不是"不命中"**） |
| `POST /api/chats/activate` | `…/:chat_id/activate` | `chat_id=""` |
| `POST /api/chats/rename` | `…/:chat_id/rename` | `chat_id=""` |
| `GET /api/chats/archive` | `GET /api/chats/:chat_id` | `chat_id="archive"`（字面量被贪婪参数段吸收，与改造前一致） |
| `GET /api/chats/` | `GET /api/chats/:chat_id` | `chat_id=""`（空尾） |
| `GET /api/chats/a/b` | `GET /api/chats/:chat_id` | `chat_id="a/b"`（含 `/`） |
| `POST /api/chats/a/b/close` | `…/:chat_id/close` | `chat_id="a/b"` |
| `GET /api/agents/` | 无命中（尾斜杠不归一） | — |
| `PUT /api/agents` | 无命中（**方法不匹配 = 不进入候选 ⇒ 无 405**） | — |

**② 三条漂移锁（元数据必填 / llms.txt 逐字节 / API.md 路径级双向）— pass**

- 三条锁各自是独立顶层用例：`oamp/test/api-routes.test.js:185`（锁①）/`:268`（锁②）/`:293`（锁③）——一条失败不掩盖另两条（用例 4 要求）。
- **我自己的双向篡改实验**（`/tmp/v0016-stage6/` 副本，未触碰产出物）：

| 篡改 | 锁① | 锁② | 锁③ | 锁输出的点名信息（摘录） |
|---|---|---|---|---|
| **新增一条登记**（`routes.push({GET /api/probe6, …})`） | **RED** | **RED** | **RED** | ②`llms.txt 与生成结果不一致：首处差异 line 11 col 8（byte 507）`；③`API.md 缺登记: GET /api/probe6` |
| **删除一条登记**（`routes.splice(4,1)`，移除 `POST /api/chats/archive`） | **RED** | **RED** | **RED** | ②`期望: ## 接口（10 条） / 实际: ## 接口（11 条）`、`行数: 期望 29 / 实际 30`；③`API.md 多出未登记路径: POST /api/chats/archive` |

⇒ 三条锁在增/删登记两个方向上均真实转红，且失败信息可定位（行/列/字节 + 缺失路径名），非"形式上的锁"。

**③ 派生面（`/api/docs` 11 条 + 8 字段 + `danger` 派生；`/llms.txt` 与包根快照逐字节相等）— pass**

- 进程内独立计算（直接 import `createApiRoutes` / `projectRoutes` / `renderLlmsTxt`）：登记 **11 条**，顺序为 `agents → chats → chats/:chat_id → chats/:chat_id/close → chats/archive → chats/:chat_id/activate → chats/:chat_id/rename → stream → events → messages → docs`；投影键集合 = `method,path,summary,params,response,errors,kind,docLink` + 派生 `danger`；`danger === (method !== 'GET')` 对全部 11 条成立（POST 恰 5 条 = true）；`handler` **未泄漏进投影**。
- `renderLlmsTxt(projectRoutes(createApiRoutes({})))` 与仓库 `oamp/llms.txt` **逐字节相等（1825 B / 1825 B）**；在副本中重跑 `node oamp/scripts/gen-llms-txt.mjs` → 输出与仓库快照 `cmp` **IDENTICAL**（生成器与快照同源、快照新鲜）。
- 隔离实例 HTTP 实测：`/api/docs` → `200 application/json; charset=utf-8`；`/llms.txt` → `200 text/plain; charset=utf-8` 且响应体与包根快照 `cmp` **IDENTICAL**（"单产物结构"成立）；`/API.md`、`/README.md` → `200 text/plain; charset=utf-8`。

### F. 测试 — **pass**

- 迭代分支上**串行**实跑（工作目录 `oamp/`）：`node --test --test-concurrency=1 test/*.test.js` → `1..251 / # tests 251 / # pass 251 / # fail 0`（`duration_ms 155358.7`）——与期望 **251** 一致。
- `oamp/test/web.test.js` 相对 base（`5a6d815`，= `main` tip）`git diff --numstat` **零输出（零字节改动）**；`oamp/test/` 下相对 base 仅新增 `api-routes.test.js`、`api-pages.test.js`（既有 20 个测试文件零改动）。
- 测试运行全程使用系统临时 socket/DB（`test/helpers/harness.js` 明确"测试绝不触碰仓库内 .runtime/"），主仓库运行中的集群未受影响（见隔离性声明）。

### G. 搭置文件（`deferred-demand-changes.md`）— **pass**

`docs/iterations/0016-api-reflection-docs/deferred-demand-changes.md` **不存在**（`ls` 返回 `No such file or directory`）⇒ **本迭代无搭置的需求变更/错误记录，无内容可摘录**。据 workflow-pb「搭置的需求变更/错误报告」，此项如实声明即可。

### H. 前端行为可达性（可观察判定面）— **pass**

逐条给出判定面 + 我的一手观测（隔离实例 `127.0.0.1:46151` + 真实无头 Chromium）：

| 用户可见验收 | 可观察判定面 | 我的一手观测结果 |
|---|---|---|
| 文档页渲染 / 与投影一致 | 浏览器 DOM 的 `.route-card` 序列 vs `GET /api/docs` 投影序列 | **11 卡片**，路径与方法序列与投影逐条相同；`document.querySelectorAll('pre').length = 0`；人工撰写标注逐字在页（`参数语义说明为人工撰写，结构与分发来自路由表`） |
| 调试台真实发送与耗时 | 结果区显示"真实状态码 · 毫秒" + 真实响应体 | 选中 `GET /api/agents` 点击发送 → 结果区 `200 · 6 ms` + 真实体 `{ "agents": [] }`；`#route-list` 渲染 **11 条**按钮（顺序与投影一致） |
| POST 防护 | `window.confirm` 调用次数 + `window.fetch` 调用数 + 服务端状态 | 选中 `POST /api/messages` 发送：`confirm` **恰 1 次**，文案逐字=`该请求会真实生效：\nPOST /api/messages\n\n确定发送？`；`fetch` 调用 **0 次**；结果区保持「（未发送）」；表单顶部 `.danger-badge` 1 处「会改变状态」，列表侧 5 条 POST 均带标识、6 条 GET 无标识 |
| SSE 面板可停 | 日志条目 + 按钮 disabled 态 | `/api/events` 订阅 → 日志「（已连接） /api/events」；点击停止 → 「（已停止） 订阅已关闭，不再接收事件」，按钮回到 订阅=enabled / 停止=disabled |
| 顶栏入口 | DOM 锚点 href + 真实导航结果 | `.nav-item` = `SPAN Workspace / SPAN Agents / SPAN Tasks`（3 占位逐字未变）+ `A 文档→/docs`、`A 调试→/debug`；点击「文档」→ 地址变为 `…/docs` |

**注（不改变本项判定）**：这 5 类里除顶栏入口外，另 4 类在自动化测试中只有**静态文本形态**断言，运行时行为目前靠阶段 5 的一次性浏览器验收（架构 §13 R-10 已显式声明该留白）；该覆盖缺口在 J 项与下一迭代候选中单列。

### I. 零启停结构性校验（"hub 不启动/不关闭 agent"仍结构性成立）— **pass**

- `oamp/src/web.js` 无任何进程启动能力：`grep "spawn|child_process|execFile|execSync"` = **零命中**；路由表 11 条中无 agent 生命周期接口（全量清单见 E③，只有 agents 快照读、chats/messages 业务、SSE、`/api/docs`）。
- 两个新页面零启停路径：`grep "启动|停止|agent start/stop"` 在 `docs.js`/`docs.html` 零命中；`debug.js` 仅 5 处「停止」全部属 **SSE 订阅停止**（`source.close()` + 按钮文案），非 agent 生命周期。
- 测试侧同款硬断言存在且**我实跑为绿**：`test/api-pages.test.js` 的「新页面脚本独立且零 agent 启停入口」（禁用 `POLL_MS` / `setTimeout(tick` 形态 + 双正则禁启停）在篡改副本中 `ok 4`；仓库全量套件中亦 pass。
- 运行时旁证：调试台可交互元素仅"11 个接口按钮 + 发送/订阅/停止"，无任何启停控件（我的浏览器 DOM 观测）。

### J. 「动态适应」的工程保证（新增/删除登记项时哪条锁会红）— **partial**

拆开说两个子项：

**J-1 llms.txt / API.md / 元数据：由测试保障 — 通过**

- 新增一条登记 → 锁①（登记集合/顺序 `assert.deepEqual`）、锁②（快照逐字节）、锁③（`API.md 缺登记: GET /api/probe6`）**三条全红**；删除一条登记 → 同样三条全红（`API.md 多出未登记路径: POST /api/chats/archive`）。证据见 E②。
- 因此"新增登记 → `llms.txt` 自动跟随"的**工程兜底 = 生成器 + 锁②**：不重跑 `node oamp/scripts/gen-llms-txt.mjs` 就必然红；"新增登记 → 必须登记进 `API.md`" 由锁③兜底；"登记项缺元数据"由锁①兜底。三者都由**测试**（而非人工约定）保障 —— 结论成立。

**J-2 文档页 / 调试台：由构造保障，渲染结果无回归断言 — 不通过**

- 构造保障成立：两页运行时 `fetch('/api/docs')` 渲染（投影取数只有一处：`docs.js:45` / `debug.js:206`；调试台另一次 `fetch` 在被用户触发发送时发生，`debug.js:92`），`test/api-pages.test.js` 另以 **反向断言**禁止页面硬编码登记中的接口路径（`assert.doesNotMatch(js, /\/api\/(agents|chats|messages|stream|events)\b/)`）并要求引用投影字段。⇒ "新增登记需要改页面"这件事**不可能漏做**（无需改动）。
- 但没有测试会在"页面渲染结果与投影不一致"时转红：我在**新增登记**的篡改副本上实跑 `test/api-pages.test.js` 的 4 个静态用例 → **4/4 全绿**（`ok 1..4 / # pass 4 / # fail 0`），即锁/用例对该缺陷完全无感。文档页"页面接口集合与 `GET /api/docs` 的 11 条一致（双向求差为空）"这一验收，目前只有阶段 5 的**一次性浏览器对拍**判定面，**无任何可重复执行的自动化断言**。

⇒ 判定 **partial**：`llms.txt`/`API.md`/元数据子项通过（有可红的锁）；文档页/调试台子项**不通过**（自动跟随由结构保证，但渲染正确性无测试兜底）。该不通过为**覆盖缺口，非功能缺陷**——功能本身已由 H 的一手浏览器观测证明正确。

---

## 汇总

- **pass：9 项**（A / B / C / D / E / F / G / H / I）
- **fail：0 项**
- **partial：1 项**（J：J-1 通过、J-2 不通过——见上）
- **blocked：0 项**
- **偏差记录：5 条**（D-1 ~ D-5）
- **下一迭代候选：8 条**
- **并发项判定**：**不适用**（本迭代 2 个 PR 为真实串行依赖，无并发分支；三项核查均判"不适用"并附证据；其中"当前有效上限"字段未按爬升公式重算，另记偏差 D-1）

---

## 偏差记录

> 实现 / 落盘记录与现有规格、协议或环境事实不一致的地方。不影响本次验收判定（A~J 中无 fail），但应被下一迭代或主 agent 显式处置。

| # | 规格 / 期望 | 实际 | 证据 | 建议处理 |
|---|---|---|---|---|
| **D-1** | workflow-pb 爬升公式：`当前有效上限 = min(起始并发数 + 累计槛位释放次数 × 起始并发数, 硬上限)`，且"每当任一并发中的 PR 返回成功合并/失败阻塞，累计槛位释放次数 +1，**重新计算**当前有效上限" | `status.md` 的 `累计槛位释放次数 = 2`（已按协议累加）但 `当前有效上限` 仍为 **3**，按公式应为 `min(3 + 2×3, 5) = 5` | `git show 73ccc8e:…/status.md` 并发配置区块；对照 0015 的同项：`docs/iterations/0015-…/status.md:33` 已记为 `5（按公式 min(3+3×3,5)=5 重算——阶段 6 核实：此前落盘未重算，已按公式修正）` | 按 0015 先例把该字段重算为 **5**（并附重算说明）；若主 agent 认为"无排队 PR 时该字段不具意义"，则应把这一例外写进 workflow-pb 的爬升公式段，否则每次都会复发同一瑕疵 |
| **D-2** | PR 卡是阶段 5 实现的判定对象，验收口径应在实现开始前定稿 | pr-001 卡在实现提交 `c3a68e4`（13:43:13）**之后**被修改（`d637a84`，13:52:24）：`文件范围` 的 `oamp/web/llms.txt` → `oamp/llms.txt`，验收 2/3 合并扩充（补 Q-9~Q-11 重叠探针与"匹配器两种形态、无正则"规格） | `git show d637a84 -- …/prs/pr-001-…md` 全文 diff；`history.md` 13:37:23 已记 architect 返工要求 pr-planner 修 pr-001 的 llms.txt 路径 2 处 | 改动方向正确（与用户确认的"仓库根"口径一致，且实现实际落在 `oamp/llms.txt`），但流程上属"实现后修订验收口径"；建议 PR 卡修正随返工指令**同批落盘**，避免验收标准与实现提交的时序倒置 |
| **D-3** | workflow-pb v0.8.0 迭代分支层的意图："main 应始终只包含已完成、已验证的迭代产出" | `main` 当前 HEAD = `5a6d815`（"docs(0016): 迭代启动 —— status/history 初始化"），即 main **已承载本迭代的进行中产物**（`docs/iterations/0016-…/status.md` + `history.md`） | `git log --format="%h %s" -1 main` → `5a6d815`；`git show --stat 5a6d815` 恰为这两个文件；`git merge-base main iteration/0016` = `5a6d815` | 协议字面步骤（"创建 status.md → 再 `git branch iteration/…`"）未说明启动产物提交落在哪条分支；建议在 v0.8.0「启动工作流」补一句"启动产物提交落在迭代分支（先建分支再提交）"，否则每次迭代 main 都会被启动提交提前污染 |
| **D-4** | 期望：SSE 每条事件在调试台日志中出现一次（architecture §6.5 的意图"每条显示事件名 + data 原文，追加不覆盖"）；§6.5 文本要求"`addEventListener(type,…)` 兜底 **+ `es.onmessage`**" | `oamp/web/debug.js:128` 设 `source.onmessage`，`:129` 又对含 `'message'` 的 `EVENT_TYPES` 逐个 `addEventListener` ⇒ `message` 帧被**渲染两遍**；而 `oamp/src/transport.js:63` 的帧恒为 `` event: ${event.type}\n ``（即 `onmessage` 兜底实际不需要） | 代码双注册（`debug.js:128-129`）+ `transport.js:63`；阶段 5 已独立复现并记录于 `clarifications/verify-20260912-140856-pr002.md` 偏差 D-1（未修复） | 下迭代删 `source.onmessage` 一行（或从 `EVENT_TYPES` 移除 `'message'`），并**同批**把 §6.5 措辞改为"逐名 `addEventListener`；帧恒带 `event:` 名，无需 `onmessage` 兜底"——本次问题是"实现忠实执行了有缺陷的文本"，只改一侧会再次不一致 |
| **D-5** | 阶段 6 验证应在干净工作区上进行，且迭代分支合并 main 前不应夹带无关改动 | 主仓库工作区存在 3 个**与 0016 无关**的未提交修改：`docs/ds/05-实例配置与初始化集.md`、`docs/multi-omp-agent-protocol.md`、`principles/execution/model-dispatch-protocol.md`（+13/−13 行） | `git status --porcelain`（验证前后一致）；我的验证未触碰这三个文件 | 确认其归属（疑为用户进行中的其他工作）；合并迭代分支进 main 时只影响已提交内容，无实质风险，但建议合并前确认/暂存，避免误提交 |

---

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制（含本轮与阶段 5 报告中未被当前验收标准覆盖者），供主 agent 与 planner 决策是否纳入下一迭代。

- **C-1 文档页 / 调试台渲染结果无自动化断言（= J-2 的不通过子项）**：目前"页面接口集合与投影一致 / 控件与 `params` 一一对应 / 逐字段渲染"只有一次性浏览器对拍。可考虑把 `docs.js` / `debug.js` 的渲染函数抽成可单测的纯函数（零依赖、不引入框架），或用最小 DOM 断言把 11 条渲染结果固化——这是本迭代最值得补的一处覆盖。
- **C-2 锁③ 对"重复出现位置"不敏感**：`API.md` 中 `GET /api/docs` 有两处（§3 登记行 + `### 3.11` 标题），单独删除登记行不会使锁③转红（阶段 5 已实测）。符合 §7.2「只锁路径集合」的集合语义，若希望锁③自身覆盖该断层，可在失败信息附"每组签名出现次数"。
- **C-3 `STATIC_FILES` 与磁盘文件的一致性无独立断言**：当前靠 pr-002 的 HTTP 可达性用例覆盖最终态；若希望更早暴露"登记了不存在的静态项"，可加一条"每个 `STATIC_FILES` 条目的文件存在"的静态断言（阶段 5 中间态的 `text/plain` 404 问题即由该缺口放大）。
- **C-4 SSE 日志无上限、无清屏**：`appendEntry` 只 append，长订阅（尤其 `/api/events` 高频上下线）会让 DOM 无界增长。
- **C-5 SSE 缺参反馈可读性**：必填 `chat_id` 为空时只表现为"（连接中断）+ 自动重连"，服务端 400 文案对用户不可见（§6.5 有意不前置拦截）；可把首个错误响应的状态码/文案落到日志区，不改变"真实呈现"取向。
- **C-6 两个新页面无返回控制台 / 互跳入口**：用户已接受该局限（§13 R-6），但接口面继续增长后，`/docs` 与 `/debug` 之间的导航价值上升。
- **C-7 新测试文件各自复制 `startWeb` 辅助**：`api-routes.test.js` 与 `api-pages.test.js` 各带一份（约 25 行），是"`web.test.js` 零字节改动"硬约束的代价；若该约束未来解除，可抽 `test/helpers/web.mjs` 去重。
- **C-8 `llms.txt` 中的端口硬编码 7788**：按 §5.3 的快照确定性取舍；若未来默认端口可配置，快照中的三条 URL 会与服务实际端口不一致，需重新决策（生成器带参 vs 快照多份）。
- **C-9 `API.md` 锚点在 `text/plain` 下不滚动**（§13 R-3 已接受）：文档页每个 `docLink` 可点开但需用户自行查找小节；若后续要提高文档体验，需评估 `.md` 转换路径（成本高，当前不建议）。

---

## 结论

**PASS**（无 fail；1 项 partial 且其不通过子项为自动化覆盖缺口而非功能缺陷；其余 9 项 pass）。

判定要点：本迭代的"三条硬契约"（匹配等价形态、三条漂移锁、三个派生面）均由我在产出物副本上**亲手复现**——匹配器两种形态零正则且重叠边界给出空串、增删各一条登记使三条锁**全部转红并点名**、`/api/docs` 投影 11 条 8 字段 `danger` 派生且 llms.txt 与包根快照逐字节相等（HTTP 与文件两侧一致）；阶段 5 的两条推进事实（`web.test.js` 零字节改动、全仓串行 **251/251**）由我在主仓库迭代分支上实跑复现；前端五类用户可见行为由我在隔离实例 + 真实浏览器上一手观测（含"POST 确认 1 次、fetch 0 次"与"SSE 停止后不再追加"）；并发项按协议如实判 **不适用**（两 PR 为真实串行依赖，附配置区块初始化/更新证据）。唯一 partial 落在 J-2：文档页/调试台的"自动跟随"由构造与反向断言保证，但渲染结果本身没有可红的测试——功能已验证正确，缺口是**可重复性与回归保护**，已在 C-1 给出处置方向。

---

## 附：本轮可复现命令与产物（已清理）

```bash
# 全量串行测试（主仓库迭代分支）
cd oamp && node --test --test-concurrency=1 test/*.test.js        # 251/251
git -C .. diff 5a6d815 HEAD --numstat -- oamp/test/web.test.js    # 空（零字节改动）

# 派生面独立计算（进程内 import，不依赖测试）
node -e "const m=await import('./oamp/src/web.js'); …"            # 11 条 / 8 字段+danger / llms 1825B 逐字节相等
cd /tmp/v0016-stage6/oamp && node scripts/gen-llms-txt.mjs && cmp llms.txt <repo>/oamp/llms.txt   # IDENTICAL

# 三条锁双向篡改（/tmp 副本，不触碰产出物）
#   新增 routes.push({GET /api/probe6,…}) / 删除 routes.splice(4,1) → 锁①②③ 全 RED（点名见 E②）
node --test --test-name-pattern="漂移锁" test/api-routes.test.js
node --test --test-name-pattern="静态契约|零 agent 启停" test/api-pages.test.js   # 篡改下仍 4/4 GREEN（J-2 证据）

# 隔离实例 + 真实浏览器（临时 socket/DB，端口 46151）
node bin/oamp.js router start   # OAMP_SOCKET=/tmp/v0016-stage6/sock/router.sock
node bin/oamp.js web start --port 46151
# /docs 11 卡片对拍投影 · /debug 真实发送 200 · 6 ms · POST confirm×1 / fetch×0 · SSE 订阅→停止 · 顶栏点击 → /docs
```

清理确认：`/tmp/v0016-stage6/`（副本、socket、临时 DB、llms 对比文件）已 `rm -rf`；隔离 router/web 已 `stop`（端口 46151 无 LISTEN）；浏览器会话已 `close`；主仓库运行中的集群（PID 15766 / 15790，`127.0.0.1:7788`）未触碰。本报告是本次验证的唯一写入。
