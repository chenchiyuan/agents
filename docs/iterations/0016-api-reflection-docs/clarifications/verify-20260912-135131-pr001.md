# 独立验证报告 — PR-001（声明式路由表 + 分发重构 + GET /api/docs + llms.txt + 三条漂移锁）

**验证者身份（反射结果）**：同级代码审查者 + 行为等价性回归审查者——熟悉 Node 内建 http/SSE 服务与「特征化测试（characterization test）」体例的资深后端评审者，判据优先级 = **可复现的行为证据 > 文本断言 > 设计叙述**；对"自证式断言"（用实现的逻辑检验实现）一律要求旁证。

**产出物**：
- worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0016-pr-001-routes`（分支 `feat/0016-pr-001-routes`，commit `c3a68e4`，base = `5a6d815`（`main` tip，`git merge-base --is-ancestor 5a6d815 HEAD` = YES））
- PR 卡：`docs/iterations/0016-api-reflection-docs/prs/pr-001-route-registry-and-derivations.md`
- 契约：`docs/iterations/0016-api-reflection-docs/architecture.md` §3.3 / §4 / §4.5 / §5 / §7 / §10

**验证标准来源**：主 agent 委托的 10 条验证标准（context 原文）+ PR 卡 10 条验收标准（作为标准 1 的判定对象）。
**验证日期**：2026-09-12
**独立性声明**：未接收任何执行过程上下文；工作区只读（验证后 `git status --porcelain` 为空）；所有篡改/对照实验均在 `/tmp` 副本中进行，已清理（见标准 10）。

---

## 逐项判定

### 标准 1：PR 卡 10 条验收逐条判定 —— **pass**

逐条结论（每条附可定位证据）：

| # | 验收条目 | 判定 | 证据 |
|---|---|---|---|
| 1 | 4 个具名导出；`createApiRoutes({})` 返回 11 项、不抛错、纯构造 | **pass** | 进程内 `import` 后调用：表项数 = 11；把 10 个依赖全换成「一被调用即抛」的 Proxy 后构造仍不抛错（`introspect.mjs` ①）。另有 6 个常量导出（`ERR_CODE`/`ROUTE_META_FIELDS`/`PARAM_FIELDS`/`PARAM_IN`/`PARAM_TYPES`/`ROUTE_KINDS`，`src/web.js:85/277-281`），为锁① 所必需（architecture §7.2）——见偏差 D-3 |
| 2 | 表驱动分发、顺序 = 改造前 `if` 链顺序、三种重叠形态 | **pass** | 隔离双服务实测（标准 2）；匹配器单测 L3 与我的进程内探针一致：`POST /api/chats/close|activate|rename` → `params={chat_id:""}`（空串命中，不是"不命中"） |
| 3 | `node --test oamp/test/api-routes.test.js` 全绿：3 锁各独立顶层用例 + C-5/Q 探针 + 匹配器 L3 含可达性 | **pass** | 实跑 `# tests 7 / # pass 7 / # fail 0`；用例名逐条对应：锁①/锁②/锁③ 各一个顶层 `test(`（`test/api-routes.test.js:185/268/293`）、`匹配器 L3：…可达性`（:309）、`L2 探针：既有 API 行为等价…`（:359）、派生面（:455）、API.md 同步（:490）；可达性断言在 `:309` 用例内（我的独立探针 findings = `[]`） |
| 4 | `web.test.js` 零字节改动且全过；其余 19 个既有测试文件未改且通过 | **pass** | `git diff 5a6d815 HEAD -- oamp/test/web.test.js` = **0 行**；`node --test test/web.test.js` → `# tests 39 / # pass 39 / # fail 0`；`git diff --name-only … -- oamp/test/` 只有新增 `api-routes.test.js`；test 目录 21 个文件 = 20 既有 + 1 新增；全仓串行 246/246 |
| 5 | 真服务 `GET /api/docs` → 200 JSON、11 条含自身、每项 `danger` + `docLink` | **pass** | 隔离服务实测：`status=200`，`content-type: application/json; charset=utf-8`；`routes.length=11`，末位 `GET /api/docs danger=false docLink=API.md#311-get-apidocs`；`danger` 与 `method!=='GET'` 完全一致（4 条 POST 全 true、7 条 GET 全 false）；投影键 = `method,path,kind,summary,params,response,errors,danger,docLink` |
| 6 | `/llms.txt` 200 + `text/plain; charset=utf-8` + 与包根快照逐字节相等；重新生成无差异 | **pass** | 实测 HTTP body 与 `oamp/llms.txt` **逐字节相等（1825 B / 1825 B）**；`node scripts/gen-llms-txt.mjs` → `cmp` = IDENTICAL（1825 B，11 条） |
| 7 | 锁③ 双向差集为空 | **pass** | 用例绿；我的独立抽取验证一致：登记侧 11 条 = 文档侧可抽取签名集合（文档侧 14 处签名归一后归入同一路径集合，含 `<chat_id>`/`<id>`/查询串三种拼写） |
| 8 | 错误兜底不缩小：`%E0%A4%A` → 502 `URI malformed`；`PUT /api/agents` → 404（无 405） | **pass** | 双服务实测：`GET /api/chats/%E0%A4%A` → `502 {"error":"router 不可达或请求失败: URI malformed","code":"UPSTREAM_UNAVAILABLE"}`（base/new 逐字相同）；`PUT /api/agents` → `404 {"error":"not found: PUT /api/agents",…}`；80 例中**零 405**（status 分布见标准 2） |
| 9 | `package.json` 未改（`dependencies` 仍 `{}`）；`hygiene.test.js` 通过 | **pass** | `git diff … -- oamp/package.json` = 0 行，文件内 `"dependencies": {}`；`node --test test/hygiene.test.js` → `# tests 3 / # pass 3 / # fail 0` |
| 10 | `API.md`：顶部新增指向 `/docs` 的引用块；§3 标题「11 条」+ 登记行 + `### 3.11`；既有 10 小节文案逐字未改 | **pass** | `git diff --numstat … -- oamp/API.md` = **25 insertions / 1 deletion**，唯一删除行是 `## 3. 接口清单（10 条）`→`（11 条）`；顶部 `oamp/API.md:13` 新增 `> 在线接口文档页：<http://127.0.0.1:7788/docs>…`；`:158` 为第 11 条登记行；`:523`+ 为 `### 3.11 GET /api/docs` 小节。（链接目标页由 pr-002 交付，按 PR 卡该项括注不在本 PR 判定范围） |

### 标准 2：行为保真（本次核心） —— **pass**

**① `web.test.js` 零字节改动 + 39/39 全绿** —— pass：`git diff` 0 行；39 pass / 0 fail（实跑）。

**② 改造前后同输入同响应（隔离双服务实测）** —— pass：
- 方法：`git archive 5a6d815 oamp` → `/tmp/.../base/oamp`；worktree `oamp` → `/tmp/.../new/oamp`（排除 `.runtime`）。各自独立 `OAMP_SOCKET`（mkdtemp）+ 独立 `OAMP_DB` + 独立端口（44771/44772），以**同一确定性种子库**（4 个对话：working/completed/closed+archived、含消息）起真实 `router start` + `web start`。
- 输入组 **80 例**：既有 10 条 API 的成功路径与全部查询参数组合、Q-1~Q-11 怪癖（`archive`/空尾/含 `/` 参数/前后缀重叠/畸形体/超大体重叠）、无 405（`PUT|DELETE|PATCH|HEAD|OPTIONS`）、空值 4 例、非法参数 7 例、匹配期抛错 2 例、静态面（既有 4 URL + 非白名单路径）与新面、SSE 首帧 3 例。
- 结果：**73/80 完全一致（status + content-type/connection/cache-control + body 归一化逐字）**；7 处差异**全部落在改造前不存在/不可达的 URL** 上：
  | URL | base | new | 性质 |
  |---|---|---|---|
  | `GET /api/docs` | 404 JSON | 200 JSON | 本 PR 新增接口（契约要求） |
  | `GET /llms.txt` | 404 JSON | 200 `text/plain` | 本 PR 新增静态面（契约要求） |
  | `GET /API.md` | 404 JSON | 200 `text/plain` | L1-02 新增静态面（契约要求） |
  | `GET /README.md` | 404 JSON | 200 `text/plain` | L1-02 新增静态面（契约要求） |
  | `GET /docs`、`GET /debug`、`GET /api-pages.css` | 404 JSON | 404 `text/plain` `not found` | 静态映射已登记、页面文件由 pr-002 交付 → **偏差 D-1** |
- 对照控制组（排除 harness 噪声）：base 连跑两次 **80/80 一致**（差异 = `[]`）⇒ 上面的 7 处差异不是时序/随机噪声。
- `GET /api/chats/<id>` 等含时间戳/计数字段经归一化（`<EPOCHMS>`/`<UUID>`）后逐一相等；SSE 首帧（`retry: 1000\n\n`）、`connection: keep-alive`、`text/event-stream; charset=utf-8` 全等；413 例的 `connection: close` 全等。
- **handler 体"逐字不动"的旁证（文本层）**：从 base 的 `if` 链抽出 12 个顶层分支体、从新文件抽出 11 个 `handler:` 体，归一化（仅把 `const chatId = decodeURIComponent(p.slice(…));` ↔ `const chatId = params.chat_id;` 对等替换 + 折叠空白）后逐字比对：**前 10 个 IDENTICAL**，第 11 个是新增 `/api/docs` handler，尾部 2 个分支是静态面（已移入 `STATIC_FILES`）。模块级差异仅：新增 `PKG_ROOT`、`STATIC_TYPES` +`.txt/.md` 两项、`ERR_CODE` 由 `const` 改为 `export const`（+注释）、`serveStatic` 基准换根、`STATIC_FILES`、`/api/docs` 头注释——无逻辑重写。

**③ 无 405、无新增错误路径** —— pass：status 分布 base `{200:28, 400:17, 404:32, 413:1, 502:2}` / new `{200:32, 400:17, 404:28, 413:1, 502:2}`：**200 的 +4 恰等于 4 个新增 URL；404 的 −4 恰为其对应项**；400/413/502 计数与文案全等；**405 计数 = 0**（两侧）。400 文案（`查询参数非法: …`/`请求体非法 JSON: …`/`标题非法: …`/`消息不能为空`/`model 非法…`）、502 文案、SSE 400 文案逐字相同。

### 标准 3：匹配等价形态（`exact` / `prefixSuffix`，无正则） —— **pass**

- 实现（`src/web.js:289-311`）只有两种判据：`pathname === pathPattern`（无参数段）与 `startsWith(prefix) && endsWith(suffix)` + `slice(prefix.length, len - suffix.length)`（单参数段）；`extract` 源码经独立扫描**不含正则**（无 `new RegExp`、无 regexp 字面量）；函数体内不出现 `/^…/` 形态判据。
- 形态分布：7 条 `exact`（`/api/agents`、`/api/chats`、`/api/chats/archive`、`/api/docs`、`/api/stream`、`/api/events`、`/api/messages`）+ 4 条 `prefixSuffix`（`/api/chats/:chat_id`（后缀空串）、`close`、`activate`、`rename`）= 11。
- 重叠边界实测（与今日逐字一致，base/new 相同）：
  - `POST /api/chats/close` → `404 {"error":"chat 不存在: ","code":"NOT_FOUND"}`（捕获为空串）
  - `POST /api/chats/activate` → 同上（空串）
  - `POST /api/chats/rename` + 畸形体 `{"title":` → **400** `请求体非法 JSON: Unexpected end of JSON input`（证明命中 rename 表项且 `readBody` 先于存在性预检）；空体/`{}` → 404 `chat 不存在: `；70 KB 体 → 413 + `connection: close`
  - 反向探测：`/api/chats/a/b/close` → `chat 不存在: a/b`；`/api/chats/%2Fclose` → 命中 `GET /api/chats/:chat_id` 参数 `/close`；`GET /api/agents/` → `not found: GET /api/agents/`（不归一尾斜杠）
- 匹配期抛错：`GET /api/chats/%E0%A4%A` → 502 `URI malformed`（`params` 解码在匹配层，由分发层同一 `try` 兜住）。

### 标准 4：三条漂移锁各自篡改一次 → 转红 —— **pass**

在 `/tmp` 的 pristine 副本上各自施加一次篡改后跑对应用例（独立于我，不看执行方结论）：

| 篡改 | 结果 | 锁输出的证据 |
|---|---|---|
| ① 某表项 `response` 置空串 | **RED**（exit=1, fail=1） | `元数据缺项: [GET /api/chats/:chat_id] response` |
| ① `errors: ['BOGUS']` | **RED** | `元数据非法: [GET /api/chats/:chat_id] errors[0]="BOGUS" 不在 ERR_CODE 值集合内` |
| ② `llms.txt` 改动一行 | **RED** | `llms.txt 与生成结果不一致：首处差异 line 23 col 25（byte 1520）` + `修复：node oamp/scripts/gen-llms-txt.mjs`（含行/列/字节偏移 + 修复命令，符合 §7.2 要求） |
| ③ 追加 `` `GET /api/ghost` `` 到 `API.md` | **RED**（D\R 方向） | `API.md 多出未登记路径: GET /api/ghost` |
| ③ 删除**全部** `` `GET /api/docs` `` 签名 | **RED**（R\D 方向） | `API.md 缺登记: GET /api/docs` |
| （对照）仅删除 `API.md:158` 一条登记行 | GREEN | 符合 §7.2「只锁路径集合」的集合语义（`:523` 的 `### 3.11` 标题仍提供该路径）；同一篡改下 `API.md 同步` 用例 **RED** ⇒ 覆盖存在（详见下一迭代候选 C-1） |

### 标准 5：派生面 —— **pass**

- `GET /api/docs`：11 条（含自身，末位）；每条含 8 元数据字段 + 派生 `danger`（`method !== 'GET'`）+ `docLink`；`kind ∈ {json,sse}`；`errors` 全部 ∈ `ERR_CODE` 值集合。
- `GET /llms.txt`：200 + `text/plain; charset=utf-8`；与包根 `oamp/llms.txt` **逐字节相等（1825 B）**；内容含 `## 接口（11 条）` 与 11 条清单行（顺序 = 表顺序）+ 三条深入链接（默认端口 7788，按 §5.3 设计）。
- `GET /API.md` / `GET /README.md`：均 200 + `text/plain; charset=utf-8`，正文即仓库原文（非副本）。
- `GET /`、`/index.html`、`/app.js`、`/style.css`：200，MIME 与改造前逐字一致。

### 标准 6：落点与纠正项 —— **pass**

- 快照落点 = **包根 `oamp/llms.txt`**（`git ls-files oamp/llms.txt` 命中、已入库；`oamp/.gitignore` 仅含 `.runtime/`、`data/`，不遮蔽）；`oamp/web/` 下**不存在** `llms.txt`（目录仅 `app.js`/`index.html`/`style.css`）。
- 匹配器**无正则**（标准 3 已证）；`STATIC_FILES`（`src/web.js:261`）映射值确实为包根相对路径。
- 空值口径按 §4.5 实测复核：`?state=`/`?archived=`/`?limit=` 全部 **200**（无「空值 → 400」路径）；产生 400 的是 `limit=0|201|abc`、`archived=2`、`state=bogus`、`from>to` —— 与 §4.5 的更正口径一致，且 base/new 逐字相同。

### 标准 7：测试 —— **pass**

- `node --test test/api-routes.test.js` → **7/7**（`# tests 7 / # pass 7 / # fail 0`）。
- 全仓**串行** `node --test --test-concurrency=1 test/*.test.js` → **`1..246` / `# tests 246 / # pass 246 / # fail 0`**（与期望 246 一致）。
- `node --test test/web.test.js` → **39/39**，且该文件对本 PR 的 `git diff` = 0 字节。

### 标准 8：边界 —— **pass**

- `git show --stat c3a68e4` 的文件集合 = `docs/…/pr-001-…-tasks.md` + `oamp/API.md` + `oamp/llms.txt`(新) + `oamp/scripts/gen-llms-txt.mjs`(新) + `oamp/src/web.js` + `oamp/test/api-routes.test.js`(新) —— 与标准允许集合**完全一致，无越界文件**。
- 零新依赖：`oamp/package.json` 未改（`git diff` 0 行），`dependencies === {}`；新代码只 import `node:` 内置与既有模块（`gen-llms-txt.mjs` 只 import `node:fs/path/url` + `../src/web.js`）。
- 既有 20 个测试文件零改动（仅新增第 21 个）。

### 标准 9：反向核对（因果隔离） —— **pass**

| 反向篡改 | 观测到的行为翻转 | 结论 |
|---|---|---|
| 把 `prefixSuffix` 判据换成正则形态 `^prefix(.*)suffix$`（架构点名的反例） | `POST /api/chats/close` → `404 not found: POST /api/chats/close`（今日为 `chat 不存在: `）；`POST /api/chats/rename` + 畸形体 → `404 not found: …`（今日为 **400**） | 证明「允许前后缀重叠的两形态匹配器」是实现今日怪癖的**必要条件**；正则形态会真实漂移 |
| 把 `matchRoute` 移出 `try`（try 只包 handler 调用） | `GET /api/chats/%E0%A4%A` → **`ECONNRESET`**（客户端拿不到 502），且进程随后退出（后续请求 `ECONNREFUSED`） | 证明「匹配 + 调用同一 try/catch」是 502 覆盖面与进程存活性的**必要条件**；缩小覆盖面会同时丢掉 502 与打挂服务（比对真实实现：502 `URI malformed`） |

### 标准 10：清理 —— **pass**

- 已删除：`/tmp/verify0016-pr001`（base/new/pristine/tamper 副本 + 全部脚本与输出）、`/tmp/verify0016-pr001-base-web.js`、`/var/folders/…/T/verify0016-*`（harness 的 mkdtemp 运行目录）。
- 已终止：反向核对中一个孤儿 `node bin/oamp.js router start`（PID 23326，cwd = 我的 `/tmp` 篡改副本；SIGINT 后正常退出）；核对结果：`ps | grep verify0016` 无残留。
- 主仓库运行中的集群**未受影响**：router PID 15766 / web PID 15790 存活，`curl http://127.0.0.1:7788/api/agents` → **200**；主仓库 `oamp/{src,test,llms.txt,API.md,scripts,package.json}` 无我的写入。
- worktree 只读：验证结束 `git status --porcelain --untracked-files=all` 为空。

---

## 汇总

- pass：**10 项**
- fail：**0 项**
- partial：**0 项**
- blocked：**0 项**
- 偏差记录：**3 条**（D-1 / D-2 / D-3，均不阻塞交付）
- 下一迭代候选：**4 条**

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| **D-1** | architecture §3.7 把 `/docs`、`/debug`、`/api-pages.css` 登记进 `STATIC_FILES`（指向 pr-002 才交付的页面文件）；静态面「不在统一错误契约内」 | PR-1 阶段这三个 URL 的响应由改造前的 **JSON 404**（`{"error":"not found: GET /docs","code":"NOT_FOUND"}`）变为 **`text/plain` 404（body `not found`）**。既有 4 个静态 URL 与 10 条 API 的 404 形态**未变** | 按架构 §3.7 接受（这三条 URL 改造前不在任何白名单内，非既有行为面）；**pr-002 验收时必须确认这三条 URL 变为 200 + 正确 MIME**；若 pr-002 缩范围，需回退这三条映射，避免长期暴露「登记了但不存在的静态项」 |
| **D-2** | architecture §3.3「`prefix` / `suffix` / `paramNames` / `kind` 全部由 `createApiRoutes` 从 `path` 纯派生」（读起来像表项上可检视的字段） | 实现只挂 `extract` 闭包（`src/web.js:289-311` + `Object.assign(route, compileRouteMatcher(route.path))`），不暴露上述字段；行为等价（7 `exact` + 4 `prefixSuffix` 已实测） | 按实现更新 §3.3 措辞（改为「形态由 `extract` 闭包表达、经 `matchRoute` 可观测」），或视为 L3 实现细节保留——**不要求改实现** |
| **D-3** | PR 卡验收 1 称「新增具名导出 `createApiRoutes` / `matchRoute` / `projectRoutes` / `renderLlmsTxt`」 | 除这 4 个外另有 6 个常量导出（`ERR_CODE`（由 `const` 改 `export const`）、`ROUTE_META_FIELDS`、`PARAM_FIELDS`、`PARAM_IN`、`PARAM_TYPES`、`ROUTE_KINDS`），是锁①「以真实结构为检查对象」的必需依赖（architecture §7.2） | 按实现更新 PR 卡措辞（补「+ 锁① 所需常量导出」）；`ERR_CODE` 由 `const` 改 `export const` 在模块导出面上属新增，无 HTTP 面影响 |

---

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制，不在当前验收标准范围内，供主 agent 与 planner 决策。

- **C-1 锁③ 的集合语义对「重复出现位置」不敏感**：`API.md` 中 `GET /api/docs` 有两处（§3 登记行 + `### 3.11` 标题），单独删除登记行不会使锁③ 转红（已实测）。这**符合** §7.2 明示的「只锁路径集合」，且同一篡改下 `API.md 同步` 用例会红（覆盖存在）。若希望锁③ 自己也覆盖该断层，可在失败信息里附带「每组签名的出现次数」，但会增加误报面（现行文案要求"零假阳性"），建议**保持现状**，仅登记知悉。
- **C-2 `STATIC_FILES` 与磁盘文件的一致性无断言**：登记的 5 个 pr-002 页面资产当前不存在（D-1）。pr-002 的页面可达性用例会覆盖最终态；若想把"已登记文件必须存在"前移，可加一条「每个 `STATIC_FILES` 条目的文件存在」的静态断言（PR-2 范围内讨论）。
- **C-3 新测试文件自带 `startWeb` 辅助与 `web.test.js` 重复约 25 行**：这是「`web.test.js` 零字节改动」硬约束下的唯一代价（§4.4 已声明）。若未来该约束解除，可抽 `test/helpers/web.mjs` 去重。
- **C-4 `llms.txt` 中端口硬编码 7788**：按 §5.3 的设计取舍（快照需确定性）。若后续把默认端口改为可配置并入库，快照中的三条 URL 会与实际服务端口不一致——届时需重新决策（生成器带参 vs 快照多份）。当前无动作。

---

## 结论

**PASS**（10 项标准全部 pass，无 fail / partial / blocked）。

判定要点：既有 API 面与既有静态面的**行为逐字节保真**由独立隔离双服务 80 例 diff 证明（73/73 既有面一致，7 处差异全部落在本 PR 有意新增的 URL 上，且经 control-run 排除噪声）；三条漂移锁各自篡改均按预期转红（锁③ 双向各一例）；两处反向篡改分别复现了架构点名的「正则形态漂移」与「try/catch 缩小 → 丢 502 且打挂进程」两个反例，说明实现的匹配器形态与单 try/catch 结构是**承载今日行为的必要条件**，而非风格选择。唯一需要下游关注的是 D-1（三个静态映射的 404 形态在 pr-002 交付前为 `text/plain`），已在偏差记录中给出明确处置。

---

## 附：可复现命令与产物清单（本次验证使用，已清理）

```bash
# 副本与基准
git archive 5a6d815 oamp | tar -x -C <WS>/base           # 改造前
tar -cf - --exclude='oamp/.runtime' oamp | tar -x -C <WS>/new   # 改造后（worktree）
# 行为等价：各起隔离 router+web（独立 OAMP_SOCKET / OAMP_DB / 端口），80 例输入逐项 diff
node run.mjs base <WS>/base/oamp out-base.json && node run.mjs new <WS>/new/oamp out-new.json && node diff.mjs
node run.mjs base2 <WS>/base/oamp out-base2.json          # 控制组：base vs base2 = 80/80
# handler 体逐字比对 / 匹配器与纯构造取证 / 三锁篡改 / 反向篡改
node extract.mjs ; node introspect.mjs ; node tamper.mjs ; node tamper2.mjs ; node patch-rev.mjs ; node reverse.mjs …
# 套件
cd oamp && node --test test/api-routes.test.js          # 7/7
node --test test/web.test.js                            # 39/39
node --test --test-concurrency=1 test/*.test.js          # 246/246
node --test test/hygiene.test.js                         # 3/3
```

产物均为 `/tmp` 下的临时文件（JSON 结果、脚本、副本），验证结束时已全部删除；本报告是本次验证的唯一写入。
