# F06：漂移锁三条（元数据必填 / 索引快照逐字节 / 契约文档路径级双向覆盖）

**功能 ID**: F06
**来源**: `demand.md` W5；决策 D-6 / D-12；澄清 M-2 / M-7；效果 E2；约束 C-1 / C-2；现状缺口 F-14（当前"文档与实现对不上"不会有任何测试发现）
**迭代**: 0016-api-reflection-docs

---

## 用户价值

"文档会不会过时"这件事不再靠自觉——只要有人漏登记、漏更新索引快照，或者漏在对外契约文档里登记新接口的路径，当场报错并指出漏在哪。

## 验收标准

1. **元数据必填锁**：每条登记缺**任一必需元数据**（方法 / 路径 / 一句话说明 / 参数元数据 / 是否有请求体）即检查失败，且失败信息**点名**是哪条登记缺哪个字段（W5① / M-2 / E2 / C-1）。
   *判定*：手动删掉某条登记的一个必填字段 → 运行既有测试命令（`npm test`）→ 观察失败，且失败信息点出该登记与该字段。
2. **索引快照逐字节锁**：索引内容的生成结果与仓库中的快照文件不逐字节相等即失败，且失败信息能定位差异所在（行 / 字节）（W5② / M-3）。
   *判定*：手动改动快照文件的少量字符 → 运行既有测试命令 → 观察失败并能定位差异。
3. **契约文档路径级双向覆盖锁**：① 登记中每条路由的路径必须在 `API.md` 中出现；② `API.md` 中出现的每个接口路径必须在登记中；**只锁路径集合，不锁文案**；缺失即失败并**点名**缺失路径（W5③ / D-6 / M-7 / C-2）。
   *判定*：a) 从 `API.md` 删掉一个既有路径 → 运行测试 → 失败并点名；b) 在 `API.md` 中加一个不存在的接口路径 → 运行测试 → 失败并点名。
4. **三条各自独立失败且可区分**：三条检查各自独立报告，一条失败不掩盖另外两条的结论（W5 / M-7）。
   *判定*：分别只破坏三处，三次运行各自报出对应的缺项，互不吞没。

## 边界（不包含）

- 不含对 `API.md` **文案**的锁定（只锁路径集合，D-6）。
- 不含对 `API.md` 章节、参数或示例完整性的锁定（C-2 只要求补一行路径）。
- **不检查"不登记也能被发现"**——本卡锁的是"登记后不可遗漏"，不承诺自动发现未登记的接口（M-2 明确不保证）。
- 不含文档页 / 调试台 / 索引文件的**内容正确性**检查（那是 F03 / F04 / F05 的验收面；本卡只管漂移）。
- 不含既有接口的行为回归（→ F02）。
- 不含运行时的服务内校验——三条均为工程保障，其触发点是测试（M-2 / W5）。

## 架构落定（阶段 3；完整契约见 `architecture.md` §7）

- **AR-06-a 三条检查的测试组织形态**：新增文件 `oamp/test/api-routes.test.js`（`npm test` 的 glob 自动纳入），**三条锁各写一个独立顶层 `test(...)`**（与 `hygiene.test.js` 的并列体例一致）⇒ 一条失败不掩盖另两条（验收 4）；三条只读文件、不起进程、不占端口、毫秒级，可与其他测试并行。
- **AR-06-b 检查对象的取法（核心：不读源码正则）**：
  ① **元数据必填锁** → 检查对象 = **`createApiRoutes({})` 返回的真实表项**（进程内直接调用登记构造器；构造器是纯构造，传空依赖即可）。逐项校验 `ROUTE_META_FIELDS` 存在且非空、`params[]` 的 `PARAM_FIELDS` 齐备且 `in`/`type` 在枚举内、`errors` 元素 ∈ `ERR_CODE` 值集合、`kind ∈ ROUTE_KINDS`。
  ② **索引快照逐字节锁** → 检查对象 = **`renderLlmsTxt(projectRoutes(createApiRoutes({})))` 的真实生成结果** vs **`oamp/llms.txt`（仓库/包根）的文件字节**（Buffer 比对）。
  ③ **契约文档路径级双向覆盖锁** → 检查对象 = **两个集合求差**：登记侧 `R = { '{METHOD} {shape(path)}' }`（`shape` 把 `/<…>/` 与 `/:name` 归一为 `/:`）；文档侧 `D` = 从 `oamp/API.md` 文本中抽取的反引号签名 `` `(GET|POST) (/api/…)` ``（截断 `?`/`#` 后归一）。`R\D` 与 `D\R` 都必须为空。**只锁路径集合，不锁任何文案**（F06 边界 / C-2 / D-6）。**实测（阶段 3）**：该规则作用于当前 `API.md` 得到的 `D` **恰好等于**既有 10 条路由的 `R`（35 处命中 / 12 种拼写变体：`<chat_id>`、`<id>`、带查询串等），现状零假阳性 —— 本轮只需把新增的 `GET /api/docs` 按 `architecture.md` §6.7 的格式加进 `API.md`。
- **AR-06-c 失败信息格式（逐条点名）**：
  ① `元数据缺项: [POST /api/chats/:chat_id/rename] params[1].desc`（每条一行，先收集再一次性断言）；
  ② `llms.txt 与生成结果不一致：首处差异 line 14 col 21（byte 412）` + 期望/实际行 + `修复：node oamp/scripts/gen-llms-txt.mjs`（长度不等时另报"期望 N 行 / 实际 M 行"）；
  ③ `API.md 缺登记: POST /api/x/y` 与 `API.md 多出未登记路径: GET /api/z`。
