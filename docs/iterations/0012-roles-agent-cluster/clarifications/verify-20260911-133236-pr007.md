# pr-007 独立验证报告（阶段 6 返工：`--approval-mode` 映射 + `tool_call` 审计源）

**验证者身份**：协议级代码审查者（ACP 客户端 / JSON-RPC 审计链路方向）——能同时读代码、读协议原始帧、读 `ps` argv 与运行期事件行三层观测面的评审者。
反射理由：产出物的判定面跨「代码（argv 派生 / 落行规则）— 协议（`session/update` 通知帧）— 进程（真实 omp argv 与行为）」三层，且核心风险是"客户端是否真的被触发/是否重复落行"，只有同时掌握这三层的人才能判定。

**产出物**：
- worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0012-pr-007-tool-call-audit`（分支 `feat/0012-pr-007-tool-call-audit`，commit **85c7738**，`git status --porcelain` 为空 = 工作区干净）
- PR 卡 `docs/iterations/0012-roles-agent-cluster/prs/pr-007-tool-call-audit.md`（18 条验收）
- 契约 `docs/iterations/0012-roles-agent-cluster/architecture.md` v1.2.0 §4.4 / §4.5 / §11.2

**验证标准来源**：主 agent 委托的 9 条验证标准 + PR 卡 ①~⑥（18 条）+ architecture v1.2.0 §4.4 / §4.5 / §11.2（契约口径）
**验证日期**：2026-09-11

**独立性声明**：未接收执行过程上下文。全部证据由本人独立重建——自建隔离临时环境（临时 `OAMP_SOCKET`/`OAMP_DB` + 随机端口 + 真实 `omp` 18.0.11，**未设 `OAMP_OMP_BIN`**）、自跑真实 omp 端到端、自建变异体做因果隔离。报告中与执行方数字不同处（如 deny 轮耗时）为本轮独立实测值，非转抄。
可复跑脚本（本人自建）：`/tmp/pr007-verify/real-e2e2.mjs`、`probe-argv.mjs`、`probe-frames.mjs`、`omp-tee.cjs`（日志 `run1.log`/`run2.log`）。

---

## 一、逐条判定（PR 卡 18 条）

### ① daemon argv 档位（3 条）

**①-1 `{tools:true,permission:'allow'}` → 含 `--approval-mode yolo`；`deny` → `always-ask`：pass**

- 代码面：`oamp/src/acp-client.js:123` `if (this.tools) args.push('--approval-mode', this.permission === 'deny' ? 'always-ask' : 'yolo');`（唯一档位派生点；全仓 `grep -rn "approval-mode" oamp/src` 仅此 1 处 + `agent.js:172`）。
- fake 断言：`oamp/test/tool-permission.test.js:389-416`（`ok 8`，`indexOf('--approval-mode')` 存在 + 紧随值断言）。
- 真实 omp `ps` 实测（本人临时环境）：`omp acp --no-skills --no-rules --no-session --model deepseek/deepseek-v4-flash --append-system-prompt <…>/roles/dev/dev.md --approval-mode yolo`；deny 实例同形以 `--approval-mode always-ask` 收尾。

**①-2 `{tools:false}` 与缺省匿名 → 不含档位且仍含 `--no-tools`；匿名 argv 逐字节一致：pass**

- fake：`tool-permission.test.js:389-416` 覆盖 `tools:false` + `{}`（缺省）两支，断言 `!includes('--approval-mode')` 且 `includes('--no-tools')`。
- 既有逐字节断言未改且全绿：`acp-daemon.test.js:546-552`（匿名 acp argv `deepEqual` 为 `['acp','--no-skills','--no-rules','--no-tools','--no-session','--model','deepseek/deepseek-v4-flash']`；该 diff **0 行删除**）。
- 真实 omp `ps`：匿名实例 = `omp acp --no-skills --no-rules --no-tools --no-session --model deepseek/deepseek-v4-flash`（与上数组逐字节一致）；`--tools off` 的角色实例 = 同形 + `--append-system-prompt`，**无** `--approval-mode`。deny 档在真实 omp 下同样按 `always-ask` 起进程。

**①-3 位置不影响取值；断言只判「存在 + 紧随值」：pass**

- 断言形态：全部使用 `argv[argv.indexOf('--approval-mode') + 1]`（`tool-permission.test.js:394-395`、`acp-daemon.test.js:539/561/575`），无位置敏感断言。
- 位置事实核对：实现追加在 argv 末尾（`--model` / `--append-system-prompt` 之后），真实 omp 实测接受该位置（`ps` 显示 `… --append-system-prompt <file> --approval-mode yolo`，且该轮成功执行，见 ③/⑤）。

### ② 一次性 `omp -p` 路径档位（3 条）

**②-1 自动化断言（acp-daemon.test.js）：pass**

- 文件 `oamp/test/acp-daemon.test.js`，`git diff HEAD~1 -- test/acp-daemon.test.js | grep -c '^-[^-]'` = **0**（只增断言，既有断言行零改动；`acp-daemon.test.js:539` 常驻档位、`:561` allow 一次性、`:568` 匿名一次性无档位、`:575` deny 一次性 `always-ask`，另 `:511-519` 新增 deny 档实例）。
- 全文件用例 7/7 通过（本人实跑）。

**②-2 冒烟（同判据）：pass —— 用真实 omp 复跑，未用 fake**

真实 omp `ps` 实测的三组一次性 argv：

| 实例 | 真实 `omp -p` argv（节选） |
|---|---|
| allow（`--tools on`，`--permission` 缺省） | `omp -p --no-session --append-system-prompt <…>/roles/dev/dev.md --approval-mode yolo 回复 OK` |
| deny（`--tools on --permission deny`） | `omp -p --no-session --append-system-prompt <…>/roles/dev/dev.md --approval-mode always-ask 回复 OK` |
| tools off | `omp -p --no-session --no-tools --append-system-prompt <…>/roles/dev/dev.md 回复 OK`（无档位） |
| 匿名（无 `--role`） | `omp -p --no-session --no-tools 回复 OK`（无档位、无注入） |

一次性路径冒烟本身亦成功：`TASK_RESULT … state=completed exit_code=0`，`duration_ms=2061`（allow 档，真实 omp）。

**②-3 代码面证据（补两处才成立）：pass**

- `oamp/src/agent.js:165` `const toolsOn = task.tools === null ? ctx.tools : task.tools;`、`:172` `if (toolsOn) args.push('--approval-mode', ctx.permission === 'deny' ? 'always-ask' : 'yolo');`；`oamp/src/agent.js:583-587` `taskCtx` 已含 `permission`（与 `ContextPool` 同源 `agent.js:575-576`）。两处均在位，与卡面「不补这两处档位必然缺失」的判定依据一致。

### ③ `TOOL_CALL` 落行规则（5 条）

**③-1 同 id 多帧 ⇒ 恰 1 行：pass（fake + 真实 omp 双层证据）**

- fake：`tool-permission.test.js:418-451`（`ok 9`）同 id 推 `tool_call(pending)` → `tool_call_update(in_progress)` → `tool_call_update(completed)`，断言 `TOOL_CALL` 恰 1 行、`status='completed'`。
- **真实 omp 原始帧实测（补充探针，透明 tee：只透传不干预）**：omp 18.0.11 对**一次**调用确实推 **3 帧**同 id 通知（`tool_call status=pending` → `tool_call_update status=in_progress` → `tool_call_update status=completed`），而 agent stdout 只落 **1 行** `TOOL_CALL` ⇒「终态首见落行 + 在飞表去重」在真实 omp 下**被真正触发**（非 no-op）：

```
[2026-09-11T05:31:51.694Z] agent TOOL_CALL instance=pb-dev-tee role=dev chat_id=chat-f8bc8a66-… context_id=ctx-19542-1 pid=19542
  tool_call_id=call_00_GvUYwfBcRtI9rGzlebnp7662 kind=edit title="Writing tee-created file" status=completed
  source=acp_tool_call path=/var/folders/…/pr007-frames-g2iZS3/tee-created.txt
```

- 实现锚点：`acp-client.js:346-367`（`_handleSessionUpdate` 在飞表 + 终态首见落行）、`:112`（`_inFlightTools` 表）。

**③-2 字段齐全（身份四键 + `pid`/`tool_call_id`/`kind`/`title`≤120/`status`/`path` 可选省略 + `source`）：pass**

- 真实 omp 落行实录（allow 档，本人环境）：身份四键 `instance=pb-dev-allow role=dev chat_id=chat-2a11… context_id=ctx-84934-1` 全部非空，`pid=84934`（= 该实例 `omp acp` 子进程 pid，与 `ps` 一致）、`tool_call_id=call_00_HJQSGp26FX5pBiaZTCog3362`、`kind=edit`、`title="Writing verification file"`、`status=completed`、`source=acp_tool_call`、`path=<临时目录>/allow-created.txt`。
- 字段集合断言：`tool-permission.test.js:418-451`（有 path 时 11 键 `deepEqual`）、`:453-472`（无 path 时断言 `!('path' in fields)`）、`title` 截断 120（`assert.equal(fields.title.length, 120)`）。
- 实现锚点：`acp-client.js:376-393`（`_auditToolCall`，`path` 缺省省略）+ `:52-59`（`readToolPath`：`rawInput.path` → `locations[0]` 对象/字符串）。

**③-3 终态前轮末结算冲账（最后观测 status，仍恰 1 行）：pass**

- `tool-permission.test.js:453-472`（`ok 10`）：只收 pending/in_progress 即结算 ⇒ 1 行、`status='in_progress'`、`kind` 沿用首帧、无 path 键。
- 实现锚点：`acp-client.js:212`（`prompt()` 的 `finally` 调 `_flushToolCalls()`）、`:369-373`（冲账实现）。真实 omp 下该分支为兜底（本轮 3 帧均到达终态）。

**③-4 两次调用 ⇒ 2 行；非本会话忽略；无工具调用轮 ⇒ 零行：pass**

- `tool-permission.test.js:474-497`（`ok 11`）三支全覆盖：两会话 `toolCallId` ⇒ `['tc-a','tc-b']` 恰 2 行；`sessionId='other-sess'` 的通知 ⇒ 0 行；无工具调用轮 ⇒ 0 行。
- 真实 omp 侧旁证：4 个实例各轮 `TOOL_CALL` 行数 = 该轮真实调用数（allow/read/deny 各 1；`--no-tools` 轮 0）。
- 实现锚点：`acp-client.js:348`（`params.sessionId !== this.sessionId` → return）。

**③-5 事件对象键集合恒定（身份缺省 `null`）；渲染层跳过 null：pass**

- `tool-permission.test.js:288-313`（缺省 `auditContext` 时身份四键存在且为 `null`，键集合 `deepEqual`）；`tool-permission.test.js:252-286`（集群身份非空）。
- 渲染层：集群实例渲染行**含**身份键（真实 omp 落行可见 `instance=… role=… chat_id=… context_id=…`）；`oamp/src/log.js:22` 跳过 `null` 逻辑未改（本卡未触碰 `log.js`）。
- 判定面遵守卡面约束：断言均针对事件对象/recorder fields，未以渲染行当判据（`tool-permission.test.js` 全程用 `recorder()` 事件数组）。

### ④ 兼容路径不回归 + 补 `source`（3 条）

**④-1 服务端 `session/request_permission` 原语义应答：pass**

- allow → `allow_once` + 恰 1 行 `TOOL_APPROVED`：`tool-permission.test.js:252-286`（`ok 3`，含 N 次调用 N 行）。
- deny → `reject_once` + 紧随 `session/cancel` + 轮次 `permission_denied`（会话保留）：`tool-permission.test.js:315-345`（`ok 5`）。
- 未知方法 → `-32601` 不挂起：`tool-permission.test.js:347-359`（`ok 6`）。
- `onPermissionRequest` 优先于静态档：`tool-permission.test.js:361-385`（`ok 7`，回归未被破坏）。

**④-2 兼容路径两事件带 `source='acp_permission'`：pass**

- 实现：`acp-client.js:441`（`_audit()` 字段集合新增 `source: 'acp_permission'`）。
- 断言：`tool-permission.test.js:312`（`assert.equal(fields.source, 'acp_permission')`）。

**④-3 既有「字段集合恒定」断言同步为 10 键，不删断言：pass**

- 断言仍在：`tool-permission.test.js:288-313`，字段集合由 9 键改为 10 键（新增 `source`），`assert.deepEqual(Object.keys(fields).sort(), [... 'source' ...].sort())`。
- 该文件 diff 共 3 行删除，全部为授权范围（fake 源注释 1 行 + 9 键数组及其断言消息 2 行）；卡面明确授权「既有断言由 9 键同步为 10 键」。

### ⑤ 握手与 NC-5 事实（2 条）

**⑤-1 `initialize` 仍以 `clientCapabilities: {}` 发出：pass**

- 实现：`acp-client.js:138`（唯一 `clientCapabilities` 出现点，值为 `{}`；全仓 `grep -rn clientCapabilities oamp/src` 仅此 1 处）。
- 断言：`tool-permission.test.js:517-525`（`ok 13`，fake 记录握手帧后 `deepEqual(init.params.clientCapabilities, {})`）。
- 真实 omp 端到端亦成立：allow 档工具真实执行成功（声明能力会走 `fs/*` 委托路径导致 `status=failed`，实测为 `completed`，反证声明为空）。

**⑤-2 NC-5（只读工具是否发通知）实测结论：pass —— 本人独立实测为分支①**

- fake 固化：`tool-permission.test.js:503-515`（`ok 12`，`kind=read` 同样落 1 行，path 取 `locations[0]`）。
- **真实 omp 18.0.11 实测（本人环境）**：向 allow 档实例投「读取 `<tmp>/read-probe.txt` 并把内容原样回复」⇒ 文件内容被正确回读（`PROBE-READ-CONTENT-42`），且落行恰 1 条：

```
[2026-09-11T05:25:34.936Z] agent TOOL_CALL instance=pb-dev-allow role=dev chat_id=chat-27b846cd-… context_id=ctx-90133-2 pid=90133
  tool_call_id=call_00_NXpIvni30QxWbOKrSqf79945 kind=read title="Reading probe file" status=completed
  source=acp_tool_call path=/var/folders/…/pr007-verify-GLj4RY/read-probe.txt
```

⇒ **只读工具同样推送 `tool_call` 通知**，E5 字面口径「每一次工具调用恰一行」**完全成立（§11.2 分支①）**，无需退化为「变更类 N=N」。
- 交付报告面：该结论已在 commit 85c7738 提交信息（"NC-5 真实 omp 18.0.11 实测定稿：只读工具（read）同样推送 tool_call 通知"）与 `tool-permission.test.js:499-502` 的用例注释中留档 ⇒ 卡面「交付报告须给出实测结论」满足（见偏差 D-2）。

### ⑥ 回归（2 条）

**⑥-1 测试全绿 + 指定文件零修改 + `acp-daemon.test.js` 只增断言：pass**

| 项 | 实测结果 |
|---|---|
| `node --test test/tool-permission.test.js` | **13/13 pass**（本人实跑） |
| `node --test test/acp-daemon.test.js` | **7/7 pass**（本人实跑） |
| `npm test`（worktree `oamp/`） | **203/203 pass**（本人实跑） |
| `test/context-pool.test.js` / `test/omp-executor.test.js` 零修改 | 成立（`git diff HEAD~1 --name-only` 仅 5 文件，不含二者） |
| `test/acp-daemon.test.js` 只增断言 | 成立：删除行计数 **0** |

**⑥-2 匿名实例行为逐字节不变（acp argv 与 `omp -p` argv）：pass**

- acp：真实 omp `ps` 显示 `omp acp --no-skills --no-rules --no-tools --no-session --model deepseek/deepseek-v4-flash`，与 `acp-daemon.test.js:546-552` 的逐字节 `deepEqual` 数组完全一致。
- `-p`：真实 omp `ps` 显示 `omp -p --no-session --no-tools 回复 OK`（无 `--approval-mode`、无 `--append-system-prompt`），与 0011 形状一致；代码面 `agent.js:165-172` 在 `toolsOn=false` 时仅追加 `--no-tools`，其余分支不可达 ⇒ 逐字节不变。
- 自动化面：`acp-daemon.test.js:565-569`（匿名一次性：含 `--no-tools`、不含 `--approval-mode`、不含 `--append-system-prompt`）。

---

## 二、委托 9 条验证标准判定

| # | 标准 | 判定 | 关键证据 |
|---|---|---|---|
| 1 | PR 卡 18 条逐条判定（可定位证据） | pass | 见上文「一、」；18/18 pass |
| 2 | argv 映射（allow→yolo / deny→always-ask / `tools=false`·匿名→无档位且含 `--no-tools`；daemon 与一次性两条路径） | pass | fake 用例 `tool-permission.test.js:389`、`acp-daemon.test.js:539/561/568/575`；**真实 omp `ps` 6 组 argv**（daemon：yolo / always-ask / `--no-tools`；一次性：yolo / always-ask / `--no-tools`；匿名两条路径均无档位） |
| 3 | `TOOL_CALL` 落行规则（去重 / 终态首见 / 轮末冲账 ⇒ N=N；非本 session 忽略；字段 & `path` 可选省略） | pass | `tool-permission.test.js:418/453/474`；真实 omp 原始帧 3 帧 → 1 行 |
| 4 | 兼容路径未回归 + `TOOL_APPROVED`/`TOOL_DENIED` 带 `source=acp_permission`（10 键） | pass | `tool-permission.test.js:252/288/312/315/347`；`acp-client.js:441` |
| 5 | 真实 omp 端到端（隔离临时环境，allow / deny） | pass | 见「三、真实 omp 端到端实测」 |
| 6 | 测试 13/13、7/7、`npm test` 203/203 | pass | 本人实跑 |
| 7 | 边界：仅 5 文件改动；`context-pool.js`/`omp-executor.test.js`/`context-pool.test.js` 零修改；`clientCapabilities` 恒空 | pass | `git diff HEAD~1 --name-only` = 5 文件（含 tasks md）；`grep -rn clientCapabilities oamp/src` 仅 `acp-client.js:138` 且为 `{}` |
| 8 | 反向核对（变异体因果隔离） | pass | 见「四、反向核对」 |
| 9 | 清理（无残留进程/socket/临时库） | pass | 见「五、清理确认」 |

---

## 三、真实 omp 端到端实测（本人独立复现，标准 5）

**环境**：真实 `omp` 18.0.11（`/Users/chenchiyuan/.bun/bin/omp`，**未设 `OAMP_OMP_BIN`**）+ 工作树 `oamp/bin/oamp.js`；自建临时目录 `os.tmpdir()/pr007-verify-*`（临时 `OAMP_SOCKET`、临时 `OAMP_DB`、随机端口 62162）；未触碰仓库 `.runtime/router.sock` 与 `data/sql.db`。脚本：`/tmp/pr007-verify/real-e2e2.mjs`。

| 轮次 | 触发 | 实测结果（本人数字） |
|---|---|---|
| ① allow（`--tools on`，permission 缺省） | 「用文件写入工具在 `<tmp>/allow-created.txt` 创建内容 hello-allow」 | 文件真实落盘（`hello-allow`，11 字节）；`TOOL_CALL … kind=edit status=completed source=acp_tool_call path=<该文件>` **恰 1 行**；`state=completed`，`error=null`，`duration_ms=4212`；回答「已写入。」 |
| ①′ allow 只读（NC-5） | 「读取 `<tmp>/read-probe.txt` 原样回复」 | 内容正确回读；`TOOL_CALL … kind=read status=completed` 恰 1 行 |
| ② deny（`--tools on --permission deny`） | 同①指令（目标 `<tmp>/deny-created.txt`） | **目标文件不存在**（无副作用）；`TOOL_CALL … status=failed` 恰 1 行；模型明确回绝「写入被拒绝（`Tool call denied by user: write`），未能创建 …」；`state=completed`，`error=null`，**`duration_ms=4545`（≤10s，不挂起）** |
| ③ 一次性路径 | `one_shot` 投「回复 OK」 | `TASK_RESULT state=completed exit_code=0`，`duration_ms=2061`；argv 见 ②-2 表 |
| ④ `--tools off` | 「请记住数字 9」 | 正常回答（非空答复，未静默失败）；该轮 `TOOL_CALL` **0 行** |

deny 档的语义与 §4.4 主机制定案一致：**omp 侧拒绝、模型明确回绝、轮次正常完成、无 `permission_denied`、无失败 out 记录、不挂起**。（执行方报告 6.4s，本人实测 4.5s——同判据 ≤10s 均成立，差异属模型/环境抖动。）

---

## 四、反向核对（变异体因果隔离，标准 8）

在 `/tmp` 副本上（**未改动 worktree**）做两处定点删除，实跑 `node --test test/tool-permission.test.js`：

| 变异 | 结果 | 结论 |
|---|---|---|
| A：删去 `acp-client.js:123` 的 `--approval-mode` 追加 | 仅用例 8（`§4.4/pr-007①` argv 档位）**not ok**，其余 12 条 ok（12 pass / 1 fail） | 档位映射的因果链唯一指向该行；该行存在是用例 8 成立的必要条件 |
| B：去掉终态判定（把终态首见分支改为恒真，即取消在飞表去重语义） | 用例 9/10/11/12（4 条落行规则）**not ok**，其余 9 条 ok（9 pass / 4 fail） | 落行规则（去重 + 冲账）是用例 9~12 成立的必要条件，且与档位映射互不耦合 |

两处变异均只影响对应判定面、不影响其它用例 ⇒ 测试对被测行为具**因果隔离性**，非「一起变色」的耦合断言。

---

## 五、清理确认（标准 9）

- 本人创建的临时运行环境（`os.tmpdir()/pr007-verify-*`、`pr007-frames-*`、`pr007-argv-*`）**全部删除**，复查 `ls -d` 无命中。
- 本人创建的临时进程（router / web / agent ×4 / 真实 omp 子进程）**全部终止**，复查 `ps` 无 `pr007-*`、`pb-dev-{allow,deny,off,tee}`、`dev-anon` 残留。
- 未使用主仓库 `.runtime/router.sock` 与 `data/sql.db`（`OAMP_SOCKET`/`OAMP_DB` 全程指向临时目录）。
- 保留物（非运行环境，属可复跑证据）：`/tmp/pr007-verify/{real-e2e2.mjs,probe-argv.mjs,probe-frames.mjs,omp-tee.cjs,run1.log,run2.log}`。
- worktree 未被修改：`git status --porcelain` 为空。
- 另见下一迭代候选 N-3：**他人**（执行侧）遗留的 `/tmp/pr007-*.mjs`、`/tmp/pr007-port.patch` 与一个残留 agent 进程（`pb-dev-probe`，其 `omp acp` 子进程带 `--approval-mode=always-ask`）仍在，非本次验收产出物，未由本人清理。

---

## 六、汇总

**PR 卡 18 条**：pass **18** / fail **0** / partial **0** / blocked **0**
**委托 9 条验证标准**：pass **9** / fail 0 / partial 0 / blocked 0

---

## 七、偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定（均无功能影响），但建议下一迭代同步。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | architecture §4.4 的 daemon 全量 argv 形态表把 `--append-system-prompt` 列在 `[--model <m>]` **之前** | 实际顺序为 `--no-session` → `--model` → `--append-system-prompt` → `--approval-mode`（`--model` 在前，为 0011 既有顺序，pr-007 未改） | 按实现微调文档该行表述（取值规则为 flag 取下一 token，位置无关，无功能影响）；或保留不动并在 §4.4 注一句「顺序不具约束力」 |
| D-2 | PR 卡 ⑤-2 / tasks 交付契约期望一份「交付报告」承载 NC-5 实测结论与 7 项交付内容 | 交付报告**未落成独立文件**，其内容存在于 commit 85c7738 提交信息与 `test/tool-permission.test.js:499-502` 注释中（结论完整、可定位） | 若需留档，把 dev 交付报告补入 `clarifications/` 或 PR 卡附录；当前结论已被独立复现，不影响验收 |
| D-3 | §4.5「取值/渲染两层口径」称事件对象中键恒定存在 | `TOOL_CALL` 的 `path` 是**唯一可省略键**（无值时该键不存在，其余 10 键恒在） | 文档已在字段清单显式豁免（"无则省略该键"），建议在「键恒定存在」句补一句「`path` 除外」，避免后续实现误按「恒 11 键」断言 |

---

## 八、下一迭代候选

- **N-1 跨轮迟到通知的归属边界**：若某 `toolCallId` 的非终态帧在轮末冲账**之后**才到达，它会被登入在飞表并在**下一轮**轮末才落行（归属轮次错位）；当前验收标准未覆盖，§4.5 亦未规定。建议下一迭代评估：按轮次归属（记 `roundId`）或在 `kill()/dispose()` 时丢弃残留。
- **N-2 兼容路径的真实环境不可覆盖**：ACP `session/request_permission` 在 omp 18.0.11 下不可触发（V-9），其正确性仅由 fake 帧级用例保证；建议在 omp 升级时复测，或为该段代码加版本注记以便将来清理。
- **N-3 执行侧临时环境残留（非本 PR 产出物）**：`/tmp/pr007-{argv,oneshot,smoke,repro}.mjs`、`/tmp/pr007-port.patch` 与残留 agent 进程 `pb-dev-probe`（pid 75613，其 `omp acp` 子进程 pid 75632 带 `--approval-mode=always-ask`）仍在运行/留存。建议主 agent 协调执行方清理。
- **N-4 `TOOL_CALL` 不区分失败来源**：deny 档（omp 侧拒绝）与"工具执行出错"在审计行上同为 `status=failed`；若后续需要按 E5 区分语义，可在事件对象增加来源字段（当前口径无此要求）。

---

## 九、结论

**PASS**（18 条验收 + 9 条委托标准全部 pass，无 fail / partial / blocked）。

判定依据要点：argv 档位映射在 **daemon 与一次性两条路径**上均经 fake 断言 + **真实 omp `ps` 六组实测**确认；`TOOL_CALL` 的「一次调用恰一行」在**真实 omp 原始帧层**被证明为有效去重（3 帧 → 1 行），而非未被触发的兜底；deny 档经**本人隔离环境复现**为「无副作用 + 明确回绝 + `duration_ms=4545`（≤10s）正常收尾」；回归面 13/13、7/7、203/203 全绿且边界文件零越界；变异体实验证明测试具备因果隔离性；临时环境已清净（无残留进程 / socket / 临时库）。

偏差记录 3 条（D-1/D-2/D-3）均为文档与留档形态问题、无功能影响，不阻塞交付，由主 agent 决定是否纳入下一迭代。
