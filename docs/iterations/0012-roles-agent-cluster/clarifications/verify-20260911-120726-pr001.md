# 独立验证报告：PR-001 角色标识与角色文件定位（role-binding）

**验证者身份**：资深 Node.js 模块与单元测试审查者（同级代码审查者视角）——审查范围是「小体量纯函数叶子模块 + 其单测」：关注导出契约是否与架构契约逐字对齐、路径解析基准是否与既有 `config.js` 同口径、单测是否真的具备失败能力（测试是否会因错误实现转红）以及测试是否污染仓库/依赖外部环境。

**产出物**：
- worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0012-pr-001-role-binding`（分支 `feat/0012-pr-001-role-binding`，HEAD = `846b7d4`，工作区 clean）
- 被验证文件：`oamp/src/role-binding.js`（新增，45 行）、`oamp/test/role-binding.test.js`（新增，94 行）
- 冻结基准：`git diff edfd710..846b7d4`（`git diff --name-status` 输出仅 `A oamp/src/role-binding.js` + `A oamp/test/role-binding.test.js`）
  - 基准说明（仓库事实，非执行过程信息）：验证时 `iteration/0012-roles-agent-cluster` 的 ref 已前移到 `bd92048`（`git show --stat bd92048` = 4 个 docs/status/tasks 文件、233 行，**纯文档提交**）；`git merge-base --is-ancestor 846b7d4 iteration/0012-roles-agent-cluster` → NO，即 PR-001 代码尚未合入迭代分支。本次验证严格按委托指定的 `base = edfd710` 与 worktree HEAD `846b7d4` 比对。

**验证标准来源**：PR 卡 `docs/iterations/0012-roles-agent-cluster/prs/pr-001-role-binding.md`「验收标准」6 条 + 委托补充标准（边界/零依赖/反向核对）+ 架构契约 `architecture.md` §3.1、§12.2 契约 2（`architecture.md:155-159`、`architecture.md:673`）

**验证日期**：2026-09-11

**独立性声明**：未接收任何执行过程信息；报告结论仅来自产出物、验证标准与仓库可观察事实。以下所有命令均在 worktree（或其临时副本）中执行，唯一写入为临时目录与本源报告，worktree 字节级未改动（验证结束时 `diff` 复核见证据 6.4）。

---

## 逐项判定

### 标准 1（卡验收 1）：模块存在、导出四函数、不 import 第三方包 —— **pass**

- 证据 1.1：`oamp/src/role-binding.js` 存在（45 行）。`grep -n "^export" oamp/src/role-binding.js` →
  `16: export function resolveRoleRoot(env = process.env)` / `25: export function resolveRoleFile(root, role)` / `30: export function instanceIdForRole(role)` / `35: export function roleFromInstanceId(instanceId, env = process.env)` —— **恰好 4 个具名导出、无 default 导出、无多余导出**。
- 证据 1.2：`grep -n "^import" oamp/src/role-binding.js` → `6: node:fs`、`7: node:url`、`8: node:path` —— 全部为 Node 内置模块，零第三方、零 `src/` 内部依赖（叶子模块性质保持）。
- 证据 1.3：符号名与 `architecture.md:673`（§12.2 契约 2）逐字一致：`instanceIdForRole` / `roleFromInstanceId` / `resolveRoleRoot` / `resolveRoleFile`。

### 标准 2（卡验收 2）：`instanceIdForRole('dev') === 'pb-dev'`；`grep -rn "'pb-'" oamp/src` 只命中 `role-binding.js` —— **pass**

- 证据 2.1：`oamp/test/role-binding.test.js:41-47`（用例 1）断言 `instanceIdForRole('dev') === 'pb-dev'`，并对 F01-1 的 10 个角色（`architect/demand/dev/planner/pr-planner/prd/progress-observer/retrospective/verifier/workflow-pb`）逐一断言 `=== 'pb-' + role`；实测该用例 ok（`# pass 8 / # fail 0`）。
- 证据 2.2：在 worktree 根执行 `grep -rn "'pb-'" oamp/src`，输出 **3 行，全部位于 `oamp/src/role-binding.js`**（`:3` 注释、`:12` `const INSTANCE_PREFIX = 'pb-'`、`:29` 注释），退出码 0；无其他模块命中。
- 证据 2.3（加严）：放宽为 `grep -rn "pb-" oamp/src`，仍只命中 `oamp/src/role-binding.js`（5 行，含注释）——不存在任何其他文件以任何拼接形态（`'pb' + '-'`、模板串等）自造前缀。
- 证据 2.4：公式单点在实现内成立——前缀常量 `INSTANCE_PREFIX`（`:12`）同时供正向拼接（`:31`）与反向正则（`:13`，`^${INSTANCE_PREFIX}(.+)$`）使用。

### 标准 3（卡验收 3）：`roleFromInstanceId('pb-dev') === 'dev'`；`'dev-1'` / `'pb-'` → `null` —— **pass**

- 证据 3.1：在 worktree 内实跑 `node --input-type=module -e "import {roleFromInstanceId} from './src/role-binding.js'; console.log(roleFromInstanceId('pb-dev'))"` → 输出 `dev`（`oamp/` 为 cwd，默认 roleRoot = worktree 根，其中 `roles/dev/dev.md` 存在）。
- 证据 3.2：`oamp/test/role-binding.test.js:70-77`（用例 7）断言 `'dev-1' → null`、`'pb-' → null`、`'' → null`、`'pb-architect' → null`、`'pb-dev-1' → null`；用例 8（`:79-84`）断言非字符串输入（`null`/`undefined`/`123`/`{}`）→ `null`。实测全绿。
- 证据 3.3（存在性条件的契约依据）：实现 `:44` 为 `return existsSync(resolveRoleFile(resolveRoleRoot(env), role)) ? role : null;`，即「`^pb-(.+)$` **且角色文件存在**」才绑定——与 `architecture.md:157`（§3.1 反向映射）及 `prd/F01-role-instance-identity-lifecycle.md:41`（AR-02 推断规则）逐字一致。卡文本只写了正则侧，未写存在性（见偏差 D-1）。
- 证据 3.4：`'pb-'` 用例的通过不依赖文件检查的偶然性——`'pb-'` 在 `^pb-(.+)$` 下直接不匹配（`.+` 至少 1 字符），与实现注释 `:34` 一致。

### 标准 4（卡验收 4）：`resolveRoleRoot(env)` 覆盖与缺省口径 —— **pass**

- 证据 4.1（同口径）：`oamp/src/config.js:10` = `const PKG_ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');`；`oamp/src/role-binding.js:11` = 同一表达式（逐字相同）。缺省分支 `:21` = `path.resolve(PKG_ROOT, '..')`。
- 证据 4.2（缺省 = 仓库根）：用例 2（`:49-52`）断言 `resolveRoleRoot({}) === path.resolve(测试文件 dir, '..', '..')` 且 `resolveRoleRoot({ OAMP_ROLE_ROOT: '' })` 同值；实测 ok。按真实仓库布局折算（`node -e` 复算）：文件位于 `agents/oamp/src/` → PKG_ROOT = `/Users/chenchiyuan/projects/agents/oamp` → 缺省 roleRoot = `/Users/chenchiyuan/projects/agents`，即卡要求的「本仓库 `agents/`」。
- 证据 4.3（覆盖）：实现 `:17-20` 对 `env.OAMP_ROLE_ROOT` 做 `typeof === 'string' && !== ''` 判定后 `path.resolve(override)`（绝对化，相对值按 cwd 解析）；用例 3（`:54-60`）断言 `'/tmp/oamp-roles'` 原样返回、`'relative/roles'` → `path.resolve('relative/roles')`。实测 ok。
- 证据 4.4（注入验证，见 §反向核对 A）：把缺省分支改为 `path.resolve(PKG_ROOT)` 后，用例 2 与用例 5 转红（2 fail），证明缺省口径被测试真实锁定。

### 标准 5（卡验收 5）：`resolveRoleFile(root, 'dev')` = `<root>/roles/dev/dev.md` 绝对路径；`OAMP_ROLE_ROOT` 覆盖后随之改变 —— **pass**

- 证据 5.1：实现 `:26` = `path.resolve(root, 'roles', role, \`${role}.md\`)`；用例 4（`:62-68`）断言 `resolveRoleFile('/tmp/root','dev') === '/tmp/root/roles/dev/dev.md'`、以真实仓库根为 root 时等于 `path.join(REPO_ROOT,'roles','dev','dev.md')`，并断言不存在的角色仍返回路径（存在性归调用方）——与卡「本模块只解析路径」及 `architecture.md:158-159`/§5.1 一致。
- 证据 5.2（覆盖传导）：用例 6（`:72-77`）以 `roleRootFixture(ROLES)`（`mkdtempSync` 建 `<tmp>/roles/<role>/<role>.md`）作为 `OAMP_ROLE_ROOT`，10 个角色经 `roleFromInstanceId` 全部返回角色名——该路径必然经过 `resolveRoleRoot(env)` → `resolveRoleFile(...)`，证明覆盖值确实改变最终解析位置。实测 ok。
- 证据 5.3（注入验证，见 §反向核对 D）：把 `resolveRoleFile` 改为扁平路径 `root/roles/<role>.md` 后，用例 4/5/6 转红（3 fail），证明该解析式被测试真实锁定。
- 覆盖度说明：卡验收 5 第二句「覆盖后随之改变」为**间接覆盖**（经 `roleFromInstanceId`），无 `resolveRoleFile(resolveRoleRoot(env), role)` 的直测断言（见偏差 D-3，不影响判定：行为已被证据 5.2/5.3 双重证实）。

### 标准 6（卡验收 6）：测试全绿、不依赖真实 omp / 网络 / tmux、既有 16 个测试文件零修改 —— **pass**

- 证据 6.1：`cd oamp && node --test test/role-binding.test.js` → `# tests 8 / # pass 8 / # fail 0`（`cancelled 0 / skipped 0 / todo 0`）。
- 证据 6.2：`cd oamp && npm test` → `# tests 160 / # pass 160 / # fail 0`（耗时 ≈37.5s），与卡「160/160」一致。
- 证据 6.3（无外部依赖）：测试文件 import 仅 `node:test` / `node:assert/strict` / `node:fs` / `node:os` / `node:url` / `node:path` + 被测模块；fixture 全部 `mkdtempSync(tmpdir())`（`:33-39`）；无网络、无 tmux、无 omp 二进制调用；未向 `oamp/` 或仓库根写任何文件。
- 证据 6.4（既有测试零修改）：`git diff --name-only edfd710..846b7d4 -- oamp/test` → 仅 `oamp/test/role-binding.test.js`（状态 `A`）；`git ls-tree -r --name-only edfd710 -- oamp/test | grep -c '\.test\.js$'` → 16，且 base 中不存在 `oamp/test/role-binding.test.js`（计数 0）。当前 worktree 测试文件数 17 = 16 + 1 新增，其余 16 个文件字节未变。

### 标准 7（委托 Boundary）：文件边界 / 依赖 / 文档零改动 —— **pass**

- 证据 7.1：`git diff --name-status edfd710..846b7d4` → 恰好 2 行：`A oamp/src/role-binding.js`、`A oamp/test/role-binding.test.js`（`--stat` = 139 insertions，0 deletions）。
- 证据 7.2：`git status --porcelain --untracked-files=all` 为空（无未提交/未跟踪残留）。
- 证据 7.3：`oamp/package.json` 的 `dependencies` 为 `{}`（且既有 `test/hygiene.test.js` 第 3 条断言强制此事实，全绿）；无 lockfile / 依赖清单变化（不在 diff 文件列表中）。
- 证据 7.4：`prd/`、`architecture.md`、`demand.md` 均不在 diff 文件列表中；worktree HEAD 提交信息为 `feat(0012): pr-001 role-binding 模块与单测（角色标识与角色文件定位单一真源）`。

---

## 反向核对（测试是否真的能失败）

方法：把两个产出文件复制到临时目录（`<tmp>/repo/oamp/{src,test}`，并补 `<tmp>/repo/roles/dev/dev.md` 以复刻仓库布局），在**临时副本**上注错，观察对应用例是否转红；worktree 未被触碰。

| 注入 | 变异内容 | 失败用例（`not ok`） | 结论 |
|---|---|---|---|
| 对照（未改） | — | 无（`# pass 8 / # fail 0`） | 布局复刻正确，基准可信 |
| A | 缺省 roleRoot 改为 `path.resolve(PKG_ROOT)`（去掉 `'..'`） | 2、5（2 fail） | 标准 4 的缺省口径被真实锁定 |
| B | `roleFromInstanceId` 去掉 `existsSync` 存在性检查（恒返回 role） | 7（1 fail） | 标准 3 的「文件缺失 → null」被真实锁定 |
| C | `instanceIdForRole` 返回 `pb-<role>-1`（加后缀） | 1（1 fail） | 标准 2 的「id 无后缀」被真实锁定 |
| D | `resolveRoleFile` 改为 `root/roles/<role>.md` | 4、5、6（3 fail） | 标准 5 的路径式与覆盖传导被真实锁定 |

复核：注入实验结束后 `diff -q` 临时副本与 worktree 原件 → `identical`，且副本再跑一次全绿（8/8）。**四个导出各自都至少有一条会因错误实现转红的用例，不存在「恒真测试」。**

---

## 汇总

- pass：**6 项**（卡验收 1–6）+ 1 项委托附加边界标准 = **7 项全部 pass**
- fail：**0 项**
- partial：**0 项**
- blocked：**0 项**

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定（卡验收 1–6 全部满足），但应在下一迭代同步文档或评估实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | PR 卡验收 3 只写「`^pb-(.+)$` 不成立即不绑定」，未提「角色文件存在」条件（`prs/pr-001-role-binding.md` 验收 3） | `roleFromInstanceId` 要求正则成立**且** `roles/<role>/<role>.md` 存在，否则 `null`（`src/role-binding.js:44`）——与架构 `architecture.md:157`（§3.1）、`architecture.md:549`（AR-02）一致 | 按架构为准；建议 PR 卡验收 3 补注「且角色文件存在」，避免后续读卡者以为纯前缀解析 |
| D-2 | `architecture.md:673`（§12.2 契约 2）写 `roleFromInstanceId(id)` | 实现签名为 `roleFromInstanceId(instanceId, env = process.env)`，多一个**可选** env 参数（`:35`）；单参调用行为不变（向后兼容，pr-004 按 1 参调用即可） | 契约文档补注该可选参数（测试用它指向 fixture 目录，替代直接改写 `process.env`） |
| D-3 | 卡验收 5 第二句「`OAMP_ROLE_ROOT` 覆盖后随之改变」；`architecture.md:436`（§5.6）列该文件的覆盖点为「四个函数 + 参数归一与优先级」 | 覆盖传导仅经用例 6（`roleFromInstanceId` + fixture root）**间接**覆盖；无 `resolveRoleFile(resolveRoleRoot({OAMP_ROLE_ROOT:X}), role)` 的直测断言；`resolveRoleFile` 的非法入参（非字符串 root/role）亦无用例 | 建议补一条直测断言；属测试覆盖度偏差，行为本身已被证据 5.2/5.3 证实 |

## 下一迭代候选

- **单起推断对部署布局敏感**：缺省 roleRoot = `PKG_ROOT/..`，仅在「`oamp/` 与 `roles/` 同级」的仓库布局下成立；若将来 oamp 被独立安装/发布，`roleFromInstanceId` 会因角色文件不存在而恒返 `null`（当前设计以本仓库为唯一目标，`architecture.md:155`/AR-01 已明确，属已知边界而非缺陷）。
- **用例 5 依赖 `process.env`**：`roleFromInstanceId('pb-dev')`（`test:70-72`）未显式传 env，若运行环境预设了 `OAMP_ROLE_ROOT`（如某些 shell 已 `export`），该用例会漂移；建议改为显式传 env（同一文件其他用例已示范）。
- **临时 fixture 无清理**：`roleRootFixture` 用 `mkdtempSync` 建目录后未删除，重复跑测会在系统临时目录累积残留；可加 `test` 的 `after`/`t.after` 清理（非验收项）。
- **叶子模块被 I/O 侵入的边界**：`roleFromInstanceId` 内含 `existsSync`（`src/role-binding.js:44`），使「纯函数」模块带有文件系统副作用；对 pr-004 的可用性无影响（同步 I/O + 确定性），但若未来要复用为 ACP/浏览器侧逻辑需注意。

## 结论

**PASS**（卡验收 1–6 与委托附加边界标准全部 pass，0 fail，0 partial，0 blocked；3 条偏差已记录，按 verifier 规约不阻塞本次交付）

> 注：偏差记录不影响结论判定——有偏差不等于 FAIL，偏差被记录即满足交付条件。
