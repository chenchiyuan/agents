# 独立验证报告 — 0012-roles-agent-cluster（阶段 2 功能规格 + 阶段 3 技术架构）

**验证者身份**：**架构评审者（兼功能规格可测性方向的测试工程师）**——反射自本次产出物类型「功能规格卡 + 技术架构文档」：判断架构是否过度设计 / 组件是否凭空 / 内部口径是否自洽，需要架构评审者的资格；判断每张卡的验收标准是否可测、判定面是否真正落地，需要测试工程师的资格。二者合并为本次的唯一验证者身份，不引入第三种视角。

**产出物**：

| # | 路径 | 说明 |
|---|---|---|
| 1 | `docs/iterations/0012-roles-agent-cluster/prd.md` | 报告级索引 + 需求追溯 + 架构维度落定（25.8KB） |
| 2 | `docs/iterations/0012-roles-agent-cluster/prd/F01-role-instance-identity-lifecycle.md` | 功能卡 F01（5.4KB） |
| 3 | `docs/iterations/0012-roles-agent-cluster/prd/F02-role-definition-loading.md` | 功能卡 F02（5.2KB） |
| 4 | `docs/iterations/0012-roles-agent-cluster/prd/F03-model-default-per-role-override.md` | 功能卡 F03（3.3KB） |
| 5 | `docs/iterations/0012-roles-agent-cluster/prd/F04-tool-toggle-per-role.md` | 功能卡 F04（4.3KB） |
| 6 | `docs/iterations/0012-roles-agent-cluster/prd/F05-permission-policy.md` | 功能卡 F05（5.0KB） |
| 7 | `docs/iterations/0012-roles-agent-cluster/prd/F06-cluster-script-config-tmux.md` | 功能卡 F06（9.2KB） |
| 8 | `docs/iterations/0012-roles-agent-cluster/prd/F07-role-working-directory.md` | 功能卡 F07（3.1KB） |
| 9 | `docs/iterations/0012-roles-agent-cluster/architecture.md` | 技术架构 v1.1.0（75.1KB，703 行） |

**对照基准（仅作追溯基准，不验证其自身）**：`docs/iterations/0012-roles-agent-cluster/demand.md`（v1.0.0，2026-09-11）。

**验证标准来源**：主 agent 委托的 8 条判定标准（原文见「逐项判定」标题）；旁证标准 = `.pb-agents/roles/workflow-pb/workflow-pb.md` 的「阶段定义」表——阶段 2 推进条件（"所有功能点有独立卡片；无 demand.md 外新增功能；架构待定项已标注 `[架构待填]`"，该文件:111）、阶段 3 推进条件（"L1 决策经用户确认；所有功能卡有技术路径；无架构内部冲突"，该文件:112）。

**验证日期**：2026-09-11

**独立性声明**：本报告未接收任何执行过程上下文（未接受"设计意图 / 为什么这么做"类信息）；验证全程只读，未修改任何被验证产出物；本报告文件是本次唯一写入。判定只依据产出物本身 + 上述验证标准。

**全局理解（先读全貌，再逐条）**：7 张卡构成"实例身份（F01）→ 角色注入（F02）→ 模型（F03）→ 工具（F04）→ permission（F05）→ 集群入口（F06）→ 按角色 cwd（F07）"一条链；architecture.md 以「§3 角色绑定 / §4 会话能力 / §5 集群入口」三块回答 AR-01~AR-20，并在 §8 做逐条落定、§11 记录 L1 确认、§11.2 收窄 E5 审计口径、§13 记风险、§14 记疑问。跨条目的一致性风险集中在：工具/permission 的默认档口径、cwd 相对基准与配置文件落点的耦合、以及"阶段 3 是否只动了架构维度"这一过程性事实——以下逐条判定。

---

## 逐项判定

### 标准 1：每张功能卡可追溯到 `demand.md`（v1.0.0）的"做什么"（W1~W8），且无 demand.md 之外新增功能点

**判定：pass**

证据（可定位）：

| 卡 | 卡内来源行 | 对应 demand W 条款 | 判定 |
|---|---|---|---|
| F01 | `prd/F01-...md:4`（"W1（D-2 + 仓库事实 F-2）、W2（G-1）、W7（G-4）"） | `demand.md:153` W1 / `:154` W2 / `:159` W7 | pass |
| F02 | `prd/F02-...md:4`（"W3（G-2 / M-1）、TC-09"） | `demand.md:155` W3 | pass |
| F03 | `prd/F03-...md:4`（"W4（TC-01 / G-6）"） | `demand.md:156` W4 | pass |
| F04 | `prd/F04-...md:4`（"W5 前半（D-1 / TC-02）"） | `demand.md:157` W5 前半 | pass |
| F05 | `prd/F05-...md:4`（"W5 后半（D-1 / TC-03）"） | `demand.md:157` W5 后半 | pass |
| F06 | `prd/F06-...md:4`（"W6（D-3 / TC-04 / TC-07）、W7（批量入口）"） | `demand.md:158` W6 / `:159` W7 | pass |
| F07 | `prd/F07-...md:4`（"W8（TC-08 用户改判）、D-6"） | `demand.md:160` W8 | pass |

覆盖性与"无新增功能点"的双向核对：

1. **W1~W8 全覆盖**：`prd.md:62-74`「W 条款（做什么）」表逐条给出 W→卡的落点（W1→F01-1/2、W2→F01-3/6、W3→F02-1~8、W4→F03-1~5、W5→F04+F05、W6→F06-1~7、W7→F01-4+F06-8、W8→F07-1~4），与 7 张卡的验收标准逐项可对上，无遗漏 W 条款。
2. **无 demand 之外的功能点**：逐卡核对验收标准，全部落在 demand 的 W / E / D / TC 条款内。抽验最容易被怀疑为"新增"的 6 条：F01-4 单起可用（`prd/F01:18` ← W7/G-4）；F01-7 多轮上下文（← demand E2 后半）；F02-8 只读消费（← N1）；F04-6 开关可配置（← W5）；F05-5 档位重启生效（← W5 + N11）；F06-8 批量入口不排他（← W7）。
3. **边界不是新增功能**：N1~N12 全部落到卡边界或全局边界（`prd.md:75-91`），卡边界措辞均为"不含/不做"，无被反向写成能力。
4. **两处需注明的追溯口径**（不构成 fail，说明判定依据）：
   - F01-7 的"不同对话之间互不串扰"未逐字出现在 W1~W8，但它是 demand 已核实的仓库事实 F-8（跨 chat 隔离）在 N10（不改 0011 语义）下的必然延续——属**非回归条款**，不是新能力（依据：`prd.md:81` 对 F01 的 E2 映射 + demand N10）。
   - F03-4 的解析优先级（`prd/F03:16`）与 F03-5 的"生效可观察"是 W4 原文（"解析链沿用 0011"）的具体化，不是新增功能面。

### 标准 2：每张卡含：功能 ID、用户价值（一句话）、验收标准（具体可测）、边界（不包含什么）

**判定：pass**

证据：7 张卡的四要素齐备且**行号可定位**（机器核对每卡 4 个标题均恰好出现 1 次，`grep -c` = 1/1/1/1）：

| 卡 | 功能 ID | 用户价值 | 验收标准 | 边界（不包含） |
|---|---|---|---|---|
| F01 | `:3` | `:9` | `:13`（7 条） | `:23`（12 条） |
| F02 | `:3` | `:9` | `:13`（8 条） | `:24`（6 条） |
| F03 | `:3` | `:9` | `:13`（5 条） | `:21`（5 条） |
| F04 | `:3` | `:9` | `:13`（6 条） | `:22`（5 条） |
| F05 | `:3` | `:9` | `:13`（5 条） | `:21`（6 条） |
| F06 | `:3` | `:9` | `:13`（8 条） | `:24`（8 条） |
| F07 | `:3` | `:9` | `:13`（4 条） | `:20`（5 条） |

**可测性抽验**（每条都能定位到"判什么"的手段，且阶段 3 后判定面已落地、不再是悬空引用）：

- F01-3 "无对话时无归属 LLM 子进程" → 判定面在 `prd/F01:40`（AR-03：`ps` 父链上溯）与 `architecture.md:210-218`（§3.5）。
- F02-2 "原文比对" → `prd/F02:15` + `architecture.md:178-191`（§3.3 判定面 2：E3 原文比对）。
- F03-5 "模型标识可观察" → `prd/F03:17` + `architecture.md:232-239`（§4.2：落库 out 记录 `model` 字段）。
- F05-2 "N 次工具调用 = N 条记录" → 判定面 `architecture.md:284-295`（§4.5）+ `:625-634`（§11.2 收窄口径，见下文偏差 #1）。
- F05-4 "不长时间挂起" → `architecture.md:253-283`（§4.4：`duration_ms ≤ 10s` + `error='permission_denied'`），判据为可执行数字，非主观词。
- F06-5 "收口无残留" → `architecture.md:420-429`（§5.5：以 pane pid 为根的子树存活检查）。
- F07-4 "窗口 cwd = 配置值" → `prd/F07:23` + `architecture.md:388-409`（§5.3：`#{pane_current_path}` / `lsof -d cwd`）。

未发现任何一张卡存在"只写效果、无判定手段"或"判定手段主观化（体验良好/正常工作）"的条目。

### 标准 3：架构维度（技术选型/数据模型/接口设计）在阶段 2 卡片中为空或缺省标注，产品维度不含技术选型

**判定：pass**

证据：

1. **阶段 2 卡片形态 = 占位标注（合规）**：`git diff ff1cd1a cc0c7c4` 的删除侧显示阶段 2（commit `ff1cd1a`，2026-09-11 11:35）7 张卡的架构维段一律为 `## 架构维度（待阶段 3 填写）` + `- \`[架构待填]\` **AR-xx**：…`（例：F02 卡两条 AR-04/AR-05 占位、F06 卡六条 AR-12~AR-17 占位、F07 卡两条 AR-18/AR-19 占位）。**未**在阶段 2 预先写入任何技术选型或数据模型。
2. **产品维度不含技术选型**：逐卡通读 `用户价值 / 验收标准 / 边界（不包含）` 三段（行号见标准 2 表），其中出现的"技术名词"全部可追溯到 demand 的**用户已确认结论或仓库事实**，不是阶段 2 自行做出的技术选择：
   - F01:15 "oamp 节点进程 / 注册 / 心跳 / 注销" ← demand W2（`:154`，`user_confirmed`）；
   - F01:16 "web 控制台 `@` 候选 / SSE / SQLite" ← demand N10 + F-8（`:194`）；
   - F02:14 "加载真实生效" ← demand W3 三条约束（`:155`）；
   - F02:31-32（边界）显式写明"**不含注入机制的实现选择**（进程级 `--append-system-prompt` vs 固定 cwd + `AGENTS.md` vs 其它）……机制定案归 AR-04"——提及候选机制是为了**排除**，属边界声明而非选型，不构成产品维度污染；
   - F04:16 "默认（常驻）对话路径硬编码禁用工具" ← demand W5 + 仓库事实 F-7（`:157`、`:128`）；
   - F06:15-21 "tmux session / up-down-status / 日志落盘 / 配置纳入 git" ← demand W6 + D-3/D-5/TC-04/TC-07；
   - F07:14 "`cwd` 字段" ← demand W8 + D-6（`:160`）。
3. **架构维度只在架构段内**：技术选型（`--append-system-prompt`、`cluster.json` 字段、`oamp cluster` 子命令、`AcpError('permission_denied')` 等）全部出现在 `## 架构维度（阶段 3 已填…）` 段内，且每段首行标注落点（如 `prd/F02:33`）。

### 标准 4：`architecture.md` 中 `[架构待填]` 全部补全（AR-01~AR-20 逐条可定位；全文无 `[架构待填]` 残留）

**判定：pass**

证据：

1. **零残留**：`grep -n '\[架构待填\]'` 对 `architecture.md` / `prd.md` / `prd/F01~F07*.md` = **0 命中**（命令以 exit 1 结束）。`[架构待填]` 仅存在于历史性文件（`history.md:60/66/76`、`progress.md:39`）与上游输入合同 `demand.md:177/183/186`——前者是过程记录、后者是阶段 1 产物，均不属本次被验证产出物。
2. **AR-01~AR-20 逐条可定位**：
   - `architecture.md:542-568`（§8「AR-01~AR-20 逐条落定」）为一个 20 行表，`grep -o '^| AR-[0-9][0-9]' | sort -u` 命中 **AR-01…AR-20 恰好 20 个**，每行都给出"落定内容（一句话）+ 详见节号"。
   - 明细落点可追溯：AR-01/02/03→§3.1/§3.4/§3.5（`:150-218`）、AR-04/05→§3.2/§3.3（`:161-191`）、AR-06~AR-11→§4.1~§4.6（`:221-305`）、AR-12~AR-17→§5.1~§5.5（`:308-429`）、AR-18/19→§5.1/§5.3（`:308-364`、`:388-409`）、AR-20→§5.6（`:430-443`）。
   - 索引侧同步：`prd.md:181-207` 的「阶段 3 落定摘要」也是 20 行（AR-01~AR-20），与 `architecture.md §8` 一一对应；`prd.md:156-180` 以「阶段 2 原始待填项（追溯保留）」保留了 20 条原始待填描述（无方括号标记），供追溯。
3. **每张卡的技术路径**：F01→AR-01/02/03、F02→AR-04/05、F03→AR-06/07、F04→AR-08/09、F05→AR-10/11、F06→AR-12~17、F07→AR-18/19（卡片 `## 架构维度` 段各自落定），AR-20 由 `prd.md:26` 全局落定。即"所有功能卡有技术路径"（workflow-pb 阶段 3 推进条件）成立。

### 标准 5：L1 决策已确认留痕：§11（或 §11.1）六项均标注"已确认（2026-09-11，用户决策）"且给出最终取值；全文无"待确认/未确认前不实施"类残留

**判定：pass**

证据（说明标注形态，避免误判）：

1. **六项均标记已确认**：`architecture.md:601` 标题「## 11. L1 决策清单（**已全部确认**，2026-09-11）」；`:603` 状态行写"L1-1~L1-6 **用户已全部确认，六项均按推荐方案**……本清单实现的**前提**（阶段 4 可直接开工）"；`:614` 标题「### 11.1 确认结果（**2026-09-11 用户决策**，逐项）」。逐项行 `:618-623` 的"确认结果"列分别为"已确认（按推荐）"×5 + "已确认（按推荐；'属 L2'的自评被接受）"（L1-6），六项齐备。
2. **六项均给出最终取值**：同表 `:618-623` 的"最终取值（= 实现契约）"列逐项给出——L1-1 进程级 `--append-system-prompt <roles/<role>/<role>.md 绝对路径>`；L1-2 tools 缺省 true + permission 两档（allow=`allow_once`+审计 / deny=`reject_once`+`session/cancel`+轮次 `permission_denied`）；L1-3 `oamp cluster up|down|status`；L1-4 仓库根 `cluster.json`；L1-5 不常驻 LLM；L1-6 4 个 flag + 推断绑定。
3. **形态说明**：日期与"用户决策"写在 §11/§11.1 的**表头与状态行**（同一表格的直接上级），逐项行本身写"已确认（按推荐）"而非逐行重复日期。以"六项是否都被标注为已确认、且有日期与最终取值"为判据，**满足**；若要求逐行重复日期字符串，则形态不同但语义等价——不构成 fail，仅在此声明验证口径。
4. **无残留**：`grep -n '待确认\|未确认前不实施'` 对 `architecture.md`/`prd.md`/`prd/*.md` = 0 命中。`architecture.md:678` 的 §13 标题含"未决项"二字，但该表 R-1~R-10 每一行都有明确"处置"列（无一条挂起等决策）；`:695` §14 的 7 条疑问全部给出"已裁定/已确认/处理"结论（例：`:697` 疑问 1 已裁定为 §11.2 口径、`:700` 疑问 4 按 W6 较宽口径落地）。即"待确认"在三个交付物中零残留。

### 标准 6：产品维度未被架构阶段改动（`git log -p -1 cc0c7c4` 核实架构阶段的 prd 改动仅限架构维度）

**判定：partial**

判定拆分（子项逐条，避免"基本通过"）：

| 子项 | 判定 | 证据 |
|---|---|---|
| 用户价值未被改 | pass | 全部 7 卡的 `## 用户价值` 段在 `git diff ff1cd1a cc0c7c4` 中零改动（diff 无该段任何 ±  行） |
| 边界（不包含）未被改 | pass | 同上，7 卡 `## 边界（不包含）` 段零改动 |
| 验收标准的**判定内容**未被改 | pass | 无任何验收条目被改写、删除或新增；7 卡的验收条目编号与文本主体逐字一致 |
| 验收标准的**行内确认日期**未被改 | **fail** | `prd/F02-...md:20`（验收 6）、`prd/F04-...md:19`（验收 5）、`prd/F06-...md:15`（验收 1）三行被修改，均为 `[user_confirmed]`（M-0x，**2026-09-10** 用户确认）→（**2026-09-11** 用户确认）；同卡 `**来源**` 行同步（F02:4、F04:4、F06:4） |
| model_inferred 列表未被改 | **fail** | `prd.md:140`（表头）、`:144-146`（M-01/M-02/M-03 三行）、`:148`（注记）在 `cc0c7c4` 中被修改，全部为确认日期 `2026-09-10`→`2026-09-11`；推导内容/依据/确认状态列未变 |
| 架构维度回填本身 | pass | 7 卡 `## 架构维度（待阶段 3 填写）`→已填、`prd.md:152-207` 新增「架构维度落定」段、`prd.md:98-100` 疑问 2/3 追加阶段 3 裁定标注——均属架构维度（允许） |

补充事实（超出第 6 条字面范围，但同一提交内，独立查出）：

- `cc0c7c4` 的 `--stat` 显示它还改了 **`demand.md`（29 insertions / 29 deletions）**，全部为确认日期 `2026-09-10`→`2026-09-11`（文档状态行、版本行、§1/§2 标题、F-4/G-1~G-6 行、§4.5 边界段、§4.6-B 表头与第三列、§5、W2/W3/W4/W7、用户确认记录表、六维自检段）。这与 `architecture.md:702`（§14.6）"`demand.md`、`roles/**`、`oamp/**` **未改动**"直接矛盾（见偏差 #4）。
- 日期口径无法从产出物核实：三个阶段提交同在 2026-09-11（`743d589` 11:27、`ff1cd1a` 11:35、`cc0c7c4` 11:51），而阶段 1/2 产物写"确认日 2026-09-10"、阶段 3 统一改为 2026-09-11（`git show 743d589:.../demand.md | grep -c '2026-09-10'` = 29，当前 = 0）。哪一天是用户实际确认日，产出物内无第三方记录可核（见偏差 #3）。
- 被验证产出物之外的 `roles/**`、`oamp/**` 在该提交中确实零改动（`--stat` 仅列 docs 文件）——§14.6 的后两半成立，前半（demand.md）不成立。

判定说明：以标准原文"验收标准/用户价值/边界/model_inferred 列表**未被改**"为判据，四类中两类被改（改动限于行内确认日期与日期列，语义内容未变），故为 **partial**（部分子项通过、部分不通过），而非 pass；又因改动均为日期、无功能语义变化，故不判 fail。

### 标准 7：架构组件可追溯到功能卡（无凭空组件）；ARCH 声称的零新第三方依赖可由 `oamp/package.json`（dependencies 为空对象）核实

**判定：pass**

证据（A. 依赖声明；B. 组件追溯；C. 既有代码锚点抽验）：

**A. 零新第三方依赖 —— 成立**

- `oamp/package.json` 实读：`"dependencies": {}`（空对象），无 `devDependencies` 字段；`engines.node >= 22`；`test = node --test test/*.test.js`——与 `architecture.md:7`（"零新第三方依赖（`oamp/package.json` 的 `dependencies` 保持 `{}`）"）与 `:35`（§1.1 "package.json … **不改**（零新依赖）"）一致。
- 说明：`tmux` 是本迭代新增的**系统级可执行依赖**（demand D-5/TC-04 已确认的运行形态），不是 npm 第三方依赖；架构已把它作为显式前置（`architecture.md:5.2` up 第一步 `tmux -V` 探活，§13 R-9），口径不冲突。

**B. 新增/改造实体 → 功能卡/AR 追溯（逐项）**

| 实体（`architecture.md:108-124` §2.2 模块布局） | 追溯到的 AR / 卡 | 判定 |
|---|---|---|
| `src/role-binding.js`（新） | AR-01（`prd.md:183`）→ F01 | 有据 |
| `src/cluster-config.js`（新） | AR-12 / AR-17（`prd.md:194/199`）→ F06 | 有据 |
| `src/cluster.js`（新） | AR-13（`prd.md:195`）→ F06-4 | 有据 |
| `cluster.json`（新，仓库根） | AR-12（`:194`）→ F06-1/7 | 有据 |
| `--role` / `--model` / `--tools` / `--permission`（新 flag） | AR-02 / AR-06 / AR-08 / AR-10 → F01/F03/F04/F05 | 有据 |
| `OAMP_ROLE_ROOT` | AR-01（`:183`） | 有据 |
| `OAMP_CLUSTER_CONFIG` / `--config` | AR-12（"路径/格式/落点"） | 有据 |
| `OAMP_TMUX_BIN` / `OAMP_CLUSTER_WAIT_MS` | AR-20（`prd.md:26`、`:207`） | 有据 |
| `status.js` 导出 `queryNodes` | AR-13（status 输出形态） | 有据 |
| 4 个新测试文件（`test/cluster-config` / `role-binding` / `cluster-actions` / `tool-permission`） | AR-20（`architecture.md:430-443` §5.6） | 有据 |
| `config.js` / `router` / `web` / `persist` / `package.json` 等"不改" | §2.3（`:125-136`）+ N10 | 有据（回归边界，非遗漏） |

- 奥卡姆检验可查：`architecture.md:583-600`（§10）对每个新实体给出"不引入它，什么无法实现"，并显式**删除**了 4 个候选实体（`--cwd` flag、`permission.timeout_ms` 键、日志目录+`.gitignore` 改动、Web/协议改造/常驻 LLM）。该表与 §2.2 一致，未发现"§10 已删除但 §2.2/§3-§5 仍在用"的残留。
- **唯一可疑项（已记入偏差 #2，不影响本判定的"组件"层面结论）**：`instance_id` 配置覆盖字段（`architecture.md:156`、`:340`、`:546`）无任何验收标准要求，且标注"本迭代不使用"——属未被要求的可配置性（YAGNI），但其本身是 AR-01/AR-17 所要求的 schema 结构内的一项，仍可定位到 AR，故不判为"凭空组件"。

**C. 架构对既有代码的锚点抽验（独立复核，未依赖产出物自述）**

| 架构声称 | 实读结果 | 判定 |
|---|---|---|
| `acp-client.js:79` 硬编码 `['acp','--no-skills','--no-rules','--no-tools','--no-session']`（`:28`/`:251`/`:277`） | `oamp/src/acp-client.js:79` 逐字一致 | 准确 |
| 服务端请求帧今天会因 `pending` 无此 id 被丢弃（`:271`，引用 `acp-client.js:262-266`） | `acp-client.js:262` `_handleMessage` 起、`:266` `const entry = this._pending.get(message.id);`、`:267` `if (!entry) return;`——机制成立；引用区间**少一行**（漏 267） | 准确（引用区间见偏差 #6） |
| `agent.js:23` `DEFAULT_OMP_TIMEOUT_MS = 300000`（`:253-283`、§4.4 判据） | `oamp/src/agent.js:23` 一致 | 准确 |
| `status.js:39` 已导出 `renderTable`（`:31`、`:386`） | `oamp/src/status.js:39` `export function renderTable(nodes) {` | 准确 |
| `log.js` 的 `event()` 永不节流、只节流 `heartbeat()`（`:32`、`:286`） | `oamp/src/log.js:36-38` `event()` 直写；`:42-48` `heartbeat()` 滑窗节流 | 准确 |
| `oamp/.gitignore` 已忽略 `.runtime/`（`:411-419` §5.4） | 实读 `oamp/.gitignore` = `.runtime/` + `data/` | 准确 |
| 10 个角色目录 + `_template`/`cdp-debug-skill` 两个非角色（`:150-157`、F01-1/2） | 实扫 `roles/*/`：恰好 10 个含同名 `<role>.md` 的目录（architect/demand/dev/planner/pr-planner/prd/progress-observer/retrospective/verifier/workflow-pb）+ `_template` + `cdp-debug-skill`，与 10 个 instance_id 清单逐字一致 | 准确 |
| 既有 16 个测试文件（`:34`） | `ls oamp/test/*.test.js \| wc -l` = 16 | 准确 |
| `MODEL_DEFAULT='deepseek/deepseek-v4-flash'`、`MODEL_RE` 形态（`:30`、§4.1 校验） | `oamp/src/config.js:16`、`oamp/src/agent.js:27` 一致 | 准确 |

### 标准 8：架构内部无冲突（如 §11 已确认取值与 §4/§5 定案文字一致、L1-4 路径基准与 §2.4 判定口径一致）

**判定：partial**

判定拆分：

| 子项 | 判定 | 证据 |
|---|---|---|
| §11 已确认取值 ↔ §4/§5 定案文字一致 | pass | L1-2 最终取值（`:619`）= §4.3 定案（`:251`）"角色 `tools` 缺省 true；常驻路径按 `tools` 决定是否传 `--no-tools`" = §4.4 定案（`:282`）"允许 → `allow_once`、拒绝 → `reject_once` + `session/cancel` + 轮次 `permission_denied`、`duration_ms ≤ 10s`"；L1-6 最终取值（`:623`）的 4 个 flag 与 §3.4（`:192-208`）逐一一致；L1-5（`:622`）= §3.5（`:210-218`）；L1-3（`:620`）= §5.2（`:365-387`） |
| L1-4 路径基准 ↔ §2.4 判定口径一致 | pass | L1-4（`:621`）"仓库根 `cluster.json`……F02-6 判定口径按 §2.4 执行" 与 §2.4（`:137-147`，末句"该取舍已随 L1-4 确认（2026-09-11）：配置保留在仓库根"）、§5.1（`:308-311`）、§14.5（`:701`）四处同口径；AR-18 的相对基准"配置文件所在目录 = root = 仓库根"（`prd/F07:31`、`architecture.md:5.1` 校验表）与之一致 |
| §3.4 的 `--tools` 缺省 ↔ §4.3 解析链 / F04-AR-08 一致 | **fail** | `architecture.md:202`（§3.4 参数表）"`--tools` … 缺省 **`off`（与 0011 一致）**" 与 `:248-249`（§4.3 解析链）"`payload.tools === true → on；…未给 → **角色开关**；无角色绑定 → off" + `prd/F04:32`（AR-08 同链）**对同一输入给出相反结论**：对一个"角色已绑定（含裸起 `oamp agent start pb-dev`，§3.4 `:200`/`:207` 明确会按 instance_id 推断绑定）但未传 `--tools`"的实例，§3.4 判 off、§4.3/AR-08 判 on（角色开关缺省 true）。该分歧直接落在 F04-1「默认全开」（`prd/F04:15`）与 F01-4「单起可用」（`prd/F01:18`）的判定上。加剧不确定的是 D-18（`:536`）"节点不读 `cluster.json`（脚本解析成 flag 传入）"——节点侧无从得知"角色开关"的配置值，§4.3 的"未给 → 角色开关"缺少取值来源说明 |
| 其他跨节抽查 | pass | ① 日志路径三处同口径（`:411-419` §5.4 / `prd/F06:38` AR-15 / `prd/F03:31` AR-07 = `<PKG_ROOT>/.runtime/cluster/`）；② up/down 契约三处同口径（`:365-387` §5.2 / `:420-429` §5.5 / `prd/F06:37` AR-16）；③ 审计口径四处同口径（`:286` §4.5 / `:625-634` §11.2 / `:684` R-3 / `:697` §14.1）；④ 窗口/cwd 三处同口径（`:388-409` §5.3 / `:192-208` §3.4 / `prd/F07:31-32` AR-18/19）；⑤ §7 决策表 D-01~D-20 的 L1 标签与 §11 一致（D-01~D-06 = L1-1~L1-6，`:517-522` ↔ `:618-623`），L2/L3 未被误标为 L1 |

补充说明（不属"架构内部冲突"，但同属一致性风险，已记偏差 #1）：F05 卡验收 2（`prd/F05:16`）字面要求"**每一次**工具调用留下一条放行记录——N 次工具调用对应 N 条记录"，而架构 §11.2（`:625-634`）把口径收窄为"每一次**受门禁**（`bash`/`edit`/`delete`/`move`）的工具调用恰好一条"。这是**产品维度与架构口径之间的差异**，架构侧已在 §4.5/§11.2/§13 R-3/§14.1 四处一致地声明并称经用户确认（`architecture.md:286`、`:631`），故不判为架构内部冲突；但卡片验收 2 的字面措辞未同步注记该口径，阶段 6 若只读卡会误判，故记入偏差并列为下一迭代候选。

---

## 汇总

| 结论 | 数量 | 条目 |
|---|---|---|
| **pass** | 6 | 标准 1、2、3、4、5、7 |
| **fail** | 0 | — |
| **partial** | 2 | 标准 6（来源行与 3 条验收标准行内确认日期、model_inferred 列表日期被改；判定内容未改）；标准 8（§3.4 `--tools` 缺省 `off` 与 §4.3 解析链/F04-AR-08 冲突；§11↔§4/§5、L1-4↔§2.4 均一致） |
| **blocked** | 0 | — |

---

## 偏差记录

> 实现/架构与规格或文档不一致的地方。不影响本次逐项判定的 pass/fail 归属，但应在下一迭代或阶段 4 开工前同步文档或评估调整。

| # | 规格/文档描述 | 实现/架构实际 | 建议处理 |
|---|---|---|---|
| 1 | F05 卡验收 2（`prd/F05:16`）："每一次工具调用都在事件日志中留下一条放行记录——N 次工具调用对应 N 条记录" | 架构口径（`architecture.md:286`/`:625-634`/`:684`/`:697`）：仅 `bash`/`edit`/`delete`/`move` 四类**受门禁**调用恰一条记录；只读工具（read/glob/grep/…）不产生请求因而无记录 | 在 F05 卡验收 2 行内注记判定口径（对齐 `architecture.md §11.2`），或在下一迭代按该口径改写验收 2 措辞（属产品维度变更，需用户决策）；阶段 6 断言务必用变更类指令 |
| 2 | F01 卡验收 1（`prd/F01:14`）："instance_id = `pb-` + 角色名……同一角色不存在第二个实例（id 无后缀）"——固定映射、命名可预期 | `architecture.md:156`/`:340`/`:546`：`cluster.json` 的 `instance_id` 字段可**显式覆盖**实例名（自述"本迭代不使用"），缺省仍为 `pb-<role>` | 删除该字段（YAGNI，无任何验收标准要求），或在 F01 边界/AR-01 明示"该字段本迭代不交付、不影响 F01-1 判定"；否则命名契约存在缺口 |
| 3 | 确认日期口径：需求/规格在阶段 1–2 记"2026-09-10 用户确认"（`git show 743d589:…/demand.md` 命中 29 处；阶段 2 的 prd 卡同） | 阶段 3 提交 `cc0c7c4` 把 demand.md（29 行）与 `prd.md`/`prd/F02`/`F04`/`F06` 的对应日期**统一改为 2026-09-11**（当前全仓 09-10 命中 0 处）；三个提交同在 2026-09-11，产出物内无用户确认记录可核哪天为真 | 归档用户确认记录（含日期），并明确"确认日"以 demand 为准的单一口径；若 09-10 为真实确认日则须回改。日期漂移本身不影响功能判定，但会污染后续迭代的追溯 |
| 4 | `architecture.md:702`（§14.6）："`demand.md`、`roles/**`、`oamp/**` **未改动**"（同句还称"验收标准、用户价值、边界、model_inferred 列表均未改动"） | 同一提交 `cc0c7c4` 实际改动 `demand.md` 29 行（全部为 09-10→09-11 日期），并改动了 `prd.md` 的 model_inferred 表（`:140`/`:144-146`/`:148`）与 3 条验收标准行的行内日期（`prd/F02:20`、`prd/F04:19`、`prd/F06:15`）；`roles/**`、`oamp/**` 确实零改动 | 修正 §14.6 表述（改为"未改动产品维度的判定内容；仅同步用户确认日期"），或回退 demand.md 的日期改动——自检声明与仓库事实必须一致，否则后续验证者会把该句当作事实依据 |
| 5 | `architecture.md:202`（§3.4 参数表）："`--tools` … 缺省 `off`（与 0011 一致）" | `architecture.md:248-249`（§4.3 解析链）与 `prd/F04:32`（AR-08）："未给 → 角色开关（缺省 true）→ on；无角色绑定 → off"；且 D-18（`:536`）说明节点不读 `cluster.json`，使"角色开关"在单起路径上的取值来源未定义 | 在 §3.4 明确两分支："无角色绑定 ⇒ off（0011 一致）；有角色绑定 ⇒ 角色开关（默认 true）"，并说明集群脚本恒显式传 `--tools`、单起由 agent 侧默认值兜底；随后同步 F04-1/F01-4 的判定口径。此项直接影响阶段 4 实现与阶段 6 断言，建议在开工前修文 |
| 6 | `architecture.md:271`（§4.4 实现面）引用"`acp-client.js:262-266`"作为"服务端请求帧被丢弃"的位置 | 实际丢弃点在 `oamp/src/acp-client.js:267`（`if (!entry) return;`），262-266 只覆盖方法头与 pending 查表，未覆盖 return 行 | 把引用修正为 `acp-client.js:262-267`（低严重度，机制结论不受影响） |

---

## 下一迭代候选

> 不在本次 8 条验收标准范围内，供主 agent / planner 决策是否纳入后续迭代。

1. **E5 字面 N=N 的可观测面**：要覆盖"每一次工具调用（含只读）"，需 omp 侧提供 `tool_call` 帧或客户端侧不存在的观测面（`architecture.md:684` R-3 已记为 NC-1、`:697` 疑问 1 已声明收窄）。当前口径下只读工具不可审计。
2. **`--config` 指向仓库外时的 root 口径未显式对齐**：`architecture.md:308-311`（§5.1 落点，缺省 = 包根上级目录）与 `:150-158`（§3.1 `roleRoot` 缺省 = `PKG_ROOT/..`、可被 `OAMP_ROLE_ROOT` 覆盖）与 AR-18（相对基准 = 配置文件所在目录）三者只在"配置文件位于仓库根"的默认路径下自动一致；显式 `--config` 指到别处时，角色文件解析根与 cwd 相对基准的取值需一句话锁死。
3. **阶段 6 用例的现场清理约定**：E2/E8 会在仓库根留下 `role-smoke.txt` / `cwd-check.txt`（`architecture.md:690` R-4 已记）。建议在阶段 6 的用例说明里写入"断言后清理"步骤，避免污染仓库与后续验证。
4. **角色文件体量成本的可观察项**：`workflow-pb` 角色 md 43KB ≈ 万级 token 的**每次会话启动**成本（`architecture.md:691` R-6）。建议阶段 6 记录一次真实首轮 TTFT / token 观测，为后续"是否按需裁剪注入"提供数据（本迭代不改机制，N1 只读消费）。

---

## 结论

**PASS**

- 8 条标准中 **6 条 pass、2 条 partial、0 条 fail、0 条 blocked**；按 verifier 报告契约（"所有条目 pass 或 partial 且无 fail"）结论为 PASS，阶段 2 / 阶段 3 产物可进入下一阶段（阶段 4 PR 规划）。
- 两条 partial 的返工面已精确定位，均为**文档一致性**问题而非功能/架构缺陷：标准 6 的改动限于确认日期（阶段 3 提交 `cc0c7c4` 改动 4 条验收标准行内日期 + model_inferred 表日期 + demand.md 29 行日期）；标准 8 的分歧是 `architecture.md:202`（§3.4）与 `:248-249`（§4.3）/`prd/F04:32`（AR-08）对"角色已绑定但未传 `--tools`"给出相反缺省。两者都已写入偏差记录 #3/#4/#5，可在阶段 4 开工前以改文 1 行方式闭环。
- 关键正面结论：AR-01~AR-20 全部落定且零 `[架构待填]` 残留；L1 六项已确认留痕并给出最终取值；零新第三方依赖可由 `oamp/package.json`（`dependencies: {}`）核实；架构对既有代码的 8 处锚点抽验全部准确（1 处引用区间偏一行，已记偏差 #6）；7 张卡与 demand W1~W8 双向可追溯、无 demand 之外新增功能点。

---

**报告路径**：`docs/iterations/0012-roles-agent-cluster/clarifications/verify-20260911-115347.md`
**回报**：结论 PASS ｜ fail 0 条 ｜ partial 2 条 ｜ 偏差记录 6 条
