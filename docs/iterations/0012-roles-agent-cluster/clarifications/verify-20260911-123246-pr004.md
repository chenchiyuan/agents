# PR-004 独立验收报告（agent 角色参数面 + 两条 LLM 路径注入 + auditContext 透传 + permission_denied 早退）

**验证者身份**：同级代码审查者（node/CLI 参数面 + ACP 子进程边界 + 进程级回归不变式方向）——能读这份代码、知道 argv 参数化与"匿名路径逐字节不变"这种约束的坑。
**产出物**：worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0012-pr-004-agent-role-binding`（分支 `feat/0012-pr-004-agent-role-binding`，HEAD=`2010b6e`；本 PR 的实现为**工作区未提交改动**：`oamp/src/agent.js`（+119/-17）、`oamp/src/context-pool.js`（+37/-13））
**验证标准来源**：主 agent 委托的 8 条 + `prs/pr-004-agent-role-binding.md`「验收标准」10 条 + `architecture.md` §3.4 / §4.1 / §4.3 / §4.5 / §12.2 契约 1
**验证日期**：2026-09-11

**验证方式**（探针全部在 `/tmp`，未向被验 worktree 写入任何文件——复核 `git status --porcelain` 与验证前一致，仅 agent.js/context-pool.js + 1 个未跟踪 doc）：
- `/tmp/p004-probe-args.sh`：无 Router 下的参数面/退出码/事件行（11 例）
- `/tmp/p004-fake-acp.cjs` + `/tmp/p004-pool-probe.mjs`：直接构造 `ContextPool` + fake ACP（daemon argv / permission / 审计字段 / 会话保留）
- `/tmp/p004-e2e-probe.mjs`：真实 Router + 真实 agent 子进程 + fake ACP（推断绑定 / 两条 LLM 路径 / 模型链）
- `/tmp/p004-smoke.mjs`：PR 卡第 10 条冒烟原样复跑 + payload>CLI 因果隔离
- 回归：`node --test test/{context-pool,acp-daemon,omp-executor,reconnect,web,role-binding,tool-permission}.test.js`

---

## 逐项判定

### A. PR 卡「验收标准」（10 条）

**A1 单起可用与推断绑定：pass**
- `OAMP_ROLE_ROOT=<worktree> node bin/oamp.js agent start pb-dev` → 首行 `AGENT_START instance=pb-dev role=dev model=deepseek/deepseek-v4-flash tools=on permission=allow role_file=<abs>/roles/dev/dev.md`，次行 `ROLE_BOUND role=dev file=<abs> source=instance_id`；同一进程经 fake ACP 完成一轮 daemon 任务（E4.4，`text=ok-1`）→ "可用"成立。
- `agent start dev-1` → 无 `ROLE_BOUND`，`AGENT_START … tools=off permission=allow`（无 role/role_file 键）；注册/心跳/退出回归由 `context-pool.test.js` 16/16 覆盖（测试 16 断言其 `omp acp` argv 仍含 `--no-tools` + `--model`）。
- 边界补充：`pb-ghost`（前缀命中但角色文件不存在）→ 不绑定、`tools=off`，与 `role-binding.js:37`（文件存在性才成立）一致。

**A2 flag 优先于推断：pass**
- `agent start pb-dev --role architect` → `ROLE_BOUND role=architect file=<abs>/roles/architect/architect.md source=flag`。实现锚点 `agent.js:536-537`。

**A3 非法取值快速失败：pass**（11 例全部可核，stderr 指名参数与非法值）
| 输入 | 退出码 | stderr |
|---|---|---|
| `--tools maybe` | 2 | `--tools 仅支持 on|off: "maybe"` |
| `--permission ask` | 2 | `--permission 仅支持 allow\|deny: "ask"` |
| `--role ''` | 2 | `--role 取值非法: ""` |
| `--model 'bad model'` | 2 | `--model 需为 1~128 位 [A-Za-z0-9._/-] 字符: "bad model"` |
| `--model ''` | 2 | 同上 |
| `--foo bar` | 2 | `未知参数: "--foo"` |
| `--role`（缺值） | 2 | `--role 缺少取值` |
- `MODEL_RE` 仍为 `agent.js:27` 的原正则（卡面锚点未漂移）。

**A4 常驻路径注入与工具开关：pass**
- pool 探针 A1/A2（`tools:true, roleFile`）：`acp --no-skills --no-rules --no-session --append-system-prompt <abs>/roles/dev/dev.md`，**不含** `--no-tools`。
- e2e E4.1/E4.2（裸起 pb-dev + daemon 任务）：`["acp","--no-skills","--no-rules","--no-session","--model","deepseek/deepseek-v4-flash","--append-system-prompt","<abs>/roles/dev/dev.md"]`。
- `--tools off`（e2e E5.4）：同 argv 追加 `--no-tools`，角色注入仍在。
- 锚点：`context-pool.js:196-197`（透传）→ `acp-client.js:108-112`（argv 拼装）。

**A5 一次性路径同源：pass**
- E2.1：`["-p","--no-session","--append-system-prompt","<abs>/roles/dev/dev.md","one-shot-injection"]`（state=completed）。
- 工具三分支在该路径的实测：E2.2（绑定 + 无 payload/CLI → on → 无 `--no-tools`）、E2.4（`payload.tools=false` → 有）、S2（`payload.tools=true` 压制 CLI `--tools off` → 无）、S3（payload 未给 → 回落 CLI off → 有）。锚点 `agent.js:166-169`。

**A6 模型解析链：pass**
- E4.3 常驻路径无覆盖 → `--model deepseek/deepseek-v4-flash`（config 默认）；E5.2 `--model role-model`（角色级）→ argv `--model role-model`；E6.1/E6.2 `OAMP_OMP_MODEL=env-model` 压制 `--model role-model` → `--model env-model`；E3.1/E6.3 `payload.model` 压制 env → `--model payload-model`。
- 「未传该 flag 的实例行为不变」：E2.3 一次性路径在 payload/env/CLI 全空时**不传** `--model`（与 0011 相同）；常驻路径仍传 config 默认（0011 同）。
- 链实现：`agent.js:276`（daemon）、`agent.js:167`（一次性）。**链的一处偏差见 D-2。**

**A7 permission_denied 为轮次级错误：pass**
- pool 探针 C 组：deny 轮 `err.code === 'permission_denied'`、message=「工具调用被 permission 策略拒绝（permission=deny）」；事件序列仅 `ACP_READY/CONTEXT_READY/TOOL_DENIED`，**无 `CONTEXT_RESET`**；`session.closed === false` 且仍在池中（同键同实例）；下一轮正常 `ok-2`，`pid`/`context_id` 与拒绝轮后一致（`ctx-63300-1`）。
- 代码锚点：`context-pool.js:235`（早退名单含 `permission_denied`）、`acp-client.js:155/182/186`（轮次级标记）。

**A8 常驻路径的审计身份字段齐全：partial**（子项一通过、子项二不通过）
- **通过**：角色绑定实例经 fake ACP 触发一次 permission 请求后，`logger.event` 收到的 `TOOL_APPROVED` 字段 = `{instance:'pb-dev', role:'dev', chat_id:'chat-A', context_id:'ctx-63298-1', pid, tool:'edit', title, tool_call_id:'tc-1', option:'allow_once'}`——**四键全非空**，`context_id` 与该会话 `session.contextId` 相等（9 键恒在）。锚点 `context-pool.js:192-206`（唯一 `new AcpClient` 点，见 A10/B8）。
- **不通过**：卡面「无角色实例 `role` 为 `null` 但**键仍在**（判定：…断言日志行）」——在真实渲染行中 `role=` **缺失**：
  `[2026-09-11T04:30:49.535Z] agent TOOL_APPROVED instance=dev-1 chat_id=chat-B context_id=ctx-63299-1 pid=63299 tool=edit title="Create file /tmp/x.txt" tool_call_id=tc-1 option=allow_once`
  同源现象：匿名实例 `AGENT_START` 行也只有 `instance/model/tools/permission`，无 `role`/`role_file` 键。
  根因：`acp-client.js:_audit` 确实传入 `role: … ?? null`（键在字段对象中），但 `log.js:22` 的 `if (value === undefined || value === null) continue;` 丢弃 null 值 → 渲染层删键。`log.js` 不在本 PR 两文件边界内且零修改，故不在工作区修复。

**A9 回归（5 文件原样全绿 + 文件零修改）：pass**
- `node --test test/context-pool.test.js` → **16/16**（含测试 16 的 argv 断言 `--no-skills/--no-rules/--no-tools/--no-session` + `--model`，`context-pool.test.js:533`）。
- `node --test test/acp-daemon.test.js test/omp-executor.test.js test/reconnect.test.js test/web.test.js` → **36/36**。
- 追加：`node --test test/role-binding.test.js test/tool-permission.test.js` → 15/15。
- `git diff --name-only HEAD -- oamp/test` **为空** → 既有 16 个测试文件（含 pr-001/pr-003 新增的 2 个）零修改。

**A10 冒烟（人工可复跑）：pass**
- 冒烟探针 S1 **原样复跑卡面命令**：临时 socket 起 Router、`OAMP_OMP_BIN=/tmp/p004-fake-acp.cjs`、`OAMP_ROLE_ROOT=<worktree>`、`node bin/oamp.js agent start pb-dev --role dev --tools on --permission allow` + 投一轮 `omp-daemon` 任务 → `FAKE_ACP_ARGS_LOG` 记录 `[...,"--append-system-prompt","<abs>/roles/dev/dev.md"]` 且**无** `--no-tools`，任务 `state=completed`。

### B. 委托方验证标准（2~8）

**B2 回归硬约束：pass** — 见 A9：`context-pool.test.js` 16/16；其 argv 断言仍含 `--no-tools`；既有测试文件零修改。

**B3 参数面：pass**
- 4 个 flag 齐备：`agent.js:469` `AGENT_FLAGS = {'--role','--model','--tools','--permission'}`；非法值/未知 flag 全部 exit 2 + stderr（A3 表）。
- 推断绑定与 flag 优先：A1/A2。
- `grep -rn "'pb-'" oamp/src` → **3 处命中，全部在 `role-binding.js`**（:3 注释、:12 `INSTANCE_PREFIX`、:29 注释）；`agent.js`/`context-pool.js` 无字面前缀拼接。

**B4 tools 三分支与 §3.4 定案一致：pass**
- ① payload 布尔优先：S2/S3/E2.4。② CLI 次之：E5.3/E5.4（role 绑定 + `--tools off` → `--no-tools`）、以及 `dev-1 --tools on` → `AGENT_START … tools=on`（CLI 覆盖无绑定内置缺省）。③ 内置缺省：绑定 → `on`（E1.2）、无绑定 → `off`（A1 第二例）。
- 锚点：`agent.js:544` `cliTools === null ? role !== null : cliTools`、`agent.js:166`。
- **agent 不读 cluster.json**：`grep -n "cluster" src/agent.js src/context-pool.js` → 无匹配。

**B5 两条 LLM 路径注入：pass** — 两条均以 fake ACP 复现：常驻 E4.1/S1、一次性 E2.1；注入值 = 角色 md **绝对路径**（`--append-system-prompt` 紧随其后）。

**B6 auditContext 透传 + permission_denied 早退：pass（附行号）**
- `context-pool.js:200-206`：`auditContext: { instance: this.agentId, role: this.pool.role, chat_id: this.chatId, get context_id(){ return session.contextId; } }`（惰性取值器，因 `ctx-<pid>-<generation>` 依赖 spawn 后的 pid）。
- `context-pool.js:235`：`if (code === 'model_unavailable' || code === 'context_busy' || code === 'permission_denied') return;`
- 数值证据：A6 探针（四键非空、`context_id` 与会话一致）。

**B7 边界：pass**
- `git diff --name-only HEAD` = `oamp/src/agent.js`、`oamp/src/context-pool.js`（仅此两文件；无新增测试文件）。
- 零新依赖：`oamp/package.json` 未在 diff 中，`dependencies === {}`。
- `prd.md` / `prd/` / `demand.md` / `architecture.md` 相对 HEAD **无改动**。
- 注：工作区另有一个未跟踪 doc 文件，见 D-4（非代码越界）。

**B8 反向核对（因果隔离）：已做，两条**
1. **auditContext**：`grep -rn "new AcpClient" oamp/src` → **唯一构造点 `context-pool.js:192`** ⇒ 常驻路径的审计身份只能来自池层透传；受控变量实测——同一 fake ACP、同一代码路径，仅把 `ContextPool({role})` 由 `'dev'` 改为缺省，审计行身份由 `role=dev`（A6）变为 `role=null`（B3）；
2. **tools 三分支**：同一 agent（`--tools off`）+ 同一 fake ACP，仅改 payload → argv 有无 `--no-tools` 随之翻转（S2 true→无 / S3 未给→有），CLI 与内置缺省另由 E5.3/E1.2 隔离。

---

## 汇总

- pass: **16 项**
- fail: **0 项**
- partial: **1 项**（A8：角色绑定侧四键非空通过；"匿名实例 role=null 但键仍在（日志行）"不通过）
- blocked: **0 项**

---

## 偏差记录

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | AR-11 / §4.5：「以上键**恒定存在**…未提供身份时其值为 `null`」（pr-004 卡第 8 条同义："`role` 为 null 但键仍在"） | `acp-client._audit` 的对象里键恒在（值 null），但真实事件行经 `log.js:22` 的 null 跳过 → 匿名实例的 `TOOL_APPROVED`/`TOOL_DENIED` **无 `role=` 键**；匿名 `AGENT_START` 亦无 `role`/`role_file` 键 | 三选一（主 agent 决策）：① 改 `log.js` 渲染（超出本 PR 两文件边界，且会触及 0010 既有格式）；② 修订 AR-11/§4.2 措辞为"字段对象内恒定存在，渲染时 null 省略"；③ 开新迭代统一口径 |
| D-2 | §2.3/§4.1 模型链：`payload > env > 角色 > config.defaults.model > 内置` | 一次性 `omp -p` 路径的链**缺 `config.defaults.model` 层**（`agent.js:167` 注释显式声明"config 默认与内置交由 omp 自身解析"）；与 0011 一致，但当仓库 config 配了非默认模型时，两条路径模型会不一致 | 若产品要求两路径一致 → 补该层；否则在 §4.1 标注"一次性路径以 omp 自身默认兜底" |
| D-3 | pr-004 卡「文件范围」：context-pool.js「向 `AcpClient` 透传 tools/roleFile/permission/**onPermissionRequest**/auditContext」 | 实际透传 tools/roleFile/permission/auditContext；**未透传 `onPermissionRequest`**（池构造参数无此项）。§4.4 的策略来源只有静态 `permission`，无验收标准要求动态钩子 | 明确取舍：若池层不需要动态策略，从卡面删除该词；若需要（例如按轮决策）则补 |
| D-4 | 卡「文件范围」= 仅 `oamp/src/agent.js` + `oamp/src/context-pool.js`；委托标准 7「无新增测试文件」 | 代码面严格满足；但 worktree 存在**未跟踪文件** `docs/iterations/0012-roles-agent-cluster/prs/pr-004-agent-role-binding-tasks.md`（主工作区同路径**不存在**此文件） | 合并前纳入提交或删除，避免悬空产物跨 worktree 分叉 |
| D-5 | 0011 行为：`agent start <id> <多余位置参数>` 被忽略（`git show HEAD:oamp/src/agent.js` 仅取 `restArgs[0]`） | 现在多余位置参数走「未知参数」分支 → stderr + **退出 2**（严格化，符合 §3.4"绝不静默忽略"精神，但非 §3.4 明文覆盖的形态） | 确认兼容性；若要保留旧宽容度则仅在 token 以 `-` 开头时报错，否则同步 §3.4 措辞 |
| D-6 | 交付物形态 | 本 PR 的实现是**工作区未提交改动**（HEAD 仍是 pr-003 合并后的 `2010b6e`），无本 PR 提交 | 合并/验收前由主 agent 决定提交策略（不阻塞本次判定） |

---

## 下一迭代候选

- **NC-1**：AR-11「键恒定存在」与渲染层 `log.js` null 省略的口径统一（同 D-1；也影响 pr-006 的端到端审计断言写法——若断言"9 键在行内"，匿名实例必挂）。
- **NC-2**：两条 LLM 路径的模型解析一致性（D-2）；建议在 pr-006 文档同步或补 `config.defaults.model` 层。
- **NC-3**：`pb-<role>` 推断失败时零信号——`OAMP_ROLE_ROOT` 配错或角色目录缺失时，实例静默退化为匿名节点（`tools=off`、无 `ROLE_BOUND`），只有 `AGENT_START` 行能看出。可考虑在前缀命中但绑定失败时落一条提示事件/stderr。
- **NC-4**：`--role` 取值校验仅挡 `/`、`\`、`.`、`..`，其余（空白/控制字符）靠角色文件存在性兜底（当前表现为 exit 2，风险低）。
- **NC-5**：一次性 `omp -p` 路径无 permission/audit 面（pr-003 O-3 已知），审计覆盖面仅常驻路径。

---

## 结论

**PASS**（0 fail / 1 partial / 0 blocked；偏差 6 条已记录，不阻塞本轮验收）

判定说明：唯一 partial 在 A8——本 PR 的**新增交付面**（角色绑定实例经池层透传 `auditContext`，四键值非空）实测通过；不通过的子项是"匿名实例渲染行仍含 `role=` 键"，其根因位于本 PR 边界之外的 `log.js:22`（null 跳过），且与 §2.3 的"匿名实例与 0011 逐字节一致"目标同源，故不作为 fail，交由主 agent 依 D-1 决策。

主 agent 可直接决策依据：**需要返工吗？** 无需返工即可继续（B7 边界干净、A9 回归全绿、A4/A5/A6/A7 两条路径与轮次语义均实测成立）；但若判定 A8 的匿名 `role=` 键为硬性验收项，则需按 D-1 另开最小改动（改 `log.js` 或改口径），本 PR 两文件内无法闭合。
