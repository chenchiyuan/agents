# 阶段 6 独立验证报告 — 0012-roles-agent-cluster（迭代最终产物整体验收）

**验证者身份**：交付前终审技术负责人（迭代整体验收）——复合身份，按验证面切换子身份取证：代码面 = 同级代码审查者（node 进程编排 / ACP 子进程边界 / CLI 参数面）；测试面 = 测试工程师（证据链、回归不变式、测试隔离性）；流程面 = 流程审计者（git 事实 × 文档声明 × 工作流规范三向对账）。
反射理由：本委托的产出物横跨「代码实现」「测试证据」「流程留痕（status/history/PR 卡）」「架构契约」四类，任一单一专家只覆盖一面；终审身份可以同时回答「实现对不对」「测试真不真」「流程留痕是否被真实更新」三个不同层级的问题，且不会把流程自述当作结论。

**产出物**：迭代分支 `iteration/0012-roles-agent-cluster`（HEAD = `ba94e9d`）全量产物
- 流程/文档：`docs/iterations/0012-roles-agent-cluster/`（demand.md、prd.md + prd/F01~F07、architecture.md、prs/ 12 个文件（6 PR + 6 tasks）、status.md、history.md、progress.md、clarifications/verify-*.md 共 8 份）
- 代码：`oamp/src/{role-binding,cluster-config,cluster}.js`（新增）、`oamp/src/{acp-client,agent,context-pool,cli,status}.js`（改造）、`cluster.json`（仓库根）、`oamp/test/{role-binding,cluster-config,tool-permission,cluster-actions}.test.js`（新增）、`oamp/test/acp-daemon.test.js`（增断言）、`oamp/README.md`（集群一节）

**验证标准来源**：主 agent 委托的标准 A~G（workflow-pb 阶段 6 + 「验证目标 · 并发调度真实执行证据」强制三项）
**验证日期**：2026-09-11
**独立性声明**：未接收任何执行过程上下文（未使用"设计意图/背景解释"类信息）；全部结论来自产出物、git 一手事实与自行执行的命令。全部探针为只读或写入 `/tmp`；唯一持久化写入 = 本报告（另有一次为取证而做的运行时目录移动与并发探针，见「验证方式 3/4」，均已恢复干净态）。
**验证方式**：① 逐文件读产出物（PR 卡 6 份、架构 §3~§5/§11~§14、status/history 及 git 中其全部历史版本、8 份 verify 报告）；② git 事实取证（`git log --graph` / `git show --stat` / merge 父节点 / `git diff main...HEAD --name-status` / `git ls-files` / `git worktree list` / `git branch -a` / `git check-ignore`）；③ 标准 F 串行实测（`npm test` 共 4 次：2 次干净态串行、2 次污染态串行）；④ 测试隔离性对照实验（两进程并发跑同一测试文件 1 次）。
**验证时点与并发写入声明（重要）**：本报告的判定以**取证期间读到的产出物内容**为准；取证过程中观测到**其他写入方同时在改产出物**——
- `docs/iterations/0012-roles-agent-cluster/architecture.md` 在取证期间被并发修改（未提交的工作区改动，mtime 2026-09-11 13:03:48；`git diff --stat` = +27/-12），新增 **V-9 / V-10 阶段 6 真实 omp 实测事实**并据此重写 §4.4（详见偏差 D-9）。
- 仓库根同期出现新的未跟踪残留 `acp-probe.txt`（13:01:24）——与本报告标准 F 的并发干扰观测互证。
- 因此：标准 E 的判定对"提交版契约（HEAD 内容）"与"工作区新契约（未提交）"作**拆分**；其余标准的判定对象均为**已提交的迭代产物**（HEAD = `ba94e9d`）。

---

## 逐项判定

### 标准 A（阶段 5 推进条件 · 逐 PR 核实）：**partial**

拆为三个子项，逐项取证：

**A-1 PR 文件齐备：pass**
- `prs/` 下 12 个文件齐备：`pr-001~006-*.md` 与对应的 `pr-*-tasks.md` 各 6 份。PR 卡七字段（上下文摘要 / 涉及功能点 / 文件范围 / 验收标准 / 参考资料 / depends_on / batch）逐份核对，6 份全部具备（逐份读取确认，无缺字段）。
- 6 份 PR 卡均被 git 跟踪并落在迭代提交中（`git show --stat` 命中：846b7d4/f82273e/e6da668/ad00c91/564f49c/76a10ac 与归档 commit 788e428/bd92048 等）。

**A-2 六个 PR 的验收标准全部通过（交叉引用已有 verify 报告）：partial**

| PR | verify 报告 | 结论 | 卡验收 | 备注 |
|---|---|---|---|---|
| pr-001 | `verify-20260911-120726-pr001.md` | PASS | 7/7 pass | 3 偏差已记录 |
| pr-002 | `verify-20260911-122919-pr002.md` | PASS | 全 pass | 5 偏差；测试 182/182 |
| pr-003 | `verify-20260911-121100-pr003.md` | PASS | 8/8 pass | 初次 1 partial（5c）→ 修复 → 复审 pass |
| pr-004 | `verify-20260911-123246-pr004.md` | PASS | 16 pass / **1 partial** | 唯一 partial = 卡面 A8 的"匿名实例渲染行"子项 |
| pr-005 | `verify-20260911-125316-pr005.md` | PASS | 9/9 pass | 4 偏差 |
| pr-006 | `verify-20260911-125511-pr006.md` | PASS | 7/7 pass | 4 偏差 |

- 5 个 PR 的卡验收标准 100% 通过；**pr-004 不是全通过**——A8 有一个子项判 partial（`verify-20260911-123246-pr004.md:60`：匿名实例的真实渲染行中 `role=` 缺失）。该子项的根因在本 PR 边界之外的 `oamp/src/log.js:22`（`null` 值被 `continue` 跳过），报告结论明确"无需返工即可继续"，且 architecture §4.5/§11.2 已按实现更新为"事件对象层 9 键恒定 / 渲染层吞 null"两层口径（见 `verify-20260911-125511-pr006.md` 偏差 1）。
- 我独立复核该根因成立：`oamp/src/log.js:22` 确为 `if (value === undefined || value === null) continue;`，`oamp/src/acp-client.js:355-369` 的 `_audit()` 确以字面量声明 9 键、身份四键取 `?? null`。即 partial 是"卡面判定面措辞与渲染层行为的摩擦"，不是功能缺陷（见偏差 D-7）。
- 判定：因 pr-004 存在 1 个 partial 子项，本子项不满足"全部通过"的字面要求 → **partial**（无返工需求，但不得记为 pass）。

**A-3 无简报外改动：partial**
- PR 实现面（逐 commit 文件集合 vs 各 PR 卡「文件范围」）**干净**：
  - 846b7d4 = `src/role-binding.js` + `test/role-binding.test.js`（⊆ pr-001 声明）
  - f82273e + 0e3f954 = `src/acp-client.js` + `test/tool-permission.test.js`（⊆ pr-003 声明）
  - e6da668 = `src/cluster-config.js` + `test/cluster-config.test.js` + `cluster.json` + `prs/pr-002-*-tasks.md`（代码面 ⊆ pr-002 声明；tasks 文档属规划归档，见偏差 D-8 类惯例问题）
  - ad00c91 = `src/agent.js` + `src/context-pool.js`（⊆ pr-004 声明）
  - 564f49c = `src/cluster.js` + `src/cli.js` + `src/status.js` + `test/cluster-actions.test.js`（⊆ pr-005 声明）
  - 76a10ac = `test/acp-daemon.test.js` + `oamp/README.md`（⊆ pr-006 声明）
  - 无任何文件出现在两个 PR 的提交中（文件范围互斥，独立复核成立）。
- **但迭代分支存在简报外改动**：`git diff main...HEAD --name-status` 含两个不属任何 PR 文件范围、也不在 `docs/iterations/0012-*` 下的已跟踪文件：
  - `docs/ds/05-实例配置与初始化集成.md`（`deepseek-chat` → `DeepSeek-V4.1-Flash`）
  - `docs/multi-omp-agent-protocol.md`（2 处 `deepseek-chat` → `DeepSeek-V4.1-Flash`）
  二者由 orchestrator 侧提交 `2010b6e`（`docs(0012): pr-003 验收+复审 PASS 并合并…`）一并扫入；`progress.md:94-97` 在阶段 3 已把它们登记为"不属任何 0012 产物文件"的工作区改动，但 `progress.md` 的记录状态与最终被提交的事实未同步 → 见偏差 D-1。
- 另有工作区未跟踪残留（`undefined/fake-tmux.cjs`、`role-smoke.txt`、`acp-probe.txt`）→ 偏差 D-2。
- 判定：**partial**（PR 实现面干净；迭代分支整体存在 2 个简报外已跟踪文件变更）。

### 标准 B-1（worktree 时间窗口重叠）：**pass**

| 批次 | 并发对 | history.md 派发条目 | 两 PR 分支提交时刻（git） | 合并时刻（git） |
|---|---|---|---|---|
| 1 | pr-001 ∥ pr-003 | 2026-09-11 11:59:45 ×2 条 | 846b7d4 **12:01:41** / f82273e **12:05:39** | 3d68353 12:08:13 / dba2b9c 12:17:59 |
| 2 | pr-002 ∥ pr-004 | 12:18:00 ×2 条 | e6da668 **12:22:47** / ad00c91 **12:34:09** | 6da6200 12:33:57 / e78ba20+11d9282 12:33:57~12:34:09 |
| 3 | pr-005 ∥ pr-006 | 12:49:49（批次汇总条目） | 76a10ac **12:39:24** / 564f49c **12:49:10** | f00bec0 12:56:12 / ba94e9d 12:56:12 |

重叠窗口的取证：
1. **批 1**：pr-003 的分支提交 `f82273e` 存在于 **12:05:39**，而 pr-001 直到 **12:08:13** 才被合并（`3d68353` = merge(bd92048, 846b7d4)）⇒ 12:05:39–12:08:13 期间两条分支并存。旁证：`status.md@bd92048`（12:05:39）同时把 pr-001 与 pr-003 标为「⏸ 验收中 / 占用」，并分别列出 `feat/0012-pr-001-role-binding`、`feat/0012-pr-003-acp-tool-permission` 两条 worktree 分支。
2. **批 2**：`e6da668`（12:22:47）与 `ad00c91`（12:34:09）相隔 11 分 22 秒，同处"派发 12:18:00 → 合并 12:33:57/12:34:09"的未合并窗口。旁证：`status.md@a1396da`（12:18:00）同时标 pr-002/pr-004「占用」并列出两条 `feat/0012-*` 分支名。
3. **批 3**：`76a10ac`（pr-006，12:39:24）与 `564f49c`（pr-005，12:49:10）相隔 9 分 46 秒，均在 12:56:12 的两次合并之前。旁证：`status.md@7952e51`（12:49:49）同时标 pr-005/pr-006「验收中 / 占用」+ 两条 `feat/0012-*` 分支名。此批的窗口重叠最硬：pr-006 的分支提交物在 pr-005 尚未提交时（相隔 9m46s）就已存在。
4. **分支独立性结构证据**：6 个 merge commit 的第二父节点恰为 6 个 PR 的分支头——`3d68353←846b7d4`、`dba2b9c←0e3f954`、`6da6200←e6da668`、`e78ba20/11d9282←ad00c91`、`f00bec0←564f49c`、`ba94e9d←76a10ac` ⇒ 6 个 PR 确在各自分支产出后合并（非线性单分支顺序提交）。
5. **可复核边界（诚实声明）**：分支与 worktree 目录已被清理（`git worktree list` 仅主工作区 + 无关的 `agents-0004-model-dispatch`；`git branch -a` 无任何 `feat/0012-*`；`.pb-agents/worktrees/` 仅剩 0004 一条），故"同时在磁盘存在"只能由上述时序 + 当时的 status 快照重建，不能再从磁盘直接观察。结论仍成立（≥2 个 PR 分支在同一时间窗口内各自持有独立提交），但记录方式偏弱 → 记入下一迭代候选 NC-6。

### 标准 B-2（并发配置区块真实初始化与更新）：**pass**

`status.md` 的 `## 并发配置（阶段 5）` 五字段在 git 中可追溯的 4 个快照（逐 commit `git show <sha>:…/status.md` 抽取）：

| 快照 commit（时刻） | 起始并发数 | 硬上限 | 当前有效上限 | 累计槛位释放次数 | 已派发总数 |
|---|---|---|---|---|---|
| `edfd710`（11:59:45，初始化） | 3 | 5 | **3** | **0** | 2 |
| `bd92048`（12:05:39） | 3 | 5 | 3 | 0 | 2 |
| `1007ea7`（12:08:13，pr-001 合并） | 3 | 5 | 3（滞后，见偏差 D-3） | **1** | 2（+1 待派：pr-002） |
| `a1396da`（12:18:00，pr-003 合并） | 3 | 5 | **5** | **2** | **4** |
| `7952e51`（12:49:49，第 2 批合并） | 3 | 5 | 5 | **4** | **6** |

- 五字段在阶段 5 初始化时全部落值（非空占位）；随后每一批合并事件都伴随真实更新：累计 0→1→2→4、已派发 2→4→6、当前有效上限 3→5。
- 判定 **pass**（初始化 + 至少 3 次后续更新均可定位到具体 commit；首跳的更新时点问题记偏差 D-3）。

### 标准 B-3（爬升公式真实触发）：**pass**

- 声明公式（本次委托）：`当前有效上限 = min(起始并发数 + 累计槛位释放次数 × 起始并发数, 硬上限)`；代入 = `min(3 + N×3, 5)`。
- 实测值：N=0 → 3（一致）；N=2 → min(9,5)=**5**（status.md 实测 5，一致）；N=4 → min(15,5)=**5**（实测 5，一致，封顶）。
- 即：该字段**不是维持初始值**——从 3 被重算为 5 并在后续保持封顶 5，`history.md` 12:08:13 / 12:18:00 两条调度决策条目亦分别记录"min(3 + 1×3, 5) = 5"与"min(3 + 2×3, 5) = 5（达硬上限）"的推导。
- 唯一不到位处：N=1 时 status.md 仍为 3（与公式的 5 不符），5 的取值直到 N=2 的快照才落盘，而同刻的 history.md 已按公式写出 5 → 记偏差 D-3。判 **pass**（"按公式重算、非维持初始值"这一核心命题有正向证据），但该跳的落盘时序不满足严格一致性。

### 标准 C（依赖正确性）：**pass**

| PR | `depends_on` 声明 | 依赖方合并事实（git） | 本 PR 合并时刻 | 判定 |
|---|---|---|---|---|
| pr-001 | （无） | — | 3d68353 12:08:13 | pass |
| pr-002 | pr-001 | 3d68353 12:08:13 | 6da6200 12:33:57 | pass |
| pr-003 | （无） | — | dba2b9c 12:17:59 | pass |
| pr-004 | pr-001, pr-003 | 12:08:13 / 12:17:59（先于 ad00c91 12:34:09） | e78ba20+11d9282 12:33:57~12:34:09 | pass |
| pr-005 | pr-002 | 6da6200 12:33:57 | f00bec0 12:56:12 | pass |
| pr-006 | pr-003, pr-004 | 12:17:59 / 12:34:09 | ba94e9d 12:56:12 | pass |

- 6 条依赖边逐条对照 `git log` 合并事实，**零依赖倒挂**（无任何 PR 在其依赖合并之前合入）。
- pr-004 经 `e78ba20`（合并 pr-002 代码）与 `11d9282`（合并 pr-004 代码）两次提交完成合入，其第二个 commit 时刻 12:34:09 与 ad00c91 的提交时刻同秒；pr-004 相对 pr-001/pr-003 的依赖满足关系不受影响。
- 依赖真实性（是否"真依赖"）已由阶段 4 独立验证（`verify-20260911-115745.md` 标准 2 pass，6 条边均有代码级耦合证据）；本轮只核"声明 × 合并事实"的一致性，结论 pass。

### 标准 D（PR 粒度：逻辑原子性 / 可审查性 / 独立性）：**pass**

| PR | 内容 | 代码行（新增文件/改动） | 依赖 | 独立判定锚点 | 复核 |
|---|---|---|---|---|---|
| pr-001 | 角色映射叶子模块 | 139 行（2 文件，全新增） | 无 | 单测 8/8 | 逻辑单一（一个公式 + 路径解析），可独立合入 ✔ |
| pr-002 | 配置加载与校验 + cluster.json | 612 行（3 文件 + tasks） | pr-001 | 单测 15/15 | 一个职责（配置真源），与 pr-001 文件互斥 ✔ |
| pr-003 | ACP 协议层（argv + permission + 审计） | 430 行（2 文件） | 无 | 单测 8/8（fake ACP 帧级） | 协议层闭环，可独立验证 ✔ |
| pr-004 | agent/pool 接线（flag + 注入 + 透传） | 139 行（2 文件改动） | pr-001, pr-003 | 参数面探针 11 例 + fake ACP | 接线层，两文件，审查面清晰 ✔ |
| pr-005 | 集群入口三动作 | 1345 行（4 文件 + tasks） | pr-002 | fake tmux 13 用例 | 最大单元，但为单一子命令闭环；文件与 pr-004 不相交 ✔ |
| pr-006 | e2e 断言 + README | 464 行（2 文件 + tasks） | pr-003, pr-004 | argsLog 断言 | 只动 test/doc，无 src 改动 ✔ |

- 六文件范围两两互斥（按 commit 文件集合独立核对，无重叠）；每个 PR 都有"在本 PR 范围内可独立判定"的验收锚点；无"回滚会牵连无关功能"的单元。
- 与阶段 4 的结论（`verify-20260911-115745.md` 标准 7 pass，判定框架一致）独立复现一致。判 **pass**。

### 标准 E（最终产物一致性：代码形态 vs architecture.md 契约，抽查 3 处）：**partial**

抽查点 1 —— **tools 三分支**（契约 §3.4 / §4.3，L1-2 定案）：
- ① payload 布尔优先：`oamp/src/agent.js:70-71`（`typeof body.tools === 'boolean' ? body.tools : null`）+ `:167`（`if (!(task.tools === null ? ctx.tools : task.tools)) args.push('--no-tools')`）
- ② CLI 次之：`oamp/src/agent.js:544`（`const effectiveTools = cliTools === null ? role !== null : cliTools`）
- ③ 内置缺省：同上表达式——有角色绑定 ⇒ on、无绑定 ⇒ off
- 生效点：`oamp/src/acp-client.js:103-107`（`if (!this.tools) args.push('--no-tools')`；`--no-skills`/`--no-rules`/`--no-session` 恒在；`roleFile` 非空 ⇒ `--append-system-prompt <abs>`）
- 与契约一致；`grep -rn "'pb-'" oamp/src` 仅命中 `role-binding.js`（公式单点，`agent.js`/`context-pool.js` 无字面前缀拼接）。**一致**。

抽查点 2 —— **permission 两档 + 审计两层口径**（契约 §4.4 / §4.5 / §11.2）：
- 两档：`oamp/src/acp-client.js:323-338`——`session/request_permission` → `allow_once` / `reject_once`；allow 记 `TOOL_APPROVED`、deny 记 `TOOL_DENIED`；未知方法回 `-32601`；deny 路径经 `:182/:186` 的 `permission_denied`（轮次级），`oamp/src/context-pool.js:235` 早退名单含该码（会话保留）
- 审计两层：`oamp/src/acp-client.js:355-369` 的 `_audit()` 以字面量声明 9 键、身份四键 `?? null`（事件对象层键恒定）；`oamp/src/log.js:22` 跳过 null（渲染层吞键）——与 §4.5/§11.2 明文的两层口径逐字对应。
- README（`oamp/README.md` 新增 85 行）含 permission 两档表与 R-3/R-4 边界声明。**一致**。

抽查点 3 —— **cluster 三动作**（契约 §5.2）：
- `oamp/src/cluster.js:29`（`ACTIONS = ['up','down','status']`）、`:336 up` / `:434 down` / `:501 statusAction`、`:584-592` 分发；`oamp/src/cli.js:15`（USAGE 一行）+ `:106-113`（分发，非法子命令 → 退出 2）；`oamp/src/status.js:84` 导出 `queryNodes` 且 `cluster.js:15` 消费之（跨组契约 4）。
- **一致**（三动作齐全、退出码语义与 §5.2 逐条对应，已由 pr-005 验收报告 9/9 与我的代码复核共同确认）。

附加取证 —— **`cluster.json` tracked 且零凭据**（F06-7 / TC-07）：
- `git ls-files cluster.json` → 命中（tracked）；
- 递归键名扫描（大小写不敏感：`token`/`secret`/`password`/`passwd`/`api_key`/`apikey`/`credential`）→ 零命中；顶层键 `session`/`web`/`router`/`roles`；`roles` 键集合 = 10 个角色（architect, demand, dev, planner, pr-planner, prd, progress-observer, retrospective, verifier, workflow-pb），不含 `_template`/`cdp-debug-skill`。**一致**。

**E 判定拆分（3 处抽查）**：
- **tools 三分支：pass**——三条分支（payload > CLI > 内置缺省）与代码逐点命中，且该契约在取证期间未被改动。
- **cluster 三动作：pass**——三动作、退出码语义、`queryNodes` 复用均与契约一致，且该契约在取证期间未被改动。
- **permission 两档与审计两层口径：partial**——对**提交版契约**（HEAD 的 architecture.md，blob `d6825e1`）**一致**（§4.4 的 ACP 应答两档 + §4.5/§11.2 的"事件对象 9 键恒定 / 渲染层吞 null"两层口径与 `acp-client.js:323-369` + `log.js:22` 逐字对应）；对**工作区在写的修订版契约**（未提交，V-9/V-10）**不一致**——修订版声明"真实 omp 的 ACP 路径从不发 `session/request_permission`，pr-003 的 permission 应答成为死代码"，两档改由 `--approval-mode yolo|always-ask` 的 argv 映射实现、审计改挂 `tool_call`/`tool_call_update` 通知，而该实现尚不存在（`prs/` 下无 pr-007 卡）。即：代码与旧契约一致、与在写的新契约不一致，且新契约的实现尚未产出 → 本抽查点不能记 pass。

### 标准 F（串行 `npm test` 全绿，应 197/197）：**pass**（附污染态复现证据）

- 干净态串行（`.runtime/` 被移除后）：`oamp` 下 `npm test` → `# tests 197 / # pass 197 / # fail 0`，退出码 0；紧接着第二次串行 → 再次 **197/197**（`/tmp/oamp-test-pristine.log`、`/tmp/oamp-test-rerun.log`）。
- 污染/并发态对照（3 次观测）：
  1. 早期两次串行运行均为 **196/197**，唯一红灯为 `up：session/窗口名与数量、每窗 -c、命令串、日志 truncate 与 git check-ignore（F06 验收 1/2/6）`，断言 `未启用角色无日志`（`cluster-actions.test.js:219`），原因是磁盘上已存在他方运行遗留的 `oamp/.runtime/cluster/pb-verifier.log`；
  2. 受控实验：两进程并发执行同一测试文件（`node --test test/cluster-actions.test.js` ×2）→ 1 个进程 13/13 绿、另 1 个进程 1 红，红灯仍落在同一用例（本次报 `ENOENT: …/.runtime/cluster/stale-sentinel.log`）。
- 结论：**串行 + 干净 `.runtime/` 下全绿 197/197 成立**（判 pass）；失败面全部指向同一原因——`cluster-actions.test.js` 使用包内固定日志目录 `oamp/.runtime/cluster/`（`:20`），多进程并发共享该目录自相干扰（用例无隔离）。该问题已由 pr-005 验收报告 D-3/下一迭代候选 NC-2 记录，本轮独立复现 → 偏差 D-6。
- 时间戳旁证（支持"早期红灯来自并发"）：观测到 `role-smoke.txt`（12:57:24）与 `acp-probe.txt`（13:01:24）在仓库根被创建，说明同期有其他进程在跑真实/半真实 smoke 探针，与早期两次串行红灯的时间窗重叠。

### 标准 G（搭置文件）：**pass**

- `test -e docs/iterations/0012-roles-agent-cluster/deferred-demand-changes.md` → **不存在**；`docs/iterations/0012-roles-agent-cluster/` 目录逐项列目录亦无该文件。
- 如实声明：**本迭代无搭置记录（deferred-demand-changes.md 从未创建）**，未做任何推测性描述。

---

## 汇总

- **pass: 7 项**（B-1 并发时间窗口重叠、B-2 并发配置真实更新、B-3 爬升公式真实触发、C 依赖正确性、D PR 粒度、F 串行测试全绿、G 无搭置记录）
- **partial: 2 项**（A 阶段 5 推进条件：A-1 pass；A-2 因 pr-004 卡 A8 有 1 个 partial 子项 → 非全通过；A-3 因迭代分支含 2 个简报外已跟踪文件变更 → 非全净。E 最终产物一致性：tools 三分支 pass、cluster 三动作 pass；permission/审计抽查点对提交版契约一致、对在写的修订版契约不一致且实现未落地 → partial）
- **fail: 0 项**
- **blocked: 0 项**
- 偏差记录：**9 条**（D-1~D-9）

## 并发调度真实执行证据（工作流强制三项）逐项结论

1. **worktree 时间窗口重叠：成立（pass）**。三批并发派发均有 ≥2 个 PR 分支在同一未合并窗口内各自持有独立提交：批 1 `f82273e`(12:05:39) 早于 `846b7d4` 的合并(12:08:13)；批 2 `e6da668`(12:22:47) 与 `ad00c91`(12:34:09) 同窗口；批 3 `76a10ac`(12:39:24) 早于 `564f49c`(12:49:10) 9m46s，二者于 12:56:12 分别合并。结构证据 = 6 个 merge commit 的第二父节点恰为 6 个独立分支头。
2. **并发配置区块真实初始化与更新：成立（pass）**。`status.md@edfd710` 初始化五字段（3/5/3/0/2），此后 `@1007ea7`（累计 1）、`@a1396da`（上限 5、累计 2、已派发 4）、`@7952e51`（累计 4、已派发 6）四次快照均有真实变化；缺陷仅为首跳时序（偏差 D-3）。
3. **爬升公式真实触发：成立（pass）**。`当前有效上限` 由 3 被重算为 5（N=2：min(3+6,5)=5）并保持封顶 5（N=4：min(3+12,5)=5），**不是维持初始值**；history.md 两条调度条目留有公式推导文本。

## 偏差记录

> 实现/流程留痕与规格/文档不一致之处。不影响本次验收判定，供主 agent 决策。

| # | 规格/文档描述 | 实际观测 | 建议处理 |
|---|---|---|---|
| D-1 | 迭代变更面 = 6 个 PR 声明的文件 + 迭代目录产物；`progress.md:94-97` 将 `docs/ds/05-实例配置与初始化集成.md`、`docs/multi-omp-agent-protocol.md`、`principles/execution/model-dispatch-protocol.md` 登记为"不属任何 0012 产物文件"的工作区改动 | `docs/ds/05-*.md` 与 `docs/multi-omp-agent-protocol.md` 已被提交进迭代分支（commit `2010b6e`，模型名 `deepseek-chat` → `DeepSeek-V4.1-Flash`）；`principles/execution/model-dispatch-protocol.md` 至今仍是**未提交**的工作区改动。即：同一批在飞改动被"部分扫入、部分留在工作区" | ① 若这批模型名统一属有意的文档口径更新 → 补一次独立提交（含仍留在工作区的 `principles/…`），并在 status/history 留痕；② 若不属本迭代 → 从迭代分支 `git revert` 这两个文件；③ 流程侧：收口提交禁止 `git add -A` 式扫描，提交前以 `git status` 对账 |
| D-2 | 阶段 6 产物应可自清理；architecture R-4 已预告 E2/E8 用例会在仓库根留 `role-smoke.txt` / `cwd-check.txt` | 仓库根存在 3 个未跟踪残留：`undefined/fake-tmux.cjs`（12:51:37，含 `#!/usr/bin/env node` 的假 tmux 脚本，路径基准显然写空了）、`role-smoke.txt`（12:57:24，内容 `ok`）、`acp-probe.txt`（13:01:24，内容 `probe-ok`）。三者均非迭代交付物，也未出现在任何提交中 | 删除三者；`undefined/` 的成因（某次手工探针把路径变量拼成 `undefined/`）应在探针脚本里加"路径非空"断言；后续 smoke 用例统一写到 `os.tmpdir()` |
| D-3 | 爬升公式 `min(起始 + 释放次数 × 起始, 硬上限)`；history.md 12:08:13 条目明写"当前有效上限 = min(3 + 1×3, 5) = 5" | 同一时刻的 `status.md@1007ea7` 仍写「当前有效上限: 3」「累计槛位释放次数: 1」→ 文件与同刻 history 自述不一致；数值 5 直到 `@a1396da`（累计 2）才落盘。即该字段在首跳滞后一拍 | 修 status.md 的更新时序（合并事件后即刻重算并落盘），或在 history/status 中显式声明"上限在下一批派发时重算"，消除同一时点两个口径 |
| D-4 | `history.md` 的「派发」条目语义 = 派发发生时刻 | 第 3 批两条派发条目时间戳均为 `12:49:49`，但 pr-006 的分支提交 `76a10ac` 为 `12:39:24`（早于条目 10 分钟）⇒ 该条目实为"批次汇总写入时刻"，非派发时刻；第 1/2 批条目时刻与派发语义一致，第 3 批不同 | 统一条目语义：或按实际派发时刻逐条落笔，或在条目中标注"（批次汇总写入）"；避免下游按时间戳重建并发窗口时被误导 |
| D-5 | 三批 PR 的验收报告应一致归档 | 第 1/2 批 4 份验收报告已提交进迭代分支；第 3 批 `clarifications/verify-20260911-125316-pr005.md`、`verify-20260911-125511-pr006.md` 仍是**未跟踪**文件（未入 git） | 补一次归档提交（与既有 `bd92048`/`7952e51` 的归档惯例一致）；本报告本身也应在提交之列 |
| D-6 | architecture §5.6 / AR-20 表述"集成测试用 fake 注入，自动化一律 fake"，未声明并发/隔离约束 | `test/cluster-actions.test.js:20` 使用包内固定目录 `oamp/.runtime/cluster/`，用例间/进程间共享；实测并发两进程跑同一文件 → 1 进程红（`未启用角色无日志` / `ENOENT stale-sentinel.log`），且污染残留会使下一次串行运行同样转红 | 二选一：① 引入 `OAMP_CLUSTER_LOG_DIR`（或测试内把 `PKG_ROOT` 指向临时根）恢复可并发；② 在 §5.6 与测试文件头显式声明"本文件必须串行执行"。现状仅在"串行 + 干净 `.runtime/`"下全绿 |
| D-7 | pr-004 卡验收 8 的判定面措辞（"无角色实例 role 的值为 null 且键仍在事件对象中 … 不得以渲染行当判据"） | 验收报告仍以"匿名实例渲染行"为不通过子项（`verify-20260911-123246-pr004.md:60`）；architecture §4.5/§11.2 事后已按实现改为"事件对象层 9 键恒定 / 渲染层吞 null"两层口径，但 pr-004 卡面原文未同步 | 卡面留档不返工即可，但应把"事件对象层 / 渲染层"两句话术固化进 PR 卡模板，避免同类摩擦在下一迭代重演 |
| D-8 | 归档惯例应一致 | `pr-002`/`pr-005`/`pr-006` 的 `*-tasks.md` 与代码**同 commit** 提交（e6da668 / 564f49c / 76a10ac），`pr-004` 的 tasks 单独归档（788e428）→ 混合惯例（pr-005 验收报告 D-4 亦已记录） | 明确一条：规划文档是否与代码同 commit；建议"代码与 tasks 同 commit、验收报告单独归档" |
| D-9 | 提交版契约 §4.4（permission = ACP `session/request_permission` 应答：allow → `allow_once` + `TOOL_APPROVED`；deny → `reject_once` + `session/cancel` + 轮次 `permission_denied`）、§4.5/§11.2（审计挂 permission 请求，N 次受门禁调用 = N 条记录）、§13 R-3（受门禁工具 = bash/edit/delete/move） | 取证期间工作区出现未提交的架构修订（V-9/V-10 阶段 6 真实 omp 实测）：① ACP 路径**从不发** `session/request_permission`（两次独立探针 0 次）⇒ pr-003 的 permission 应答与 pr-006 的审计断言链路在真实 omp 下**不可触发**（F05 的"每次受门禁调用恰一条审计"在真实环境没有来源）；② 工具真实执行走 `--approval-mode`，deny 档由 omp 自行拒绝、该轮**正常 completed**（不产生 `permission_denied`、不落失败 out 记录）；③ `tool_call`/`tool_call_update` 通知**存在**且含 `toolCallId`/`title`/`kind`/`status`/`rawInput.path`，是可用审计源（同时证伪提交版 W-4"ACP 不发 tool_call 帧"）；④ 声明 `clientCapabilities.fs`/`terminal` 会使 omp 把文件写入/终端委托给客户端 ⇒ 必须保持空声明。受影响范围：F05 卡、§4.4/§4.5/§11.2、pr-003 的两档行为、pr-006 的审计断言（代码本身无需回滚，属"契约演进后成死代码"） | 随阶段 6 收口：① 在写的架构修订落盘提交；② 补 pr-007 卡（架构修订已引用"pr-007 卡"，但 `prs/` 下尚不存在）实现 `--approval-mode` 映射 + `tool_call` 通知审计 + 空 `clientCapabilities` 不变式；③ 同步 F05 卡与 §11.2 的判定口径；④ 在 status/history 留痕该契约变更。不阻塞本迭代其余产物的一致性判定，但**阻断"F05 在真实环境成立"这一结论** |

## 下一迭代候选

- **NC-1 审计面覆盖收窄（只读工具无记录）**：受门禁工具仅 `bash`/`edit`/`delete`/`move`，字面 N=N（含只读）需 omp 提供 tool_call 帧（architecture §13 R-3 已登记；阶段 6 断言已按"变更类指令"口径执行）。
- **NC-2 cluster 测试可并发化**：见偏差 D-6；建议引入日志目录注入点，恢复 `node --test` 默认并发下的可用性。
- **NC-3 `OAMP_CLUSTER_WAIT_MS` 语义复用**：同一 env 同时作为 `up` 的就绪等待与 `down` 的子树退出等待上限（pr-005 报告 D-1，`cluster.js:452`）；建议文档化或拆成两个注入点。
- **NC-4 用法行 `--wait` 作用域**：`oamp cluster up|down|status [--config] [--wait]` 的排版让人误以为 `--wait` 对三动作通用，实现只允许 `up`（退出 2）（pr-005 报告 D-2）。
- **NC-5 一次性 `omp -p` 路径的模型链层级差**：角色级 `--model` 与 `config.defaults.model` 不作用于一次性路径（architecture §4.1 / §14.8 已留档）；如产品要求覆盖，属新增范围。
- **NC-6 并发证据的持久化**：worktree/分支现已清理，并发窗口只能由时序重建；建议后续迭代保留 worktree 记录（或落一份 `worktree list` 快照进 history），让"并发真实发生"可被第三方直接观察。
- **NC-7 真实环境验收（G7）**：本轮证据全部来自 fake 层（fake tmux / fake ACP / 无真实 LLM）；architecture §5.6 与 §12.2 的 G7（真实 omp + 真实 tmux 的 E1~E8）不在本次 A~G 标准范围内，本报告不判定。**其中一部分已由主 agent 在阶段 6 自行执行**：取证期间工作区出现 V-9/V-10 真实 omp 实测（见偏差 D-9），其结论直接推翻了提交版契约的 permission/审计机制；余下真实环境项（E3 角色规则原文生效、E7 真实 tmux 收口、E8 真实 cwd 落点）仍未在本报告中取证。
- **NC-8 凭据模式表双份维护**：`cluster-config.js:21` 的模式数组与 `test/hygiene.test.js` 的 `FORBIDDEN_WORDS` 各维护一份（均为规避字面量自匹配的字符串拼接写法，受既有 hygiene 断言约束）；若重构，可考虑单点定义。

## 结论

**PASS**（9 项标准：7 pass / 2 partial / 0 fail / 0 blocked；偏差 9 条、下一迭代候选 8 条）

- 可直接用于"是否返工"的决策：**已提交产物无需返工即可关闭本迭代**——A 的两条成因均非功能缺陷（pr-004 的 A8 是判定面措辞摩擦且架构已按实现更新口径，偏差 D-7；简报外改动是两个非代码文档的模型名改写，偏差 D-1，可用一次独立提交或 `revert` 收口）。**但"F05 在真实环境成立"这一结论当前不成立**：取证期间的真实 omp 实测（V-9/V-10，偏差 D-9）显示 permission 应答链路在真实 omp 下不可触发、审计没有来源；架构修订正在写入，而实现 PR（pr-007）尚未产出 ⇒ 需随阶段 6 收口新增 pr-007 才能真正闭合 F05，本报告不对该项作 pass 判定。
- 并发调度真实执行证据三项（标准 B-1/B-2/B-3）**全部成立**，且均有 git 可复核的时序/数值证据；最弱的一环是"分支已清理导致并发窗口只能重建"（NC-6）与"首跳上限更新滞后一拍"（偏差 D-3）。
- 未核实边界：真实 tmux + 真实 omp 的 E1~E8（G7）超出本次 A~G 标准范围，本报告不作判定（NC-7）。
