# PR-002 独立验收报告（集群配置加载与校验 + 仓库根 cluster.json）

**验证者身份**：同级代码审查者——Node.js ESM 叶子模块 + 配置加载/校验面的代码审查者（熟悉 `src/config.js` 的「env > 文件 > 内置默认 + 非法即抛错」风格、只读文件系统边界与 Map 保序语义）

**产出物**：
- worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0012-pr-002-cluster-config`
  - 分支 `feat/0012-pr-002-cluster-config`，HEAD = `e6da668`，base = `2010b6e`（迭代分支，含 pr-001 / pr-003）
  - `oamp/src/cluster-config.js`（197 行）、`oamp/test/cluster-config.test.js`（250 行）、`cluster.json`（26 行 / 422 B）、`docs/iterations/0012-roles-agent-cluster/prs/pr-002-cluster-config-tasks.md`（139 行）
- PR 卡：`docs/iterations/0012-roles-agent-cluster/prs/pr-002-cluster-config.md`
- 契约：`architecture.md` §5.1（`:311-365`）、§3.1（`:150-160`）、§12.2 契约 3（`:677`）

**验证标准来源**：主 agent 委托的 7 条验证标准 + PR 卡「验收标准」8 条 + `architecture.md` §5.1 / §3.1 / §12.2 契约 3

**验证日期**：2026-09-11
**验证方式**：纯只读——worktree 零写入（反向核对在 `/tmp` 副本中进行）；唯一写入 = 本报告

---

## 逐项判定

### A. PR 卡「验收标准」8 条

#### A1. 返回形状 = §12.2 契约 3 —— **pass**

- 证据（实测探针，读真实仓库根 `cluster.json`）：`Object.keys(real).sort()` = `roles,root,router,session,web`；`real.roles instanceof Map === true`、`size === 10`；首项 = `["architect",{"instanceId":"pb-architect","enabled":true,"tools":true,"permission":"allow","cwd":"<repo-root 绝对路径>"}]`；角色值键集合 = `cwd,enabled,instanceId,model,permission,tools`。
- 契约原文 `architecture.md:677`：`loadClusterConfig({ path, env }) → { session, root, web, router, roles: Map<role, {instanceId, enabled, model, tools, permission, cwd}> }` —— 逐键一致；`roles` 的 **Map 键 = 角色名**（`[...roles.keys()]` 实测为角色名列表，非 instance_id）。
- `src/cluster-config.js:173` 导出签名 `loadClusterConfig({ path: explicitPath, env } = {})` 与契约一致。

#### A2. 缺省值正确 —— **pass**

- `session='oamp-cluster'`（`:11`）、`web.port=7788`（`:12`）、`router.socket=null`（`:121-123`）、角色段 `enabled=true` / `tools=true` / `permission='allow'` / `cwd='.'`（`:132-156` 的 `normalizeRoleSection`）。测试 `oamp/test/cluster-config.test.js:89-103`（缺省值填充）断言五键全部命中。
- `cwd='.'` = 配置文件所在目录：`:150` `path.resolve(root, section.cwd === undefined ? CWD_DEFAULT : …)`，`root = path.dirname(configPath)`（`:184`）；实测缺省 `cwd === root`。
- `instanceId` 缺省 = `instanceIdForRole(role)` 且**本模块零内联拼接**：`:9` `import { instanceIdForRole, resolveRoleFile, resolveRoleRoot } from './role-binding.js'`；`:152` 返回值 `instanceId: instanceIdForRole(role)`。
  - `grep -rn "'pb-'" oamp/src` 命中 **3 处全部在 `oamp/src/role-binding.js`**（`:3`、`:12`、`:29`），`cluster-config.js` 零命中。

#### A3. cwd 解析语义 + enabled:false 保留 —— **pass**

- 相对基准 = 配置文件所在目录：测试 `:105-128`（覆盖值，`cwd:'sub'` → `<root>/sub`）；绝对路径原样（`cwd:'/tmp'` → `/tmp`）。
- 返回恒为绝对路径：`path.resolve` 收口。
- 不做 `~` 展开：测试 `:130-134` —— `cwd:'~/work'`（且目录真实存在）解析为 `<root>/~/work`，未展开为 `$HOME`。
- `enabled:false` 角色保留在 Map 且 `enabled===false`：测试 `:184-194` —— `dev` 的角色文件与 cwd **都不存在**仍不抛错，`roles.size===2`、`roles.get('dev').enabled===false`、`cwd` 仍归一化为绝对路径；`:160-162` 的早退（`if (!normalized.enabled) return;`）是跳过预检的落点。

#### A4. 仓库根 cluster.json：10 个真实角色、不含 `_template` / `cdp-debug-skill` —— **pass**

- 实测加载：`roles.size === 10`，键序 = `architect,demand,dev,planner,pr-planner,prd,progress-observer,retrospective,verifier,workflow-pb`（与 `architecture.md:326-331` 顺序逐项一致）。
- `roles.has('_template') === false`、`roles.has('cdp-debug-skill') === false`（测试 `:46-62` 显式断言）。
- 真实性核对：`ls roles/` = 12 个目录（10 个真实角色 + `_template` + `cdp-debug-skill`），`cluster.json` 的键集合恰为其中的 10 个真实角色——无遗漏、无虚构、无模板项。

#### A5. 非法输入全部抛错（9 类） —— **pass**

测试 `:136-182` 覆盖 19 个非法用例 + 文件缺失 + JSON 非法 + 角色文件缺失 + cwd 两类；逐类对照卡面 9 项：

| 卡面情形 | 落点 | 实测消息（含字段 + 当前值） |
|---|---|---|
| 文件不存在 | `:82-85` / 测试 `:137-138` | `配置文件不存在或不可读: <path>（ENOENT）` |
| 非法 JSON | `:87-91` / 测试 `:140-141` | `配置文件不是合法 JSON: <path>（<parser 原因>）` |
| 非对象 / 角色段类型错 | `:35-40` / `:133` | `配置文件顶层 需为对象` / `roles.dev 需为对象` |
| `permission` 非 allow\|deny | `:57-62` | `roles.dev.permission 需为 'allow' 或 'deny'（当前值 "always"）` |
| `tools` 非布尔 | `:42-47` | `roles.dev.tools 需为布尔值（当前值 "on"）` |
| `web.port` 非 1~65535 | `:64-69` | `web.port 需为 1~65535 的整数（当前值 99999）` |
| 角色文件不存在 | `:163-166` | `角色 dev 的角色文件不存在: <root>/roles/dev/dev.md` |
| cwd 不存在 / 非目录 | `:167-169` | `角色 dev 的 cwd 不存在或不是目录: <path>` |

- 「由调用方收口为 `oamp cluster: 配置错误: <原因>` + 退出码 2」：模块**刻意不带前缀**（`:74` 注释、MI-4），收口方 `src/cluster.js` 属 pr-005（本 worktree 无该文件，已核实 `oamp/src/cluster.js` 不存在）；卡面原文即写"由调用方收口"，故本 PR 责任面正确。

#### A6. 凭据类键递归扫描 + 未知键忽略 —— **pass**

- 扫描口径（`:21` `CREDENTIAL_KEY_PATTERNS`、`:97-110` 递归）：键名小写化 + 去 `-`/`_`/空格后**包含**匹配，覆盖架构 §5.1 全部 7 个键名（`token`/`secret`/`password`/`passwd`/`api_key`/`apikey`/`credential`）。
- 独立矩阵实测（19 个变体，全部命中）：`token`,`secret`,`password`,`passwd`,`api_key`,`apikey`,`credential`,`TOKEN`,`Secret`,`PASSWORD`,`Passwd`,`API_KEY`,`ApiKey`,`CREDENTIAL`,`api-key`,`Api Key`,`githubToken`,`my_secret_x`,`authCredential` → 未命中数 = **0**；数组内嵌 `[{token:'x'}]` 亦命中（`:98-101`）。
- 消息给字段路径：测试 `:196-211` 断言 `凭据类字段: roles.dev.password`、`auth.Secret`、`web.apikey`、`roles.dev.extra.credential` 等；根级路径不留前导点（实测 `凭据类字段: token`）。
- 未知键忽略（不报错）：测试 `:213-224`（顶层 `bogus`、`web.foo`、`router.baz`、`roles.dev.unknown`）；独立探针实测 `roles.dev.unknownRole` 被忽略、返回归一值 `{instanceId:'pb-dev',enabled:true,tools:true,permission:'allow',cwd:<root>}`、`web==={port:7788}`。
- 源文件不自命中既有卫生测试：`test/hygiene.test.js` 在 `npm test` 全量中通过（模式串用字符串拼接构造，`:20-21` 注释与实现一致）。

#### A7. cluster.json 被 git 跟踪 + 内容零凭据 —— **pass**

- `git ls-files cluster.json` → `cluster.json`（命中）。
- `git check-ignore -v cluster.json` → 无命中（未被忽略，确为 tracked 而非"可提交但被忽略"）。
- `grep -inE 'token|secret|password|passwd|api.?key|credential' cluster.json` → **零命中**（独立于模块内扫描的第二次取证）。

#### A8. 单文件测试全绿 + 既有测试零修改 —— **pass**

- `node --test test/cluster-config.test.js` → `# tests 15 / # pass 15 / # fail 0`。
- 自建自删：`tmpFixture` 用 `mkdtempSync(path.join(tmpdir(),'oamp-cluster-config-'))` + `t.after(() => rmSync(root,{recursive:true,force:true}))`（`:30-32`）；**未新增任何 fixture 文件**（本 PR 变更文件清单无 fixture）。
- 既有测试零修改：`git diff --name-only 2010b6e HEAD -- oamp/test/` → **仅 `oamp/test/cluster-config.test.js`（新增）**。
  - 精确口径：base `2010b6e` 有 18 个测试文件（0011 交付态 16 个 + pr-001 的 `role-binding.test.js` + pr-003 的 `tool-permission.test.js`）；HEAD 为 19 个（本 PR +1）。本 PR 对既有文件修改数 = **0**，卡面「既有 16 个测试文件零修改」成立。

### B. 委托补充验证标准（context 2~7）

#### B1. contracts 一致 —— **pass**

见 A1 + A2。另核实：`roles` 键 = 角色名（非 instance_id，`architecture.md:677` 原文与 O-1 裁决一致）、`instanceId` 来自 `role-binding.js`（`:9`/`:152`）、`grep -rn "'pb-'" oamp/src` 仅命中 `role-binding.js`（3/3）。

#### B2. 校验完备性 + 无静默降级 —— **pass**

- 「加载即全量校验」：`loadClusterConfig` 在 `return` 前完成 parse → 凭据扫描 → 顶层归一 → 角色段归一 → enabled 角色预检（`:186-196`）。
- 无静默降级（逐 catch 审计）：`readFileSync` catch **rethrow**（`:83-85`）；`JSON.parse` catch **rethrow**（`:89-91`）；`isDirectory` catch 返回 `false`（`:74-76`）→ 调用方 `:167-169` 抛错。全模块无吞异常分支、无 `?? 默认值` 型静默回落。
- `enabled:false` 跳过预检是**规格明示**的例外（§5.1「是否起窗口由 pr-005 决定」），非静默降级。

#### B3. cluster.json 三项事实 —— **pass**

tracked（A7）、零凭据（A7）、10 个真实角色且不含 `_template`/`cdp-debug-skill`（A4）。

#### B4. 测试全量 —— **pass**

- `node --test test/cluster-config.test.js` → **15/15 pass / 0 fail**。
- `npm test`（`node --test test/*.test.js`）→ **`# tests 182 / # pass 182 / # fail 0`**，与委托期望 182/182 一致。
- 既有测试文件修改数 = 0（A8）。

#### B5. 边界（仅新增 3 文件 + tasks；零新依赖；未改 prd/architecture/demand/其他 src） —— **pass**

- `git diff --name-only 2010b6e HEAD` → 恰为 4 个文件：`cluster.json`、`oamp/src/cluster-config.js`、`oamp/test/cluster-config.test.js`、`docs/iterations/0012-roles-agent-cluster/prs/pr-002-cluster-config-tasks.md`。
- `git diff 2010b6e HEAD -- oamp/package.json` → 空（零新依赖；`dependencies` 未动）。
- `git diff --name-only 2010b6e HEAD -- oamp/src` → 仅 `oamp/src/cluster-config.js`（`agent.js`/`acp-client.js`/`context-pool.js`/`cli.js` 等零改动）。
- `git diff --name-only 2010b6e HEAD -- <prd/> <demand.md> <architecture.md>` → 空（prd / architecture / demand 零改动）。
- worktree 洁净：`git status --short` 无输出（无未提交残留、无越界文件）。

#### B6. 反向核对（因果隔离） —— **pass**

在 `/tmp` 副本（`oamp/` + `roles/` + `cluster.json` 完整拷贝，worktree 零触碰）中对实现施加 4 组**单点删除**，逐一验证对应测试转红、还原后转绿：

| # | 删除的校验 | 突变 | 对应测试 | 结果 |
|---|---|---|---|---|
| M1 | 凭据扫描调用（`:187`） | 注释该行 | 「凭据类键递归扫描…」 | **fail 1**（0 pass） |
| M2 | `instance_id`/`instanceId` 覆盖拒绝（`:24` 置空数组） | `INSTANCE_OVERRIDE_KEYS = []` | 「角色段 instance_id / instanceId 字段 → 抛错」 | **fail 1**（0 pass） |
| M3 | 角色文件存在性预检（`:164`） | `if (false)` | 「enabled 角色的角色文件不存在 → 抛错」 | **fail 1**（0 pass） |
| M4 | cwd 目录性预检（`:167`） | `if (false)` | 「enabled 角色的 cwd 不存在或非目录 → 抛错」 | **fail 1**（0 pass） |

- 还原后同一用例 `pass 1 / fail 0`。四组均证明测试与校验点之间存在**真实因果**，非"恒真断言"。

---

## 汇总

| 判定 | 数量 | 明细 |
|---|---|---|
| **pass** | **14** | A1~A8（PR 卡 8 条）+ B1~B6（委托 6 条） |
| **fail** | **0** | — |
| **partial** | **0** | — |
| **blocked** | **0** | — |

---

## 偏差记录

> 实现与现有规格/文档不一致或规格未定义、由实现自行裁定的地方。**不影响本次验收判定**（A/B 全部 pass），但应在下一迭代同步文档或评估实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D1 | `architecture.md:324-332` 的「交付初版」schema 把所有 10 个角色写成空对象 `{}` | `cluster.json` 的 `dev` 段显式写出 `enabled:true / tools:true / permission:"allow" / cwd:"."`（值 = 缺省，运行行为零变化）——任务卡 MI-6 声明为"schema 自述"，但该推断项在任务文件中标注"需主 agent 确认" | 主 agent 确认 MI-6；或把 `dev` 段改回 `{}` 与架构示例逐字一致（二选一，无行为差异） |
| D2 | `architecture.md:354` 只给凭据键名清单 + "大小写不敏感"，**未定义匹配算法** | 实现取"小写化 + 去 `-`/`_`/空格 + 包含匹配"（`:21`/`:99-104`），比字面精确匹配**更严格**（`githubToken`、`my_secret_x`、`api-key` 均被拒） | 在 §5.1 校验表补一行算法定义（或明确"精确 + 大小写不敏感"收窄）；当前实现属保守侧，误报可在配置中改名消除 |
| D3 | `architecture.md:353`「未知键：忽略（不报错）」 | 角色段出现 `instance_id` / `instanceId` 时**抛错**（`:134-137`），与"未知键忽略"字面冲突；该取舍依据 §3.1「公式只此一处、不提供覆盖字段」，任务卡 MI-2 记录 | 在 §5.1 校验表补一行例外：「`instance_id`/`instanceId` 不按未知键忽略，命中即抛错（§3.1）」 |
| D4 | 任务卡 T3 措辞「先结构后环境」 | 实际顺序为 parse → **凭据扫描** → 顶层类型校验 → 角色段校验 → 角色预检（`:186-196`）；结构/类型非法 + 含凭据键的配置会**先报凭据错**（实测：`{token:'x', web:{port:0}}` → `配置含凭据类字段: token`） | 在 §5.1 或任务卡显式写明校验顺序（凭据扫描先于类型校验）；不影响任何单项判定 |
| D5 | 任务卡 T6 的 MI-1~MI-6 标注"**需主 agent 确认**"（`pr-002-cluster-config-tasks.md` 末表） | 在迭代目录（`status.md` / `history.md` / `clarifications/*`）**未找到** MI-1~MI-6 的确认记录；本 PR 实现按这 6 条推断落地 | 主 agent 在 `status.md` 或 `clarifications/` 补一条 MI-1~MI-6 确认记录（内容与实现一致，实测无偏离） |

---

## 下一迭代候选

> 不在当前验收标准范围内，供主 agent / planner 决策。

- **NC-1（校验顺序的文档化）**：D4 的校验顺序目前只隐含在代码执行序中；pr-005 收口错误前缀/退出码时若依赖"哪类错误先暴露"，需在同一处文档写明顺序。
- **NC-2（凭据扫描的算法边界）**：D2 的包含匹配会拒掉 `my_secret_x` 这类兼容命名；若未来用户配置里出现合法的非凭据键被误拒，可考虑改为"精确键名 + 常见组合还原"（如 `apiKey`→`api_key`）两级判定。当前无需求，仅记录。
- **NC-3（`roles` 为空的语义）**：`loadClusterConfig` 允许 `roles` 缺省 → 空 Map（测试 `:244-249`，F06-1「角色清单完全由配置驱动」）。pr-005 的 `cluster up` 面对空 Map 的行为（起一个只有 router/web 的集群？还是视为配置错误？）未在任何文档定义——建议在 pr-005 卡中显式裁定。
- **NC-4（`cwd` 存在性校验的 TOCTOU 窗口）**：预检与后续 tmux `-c` 之间目录可能消失；属极低概率，记录备用。

---

## 结论

**PASS**

- 14 项验证标准全部 **pass**（PR 卡 8 条 + 委托补充 6 条），0 fail / 0 partial / 0 blocked。
- 三项硬事实全部独立复现：`node --test test/cluster-config.test.js` 15/15；`npm test` 182/182；`git ls-files cluster.json` 命中且仓库根 `cluster.json` 零凭据。
- 反向核对（4 组单点删除 → 对应测试转红 → 还原转绿）证明校验点与测试之间存在真实因果。
- 5 条偏差均为**文档一致性 / 规格未定义处**，已被本报告显式记录，不阻塞本迭代关闭；其中 D5（MI-1~MI-6 缺确认留痕）建议主 agent 顺手补记。
