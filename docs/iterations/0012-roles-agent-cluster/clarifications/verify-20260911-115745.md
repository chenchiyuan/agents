# 独立验证报告 — 0012-roles-agent-cluster 阶段 4（PR 规划）

**验证者身份**：构建依赖图审查者 / 冲刺计划审查者（Release Train Planner）——职责是把 `prs/*.md` 当一份即将进入并发调度的提交单元图来审：依赖是否有真实代码耦合支撑、图是否可拓扑排序、文件范围是否互斥、功能点是否闭合、每个单元的验收面是否能在其自身合并时点独立判定、粒度是否会被一次 review 吃下。

**产出物**：
- `docs/iterations/0012-roles-agent-cluster/prs/pr-001-role-binding.md`
- `docs/iterations/0012-roles-agent-cluster/prs/pr-002-cluster-config.md`
- `docs/iterations/0012-roles-agent-cluster/prs/pr-003-acp-tool-permission.md`
- `docs/iterations/0012-roles-agent-cluster/prs/pr-004-agent-role-binding.md`
- `docs/iterations/0012-roles-agent-cluster/prs/pr-005-cluster-entry.md`
- `docs/iterations/0012-roles-agent-cluster/prs/pr-006-e2e-and-docs.md`

**验证标准来源**：
1. `.pb-agents/roles/workflow-pb/workflow-pb.md` §PR 文件格式规范（七字段）、§阶段定义（阶段 4 推进条件）、§阶段 5（依赖解锁语义）
2. 主 agent 委托中列出的 7 条验证标准（格式 / 依赖正确性（代码级证据）/ 无环 / 文件范围互斥 / F01~F07 覆盖 / 验收标准可独立判断 / 粒度）
3. 功能点基准：`docs/iterations/0012-roles-agent-cluster/prd.md`（F01~F07）+ `prd/F01~F07-*.md`
4. 代码库基准：`/Users/chenchiyuan/projects/agents/oamp/`（对每条 `depends_on` 做实际 grep / read）

**验证日期**：2026-09-11

**独立性声明**：本报告未接收任何执行过程上下文（未询问产出者意图、未读取阶段 4 执行会话记录）；判定仅基于上述产出物、标准与代码库现状。除本报告外未写入任何文件。

---

## 逐项判定

### 标准 1：每个 PR 文件含七字段，字段名与工作流规范一致 — **pass**

证据（`grep -n '^## ' prs/*.md`，6 个文件全部命中且顺序一致）：

| 文件 | 七字段齐备 | 字段名逐字一致 |
|---|---|---|
| pr-001 | ✓（`上下文摘要` L3 / `涉及功能点` L7 / `文件范围` L12 / `验收标准` L17 / `参考资料` L26 / `depends_on` L32 / `batch` L36） | ✓ |
| pr-002 | ✓（L41/45/53/59/70/77/81） | ✓ |
| pr-003 | ✓（L86/90/96/101/112/118/122） | ✓ |
| pr-004 | ✓（L127/131/139/144/156/162/167） | ✓ |
| pr-005 | ✓（L172/176/182/189/201/207/211） | ✓ |
| pr-006 | ✓（L216/220/227/232/243/250/255） | ✓ |

- 与规范 §PR 文件格式规范 1~7 的字段名逐字相同（`上下文摘要`/`涉及功能点`/`文件范围`/`验收标准`/`参考资料`/`depends_on`/`batch`）。
- `depends_on` 字段在无依赖时未被省略：pr-001、pr-003 均写明 `（无 —— …）`，符合规范「可为空列表，但字段必须存在……无依赖时写（无）」。
- `batch` 均为纯数字（1/2/2/2/3/3），符合规范「粗粒度分组标记，不是调度依据」。
- 文件名格式 `pr-{NNN-描述}.md` 全部符合；编号 001~006 连续无缺口。

### 标准 2：`depends_on` 每条依赖都能在代码库找到真实耦合证据 — **pass**（6 条依赖边全部有代码级证据；4 处锚点行号漂移见偏差记录 D4~D7，不影响依赖成立）

逐一验证（代码库 `/Users/chenchiyuan/projects/agents/oamp/`）：

| 依赖边 | 声称的耦合 | 实际核验结果 |
|---|---|---|
| pr-002 → pr-001 | `cluster-config.js` 必须 import `instanceIdForRole` / `resolveRoleFile` | **成立**。`architecture.md:673-674` 跨组契约 2/3 明确 `instanceId` 由 `role-binding.js` 单点导出、`loadClusterConfig` 返回形状含 `roles.<role>.instanceId`；`grep -rn "'pb-'" src/*.js` 无任何命中，`src/role-binding.js` 当前不存在（`src/` 无该文件）——pr-002 的 instanceId 默认值无第二个来源，缺 pr-001 即 `ERR_MODULE_NOT_FOUND` |
| pr-004 → pr-001 | `agent.js` 需 `roleFromInstanceId`/`resolveRoleRoot`/`resolveRoleFile` | **成立**。`src/agent.js:10-15` 实际 import 仅 `config` / `node-client` / `log` / `context-pool` / `node:child_process` / `node:crypto`（已读原文），**无任何角色映射来源**；`grep -rn "pb-" src/*.js` 无命中 |
| pr-004 → pr-003 | `context-pool.js:173-180` 现构造 `new AcpClient({ bin, model, cwd, logger, onExit })`，本 PR 追加 4 个新键；`context-pool.js:202-204` 早退名单现仅 `model_unavailable`/`context_busy` | **逐字成立**。`src/context-pool.js:173` = `const client = new AcpClient({`，`:174-178` = `bin`/`model`/`cwd`/`logger`/`onExit`（与声称完全一致）；`:202` = `_failSession(err) {`，`:204` = `if (code === 'model_unavailable' || code === 'context_busy') return;`（**行号与内容逐字命中**）。新键名由 `architecture.md:672` 跨组契约 1 定义 |
| pr-005 → pr-002 | `cluster.js` 必须 import `loadClusterConfig` | **成立**。`architecture.md:674` 跨组契约 3 定义返回形状；`src/cluster.js` 当前不存在；`src/cli.js:40-44` 实际存在 `ERR_MODULE_NOT_FOUND → "oamp: <label> 模块尚未实现（<file> 不存在）" + return 1` 的快速失败路径（已读原文），与 pr-005 声称一致 |
| pr-006 → pr-003 | 常驻 argv 拼装点 `acp-client.js:79` 现行硬编码 `--no-tools`；permission 帧现被 `_pending` 查找丢弃 | **成立**。`src/acp-client.js:79` = `const args = ['acp', '--no-skills', '--no-rules', '--no-tools', '--no-session'];`（**逐字命中**）；`_handleMessage` 定义于 `:262`，`:265-267` 为 `if (message.id !== undefined …)` → `const entry = this._pending.get(message.id);` → `if (!entry) return;`——即 V-6 挂起根因确认 |
| pr-006 → pr-004 | flag 解析、`pb-` 推断、两条路径注入与 `ROLE_BOUND`/`AGENT_START` 在 `agent.js:459-492`、`:150-161` | **成立**。`src/agent.js:150` = `function runOmpTask(…)`，`:158` = `const args = ['-p', '--no-session'];`，`:159-160` = `[--no-tools]`/`[--model]`（与声称的"只有 -p/--no-session/[--no-tools]/[--model]"一致）；`:459` = `export default async function startAgent(restArgs) {`，`:485` = `const pool = new ContextPool({`，`:492` = `const taskCtx = …` |

- 反向检查（依赖是否被漏报）：pr-001 与 pr-003 声明 `（无）` 正确——二者为叶子模块/协议层改造，`acp-client.js` 新参数全带默认值、既有 5 键构造调用不变（`context-pool.js:173-178` 已核）；未发现"被声明为无依赖但实际 import 了其它 PR 新符号"的情况。
- 依赖边均属"共享符号 / 接口 / 文件引用"三类，无一条是文档叙述顺序。

### 标准 3：依赖图无环 — **pass**

依赖图（边 = A 依赖 B）：

```
pr-002 → pr-001      pr-004 → pr-001
pr-004 → pr-003      pr-005 → pr-002
pr-006 → pr-003      pr-006 → pr-004
```

入度/出度核验后拓扑序存在且唯一层级为：`{pr-001, pr-003} → {pr-002, pr-004} → {pr-005, pr-006}`（pr-002 与 pr-004 之间无相互边；pr-005/pr-006 无出边到彼此）。无自环、无回边，**图可拓扑排序**。

### 标准 4：不同 PR 的文件范围无同一文件被两个 PR 声明 — **pass**

全量文件范围（去重后 15 条，两两互斥）：

| PR | 文件范围 |
|---|---|
| pr-001 | `oamp/src/role-binding.js`(新)、`oamp/test/role-binding.test.js`(新) |
| pr-002 | `oamp/src/cluster-config.js`(新)、`oamp/test/cluster-config.test.js`(新)、`cluster.json`(新) |
| pr-003 | `oamp/src/acp-client.js`(改)、`oamp/test/tool-permission.test.js`(新) |
| pr-004 | `oamp/src/agent.js`(改)、`oamp/src/context-pool.js`(改) |
| pr-005 | `oamp/src/cluster.js`(新)、`oamp/src/cli.js`(改)、`oamp/src/status.js`(改)、`oamp/test/cluster-actions.test.js`(新) |
| pr-006 | `oamp/test/acp-daemon.test.js`(改)、`oamp/README.md`(改) |

- 交叉检查：15 条文件路径在 6 个 PR 间**零重复**（无任一文件被两个 PR 同时声明）。
- 反向覆盖：`architecture.md:639-658` §12.1「新增」4 个新测试文件 + 3 个新 src 文件 + `cluster.json`、以及「改造」7 个文件，与上表一一对应，**变更面无遗漏**。
- 附带核验的仓库事实：`ls test/*.test.js | wc -l` = 16（与 pr-001/002/003 的"既有 16 个测试文件"、pr-006 的"其余 15 个"（16 − 本 PR 自己修改的 `acp-daemon.test.js`）口径一致）；`ls ../roles/*/` 恰为 `_template`、`cdp-debug-skill` + 声称的 10 个角色；`cluster.json` 当前不存在、`grep -rln "oamp cluster" src/ bin/` 无命中（pr-002/pr-005「新建」定性成立）。

### 标准 5：`prd.md` 的 F01~F07 每个功能点被至少一个 PR 引用 — **pass**

| 功能点 | 被引用的 PR | 覆盖次数 |
|---|---|---|
| F01 角色实例身份与生命周期 | pr-001、pr-004、pr-005、pr-006 | 4 |
| F02 角色规则加载与生效 | pr-001、pr-003、pr-004、pr-006 | 4 |
| F03 默认模型与按角色覆盖 | pr-002、pr-004 | 2 |
| F04 工具能力开关 | pr-002、pr-003、pr-004、pr-006 | 4 |
| F05 permission 策略 | pr-002、pr-003、pr-004、pr-006 | 4 |
| F06 集群入口脚本与配置 | pr-002、pr-005 | 2 |
| F07 按角色工作目录 | pr-002、pr-005 | 2 |

- 7/7 全覆盖，无功能点遗漏；引用形式均为 `prd/*.md` 的 F-ID 列表，符合规范。
- 与 `prd.md` §功能点索引（F01~F07，共 7 个）逐一对应，无超出 prd 的额外功能点被引用。

### 标准 6：每个 PR 至少一条验收标准，且不依赖其他 PR 完成即可判断 — **pass**

判定口径：验收标准只可断言**本 PR 文件范围内**、在其自身合并时点（其 `depends_on` 已合入）可观察的结果；不得要求尚未合入的其它 PR 的产物。

| PR | 可独立判断的验收标准（抽样，均只需本 PR 产物） |
|---|---|
| pr-001 | 「`oamp/src/role-binding.js` 存在，导出 4 个函数且不 import 任何第三方包」+「`node --test test/role-binding.test.js` 全绿」——纯本文件可判 |
| pr-002 | 「`loadClusterConfig({path, env})` 返回形状」「缺省值正确」「非法输入全部抛错」「`git ls-files cluster.json` 命中」——均由本 PR 的模块单测（临时目录自建配置）判定，不需任何下游 PR |
| pr-003 | 「`tools=true` → argv 不含 `--no-tools`」「允许档回 `allow_once`、同会话两次 → 恰 2 行 `TOOL_APPROVED`」「未知方法回 `-32601`」——fake ACP 内联在本 PR 新测试内，自足 |
| pr-004 | 「非法取值快速失败 → 退出码 2」「`permission_denied` 为轮次级（下一轮可正常完成，不产生 `CONTEXT_RESET`）」「回归：既有 5 个测试文件原样全绿」+ 末尾一条**人工可复跑冒烟**（fake omp 记录 argv）——常驻/一次性注入的断言由自足冒烟覆盖 |
| pr-005 | 「fake tmux 记录中窗口名/数量/`-c <cwd>`/末尾 `tee -a`」「`up` 幂等退出 0」「`down` 序列」「`tmux -V` 探活失败退出 2」——全部以 `OAMP_TMUX_BIN` 假 tmux 判定，不启真实 agent |
| pr-006 | 「匿名实例 argv 与既有断言逐项一致（含 `--no-tools` 且无 `--append-system-prompt`）」「既有 E-1~E-5 与 F08 用例只增不删」「`README.md` 新增集群一节」——测试文件自身可判 |

- 未发现任何 PR 把"另一个未合入 PR 的行为"写成本 PR 的验收断言：唯一两处跨 PR 引用（pr-004 的"自动化固化在 pr-006"、pr-005 的"缺省 = 仓库根 `cluster.json`（pr-002 产物）"）分别指向**本 PR 已提供的自足冒烟**（pr-004）与**已声明的上游依赖产物**（pr-005 → pr-002），均不违反"不依赖其他 PR 完成"。
- 另核验：pr-006 的两条断言确实需要 pr-003/pr-004 先行（`acp-client.js:79` 与 `agent.js:150-161` 的实际内容已核实），与其 `depends_on` 声明自洽。

### 标准 7：粒度符合「逻辑原子性 / 可审查性 / 独立性」框架 — **pass**

| PR | 逻辑原子性 | 可审查性 | 独立性（回滚牵连面） | 判定 |
|---|---|---|---|---|
| pr-001 | 单叶子模块（4 个纯函数）+ 单测 | 2 文件、零依赖 | 回滚只撤 role-binding 模块，不牵连任何功能 | ✓ |
| pr-002 | "配置加载 + 全量校验"一件事（schema 校验与加载不可分） | 3 文件（1 模块 + 1 数据 + 1 单测） | 回滚只撤配置面 | ✓ |
| pr-003 | 单文件 `acp-client.js` 的 argv 参数化 + permission 应答（同一帧处理面，`architecture.md:665` G3 已如此归组） | 2 文件 | 回滚只撤协议层，无下游被牵连（新参数带默认值） | ✓ |
| pr-004 | "agent 角色参数面 + 两条 LLM 路径注入"——`agent.js` 与 `context-pool.js` 是同一透传链的上下两段，拆开会产生不可独立验证的中间态 | 2 文件、同一变更链 | 回滚撤角色绑定，不牵连集群/测试 | ✓ |
| pr-005 | "集群入口三动作"一件事（`cluster.js` 新建 + `cli.js` 分发 + `status.js` 抽 `queryNodes` 供 status 段复用，后者是该 PR 的直接内部依赖） | 4 文件、无跨模块语义混合 | 回滚撤集群入口；`status.js` 仅"抽出并导出"、default 行为不变，回滚不影响既有 status | ✓ |
| pr-006 | "端到端断言收口 + 文档同步"——测试与 README 同属收口交付，diff 小 | 2 文件 | 回滚只撤断言与文档，不动 `src/` | ✓ |

- 未发现"回滚会牵连无关功能"的 PR：每个 PR 的文件集合都服务同一交付单元。
- 未发现"跨模块混合无法一次审查"的 PR：最大 diff 面为 pr-005 的 4 个文件，且其中 3 个是本 PR 自身语义的一体三面（入口 / 分发 / 状态复用）。
- 注：pr-003 同时承载 F04（工具开关）与 F05（permission）两个功能点的行为，但二者落在同一文件的同一修改面（`start()` argv + `_handleMessage`），架构 §12.2 亦按 G3 归为一组，不构成"无法一次审查"。

---

## 汇总

- **pass：7 项**
- **fail：0 项**
- **partial：0 项**
- **blocked：0 项**

## 偏差记录

> 产出物与现有规格/文档不一致的地方。不影响本次验收判定（7 条标准全部 pass），但应在下一迭代同步或在阶段 5 执行时注意。

| # | 规格/文档描述 | 实际（代码/文档）行为 | 建议处理 |
|---|---|---|---|
| D1 | `architecture.md:664` §12.2 把 G2（`cluster-config.js` + `cluster.json`）标为「依赖：无（自带 schema 校验）／∥ G1」 | pr-002 声明 `depends_on: pr-001`，且证据成立（跨组契约 3 要求返回 `roles.<role>.instanceId`，按 §3.1/AR-01 该公式单点收敛于 `role-binding.js`，`grep -rn "'pb-'" src/*.js` 无第二来源） | **以 PR 卡为准**（有代码级证据）；后续迭代把 `architecture.md` §12.2 的 G2 行同步为「依赖 G1」，避免分组表与实际依赖图再次分叉 |
| D2 | `architecture.md:668` §12.2 把 G6 标为「依赖 G4 + G5」 | pr-006 声明 `depends_on: pr-003 + pr-004`，并在卡内显式记录该差异（依据 §5.6 的断言面只到 argv 级，集群编排判定面在 pr-005 的 `cluster-actions.test.js`） | 接受 pr-006 的判据（其文件范围与验收面确实不含 `cluster.js` / `cli.js`）；若要求 README 集群一节在 `oamp cluster` 可验证后再落，则补 `pr-005`——代价是末位 PR 变串行，建议维持现状 |
| D3 | workflow-pb「每一条依赖必须有代码级证据（共享符号/接口/文件）」的严格读法 | pr-005 未声明对 pr-004 的依赖，但其 §验收标准 1 生成的 tmux 窗口命令含 `--role` / `--tools` / `--permission`——这三个 flag 由 pr-004 在 `agent.js` 落地（AR-02），构成**接口级耦合**。`architecture.md:667` §12.2 显式写明「G5 … 与 G4 文件不相交，可并行」，故当前声明与架构裁决一致 | 维持现状（架构已裁决并行，且 pr-005 的验收面只断言命令**字符串**，不启真实 agent，可独立判定）；在阶段 5 排程时确保迭代分支在阶段 6 前同时含 pr-004，以支撑 E1/E4 的真实验收 |
| D4 | pr-006 参考资料称「`oamp/test/acp-daemon.test.js:12`（`spawn`）」 | `:11` = `import { spawn } from 'node:child_process';`，`:12` = `import { DatabaseSync } from 'node:sqlite';`（行号偏移 1） | 不影响判定；阶段 5 执行者按符号名（`spawn` import）定位，后续 PR 卡规范建议"锚点写符号名 + 行号" |
| D5 | pr-003 称「现状这类帧因 `_pending` 无此 id 被 `return` 丢弃（`oamp/src/acp-client.js:264-266`）」、「`:57/59-60`（构造参数）」 | 丢弃发生在 `:265-267`（`if (!entry) return;` 在 `:267`；`:264` 是 `_onAnyMessage` 行）；构造器签名在 `:55`，`:57/59/60` 仅 `modelArg`/`logger`/`onExit` 三个字段赋值（`:58` 为 `cwd`） | 结论（V-6 根因）不变，仅行号窗口偏移；阶段 5 按符号定位 |
| D6 | pr-005 参考资料称「`oamp/src/cli.js:4-31`（USAGE）」 | `USAGE` 模板字面量实际为 `:4-27`（`:27` = 反引号+分号）；`:29-32` 已是 `usageError` 函数 | 同上，行号窗口偏宽 4 行 |
| D7 | pr-005 参考资料称「`oamp/src/status.js:84-117`（`queryNodes` 的抽取来源）」 | 实际查询区为 `:81-117`（`:81` 起 `loadConfig` try 块；`:84` 已是 try 内部）；`:117` = `process.stdout.write(\`${renderTable(nodes)}\n\`)` 命中 | 同上；抽取面本身（config 读取 → socket 连接 → `router.status` 请求 → `nodes` 归一化）落在该区间内，语义正确 |

## 下一迭代候选

- **NC-1（依赖语义分层）**：D3 暴露"接口级耦合"在 `depends_on`（硬依赖，阻塞解锁）与"无依赖"之间无处安放。建议 workflow-pb 在 `depends_on` 之外定义"软依赖/接口依赖"标注（不参与解锁判定，仅供排程与阶段 6 集成检查），否则每个跨 PR 接口都会被记为"未声明依赖"。
- **NC-2（架构分组表回填）**：D1/D2 说明 `architecture.md` §12.2 的 G 组依赖列与阶段 4 的 PR 卡存在 2 处分叉。建议阶段 6 或下一迭代以 PR 卡实际 `depends_on` 回填/复核 §12.2，避免后续迭代继续引用过期分组表排程。
- **NC-3（测试文件计数表述统一）**：PR 卡混用「既有 16 个测试文件零修改」（pr-001/002/003）与「其余 15 个既有测试文件」（pr-006）。经核验二者指代不同集合（16 = 全部既有 `.test.js`；15 = 扣除 pr-006 自身修改的 `acp-daemon.test.js`），语义自洽但表述易误读。后续统一为「既有 16 个 `.test.js` 中本 PR 修改 N 个，其余 16−N 个零修改」。
- **NC-4（验收锚点写法）**：D4~D7 共 4 处锚点行号漂移说明"行号锚点"在 PR 卡里是易腐信息。建议 PR 卡参考资料统一写「符号名 @ 文件:行号（行号仅供快速跳转）」，并在阶段 5 执行者 brief 中说明以符号名为准。

## 结论

**PASS**（7 项标准全部 pass，无 fail / partial / blocked；7 条偏差记录不影响本次阶段 4 的推进判定，可作为阶段 5 执行注意事项与下一迭代输入）。

依据：① 6 个 PR 文件均完整满足七字段规范；② 6 条 `depends_on` 依赖边全部在 `oamp/src` 找到可定位的代码级耦合证据（其中 pr-004 → pr-003 的两处锚点 `context-pool.js:173-180` / `:202-204` 逐字命中，pr-006 → pr-003 的 `acp-client.js:79` 逐字命中）；③ 依赖图无环且可拓扑排序；④ 15 条文件范围两两互斥且覆盖 §12.1 全部变更面；⑤ F01~F07 全覆盖；⑥ 每个 PR 均含至少一条在本 PR 范围内可独立判定的验收标准；⑦ 6 个 PR 粒度均符合逻辑原子性/可审查性/独立性，无回滚牵连无关功能的单元。
