# PR-003 独立验收报告 — ACP 客户端 argv 参数化 + permission 应答与审计

**验证者身份（反射结果）**：同级代码审查者 × ACP/JSON-RPC 协议级验证者——面向"常驻子进程 + 按行 JSON-RPC + fake 二进制帧级断言"这类产出物的评审者，关注协议应答形态、被判必回的帧是否真被回、回归不变式是否逐字节成立；判定手段以帧级证据与变异测试为主，不采信叙述。

**产出物**：worktree `.pb-agents/worktrees/0012-pr-003-acp-tool-permission`（分支 `feat/0012-pr-003-acp-tool-permission`，HEAD `f82273e`，base `edfd710`；`git status --porcelain` 为空 = 待验状态即提交态）
- 改造：`oamp/src/acp-client.js`（318 → 422 行，`+119 / -15`）
- 新增：`oamp/test/tool-permission.test.js`（311 行）
- `git diff --name-status edfd710 f82273e` 的全部输出：`M oamp/src/acp-client.js`、`A oamp/test/tool-permission.test.js`（无第三个文件）

**验证标准来源**：主 agent 委托的 7 条验证标准 + PR 卡 `docs/iterations/0012-roles-agent-cluster/prs/pr-003-acp-tool-permission.md`「验收标准」8 条 + `architecture.md` §3.2 / §4.3 / §4.4 / §4.5 / §11.2 / §12.2

**验证日期**：2026-09-11

**独立性声明**：未接收任何执行过程信息（设计取舍、作者意图、调试经过均未提供、未使用）。全部证据来自产出物本身、架构/PRD 文档与本次独立执行的命令。被验证 worktree **保持只读**——所有变异测试与行为探针均在 `/tmp` 副本上进行；唯一写入 = 本报告。

---

## 取证环境与命令（可复查）

| # | 命令 | 结果 |
|---|---|---|
| E-1 | `node --test test/tool-permission.test.js`（worktree `oamp/`） | `# pass 6 / # fail 0`（6/6） |
| E-2 | `node --test test/context-pool.test.js` | `# pass 16 / # fail 0`（16/16） |
| E-3 | `npm test`（=`node --test test/*.test.js`） | `1..158 / # pass 158 / # fail 0` |
| E-4 | `git diff --stat edfd710 HEAD` | 仅 2 文件（见上）；`oamp/package.json` 未改；`dependencies: {}` 未变 |
| E-5 | `git ls-tree -r` 计数 | base 16 个 `*.test.js` → HEAD 17 个；既有文件 `--name-status` 无 M 行 |
| E-6 | 逐字节 argv 对比探针（`/tmp/pr003mut/argv-diff2.mjs`，同一 fake omp、同一构造 `{bin,cwd,model}`；base 版 = `git show edfd710:oamp/src/acp-client.js` 落盘，318 行、其 `:79` 为卡片所述硬编码 argv） | base(edfd710) 与 HEAD(f82273e) 的 spawn argv **完全相同**：`["acp","--no-skills","--no-rules","--no-tools","--no-session","--model","deepseek/deepseek-v4-flash"]` → `identical: true`；HEAD 缺省（`tools=false`）仍含 `--no-tools` |
| E-7 | 变异测试 4 组（对 **HEAD 版** `acp-client.js` 的 `/tmp` 副本注入变异，逐组回滚后复跑 E-1；被验 worktree 未写入） | M1 `allow_once→allow_always` 仅红 allow 用例；M2 `!this.tools→this.tools` 红 2 条 argv 用例；M3 删 deny 分支 `this.cancel()` 仅红 deny 用例；M4 `-32601→-32600` 仅红 unknown 用例（明细见标准 7） |
| E-8 | 行为探针（`/tmp/pr003mut/probe.mjs`）：同一 fake 服务端，**HEAD 版**与**「HEAD 版删去服务端请求分支」**对照（模拟修复前代码） | HEAD：回帧 `{outcome:{outcome:'selected',optionId:'allow_once'}}`；**删掉 HEAD 的服务端请求分支后**（模拟修复前）：回帧 `null`、服务端标记 `prompt_settled_without_reply`、审计 0 条 → 帧被静默丢弃 |
| E-9 | 行为探针（`/tmp/pr003mut/hook-probe.mjs`）：`onPermissionRequest: async () => 'deny'`，静态 `permission:'allow'` | 实际回 `allow_once` + 1 条 `TOOL_APPROVED`（异步钩子被静默忽略，见 NC-3） |

---

## 逐项判定

### 标准 1 — PR 卡「验收标准」逐条判定：**pass**（8/8）

| 卡片条目 | 判定 | 可定位证据 |
|---|---|---|
| 1. 既有调用零感知（不传 `tools` → argv 仍含 `--no-tools`；不传 `roleFile` → argv 无 `--append-system-prompt`） | **pass** | E-6 逐字节相同；缺省值 `tools = false` / `roleFile = null`（`acp-client.js:66-67`）→ `acp-client.js:103-107` 的 argv 拼装恒含 `--no-tools`；`test/tool-permission.test.js:179` 断言无 `--append-system-prompt`；`oamp/src/context-pool.js:173-180` 只传 `bin/model/cwd/logger/onExit` 5 键（该文件本 PR 未改，构造面零变化） |
| 2. `tools=true` → argv 不含 `--no-tools`；`roleFile=<绝对路径>` → argv 含 `--append-system-prompt <绝对路径>`；三项 `--no-*` 恒在 | **pass** | `acp-client.js:103-107`；`test/tool-permission.test.js:170`（不含 `--no-tools`）、`:171-173`（三项恒在循环）、`:200`（`argv[idx+1] === roleFile` 且 `path.isAbsolute` 断言 :201） |
| 3. 允许档：回 `{outcome:{outcome:'selected',optionId:'allow_once'}}`、该轮不挂起；同会话两次请求 → 恰 2 行 `TOOL_APPROVED` | **pass** | `acp-client.js:327`；`test/tool-permission.test.js:218-222`（2 个回帧逐条 deepEqual `allow_once`）、`:214-215`（两轮均 `end_turn` = 未挂起）、`:225`（`approved.length === 2`）、`:226`（`TOOL_DENIED` 为 0）；fake 服务端仅在收到应答后才结算 prompt（`test/tool-permission.test.js:92-101`，结算 `send` 在 `:101`）→ 轮次结算即证明客户端已回帧 |
| 4. 审计字段齐全（instance/role/chat_id/context_id/pid/tool/title≤120/tool_call_id/option），走 `event()` | **pass**（注：卡片未要求"无身份时字段仍存在"，该子项见标准 5） | `acp-client.js:355-364`（`_audit`：身份四字段展开 + pid/tool/title.slice(120)/tool_call_id/option）；`logger.event` 调用点 `:329`/`:334`；`test/tool-permission.test.js:229-238` 逐一断言 9 字段，`:237` 断言 `title.length === 120`（fake 构造 140+ 字符标题）；`src/log.js:35-37` `event()` 无条件写行（无节流） |
| 5. 拒绝档：回 `reject_once` → 立即 `session/cancel` → `prompt()` 抛 `AcpError{code:'permission_denied'}`；同会话不因此销毁 | **pass** | 顺序由实现保证：`acp-client.js:327`（回帧）→ `:332`（置标记）→ `:333`（cancel）；`:182` / `:186` 两处抛 `permission_denied`；`test/tool-permission.test.js:259`（reject_once）、`:261-262`（frames 含 `session/cancel` 且 sessionId 匹配）、`:252-255`（谓词 `:254`，正则含 `permission=deny`）、`:270`（`client.dead === false`）；`PERMISSION_DENIED_TEXT` = `acp-client.js:18` |
| 6. 未知服务端方法 → `-32601`，既不静默丢弃也不挂起 | **pass** | `acp-client.js:338`（`_respondError(message.id, -32601, 'Method not found')`）；`test/tool-permission.test.js:279`（该轮 `end_turn`，未挂起）、`:282-284`（回帧 result 为 null、error.code `-32601`、message `Method not found`） |
| 7. 服务端请求识别条件 = `id` 存在且 `method` 为字符串，且先于 `_pending` 查找 | **pass** | `acp-client.js:301`（id 非空）→ `:303`（`typeof message.method === 'string'`）→ `:304` 调用 → `:305` return；`:307` 才是 `_pending.get`，`:308` 为丢弃点 → **分支严格先于 pending 查找**（base 版丢弃点原为 `:267`，卡片锚点与 base 一致：`git show edfd710:oamp/src/acp-client.js` 中 `:264`=`_onAnyMessage`、`:267`=`if (!entry) return;`）；判定面 E-8 见标准 7 的反向核对 |
| 8. `node --test test/tool-permission.test.js` 全绿；`context-pool.test.js` / `web.test.js` / `acp-daemon.test.js` 原样全绿 | **pass** | E-1 6/6；E-2 16/16；E-3 全套 158/158；E-5 既有 16 个测试文件零修改（`--name-status` 无 M）；fake 内联于测试文件（`test/tool-permission.test.js:17-104`，沿用 `context-pool.test.js` 的 FAKE_ACP_SOURCE 形态）；无新增测试文件（卡片文件范围仅 `tool-permission.test.js`） |

### 标准 2 — 回归硬约束（既有调用方零感知）：**pass**

- **逐字节 argv 不变**：E-6 `identical: true`（真实 base `edfd710` 与 HEAD `f82273e` 在既有 5 键调用下 spawn 的 argv 逐元素相同，且 base 的硬编码 argv 位置与卡片锚点 `:79` 一致）。这比"仍含 `--no-tools`"更强。
- `node --test test/context-pool.test.js` → 16/16 通过（E-2）。
- `dev-1` argv 断言仍含 `--no-tools`：`oamp/test/context-pool.test.js:525-536`，其中 `:530-534` 为 `for (const flag of ['--no-skills','--no-rules','--no-tools','--no-session']) assert.ok(acp.includes(flag))`；该文件未被修改（E-5、E-4）。
- 既有 16 个测试文件零修改：E-5（base 16 → HEAD 17，唯一新增为 `tool-permission.test.js`，无任何 M 行）。

### 标准 3 — 测试：**pass**

- E-1：`1..6 / # pass 6 / # fail 0 / # skipped 0`（期望 6/6 ✓）。
- E-3：`1..158 / # pass 158 / # fail 0`（期望 158/158 ✓）。

### 标准 4 — permission 语义：**pass**

| 子项 | 判定 | 证据 |
|---|---|---|
| allow 档恒回 `allow_once`，**不得** `allow_always` | pass | `acp-client.js:327`；`grep -rn "allow_always" oamp/src/` → **0 命中**（服务端 options 里的 `allow_always` 仅出现在 fake 帧构造与测试注释，非客户端选择） |
| deny 档回 `reject_once` | pass | `acp-client.js:327`；`test/tool-permission.test.js:259` |
| deny 档发 `session/cancel` | pass | `acp-client.js:333`；`:261-262` 断言 frames 含 `session/cancel` 且 `params.sessionId === 'sess-1'` |
| deny 档该轮错误码 `permission_denied` | pass | `acp-client.js:182` / `:186`；`:252-255` 断言 `err.name === 'AcpError' && err.code === 'permission_denied'` |
| deny 档会话保留（进程不死） | pass（PR-003 范围内） | `:270` `client.dead === false`；AcpClient 未调用 `kill()`/`dispose()`，`_onProcessGone` 未被触发。**端到端"会话保留"仍依赖 PR-004**：`context-pool.js:204` 的 `_failSession` 早退名单当前只有 `model_unavailable`/`context_busy`，见 NC-1（卡片已将该归类显式指派给 pr-004，故不扣本 PR） |
| 未知方法回 `-32601` | pass | `acp-client.js:338`；`:282-284` |
| 服务端请求分支先于 `_pending` 查找 | pass | `acp-client.js:301-308`（分支 303-306 严格在 `:307` 之前）；反向核对见标准 7 |

### 标准 5 — 审计：**partial**

拆分子项：

- **5a 每次 permission 请求恰一条 `TOOL_APPROVED`/`TOOL_DENIED`：pass**
  2 次请求 → 2 条（`test/tool-permission.test.js:225`，`:226` 断言 TOOL_DENIED=0）；deny 1 次请求 → 1 条 `TOOL_DENIED`（`:266-267`，`:269` 断言 TOOL_APPROVED=0）；独立探针 E-8 亦为 1 请求 = 1 条。
- **5b 提供身份时字段齐全（instance/role/chat_id/context_id + pid/tool/title/tool_call_id/option）：pass**
  `test/tool-permission.test.js:229-238` 逐字段断言；探针 E-8 `base-with-auditCtx` 的事件键集合 = `["chat_id","context_id","instance","option","pid","role","title","tool","tool_call_id"]`（9/9）。
- **5c 未提供身份（缺省）时字段仍存在（值可为空）：fail（范围限定：仅缺省路径）**
  证据（E-8 探针 `base-no-auditCtx`，`auditContext` 缺省）：事件键集合 = `["option","pid","title","tool","tool_call_id"]`——`instance` / `role` / `chat_id` / `context_id` **四个键不存在**（不是空值）。成因可定位：`acp-client.js:80` 将缺省归一为 `null`，`:358` 以 `...(this.auditContext || {})` 展开 → 缺省时展开为空对象。
  满足本子项的可判定条件：未提供 `auditContext` 时事件仍含上述四键（值可为 `null`/`''`）。
  影响面：PR-003 内无生产调用方（`context-pool.js:173-180` 尚未透传），故当前不可观测；一旦 PR-004 漏传 `auditContext`，审计行会静默丢失身份维度（与 §4.5 字段定案、E5 的"谁被放行"判据相关）。
- **走永不节流的 `event()`（AR-11）：pass** —— `acp-client.js:329`/`:334` 调 `logger.event`；`src/log.js:35-37` 的 `event()` 无条件写行。

### 标准 6 — 边界：**pass**

- 改动面 = `oamp/src/acp-client.js` + 新增 `oamp/test/tool-permission.test.js`，无第三文件（E-4 全量 `--name-status`）。
- 零新依赖：`oamp/package.json` 未改（E-4），`dependencies: {}`；新增 import 仅 `test`/`assert`/`fs`/`os`/`path` 与既有 `../src/acp-client.js`（测试文件 :8-13）。
- 未改 prd / architecture / demand：不在 `git diff edfd710 f82273e` 的改动集内（改动集只有上述 2 个 `oamp/**` 文件）。
- worktree 干净（未提交残留 = 0）。

### 标准 7 — 反向核对（因果隔离）：**pass**（已做）

在 `/tmp` 副本上注入错误行为，逐组回滚后复跑 E-1，观察红点是否精确落到对应用例（证明断言不是"恒真"）：

| 变异 | 出现红灯的用例 | 是否精确 |
|---|---|---|
| M1 `allow ? 'allow_once'` → `'allow_always'`（`acp-client.js:327`） | 仅「F05-2/§11.2：允许档恒回 allow_once…」 | ✓ 只红 1 条 |
| M2 `if (!this.tools)` → `if (this.tools)`（`:104`） | 「F04/AR-08/AR-09：tools=true → argv 不含 --no-tools…」+「F02/AR-04：roleFile → argv…」 | ✓ 2 条 argv 类用例红（第 2 条的 `assert.ok(!argv.includes('--no-tools'))` 亦命中），其余 4 条全绿 |
| M3 删除 deny 分支的 `this.cancel();`（`:333`） | 仅「F05-3/F05-4：拒绝档…」 | ✓ 只红 1 条 |
| M4 `-32601` → `-32600`（`:338`） | 仅「§4.4/W-5：未知服务端方法回 -32601…」 | ✓ 只红 1 条 |
| M5 删除服务端请求分支（`acp-client.js:302-306`，即修复前行为） | 见 E-8 探针（未跑仓库测试文件：真实 omp 语义下该轮会挂到 300s 超时；改用等价短期探针） | ✓ 帧被静默丢弃：`reply: null`、`prompt_settled_without_reply: true`、`auditCount: 0`；HEAD 版同样场景 `reply.optionId === 'allow_once'`、`auditCount: 1` → 标准 1-条目 7 与标准 4 的"先于 pending 查找"因果成立 |

---

## 汇总

- **pass：6 项**（标准 1、2、3、4、6、7；其中标准 1 内部 8/8 条通过）
- **fail：0 项**
- **partial：1 项**（标准 5 —— 5a/5b pass，5c fail，见上拆分）
- **blocked：0 项**

---

## 偏差记录

> 实现与现有规格/文档不一致之处。不影响本次验收判定（判定见上），供主 agent 决定同步文档或调整实现。

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| `architecture.md` §12.2 跨组契约 1 列 `AcpClient` 构造参数为 9 项：`{bin, model, cwd, tools, roleFile, permission, logger, onExit, onPermissionRequest}`；PR 卡「上下文摘要」亦只列新增 `tools`/`roleFile`/`permission`/`onPermissionRequest` 四项 | 实现存在**第 10 个**构造参数 `auditContext`（`acp-client.js:69`、`:80`），且它是 §4.5 身份字段的**唯一**来源；PR-004（`context-pool.js:173-180` 的 `_ensureClient`）尚未透传 | 按实现更新 §12.2 契约 1（补 `auditContext`），并在 PR-004 卡中显式写入"必须透传 `auditContext:{instance,role,chat_id,context_id}`"——否则 PR-004 落地后审计行会缺身份维度（与标准 5c 叠加即为 §4.5 违约） |
| `architecture.md` §4.5 定案表把 `instance / role / chat_id / context_id` 列为审计事件的**无条件**字段（表内未设"身份可得时才有"的前提） | 缺省（未传 `auditContext`）时这 4 个键**完全不出现**（探针 E-8：键集合仅 `option/pid/title/tool/tool_call_id`） | 二选一：① 按实现补文档口径（"身份四字段在调用方提供 `auditContext` 时存在"）；② 或让实现恒带四键（缺省为 `null`）。该条与标准 5c 同源，主 agent 若判需闭合，改动面仅 `acp-client.js:358`（`_audit` 的展开表达式）一行 |

---

## 下一迭代候选

> 不在本次验收标准范围内的优化点、边界问题或遗留限制，供主 agent / planner 决策。

- **NC-1（跨 PR 一致性，高优先）**：`context-pool.js:204` 的 `_failSession` 早退名单目前仅 `model_unavailable` / `context_busy`。若 PR-004 不加入 `permission_denied`，deny 档轮次会走 `_remove` + `CONTEXT_RESET` + `context_reset` notice → 端到端"会话保留"（§4.4）不成立。卡片已将归类指派给 pr-004，但该 PR 卡尚未落盘（本迭代 PR-004 待实现），建议在 PR-004 验收时列为必查项。
- **NC-2（测试覆盖）**：deny 档用例只断言 `client.dead === false`（`test/tool-permission.test.js:270`），未覆盖"拒绝之后同一会话下一轮仍可正常对话"。建议在 PR-004 / G6 端到端（`acp-daemon.test.js`）补一轮"拒绝 → 再发一轮 → 仍 completed"。
- **NC-3（契约/健壮性，已实测）**：`onPermissionRequest` 若返回 Promise（异步钩子）会被静默忽略并回落静态 `permission`——探针 E-9：钩子 `async () => 'deny'` + 静态 `allow` → 实际回 `allow_once` + `TOOL_APPROVED`（**拒绝意图被吞**）。§12.2 契约 1 只写 `'allow'|'deny'` 未明确同步性；若 PR-004 将来用该钩子做动态策略，建议显式约定（同步返回 / 否则视为配置错误响亮失败）。
- **NC-4（观察点）**：`_permissionDecision` 对钩子抛出的异常无保护，异常会从 `child.stdout` 的 `data` 回调路径冒泡（`acp-client.js:299` → `:304` → `:342`，钩子调用点 `:344`），可能导致 agent 进程未捕获异常退出；当前无生产调用方，属未来接钩子时的风险面。

---

## 结论

**PASS**（无 fail 条目；1 项 partial 拆解后仅"未提供 `auditContext` 时身份字段不存在"这一子项不满足，且发生在 PR-003 内不可观测的缺省路径上）

- 7 条验证标准：pass 6 / fail 0 / partial 1 / blocked 0
- PR 卡 8 条验收标准：8/8 pass
- 偏差记录：2 条（均为"实现先行、文档/契约未同步"型，不阻塞交付）
- 主 agent 决策点：(a) 标准 5c 是判为返工（`acp-client.js:358` 一行）还是记为偏差；(b) §12.2 契约 1 与 PR-004 卡是否补 `auditContext` 透传要求；(c) NC-1 是否作为 PR-004 的验收必查项。

---


## 复审记录 — 标准 5c 修复后复验（2026-09-11）

**复审对象**：worktree HEAD `0e3f954`（`feat(0012/pr-003): ACP 客户端 argv 参数化 + permission 应答与审计`，父 `f82273e`）。被复审前本报告结论：标准 5 = partial（5c fail）。
**复审范围**：主 agent 指定只核标准 5c 与回归面，不重跑全篇——标准 1/2/3/4/6/7 的原判定在 `0e3f954` 上由本节的回归证据覆盖（改动面仅 `_audit()` 与新增 1 个用例，不触及 argv/应答/取消路径）。
**独立性**：未接收执行过程信息（仅收到 commit 号与改动范围转述），结论全部由本轮独立执行取证；被验 worktree 未写入（`git status --porcelain` 为空），变异在 `/tmp/pr003rr` 副本上进行。

### 5c 判定：**pass**

1. **键集合恒定（代码级证据）**：`oamp/src/acp-client.js:355-369` 的 `_audit()` 以显式字面量声明 9 个键——身份四键 `:359-362` 取 `identity.<key> ?? null`（`identity = this.auditContext || {}`，`:357`），`pid` `:363`、`tool` `:364`、`title` `:365`、`tool_call_id` `:366`、`option` `:367`；**已不再使用 `...spread`** → 缺省时键不会消失。
2. **运行期证据（新用例）**：`oamp/test/tool-permission.test.js:241-265`——`Object.keys(fields).sort()` 与 9 键字面量 `deepEqual`（`:253-257`），四个身份键断言为 `null`（`:258-261`）。
3. **因果隔离（实测，非论证）**：将 `_audit()` 回退为旧写法（`...(this.auditContext || {})`，即 `:357-362` 还原为展开式）后跑该测试文件 → **唯一红灯**为「§4.5：缺省 auditContext 时审计字段集合恒定（身份四键存在且为 null）」，其余 6 条全绿；复原后 7/7。→ 新用例确因该缺陷转红，非恒真断言。

### 回归面复验

- `node --test test/tool-permission.test.js` → **7/7**（fail 0）
- `node --test test/context-pool.test.js` → **16/16**（fail 0）
- `npm test` → **159/159**（fail 0）
- 范围：`git diff --name-only edfd710 0e3f954` 仍为 `oamp/src/acp-client.js` + `oamp/test/tool-permission.test.js` 两个文件（既有 16 个测试文件 `--name-status` 无 M 行，16 → 17；非 `oamp/` 改动 0）；`0e3f954` 相对 `f82273e` 亦仅这 2 文件（+34 / -4）。

### 复审结论与新偏差

- **标准 5c：pass** → 标准 5 由 partial 升为 **pass**；本次验收 7 条标准 **7/7 pass**（fail 0 / partial 0），**整体复审结论：PASS**。本节判定替代本报告「结论」段中关于标准 5 的 partial 计数（该段保留为 `f82273e` 时点的历史记录）。
- **新偏差：未检测到。** 原偏差记录第 2 条（§4.5 字段列表 "无条件" ↔ 实现"条件存在"）随本次修复**闭合**（实现现恒带四键，与 §4.5 定案表一致）；原表格保留为历史记录。原偏差第 1 条（§12.2 契约 1 未列 `auditContext` 构造参数）**仍然成立**，处置建议不变。
- NC-1 ~ NC-4 不受本次修复影响，仍然有效（NC-1 仍是 PR-004 验收的必查项）。
