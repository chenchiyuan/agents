# 验证报告 · pr-001（PR 级复验 · 第二轮）

**验证者身份**：契约审查者 + 同级文本审查者——对"规范 / SKILL 的文本改动 + 规范条款落地"做逐条契约核验与逐字文本比对；核验手段仅限只读 git 命令与文件读取，不运行任何写入性命令。

**产出物**：

- worktree `.pb-agents/worktrees/pr-001`：分支 `feat/0019-pr-001-workflow-pb-protocol`、HEAD `c034dab`、base `15d5eaf`
- 变更集 `git diff 15d5eaf..c034dab`（`--name-status` 实测 6 文件 = 5 `M` + 1 `A`）
- 落地文件（只读）：`roles/workflow-pb/workflow-pb.md`（736 行）、`.claude/skills/workflow-pb/SKILL.md`（386 行）、`roles/workflow-pb/data/workflow-pb-changelog.md`、`roles/workflow-pb/memory.md`、`.claude/skills/workflow-pb/memory.md`、`.claude/skills/workflow-pb/data/skill-optimization-v1.12.0.md`

**验证标准来源**：

1. `docs/iterations/0019-worktree-isolation-protocol/prs/pr-001-workflow-pb-workspace-isolation-protocol.md` 的「验收标准」（实测 `- [ ]` = 32 条）
2. `docs/iterations/0019-worktree-isolation-protocol/prs/pr-001-tasks.md`（**修订 r2**，含「计数口径定版（r2）」与两条裁定登记）的 T1~T9「验收标准」与「验证方法」
3. `docs/iterations/0019-worktree-isolation-protocol/architecture.md` **v1.2.2**：§2.3 文末术语限定枚举、§5.1（W1~W20）、§5.2（S1~S10）、§5.3 零改动清单、§5.4、§5.5、§8.0~§8.5 措辞块、§3.3、§6.1（ADR-1~ADR-4）、§7（Q-1~Q-5）
4. 委托的 6 条顶层验证标准：① PR 文件验收标准逐条；② tasks r2 T1~T9 逐条（含 grep / diff / 计数判据按该文件口径实跑）；③ 文件范围；④ 与 architecture v1.2.2 一致性；⑤ 工作区纪律（规则 A）；⑥ 可施工性

**验证日期**：2026-09-12

**判定口径**：条目只有 pass / fail / partial / blocked；全部条目 pass 才 PASS。本报告的全部行号均为**落地后（HEAD `c034dab`）**的行号，基线行号单独标注。

---

## 标准 1 · PR 文件「验收标准」32 条逐条判定

| # | 验收条目 | 判定 | 证据（落地后定位） |
|---|---|---|---|
| 1 | 变更面编号对齐（W1~W20 / S1~S10，不出现旧编号区间） | pass | 6 个落地文件内 `grep 'W1~W17\|W1-W17'` = 0 命中；tasks r2 引用区间为 W1~W20 + S1~S10；W19（`:124`）/ W20（`:365`）两条承载条目均实际落地 |
| 2 | W1 版本与留痕 | pass | `workflow-pb.md:25` = `**版本**: 0.9.0`；`:65-74` 新增 `## v0.9.0 变更说明（相对 v0.8.0）`（四段式：新增概述 / 目的 / 不改变的部分 / 生效范围），位置与 `:45`（v0.7.0）、`:55`（v0.8.0）并列；`changelog:3` 新增 `## v0.9.0（2026-09-12）`；`roles/workflow-pb/memory.md:8` 顶部新增索引行 |
| 3 | W2/W3 术语限定 + F07 验收 5 | pass | `:137` `### 规则 A：PR worktree 隔离`；`:139` 独立 **PR** worktree 分支；`:145` 每个 **PR worktree** 变更；`:141` / `:147` 两条 `判断方式：` 行与基线（`sed -n '131p;137p'` → 基线 `9ede9ea`）**逐字相同**（逐字节比对无差异） |
| 4 | W4 规则 C | pass | `:149-163`：`git worktree` 默认且唯一被规范化形态 + `git clone` 仅兜底**声明**、无执行步骤（`:151`）；每迭代一个、阶段 1~6 保持检出（`:151`）；"仓库主工作区不作为任何迭代的会话工作区"（`:153`）；"PR worktree 不得被当作会话工作区使用"（`:155`）；落点与建立时点 + 命令（`:157-161`）；`:163` 三条 git 原语判据 |
| 5 | W5 规则 D | pass | `:165-184`：三条判据（`:169-171`）、发现即停（`:167`）、硬停输出形态含创建命令（`:173-181`）、显式写明**不复用** `⛔ 需要你的决策`（`:173`）、约束主体 = 主 agent 会话 + 阶段 5 子 agent 以被派发时所在工作区为准（`:167`）、`:184` 判断方式行 |
| 6 | W6 规则 E | pass | `:186-190`：所有分支名须含迭代编号、PR 分支规范化 `feat/{迭代编号}-pr-{NNN}-{slug}`、base 与合并目标不变、不追溯既有分支、`:190` 以 `git branch --list` 判断 |
| 7 | W7 规则 F | pass | `:192-200`：三类写操作点名（`git -C` / `--work-tree` / 文件写入，`:194`）、"本条款不可能被 git 强制执行"（`:196`）、闭集例外两项（`:198`）、"先取备份、后操作"（`:194`）、`:200` 判断方式行 |
| 8 | W8 规则 G | pass | `:202-206`：工作区与分支原样保留、清理权仅归归属会话或用户、"并列、不互相替代"（`:204`）、无任何清理动作；既有现场保留条款（`:386` 阶段 5 第 4 点、`:411` progress-observer 核实项、`:592`/`:594`/`:596` 状态符号、槛位取值与 worktree 分支列标注约定）**无 diff hunk** |
| 9 | W9 隔离边界声明 | pass | `:208-228`：覆盖三项（`:210`）；不覆盖三项 + 逐项负面约束（`:212-215`）；"同一分支不能在两处同时检出"硬约束 + 会话工作区永不 checkout `main`（`:217`）；非 git 四项 + 两条约束（`:219-226`）；已知代价（`:228`）。越界承诺自查：`grep '跨机器隔离\|跨工作区写入会被\|refs 也被隔离'` = 0 命中（`跨机器` 仅出现在 `:151` 的兜底**免责**句"也不保证该场景的隔离"内） |
| 10 | W10 角色定义来源与部署 | pass | `:230-242`：真源 = 被 git 追踪内容 + `git ls-files --error-unmatch`（`:232`）；两场景共用同一份规范与 skill（`:236`）；"零动作"含义限定（`:238`）；下游分发不算运行前提（`:234`/`:238`）；只读约束（`:240`）、`:242` 判断方式行 |
| 11 | W11 启动工作流 | pass | `:327-328` 步骤 1 之前的会话工作区校验句（引用规则 C/D、判据 = 三条 git 原语、不新增步骤号）；`:333` 4.5 步保留位置与触发时机、创建动作声明为已完成、`status.md` 的 `**迭代分支**` 字段写入保留；`grep '主 agent 根目录'` = 0 命中；步骤号实测 1/2/3/4/4.5/5 不变（`:329-336`） |
| 12 | W12 brief 构建 | pass | `:343` / `:351` / `:353` 三处 → `{角色定义根}/...`；该节（`:341-367`）diff 仅 4 行（3 路径 + `:365`），其余措辞与"brief **至少**包含…"字段名列表逐字不变；`grep '\.pb-agents/roles' workflow-pb.md` = 3 行（`:69` 变更说明历史叙述 / `:234` `:236` 下游副本语境） |
| 13 | W13 阶段 5 步骤 2 | pass | `:384` `<path>` → `.pb-agents/worktrees/{迭代编号}-pr-{NNN}-{slug}`、`<pr分支名>` → `feat/{迭代编号}-pr-{NNN}-{slug}`、补"在该会话工作区内执行"；base `iteration/{迭代ID}`、解锁判据、槛位算法文字逐字不变（该行 diff 仅命令段） |
| 14 | W14 收口 | pass | `:440` 标题补"（在仓库主工作区执行）"；`:442` 三步为 `git -C <仓库主工作区> …`；`:447` 第 3 步前置 `git -C <会话工作区> checkout --detach`（写明是 git 硬约束的必然前置、不是新增收口步骤）；commit message（`:449`）、冲突处理（`:451`）、status 字段（`:453`）逐字不变 |
| 15 | W15 文档路径协议 | pass | `:461` 新增"全部路径相对该会话所在的迭代工作区根目录判读"；该处为纯插入 hunk，目录树与文件清单无改动 |
| 16 | W16 角色读取指南 `:579` 真源写法保留 | pass | `:690` `1. **工作流文件路径**：`roles/workflow-pb/workflow-pb.md`` 逐字保留；`git diff` 的 hunk 列表中无该区域 hunk（基线 `:579` 未被触碰） |
| 17 | W17 原则引用词 | pass | `:722` "提交管理约束（**PR worktree** 隔离、PR 文件前置）"；该行 diff 仅该术语 token |
| 18 | 术语限定面 7 处（唯一基准 = §2.3 文末枚举，按行计） | pass | 删除行中含 `worktree` 的仅 7 处 + `:277` 非术语位移（`grep '^-' diff \| grep worktree` 实测）；逐行 diff 证实每处只改 worktree token：`:124`（W19）/`:137`（W2）/`:139`（W2）/`:145`（W3）/`:365`（W20）/`:722`（W17）/ SKILL `:305`（S7）= 7/7，无第八处 |
| 19 | `:114` 阶段定义表（承载体 W19） | pass | `:124` 一行仅 `worktree` → `**PR worktree**`（逐行 diff：其余四列逐字相同）；`## 阶段定义` 表内无其它 hunk |
| 20 | `:258` brief 字段行（承载体 W20） | pass | `:365` = `PR worktree 分支：{分支名}` |
| 21 | S1 CRITICAL 语义反转 | pass | SKILL `:42` 全文与 arch §8.4 给定改写文本逐字一致；含"不得在 agents 仓库内读取被忽略的 `.pb-agents/roles/` 副本"与"`tools/install-pb-agents.sh` 是面向下游的可选分发手段，不是本协议的运行前提" |
| 22 | S2 新增 CRITICAL | pass | SKILL `:44` |
| 23 | S3/S4 Step 0 与 Tools | pass | SKILL `:98`（启动校验 + 角色来源解析，删除安装检测）；`:136` 第 1 步启动校验（三判据 → 输出创建命令并发现即停）、`:137` 第 2 步角色来源解析（一次解析、会话内复用、写入每次 brief） |
| 24 | S5 章节改名与内容 | pass | SKILL `:222` `## § 角色文件来源与部署`；`:224` 真源规则 + 判据引用；`:226` 两场景（上游零动作 / 下游"可选分发手段，不是本协议的运行前提"）；`:228` 解析动作；`:230` 只读约束；"未安装即停止"/"不得退回读 `roles/`" `grep` = 0 命中 |
| 25 | S6 32 处路径替换 | pass | 基线 `grep -c '\.pb-agents/roles'` = **32 行**（出现 35 次）；落地后 = **2 行**（`:42` 下游副本语境、`:227` 下游分发语境），符合 arch R-4「≤2 行」；不存在"必须用 `.pb-agents/roles/`"与"不得读 `roles/`"并存表述 |
| 26 | S7 brief 模板 | pass | SKILL `:305` = `PR worktree 分支：{分支名}`，与规范 `:365` 逐字一致 |
| 27 | S8 Step 5 步骤 2 | pass | SKILL `:174` 补 PR worktree 落点 `<会话工作区>/.pb-agents/worktrees/{迭代编号}-pr-{NNN}-{slug}` + 分支 + base；并发/解锁语义文字不变（同行 diff 仅插入该引用段） |
| 28 | S9 隔离边界引用行 | pass | SKILL `:105`「隔离边界（覆盖 / 不覆盖）与非 git 共享资源见规范 §隔离边界声明（只引用，不重复定义）」；SKILL 内无覆盖/不覆盖清单或资源表的平行列举 |
| 29 | S10 版本与章节名引用 | pass | SKILL `:4`（v0.9.0）、`:30`（`1.12.0（对应规范 workflow-pb v0.9.0）`）、`:60`（Purpose v0.9.0）三处全同步；`grep 'v0\.8\.0' SKILL.md` = **0 命中**；`:31`、`:367` 章节名引用同步为「§ 角色文件来源与部署」；新建 `data/skill-optimization-v1.12.0.md`（五段式 + 一致性核对）；`.claude/skills/workflow-pb/memory.md:5` 新增索引行 |
| 30 | 零改动面核对 | pass | 规范 18 hunk / SKILL 30 hunk **逐 hunk 全部可归类**（明细见标准 2 的 T9-1）；零改动清单各节无 hunk；`git diff --stat 15d5eaf` 实测仅 6 文件，不含 `tools/install-pb-agents.sh` / `.gitignore` / 9 个执行角色 / `principles/**` / `oamp/**` / `docs/**` / 既有 `skill-optimization-v1.*` 历史文件 |
| 31 | F13 验收 1/2/3 | pass | 新增/改写条款全部为"条款 + 紧跟一行 `判断方式：`"体例；`grep -c '^#### 判断方式'` = 0；`grep -c '^判断方式：'` = 8（规则集 7 + 来源节 1）；落地手段仅读文本 + git 原语（`worktree`/`rev-parse`/`ls-files`/`branch`/`merge`）；无 hook / 访问控制 / 路径守卫 / 守护进程 / 新配置键 / 新 env / 新文档类型（`:196` 明文声明不引入 hook 与访问控制） |
| 32 | 生效范围不变 | pass | `:244` `### 约束生效范围` 与基线逐字相同（非 `+`/`-` 行）；新条款全部位于 `:244` 之前（规则 C `:149` … 来源节收尾 `:242`）；`grep -rn '立即生效\|追溯适用'` = 0 命中；变更说明写明"不要求本迭代（0019）自身迁移" |

**小计：32 pass / 0 fail / 0 partial / 0 blocked**

---

## 标准 2 · tasks r2「T1~T9 验收标准」逐条判定（含 grep / diff / 计数判据实跑）

### T1 · 规范·提交管理约束区（12 项）

| 条目 | 判定 | 证据 |
|---|---|---|
| T1-1 规则 A 标题限定 | pass | `:137`（基线 `:127`）逐行 diff 仅该 token |
| T1-2 规则 B 术语限定 | pass | `:145`（基线 `:135`）逐行 diff 仅该 token |
| T1-3 两条判断方式行逐字不动 | pass | `:141`/`:147` 与基线 `:131`/`:137` 逐字节相同 |
| T1-4 新增规则 C 五项内容 | pass | `:149-163`（五项逐项落位，见标准 1 第 4 条） |
| T1-5 规则 C 的判断方式行 | pass | `:163` 同属一个 `###`、非子标题、三条 git 原语判据 |
| T1-6 新增规则 D | pass | `:165-184`（判据三条 + 发现即停 + 硬停形态 + 约束主体 + 子 agent 归属） |
| T1-7 新增规则 E | pass | `:186-190` |
| T1-8 新增规则 F | pass | `:192-200` |
| T1-9 新增规则 G | pass | `:202-206` |
| T1-10 插入位置与生效范围 | pass | 五条规则位于 `:149`~`:206`，`### 约束生效范围` 在 `:244`（在其之后、该句之前） |
| T1-11 体例与零越界 | pass | `grep -n '^### 规则'` = **7 行**且顺序 A/B/C/D/E/F/G；`grep -n '^判断方式：'` = **8 行**，拆分为规则集 7（`:141`/`:147`/`:163`/`:184`/`:190`/`:200`/`:206`）+ 来源节 1（`:242`），且 `:141`/`:147` 与改前逐字相同；`grep -c '^#### 判断方式'` = 0；`## 验证目标` 的带 bullet `- **判断方式**：` 不计入（行首非精确匹配） |
| T1-12 本任务不触碰区 | pass | `## 阶段定义` 表（除 `:124`）/ `## PR 文件格式规范` / `## 验证目标` / `## 状态追踪协议` / `## 历史记录协议` / v0.2.0~v0.8.0 变更说明：hunk 列表中无对应区域 |

### T2 · 规范·两个新节（8 项）

| 条目 | 判定 | 证据 |
|---|---|---|
| T2-1 覆盖三项 | pass | `:210` |
| T2-2 不覆盖三项 + 逐项负面约束 | pass | `:212-216`（stash / 分支命名空间 / `git branch --merged` / 共享对象库 / config·hooks·remotes 各附负面约束） |
| T2-3 git 硬约束句 | pass | `:217` |
| T2-4 非 git 四项 + 两条约束 | pass | `:219-226`（7788 / CPU 负载 / `/tmp` / 进程内作业注册表；约束①"不要求修改默认端口值"、约束②"安静环境重跑"）；`grep 'ls-files\|stash\|--merged\|7788\|/tmp\|安静环境'` 逐项命中（`:213`/`:223`/`:224`/`:225`/`:232`/`:242`） |
| T2-5 已知代价与不夸大 | pass | `:227` 写"后移到收口合并期处置"、明文不定义自动解决；三类越界承诺 0 命中 |
| T2-6 角色定义来源与部署 | pass | `:230-242`（真源判据 / 单次解析 / 两场景共用 / 零动作含义 / 只读约束 / 判断方式） |
| T2-7 位置口径 | pass | `:208`（隔离边界）与 `:230`（来源节）均 < `:244`（`### 约束生效范围`），且均位于 `## 提交管理约束` 内 |
| T2-8 可判定性与不平行定义 | pass | 两条非 git 约束各带判断方式（`:223`/`:224` 行内）；两节均写在规范内，未新增文件、SKILL 侧仅引用（`:105`） |

### T3 · 规范·启动工作流（5 项）

| 条目 | 判定 | 证据 |
|---|---|---|
| T3-1 校验引用句 | pass | `:327-328`，位于步骤 1（`:329`）之前；步骤号体系仍为 1~5 + 4.5 |
| T3-2 4.5 动作改写为声明已完成 | pass | `:333`（"已由会话工作区创建步骤在**会话启动之前**完成"） |
| T3-3 4.5 保留 status.md 字段写入 | pass | `:333` 句尾仍写 `**迭代分支**: iteration/{迭代ID}` |
| T3-4 指代重载 | pass | `:333` "**该会话所在的迭代工作区**（根目录）保持检出该分支"；`grep '主 agent 根目录'` = 0 命中；原 `git checkout` 动作被重述为"本步骤不再执行" |
| T3-5 其余零改动 | pass | 步骤 1~4、步骤 5 字段清单、`## 调度指南` 其它小节均无 hunk |

### T4 · 规范·brief 构建路径替换（5 项）

| 条目 | 判定 | 证据 |
|---|---|---|
| T4-1 三处路径替换 | pass | `:343`/`:351`/`:353`；`grep -n '\.pb-agents/roles' workflow-pb.md` = **3 行**：`:234`/`:236`（下游副本语境 = 规范性表述）+ `:69`（`## v0.9.0 变更说明`「目的」段历史叙述，按 r2 T4-1 排除语**不在判据范围**）；brief 构建节内已无该串 |
| T4-2 `:258` 模板行限定 | pass | `:365`；与 SKILL `:305` 措辞一致 |
| T4-3 该节其余逐字不变 | pass | 该节 diff 仅 4 行；"brief **至少**包含以下字段"、逐行 `字段名：值` 要求、全部字段名逐字保留 |
| T4-4 `:579` 真源写法逐字保留 | pass | `:690` 未被改动，diff 中无该区域 hunk |
| T4-5 不引入解析规则 | pass | 该节无 `{角色定义根}` 解析步骤/判据（解析规则唯一真源 = `:236`） |

### T5 · 规范·阶段 5 落点与收口（6 项）

| 条目 | 判定 | 证据 |
|---|---|---|
| T5-1 步骤 2 落点具体化 | pass | `:384`（`<path>` / `<分支名>` 填充 + "在该会话工作区内执行"） |
| T5-2 该节其余逐字不变 | pass | base、解锁判据（含"槛位空置不放宽"）、槛位算法公式均无 hunk |
| T5-3 收口三步改 `git -C` 形式 | pass | `:443`/`:444`/`:445` |
| T5-4 前置游离动作 | pass | `:447`；`grep -n 'checkout --detach'` = **1 行**（`:447`） |
| T5-5 执行位置限定 | pass | `:440` 标题 + `:442` 正文补"在**仓库主工作区**执行" |
| T5-6 不变部分逐字保留 | pass | commit message（`:449`）、冲突上报（`:451`）、status 字段（`:453`）逐字不变；无自动化合并 |

> T5「验证方法」的 `grep -n 'git -C'` ⇒ 4 处**未按字面成立**（实测 6 行：`:67` / `:194` / `:443` / `:444` / `:445` / `:447`）。判据所指的 4 处（收口三步 + 前置游离）**全部存在且形式正确**；另 2 行是同一 PR 内**其它条目的必需内容**（W1 变更说明叙述、W7 规则 F 的写操作点名）。判为**文档计数口径偏差**（见偏差记录 D-1），T5 的 6 条验收标准本身全部 pass。

### T6 · 规范·散点（5 项）

| 条目 | 判定 | 证据 |
|---|---|---|
| T6-1 `:114` 阶段 5 行术语限定 | pass | `:124` 逐行 diff 仅 worktree token；其余单元格逐字不动 |
| T6-2 文档路径协议补判读主体 | pass | `:461`；目录树与文件清单不变（纯插入 hunk） |
| T6-3 原则引用词同步 | pass | `:722` |
| T6-4 术语限定面不扩不缩 | pass | 见标准 1 第 18 条：删除行中含 `worktree` 者恰为 7 处 + `:277` 非术语位移；其余出现点无 hunk |
| T6-5 本任务不触碰区 | pass | `## 何时更新本文件` / `## 验证目标` / `## 状态追踪协议` / `## 历史记录协议` / `### 可观测性` / `### 阶段回退` / `### 需要用户决策的情况` / `### 终止工作流` 均无 hunk |

### T7 · 规范·版本与留痕（5 项）

| 条目 | 判定 | 证据 |
|---|---|---|
| T7-1 版本行 | pass | `:25` = `0.9.0` |
| T7-2 v0.9.0 变更说明 | pass | `:65-74` 四段式，位置与 `:45`/`:55` 并列；"生效范围"段与 `:244` 口径一致（不追溯、不要求 0019 迁移） |
| T7-3 changelog 条目 | pass | `changelog:3` 位于标题行之后、`## v0.8.0（2026-09-08）`（`:32`）之前；五段（触发/方案/未改变的部分/生效范围 + 具体改动列表）与 v0.8.0 条目同形同序；该文件 `git diff --numstat` = **29 插入 / 0 删除** |
| T7-4 memory 索引行 | pass | `memory.md:8` 在标题之后、v0.8.0 行（`:9`）之前；格式与既有行一致；`numstat` = 1/0 |
| T7-5 范围纪律 | pass | T7 未触及 SKILL 侧两个留痕文件（那两个文件仅在 T8 对应 hunk 中被改） |

### T8 · SKILL·全量改写与留痕（14 项）

| 条目 | 判定 | 证据 |
|---|---|---|
| T8-1 S1 语义反转 | pass | `:42`（与 arch §8.4 文本逐字一致） |
| T8-2 S2 新增 CRITICAL | pass | `:44` |
| T8-3 S3 Tools | pass | `:98`（启动校验 + 角色来源解析；安装检测句删除） |
| T8-4 S4 Step 0 两步 | pass | `:136`（启动校验：三判据 → 输出创建命令并发现即停）、`:137`（角色来源解析：一次解析 / 会话内复用 / 写入每次 brief），与原第 2、3 步（现 `:138`/`:139`）分离 |
| T8-5 S5 章节改名与内容 | pass | `:222`/`:224`/`:226`/`:228`/`:230`；"未安装即停止"与"不得退回读 `roles/`" 均删除 |
| T8-6 S6 32 处机械替换 | pass | 基线 `grep -c` = 32 行（`grep -o \| wc -l` = 35 次，与 r2 ④ 口径一致）；落地后 = 2 行，且均为"可选分发手段 / 下游副本"语境 |
| T8-7 S7 brief 模板 | pass | `:305` |
| T8-8 S8 Step 5 落点引用 | pass | `:174`；并发/解锁语义未改 |
| T8-9 S9 隔离边界引用行 | pass | `:105`（只引用不重复定义） |
| T8-10 S10 版本与章节名引用 | pass | `:4`/`:30`/`:60` 三处同步；`grep -n 'v0\.8\.0'` = **0 命中**；`:31`/`:367` 同步为「§ 角色文件来源与部署」 |
| T8-11 新建 v1.12.0 记录 | pass | 五段齐备（触发 `:3` / 根因 `:5` / 方案 `:7` / 具体改动 `:9-25` / 未改变的部分 `:27`）+「一致性核对」`:29-35`；文件 `git diff --name-status` = `A` |
| T8-12 skills memory 索引行 | pass | `.claude/skills/workflow-pb/memory.md:5`，位于 `> 每行对应…` 之后、v1.11.0 行（`:6`）之前；格式与既有行一致；`numstat` = 1/0 |
| T8-13 零改动面 | pass | 其余 CRITICAL 均无 diff hunk（基线 9 条中仅 `:42` 被改写、`:44` 为新增，其余 8 条逐字不动）；`## Purpose`（除版本数字）/ `## Success criteria` / `## Strategy` / `## Important facts` / `Step 1~4`、`Gate 4→5`、`Step 6` / `## § 对外协议` / `## § 用户决策点与暂停格式` / `## § status.md`·`history.md 更新时机` / `## Safety` 的其余条目：**除 S6 明列路径引用行外无 hunk**（按 r2 的"更具体的 S6 枚举优先"口径，`:141`/`:174`/`:190`/`:216`/`:341`/`:355`/`:367`/`:385` 均为 arch §2.4 变更面表内明列的路径行，归类 = S6，不判为"零改动面被改"）；`tools/install-pb-agents.sh` 与 `.gitignore` 不在 diff 中 |
| T8-14 `:32` 变更历史索引行保持原样 | pass | 该行（`**变更历史**: 见 data/skill-optimization-v1.1.0.md … v1.10.0.md`）无 hunk，未追加 v1.11.0/v1.12.0 |

### T9 · 回归核对（9 项）

| 条目 | 判定 | 证据 |
|---|---|---|
| T9-1 逐 hunk 可归类 | pass | 规范 **18 hunk**：`:25`→W1；`:65-74`→W1；`:124`→W19；`:137`→W2；`:139`→W2；`:145`→W3；`:149-243`→W4/W5/W6/W7/W8/W9/W10；`:327-328`→W11；`:333`→W11；`:343`→W12；`:351`→W12；`:353`→W12；`:365`→W20；`:384`→W13；`:440`→W14；`:442-447`→W14；`:461`→W15；`:722`→W17。SKILL **30 hunk**：`:4`/`:30`/`:60`→S10；`:42`→S1；`:44`→S2；`:52`/`:145`/`:180`/`:190`/`:216`/`:240`/`:245`/`:250`/`:255`/`:260-262`/`:267`/`:272`/`:287`/`:289`/`:341`/`:355`/`:367-368`/`:385-386`→S6；`:98`→S3；`:105`→S9；`:136-139`→S4；`:174`→S8；`:222-230`→S5；`:305`→S7。**无无法归类的 hunk** |
| T9-2 术语限定 7/7 命中承载条目 | pass | 规范 `:124`→W19、`:137`/`:139`→W2、`:145`→W3、`:365`→W20、`:722`→W17；SKILL `:305`→S7；无第八处多余改写（其余出现点按 arch §2.3 文末判定规则保持不动；`:384` 为非术语位移 = W13，不计入） |
| T9-3 规范零改动清单无 hunk | pass | `## PR 文件格式规范`（`:250-321`）、`## 验证目标`（`:496-543`）、`## 状态追踪协议`（`:545-608`）、`## 历史记录协议`（`:610-685`）、`### 约束生效范围`（`:244`，仅上下文行）、v0.2.0~v0.8.0 变更说明（`:45-113`）、`### 阶段回退` / `### 需要用户决策的情况` / `### 终止工作流` / `### 可观测性`、`## 阶段定义`（除 `:124` 术语限定）：均无 hunk |
| T9-4 SKILL 零改动面无 hunk | pass | 见 T8-13（含 S6 路径行的范围排除，归类 = S6） |
| T9-5 diff 外文件不得出现 | pass | `git diff --name-status 15d5eaf..c034dab` = 6 文件（5 `M` + 1 `A`）；不含 `tools/install-pb-agents.sh`、`.gitignore`、9 个执行角色、`roles/cdp-debug-skill/**`、`principles/**`、`oamp/**`、`docs/**`、`.pb-agents/**`、既有 `data/skill-optimization-v1.*` 历史文件 |
| T9-6 生效范围核对 | pass | `:244` 逐字未改且非 `+`/`-` 行；`grep '立即生效\|追溯适用'` = 0 命中 |
| T9-7 F13 形态核对 | pass | 见标准 1 第 31 条 |
| T9-8 编号区间口径 | pass | 变更文件内无"W1~W17 + `:114` 条目"类旧区间表述（`grep` = 0 命中）；引用一律 W1~W20 / S1~S10 |
| T9-9 核对动作只读 | pass | 本次 diff 未含任何新增核对报告文件；6 个改动文件与 T9 核对动作无关 |

**小计：12 + 8 + 5 + 5 + 6 + 5 + 5 + 14 + 9 = 69 pass / 0 fail / 0 partial / 0 blocked**

---

## 标准 3 · 文件范围

| 条目 | 判定 | 证据 |
|---|---|---|
| 写入面恰为 PR 声明的 6 文件（5 改 + 1 新建） | pass | `git diff --name-status`：`M` `.claude/skills/workflow-pb/SKILL.md`；`A` `.claude/skills/workflow-pb/data/skill-optimization-v1.12.0.md`；`M` `.claude/skills/workflow-pb/memory.md`；`M` `roles/workflow-pb/data/workflow-pb-changelog.md`；`M` `roles/workflow-pb/memory.md`；`M` `roles/workflow-pb/workflow-pb.md` |
| 无第 7 个被改动文件 | pass | `--name-only` / `--stat` 一致；`git diff --stat 15d5eaf..c034dab` 汇总 `6 files changed, 243 insertions(+), 59 deletions(-)`（44+40+1+29+1+128 插入；42+17 删除），与逐文件 numstat 吻合 |

---

## 标准 4 · 与 architecture v1.2.2 一致性

| 条目 | 判定 | 证据 |
|---|---|---|
| 条文措辞 / 落点与 §8 一致 | pass | 逐句比对 §8.1（规则 C）、§8.2（隔离边界声明）、§8.3（规则 F）、§8.4（角色定义来源与部署 + SKILL `:42` CRITICAL + Step 0 两步）、§8.5（规则 E；规则 D 对照 §3.3）：**语义与措辞逐字等价**，仅存在行内折行等排版级差异；规则 D 硬停块（`:176-181`）与 §3.3 文本块一致 |
| W1~W20 均已落地 | pass | W1 `:25`/`:65`；W2 `:137`/`:139`；W3 `:145`；W4 `:149`；W5 `:165`；W6 `:186`；W7 `:192`；W8 `:202`；W9 `:208`；W10 `:230`；W11 `:327`/`:333`；W12 `:343`/`:351`/`:353`；W13 `:384`；W14 `:440`/`:442`；W15 `:461`；W16 `:690`（保留）；W17 `:722`；W18（零改动，`## 何时更新本文件` 无 hunk）；W19 `:124`；W20 `:365` |
| S1~S10 均已落地 | pass | S1 `:42`；S2 `:44`；S3 `:98`；S4 `:136-137`；S5 `:222-230`；S6 32→2 行；S7 `:305`；S8 `:174`；S9 `:105`；S10 `:4`/`:30`/`:60` + `:31`/`:367` |
| §5.3 零改动清单未被误改 | pass | 清单内文件 / 节均无 hunk（标准 1 第 30 条、T9-3/T9-5）；SKILL 侧 S6 路径引用行按 r2 排除语口径归类为 S6，不计为零改动面被改 |

---

## 标准 5 · 工作区纪律（规则 A）

| 条目 | 判定 | 证据 |
|---|---|---|
| 本 PR 改动只存在于自身 worktree 分支 | pass | `git worktree list` 仅 2 个工作区：主工作区（`995fc59`，`iteration/0019-worktree-isolation-protocol`）与 `.pb-agents/worktrees/pr-001`（`c034dab`，`feat/0019-pr-001-workflow-pb-protocol`） |
| 主工作区无本 PR 范围改动 | pass | 主工作区 `git status --porcelain` 为空；`git diff --stat 15d5eaf..995fc59 -- roles/workflow-pb/ .claude/skills/workflow-pb/` 输出为空（该迭代分支上这两个路径相对基线零变化，即本 PR 内容尚未进入迭代分支） |
| 其它 worktree 无本 PR 范围改动 | pass | 无其它 worktree；pr-001 worktree 自身 `git status --porcelain` 为空（工作树干净 = 全部改动已提交在该分支上） |

---

## 标准 6 · 可施工性（自相矛盾 / 悬空引用 / 版本与章节名一致性）

| 条目 | 判定 | 证据 |
|---|---|---|
| 新增章节均存在且被引用指向正确 | pass | 规范侧被引用对象全在：`规则 C`（`:149`）/`规则 D`（`:165`）/`隔离边界声明`（`:208`）/`角色定义来源与部署`（`:230`）；SKILL 侧引用「规范 §规则 D」「规范 §角色定义来源与部署」「规范 §隔离边界声明」均命中规范实际章节名；SKILL 自身章节 `## § 角色文件来源与部署`（`:222`）存在，被 `:31`/`:98`/`:367` 正确引用 |
| 无指向已删章节的残留引用 | pass | 交付的 SKILL 内 `grep '角色文件路径与安装'` = **0 命中**；规范内同理 |
| 版本号一致 | pass | 规范 `:25` = 0.9.0；SKILL `:4`/`:30`/`:60` 与 v0.9.0 对齐，`:30` 为 `1.12.0（对应规范 workflow-pb v0.9.0）`；`grep 'v0\.8\.0' SKILL.md` = 0 命中 |
| 内部无自相矛盾 | pass | 规则 C（会话工作区检出迭代分支、不切 `main`）↔ 规则 D 判据 3 ↔ 隔离边界声明"会话工作区永不 checkout `main`" ↔ 收口 `git -C <仓库主工作区> checkout main` ↔ 规则 F 例外②：互相自洽；`git -C <会话工作区> checkout --detach` 发生在阶段 6 pass 之后的收口，与规则 C 的"阶段 1~6 期间"范围无冲突；SKILL Step 0 重编号后为 1~4，`grep '第 [0-9] 步'` = 0 命中（无按旧步号的交叉引用） |
| 新记录内部自洽 | pass | `skill-optimization-v1.12.0.md` 的一致性核对段所列 6 条均可由落地文件复核；其中"其余 6 条 CRITICAL"为**计数表述偏差**（见 D-2），不构成悬空引用或自相矛盾 |

---

## 汇总

- **standard 1（PR 文件 32 条）**：pass 32 / fail 0 / partial 0 / blocked 0
- **standard 2（tasks r2 T1~T9，69 项）**：pass 69 / fail 0 / partial 0 / blocked 0
- **standard 3~6（标准 3 = 2 条 / 标准 4 = 4 条 / 标准 5 = 3 条 / 标准 6 = 5 条，共 14 条）**：pass 14 / fail 0 / partial 0 / blocked 0
- **合计**：pass **115** / fail **0** / partial **0** / blocked **0**

## 偏差记录

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | tasks r2 **T5「验证方法」**：`grep -n 'git -C' roles/workflow-pb/workflow-pb.md` ⇒ **4 处命中（收口三步 + 前置游离）** | 实测 **6 行**：`:443`/`:444`/`:445`（收口三步）、`:447`（前置游离）——判据所指 4 处全部存在且形式正确；另有 `:194`（规则 F 的三类写操作点名，W7 必需）与 `:67`（v0.9.0 变更说明叙述，W1 必需） | **实现无需改动**；建议把该计数口径限定为"收口小节内 4 处"或把整文件口径改为 6（tasks r2 与 arch 同步）；属标准文档计数面不完备，非实现缺陷 |
| D-2 | tasks r2 **T8-13** / arch **§5.2 零改动**（并见 §1.1 基线表述「8 条 CRITICAL」）："**其余 6 条** CRITICAL … 一律无 diff hunk" | 基线实测 **9 条** `**CRITICAL`（`:36`/`:38`/`:40`/`:42`/`:44`/`:46`/`:48`/`:50`/`:52`）；本次仅改写 `:42`、新增 `:44`（S2），**其余 8 条**逐字不动、无 diff hunk（与"无 hunk"要求一致） | **实现无需改动**；建议修正 arch §1.1 / §5.2 与 tasks r2 T8-13 的计数（9 / 8），并同步新记录 `skill-optimization-v1.12.0.md:27`「未改变的部分」中的同一表述 |
| D-3 | tasks r2 **T8-13 / T9-4** 的排除语举例："（`Step 6`、`## § status.md 更新时机`、`## § history.md 更新时机` 内**各有 1 行**角色文件路径引用…）" | 按"更具体的 S6 枚举优先"，实际被改的 S6 路径行还包含位于零改动清单小节的 `Step 1~4`（`:141`）、`Step 5`（`:176`）、`Step 6`（`:190`）、`## § 对外协议·文档协议`（`:212`）、`## § status.md 更新时机`（`:341`）、`## § history.md 更新时机`（`:355`）、Resources（`:367`）、Safety（`:385-386`）——均为 arch §2.4 变更面表内明列（#10~#13、#19~#33）的改写目标 | **实现无需改动**（符合任务图自述的"更具体的 S6 枚举优先"口径）；建议把排除语举例补全为"凡 arch §2.4 在变更面表内列出的路径行"，避免下次按举例字面判为越界 |

## 下一迭代候选

1. **`SKILL.md:32` 变更历史索引行缺 v1.11.0 / v1.12.0 两条**：本次按 T8-14 有意保持不动（避免产生无法归类的 hunk）。建议下一迭代一次性补齐索引行（含 v1.11.0、v1.12.0），使 SKILL 索引与 `data/skill-optimization-v1.*.md` 实际文件一一对应。
2. **术语限定面的计数口径统一**：arch §2.3 正文曾出现 26 / 27 / 28 一类历史表述（本次已收敛为 §2.3 文末的 7 处唯一口径）；建议后续把 §2.3 正文的旧数字直接标注为"作废，见文末唯一口径"，避免下游再次引用过期计数。
3. **计数类判据的核对面显式化**：本 PR 出现的 D-1 / D-2 两类偏差同源——判据给的是"全文件 grep 计数"而意图是"某一区域的核对面"。建议下一迭代的 tasks 模板统一要求写明核对面（文件 / 章节 / 行区间）。

## 结论

**PASS**

- 依据：115/115 条判定为 pass，无 fail、无 partial、无 blocked；3 条偏差均为**标准文档计数/举例面**的不完备，实现侧零缺陷（每条偏差均已被同 PR 的其它必需条目解释）。
- 复验要点：规范 18 hunk 与 SKILL 30 hunk **全部可归类**到 W1~W20 / S1~S10；术语限定 7/7；S6 路径 32 行 → 2 行；`v0.8.0` 在 SKILL 内 0 命中；写入面恰为 6 文件；主工作区与其它工作区无本 PR 范围改动。
