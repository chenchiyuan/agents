# Gate 独立验证报告（阶段 4 → 5 入口，**第三轮**）

> **Gate 结论口径（本报告采用，明确声明）**：结论 = **PASS 仅当全部验证条目（本报告共 6 项）均为 pass**；出现任一 `partial` 或 `fail` 即判 **FAIL**（汇总计数照常列出）。
>
> **报告落点说明**：本报告按工作流路径写入 `docs/iterations/0019-worktree-isolation-protocol/clarifications/`，**未**写入角色默认的 `roles/verifier/data/`（或其在宿主的 `.pb-agents/roles/verifier/data/`）——原因：宿主 `.pb-agents/roles/` 为**只读部署副本**，且该目录的写入**不在本迭代变更面内**（见 `architecture.md` §5.3 零改动清单）。
>
> **独立性声明**：本次验证**未接收执行过程上下文**——未读取前两轮 Gate 报告（`verify-gate-stage4-20260912-191730.md` / `verify-gate-stage4-r2-20260912-192704.md`）的内容，未使用 `history.md` / `status.md` 中的裁定条目作为"设计意图"解释材料（仅按验证标准 4 的要求，对 `history.md` 的 2026-09-12 19:05:00 条目做**引用可查性核对**）。验证过程未修改任何被验证产出物，未跑测试，未执行 git 命令。

---

## 验证者身份（反射结果）

**独立 PR 规划审查者（发布／集成规划审查者），兼协议文本一致性审校者。**

反射依据：产出物类型 = "规范类迭代的 PR 拆分与依赖规划文档"（两份 PR 文件），验证标准覆盖 ①文件格式合规 ②PR 粒度 ③依赖图正确性 ④与架构基准（变更面清单）一致性 ⑤可施工性/可判定性。该组合要求的是"能从文件本身独立判定拆分粒度与依赖图是否成立、并能把每一条施工条目对回架构基准编号"的审查者，而非代码审查者或产品审查者。

**本次验证对象与标准复述**：对 `prs/` 下 2 份 PR 文件，逐项判定其七字段格式（含上下文摘要 Unicode 字符总数 ≤ 200）、PR 粒度三锚点、依赖正确性四子项、与 `architecture.md` v1.2.2 §5 变更面清单（定版 W1~W20 / S1~S10 / 零改动清单 / 惯例文件同步）及 §2.3 术语限定 7 处承载映射、§6.1 ADR 登记的一致性、交付前置条件，以及"PR 间文件范围无重叠"是否被不可避免违反。

---

- **产出物**：`docs/iterations/0019-worktree-isolation-protocol/prs/pr-001-workflow-pb-workspace-isolation-protocol.md`（93 行）、`docs/iterations/0019-worktree-isolation-protocol/prs/pr-002-cross-iteration-d2-registration.md`（53 行）
- **验证标准来源**：主 agent 委托内联的 6 条标准（阶段 4 → 5 入口 Gate，第三轮复验）
- **验证日期**：2026-09-12

---

## 逐项判定

### 1. 格式规范 —— **pass**

**1.1 七字段含且仅含**：

| PR 文件 | `## ` 级字段（按出现顺序） | 与规范「PR 文件格式规范」（`roles/workflow-pb/workflow-pb.md:145-216`）比对 |
|---|---|---|
| pr-001 | 上下文摘要(`:3`) / 涉及功能点(`:7`) / 文件范围(`:24`) / 验收标准(`:35`) / 参考资料(`:70`) / depends_on(`:83`) / batch(`:89`) | 7/7，名称与顺序**完全一致**；无第 8 个 `##` 字段 |
| pr-002 | 上下文摘要(`:3`) / 涉及功能点(`:7`) / 文件范围(`:11`) / 验收标准(`:20`) / 参考资料(`:31`) / depends_on(`:43`) / batch(`:49`) | 7/7，同上 |

- 规范要求"每个 PR 文件必须包含以下七个字段，缺一不可"：两文件均满足，无缺项、无多余字段（实测 `## ` 级标题计数 = 7，无其它 `## ` 标题）。
- `depends_on` 无依赖写法合规：规范 `:209-210`「无依赖时写 `（无）`」——两文件均为 `（无）`（pr-001 `:85`、pr-002 `:45`）。
- `batch` 值合规：规范 `:213-216`，两文件均为 `1`（可读的粗粒度分组标记）。
- `涉及功能点` 形态与既有惯例一致：`0008-iteration-branch-workflow/prs/pr-001-*.md` 已用 `F01：描述` 加注形式；pr-001 的 `F01（…）` 属同一惯例，不构成格式偏差。

**1.2 上下文摘要 ≤ 200（主 agent 裁定口径 = Unicode 字符总数，含标点与空格，不分中英文）**：独立实测（Python `len()`，非引用 PR 自称值）

| PR | 摘要字符数 | 判定 |
|---|---|---|
| pr-001 | **187** | ≤ 200 ✔（与 PR 自报 187 一致） |
| pr-002 | **198** | ≤ 200 ✔（与 PR 自报 198 一致） |

- 证据位置：pr-001 `:5` 单行；pr-002 `:5` 单行（摘要均为单行，无换行计入口径争议）。

### 2. PR 粒度判断（逐 PR 三锚点） —— **pass**

**pr-001-workflow-pb-workspace-isolation-protocol**

- **逻辑原子性：pass** —— 该 PR 只有一个可陈述的目标："把『会话—工作区绑定』协议落到规范与 SKILL 文本（v0.9.0 / v1.12.0）并同步两侧惯例留痕"。其承载面 F01~F14 属**同一协议修订**（架构 §5.1 的 W1~W20 与 §5.2 的 S1~S10），不是多个不相关需求拼盘；文件范围（`prs/pr-001…:24-33`，6 个文件）全部服务于该目标，且其中 4 个是**同一版本的惯例留痕**（changelog / memory ×2 / skill-optimization 新文件，架构 §5.4）。
- **可审查性：pass** —— 验收标准把施工面切成 **32 条**可独立勾选条目（`:37-68`），每条带行号锚点（`:114` / `:127` / `:129` / `:135` / `:258` / `:277` / `:303` / `:383` 等）或可直接执行的判据（`grep -n 'v0\.8\.0'` = 0 命中；逐 hunk 归类到 W/S 编号）；审查者不需要读通全文即可逐条核对。规模（6 文件 / 约 30 个变更条目）处于单 PR 可审查上限，但拆分点不存在——SKILL 侧改写后的文本直接引用规范**新增**章节名（`:57` S1 与 `:59-60` S3/S5 引用 `§规则 D` / `§角色定义来源与部署`），先落地的一侧必然留下悬空引用；F08 验收 3 的判据本身是跨文件判据（PR `:24-33` 末段）。
- **独立性：pass** —— `depends_on`（无）+ 文件范围与 pr-002 的 2 个文件**真交集为空**（逐文件核，见第 3 项 ①②③）；规范 `### 约束生效范围`（`workflow-pb.md:141`：仅从下一个迭代起生效）使本 PR 的规则对 0019 自身执行不产生时序耦合。

**pr-002-cross-iteration-d2-registration**

- **逻辑原子性：pass** —— 单一目标：F15 的 D-2 承接登记与跨迭代留痕（两处 `status.md` 同格式改写 + 编号口径行，`:20-29`）；不含修复（`:27` 显式排除 `project_id` 相关改动），不含 0017 其它悬空候选。
- **可审查性：pass** —— 8 条验收标准（`:22-29`）每条可直接判定，且对**最易失控的一面**（改动 `status.md` 这个主线持续维护的文件）给出闭集约束："diff 只落在 §待确认项 的 D-2 段，该文件其它 hunk 数必须为 **0**"（`:28`）与"合并前以当时迭代分支重基线、冲突时以主线版为基础只重放该单段"（`:29`）。
- **独立性：pass** —— `depends_on`（无）；与 pr-001 的 6 个文件无交集；文本上不引用 pr-001 新增的任何章节名（逐条核对 pr-002 全文引用的章节：架构 §5.3/§5.4/§6.1/§7/§8.7/§10 R-6、`prd/F15`、`demand.md`、`history.md`、两处 `status.md`、`progress.md:66`——均非 pr-001 的产物），故可独立合并、独立验收。

### 3. 依赖正确性 —— **pass**

**① `depends_on` 声明的可核实证据（空集是否合理）：pass**

- 两文件均为空集（pr-001 `:85`、pr-002 `:45`），且均给出**可核对的理由**而非空口：pr-001 `:87`（零改动面文件不产生 PR，也不产生依赖边）、pr-002 `:47`（无文本耦合 + 文件无交集）。
- 独立核真：
  - pr-001 声明的零改动面（`tools/install-pb-agents.sh`、仓库根 `.gitignore`、9 个执行角色文件、`.pb-agents/**`、`oamp/**`）**确实不出现在任何 PR 的文件范围内**（逐条核对两 PR 的文件范围段）→ pr-001 不因这些文件被其它 PR 改动而产生依赖边。
  - pr-002 的"无文本耦合"成立：其验收标准与备注中不出现 pr-001 新增的章节名（`§规则 C~G` / `§隔离边界声明` / `§角色定义来源与部署`）或其载体条目编号（W/S 编号），只引用"规则 F 的例外为闭集两项"这一**概念**作为不开例外的理由（`:53` 备注 ④）——该概念不要求在 pr-002 施工时先存在（且按 `workflow-pb.md:141` 生效范围，新规则对 0019 自身不生效）。
  - 规范对 depends_on 的证据要求是"代码级证据（共享符号/接口/文件）"（`workflow-pb.md:203-204`）：两 PR 无共享文件、无共享新增符号 → 空集**正确**，非漏填。

**② 依赖图无环：pass** —— 节点 = 2，边 = 0，无环（不可构造环）。

**③ PR 间文件范围无重叠（逐文件真交集）：pass**

- pr-001（6）：`roles/workflow-pb/workflow-pb.md`、`roles/workflow-pb/data/workflow-pb-changelog.md`、`roles/workflow-pb/memory.md`、`.claude/skills/workflow-pb/SKILL.md`、`.claude/skills/workflow-pb/data/skill-optimization-v1.12.0.md`、`.claude/skills/workflow-pb/memory.md`
- pr-002（2）：`docs/iterations/0017-project-workspace/status.md`、`docs/iterations/0019-worktree-isolation-protocol/status.md`
- 两两真交集 = **∅**（8 个路径去重后仍为 8，无同路径、无目录/文件包含关系）。

**④ F01~F15 覆盖（无遗漏无多余）：pass**

- `prd/` 实测恰为 15 张卡（`F01-session-workspace-form.md` … `F15-d2-registration-trace.md`）；`prd.md:23-37` 的功能索引亦为 F01~F15。
- 引用侧：pr-001 `涉及功能点` = F01~F14（14 项，逐项与 `prd.md:23-36` 的卡片名对读一致）；pr-002 = F15（`:9`）。**并集 = F01~F15 全覆盖**，无遗漏；无 prd 之外的 F 编号（pr-002 正文顺带出现 F09 是"规则 F 例外闭集"的引用，不构成第二次认领）。

### 4. 与 `architecture.md` v1.2.2 的一致性 —— **pass**

**4.1 逐 PR 核对文件范围/验收标准 与 §5 变更面清单**

| 架构条目 | PR 承载 | 一致 |
|---|---|---|
| §5.1 W1（头部版本 0.8.0→0.9.0 + `## v0.9.0 变更说明` 四段式）| pr-001 `:38` | ✔（实测 `workflow-pb.md:25` = `**版本**: 0.8.0`；既有 `## v0.7.0`(`:45`)/`## v0.8.0`(`:55`) 变更说明段落位） |
| §5.1 W2/W3（规则 A/B 术语限定，**判据行逐字不动**）| pr-001 `:39` | ✔（实测 `:127`/`:129`/`:135` 为待限定行；`:131`/`:137` 为判断方式行，PR 明确要求其逐字相同） |
| §5.1 W4 规则 C（5 项内容 + 三条判据 + 落点与建立时点）| pr-001 `:40`（①~⑤ 与 §8.1 逐项对读） | ✔（插入位置"规则 B 之后、`### 约束生效范围`（`:139`）之前"与实测行号一致） |
| §5.1 W5 规则 D | pr-001 `:41` | ✔（三判据、发现即停、硬停形态不复用 `⛔ 需要你的决策`、约束主体、子 agent 归属，均与 §3.3/§8.5 一致） |
| §5.1 W6 规则 E | pr-001 `:42` | ✔（含编号口径 = 「编号段」、`feat/{迭代编号}-pr-{NNN}-{slug}`、base/合并目标不变、不追溯、`git branch --list` 判据 = §8.5 规则 E 原文） |
| §5.1 W7 规则 F | pr-001 `:43` | ✔（三类写操作 / "不可能被 git 强制" / **闭集例外两项** / 先备份后操作，与 §8.3 一致） |
| §5.1 W8 规则 G | pr-001 `:44` | ✔（并列不替代声明；不含清理动作；既有现场保留条款 `:304`/`:481`/`:485` 逐字不动——三处实测存在） |
| §5.1 W9 隔离边界声明 | pr-001 `:45` | ✔（覆盖三项 / 不覆盖三项 + 逐项负面约束 + 同一分支硬约束 / 非 git 四项 + 两条约束 / 已知代价；含"不出现三类越界承诺"的否定判据 = §8.2 自查项） |
| §5.1 W10 角色定义来源与部署 | pr-001 `:46` | ✔（`git ls-files --error-unmatch` 判据 / 单次解析 / 两场景 / 零动作含义 / 下游不算运行前提 / 只读约束 = §8.4 原文要素） |
| §5.1 W11 启动工作流 | pr-001 `:47` | ✔（步骤 1 前补校验引用、**不新增步骤号**（§7 Q-5）、4.5 保留位置与时机且"创建迭代分支"改写为声明已完成、保留 `status.md` 字段写入） |
| §5.1 W12 brief 构建（`:236`/`:244`/`:246` 路径替换；`:258` 除外）| pr-001 `:48` | ✔（三处行号实测命中；"除 §2.3 枚举的 7 处术语限定外其余逐字不变"= §5.1-W12 范围声明 v1.2.2 原文；归属 **F14 验收 1** 与 §12 的归因更正一致，未见前版误引 F08 验收 2） |
| §5.1 W13 阶段 5 步骤 2 | pr-001 `:49` | ✔（`:277` 实测含 `<path>`/`<pr分支名>`；base 与解锁判据、槛位算法逐字不变 = §5.1-W13） |
| §5.1 W14 收口 | pr-001 `:50` | ✔（三步 `git -C <仓库主工作区>` + 第 3 步前前置游离 `git -C <会话工作区> checkout --detach`；实测 `:333-338`；动作顺序/触发时机/commit message/冲突上报不变；不引入自动化合并） |
| §5.1 W15 文档路径协议 | pr-001 `:51` | ✔（`:348` 实测为 `## 文档路径协议`） |
| §5.1 W16 角色读取指南 | pr-001 `:52` | ✔（实测 `:579` = `1. **工作流文件路径**：roles/workflow-pb/workflow-pb.md`，真源写法） |
| §5.1 W17 原则引用词 | pr-001 `:53` | ✔（实测 `:611` 含"（worktree 隔离、PR 文件前置）"） |
| §5.1 W19 阶段定义·阶段 5 行（`:114`）| pr-001 `:55`（显式写 **W19**） | ✔（实测 `:114` = 「逐 PR 在独立 worktree 分支…」；仅限定 `worktree` 一词、四列不动） |
| §5.1 W20 brief 阶段 5 模板行（`:258`）| pr-001 `:56` | ✔（实测 `:258` = `worktree 分支：{分支名}`，落在 §brief 构建节内）——**载体以描述式指代（"v1.2.2 新增/扩写的 W 条目"），未写字面 `W20`**，见偏差记录 #1 |
| §5.2 S1~S10 | pr-001 `:57-65`（S1 / S2 / S3-S4 / S5 / S6 / S7 / S8 / S9 / S10 逐条） | ✔（逐条对读：S10 的 `:4`/`:30`/`:58` 实测三处全部命中 `v0.8.0` 且**全文仅此三处**；S6 的行号枚举 `:31/:50/:96/:97/:133/:141/:176/:186/:212/:220/:224/:228/:238-366/:383/:384` 实测逐条命中；S7 `:303` 实测 = `worktree 分支：{分支名}`；S2 措辞与 §5.2-S2 逐字同） |
| §5.3 零改动清单 | pr-001 `:30-33` + `:66` | ✔（零改动面被明确排除，且 `:66` 的 diff 判据声明 diff 中不得出现 `tools/install-pb-agents.sh`、`.gitignore`、9 个角色文件、`principles/**`、`oamp/**`）——枚举未含 `## 何时更新本文件`，见偏差记录 #5 |
| §5.4 惯例文件同步 | pr-001 `:26-29`（changelog v0.9.0 / `roles/workflow-pb/memory.md` / 新建 `skill-optimization-v1.12.0.md` / `.claude/skills/workflow-pb/memory.md`）；pr-002（0017 + 0019 两处 status） | ✔（实测 `data/workflow-pb-changelog.md:3` 为 v0.8.0 四段式条目 = 追加体例先例；`roles/workflow-pb/memory.md:8` 为 `- **v0.8.0（…）**：…` 索引行 = 追加体例先例；`.claude/skills/workflow-pb/memory.md:5` 为 v1.11.0 索引行 = 追加体例先例；0018 一侧按 ADR-3 只登记不写入，与 §5.3 末行/§5.4/§8.7 收窄一致） |
| §5.5 术语限定 7 处 → 承载条目映射 | pr-001 `:54` + `:55` + `:56` + `:39` + `:53` + `:62` | **6/7 显式命名，1/7 描述式指代**：`:114`→W19 ✔显式；`:127`/`:129`→W2 ✔（`:39`）；`:135`→W3 ✔（`:39`）；`:611`→W17 ✔（`:53`）；SKILL `:303`→S7 ✔（`:62`）；`:258`→W20 **未写字面编号**（描述式，唯一可推定），见偏差记录 #1 |

**4.2 引用裁定编号的可查性（ADR-1~ADR-4）**

- pr-001 `:93` 备注 ① 引用 **ADR-1 / ADR-2**（同处 `:55` / `:65`）；pr-002 `:16` / `:24` / `:28` / `:29` / `:40` / `:53` 引用 **ADR-3 / ADR-4**，并给出指针 = `history.md` 的"2026-09-12 19:05:00 · 调度决策 · Gate确认"条目。
- `architecture.md` §6.1 实测存在 ADR-1~ADR-4 四条登记，结论与 pr-001/pr-002 的引用语义逐条一致：ADR-1 = `:114` 术语限定 + 零改动清单收窄（= §5.1-W19）；ADR-2 = SKILL 三处版本同步（= §5.2-S10）；ADR-3 = 0018 由既有提交 `89182b7` 承载、**不开跨工作区例外**、pr-002 只做 0017 + 0019 两处；ADR-4 = pr-002 只改 `0019/status.md` 的 D-2 单段 + 合并前重基线。
- `history.md:197-201` 实测存在该 19:05:00 条目，四项裁定的**内容**与 §6.1 一致（该条目以 `Q-1~Q-4` 记名，`Q-n` → `ADR-n` 的映射写在 `architecture.md` §7 顶部编号空间说明中）。**内容可查、编号需经映射**，见偏差记录 #3。
- `demand.md`（需求基线）核对不越界：`N2`（`:218`）明确"D-2 由单独的小迭代承接、三处 status 互相引用留痕、本迭代不承接"，pr-002 的验收面与之同向；`N1`（`:217`）"不改阶段序列 / 推进条件 / 并发槛位算法 / PR 文件七字段格式"未被 pr-001 触碰（其对阶段定义表的唯一改动是 `:114` 的术语限定，见 ADR-1 的收窄读法）。

### 5. 交付前置条件（可判定性 / 阻塞项） —— **pass**

- **pr-001：32 条验收标准全部可直接执行判定**（`:37-68`）。判据形态 = ①逐项文本比对（带行号锚点）②可执行命令（`grep -n 'v0\.8\.0'` 改后 0 命中；`grep -n '\.pb-agents/roles'` 残留语境核对；`git diff <v0.8.0 基线 @ 9ede9ea>` 逐 hunk 归类到 W/S 编号）。其中 2 条含条件句"若 v1.2.2 尚未落盘：以 v1.2.1 …为准"（`:37`、`:70` 参考资料）——**该条件已不成立**（`architecture.md` 头部实测版本行 = **1.2.2**，且 §5.1 表后含"定版 W1~W20"段、§5.5 含 7/7 映射段、§6.1 含 ADR 登记），条件句为死分支，不构成不可判定。
- **pr-002：8 条验收标准全部可直接执行判定**（`:22-29`）。唯一与迭代外事实相关的条目（`:24` ADR-3 外部证据 = 分支 `iteration/0018-chat-agent-subagent-protocol` 的提交 `89182b7` 的 C-3 行）**已自我限缩**为"只登记该事实、不读取/不校验该分支文件的其它内容、不写入"，其可判定形态 = 核对**本 PR 文本是否如实登记该事实**（PR 文本已登记 ✔）；分支侧文件在本迭代不可达（实测 `docs/iterations/` 下**无** `0018-chat-agent-subagent-protocol` 目录；`progress.md:66` 记录该目录仅存在于其分支）——属**架构已裁定（ADR-3）的既成事实**，不是待裁决项。
- **无必须由主 agent 先裁决才能施工的阻塞项**：ADR-1~ADR-4 四项均已落盘（`architecture.md` §6.1 + `history.md:197-201`），PR 对该四者的处置与裁定结论一致；pr-002 的 `status.md` 争用问题已由 ADR-4 给出可执行程序（重基线 + 冲突时只重放 D-2 单段）；pr-001 的阶段定义表改动已由 ADR-1 收窄读法授权（不影响 F14 验收 2 的四列内容）。
- **已知的施工期实测风险（非阻塞、非本 PR 缺陷）**：`architecture.md` §10 R-2（嵌套 worktree 未在本机实测，阶段 5 首个 PR 必测）——PR 未对其作出承诺或规避，属架构登记项。

### 6. 格式规范的硬冲突检查（「PR 间文件范围无重叠」是否不可避免被违反） —— **pass**

- 结论：**未被违反，也不存在"不可避免被违反"的结构性原因**。两 PR 的 8 个文件路径互不相交（见第 3 项 ③），不存在"同一文件必须被两个 PR 各改一部分才能成立"的情形。
- 需要区分的是**另一种冲突**：pr-002 改 `docs/iterations/0019-worktree-isolation-protocol/status.md`，而该文件由**主线（主 agent）持续维护**——这是 **PR × 主线** 的并发写入，不属于"PR 间文件范围无重叠"的判据范围。该情形已被架构显式裁定（§6.1 ADR-4 + §7 编号空间说明）并落到 pr-002 的验收标准（`:28` 单段约束、`:29` 重基线程序），故不构成硬冲突。

---

## 汇总

- **pass: 6 项 / fail: 0 项 / partial: 0 项 / blocked: 0 项**
- 附加统计：偏差记录 **8 条**（均为"规格表述 vs 实现实际"的表述级/口径级差异，**无一影响上列 6 项标准的成立**）；下一迭代候选 **5 条**。

## 偏差记录

| # | 规格／文档描述 | 实现实际 | 建议处理 |
|---|---|---|---|
| 1 | `architecture.md` §5.1 表后「变更面清单定版编号区间与全清单」段与 §12 均称"由 pr-planner 在 PR-001 中同步为 **W1~W20**"；§5.5 要求 `:258` → **W20** 唯一承载 | pr-001 全文**不出现** `W20` 字样，也不出现 `W1~W20`（实测 `W20` 计数 = 0；改用 "W1 至**最终编号**／最终编号区间"（`:26` / `:37` / `:72`）与"v1.2.2 **新增 / 扩写的 W 条目**"（`:26` / `:37` / `:56`）这类描述式指代） | **不返工**：`:258` 的载体在 v1.2.2 中唯一（W20），语义对应成立。阶段 5 dev 应按 §5.5 直接归 **W20**；阶段 6 逐处核对术语限定 7 处时按 §5.5 映射表判读，不要求 PR 文本字面含编号。若追求字面一致，可在 PR 修订时把 `:56` 的"v1.2.2 新增／扩写的 W 条目"补写为"`§5.1-W20`"（一个词，非阻塞） |
| 2 | pr-001 `:61` 的 S6 验收判据："改动后 `grep -n '\.pb-agents/roles' SKILL.md` **仅剩**『可选分发手段／下游业务项目副本』语境（**预期 ≤2 处**，架构 R-4 判据）" | 与同一 PR 的 S1/S5 要求张力：S1 的改写措辞（architecture §8.4）在 `:42` 单行即含 **2 处** `.pb-agents/roles/`；S5 要求保留"下游 = `.pb-agents/roles/`"场景 + `:222`/`:228` 的安装与只读约束保留事实描述 ⇒ 改动后合理残留 ≥3~5 行、>2 处。实测改动前为 35 个 token／**32 行**（架构 §12 的"32"= `grep -c` 行数口径） | **不返工**：以判据的**语境要求**为准（剩余均属"下游副本／可选分发"语境即通过），**不按数量阈值判 fail**；建议阶段 6 报告明确写出实际残留计数与逐行语境归类。若需保留数字，应由 architect 在下一轮把"≤2"修正为与 S1/S5 相容的值（如"≤5 行"） |
| 3 | pr-001 `:93` 备注 ① 与 pr-002 `:53` 备注 ①："ADR-1~ADR-4 **原文见** `history.md` 的 2026-09-12 19:05:00 条目" | `history.md:197-201` 该条目以 **`Q-1~Q-4`** 记名，全文**不含** `ADR-` 字面标签；`Q-n → ADR-n` 的映射写在 `architecture.md` §7 顶部编号空间说明与 §6.1 的"落点"列 | **不返工**：内容逐条一致（ADR-1↔Q-1①、ADR-2↔Q-2①、ADR-3↔Q-3②、ADR-4↔Q-4(a)），可查性经 §6.1/§7 映射成立。建议在阶段 6 的引用可查性核对中记录"按映射可查"，或将 PR 备注的指针改写为"`history.md:197-201` 的 Q-1~Q-4（= `architecture.md` §6.1 的 ADR-1~ADR-4）" |
| 4 | pr-001 `:70` 参考资料列 `roles/workflow-pb/workflow-pb.md` 分区区间 "`:329-344` 合并进 main" | 实测 `## 迭代分支合并进 main` 起于 **`:333`**（`:329` 属上一小节 `### 终止工作流` 区；`:333-344` 与 architecture §5.1-W14 的位置列一致） | **不返工**：纯定位区间起点偏早 4 行，不影响任何验收判定。阶段 5/6 以 `:333-344` 为准；若 PR 有其它修订，可顺带改为 `:333-344` |
| 5 | pr-001 `:66` 零改动面核对的枚举清单（"规范零改动清单（… 验证目标、状态追踪协议、历史记录协议、`### 约束生效范围`、v0.2.0~v0.8.0 变更说明、需用户决策、终止工作流、阶段回退、可观测性 …）"）未列 `## 何时更新本文件` | `workflow-pb.md:615` 实测存在 `## 何时更新本文件`，且 `architecture.md` §5.1 的 **W18** 行明确把它列为**零改动**行 | **不返工**：该节属 W18（零改动），阶段 6 逐条核对零改动面时应以 §5.1-W18 为准把该节纳入；建议在阶段 6 报告的零改动清单中显式补入该节，避免以 PR 枚举为唯一清单时漏项 |
| 6 | `prd/F15` 验收 3（产品维度）："三处文本中 **`D-2` 的指代唯一**…*判定*：逐处读 `D-2` 的出现位置与所指" | pr-002 `:26` 把判据收窄为"**本次改动的 D-2 段落内**，凡跨迭代引用一律带迭代号；其余既有裸 `D-2` 出现位置（实测 `0019/status.md:15` / `:32` / `:52` / `:60`）不属于本 PR 判据范围且不改动" | **不返工**（与 `architecture.md` §8.7"`D-2` 在**跨迭代引用处**一律带迭代号、无两义"的读法一致；`status.md:35`（D-3 段）虽含 `D-2` 字样但均带迭代限定词，不构成两义）。建议阶段 6 按 §8.7 读法核对两处改动文本，不按 F15 卡的字面"三处逐处"扩大判据面 |
| 7 | 规范对 `batch` 的定义（`workflow-pb.md:213-214`）："人工速览用的粗粒度分组标记，**不是调度依据**"；对 `depends_on` 的定义（`:203-207`）："真正依赖的其他 PR 文件名列表" | 两 PR 的 `batch` 字段（pr-001 `:89-93`、pr-002 `:49-53`）与 `depends_on` 字段（pr-001 `:83-87`、pr-002 `:43-47`）内各带一段长篇 `>` 备注（含 ADR 登记、字数口径、粒度论证、例外边界），内容明显超出字段定义用途 | **不返工**（信息有追溯价值，且这些内容若移出将构成第 8 个 `##` 字段，反而违反"含且仅含七字段"）。知悉即可；若主 agent 希望收紧字段语义，应在**下一迭代**以规范条款形式统一（本迭代属零改动面，不得顺手改） |
| 8 | "**32 处** `.pb-agents/roles` 引用"（pr-001 `:61`；architecture §5.2-S6 / §12） | 实测 `SKILL.md`：**32 行**含该串，但该串**出现 35 次**（部分行含 2 次，如 `:224`）。架构 §12 已注明"32 = 实测 `grep -c`"（按行计数） | **不返工**：口径已由架构 §12 确定为**按行计**，PR 与该口径一致。建议阶段 5/6 核对时统一使用 `grep -c`（行数）而非 `grep -o \| wc -l`（次数），并在报告中写明所用口径 |

## 下一迭代候选

1. **把 `## 何时更新本文件` 显式纳入零改动清单**（`architecture.md` §5.1 零改动段 + 阶段 6 核对清单；当前仅 W18 行单点承载，见偏差记录 #5）。
2. **修正 S6/R-4 的残留数量阈值**（把"预期 ≤2 处"改为与 S1/S5 相容的判据，或直接改为纯语境判据；见偏差记录 #2）。
3. **嵌套 worktree 实测**（`architecture.md` §10 R-2：PR worktree 落在会话工作区内，阶段 5 第一个 PR 必测创建 → `git worktree list` → `remove`；失败则回退"平铺 + 例外"备选）。
4. **0018 恢复执行时的核对项**：`0018/status.md` 的 C-3 行是否含"编号口径行"（`<迭代号>/D-<n>` 写法），以及三处引用链在 0018 侧恢复后的最终一致性（`prd/F15` 架构落地段与 pr-002 `:53` 备注 ④ 已登记该待核项）。
5. **规范内 `.pb-agents/` 残留语境的口径登记**（若阶段 5 施工后 `SKILL.md` / `workflow-pb.md` 中仍有多处 `.pb-agents/roles/` 出现，建议在 `skill-optimization-v1.12.0.md` 中列出"哪些是下游副本语境"的清册，避免后续迭代把"零残留"误读为验收目标）。

## 结论

**PASS**

理由：6 项验证标准**全部为 pass**（fail 0 / partial 0 / blocked 0）——两 PR 文件格式合规（七字段齐备、摘要 187/198 ≤ 200 Unicode 字符）、粒度三锚点逐 PR 成立、依赖图正确（空集有据、无环、8 个文件真交集为空、F01~F15 全覆盖无遗漏无多余）、与 `architecture.md` v1.2.2 的 W1~W20 / S1~S10 / 零改动清单 / 惯例文件同步 / 术语限定 7 处承载映射 / ADR-1~ADR-4 登记逐条一致、验收标准可执行判定且无待裁决阻塞项、"PR 间文件范围无重叠"未被违反。8 条偏差记录均为表述级/口径级差异，**不阻塞阶段 5 开工**，可在阶段 5 施工时按建议处理即时消化，无需回退阶段 4。

**回报（精简）**：报告路径 `docs/iterations/0019-worktree-isolation-protocol/clarifications/verify-gate-stage4-r3-20260912-193332.md`；结论 **PASS**；fail **0** 条；partial **0** 条；偏差记录 **8** 条。
