# 澄清记录 · prd-round-2（迭代 0019-worktree-isolation-protocol，阶段 2 第二轮修订）

**caller**: prd
**时间**: 2026-09-12
**本轮性质**: 阶段 2 **裁决落盘**。记录 `clarifications/prd-round-1.md` 转呈的 **9 项 MI** 的「MI → 推荐 → 用户裁决 → 落定位置」，以及**主 agent 对 3 项体例缺口的裁定与落点**。本轮**未新增任何 MI**、**未新增任何功能点或 W 项**。
**输入（用户裁决，原文转达）**：
- **组 A（MI-01 / MI-02 / MI-03 / MI-05）**：「**全部按推荐**」
- **组 B（MI-04 / MI-06 / MI-08 / MI-09）**：「**全部按推荐**」
- **组 C（MI-07）**：「**载体不限（推荐）**」

**结果**: **九项全部按推荐落地，无一项被驳回**；卡内 9 张受影响卡（F01 / F02 / F03 / F06 / F07 / F08 / F09 / F11 / F12）的 `[model_inferred MI-xx]` 标记**已全部转为 `[user_confirmed MI-xx]`**，`prd.md` 状态改为「阶段 2 已收敛」、版本递增至 **v0.2.0**。**`model_inferred` 残留数 = 0**（`prd.md` 与 `prd/**` 中仅作为术语出现在文档状态与来源说明中）。

---

## 一、逐条裁决落定（MI → 推荐 → 用户裁决 → 落定位置）

| 编号 | 转呈问题（prd-round-1） | 推荐值 | 用户裁决 | 落定位置（卡 · 验收条目） | 卡内落地措辞要点 |
|---|---|---|---|---|---|
| **MI-01** | 启动校验不通过时"硬停"的时点：发现即停 vs 允许先做只读分析 | 发现即停 | **按推荐** | F03 · 验收 3 | 标题改为「**不满足即硬停（发现即停）**」；正文写明"会话**发现即停**——不进入任何阶段工作、不产生任何写入，随即停止推进，并输出创建命令" |
| **MI-02** | 阶段 5 父子层判定口径：子 agent 的"本会话工作区" = 被派发时所在的工作区（该 PR worktree）vs 主 agent 会话工作区 | 以写入者实际被派发的工作区为准 | **按推荐** | F03 · 验收 4；F07 · 验收 3；F09 · 验收 2（判定句） | F03 验收 4 保留"子 agent 在其被派发时所在的工作区内工作、不因此被判定违反启动契约"；F07 验收 3 保留"可判定的违规形态"（不同目录 + 不跨越 PR 生命周期）；F09 验收 2 判定句补注"阶段 5 的子 agent 以『其被派发时所在的工作区』为准" |
| **MI-03** | "每迭代一个工作区"与跨迭代复用的关系 | 必须换工作区（阶段 1~6 不得跨迭代混用） | **按推荐** | F02 · 验收 4 | 「工作区按迭代划分、不跨迭代复用」直接生效；判定 = 同一工作区跨两个迭代即不通过 |
| **MI-04** | E7"分支名含迭代 ID"的判据口径：字符串包含即可 vs 要求统一格式 | 字符串包含该迭代编号即可 | **按推荐** | F06 · 验收 2（判定句） | 判定句注为"「含」= 分支名中**字符串包含该迭代编号**即可、**不要求统一格式**（命名形态留 `[架构待填]` T-03）" |
| **MI-05** | E1~E4 的判定样本来源与时点（N3 使本迭代不产生真实并行会话记录） | 构造样本（阶段 6 同仓建两个工作区）；真实并行样本留下一篇迭代 | **按推荐** | F01 · 验收 3~4；F02 · 验收 2~3；F03 · 验收 1（判定句） | F01 验收 3 写明"判定样本 = 本迭代阶段 6 按新规范在同一仓库构造的两个工作区"、正文由"真实并行"改为"并行（构造样本亦可）"；F01 验收 4 与 F02 验收 2~3 引用同一样本；F03 验收 1 判定句补注样本来源 |
| **MI-06** | "零动作"对下游（业务项目）场景的承诺边界 | 只覆盖"角色定义真源随检出即得"的场景（本仓库）；下游"先分发角色文件"不算运行前提 | **按推荐** | F08 · 验收 5 | 验收 5 补写"**零动作承诺只覆盖『角色定义真源随检出即得』的场景（本仓库）**——下游业务项目仍可有『先分发角色文件』一步，但该步骤**不算本协议的运行前提**" |
| **MI-07** | E13 判定用的"执行记录"载体口径 | 载体不限（会话日志 / `history.md` / 提交 diff / progress 报告均可）；不新增登记义务 | **按推荐（载体不限）** | F09 · 验收 2 | 验收 2 写明"**载体不限**——会话日志、`history.md`、提交 diff、progress 报告均可，只要『写入动作 → 目标路径』可核对；**不新增登记义务**" |
| **MI-08** | W10 约束①中"真实服务"的范围 + 是否要求改默认端口值 | 范围 = 会话实际启动的长驻服务；不要求修改默认端口值 | **按推荐** | F11 · 验收 2 | 验收 2 注明"『真实服务』= 会话实际启动的长驻服务；**不要求修改默认端口值**"；与 F11 边界（不改默认端口、不触及 `oamp/**`，N7）一致 |
| **MI-09** | E10 隔离边界声明的落点范围：规范一处为真源 vs 两处齐写 | 规范一处为真源，SKILL 按"不重复定义"原则只引用 | **按推荐** | F12 · 验收 6 | 验收 6 保留"声明以**规范文件为真源**、SKILL **引用**而不重复定义"；判定 = 两处各写一份完整声明即不通过 |

**九项均按推荐值原样落地，无一项被改写或驳回**——故 `prd-round-1.md` 中 9 项「推荐值」列即最终生效口径，卡内无任何因驳回而产生的替代读数。

---

## 二、主 agent 对 3 项体例缺口的裁定与落点

> 这 3 项**不是 MI**（它们是需求层的**编号缺口**，已在 `prd-round-1.md` §三与 `prd.md`「疑问与越界」1~3 留痕）。本轮由**主 agent 裁定**，三项均**采纳 prd 的处置与理由**（沿用既有体例先例），回退口继续保留。

| # | 缺口 | 主 agent 裁定 | 落点 | 回退口（继续保留） |
|---|---|---|---|---|
| 1 | **E10 在 W 清单中无对应条目**（依据 P-9 ① / P-10 ①，见 `demand.md` §4 / §5 / §7） | **保留 F12〔声明项〕** | 索引「功能点索引」F12 行、「需求追溯·E 条款」E10 行、「包含」段、「疑问与越界」1 | 若后续认为应回 demand 补一条 W 或降级为全局边界 → 删 F12 + 同步索引的 E→卡映射与「包含」段，**其余 14 张卡与本文件其余结论不受影响** |
| 2 | **E5 / N4 同样无 W 条目**（§7 记 P-11 ① 落入 N4、E5） | **与 W11 合并为 F13（保留现状）** | 索引「功能点索引」F13 行（来源 W11；N4；E5）、「需求追溯·W 条款」W11 行、「需求追溯·E 条款」E5 行、「疑问与越界」2 | 若后续认为该合并不当 → 可拆为两张卡，**F13 的验收条目本身无需改写** |
| 3 | **E12 / N2 是登记义务而非能力**（主 agent 的一次性行动项） | **保留 F15〔保证项·登记义务〕** | 索引「功能点索引」F15 行、「需求追溯·E 条款」E12 行、「需求追溯·N 条款」N2 行、「疑问与越界」3 | 若后续认为应记为全局边界而非卡 → 删 F15，**其余卡不受影响** |

---

## 三、本轮写入范围与未越界声明

- **本轮写入**（全部为已存在文件的修订或新建）：
  1. `prd.md`：版本 `0.1.0 → 0.2.0`；状态改为「阶段 2 **已收敛**」；「MI 判定口径」段标题/表头/引言改为**已裁决**并记录用户裁决原文与三组映射；「疑问与越界」1~3 补入主 agent 裁定结论（含裁定者与回退口）、第 5 项补入裁决结果、第 6 项补入本轮写入范围。
  2. **9 张受影响卡**：F01、F02、F03、F06、F07、F08、F09、F11、F12 —— 删除全部 `[model_inferred MI-xx]` 标记，替换为 `[user_confirmed MI-xx]` 与按推荐值定案的验收口径（**保留可核对的具体判定语句**，未弱化任何判定）。
  3. `clarifications/prd-round-2.md`（本文件，**新建**）。
- **未改动**：`prd/` 下其余 6 张卡（F04 / F05 / F10 / F13 / F14 / F15 —— 无 MI 来源，卡内本无 `model_inferred` 标记）；功能点数量、W/N/E 追溯关系、边界内容、`[架构待填]` T-01~T-16 全部不变。
- **未触碰**：`demand.md` / `status.md` / `history.md` / `clarifications/` 下既有文件（round-1~4、incident、retro-0017-phase1、各 proposals）；任何代码与 `roles/**`、`.pb-agents/**`、`.claude/**`、`tools/**`；**未运行任何测试或构建**；**未执行任何 git 命令**。
- **未做架构决策**：T-01~T-16 继续保持留白；本轮所有改动均限于验收口径与判定方式，无技术选型、数据模型、接口设计、命令与路径细节。

---

## 四、残留自检

| 检查项 | 结论 |
|---|---|
| `[model_inferred` 标记残留（`prd.md` + `prd/**`） | **0 处**（grep `\[model_inferred` 无命中） |
| `model_inferred` 作为术语出现 | 仅出现在 `prd.md`「版本」「状态」「输入合同」「MI 判定口径」引言、「疑问与越界」5 与各卡的来源说明句中——**均为状态/来源说明，非待确认标记** |
| `[user_confirmed MI-xx]` 标记落点 | F01（MI-05 ×2）、F02（MI-05 ×2、MI-03）、F03（MI-01、MI-02、MI-05）、F06（MI-04）、F07（MI-02）、F08（MI-06）、F09（MI-07、MI-02）、F11（MI-08）、F12（MI-09） |
| 功能点数量 / W 项 / E→卡映射 | 与第一轮一致：**15 张卡**（12 需求功能点 + 1 声明项 + 2 保证项）、W1~W12 全覆盖、E1~E13 全部有承载卡 |
