# 阶段 6 最终独立验证报告 · 0019-worktree-isolation-protocol

**验证者身份（反射结果）**：本委托的产出物是「一份流程协议本体的改动 + 其全部阶段产物（需求合同 → 功能卡 → 架构 → PR 规划 → 已合并实现）」，且委托方额外要求核查协议自身规定的三项「并发调度真实执行证据」并执行一次 `git worktree` 构造样本实测。据此反射出的身份是——**协议发布前评审者，四重视角合一**：
① **规范文本评审者**（条款可达性 / 自洽性 / 可判定性）；② **需求可追溯性评审者**（demand → prd → 实现逐条落地）；③ **架构一致性评审者**（PR 规划 / ADR 裁定 / 实现三方对齐）；④ **git 拓扑取证评审者**（并发、worktree、分支、合并均为 git 一手证据）。
四项均以「产出物 + 验证标准」为唯一判据，不使用任何执行过程上下文。

**产出物**：`docs/iterations/0019-worktree-isolation-protocol/`（demand.md v1.3.0 / prd.md v0.2.0 + `prd/F01~F15` / architecture.md v1.2.2 / `prs/pr-001*.md` + `prs/pr-002*.md` 及其 tasks / status.md / history.md / progress.md / clarifications/）+ 已合并实现（迭代分支 HEAD `dbef2b5` = 指派基线；其后 `4234b0c` 为纯 docs 提交、`dbef2b5` 仍是其祖先）：`roles/workflow-pb/workflow-pb.md`（v0.9.0）、`.claude/skills/workflow-pb/SKILL.md`（v1.12.0）、`roles/workflow-pb/data/workflow-pb-changelog.md`、`roles/workflow-pb/memory.md`、`.claude/skills/workflow-pb/memory.md`、`.claude/skills/workflow-pb/data/skill-optimization-v1.12.0.md`

**验证标准来源**：主 agent 委托内联的 8 条验证标准（阶段 5 推进条件复核 / 需求到实现追溯 / 并发调度真实执行证据三项 / 实现与规范内部一致性 / 边界遵守 / 阶段 4 产物与最终实现一致性 / 搭置需求变更报告 / MI-05 构造样本验证 E1~E4）

**验证日期**：2026-09-12（验证会话时间窗 20:00–20:05 +0800）
**验证基线**：迭代分支 `iteration/0019-worktree-isolation-protocol`（开始核对时 HEAD `4234b0c`；实现本体在 `dbef2b5`，`git merge-base --is-ancestor dbef2b5 HEAD` → 真）
**写入范围**：除本报告外，唯一写入性动作 = 标准 8 授权的 `/tmp` 构造样本（已完整清理，见标准 8 证据链）；主工作区与迭代分支**零文件改动**（清理后 `git status --porcelain -uall` 为空、`git worktree list` 仅 1 项、`git branch --list` 仅 3 条，均与实验前基线逐字节相同）。

---

## 逐项判定

### 标准 1 · 阶段 5 推进条件复核（PR 文件范围逐条相符 + 无简报外改动）

**判定：pass**

证据：
1. **pr-001 文件范围逐条相符**：PR 文件声明 6 个文件（`prs/pr-001-workflow-pb-workspace-isolation-protocol.md` §文件范围）；`git diff --name-only dbef2b5^1..dbef2b5`（该 PR 的合并引入面）= 6 个文件，逐条一致：
   `roles/workflow-pb/workflow-pb.md`、`roles/workflow-pb/data/workflow-pb-changelog.md`、`roles/workflow-pb/memory.md`、`.claude/skills/workflow-pb/SKILL.md`、`.claude/skills/workflow-pb/data/skill-optimization-v1.12.0.md`（新建）、`.claude/skills/workflow-pb/memory.md`；其源提交 `c034dab` 的文件面同为这 6 个（`git show --name-only c034dab`）。声明的"不涉及"面（`tools/install-pb-agents.sh` / `.gitignore` / 9 个执行角色文件 / `.pb-agents/**` / `oamp/**`）在 diff 中 0 命中。
2. **pr-002 文件范围逐条相符**：PR 文件声明 2 个文件（`0017/status.md` + `0019/status.md`）；`git diff --name-only 1ca983d^1..1ca983d` = 恰这 2 个；源提交 `d038e3c` 的 2 个 hunk 分别落在 `docs/iterations/0017-project-workspace/status.md:46` 与 `docs/iterations/0019-worktree-isolation-protocol/status.md:34`（`git diff -U0 d038e3c^ d038e3c` → 仅 `@@ -46 +46,4 @@` / `@@ -34 +34,4 @@`）。声明"不涉及"的 `0018/status.md` 在 diff 中 0 命中。
3. **合并后迭代分支无简报外改动**：以阶段 5 起点 `81a7aed` 为基线取全量变更面（`git diff --name-only 81a7aed..dbef2b5`）共 15 个文件 = 两 PR 的 8 个文件面 + **7 个迭代过程台账文件**（`0019/{history.md, progress.md, status.md}`、`prs/pr-001-tasks.md`、`clarifications/` 4 份验证/报告：`verify-gate-stage4-r3-*`、`verify-pr-001-*`、`verify-pr-001-r2-*`、`verify-pr-002-*`）。后 7 个是工作流自身规定的主线台账与验证产物（阶段 5 的 tasks/验收报告/progress 快照/history 记录/status 视图），**不含任何规范、SKILL、代码、配置文件的越界变更**；除这两类外无第三个文件被改动。
4. 指派核对式 `git diff --name-only 995fc59^..dbef2b5` = 8 个文件，即 pr-001 的 6 个 + `/tmp` 之外的 2 个主线台账（`prs/pr-001-tasks.md`（提交 `995fc59` 的判据口径修正）、`clarifications/verify-pr-001-r2-20260912-195822.md`（提交 `e17edb2` 的复验报告））——两者都是第 3 点枚举中的主线台账类文件，非 PR 文件面，亦非越界改动。
5. 迭代分支 tip 现状：`4234b0c`（阶段 5 完成台账，仅改 `0019/status.md`）为 `dbef2b5` 之后的唯一新增提交；核对时刻 `git status --porcelain -uall` 为空。

### 标准 2 · 需求到实现的追溯（E1~E13 逐条落地 + prd/F01~F15 验收成立）

**判定：pass**

证据 ① **E1~E13 逐条落地位置（在已合并实现中可定位）**：

| E | 落地载体（文件 + 行） | 结论 |
|---|---|---|
| E1 两会话工作区路径不同、互不改内容 | `workflow-pb.md:149-163`（规则 C 全文 + 三条判据）；实测样本见标准 8（两条 worktree 路径互异，A 的写入/提交后 B 的 tracked+untracked 文件哈希不变） | pass |
| E2 切换分支互不阻塞、互不覆盖 | `workflow-pb.md:151,163`（会话工作区各自检出 → 工作目录/索引/HEAD 三层分离）；标准 8 实测：B 留未提交改动时 A 完成 `checkout -b` + `checkout` 两次切换，B 的分支与改动 md5 前后不变、切换未被阻塞 | pass |
| E3 提交只出现在自己分支 | `workflow-pb.md:149-151`（工作区绑定 `iteration/{迭代ID}`）；标准 8 实测：A 的提交 `19bfd73` 仅出现在 A 的两条分支上，`--is-ancestor` 对 B 分支、迭代分支、main 三者均为假 | pass |
| E4 并发产出提交时间交叉、各自完整找回 | `workflow-pb.md:149-151`；标准 8 实测：A(20:04:33, 20:04:40) 与 B(20:04:36, 20:04:41 附近) 提交时间交叉，四条产物分别在各自分支 `git show` 完整取回，且互不存在于对方分支 | pass |
| E5 新增/改写条款自带判断方式 | 规范 8 条独立 `判断方式：` 行（`workflow-pb.md:141`(A) `147`(B) `163`(C) `184`(D) `190`(E) `200`(F) `206`(G) `242`(角色定义来源)）+ F11 两条约束的判断方式内联于 `:218-225` 表格单元格 | pass（一处粒度缺口见偏差 D-1） |
| E6 暂停/终止现场保留 | `workflow-pb.md:202-206`（规则 G 条款 + 判断方式：`git worktree list` × `git branch --list 'iteration/*'` 前后对照） | pass |
| E7 分支名均含迭代 ID | `workflow-pb.md:186-190`（规则 E + `git branch --list` 判据）；本迭代真实分支名 `iteration/0019-worktree-isolation-protocol`、`feat/0019-pr-001-workflow-pb-protocol`、`feat/0019-pr-002-d2-registration`（history.md 19:35:16 / 19:47:00），标准 8 样本创建的 4 条分支含 `0019` | pass |
| E8 合并提交出现在仓库主工作区、迭代工作区不 checkout main | `workflow-pb.md:440-455`（标题「（在仓库主工作区执行）」+ 三步全部 `git -C <仓库主工作区>` + 第 3 步前 `git -C <会话工作区> checkout --detach`）；`workflow-pb.md:217` 硬约束句 | pass（条款落地成立；真实收口实例尚未发生——本迭代阶段 6 未 pass，属"下一个实例"见下一迭代候选 N-4） |
| E9 新工作区零安装动作可读完整角色定义 | `workflow-pb.md:230-242`（真源规则 + `git ls-files --error-unmatch` 判据）；标准 8 实测：新建两条 worktree 后**未执行任何安装动作**，`roles/` 下 12 个目录齐全、`git ls-files roles` = 62 条、`git ls-files --error-unmatch roles/verifier/verifier.md` 成功，且工作区内不存在 `.pb-agents/roles/` 副本 | pass |
| E10 隔离边界声明（覆盖/不覆盖 + 负面约束） | `workflow-pb.md:208-217`（覆盖三项 / 不覆盖三项 + 逐项负面约束 + 同分支硬约束句） | pass |
| E11 非 git 共享资源声明 + 两条约束 | `workflow-pb.md:219-225`（四项资源表 + ①端口 ②负载两条约束各带判断方式 + `:224` "本约束不要求修改默认端口值/测试面自带随机端口不受约束" + `:228` 已知代价） | pass |
| E12 三处 status 对 D-2 互指、同一承接方、无编号歧义 | `0017/status.md:47-50`（三段式 + 引用链 + 编号口径行）、`0019/status.md:34-37`（同构 + 编号口径行）、`0018` 侧 = 既有提交 `89182b7` 的 `docs/iterations/0018-chat-agent-subagent-protocol/status.md:34` C-3 行（"由 0019 完成后起的单独小迭代承接"+"三处 status（0017/0018/0019）互相引用留痕"，本报告独立 `git show` 核对） | pass |
| E13 会话执行记录写入路径均在本工作区内（跨区须先备份） | `workflow-pb.md:192-200`（规则 F 条款 + 三类写操作点名 + 闭集例外两项 + 判断方式）；本迭代执行记录中出现一次跨区写入无前置备份（偏差 D-5），条款本身落地成立 | pass |

证据 ② **prd/F01~F15 逐卡结论**（判决依据 = 卡内「验收标准」逐条对已合并实现的文本核对，卡内「架构落地」段用于定位）：

| 卡 | 验收条数 | 结论 | 关键证据 |
|---|---|---|---|
| F01 会话工作区形态 | 5 | pass | `:151` "**默认且唯一**被规范化的形态" + clone 仅一处出现且为声明句（无任何步骤句，全文件 `grep clone` = 1 命中）；`:151` 末句"不保证该场景的隔离"；标准 8 实测验收 3/4 |
| F02 每迭代一个工作区 | 5 | pass | `:151`（每迭代一个 + 阶段 1~6 保持 + 跨迭代须新建）；标准 8 实测验收 2/3；`:217` 同分支不能两处检出（验收 5 无隐含要求） |
| F03 启动契约与硬停 | 5 | pass | `:165-184`（三条 git 原语判据 + 发现即停 + 硬停输出块含创建命令且不复用 `⛔ 需要你的决策` + 约束主体 = 主 agent 会话 + 阶段 5 子 agent 以被派发工作区为准）；标准 8 实测三条判据在样本工作区成立（`--show-toplevel`=cwd、`--git-dir`≠`--git-common-dir`、`--show-current` 输出分支名） |
| F04 指代重载 | 4 | pass | `:457-461`（"全部路径相对该会话所在的迭代工作区根目录判读"）；`:153` 显式声明；4.5 步仅改主体指代（`git diff -U0` hunk `@@ -226 +333 @@` 单行）；全文 `grep "主 agent 根目录"` = 0 命中（SKILL 亦 0），残留的 `仓库根`（`:71` 变更说明内指仓库根 `.gitignore`）与 `checkout main`（`:443` 位于 `git -C <仓库主工作区>` 命令内）均非旧主体判读 |
| F05 合并固定在主工作区 | 4 | pass | `:436-455`（标题与三步 + 前置游离动作）；全文无"会话工作区切到 main"的残留表述；无自动化合并机制 |
| F06 分支命名 | 4 | pass | `:186-190`（含迭代编号 + PR 分支规范化 `feat/{迭代编号}-pr-{NNN}-{slug}` + base/合并目标不变 + 只约束新分支；含 MI-04 字符串包含口径）；真实记录见 E7 行 |
| F07 父子层与 PR worktree 定位 | 5 | pass | `:155`（父层/短生命周期层 + base 仍为迭代分支 + "**PR worktree 不得被当作会话工作区使用**"）；术语限定 7 处落地（见标准 4）；`:384` 阶段 5 步骤 2 落点；标准 8 实测 nested PR worktree 与 session 工作区为不同目录、且 nested 提交不影响 A/B 两侧 HEAD 与 status |
| F08 角色定义来源 | 6 | pass | `:230-242`（真源 = 被追踪内容 + 两场景 + 零动作准确含义 + 只读约束）；SKILL `:42` CRITICAL 语义反转、`grep 不得退回读` = 0 命中、`grep .pb-agents/roles` 仅剩 2 处下游副本语境（`:42`/`:227`）、`grep v0.8.0` = 0 命中；标准 8 实测 E9 |
| F09 跨工作区写入禁止 | 5 | pass | `:192-200`（三类写操作 + 不可能被 git 强制 + 闭集例外两项 + 先备份后操作 + 判断方式）；无 hook / 访问控制 / 路径守卫（`.git/hooks` 仅 sample，diff 无 hook/guard/`*.sh`） |
| F10 暂停/终止现场保留 | 5 | pass | `:202-206`（保留 + 清理权归属 + 与既有条款并列不互相替代 + 不含任何清理动作）；既有现场保留条款（`:279` 段、`:481`/`:485`）经 diff 核对零改动 |
| F11 非 git 共享资源 | 5 | pass | `:219-228`（四项资源 + 两条约束各带判断方式 + "不要求修改默认端口值" + 约束对象为"真实服务"，测试面随机端口不受约束）；零机制、未触 `oamp/**` |
| F12 隔离边界声明（声明项） | 6 | pass | `:208-228`（覆盖三项 / 不覆盖三项 + 逐项负面约束 + 硬约束句 + 已知代价"后移到收口合并期"且无"自动解决"）；无越界承诺（全文无"跨机器隔离""写入会被阻止""refs 也被隔离"）；SKILL 侧仅引用行（`:105`） |
| F13 规范形态约束 | 5 | pass | 判据只用 git 原语 + 读文本（`:141/147/163/184/190/200/206/242` 的判据命令集 = `rev-parse`/`worktree`/`ls-files`/`branch`/`merge`）；无 hook/脚本/宿主特性前置；未新建平行定义文档（变更面仅两文件 + 惯例记录文件） |
| F14 既有条款零破坏 | 7 | pass | 见标准 4 的 hunk 归类与零改动面核对（逐 hunk 可归类、零改动区无 hunk、`约束生效范围` 逐字未改、CRITICAL 仅 1 处语义反转 + 1 处新增、无 `oamp/**`） |
| F15 D-2 承接登记 | 4 | pass | 两处 status 三段式 + 编号口径行（`0017:47-50`、`0019:34-37`）+ 0018 外部证据；全迭代 diff 中无 `project_id` 修复（`git diff 9ede9ea dbef2b5 --name-only` 无 `oamp/**`，命中项全在登记/需求/规格文档内） |

### 标准 3 · 并发调度真实执行证据（工作流强制三项）

**判定：pass**（三项均成立，第 3 项本迭代**触发了爬升条件**，不适用"不适用"口径）

**① worktree 时间窗口重叠：pass**

时间线（全部为文件/git 证据）：`history.md:316-320`（2026-09-12 **19:35:16** 调度决策记录："初始化并发配置 …创建两个 PR worktree（`.pb-agents/worktrees/pr-001`、`.pb-agents/worktrees/pr-002`，分支 `feat/0019-pr-001-…`、`feat/0019-pr-002-…`，base = `15d5eaf`）；首波并发派发两个 `planner`"）→ 两 PR 源提交落盘（`d038e3c` 19:41:54 pr-002 / `c034dab` 19:46:01 pr-001，`git log --format=%cI`）→ **两会话交叉观测证据**：`clarifications/verify-pr-001-20260912-195010.md:157` 记载 pr-001 的验收者执行 `git -C .pb-agents/worktrees/pr-002 status --porcelain` 与 `git diff --name-only 15d5eaf..HEAD` 并获得输出（"工作树**干净**；diff 仅两处 status.md"）⇒ **该时刻 pr-002 的 worktree 实际存在于磁盘**；同一报告 `:157` 同时记载的自身工作区 = `.pb-agents/worktrees/pr-001`（`:4` 声明产出物路径）⇒ **两条 PR worktree 在同一时间窗内并存**；`clarifications/verify-pr-002-20260912-194959.md:118`（V-6）反向记载"主工作区与 `.pb-agents/worktrees/pr-001` 不得出现本 PR 范围的改动"并给出对 pr-001 worktree 的核对结果 ⇒ 同一时窗的反向佐证。窗口闭合：pr-002 worktree 于 `1ca983d`（19:52:32）合并后清理，pr-001 worktree 至 `dbef2b5`（20:01:20）合并后清理（`progress.md:137,153` 在 19:54 观测时 `git worktree list --porcelain` 仅剩 pr-001）⇒ **重叠窗口 ≈ 19:35–19:52（约 17 分钟）**。补充证据：`progress.md:181-192`（已派发 2 / 已合并 1 / 在飞 1、无闲置 PR）。

**② 并发配置区块真实初始化与更新：pass**

`git show <commit>:…/0019/status.md` 逐版本取证：

| 提交（时间） | 阶段 | 起始并发数 | 硬上限 | 当前有效上限 | 累计槛位释放次数 | 已派发总数 |
|---|---|---|---|---|---|---|
| `81a7aed`（19:40:05）首个含该区块的提交 | 阶段 5 入口 | 3 | 5 | 3 | 0 | 2 |
| `1ca983d`（19:52:32，pr-002 合并） | 合并后 | 3 | 5 | 3 | 0 | 2 |
| `cefa78a`（19:55:30） | 台账修正 | 3 | 5 | **5** | **1** | 2 |
| `4234b0c`（20:01:49，两 PR 合并完成） | 阶段 5 完成 | 3 | 5 | **5** | **2** | 2 |

⇒ 五个字段在阶段 4→5 入口被真实初始化（全部有值），此后随每一次 PR 合并被真实更新，**累计槛位释放次数走完 0 → 1 → 2**，非"初始化后再未变化"。（时序瑕疵见偏差 D-3、D-4。）

**③ 爬升公式真实触发：pass**

`当前有效上限` 由初始 **3** 变为 **5**：`cefa78a` 起记为 `5（按公式 min(3 + 1×3, 5) 重算，已达硬上限）`——代入 `min(3 + 1×3, 5) = min(6,5) = 5`，与规范 `workflow-pb.md:394-401` 的 `min(起始并发数 + 累计槛位释放次数 × 起始并发数, 硬上限)` 及硬上限 `2×起始-1 = 5` 一致；`4234b0c` 的区块旁注进一步写明 `min(3+2×3,5)` 触顶。⇒ 未维持初始值，公式被真实执行并达硬上限。（区块内公式代入值的刷新瑕疵见偏差 D-4。）

### 标准 4 · 实现与规范的内部一致性

**判定：pass**

证据：
1. **悬空引用：无**。SKILL 对规范的全部章节引用逐条命中规范实体：`§规则 D`（`:136`/`:44`/`:98`）→ `workflow-pb.md:165`；`§角色定义来源与部署`（`:42`/`:137`/`:224`/`:385`）→ `:230`；`§隔离边界声明`（`:105`）→ `:208`；`§阶段定义`（`:145`/`:292-295`）→ `:114`；`§状态追踪协议`（`:341`）→ `:545`；`§历史记录协议`（`:355`）→ `:610`；`§「并发调度真实执行证据（下一个多 PR 迭代强制核查）」`（`:190`）→ `:520`；`§「阶段 5」`（`:180`）→ `:379`；规范侧引用 `data/workflow-pb-changelog.md`（`:37`）与 SKILL 侧 `data/skill-optimization-v1.12.0.md`（经 `.claude/skills/workflow-pb/memory.md:5` 索引）均真实存在。
2. **版本号一致**：`workflow-pb.md:25` = 0.9.0；`SKILL.md:30` = `1.12.0（对应规范 workflow-pb v0.9.0）`；`SKILL.md:3` description（v0.9.0）与 `:60` Purpose（v0.9.0）同步；`grep -n 'v0\.8\.0' SKILL.md` = **0 命中**（ADR-2 判据）；`roles/workflow-pb/memory.md` 首行 v0.9.0、`workflow-pb-changelog.md:3` `## v0.9.0（2026-09-12）` 齐备。
3. **重复定义：新增内容无**。隔离边界声明与非 git 共享资源只在规范定义一处、SKILL `:105` 为纯引用行（MI-09 口径成立）；启动校验判据（三条 git 原语）在 SKILL `:136` 为"执行动作 + 引用规范 §规则 D"，`skill-optimization-v1.12.0.md` 的"一致性核对"段亦逐条声明两侧一致。既有层面唯一的重叠表述（SKILL `:180` 的"并发槛位算法要点"含公式摘录）带显式指向"完整公式见规范 §「阶段 5」"，属要点/真源分层，非平行定义；且该段为既有内容、本次 diff 未触碰（F14 零改动面）。
4. **新增条款均带「判断方式」行**：新增条款 6 处（规则 C `:163`、D `:184`、E `:190`、F `:200`、G `:206`、角色定义来源与部署 `:242`）全部为"条款正文 + 紧跟一行 `判断方式：`"体例（未新增 `####` 子标题、未写入独立小节），T-16 体例成立；F11 两条约束的判断方式内联于同一节表格（`:222`/`:223` 单元格）。粒度缺口见偏差 D-1。
5. **零改动面/hunk 归类（兼作 F14 验收 1/2/3/7 的证据）**：`git diff -U0 9ede9ea dbef2b5 -- roles/workflow-pb/workflow-pb.md` 共 16 个 hunk，逐个可归类：`:25`→W1；`:65,10`→W1（新增 v0.9.0 变更说明）；`:124`→ADR-1/W19（阶段定义表阶段 5 行术语限定）；`:137`/`:139`/`:145`→W2（规则 A 标题、规则 A 正文、规则 B 正文）；`:149,95`→W4~W10（规则 C~G + 隔离边界声明 + 角色定义来源与部署，插入区间止于 `:243`，使 `### 约束生效范围`（`:244`）保持逐字未改）；`:327,2`/`:333`→W11；`:343`/`:351`/`:353`/`:365`→W12；`:384`→W13；`:440`/`:442,6`→W14；`:461,2`→W15；`:722`→W17。**零改动区无任何 hunk**：`## PR 文件格式规范`（`:250-321`，七字段）、并发槛位算法（`:393-401`）、`## 验证目标`（`:496-543`）、`## 状态追踪协议`（`:545-608`）、`## 历史记录协议`（`:610-685`）、`## 角色读取指南`（`:687-704`，W16 真源写法保留）、`## 何时更新本文件`（`:726-`）、v0.2.0~v0.8.0 变更说明（`:75-113`）。
6. **无越界机制**：`git diff --name-only 9ede9ea dbef2b5 | grep -iE "hook|guard|\.sh$"` = 0 命中；`.git/hooks` 仅 sample 文件；`oamp/**`、`tools/**`、`.gitignore`、9 个执行角色文件零改动。

### 标准 5 · 边界遵守（N1~N10 零半成品）

**判定：pass**

| 边界 | 结论 | 证据 |
|---|---|---|
| N1 不改阶段序列/职责/推进条件/并发槛位算法/PR 七字段 | pass | 阶段定义表的唯一 hunk 为 `:124` 阶段 5 行的术语限定（ADR-1 授权）；`:250-321` 七字段与 `:393-401` 算法无 hunk |
| N2 不承接 D-2 | pass | 全量 diff 无 `oamp/**`；`project_id` 命中项全在登记文本与需求/规格文档内；无新增"登记元数据完备性"条款 |
| N3 不追溯、0019 自身不迁移 | pass | `:246` "仅从下一个使用本工作流的迭代起生效，不追溯已完成迭代" + `:69` "也不要求本迭代（0019）自身迁移"；事实上本迭代 PR worktree 仍落在旧惯例路径 `.pb-agents/worktrees/pr-001`（`progress.md:139` 实测记录），迁移样本留给下一个迭代 |
| N4 不做自动化守卫/hook | pass | 规则 F `:196` 显式"本协议不引入 hook、访问控制或路径守卫"；`.git/hooks` 无新增；diff 无脚本 |
| N5 不新增清理动作 | pass | 规则 G 末句"本条款**不含任何清理动作**"；新增面全文无新增清理条款；`worktree remove`/`branch -D` 的既有收口三步未扩写 |
| N6 不保证跨机器/多人 | pass | `:151` "不为它定义任何流程，也不保证该场景的隔离"；声明处**无** clone 的执行步骤句（`grep clone` 全文仅 1 处，为声明句） |
| N7 不改 `oamp/` | pass | 变更文件清单 0 命中 `oamp/` |
| N8 规则 A/B 实质语义不变 | pass | `:137-147` 仅"独立 worktree 分支"→"独立 **PR** worktree 分支"的术语限定；两条 `判断方式：` 行（`:141`/`:147`）经 diff 确认为非 diff 行（逐字未改） |
| N9 留白交阶段 3 | pass | architecture.md v1.2.2 的 T-01~T-16 全填（`§11 prd 补全记录`、`§3.2` 命令表、`§8.*` 条款落点），实现面与之一致 |
| N10 不把 W12 写成可被 git 强制 | pass | `:196` 性质声明；`:212-216` 未覆盖项负面约束不含访问控制机制；无 guard 文件 |

### 标准 6 · 阶段 4 产物（PR 规划）与最终实现的致性

**判定：pass**

证据：
1. **pr-001 声明的验收标准在合并态成立**：逐条核对结果——`变更面编号对齐`（hunk 归类与 architecture v1.2.2 `§5.1`（W1~W19，`:458-470` 含 W19 = `:114` 术语限定）一致）；`W1 版本与留痕`（`:25`/`:65`/changelog `:3`/memory 首行）；`W2/W3 术语限定 + 判断方式行逐字不变`；`W4~W8` 规则 C~G（条款 + 判据齐备）；`W9/W10` 两节声明；`W11/W12/W13/W14/W15/W16/W17` 逐处落地（分别见标准 4 第 5 条的 hunk 映射）；`术语限定 7 处`（规范 6 处 + SKILL `:305` 1 处，实测措辞两侧一致）；`S1~S10`（CRITICAL 反转、新增启动校验 CRITICAL、Tools/Step 0 两步、章节改名、32 处路径替换后仅剩 2 处允许语境、brief 模板行、Step 5 落点、隔离边界引用行、三处版本同步）；`零改动面核对`（零改动区无 hunk、无 `tools/install-pb-agents.sh`/`.gitignore`/角色文件/`oamp`/`principles` diff）；`F13 验收 1/2/3`；`生效范围不变`（`:244-246` 逐字）。
2. **pr-002 声明的验收标准在合并态成立**：`0017 落点`（三段式 + 编号口径行，`0017/status.md:47-50`）；`0019 落点`（同构，`:34-37`）；`ADR-3 外部证据`（`89182b7` C-3 行经独立 `git show` 核对，本 PR 未写入 0018 文件，diff 0 命中）；`三处互相引用 + 同一承接方`；`编号口径无两义`（两处均以 `0017/D-2` 形式书写）；`不含修复`；`单段改动约束`（`d038e3c` 的 0019 侧单 hunk `@@ -34 +34,4 @@`；合并结果 `git diff 1ca983d^1 1ca983d -- 0019/status.md` 亦仅该单段 hunk = `4/1` 行，主线段落完整保留）。
3. **ADR-1~ADR-4 引用与实现一致**：ADR-1（`history.md:197-199` 裁定 Q-1 = ①）→ `:124` 术语限定已落 + architecture `§5.1-W19`；ADR-2（Q-2 = ①）→ SKILL 三处版本（`:4`/`:30`/`:58`）全部同步、`v0.8.0` 0 命中；ADR-3（Q-3 = ②）→ 0018 侧只引用不写入、规则 F 例外保持闭集两项；ADR-4（Q-4 = (a)）→ 0019 status 仅 D-2 单段改动成立（合并方式差异见偏差 D-2）。
4. `pr-001` / `pr-002` 的 `depends_on` 均为"（无）"，两 PR 文件面真交集为 0、可同波并发——与实现面一致（`dbef2b5` 与 `1ca983d` 两个独立合并、文件面无交集）。

### 标准 7 · 搭置的需求变更报告

**判定：pass · 无搭置项**

证据：`docs/iterations/0019-worktree-isolation-protocol/deferred-demand-changes.md` **不存在**（`ls` → `No such file or directory`；`git diff --name-only 81a7aed..dbef2b5` 与 `main...dbef2b5` 的文件清单中亦无该路径）。按委托口径，本报告写明「**无搭置项**」，无内容可透传，用户无需据此发起新迭代。

### 标准 8 · MI-05 构造样本验证 E1~E4（唯一被授权的写动作）

**判定：pass**（E1/E2/E3/E4 全部成立，清理后零残留）

**样本构建**（样本根 `/tmp/mi05-verify-20260912-200424`，起于 `dbef2b5`）：两条独立 worktree（`session-a` / `session-b`，各带临时分支 `tmp-mi05/0019-sample-a` / `-b`）+ **session-a 内嵌一条 PR 层 worktree**（`session-a/.pb-agents/worktrees/0019-pr-900-mi05`，分支 `tmp-mi05/0019-pr-900`）——后者同时闭合 planner 在 `pr-001-tasks` 提请的 **R-2（嵌套 worktree 实测义务）**。受 git 硬约束"同一分支不能两处检出"限制，样本工作区检出的是临时分支而非 `iteration/{迭代ID}`（主工作区已检出迭代分支，且本验证不得触碰迭代分支）；此为本样本与真实形态的**唯一已知差异**，不影响 E1~E4 的判定对象（工作目录/索引/HEAD 三层分离与分支隔离）。

**E1~E4 判定与证据**（完整命令与输出逐条留档于会话记录，下列为关键命令 + 观察值）：

| 项 | 判定 | 关键命令与观察 |
|---|---|---|
| E1 工作区路径不同 + 互不改变对方内容 | pass | `git worktree list` → 主工作区 `4234b0c [iteration/0019-…]` / `session-a dbef2b5 [tmp-mi05/0019-sample-a]` / `session-a/.pb-agents/worktrees/0019-pr-900-mi05 dbef2b5 [tmp-mi05/0019-pr-900]` / `session-b dbef2b5 [tmp-mi05/0019-sample-b]`（4 项并存）；`rev-parse --show-toplevel` 三者互异。A 侧写入（修改 tracked `roles/workflow-pb/memory.md` + 新建 `A-artifact.md`）并提交 `19bfd73` 后，B 侧 `git hash-object` 对同路径 tracked 文件与自身 untracked 文件（`B-untracked.md`）**前后哈希相同**，`git status --porcelain -uall` 前后逐字节相同（仅 `?? B-untracked.md`），`rev-parse HEAD` 仍 `dbef2b5` |
| E2 切换分支互不阻塞、互不覆盖 | pass | B 侧留下**未提交** tracked 改动后，A 侧执行 `checkout -b tmp-mi05/0019-a-switch` 与 `checkout tmp-mi05/0019-sample-a` 两次切换均 `exit=0`（未被 B 的改动阻塞）；B 侧 `branch --show-current` = `tmp-mi05/0019-sample-b`（不变）、未提交改动哈希不变、`status` 前后逐字节相同（改动未被覆盖） |
| E3 提交只出现在自己分支 | pass | A 的提交 `19bfd73`：`git branch --all --contains 19bfd73` → 仅 `tmp-mi05/0019-a-switch` 与 `tmp-mi05/0019-sample-a`（同属 A 会话的分支）；`git merge-base --is-ancestor 19bfd73 <B 分支>` / `<迭代分支>` / `main` 三者 `exit=1`（均不含） |
| E4 并发产出提交时间交叉、各自完整找回 | pass | 提交序列 B(`bda5cc2`) → A(`406fa7b`, 20:04:40) → B(`bd4f987`)，`%cI` 时间交叉；A 侧 `git show <A 分支>:A-artifact.md` / `:A-artifact2.md` 完整取回，B 侧 `:B-artifact-v1.md` / `:B-artifact-v2.md` 完整取回；反向 `git cat-file -e <A 分支>:B-artifact-*.md` 与 `<B 分支>:A-artifact*.md` 均**不存在**；`log --oneline A --not B` = A 的两个提交、逆向同构 ⇒ 两侧产物各自完整、互不混入 |
| 附带：嵌套 PR worktree 隔离 + F07 父子层 | pass | nested 提交 `d7d66aa` 后，`merge-base --is-ancestor d7d66aa <A 分支>` = 假；A 侧 `status` 仍为空、B 侧 status 与 HEAD（`bd4f987`）不变 ⇒ PR 层写入不污染会话层；nested 落点在 `session-a/.pb-agents/worktrees/` 下（被 `.gitignore:2` 命中，故 A 的 `git status` 保持干净）与 `:155` 的父子层约定一致 |
| 附带：E9 零安装动作 | pass | 样本工作区未执行任何安装/初始化动作，`roles/` 12 个子目录齐全、`git ls-files roles` = 62、`ls-files --error-unmatch roles/verifier/verifier.md` 成功、`.pb-agents/roles/` 副本不存在 |
| 附带：E7 分支名含迭代编号 | pass | 样本创建的 4 条分支名（`tmp-mi05/0019-sample-a` / `-b` / `0019-pr-900` / `0019-a-switch`）逐条含 `0019`；无同名争用 |

**清理与无残留证据**（授权动作：`git worktree remove` + `git branch -D`）：
`git worktree remove --force` × 3（nested → session-a → session-b，均 `exit=0`）→ `git branch -D tmp-mi05/0019-pr-900 tmp-mi05/0019-a-switch tmp-mi05/0019-sample-a tmp-mi05/0019-sample-b`（删除回显 was `d7d66aa`/`19bfd73`/`406fa7b`/`bd4f987`）→ `rm -rf /tmp/mi05-verify-20260912-200424`。清理后与实验前基线逐项比对全部相同：`git worktree list` = `/Users/chenchiyuan/projects/agents  4234b0c [iteration/0019-worktree-isolation-protocol]`（1 项）；`git branch --list` = `0018` / `0019`(当前) / `main`（3 条）；`git status --porcelain=v1 -uall` = 空；`git rev-parse HEAD` = `4234b0c…`；`refs/heads/iteration/0019-…` = `4234b0c…`；`.git/worktrees` 目录不存在；`for-each-ref refs/heads/tmp-mi05` 无输出；`/tmp` 样本根已删除。**主工作区与迭代分支零文件改动、零 ref 改动。**

---

## 汇总

- pass: **8 项**（标准 1~8）
- fail: **0 项**
- partial: **0 项**
- blocked: **0 项**

## 偏差记录

（均为"实现/产物实际行为 与 规格/文档描述"的差异，不阻塞本迭代关闭；每条附建议处理）

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | F13 验收 2 与 pr-001 验收：新增/改写条款均"紧跟一行 `判断方式：`" | 5 条新规则 + 「角色定义来源与部署」均合规；**「隔离边界声明」章节本身无独立 `判断方式：` 行**，其两条约束（端口/负载）的判断方式内联于 `:222`/`:223` 表格单元格，另两行（临时目录 / 进程内作业注册表）仅写约束语句、无判断方式措辞 | 下一迭代补一行章节级判据（或补齐表格两行的判断方式）。F12 卡的验收标准未要求此行、F11 的两条约束已各带判据，故不阻塞 |
| D-2 | pr-002 验收「合并前置（重基线，ADR-4）」：合并前以当时迭代分支 **rebase** | 实际为 `--no-ff` **merge**（`1ca983d`，源提交 `d038e3c` 父提交仍为 `15d5eaf`、未被重写）；ADR-4 的**实质**约束成立（`git diff 1ca983d^1 1ca983d -- 0019/status.md` 仅 D-2 单段 4/1 行、主线段落完整保留） | 该差异已由 progress-observer 记为 F-5（`progress.md:226-230`）并进入主 agent 台账修正；建议下一迭代把"重基线"表述改为"合并前确保只重放单段改动（rebase 或 merge 均可）"，避免文字与方法绑定 |
| D-3 | 规范「每次有槛位释放（PR 完成合并或判定失败/阻塞）时…重新计算」（即时性） | 槛位释放 1 的实际更新发生在合并提交 `1ca983d`（19:52:32）**之后约 3 分钟**的 `cefa78a`（19:55:30）；该窗口内已提交的 `status.md` 仍记 `累计 0 / 有效上限 3`（progress-observer 记为 F-7） | 数值最终正确且未影响派发决策（无排队 PR）；建议下一迭代把"merge 的同一调度步骤内更新并发配置区块"写入 SKILL 的 status 更新时机清单 |
| D-4 | 规范字段 `**当前有效上限**`（初始等于起始并发数，随后按公式重算） | 值正确（3 → 5，达硬上限），但 `4234b0c` 版本仍写 `min(3 + 1×3, 5)`（`累计槛位释放次数` 已为 2，正确代入应为 `min(3+2×3,5)`）；同一区块的旁注已写 `min(3+2×3,5)` | 建议把"公式代入值与累计次数同步刷新"加入 status.md 格式注释，消除同一区块内的两种代入值 |
| D-5 | 规则 F / F09 验收 3：跨工作区写入须"先取备份、后操作"（备份动作须位于写操作之前） | 本迭代执行记录中出现**一次反例**：pr-001 的 dev 会话以相对路径完成 14 次编辑，实际落在**仓库主工作区**；其恢复方式为 `cp` 进 worktree + `git checkout HEAD --`，**不存在位于写操作之前的备份动作**（`history.md` 19:52:00 dev 报告 + 19:53:00 裁定；主工作区经核验零内容损失；`status.md` 已登记为"本迭代 W12/F09 的活体实例"） | 条款本身落地成立、实施事故已登记且无损失；建议下一迭代在阶段 5 的 brief 中显式提示规则 F（不改机制、只加派发提示），并在需要跨区操作时把"先备份"写成可核对的显式步骤 |
| D-6 | pr-001 验收 S10：新建 `data/skill-optimization-v1.12.0.md` 并追加索引 | 新文件已建、`.claude/skills/workflow-pb/memory.md` 顶部索引行已加；但 **SKILL `:32` 「变更历史」行与 `:367` Resources 列表未含 v1.11.0 / v1.12.0**（该文件自述为"避免无法归类的改动"的有意不改，`skill-optimization-v1.12.0.md` 「未改变的部分」段） | 不阻塞（F14 零改动面要求）；建议下一迭代补索引行，否则该记录仅能经 memory.md 找到，且 `:32` 行已积欠 v1.11.0 |
| D-7 | 规范 `§隔离边界声明` 名称与 SKILL `:105` 引用一致 | `skill-optimization-v1.12.0.md` 两处写作"措辞见**规范 §8.4**"（`:42` 对应行与文末关联文件），而 `§8.4` 实为 `architecture.md` 的章节号，规范侧对应章节名为 `§角色定义来源与部署` | 建议下一迭代把该记录的引用改为 `architecture.md §8.4`，避免"规范 §8.4"指向不存在的规范章节 |

## 下一迭代候选

1. **首个真实迁移样本（最高优先）**：以 v0.9.0 规定的新落点开工——`<仓库主工作区>/.pb-agents/worktrees/{迭代ID}` 作为会话工作区、PR worktree 嵌套其下；据此复判 E1~E4（本迭代 MI-05 只能做构造样本，N3 明示不迁移）。
2. **真实收口实例**：下一个迭代收口时按 `:440-455` 在仓库主工作区执行三步 + 前置游离动作，闭合 E8 的实例证据（本迭代尚未发生）。
3. **D-1**：为「隔离边界声明」补章节级 `判断方式：` 行或补齐两行表格的判据。
4. **D-4 / D-3**：并发配置区块的"即时更新 + 公式代入值同步刷新"写入 SKILL 的 status 更新时机清单。
5. **D-6**：SKILL `:32` / `:367` 补齐 v1.11.0、v1.12.0 索引。
6. **D-5**：阶段 5 brief 内联规则 F 提示；跨区操作时把"先备份"显式化为可核对步骤。
7. **0018 恢复执行时的核对项**：按架构 `§8.7`（收窄版）核对 `89182b7` 的 C-3 行是否含编号口径行（本迭代不写入该分支）。
8. **D-7**：修正 `skill-optimization-v1.12.0.md` 的"规范 §8.4"引用为 `architecture.md §8.4`。

## 结论

**PASS**

（8 项标准全部 pass；fail 0 / partial 0 / blocked 0；偏差记录 7 条，均不阻塞本迭代关闭。报告未使用任何执行过程上下文，未修改任何被验证产物——除本报告与已清理的 `/tmp` 构造样本外，主工作区与迭代分支零写入。）
