# 澄清记录 · round-3（迭代 0019-worktree-isolation-protocol，阶段 1 需求收敛）

**caller**: demand
**时间**: 2026-09-12
**本轮性质**: **收敛收口轮**——接收第三轮裁决（P-1 / P-8 / P-15 / D-2 / D-1）与 0017 复盘实证，把 `demand.md` 收敛为可交付版本
**最终版本**: `demand.md` **v1.2.0**（自 v1.1.0 递增）
**本轮产物**: 本文件 + `clarifications/demand-round-3-proposals.md`（仅 P-16）+ `demand.md` v1.2.0
**会话限制（硬性）**: 子 agent，无实时对话通道，**零自问自答**；P-16 保持 `[model_inferred]` 不自行定案

---

## 一、本轮输入

| # | 输入 | 性质 | 处理 |
|---|---|---|---|
| 1 | 第三轮裁决原话（P-1 ① / P-8 ① / P-15 ① / D-2 ① / D-1「已停」） | `user_confirmed` | 逐条落入 `demand.md` 结论段，见 §二 |
| 2 | `clarifications/retro-0017-phase1.md`（397 行，Q-a/Q-b/Q-c + §二 发现 + G-1~G-5 + U-1~U-9 + C-1~C-5） | `实测`（复盘独立取证，未复用 incident 结论） | 作为**实证依据**替换全部推断型推荐理由（映射见 `demand.md` 第一段 §5） |
| 3 | Q-c 暴露的两处需求层面事实 | `实测` | ① 更正 N5 的过期表述；② 新提案 P-16（`[model_inferred]`） |

**本轮读取范围**：`retro-0017-phase1.md`（全文 397 行，分两次读）；`demand.md`（v1.1.0 自身产物）；`0017-project-workspace/status.md`（第二轮预读，用于准备 P-1 判定规则）；`docs/iterations/0019-…/clarifications/` 目录状态（确认 `retro-0017-phase1.md` 已落盘、其余文件未变）。**未重读** incident（其结论已由复盘独立复核并交叉对拍）。

---

## 二、逐条：P 项 → 用户裁决 → 落定结论 → 引用证据

| P 项 | 用户裁决（逐字） | 落定结论 | 引用证据 |
|---|---|---|---|
| **P-1** | 「① worktree 为默认，clone 仅作跨机兜底（推荐）」 | worktree 为规范默认且唯一被规范化的形态；clone 仅作"无法共享同一 `.git`（跨机器/跨用户/无写权限）"的兜底，规范只声明该场景不受保证、**不为其定义流程**（W1、N6） | **Q-a A-2 三条**：① 0017 四类损害全部落在 worktree 隔离的三层（工作目录/索引/HEAD）；② 同一机制已在 0017 的 **PR 粒度**证成（`pr-001 ∥ pr-003`，两条分支同基点 `2af9f3d`、提交 16:21:41/16:19:28 均早于首个合并 16:35:51，两 worktree 曾同时在盘，主工作树零残留 = G-1）；③ clone 的额外隔离（独立 refs/对象库）在 0017 证据里**无对应需求**，代价是双份对象库 + 第二套流程。另：Q-a A-1 的逐层隔离表定义能力边界（→ E10） |
| **P-8** | 「① 改读被追踪的 roles/（推荐）」 | 角色定义读取来源改为**被 git 追踪的内容**（本仓库以被跟踪的 `roles/**` 为来源，新工作区检出即得）；`.pb-agents/` 与 `tools/install-pb-agents.sh` 降级为"仅面向外部业务项目的可选分发手段"；须改写 SKILL 中「不得退回读 `roles/` 源码目录」的 CRITICAL，并写清**上游研发场景与下游部署场景如何共用同一份规范**（W8） | **Q-b**：结论 1（本仓库角色真源 `roles/**` 本来就随检出出现，瓶颈只是 `SKILL.md:42/220` 把根路径钉死在被忽略的 `.pb-agents/roles/`）；B-2（`git ls-files roles` = **62 个跟踪文件**、结构完整）；B-3 方案 A2（唯一在"不依赖安装脚本"约束下既零动作、又不引入新机制类型的解）；结论 2（**零动作的准确含义 = 不需要人手动跑安装脚本、不需要额外动作**；业务项目场景角色文件本来就是安装产物 ⇒ 必须在需求层写清两场景，否则架构阶段会出现两条无法同时满足的要求） |
| **P-15**（新范围项） | 「①（新范围项，用户批准）」 | **非 git 共享资源纳入为最小声明与约束**：声明不被隔离的资源清单（默认端口 / CPU 与负载 / 临时目录 / 进程内作业注册表）；两条约束——①真实服务不得并发占用默认端口；②受负载影响的偶发失败不得当回归依据（W10、E11） | **Q-a A-4.3**：默认端口 `7788` 固定（`oamp/README.md:89/123`，`OAMP_WEB_PORT` 可覆盖）、测试面已规避（`oamp/test/project-workspace.test.js:121-123` 的 `pickPort()` = `41000 + rand(2000)`）；并发跑 6 个 `node --test` → **271/272**、安静环境 **272/272**（`verify-pr-004-…md:285/314`）；`oamp/test/router-registry.test.js:135` SIGINT 用例负载敏感；pr-001 dev 首跑 1 例无法解释失败、同代码连跑 4 次全绿（`0017/history.md:196`）；`/tmp` 与进程内 `hub jobs` 注册表同样不被隔离 |
| **D-2**（去向） | 「①」（单独小迭代承接 + 三处 status 互相引用留痕） | 本迭代**不承接** 0017 遗留缺陷 D-2；由单独小迭代在 0019 完成后承接；0017/0018/0019 三处 status 互相引用留痕（N2、E12） | **Q-c C-4**：D-2 有登记无承接（0017 登记为"下一迭代第一优先候选"、用户裁决不纳入 0018、0019 v1.1.0 的 N2 也已排除）⇒ 追责对象是**承接机制**（复盘 P-6），不是本迭代范围；缺陷真实性由 pr-002 验证者实测（`verify-pr-002-…md:247` 的 `params` 原文比对 + `/docs` 投影缺失）与阶段 6 独立复核（`verify-stage6-…md:269` ④(b)，`/debug` 实测 400）确认 |
| **D-1** | 「已停，继续推进」 | 第二写入方已停止，D-1 关闭（已确认事实） | 用户原话；`demand.md` 第一段 §2 据此修正首轮"风险并未消失"的表述为"风险窗口已关闭、机制缺口未修复" |
| **P-2**（复核） | 第二轮「① 启动契约 + 硬停」 | 结论不变，但**理由依据从推断升级为实证**：新增 Q-b B-1 前提 1（全部文档协议按 cwd 相对路径判读 ⇒ 会话 cwd 必须在新工作区根目录）与 U-6（主 agent 能否自迁移 cwd **不可核实**） | `SKILL.md:96-97`（相对路径检测）、`workflow-pb.md` §状态追踪/文档路径协议；U-6 |
| **P-6**（复核） | 第二轮「① 固定在仓库主工作区」 | 结论不变，理由依据升级：复盘 A-4.2 独立确认现行"合并进 main 三步"**全程假定工作区可以被切到 main**（并如实标注 0017 未直接撞上此错，属结构性推断） | Q-a A-4.2；`0017/history.md` 17:41:44 / 17:42:52 两条调度决策；reflog |
| **P-9 / P-7**（复核） | 第二轮「①」 | 结论不变，理由依据升级：A-4.1 给出 0017 中"共享 refs 可被误读"的一手实例（`git branch --merged iteration/0017-…` 把 `feat/pr-004` 列为已合并，因与迭代分支**同点**） | `progress.md` §5 第 5 条；Q-a A-4.1 |
| **P-3 / P-4 / P-5 / P-10 / P-11 / P-12 / P-13 / P-14**（复核） | 第二轮「全部按推荐」 | 逐条保持，落入位置见 `demand.md` §7 对照表 | 判定规则表逐行核对结果见 `demand.md` 第一段 §5 |
| **P-16**（本轮新增） | **无裁决** | 作为**新提案**单列并保持 `[model_inferred]`，未生效、不进入下游 | Q-a A-4.5（隔离不是强制）+ `0017/history.md:204`（pr-003 dev 在自己的 worktree 内工作期间**误写主工作树三笔 edit**，靠 `cp` 备份 + `git checkout --` 还原） |

---

## 三、本轮必须处理的两处需求层事实（Q-c）

### ① N5 的过期表述已更正

- **原表述（v1.0.0 / v1.1.0）**：「不清理现存残留（`/tmp/pb-0017-residual/`、`.pb-agents/worktrees/` 与 `.worktrees/` 两个空目录、**孤立分支 `iteration/0004-model-dispatch`**）」。
- **实测更正**：复盘 C-5 #9 —— `git show-ref` / `git branch -a` 仅输出 `iteration/0018-…`、`iteration/0019-…`、`main`、`origin/main`，**不存在 `iteration/0004-model-dispatch`**；其余三项残留确实存在（`/tmp/pb-0017-residual/` 3 文件；`.pb-agents/worktrees/` 空、mtime 17:30；`.worktrees/` 空）。
- **v1.2.0 的新表述**：残留清单更正为上述三项，并显式注明"**不存在孤立分支 `iteration/0004-model-dispatch`**"；更正依据一并写入该行（源自 0008 迭代文档的过期记载）。
- **性质**：一次性事实过期，不影响任何结论——但按角色"发现起点材料存在内部矛盾时先指出矛盾"的规则，本项由复盘提出、本轮如实更正。

### ② P-16 新提案（未裁决，已隔离）

见 `clarifications/demand-round-3-proposals.md`。**未写入 `demand.md` 结论段**，仅登记在 §7 待确认清单；`demand.md` 中不存在任何 P-16 对应的"做什么/不做什么/效果"条目。

---

## 四、`demand.md` 最终版本与两段自检结论

**最终版本**：`demand.md` **v1.2.0**（第三轮收口版 · 可交付）

| 自检项 | 结论 |
|---|---|
| 第一段：每条澄清结论都有来源标注，无待确认的 `model_inferred` | ✅ 第一段全为 `user_confirmed` / `实测` / `零改动项` / 参考视角（R-x 已标注不进结论）；`model_inferred` 仅存于 §7 待确认清单的 P-16 一行 |
| 第二段：五个问题都有明确答案 | ✅ 做什么 11 条（W1~W11）、不做什么 9 条（N1~N9）、大概怎么做（形态 + 能力边界 + 留白给阶段 3）、达到什么效果 12 条（E1~E12）、为什么（含 D-1 关闭后的修正表述） |
| 边界：每条"不做什么"都能说清"为什么这次不做" | ✅ N1~N9 逐条附理由；N5/N8 为零改动项并单列说明 |
| 六维诊断：最终草稿跑不出新缺口 | ✅ 六维全部判定闭合（含维度 6：W1~W11 全为 `user_confirmed`，无未问过雏形的条目）；C-1~C-8 关闭表逐条给出"由哪条裁决解决" |
| 提问方式：本轮呈现的都是"提案"而非开放问题 | ✅ 第三轮无需提问（4 项均为用户主动裁决 + 复盘实证）；P-16 为提案（选项 + 推荐 + 理由 + 影响面 + 参考视角 R-8） |
| 方案雏形：每个"做什么"条目都经过一次主动询问 | ✅ 11 条全部有用户明确回应（首轮原话 / 第二轮裁决 / 第三轮裁决 / 第三轮新批准范围） |

---

## 五、来源标注统计（本版）

| 类别 | 数量 | 说明 |
|---|---|---|
| `user_confirmed` | 三轮原话（首轮 1 段、第二轮 6 行、第三轮 5 行）+ 第二轮约束 5 条 + 明说边界 6 条 | 只含用户说出口的内容 |
| `实测` | 复盘 Q-a/Q-b/Q-c/G-1/C-1~C-5（带行号与 git 输出）+ 事故四类损害及复盘复核 + 既有条款 | 均可回溯到文件行号或只读 git 输出 |
| `model_inferred`（**未生效**） | **1 项（P-16）** | 已隔离，不进入下游 |
| `零改动项` | N5、N8 | 不计入待裁决 |
| 参考视角（不进结论） | R-1 ~ R-8 | 全部标注"这是参考视角，不是架构结论" |
| 已关闭的 `[UNVERIFIED]` | `.worktrees/` 是否被忽略（复盘 B-2 关闭）；`roles/**` 是否被跟踪（复盘 B-2 关闭） | 其余未核实项保留在 `demand.md` §2 末 |

---

## 六、未越界声明

- 本轮**唯一写入**三个文件：`demand.md`（重写为 v1.2.0）、`clarifications/round-3.md`（本文件）、`clarifications/demand-round-3-proposals.md`（新建）。
- **未修改**：`status.md`、`history.md`、`clarifications/{incident-20260912.md, round-1.md, demand-round-1-proposals.md, demand-round-2-proposals.md, retro-0017-phase1.md}`；`roles/**`、`.pb-agents/**`、`.claude/**`、`tools/**`；任何代码（含 `oamp/**`）；未产出 `prd/`、`architecture.md`、`prs/**`。
- **未执行任何 git 命令**（复盘的行号与 git 输出按 `实测` 引用，非本次自行取证）；**未运行测试或构建**。
- **未做架构决策**：worktree 落点目录、启动检测判据形态、PR 分支命名细则、`.pb-agents/` 降级后规范文本的组织方式一律留白（N9）。
- **未把 P-16 写成已定案**：它不在任何"做什么/不做什么/效果"条目中。
