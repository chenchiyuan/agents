# 澄清记录 · prd-round-1（迭代 0019-worktree-isolation-protocol，阶段 2 功能规格）

**caller**: prd
**时间**: 2026-09-12
**本轮性质**: 阶段 2 拆卡时产生的 **9 项 `model_inferred` 判定口径推导**——它们**不是**需求结论（`demand.md` v1.3.0 的 `model_inferred` 已归零），而是拆卡过程中暴露的"**验收标准无法判定之处**"。本文件供**主 agent 真实阻塞式转呈用户裁决**（子 agent 无直接对话通道，**严格禁止自问自答**）。
**本轮不需要**需求层的重新澄清：W1~W12 / N1~N10 / E1~E13 / P-1~P-16 / C-1~C-8 之间未发现矛盾或不清晰（见 `prd.md`「疑问与越界」5）。
**状态**: **9 项全部待确认**。用户裁决后，卡内标记按 `[model_inferred MI-xx]` → `[user_confirmed MI-xx]` 同步；若被驳回，按裁决改写对应验收 / 边界并重新走 Verify。

---

## 一、转呈清单（建议按组提问，9 项可并为 3 组）

### 组 A：隔离单位的判定口径（F01 / F02 / F03 / F07 / F09）

| 编号 | 来源卡 | 待确认 | 推荐值 | 备选读法 | 不问清会怎样 |
|---|---|---|---|---|---|
| **MI-01** | F03 验收 3 | 启动校验不通过时"**硬停**"的时点：发现即停，还是允许先做完只读分析再停 | **发现即停**——不进入任何阶段工作、不产生任何写入 | 先完成当前只读分析再停（"停"的时点后移） | 判定者无法判断"停之前做了哪些动作"是否违规；"硬停"变成没有下限的词 |
| **MI-02** | F03 验收 4、F07 验收 3、F09 验收 2 | **阶段 5 的父子层判定口径**：子 agent（planner / dev / verifier）的"本会话工作区"是**被派发时所在的工作区**（该 PR 的 worktree），还是统一指主 agent 的会话工作区 | **以写入者实际被派发的工作区为准**——子 agent 在所属 PR worktree 内即合规；写入父层会话工作区或他人 PR worktree 即违反 | 一律以主 agent 会话工作区为准（父子层视为一个整体，凡在父层之下的子层内均合规） | W12 的"本会话工作区"与 W3 的父子层在阶段 5 **必然相遇**；不定口径则同一次 dev 写入可被判为既违规又合规 |
| **MI-03** | F02 验收 4 | "**每迭代一个工作区**"与跨迭代复用的关系：同一会话参与连续两个迭代时，是否必须为第二个迭代换工作区 | **必须换**——工作区按迭代划分，阶段 1~6 期间不得跨迭代混用 | 允许收口后原地切到下一迭代分支复用（"每迭代一个"只约束同一时刻） | 系列迭代（如 0018→0019 这类紧接着的迭代）会不会"顺手复用"上一个是规范被违反的第一现场，无口径则无从判定 |
| **MI-05** | F01 验收 3~4、F02 验收 2~3、F03 验收 1 | **E1~E4 的判定样本来源与时点**——N3 规定本迭代自身不迁移，故本迭代**不产生真实并行会话记录** | **构造样本**：阶段 6 在同一仓库按新规范建两个工作区、在其中各做一次写入 / 提交 / 切换，逐条对照 E1~E4；**真实并行会话样本**留给下一个迭代（首个真实使用新规范的迭代） | 本迭代阶段 6 对该四项判定 partial，等真实并行会话出现后再补判 | E1~E4 是本迭代四类损害的核心裸判定，样本来源不定则阶段 6 无法组织验证（只能读到"条款文字正确"） |

### 组 B：条款与声明的边界（F06 / F08 / F11 / F12）

| 编号 | 来源卡 | 待确认 | 推荐值 | 备选读法 | 不问清会怎样 |
|---|---|---|---|---|---|
| **MI-04** | F06 验收 2 | E7"**分支名含迭代 ID**"的判据口径：字符串包含即可，还是要求统一格式 | **字符串包含该迭代编号即可**（不要求统一前缀 / 格式；具体命名形态留架构 T-03） | 要求统一格式（如 `<类目>/<迭代ID>-<slug>`），格式不符即违反 | 同一份执行记录，用"含即可"判是绿的、用"统一格式"判是红的；判定者各执一词 |
| **MI-06** | F08 验收 5 | "**零动作**"对**下游（业务项目）**场景的承诺边界——E9 只覆盖本仓库场景 | **零动作承诺只覆盖"角色定义真源随检出即得"的场景（本仓库）**；下游仍允许"先分发角色文件"这一步，但该步骤**不算本协议的运行前提**（可选的独立动作） | 把零动作写成对两场景都成立的硬承诺（要求业务项目把角色文件纳入版本库） | W8 已写明"这一区分必须在规范里写清，否则架构阶段会出现两条无法同时满足的要求"（`demand.md` §3.2）——不裁决则架构阶段必卡 |
| **MI-08** | F11 验收 2 | W10 约束①中"**真实服务**"的范围，以及**是否要求改默认端口值本身** | **范围 = 会话实际启动的长驻服务**（区别于测试面自带的随机端口）；约束只要求"并发场景不争用同一端口"，**不要求修改默认端口值** | 要求把默认端口改为按会话派生 | 备选读法会触及 `oamp/` 产品代码（与 N7"不改 `oamp/`"直接冲突）；不裁决则约束①的可执行边界不明 |
| **MI-09** | F12 验收 6 | E10 隔离边界声明的**落点范围**：规范一处为真源、SKILL 引用，还是两处都必须完整写 | **规范一处为真源**，SKILL 按既有"不重复定义"原则只引用 | 两处都必须完整写一遍 | 与既有"协调者定义契约、不重复定义"约束（C-5 同源）叠加后需拍板；不定则改完后仍可能出现两处措辞漂移 |

### 组 C：判定载体（F09）

| 编号 | 来源卡 | 待确认 | 推荐值 | 备选读法 | 不问清会怎样 |
|---|---|---|---|---|---|
| **MI-07** | F09 验收 2~3 | E13 中"**执行记录**"的载体口径 | **载体不限**——凡能观察到"写入动作 → 目标路径"的记录均可（会话日志、`history.md`、提交 diff、progress 报告），只要路径可核对 | 规范要求把"写入路径"显式登记到某固定载体（新增一项登记义务） | 备选读法会新增一项登记义务（超出 W12 的"负面约束 + 判断方式"）；不定则 E13 的判定者不知道拿什么当证据 |

---

## 二、已按 `demand.md` 明确口径落卡、**不列为 MI** 的相邻事项（供核对，避免误当成遗漏）

| 事项 | 依据 | 落卡 |
|---|---|---|
| worktree 为唯一被规范化形态；clone 仅兜底声明、不定义流程 | W1 / P-1 ① / N6 | F01 验收 1~2、F01 边界 |
| 会话 cwd = 工作区根目录；全部文档协议按相对路径判读 | W4（Q-b B-1 前提 1 实测） | F03 验收 1 |
| 合并固定仓库主工作区；会话工作区永不 checkout main | W6 / P-6 ① / E8 | F05 验收 1~2 |
| 仓库主工作区不作为任何迭代的工作区 | W5 / P-5 ① | F04 验收 2 |
| PR worktree base 仍为迭代分支、合并进迭代分支才解锁下游 | W3 / N8 | F07 验收 1、4 |
| `.pb-agents/` 与安装脚本降级为可选分发手段 | W8 / P-8 ① / C-7 | F08 验收 4 |
| 跨工作区写入条款"不可能被 git 强制" | W12 / N10 / P-16 ④ | F09 验收 4、F13 验收 3 |
| 非 git 共享资源四项清单 | W10 / P-15 ① / E11 | F11 验收 1 |
| 冲突后移到合并期为已知代价 | P-10 ① | F12 验收 4 |
| 不追溯已完成迭代；0019 自身不迁移 | N3 / P-14 ① | F14 验收 6 |
| D-2 由单独小迭代承接 + 三处 status 互相引用 | N2 / D-2 ① / E12 | F15 验收 2~4 |

---

## 三、编号缺口与回退口（**需求层事实，非 MI**）

| # | 缺口 | 本阶段处置 | 回退口 |
|---|---|---|---|
| 1 | **E10** 在 W 清单中无对应条目（其依据为 P-9 ① / P-10 ①，已在 `demand.md` §4 / §5 / §7 写明） | 承载为 **F12〔声明项〕** | 若认为应回 demand 补 W 或降级为全局边界 → 删 F12 一张卡 + 同步索引 E→卡映射，其余 14 张卡不受影响 |
| 2 | **E5 / N4** 同样无 W 条目（P-11 ① 落入 N4、E5） | 与 W11 合并为 **F13**（W11 为其需求源） | 若认为该合并不当 → 可拆为两张卡，F13 的验收条目本身无需改写 |
| 3 | **E12 / N2** 是主 agent 的一次性登记行动项，不属"agent 在独立 worktree 上工作"的改动面 | 承载为 **F15〔保证项·登记义务〕**（体例先例：0017 F10） | 若认为应记为全局边界而非卡 → 删 F15，其余卡不受影响 |

**这三项均已在 `prd.md`「疑问与越界」1~3 留痕**，本阶段未自行增补需求条目。

---

## 四、未越界声明

- 本文件与 `prd.md`、`prd/F01~F15*.md` 是该阶段**唯一写入**（全部为新建文件）。
- **未修改** `demand.md` / `status.md` / `history.md` / `clarifications/` 下已有文件（round-1~4、incident、retro-0017-phase1、各 proposals 均未触碰）。
- **未触碰**任何代码与 `roles/**`、`.pb-agents/**`、`.claude/**`、`tools/**`（`roles/workflow-pb/workflow-pb.md`、`.claude/skills/workflow-pb/SKILL.md`、`tools/install-pb-agents.sh`、仓库根 `.gitignore` 仅作只读参考，未写入）；**未运行任何测试或构建**；**未执行任何 git 命令**。
- 本阶段未做任何架构决策：技术选型、数据模型、接口设计、命令与路径解析一律以 `[架构待填]`（T-01~T-16）留白。
