# 工作流进度

**工作流**: workflow-pb v0.8.0
**迭代**: 0019-worktree-isolation-protocol
**当前阶段**: 功能规格（阶段 2）
**迭代分支**: iteration/0019-worktree-isolation-protocol（base = main @ `9ede9ea`，含 0017 全部产出）
**状态**: 进行中
**history**: 开启
**前置**: 本迭代由用户临时插入并定为**最高优先级**，0018-chat-agent-subagent-protocol 已按其裁决暂停在阶段 2 已收敛处。

## 阶段状态

| # | 阶段 | 完成 | 已验证 | 备注 |
|---|---|---|---|---|
| 1 | 需求收敛 | ✅ | ⬜ | demand.md **v1.3.0**；P-1~P-16 + D-2 共 17 项全部经用户裁决；`model_inferred` 归零；C-1~C-8 全部已决；输入含 0017 复盘 Phase 1（`clarifications/retro-0017-phase1.md`） |
| 2 | 功能规格 | ⏸ | ⬜ | 已派发 prd；输入 = demand.md v1.3.0（W1~W12 / N1~N10 / E1~E13） |
| 3 | 技术架构 | ⬜ | ⬜ | |
| 4 | PR 规划 | ⬜ | ⬜ | |
| 5 | PR 实现 | ⬜ | ⬜ | 逐 PR 状态见下 |
| 6 | 独立验证 | — | — | 按需触发，不计入线性进度 |

## PR 实现子状态（阶段 5 展开）

待阶段 4 产出 `prs/` 后初始化。

## 并发配置（阶段 5）

待阶段 4→5 入口初始化。

## 待确认项

- **无用户待确认项**（P-1~P-16 + D-2 全部裁决并落盘，`model_inferred` 归零）。
- **D-1（已解决）**：用户答复「已停，继续推进」；主 agent 的「落盘即提交 + 切分支前核对」自保措施继续执行。
- **D-2（跨迭代承接项，用户裁决 ①）**：0017 遗留缺陷（`/api/chats`、`/api/messages` 路由登记元数据缺 `project_id`）**由 0019 完成后起的单独小迭代承接**，需在 0017 / 0018 / 0019 三处 status 互相引用留痕。主 agent 已在三处登记（0018 status 的 C-3、0019 本项、0017 status 的「下一迭代候选」）。
- **D-3（编号冲突，待架构阶段统一）**：0017 的 `D-2`（缺陷编号）与 0019 早期使用的 `D-2`（verify-stage6 一字符差异）同号不同义；本迭代已改用 `D-*` 仅指 0019 自身台账项，跨迭代引用时将显式写明迭代号。
- **D-4（0017 收尾产物的一字符差异，备查）**：0017 `verify-stage6-20260912-173934.md` 提交版第 77 行（`695-698`）与第 160 行（`695-699`）自相矛盾，源真值在 `oamp/src/web.js` 696-697（复盘 Q-c 结论）；不属本迭代范围，仅留痕。
- **D-5（澄清记录缺档，留痕）**：`clarifications/` 下无 `round-2.md`（第二轮记录仅存于 `demand-round-2-proposals.md`，其正文自述第二轮有 2 项待裁决）；由 progress-observer 首次观测发现（F-3），内容未丢失，仅记录体例不完整。
- **D-6（progress-observer 首次观测的 6 条不一致，逐条处置）**：F-1 工作区改动 = 本迭代进行中的 history 写入（非他人改动，已随提交消除）；F-2 本文件原用词「分两次提交」与 git 事实（3 个提交）不符 → **已修正**；F-3 → 见 D-5；F-4/F-5 为引用位置说明；F-6 跨迭代项 → 0018 status 的 stash 声明与实际不符（`refs/stash` 不存在）→ **已用临时 worktree 修正 0018 status**。
- **主 agent 偏离记录（留痕）**：① `iteration/0019-…` 分支**在阶段 1 通过前即创建**（规范为阶段 1 通过后创建）——原因是用户裁决「先做 0019、暂停 0018」要求立即离开 0018 分支工作区，属操作必要偏离；② 0019 的产物在阶段 1 期间即分三次提交进本分支（`0a89a4d`、`c3b5152`、`27f280b`），理由同 D-1 自保措施。

## 用户确认记录（Gate）

- **迭代 ID**：`0019-worktree-isolation-protocol`（主 agent 拟定并采用）。
- **优先级与落地方式**：最高优先级；先做 0019、暂停 0018（用户裁决 A）。
- **P-1 = ①**：worktree 为规范默认且唯一被规范化形态；clone 仅作跨机/无写权限兜底（依据 0017 复盘 Q-a）。
- **P-2 = ①**：启动契约 + 硬停（不在本会话专属工作区 → 停止推进并输出创建命令）。
- **P-6 = ①**：合并进 main 固定在仓库主工作区执行，会话工作区永不 checkout main。
- **P-8 = ①**：角色定义读取来源改为被 git 追踪的内容（本仓库以 `roles/**` 为源），`.pb-agents/` 与安装脚本降级为面向业务项目的可选分发手段。
- **P-15 = ①（新增范围）**：非 git 共享资源（默认端口占用、负载致偶发失败、`/tmp`）纳入为最小声明与约束。
- **P-16 = ①（新增范围）**：跨工作区写入的负面约束 + 判断方式，并写明该条款不可能被 git 强制。
- **P-3 / P-4 / P-5 / P-7 / P-9 / P-10 / P-11 / P-12 / P-13 / P-14 = 全部按推荐**：每迭代一个工作区 / PR worktree 为子层 / 「主 agent 根目录」改写为「该会话的迭代工作区根目录」/ 分支名必含迭代 ID / 共享 refs 负面约束 / 接受冲突后移到合并期 / 只做文本级条款不做 hook / 暂停现场原样保留 / 只保证同机同仓库多会话 / 沿用不追溯生效。
- **D-2 = ①**：由单独小迭代承接，本迭代不承接。

## 更新日志

- 2026-09-12: 迭代启动（Step 0 前提声明：本宿主具备真实阻塞式人机交互通道 `ask`）。
- 2026-09-12: 创建迭代分支 `iteration/0019-worktree-isolation-protocol`（base = main `9ede9ea`）+ 事故一手记录 → 派发 demand 第一轮。
- 2026-09-12: 用户裁决 P-2/P-6 + 其余 10 项按推荐；P-1 要求「先复盘迭代 0017」；P-8 要求重拟方案（不依赖工具、属 git 使用规范）→ 派发 `retrospective` 对 0017 做 Phase 1 扫描。
- 2026-09-12: 0017 复盘产出（Q-a worktree 足够且更优 + 5 类残留、Q-b 零动作路径、Q-c 11 项漂移核查）→ demand 第二轮落定 12 项 + 重拟 P-1/P-8 选项。
- 2026-09-12: 用户裁决 P-1 ① / P-8 ① / P-15 ①（新范围）/ D-2 ①（另起小迭代）→ demand 第三轮收口（v1.2.0），仅剩 P-16。
- 2026-09-12: 用户裁决 P-16 ① → demand 第四轮归零（v1.3.0）→ **阶段 1 推进条件三项全部通过 ✅**；阶段 1 产物分三次提交（`0a89a4d`、`c3b5152`、`27f280b`）→ 进入阶段 2（功能规格），派发 prd。
- 2026-09-12: 自动派发 progress-observer（阶段 1 推进完成触发）→ 首次产出 `progress.md`（199 行，观测基线 `27f280b`），独立核实出 6 条不一致（F-1~F-6）与 5 条无法核实项 → 主 agent 逐条处置（本文件用词修正、0018 status 的 stash 声明经临时 worktree 修正、round-2.md 缺档留痕）。
