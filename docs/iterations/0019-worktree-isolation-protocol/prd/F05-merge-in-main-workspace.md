# F05：合并动作固定在仓库主工作区

**功能 ID**: F05
**来源**: `demand.md` W6（P-6 ①，`user_confirmed`）；效果 E8；C-2（git 硬约束）；实证 `retro-0017-phase1.md` Q-a A-4.2（证据来源）
**迭代**: 0019-worktree-isolation-protocol
**产品对象（引用 `demand.md` 只读事实，非本阶段决策）**: 既有「迭代分支合并进 main」三步（`checkout main` → `merge --no-ff` → `branch -d`）

---

## 用户价值

收口合并不再"切走"任何一个会话的地盘——正在干活的会话不会因为别人合并而被打断，也不会被拽到 main 上。

## 验收标准

1. **合并固定在主工作区执行**（W6 / P-6 ①）：规范要求"迭代分支合并进 main"的动作**固定在仓库主工作区执行**；**动作顺序与触发时机不变**（仍是阶段 6 判定 pass 之后、仍是同样的三个动作序列）。
   *判定*：读规范条款 + 任取一次收口记录，确认该动作序列是在仓库主工作区执行的 → 出现"在会话工作区内执行合并"即不通过。
2. **会话工作区永不 checkout main**（E8）：任取一次迭代收口：`main` 的合并提交出现在**仓库主工作区**，而该迭代的工作区在**合并动作前后始终检出在迭代分支上**（未 checkout main）。
   *判定*：对照收口记录中两个位置的检出分支（合并动作前后各取一次）→ 迭代工作区出现 main 检出即不通过。
3. **消除"假定工作区可切到 main"的残留表述**（W6 / C-2）：既有条款中任何隐含"工作区可以被切到 main"的表述已被一并消除或改写为指向仓库主工作区。
   *判定*：全文检索相关表述（切到 main / checkout main / 当前工作区在 main），逐条对改后的主体 → 任一条仍假定会话工作区可切到 main 即不通过。
4. **不引入自动化合并**（N4 / W11）：本卡只要求文本级条款，不引入任何脚本 / hook / 工具来自动执行合并。
   *判定*：检索规范文本，不存在"自动执行合并"的机制条款。

## 边界（不包含）

- 不含具体命令、前置检查与"如何确认当前工作区是仓库主工作区"（→ T-07 / T-08，见 `architecture.md` §3.2 命令 6/7 与 §3.2 命令 3）。
- 不含迭代分支的创建时机与合并进 main 的触发条件改动（N1）——两者均保持现状。
- 不含现场保留（→ F10）、不含指代改写（→ F04）。
- 不含"合并冲突的自动解决"——冲突后移到合并期是**已知代价**（P-10 ①），由 F12 声明，不在本卡。

## 架构落地（阶段 3 已填，见 `architecture.md` §3.2 / §5.1-W14 / §7 Q-2；产品维度未改一字）

- **T-07 命令与前置检查**：三步全部补执行位置与 `git -C` 形式——`git -C <仓库主工作区> checkout main` → `git -C <仓库主工作区> merge --no-ff iteration/{迭代ID}` → `git -C <仓库主工作区> branch -d iteration/{迭代ID}`；commit message 格式、冲突时停止上报、`status.md` 的 `**迭代分支**` 字段改写**均不变**。**前置检查**（步骤 1 之前）= `git rev-parse --git-dir` == `git rev-parse --git-common-dir`（确认当前是仓库主工作区；与 F03 的启动判据共用同一 git 原语，`architecture.md` §3.2 命令 3）。**前置动作**（步骤 3 之前）= 若 `iteration/{迭代ID}` 仍被会话工作区检出，先 `git -C <会话工作区> checkout --detach`——否则 `git branch -d` 会被 git 拒绝（同一分支不能两处检出，硬约束）。该动作**不是新增收口步骤**，三步顺序与触发时机不变（`architecture.md` §7 Q-2）。
- **T-08 残留表述定位与改写形态**：需消除"假定工作区可切到 main"的表述共两处——`workflow-pb.md:226`（唯一的"主 agent 根目录"句 + `git checkout` 切换动作）、`workflow-pb.md:333-338`（三步未写执行位置）；改写形态 = 补执行位置 + `git -C` 形式，**不删除任何动作**。`v0.2.0 ~ v0.8.0` 的变更说明（`:57/59/61/63/81/83/94/104-108/136-148`）与 `roles/workflow-pb/data/workflow-pb-changelog.md` 的历史记录**零改动**（改写即伪造历史），本次改动以新增 `## v0.9.0 变更说明` 承载。`SKILL.md` 全文无「checkout main / 合并进 main」出现（0 命中），不受影响；`roles/progress-observer/progress-observer.md:24` 的「主分支」为 v0.8.0 前的陈旧术语，属执行角色文件，**只提及不修**（F14：不含顺手改进）。
- **不引入自动化合并**（N4 / W11）：全部为文本条款 + git 原生命令，不新增脚本 / hook / 工具。
