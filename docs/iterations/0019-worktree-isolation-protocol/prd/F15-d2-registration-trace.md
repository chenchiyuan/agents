# F15：D-2 承接登记与跨迭代留痕**〔保证项·登记义务，非新增能力〕**

**功能 ID**: F15
**来源**: `demand.md` 边界 **N2**（D-2 ① 裁决，`user_confirmed`）；效果 **E12**；实证 `retro-0017-phase1.md` Q-c C-4（D-2 有登记无承接）+ 编号歧义（证据来源）
**迭代**: 0019-worktree-isolation-protocol
**产品对象（引用 `demand.md` 只读事实，非本阶段决策）**: 0017 `status.md` 的「下一迭代候选」登记（D-2 = `/api/chats`、`/api/messages` 登记元数据缺 `project_id`）；0018 / 0019 两处 `status.md`

---

## 用户价值

被两个迭代都排除的遗留缺陷不会再无声消失——三处状态文件互相指向同一个承接方，谁都看得出它归谁。

## 验收标准

1. **本迭代不承接 D-2**（N2）：0019 的产物与规范改动中**不包含** `/api/chats`、`/api/messages` 登记元数据缺 `project_id` 的修复；也不为它新增任何条款。
   *判定*：检索本迭代的改动清单与全部产物 → 出现该修复或其相关条款即不通过。
2. **三处 status 互相引用**（E12）：任取 **0017 / 0018 / 0019 三处 `status.md`**：对 **D-2** 的登记**互相引用、指向同一承接方**（"单独的小迭代，0019 完成后起"）。
   *判定*：读三处文本，检查引用链——0017 指 0018/0019、0018 指 0019、0019 指明承接方与起始条件（或三处共同指向同一承接方）→ 任一处断链或指向不同承接方即不通过。
3. **无跨迭代编号歧义**（E12 / 复盘 C-4）：三处文本中 **`D-2` 的指代唯一**——不出现同一编号在跨迭代引用时指向不同事项的情况（复盘指出 0019 曾另有一个同名 D-2）。
   *判定*：逐处读 `D-2` 的出现位置与所指 → 出现两义即不通过。
4. **承接方与起始条件写明**（N2）：登记中写明 D-2 **由单独的小迭代承接、在 0019 完成后起**（不写"下一迭代"这类未绑定指代）。
   *判定*：读三处登记文本 → 出现未绑定指代或缺少起始条件，即不通过。

## 边界（不包含）

- **不含 D-2 的修复内容**（N2：本迭代不做；由单独小迭代承接）。
- 不含承接迭代的排期、编号与产物（本卡只要求"登记与引用链成立"）。
- 不含 0017 其它悬空候选（D-1、pr-004 的 N1~N6、具名导出候选）的承接——`demand.md` 只对 D-2 作出裁决。
- 不含对既有 `status.md` 其它内容的改动（只做 D-2 登记与引用链相关的最小写入）。
- 不含 push / 远端同步条款（复盘 C-3 的规范空白不在本迭代范围）。

## 架构落地（阶段 3 已填，见 `architecture.md` §8.7；产品维度未改一字）

- **T-15 三处写法**：三处（`0017/status.md` 的「下一迭代候选」第 1 条 / `0018/status.md` 的 C-3 / `0019/status.md` 的待确认项 D-2）统一改为**三段式 + 一行编号口径**：① 缺陷陈述（`GET /api/chats` 与 `POST /api/messages` 的路由登记元数据缺 `project_id`）；② 承接方与起始条件（**单独的小迭代**，`0019-worktree-isolation-protocol` 完成之后启动）；③ 引用链（`0017` → `0018`（显式不纳入）→ `0019`（N2 显式排除并登记承接条件））。
- **编号歧义规避**：三处同段各加一行"跨迭代引用一律写成 `<迭代号>/D-<n>`（如 `0017/D-2`）；本迭代自身台账项不与 `0017/D-2` 复用同一编号（自 `D-3` 起）"。
- **判据自查**（本卡验收 2~4）：三处均写明承接方与起始条件且指向**同一承接方**；`D-2` 在跨迭代引用处一律带迭代号、无两义；不出现"下一迭代"这类未绑定指代。**本迭代不承接该缺陷**（验收 1）：产物与规范改动中不含其修复或相关条款。
