# F14：既有条款零破坏（回归锁）**〔保证项，非新增功能〕**

**功能 ID**: F14
**来源**: `demand.md` 边界 **N1 / N3 / N5 / N7 / N8**（其中 N5 / N8 为**零改动项**）；§1.2 约束①（"修改的只是 agent 在工作时在独立 worktree 上工作"）；体例先例：0017 的 F09「回归锁」、0016 的 F02
**迭代**: 0019-worktree-isolation-protocol
**产品对象（引用 `demand.md` 只读事实，非本阶段决策）**: 既有「阶段定义」（6 阶段）、「提交管理约束·规则 A / 规则 B」、「阶段 5 并发调度与槛位算法」、「PR 文件格式规范」（七字段）、「状态追踪协议」、「文档路径协议」

---

## 用户价值

这次只动"工作区归属"这一层——阶段怎么走、PR 怎么规划、失败怎么处理、文档落在哪，一页都不变。

## 验收标准

1. **变更面闭合、无越界改写**（§1.2 约束① / P-11 ①）：本迭代对规范与 SKILL 的**全部改动逐条可追溯**到一个"工作区归属"条款（F01~F13 所承载的 W / E 条款）；**不存在与工作区归属无关的改写**。
   *判定*：取改动前后的文本 diff，逐条归类到 F01~F13 的某一条 → 出现无法归类的改动即不通过。
2. **阶段序列与推进条件零变化**（N1）：阶段序列（6 阶段）、各阶段职责、各阶段推进条件、并发槛位算法（起始 / 硬上限 / 爬升公式）、PR 文件**七字段格式**逐条不变。
   *判定*：逐条对比改动前后的对应文本 → 任一条发生变化即不通过。
3. **规则 A / 规则 B 实质语义不变**（N8 / C-4）：代码变更仍**必须走 PR worktree**（base 与合并目标仍为迭代分支）；PR 文件仍须在 merge 前存在。本迭代只在"会话在哪个工作区"这一层之上叠加条款，不改这两条约束的实质语义。
   *判定*：读改动后的规则 A / 规则 B，逐句与改前对照 → 出现约束放松或收紧即不通过。
4. **不新增清理动作**（N5）：本迭代不新增"清理历史残留"的任何条款或动作（残留清单维持更正后的现状，不改动、不新增）。
   *判定*：检索规范文本的新增段落，无清理条款 → 出现即不通过。
5. **不改 `oamp/` 产品代码**（N7）：本迭代产出中**不含**对 `oamp/**` 的任何改动。
   *判定*：取本迭代的改动文件清单 → 出现 `oamp/**` 即不通过。
6. **生效范围声明：不追溯、0019 自身不迁移**（N3 / P-14 ①）：规范条款**从下一个迭代起生效**；本迭代（0019）自身**不迁移**（继续按现有自保措施执行），规范中不存在要求 0019 立即迁移的表述。
   *判定*：读规范条款的生效范围声明 → 出现"立即生效 / 追溯适用"或要求 0019 自身迁移的表述，即不通过。
7. **既有护栏条款不被削弱**（既有条款）：既有 CRITICAL 中与工作区无关的护栏（如"阶段 1 不可跳过""用户决策点必须暂停""不得对失败 / 阻塞 PR 清理现场""不修改执行角色产物内容"等）逐条保持。
   *判定*：逐条对比改动前后的 CRITICAL 清单 → 出现删除或语义放宽即不通过。

## 边界（不包含）

- **不含本迭代声明的变更点本身的功能内容**（分别由 F01~F13 承载）；本卡只声明"除这些变更点之外，零变化"。
- 不含回归核对手法的实现（→ T-14，见 `architecture.md` §5.5）。
- 不含"顺手改进"：不统一既有措辞、不重构既有章节、不调整既有条款编号体系、不清理既有冗余表述。
- 不含既有测试面 / 测试文件的任何改动（本迭代不改 `oamp/**`）。
- 不含 clone 场景的流程定义（N6，且不为 clone 定义流程本身即"零新增"）。

## 架构落地（阶段 3 已填，见 `architecture.md` §5.3 / §5.5；产品维度未改一字）

- **T-14 核对组织形态**：① **基线** = 本迭代开始前的 `roles/workflow-pb/workflow-pb.md` v0.8.0 与 `.claude/skills/workflow-pb/SKILL.md` v1.11.0（@ `9ede9ea`）；② **手段** = `git diff <base> -- roles/workflow-pb/workflow-pb.md .claude/skills/workflow-pb/SKILL.md`，逐 hunk 归类（**只读、git 原生、零工具**）；③ **判据** = 每个 hunk 必须能归到 `architecture.md` §5.1 的 W1~W17 或 §5.2 的 S1~S10 某个编号（该编号再追溯到 F01~F13）——出现**无法归类**的 hunk 即本卡验收 1 不通过；④ **零改动面** = 按 `architecture.md` §5.1/§5.2 的「零改动」清单逐条对照（阶段定义表 / PR 七字段 / 并发槛位算法 / 验证目标 / 状态追踪协议 / 历史记录协议 / `### 约束生效范围` / 既有变更说明 / 既有 CRITICAL），清单内任一节出现 hunk 即不通过；`§5.3` 列出的文件（`oamp/**`、`principles/**`、`tools/**`、`.gitignore`、9 个执行角色文件等）在 diff 中**不得出现**。
- **变更点清单的载体** = `architecture.md`（本迭代一次性组织形态，**不写入规范**「验证目标」章节——避免推测性扩展，`architecture.md` L2-10）。
- **生效范围零改动**（本卡验收 6）：沿用既有 `### 约束生效范围` 原句；新规则追加在该句**之前**，自动被"仅从下一个使用本工作流的迭代起生效、不追溯已完成迭代"覆盖，规范中不出现要求 0019 自身迁移的表述。
