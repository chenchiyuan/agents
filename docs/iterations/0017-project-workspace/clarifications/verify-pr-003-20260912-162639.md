# pr-003 独立验收报告（PR 级）

**验证者身份（反射结果）**：`产出物 = 前端实现 PR（HTML/JS/CSS 三文件）+ PR 验收标准 + 两张产品功能卡` ⇒ 反射身份 = **同级前端代码审查者（契约/行为面）兼任 Web 控制台验收人**。验证角度：先读 diff 与验收标准对照，再以真实运行前端（无头浏览器）观测行为，最后以测试证据复核既有行为零变化。本报告不采信任何执行过程叙述。

**验证日期**：2026-09-12
**产出物**：
- 代码：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/pr-003`（分支 `feat/pr-003`，HEAD `40151ff`，工作区干净：`git status --porcelain` 空输出）
- PR 验收标准：`docs/iterations/0017-project-workspace/prs/pr-003-project-layer-ui.md`
- 功能卡：`docs/iterations/0017-project-workspace/prd/F04-project-list-page.md`、`prd/F05-project-workspace.md`

**验证标准来源**：主 agent 委托内联的 5 项验证标准 + 上述 PR 文件「验收标准」+ F04/F05 卡「验收标准」。
**独立性声明**：未读 `history.md`、未读开发报告、未读 `clarifications/` 下任何既有文件的内容（仅为确认写入目录存在而 `ls` 过该目录的文件名一次，未读取内容）。被验证产物零修改；未执行任何 git 写命令。

**方法与证据来源**（三类，互相独立）：
1. **静态审计**：`git diff --name-only / --numstat / -U0`（PR 范围与逐行改动）、`git diff -- test/`（测试面）。
2. **真实运行**：在 pr-003 worktree 内启动被验证产物自身 —— `node bin/oamp.js web start --port 41917`（`OAMP_DB=/tmp/pr003-verify/sql.db`，临时目录，未触碰仓库 `.runtime/`），用无头 Chromium 观测 DOM 状态、`getComputedStyle` 生效值与真实发起的 `/api/*` 请求序列与请求体。
3. **测试执行**：`node --test test/web.test.js test/api-pages.test.js` 与全量 `npm test`（`TMPDIR` 指向系统临时目录）。

> **运行环境的必要条件披露**：服务端 `GET/POST /api/projects` 在本 worktree **不存在**（`grep -rn "api/projects" src/` 无命中；`curl /api/projects` → `404 {"error":"not found: GET /api/projects","code":"NOT_FOUND"}`），该路由属 pr-002，尚未落地（`.pb-agents/worktrees/` 下只有 pr-001 / pr-003，无 `feat/pr-002` 分支）。因此凡需要项目数据的运行期观测，均在浏览器**网络层**以 stub 响应（`page.setRequestInterception`）补齐该依赖，**以隔离并检验前端自身行为**；stub 只在浏览器内存中生效，未写入任何文件、未改动任何产物。凡受此影响的判定，下文逐条标注。

---

## 一、PR 文件「验收标准」逐条判定

| # | 验收条目（摘） | 判定 | 可定位证据 |
|---|---|---|---|
| AC-1 | `index.html` 含全部新增静态节点；`#project-bar` 在 `.topnav` 之后；`#btn-back-projects` `href="/"`；`main.layout` 既有元素 + `.hidden` | **pass** | `web/index.html:20-22`（`#project-bar`/`#btn-back-projects href="/"`/`#current-project-name`，位于 `</nav>`(:18) 之后、`.topright`(:24) 之前，且在 `<header class="topbar">`(:10)~`</header>`(:29) 之内）；`:32-40`（`#projects-view` + `#project-repo`/`#project-name`/`#btn-create-project`/`#project-list`/`#project-hint` 五个子节点）；`:43`（`<main class="layout hidden">`）。运行期实测全部节点存在：`hasRepoInput/hasNameInput/hasCreateBtn/hasProjectList/hasHint = true`，`backHref = "/"`，元素为 `A` 标签、文案 `← 项目列表` |
| AC-2 | `init()` 为 boot 分派：`loadAgents` → `connectAgentEvents` → `resolveCurrentProject()`；`/` ⇒ 列表视图 + `main.layout` 隐藏 + 不调 `/api/chats`；已知 id ⇒ 工作台；未知 id ⇒ 回落列表视图不报错 | **pass** | `web/app.js:977-988`（`await loadAgents()` :978 → `connectAgentEvents()` :979 → `const project = await resolveCurrentProject()` :980 → `if (!project) { showProjectList(); return; }` :981-984 → `showWorkspace(project)` :985 → `loadChats()` :986 → `renderChat()` :987）。运行期 `/`：`projects-view` computed `display:block`、`main.layout` computed `display:none`、`project-bar` `display:none`、composer `offsetParent === null`；**实际请求集 = `GET /api/agents`、`GET /api/projects`、`GET /api/events`，无任何 `/api/chats`**。运行期 `/?project=does-not-exist`（无 stub、真实 404 后端）：仍为列表视图（`projects-view` block / `main` none / `project-bar` none），无报错、无空工作台。已知 id 分支（`/?project=p2`，`/api/projects` 走 stub）：`main` `display:flex`、`project-bar` `display:flex`、composer 可见、`GET /api/chats?project_id=p2` |
| AC-3 | 三处调用点带项目参数：`loadChats()`/`loadArchived()` URL 含 `project_id=`（`encodeURIComponent`）；`send()` 请求体含 `project_id: state.projectId` | **pass** | `web/app.js:668`（`` api(`/api/chats?project_id=${encodeURIComponent(state.projectId)}`) ``）；`:683-685`（`` `/api/chats?archived=1&limit=${ARCHIVE_PAGE_SIZE}&offset=${offset}&project_id=${encodeURIComponent(state.projectId)}` ``）；`:751`（payload 增 `project_id: state.projectId`）。运行期实测请求序列：`GET /api/chats?project_id=p2`、点击「归档」后 `GET /api/chats?archived=1&limit=200&offset=0&project_id=p2`；工作台内发送实测请求体逐字为 `{"agent_id":"dev-1","text":"@dev-1 hello","project_id":"p2"}` |
| AC-4 | `loadProjects()`/`renderProjects()`/`createProject()`/`showProjectList()`/`showWorkspace()` 齐备；行显示 名称+地址+对话数+最近活动时间（`null` → `—` 不报错）；创建成功停留列表并原地出现（不跳转、不轮询）；创建失败（400/409）文案落 `#project-hint` | **pass** | 函数齐备：`app.js:579`(loadProjects)/`:591`(renderProjects)/`:615`(createProject)/`:651`(showProjectList)/`:659`(showWorkspace)。行信息量运行期实测两行渲染为：`ProjA \| https://x/a.git \| 2 个对话 \| 1h 前`、`ProjB \| https://x/b.git \| 0 个对话 \| —`（`last_activity_at: null` → `—`，未抛错；实现 `app.js:601` 用 `fmtAgo(p.last_activity_at ?? undefined)` 走既有 `fmtAgo` 的 `Number.isFinite` 分支，见 `app.js:77-84`）。创建成功（stub 201）实测：`location.href` 仍为 `http://127.0.0.1:41917/`（无 `project` 参数）、`main`/`project-bar` 仍为 `none`、项目列表首行出现 `ProjNew`、两个输入框被清空、期间请求恰为 `POST /api/projects` + `GET /api/projects` 各一次（无轮询）。空仓库地址点击实测：`#project-hint` 文案 `请填写仓库地址`、`class="project-hint error"`；无后端时失败路径实测文案 `not found: POST /api/projects` 落 `#project-hint`（`class` 含 `error`），输入框内容保留、页面未跳转。见偏差 D-02 |
| AC-5 | `style.css` 追加三条真隐藏规则（`display:none`），既有规则逐字不动 | **pass** | `web/style.css:366-368`：`.layout.hidden { display: none; }` / `.projects-view.hidden { display: none; }` / `.project-bar.hidden { display: none; }`；`git diff --numstat` = `52 0`（**零删除行**，纯追加，新增块 `:365-416`）；必要条件溯源：既有 `.hidden` 确认是逐组件定义（`style.css:64` `.agent-panel.hidden`、`:203`、`:204`、`:295`），无全局规则 |
| AC-6 | 零新增静态资产：`STATIC_FILES`（12 键）与 `GET /` → `web/index.html` 不变；既有前端契约断言仍绿 | **pass** | `git diff --name-only iteration/0017-project-workspace...HEAD` 仅三文件，`src/` 零改动（`git diff --stat -- src/` 空输出）⇒ `src/web.js` 未变；`src/web.js:261-274` 的 `STATIC_FILES` 实测 12 个键（`/`、`/index.html`、`/app.js`、`/style.css`、`/docs`、`/docs.js`、`/debug`、`/debug.js`、`/api-pages.css`、`/llms.txt`、`/API.md`、`/README.md`），`'/': 'web/index.html'` 在 `:262`。断言面实测：`grep -c "POLL_MS" web/app.js` = 0、`grep -c "setTimeout(tick"` = 0；`web.test.js` + `api-pages.test.js` 44/44 通过（见第五节） |
| AC-7 | `app.js` 零改写清单：SSE 订阅 / 归档 / 关闭 / 激活 / 改名 / 标题行内编辑 / `@` 补全 / working 计时 / `isReadonly` / 列表内存过滤；页面文案不含"已 clone / 已在项目根目录" | **pass** | `git diff -U0` 的 `app.js` 全部删除行仅 **4 行**，逐行为 `app.js:668`（loadChats URL）、`:683`（loadArchived URL）、`:751`（send payload）、旧 `connectAgentEvents()`（旧行 886，现前移至 `:979`，属 AC-2 明示顺序）；其余 105 行为纯新增（改动块边界仅 `@@ -18,0 +19,3 @@`、`@@ -573,0 +577,89 @@`、`@@ -825,0 +920 @@`、`@@ -883,0 +979,7 @@`），零改写清单涉及区域不在任何改动块内。`grep -rn "已 clone\|已在项目根目录\|clone" web/` 无命中 |

---

## 二、「文件范围」合规

| 检查 | 判定 | 证据 |
|---|---|---|
| `git -C .../pr-003 diff --name-only iteration/0017-project-workspace...HEAD` 恰为 PR 声明的三个文件（不多不少） | **pass** | 实测输出逐字为 `oamp/web/app.js` / `oamp/web/index.html` / `oamp/web/style.css`，与 PR「文件范围」三条完全一致；`git status --porcelain` 空 ⇒ 无未提交游离改动；`--numstat` = `109 4`（app.js）、`18 1`（index.html）、`52 0`（style.css），`git diff --stat` 合计 `175 insertions(+), 5 deletions(-)`，与三文件逐行审计一致 |

---

## 三、F04 / F05 产品维度验收逐条判定

### F04（首屏项目列表页）

| # | 验收条目（摘） | 判定 | 可定位证据 |
|---|---|---|---|
| F04-1 | 首屏即项目列表（未选项目打开页面，主体是项目列表） | **pass** | 运行期 `/`：`#projects-view` computed `display:block`（含 `.projects-title`「项目」、新建输入区、`#project-list`），`main.layout` `display:none` |
| F04-2 | 未选项目时看不到对话、不能发消息 | **pass** | 运行期 `/`：`main.layout` 整块 `display:none`（对话左栏/右栏/composer 均在其中），`document.getElementById('input').offsetParent === null`；请求集中**完全没有 `/api/chats`**（列表视图下实测请求仅 `/api/agents`、`/api/projects`、`/api/events`）⇒ 既无可见对话内容、也无可用发送入口 |
| F04-3 | 首屏可新建项目，最小输入 = 仓库地址，名称可留空 | **pass** | `web/index.html:34-37`（两个 `input` + 一个 `button`）；`app.js:615-638`：`repo_url = $('project-repo').value.trim()` 必填（空则 `请填写仓库地址` 提示 + `return`，`:619-624`），`name` 仅在非空时入 body（`:626-627`）⇒ 名称可留空。运行期空仓库地址点击实测如上；运行期填写仓库地址 + 留空名称时 `POST /api/projects` body 只含 `repo_url`（实测：仓库地址输入 `"  https://x/new.git  "`、名称输入 `"   "`（纯空白）→ 请求体逐字为 `{"repo_url":"https://x/new.git"}`，即 repo 已 trim、空名不入 body） |
| F04-4 | 列表中每个项目都能进入其工作台 | **pass** | `app.js:591-611`：每行渲染为 `<button class="project-item" data-project="…">`，逐个绑定 `onclick → location.href = '/?project=' + encodeURIComponent(projectId)`；运行期实测 `data-project` 为 `p1`/`p2`，且 `/?project=p2` 确实进入工作台（`main display:flex`，composer 可见） |
| F04-5 | 列表信息量：名称 + 仓库地址 + 对话数 + 最近活动时间；后两项由既有对话数据派生、零新增存储；无对话显示占位不报错 | **partial** | **前端展示义务 pass**：四项全部渲染（运行期两行实测 `ProjA \| https://x/a.git \| 2 个对话 \| 1h 前`；`ProjB \| … \| 0 个对话 \| —`），`null` 走既有 `fmtAgo` 的 `Number.isFinite` 分支渲染 `—`（`app.js:77-84`、`:601`），无额外存储字段、无前端派生计算（`renderProjects` 只读 `chat_count` / `last_activity_at` 原值）。**服务端面 blocked**：卡的判定口径要求「2 个对话 → 对话数为 2、最近活动时间不早于该消息时间」，其数据源 `GET /api/projects` 的聚合（`COUNT(chats)` 含归档/关闭、`MAX(chats.updated_at)`）属 pr-002，**在本 worktree 不存在**（`curl` → 404，`src/` 无该路由）⇒ 该判定无法在本产出物内执行。见偏差 D-01 |
| F04-6 | 创建后停留列表页、该项目原地出现（无需手动刷新）、不自动跳进工作台 | **pass** | 运行期（`/api/projects` 走 stub，条件见偏差 D-01）：创建后 `location.href = http://127.0.0.1:41917/`（无 `project` 参数）、`main`/`project-bar` 仍 `none`、首行出现新项目 `ProjNew`、输入框清空；请求序列恰为 `POST /api/projects` + `GET /api/projects` 各一次（无轮询，`app.js:615-638` 无定时器；`POLL_MS` 计数 0）。服务端持久化本身不在本产出物范围 |

### F05（项目工作台）

| # | 验收条目（摘） | 判定 | 可定位证据 |
|---|---|---|---|
| F05-1 | 进入项目即进入工作台，沿用既有三栏布局 | **pass** | 运行期 `/?project=p2`：`main.layout` computed `display:flex`（composer `offsetParent` 非空）；`#projects-view` `display:none`。整页导航进入（`app.js:605-609`；`location.href` 语句在 `:608`），未新增页面资产（`STATIC_FILES` 12 键未变） |
| F05-2 | 顶栏显示当前项目（最小口径 = 项目名） | **pass** | 运行期 `/?project=p2`：`#project-bar` computed `display:flex`（`app.js:659-661`），`#current-project-name.textContent === "ProjB"`（`app.js:662`，取值自 `resolveCurrentProject` 在 `GET /api/projects` 全量列表内的定位结果 `app.js:641-648`） |
| F05-3 | 顶栏提供返回项目列表入口，点击后回到首屏项目列表 | **pass** | `web/index.html:21` 静态元素 `<a id="btn-back-projects" href="/">← 项目列表</a>`（未选中项目时该 `#project-bar` 整体隐藏，`:20` 带 `hidden` 初值）；运行期实测元素为 `A`、`href="/"`、文案 `← 项目列表` ⇒ 浏览器原生整页导航回落 `/` |
| F05-4 | 工作台内对话范围 = 当前项目；切到项目 B 或返回列表页后 A 的对话不可见 | **partial** | **前端层 pass**：列表视图下零 `/api/chats` 请求（见 F04-2）；工作台内两处取数 URL 均带 `project_id`（`app.js:668`、`:683-685`）；返回/切换走整页导航（`href="/"` 与 `location.href=…`），上一个项目的 `state.chats`/`state.chat`/SSE/归档页随页面销毁天然清空（无页内切换路径）。**DB 级隔离 blocked**：「切到 B 后 A 的对话不可见」依赖服务端对 `project_id` 的过滤（pr-002），本 worktree 无该面；实测 `GET /api/chats?project_id=p2` 中的 `project_id` 被既有路由忽略（无此过滤实现）。见偏差 D-01。等价证据：`grep -rn "project_id|projectId" src/` 在 `oamp/src` 下**零命中**，即该 worktree 的服务端从未读取该参数（未知查询参数不被校验），故前端已发出的 `project_id` 在此冻结态下不产生任何过滤效果 |
| F05-5 | 工作台内新建对话自动归属当前项目，全程无项目选择步骤 | **partial** | **前端层 pass**：工作台内实测发送请求体逐字 `{"agent_id":"dev-1","text":"@dev-1 hello","project_id":"p2"}`（`app.js:751`），UI 上不存在任何项目选择控件（`#projects-view` 工作台态 `display:none`），`#btn-new` 既有语义未改（`app.js` 零改写清单内）。**归属落库 blocked**：「该对话归属 A、出现在 A 的对话列表」由服务端（pr-002 的必填 `project_id` 面 + 过滤）决定，本 worktree 无该面。见偏差 D-01 |
| F05-6 | 既有对话能力照常可用（发消息、实时订阅、归档/关闭/激活/改名等，语义与状态机不变） | **pass** | `git diff -U0` 证明 SSE 订阅/`handleEvent`/`appendChunk`、归档/关闭/激活/改名、标题行内编辑、`@` 补全、working 计时、`isReadonly`、列表内存过滤所在行**零改写**（全部删除行仅 4 行，均非这些区域）；全量 `npm test` 251/251 通过（含 SSE、归档分页、改名、错误契约、集群动作等既有语义用例）；运行期工作台内 `/api/events` 全局事件流按既有方式建立（`app.js:979`） |

---

## 四、既有行为零变化

| 检查 | 判定 | 证据 |
|---|---|---|
| 本 PR 是否改写了任何 `oamp/test/**` 文件（不应改写） | **pass（零改写）** | `git diff --name-only iteration/0017-project-workspace...HEAD -- oamp/test/` **空输出**（测试目录与迭代分支逐字节一致）；`git diff --stat -- test/` 亦为空 |
| 既有前端静态断言零改写 | **pass** | 断言源码逐字未变：`test/web.test.js:295` = `assert.match(html, /Web Console/);`；`:1030-1031` = `assert.doesNotMatch(appJs, /POLL_MS/, …)` 与 `assert.doesNotMatch(appJs, /setTimeout\(tick/, …)`；`:1606` = `assert.match(html, /id="conn-status"[\s\S]*id="agent-panel"/, …)`；`test/api-pages.test.js:59-63` = `/docs`、`/debug` 两个 href 与 `Workspace`/`Agents`/`Tasks` 三个占位项逐字断言；`:188-189` = 页面内 `href="/docs"`、`href="/debug"` 断言。文件整体无 diff（上一行）⇒ 零改写成立 |
| 上述断言通过 | **pass** | 命令 `node --test test/web.test.js test/api-pages.test.js` → `# pass 44 / # fail 0`（含承载上述断言的用例：`Web：静态页可访问 + /api/agents 返回在线 agent（F08-d 等价）`、`Web：前端契约——轮询消失、SSE 订阅、新控件、@ 与 ! 保留（F08-4）`、`Web：前端静态契约——顶栏 agent 列表（…）`、`接口页静态契约：控制台顶栏 +2 真实入口（/docs、/debug），既有 3 个占位项逐字未变（F03-1/2、F04-1）`、`新页面 HTTP 可达性与响应类型：…（PR-002 验收 1）`）；全量 `npm test` → `# pass 251 / # fail 0` |

---

## 五、实际执行证据（命令与结果如实记录）

在 `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/pr-003/oamp` 下执行；`TMPDIR` 指向系统临时目录，未污染仓库。

| # | 命令 | 结果 |
|---|---|---|
| E-1 | `TMPDIR=$(mktemp -d) node --test test/web.test.js test/api-pages.test.js` | `1..44` / `# tests 44` / `# pass 44` / **`# fail 0`** / `cancelled 0` / `skipped 0` / `duration_ms 56801.586833` |
| E-2 | `TMPDIR=$(mktemp -d) npm test`（= `node --test test/*.test.js`，全量） | `1..251` / `# tests 251` / `# pass 251` / **`# fail 0`** / `cancelled 0` / `skipped 0` / `duration_ms 59210.59475` |
| E-3 | `node bin/oamp.js web start --port 41917`（`OAMP_DB=/tmp/pr003-verify/sql.db`、`OAMP_SOCKET=/tmp/pr003-verify/sock`，就绪信号 `WEB_READY` 实测命中）+ 无头 Chromium 观测 | 见第一节 AC-2/AC-3/AC-4、第三节各项的「运行期实测」证据；关键观测：`/` 请求集无 `/api/chats`；`/?project=does-not-exist` 回落列表视图无报错；`/?project=p2` 顶栏显示 `ProjB` 且 chats/archive URL 带 `project_id=p2`；`POST /api/messages` body 含 `project_id:"p2"`；创建成功后停留 `/` 且新项目原地出现 |
| E-4 | `curl -s http://127.0.0.1:41917/api/projects` | `404` + `{"error":"not found: GET /api/projects","code":"NOT_FOUND"}`（用于确认 pr-002 服务端面在本 worktree 缺失，非产物缺陷） |

> E-3 中凡涉及项目数据的 `/api/projects` 响应由浏览器网络层 stub 提供（原因与影响见开头披露与偏差 D-01）；`/api/chats`、`/api/messages`、`/api/agents`、`/api/events` 不 stub 时同样被观测，用于 AC-2「不调 `/api/chats`」与 AC-3 URL/请求体证据。

---

## 六、汇总

| 判定 | 条目数 | 明细 |
|---|---|---|
| **pass** | 19 | PR-AC 1~7（7）；文件范围（1）；F04-1/2/3/4/6（5）、F05-1/2/3/6（4）；测试零改写（2） |
| **partial** | 3 | F04-5（服务端聚合面不可判）、F05-4（DB 级跨项目隔离不可判）、F05-5（归属落库不可判）——三者同一根因：pr-002 未落地 |
| **fail** | 0 | — |
| **blocked** | 0 | 产出物本身可判；三处 partial 的不可判部分源于外部未交付依赖，非产出物无法对照标准 |

**逐项合计 22 项：pass 19 / partial 3 / fail 0 / blocked 0。**

---

## 七、偏差记录

| ID | 偏差 | 影响 | 证据 |
|---|---|---|---|
| D-01 | 本 worktree 冻结的代码（`feat/pr-003`，HEAD `40151ff`）中**不存在** `GET/POST /api/projects` 服务端面（该面属 pr-002，`feat/pr-002` 分支/worktree 均不存在）；运行期凡涉及项目数据的观测均由浏览器网络层 stub 提供 | 使 F04-5 的服务端聚合面、F05-4 的 DB 级隔离、F05-5 的归属落库三项无法在本产出物内判定（partial）；不影响本 PR 自身验收标准 1~7 与文件范围/零改写判定。PR 文件已自述「运行期互补」，与实测一致 | `grep -rn "api/projects" src/` 无命中；`curl /api/projects` → 404（E-4）；`ls .pb-agents/worktrees/` 仅 `pr-001`/`pr-003` |
| D-02 | `/?project=<未知 id>` 的回落路径**重复请求一次 `GET /api/projects`** | 非验收违规（AC-2 只要求「不报错、不渲染空工作台」，实测满足）；属多一次请求的冗余，不影响正确性 | 实测请求序列（stub 后端）：`GET /api/agents`、`GET /api/projects`、`GET /api/events`、`GET /api/agents`、`GET /api/projects` —— 两条 `GET /api/projects`。成因：`resolveCurrentProject()`（`app.js:641-648`，内含 `loadProjects()`）返回 `null` 后，`showProjectList()`（`app.js:651-656`）再调一次 `loadProjects()` |
| D-03 | PR 文件「参考资料」与验收标准第 6 条写作 `web.js`（`oamp/web.js`），实际该文件位于 `oamp/src/web.js`；「文件范围」三文件不含 `src/web.js` | 文档措辞偏差，产物本身合规（`src/` 零改动、`STATIC_FILES` 12 键未变已实测） | `find . -name "web.js"` → `./src/web.js`；`git diff --stat -- src/` 空输出 |
| D-04 | 未知 id 回落路径下 `state.projectId` 保留为未知值（`app.js:644` 赋值后不回滚），未重置为 `null` | 现状无消费者（列表视图不取 `/api/chats`），不构成现状缺陷；属潜在脏状态继承面 | `app.js:641-648`（`state.projectId = projectId` 后 `find` 得 `null` 即返回，无重置）；`showProjectList()` `app.js:651-656` 未触碰 `state.projectId` |

---

## 八、下一迭代候选

1. **pr-002 落地后补验三项 partial**（优先级最高）：在同一工作树内做真实端到端——建 1 个项目 + 该项目下 2 个对话并发送 1 条消息 → 断言列表「对话数为 2」「最近活动时间不早于该消息时间」；进入项目 B 断言 A 的对话不可见；工作台内新建并发消息 → 断言该对话归属当前项目并出现在该项目列表。
2. 消除 D-02 的重复请求：`showProjectList()` 复用 `resolveCurrentProject()` 已取回的 `state.projects`（或仅在未取过数时 `loadProjects()`）。
3. 消除 D-04 的潜在脏状态：未知 id 回落时重置 `state.projectId = null`（或在 `showProjectList()` 内统一清理项目选择态）。
4. 订正 PR 文件中的 `oamp/web.js` 引用为 `oamp/src/web.js`（D-03）。

---

## 九、结论

# 结论：PASS

- 本 PR 自身交付范围内的 **7 条验收标准全部 pass**，**文件范围恰为声明的三个文件**，**`oamp/test/**` 零改写**、既有前端静态断言零改写且通过，**全量测试 251/251 通过**，`app.js` 仅 4 行删除（均为 AC-2 明示的三处调用点带参与 boot 顺序前移），SSE/归档/改名/补全/计时/只读/过滤等既有行为零触碰。
- **fail 条目数：0**；**偏差记录条数：4（D-01~D-04）**。
- 3 项 partial（F04-5 服务端聚合面、F05-4 DB 级隔离、F05-5 归属落库）**全部且仅仅**源于 pr-002 服务端面在本 worktree 未落地（D-01），并非本 PR 的缺陷或未完成项；这三项须在 pr-002 合并后按第八节第 1 条补验，在此之前不应视为已验证。
