# pr-001 独立验收报告（阶段 5 · PR 级）

**验证日期**: 2026-09-12 16:34:34 CST
**验证者身份（反射结果）**: 产出物 = agent 进程内两处生产代码改动（`oamp/src/agent.js` / `oamp/src/context-pool.js` 的项目上下文注入落地）⇒ 反射为 **同级代码审查者**，并对"可观测行为"部分兼任**端到端行为观测者**（以独立装置复现一次性/常驻两条路径，不用被验证方的测试）
**产出物**（只读）:
- 代码：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/pr-001`（分支 `feat/pr-001`，HEAD `2e9a950`）
- PR 单：`docs/iterations/0017-project-workspace/prs/pr-001-project-context-injection.md`
- 功能卡：`docs/iterations/0017-project-workspace/prd/F06-dispatch-payload-project-context.md`、`prd/F07-context-content-and-injection.md`
- 架构落地契约（判定尺度）：`docs/iterations/0017-project-workspace/architecture.md` §4.1 / §4.2 / §4.3

**验证标准来源**: 主 agent 委托内联的 5 条验证标准（PR 验收标准逐条 / 文件范围 / F06+F07 产品维度 / 既有测试零改写 / 实际执行证据）
**独立性声明**: 未读取 `history.md`、dev 报告、本迭代 `clarifications/` 下任何既有文件，未与 DevPr001 / DevPr003 交流执行过程；未修改 worktree 内任何文件，未执行任何 git 写命令。定向测试与独立观测装置的全部临时产物落在系统临时目录（`/tmp/oamp-verify-pr001/`），worktree 保持 `git status --short` 空。

---

## 一、逐项判定

### 标准 1：PR 文件「验收标准」逐条判定（7 条）

| # | 判定 | 证据 |
|---|---|---|
| 1 | **pass** | ① 唯一渲染点存在且为模块级纯函数：`oamp/src/agent.js:57-59` `renderProjectContext()`，全文 `grep -rn "【项目上下文】" src/` **仅命中 `agent.js:58` 一处**；输出恰为 `【项目上下文】` + `- 项目名：` / `- 仓库地址：` / `- 工作约定：` 三行。② 载荷解析：`agent.js:43-50` `parseProject()`（三要素均为非空字符串才返回对象），在 `agent.js:81` `parseTaskBody` 内调用（`const project = parseProject(body.project);`）。③ 三行取值全部来自载荷字段：`agent.js:58` 逐字插值 `project.name` / `project.repo_url` / `project.agreement`；`grep -rn "该项目仓库\|自选落点\|PROJECT_AGREEMENT" src/` **零命中** ⇒ 未复制约定文本；`agent.js:13-20` 的 import 列表（config/node-client/log/context-pool/role-binding/node:child_process/fs/crypto）**不含 `web.js`** ⇒ 不反向依赖。④ 行为级：独立观测断言 `block(PROJECT) === 【项目上下文】\n- 项目名：demo\n- 仓库地址：https://github.com/acme/demo.git\n- 工作约定：<载荷原文>`（观测装置测试 1、2 均通过）。 |
| 2 | **pass** | 合法对象 ⇒ 注入（观测测试 1-a：末位 argv 含块）；缺失 / `null` / `'a-string'` / `42` / `['x']` / `{}` / 缺 `agreement` / `agreement: ''` / `name: 1`（共 8 种非法形态）⇒ 全部 `state: 'completed'`（**不拒收任务**）且末位 argv **逐字等于原文、无任何注入痕迹**（观测测试 1-c）。体例对照既有 `label`：`agent.js:80` `const label = typeof body.label === 'string' ? body.label : null;` 与 `parseProject` 同处、同风格。 |
| 3 | **pass** | 观测测试 1-a：`FAKE_ACP_ARGS_LOG` 记录的 argv 末位 = `【项目上下文】\n- 项目名：demo\n- 仓库地址：https://github.com/acme/demo.git\n- 工作约定：…\n\nP-合法`，且 argv 中仅末位含块（`last1.filter(a => a.includes('【项目上下文】')).length === 1`，其余元素不含 `项目名`/`repo_url`）；未携带时末位 `=== 'P-未携带'`。施加点确为 `agent.js:198` `args.push(projectContext ? `${projectContext}\n\n${task.prompt}` : task.prompt)`（原 `agent.js:173` 的 `args.push(task.prompt)` 处）。<br>⚠ 措辞偏差见「偏差记录 D-1」：PR 写作「+ 换行」，实现为 `\n\n`，与 architecture §4.2 逐字一致。 |
| 4 | **pass** | 观测测试 2（经真实 agent → ContextPool → fake ACP，观测量 = fake ACP 记录的 `session/prompt` 文本）：同 chat `chat-e2e` 首轮文本 `= 块 + \n\n + D-首轮`；次轮文本 `=== 'D-次轮'`（**不重复注入**）且 `pid` 与首轮相同（复用同一常驻实例）；新 chat `chat-other` 文本再次带块（`pid` 不同）；未携带 project 的全新 chat 文本 `=== 'D-无项目'`。重建重注入：观测测试 3-a（崩溃后同 chat 下轮新会话文本带块、`pid` 变化）、3-b（`pool.release()` 后同 chat 下轮文本带块、会话对象与 `pid` 均变化）。 |
| 5 | **pass** | 三个分支均实测：<br>· 崩溃：观测测试 3-a —— 首轮（带块）以 `context_crashed` 失败后 `s1.sentTurns === 0`；同 chat 下一轮重建会话文本**重新带块**。<br>· `model_unavailable`：观测测试 3-c —— `sentTurns === 0`、且故障轮**无 `session/prompt` 记录**（送达前失败）；随后送达轮文本 `= 块 + \n\n + MU2` 补上注入，`sentTurns` 依次 1 → 2，第三轮不再注入。<br>· `context_busy`：观测测试 4 —— 10 轮并发（1 在飞 + 8 排队）第 10 轮 `context_busy`；`prompt_start` 数 `=== 9`、被拒轮原文 `BU10` **从未送达 ACP**、`session.sentTurns === 9`（被拒轮不贡献任何计数）。代码侧同证：`context-pool.js:148-150`（`context_busy` 在 `queue.push` 之前 return）与 `sentTurns` 唯一自增点 `context-pool.js:184`（在 `client.prompt` 成功返回之后）。 |
| 6 | **pass** | ① 22 个既有测试文件零改写：`git diff --name-only iteration/0017-project-workspace...HEAD -- oamp/test` **输出为空**；`git show --stat HEAD` 仅 2 个 src 文件。② 全量绿：`cd /…/pr-001/oamp && npm test` → `# tests 251 / # pass 251 / # fail 0`（耗时 55.9s）。③ 既有观测面仍为用户原文：观测测试 1 断言 `task.update{event:'started'}.prompt === 'P-合法'`、agent stdout 行 `TASK_STARTED … label=L-合法`（label 未被污染）；`agent.js:308-317`（daemon 侧 `TASK_STARTED` 只带 `task.label`）与 `agent.js:200-201`（omp 侧 `sendUpdate` 只发 `task.prompt.slice(0,500)`）逐行核对一致。 |
| 7 | **pass** | ① 协议方法面：`git diff …` 未触碰 `src/router.js` 与任何协议文档；README.md:107-108 的公开方法面逐字未变（7 个：`agent.register` / `agent.heartbeat` / `agent.deregister` / `message.send` / `message.deliver` / `message.ack` / `router.status`）；`project` 只出现在 `payload.body` 内容层（`agent.js:81`）。② 上下文池语义：`context-pool.js` 全部 diff = `+this.sentTurns = 0`（构造）+ `+projectContext = null`（可选参数）+ `+projectContext`（turn 携带）+ `_pump()` 内 2 行前缀判定与 1 行自增——键（`getOrCreate`）、FIFO、`QUEUE_LIMIT = 8`、LRU（`_evictIfNeeded`）、并发（`inFlight`）**零改动**；`node --test test/context-pool.test.js test/omp-executor.test.js test/acp-daemon.test.js` → `# tests 27 / # pass 27 / # fail 0`。 |

### 标准 2：「文件范围」合规

**判定: pass**

- 命令与输出：`git -C /…/.pb-agents/worktrees/pr-001 diff --name-only iteration/0017-project-workspace...HEAD` → `oamp/src/agent.js`、`oamp/src/context-pool.js`（**恰 2 个，无多无少**）；`diff --stat` → `2 files changed, 39 insertions(+), 5 deletions(-)`。
- PR 文件「文件范围」列出的即为该 2 个文件（均标注「修改」）⇒ 两者**逐字相等**（不是"子集"）。
- 补充：`git status --short` 为空（无未提交改动），HEAD = `2e9a950`，与委托给定一致。

### 标准 3：F06 / F07 产品维度验收逐条判定

**F06（载体与路径范围）** —— 判定基于 HEAD 上"载体是否存在"的证据

| # | 判定 | 证据 |
|---|---|---|
| 1 结构化项目字段（派发侧） | **blocked** | 派发装配端在 HEAD 上不存在：`grep -c "PROJECT_AGREEMENT\|body.project" oamp/src/web.js` → `0`（`web.js` 全域无项目代码）；pr-001 文件范围不含 `web.js`。接收侧已落地（`agent.js:43-50` 解析 `{name, repo_url, agreement}` 三要素）——缺的是"载荷由谁装出来"，无法在本 PR 判定。 |
| 2 两条 LLM 路径都携带 | **blocked** | 同 1：`task.request` 的载荷装配点（`POST /api/messages` 的两条 LLM 分支）在本 PR 未改动。agent 侧两条路径**均能消费** `project` 已实测（观测测试 1 一次性路径 / 测试 2 常驻路径）。 |
| 3 shell 路径不携带 | **blocked** | 载荷端证据缺失（同 1）。agent 侧半边证据：`agent.js:140` shell 分支返回体为 `{executor:'shell', command, args, timeoutMs, label}`，**不含 `project`** ⇒ 即使载荷误带，shell 路径也不会注入。 |
| 4 用户原文不被污染 | **blocked** | 判定载体是对话库 `messages.text`，由 web/persist 写入，本 PR 未触碰该面（`grep -c` 于 `web.js` / `persist.js` 均为 `0`）。agent 侧结构性证据：注入只发生在 `spawn` 的 argv 末位与 ACP `session/prompt` 文本，`agent.js` 全文件不写 `messages` 表。 |
| 5 不改协议方法面 | **pass** | 见标准 1 第 7 条 ①。 |
| 6 不改上下文池键与串并发语义 | **pass** | 见标准 1 第 7 条 ②；`context-pool.test.js` 的键复用/新 chat 隔离/同键串行/上限 8/异键并发/LRU 用例全部通过（含于 27 项定向测试）。 |

**F07（内容与 agent 侧注入）**

| # | 判定 | 证据 |
|---|---|---|
| 1 内容三要素 | **partial** | 已通过的子项：渲染三行（项目名 / 仓库地址 / 工作约定）结构成立且取值逐字来自载荷（标准 1 第 1 条④）。未通过的子项：三要素中「一段**相对**工作约定」的**文本本体**在 HEAD 上不存在——`grep -rn "该项目仓库\|自选落点\|PROJECT_AGREEMENT" src/` 零命中，`web.js` 无 `PROJECT_AGREEMENT` 常量 ⇒ "约定写的是什么"在本 PR 无内容可核。 |
| 2 约定为相对约定、无绝对路径 | **blocked** | 无约定文本可读（同 1）。仅能给出结构性旁证：`agent.js:58` 是纯插值，不产生任何路径字样。 |
| 3 约定覆盖三件事 | **blocked** | 无约定文本可读（同 1）。 |
| 4 由 agent 侧注入 | **pass** | `src/` 全域 `【项目上下文】` **唯一出现点 = `agent.js:58`**（渲染点）；`agent.js` import 列表不含 `web.js`；`web.js` 无注入文本（`grep -c` = 0）；两处施加点均已实测（`agent.js:198` 一次性 argv；`agent.js:326` → `context-pool.js:178` 常驻首轮）。 |
| 5 传达可核对（仓库地址字符串出现） | **pass** | 观测测试 1：one-shot 的 `task.update{kind:'stdout'}` 行含 `https://github.com/acme/demo.git`（argv 末位确被 omp 收到）；观测测试 2：常驻路径 `task.result.text` 含该地址（fake ACP 回显 `收到：<注入后文本>`）。判定口径遵守 C-4：只判"出现"，不判是否真的 clone。注：完整链路（对话→web 装配）待 pr-002，本次以直投 `task.request` 的等价载荷观测。 |
| 6 产物落点约定随上下文下达 | **blocked** | 载体是 `agreement` 文本第 ③ 条，该文本真源未落地（同 1）。 |
| 7 不出现超能力承诺 | **blocked** | 待检索对象（注入内容与页面文案）在 HEAD 上均不存在（`web.js` / `index.html` 无项目文案；`agent.js` 无文案常量）。 |

### 标准 4：既有行为零变化（是否改写 `oamp/test/**`）

**判定: pass**

- `git diff --name-only iteration/0017-project-workspace...HEAD -- oamp/test` → **空输出**（未改写任何既有测试文件）。
- `git show --stat 2e9a950` → 仅 `oamp/src/agent.js`（29 增 / 2 删）与 `oamp/src/context-pool.js`（10 增 / 3 删），合计 39 增 / 5 删。
- 行为面零变化另由全量套件独立证明：`npm test` 251/251 通过。补充核对：`grep -rn "project" test/` 显示既有用例**均不携带 `project` 载荷**（唯一 `project*` 命中为 `api-routes.test.js` 的路由投影函数名与 `acp-daemon.test.js` 的本地变量名，与项目上下文无关）⇒ 既有 `web.test.js` / `acp-daemon.test.js` 的 argv 与回显断言落在"未携带即不注入"分支上，因此对注入改动天然免疫；这也意味着"携带时应注入"在仓库内**无既有断言**（见偏差记录 D-3）。

### 标准 5：实际执行证据

**判定: pass** —— 全部命令与结果如实记录（均在 worktree 的 `oamp/` 下运行，未产生仓库内临时产物）

| # | 命令（cwd） | 结果 |
|---|---|---|
| E1 | `npm test`（`/…/worktrees/pr-001/oamp`） | `# tests 251 / # suites 0 / # pass 251 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0`；`duration_ms 55921.9`；node `v22.15.0` |
| E2 | `node --test test/context-pool.test.js test/omp-executor.test.js test/acp-daemon.test.js`（同 cwd） | `# tests 27 / # pass 27 / # fail 0`；`duration_ms 24904.5` |
| E3 | `node --test observe.mjs`（`/tmp/oamp-verify-pr001`，独立观测装置，见附录） | `1..4 / # pass 4 / # fail 0`，退出码 0；日志 `/tmp/oamp-verify-pr001/observe-run1.log` |
| E4 | 结构证据命令 | `git diff --name-only/--stat`、`git status --short`、`grep -rn` 若干（逐条已在上文引用） |

---

## 二、汇总

| 判定 | 计数 | 条目 |
|---|---|---|
| **pass** | **14** | PR 1~7（7）· 文件范围（1）· F06-5/6（2）· F07-4/5（2）· 既有测试零改写（1）· 执行证据（1） |
| **partial** | **1** | F07-1（三要素结构成立；约定文本本体未落地） |
| **blocked** | **8** | F06-1/2/3/4、F07-2/3/6/7 —— **同一根因**：派发装配端（`web.js` 的 `PROJECT_AGREEMENT` 与 `payload.body.project` 装配）在 HEAD 上不存在，属投递端 PR（PR 单上下文摘要自述「投递端在 pr-002」）范围 |
| **fail** | **0** | — |
| 合计 | 23 | —— |

---

## 三、偏差记录（规格描述 vs 实现实际行为）

| # | 规格描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | PR 验收 3：「末位 argv = `【项目上下文】…` **+ 换行** + 原文」 | 实现为块 + **空行**（`\n\n`）+ 原文（`agent.js:198`、`context-pool.js:178`），与 architecture §4.2 / §4.3 的逐字契约一致 | 不是实现缺陷；建议把 PR 措辞改为「块 + 空行（`\n\n`）+ 原文」，与 architecture 同源，避免后续断言按"单换行"写错 |
| D-2 | PR 验收 1 括注：「约定文本的真源 = `web.js` 的 `PROJECT_AGREEMENT`，经载荷携带」 | HEAD 上 `PROJECT_AGREEMENT` 在 `oamp/` 全域**零命中**（`web.js` 无项目代码）；agent 侧"只渲染、不复制、不 import"成立 | 属 pr-002 范围。**合并顺序警示**：若 pr-002 未随阶段 5 落地该常量，F07 验收 2/3/6/7 与 F06 验收 1~4 在本迭代内将始终无可核对象 |
| D-3 | architecture §4.1 括注「"未携带即不注入"这条向后兼容性**有测试证据**」；§8.3 提及 `test/project-workspace.test.js ★新` | pr-001 的 diff **零测试改动**（文件范围仅 2 个 src 文件）；携带/未携带两向断言在本 PR 内**无仓库内测试证据**，仅由本报告的临时观测装置覆盖 | 归入下一迭代候选 C-1；若 pr-002/003 的新测试文件覆盖了注入断言，则本条自动闭合 |
| D-4 | PR 验收 5：「`context_busy`（入队即拒）…不消费首轮名额」 | 被拒轮次在 `queue.push` 之前返回（`context-pool.js:148-150`），**根本不进入会话** ⇒ 其"不消费"表现为 `sentTurns` 不受影响（实测 9 轮受理 = 9 轮送达）。"被拒后同一会话的首个送达轮次补上"在语义上不可构造（满队列前提即意味着名额已被在飞/排队轮次消费） | 实现无缺陷；建议表述收敛为「被拒轮次不进入会话、不计入 `sentTurns`」，与代码事实同构 |
| D-5 | PR 验收 6：「协议方法面仍 **7 个**」 | README.md:107-108 的公开方法面逐字 7 个（未改动）；`router.js` 的实现内 `case` 另有 `router.task_get` / `router.task_list` 两个（既有，非本 PR 引入，属 status/CLI 观测面） | 口径澄清即可：「7 = 公开方法面」。本 PR 未触碰 `router.js` 与协议文档 ⇒ 无新增/删除成立 |
| D-6 | F07 架构落地：「`permission_denied` … 会**多注入一次**（无害）」 | 未在本轮构造该场景（属已验证方显式接受的既知行为，且需 deny 档 + fake 工具调用编排） | 不判 fail；如后续要收紧，见下一迭代候选 C-3 |

---

## 四、下一迭代候选

- **C-1**（测试证据缺口，优先级最高）：为项目上下文注入补仓库内回归断言 —— 至少三向：① 一次性路径 `omp -p` 末位 argv = 块 + 空行 + 原文；② 常驻路径首轮一次 / 次轮只发原文 / 会话重建后重新注入；③ 缺失与非对象形态"不注入且不拒收"。落点可复用本报告附录描述的观测装置（fake bin 经 `OAMP_OMP_BIN` 注入）。
- **C-2**（跨 PR 闭合项）：pr-002 落地 `PROJECT_AGREEMENT` 与两分支载荷装配后，补齐 F06 验收 1~4（载荷携带、shell 不携带、`messages.text` 原文比对）与 F07 验收 2/3/7（无绝对路径 / 三件事齐备 / 无超能力承诺）的文本级断言 —— 本轮这 8 条 blocked 将随之可判定。
- **C-3**（低优先，可选）：`permission_denied` 轮的重复注入若需收紧，可在 `ContextSession` 增"已送达"标记；architecture §4.2 已判定当前做法无害（文本幂等）且显式否决为它加状态机，故本迭代不建议动。
- **C-4**（低优先）：PR 单措辞与 architecture 对齐（D-1「换行」→「空行 `\n\n`」），减少后续按 PR 单写断言时的歧义。

---

## 五、结论

**结论: PASS**

- PR 文件 7 条验收标准**逐条 pass**（0 fail）；5 条验证标准全部有判定与可定位证据。
- 文件范围**恰为** PR 声明文件（2 个），既有 22 个测试文件零改写，全量 `npm test` **251/251 绿**，定向 27/27 绿，独立观测 4/4 绿。
- 本 PR 是否需返工：**不需要**。唯一 partial（F07-1）与 8 条 blocked 的根因**均在本 PR 文件范围之外**（派发装配端 `web.js` / 对话存储，PR 单自述属 pr-002），agent 侧交付面（渲染唯一性、两路径注入时机、失败不消费名额、零回归）已全部实测成立。
- **给主 agent 的合并口径提示**：若采用"PR 验收标准逐字全部成立才可视为阶段 5 完成"的更严口径，则需在 pr-002 落地 `PROJECT_AGREEMENT` 后复核 D-2 相关条目（F06-1~4、F07-2/3/6/7）；在只判 pr-001 自身交付面的口径下，本 PR 已可合并。

---

## 附录：独立观测装置（E3 的可复现说明）

装置位于系统临时目录（不进入仓库、不入 worktree），全部为本次验证新建：

- `/tmp/oamp-verify-pr001/fake-omp.cjs` —— fake omp：`-p` 形态写 `FAKE_ACP_ARGS_LOG` 并回显末位 argv；`acp` 形态实现 `initialize` / `session/new` / `session/set_config_option` / `session/prompt`，把每轮 `session/prompt` 文本写 `FAKE_ACP_EVENTS_LOG`，并支持故障注入 `FAKE_ACP_SLEEP_MS` / `FAKE_ACP_CRASH_ON_PROMPT` / `FAKE_ACP_UNKNOWN_MODEL` / `FAKE_ACP_FIXED_MODEL`（模拟"ACP 实报 currentValue ≠ 请求值"以触达 `model_unavailable` 分支）。
- `/tmp/oamp-verify-pr001/observe.mjs` —— 4 个用例，复用 worktree 内 `test/helpers/harness.js`（真实 Router/agent 子进程）与 `test/helpers/fake-node.js`（假 web 节点），以**绝对路径 import** 引用，不复制、不修改被验证代码：
  1. 一次性路径：合法 ⇒ 末位 argv `= 块 + \n\n + 原文`（且块仅现于末位）；缺失 + 8 种非法形态 ⇒ 末位逐字等于原文且任务 completed；同时断言 `started.prompt` / `TASK_STARTED label` 仍为原文。
  2. 常驻路径端到端：首轮带块、次轮只发原文（同 `pid`）、新 chat 带块（异 `pid`）、无 project 的新 chat 不注入。
  3. 进程内直驱 `ContextPool`：崩溃（`sentTurns` 保持 0）/ `release` 后重建 ⇒ 重新注入；`model_unavailable` ⇒ 无 `session/prompt`、`sentTurns` 保持 0、下个送达轮次补上注入且名额随后耗尽。
  4. 进程内直驱 `ContextPool`：10 轮并发第 10 轮 `context_busy`；被拒轮不进 ACP（`prompt_start` = 9）、`sentTurns === 9`、后续轮不再注入。
- 判定尺度取自 architecture §4.2 / §4.3 的逐字契约（块格式、`\n\n`、`sentTurns` 语义），未采用"通用最佳实践"替代。

（报告完）
