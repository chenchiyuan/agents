# 澄清记录 · round-2（迭代 0017-project-workspace，阶段 1 需求收敛 · 第二轮）

**caller**: demand
**时间**: 2026-09-12
**本轮性质**: 收敛轮——把用户对 P-1~P-11 的裁决逐条落进 `demand.md`（v1.0.0 → **v1.1.0**），把受裁决覆盖的 `model_inferred` 全部转 `[user_confirmed]`，再对最终草稿跑一次六维诊断
**会话限制**: 子 agent（无实时对话通道）；本轮**不需要**新通道——用户裁决已由主 agent 用真实阻塞式提问取得（原文照录于下）

---

## 一、输入（本轮起点）

| # | 材料 | 用途 |
|---|---|---|
| 1 | `docs/iterations/0017-project-workspace/demand.md` v1.0.0 | 本轮被升级的对象（两段结构保留） |
| 2 | `docs/iterations/0017-project-workspace/clarifications/round-1.md` | 首轮六维诊断与 P 项的来源 |
| 3 | 用户对 P-1~P-11 的裁决（brief 内联，原文照录） | 本轮的**唯一**新增用户输入（全部 `user_confirmed`） |

**本轮补做的只读侦察（1 处，最小范围）**：`oamp/test/persist.test.js:56-118`——为判定 P-5 裁决对既有测试面的冲击（结果落 F-3b / C-7）。未读其它 `oamp/**` 文件。

---

## 二、用户裁决原文照录（`user_confirmed`）

| # | 议题 | 用户原话 |
|---|---|---|
| **P-4** | 项目上下文的内容与载体 | 「按推荐：payload 结构化字段 + agent 侧注入」 |
| **P-4 备注** | 是否要求 agent 自动在项目根目录工作成为本迭代承诺 | 「按推荐：不要求（留后续迭代）」 |
| **P-5** | 旧库清空的执行方式 | 「**启动时删库重建，project_id NOT NULL（我的倾向）**」 |
| **P-2** | 对话是否强绑项目（含 API 层） | 「按推荐：强绑（API 层强制）」 |
| **P-1/P-3/P-6/P-7/P-8/P-9/P-10/P-11** | 其余 8 条 | 「按推荐：8 条全部采纳」 |

---

## 三、逐条落地结果

| # | 落地位置（v1.1.0） | 结果 |
|---|---|---|
| P-1 | §4.1 M-1、第二段 W1、附清单转换表 | 四字段 + 路径不入库 → `user_confirmed` |
| P-2 | §4.1 M-2、§4.2 缺口 1、第二段 W2、§3 HTTP 层、C-7 ① | 强绑 + `project_id NOT NULL` + API 层强制（缺归属 → `400 INVALID_PARAM`） |
| P-3 | §4.1 M-3、第二段 W1、N6 | 只填地址 / 原样 URL 唯一 / 重复 `409` / 不归一化不校验可达性 |
| P-4 | §4.1 M-4·M-6、§4.6-B1/B3、第二段 W4、§3 派发层、**新增 E9** | 内容 = 项目名 + `repo_url` + 相对工作约定；载体 = payload 结构化 `project` 字段 + agent 侧注入（不进 `messages.text`）；范围 = 常驻 + 一次性，shell 不携带 |
| P-4 备注 | §4.2 缺口 2、§4.4 **C-4**、§4.6-B2、第二段 W4、**N14** | **原样保留**能力边界：本迭代只保证"上下文准确传达"，**不要求**"agent 自动在项目根目录工作"；并升格为 N14（显式否决项，结论段不得出现该承诺） |
| **P-5** | §4.1 **M-8 改写**、§4.4 **C-5 改写**、第二段 **W6 改写**、§3 **数据层改写**、**E6 改写**、**§6.2 留痕**、附清单转换表 | **启动时探测 `chats` 缺 `project_id` 列 → 删库重建；`project_id NOT NULL`**；不再保留幂等加列路径；补记技术依据（SQLite `ADD COLUMN` 无法给非空表加 `NOT NULL` 列，F-22） |
| P-6 | §4.1、§4.6-B4、第二段 W3、§3 UI 层 | 独立首屏项目列表页 → 项目工作台 + 顶栏返回入口 |
| P-7 | 第二段 N5 | 删除 / 改名 / 归档本次都不做 |
| P-8 | §4.1 M-7、第二段 W5·N11 | 不引入"迭代"实体（层级 = 项目 → 对话） |
| P-9 | §4.4 C-1、第二段 **N15** | 本迭代 0017 文档**留在 agents 仓库**，不做任何迁移（补入 N15 使该边界显式化） |
| P-10 | 第二段 N10 | 「犹豫」按「由于」理解，语义已写明 |
| P-11 | 第二段 W3、§3 UI 层 | 项目列表显示项目名 + 地址 + 对话数 + 最近活动时间（零新增存储） |

**P-5 的特别处理（brief 明确要求）**：用户裁决与 demand 原推荐**相反**，因此除改写四张表（M-8 / C-5 / W6 / §3 数据层）与 E6 外，另在 `demand.md` **§6.2** 单列留痕：原推荐内容 + 用户裁决原文 + 裁决的技术依据（F-22）+ 用户优先级读出 + 三项必须让阶段 2 知道的后果（旧库将被自动销毁；F-2 机制不适用于 `project_id`；`persist.test.js` 的旧库补列断言**语义整体反转**）。

---

## 四、来源转换（`model_inferred` → `user_confirmed`）

| 原 `model_inferred` | 覆盖裁决 | 转换后 |
|---|---|---|
| M-1 | P-1 | `user_confirmed` |
| M-2 | P-2 | `user_confirmed` |
| M-3 | P-3 | `user_confirmed` |
| M-4 | P-4 | `user_confirmed` |
| M-6 | P-4 | `user_confirmed` |
| M-7 | P-8 | `user_confirmed` |
| M-8 | **P-5（与原推荐相反）** | `user_confirmed`（按用户口径落地） |
| W1/W2/W3/W5 的形态层 | P-1/P-2/P-3/P-6/P-11/P-8 | `user_confirmed` |
| W4 的形态层 | P-4 | `user_confirmed` |
| W6 的执行方式 | P-5 | `user_confirmed` |
| "是否要求 agent 自动在项目根目录工作" | P-4 备注 | `user_confirmed`（作为显式否决项落入 C-4 / N14） |
| C-5 二选一 | P-5 | `user_confirmed`（删库重建） |

**结果**：`demand.md` 的 `model_inferred` **归零**。新增一类标注 **`派生`**（由已确认裁决直接推出、无独立决策空间、留痕可回退），仅 1 条需求层条目 + 3 条实现层后果。

---

## 五、第二轮六维诊断（对落定裁决后的最终草稿再跑一遍）

| 维度 | 诊断结果 | 是否跑出新缺口 |
|---|---|---|
| 1 **明确性** | M-1~M-8 各有唯一填法且全为 `user_confirmed`；无"同一句话两种读法"残留 | **无** |
| 2 **逻辑闭环** | why 三条 → 五条 what 逐条可推出；缺口 1 由 P-2 采纳（API 层强制）、缺口 2 由 P-4 备注裁决"不要求"（边界保留）、缺口 3 为既有工程约束继承 | **无** |
| 3 **边界完整性** | N1~N15 每条附"为什么这次不做"；用户明说边界 11 条全部落入 N 项；5 条主动收敛判断仍成立（"清空旧库"在 P-5 后更彻底，理由强化） | **无** |
| 4 **内部一致性** | C-1~C-7 全部已决；C-5 改写后与 F-22 自洽；**新增识别**：C-7 的必然变更点由 2 处扩到 4 处（+`chats` 列集断言、**`persist.test.js` 旧库补列断言反转**）——属"既有约束的后果清点"，不是新的需求冲突 | **无**（无新矛盾） |
| 5 **可验证性** | E1~E9 全部裸可判定；E6 按 P-5 改写为"旧 schema 库启动即被重建"；新增 E9（原文历史不被污染）锁住 P-4 的载体选择 | **无** |
| 6 **方案雏形** | W1~W6 的"怎么做"全部取得用户明确回应（P-1/P-2/P-3/P-4/P-6/P-8/P-11 + P-4 备注 + P-5）；W4 与 W6 的形态缺口（首轮唯一未闭合项）**已闭合** | **无** |

**结论**：六维均跑不出新缺口，**且无活跃冲突**（C-1~C-7 无待决项、无待确认 P 项）⇒ **停止条件满足**。

唯一需要在报告里点名的次级事项（**不是新缺口**）：见下节「派生口径」。

---

## 六、派生口径（1 条，可回退）

- **内容**：`GET /api/chats` **缺项目参数** → 返回 `400 INVALID_PARAM`（不提供跨项目全量列表）。
- **来源**：`派生`——用户采纳的 P-2 含「`GET /api/chats` 以项目为范围」+「API 层强制」。若缺参时返回全量，则"以项目为范围""跨项目不混列"在 API 层不成立。
- **为何不算新缺口**：不引入任何新用户意图，只是把已采纳裁决在列表查询这一处的直接后果写清楚，不改变 W1~W6 任何一条。
- **回退口**：主 agent 若认为"无参返回全量（运维/调试）"应保留，可把 W2 的列表口径改为"参数可选，缺省返回全部"，其余结论不受影响。已在 `demand.md` §6.3 留痕。

---

## 七、未越界声明

- 只写了 `docs/iterations/0017-project-workspace/` 下的两个文件（`demand.md`、`clarifications/round-2.md`）。
- **未触碰** `oamp/**`（仅只读 `oamp/test/persist.test.js` 的 56-118 行做事实核对）、`.pb-agents/**`、角色 `data/`/`memory.md`、仓库其它 docs。
- **未执行任何 git 命令**。
- 未做功能规格拆解、未做架构决策：架构层留白集中在 `demand.md` §3 `[架构待填]` 与 §4.6-B。
