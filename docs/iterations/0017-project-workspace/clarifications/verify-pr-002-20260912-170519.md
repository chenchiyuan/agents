# pr-002 独立验收报告（0017-project-workspace 阶段 5）

## 0. 验证者身份（反射结果）

**反射结论：数据层 / HTTP 契约的独立验收者（同类代码审查者 + 接口契约审查者 + 测试工程师三重身份）**

| 产出物类型 | 反射出的验证者身份 | 判定视角 |
|---|---|---|
| `oamp/src/persist.js`（SQLite DDL / 读口写口 / 旧库重建） | 同级代码审查者（数据层） | 结构直读 + 行为实测 |
| `oamp/src/web.js`（路由表 / 参数校验 / 派发载荷） | 接口契约审查者 | 契约逐字比对 + 载荷抓取 |
| `oamp/test/*.test.js` 的 diff（既有变更面） | 测试工程师 | 逐 hunk 归类 + 全量执行 |
| PR 文件「验收标准」与 7 张功能卡产品维度 | 需求-实现追踪审查者 | 逐条判定 |

**独立性声明**：本报告未读取 `history.md`、未读取任何 dev 报告、未读取 `clarifications/` 下既有文件、未读取 `pr-002-*-tasks.md`。全部证据来自：被验证产物本体（源码 / 测试 / 文档）、`git diff`、`npm test` 执行结果、以及本验证者自建的独立探针。**未修改 worktree 内任何文件**（全部临时产物落 `/tmp` 与系统临时目录），未执行任何 git 写命令。

## 1. 产出物

| 项 | 值 |
|---|---|
| 代码 | `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/pr-002` |
| 分支 / HEAD | `feat/pr-002` / `f97cff32cc49c9574bdf70c67589ff011eef368b`（`feat(0017): 项目数据层与 HTTP 面落地（pr-002 / F01 F02 F03 F06 F08 F10）`） |
| 基线 | `iteration/0017-project-workspace...HEAD` |
| diff 规模 | 9 文件 / +711 −374 |
| PR 验收标准 | `docs/iterations/0017-project-workspace/prs/pr-002-project-data-and-http.md` |
| 功能卡（产品维度） | `docs/iterations/0017-project-workspace/prd/F01/F02/F03/F06/F08/F09/F10*.md` |
| 架构契约 | `docs/iterations/0017-project-workspace/architecture.md` §2/§3/§6/§7/§8（含 §8.2 既有变更面、§8.3 四类形态） |
| 验证日期 | 2026-09-12 |

## 2. 验证方法（证据来源清单）

| # | 手段 | 用途 |
|---|---|---|
| E-A | `git diff -U0/-U3 iteration/0017-project-workspace...HEAD -- <路径>` | 文件范围、逐 hunk 归类、文案逐字比对 |
| E-B | `npm test`（`oamp/` 下，全量） | 工程锁 + 回归面实测 |
| E-C | 本验证者自建探针（`/tmp/pr002-probe.mjs`，真实 `oamp web start` 子进程 + 真实 Router + `startFakeNode` + `node:sqlite` 直读）共 **64 项** | F01/F02/F03/F06/F08 的独立行为复现 |
| E-D | 本验证者自建旧库启动探针（`/tmp/pr002-legacy-web.mjs`） | F08-2「旧库启动即重建、服务可用」端到端 |
| E-E | 本验证者自建 DDL/约束直读（`pragma_table_info` / `pragma_index_list` / `sqlite_master` / `pragma_foreign_key_list` + 原始 INSERT） | §2.1 结构契约与 `NOT NULL` / FK 的结构性拦截 |
| E-F | 本验证者自建 413 / 元数据探针（`/tmp/pr002-413.mjs`） | 路由表 `errors` 元数据与实现产生面的比对 |

---

## 3. 逐项判定

### 标准 1：PR 文件「验收标准」逐条判定（9 条）

| # | 验收标准（摘要） | 判定 | 可定位证据 |
|---|---|---|---|
| 1 | `persist.js` schema 落地 §2.1 | **pass** | E-E：`projects` 列集 `project_id(TEXT,PK)/name(TEXT,NOT NULL)/repo_url(TEXT,NOT NULL,UNIQUE)/created_at(INTEGER,NOT NULL)`；`chats` 10 列且 `project_id` 位序 = 2、`notnull=1`、`pragma_foreign_key_list('chats') → projects.project_id`；`sqlite_master` 中 `CREATE INDEX idx_chats_project_updated ON chats(project_id, updated_at DESC)`；`src/persist.js:66-67` 的 `CHAT_COLUMNS` / `MESSAGE_COLUMNS` 与 diff 前逐字相同 |
| 2 | `GET /api/projects` 形状 / 排序 / 无分页 / 派生列 | **pass** | E-C：`200 {projects:[{project_id,name,repo_url,created_at,chat_count,last_activity_at}]}`；6 项目排序满足 `created_at DESC, project_id DESC`；响应键仅 `projects`（无 `total/limit/offset`）；无对话 → `chat_count=0 && last_activity_at===null`；归档后 `chat_count` 仍计入（`chat_count` 与 `sqlite COUNT(*)` 相等）；`last_activity_at === MAX(chats.updated_at)`；`src/persist.js:377-392` 单条聚合 SQL |
| 3 | `POST /api/projects` 400/409/派生/不归一化 | **pass** | E-C：缺 / 空白 / 非字符串 `repo_url` → `400 {"error":"需要 repo_url（非空字符串）","code":"INVALID_PARAM"}` 且项目数 0→0；重复 → `409 {"error":"项目已存在: https://github.com/acme/demo.git","code":"CONFLICT"}` 且项目数不变；`demo.git`→`demo`、空 name→派生、非字符串 name→派生、`///`→派生为空兜底原文；`…/demo` 与 `…/demo.git` 各自创建成功；`完全不存在的地址/不是URL` 创建成功；实现见 `src/web.js:751-791`、`src/persist.js:318-331` |
| 4 | `GET /api/chats` 项目范围与缺参 400 | **pass** | E-C：缺参 → `400 {"error":"查询参数非法: project_id 不能为空（对话列表以项目为范围）"}`；`?project_id=`（空值）→ **同文案同码**；未知 `project_id` → `200 {chats:[],total:0,limit:50,offset:0}`；A/B 两项目列表互斥；行内字段 = `chat_id,title,agent_id,state,created_at,updated_at,archived_at,message_count`（**无 `project_id`**）；E-B：`web.test.js` 过滤/时间/分页/非法参用例（均带 `project_id`）全绿；`src/persist.js:283-287`（`readRequiredString`）、`:375-380`（`LIST_FROM` 增 `AND c.project_id = p.project`）、`:281`（`countChats` 与列表共用 `LIST_FROM`） |
| 5 | `POST /api/messages` 项目归属与顺序契约 | **pass** | E-C：新建缺 / 空 → `400 {"error":"新对话需要 project_id（对话必须归属一个项目）"}` 且 `chats` 行数 0→0；未知项目 → `400 {"error":"项目不存在: prj-nope"}` 且不产生对话；既有对话不带 `project_id` → `200`；顺序证据：空正文仍报「消息不能为空」、非法 `model` 仍先于项目校验报错、畸形 JSON 仍 400（新 400 只在既有路径不可能报错的场景出现）；`src/web.js:662-678`（校验插在 `existing`/只读 409 之后、`insertInput` 之前） |
| 6 | 派发载荷 `project` 三要素 / shell 不变 / 原文不被污染 | **pass** | E-C 抓取真实 `task.request`：常驻 = `{executor:'omp-daemon', chat_id, prompt:'项目上下文探针', label:'项目上下文探针', project:{name:'demo', repo_url:'https://github.com/acme/demo.git', agreement:<§4.3 原文逐字>}}`；一次性 = `{executor:'omp', prompt, label, project:{…}}`（无 `chat_id`）；shell = `{"command":"/bin/sh","args":["-c","echo probe-ok"],"label":"!echo probe-ok"}` **逐字相等、无 `project`**；`project` 三键无 `project_id`、无本机路径；`payload.prompt` = 用户原文（web 不注入文本）；`label` 不带项目块；`GET /api/chats/<id>.messages[0].text` = 输入原文；`agreement` = `architecture.md §4.3` 文本逐字、零绝对路径、零超能力表述（`src/web.js:40-44` 常量唯一真源） |
| 7 | `openDb()` 旧库重建 / 不重建 / 删库重启 / 删 `migrate()` | **pass** | E-D：以 7 列旧库启动 `oamp web start` → `WEB_READY` 正常、stdout 含 `DB_REBUILT path=…`；列集 = 新 10 列、`projects` 表存在、`chats`/`messages` 行数 0；E-C/E-E：二次 `openDb` 不重建（项目与对话数据保留）、删库文件后重启 = 新建空库且可建项目/对话；`src/persist.js` 中 `MIGRATIONS` 与 `migrate()` 已不存在（grep 无命中），`isLegacyChats()` 为唯一判据（`:47-52`），`openDb` 内 `db.close() → rmSync(dbPath,{force:true}) → new DatabaseSync`（`:184-194`） |
| 8 | F10 三面与三锁 | **pass** | E-B（`api-routes.test.js` 三条锁用例全绿）+ E-C 独立复核：`EXPECTED_SIGNATURES` 13 条且末位为两条 `/api/projects`（`test/api-routes.test.js:45-50`）；`GET /api/docs` 投影 13 条、`danger` 6 条；`llms.txt` 与 `renderLlmsTxt(projectRoutes(createApiRoutes({})))` **由本验证者独立重算，字节相等**（1995 B / 1995 B）；`API.md` 含 `## 3. 接口清单（13 条）`、`| 12 |`/`| 13 |` 行、`### 3.12 \`GET /api/projects\``/`### 3.13 \`POST /api/projects\``；`package.json` 的 `dependencies === {}`；本 PR 未触碰 `oamp/web/**`（`/docs`、`/debug` 零改动）。**注：`POST /api/projects` 的 `errors` 元数据与实现产生面不一致 —— 见偏差 A-1** |
| 9 | 既有 4 个测试文件 diff 只含四类形态 | **pass** | 逐 hunk 归类表见「标准 4」。无断言语义放宽、无既有断言删除（唯一删除项 = C-7④ 语义反转对象）、无超时/等待策略调整、无整文件格式化、无顺手文案改动。**3 处非四类字面形态（fixture 建项目）见偏差 A-2** |
| 10 | 其余 18 个测试文件零改写且全绿 | **pass** | E-A：`oamp/test/` 共 22 个 `*.test.js`，diff 命中 4 个（`persist`/`web`/`api-routes`/`acp-daemon`），**18 个零改动**；其中 `oamp/test/api-pages.test.js` 与 `oamp/test/hygiene.test.js` 的 `git diff --name-only` 输出为 **0 行**（逐一核实）；E-B：全量 251 用例全绿 |

### 标准 2：「文件范围」合规

**判定：pass**

命令（在验证者侧执行，只读）：
```
git -C .pb-agents/worktrees/pr-002 diff --name-only iteration/0017-project-workspace...HEAD
```
实际输出恰 9 项，与 PR 文件「文件范围」逐条一致（不多不少）：

| # | PR 文件范围 | diff 实际 |
|---|---|---|
| 1 | `oamp/src/persist.js`（修改） | ✓ |
| 2 | `oamp/src/web.js`（修改） | ✓ |
| 3 | `oamp/API.md`（修改） | ✓ |
| 4 | `oamp/llms.txt`（重新生成） | ✓（diff 仅标题计数 11→13 与清单 +2 行，与脚本产物一致，非手改） |
| 5 | `oamp/README.md`（修改） | ✓ |
| 6 | `oamp/test/persist.test.js`（§8.2 ①） | ✓ |
| 7 | `oamp/test/web.test.js`（§8.2 ②） | ✓ |
| 8 | `oamp/test/api-routes.test.js`（§8.2 ③） | ✓ |
| 9 | `oamp/test/acp-daemon.test.js`（§8.2 ④） | ✓ |

未出现 `oamp/web/**`（`/docs`、`/debug` 零改动）、未出现新增文件、未出现 `context-pool.js` / `agent.js` / `router.js` / `rpc.js` / `node-client.js`。

### 标准 3：7 张功能卡产品维度验收逐条判定

#### F01 项目实体（7 条，全部 pass）

| # | 判定 | 证据 |
|---|---|---|
| 1 | pass | `POST /api/projects {repo_url:'https://github.com/acme/demo.git'}`（name 留空）→ `200`，`name === 'demo'`；空 / 非字符串 name 同样派生（E-C） |
| 2 | pass | `GET /api/projects` 返回全部已创建项目（6/6），未创建的不出现；`GET /api/chats?project_id=<未知>` 返回空列表而非跨项目全量 |
| 3 | pass | sqlite 直读项目行键恰 `project_id,name,repo_url,created_at`（无路径类字段）；`architecture.md §2.3` 未引入路径存储 |
| 4 | pass | 缺 / 空 / 非字符串 → `400 INVALID_PARAM`「需要 repo_url（非空字符串）」且项目数 0→0；`完全不存在的地址/不是URL`、`x`、`///` 均创建成功（只校验非空、不校验形态/域名） |
| 5 | pass | 连续两次同地址 → 第二次 `409 CONFLICT`「项目已存在: …」，列表仍 1 条；`persist.js:321-327` 用 `ON CONFLICT DO NOTHING + changes===0` 判定，不解析 SQLite 文案 |
| 6 | pass | `…/demo` 与 `…/demo.git` 各自创建成功（列表 2 条） |
| 7 | pass | 不存在 / 不可访问地址创建成功；E-A：`src/persist.js` 仅 import `node:sqlite` / `node:fs` / `node:crypto` / `node:path`（`:8-11`），`createProject` 方法体内零网络调用、零凭据入参、零仓库元数据拉取 |

#### F02 新建对话必须归属项目（5 条，全部 pass）

| # | 判定 | 证据 |
|---|---|---|
| 1 | pass | 带归属新建 → `200`；该对话出现在**该项目**列表（`total=1`）、不出现在其他项目列表（`total=0`） |
| 2 | pass | 缺 / 空 `project_id` → `400 INVALID_PARAM`「新对话需要 project_id（对话必须归属一个项目）」，`chats` 行数 0→0 |
| 3 | pass | `pragma_table_info('chats')` 中 `project_id` 的 `notnull=1`；原始 INSERT 省略该列 → `NOT NULL constraint failed: chats.project_id`；`pragma_foreign_key_list('chats')` = `projects(project_id)`，原始 INSERT 指向 `prj-nope` → `FOREIGN KEY constraint failed`（`PRAGMA foreign_keys=ON` 在 `openDb` 内既有） |
| 4 | pass | 未知项目 → `400 INVALID_PARAM`「项目不存在: prj-nope」，**非 404**，不产生对话 |
| 5 | pass（归属说明见下） | 判定面在前端。分支已含 pr-003 的前端产物：`oamp/web/app.js:751` 的 `payload = { chat_id, agent_id, text, project_id: state.projectId }`（工作台内新建自动归属、无项目选择步骤）。**该行为不在 pr-002 的 diff 内**（pr-003 交付），本报告不对其作 pr-002 归属判定 |

#### F03 对话列表以项目为范围（5 条，全部 pass）

| # | 判定 | 证据 |
|---|---|---|
| 1 | pass | A/B 两项目各建对话，逐项目查询互不包含（E-C）；`LIST_FROM` 增 `AND c.project_id = p.project`（`persist.js:379-380`） |
| 2 | pass | 不带参数 → `400`；带空值（`?project_id=`）→ **同样 400**；不返回跨项目全量列表 |
| 3 | pass（归属说明见下） | 前端产物 `app.js:668`（`/api/chats?project_id=…`）、`:684`（归档视图同带项目参数）、整页导航切换（`app.js:604` 注释所述机制）；**不在 pr-002 diff 内**（pr-003 交付） |
| 4 | pass | E-B：`web.test.js` 的 `q`/`agent`/`state`/`from`/`to`/`archived`/`limit`/`offset` 全部用例在带 `project_id` 后全绿；`api-routes.test.js` 的 Q-6（空值 = 无参 → 200）与 Q-7（真正 400 的三类）在补参后仍指向原参数语义；E-C：`total` 与集合同源（`countChats` 复用 `LIST_FROM`，`persist.js:281`） |
| 5 | pass | 库中不同项目对话行的 `project_id` 分别归属（E-E 直读）；外键 + NOT NULL 结构性保证不混列 |

#### F06 派发载荷携带项目上下文（6 条，全部 pass）

| # | 判定 | 证据 |
|---|---|---|
| 1 | pass | 载荷 `project` = `{name, repo_url, agreement}` 结构化字段（E-C 实抓）；非把文字拼进用户消息正文 |
| 2 | pass | `omp-daemon` 与 `omp` 两条路径的 `task.request` payload 均携带 `project`（E-C 实抓两次派发） |
| 3 | pass | `!echo probe-ok` 的 payload **逐字 JSON 相等** 于 `{"command":"/bin/sh","args":["-c","echo probe-ok"],"label":"!echo probe-ok"}`，无 `project` 键 |
| 4 | pass | `GET /api/chats/<id>` 的 `messages[0].text` = 输入原文；`payload.prompt` = 原文（web 侧零注入文本） |
| 5 | pass | E-A：本 PR 未触碰 `src/rpc.js` / `src/router.js` / `src/node-client.js`（diff 命中 0 文件）；`README.md:107-108` 的七方法（`agent.register` / `agent.heartbeat` / `agent.deregister` / `message.send` / `message.deliver` / `message.ack` / `router.status`）在 README diff 中零改动 |
| 6 | pass | E-A：本 PR 未触碰 `src/context-pool.js`；`oamp/test/context-pool.test.js` 零改动（`git diff --name-only` 0 行）且全绿 |

#### F08 旧结构库启动即重建（6 条，全部 pass）

| # | 判定 | 证据 |
|---|---|---|
| 1 | pass | `isLegacyChats()`（`persist.js:47-52`）：`pragma_table_info('chats')` 有行且无 `project_id` ⇒ 旧结构 |
| 2 | pass | E-D 端到端：以 7 列旧库 + 历史行启动 `oamp web start` → `WEB_READY`、无报错、stdout `DB_REBUILT path=…`；启动后 `GET /api/projects` = `200 {"projects":[]}`、`GET /api/chats`（缺参）= `400`、`POST /api/projects` 与新对话均可用 |
| 3 | pass | 重建后 `chats` 行数 0、`messages` 行数 0；不为旧对话造兜底归属 |
| 4 | pass | `projects` 表存在；`chats.project_id` 的 `notnull=1` 且带外键 |
| 5 | pass | 新结构库二次 `openDb` / 二次启动不重建（项目与对话数据保留）；删库文件后重启 = 新建空库且可建项目 + 对话（E-C / E-E） |
| 6 | pass | `MIGRATIONS` 与 `migrate()` 在 `src/persist.js` 中已删除（无残留补列路径）；`rmSync` 只删主库文件本体 |

#### F09 既有行为零变化（回归锁）（5 条，全部 pass）

| # | 判定 | 证据 |
|---|---|---|
| 1 | pass | E-B：全量 251 用例在同一批既有断言下全绿 —— 含统一错误契约（5 码一一映射 / 键集合恰 `{error,code}`）用例；E-C：`DELETE /api/projects` → `404 {"error":"not found: DELETE /api/projects","code":"NOT_FOUND"}`（**不引入 405**）；SSE / 对账 / 归档 / 激活 / 改名 / 只读 409 用例全部通过 |
| 2 | pass | 4 个既有测试文件的改动全部可归入 §8.2 ①~④（见标准 4 归类表）；其余 18 个测试文件零改写且全绿 |
| 3 | pass | 变更面闭合：全部 9 文件的每一处 diff 均落在 §8.2 登记的四类 + 机械前置（fixture）内，未发现越界改动；A-1 为元数据一致性问题、A-2/A-3 为形态归类问题，均不构成"既有行为变更" |
| 4 | pass | 协议方法面 7 个未变（同 F06-5 证据） |
| 5 | pass | E-A：本 PR 未触碰监听地址 / 端口 / 鉴权相关代码，未新增对外监听或 agent 启停入口；`src/web.js` 的 9 条既有表项内容逐字未动 |

#### F10 新增项目类接口的登记义务与工程锁（5 条，全部 pass）

| # | 判定 | 证据 |
|---|---|---|
| 1 | pass | E-C：`GET /api/docs` 投影 13 条（含两条 `/api/projects`）⇒ 文档页 / 调试台自动出现；`llms.txt`（HTTP 面与仓库快照同字节）含两行；本 PR 未改 `oamp/web/**`（`/docs`、`/debug` 零改动） |
| 2 | pass | `API.md` 含 `### 3.12 \`GET /api/projects\``、`### 3.13 \`POST /api/projects\``、表行 `| 12 |`/`| 13 |`（E-A）；锁③（路径级双向覆盖）全绿 |
| 3 | pass | 锁①（元数据必填 + `errors ⊂ ERR_CODE`）绿；锁②（llms.txt 逐字节）**由验证者独立重算字节相等**；锁③绿（E-B） |
| 4 | pass | `package.json` `dependencies === {}`（Node 直读）；`hygiene.test.js` 零改动且全绿 |
| 5 | pass | 既有 11 条表项的表序与内文逐字未动；`/api/chats`、`/api/messages` 仅增参数与校验，既有错误的码/文案/顺序不变（E-C 顺序证据） |

### 标准 4：既有变更面合规（4 个测试文件逐处归类）

归类口径 = `architecture.md` §8.3 四类形态：① 对话列表调用点新增 `project_id`；② 新建路径补 `project_id`；③ 同类机械补参（persist schema / 旧库断言 / 建行调用点参数 / `listChats` 调用点）；④ 派发载荷观测断言的「前缀 + 原文」改写 + 登记面计数 11→13。

**判定：pass**（逐 hunk 全部归类完成；无一处无法归类）

| 文件 | diff 规模 | 归类明细 | 判定 |
|---|---|---|---|
| `test/persist.test.js` | +201/−174 | (a) import `rmSync`（③ 旧库断言的"删库文件后重启"分支）；(b) `openTempDb()` 建 1 个项目 fixture 并返回 `projectId`（③，§8.2①(c) 明列）；(c) `insertInput`/`upsertChat` 全部建行调用点补 `projectId`（③）；(d) 全部 `listChats()` / `listChats({…})` 调用点补 `project`（③）；(e) `schema` 用例列集断言 → 新 10 列 + `projects` 表（③）；(f) 旧库用例整条语义反转（③ = C-7④）；(g) `writers` 名单断言扩列 4 个新 API（③ 同类机械补参，见 A-3）；(h) 用例名随语义同步 | pass |
| `test/web.test.js` | +108/−103 | (a) `setup()` 建项目 fixture（见 A-2）；(b) `/api/chats` 列表调用点补 `project_id`（①）；(c) `POST /api/messages` 新建路径补 `project_id`（②）；(d) 4 处"桩回显"字符串断言改写为 `收到：【项目上下文】[\s\S]*<原文>`（④，`:443`/`:546`/`:549`/`:943`）；(e) argv 末位断言 `=== '一次性问题'` → `startsWith('【项目上下文】') && endsWith('\n\n一次性问题')`（④，`:557`）；(f) 自建 web 的用例（`:1006`）就地建 fixture | pass |
| `test/api-routes.test.js` | +18/−9 | (a) `EXPECTED_SIGNATURES` +2（④）；(b) `setupWeb()` 建项目 fixture（A-2）；(c) Q-6 空值语义 4 条 URL 补 `&project_id=`（① 附带：§8.2②(c) 同义断言）；(d) Q-7 三类真 400 的 URL 补 `&project_id=`（① 附带，防"缺 project_id 掩盖原参数校验"）；(e) SSE 探针的 `/api/messages` 调用带全新 `chat_id` ⇒ 补 `project_id`（②）；(f) `/api/docs` 投影用例名与 `danger` 计数 5→6（④）；(g) `llms.txt` 计数 11→13（④）；(h) `API.md` 同步用例：计数 11→13 + `§3.12`/`§3.13` + `| 12 |`/`| 13 |`（④） | pass |
| `test/acp-daemon.test.js` | +28/−20 | (a) `setup()` 与两处自建 web（`:514`/`:686`）建项目 fixture（A-2）；(b) 新建对话调用点补 `project_id`（②）；(c) `/api/chats` 列表调用点补 `project_id`（①）；(d) 一次性回显断言 `one-shot answer: 请记住数字 7` → `one-shot answer: 【项目上下文】[\s\S]*请记住数字 7`（④，`:411`） | pass |

**禁止形态核查（逐条未发现）**：断言语义放宽 ✗ 未发现；既有断言删除 ✗（唯一删除项 = 旧库用例的旧行断言，属 C-7④ 语义反转对象）；超时 / 等待策略调整 ✗（`waitFor` 的 `timeoutMs` 全部未变）；整文件格式化 ✗（diff 无空白批量变动）；"顺手改进"文案 ✗。

**零改动核实**：
```
git diff --name-only iteration/0017-project-workspace...HEAD -- oamp/test/api-pages.test.js oamp/test/hygiene.test.js
→ （0 行输出）
```

### 标准 5：工程锁真实通过

**判定：pass**

| 锁 | 判定 | 证据 |
|---|---|---|
| ① 路由表元数据必填 | pass | E-B 锁①用例绿；E-C：13 条表项 8 字段齐备、`errors` 取值均在 `ERR_CODE` 值集合内（`GET /api/projects` 为 `[]`，`POST /api/projects` 为 `['INVALID_PARAM','CONFLICT']`）；表序末位 = 两条项目面，既有 11 条未动 |
| ② `llms.txt` 逐字节 | pass | **独立复核**（不依赖 PR 的测试）：`Buffer.compare(renderLlmsTxt(projectRoutes(createApiRoutes({}))), readFileSync('oamp/llms.txt')) === 0`，两侧均 1995 字节；文件 diff 仅"（11 条）→（13 条）"+ 2 行清单，形态与生成器一致（非手改） |
| ③ `API.md` 路径级双向覆盖 | pass | E-B 锁③用例绿；E-C：登记集合（13）与文档反引号签名集合双向无缺无多；`API.md` 含 `## 3. 接口清单（13 条）`、`| 12 |`/`| 13 |`、`§3.12`/`§3.13` 反引号签名 |
| 零依赖锁 `dependencies === {}` | pass | E-C：`require('./package.json').dependencies` → `{}`；`hygiene.test.js` 零改动且全绿 |

### 标准 6：实际执行证据（全量 `npm test`）

**判定：pass**

命令（在 worktree 内、以系统临时目录为 `TMPDIR`，未污染仓库）：
```
cd /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/pr-002/oamp
TMPDIR=$(mktemp -d /tmp/pr002-test-XXXXXX) npm test
```
脚本 = `node --test test/*.test.js`（`oamp/package.json` 的 `scripts.test`）。结果（原样记录）：

```
1..251
# tests 251
# suites 0
# pass 251
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 55939.158833
```

补充：本验证者自建独立探针（E-C/E-D/E-E/E-F）共 **64 项全 pass**，覆盖 F01/F02/F03/F06/F08/F09/F10 的行为面（含 413、404 兜底、FK/NOT NULL 结构性拦截、旧库启动端到端）。

---

## 4. 汇总

| 验证标准 | 判定 | fail 条目数 |
|---|---|---|
| 1. PR 文件验收标准 9 条 | pass | 0 |
| 2. 文件范围（恰 9 文件） | pass | 0 |
| 3. 7 张卡产品维度逐条 | pass | 0（F02-5 / F03-3 两条的判定面属 pr-003，已标注归属） |
| 4. 既有变更面合规（4 文件逐处归类 + 2 文件零改动） | pass | 0 |
| 5. 工程锁（3 漂移锁 + 零依赖锁） | pass | 0 |
| 6. 实际执行证据（全量 `npm test`） | pass | 0 |

- **fail 条目数：0**
- **partial 条目数：0**
- **blocked 条目数：0**
- **偏差记录条数：7**（A-1 中；A-2/A-3 低；A-4/A-5/A-6 信息；A-7 待确认）
- 未出现"基本通过"类模糊判定。

---

## 5. 偏差记录

| # | 偏差 | 严重度 | 行级证据 | 影响 | 判定归属 |
|---|---|---|---|---|---|
| A-1 | **`POST /api/projects` 的登记 `errors` 元数据与其实现产生面不一致**：登记 `['INVALID_PARAM','CONFLICT']`，但 handler 读体失败时会真的返回 `413 PAYLOAD_TOO_LARGE`（实测 `POST /api/projects` 超 64 KiB 体 → `413 {"error":"请求体过大（上限 65536 字节）","code":"PAYLOAD_TOO_LARGE"}`）；`API.md §3.13` 的错误表**已列** 413，而同形态的 `POST /api/messages` 登记的 `errors` **含** `PAYLOAD_TOO_LARGE` | 中 | `oamp/src/web.js:757`（`errors: ['INVALID_PARAM','CONFLICT']`）；`oamp/src/web.js:764-771`（catch 内 `sendError(res, status, status === 413 ? ERR_CODE.PAYLOAD_TOO_LARGE : …)`）；`oamp/src/web.js:611`（`POST /api/messages` 对照：`['INVALID_PARAM','PAYLOAD_TOO_LARGE','CONFLICT']`）；`oamp/API.md` §3.13 错误表含 `PAYLOAD_TOO_LARGE`/413 行 | `/api/docs` 投影与 `/docs` / `/debug` 的错误面少一码（下游按元数据生成客户端错误处理时会漏 413）。锁①只校验"`errors` 取值 ∈ `ERR_CODE`"，故三锁仍全绿 —— 属元数据完备性缺口，非锁失效 | 不落入验收标准任一条（标准 1-8 的枚举项全过）⇒ 标准 1-8 判 pass，本条记偏差 |
| A-2 | **3 个测试文件的 `setup` helper 新增"经 HTTP 建项目 fixture"**（`web.test.js:265`、`api-routes.test.js:103`、`acp-daemon.test.js:253/514/686`），不在 §8.3 四类的**字面**枚举内 | 低 | 上述行号；对照 `architecture.md` §8.2①(c) 已明列 `persist.test.js` 的"1 个项目 fixture" | 无功能影响；归类上属标准①②的**必要前置**（无项目则无法取得合法 `project_id`）。§8.3 的枚举以 "persist 的 schema / 旧库断言 / 建行调用点参数" 描述第③类，未预列 HTTP 侧 fixture | 标准 4 判 pass，记偏差（口径建议见下一迭代候选 3） |
| A-3 | `persist.test.js` 的 `writers` 名单断言扩列 4 个新 API（`createProject`/`getProject`/`listProjects`/`projectByChat`） | 低 | `oamp/test/persist.test.js:183` | 断言意图未变（仍验"不存在接受 `direction` 的通用写口"）；属 persist 接口面的机械同步（§8.3 第③类"同类机械补参"） | 标准 4 判 pass，记偏差 |
| A-4 | 旧库用例删除了旧行的 `archived_at`/`context_released`/`state`/`title`/`updated_at` 断言 | 信息 | diff `oamp/test/persist.test.js`（原 `assert.equal(old.archived_at, null, '既有行视为未归档（M-3 / N-9）')` 等被替换为「列集 + 旧行消失 + 行数 0 + 二次不重建 + 删库重启」） | 属 §8.2①(b) / C-7④ 明示的"整条断言语义反转"对象，非无关断言删除（新用例的断言密度高于原用例） | 标准 4 判 pass，记偏差 |
| A-5 | 本 PR **不含新测试文件**（`oamp/test/project-workspace.test.js` 不存在），F01/F03 的新能力在本 PR 内无专项断言 | 信息 | `oamp/test/` 22 个文件的 diff 命中 4 个；`architecture.md` §8.1 指定的新断言落点由 `prs/pr-004-project-workspace-acceptance.md`「文件范围：oamp/test/project-workspace.test.js（新建）」承载 | 本 PR 的"全绿"不能单独证明 F01/F03；本报告以自建探针（E-C，64 项）补齐该部分证据。此为迭代内的 PR 切分，不是本 PR 的缺陷 | 标准 1-10 判 pass，记偏差 |
| A-6 | PR 提交信息的能力点标注为 `(F01 F02 F03 F06 F08 F10)`，未含 F09 | 信息 | `git log -1` 提交标题；`architecture.md` §8.1「F09 的判定载体是 pr-002 的四个既有测试文件的 diff 形态」 | F09 的判定载体确实落在本 PR（见标准 3 / F09 与标准 4），标注遗漏不影响判定 | 记偏差 |
| A-7 | `/api/chats`（`web.js:352-361`）与 `/api/messages`（`web.js:604-613`）的路由表 `params` **未登记**新增的 `project_id` | 中（待确认口径） | 上述 params 数组（`project_id` 不在其中）；`API.md` §3.2 参数表与 §3.8 参数表**均已登记** `project_id`；`GET /api/docs` 投影亦不含该参数 | `/docs` / `/debug` 的字段级视图在这两条既有接口上看不到新参数；调试台按表单调用 `/api/chats` 会直接 `400`（无输入 `project_id` 的位置）。合规依据：`architecture.md` §6.1 明写"既有 11 条表项的**表序与内文逐字不动**"，故实现选择不改属**遵从契约**；缺口在"三面 vs `API.md`"之间 | 不落入验收标准任一条 ⇒ 判 pass，记偏差（口径建议见下一迭代候选 1） |

---

## 6. 下一迭代候选

| # | 候选 | 触发偏差 | 建议口径 |
|---|---|---|---|
| 1 | `/api/chats` 与 `/api/messages` 的 `params` 元数据补登 `project_id`（连带解锁"既有 11 条内文逐字不动"与"三面需完整反映面"的冲突口径） | A-7 | 需先定级：若认为"新增参数属项目维度的必然变更面"，则应允许修改既有表项的 `params` 数组（表序与其余字段仍不动），并在 §8.2/§8.3 显式登记为第五类形态；否则应把"调试台无法填写必填参数"记为已知限制 |
| 2 | `POST /api/projects` 的 `errors` 补 `PAYLOAD_TOO_LARGE`（与 `POST /api/messages` 对齐），并把"登记的 `errors` 必须覆盖 handler 实际可达的错误码"升级为锁①的可机检口径（现状仅校验取值合法性） | A-1 | 低风险；`API.md §3.13` 已先落地该码，改元数据即闭环 |
| 3 | 把"测试 helper 建 fixture（HTTP / persist 两侧）"补进 `architecture.md` §8.3 的形态枚举，消除"合规但不可归类"的口径摩擦 | A-2 / A-3 | 低风险；纯文档口径 |
| 4 | 建立"新能力必须有对应新断言"的落点核对项（本迭代的 F01/F03 断言在 pr-004 才落地，PR 级验收时无法在 pr-002 内自证） | A-5 | 与 pr-004 一并验收时回看即可；如后续迭代沿用多 PR 切分，建议在 PR 文件里显式标注"新能力断言落在同迭代的哪个 PR" |

---

## 7. 结论

# **PASS**

**依据**：六项验证标准全部 pass；fail 条目 0；partial 条目 0；blocked 条目 0。全量 `npm test` = 251 tests / 251 pass / 0 fail；独立探针 64/64 pass；文件范围恰为 PR 文件列出的 9 个；四条工程锁（三条漂移锁 + 零依赖锁）真实通过（`llms.txt` 逐字节由验证者独立重算确认）；既有 4 个测试文件的每一处 diff 均可归入 §8.3 的四类形态（+ 已记录的机械前置），`api-pages.test.js` 与 `hygiene.test.js` 零改动。

**主 agent 可直接据此判定"可以继续"**（进入阶段 5 的后续 PR 或下一阶段），并注意偏差 **A-1（`POST /api/projects` 的 `errors` 元数据缺 413）** 与 **A-7（两条既有接口的 `params` 未登记 `project_id`，调试台无法填写该必填参数）** —— 二者均不违反本 PR 的验收标准与工程锁，属"登记面完备性"的下一迭代参数。

**验证者签名**：独立技术鉴定人（数据层 / HTTP 契约 / 测试面三重身份；未接触执行过程上下文）
**报告路径**：`docs/iterations/0017-project-workspace/clarifications/verify-pr-002-20260912-170519.md`
