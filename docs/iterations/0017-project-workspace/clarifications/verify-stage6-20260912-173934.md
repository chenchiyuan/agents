# 阶段 6 独立验证报告（0017-project-workspace · 合并态全量复核）

- **迭代**：0017-project-workspace
- **被验证对象**：迭代分支 `iteration/0017-project-workspace` **合并态**（HEAD `428e672`，工作树 `/Users/chenchiyuan/projects/agents`）+ `docs/iterations/0017-project-workspace/**` 全部阶段产物
- **验证日期**：2026-09-12 17:39:34 CST
- **报告路径**：`docs/iterations/0017-project-workspace/clarifications/verify-stage6-20260912-173934.md`

---

## 0. 验证者身份（反射结果）

**产出物类型** = 一个完整迭代的合并态产物（4 个 PR 的代码 + 10 张功能卡 + 阶段 1~4 文档 + 阶段 5 调度台账）⇒ 反射身份 = **发布前评审者（需求 / 架构 / 测试 / 交付四重视角）**。

| 判定视角 | 覆盖对象 | 手段 |
|---|---|---|
| 需求-实现追踪审查者 | `prd/F01~F10*.md` 的产品维度验收 + 4 份 PR 文件的验收标准 | 逐条对照 + 真实进程实测 |
| 数据层 / HTTP 契约审查者 | `oamp/src/persist.js`、`oamp/src/web.js`、`oamp/API.md` | 结构直读 + sqlite 直读 + 真实 HTTP |
| 前端契约审查者 | `oamp/web/{index.html,app.js,style.css}` | 源码直读 + 真实浏览器运行观测 |
| 测试工程师 | `oamp/test/**`（含新增 `project-workspace.test.js`） | 亲自执行全量套件 + 断言 diff 归类 |
| 交付 / 调度台账审查者 | `status.md` / `progress.md` / 4 份 PR 级验收报告 / git 拓扑 | git 只读命令 + 文档与事实逐项对拍 |

**独立性声明（红线遵守）**：
- **未读取** `history.md`（主 agent 执行过程流水）；未与任何执行角色交流。
- **未采信**任何角色报告的结论作为判定依据：本报告每一条判定均建立在本验证者亲自执行的命令输出、亲自读取的合并态代码/产物、亲自驱动的真实进程与浏览器观测之上。PR 级验收报告仅被用作"偏差清单是否存在"的核对对象与 worktree 现场的时间证人（见标准 2-①）。
- **只读**：未修改任何被验证产物，未执行任何 git 写命令；探针脚本与临时产物全部落系统临时目录（`/tmp/vstage6/`），真实库 `oamp/data/sql.db` 未被触碰（mtime 仍为 `14:54:04`，早于本次验证）。
- 验证结束后仓库状态与开始时一致（`git status --porcelain` 仍为 ` M history.md` / ` M status.md` / `?? docs/iterations/0018-…/`，均非本次产生）。

---

## 1. 产出物清单

| 类别 | 路径 | 事实 |
|---|---|---|
| 迭代分支 | `iteration/0017-project-workspace` @ `428e672` | 4 个合并提交：`9e1f4d4`(pr-001)、`a9fef01`(pr-003)、`0d726f2`(pr-002)、`428e672`(pr-004) |
| PR 文件 | `prs/pr-001…pr-004` | 4 份，均存在（含 `-tasks.md` 共 8 个文件） |
| 功能卡 | `prd/F01…F10` | 10 张，均存在 |
| 阶段产物 | `demand.md` / `prd.md` / `architecture.md` | 均存在 |
| 台账 | `status.md`（工作区版本）/ `progress.md` | 均存在；`status.md` 相对 HEAD 有未提交改动 |
| 代码 | `oamp/src/*`、`oamp/web/*`、`oamp/API.md`、`oamp/llms.txt`、`oamp/README.md` | 合并态；全量 49 文件 / +7417 −384 相对 main |
| 测试 | `oamp/test/**` | 23 个 `*.test.js`（22 既有 + 1 新增） |

---

## 2. 验证标准来源

主 agent 内联的 6 项验证标准（阶段 5 推进条件复核 / 并发调度真实执行证据三项 / 合并态全量测试 / 十张功能卡合并态复核含两处重点 / 文档与事实一致性 / 偏差记录完整性），辅以 `workflow-pb.md` §验证目标（阶段 5 与阶段 6）与 `prd/F01~F10` 的产品维度条文、4 份 PR 文件的「验收标准」。

---

## 3. 逐项判定

### 标准 1：阶段 5 推进条件复核（4 PR × 3 项，全部基于合并态真实代码）

命令（只读）：`git diff --name-status <base>...<PR tip>`、`git log --merges/--parents`、源码直读、`npm test`。

| PR | ① 验收标准全部通过 | ② 无简报外改动 | ③ PR 文件存在 | 判定 |
|---|---|---|---|---|
| pr-001 | **pass**（7/7） | **pass** | **pass** | **pass** |
| pr-002 | **pass**（10/10） | **pass** | **pass** | **pass** |
| pr-003 | **pass**（7/7） | **pass** | **pass** | **pass** |
| pr-004 | **pass**（8/8） | **pass** | **pass** | **pass** |

**pr-001（`2af9f3d...2e9a950`，恰 2 文件 `M`：`src/agent.js` + `src/context-pool.js`）**
- AC1 唯一渲染点：`src/agent.js:57-59` `renderProjectContext()`；全 `src/` 中 `【项目上下文】` **仅此 1 处**（`grep -rn "【项目上下文】" src/` 单命中）；载荷解析 `parseProject()`（`:43-50`，三要素非空字符串才合法）在 `parseTaskBody`（`:81`）调用；`src/agent.js` import 列表不含 `web.js`。
- AC2 向后兼容：`parseProject` 对 `null`/字符串/数字/数组/缺字段一律返回 `null`（`:44-50` 逐行），任务不拒收（`parseTaskBody` 无相关 reject 分支）——与既有 `label` 的 `typeof body.label === 'string' ? … : null`（`:80`）同址同风格。
- AC3 一次性路径：`src/agent.js:198` `args.push(projectContext ? `${projectContext}\n\n${task.prompt}` : task.prompt)`（argv 末位）；未携带时末位逐字等于原文（同一三元表达式的 else 支）。
- AC4/AC5 常驻路径：`src/context-pool.js:146`（`prompt(...)` 收 `projectContext = null` 可选参数）、`:178`（`this.sentTurns === 0 && turn.projectContext` 才前缀）、`:184`（`this.sentTurns += 1` 位于 `await client.prompt(...)` **成功返回之后**）⇒ `context_busy`（`:148-150` 入队即拒，进会话前返回）与 `model_unavailable`/崩溃均不消费名额；会话重建（`dispose`/`_failSession`）后 `sentTurns` 归零重新注入。
- AC6 既有行为：pr-001 的 diff **零测试改动**（`git diff --name-status` 仅 2 个 src 文件）；全量 272/272（见标准 3）。
- AC7 语义不变：`context-pool.js` 的改动仅为"多一个可选参数 + 一个只增计数 + 前缀判定"，键（`getOrCreate`）/队列上限/`inFlight` 串行逻辑未出现在 diff 中。

**pr-002（`a9fef01...f97cff3`，恰 9 文件，与 PR 文件「文件范围」逐条一致）**
- AC1 schema：`src/persist.js:14-20` `projects(project_id TEXT PRIMARY KEY, name TEXT NOT NULL, repo_url TEXT NOT NULL UNIQUE, created_at INTEGER NOT NULL)`；`chats` 10 列且 `project_id` 为 **cid=1 / notnull=1 / FK→projects.project_id**（本验证者以真实 9 列旧库启动后 sqlite 直读，见标准 4-F08）；`idx_chats_project_updated`（`:46`）；`CHAT_COLUMNS` / `MESSAGE_COLUMNS`（`:65-66`）与 `main` 版本（`main:oamp/src/persist.js:61-62`）**逐字相同**。
- AC2 `GET /api/projects`：`src/persist.js:219-226` 单条聚合 SQL（`COUNT` / `MAX` 左连接 + `ORDER BY created_at DESC, project_id DESC`）；实测响应键 `[project_id,name,repo_url,created_at,chat_count,last_activity_at]`，无分页参数（`?limit=1` 仍返回全部 5 条）。
- AC3 `POST /api/projects`：实测 `{}` → `400 需要 repo_url（非空字符串）` 且项目数 0→0；重复地址 → `409 项目已存在: https://github.com/acme/dup`；`dup` 与 `dup.git` 各自 200（不归一化）；`not-a-url`、`git@example.internal:x.git` 均 200；非字符串 `name` 派生（`n2`）。
- AC4 `GET /api/chats`：实测 `?project_id=`（空值）与无参**同码同文案** `400 查询参数非法: project_id 不能为空（对话列表以项目为范围）`；未知 `project_id` → `200` 空列表；行内字段不含 `project_id`；`total` 与集合共用 `LIST_FROM`（`persist.js:281` `countChats` 用同一 `LIST_FROM`）。
- AC5 `POST /api/messages`：新建缺归属 → `400 新对话需要 project_id（对话必须归属一个项目）`；未知项目 → `400 项目不存在: prj-nope`；校验位置在 `src/web.js` 的只读 409 之后、`db.insertInput` 之前（顺序证据：空正文仍报"消息不能为空"、非法 model 仍先报、畸形 JSON 仍 400）。
- AC6 派发载荷：`src/web.js:692` `project = {name, repo_url, agreement: PROJECT_AGREEMENT}`，仅装配进两条 LLM 分支（`:695-698`），shell 分支 `{command:'/bin/sh', args:['-c',…], label}` 无 `project`；`insertInput` 的 `text` 为用户原文。
- AC7 `openDb()`：`persist.js:182-194` 旧库判据 `isLegacyChats`（`:47-52`）→ `db.close()` → `rmSync` → 重新 open + `DB_REBUILT path=…`；**本验证者真实 9 列旧库端到端复验**（见标准 4-F08）；`MIGRATIONS` / `migrate(` / `ALTER TABLE` 在 `src/persist.js` 中 **零命中**（已删除，无补列路径）。
- AC8 F10 三面三锁：13 条路由（`GET/POST /api/projects` 追加末位），`/api/docs` 投影 13 条、`danger` 6 条；`llms.txt` 与本验证者独立重算的 `renderLlmsTxt(projectRoutes(createApiRoutes({})))` **字节相等**（1995 B / 1995 B，`Buffer.compare === 0`）；`API.md` 归一化后路径级双向覆盖无缺（`<chat_id>` → `:` 归一，含两处新签名与 `## 3. 接口清单（13 条）`）；`dependencies === {}`。
- AC9 既有 4 个测试文件 diff 归四类：删除的 `assert.` 行与新增行比 = persist 41→104、web 5→98、api-routes 3→16、acp-daemon 1→14（**净增断言，无断言语义放宽**）；逐条核对删除行：persist 侧为 `listChats(...)` 调用点补 `project`（合并文件中 `listChats({ project` 42 处）与旧库断言语义反转；web/acp 侧为"桩回显"改为 `收到：【项目上下文】…`、argv 末位改为 `startsWith('【项目上下文】') && endsWith('\n\n一次性问题')`；api-routes 侧为 5→6、11→13 计数。无既有断言被删除而未替换、无超时/等待策略调整、无整文件格式化。
- AC10 其余 18 个测试文件零改写：`oamp/test/` 共 23 个文件，整迭代 diff 命中 5 个（4 既有 + 1 新增）⇒ 其余 18 个逐字未动（`git diff --name-only main...HEAD -- oamp/test/` 输出即此 5 行）。
- **既有 11 条路由表项元数据逐字不变（独立复核）**：把 `main:oamp/src/web.js` 与合并态 `src/web.js` 分别加载并 `projectRoutes()`，逐项 JSON 序列化比对 —— **11 条中 0 条元数据有差异**（params/errors/summary/response/docLink/danger 全等），新增恰为末尾两条 `/api/projects`。

**pr-003（`2af9f3d...40151ff`，恰 3 文件）**
- AC1 静态节点：`web/index.html:20-22`（`#project-bar` 位于 `</nav>` 之后）、`:32-40`（`#projects-view` 及五子节点）、`:43`（`<main class="layout hidden">`）。
- AC2 boot 分派：**真实浏览器实测** —— `/`：`#projects-view` = `block`、`main.layout` = `none`、`#project-bar` = `none`，请求集 `GET /api/agents, GET /api/projects, GET /api/events`（**无 `/api/chats`**）；`/?project=prj-does-not-exist`：回落列表视图（`main` = `none`），无报错。
- AC3 三处调用点：浏览器实测 —— 进入项目后归档视图请求 `GET /api/chats?archived=1&limit=200&offset=0&project_id=prj-6a13…`；发送请求体逐字 `{"agent_id":"fake-agent","text":"@fake-agent 项目上下文探针","project_id":"prj-6a13ef37-…"}`；随后 `GET /api/chats?project_id=…`。
- AC4 项目层取数与渲染：源码 `app.js:579/592/615/641/651/659` 五函数齐备；浏览器实测列表行渲染、`last_activity_at: null` → `—`（`fmtAgo(p.last_activity_at ?? undefined)`）；创建后停留 `/` 且原地出现（pr-004 断言面 + pr-004 报告的同款运行期复核，本报告独立复现了"停留 + 不调 /api/chats"这一半）。
- AC5 三条真隐藏规则：`web/style.css:366-368` `.layout.hidden` / `.projects-view.hidden` / `.project-bar.hidden { display: none; }`；`git diff --numstat` = `52 0`（纯追加、零删除行）。
- AC6 零新增静态资产：`STATIC_FILES` = **12 键**（本验证者正则计数），`'/': '/index.html'` 在场；`src/` 零改动（diff 仅 `web/` 三文件）。
- AC7 零改写清单：`web/app.js` 删除行仅 4 行（三处调用点 + boot 顺序前移），SSE/归档/改名/补全/计时/只读/过滤区域不在改动块内；`grep -rn "已 clone\|已在项目根目录"` 在 `web/` + `src/` 中仅命中 `src/web.js:41` 的**注释**（否定式实现说明，非页面文案/注入内容）。

**pr-004（`0d726f2...60ba902`，`A oamp/test/project-workspace.test.js` 唯一改动）**
- AC1：唯一新增文件 1317 行、21 个 `test(`；`git diff --name-status` 恰 1 个 `A`（无既有测试/源码/文档改动）；与 `60ba902:` 版本逐字节相同（`git diff` 空）；本验证者亲自跑全量 272/272。
- AC2~AC7（F01/F02/F03/F06/F07/F08/F10 断言）：全量套件中该文件 21 个用例全绿；关键断言位置本验证者抽读确认（如 `test:1005` 末位 argv 严格相等、`test:443-463` 派生列 sqlite 对拍、`test:1012/1017` 首轮注入与次轮不注入）。
- AC8 隔离：`test/project-workspace.test.js:110/229/985/1115` 一律 `fs.mkdtempSync(path.join(os.tmpdir(), …))`（另 `:1110` 的 `startWeb` 亦以临时库为 `OAMP_DB`）；`:225` 注入 `OAMP_OMP_BIN` fake；全量运行后 `oamp/data/sql.db` mtime 未变（14:54:04）、`git status --porcelain --ignored -- oamp` 无新增垃圾。

### 标准 2：并发调度真实执行证据三项

**① worktree 时间窗口重叠 —— pass（并如实标注 git 能证明什么、不能证明什么）**

| 证据 | 内容 |
|---|---|
| 拓扑（`git log --format='%h %p %ad %s'`） | `2e9a950`(pr-001) 与 `40151ff`(pr-003) 的父提交**同为 `2af9f3d`**，二者互不包含（非祖先关系）⇒ 同一基点上的两条独立分支 |
| 时间（精确到秒） | `40151ff` = `16:19:28`、`2e9a950` = `16:21:41`（相隔 2 分 13 秒）；**首个合并** `9e1f4d4`/`a9fef01` 均为 `16:35:51` ⇒ 两条分支的提交在**任何合并发生之前**已各自存在 |
| 磁盘现场（独立旁证） | `clarifications/verify-pr-003-20260912-162639.md`（16:26:39 落盘）与 `verify-pr-001-20260912-163434.md`（16:34:34 落盘）**两份独立验证报告**各自记录其观测时刻 `.pb-agents/worktrees/` 下**同时存在** `pr-001` 与 `pr-003` 两个 worktree 目录（pr-003 报告 D-01 证据栏逐字：「`ls .pb-agents/worktrees/` 仅 `pr-001`/`pr-003`」）；两者的合并发生在两份验证完成**之后**（`16:35:51`） |
| **git 能证明 / 不能证明** | **能**：两条特性提交同基点、互不包含、均早于首个合并，且两个 worktree 目录在同一时间段被独立观测到共存（⇒"两条并发分支同时开放"成立；"先后串行后再声称并发"这一误报形态被排除）。**不能**：git 不记录分支/worktree 的**存在区间**——`feat/pr-*` 分支已删除、`.git/worktrees/` 目录已不存在、`.pb-agents/worktrees/` 已清空（mtime 17:30:53），故**进程级并行**（两个子 agent 真正同时运行）无法由 git 证明；同样无法排除"同一执行者在两个 worktree 中交替推进"。本项按工作流口径（存在 ≥2 个 worktree 分支在磁盘上同时存在过）判 **pass**。 |

**② 并发配置区块真实初始化与更新 —— pass**

在 `428e672`（HEAD）中五个字段**均有值**；跨 5 个提交追踪其演化（`git cat-file -p <commit>:…/status.md`）：

| 提交（时刻） | 起始并发数 | 硬上限 | 当前有效上限 | 累计槛位释放次数 | 已派发总数 |
|---|---|---|---|---|---|
| `2af9f3d`（16:09，阶段 4 产物） | 3 | 5 | **3** | **0** | **0** |
| `b3f5a82`（16:35，pr-001/pr-003 派发记录） | 3 | 5 | 3 | 0 | **2** |
| `fc76fcd`（16:43，pr-002 派发记录） | 3 | 5 | **5**（爬升） | **2** | **3** |
| `20ddb09`（17:12，pr-004 派发记录） | 3 | 5 | 5 | **3** | **4** |
| `428e672`（17:30，合并后） | 3 | 5 | 5 | 3 | 4 |
| **工作区版本**（17:32 未提交） | 3 | 5 | 5 | **4** | 4 |

⇒ 字段非"初始化后再未变化"：已派发总数 0→2→3→4，释放次数 0→2→3→4，有效上限 3→5，逐次随合并/派发被真实更新。

**③ 爬升公式真实触发 —— pass（数值正确；公式注记存在滞后，见 D-3）**

- 爬升确已触发：`fc76fcd` 时刻 `当前有效上限` 由初始 3 变为 5（释放 2 次后 `min(3 + 2×3, 5) = 5`），此后维持硬上限。
- 终值复核：`min(起始 3 + 累计释放 4 × 起始 3, 硬上限 5) = min(15, 5) = 5` —— 与 `status.md` 声明的 `5` **一致**；`硬上限 = 2×3−1 = 5` 亦与公式一致。
- 注记偏差：`status.md` 括注写 `min(3 + 3×3, 5)`，而相邻字段 `累计槛位释放次数 = 4` ⇒ 括号内的乘数（3）与字段值（4）不同步（数值 5 不受影响，因两式均被硬上限截断）。记为 D-3。

### 标准 3：合并态全量测试真实通过 —— pass

命令与原始结果（cwd = `/Users/chenchiyuan/projects/agents/oamp`，`npm test` = `node --test test/*.test.js`）：

```
1..272
# tests 272
# suites 0
# pass 272
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 56986.369584
```

- 与基线对齐：main = 251；本迭代新增 21 个用例（pr-004 的 `project-workspace.test.js`）⇒ 272 = 251 + 21，无用例被删/被 skip（`skipped 0`、`todo 0`）。
- 运行前后 `oamp/data/sql.db` mtime 不变（未污染真实库）；无仓库内临时产物。

### 标准 4：十张功能卡在合并态复核

判定口径：**pass** = 该卡全部产品维度条文在合并态有可定位证据且本验证者复核通过；**partial** = 有条文缺证据或证据不足；**fail** = 有反证。

| 卡 | 判定 | 复核要点（本验证者亲自执行的证据） |
|---|---|---|
| F01 项目实体（7 条） | **pass** | 派生名（空名 → `alpha`）、不归一化（`dup`/`dup.git` 双 200）、可疑地址可建（`not-a-url`、SSH 形态均 200）、缺失地址 400 且**条数不变**（0→0）、重复 409、列表四要素 + 两派生列、排序 `created_at DESC` 实测降序、无分页（`?limit=1` 仍 5 条）；库内项目行仅 `project_id/name/repo_url/created_at`（DDL 直读，无路径字段） |
| F02 新建对话必须归属（5 条） | **pass** | 带归属新建 200 且落库 `project_id` = 请求值（sqlite 直读）、缺/空归属 400 且对话数不变、未知项目 400（非 404）、`pragma_table_info('chats').project_id` `notnull=1` + `FK→projects.project_id`（真实旧库重建后直读）、前端 `send()` 带 `state.projectId`（浏览器实测请求体） |
| F03 列表以项目为范围（5 条） | **pass** | A/B 两项目列表互斥（A=2 条、B=0 条，sqlite 对拍）、缺参与**空值**都 400、既有 `q/agent/state/from/to/archived/limit/offset` 在项目范围内语义不变（`state=idle` → 400 文案逐字）、跨项目不混列在数据层可核对 |
| F04 项目层入口（6 条） | **pass** | 首屏列表视图（浏览器：`#projects-view` block / `main` none）、未选项目零 `/api/chats` 请求、可新建（输入框 + 地址必填）、可进入、**列表信息量（重点 a：原 partial 项）**、创建后停留原地出现 |
| F05 项目工作台（6 条） | **pass** | 进入即工作台（`main` flex）、顶栏项目名（实测"dup"）、返回入口 `href="/"`、**对话范围 = 当前项目（重点 a）**、**新建自动归属（重点 a）**、既有能力照常（全量 272/272 + 误差契约实测无 405） |
| F06 派发载荷（6 条） | **pass** | `payload.body.project` 三要素（`web.js:692`）+ 两条 LLM 分支装配（`:695-699`）、shell 分支无 `project`（逐字 `{command,args,label}`）、`messages.text` 为原文、协议方法面 7 个未变、上下文池键/并发语义未变（pr-001 diff 仅 2 文件、`context-pool.test.js` 零改动且绿） |
| F07 上下文内容与注入（7 条） | **pass** | 渲染三行逐字（`agent.js:58`）；`PROJECT_AGREEMENT`（`web.js:43-44`）与 `architecture.md` §4.3 T-12 **逐字相等**（本验证者正则抽取比对 `equal: true`）；约定含 clone/仓库根目录/产物落点三件事；零绝对路径（正则复核 false）；无超能力表述（`web/`+`src/` 仅命中一条否定式注释）；仓库地址字符串在 argv/回显/载荷三处出现（pr-004 断言 + 本验证者的请求体观测） |
| F08 旧库重建（6 条） | **pass** | **本验证者独立端到端**：造真实 9 列旧库（含 1 对话 + 1 消息）→ 启动 `oamp web start` → stdout `DB_REBUILT path=<dbPath>` + `WEB_READY`、stderr 无 Error；重建后列集 = `chat_id, project_id(NN), title(NN), agent_id, state(NN), created_at(NN), updated_at(NN), closed_at, archived_at, context_released(NN)`、`projects` 表存在、`chats/messages/projects` 行数 = 0/0/0、FK = `projects.project_id`；二次启动无 `DB_REBUILT`；`MIGRATIONS`/`migrate(`/`ALTER TABLE` 零命中 |
| F09 既有行为零变化（5 条） | **pass** | 无 405（实测 `DELETE /api/projects` → `404 not found: DELETE /api/projects`、`DELETE /api/agents` → 404）；统一错误契约键集合 `{error,code}` 实测；既有 11 条路由元数据逐字不变（独立比对 0 差异）；既有测试面仅在四类形态内改动（断言净增）；协议面 7 个不变；`127.0.0.1` 监听与无鉴权未变（`src/web.js` 未触碰监听/鉴权代码） |
| F10 登记义务与工程锁（5 条） | **pass（按条文；迭代级缺口见 D-2）** | 三面自动出现（`/docs` 200 且 13 区块、`/debug` 200、`llms.txt` 两条新行 + 逐字节相等）；`API.md` 路径级双向覆盖（归一化后无缺）；三锁全绿（本验证者独立重算 llms.txt 与元数据校验）；`dependencies === {}`；新增登记不改既有接口**元数据**行为（0 差异）。**注**：卡条文只覆盖"新增项目类接口的登记"，未覆盖"既有接口的新必填参数是否登记"——该缺口不构成本卡 fail，但与用户价值同源，已记 D-2 |

**重点复核 (a)：pr-003 单 PR 验收时标 partial 的三项（服务端依赖面）在合并态是否真实成立 —— 三项全部成立**

| 项 | pr-003 报告判定 | 合并态本验证者证据 | 结论 |
|---|---|---|---|
| F04-5 列表信息量（服务端聚合面） | partial（`GET /api/projects` 在 pr-003 worktree 不存在） | 真实进程：项目 A 下建 2 个对话各发 1 条消息 → `GET /api/projects` 返回 A `chat_count = 2`、`last_activity_at = 1789205653307`，消息发送窗口 `[1789205653301, 1789205653311]` ⇒ **不早于该消息时间**；B `chat_count = 0`、`last_activity_at = null`；sqlite 直读 `COUNT=2 / MAX=1789205653307` 与响应逐位相等 | **已成立（pass）** |
| F05-4 工作台对话范围 = 当前项目（DB 级隔离） | partial | `GET /api/chats?project_id=A` → `total 2`、含两个新建对话；同参 B → `total 0`、`chats []`；浏览器实测工作台侧请求仅 `…project_id=<当前项目>` | **已成立（pass）** |
| F05-5 工作台内新建自动归属 | partial | 浏览器实抓 `POST /api/messages` 请求体含 `project_id` = 当前项目；服务端 sqlite 直读两行 chatter 的 `project_id` 均 = A，且出现在 A 列表、不出现于 B 列表 | **已成立（pass）** |

**重点复核 (b)：pr-002 验收报告记录的 A-7 偏差是否构成用户可见缺陷 —— 构成，且已实证**

| 观测面 | 实测结论 | 证据 |
|---|---|---|
| 路由登记 | `/api/chats` 与 `/api/messages` 的 `params` **均未登记 `project_id`**（前者 8 个参数无 `project_id`，后者 5 个） | `oamp/src/web.js` 表项直读；`GET /api/docs` 投影逐字段列出 |
| `/docs` 文档页 | `/api/chats` 参数表 8 行、`/api/messages` 请求体字段 5 行，**均无 `project_id` 行**；后者 `chat_id` 说明仍写"不存在则新建"，未提示新建必须携带 `project_id` | 真实浏览器（`/docs`）DOM 抽取 |
| `/debug` 调试台 | 表单控件**完全由投影生成**（`web/debug.js` `renderPanel()` → `route.params.map(fieldHtml)`），故 `GET /api/chats` 只渲染 `q/agent/state/from/to/archived/limit/offset` 八个控件，`project_id` **无输入位**；点击「发送」实测返回 `400 · 7ms` `{"error":"查询参数非法: project_id 不能为空（对话列表以项目为范围）","code":"INVALID_PARAM"}` | 真实浏览器（`/debug`）控件枚举 + 真实请求响应 |
| 影响面 | 0016 交付的调试台对**已登记的两条既有接口**（`GET /api/chats`、`POST /api/messages` 的新建路径）**无法完成任何调用**；文档页对这两条接口的参数面**呈现不完整** | 上两行 |

⇒ **A-7 = 用户可见缺陷（严重度：中）**。它不违反 0017 任何一张卡或任何一份 PR 的验收标准（`architecture.md` §6.1 要求"既有 11 条内文逐字不动"，实现是遵从契约的结果），但**破坏了 0016 既有能力（调试台/文档页）在本迭代后的可用性**，且该破坏在阶段 5 的 PR 级验收中仅作为"待确认口径"留痕、未升级为返工项。

### 标准 5：文档与事实一致性

对 `status.md`（工作区版本）逐项与 git / 文件系统事实对拍：

| status.md 声明 | 事实（本验证者复核） | 判定 |
|---|---|---|
| 阶段 5「4/4 PR 全部合并进迭代分支（pr-001 9e1f4d4 / pr-003 a9fef01 / pr-002 0d726f2 / pr-004 428e672）」 | `git log --merges main..iteration/0017-project-workspace` 恰 4 条且哈希逐一相符 | **一致** |
| 阶段 5「合并态全量 272/272」 | 亲自运行 = 272/272 / 0 fail | **一致** |
| PR 子状态表「worktree 分支（已清理）」×4 | `.pb-agents/worktrees/` 为空、`.git/worktrees/` 不存在、`git branch -a` 无 `feat/*` | **一致** |
| PR 子状态「已合并 ✅ / 槛位状态 已释放」×4 | 4 个合并提交存在；无 `feat/*` 残留 | **一致** |
| 并发配置五字段有值且被更新 | 五字段均有值；跨 5 个提交追踪到 0→2→3→4 与 3→5 的变化 | **一致** |
| 阶段 6「⏸ 最终验证执行中」 | 本报告即为该验证 | **一致** |
| 更新日志最后一条止于 pr-002 合并（「全量 251/251」），无 pr-004 合并条目 | pr-004 已于 `428e672` 合并、合并态为 272/272 | **不一致（日志滞后，见 D-4）** |
| `当前有效上限 5（按公式 min(3 + 3×3, 5) 重算）` 与相邻 `累计槛位释放次数: 4` | 括注乘数 3 ≠ 字段值 4（数值 5 正确） | **不一致（注记滞后，见 D-3）** |
| 声明载体 | `status.md` 相对 HEAD **有未提交改动**（HEAD 版本仍写"3/4 PR 已合并、pr-004 占用"）；即阶段 5 完成态只存在于工作区 | **事实记录（D-4 附带）** |
| `progress.md` §3 的三条特性提交父提交与"pr-004 现场建于 17:06:25" | 与 git 拓扑、`reflog` 一致（本验证者复核 `git log --parents`） | **一致** |

### 标准 6：偏差记录完整性

| PR 级报告 | 报告内偏差条数 | 本验证者复核 | 是否阻塞交付 | 是否被静默忽略 |
|---|---|---|---|---|
| `verify-pr-001-20260912-163434.md` | **6**（D-1~D-6） | 逐条存在且各有规格描述 / 实现实际行为 / 建议处理三栏 | 否（结论 PASS） | 否。D-1（`\n\n` 与 PR 措辞）、D-5（协议方法面口径）为措辞级；D-2（合并顺序警示）已被 pr-002 落地闭合；D-3（无仓库内注入断言）**已被 pr-004 的新测试文件闭合**（`project-workspace.test.js:1005/1012/1017`） |
| `verify-pr-002-20260912-170519.md` | **7**（A-1~A-7） | 逐条存在；A-1（413 元数据）与 A-7（`project_id` 未登记）本验证者独立复现（见标准 4-b） | 否（结论 PASS） | 否。A-1/A-7 已升级至 `status.md`「待确认项·已知偏差（下一迭代候选，主 agent 裁定不返工）」；A-2~A-6 为低/信息级并各有候选编号 |
| `verify-pr-003-20260912-162639.md` | **4**（D-01~D-04） | 逐条存在；D-01 的 3 项 partial 已在合并态由本报告闭合（标准 4-a）；D-02（重复 `GET /api/projects`）与 D-04（`state.projectId` 未重置）在合并态**仍成立**（浏览器实测未知项目回落时 `/api/projects` 请求 2 次） | 否（结论 PASS；partial 属外部依赖未落地） | 否。3 项 partial 记入 `status.md` 待办；D-02/D-04 留于报告并有候选 2/3 |
| `verify-pr-004-20260912-172839.md` | **9**（D1~D9） | 逐条存在；D5（一轮 271/272 的 SIGINT 抖动）本验证者在安静环境未复现（272/272） | 否（结论 PASS） | 否。D1~D9 均有候选编号 N1~N6 |

- 合计偏差留痕 **26 条**（6+7+4+9），四份报告结论均为 PASS，无一条采取"静默删除/不记录"处理。
- 跨迭代闭合检查：**唯一存在"未闭合"性质的 3 条**（pr-003 的 3 项 partial）已在本报告标准 4-(a) 逐项闭合为"已真实成立"；A-1/A-7 保持未修复（主 agent 明示裁定不返工），属"已记录、待决策"而非"被忽略"。

---

## 4. 汇总

| 验证标准 | 判定 | fail | partial | blocked |
|---|---|---|---|---|
| 1. 阶段 5 推进条件复核（4 PR × 3 项 = 12 项） | **pass**（12/12） | 0 | 0 | 0 |
| 2. 并发调度真实执行证据三项 | **pass**（3/3） | 0 | 0 | 0 |
| 3. 合并态全量测试（272 用例） | **pass**（272/272，fail 0） | 0 | 0 | 0 |
| 4. 十张功能卡合并态复查（53 条卡内条文 + 2 个重点项） | **pass**（F01~F10 全 pass；重点 a 三项已成立；重点 b 确认构成用户可见缺陷 → D-2） | 0 | 0 | 0 |
| 5. 文档与事实一致性（9 项对拍） | **pass**（7 项一致 / 2 项不一致均记偏差，不影响推进） | 0 | 0 | 0 |
| 6. 偏差记录完整性（4 份报告 26 条） | **pass** | 0 | 0 | 0 |

- **fail 条目数：0**
- **partial 条目数：0**
- **blocked 条目数：0**
- **偏差记录条数：7**（D-1 ~ D-7；其中 D-2 为用户可见缺陷，严重度中）
- 未使用"基本通过"一类模糊判定；每一条判定均附可定位证据（文件:行 / 命令输出 / 浏览器观测值）。

---

## 5. 偏差记录

| # | 规格描述（或应然） | 实现实际行为 | 严重度 | 可定位证据 | 建议处理 |
|---|---|---|---|---|---|
| **D-1** | `POST /api/projects` 的登记 `errors` 应覆盖 handler 实际可达的错误码（`POST /api/messages` 的登记即含 `PAYLOAD_TOO_LARGE`） | 登记 `['INVALID_PARAM','CONFLICT']`；实测超 64 KiB 体返回 `413 {"error":"请求体过大（上限 65536 字节）","code":"PAYLOAD_TOO_LARGE"}`；`API.md §3.13` 错误表**已列 413**，而 `/api/docs` 投影（⇒ `/docs` 页面错误码一栏）**无 413** | 中 | `src/web.js:761`（errors 数组）、`:770`（catch 内 413 分支）、`:617`（messages 对照）；实测响应与 `/api/docs` 投影输出 | 元数据补 `PAYLOAD_TOO_LARGE`（与 `API.md` 对齐）；可把"登记 errors 必须覆盖 handler 实际可达码"升格为锁①的可机检口径（现状锁①只校验取值合法性） |
| **D-2** | 新增的**必填**参数应在三面（`/api/docs` 投影 → `/docs` 页 / `/debug` 台）可呈现、可填写 | `GET /api/chats` 与 `POST /api/messages` 的 `params` **未登记 `project_id`**：`/docs` 两条接口的参数表/请求体字段表无该行；`/debug` 无输入控件，点击发送实测 `400 查询参数非法: project_id 不能为空`。⇒ **用户可见缺陷**：调试台对这两条既有接口不可用，文档页参数面不完整 | **中（用户可见）** | `src/web.js:352-361`（chats params）、`:609-615`（messages params）；浏览器实测 `/docs` 参数表与 `/debug` 控件枚举、`400` 响应；`API.md §3.2/§3.8` 参数表**已登记** `project_id`（文档与三面不一致，缺口在"三面"侧） | 登记 `project_id`（`in: 'query'` / `'body'`）；若坚持 `architecture.md` §6.1「既有 11 条内文逐字不动」，则须在 §8.2/§8.3 显式登记第五类必然变更面，并至少让 `/debug` 能填写该参数（否则应把"调试台不可用"写为已知限制） |
| **D-3** | `当前有效上限` 的计算式注记应与相邻字段一致 | `当前有效上限: 5（按公式 min(3 + 3×3, 5) 重算）` 而 `累计槛位释放次数: 4` ⇒ 括注乘数（3）滞后于字段值（4）；数值 5 本身正确（`min(3+4×3,5) = min(3+3×3,5) = 5`，均被硬上限截断） | 低 | `status.md:34-35`；本报告标准 2-③ 的公式复算 | 括注改为 `min(3 + 4×3, 5)`（或去掉算式只留结论），并考虑把"注记算式必须与字段值自洽"写入台账更新检查项 |
| **D-4** | `status.md` 更新日志应覆盖到最终合并 | 更新日志最后一条止于"pr-002 实现 → 验收 PASS → 合并（0d726f2），全量 251/251 → 解锁 pr-004 并派发"，**无 pr-004 合并条目**；同时 `status.md` 相对 `428e672` **未提交**（HEAD 版本仍写"3/4 PR 已合并"、"pr-004 占用"）⇒ 阶段 5 完成态目前只存在于工作区文件 | 低 | `status.md:15-17`、`:28-36`、更新日志段；`git status --porcelain` = ` M status.md`；`git diff -- status.md`（工作区 vs HEAD 差异即上述字段） | 随阶段 6 收口一并提交，并补 pr-004 合并条目（含合并态 272/272） |
| **D-5** | 未知 `project_id` 回落项目列表视图时不应重复取数；回落后不应保留脏状态 | 浏览器实测 `/?project=prj-does-not-exist`：请求序列出现 **两次** `GET /api/projects`（成因：`resolveCurrentProject()` 内已 `loadProjects()`，`showProjectList()` 再调一次）；`state.projectId` 保留为未知值不回滚 | 低（无功能影响） | 浏览器请求日志（本报告标准 1-pr-003 AC2 观测）；`web/app.js:641-656`；与 `verify-pr-003` 报告 D-02/D-04 同源 | 按 pr-003 报告候选 2/3 处理（复用已取回的 `state.projects`；回落时重置 `state.projectId`） |
| **D-6** | 全量套件应稳定全绿 | 安静环境本验证者 272/272（首轮即全绿）；`verify-pr-004` 报告记录过一轮 271/272（`test/router-registry.test.js:135` SIGINT `exit.code null !== 0`，53.4ms） | 低（环境敏感） | 本报告标准 3 输出 vs `verify-pr-004` 报告 §2.5 表 (b) 第 1 次 | 按 pr-004 候选 N3 处理：`startRouter().stop()` 对 `{code:null, signal:'SIGINT'}` 增加诊断/有限重试，消除负载下的偶发红 |
| **D-7** | 旧库重建的测试 fixture 应覆盖真实前版 schema | `test/project-workspace.test.js:1079-1106` 的 `writeLegacyDb()` 造 **7 列**旧 `chats`（缺 `archived_at`/`context_released`），而真实前版为 **9 列**；本验证者以真实 9 列旧库独立复验通过（重建、列集、FK、行数 0/0/0、二次启动不重建） | 低（覆盖窄化） | 同上行号；本报告标准 4-F08 的独立探针输出 | 按 pr-004 候选 N2 处理：以 9 列真实旧 schema 为主 fixture（可保留 7 列作第二形态） |

---

## 6. 下一迭代候选

| # | 候选 | 来源偏差 | 建议口径 |
|---|---|---|---|
| 1 | **`project_id` 登记与调试台可用性修复**（把 `project_id` 登记进 `GET /api/chats` 与 `POST /api/messages` 的 `params`；同时确立"条件必填"的表达方式——如 `required: false` + 说明"仅新建对话时必填"，或给 `params` 增 `requiredWhen` 语义），并解锁 `architecture.md` §6.1「既有 11 条内文逐字不动」与本项的口径冲突 | D-2（A-7） | **优先级最高**：这是本迭代唯一被实证的用户可见缺陷（0016 调试台对两条既有接口不可用）。需先裁决"参数登记"是否属项目维度的必然变更面 |
| 2 | `POST /api/projects` 的 `errors` 补 `PAYLOAD_TOO_LARGE`，并把"登记 errors ⊇ handler 实际可达码"做成锁①的可机检项 | D-1（A-1） | 低风险；`API.md §3.13` 已先落地 413，改元数据即闭环 |
| 3 | 台账自洽检查：`当前有效上限` 的注记算式与 `累计槛位释放次数` 强制同步；更新日志必须覆盖到最终合并提交 | D-3 / D-4 | 低风险；可写成主 agent 的"合并后必做"清单（0015 / 0016 均出现过"有效上限字段修正"提交，属复发模式） |
| 4 | 未知项目回落路径的质量修整：复用已取回的 `state.projects`、回落时重置 `state.projectId` | D-5 | 低风险；无功能影响，纯冗余与脏状态 |
| 5 | 前端运行期 E2E 回归补位：现有前端断言是文本级静态契约，"首屏视图互斥 / 创建原地出现 / 进入与返回 / 按项目取列表 / 未知 id 回落"无自动化运行期保护 | 标准 4-F04/F05 的证据层级 | 中；可复用 pr-004 报告所述浏览器脚本形态（本报告亦以同法复核） |
| 6 | 断言强度升级：`POLL_MS` 文本锁、`MIGRATIONS` 文本锁、`assert.ok(chatId)` 三处弱断言改为行为级判据；`router-registry` SIGINT 竞态加固；旧库 fixture 保真为 9 列 | D-6 / D-7（pr-004 D1/D2/D4/D6/D7） | 低-中；均为"绿但不够强"的已知形态 |
| 7 | 既有 11 条路由表项的**参数面**审计：本次只核对了"登记集合与既有元数据逐字不变"，未核查"既有元数据是否已与实现完全对齐"（D-2 即此类） | D-2 的推广 | 低；建议纳入 0016 页面类能力的常规回归项 |

---

## 7. 结论

# **PASS**

**判定依据**：
1. **阶段 5 推进条件**：4 个 PR 在合并态逐一复核，12 项（验收标准通过 / 无简报外改动 / PR 文件存在）全部成立；每份 PR 的 diff 文件集合与自身「文件范围」**逐条相等**（2 / 9 / 3 / 1 个文件），无边界外改动；既有 11 条路由元数据与 main **零差异**。
2. **并发调度真实执行证据**：三项全部通过 —— ① 两条同基点的并发分支（`2af9f3d` → `2e9a950` / `40151ff`）在任何合并之前均已提交，且两份独立验证报告在其观测时刻记录两个 PR worktree 在磁盘上共存（同时如实标注：git 不能证明进程级并行，因分支与 worktree 注册已清理）；② 并发配置五字段均有值且跨 5 个提交被真实更新（0→2→3→4 / 3→5）；③ 爬升公式确已触发、终值 `min(3+4×3,5)=5` 正确。
3. **合并态全量测试**：本验证者亲自运行 `npm test` = **272 tests / 272 pass / 0 fail / 0 skipped**（≈57s），相对 main（251）新增 21 个用例。
4. **十张功能卡**：F01~F10 全部 pass；**重点 (a)** pr-003 遗留的 3 项 partial（F04-5 / F05-4 / F05-5）在合并态**已真实成立**（真实进程 + sqlite 直读 + 浏览器实抓请求三路证据）；**重点 (b)** A-7 偏差**构成用户可见缺陷**（`/docs` 参数面缺该必填参数、`/debug` 无法填写且调用必 400），已记入 D-2 并列为下一迭代第一优先候选 —— 它不违反 0017 任何卡或 PR 的验收标准，故不改变本报告的 PASS 判定。
5. **文档与事实一致性**：9 项对拍中 7 项完全一致，2 项为注记/日志滞后（D-3、D-4），不影响推进判定。
6. **偏差记录完整性**：4 份 PR 级验收报告共 **26 条偏差**全部如实记录、各有处理建议，四份结论均为 PASS 且未阻塞交付；未发现被静默忽略的偏差。

**结论：合并态迭代产物可交付。** 交付前建议（不构成 FAIL）：至少处理 **D-2**（用户可见缺陷）或将其明确写入 0018 的首个任务；并随阶段 6 收口一并提交 `status.md`（含 D-3 / D-4 的台账修正）。

**验证者签名**：发布前评审者（需求 / 架构 / 测试 / 交付四重视角；未接触执行过程上下文，未采信任何角色报告结论）
**报告路径**：`docs/iterations/0017-project-workspace/clarifications/verify-stage6-20260912-173934.md`
