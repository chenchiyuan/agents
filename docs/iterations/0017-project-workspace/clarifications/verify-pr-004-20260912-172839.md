# pr-004 独立验收报告（阶段 5 / PR 级）

- **迭代**：0017-project-workspace
- **被验证产物**：`.pb-agents/worktrees/pr-004`（分支 `feat/pr-004`，HEAD `60ba902`，基线 `0d726f2`）
- **验收标准来源**：`docs/iterations/0017-project-workspace/prs/pr-004-project-workspace-acceptance.md`（逐条）+ 主 agent 内联的 6 项验证标准 + `prd/F01~F08`、`prd/F10`（产品维度）+ `architecture.md` §8.1 / §8.4（断言落点与观测面）
- **验证日期**：2026-09-12 17:28:39
- **报告路径**：`docs/iterations/0017-project-workspace/clarifications/verify-pr-004-20260912-172839.md`

---

## 0. 验证者身份（反射结果）

**产出物类型** = 测试代码（新增测试文件）+ 该文件对 F01~F08 / F10 产品维度的验收断言。

**反射身份** = **测试工程师（兼代码审查者）**。判定三原则：

1. **覆盖审查**：每条验收条文是否有可定位的断言；
2. **可证伪审查**：断言是否有正反双向判据（用变异实验证明"改坏会红"，而不是只看"现在是绿"）；
3. **实际执行审查**：断言是否真的被执行（用例级 skip / 被 catch 吞掉 / 抽取失败即静默）。

**辅助身份** = 接口契约审查者（F01~F03 / F10 的 HTTP 契约面）+ 前端契约审查者（F04 / F05 的静态契约，并由本验证者用真实浏览器独立复核运行期行为）。

**独立性声明**：本次仅读取了「PR 验收标准文件、9 张 PRD 卡、architecture 相关节、被验证 worktree 的源码/测试/文档、运行测试产生的 stdout 日志」。**未读** `history.md`、dev 报告、`clarifications/` 下既有文件（含 `verify-pr-001/002/003`）。全程未修改被验证 worktree 内任何文件（验证前后 `git status --porcelain` 均为空）。

---

## 1. 产出物

| 项 | 值 |
|---|---|
| 唯一新增文件 | `oamp/test/project-workspace.test.js`（1317 行，1 个 blob） |
| git blob | `1e455edfafa13eec21ec114fecaf32777e2bbce4` |
| sha256（工作树 = HEAD） | `643fff50238e1b5518ddfe4da4a448d4fde22995a665aa6e502f8fe17a0f3f3b` |
| 用例数 | 21 个 `test()`（F01 ×3、F02 ×4、F03 ×3、F04 ×3、F06 ×2、F07 ×2、F08 ×2、F10 ×2） |
| 全分支变更面 | 仅此一个文件（`A`，无删除、无重命名、无其它改动） |

---

## 2. 逐项判定

### 2.1 标准 1：PR 文件「验收标准」逐条（8 条）

| # | 条文（摘要） | 判定 | 可定位证据 |
|---|---|---|---|
| 1 | 新文件存在且 `npm test` 全绿；本 PR 不修改任何既有测试文件与任何 `oamp/src/**`、`oamp/web/**`、文档文件 | **pass** | `git diff --name-status 0d726f2...HEAD` → `A oamp/test/project-workspace.test.js`（唯一）；`git status --porcelain` 空；单文件 21/21；安静环境全量 `272 tests / 272 pass / 0 fail / 0 skipped / 0 todo`（`/tmp/pr004-full2.log`）。首轮全量 271/272 的偶发红见偏差 D5（外部并发负载所致；安静环境重跑全绿、该用例单跑 5 次全绿，已排除为本 PR 缺陷） |
| 2 | F01：往返 + 派生名 + 不归一化 + 400/409 + 排序 + 无分页 + 无路径字段 | **pass** | `test:314-368`（往返/四要素/派生四种入参形态/兜底）、`test:370-431`（不归一化、可疑地址可建、400 逐字、409 逐字、排序、`?limit=1` 无分页）、`test:433-468`（派生列 sqlite 对拍） |
| 3 | F02 / F03：归属必填三类 400、未知项目 400、合法新建落库、项目范围列表与 `total` 同源、8 类既有过滤、`pragma_table_info` + `NOT NULL`（E4） | **pass** | `test:471-518`、`test:521-558`、`test:560-607`、`test:609-659`、`test:662-710`、`test:712-781`、`test:783-804` |
| 4 | F04 / F05 静态契约：9 个节点、boot 分派与 3 处带参调用点、3 条真隐藏规则 | **pass** | `test:806-908`（九节点 + 结构契约 + boot 顺序 + 三处调用点 + 零 `POLL_MS` + 三条 `display:none`）。本验证者另以真实浏览器复核运行期行为（见 §2.4.5） |
| 5 | F06 / F07：三要素、两条 LLM 分支、shell 无 `project`、末位 argv 逐字、回显含 `repo_url`、`agreement` 零绝对路径、E9 原文逐字、方法面 7 个 | **pass** | `test:910-963`、`test:965-978`、`test:984-1031`、`test:1033-1062` |
| 6 | F08：旧库重建（列集/行数/无兜底/告知行）+ 三次启动对拍（E6） | **pass** | `test:1064-1164`、`test:1166-1204`；本验证者以**真实 9 列旧库**独立复验（见 §2.4.1②） |
| 7 | F10：`/api/docs` 13 条（写 6 条）、两面自动出现、`llms.txt` 逐字节、`API.md` 双向、`dependencies === {}` | **pass** | `test:1206-1287`、`test:1289-1317` |
| 8 | 断言不依赖真实 omp / 真实 LLM / 外网，且不写仓库内 `oamp/data/sql.db` | **pass** | `test:225`（`OAMP_OMP_BIN=FAKE_BIN`）、`test:231-238`（`OAMP_DB` 指向 `os.tmpdir`）；`src/web.js` 全文件 `fetch(`/`spawn(` **零匹配**；运行前后 `oamp/data` 不存在、`git status --porcelain --ignored` 空 |

**标准 1 判定：pass（8/8）**

### 2.2 标准 2：「文件范围」合规

命令与输出（原样）：

```
$ git -C .pb-agents/worktrees/pr-004 diff --name-only 0d726f2...HEAD
oamp/test/project-workspace.test.js

$ git -C .pb-agents/worktrees/pr-004 diff --name-status 0d726f2...HEAD
A	oamp/test/project-workspace.test.js
```

逐项核对：

| 核对对象 | 期望 | 实测 | 判定 |
|---|---|---|---|
| PR「文件范围」唯一新文件 | 仅 `oamp/test/project-workspace.test.js` | diff 恰为该一个 `A` | pass |
| 既有测试文件（基线内 `oamp/test/**/*.test.js`） | 零改动 | 基线 `git ls-tree` 实测 **22** 个 `.test.js`；diff 中零出现 | pass |
| `oamp/src/**` | 零改动 | diff 零出现 | pass |
| `oamp/web/**` | 零改动 | diff 零出现 | pass |
| `oamp/API.md` / `oamp/llms.txt` / `oamp/README.md` / `oamp/package.json` | 零改动 | diff 零出现（对上述路径显式限定重跑 diff，输出仍只有那 1 个 `A`） | pass |
| worktree 工作区 | 无未提交改动 | `git status --porcelain` 空；`--ignored` 亦空（无 `.runtime/`、无 `data/`、无 `node_modules/`） | pass |

**标准 2 判定：pass**

### 2.3 标准 3：功能卡产品维度逐条（F01~F08、F10，共 53 条）

> 判定口径（本报告统一）：**pass** = 该条在本 PR 声明的判定载体（architecture §8.1 的断言落点 / PR 验收标准）上有可定位且可证伪的证据，且本验证者复核通过；**partial** = 某一必需子维度缺证据或证据强度不足；**fail** = 有证据表明不符合；**blocked** = 无法判定。
> 「证据层」列注明该条证据的类型：`静态契约`（读源码/文本断言）、`API`（真实进程 HTTP + sqlite 直读）、`运行期`（本验证者用真实浏览器在真实 `oamp web start` 上执行）。

#### F01 项目实体（7 条）

| # | 条文 | 判定 | 证据（层） |
|---|---|---|---|
| 1 | 只填地址即可创建；名称留空 ⇒ 展示名 = 地址尾段去 `.git` | pass | `test:314-340`：`repo_url=https://github.com/acme/demo.git` → `name='demo'`（API） |
| 2 | 项目可被列出；创建的在、未创建的不在 | pass | `test:341-355` 往返；`test:379` 两次创建后 `projects.length===2`（API） |
| 3 | 最小四要素可核对、本地路径不在其中 | pass | `test:359-364`：`pragma_table_info('projects')` 恰 `[project_id,name,repo_url,created_at]` + 行值逐字（API） |
| 4 | 地址必填；**不做形态/域名校验** | pass | `test:396-405`：缺 / `''` / 空白 / 非字符串 → 400 `需要 repo_url（非空字符串）` 且条数不变；`test:381-384`：`not-a-url` 与 `git@example.internal:x.git` 均 200（API） |
| 5 | 重复地址 → 409 `CONFLICT` 且条数不变 | pass | `test:407-413`（含 trim 后原样入库的正例）（API） |
| 6 | 不归一化：尾 `.git` 与无 `.git` 是两个项目 | pass | `test:374-379` + `test:388-392` 库内值与入参排序后逐字相等（API） |
| 7 | 不校验可达性、创建过程无网络/凭据 | pass | `test:381-384`（可疑地址成功）+ `src/web.js` 全文件无 `fetch`/`spawn`（结构性）（API + 静态） |

#### F02 新建对话必须归属项目（5 条）

| # | 条文 | 判定 | 证据（层） |
|---|---|---|---|
| 1 | 带项目归属即可新建、出现在该项目列表 | pass | `test:527-545`（落库归属 = 请求的 `project_id`；A 列表含、B 列表空）（API） |
| 2 | 缺归属的既有新建路径 → 400 且不产生任何对话 | pass | `test:483-505`（缺 / `''` / 数字 → 400 文案逐字）+ `test:503` 双表计数不变 + `test:505` SSE 零帧（API） |
| 3 | 新建对话行归属必填且指向真实项目 | pass | `test:617-655`：`project_id` 列 `notnull=1`、`cid=1`、`pragma_foreign_key_list` 指向 `projects.project_id`、缺列 INSERT 被 NOT NULL 拒、未知 `project_id` 被外键拒（API） |
| 4 | 未知项目 → 400（不采用 404） | pass | `test:498-500`：`400 / INVALID_PARAM / 项目不存在: prj-does-not-exist`（API） |
| 5 | 工作台内新建自动归属（无项目选择步骤） | pass | 后端归属强制（上表 1/3）+ 前端调用点 `send()` 内 `project_id: state.projectId`（`test:874`，静态契约）；本验证者运行期确认工作台按 `?project=` 取列表（§2.4.5）；**运行期"点击发送"未执行**（需在线 agent，见偏差 D8） |

#### F03 对话列表以项目为范围（5 条）

| # | 条文 | 判定 | 证据（层） |
|---|---|---|---|
| 1 | 列表以项目为范围，互不包含对方 | pass | `test:678-689`（HTTP 集合与 sqlite 直读集合逐字相等）（API） |
| 2 | 缺参 / **空值** 都 → 400，不返回跨项目全量 | pass | `test:668-672`：`/api/chats` 与 `/api/chats?project_id=` 均 400 且文案逐字（API） |
| 3 | 切换项目即切换列表内容 | pass | 机制：整页导航 + `state.projectId` 取参（静态契约）；本验证者运行期复核 A 侧与返回列表页（列表页零 `/api/chats` 请求）（运行期）；B 侧未逐次复核（偏差 D9） |
| 4 | 既有 8 类过滤 / 分页在项目范围内语义不变 | pass | `test:717-781`：`q`（标题/正文/`%` 转义）、`agent`、四态 `state` 与库内对拍、`from/to` 闭区间与 `from>to` 400、`archived` 混合态、`limit/offset` 非法值 400 且归因不串（API） |
| 5 | 跨项目不混列在数据层可核对 | pass | `test:685-689` + `test:536`（sqlite 直读）（API） |

#### F04 项目层入口（6 条）

| # | 条文 | 判定 | 证据（层） |
|---|---|---|---|
| 1 | 首屏即项目列表 | pass | `test:806-840`（九节点、`#projects-view` 先于 `main.layout`）；运行期：`#projects-view` `display:block`、`main.layout` `none`（运行期） |
| 2 | 未选项目时看不到对话、不能发消息 | pass | `test:884-888`（三条真隐藏规则）；运行期：`#input.checkVisibility() === false`、`offsetParent === null`、`main.layout` 不可见（运行期） |
| 3 | 首屏可新建项目（最小输入 = 地址，名称可留空） | pass | `test:892-903`（`#btn-create-project` 绑定 + 只填 repo 可提交）；运行期：经真实 UI 只填地址点击 → 200 且列表出现该项目（运行期） |
| 4 | 首屏可进入项目 | pass | `test:846-853`（`renderProjects` 内整页导航）；运行期：点击项目行 → `/?project=prj-…`，`main.layout` 可见（运行期） |
| 5 | 列表信息量四项（项目名/地址/对话数/最近活动时间），后两项派生 | pass | 后端派生列 sqlite 对拍（`test:450-462`：`chat_count` 含已归档/已关闭、`last_activity_at = MAX(updated_at)`、无对话为 `null`）；前端四项渲染断言（`test:890-895`）；运行期观察到 `ui-runtime | https://… | 0 个对话 | —`（`null → '—'`）（API + 静态 + 运行期） |
| 6 | 创建后原地出现、不跳进工作台 | pass | `test:899-901`（`await loadProjects()`、`doesNotMatch(/location/)`）；运行期：URL 停留 `/`、项目行原地出现、无手动刷新（运行期） |

#### F05 项目工作台（6 条）

| # | 条文 | 判定 | 证据（层） |
|---|---|---|---|
| 1 | 进入项目即工作台，沿用三栏布局 | pass | 运行期：`main.layout` `flex`、`#projects-view` `none`（运行期）；`test:822-840` 节点/结构契约（静态契约） |
| 2 | 顶栏显示当前项目（最小口径 = 项目名） | pass | 运行期 `#current-project-name.textContent === 'ui-runtime'`；`test:822-840`（静态契约 + 运行期） |
| 3 | 返回入口可回到首屏 | pass | `test:839`（`href="/"`）；运行期：点击 `#btn-back-projects` → URL `/`、项目列表可见、`main.layout` 隐藏（运行期） |
| 4 | 工作台内对话范围 = 当前项目 | pass | 运行期：工作台加载的唯一列表请求为 `/api/chats?project_id=prj-…`（运行期）；`test:866-869`（`loadChats`/`loadArchived` 带参，静态契约） |
| 5 | 工作台内新建自动归属（MI-07） | pass | `test:874`（`send()` 携带 `state.projectId`）+ 后端归属强制用例（见 F02-1/3）；运行期未执行点击发送（偏差 D8） |
| 6 | 既有对话能力照常可用（N13） | pass | 既有 `web.test.js` / `acp-daemon.test.js` 等 22 个文件**零改动**且全量 272/272 绿（回归锁由既有用例承载）；本 PR 对前端只加 3 处调用点（diff 证据） |

#### F06 派发载荷携带项目上下文（6 条）

| # | 条文 | 判定 | 证据（层） |
|---|---|---|---|
| 1 | 结构化字段（不含进用户正文） | pass | `test:918-927`：`payload.body.project` 键集合恰 `[agreement,name,repo_url]`；`prompt` 逐字等于原文；信封顶层零新增键（API） |
| 2 | 两条 LLM 路径都携带 | pass | `test:915-946`：`captureTask` 分别捕 `executor='omp-daemon'` 与 `'omp'` 两支载荷，均含三要素（API） |
| 3 | shell 路径不携带 | pass | `test:952-955`：`Object.keys(shellBody)` 恰 `[args,command,label]`，`command='/bin/sh'`（API） |
| 4 | 用户原文不被污染 | pass | `test:1020-1027`（`messages.text` sqlite 逐字）+ `test:1021`（HTTP 口径）（API） |
| 5 | 协议方法面仍 7 个 | pass | `test:965-978`（README 逐字列 7 项，无新增无删除）+ pr-004 对 `oamp/src/**` 零改动（diff 证据）；**断言为文档面锁**，见偏差 D3 |
| 6 | 上下文池键 / 串并并发语义不变 | pass | `src/context-pool.js` 与基线逐字一致（本 PR 零改动）+ 既有 `context-pool.test.js` 未改动且全绿（回归锁） |

#### F07 上下文内容与 agent 侧注入（7 条）

| # | 条文 | 判定 | 证据（层） |
|---|---|---|---|
| 1 | 内容三要素（项目名/仓库地址/相对工作约定） | pass | `test:1036-1039`（渲染块三行逐字）+ `test:920-926`（三要素逐字来自库内行）（静态 + API） |
| 2 | 无绝对路径 / 盘符 / 主机名 | pass | `test:1051-1053`（对 `AGREEMENT` 与约定行做 `^\//m`、`[A-Za-z]:\\`、`//` 三重否定）+ `test:941-944`（载荷不含仓库根路径）（静态） |
| 3 | 约定覆盖三件事（clone / 仓库根目录工作 / 产物写入该仓库） | pass | `test:1042-1047`（`clone`、`本地尚无该仓库`、`根目录` + `docs/PR/commit/分支/写入该仓库` 关键字逐条）（静态） |
| 4 | 由 agent 侧注入 | pass | `test:998-1013`：argv（一次性）与回显（常驻）均来自 agent 进程内渲染；`src/agent.js:57` `renderProjectContext` 为唯一渲染点，`web.js` 不产生注入文本（静态 + API） |
| 5 | 传达可核对（地址字符串出现即真） | pass | `test:1005`（末位 argv 逐字含块）、`test:1011-1013`（`out.text` 含 `repo_url`）、`test:924`（载荷含 `repo_url`）（API） |
| 6 | 产物落点约定随上下文下达 | pass | `test:1045-1047`（约定第 ③ 条关键字）（静态） |
| 7 | 不出现超能力承诺 | pass | `test:1056-1061`：对 `AGREEMENT` / 渲染块 / `index.html` / `app.js` 做 `/已 ?clone|已对齐目录|已进入项目根目录|系统会自动/` 否定（静态） |

#### F08 旧结构库启动即重建（6 条）

| # | 条文 | 判定 | 证据（层） |
|---|---|---|---|
| 1 | 旧结构可被识别 | pass | `test:1124-1128`（前置断言旧库 7 列无 `project_id`）→ 启动后列集变新 schema（API） |
| 2 | 启动即重建、服务可用且无报错 | pass | `test:1128-1132`（`stderr` 无 `Error/未捕获/unhandled`）+ 列集/表存在断言；本验证者以**真实 9 列旧库**独立复验（§2.4.1②）（API） |
| 3 | 旧对话不残留、无兜底归属 | pass | `test:1140-1143`：`chats`/`messages` 行数 0、`chat-old` 不存在、`projects` 行数 0（API） |
| 4 | 新结构含必填归属 | pass | `test:1146-1151`：`project_id` `cid=1` / `notnull=1` / FK 指向 `projects.project_id`（API） |
| 5 | 新结构不被重建；删库重启 = 新建空库可用 | pass | `test:1166-1204`：三次启动对拍（重建 → 不重建且数据保留 → 删库后不重建且可建项目/对话）（API） |
| 6 | 无兼容路径 | pass | `test:1133-1138`（表集合断言：无迁移表/备份表）+ `test:1162-1163`（源码无 `MIGRATIONS`/`migrate(`/`ALTER TABLE chats`）—— 文本级，见偏差 D4（API + 静态） |

#### F10 登记义务与工程锁（5 条）

| # | 条文 | 判定 | 证据（层） |
|---|---|---|---|
| 1 | 三面自动出现（文档页 / 调试台 / AI 索引） | pass | `test:1289-1317`：`/api/docs` 投影 13 条、`/docs` 与 `/debug` 均 200 且 `text/html`、两页源码去重后 `/api/` 字面量恰 `['/api/docs']`（API + 静态） |
| 2 | `API.md` 有路径行（双向覆盖） | pass | `test:1270-1282`：签名 + `### 3.12/3.13` + `| 12 |/| 13 |` 在场，且"文档签名 ⊆ 登记集合"反向集合为空（静态） |
| 3 | 三条漂移锁全绿 | pass | `test:1227-1268`：13 条签名逐位、8 个元数据字段必填、`llms.txt` 与 `renderLlmsTxt(projectRoutes(createApiRoutes({})))` **字节相等**（`Buffer.compare === 0`）（静态） |
| 4 | 零依赖锁保持 | pass | `test:1285-1286`：`package.json` 的 `dependencies === {}`（静态） |
| 5 | 新增登记不改变既有接口行为 | pass | 既有 11 条签名与顺序逐位断言（`test:1229-1231`）+ 既有 `api-routes.test.js` / `web.test.js` 零改动且全绿（回归锁） |

**标准 3 判定：pass（53/53）**

### 2.4 标准 4：断言强度与真实性（不得只看「绿」）

#### 2.4.1 三处易假绿点

**① 未知 `project_id` 的 INSERT 外键拒收是否在打开 `PRAGMA foreign_keys` 的直连句柄上验证 —— 是（pass）**

- 证据：`test:284-287` `openRaw()` 显式 `db.exec('PRAGMA foreign_keys = ON')`；用例 `test:609-659` 用该句柄执行两条 INSERT 并 `assert.throws(..., /constraint failed/)`，随后 `countOf(...'SELECT COUNT(*) FROM chats') === 1` 佐证"两次被拒不落行"。
- 可证伪性（本验证者变异实验 **M1**）：在 `/tmp/pr004-mutant/oamp` 副本中把 `src/persist.js` 的 `project_id TEXT NOT NULL REFERENCES projects(project_id)` 改为无 `REFERENCES` → `node --test` 该文件相关用例 **2 红 / 0 绿**，报错 `应存在指向 projects.project_id 的引用：[]`。⇒ 该断言不是"句柄上随便跑跑就绿"的空断言。

**② `DB_REBUILT path=<dbPath>` 是否真的从 stdout 观测 —— 是（pass）**

- 证据：`src/persist.js:191` `process.stdout.write(\`DB_REBUILT path=${dbPath}\n\`)`；`test:129-155` 分别收集 `child.stdout` / `child.stderr` 并暴露 `stdout()` / `stderr()`；`test:1158` 断言 `web.stdout().includes(\`DB_REBUILT path=${dbPath}\n\`)`（**path 逐字 + 末尾换行**），`test:1182/1196` 反向断言"不重建时 stdout 无该行"。
- 可证伪性（变异 **M3**）：把该行改到 `process.stderr.write` → `F08：旧结构库…` 用例 **红**（1 红 / 0 绿）。
- 独立复验：本验证者以**真实 9 列旧库**启动真实 `oamp web start`，进程 stdout 捕获到 `DB_REBUILT path=/tmp/pr004-ui/sql.db`（见 ①下方复验块）。

**③ 一次性路径末位 argv 逐字是否 = 「块 + `\n\n` + 原文」—— 是（pass）**

- 证据：`test:1003-1005`：`assert.equal(oneShots[0][oneShots[0].length - 1], \`${rendered}\n\n${oneShotText}\`)` —— **严格相等**，非子串；`rendered` 由 `test:302-304` 按 agent 侧同模板逐字拼接；`assert.equal(oneShots.length, 1)` 保证样本唯一（不会用多次派发的某一次掩盖）。
- 可证伪性（变异 **M2**）：把 `src/agent.js` 的 `` `${projectContext}\n\n${task.prompt}` `` 多加一个 `\n` → `F07：一次性路径…` 用例 **红**（严格相等失败，diff 直接打出内容）。一个字符的偏差即红。

**本验证者独立复验（真实 9 列旧库 → 重建）**：

```
输入：/tmp/pr004-ui/sql.db（按 8ed6901 的真实旧 schema 造：chats 9 列含 archived_at/context_released，1 条 chat + 1 条 message）
web 进程 stdout：DB_REBUILT path=/tmp/pr004-ui/sql.db
重建库直读：cols= 0:chat_id,1:project_id(NN),2:title(NN),3:agent_id,4:state(NN),5:created_at(NN),
                 6:updated_at(NN),7:closed_at,8:archived_at,9:context_released(NN)
            tables= chats,messages,projects,sqlite_sequence
            rows: chats=0 messages=0 projects=0
            fk=[{table:"projects",from:"project_id",to:"project_id"}]
```

#### 2.4.2 正反双向判据（抽样）

| 断言面 | 正例判据 | 负例判据（反向） |
|---|---|---|
| F01 派生名 | `demo.git → demo`、非字符串/空白/省略三形态同派生 | 空派生 ⇒ 兜底地址原文 `.git`；400/409 各带"条数不变" |
| F01 不归一化 | 两种地址形态均 200 且库内逐字 | 库内排序集合与入参逐字比较（归一化即不等） |
| F03 范围 | A 列表 = A 的 sqlite 行 | B 列表为空；`total` 与集合同源（分页两页不重复） |
| F07 注入 | 首轮 argv / 回显含整块 | 次轮 `assert.equal(messages[3].text, '收到：'+次轮原文)` + `doesNotMatch(/【项目上下文】/)` |
| F08 重建 | 旧库 → 新列集 + 行数 0 | 新结构二次启动 `stdout` 无 `DB_REBUILT`；删库启动不重建 |
| F10 登记 | 13 条签名 / 快照字节相等 / 元数据必填 | API.md 文档签名 ⊆ 登记集合（反向集合必须为空） |

#### 2.4.3 变异实验汇总（在 `/tmp/pr004-mutant` 副本执行，**未触碰被验证 worktree**）

| 变异 | 改动 | 期望 | 实测 |
|---|---|---|---|
| M1 | `persist.js` 去掉 `REFERENCES projects(project_id)` | FK 断言红 | 2 tests / 0 pass / **2 fail**（`应存在指向 projects.project_id 的引用：[]`） |
| M2 | `agent.js` 一次性 prompt 加一个 `\n` | argv 逐字断言红 | 1 tests / 0 pass / **1 fail** |
| M3 | `persist.js` `DB_REBUILT` 改投 stderr | stdout 断言红 | 1 tests / 0 pass / **1 fail** |
| M4 | `context-pool.js` 去掉 `sentTurns === 0`（每轮都注入） | 次轮不注入断言红 | 1 tests / 0 pass / **1 fail** |
| M5 | `web.js` 一次性分支去掉 `project` | 承载断言红 | 1 tests / 0 pass / **1 fail** |
| M6 | `web.js` 常量改名 `PROJECT_AGREEMENT → PROJECT_AGREEMENT_RENAMED` | 常量抽取失败即红 | 文件级 **1 fail**（模块加载期 `assert.ok(found)` 抛错） |

> 复现路径保留在 `/tmp/pr004-mutant/oamp`（每次变异前从 worktree 拷回原文件）。

#### 2.4.4 抽取与判定工具的保真性

- `functionBody(src, name)`（`test:306-310`）用于静态契约断言。本验证者用同款正则对 `app.js` 抽取 9 个被断言函数（`loadProjects` / `renderProjects` / `createProject` / `resolveCurrentProject` / `showProjectList` / `showWorkspace` / `loadChats` / `loadArchived` / `send`）逐一比对源码：**均为完整函数体，无截断**（例如 `send` 抽出 46 行 / 1666 字符，与 `web/app.js` 源文一致）。⇒ `doesNotMatch` 类断言不是"因截断而恒真"。
- `readProjectAgreement()`（`test:294-298`）：正则抽取 + `assert.ok(found)`；M6 证明抽取失败会红。
- `AGREEMENT` 只从 `src/web.js` 抽一份（不在测试里复制第二份文本），渲染期望块与 `src/agent.js:57` 同模板 —— 真源单一，无"两边同时写错"风险。

#### 2.4.5 前端断言的真实性（本验证者补充的运行期复核）

本 PR 明示前端断言是「文本级静态契约」，不做运行期 DOM 断言。为判断该静态契约是否真的对应可用界面，本验证者在真实 `oamp web start`（临时 `OAMP_DB`）上驱动真实浏览器：

| 观察 | 结果 |
|---|---|
| 首屏 `#projects-view` / `main.layout` / `#project-bar` | `block` / `none` / `none`；`#input.checkVisibility() === false`、`offsetParent === null` |
| 首屏网络请求 | 仅 `/api/agents`、`/api/projects`（**无 `/api/chats`**，与 `test:857-859` 的静态断言同向） |
| 经 UI 只填地址创建 | 成功；URL 停留 `/`；项目行原地出现：`ui-runtime | https://example.com/ui-runtime.git | 0 个对话 | —` |
| 点击项目行 | 整页导航到 `/?project=prj-4311…`；`main.layout` `flex`、`#projects-view` `none`、`#project-bar` `flex`、`#current-project-name` = `ui-runtime` |
| 工作台列表请求 | `/api/chats?project_id=prj-4311…`（**唯一列表请求**） |
| 点击 `← 项目列表` | 回到 `/`；`#projects-view` `block`、`main.layout` `none`；走 `/api/projects`，未请求 `/api/chats` |
| `?project=<未知 id>` | 回落项目列表视图（`main.layout` 隐藏），未渲染空工作台、未报错 |

**标准 4 判定：pass**（另见偏差 D1~D4、D6、D7 的强度备注）

### 2.5 标准 5：实际执行证据

在 `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/pr-004/oamp` 下执行，原样计数：

| 运行 | 命令 | tests | pass | fail | skipped | todo | 耗时 |
|---|---|---|---|---|---|---|---|
| (a) 单文件 | `node --test --test-concurrency=1 test/project-workspace.test.js` | 21 | **21** | **0** | 0 | 0 | 12.9s |
| (b) 全量 第 1 次 | `npm test`（= `node --test test/*.test.js`） | 272 | **271** | **1** | 0 | 0 | 56.8s |
| (b) 全量 第 2 次（安静环境） | `npm test` | 272 | **272** | **0** | 0 | 0 | 57.2s |

第 1 次的唯一失败项（**既有用例，非本 PR 文件**）：

```
not ok 195 - F02-8/D16：Router SIGINT 干净退出 0、不悬挂、socket 文件被删
location: test/router-registry.test.js:135
error: Expected values to be strictly equal: null !== 0   （exit.code，duration_ms 53.4）
```

追证：

- 该用例单独连跑 5 次 → 每次 `8 pass / 0 fail`（全绿）；
- 第 1 次运行期间，本验证者**正在并发跑变异实验**（6 个额外的 `node --test` 进程，各自拉起 Router/agent 子进程）——第 2 次在无外部负载时全量重跑全绿；
- 该文件在本 PR 零改动（diff 证据），失败形态是"`ROUTER_READY` 返回后立即 SIGINT，子进程尚未装好信号处理器 ⇒ 被信号默认动作终止（`code=null`）"，属 harness 的时序窗口被负载放大。

**标准 5 判定：pass**（如实记录两轮计数；归因见偏差 D5）

### 2.6 标准 6：测试隔离

| 核对项 | 证据 | 判定 |
|---|---|---|
| 零外网 | 新文件内 `fetch` 目标仅 `http://127.0.0.1:<port>`（`test:145`）；`github.com` / `example.com` 仅作**数据**；被测源码 `src/web.js` 全文件 `fetch(`/`spawn(` 零匹配 | pass |
| 零真实 omp | `test:225` 对每个 agent 显式注入 `OAMP_OMP_BIN: FAKE_BIN`（本地 `fake-acp.cjs` 桩，`test:35-107`）；不需要 agent 的用例 `withAgent:false` 根本不拉起 agent；web 进程自身不 spawn omp | pass |
| 零真实 tmux | 全文件无 `tmux` 相关引用 | pass |
| `OAMP_DB` 一律临时目录 | `test:231-238`（`mkdtemp(os.tmpdir(),'oamp-project-ws-')`）、`test:1110`（F08 临时库）；`FAKE_DIR`（`test:110-119`）、argv 日志（`test:984-986`）同在 `os.tmpdir` 且 teardown 清理 | pass |
| 未触碰 `oamp/data/sql.db` | worktree 内 `oamp/data` **不存在**（验证前后一致）；`git status --porcelain --ignored` 空（无 `.runtime/`、无遗留临时文件） | pass |
| 端口 | `pickPort()` 随机 41000–42999，`startWeb` 显式 `--port`，无固定端口常量冲突 | pass |
| 断言不被吞 | 全文 `try/catch` 扫描：catch 仅出现于 fake 桩的 `JSON.parse`、SSE 坏帧解析、退出清理；所有 DB 用途为 `try/finally`（无 catch） | pass |

**标准 6 判定：pass**

---

## 3. 汇总

| 验证标准 | 判定 | 计数 |
|---|---|---|
| 1. PR 验收标准逐条 | **pass** | 8/8 pass |
| 2. 文件范围合规 | **pass** | 唯一新文件；22 个既有测试文件 + `src/**` + `web/**` + 4 个文档/清单文件零改动 |
| 3. 功能卡产品维度逐条 | **pass** | 53/53 pass（F01 7、F02 5、F03 5、F04 6、F05 6、F06 6、F07 7、F08 6、F10 5） |
| 4. 断言强度与真实性 | **pass** | 3 处易假绿点全部实证；6 个变异均如期变红；skip/todo 0；无"未生效"断言 |
| 5. 实际执行证据 | **pass** | 单文件 21/21；全量安静环境 272/272；另记录一轮受外部负载干扰的 271/272 |
| 6. 测试隔离 | **pass** | 零外网 / 零真实 omp / 零真实 tmux / 未触碰仓库库文件 |

**fail 0 条 / partial 0 条 / blocked 0 条；偏差记录 9 条（均不改变 PASS 判定）。**

---

## 4. 偏差记录

| # | 位置（可定位） | 现象 | 影响判定 | 备注 |
|---|---|---|---|---|
| D1 | `test:1079-1106` `writeLegacyDb()` | 旧库 fixture 造的是 **7 列** chats（缺 `archived_at` / `context_released`），与真实前版 schema（`8ed6901`，**9 列**）不一致 | 否（覆盖窄化，非缺陷；本验证者以真实 9 列旧库独立复验：重建成功、列集正确、行数 0/0、FK 在场） | 建议按 D 类候选 N2 加固 |
| D2 | `test:984` 用例标题「一次性路径**每次**注入」，`test:1003-1005` 仅 1 次一次性派发样本 | 标题强于证据：只证明了"首次派发注入"，未证明"第二次派发仍注入" | 否（实现侧 `agent.js:198` 为无状态三元表达式，结构上必然每次注入；PRD 无"每次"N 次验收条文） | 见候选 N5 |
| D3 | `test:965-978` | 「协议方法面仍 7 个」读的是 **README 文本**，不是协议实现表 ⇒ 文档与实现漂移时该断言仍绿 | 否（实现面由既有 `delivery-contract.test.js` 闭环覆盖且本 PR 未改 `src/**`） | 见候选 N4 |
| D4 | `test:1162-1163` | F08「无兼容路径」用源码正则 `MIGRATIONS\|migrate(`/`ALTER TABLE chats` ⇒ 改名/别名化即绕过 | 否（同页另有"表集合恰 `chats/messages/projects/sqlite_sequence`"的行为级断言兜底） | 见候选 N6 |
| D5 | `test/router-registry.test.js:135`（既有文件，本 PR 零改动） | 全量首轮在外部并发负载下偶发红：`exit.code` `null !== 0`（53ms）；安静重跑 272/272 绿、单文件连跑 5×8/8 绿 | 否（环境负载时序抖动，非本 PR 引入；如实记录两轮计数） | 见候选 N3 |
| D6 | `test:876-878` | `assert.doesNotMatch(appJs, /POLL_MS/)` 只锁常量名 ⇒ 改名实现的轮询不会被发现 | 否（与架构声明的"既有前端静态契约"同口径，非本 PR 新增风险） | 见候选 N6 |
| D7 | `test:464` | `assert.ok(first.chatId, '第一条对话仍在库内…')` 近乎恒真（UUID 非空即过），是装饰性断言 | 否（同用例其余断言为 sqlite 对拍） | 见候选 N6 |
| D8 | `test:806-908`（F04/F05 断言面） | 本 PR 内前端仅有静态契约断言；本验证者已用浏览器复核首屏/创建/进入/返回/按项目取列表/未知项目回落，但"工作台内实际点击发送"未运行期执行（需在线 agent） | 否（PR 明示判定载体为静态契约；后端归属强制由 API 用例覆盖） | 见候选 N1 |
| D9 | `test:662-710`（F03-3「三次切换」） | 运行期仅复核了 A 侧与"返回项目列表页"，A→B 的第二次切换未逐次复核（机制同构：整页导航 + `state.projectId` 取参） | 否 | — |

---

## 5. 下一迭代候选

1. **N1 · F04/F05 运行期回归缺口**：本 PR 对前端只提供文本级静态契约，界面行为在回归中无自动化保护。建议为「首屏视图互斥 / 创建原地出现 / 进入与返回 / 按项目取列表 / 未知项目回落」建立最小 E2E（可复用本次的浏览器脚本形态）。
2. **N2 · 旧库 fixture 保真**：用 `8ed6901` 的真实 9 列旧 schema 作为主 fixture（可保留 7 列作为第二形态），让 F08 覆盖真实升级路径。
3. **N3 · harness SIGINT 竞态**：`startRouter().stop()` 在 `ROUTER_READY` 后立即发 SIGINT，存在"信号先于处理器安装"窗口；建议 `stop()` 对 `{code:null, signal:'SIGINT'}` 显式诊断/重试，消除负载下的偶发红。
4. **N4 · 方法面锁的强度**：把"协议方法面 7 个"从 README 文本锁升级为对 router 方法表/未知方法的实现级断言。
5. **N5 · 一次性路径「每次注入」**：补第二个一次性派发样本（或断言 `--no-session` 无状态路径下每轮都带块）。
6. **N6 · 弱断言清理**：`POLL_MS` 文本锁、`MIGRATIONS` 文本锁、`assert.ok(chatId)` 三处改为行为级判据。

---

## 6. 结论

# PASS

判定依据：

- PR「验收标准」**8/8 pass**；「文件范围」精确为唯一新文件 `oamp/test/project-workspace.test.js`（22 个既有测试文件、`oamp/src/**`、`oamp/web/**`、`API.md`、`llms.txt`、`README.md`、`package.json` 全部零改动）；
- F01~F08 与 F10 的产品维度 **53/53 pass**；
- 三处易假绿点均**实证为真**（含 6 个变异实验全部如期变红），无用例级 `skip`/`todo`，无"断言实际未生效"的情形；
- 实际执行：单文件 **21/21**、安静环境全量 **272/272**（另一轮受外部并发负载干扰的 271/272 已如实记录并归因）；
- 隔离面：零外网、零真实 omp、零真实 tmux、未触碰仓库内 `oamp/data/sql.db`。

9 条偏差记录（D1~D9）均为**覆盖窄化或断言强度**类备注，不影响本 PR 的判定；其中 D5（既有用例的负载时序抖动）与 D1/D2/D3/D4/D6 已登记为下一迭代候选。
