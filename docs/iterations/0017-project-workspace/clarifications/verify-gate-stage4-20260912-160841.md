# 独立验证报告 — Gate：阶段 4 → 5 入口（0017-project-workspace / PR 规划产物）

**验证者身份（反射结果）**：PR 规划质量审查者（能识别循环依赖与粒度问题的冲刺计划审查者），叠加**代码级依赖审计视角**——因为验证标准第 5 条要求依赖证据必须落到代码库的真实耦合（共享符号 / 接口 / 文件），文档自述不作为采信依据。

**产出物**（只读，未修改）：

- `docs/iterations/0017-project-workspace/prs/pr-001-project-context-injection.md`
- `docs/iterations/0017-project-workspace/prs/pr-002-project-data-and-http.md`
- `docs/iterations/0017-project-workspace/prs/pr-003-project-layer-ui.md`
- `docs/iterations/0017-project-workspace/prs/pr-004-project-workspace-acceptance.md`

**验证标准来源**：主 agent 委托内联的 6 条标准（七字段格式规范 / 功能点全覆盖 / 文件范围无重叠 / 依赖图无环 / 依赖正确性 / PR 粒度判断框架）。

**验证日期**：2026-09-12

**独立性声明**：

- 未读取 `history.md`、`clarifications/` 下任何既有文件、pr-planner 的报告或任何执行过程上下文。
- 除 4 份待验证产物的引用对象外，只额外读取了两份**规范文本**用于解释标准口径：`.pb-agents/roles/pr-planner/pr-planner.md`（「PR 文件格式规范」与「PR 粒度判断锚点」段落）、`.pb-agents/roles/workflow-pb/workflow-pb.md`（阶段 4/6 的完成定义行）。这是**标准的来源**而非产出过程的上下文。
- 交叉核对所用材料：`docs/iterations/0017-project-workspace/prd/F01~F10*.md`、`architecture.md`、`oamp/**`（只读）。`deferred-demand-changes.md` 经核实不存在，未摘录。
- 未执行任何 git 写命令；未修改任何被验证产物；未触碰 `oamp/**`。

**验证边界（未覆盖的面）**：本报告只对上述 6 条内联标准逐项判定。`architecture.md` 与 `prd/*.md` 的正确性未被评估，仅在 PR 文件引用它们时作为对照物；阶段 4 之前的产物质量不在本次范围内。

---

## 逐项判定

### 标准 1：七字段格式规范 —— **partial**

**子项 A｜七字段齐备且非空：pass（4/4）**

以 `^## ` 标题抽取实测，4 份文件各含且仅含 7 个字段，顺序与规范一致（上下文摘要 / 涉及功能点 / 文件范围 / 验收标准 / 参考资料 / depends_on / batch），无空缺字段、无空值字段。

| 文件 | `##` 字段序列 | 空字段 |
|---|---|---|
| pr-001 | 7 个，顺序相符 | 无 |
| pr-002 | 7 个，顺序相符 | 无 |
| pr-003 | 7 个，顺序相符 | 无 |
| pr-004 | 7 个，顺序相符 | 无 |

补充观察：pr-002 / pr-003 / pr-004 在 `batch` 字段之后各有一段无标题的补充说明（"备注 / 说明"）。它们不构成第 8 个 `##` 字段，规范未禁止字段外补充说明，不计为格式违规；其中 pr-002 的备注内容经代码核实成立（见偏差 B-1）。

**子项 B｜上下文摘要 ≤200 字：blocked（无法判定——规格未定义"字"的计量口径）**

实测 4 份摘要的字数（`## 上下文摘要` 与下一 `##` 之间的正文）：

| 文件 | 非空白字符数 | 汉字数 | 汉字+中文标点 |
|---|---|---|---|
| pr-001 | 221 | 115 | 130 |
| pr-002 | 270 | 79 | 93 |
| pr-003 | 264 | 81 | 96 |
| pr-004 | 216 | 85 | 98 |

`pr-planner.md` 的字段规范原文只写「[该 PR 的目标、背景和关键约束，200 字以内]」，未定义计量口径。两种最常用口径给出**相反结论**：非空白字符口径下 4/4 超限（超 8%~35%）；汉字口径下 4/4 合规。缺"口径定义"这一项信息才能判定，故本子项判 **blocked**，不猜测口径（对应偏差 B-7）。

**子项 C｜验收标准 ≥1 条：pass（4/4）**

| 文件 | 验收标准条数 |
|---|---|
| pr-001 | 7 |
| pr-002 | 10 |
| pr-003 | 7 |
| pr-004 | 8 |

均为可核对断言而非愿景式描述（例：pr-002 第 1 条给出 DDL 逐列形态；pr-004 第 2 条给出 `400` 文案原文与"项目条数不变"的判定面）。

**子项 D｜验收标准"可独立判断，不依赖其他 PR 完成才能判断"：2 pass + 2 partial**

| 文件 | 判定 | 逐条拆分 |
|---|---|---|
| pr-001 | pass | 7/7 条可仅凭 pr-001 判定：一次性路径经 `FAKE_ACP_ARGS_LOG` 的 argv 末位观测（`web.test.js:554`、`acp-daemon.test.js:36` 同款体例）、常驻路径经 fake ACP 回显观测、既有 22 个测试文件零改写。载荷由测试直投，不依赖投递端。 |
| pr-002 | partial | 9/10 条可独立判定（schema DDL、两条新路由、`/api/chats` 与 `/api/messages` 校验、openDb 重建、派发载荷装配、F10 三面与三锁、diff 形态核对）；**第 10 条的"全绿"部分不可**——`其余 18 个测试文件零改写且全绿` 中"零改写"可判（实测 18 个文件对变更面零命中，见标准 5 的反向核实），"全绿"须 pr-001 在场（该 PR 的 4 个测试文件含被 pr-001 改写的派发观测断言）。 |
| pr-003 | pass | 7/7 条均为**静态契约**判定（`index.html` 的新增静态节点、`app.js` 的 boot 分派与三处带参调用点、`style.css` 三条真隐藏规则），全部落在本 PR 自己的文件范围内；`web.js` 忽略未知 `project_id` 参数（`web.js:344-345` 的查询参数处理现状）⇒ 单独合入不产生测试红。 |
| pr-004 | partial | 仅第 1 条**部分**可独立判定（"新文件存在"与负向约束"不修改任何既有测试文件与任何 `oamp/src/**`、`oamp/web/**`、文档文件"可由 diff 判定；但同条的"`npm test` 全绿"不可）；第 2~8 条（F01~F10 的行为断言）均须 pr-001/002/003 的产物在场。此为**下游验收测试 PR 的固有形态**，其依赖已在 `depends_on` 逐条如实声明（见标准 5），故记为 partial 而非缺陷。 |

**标准 1 结论**：partial——子项 A、C 通过；子项 B 无法判定（口径缺失）；子项 D 中 2 份 pass、2 份 partial（拆分如上，无条目判 fail）。

---

### 标准 2：功能点全覆盖 —— **pass**

`prd/` 目录实测 10 个文件，文件内出现的 F-ID 全集 = {F01…F10}，无 F11 及以上的散落 ID。逐一核对 4 份 PR 的「涉及功能点」字段（只取该字段内文本）：

| F-ID | 被引用方 | 覆盖 |
|---|---|---|
| F01 | pr-002、pr-004 | ✅ |
| F02 | pr-002、pr-003、pr-004 | ✅ |
| F03 | pr-002、pr-003、pr-004 | ✅ |
| F04 | pr-003、pr-004 | ✅ |
| F05 | pr-003、pr-004 | ✅ |
| F06 | pr-001、pr-002、pr-004 | ✅ |
| F07 | pr-001、pr-004 | ✅ |
| F08 | pr-002、pr-004 | ✅ |
| F09 | pr-002 | ✅ |
| F10 | pr-002、pr-004 | ✅ |

**未被覆盖的 F-ID：无（∅）**。

补充核实（覆盖语义而非仅字段存在）：F09 的判定载体是 pr-002 的"既有 4 个测试文件 diff 形态"（pr-004 的"说明"段落已交代为何不落新文件），与 F10 的"三面 / 三锁"在 pr-002 第 8 条验收标准中均有对应条目，未出现"字段引用了但无判定载体"的挂空引用。

---

### 标准 3：文件范围无重叠 —— **pass**

4 份 PR 的「文件范围」共 15 条路径（pr-001: 2；pr-002: 9；pr-003: 3；pr-004: 1），两两交集实测：

| 交集 | 结果 |
|---|---|
| pr-001 ∩ pr-002 | ∅ |
| pr-001 ∩ pr-003 | ∅ |
| pr-001 ∩ pr-004 | ∅ |
| pr-002 ∩ pr-003 | ∅ |
| pr-002 ∩ pr-004 | ∅ |
| pr-003 ∩ pr-004 | ∅ |

集合明细（均无重复归属）：

- pr-001：`oamp/src/agent.js`（修改）、`oamp/src/context-pool.js`（修改）
- pr-002：`oamp/src/persist.js`、`oamp/src/web.js`、`oamp/API.md`、`oamp/llms.txt`、`oamp/README.md`、`oamp/test/persist.test.js`、`oamp/test/web.test.js`、`oamp/test/api-routes.test.js`、`oamp/test/acp-daemon.test.js`
- pr-003：`oamp/web/index.html`、`oamp/web/app.js`、`oamp/web/style.css`
- pr-004：`oamp/test/project-workspace.test.js`（新建，实测仓库内不存在，符合"新建"声明）

补充核实（声明之外的隐含改动面）：pr-002 的 4 个测试文件 + pr-004 的 1 个新文件 = 5 个测试文件，与"22 个既有测试文件"的关系自洽（pr-001 主张 22 个零改写、pr-002 主张 4 个机械变更 + 其余 18 个零改写；18 = 22 − 4）。反向实测：全 22 个测试文件中触及 `GET /api/chats` / `POST /api/messages` / persist API 的**恰好是那 4 个**（`web.test.js` 70/28 处、`api-routes.test.js` 38/3 处、`acp-daemon.test.js` 5/2 处、`persist.test.js` DB API 139 处），其余文件零命中（`api-pages.test.js` 与 `config-file.test.js` 仅出现 `OAMP_DB` 路径字符串，无 schema / CRUD 断言，对 pr-002 与 pr-003 的追加式改动安全）。未发现任何 PR 声明的文件范围之外还存在必然改动面。

---

### 标准 4：依赖图无环 —— **pass**

以 `depends_on` 为边：

| 边 | 目标 | 批次 |
|---|---|---|
| pr-002 → pr-001 | pr-002-project-data-and-http.md → pr-001-project-context-injection.md | 2 → 1 |
| pr-004 → pr-001 | pr-004-project-workspace-acceptance.md → pr-001 | 3 → 1 |
| pr-004 → pr-002 | pr-004 → pr-002 | 3 → 2 |
| pr-004 → pr-003 | pr-004 → pr-003 | 3 → 2 |

pr-001、pr-003 的 `depends_on` 为「（无）」（字段存在，空列表形态合规）。

- **环检测**：拓扑序 `pr-001 → {pr-002, pr-003} → pr-004` 存在，无环（4 条边全部指向早已存在的节点，DFS 无回边）。
- **批次一致性**：每条边的 `batch` 均严格递增（1 → 2 → 3），与依赖方向一致；无"批次逆序"的隐性冲突。记录：规范明示 `batch` 只是人工速览标记、**不是调度依据**，故此项仅作一致性记录，不参与判定。
- 未声明依赖的节点（pr-003）不引入边；其运行期互补关系见偏差 B-4。

---

### 标准 5：依赖正确性（代码级核实） —— **pass**

**核实方法**：对每条 `depends_on` 声明，用 `grep` 在 `oamp/**` 内逐条落点核对共享符号 / 接口 / 文件引用，并逐行核对 PR 文件引用的行号与内容；PR 文件内的文字理由不作为采信依据。

**D1｜pr-002 → pr-001：成立**

- 共享接口 `payload.body.project`（生产者 / 消费者）：
  - 生产者（pr-002 的文件范围）：`oamp/src/web.js:667`（`const payloadBody = …`，现状无 `project` 字段，pr-002 在此装配）✓ 行号与内容实测相符。
  - 消费者（pr-001 的文件范围）：`oamp/src/agent.js:46`（`parseTaskBody`，payload 解析入口）✓；现状无 `project` 分支，`agent.js:59` 体例为 `typeof body.label === 'string' ? … : null`，与 pr-001 声称的对照体例一致。
- 共享观测面（pr-002 的文件范围含 `oamp/test/web.test.js`，pr-001 的文件范围含产生该观测物的两个源文件）：
  - `oamp/test/web.test.js:443` `收到：web-contract-check` ✓、`:546` `收到：默认路径问题` ✓、`:549` `one-shot answer: 一次性问题` ✓、`:557` `a[a.length - 1] === '一次性问题'` ✓、`:943` `收到：无订阅者` ✓——5 处行号与内容逐字相符。
  - `oamp/test/acp-daemon.test.js:411` `one-shot answer: 请记住数字 7` ✓。
  - 产生侧：`oamp/src/agent.js:173` `args.push(task.prompt);` ✓（一次性路径 argv 末位，pr-001 的施加点）；`oamp/src/context-pool.js:112` `class ContextSession` ✓、`:144` `prompt(text, { model = null, … } = {})` ✓（pr-001 声称"多收 1 个可选参数"的现状签名）、`:165` `async _pump()` ✓。
  - 交叉核实：`grep sentTurns oamp/src/**` 零命中 ⇒ pr-001 声称新增的首轮计数器确为新增符号，非既有物；`web.test.js:554-557` 自读 `FAKE_ACP_ARGS_LOG` 生成的 argv 数组，与 pr-001 声称的观测面一致。
- 判定：依赖真实（共享接口 + 共享观测面），**pass**。其理由段落中含一处反向不成立的因果表述 → 偏差 B-3（依赖边本身不受影响）。

**D2｜pr-004 → pr-001：成立**

- pr-004 声称读取的产物：`agent.js` 一次性路径 argv 末位前缀、`context-pool.js` 的 `sentTurns === 0` 首轮分支——两者均为 pr-001 文件范围内的新增物（`sentTurns` 零命中已核实）。
- 观测面实测存在：`oamp/test/acp-daemon.test.js:26`（`观测面：FAKE_ACP_ARGS_LOG（启动参数集…）`）、`:36`（fake 脚本 `fs.appendFileSync(process.env.FAKE_ACP_ARGS_LOG, …)`）、`:118-120`（`FAKE_BIN` 写入 + `OAMP_OMP_BIN` 注入）、`:243-249`（`setup()` 起真实 Router + agent + web + 临时 `OAMP_DB`）；`oamp/test/helpers/harness.js` 实测导出 `startRouter` / `startAgent` / `buildEnv` / `waitFor` ✓（与 pr-004 参考资料逐字相符）。
- 判定：**pass**。

**D3｜pr-004 → pr-002：成立**

- 断言直接调用的产物与代码锚点实测：`oamp/src/web.js:313`（`createApiRoutes`）✓、`:314`（`const routes = [`，11 条表项）✓、`:344-345`（`GET /api/chats` 表项）✓、`:600-601`（`POST /api/messages` 表项）✓；`oamp/src/persist.js:12`（`SCHEMA`）✓、`:47`（`migrate`，pr-002 声明删除）✓、`:61`（`CHAT_COLUMNS`，9 列）✓、`:153`（`openDb`）✓、`:225`（`insertInput`）✓、`:280`（`listChats`）✓；快照字节面 `oamp/test/api-routes.test.js:32`（`LLMS_SNAPSHOT = …/llms.txt`）✓、`:33`（`API_MD`）✓，且 `oamp/llms.txt`、`oamp/API.md`（`## 3. 接口清单（11 条）`）、`oamp/scripts/gen-llms-txt.mjs` 均实测存在。
- 计数一致性交叉核实：`EXPECTED_SIGNATURES` 实测 11 条（`api-routes.test.js:36`）⇒ "11 → 13" 与"写接口 5 → 6"（`:468` 现断言 5 条）成立；`STATIC_FILES` 实测 12 个键（`web.js:261-274`）与 pr-003 声明相符；零依赖锁实测在 `oamp/test/hygiene.test.js:61-63`（`dependencies` 为空）。
- 判定：**pass**。

**D4｜pr-004 → pr-003：成立**

- 静态契约读取的节点与函数实测：`oamp/web/index.html:10`（`<header class="topbar">`）、`:12-13`（`.topnav` 与既有 nav 项）、`:26`（`<main class="layout">`）✓；`#projects-view` / `#project-bar` / `#project-repo` 等节点在前端文件中**零命中** ⇒ 确为 pr-003 新增；`oamp/web/app.js:574`（`loadChats`）、`:586`（`loadArchived`）、`:632`（`send`）、`:800`（`bind`）、`:881`（`boot` 块：`bind();` 起）、`:882`（`(async function init() {`）✓；`oamp/web/style.css:64/203/204/295` 实测为本仓 `.hidden` 的**逐组件**规则（无全局 `.hidden` 规则），与 pr-003 声称的现状与待补规则口径一致。
- 判定：**pass**。

**反向核实（防"依赖漏报"）**：除上述 4 条声明外，未发现该 PR 集内存在**未被声明但真实存在**的代码级耦合边（对 4 份产物的文件范围做全量交叉：唯一一处"产品级耦合但未入 depends_on"的是 pr-003 与 pr-002 的运行期互补，已记入偏差 B-4；pr-003 已在其字段外说明中披露，且不构成测试失败前提）。

---

### 标准 6：PR 粒度判断框架（三锚点 × 4 份） —— **partial**

| PR | 逻辑原子性 | 可审查性 | 独立性 | 依据 |
|---|---|---|---|---|
| pr-001 | pass | pass | pass | 2 文件 / 单一可描述动作"agent 侧项目上下文注入"；回滚只移除注入与计数器，不触碰键、队列、并发、淘汰语义（`context-pool.js:9` `QUEUE_LIMIT = 8`、`:144-154` 入队、`:165-186` 单飞 `_pump` 为本 PR 声明不动的既有语义）；AC 全部经既有观测面自证。 |
| pr-002 | pass | pass | **partial** | 9 文件 / 7 个 F-ID，但内聚性有代码级支撑而非"省事"：路由表新增与 `API.md`、`llms.txt`、`EXPECTED_SIGNATURES` 必须同 PR 落地——否则漂移锁②③与元数据锁必红（`api-routes.test.js:185`、`:268`、`:293` 三条锁实测在场）；`chats.project_id NOT NULL` 与删除 `persist.js:42-53` 的 `MIGRATIONS`/`migrate()`（补列路径无法承载 NOT NULL FK 列）同样不可分。可审查性：reviewer 只需一个心智模型"项目维度下沉到持久层与 HTTP 层"，diff 形态被 §8.3 四类形态约束。独立性 partial 的拆分见标准 1 子项 D。 |
| pr-003 | pass | pass | pass | 3 个前端文件 / 单一动作"项目层视图"；零新增静态资产（`web.js:261-274` 的 12 键白名单不变）；既有前端断言为文本级静态契约（`web.test.js:1030-1031` `doesNotMatch(appJs, /POLL_MS/)` / `/setTimeout\(tick/` 实测在场），单独合入无红。 |
| pr-004 | pass | pass | **partial** | 单文件新建 / 单一动作"为新能力建独立断言面"，不动任何既有文件；8 条 AC 中 7 条须上游三 PR 产物在场才可判定（依赖已在 `depends_on` 逐条声明并经标准 5 核实），1 条（第 1 条）仅其负向约束可独立判定。 |

**标准 6 结论**：partial——12 个锚点判定中 10 pass、2 partial（均为"独立性"锚点，且均落在**已如实声明 depends_on** 的节点上）；无锚点判 fail。张力点：规范未给"纯下游验收测试 PR"的独立性例外口径（见候选 N-2）。

---

## 汇总

- **标准条目级**（6 条）：pass 4（标准 2/3/4/5）｜ fail 0｜ partial 2（标准 1/6）｜ blocked 0
- **子项级**（26 项）：pass 21 ｜ fail 0 ｜ partial 4 ｜ blocked 1（标准 1 子项 B：摘要字数口径未定义）
- 需返工的条目：**0**
- 偏差记录：**7 条**（B-1~B-7，均不阻塞本 gate）

---

## 偏差记录

> 产出物所引用的规格 / 文档与代码库实际状态不一致之处，或产出物内部表述与代码实测不一致之处。不影响本次验收判定，但应在下一迭代同步文档或评估是否调整实现。

| # | 规格 / 文档描述 | 实现 / 代码库实际行为 | 建议处理 |
|---|---|---|---|
| B-1 | `architecture.md` §8.2 ②(d) 把 `web.test.js:632` / `:638` 一并列入"派发观测断言形态必然改写"的清单 | 代码实测：`:611` 先发"第一轮建 chat"建 chat，`:614` 再向同 `(chat_id, agent_id)` 发"流式检查第二轮"⇒ `:632`（流式增量长度）与 `:638`（终态文本）属**同一会话第二轮**，按"首轮一次"语义不注入、无需改写。pr-002 的备注已指出该出入并以代码为准（独立复核：结论正确） | 以 pr-002 备注为准；下一迭代回填 `architecture.md` §8.2②(d) 的行清单（移出 `:632` / `:638`） |
| B-2 | `architecture.md` §8.2 ⑥ 枚举"其余 17 个文件零改动"，同段算术写"4 变更 + 18 零改动 = 22"，枚举漏列 `test/api-pages.test.js` | 代码实测：`api-pages.test.js` 对 `/api/chats`、`/api/messages`、persist API **零命中**，其断言为 `index.html` 顶栏文本与 `style.css` 规则（`:57-70`），对 pr-003 的追加式改动安全 ⇒ 确实零改写。pr-002 的验收标准按 18 迁移并显式点名 `api-pages.test.js`（口径更准） | 以 pr-002 的 18 为准；下一迭代回填 `architecture.md` §8.2⑥ 的枚举清单 |
| B-3 | pr-002 的 `depends_on` 理由："缺 pr-001 ⇒ 上述改写后的断言必红；**保留旧断言同样必红（输出已多出前缀）**" | 后半句反向不成立：无 pr-001 时 `payload.body.project` 不会被消费（`agent.js:46-60` 现无 `project` 分支，`sentTurns` 零命中）⇒ 保留旧断言的 pr-002 分支仍绿。依赖成立的真实原因 = pr-002 验收标准第 9 条**强制**把 4 个测试文件的派发观测断言改写为"含前缀"形态（§8.3 形态④），该形态的绿只可能在 pr-001 在场时取得 | 依赖边成立（共享 `payload.body.project` 接口 + 共享观测面，已代码核实），**不返工**；阶段 5 以"改写后的断言形态"作为依赖判据，不采信该句 |
| B-4 | pr-003 的 `depends_on` 为「（无）」（其字段外"说明"已披露运行期互补） | 机器可读依赖边缺失：`batch` 2 内 pr-002 与 pr-003 无先后约束（规范明示 `batch` 不是调度依据）。若 batch 2 逆序合并或单独回滚 pr-002，会出现"半建成控制台"（项目列表取数 404、工作台内新建对话 400） | 阶段 5 明确 batch 2 的合并顺序（pr-002 先于 pr-003），或在 pr-003 的 `depends_on` 补齐该边；不阻塞本 gate |
| B-5 | pr-002 的文件范围含 `oamp/README.md`，但 10 条验收标准无一条给出 README 的判定口径 | `architecture.md`（§1.1 表末行）把 README 变更标注为"**非卡内要求**（§10.3 **D-03**）"，即该文件的改动内容取决于一条**待主 agent 确认**的口径点 | 阶段 5 前补一条 README 判定口径，或按 D-03 的结论回填；否则该文件的改动无验收约束 |
| B-6 | 术语同形冲突：`oamp/src/web.js:732` 既有 `projectRoutes(routes)` 语义为"**投影** routes"（与"项目"无关），而本迭代引入 `projects` / `project_id` / `PROJECT_AGREEMENT` 语义域；pr-002 的验收标准同时要求 `renderLlmsTxt(projectRoutes(createApiRoutes({})))` 逐字节相等 | 同一 PR 内两类语义并存（投影 vs 项目），实现者存在误读/误改投影函数的风险（该函数是三条漂移锁②的生成链路） | 阶段 5 的 PR 任务简报显式区分两个语义域（如"`projectRoutes` = 投影，勿改"）；改名评估列入下一迭代候选 |
| B-7 | "上下文摘要 200 字以内"未定义计量口径 | 实测：非空白字符 216~270（4/4 超限）；汉字 115/79/81/85（4/4 合规） | 由主 agent 裁定口径；若取字符口径需回写 4 份摘要，或按候选 N-1 在规范中补口径 |

---

## 下一迭代候选

- **N-1｜"200 字"缺计量口径**：`pr-planner.md` 的字段规范未定义"字"的计量方式，导致同 4 份产物在不同口径下结论相反，且该格式项**无法机械校验**。建议在规范中写死口径（推荐"非空白字符数"或"仅计汉字"，二选一）。
- **N-2｜纯下游验收测试 PR 的独立性例外**：pr-004 的 8 条 AC 中 7 条必须在 pr-001/002/003 合并后判定，与"验收标准可在不依赖其他 PR 合并的情况下判断"的锚点原文存在结构性张力。规范未给此类 PR 的例外口径（允许 `depends_on` 前置 + 以负向约束作为可独立条目），建议显式补齐，否则下一迭代会重复产生同类 partial。
- **N-3｜pr-002 体量回归验证**：9 文件 / 7 个 F-ID / 10 条 AC，是本次最大提交单元。本次不拆有代码级理由（漂移锁②③ + NOT NULL 归属列 ⇒ 文档与 schema 必须同 PR，见标准 6 证据）。建议下一迭代用实际 review 时长/逃逸缺陷回归验证该粒度判断，避免"因为锁在所以永远不拆"。
- **N-4｜`projectRoutes` 与 `projects` 语义同形（见偏差 B-6）**：评估是否改名。改名将触碰既有测试（`api-routes.test.js:269` 等）与文档，需独立评估成本与收益。
- **N-5｜README 同步的判定口径缺失（见偏差 B-5）**：与 `architecture.md` §10.3 D-03 的待确认状态绑定，需在 D-03 落地后回填。
- **N-6｜测试面"必然变更"清单的双份真源**：`architecture.md` §8.2② 与 pr-002 的验收标准各维护一份行清单，本次已出现一处不一致（偏差 B-1）。建议把该清单收敛到单一真源（architecture 只指向 PR 文件，或反之），降低下一迭代的核对成本。

---

## 结论

**PASS**

- 6 条标准：通过 4、部分通过 2（标准 1 七字段格式规范、标准 6 粒度框架）、不通过 **0**。
- 无 fail 条目 ⇒ 不构成返工需求。
- 2 条 partial 与 1 个 blocked 子项均已逐条拆分到子项级并给出可定位证据（文件:行号 / 字段名 / 实测计数），主 agent 可直接判断：**可以继续进入阶段 5**。
- 7 条偏差已记录，均不阻塞本 gate；其中 B-4（batch 2 合并顺序）与 B-5（README 判定口径）建议在阶段 5 派发前一次性处理，成本最低。
- 摘要字数口径（B-7）若被主 agent 裁定为"非空白字符"口径，则标准 1 子项 B 由 blocked 转为 fail（4/4 超限），届时需回写 4 份摘要；本报告不替主 agent 裁定该口径。
