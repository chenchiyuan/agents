# 需求合同：测试套件清零与工作流口径解冲突（迭代 0026-test-protocol-and-suite-reset）

## 文档状态

**本版**：v1.1.0（2026-09-15，阶段 3→4 方案确认门处经用户范围裁决后修订；v1.0.0 见 `21f7bea`）
**工作区地址**：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0026-test-protocol-and-suite-reset`
**迭代分支**：`iteration/0026-test-protocol-and-suite-reset`（base = `main` @ `ea8943e`）
**角色定义根**：`/Users/chenchiyuan/projects/agents/roles/`（`git ls-files --error-unmatch` 通过 = 被追踪真源）
**决策来源图例**：`user_confirmed` = 用户明说 / 拍板 / 改判；`model_inferred` = 推断，**须经用户确认方生效**；`实测` = 本机技术侦察；`仓库事实` = 代码 / 文档一手核对；`user_deferred` = 用户明示后置
**待确认项**：**无**

> **v1.0.0 → v1.1.0 的修订**：用户在方案确认门裁决「**此次不处理任何测试协议的事情**」，据此把原 W-3（产出测试协议）**整块移出本迭代**，本迭代缩为「清理 + 工作流口径解冲突 + 影响面登记」。修订依据与代价见 §4.4、§6、§8。

---

# 第一段：澄清依据

## 1. 用户表述（`user_confirmed`，原文照录）

> 问题似乎已经明确，大头时间耗费在了bash 21.6m（跑 node --test、一次性脚本），也就是经常会执行全量测试。而目前我们没有测试的标准协议定义，test脚本会无序扩张，往后时间会越来越长。请针对此问题与我深入讨论，可以使用demand skill

## 2. 起点核验：一处量级必须修正（`实测`）

用户表述含三条论断，**两条成立、一条量级需修正**：

| # | 论断 | 核验 | 证据 |
|---|---|---|---|
| 1 | 大头耗在 bash 跑 `node --test` 与一次性脚本 | **方向成立、量级需修正** | 0025 迭代 165 分钟窗口：模型推理 132.7m（80%）、子 agent 工具执行 31.2m（19%），其中 bash 22.9m。bash 内部：**全量套件约 10–12 分钟（占迭代 6–7%）**，另一半是 **28 处 `/tmp/*.mjs` 一次性验收脚本** |
| 2 | 没有测试的标准协议定义 | **成立** | 全仓 `grep 测试协议 / 测试规范 / test-protocol` 零命中 |
| 3 | test 脚本会无序扩张，往后越来越长 | **成立，且边际在加速** | 0016 251 测试/55.7s → 0018 285/57.1s → 0021 333/61.1s → 0024 387/68.5s；每测试边际成本 0.04s → 0.09s → **0.14s** |

## 3. 提问前的补充实测（改变问题定位的五条）

| # | 事实 | 数值 / 证据 |
|---|---|---|
| F-1 | **套件墙钟由最慢的单文件决定，不是由测试总数决定** | `oamp/test/web.test.js` 单文件 61.0s（39 用例 / 1663 行），占全量 68.5s 的 **89%**；排除它后全量 **31.7s**。单文件独立跑合计 273s ⇒ `node --test` 按文件并行、文件内串行 |
| F-2 | **60 秒是前台上限，套件已越过 4 个迭代** | 跨线发生在 0020/0021 之间；越线后单次全量实际成本 = 60s 前台 + 后台化 + ≥1 额外模型回合 + `sleep 10` 轮询粒度 ≈ **85–95s**（实测轮询：`for i in $(seq 1 60); do grep -q '^# fail' /tmp/…; sleep 10; done`） |
| F-3 | **运行策略全靠临场发挥** | 382 次 bash 中 **277 次用默认 `timeout`**；15 次全量调用仅 4 次显式设值（900/1200/1800/3600）；仅 pr003-verifier / pr005-dev 用对 `async` |
| F-4 | **三份文档对"测试该不该落盘"给出三种口径** | `workflow-pb.md:56` 要求 PR 产出含测试 ／ `roles/dev/dev.md:107,110` 禁止 dev 写测试与跑全量 ／ `roles/verifier/verifier.md` 全文未提。0025 实测 `pr005-dev` 跑 2 次全量 = **违反 dev 角色现行规则且无人判定** |
| F-5 | **删除存量测试对运行时零影响** | `oamp/src` `oamp/web` `oamp/bin` 全仓 grep 无 `test/` 引用；仓库外引用共 7 行（见 §5） |

## 4. 决策记录（17 项，全部 `user_confirmed`）

### 4.1 运行面（Q1–Q5）

| # | 议题 | 用户决策 | 后果 |
|---|---|---|---|
| **Q1** | 本次迭代边界 | **本次不新增测试协议条款**（后经 Q7 收窄为"只改阶段 5 输出契约那一行"；再经 Q15 收窄为"本迭代不产出协议"） | workflow-pb 结构不变 |
| **Q2** | 运行粒度 | **只跑本 PR 生成的新测试用例；全量只在迭代交付时跑** | ⚠️ **随协议延期**（无协议承载，本迭代不落盘；见 §8） |
| **Q3** | 单文件粒度上限 | **不硬性限制；理论上应只保留必须的测试用例** | ⚠️ **随协议延期** |
| **Q4** | 测试归属 | **由单独的服务维护；默认不落测试用例；PR 内产生的测完即清除** | 与 F-4 的 dev.md 现行规则一致；协议侧承载随 Q15 延期 |
| **Q5** | 一次性验收脚本 | **跑完即删** | ⚠️ **随协议延期** |

### 4.2 容器与口径（Q6–Q10）

| # | 议题 | 用户决策 | 后果 |
|---|---|---|---|
| **Q6** | 迭代容器 | **新开 0026**；0025 按现状跑完 | 0025 的 pr-006~pr-010 为测试 PR，落地后可能被本迭代清理 |
| **Q7** | 工作流口径 | **只改阶段 5 输出契约那一行**（去「+ 通过验证标准的测试」） | 消除 F-4 中的工作流侧口径；**保留在本迭代** |
| **Q8** | 存量测试处置 | **全部清理**（未采纳"按跨迭代契约意义筛选"的推荐） | 机械锁类架构守护一并移除，代价见 §6-1 |
| **Q9** | 顺序 | **先清理 → 后定协议** | 协议移出后该顺序约束在本迭代内失效；代价见 §6-2 |
| **Q10** | 维护主体 | **新增横切角色（暂名 test-keeper），本次先不做** | 保留集暂无维护主体 |

### 4.3 删除边界（Q11–Q14）

| # | 议题 | 用户决策 | 后果 |
|---|---|---|---|
| **Q11** | 协议承载形态 | **不重要，之后（撰写协议时）再定**（`user_deferred`） | 经 Q15 后该问题整块移交后续迭代 |
| **Q12** | 删除边界 | **整目录 + `package.json` + `README` 三处全收** | `oamp/test/**`（含 `helpers/`）、`oamp/package.json` 的 `test` script、`oamp/README.md:195` 的「漂移锁…由 `npm test` 强制」 |
| **Q13** | 协议是否覆盖零测试态验收判定口径 | **包含** | ⚠️ **随协议延期**（该口径本迭代不落盘；其空缺代价见 §6-3） |
| **Q14** | 保留集重建触发 | **零测试交付 + 协议内写死触发条件** | ⚠️ **随协议延期** |

### 4.4 方案确认门处的范围裁决（Q15–Q17，2026-09-15 17:5x）

| # | 议题 | 用户决策 | 后果 |
|---|---|---|---|
| **Q15** | 测试协议在本迭代的归属 | **「此次不处理任何测试协议的事情」** = 协议整块移出 0026（字面读法，用户明确选择） | 原 W-3（F03~F08 六张卡）**整块出局**；协议登记为后续迭代的输入；`architecture.md` 修订为"本迭代无架构决策"；**§6 逻辑闭环风险由假设变为事实** |
| **Q16** | 变更历史留痕 | **不留痕** | 不升任何版本号、不追加 changelog 条目；规范体变更历史在本迭代留一段空档 |
| **Q17** | `oamp/README.md:101-102` 失效句 | **纳入 F01 处置面** | F01 处置面由 `:195` 扩为 `:195` + `:101-102` 两处 |

## 5. 删除影响面清单（`仓库事实`，逐处核对）

`oamp/test/` 之外，全仓引用测试的位置共 **7 行**，其中 **5 行有文件动作**（第 1/2/3/3b/6 行），另 2 行**随协议延期、本迭代只登记**（第 4/5 行）：

| # | 位置 | 现状 | 删光后状态 | 处置 |
|---|---|---|---|---|
| 1 | `roles/workflow-pb/workflow-pb.md:56` | 阶段 5 输出契约 = `… + 代码 + 通过验证标准的测试` | 与 Q4 冲突 | **改**（Q7） |
| 2 | `oamp/package.json:13` | `"test": "node --test test/*.test.js"` | 指向不存在路径，`npm test` 报错 | **删该 script**（Q12） |
| 3 | `oamp/README.md:195` | 「漂移锁…三条锁**由 `npm test` 强制**」 | 对外承诺失效 | **删该句**（Q12） |
| 3b | `oamp/README.md:101-102` | 「**自动化测试**将 interval 缩到 30–100ms、timeout 缩到 200–400ms、日志窗口缩到 ~300ms，使全链路秒级完成」 | 描述的是一个已不存在的机制 | **改**（Q17；与第 3 行同文件同 PR） |
| 4 | `workflow-pb.md:56` 同行推进条件 | 「该 PR **验收标准全部通过**」 | 失去"套件全绿"这一方便判据 | **随协议延期** —— 本迭代只登记，不处置（Q15） |
| 5 | `roles/verifier/verifier.md` | 全文未提跑什么测试 | 阶段 6 少一种取证手段 | **随协议延期** —— 本迭代只登记，不处置（Q15） |
| 6 | `docs/iteration-time-analysis.md:236` | 「测试执行本身不是瓶颈」 | 历史结论失效 | **行尾加失效标注**（F09） |

## 6. 逻辑闭环复核（六维诊断 §逻辑闭环）

从已确认的 **why** 反推 **what**：

- 已确认的 why = 「无协议 ⇒ 每次派发重付固定成本（60s 台阶 + 轮询 + 额外回合）+ 三份文档口径矛盾导致执行角色临场取舍 + 无刹车导致单文件持续变长」
- 由此可推导的 what = **立协议**；**清存量**不是解，是"清零重建"策略的前置动作
- **v1.0.0 的复核结论是**：「清存的正当性完全依赖于协议在同一迭代内产出；若协议最终不落在本迭代，清理就只剩代价没有收益」
- **Q15 之后该条件已不成立**——协议被移出。复核结论因此**由假设变为事实**：本迭代是一次**只有代价、收益待兑现**的清理。此结论不改变用户裁决，只作显式登记（§8-1）

### 6.1 随范围修订新增的三处空缺（显式登记，不静默）

| # | 空缺 | 谁承担 | 何时闭合 |
|---|---|---|---|
| 1 | **零回归保护窗口**：从本迭代交付到协议落地之间，仓库无任何自动回归保护 | 无主体（Q10 的 test-keeper 也未做） | 协议迭代 |
| 2 | **阶段 5 失去方便判据**：「验收标准全部通过」不再有"套件全绿"可依，只剩逐条裸判定（判法未写死） | 主 agent / verifier 临场 | 协议迭代（Q13 的内容） |
| 3 | **保留集空集无出口**：Q14 的"重建触发条件"随协议延期，空集状态无约定出口 | 无主体 | 协议迭代 |

## 7. 显式后置项（`user_deferred`）

**测试协议（整块）**：用户裁决「此次不处理任何测试协议的事情」（Q15）。

- 该后置是**整块**的，不是仅"承载形态"：协议的内容（运行粒度 / 归属与时机 / 一次性脚本处置 / 零测试态验收与取证口径 / 重建触发条件）与承载形态**一体后置**。
- **随包传递的已核实事实**（供后续迭代直接取用，不需要重做侦察）：§2 的三条论断核验、§3 的 F-1~F-5 五条实测、§5 的六处影响面清单、§6.1 的三处空缺、以及 Q2/Q3/Q4/Q5/Q10/Q13/Q14 七项已裁决但未落盘的口径。
- 阶段 2 已产出的 F03~F08 六张协议卡与其架构维度补全，可从 `02fcc33` / `a0fa71c` 恢复。

---

# 第二段：需求结论

## 1. 做什么

**W-1｜清理存量测试资产**（Q8 / Q12 / Q17）
- 删除 `oamp/test/` 整目录（含 `helpers/harness.js`、`helpers/fake-node.js`）
- `oamp/package.json` 移除 `scripts.test`（其余字段字面不变，`dependencies` 仍为空对象）
- `oamp/README.md:195` 删除「漂移锁…由 `npm test` 强制」那一句；保留同段"存在三条漂移锁"的事实描述与「重新生成索引快照」命令
- `oamp/README.md:101-102` 删除「自动化测试将 interval 缩到 30–100ms、timeout 缩到 200–400ms、日志窗口缩到 ~300ms，使全链路秒级完成」这一句
- 不引入任何替代机制（无 CI、无 git hook、无替代 script、无占位文件）

**W-2｜工作流阶段 5 输出契约去掉测试产出**（Q7）
- `roles/workflow-pb/workflow-pb.md:56` 输出契约列去掉「+ 通过验证标准的测试」；同行其余内容字面不变

**W-3｜登记 7 行影响面并闭合**（原 W-4 重编号）
- §5 表中第 1/2/3/3b 处由 W-1/W-2 直接改
- 第 6 处（`docs/iteration-time-analysis.md:236`）行尾追加失效标注，点明失效原因与生效迭代
- 第 4/5 处**登记为「随协议延期」**，本迭代不做文件动作

## 2. 不做什么

| 不做 | 为什么这次不做 |
|---|---|
| **不产出测试协议**（整块） | Q15 明示「此次不处理任何测试协议的事情」；协议整块移交后续迭代（§7） |
| **不新增任何测试用例**（零测试交付） | Q8 明确全删；协议未定前建测试等于在无判据状态下做保留决策 |
| **不新增 test-keeper 角色** | Q10 裁决"本次先不做"；角色定义本身需要独立迭代 |
| **不改任何角色文件**（`roles/dev/`、`roles/verifier/`） | 阶段 5 零测试态口径随协议延期；dev.md 现行规则已与 Q4 同向，无需改 |
| **不改任何生产代码**（`oamp/src` `oamp/web` `oamp/bin`） | 本迭代是测试资产与工作流口径的治理，不涉及功能 |
| **不引入 CI / git hook / 第三方依赖** | 与仓库既有立场一致：零第三方依赖、`scm-protocol.md`「本协议不引入 hook 参与强制」 |
| **不升任何版本号、不写变更历史条目** | Q16 明示不留痕 |
| **不处置 §5 第 4/5 处** | 它们的处置内容是协议内容（Q13），随 Q15 延期；本迭代只登记 |
| **不回改 0025** | Q6：0025 按现状跑完 |

## 3. 大概怎么做

- **改动面收敛为 3 组互不重叠的文件面**，天然给出 3 个可独立合并的提交单元：

| 单元 | 文件面 | 动作 |
|---|---|---|
| ① 清理 | `oamp/test/**`、`oamp/package.json`、`oamp/README.md` | 删除 |
| ② 工作流口径 | `roles/workflow-pb/workflow-pb.md`（`:56` 一行） | 删「+ 通过验证标准的测试」 |
| ③ 影响面闭环 | `docs/iteration-time-analysis.md`（`:236` 行尾） | 追加失效标注 |

- **顺序**：①②③ 之间**无强制顺序**（Q9 的顺序约束随协议移出而失效）。③ 的验收判据读 ① 与 ② 的产物状态，故 ③ 排在 ① 与 ② 之后更省事——但这是执行便利，不是依赖，具体切分与依赖边归阶段 4。
- **协议侧的一切口径不落盘**：见 §7 随包传递清单。

## 4. 达到什么效果（裸判定）

1. `oamp/test/` 目录不存在；`git ls-files oamp/test` 零命中
2. `oamp/package.json` 无 `scripts.test`；`grep -n '"test"' oamp/package.json` 零命中
3. `oamp/README.md` 全文不含「由 `npm test` 强制」，也不含「自动化测试将 interval 缩到 30–100ms」
4. `grep -n "通过验证标准的测试" roles/workflow-pb/workflow-pb.md` 零命中
5. `docs/iteration-time-analysis.md:236` 附近可读到失效标注，且标注点明原因与生效迭代
6. 面对 §5 的全部 6 行，任何人都能说出"这一处现在是什么状态、为什么"——第 1/2/3/3b 处已改，第 4/5 处标注为随协议延期，第 6 处已加注

## 5. 为什么这么做

| 已确认的现状 | 推出的动作 |
|---|---|
| 测试 14986 行 > 生产代码 10393 行；套件墙钟 68.5s 且由最慢单文件独占 89% | 用户选择清零重建而非筛选治理（Q8） |
| 三份文档口径矛盾，0025 实测 `pr005-dev` 违反 dev.md 且无人判定 | 消除工作流侧那一份口径（W-2） |
| `oamp/README.md` 有两处声明描述已不存在的机制（`:195` 与 `:101-102`） | 同一 PR 内一并删除（Q12 / Q17） |
| 删除后 6 处引用会悬空 | 逐处给出归属，悬空的显式标为"随协议延期"（W-3） |
| 生产代码零引用测试面 | 删除对运行时零影响（F-5 已核实） |

## 6. 被否决的路径（需求边界的一部分）

| 被否决项 | 否决理由 | 已登记的代价 |
|---|---|---|
| **1. 按「跨迭代契约意义」筛选保留**（Q8 推荐被否决） | 用户裁决全删 | 机械锁类（`zero-intrusion.test.js` 326ms / `hygiene.test.js` 88ms / `cli.test.js` 269ms，合计占全量墙钟 1%）一并移除，**架构不变量的自动检出中断** |
| **2. 先定协议后清理**（Q9 推荐被否决） | 用户裁决先清理 | 零回归保护窗口；因 Q15 该窗口不再由本迭代闭合，见 §6.1-1 |
| **3. 保留 `helpers/` 脚手架**（Q12 B 被否决） | 用户选全收 | 保留集首次重建时需重写测试基建（`harness.js` 6.8KB 一个导出面） |
| **4. 协议保留在本迭代**（Q15 未选 B） | 用户裁决整块移出 | §6.1 的三处空缺；§6 的复核结论由假设变为事实 |
| **5. 变更历史破例留痕**（Q16 未选） | 用户裁决不留痕 | 规范体在本迭代留一段变更历史空档 |
| **6. `oamp/README.md:101-102` 只登记不处置**（Q17 未选） | 用户选纳入 | 无 |
| **7. 实现 PR 可落测试**（Q4 推荐被否决） | 用户裁决 PR 不落测试 | 回归保护延后至保留集重建 |

## 7. 验证方法

| # | 检查动作 | 可观察信号 |
|---|---|---|
| V-1 | `ls oamp/test/` 与 `git ls-files oamp/test` | 目录不存在 / 零命中 |
| V-2 | `grep -n '"test"' oamp/package.json` | 零命中 |
| V-3 | `grep -n "npm test" oamp/README.md` 与 `grep -n "自动化测试" oamp/README.md` | 均零命中 |
| V-4 | `grep -n "通过验证标准的测试" roles/workflow-pb/workflow-pb.md` | 零命中（历史澄清记录 `docs/iterations/*/clarifications/` 里的同名引用是历史引文，不在判据面内） |
| V-5 | 读 `docs/iteration-time-analysis.md:236` 所在行 | 行尾有失效标注，标注含失效原因与生效迭代 |
| V-6 | `git diff` 逐文件核对改动面 | 只出现 ① ② ③ 三组文件，无越界改动 |

## 8. 范围的显式代价登记（`user_confirmed`，不作阻塞）

1. **本迭代是一次"只有代价、收益待兑现"的清理**：§6 的复核结论在 Q15 之后成为事实。用户已知悉并裁决。
2. **零测试态与三处空缺**（§6.1）**无本迭代内的闭合动作**，全部依赖后续的协议迭代。
3. **随包传递**：§7 列出的已核实事实与七项已裁决口径，是后续迭代写协议时的直接输入——本次不做二次侦察。
