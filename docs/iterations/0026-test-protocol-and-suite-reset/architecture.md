# architecture.md — 0026-test-protocol-and-suite-reset

**版本**: 0.2.0
**迭代**: 0026-test-protocol-and-suite-reset
**创建日期**: 2026-09-15
**阶段**: 技术架构（阶段 3，**范围修订后重做**）
**输入**: `prd.md` **v0.2.0**（F01 / F02 / F09 三张卡，**0 条 `[架构待填]`**）+ 现有规范体与代码库（基线 `ea8943e`）
**本版修订**: 用户在阶段 3→4 方案确认门裁决 Q15「此次不处理任何测试协议的事情」→ 测试协议**整块移出本迭代**，`prd.md` 由 v0.1.0 收缩为 v0.2.0（9 张卡 → 3 张，F03~F08 删除）。v0.1.0（`a0fa71c`，328 行）服务的需求含「W-3 产出测试协议」，其唯一技术缺口 D-1（**L1**：协议承载形态）随协议出局而作废。本版按收缩后范围**重算**改动面与依赖边，不是在 v0.1.0 上打补丁；作废清单见 §6.3。**追加轮裁决**：Q18（删除 `oamp/scripts/testenv.mjs`）/ Q19（只登记 `oamp/src/cluster-config.js:19` 注释）见 §6.1。

**一句话结论**：本迭代**没有架构决策**——三张卡分别是"整目录删除"、"一行内删字"、"一行行尾加注"，无一触及组件边界、数据模型或技术选型。阶段 3 的全部产出因此是**改动面与依赖边的重算**，供阶段 4 直接消费。

---

## 1. 现状基线（极简）

### 1.1 本迭代接入的契约体结构（基线 `ea8943e`）

`ea8943e` 按关注点把 workflow-pb 规范拆为"主文件 + `data/` 子协议"形态：

| 文件 | 规模 | 与本迭代的关系 |
|---|---|---|
| `roles/workflow-pb/workflow-pb.md`（v0.13.0） | 418 行 | **F02 的唯一改动对象**：`:56`（阶段定义表阶段 5 行）删「+ 通过验证标准的测试」 |
| `roles/workflow-pb/data/scm-protocol.md`（v1.0.0） | 165 行 | **零改动** |
| `roles/workflow-pb/data/formats.md`（v1.0.0） | 228 行 | **零改动**（v0.1.0 曾计划在此新增 `## 测试协议` 一节，已随协议作废） |
| `roles/workflow-pb/data/` 另 4 份（3 份设计期笔记 + `workflow-pb-changelog.md`） | — | **零改动** |

主文件以"索引，权威定义见 `data/xxx.md`"接入两份子协议（`:79` / `:101` / `:355`），权威定义各只存在一处。**本迭代不动这个结构**：没有任何卡要求在 `data/` 下新增或改写子协议。

阶段 3 自身的接口也在本文件内闭合：主文件 `:54` 定义阶段 3 的完成条件 =「L1 决策经用户确认；所有功能卡有技术路径；无架构内部冲突」——§2 与 §7 即对照它自检。

### 1.2 与三张卡相关的既有事实（**一手核实**，非引用上游转述）

| # | 事实 | 核实方式 | 对本迭代的意义 |
|---|---|---|---|
| A | `oamp/src` `oamp/web` `oamp/bin` 与测试资产**无代码级耦合**：三目录对 `test/` 的引用只有 `src/cluster-config.js:19` 的**一处注释**，0 个 `import` / `require` | `grep -rn "test/" oamp/src oamp/web oamp/bin` | F01 的删除对运行时零影响（F-5 的**结论**成立：注释无运行时效果）。措辞精确化见 §5.5；`oamp/scripts/` **不在这三目录内**，另有一处**可执行 `import`**（会实际报错），见 §6.1-A |
| B | 仓库**无 `.github/`**、无 git hook | `ls -d .github` | F01 验收 4「不引入替代机制」天然满足；删除不会撞上任何自动门禁 |
| C | `oamp/README.md:195` 与 `:101-102` 两句与 `demand.md` §5 第 3 / 3b 行引文逐字一致（`:101-102` 为实际行号，§5 记作 `:100-101`，`prd.md` §疑问 5 已登记） | `sed -n '99,103p;194,197p' oamp/README.md` | F01 验收 3 的判据以**引文**为准，行号漂移不影响可核查性 |
| D | `oamp/package.json` 的 `scripts.test` 实际在 **`:12`**（`demand.md` §5 第 2 行记作 `:13`） | `cat -n oamp/package.json` | 同上：F01 验收 2 的判据是键名 `grep '"test"'`，非行号，无影响 |
| E | `docs/iteration-time-analysis.md:236` 逐字 =「**测试执行本身不是瓶颈**：全量测试 `node --test oamp/test/*.test.js` 约 64 秒（0021 收口记录）…」 | `awk NR==236` | F09 第 6 处的加注对象确定；该行**自身即含** `oamp/test/*.test.js` 引用，删除后既失效又指向不存在路径 |
| F | `.claude/skills/workflow-pb/SKILL.md` 全文「测试」零命中 | `grep -c "测试"` | F02 无需同步第二份拷贝（`prd.md` §疑问 2 成立） |

---

## 2. 无架构决策的判定（本次修订的核心）

### 2.1 逐卡判定：为什么每张卡都没有架构空间

**判定尺子**（来自角色定义的成功标准）：本文件里的"组件"= 文档实体 / 代码模块；"架构决策"= 改变组件边界、数据模型、接口约定或技术选型；**"架构空间"的判据 = 该卡的动作是否必须回答"落在哪个组件 / 用什么机制承载"这个问题**。

| 卡 | 动作面 | 是否必须回答承载问题 | 判定 |
|---|---|---|---|
| **F01** | ① 整目录删除 `oamp/test/**`（含 `helpers/`）；② `oamp/package.json` 移除 `scripts.test` 一个 key；③ `oamp/README.md` 删两句文本。**明文不引入替代机制**（无 CI、无 hook、无替代 script、无占位文件） | **否**——这是本次判定的关键：被删资产原本承担的职责（回归保护 / 漂移锁强制）**不被任何新组件接管**，F01 验收 4 把"不引入替代机制"写成硬约束，等于上游已裁定"不需要承载形态"。无接替者 ⇒ 无承载形态问题 ⇒ **无架构空间**。（对照：v0.1.0 的 D-1 之所以是 L1，恰恰因为它要为协议找承载；协议出局后承载问题整体消失） | **无架构空间** |
| **F02** | `roles/workflow-pb/workflow-pb.md:56` 一行内删 8 个字（「+ 通过验证标准的测试」）；同行的推进条件列与输出列其余内容字面不变；不加解释性章节；不改角色文件 | **否**——删除发生在**一个既有单元格内部**，不改变任何组件的职责边界：主文件的职责（流程契约主体）不变、`data/` 两份子协议的职责不变、索引关系不变。F02 验收 4（`git diff` 只出现这一行）把改动面物理锁死在一行内，连"给这一行加个注"都在约束之外 | **无架构空间** |
| **F09** | 一件文件动作：`docs/iteration-time-analysis.md:236` 行尾**同行追加**失效括注（点明原因与生效迭代）；另 6 行只登记、零文件动作（第 1/2/3/3b 行由 F01/F02 改，第 4/5 行随协议延期） | **否**——加注是"同一 bullet 行尾追加自然语言括注"：不新增章节结构、不重写既有分析与数值、不定义数据格式、不新增命名实体。第 4/5 行的**登记**更是零文件动作。（对照：v0.1.0 的 D-5/D-6 在设计"零测试态判据怎么判"，那是 F03~F08 的接口，已随协议出局；F09 **不接手**它，只做登记） | **无架构空间** |

### 2.2 结论：本迭代 L1 = 无、L2 = 无、L3 = 无

- **L1**（引入新技术栈 / 改核心模块职责 / 影响系统整体边界）：**无**。三张卡无一改变 `roles/workflow-pb/` 规范体的结构、组件边界或技术栈；无一引入新文件、新角色、新机制。
- **L2**（局部模块结构 / 数据流设计 / 栈内选型）：**无**。无接口、无数据流可设计。
- **L3**（实现细节 / 命名约定）：**无**。本次新增文本是**无编号的自然语言括注与删字**，不产生命名实体（v0.1.0 的 D-8「规则编号 T1~T5」随协议作废）。

**L1 = 无 的直接后果**：阶段 3 完成条件的第一项「L1 决策经用户确认」本次**空真（vacuously true）**成立——不存在需要用户拍板的架构决策。因此阶段 3 无需再经方案确认门，其产物（本文件 + 0 条 `[架构待填]` 补全）即为完成态。**本节存在的意义，就是让"阶段 3 已完成"这个结论有依据可查，而不是靠"没有反对意见"。**

### 2.3 §6.1 的两处越界均已裁决，且都不引入架构决策

一手核实发现两处 §5 清单之外的、指向已删除测试资产的结构性引用（其中一处是**可执行的 `import`**，非注释），详见 §6.1。**两处已于 2026-09-15 18:0x 的方案确认门追加轮裁决**：A（可执行 `import`）→ **Q18：删除整个文件**；B（注释）→ **Q19：只登记、不处置**。两项处置分别是"删除一个文件"与"零文件动作"，**都不产生架构空间**——§2.2 的结论不但稳健，且裁决后无需任何补充：本迭代 L1 = 无、L2 = 无 的结论与裁决结果无关地成立。

---

## 3. 改动面全景（file-face）

### 3.1 三组互不重叠的文件面

| 单元 | 文件面 | 动作 | 承载卡 |
|---|---|---|---|
| ① 清理 | `oamp/test/**`（整目录，含 `helpers/`）、`oamp/package.json`（`scripts.test` 一个 key）、`oamp/README.md`（`:195` 一句 + `:101-102` 一句）、`oamp/scripts/testenv.mjs`（整文件，Q18 裁决纳入） | 删除 | **F01**（验收 1/2/3/6） |
| ② 工作流口径 | `roles/workflow-pb/workflow-pb.md`（`:56`，**仅一行**） | 行内删字 | **F02**（验收 1/2/4） |
| ③ 影响面闭环 | `docs/iteration-time-analysis.md`（`:236`，**仅一行**） | 行尾加注 | **F09**（验收 2） |

**三组两两不重叠**（文件路径集合无交集）——事实来自 `demand.md` §3，本节只做重算与确认，不新增判断。

**与 v0.1.0 的差异（重算，非照抄）**：v0.1.0 §1.4 是**四组**（F01 / F02 / F03~F08 / F09）。收缩后 F03~F08 出局，其文件面（拟新增于 `data/formats.md` 的 `## 测试协议` 一节）**整组消失**，故 4 → 3。**注意**：这不是"把两组并成一组"，而是**删掉一组**；`roles/workflow-pb/data/` 在本迭代**零改动**。

### 3.2 给阶段 4 的直接后果

1. **三个可独立合并的提交单元天然成立**，无需取舍：①②③ 文件面无重叠。
2. **不存在"多张卡必须落在同一 PR"的情形**（v0.1.0 中 F03~F08 六卡同 PR 的约束随协议出局）。
3. **`oamp/README.md` 的两处动作必须落在同一 PR**：F01 验收 3 的两条检索同文件（`demand.md` §5 第 3b 行明记"与第 3 行同文件同 PR"）。
4. **§6.1 的两处越界已裁决（Q18 / Q19），三组不变**：A 的删除属 F01 的处置面——`oamp/scripts/testenv.mjs` 并入 ① 清理组（该组内多一个删除对象），**不新增文件面组**；B 零文件动作。故本迭代的 PR 切分依据仍是 §3.1 的三组互不重叠文件面。

---

## 4. 依赖边证据形态（交阶段 4）

### 4.1 三条边的判定

| 边 | 判定 | 依据 |
|---|---|---|
| **F01 ↔ F02** | **无依赖** | 文件面无交集；判据互不读取（F02 验收 1 的 `grep` 只查 `workflow-pb.md`，F01 的三条判据只查 `oamp/**`）。`demand.md` §3：①② 之间无强制顺序 |
| **F01 → F09** | **判据读取边**（非共享文件） | F09 验收标准 1 的第 1/2/3/3b 行判据（V-2 / V-3 / V-4 零命中）读的是 **F01 产物的文件状态**（`oamp/package.json`、`oamp/README.md` 两句） |
| **F02 → F09** | **判据读取边**（非共享文件） | F09 验收标准 1 第 1 行判据（V-4 零命中）读的是 **F02 产物的文件状态**（`workflow-pb.md:56`） |

**F09 的自身文件范围只有 `docs/iteration-time-analysis.md`**，与 F01/F02 的文件面**零交集**——因此这两条边**不是共享文件依赖，而是"下游判据读上游产物状态"**。

### 4.2 证据形态与 `formats.md` §6 判据的关系（阶段 4 需显式回答的一个口径问题）

`roles/workflow-pb/data/formats.md` §6 对 `depends_on` 的判据原文是：

> 该 PR 真正依赖的其他 PR 文件名列表，可为空列表，但字段必须存在。**每一条依赖必须有代码级证据（共享符号 / 接口 / 文件），不能是文档叙述顺序。**

本迭代这两条边的证据形态是**可重放的文件状态读取**（例：`grep -n '"test"' oamp/package.json` 零命中、`grep -n "通过验证标准的测试" roles/workflow-pb/workflow-pb.md` 零命中），**不是共享文件**。于是阶段 4 面临一个二选一，两条路各有代价（**本架构侧不作裁决**——切分与依赖边归阶段 4）：

| 路线 | 形态 | 代价 |
|---|---|---|
| **(a) 登记 `depends_on`** | F09 的 PR 文件写 `- pr-00X-*.md（理由：F09 验收标准 1 第 1/2/3/3b 行的判据读该 PR 改动的文件状态）` | 理由行缺 `formats.md` §6 字面要求的"共享符号 / 接口 / 文件"三类证据之一 → 有被 §验证目标「依赖正确性验证」判为"把顺序便利误判成依赖"的风险 |
| **(b) 不登记**（三 PR 并发） | `depends_on` 写 `（无）` | F09 验收标准 1 的第 1/2/3/3b 行在并发窗口内**无法通过**（此时 F01/F02 尚未合并，那几条 `grep` 仍命中）。按 `formats.md` §验收标准「至少一条可独立判断」，F09 的验收 2/3/4 仍可独立判，**不违反格式规范**；但合并后必须由主 agent 在 F01/F02 落地后**重跑那几条检索**作为最终确认 |

**给阶段 4 的建议（非裁决）**：若按 (a) 登记，理由行必须写明"判据读产物状态"这一定性，并明说它不满足 §6 的文件级证据要求（把口径问题显式交给阶段 6 判）；若按 (b) 不登记，则 F09 的 PR 文件应在验收标准处写明"第 1 行判据的最终确认以 F01/F02 合并后复核为准"。

**两者的共同前提**：`demand.md` §3 明记 ③ 排在 ①② 之后"是执行便利、**不是依赖**"——本架构侧如实转达该定性，不做改判。

---

## 5. 保留的一手核实事实（供阶段 4 与后续迭代）

以下事实与本次收缩无关，已一手核实，保留备查。

| # | 事实 | 说明 |
|---|---|---|
| 5.1 | `tools/install-pb-agents.sh` 只分发 `<role>/<role>.md`、**不分发 `data/`**（脚本 `:6-8` 注释明示该约定）→ 下游业务项目拿不到任何子协议，主文件对 `data/xxx.md` 的索引引用在下游断链 | **既有结构性缺口，与本迭代无关**（本迭代对 `data/` 零改动）。保留备查：后续若再谈"协议落在哪里"，这是"落在 `data/` 下对下游不可见"的既成事实 |
| 5.2 | `roles/workflow-pb/data/formats.md:3` 版本行标注「1.0.0（对应 workflow-pb v0.12.0）」，而主文件已是 v0.13.0 | 既有失准，历史遗留。本迭代不改（Q16 不留痕、零版本动作） |
| 5.3 | `roles/workflow-pb/workflow-pb.md:398` 的分区判据只有三个桶（`scm-protocol.md` / `formats.md` / 主文件），**无对应"测试纪律"的桶** | 该判据空白是 v0.1.0 协议承载形态决策的事实起点。**协议出局后本次不涉及，本架构侧不再主张补齐它**（补齐需改主文件，而 F02 把改动面锁在 `:56` 一行） |
| 5.4 | `oamp/README.md` 多处 env 表备注含「（测试用）」「（测试注入…用）」等表述（`:79` `:91` `:92` `:95` `:98` `:99` `:116` `:187` `:223`） | F01 只删两处句子、不动 env 表。这些备注描述的是 **env 可覆盖性这一设计性质**，删除测试后适用面收窄，但**不是指向已删除资产的有效引用** → F01 不处置、F09 不登记 |
| 5.5 | `demand.md` §3 F-5 的措辞「`oamp/src` `oamp/web` `oamp/bin` 全仓 grep 无 `test/` 引用」**不精确**：`oamp/src/cluster-config.js:19` 有一处**注释**引用 `test/hygiene.test.js` | F-5 的**结论**（删除对运行时零影响）仍成立——注释无运行时效果、无 import。本迭代不改该文件（F01 验收 5 冻结 `oamp/src`），**只精确化措辞、不处置**（Q19 已按同一口径裁决为只登记、不处置） |

---

## 6. 越界与裁决记录

### 6.1 两处 §5 清单之外的、指向已删除测试资产的结构性引用（**已裁决**：Q18 / Q19）

一手全仓核实（`grep` 范围：全仓，排除 `oamp/test/` 与 `docs/iterations/**`）发现 `demand.md` §5「共 7 行」之外还有两处**结构性引用**。两处已于 2026-09-15 18:0x 的方案确认门追加轮裁决（A → **Q18**，B → **Q19**）：

| # | 位置 | 性质 | 与现有卡片的关系 | 裁决与处置 |
|---|---|---|---|---|
| **A** | `oamp/scripts/testenv.mjs:11`：`import { startRouter, startAgent, queryStatus, waitFor, stopAll, buildEnv } from '../test/helpers/harness.js';`（另 `:3` `:7` 两处注释同指） | **可执行的 `import`**——F01 删除 `oamp/test/helpers/harness.js` 后，该脚本**在 import 阶段即失败**，从"能跑的 demo/冒烟脚本"变为"必然报错的脚本"。它不是失效**声明**，是失效**依赖** | **发现时**不在 F01 文件面（当时为 `oamp/test/**` + `package.json` + `README.md`）、不在原 §5 七行；曾使 F09 验收标准 3 的原措辞（「不存在指向已删除测试资产的有效引用」）按字面不成立 | **已裁决（Q18）：删除整个文件**——`oamp/scripts/` 不在 F01 验收 5 的冻结面内，故纳入处置**无需开任何冻接口子**；已随裁决写入 F01 验收 6（见 §3.1） |
| **B** | `oamp/src/cluster-config.js:19` 注释：「…字符串拼接构造，避免被 `test/hygiene.test.js` 的源文件扫描（词边界、大小写不敏感）判为自发命中。」 | 注释，**无功能影响**；但指向已删除文件，且所记载的设计理由（`'to' + 'ken'` 拼接是为躲开测试扫描）随之失效 | 该文件在 F01 验收 5 的**冻结面内**（`oamp/src` 零改动）；曾与 F09 验收 3 的原措辞字面冲突 | **已裁决（Q19）：只登记、不处置**（`oamp/src` 冻结面原样保持） |

**当时为什么上报而非自行处置**：两处都要求"扩大 F01 的文件面"或"收窄 F09 的验收措辞面"——前者是**改动面边界（产品维度）**，后者是**功能卡验收标准**，两者都在架构角色的红线之外（不修改 `prd/*.md` 的产品维度；架构冲突通过报告解决）。该上报已由下述裁决闭合。

**三个选项与最终裁决**：

| 选项 | 形态 | 代价 | 判定 |
|---|---|---|---|
| 1. 纳入 F01 文件面（A、B 都处置） | A 随 `oamp/test/` 一并删除、B 改注释措辞 | 仅当"B 也处置"时才需要收窄冻结面；本次裁决**未处置 B**，故该代价不发生（见下） | 未采纳 |
| 2. 只登记、不处置（A、B 都登记） | A / B 作为第 8 / 9 行登记为既有遗留 | A 是**实际功能破损**（不是"看起来能跑"，是"跑不起来"），登记它等于接受一个必然报错的脚本留在库里 | 未采纳 |
| **3. 只处置 A、登记 B** | A 是 `import`（会报错）、B 是注释（不会报错）——按"是否产生功能破损"分级处置 | — | **已采纳（Q18 / Q19）** |

**裁决结论与一处代价更正**：

- **Q18（A）→ 删除整个文件** `oamp/scripts/testenv.mjs`：该脚本的存在理由（文件头注释自述"复用 `test/helpers/harness.js` 的真实进程拉起能力"）随 F01 一起消失，删除即消解失效依赖。
- **Q19（B）→ 只登记、不处置**：注释无功能影响，`oamp/src` 冻结面原样保持。
- **对选项 1 代价陈述的更正（主 agent 判断，据此更新）**：选项 1 原写的"F01 验收 5 的冻结面需收窄（改为冻结 `oamp/web` `oamp/bin` + `oamp/src` 除该注释外）"**在本裁决下不成立**——裁决只处置 A、不处置 B ⇒ **不碰 `oamp/src/cluster-config.js:19`，冻结面原样保持**「`oamp/src`、`oamp/web`、`oamp/bin` 三目录零改动」；且 A 所在的 `oamp/scripts/` **本就不在冻结面枚举内**（冻结面枚举的是 src / web / bin 三个目录），故删除 `testenv.mjs` **不需要开任何冻接口子**。原代价句的前提是"A、B 都处置"，**读者不得据其推断本次开了冻接口子**。
- **F09 验收标准 3 的闭合口径**：`prd` 侧将把该条收窄为「**可执行引用（`import` / `require` / `spawn` 路径）必须零命中；注释类引用允许存在但须逐条登记**」——A 由 Q18 的删除满足前半句（零命中），B 由 Q19 的登记满足后半句。本文件不改 `prd/*.md`，此处仅引用该结果。

**裁决后的落点**：A 的删除**属 F01 的处置面**——`oamp/scripts/testenv.mjs` 并入 ① 清理组成为该组内多一个删除对象，**不新增文件面组**（三组不变，见 §3.1 / §3.2）；B 零文件动作。A 仍是本次核实中**唯一一处会产生实际功能破损**的引用；两项处置都不改变 §2 的"无架构决策"结论（§2.3）。

### 6.2 已登记、本迭代不处置的事项

| # | 事项 | 处置 |
|---|---|---|
| 1 | §5 第 4/5 行（`workflow-pb.md:56` 同行推进条件的判据、`roles/verifier/verifier.md` 的取证口径） | **随协议延期**，本迭代只登记（Q15）；F09 验收 1 的第 4/5 行即此登记 |
| 2 | 规范体变更历史在本迭代的空档（Q16 不留痕：不升版本号、不追 changelog 条目） | **用户已裁决**，非待裁决项（v0.1.0 的 D-4「是否破例留痕」待裁决随之作废） |
| 3 | §5.2 / §5.4 / §5.5 的既有失准与措辞不精确 | 只登记、不处置（外科手术式：不在本次需求范围内） |
| 4 | §6.1 的 B（`oamp/src/cluster-config.js:19` 注释引用 `test/hygiene.test.js`） | **Q19：只登记、不处置**——注释无功能影响，`oamp/src` 冻结面原样保持（归属与判据见 §6.1） |

### 6.3 从 v0.1.0 删除 / 作废的内容（如实登记）

| v0.1.0 内容 | 处置 | 理由 |
|---|---|---|
| §1.2「分区判据（`workflow-pb.md:398`）空白」整节 | 降为 §5.3 保留事实，**删除"本次要补一条判据"的主张** | 该空白是协议承载形态决策的前提；协议出局，本次不涉及 |
| §1.3「与测试资产邻接的既有条款」表（`scm-protocol.md` §隔离边界声明 ×2、`dev.md:107/110`、`verifier.md`） | **删除** | 整表服务于"新协议必须与它们不自相矛盾"——新协议不存在，接触面消失。（其中"不改 `dev.md` / `verifier.md`"这一**结果**由 F01/F02/F09 的边界各自承载，不依赖本节） |
| §1.4「四组文件面」 | **删除并重算为三组**（§3.1） | F03~F08 出局，其 `data/formats.md` 文件面整组消失——是减少一组，不是合并 |
| §2「唯一真正的技术缺口是承载形态」整节 | **删除** | 缺口是"协议文本落在哪个文件"；协议出局 ⇒ 缺口不存在 |
| **D-1（L1，协议承载形态；含方案 A~D 对比、"三项前置授权"）** | **删除**（本迭代**唯一 L1** 随之作废） | 承载形态与协议**一体后置**（`demand.md` §7）；保留即等于为被移出的协议补替代方案（本次明确禁止） |
| D-2（协议节位置 / 标题 / 自描述）、D-3（与 `scm-protocol.md` 条款的关系消解）、D-4（版本动作）、D-5（运行粒度按文件面 + 默认空集）、D-6（跨迭代契约判据）、D-7（不改角色文件）、D-8（T1~T5 命名） | **全部删除** | 全部是协议正文的设计决策（协议内容与承载形态一体后置）。D-4 的"版本动作"结论另已由 Q16 直接裁决覆盖（§6.2-2） |
| §4「组件图与核心数据流」（mermaid 图 + 5 步数据流） | **删除** | 该图描述的是"规则 T1~T5 如何被执行到"——T1~T5 不存在。收缩后本迭代**无跨组件数据流可画** |
| §5「协议节结构规格（T1~T5）」整节 | **删除** | 协议正文规格，随协议移出 |
| §6 依赖边中的「**F01 → F03~F08 是语义前置而非共享文件**」结论 | **删除** | 该边的下游（F03~F08）已不存在 |
| §7「新增实体登记（奥卡姆检验）」5 行表 | **删除表，保留结论** | 5 行全部是"某协议承载 / 机制候选不引入"的检验；收缩后本迭代**本就不新增任何实体**，登记表失去对象。结论并入 §7（新增实体 = 0） |
| §8「验证（本方案自检）」6 条 | **重写为 §7** | 其中 4 条（六张卡 `[架构待填]`、与 `scm-protocol.md` 的关系、与 F02 验收 4 不冲突、与四组文件面相容）以协议或四组文件面为前提，随本次收缩作废 |
| §9 越界表第 1 项（`oamp/README.md:101-102` 记为"越界项"） | **移除该定性** | 已由用户 Q17 裁决**纳入 F01 处置面**（F01 验收 3 第二项）——不再是越界项 |
| §9 越界表第 3 项（`SKILL.md:34` 把 `formats.md` 标注为「数据格式规范」不精确） | **删除** | 该"不精确"是方案 A（协议并入 `formats.md`）的副作用；方案 A 出局 ⇒ 副作用消失，标签恢复精确 |
| §9 越界表第 4 项（`roles/dev/dev.md:110` 零测试后退化为恒真） | **删除** | 该项是"协议侧需消解的对象"；协议出局后无需消解，且本迭代不改 `dev.md` |
| §9 越界表第 5 项（`install-pb-agents.sh` 不分发 `data/`） | **保留**（→ §5.1） | 与本次收缩无关的结构性事实，对后续迭代有用 |
| §9 越界表第 6 项（changelog 空档需裁决） | **降为已裁决事项**（§6.2-2） | Q16 已裁决不留痕，非待裁决 |

---

## 7. 验证（本方案自检）

对照阶段 3 的完成条件（`workflow-pb.md:54`）三项：

| 完成条件 | 自检结果 |
|---|---|
| **L1 决策经用户确认** | **无 L1 决策**（§2.2）→ 条件**空真成立**；本文件不含任何待确认的 L1 项 |
| **所有功能卡有技术路径** | **3/3**：F01 = 整目录删除 `oamp/test/**` + 一个整文件删除 `oamp/scripts/testenv.mjs`（Q18）+ `package.json` 一个 key 移除 + `README.md` 两句文本删除（文件面 ①，验收 1/2/3/6）；F02 = 一行内删字（文件面 ②）；F09 = 一行行尾加注 + 其余登记行零文件动作（文件面 ③，其中 3b 行与 F01 同文件同 PR） |
| **无架构内部冲突** | 本迭代无跨组件约束，**无架构内部冲突面**。逐卡边界互斥成立：F01 验收 5 冻结 `oamp/src`/`web`/`bin`（Q19 后**原样保持**，未开冻接口子），F02 验收 4 锁 `workflow-pb.md` 一行，F09 验收 4 与 §5 处置列一致。§6.1 的两处仓库事实（与 F09 验收标准 3 原措辞字面冲突）**已由 Q18 / Q19 裁决闭合**（Q18 删文件 ⇒ 可执行引用口径零命中；Q19 登记 ⇒ 注释类引用逐条登记），本侧未自行处置 |

**输出契约自检**：`prd/*.md` 中 `[架构待填]` 项 = **0 条**——F01 / F02 / F09 三张卡的「架构维度」均写着"无待填项"，与本文件的逐卡判定一致。本阶段**无需也无权**修改任何 `prd/*.md`。

**奥卡姆检验**：本迭代新增实体 = **0 个文件、0 个组件、0 个新技术选型**（§2.2、§3.1）。全部动作为**删除与改字**。
