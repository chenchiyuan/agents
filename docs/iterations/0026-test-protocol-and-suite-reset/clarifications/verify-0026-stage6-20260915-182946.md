# 阶段 6 独立验证报告：迭代 0026 三 PR 合并后联合状态

**验证者身份（反射结果）**：**仓库状态与改动面封闭性的独立审计员（release-state auditor）**次级身份：文本检索判据的复核者（判定面 = git 面 + 文本检索，本迭代无可运行套件——测试资产被本迭代自身清零，故"跑起来看"类取证手段在客观上不存在）。

**产出物**：迭代分支 `iteration/0026-test-protocol-and-suite-reset` 的当前最终态
- `git -C <工作区> rev-parse HEAD` = `43d82ff8018aa02b903bf78eaa24050cd7b624e4`（`43d82ff merge: pr-003 impact surface registration into iteration/0026`）
- `HEAD^{tree}` = `505cfdc2e4eefcca15cb42095036e7e937b07478`
- 基线 `ea8943e`（`main`，迭代开工前）；`git merge-base ea8943e HEAD` = `ea8943e`（分支自基线线性可追，三 PR 已全部合并）
- 工作区 `git status --porcelain` 为空（除 `.pb-agents/` 被 `.gitignore` 忽略），即**判定面 = HEAD 树 = 磁盘工作树**，无未提交残留

**验证标准来源**：主 agent 委托的三条（① `prd/F09-impact-surface-registration.md` 验收 3 / 验收 4；② `demand.md` §4 六条裸判定 + §7 V-1~V-6；③ 三个 PR 文件各自的验收标准）+ 委托给定的验证标准口径（终点态 = 三 PR 合并后迭代分支；阶段 5 产物不计入「改动面封闭」判据；判据编号以 `prd/*` 功能卡序号为准）

**验证日期**：2026-09-15

**独立性声明**：未接收任何执行过程上下文（未询问"怎么做的""为什么这么设计"）。判定只读产出物（HEAD 树 + 三 PR 文件 + 三张功能卡 + demand）+ 上列标准，并以 `git`/文本检索一手取证。以下每条判定都附可被第三人重放的命令与输出。`deferred-demand-changes.md` 存在性已单独核实：**该文件不存在**（`ls` 返回 No such file），故本报告无"搭置需求变更原文摘录"区块（工作流 §验证目标 该条款的触发条件不成立）。

---

## 标准 ①：F09 验收标准 3 与 4（本次核心对象）

### 9 行影响面逐行状态（F09 验收 1 的可核查性前置）

| # | 位置 | HEAD 实际状态 | 一手证据 |
|---|---|---|---|
| 1 | `roles/workflow-pb/workflow-pb.md:56` | 输出列已不含测试字样 | `git grep "通过验证标准的测试" HEAD` 在 `roles/` 下零命中；`sed -n '56p'` 读作 `… + 代码 \| 该 PR 验收标准全部通过；…` |
| 2 | `oamp/package.json` | `scripts` 容器整体不存在（唯一 key 即 `test`） | `git diff ea8943e HEAD -- oamp/package.json` 只删 `"scripts": {"test": "node --test test/*.test.js"}`；`grep -n '"test"' oamp/package.json` exit=1 |
| 3 | `oamp/README.md:195` | 「由 `npm test` 强制」已删，三条漂移锁事实描述保留 | `grep -n "npm test" oamp/README.md` exit=1；现读作 `- **漂移锁**：元数据必填 / 索引快照逐字节 / \`API.md\` 路径登记三条锁` |
| 3b | `oamp/README.md:101-102` | 自动化测试缩参句已删，env 表与"快速失败"句保留 | `grep -n "自动化测试" oamp/README.md` exit=1；`OAMP_TMUX_BIN` / `OAMP_CLUSTER_WAIT_MS` 两行与「数值类 env 一律要求正整数…」在场 |
| 4 | `workflow-pb.md:56` 同行推进条件 | 登记成立、零文件动作 | 新旧 `:56` 按 `\|` 切分 8 单元格逐格比对：cell 6（推进条件）**SAME（逐字节相同）** |
| 5 | `roles/verifier/verifier.md` | 登记成立、零文件动作 | 该路径不出现在 `git diff --name-status ea8943e HEAD` |
| 6 | `docs/iteration-time-analysis.md:236` | 行尾已加失效标注 | 见下 ①-C |
| 7 | `oamp/scripts/testenv.mjs` | 整个文件已删除 | `ls oamp/scripts/testenv.mjs` → No such file；`git ls-files oamp/scripts` → 只剩 `oamp/scripts/gen-llms-txt.mjs` |
| 8 | `oamp/src/cluster-config.js:19` | 注释原样在场、零文件动作、已登记 | 该文件不在 diff；`grep -n "hygiene" oamp/src/cluster-config.js` → `:19 … 避免被 test/hygiene.test.js 的…` |

9 行全部有可核查归属结论；F09 验收 1 判 **pass**。

### ①-A 验收 3 前半：可执行引用（路径指向 `oamp/test/**` 的 import / require / spawn）零命中 → **pass**

判据面：HEAD 树全部受版本控制文件，排除 `docs/iterations/**`（F09 验收 3 明示"历史迭代产物内的历史引文不在判据面内"，与 V-4 口径一致）。

```
$ git -C <工作区> grep -n -I -E "from ['\"][^'\"]*test/"            HEAD -- . ':(exclude)docs/iterations'   → ZERO
$ git -C <工作区> grep -n -I -E "require\(['\"][^'\"]*test/"        HEAD -- . ':(exclude)docs/iterations'   → ZERO
$ git -C <工作区> grep -n -I -E "spawn[A-Za-z]*\([^)]*test/"        HEAD -- . ':(exclude)docs/iterations'   → ZERO
$ git -C <工作区> grep -n -I -E "import\(['\"][^'\"]*test/"         HEAD -- . ':(exclude)docs/iterations'   → ZERO
$ git -C <工作区> grep -n -I -E "oamp/test"                         HEAD -- . ':(exclude)docs/iterations'
  → 唯一命中 docs/iteration-time-analysis.md:236（登记第 6 行，历史引文，非可执行）
$ git -C <工作区> grep -n -I -E "test/helpers|'\.\./test|\"\.\./test" HEAD -- . ':(exclude)docs/iterations' → ZERO
$ (ls oamp/test) → No such file or directory ; git ls-files oamp/test | wc -l → 0
$ git ls-files oamp/scripts → oamp/scripts/gen-llms-txt.mjs
```

补充：`git grep -n -E "test/"` 在该面内的 3 处命中经逐条判别为 ①`docs/iteration-time-analysis.md:236`（登记第 6 行）②`oamp/src/cluster-config.js:19`（登记第 8 行）③`docs/multi-omp-agent-protocol.md:593` 的 `a2a-protocol.org/latest/specification/`（`lates**t/spec**ification` 的子串误命中，与测试无关）。产码面（`oamp/src`、`oamp/bin`、`oamp/scripts`、`tools/`、`tests/`）零可执行引用。

### ①-B 验收 3 后半：注释类引用"允许存在，但必须逐条登记" → **partial**

**通过部分**：§5 第 8 行这一处注释类引用在场且已登记——`oamp/src/cluster-config.js:19`（「…避免被 `test/hygiene.test.js` 的源文件扫描…」），登记载体可读于 `prd/F09-….md` 验收 1 表第 8 行与 `prs/pr-003-….md` 验收 4 表第 8 行，处置为「只登记不处置（Q19）」，文件零改动。

**不通过部分**：判据面内存在**第 3 处 `.test.js` 引用未登记**：

| 位置 | 内容 | 为何是不通过 |
|---|---|---|
| `roles/architect/data/0013-chat-archive-arch-decision-notes.md:15` | 「…同时它天然满足两个硬约束：① 幂等…② 新库与迁移库同构…——**`persist.test.js` 的列名断言是顺序敏感的**，列序错了迁移库与新建库就过不了同一条断言）。」 | 该文件路径不在 `docs/iterations/**` 判据面之外，故在判据面内；`persist.test.js` = 本迭代删除的 `oamp/test/persist.test.js`（在 35 个 `D` 条目内）；该位置**不在 §5 九行清单内**，无处置结论登记 |

穷尽性证据（判据面内 `.test.js` 形态的引用**恰好 3 处**）：

```
$ git -C <工作区> grep -n -I -E "\.test\.js" HEAD -- . ':(exclude)docs/iterations'
  docs/iteration-time-analysis.md:236                       → 已登记（第 6 行）
  oamp/src/cluster-config.js:19                             → 已登记（第 8 行）
  roles/architect/data/0013-chat-archive-arch-decision-notes.md:15 → 未登记
```
（另有 `harness` / `fake-node` / `zero-intrusion` / `hygiene` 四类 basename 的补充扫描：`harness.js`、`fake-node`、`zero-intrusion` 零命中；`hygiene` 仅 `cluster-config.js:19` 一处，已登记。）

**根因**（供主 agent 决策用，不是我的处置动作）：§5 的扫描口径是 `test/` 子串，而 `persist.test.js` 不含 `test/` 子串，故第 10 处从扫描中逃逸；`demand.md` §3-F5 的「全仓引用测试的位置共 9 行」据此不成立为穷尽结论。

### ①-C 验收 2（伴生核对，F09 验收 2 的加注条款）→ **pass**

```
$ git -C <工作区> diff ea8943e HEAD -- docs/iteration-time-analysis.md
@@ -233,7 +233,7 @@
-- **测试执行本身不是瓶颈**：全量测试 `node --test oamp/test/*.test.js` 约 64 秒（0021 收口记录），相对 13 分钟的验收派发可忽略。
+- **测试执行本身不是瓶颈**：全量测试 `node --test oamp/test/*.test.js` 约 64 秒（0021 收口记录），相对 13 分钟的验收派发可忽略。（0026 加注：本结论已失效——0026 迭代（F01）清洗 `oamp/test/`，该全量套件不再存在。）
```
- 标注落在**原句同行行尾**（读者读到该句即看到）；点明**失效原因**（"0026 迭代（F01）清洗 `oamp/test/`，该全量套件不再存在"）与**生效迭代**（0026）；
- 该文档 diff 为**同一行的增补**（`1` 行修改，无新增行、无删除行）；原句文字与数值逐字保留（`node --test oamp/test/*.test.js`、`约 64 秒`、`0021 收口记录` 全在场）；该文件其余行零改动。

### ①-D 验收 4 前半：第 1/2/3/3b/6/7 行所在文件逐文件核对，动作与处置列一致 → **pass**

```
$ git -C <工作区> diff --name-status ea8943e HEAD | awk '{print $1}' | sort | uniq -c
     19 A
     35 D
      4 M
```
| 行 | 文件 | diff 条目 | 动作与处置列是否一致 |
|---|---|---|---|
| 1 | `roles/workflow-pb/workflow-pb.md` | `M` | 一致（`:56` 行内删 30 字节 = 「+ 通过验证标准的测试」） |
| 2 | `oamp/package.json` | `M` | 一致（移除 `scripts` 容器；该容器仅含 `test` 一个 key） |
| 3 / 3b | `oamp/README.md` | `M` | 一致（删两句，保留项逐字在场） |
| 6 | `docs/iteration-time-analysis.md` | `M` | 一致（1 行行尾加注） |
| 7 | `oamp/scripts/testenv.mjs` | `D` | 一致（整文件删除） |

5 个文件逐一有动作、动作形态与处置列（改 / 删）一致。

### ①-E 验收 4 后半：第 4/5/8 行不产生任何文件改动 → **pass**

- 第 4 行：新旧 `:56` 切分 8 单元格，**推进条件单元格逐字节相同**（`cell 6 SAME`）；整行字节数 `450 → 420`，差值 `30` 与被删片段「 + 通过验证标准的测试」的 UTF-8 字节数完全吻合（9 汉字 × 3 = 27，加两个空格与 `+` 共 3 = 30）——即该文件的唯一改动不落在推进条件单元格上。
- 第 5 行：`roles/verifier/verifier.md` 不出现在 `--name-status` 输出。
- 第 8 行：`oamp/src/cluster-config.js` 不出现在 `--name-status` 输出；其 `:19` 注释原样在场。

### ①-F 验收 4 的"无越界"外沿（口径内）→ **pass（附口径残余登记）**

全树 diff 的 58 个条目可分为三组加两类过程产物：

- **①清理组**：34 × `oamp/test/**`（`D`，F01 验收 1 的目标面）+ `oamp/package.json`（`M`）+ `oamp/README.md`（`M`）
- **②工作流口径组**：`roles/workflow-pb/workflow-pb.md`（`M`）
- **③影响面闭环组**：`docs/iteration-time-analysis.md`（`M`）
- **第 7 行**：`oamp/scripts/testenv.mjs`（`D`）
- **迭代过程产物**：19 × `A`，全部位于 `docs/iterations/0026-test-protocol-and-suite-reset/`，其中 7 份是委托明示排除的阶段 5 产物（3 × `prs/*-tasks.md` + 3 × `clarifications/verify-pr-00X-*.md` + 第 7 份 `clarifications/demand-0026-*.md`），另 12 份是阶段 1~4 过程产物（`demand.md` / `prd.md` / `prd/` 3 卡 / `architecture.md` / `status.md` / `progress.md` / `history.md` / 3 个 PR 文件）

判定：在「§5 引用面（`oamp/test/` 之外的引用位置）」这条口径线上，发生文件动作的**恰为 ① ② ③ 三组的 4 个文件 + 第 7 行的 1 个文件**，与 V-6「只出现 ① ② ③ 三组文件，无越界改动」一致；`oamp/src` / `oamp/web` / `oamp/bin` 零改动（F01 验收 5 成立）。**口径残余**（不构成越界，但判据文本未覆盖）记入偏差 D-2。

---

## 标准 ②：`demand.md` §4 六条裸判定 + §7 V-1~V-6

| 条目 | 判定 | 证据（可重放） |
|---|---|---|
| §4-1 `oamp/test/` 不存在、`git ls-files oamp/test` 零命中 | **pass** | `ls -d oamp/test` → No such file；`git ls-files oamp/test \| wc -l` → `0`；diff 中 34 个 `D` 条目全部为 `oamp/test/**` |
| §4-2 `oamp/package.json` 无 `scripts.test`、`grep -n '"test"'` 零命中 | **pass** | `grep -n '"test"' oamp/package.json` → exit=1；文件和基线比对后仅少 `scripts` 容器（`name`/`private`/`type`/`bin`/`engines`/`dependencies` 逐字不变，`dependencies` 仍 `{}`） |
| §4-3 README 不含「由 `npm test` 强制」与「自动化测试将 interval 缩到 30–100ms」 | **pass** | `grep -n "npm test" oamp/README.md` exit=1；`grep -n "自动化测试" oamp/README.md` exit=1 |
| §4-4 `grep -n "通过验证标准的测试" roles/workflow-pb/workflow-pb.md` 零命中 | **pass** | 该 grep exit=1；扩到 `roles/` + `oamp/` + `.claude/` 亦零命中 |
| §4-5 `docs/iteration-time-analysis.md:236` 附近可读到失效标注，点明原因与生效迭代 | **pass** | 见 ①-C |
| §4-6 §5 每行状态可读（"这一处现在是什么状态、为什么"） | **pass** | 见上文 9 行表：第 1/2/3/3b/6/7 行已有动作，第 4/5/8 行已登记为零动作，8 个编号行（含 3b 共 9 行）状态与理由均可读（载体：`prd/F09-….md` 验收 1 表 + `prs/pr-003-….md` 验收 4 表）。（该条自身措辞与 §5 现行行数不一致 → 偏差 D-1） |
| V-1 `ls oamp/test/` 与 `git ls-files oamp/test` | **pass** | 目录不存在；`git ls-files` 零命中 |
| V-2 `grep -n '"test"' oamp/package.json` 零命中 | **pass** | exit=1 |
| V-3 `grep -n "npm test"` 与 `grep -n "自动化测试"`（`oamp/README.md`）均零命中 | **pass** | 两条均 exit=1 |
| V-4 `grep -n "通过验证标准的测试" roles/workflow-pb/workflow-pb.md` 零命中 | **pass** | 零命中。全树 `git grep` 该短语的命中**全部**落在 `docs/iterations/**`（`0003` / `0011` / `0026` 三处历史与本次过程产物），与 V-4 排除口径（"历史澄清记录里的同名引用是历史引文"）一致；`roles/` 与 `.claude/` 面已清零 |
| V-5 读 `docs/iteration-time-analysis.md:236` 所在行 | **pass** | 见 ①-C |
| V-6 `git diff` 逐文件核对改动面（只出现 ①②③ 三组，无越界） | **pass** | 见 ①-F：判据面内恰好 ①（`oamp/test/**` + `package.json` + `README.md`）+ ②（`workflow-pb.md`）+ ③（`iteration-time-analysis.md`）；另 35 个 `D` 中的 `testenv.mjs` 属 F01 验收 6 / §5 第 7 行，19 个 `A` 为迭代过程产物（偏差 D-2） |

---

## 标准 ③：三个 PR 文件各自的验收标准

### pr-001（F01 · 存量测试资产清零）

| PR 验收条目 | 判定 | 证据 |
|---|---|---|
| 1 `oamp/test/` 不存在；`git ls-files` 零命中（原 34 个文件全消失，含 `helpers/harness.js`、`helpers/fake-node.js`） | **pass** | 目录不存在；`git ls-files oamp/test` → 0；diff 的 34 个 `D` 中含 `oamp/test/helpers/harness.js`、`oamp/test/helpers/fake-node.js` 与 32 个 `*.test.js` |
| 2 `package.json` 不含 `scripts.test`；其余字段逐字不变、`dependencies` 仍为空对象 | **pass** | `grep '"test"'` exit=1；diff 仅删 `scripts` 容器（该容器只含 `test` key，等价于"移除该 key"）；`dependencies: {}` 保留 |
| 3 README 两处零命中且保留项在场逐字不变 | **pass** | `grep "由 \`npm test\` 强制"` exit=1、`grep "自动化测试将 interval 缩到"` exit=1；`重新生成索引快照` 命令行、`OAMP_TMUX_BIN` / `OAMP_CLUSTER_WAIT_MS` env 表行、「数值类 env 一律要求正整数…快速失败」句均在场 |
| 4 `oamp/scripts/testenv.mjs` 不存在；`git ls-files oamp/scripts` 只剩 `gen-llms-txt.mjs` | **pass** | `ls` No such file；`git ls-files oamp/scripts` → `oamp/scripts/gen-llms-txt.mjs` |
| 5 未引入替代机制（无 CI 配置 / git hook / 替代 script / `oamp/test/` 占位文件） | **pass** | 无 `.github/`、无 `.githooks/`、无 `.husky/`；`package.json` 无任何 `scripts` 容器；`oamp/test/` 不存在（无占位）；diff 的 `A` 条目全部在 `docs/iterations/0026-*/` 下 |
| 6 `oamp/src`、`oamp/web`、`oamp/bin` 零改动 | **pass** | 三个目录下无任何路径出现在 `--name-status` 输出 |
| 7 本 PR 的 diff 只含 `package.json` / `README.md` / `testenv.mjs`（`oamp/test/**` 以删除条目核） | **pass**（迭代级读法） | 归因于 F01 的改动条目 = `M oamp/package.json` + `M oamp/README.md` + `D oamp/scripts/testenv.mjs` + 34 × `D oamp/test/**`；无第四条路径 |

### pr-002（F02 · 阶段 5 输出契约去掉测试产出）

| PR 验收条目 | 判定 | 证据 |
|---|---|---|
| 1 `grep -n "通过验证标准的测试" roles/workflow-pb/workflow-pb.md` 零命中；`:56` 输出列读作「`prs/pr-{NNN}-tasks.md`（该 PR 内部任务列表，供 dev 消费）+ 代码」 | **pass** | grep exit=1；`sed -n '56p'` 输出列逐字为「`prs/pr-{NNN}-tasks.md`（该 PR 内部任务列表，供 dev 消费）+ 代码」；该行 `grep -c '测试'` → `0` |
| 2 同行其余内容逐字不变（输出列仍含 `prs/pr-{NNN}-tasks.md`；推进条件列仍为「该 PR 验收标准全部通过；无简报外改动；对应 PR 文件存在（合并前置）」） | **pass** | 8 单元格逐格比对：除 cell 5（输出列，仅少被删短语）外全部 `SAME`；推进条件 cell 逐字节相同 |
| 3 `git diff roles/workflow-pb/workflow-pb.md` 只出现这一行 | **pass** | diff 为单 hunk、单行 `-`/`+`；无第 2 行改动、无新增/改写章节、版本号 `0.13.0` 未变 |
| 4 阶段 2~4、阶段 6 对应行的输出列与推进条件列零改动 | **pass** | 同 hunk 上下文（`:53-57`）显示阶段 2/3/4/6 行原样 |
| 5 `roles/dev/dev.md`、`roles/verifier/verifier.md`、`roles/workflow-pb/data/**`、`.claude/skills/workflow-pb/SKILL.md`、`docs/iterations/**`（历史引文）零改动 | **pass** | 四个路径面均不在 `--name-status` 输出；`.claude/skills/workflow-pb/SKILL.md` 对 `测试\|test` 的命中数为 `0` |
| F02 卡验收 3：与 `roles/dev/dev.md:107` / `:110` 不再冲突 | **pass** | `dev.md:107`「不写测试用例（除非简报明确要求）」、`:110`「不派发子 agent、不跑全量测试套件…」；工作流行输出列已不含测试产出要求，两侧不再互斥 |

### pr-003（F09 · 影响面闭环登记 + 时延分析失效加注）

| PR 验收条目 | 判定 | 证据 |
|---|---|---|
| 1 `:236` 行尾存在失效标注，点明失效原因与生效迭代 | **pass** | 见 ①-C（"0026 迭代（F01）清洗 `oamp/test/`，该全量套件不再存在"；生效迭代 0026） |
| 2 该行原有分析文字与数值逐字保留；该文档其余行零改动 | **pass** | diff 为 1 行 `-`/`+`，删除侧文字与新增侧前缀逐字相同；`node --test oamp/test/*.test.js`、`约 64 秒`、`0021 收口记录` 全在场；`:234-236` 邻域 bullet 无其他改动 |
| 3 `git diff --name-only` 只含 `docs/iteration-time-analysis.md`；该文件 diff 为同一行增补 | **pass**（迭代级读法） | 该文件的 diff 全貌即上文 ①-C（单行增补）；迭代合并态下该文件确为唯一由本 PR 贡献改动的文件（另 3 个 `M` 归 F01/F02） |
| 4 §5 九行每行有可核查归属结论，且与 `prd/*.md` 及三 PR 文件互不冲突 | **pass** | pr-003 验收 4 表、`prd/F09-….md` 验收 1 表、三 PR 文件的归属声明三方一致；仅编号表述差异（pr-003 写「第 7 行归 pr-001 验收 4（= F01 卡验收 6）」，F01 卡该项编号为验收 6——该 PR 文件已显式给出等式，不构成冲突） |
| 5 本 PR 不新增文件、不为其他历史文档追加失效标注、不承载协议内容 | **pass**（口径内） | 归因于 pr-003 的改动面为零新增文件（该 PR 的 `prs/pr-003-tasks.md` 与验证报告属阶段 5 过程产物，按委托口径不计入）；`docs/iteration-time-analysis.md` 之外无其他历史文档被加注；无替代卡 / 占位文件 |

### 三 PR 整体一致性意见 → **pass（附 1 条编号表述差异，见下）**

- 判据归属无冲突：跨 PR 判据（F09 验收 3 / 验收 4）在 `prs/pr-003-….md` 中被显式声明为"迭代级收口核对，判定面跨越三个 PR 合并后的仓库状态 ⇒ 归阶段 6 端到端验证"，与本次委托对这两条的指定一致；pr-001 / pr-002 未重复声明这些跨 PR 判据。
- 文件范围互不重叠：三 PR 的文件路径集合两两无交集（`oamp/**` / `roles/workflow-pb/workflow-pb.md` / `docs/iteration-time-analysis.md`），合并后无相互覆盖的痕迹（三者的改动在 HEAD 上均可独立辨识）。
- 与上游卡面一致：F01 验收 6（`testenv.mjs`）与 F02 验收 1~4 在 PR 文件中逐条对应；F09 验收 1~4 在 pr-003 中对应到 5 条 PR 自身判据 + 跨 PR 移交声明。
- 唯一表述差异：pr-003 用「pr-001 验收 4」指代 F01 卡验收 6，同表已标明等式，判定为口径表述差异而非判据冲突（不构成 fail）。

---

## 汇总

- **pass：34 项**
- **fail：0 项（无需返工）**
- **partial：1 项**（①-B：`prd/F09-….md` 验收 3 后半「注释类引用逐条登记」——第 8 行已登记，另有 1 处在判据面内未登记）
- **blocked：0 项**

其中本次核心对象的最终判定：

| 核心对象 | 判定 |
|---|---|
| F09 验收 3（可执行引用零命中 **+** 注释类引用逐条登记） | 前半 **pass** / 后半 **partial** ⇒ 整条 **partial** |
| F09 验收 4（改动面与 §5 处置列一致） | **pass**（4-a / 4-b / 4-c 三个子项全通过） |

主 agent 可据此直接决策：**无 fail，迭代可按流程关闭**；F09 验收 3 的 partial 由偏差记录 D-3 承载，是否补登记或开新迭代由主 agent 决定（偏差记录不阻塞本次交付）。

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定（不改 pass/fail），但应同步文档或评估是否调整实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | `demand.md` §4-6：「面对 §5 的**全部 6 行**，任何人都能说出…***第 1/2/3/3b 处已改，第 4/5 处标注为随协议延期，第 6 处已加注**」 | §5 现行清单为 **9 行**（Q18/Q19 追加第 7/8 行后）；该条计数与列举未覆盖第 7 行（已删文件）与第 8 行（只登记），"全部 6 行"的行数与"7 项列举"亦自相矛盾 | 按实现更新文档：把该条改写为 9 行口径（或在 §5 表头注明该条写于 Q17 之前）。属 `demand.md` 的内部一致性，不影响判据成立 |
| D-2 | 委托口径：**阶段 5 产出**（`prs/*-tasks.md`、`clarifications/verify-pr-00X-*.md`）不计入「不新增文件 / 改动面封闭」判据面 | 合并态实际新增 **19 个**文件，其中 7 份属阶段 5 产物（明示排除），另 **12 份**属阶段 1~4 过程产物（`demand.md` / `prd.md` / `prd/*` 3 卡 / `architecture.md` / `status.md` / `progress.md` / `history.md` / 3 个 PR 文件），口径未明示是否排除 | 按实现/既有口径澄清：F09 验收 3 与 V-4 已有"历史与迭代产物内的引文不在判据面内"的同类排除，建议把口径一次写全（迭代过程产物整体排除），避免下一位验证者重做同一判断 |
| D-3 | `demand.md` §3-F5 与 §5「`oamp/test/` 之外，**全仓引用测试的位置共 9 行**」（穷尽性结论）；F09 验收 3「注释类引用…必须逐条登记」 | 判据面内另有第 10 处引用未登记：`roles/architect/data/0013-chat-archive-arch-decision-notes.md:15` 指向已删除的 `oamp/test/persist.test.js`（列名断言顺序敏感）。根因是 §5 的扫描口径为 `test/` 子串，`persist.test.js` 不含该子串 | 二选一：①按 Q19 的同类处置**补登记**该行为「既有遗留、只登记不处置」（或就近加注）；②若认为 `roles/*/data/` 历史决策笔记不在清理面内，则在 §5 表头显式声明扫描口径与排除面（`docs/iterations/**` + 历史决策笔记）。本项为 ①-B partial 的唯一成因 |
| D-4 | `prd.md` 疑问/越界 7：「`demand.md` §5 第 7/8 行已按 Q18/Q19 书写，但同文件 **§4.4 决策表仍止于 Q17**、**文档头仍自述 v1.1.0**」 | `HEAD` 的 `demand.md` 已是 **v1.2.0**，§4.4 标题为「方案确认门处的范围裁决（**Q15–Q19**）」并含 Q18/Q19 两行；§4 标题计数为 19 项 | 按实现更新文档：`prd.md` 该条登记已过时，可标注为"已闭合"（`demand.md` 已修订），避免后续读者据此判断存在上游缺口 |
| D-5 | `prd/F01-….md` 验收 2 / `prs/pr-001-….md` 验收 2：措辞为「不含 `scripts.test` **这个 key**（是整体移除，不是把它改指向空套件）」/「移除 `scripts.test` 一个 key」 | 实现为**整体移除 `scripts` 容器**（该容器内只有 `test` 一个 key，故结果等价）；commit `97b5c63` 的说明亦为"整块移除 scripts 容器" | 无需改实现（结果与验收等价）；建议在文档侧统一为「移除 `scripts` 容器（其中唯一条目为 `test`）」，以免后续重建脚本时对"容器是否保留"产生歧义 |

**偏差计数：5 条**。

---

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制。不在当前验收标准范围内，供主 agent 与 planner 决策是否纳入下一迭代。

1. **`roles/architect/data/0013-….md:15` 的 `persist.test.js` 引用**：不只是"少登记一行"——它证明 §5 的穷尽性结论建立在单一子串扫描口径上。若协议迭代要重建"引用面盘点"机制，建议口径定为「`test/` 子串 + `.test.js` 后缀 + 已知 basename 三路交叉」，并把排除面显式写死（D-3 的根因）。
2. **顶层 `tests/*.sh`（`test-check-role-structure.sh` / `test-check-model-dispatch-protocol.sh`）仍在**：本迭代"零测试交付"的口径实际只覆盖 `oamp/test/**`，仓库级并非零测试态。协议迭代若要定义"何时、是否重建测试"，需要先回答这两个脚本属于哪一类（保留 / 迁移 / 同批清零）。
3. **`oamp/README.md` env 表的「测试注入用 / 测试用」注解失去消费者**：`OAMP_TMUX_BIN`(`:98`)、`OAMP_CLUSTER_WAIT_MS`(`:99`)、`OAMP_OMP_BIN`(`:95`)、`OAMP_SOCKET`(`:79`) 等注解描述的是"测试注入"这一用途，而唯一自动消费者（套件）已清零。F01 验收 3 明确要求这些行**保留不变**，故本迭代无错；但协议迭代可评估措辞（改为"手测/并行调试用"）或保留原样。
4. **`oamp/scripts/testenv.mjs` 删除后仓库失去唯一人工端到端冒烟工具**（`demand.md` §4.4 Q18 已登记代价：删 harness 后该脚本本已不可运行）。协议迭代的"保留集重建触发条件"应显式回答是否需要等价物，否则零回归保护窗口（§6.1-1）会与"人工冒烟"一起消失。
5. **阶段 5 推进条件「该 PR 验收标准全部通过」的实际判据样本**：本迭代在零测试态下，三条 PR 的判据全部由「git 面 + 文本检索 + 逐字节比对」承载（可见三份 `clarifications/verify-pr-00X-*.md` 与 `prs/pr-002-tasks.md` 的 md5 期望值）。这三个 PR 的验收写法是协议迭代定义"零测试态验收与取证口径"（Q13）时最直接的一手样本，建议随包传递而不必重做侦察。
6. **`docs/iteration-time-analysis.md` 的其余"测试"表述**：`:236` 已加注，但该文档其他位置（如耗时归因、迭代对比表）不涉及具体测试资产，故按 F09 边界未加注——本轮无问题，仅登记为"本迭代的加注面就是同一行"这一事实，供后续同类审计参考。

---

## 结论

**PASS**

- 所有条目均有明确判定：pass 34 项、partial 1 项、fail 0 项、blocked 0 项；
- 本次核心对象：F09 **验收 3 = partial**（可执行引用零命中子项通过；注释类引用逐条登记子项有 1 处未登记，见 D-3）；F09 **验收 4 = pass**（五文件动作一致 + 第 4/5/8 行零动作 + 口径内无越界）；
- 偏差记录 5 条（D-1~D-5），不阻塞交付；其中 D-3 是 partial 的成因，建议主 agent 明确"补登记 / 改口径"二选一后再决定是否开新迭代；
- `deferred-demand-changes.md` 不存在，无需求变更搭置内容需透传。

（本报告不修改任何被验证产物；报告写入时间戳：2026-09-15 18:29:46，未提交。）
