# 澄清记录 — 0026 阶段 1（测试协议与套件清零）

**caller**: demand（主 agent 内联执行）
**轮次**: 5 轮，共 14 个决策点
**时间**: 2026-09-15 17:0x–17:2x
**来源图例**: `user_confirmed` / `实测` / `仓库事实` / `user_deferred`（用户明示后置）

---

## 起点前提核验（提问前完成，`实测`）

用户原始表述含三条论断，逐条核验后**一条量级需修正**：

| # | 用户论断 | 核验结果 | 证据 |
|---|---|---|---|
| 1 | 大头时间耗在 bash（21.6m）跑 node --test 与一次性脚本 | **部分成立**：bash 22.9m 成立，但其中全量套件约 10–12 分钟（占迭代 165 分钟的 6–7%），另一半是 28 处 `/tmp/*.mjs` 一次性脚本 | `~/.omp/agent/sessions/-projects-agents/2026-09-15T05-40-16-968Z_*/` 16 份子 agent 流水逐回合 tool-residual 归集 |
| 2 | 没有测试的标准协议定义 | **成立** | 全仓 `grep 测试协议/测试规范/test-protocol` 零命中 |
| 3 | test 脚本会无序扩张，往后时间越来越长 | **成立，且边际在加速** | 0016 251 测试/55.7s → 0018 285/57.1s → 0021 333/61.1s → 0024 387/68.5s；每测试边际成本 0.04s → 0.09s → 0.14s |

**提问前补充实测（改变问题定位）**：

- 套件墙钟由**最慢的单文件**决定：`oamp/test/web.test.js` 单文件 61.0s（39 用例 / 1663 行），占全量墙钟 89%；排除它后全量 31.7s
- 单文件独立跑合计 273s，一次全量 68.5s ⇒ `node --test` 按文件并行、文件内串行
- 60s 是 bash 工具前台上限；套件跨越该线始于 0020/0021 之间，已 4 个迭代；越线后单次全量实际成本 85–95s
- 382 次 bash 中 277 次用默认 `timeout`；15 次全量调用仅 4 次显式设值 ⇒ 运行策略全靠临场发挥
- 测试 14986 行 vs 生产代码 10393 行（测试多 44%）
- 生产代码零引用 `oamp/test/`（`oamp/src` `oamp/web` `oamp/bin` 全仓 grep 无命中）
- 三份文档对"测试该不该落盘"给出三种口径：`workflow-pb.md:56` 要求 PR 产出含测试；`roles/dev/dev.md:107/110` 禁止 dev 写测试与跑全量；`roles/verifier/verifier.md` 全文未提

---

## 第 1 轮（Q1–Q5）

| # | 决策点 | 主 agent 推荐 | 用户裁决 | 来源 |
|---|---|---|---|---|
| Q1 | 本次迭代边界 | A 只写协议 + 存量不追溯 | **本次不修改协议**（后经 Q7 收窄） | `user_confirmed` |
| Q2 | 运行粒度 | B 两档：快档 ≤15s + 全量 | **只跑本 PR 生成的新测试用例；全量只在迭代交付时跑** | `user_confirmed` |
| Q3 | 单文件粒度上限 | A 设单文件耗时上限 + 越限拆文件 | **不硬性限制；理论上应只保留必须的测试用例** | `user_confirmed` |
| Q4 | 测试归属 | B 实现 PR 可落测试 | **测试用例应有单独的服务维护；默认不落测试用例；PR 内产生的测完即清除** | `user_confirmed` |
| Q5 | 一次性验收脚本处置 | B 默认落盘 + 白名单豁免 | **临时脚本跑完即删** | `user_confirmed` |
| — | 额外（用户主动提出） | — | **已有测试用例先全删，后续定完协议再考虑如何新建** | `user_confirmed` |

## 第 2 轮（Q6–Q10）

| # | 决策点 | 主 agent 推荐 | 用户裁决 | 来源 |
|---|---|---|---|---|
| Q6 | 迭代容器 | A 新开 0026，0025 照跑 | **新开 0026** | `user_confirmed` |
| Q7 | 协议边界 | A 只改阶段 5 输出契约那一行 | **按推荐来** | `user_confirmed` |
| Q8 | 存量测试处置 | C 按「跨迭代契约意义」筛选 | **A 存量测试全部清理**（未采纳推荐） | `user_confirmed` |
| Q9 | 顺序与时点 | A 先定协议后清理 | **B 先清理 → 后定协议**（未采纳推荐） | `user_confirmed` |
| Q10 | 维护主体 | A 新增横切角色 | **A，但本次先不做** | `user_confirmed` |

## 第 3 轮（Q11–Q14）

| # | 决策点 | 主 agent 推荐 | 用户裁决 | 来源 |
|---|---|---|---|---|
| Q11 | 协议承载形态 | A `roles/workflow-pb/data/test-protocol.md` | **不重要，之后（撰写协议时）再定** | `user_deferred` |
| Q12 | 删除边界 | A 整目录 + package.json + README 三处全收 | **A** | `user_confirmed` |
| Q13 | 协议是否覆盖零测试态的验收判定口径 | A 包含 | **A** | `user_confirmed` |
| Q14 | 保留集重建触发 | A 零测试 + 协议写死触发条件 | **A** | `user_confirmed` |

---

## 追问反射记录（关键决策，非省略）

三个被标记为关键决策的决策点（推翻会连带下游产物大改），逐条留痕：

**Q8（存量清零 vs 筛选）——追问反射：2 个角度**
1. 「机械锁类（`zero-intrusion.test.js` 326ms / `hygiene.test.js` 88ms / `cli.test.js` 269ms）断言的是架构不变量而非行为，占全量墙钟 1%，删它们省不下时间却失去唯一的架构违规自动检出」——用户裁决仍选全清，理由未展开
2. 「按判据筛保留集的判定成本 = 一次人工判定；清零点成本 = 0」——用户选清零
→ 反射后仍有可问角度，用户已明确裁决，按裁决记录

**Q9（顺序）——追问反射：1 个角度**
1. 「先删后定协议 ⇒ 从删除到协议落地之间零回归保护；窗口起点正好卡在 0025 的 7 个排队 PR 上（含 5 个测试 PR）」
→ 用户裁决 B（先清理后定协议），且 Q6 已同时选择「0025 照跑完」；两条后果已登记入 `demand.md` §6

**Q14（重建触发）——追问反射：1 个角度**
1. 「Q10 裁决 test-keeper 本次不做 ⇒ 保留集无维护主体；无触发条件则永久冻结在空集」
→ 用户裁决 A（协议内写死触发条件），角度已消解

---

## 一处主 agent 判断错误的事后更正（`实测`）

**第 2 轮 Q7 的代价估计偏小**：主 agent 当时告知用户"只改一行"。逐处核对后，删光测试会令 **6 处**措辞悬空，其中 3 处必须改文件：

| # | 位置 | 删光后状态 |
|---|---|---|
| 1 | `roles/workflow-pb/workflow-pb.md:56` 输出契约 | 须改（用户已裁决） |
| 2 | `oamp/package.json:13` `"test"` script | 指向不存在路径，`npm test` 报错 |
| 3 | `oamp/README.md:195` 「漂移锁…由 `npm test` 强制」 | 对外承诺失效 |
| 4 | `workflow-pb.md:56` 同行推进条件「验收标准全部通过」 | 无机械判据 |
| 5 | `roles/verifier/verifier.md` | 阶段 6 取证手段减少，全文无对应条款 |
| 6 | `docs/iteration-time-analysis.md:236` 「测试执行本身不是瓶颈」 | 历史结论失效，需标注 |

该更正已在第 3 轮开场显式呈现给用户，并据此生成 Q12（删除边界）。
