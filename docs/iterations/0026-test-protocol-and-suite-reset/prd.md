# prd.md — 0026-test-protocol-and-suite-reset

**版本**: 0.3.0
**迭代**: 0026-test-protocol-and-suite-reset
**创建日期**: 2026-09-15
**阶段**: 功能规格（阶段 2）
**输入合同**: `docs/iterations/0026-test-protocol-and-suite-reset/demand.md`（文档头自述 **v1.1.0**；§5 影响面 9 行清单与 W-1 已落 Q18 / Q19 裁决内容，§4.4 决策表仍止于 Q17——上游登记缺口见「疑问/越界」7；v1.0.0 见 `21f7bea`）
**本版修订（0.3.0）**: 阶段 3 全仓核实（`grep` 覆盖面扩到 `oamp/scripts/`）另发现两处指向 `oamp/test/` 的结构性引用，用户在方案确认门追加裁决 Q18（`oamp/scripts/testenv.mjs` 是可执行 import → 删除整个文件）/ Q19（`oamp/src/cluster-config.js:19` 是注释 → 只登记不处置）→ F01 处置面 +1（验收标准 6），F09 影响面由 7 行扩为 **9 行**、验收标准 3 按「可执行引用零命中 / 注释类引用逐条登记」两类收窄。卡数不变（**3 张**），无新增功能点。
**0.2.0 修订（留档）**: 用户在阶段 3→4 方案确认门裁决 Q15「此次不处理任何测试协议的事情」→ 原 W-3（产出测试协议）整块移出本迭代。卡面由 9 张收缩为 **3 张**（删 F03~F08；F01 处置面按 Q17 扩为 `oamp/README.md` 两处；F09 按重编号后的 W-3 与 7 行影响面清单改写）。v0.1.0 见 `02fcc33`，其中六张协议卡及其架构维度见 `02fcc33` / `a0fa71c`。

**说明**：本迭代改动的不是业务功能，而是"测试资产的有无"与"工作流 / 文档文本"。因此本迭代的"用户可见行为"落在两处可核查的面：①仓库状态（某个目录、某个 key、某句话是否存在）；②工作流与文档文本的内容（是否还含已被推翻的产出要求、失效结论是否就近标注）。每张卡的验收标准都可由读文件、跑检索、看 `git diff` 直接核查，不需要运行程序或做交互测试。

---

## 功能点索引

| ID | 功能名 | 用户价值摘要 | 规格卡 | 来源条款 | 架构维度 |
|---|---|---|---|---|---|
| F01 | 存量测试资产清零（含 `oamp/scripts/testenv.mjs` 整文件、`oamp/README.md` 两处失效声明） | 仓库里不再有"看起来能跑"的测试入口（`npm test` 与最小测试环境脚本一并消失），也不再有指向它的失效承诺，派发里不再重付全量套件的固定成本 | [F01](prd/F01-test-assets-zeroing.md) | 做什么 W-1（五条动作）；Q8 / Q12 / Q17 / Q18；§5 第 2/3/3b/7 行 | 无待填项 |
| F02 | 阶段 5 输出契约去掉测试产出 | 工作流口径与"PR 不落测试"一致，执行角色不再临场取舍 | [F02](prd/F02-stage5-output-contract-drop-tests.md) | 做什么 W-2；Q7；§5 第 1 行 | 无待填项 |
| F09 | 影响面闭环登记（9 行引用全部有归属） | 删测试不留悬空承诺——9 行影响面全部有归属（4 改 / 1 删文件 / 2 登记随协议延期 / 1 登记既有遗留 / 1 加注），每处引用都有归宿 | [F09](prd/F09-impact-surface-registration.md) | 做什么 W-3；§5 九行清单（含 Q18 / Q19 两处追加）；V-1 / V-2 / V-3 / V-4 / V-5 | 无待填项 |

**合计**：**3 张功能卡**。

**覆盖关系（双向可追溯）**：

| demand.md「做什么」 | 对应卡片 |
|---|---|
| W-1 清理存量测试资产 | F01 |
| W-2 改工作流阶段 5 输出契约一行 | F02 |
| W-3 登记 9 行影响面并闭合（原 W-4 重编号） | F09 |

原 W-3（产出测试协议）已整块移出本迭代（Q15），其承载的 F03~F08 六张卡一并删除，**不产生替代卡、替代说明或占位文件**；本迭代无 demand.md 之外的新增功能点。

---

## 本次迭代边界说明

### 包含

- 清零 `oamp/test/`（含 `helpers/`）、`oamp/package.json` 的 `scripts.test`、`oamp/README.md` 两处已失效的测试声明（`:195`「漂移锁…由 `npm test` 强制」整句；「自动化测试将 interval 缩到 30–100ms…」整句）、`oamp/scripts/testenv.mjs` 整个文件（其 `:11` 的 import 依赖 `test/helpers/harness.js`，删 harness 后 import 阶段即报错）（F01）。
- 工作流阶段 5 输出契约去掉「+ 通过验证标准的测试」（F02）。
- §5 全部 9 行影响面的归属登记与闭合：第 1/2/3/3b/7 行发生文件动作、第 4/5 行登记为随协议延期、第 8 行登记为既有遗留（只登记不处置）、第 6 行就近加注（F09）。

### 不包含（对应 demand.md §2 与 §7，作为各卡边界项落实，不单独拆保证项卡）

| 不做 | 承载位置 |
|---|---|
| 不产出测试协议（整块，含其承载形态） | F01 边界、F02 边界、F09 边界（Q15） |
| 不新增任何测试用例（零测试交付） | F01 边界 |
| 不新增 test-keeper 角色 | F01 边界（Q10） |
| 不改任何角色文件（`roles/dev/`、`roles/verifier/`） | F02 边界、F09 验收 1 第 5 行 |
| 不改任何生产代码（`oamp/src` `oamp/web` `oamp/bin`） | F01 验收 5 / 边界 |
| 不引入 CI / git hook / 第三方依赖 | F01 验收 4 |
| 不处置 §5 第 4/5 处（随协议延期）与第 8 处（注释，只登记不处置） | F09 验收 1 第 4/5/8 行、F09 边界 |
| 不升任何版本号、不写变更历史条目 | 迭代级（Q16），非功能卡边界 |
| 不回改 0025 | F09 边界 |

### 已登记的被否决路径（demand.md §6）不得作为功能点回填

1. 按「跨迭代契约意义」筛选保留 → 被 Q8 否决，F01 边界；
2. 先定协议后清理 → 随协议移出而失效，见下方「顺序约束」；
3. 保留 `helpers/` 脚手架 → 被 Q12 否决，F01 边界；
4. 协议不含验收口径 → 被 Q13 否决（该口径随协议整块延期）；
5. 保留集不写触发条件 → 被 Q14 否决（同上）；
6. `oamp/README.md:101-102` 只登记不处置 → 被 Q17 否决，F01 验收 3；
7. 实现 PR 可落测试 → 被 Q4 否决（随协议整块延期，本迭代不承载）。

### 顺序约束（demand.md §3）

三组文件面——① 清理、② 工作流口径、③ 影响面闭环——之间**无强制顺序**：Q9 的「先清理 → 后定协议」随协议移出本迭代而失效。③ 的判据读 ① 与 ② 的产物状态，故 ③ 排在 ① 与 ② 之后更省事，但这是执行便利，不是依赖；具体切分与依赖边归阶段 4。顺序本身不是功能点。

### 交付前置条件（demand.md §6.1）

原「清理 + 协议必须同迭代闭合」的前置已随协议移出而不成立。本迭代的显式空缺改为 `demand.md` §6.1 登记的三处：零回归保护窗口、阶段 5 失去方便判据、保留集空集无出口——三者均依赖后续的协议迭代闭合，本迭代无对应动作，不构成阻塞。

---

## 架构待填汇总（交阶段 3）

本迭代无架构待填项。

F01（整目录删除 + 两处 README 文本行移除）、F02（一行输出契约文本改动）、F09（登记与就近加注）均不涉及技术选型 / 数据模型 / 接口设计，故本次修订后不存在任何 `[架构待填]` 标记。

---

## model_inferred 汇总

| 卡片 | 推导内容 | 推导依据 | 状态 |
|---|---|---|---|
| F09 | 第 6 处的「加注失效说明」是本迭代交付物（而非仅登记在案） | §5 第 6 行「处置」列写的是"加注失效说明"，但 W-4 表述为"登记…处置随 W-3 一并落定"，未明说动作时点 | **已由主 agent 确认生效** |

（v0.1.0 的 F04 / F06 两项 model_inferred 随卡出局，本版不再列出。）

---

## 疑问/越界

1. **§5 表头措辞张力（已解决）**：原表头写「其中 **3 处必须改文件**」，与第 6 行处置「加注失效说明」本身也是文件改动相冲突。该张力已由主 agent 勘误解决——表头已更正为「共 9 行、其中 6 行有文件动作（第 1/2/3/3b/6/7 行），另 3 行只登记、无文件动作（第 4/5 行随协议延期；第 8 行为既有遗留）」并新增标号 `3b` 行。表头计数口径本身仍被挤压：第 2/3 行语义上是"删"、被并入"改"这一类——本卡按 demand 的字面分类登记，不改表头口径；本版 F09 验收 1 的表格按 9 行登记。
2. **`SKILL.md` 未纳入改动面**：实测 `.claude/skills/workflow-pb/SKILL.md` 全文不含"测试"字样（全仓检索零命中），因此不存在需要与 `workflow-pb.md:56` 同步的第二份拷贝；F02 未擅自扩大改动面。
3. **§7 显式后置项未写卡**：协议内容与其承载形态一并后置（`user_deferred`，Q15），不构成功能点；本阶段不为它出卡，也不为被移出的六张卡补任何替代物。
4. **本次范围修订的来源（登记）**：本版卡面收缩不由本阶段推断，而是用户在阶段 3→4 方案确认门处的范围裁决——Q15「此次不处理任何测试协议的事情」（原 W-3 整块移出）、Q16（不留痕）、Q17（`oamp/README.md:101-102` 纳入 F01 处置面）。0.3.0 的追加裁决 Q18（删 `oamp/scripts/testenv.mjs` 整文件）/ Q19（`oamp/src/cluster-config.js:19` 只登记不处置）同为用户裁决而非本阶段推断，落在 F01 验收 6 与 F09 第 8 行 / 验收 3。修订前的 9 张卡与协议承载形态决策见 `02fcc33` / `a0fa71c`，可从 git 恢复，本阶段不留副本。
5. **判据引用口径的对齐（登记）**：`demand.md` §5 第 3b 行与主工作区一手核对对该句均记为 `oamp/README.md:101-102`（此前版本记作 `:100-101`，已随上游同步消除；判据以引文为准，F01 验收 3 已注明）；另按 `demand.md` §7 现行编号，README 两处检索为 V-3、`workflow-pb.md` 那一处为 V-4、`:236` 加注为 V-5，F02 / F09 的判据引用已对齐至此。两处均为引用口径对齐，不涉及功能点增删。
6. 除上述外未发现 demand.md 内部矛盾或不清晰之处：W-1 / W-2 / W-3 全部有对应卡片，无 demand.md 之外的新增功能点，9 行影响面全部有归属。
7. **上游登记缺口（只登记，不代改）**：`demand.md` §5 第 7/8 行与 W-1 已按 Q18 / Q19 裁决书写，但同文件 §4.4 决策表仍止于 Q17、文档头仍自述 v1.1.0（正文「决策记录（17 项）」的计数同样未含这两项）。本版卡面按 §5 / W-1 的实际内容同步（Q18 → F01 验收 6 与来源行；Q19 → F09 第 8 行、验收 3 与边界），**不代改 `demand.md`**；该缺口请主 agent 处置。
