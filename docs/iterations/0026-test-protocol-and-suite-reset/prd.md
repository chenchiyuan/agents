# prd.md — 0026-test-protocol-and-suite-reset

**版本**: 0.1.0
**迭代**: 0026-test-protocol-and-suite-reset
**创建日期**: 2026-09-15
**阶段**: 功能规格（阶段 2）
**输入合同**: `docs/iterations/0026-test-protocol-and-suite-reset/demand.md`（v1.0.0，14 项决策全 `user_confirmed`）

**说明**：本迭代改动的不是业务功能，而是"测试资产的有无"与"工作流 / 协议文本"。因此本迭代的"用户可见行为"落在两处可核查的面：①仓库状态（某个目录、某个 key、某句话是否存在）；②协议与工作流文本的内容（是否包含某条规则、每条约束是否带判断方式）。每张卡的验收标准都可由读文件、跑检索、看 `git diff` 直接核查，不需要运行程序或做交互测试。

---

## 功能点索引

| ID | 功能名 | 用户价值摘要 | 规格卡 | 来源条款 | 架构维度 |
|---|---|---|---|---|---|
| F01 | 存量测试资产清零 | 仓库里不再有"看起来能跑"的测试入口，派发里也不再重付全量套件的固定成本 | [F01](prd/F01-test-assets-zeroing.md) | 做什么 W-1；Q8 / Q12；§5 第 2/3 行 | 无待填项 |
| F02 | 阶段 5 输出契约去掉测试产出 | 工作流口径与"PR 不落测试"一致，执行角色不再临场取舍 | [F02](prd/F02-stage5-output-contract-drop-tests.md) | 做什么 W-2；Q7；§5 第 1 行 | 无待填项 |
| F03 | 协议·运行粒度规则 | 每次派发只跑本 PR 的用例，全量只在迭代交付时跑 | [F03](prd/F03-protocol-run-granularity.md) | 做什么 W-3 第 1 条；Q2 | `[架构待填]`（协议承载形态） |
| F04 | 协议·测试归属与一次性脚本处置 | 用例与一次性脚本不回流成仓库资产，跑完即清 | [F04](prd/F04-protocol-test-ownership-and-throwaway-scripts.md) | 做什么 W-3 第 2/3 条；Q4 / Q5 | `[架构待填]`（协议承载形态） |
| F05 | 协议·阶段 5 零测试态验收判据 | 没有套件可跑时验收仍可判定，不降级成主观感觉 | [F05](prd/F05-protocol-zero-test-stage5-verdict.md) | 做什么 W-3 第 4 条前半；Q13；§5 第 4 行 | `[架构待填]`（协议承载形态） |
| F06 | 协议·阶段 6 零测试态取证口径 | "本次无跨迭代回归证据"被显式写进报告，不静默消失 | [F06](prd/F06-protocol-zero-test-stage6-evidence.md) | 做什么 W-3 第 4 条后半；Q13；§5 第 5 行 | `[架构待填]`（协议承载形态） |
| F07 | 协议·保留集重建触发条件 | 空集状态有明确出口，回归保护不会永久缺席 | [F07](prd/F07-protocol-retention-set-rebuild-trigger.md) | 做什么 W-3 第 5 条；Q14 | `[架构待填]`（协议承载形态） |
| F08 | 协议·约束可独立裁决（每条附判断方式） | 没参与讨论的人也能判定某次派发是否违规，不问作者 | [F08](prd/F08-protocol-judgeable-constraints.md) | §3「大概怎么做」协议写法；§4 第 3/4 条；V-4 | `[架构待填]`（协议承载形态） |
| F09 | 影响面闭环登记（6 处引用全部有归属） | 删测试不留悬空承诺，每处引用都有归宿 | [F09](prd/F09-impact-surface-registration.md) | 做什么 W-4；§5 六处清单；V-1 / V-2 / V-3 | 无待填项 |

**合计**：**9 张功能卡**。

**覆盖关系（双向可追溯）**：

| demand.md「做什么」 | 对应卡片 |
|---|---|
| W-1 清理存量测试 | F01 |
| W-2 改工作流阶段 5 输出契约一行 | F02 |
| W-3 产出测试协议（5 条内容） | F03（第 1 条）/ F04（第 2、3 条）/ F05（第 4 条前半）/ F06（第 4 条后半）/ F07（第 5 条） |
| W-4 登记 6 处影响面 | F09 |

F08 不对应某一个 W 条目，它承载 §3「协议写法：每条约束后附 `判断方式：…`」与 §4 第 3/4 条两项已确认的裸判定；它是 F03~F07 全部约束的可裁决性要求，不新增任何约束内容。

---

## 本次迭代边界说明

### 包含

- 清零 `oamp/test/`（含 `helpers/`）、`oamp/package.json` 的 `scripts.test`、`oamp/README.md` 第 195 行的「由 `npm test` 强制」（F01）。
- 工作流阶段 5 输出契约去掉「+ 通过验证标准的测试」（F02）。
- 产出测试协议内容：运行粒度、归属与一次性脚本处置、阶段 5 零测试态验收判据、阶段 6 零测试态取证口径、保留集重建触发条件，且每条约束附 `判断方式：`（F03~F08）。
- 6 处影响面引用全部有归属，其中 `docs/iteration-time-analysis.md` 的失效结论就近加注（F09）。

### 不包含（对应 demand.md §2，作为各卡边界项落实，不单独拆保证项卡）

| 不做 | 承载位置 |
|---|---|
| 不新增任何测试用例（零测试交付） | F01 边界、F04 验收 1/边界 |
| 不新增 test-keeper 角色 | F04 边界、F07 边界、F08 边界 |
| 不改任何生产代码（`oamp/src` `oamp/web` `oamp/bin`） | F01 验收 5 / 边界 |
| 不引入 CI / git hook / 第三方依赖 | F01 验收 4、F08 边界 |
| 不定协议承载形态 | F03~F08 的「架构维度」列 + F08 边界 |
| 不回改 0025 | F09 边界 |

### 已登记的被否决路径（§6）不得作为功能点回填

1. 按「跨迭代契约意义」筛选保留 → F07 边界明确不得回填；
2. 先定协议后清理 → 见下方「顺序约束」；
3. 保留 `helpers/` 脚手架 → F01 边界；
4. 协议不含验收口径 → 被 Q13 否决，F05/F06 已覆盖；
5. 保留集不写触发条件 → 被 Q14 否决，F07 已覆盖；
6. 实现 PR 可落测试 → 被 Q4 否决，F04 已覆盖。

### 顺序约束（§3 第一条；Q9 裁决「先清理 → 后定协议」）

F01 → F05/F06/F07/F08（协议内容）→ F02 与 F09 的第 1/2/3 处。清理是本迭代的第一个交付单元，协议是最后一个；顺序本身不是功能点，是 PR 规划阶段（阶段 4）的依赖输入。

### 交付前置条件（§6 逻辑闭环复核）

本迭代的两件交付物（清理 + 协议）必须在同一次迭代内闭合——清理的正当性完全依赖"协议在同一迭代内产出"。此条不构成阻塞，登记为交付前置条件。

---

## 架构待填汇总（交阶段 3）

| 卡片 | 待填项 |
|---|---|
| F03 / F04 / F05 / F06 / F07 / F08 | 协议承载形态：协议文本落在哪个文件（demand.md §7 明示 `user_deferred`，本阶段不作判定；§7 记录的默认读数仅供参考，需在阶段 3 或撰写协议时定稿） |

F01 / F02 / F09 无架构待填项（纯删除、纯文本行改动、纯加注，不涉及技术选型 / 数据模型 / 接口设计）。

---

## model_inferred 汇总（待主 agent 确认）

| 卡片 | 推导内容 | 推导依据 | 状态 |
|---|---|---|---|
| F04 | 「PR 默认不落测试用例」的例外形态：例外只能由简报显式给出（锚定 `roles/dev/dev.md:107` 的既有措辞） | Q4 的"默认"措辞 + F-4 已核对的 dev.md 现行文本 | 待确认 |
| F06 | 缺「跨迭代回归证据」本身不导致 fail 判定（缺声明才是报告不合格） | 零测试交付是 Q8/Q14 已确认的既定状态，非缺陷；由此推导缺失证据不构成验证失败 | 待确认 |
| F09 | 第 6 处的「加注失效说明」是本迭代交付物（而非仅登记在案） | §5 第 6 行「处置」列写的是"加注失效说明"，但 W-4 表述为"登记…处置随 W-3 一并落定"，未明说动作时点 | 待确认 |

以上 3 项在得到主 agent 确认前不作为既成事实使用。

---

## 疑问/越界

1. **§5 表头与第 6 行处置的措辞张力**：表头写"其中 **3 处必须改文件**"，而第 6 行的处置「加注失效说明」本身也是一处文件改动。本阶段未自行判定该矛盾，而是把"加注属本迭代交付物"这一读数登记为 F09 的 `[model_inferred]`，交主 agent 确认。
2. **`SKILL.md` 未纳入改动面**：实测 `.claude/skills/workflow-pb/SKILL.md` 全文不含"测试"字样（全仓检索零命中），因此不存在需要与 `workflow-pb.md:56` 同步的第二份拷贝；F02 未擅自扩大改动面。
3. **§7 显式后置项未写卡**：协议承载形态（`user_deferred`）不构成功能点，只在 F03~F08 上以「架构维度 = `[架构待填]`」承载，未为它单独出卡，也未在本阶段判定落点。
4. 除第 1 点外，未发现 demand.md 内部矛盾或不清晰之处；W-1~W-4 全部有对应卡片，无 demand.md 之外的新增功能点。
