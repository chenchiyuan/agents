# F09 · 影响面闭环登记（9 行引用全部有归属）

**来源**：demand.md 做什么 W-3（原 W-4 重编号）；结论 Q12 / Q15 / Q17 / Q18 / Q19；§5 影响面清单（**9 行**：1/2/3/3b/4/5/6/7/8）；验证方法 V-1 / V-2 / V-3 / V-4 / V-5（第 7 行另用 V-1 同族的 `ls` / `git ls-files` 判据）

## 用户价值

"删测试"不留悬空的失效承诺——仓库里每一处曾经指向测试的地方都有明确归宿（已改 / 已删文件解除 / 已登记为随协议延期 / 只登记不处置 / 已就近加注），后面读到这些位置的人（人或 agent）不会被指向一个已经不存在的东西，也不会误以为还有一份协议在后面兜着。

## 验收标准

1. §5 表列的 9 行引用逐一有可核查的处置结论：

   | # | 位置 | 处置 | 判据 |
   |---|---|---|---|
   | 1 | `roles/workflow-pb/workflow-pb.md:56`（阶段 5 输出契约） | 经 F02 改动后不含测试字样 | V-4 零命中 |
   | 2 | `oamp/package.json`（`scripts.test`） | 经 F01 移除 | V-2 零命中 |
   | 3 | `oamp/README.md:195`（「漂移锁…由 `npm test` 强制」） | 经 F01 移除该句 | V-3 零命中 |
   | 3b | `oamp/README.md:101-102`（「自动化测试将 interval 缩到 30–100ms…」） | 经 F01 移除该句 | V-3 零命中 |
   | 4 | workflow-pb.md 同行推进条件「该 PR 验收标准全部通过」（无机械判据） | 随协议延期——本迭代只登记，不处置（Q15） | 登记在案，无文件动作 |
   | 5 | `roles/verifier/verifier.md`（全文未提跑什么测试） | 随协议延期——本迭代只登记，不处置（Q15） | 登记在案，无文件动作 |
   | 6 | `docs/iteration-time-analysis.md:236` 的「测试执行本身不是瓶颈」结论 | 就近加注失效说明（见第 2 条） | V-5：原句位置可读到标记 |
   | 7 | `oamp/scripts/testenv.mjs:11`（可执行 `import` 指向 `test/helpers/harness.js`） | 经 F01 删除整个文件（Q18） | V-1 同族判据：`ls oamp/scripts/testenv.mjs` 不存在、`git ls-files oamp/scripts` 不含该文件 |
   | 8 | `oamp/src/cluster-config.js:19`（注释里指向 `test/hygiene.test.js`） | **只登记不处置**（Q19）——该文件在 F01 验收 5 的冻结面内，本次不动 | 登记在案，无文件动作 |

2. 第 6 处的失效加注落在原句所在位置附近（读者读到该句即能看到标记），点明失效原因（0026 清洗了 `oamp/test/`，全量套件不再存在）与生效迭代；不重写该文档的既有分析与数值。
3. 除上述 9 行之外不留新的未处置引用——"不留悬空"按两类引用分别成立（原表述「改动后的仓库中不存在指向已删除测试资产的有效引用」被第 7 行（已处置解除）与第 8 行（注释、保留）证伪，故按两类分别定判据）：
   - **可执行引用**（路径指向 `oamp/test/**` 的 `import` / `require` / `spawn`）：改动后的仓库中**零命中**——第 7 行即此类，删前存在、随 F01 删除该文件而解除；
   - **注释类引用**：允许存在，但必须逐条登记且带处置结论——第 8 行即此登记（历史迭代产物 `docs/iterations/**` 内的历史引文不在判据面内，与 V-4 口径一致）。
4. 改动面与 §5 的处置列一致：本次实际发生文件动作的只有第 1/2/3/3b/6/7 行所在文件——`roles/workflow-pb/workflow-pb.md`、`oamp/package.json`、`oamp/README.md`、`docs/iteration-time-analysis.md`、`oamp/scripts/testenv.mjs`（`git diff` 逐文件核对，V-6）；第 4/5/8 行不产生任何文件改动。

## 边界（不包含）

- 不重写 `docs/iteration-time-analysis.md` 的分析内容与数值——只加失效标注（外科手术式）。
- 不为其他历史文档追加失效标注（§5 只列了这一处；第 3/3b 行与第 7 行同 PR，均由 F01 处置）。
- 不回改 0025 迭代产物（Q6：0025 按现状跑完）。
- 不承载协议内容：第 4/5 处的口径随协议迭代（Q15），本卡只要求这两处的登记成立，本迭代不对这两处做任何文件动作。
- 不处置 §5 第 8 处（`oamp/src/cluster-config.js:19`）：该处是注释、无功能影响，只登记不处置（Q19）；该文件在 F01 验收 5 的冻结面内，本卡对它零改动。
- 不为被移出的协议产出替代卡、替代说明或占位文件。

## 架构维度

无待填项。

## model_inferred

1. **第 6 处的"加注"是本迭代交付物，而非仅登记在案**：§5 第 6 行「处置」列写的是"加注失效说明"，但 W-4 的表述是"登记 6 处影响面…第 4/5/6 项的处置随 W-3 一并落定"，未明说加注这个动作本身在本迭代内执行。本卡按 §5「处置」列读作"本迭代内落定"，并把加注动作绑定到本卡自身所在的 PR——即在本 PR 的交付动作内完成加注。**待主 agent 确认。**
