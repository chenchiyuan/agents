# F09 · 影响面闭环登记（6 处引用全部有归属）

**来源**：demand.md 做什么 W-4；§5 影响面清单（6 处）；验证方法 V-1 / V-2 / V-3

## 用户价值

"删测试"不留悬空的失效承诺——仓库里每一处曾经指向测试的地方都有明确归宿（已改 / 由协议的新判据接管 / 已标注失效），后面读到这些位置的人（人或 agent）不会被指向一个已经不存在的东西。

## 验收标准

1. §5 表列的 6 处引用逐一有可核查的处置结论：

   | # | 位置 | 处置 | 判据 |
   |---|---|---|---|
   | 1 | `roles/workflow-pb/workflow-pb.md:56`（阶段 5 输出契约） | 经 F02 改动后不含测试字样 | V-3 零命中 |
   | 2 | `oamp/package.json`（`scripts.test`） | 经 F01 移除 | V-2 零命中 |
   | 3 | `oamp/README.md`（「漂移锁…由 `npm test` 强制」） | 经 F01 移除该句 | V-2 零命中 |
   | 4 | workflow-pb.md 同行推进条件「该 PR 验收标准全部通过」（无机械判据） | 由协议给出零测试态判据（F05） | 协议内可定位 |
   | 5 | `roles/verifier/verifier.md`（全文未提跑什么测试） | 由协议给出阶段 6 取证口径（F06） | 协议内可定位 |
   | 6 | `docs/iteration-time-analysis.md` 的「测试执行本身不是瓶颈」结论 | 就近加注失效说明（见第 2 条） | 原句位置可读到标记 |

2. 第 6 处的失效加注落在原句所在位置附近（读者读到该句即能看到标记），点明失效原因（0026 清洗了 `oamp/test/`，全量套件不再存在）与生效迭代；不重写该文档的既有分析与数值。
3. 除上述 6 处之外不留新的未处置引用：改动后的仓库中不存在指向已删除测试资产的有效引用（历史迭代产物 `docs/iterations/**` 内的历史引文不在判据面内，与 V-3 口径一致）。

## 边界（不包含）

- 不重写 `docs/iteration-time-analysis.md` 的分析内容与数值——只加失效标注（外科手术式）。
- 不为其他历史文档追加失效标注（§5 只列了这一处）。
- 不回改 0025 迭代产物（Q6：0025 按现状跑完）。
- 第 4/5 处要补的协议口径内容归 F05 / F06，本卡只要求这两处的归属成立。

## 架构维度

无待填项。

## model_inferred

1. **第 6 处的"加注"是本迭代交付物，而非仅登记在案**：§5 第 6 行「处置」列写的是"加注失效说明"，但 W-4 的表述是"登记 6 处影响面…第 4/5/6 项的处置随 W-3 一并落定"，未明说加注这个动作本身在本迭代内执行。本卡按 §5「处置」列读作"本迭代内落定"，并把加注时点绑定到协议的同一交付单元。**待主 agent 确认。**
