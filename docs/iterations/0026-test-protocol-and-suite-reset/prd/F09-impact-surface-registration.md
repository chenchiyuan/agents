# F09 · 影响面闭环登记（7 行引用全部有归属）

**来源**：demand.md 做什么 W-3（原 W-4 重编号）；结论 Q12 / Q15 / Q17；§5 影响面清单（7 行：1/2/3/3b/4/5/6）；验证方法 V-1 / V-2 / V-3 / V-4 / V-5

## 用户价值

"删测试"不留悬空的失效承诺——仓库里每一处曾经指向测试的地方都有明确归宿（已改 / 已登记为随协议延期 / 已标注失效），后面读到这些位置的人（人或 agent）不会被指向一个已经不存在的东西，也不会误以为还有一份协议在后面兜着。

## 验收标准

1. §5 表列的 7 行引用逐一有可核查的处置结论：

   | # | 位置 | 处置 | 判据 |
   |---|---|---|---|
   | 1 | `roles/workflow-pb/workflow-pb.md:56`（阶段 5 输出契约） | 经 F02 改动后不含测试字样 | V-4 零命中 |
   | 2 | `oamp/package.json`（`scripts.test`） | 经 F01 移除 | V-2 零命中 |
   | 3 | `oamp/README.md:195`（「漂移锁…由 `npm test` 强制」） | 经 F01 移除该句 | V-3 零命中 |
   | 3b | `oamp/README.md:100-101`（「自动化测试将 interval 缩到 30–100ms…」） | 经 F01 移除该句 | V-3 零命中 |
   | 4 | workflow-pb.md 同行推进条件「该 PR 验收标准全部通过」（无机械判据） | 随协议延期——本迭代只登记，不处置（Q15） | 登记在案，无文件动作 |
   | 5 | `roles/verifier/verifier.md`（全文未提跑什么测试） | 随协议延期——本迭代只登记，不处置（Q15） | 登记在案，无文件动作 |
   | 6 | `docs/iteration-time-analysis.md:236` 的「测试执行本身不是瓶颈」结论 | 就近加注失效说明（见第 2 条） | V-5：原句位置可读到标记 |

2. 第 6 处的失效加注落在原句所在位置附近（读者读到该句即能看到标记），点明失效原因（0026 清洗了 `oamp/test/`，全量套件不再存在）与生效迭代；不重写该文档的既有分析与数值。
3. 除上述 7 行之外不留新的未处置引用：改动后的仓库中不存在指向已删除测试资产的有效引用（历史迭代产物 `docs/iterations/**` 内的历史引文不在判据面内，与 V-4 口径一致）。
4. 改动面与 §5 的处置列一致：本次实际发生文件动作的只有第 1/2/3/3b/6 行所在文件（`git diff` 逐文件核对，V-6），第 4/5 行不产生任何文件改动。

## 边界（不包含）

- 不重写 `docs/iteration-time-analysis.md` 的分析内容与数值——只加失效标注（外科手术式）。
- 不为其他历史文档追加失效标注（§5 只列了这一处，第 3b 行与第 3 行同文件同 PR，由 F01 处置）。
- 不回改 0025 迭代产物（Q6：0025 按现状跑完）。
- 不承载协议内容：第 4/5 处的协议口径随协议迭代（Q15），本卡只要求这两处的登记成立，本迭代不对这两处做任何文件动作。
- 不为被移出的协议产出替代卡、替代说明或占位文件。

## 架构维度

无待填项。

## model_inferred

1. **第 6 处的"加注"是本迭代交付物，而非仅登记在案**：§5 第 6 行「处置」列写的是"加注失效说明"，但 W-4 的表述是"登记 6 处影响面…第 4/5/6 项的处置随 W-3 一并落定"，未明说加注这个动作本身在本迭代内执行。本卡按 §5「处置」列读作"本迭代内落定"，并把加注动作绑定到本卡自身所在的 PR——即在本 PR 的交付动作内完成加注。**待主 agent 确认。**
