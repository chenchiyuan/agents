# verify-20260911T163944-pr002 — PR-002 独立验收

## 身份

**反射出的验证者身份**：*全栈接口契约审查者 + 浏览器端交互验证者*
（依据产出物类型 = 「HTTP API + 浏览器 UI + 契约测试」三合一交付物：需要同时具备
①「能读懂单列 UPDATE / 只读谓词提取 / 错误码全表」的**同级代码审查者**视角，
②「能在真实浏览器里重放 Enter / 失焦 / Esc / 失败恢复」的**前端交互验证者**视角，
③「能判断断言是否真能捕获缺陷」的**测试有效性审查者**视角。）

## 产出物

- worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0014-pr-002-web`
- 分支 / 提交：`feat/0014-pr-002-web` @ `520a8a6`（`git status --porcelain` 为空，验收全程未改动该 worktree）
- 变更面（`git diff --name-only 8c68407 HEAD`，共 7 个文件）：
  `oamp/src/web.js`、`oamp/web/index.html`、`oamp/web/app.js`、`oamp/web/style.css`、
  `oamp/test/web.test.js`、`oamp/README.md`、`docs/.../prs/pr-002-rename-api-ui-contract-tasks.md`

## 验证标准来源

本次委托指定的 8 条标准（含 PR 卡验收 + 契约文档）：

- `docs/iterations/0014-chat-rename/prs/pr-002-rename-api-ui-contract.md`（验收标准 6 条）
- `docs/iterations/0014-chat-rename/architecture.md` §4（只读单一真源）/ §5（接口形态）/
  §6.4（就地同步）/ §8.2（web.test.js 契约同步）/ §8.3（README 同步）

**独立性声明**：本次验收不接收任何执行过程上下文；结论仅由产出物、契约文档与自建隔离环境的实测证据得出。

## 验证日期

2026-09-11

---

## 验证环境（自建隔离，未触碰主仓库集群）

| 项 | 取值 |
|---|---|
| 主仓库集群 | `oamp.js web start --port 7788` + 10 个 agent 进程（`/Users/chenchiyuan/projects/agents/oamp`）——**全程未触碰** |
| 验收用 socket | harness `makeTempSocketDir()` 临时目录（如 `.../T/oamp-test-LzIfXm/router.sock`） |
| 验收用库 | `.../T/pr002-browser-cVUjPh/sql.db`、`.../T/pr002-verify-db-*/sql.db`（均为临时目录） |
| 验收用端口 | API 检查 43000–44500 随机；浏览器实测 44618；反向核对 44999 |
| 浏览器 | headless Chromium（browser 工具，tab `main` / `mut`），真实页面 `http://127.0.0.1:44618/` |

---

## 逐项判定

### 标准 1：PR 卡验收逐条判定 — **pass**

| # | PR 卡验收条目 | 判定 | 可定位证据 |
|---|---|---|---|
| 1.1 | `cd oamp && node --test test/web.test.js` 全绿 | **pass** | `# tests 33 / # pass 33 / # fail 0`（日志 `/tmp/pr002-verify/tests.log`） |
| 1.2 | 新增用例覆盖 200（响应 `title` = trim 后权威值、`GET /api/chats/<id>` 与库值一致） | **pass** | `test/web.test.js:1250-1275`（`:1262` `assert.deepEqual(renamed.body, …)`；`:1265` `detail.chat.title`） |
| 1.3 | 400 全表：`{title:123}` / 空体 `{}` / `{title:null}` / `''` / `'   '` / 101 单位，且读回标题不变 | **pass** | `test/web.test.js:1277-1297`；独立复现 27/27（见标准 2） |
| 1.4 | 404（未知对话） | **pass** | `test/web.test.js:1314`（`assert.equal(unknown.body.error, …)` 逐字 `chat 不存在: chat-does-not-exist`） |
| 1.5 | 409 双路径：归档后文案含「已归档」、closed 后文案含「已关闭」，库值不变 | **pass** | `test/web.test.js:1299-1322`（`:1309` / 已关闭、`:1320` / 已归档）；独立复现见标准 2 |
| 1.6 | 畸形 JSON 400 / 超限 413 | **pass** | `test/web.test.js:1338-1353`（含 `connection: close` 断言） |
| 2 | 不置顶（服务端断言）：`updated_at` 逐字相等 + `GET /api/chats` 的 `chat_id` 顺序逐项不变 | **pass** | `test/web.test.js:1266`、`:1272`；独立复现 `no-pin-updated_at` / `no-reorder`（before=after=1700000003000，顺序 `[aaa,bbb,ccc]` 不变） |
| 3 | 只读判定不分叉：同一对话「改名 409」与「发消息 409」同真；既有 closed / archived 409 文案与状态码逐字未变 | **pass** | `src/web.js:64-66`（`isReadonly`）、`:483-521`（`/rename` 路由）、`:563`（`/api/messages` 改调共用谓词）；独立复现 `messages-409-archived` = `chat 已归档（只读），不接受新输入`、`messages-409-closed` = `chat 已关闭，不接受新输入`（逐字）；`rename 409 ⟺ messages 409` 同真 |
| 4 | 静态契约断言通过 | **pass** | `index.html:41` 含 `id="detail-title-input"` + `maxlength="100"`；`app.js:66` `function isReadonly` + `:25/:166+` `titleEdit`；`style.css:171` `.detail-title-input`；对应断言 `test/web.test.js:1024-1028`（注释行 :1023） |
| 5 | 浏览器实测（真实页面）：6 项子验收 | **pass** | 见标准 3（逐项实测结果） |
| 6 | 回归锁零修改通过：`web.test.js:415-431` 与既有全部断言 | **pass** | 见标准 4（删除行 = 0）+ 标准 5（226/226 全绿） |

**PR 卡硬约束**（`test/web.test.js` 的两段修改必须同属本 PR）：`git diff HEAD~1 HEAD -- oamp/test/web.test.js` 同时包含
`:1024-1028`（静态契约段，注释行 :1023）与 `:1249-1365`（末尾 API 用例段，共 5 个新 test），两段同属 `520a8a6` 一个提交 — **pass**。

---

### 标准 2：API 行为（自建隔离环境复现） — **pass**

**复现方式**：`/tmp/pr002-verify/api-check.mjs` —— 独立临时 DB（直接 `INSERT` 播种 4 条对话：普通×2 / 已关闭 / 已归档）
+ 临时 socket 上的真实 Router + 真实 `oamp web start` 子进程（随机 43xxx 端口），经真实 HTTP 驱动。
**结果：`total=27 pass=27 fail=0`。**

| 观察点 | 实测结果 |
|---|---|
| 200 | `status=200`，`body={"chat_id":"chat-aaa-normal","title":"季度复盘"}`（送 `'  季度复盘  '` → trim 后为权威值） |
| 200 后库值 | `GET /api/chats/chat-aaa-normal` → `title='季度复盘'` |
| **不置顶** | `updated_at`：改名前 `1700000003000` → 改名后 `1700000003000`（逐字相等） |
| **不重排** | `chat_id` 顺序 `[chat-aaa-normal, chat-bbb-normal2, chat-ccc-closed]` 前后逐项相同 |
| 其余列不动 | `agent_id='dev-1'`、`state='completed'`、`archived_at=null` 全部不变 |
| 400 ×9 | `{title:123}` / `{}` / `{title:null}` / `''` / `'   '` / `'x'.repeat(101)` / `'😀'.repeat(51)`（102 单位）/ body=`null` / body=`42` → **全部 400**，且每次读回标题不变 |
| 400 文案 | `标题非法: 需为字符串（当前值 …）` / `标题非法: 不能为空或全为空白` / `标题非法: 长度需 <= 100（当前 101）`（与 §5.2 全表逐字一致） |
| 100 单位边界 | `'y'.repeat(100)` → **200**，`title.length = 100` |
| 404 | `status=404`，`error='chat 不存在: chat-nope'`（与 `/close`、`/activate` 逐字同形） |
| 409 已归档 | `status=409`，`error='chat 已归档（只读），不可改名'`，库值 `'已归档标题'` 不变 |
| 409 已关闭 | `status=409`，`error='chat 已关闭（只读），不可改名'`，库值 `'已关闭标题'` 不变 |
| **只读不分叉** | 归档：renamed 409 ∧ messages 409；关闭：renamed 409 ∧ messages 409 —— 同真 |
| **/api/messages 逐字未变** | 归档 → `'chat 已归档（只读），不接受新输入'`；关闭 → `'chat 已关闭，不接受新输入'`（状态码 409） |
| 畸形 JSON | `status=400`，`error='请求体非法 JSON: …'` |
| 超限 | `status=413`，`connection: close`，`error='请求体过大（上限 65536 字节）'` |
| 幂等 | 同值改名 → 200 且返回同值 |
| URL 编码 id | `chat 带空格/斜杠` 经 `encodeURIComponent` → 200，返回 `chat_id` 解码正确 |

---

### 标准 3：浏览器实测（真实浏览器 + 隔离环境） — **pass**

**环境**：`http://127.0.0.1:44618/`（隔离 socket + 隔离库；播种 5 条对话，主列表顺序
`第一条对话 / 第二条对话 / 第三条对话 / 已关闭对话`，另有一条已归档）。网络层用**页内 `window.fetch` 包装器**计数（跨 `run` 持续）。

| # | 子验收 | 判定 | 实测证据 |
|---|---|---|---|
| ① | 点击可编辑对话标题 → 输入框出现且**原值预选** | **pass** | 点 `#detail-title` 后：`inputHidden=false`、`value='第二条对话'`、`selectionStart=0`、`selectionEnd=5`（= 全选）、`h1Hidden=true`、`document.activeElement===input`、`maxLength=100` |
| ① | Enter 保存 → 详情头与左栏同步更新 | **pass** | `h1='浏览器改名结果'`；左栏 `chat-br-beta` 的 `.title` 同为 `'浏览器改名结果'`；`#hint` 空；服务端复核 `GET /api/chats` → `title='浏览器改名结果'` |
| ① | **列表位置不变** | **pass** | 改名前顺序 `[alpha,beta,gamma,closed]` → 改名后**逐项相同**；`updated_at` 无变化（`1789029302576` 不变）；四项时间显示字符串前后相同 |
| ① | Enter 恰一次保存 | **pass** | 页内 fetch 计数：`renamePostCount=1`（无 Enter 后失焦的二次提交） |
| ① | 失焦恰一次保存 | **pass** | 输入新值后点 `#detail-meta` 触发 blur → `renamePostCount=1`，`h1='失焦保存标题'`，左栏同步 `'失焦保存标题'`，顺序不变 |
| ② | Esc 取消 → 恢复原名且**不发请求** | **pass** | 点标题进入编辑 → 输入 `'不应该保存'` → 按 Esc：`inputHidden=true`、`h1='浏览器改名结果'`（原名）；此后点击 `#detail-meta`：`newRequests=[]`、`renamePosts=0`（**未补保存**） |
| ③ | 空值 → 保存被拒、退出编辑态、恢复原值 | **pass** | 全选后 `Backspace`（值 `''`）→ Enter：`h1='浏览器改名结果'`、`inputHidden=true`、`hint=''`、`newRequests=[]` |
| ③ | 全空白 → 同上 | **pass** | 值 `'   '` → Enter：`h1='浏览器改名结果'`、`inputHidden=true`、`renamePosts=0` |
| ④ | 已关闭对话标题不可进入编辑 | **pass** | `#detail-title` 无 `.editable`（`editable=false`），`cursor=auto`（无暗示）；点击后 `inputHidden=true`，无编辑态、无请求 |
| ④ | 已归档对话标题不可进入编辑 | **pass** | 切「归档」筛选（`.filter.active=archived`，列表仅 `chat-br-arch`，服务端 `archived_at=1789029300976`）；点标题 → 无 `.editable`、`cursor=auto`；点击后 `inputHidden=true`、`h1='已归档对话'` 不变 |
| ⑤ | 保存失败 → 标题恢复原值 + 既有提示位可见 | **pass** | 绕过 `maxlength` 注入 101 字符 → Enter → `POST /rename` 返回 400；`h1='第三条对话'`（恢复原值）、左栏同名恢复、`#hint` = `'改名失败：标题非法: 长度需 <= 100（当前 101）'`、`className='hint error'`、`display=block`、`visibility=visible`、`color=rgb(185,28,28)`、高度 17.05px（真实可见） |

---

### 标准 4：既有断言零删改 — **pass**

```
$ git diff -U0 HEAD~1 HEAD -- oamp/test/web.test.js | grep -c '^-[^-]'
0
$ git diff --numstat HEAD~1 HEAD -- oamp/test/web.test.js
125	0	oamp/test/web.test.js
```

删除行 = **0**，全部为追加（静态契约 7 行 + 改名用例段 118 行）。
回归锁 `oamp/test/web.test.js:415-431`（「标题取首条输入 40 字符且后续输入不改标题」）文本原样存在（逐行比对无差异）。

---

### 标准 5：测试全绿 — **pass**

| 命令 | 结果 |
|---|---|
| `cd oamp && node --test test/web.test.js` | `# tests 33 / # pass 33 / # fail 0 / duration_ms 50984` |
| `cd oamp && node --test --test-concurrency=1 test/*.test.js` | `1..226 / # tests 226 / # pass 226 / # fail 0 / # cancelled 0 / # skipped 0 / duration_ms 141573`（**期望 226 命中**） |

两项均在隔离环境下运行（harness 自带随机端口 41000–43000 + 临时 `OAMP_DB`），未写主仓库 `data/sql.db`。

---

### 标准 6：变更边界 — **pass**

`git diff --name-only 8c68407 HEAD` 输出恰为 7 个文件：

```
docs/iterations/0014-chat-rename/prs/pr-002-rename-api-ui-contract-tasks.md   ← tasks 文件（允许）
oamp/README.md                                                                ← 允许
oamp/src/web.js                                                               ← 允许
oamp/test/web.test.js                                                         ← 允许
oamp/web/app.js                                                               ← 允许
oamp/web/index.html                                                           ← 允许
oamp/web/style.css                                                            ← 允许
```

- `oamp/src/persist.js`：**零改动**（不在 diff 中）。
- 其余测试文件（`persist.test.js` / harness / fake-node 等）：**零改动**（diff 中只有 `web.test.js`）。
- 无新增文件（无新模块 / 新依赖 / 新 schema）。

---

### 标准 7：反向核对（因果隔离） — **pass**

在**临时副本**（`/tmp/pr002-verify/mutated`，验收结束后已删除；被验 worktree 未被改动）施加两处定点变异，观察断言是否真的捕获缺陷：

**变异 A —— 把只读判定改成永假**（`src/web.js` 的 `isReadonly` → `return false;`）

```
$ cd /tmp/pr002-verify/mutated && node --test test/web.test.js
not ok 20 - Web：关闭 chat——幂等 / 不删数据 / 转发 context_released / 后续提交 409
not ok 24 - Web：批量归档——三键响应 / 范围与幂等 / 归档视图查询与 400 / 只读双路径（…）
not ok 32 - Web：改名——只读不分叉（改名 409 与发消息 409 同真）+ 畸形 JSON 400 / 超限 413（F03-3）
# tests 33 / # pass 30 / # fail 3
```

⇒ 只读谓词被抽出后仍是**唯一判定真源**：一旦失真，3 条既有/新增断言立刻转红，断言非空转。
（同一变异下「404 / 409 双路径」用例仍通过 —— 因为 `persist.renameChat` 的 SQL 守卫（§4.1 ②）返回 `null`，
路由落到防御性 409 分支，其文案同时含「已归档」「已关闭」。这是契约 §5.2 明示的**结构性兜底**在起作用，非缺陷。）

**变异 B —— 去掉 Esc 的取消语义**（`web/app.js` 的 Escape 分支删去 `exitTitleEdit();`）

在同一真实浏览器场景下重放「进编辑 → 输入 → Esc → 点别处」：

```
afterEsc:  { inputHidden: false, inputValue: 'Esc不取消就会保存', h1: '第二条对话' }
afterBlur: { inputHidden: true,  h1: 'Esc不取消就会保存' }
renamePosts: [{ url: '/api/chats/chat-br-beta/rename', method: 'POST', body: '{"title":"Esc不取消就会保存"}' }]
verdict: MUTATION B DETECTED
```

⇒ 原实现的 Esc **确实**是「不发请求」的因果来源（未变异时同一序列 `newRequests=[]`、`renamePosts=0`）。

---

### 标准 8：清理 — **pass**

| 资源 | 处理 | 复核 |
|---|---|---|
| `pr002-browser-env` / `pr002-mutated-env` 常驻进程 | `hub stop` → 两个均 `exited exit=0` | 无残留 node 子进程 |
| 隔离 Router + Router socket 临时目录 | 由 env 脚本 shutdown 钩子 `stopAll` + `rm -rf` | `.../T/oamp-test-LzIfXm`、`.../T/oamp-test-WXZRr5`：`already gone` |
| 隔离数据库目录 | shutdown 钩子 `rm -rf` | `ls -d pr002-*` → `(none)` |
| 浏览器标签 | `browser close`（`main` / `mut`） | `Released managed tab` |
| 端口 | — | `lsof -iTCP:44618 -iTCP:44999` → free |
| 反向核对副本 | `rm -rf /tmp/pr002-verify/mutated` | 已删除（脚本与日志保留作证据，不属运行环境） |
| 被验 worktree | 全程只读 | `git status --porcelain` → 空 |
| 主仓库集群（端口 7788 + 10 agent） | **未被触碰** | `pgrep` 仅显示主仓库既有进程（PID 35735–35861） |

> 遗留说明（不属本次创建）：OS 临时目录存在 293 个 `oamp-test-*` 目录（时间跨度 Sep 9 – Sep 11），
> 是本仓测试 harness `makeTempSocketDir()` 的历史遗留（跨会话累积）。本次**只删除自己两处具名 socket 目录**，
> 未按 mtime 批量删除，以免误删并行 sibling 正在使用的 socket。已列入下一迭代候选。

---

## 汇总

- **pass: 8 项**
- **fail: 0 项**
- **partial: 0 项**
- **blocked: 0 项**

补充计数（供主 agent 直接判断）：
- 独立 API 复现：27/27
- 浏览器实测子项：11/11
- 测试：`web.test.js` 33/33；全仓串行 226/226
- 反向核对：2/2 变异均被捕获

---

## 偏差记录

| # | 规格/文档描述 | 实现实际行为 | 严重度 | 建议处理 |
|---|---|---|---|---|
| D-001 | `architecture.md` §4.1 的 ③ 代码块逐字给出 `function isReadonly(chat) { return chat.archived_at !== null \|\| chat.state === 'closed'; }`，并标注「与 ① **同形同值**」 | `oamp/web/app.js:66-68` 实现为 `return chat.state === 'closed' \|\| chat.archived_at !== null;`（**操作数顺序相反**） | 低（语义完全同值：`\|\|` 交换律 + 两侧均无副作用；§6.2 表格本身也只要求「同值」） | **无需改实现**。若要满足「同形」的字面要求，把 §4.1 的 ③ 代码块顺序对齐实现，或把 §4.1 的表述由「同形同值」改为「同值（操作数顺序可不同）」 |
| D-002 | `architecture.md` §1.1 / §1.3-5 / §4.1 以**行号**引用既有落点：`web.js:516`（判定唯一落点）、`app.js:165`（关闭按钮禁用条件）、`app.js:164`（只读文案）、`app.js:510`（`closeCurrentChat` 守卫）、`app.js:94-100,402-411`、`app.js:427-437,143-147`、`app.js:357-366` | 本 PR 在 `app.js` 前部新增 `isReadonly` 与 `titleEdit` 后，这些行号整体下移；例如「关闭对话」按钮的完整只读表达式现位于 `oamp/web/app.js:245`（原 `:165`） | 低（属**取证时点的历史锚点**，非行为不一致；功能与实现完全吻合） | **无需改实现**。若后续仍需按锚点复查，建议把这些引用改为**符号引用**（函数名 / 选择器 / 断言文本），或在 §1 顶部标注「行号为 2026-09-11 阶段 3 快照」 |

> 两条均为**文档侧**偏差，不构成对本次验收判定的影响。

---

## 下一迭代候选

- **跨客户端 / 跨标签页标题同步缺失**：改名只在当前页就地回填，其他标签页需刷新才看到新标题
  （架构 §5.5 由 D-8 明确排除，实现一致）。若产品后续要求多端一致，需要新增 `title` 类 SSE 事件 —— 属**新需求**而非缺陷。
- **改名无历史/审计**：`chats.title` 单列覆盖写，无「改名历史」记录。当前无此需求；若后续出现（如"最近修改"视图）需新表。
- **`renameChat` 双 409 文案**：预检路径给 `已归档/已关闭（只读），不可改名`，防御性路径给 `chat 只读（已归档或已关闭），不可改名`
  （单进程同步语句下**不可达**，契约 §5.2 已声明）。若未来引入多进程/异步写入使该路径可达，两条文案需统一。
- **测试 harness 临时目录不回收**：`test/helpers/harness.js` 的 `makeTempSocketDir()` 每起一个 Router 就在 OS 临时目录留一个
  `oamp-test-*`（本机已累积 293 个，跨度 ≥2 天）。建议下一迭代在 harness 的 `stop()` 中回收 socket 目录（低风险、纯卫生）。
- **`maxlength` 与后端同尺但未互相锁定**：前端 `maxlength="100"`（UTF-16 code unit）与 `readTitle` 的 `String.length` 同口径，
  但两者是**两处独立字面量**（`TITLE_MAX_MANUAL`）。当前一致（已实测 51 emoji = 102 → 400）；若未来上限变更，两处需同步改。

---

## 结论

**PASS**

所有 8 条验证标准均为 pass，无 fail、无 partial、无 blocked。PR 卡 6 条验收标准逐条有可定位证据；
API 行为（200/400/404/409 双路径/413、不置顶、只读不分叉）在自建隔离环境中 27/27 复现；
5 组浏览器交互在真实页面上 11/11 实测通过；既有断言零删改（删除行 0）且全仓串行 226/226 全绿；
两处反向变异均被捕获，证明断言具备因果敏感性；隔离环境已清理干净，被验 worktree 与主仓库集群均未被触碰。

偏差记录 2 条，**均为文档侧低severity问题**（操作数顺序的「同形」字面口径、架构文档行号锚点漂移），
按角色约定**不影响结论判定**，移交主 agent 决定是否在文档层面同步。
