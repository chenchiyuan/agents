# 验证报告：0014-chat-rename 阶段 2/3/4 产物（独立验证）

**验证者身份**：功能规格评审者 + 迭代架构评审者 + 冲刺计划（PR 规划）审查者（三重反射：本次同时验证功能卡、架构文档、PR 规划三类产出物）

**产出物**：
- 阶段 2：`docs/iterations/0014-chat-rename/prd.md`、`prd/F01-inline-title-edit-commit.md`、`F02-title-validation.md`、`F03-title-readonly-boundary.md`、`F04-rename-sync-no-reorder.md`、`F05-autotitle-unchanged.md`
- 阶段 3：`docs/iterations/0014-chat-rename/architecture.md`
- 阶段 4：`prs/pr-001-persist-rename-write-port.md`、`prs/pr-002-rename-api-ui-contract.md`
- 对照基准：`demand.md`（v0.2.0）、`oamp/**` 代码库现状（HEAD = 5f966b1，`persist.js` 等以 HEAD 为准）、`.pb-agents/roles/workflow-pb/workflow-pb.md`

**验证标准来源**：主 agent 委托的 8 条标准（prd 覆盖性 / 无技术污染 / architecture 补全 / L1 判定 / 两处硬契约 / prs 格式与依赖 / 验收标准可独立判断 / 回归影响面）

**验证日期**：2026-09-11

**独立性声明**：未接收任何执行过程上下文；仅读取产出物与验证标准，并对 `oamp/**` 现状做只读侦察与一次隔离临时库（`/tmp`）行为实验。**未修改任何被验证产出物**。

> 环境说明（不影响判定）：验证期间阶段 5 已并行启动——工作树 `oamp/src/persist.js` 有 +29 行未提交改动（`TITLE_MAX_MANUAL` / `readTitle` / `stmts.renameChat` / `renameChat` 包装，未提交），且存在 `.pb-agents/worktrees/0014-pr-001-persist`、`0014-pr-002-web`。本报告所有代码锚点均以 **git HEAD（`5f966b1`）** 为准，与 architecture 取证基线一致，故并行改动不影响判定。

---

## 逐项判定

### 标准 1：prd 覆盖性 —— **pass**

**子项 1.1「每卡可追溯到 demand A-1~A-5，无新增功能点」：pass**
- 覆盖映射（`prd.md` 功能点索引 + 各卡 `来源` 行）：A-1→F01、A-2→F02、A-3→F03、A-4→F04、A-5→F05，一一对应，无第六张卡。
- `prd.md` 需求追溯表 A/N/E/D/M/C 各条款均有落点；N-1~N-7 全部落入卡边界或全局边界，未新增能力。
- 逐卡核对验收标准未越出 demand：F01-6（持久化 E-8）、F03-6（属性不变，D-13/C-1 推论）、F04-5（不刷新 updated_at，C-1）、F04-6（与 F03-6 同判据的重复陈述）、F05-5（手动 100 / 自动 40 并存，M-4）——均为 demand 已确认结论的直接落点，**无 demand 之外的新增功能点**。
- 无 F06 及任何新功能 ID。

**子项 1.2「每卡含功能 ID / 用户价值 / 验收标准 / 边界」：pass**
- 5 张卡 `grep -nE "^## "` 均含：`**功能 ID**`（F01:3 … F05:3）、`## 用户价值`、`## 验收标准`、`## 边界（不包含）`。
- 验收标准条目数：F01 6 条、F02 6 条、F03 6 条、F04 6 条、F05 5 条，均可判定（含"判定："锚点）。

**子项 1.3「`[model_inferred]` 零残留」：pass**
- `grep -rn "\[model_inferred" docs/iterations/0014-chat-rename/` 仅命中 `architecture.md:863`（R-9 的**引述性**描述）；`prd/F0*.md` 与 `prd.md` 的卡内验收标准已全部改为 `[user_confirmed M-0x]`，**零活体标记**。
- 卡内标记抽查：F01:13/14/17、F02:22/24/26/28、F03:19/21、F04:20/29 均为 `[user_confirmed …]`。

**证据**：`docs/iterations/0014-chat-rename/prd.md:16-22,60-92,96-130`；`prd/F01…F05` 各卡头部与验收段；`demand.md` §1（第二段）§2。

---

### 标准 2：无技术污染 —— **pass**

- 逐卡以技术 token 正则（`/api/|persist\.js|web\.js|app\.js|index\.html|style\.css|SELECT|UPDATE |maxlength|textContent|SSE|SQL|HTTP|端点|接口|#detail|titleEdit|isReadonly|renameChat|updated_at|archived_at|AR-[0-9]`）扫描：命中**全部落在各卡「架构维度（阶段 3 已填）」段**（F01:35-40、F02:34-35、F03:37-38、F04:35-36、F05:32）——这正是阶段 2 约定的架构区。
- 产品维度段（用户价值 / 验收标准 / 边界）无 DOM 元素名、函数名、接口路径、列名或语句写法。唯一例外是 **F05 边界 `:28`** 引用了 `AR-12 / AR-13` 与"测试断言与文档"——但 `prd.md` 自定的边界禁令为"不出现 DOM 元素名、函数名、接口路径、列名或语句写法"，`AR-12/AR-13` 不属其中任一，且该条是 `demand.md` §3 已知成本的转述，**不构成违规**。
- 架构维度不再留白：`grep "\[架构待填\]"` 在 `prd.md` / `prd/F0*.md` / `architecture.md` 中 **零命中**（仅 `history.md:55` 有"已补全 [架构待填] 条目数：13"的阶段小结文字）。

**证据**：上述 grep 命中行号；`prd/F05:28`。

---

### 标准 3：architecture 补全 —— **partial**（2 通过 / 1 不通过）

**子项 3.1「AR-01~AR-13 逐条可定位」：pass**
- `architecture.md` §10 表 `:\| AR-\d\d \|` 命中 13 行（:740-752，AR-01~AR-13）；每条同时具备"落定内容 + 详见小节"。
- 逐条可在正文定位：AR-01/02→§6.1/§6.2/§3.1/§5.2，AR-03/04/05→§6.3/§6.4，AR-06/07→§3.3/§6.6，AR-08/09→§4.1/§4.2/§6.5，AR-10/11→§6.4/§3.1，AR-12→§3.3/§7.3/§8.4，AR-13→§8。
- 逐卡回填：F01→AR-01~05、F02→AR-06~07、F03→AR-08~09、F04→AR-10~11、F05→AR-12（+全局 AR-13 在 prd.md 摘要表内），与 `prd.md` 索引列（`AR-01~AR-05` 等）一致、无漏项。

**子项 3.2「全文无 `[架构待填]` 残留」：pass**
- `architecture.md`、`prd.md`、`prd/F0*.md` 零命中；`prd.md` 仅保留"### 阶段 2 原始待填项（追溯保留）"表格（无占位标记）。

**子项 3.3「架构阶段对 prd 的改动仅限架构维度」：fail**
- `git diff a8146f6 5a67574 -- docs/iterations/0014-chat-rename/prd.md docs/iterations/0014-chat-rename/prd/` 显示阶段 3 提交（`5a67574`）除填入架构维度外，还改动了**产品维度行**：
  - F01 验收 2、3、5 行内标记：`[model_inferred M-03/M-04]` → `[user_confirmed …]`；
  - F02 验收 1/2/3/5/6 行：标记改写，且验收 3 追加"粘贴超过 100 单位的内容时自动截断到 100，不额外弹出超长错误提示"、验收 6 将"取值经阶段 2 简报确认"改为"2026-09-11 用户确认"（**验收标准文案被改写**）；
  - F03 验收 2/5、F04 验收 3/边界 M-08 行标记改写；
  - `prd.md`：功能点索引表头与 model_inferred 列、M-8/M-9/M-10/M-11/C-6 追溯行、`model_inferred 列表`状态列、"简报已确认项"→"已确认项"表（含 5 行内容）、"拆分理由"中一行。
- 与文档自述冲突：`architecture.md` §16.2 声称"**未改动任何产品维度**（用户价值 / 验收标准 / 边界 / `model_inferred` 列表 / 追溯表）"；§15 R-9 声称"**卡内验收标准里的 `[model_inferred M-0x]` 标记仍保留阶段 2 的写法**"——**两处均与实际 diff 不符**（标记实际已改；列表/追溯表实际已改）。
- 说明（不改变判定）：这些改动无功能范围扩张（F02 追加句即 D-6 原文），且是标准 1.3"零残留"所必需；但标准 3.3 的字面要求是"仅限架构维度"，且文档自述与提交事实矛盾，故判 fail。

**证据**：`git show --stat 5a67574`；上述 `git diff a8146f6 5a67574` 输出；`architecture.md:863`（R-9）、§16.2。

---

### 标准 4：L1 判定正确性（独立复核） —— **pass**

**architecture 自述**：`architecture.md` §13「本迭代无 L1 决策」，逐项否掉三个触发条件。

**我的独立复核**（不采信自述）：

| L1 触发条件 | 我的核实 | 判定 |
|---|---|---|
| 引入新技术栈 | 方案仍在 `node:sqlite`（`persist.js:11`）+ 内建 http（`web.js:1-2`）+ 原生 JS；`oamp/package.json` 的 `dependencies` 实测为 `{}` | 否 |
| 改变现有核心模块职责 | `persist.js` 现状职责 = 建库/幂等迁移/写口族（`insertInput`/`insertOutput`/`upsertChat`/`closeChat`/`archiveChat`/`activateChat`）+ 校验函数族（`readLimit`/`readOffset`/`readArchived`/`readTimestamp`/`readOptionalString`）+ 查询/关闭。本次 = 写口族 +1（`renameChat`）、校验族 +1（`readTitle`）——**同类增项，非职责变更**。`web.js` 现状职责 = 静态 + JSON API + SSE；本次 = 路由 +1（同形于既有 `/close`、`/activate`）。前端仍是"静态 DOM + 事件 + 内存态"，+1 编辑态。无模块新增/删除/拆分/合并 | 否 |
| 影响系统整体边界 | 不新增进程/端口/传输/协议/事件类型；不改数据层（`title` 列已存在，零列/零迁移/零索引）。新增 HTTP 端点属既有接口内的加法（与 0013 的 `/archive`、`/activate` 同性质），不改变"谁以什么传输访问谁" | 否 |

**对指定疑点的专项判断——「具名化 `isReadonly` 并同时改 `/api/messages` 的 409 调用」是否属于改变既有模块职责？**
- **判定：不属于。** 理由（对照现状代码）：
  1. 现状 `web.js:516-522` 已是内联判定 `existing.chat.archived_at !== null || existing.chat.state === 'closed'` + 双分支文案；方案（`architecture.md` §4.1）仅把该**同一表达式**提取为模块级 `isReadonly(chat)`，判定行改为 `if (existing && isReadonly(existing.chat))`，文案三元表达式原样保留 → **对全部输入求值结果、状态码、响应体逐字不变**。
  2. 提取后的函数仍是 `web.js` 内部的纯谓词，不跨模块、不改对外接口、不引入新抽象层（3 行）。这是**行为保持型 extract-function**，属重构而非职责变化；`web.js` 的模块职责（HTTP/JSON API/SSE）边界未被触碰。
  3. 无新增模块边界/依赖方向变化：`persist` 导出 +1 是对既有写口族的加法，调用方仍是 `web.js`（`web.js:26` 既已 `import { openDb }`）。
- 残留提示（供主 agent 权衡，不影响 L1 判定）：该提取把 0013 已交付代码纳入本次改动面，**爆炸半径扩大**，属 implementation 风险而非架构级别变化；若主 agent 更保守，可采用 §13 K-1 的退回方案（新路由内联同表达式），行为等价。

**结论**：architecture 的"无 L1"判定**正确**。

**证据**：`oamp/package.json`（`dependencies: {}`）；`oamp/src/persist.js:90-134,145-199,279-283`；`oamp/src/web.js:26,404,425,454,516-522`；`architecture.md` §13。

---

### 标准 5：两处硬契约可执行性 —— **pass**

**§3.1 改名写语句**
- 方案语句：`UPDATE chats SET title = ? WHERE chat_id = ? AND archived_at IS NULL AND state != 'closed'`，以 `stmts.renameChat.run(normalized, chatId).changes > 0` 判定。
- 与现状自洽性：`chats` 表存在 `title`/`state`（NOT NULL）/`archived_at`（`persist.js:14-22`）；`.run(...).changes` 已是本仓既有用法（`persist.js:229` `archiveChat`、`:233` `activateChat`、`:243` `sweep.run().changes`）→ API 可用。
- **行为实验（独立复核 S-7）**：以 `node:sqlite` 于 `/tmp` 隔离库复刻同 schema 与同语句，实跑结果：

| 场景 | changes | 结果列 |
|---|---|---|
| 普通行（未归档非 closed） | **1** | `title`=新值；`updated_at`=100（未变）、`agent_id`/`state`/`closed_at`/`archived_at`/`context_released` 逐列不变 |
| 同值再写 | **1** | 幂等成功（与 §3.1 表一致） |
| 已归档（archived_at=777） | **0** | title 不变 |
| 已关闭（state='closed'） | **0** | title 不变 |
| 已关闭且已归档 | **0** | — |
| 行不存在 | **0** | — |

  → §3.1「`changes` 语义表」「逐列声明（不触 `updated_at`/`agent_id`/`state`/`archived_at`/`closed_at`/`created_at`/`context_released`）」**实测成立**，非推断。
- `readTitle` 口径实测：100 字符通过、101 拒绝；`'😀'.repeat(50)`（100 单位）通过、`'😀'.repeat(51)`（102 单位）拒绝；`'  季度复盘  '`→`'季度复盘'`；`'季度  复盘'` 内部空白保留；`'　'`（U+3000）与 `\u00a0`（NBSP）均判"全空白"拒绝；`'😀'.length === 2`（UTF-16 code unit）。**与 F02 验收 1/2/4、pr-001 验收第 4/5 条逐项吻合**。
- `TITLE_MAX_MANUAL = 100` 落点：`architecture.md` §3.3 置于 `persist.js` 常量区（与 `TITLE_MAX=40` 并存、互不引用），`readTitle` 内建在 `renameChat` 写口内；与 F02 AR-06、pr-001 文件范围（`persist.js`）一致 → 落点唯一、无第二处校验。
- 不复用 `upsertChat` 的四条违反实测成立：现状 `persist.js:150-156` 确为 `ON CONFLICT DO UPDATE SET title/agent_id/updated_at ... WHERE chats.state != 'closed'`，包装 `upsertChat`（`:225-227`）不读 `changes`、不检查 `archived_at` → §3.2 逐条对照准确。

**§4 只读单一真源**
- 与 `web.js:515-522` 同值：现状判定行 `web.js:516` 为 `existing.chat.archived_at !== null || existing.chat.state === 'closed'`，文案三元在 `:517-521`；方案 `isReadonly(chat)` 返回同表达式，`/api/messages` 改为调用它 → **求值同值、文案同字**。
- 前端同形谓词：现状 `app.js:165` 为 `chat.state === 'closed' || chat.archived_at !== null`（两项顺序相反，`||` 下同值）；方案提取 `isReadonly` 并让标题编辑门与 `:165` 共用 → 同值，可行。
- SQL 侧双守卫 = 同一规则的第二处字面书写，未命中 ⇒ `changes=0` ⇒ 上层 409，**不静默成功**（实测支持）。三处落点（web/app/SQL）与 AR-08 一致。
- 附加核查：`persist.js` 的 `archiveChat`（`:169-170`）也带 `archived_at IS NULL AND state != 'working'`，与本次 rename 守卫**语义不同**（归档动作 vs 只读面），不构成口径分叉。

**结论**：两处硬契约自洽、可执行，且 `readTitle`/`TITLE_MAX_MANUAL=100` 落点唯一。

**证据**：临时探针输出（已删除脚本，未触碰 `oamp/data/sql.db`）；`oamp/src/persist.js:14-22,145-156,169-184,229-243,279-283`；`oamp/src/web.js:404,425,454,516-522`；`architecture.md` §3.1/§3.2/§3.3/§4.1。

---

### 标准 6：prs 格式与依赖正确性 —— **pass**

**子项 6.1「两文件七字段齐备」：pass**
- `grep -nE "^## "`：pr-001 = 上下文摘要/涉及功能点/文件范围/验收标准/参考资料/depends_on/batch（7 个，:3/:7/:14/:19/:28/:36/:42）；pr-002 同（:3/:7/:15/:24/:33/:42/:46）。pr-001 `depends_on` 为`（无）`、`batch=1`；pr-002 `batch=2`。字段内容均非空且符合 workflow 规范（`.pb-agents/roles/workflow-pb/workflow-pb.md:145-215`）。

**子项 6.2「depends_on（pr-002 → pr-001）证据真实」：pass**（逐条核实）
- `oamp/src/web.js:26` `import { openDb } from './persist.js';` → **行号与符号均正确**。
- `oamp/src/web.js:180` `db = openDb(config.dbPath);` → **正确**。
- `oamp/src/persist.js:279-283` 的返回块确为 `{ insertInput, insertOutput, upsertChat, closeChat, archiveChat, activateChat, listArchivable, startupSweep, listChats, getChat, close }`，**当前不含 `renameChat`** → 正确。
- `oamp/test/web.test.js:123` `async function startWeb(socketPath, port, envExtra = {})`，`:124` `spawn(process.execPath, [BIN, 'web', 'start', ...])` → **"spawn 拉真实服务进程"成立**。
- 因果成立：`/rename` 路由调用 `db.renameChat`，在 pr-001 缺席时抛 TypeError → 被 `web.js:573-574` 外层 catch 转 **502**，改名用例失败 → 依赖为**代码级真实耦合**，非文档顺序偏好。

**子项 6.3「文件范围无重叠」：pass**
- pr-001 = `oamp/src/persist.js`、`oamp/test/persist.test.js`；pr-002 = `oamp/src/web.js`、`oamp/web/index.html`、`oamp/web/app.js`、`oamp/web/style.css`、`oamp/test/web.test.js`、`oamp/README.md` → **零交集**。

**子项 6.4「依赖图无环」：pass** —— 唯一边 pr-002→pr-001，pr-001 `depends_on=（无）`，单边无环。

**子项 6.5「F01~F05 全覆盖」：pass** —— pr-001 声明 F01/F02/F03/F05；pr-002 声明 F01~F05；并集 = F01~F05，无遗漏（F04 由 pr-002 独担，其服务端断言依赖 pr-001 语句，已在依赖中声明）。

**子项 6.6「遵守 architecture §14.2 的 `web.test.js` 双段修改硬约束」：pass**
- `architecture.md` §14.2 ⚠️ 要求：`web.test.js` 被 G2（末尾 API 用例段）与 G3（静态契约段）同时修改，**必须串行或同一 owner 合并**。
- pr-002 的**文件范围**同时包含 `oamp/test/web.test.js`，其**上下文摘要**显式声明"`web.test.js` 的两个段修改…必须同属本 PR，不得拆给两个 PR（文件范围互斥）"；pr-001 不触碰 `web.test.js` → 单一 owner、合规。
- 段位锚点核对：现状静态契约测试为 `web.test.js:1000-1022`（F08-4）与 `:1026-1044`（pr-008），pr-002 称 `:1000-1043` 属包含两者的大致区间，覆盖无误。

**证据**：两份 pr 文件；`oamp/src/web.js:26,180,516-522,565-574`；`oamp/src/persist.js:279-283`；`oamp/test/web.test.js:123-124,1000-1044`；`architecture.md` §14.2。

---

### 标准 7：验收标准可独立判断 —— **partial**（pr-001 通过 / pr-002 不通过）

- **pr-001：pass** —— 6 条验收全部由 `oamp/test/persist.test.js` 直接驱动 `openDb()` 句柄判定，不经过 `web.js` 与 HTTP；文件自身注明"可独立合并、独立验收，不依赖任何其他 PR"，成立。
- **pr-002：fail（字面）** —— 6 条验收中：
  - 「`node --test test/web.test.js` 全绿（改名 200/400/404/409/413 用例）」「不置顶服务端断言」「只读不分叉」「浏览器实测」「回归锁通过」**均需 `db.renameChat` 先由 pr-001 落入 `persist.js` 导出表**（否则路由 TypeError→502，web.test.js 无法全绿）；
  - 仅「静态契约断言（`index.html`/`app.js`/`style.css` 内容）」「回归锁中不涉及改名的既有断言」可脱离 pr-001 判断，满足 workflow「**至少一条**可独立判断」的格式底线（workflow-pb.md:181），**不满足本委托标准 7 的"每条 PR 的验收标准不依赖其他 PR 完成即可判断"字面**。
- **性质说明（关键，供主 agent 决策）**：pr-002 对 pr-001 的依赖是**声明内真实硬依赖**（共享符号 `db.renameChat`，有代码级证据），workflow 的依赖正确性验收条件（workflow-pb.md:405-407）**不**要求验收标准对已声明前置 PR 独立；因此**本项不需返工**——除非主 agent 要求把 pr-002 的验收标准拆分为"可脱离 pr-001 的部分"与"联调部分"两段陈述。

**证据**：`prs/pr-001…:19-26,36-40`；`prs/pr-002…:24-31,42-44`；`oamp/src/web.js:573-574`；`.pb-agents/roles/workflow-pb/workflow-pb.md:176-184,399,405-407`。

---

### 标准 8：回归影响面准确性 —— **pass**

**pr-001 声称**：需改 `persist.test.js:171-179` 写口白名单（7 项 → 8 项），其余回归锁零修改。
- 核实：`oamp/test/persist.test.js:171-179` 为「结构面」测试，`:176` 断言 `writers = [...7 项]`；其取数逻辑（`:173-175`）为 `Object.keys(db)` 减去 `['listChats','getChat','close','listArchivable']` → **新增导出 `renameChat` 必然使其失败，必须改**，pr-001 识别准确。
- 完整性核查：全仓测试中只有 `persist.test.js:12` 导入 `persist.js`；`Object.keys` 枚举另有 `persist.test.js:365/368`（message/chat 字段集）与 `web.test.js:1062`（归档响应三键），均与新增写口无关 → **写口白名单是唯一必须修改的既有断言**，pr-001 的影响面声明**完整**。
- 「列名/schema 断言零修改」核对：`:60-78`（两表两索引 + 两表列名）与 `:80-82`（`CHATS_COLUMNS_9`）本次零 schema 变更 → 不需改，正确。
- 「自动生成规则断言零修改」核对：`:233-244`（40 截断 + `'新对话'` 兜底）不在改动面 → 正确。

**pr-002 声称**：既有断言零删改。
- 核实：其改动面（新增路由 / 提取 `isReadonly` / 前端标题编辑 / 新增测试用例段 / 静态契约**追加**断言）不触碰任何既有断言文本；`web.test.js:415-431`（后续输入不改标题）、`:1000-1022`、`:1026-1044`（静态契约）在现状中均为正则/行为断言，新增 `id="detail-title-input"`、`maxlength="100"`、`isReadonly`、`titleEdit`、`.detail-title-input` **不会**使其失败（逐条核对 `assert.doesNotMatch(appJs, /POLL_MS/)`、`assert.match(html, /id="btn-close"/)` 等）。
- 完整性核查：`web.test.js` 的既有 404 断言（`:377,474,961,1210`）针对已知路由，新增 `/rename` 后缀分支不吞并它们；无其它文件枚举 `web.js` 路由集合 → 影响面声明**完整**。
- 回归锁锚点核对：`web.test.js:415-431` 与 pr-002 引用一致（测试 `:415`、断言 `:431`）。

**证据**：`oamp/test/persist.test.js:12,60-82,171-179,233-244,365-368`；`oamp/test/web.test.js:123,377,415-431,474,961,1000-1044,1062,1210`；`prs/pr-001…:19-26`、`prs/pr-002…:24-31`。

---

## 汇总

- **pass：6 项**（标准 1、2、4、5、6、8）
- **fail：0 项**
- **partial：2 项**（标准 3：子项 3.1/3.2 通过、3.3 不通过；标准 7：pr-001 通过、pr-002 字面不通过）
- **blocked：0 项**

---

## 偏差记录

> 实现/产出物与规格或自述不一致的地方。不影响本次验收判定（无 fail），但应在下一迭代或阶段 5 同步。

| # | 规格/文档描述 | 实际事实 | 建议处理 |
|---|---|---|---|
| 1 | `architecture.md` §16.2「**未改动任何产品维度**（用户价值 / 验收标准 / 边界 / `model_inferred` 列表 / 追溯表）」 | 阶段 3 提交 `5a67574` 实际修改了 `prd.md` 的 model_inferred 列表、追溯表、已确认项表与 F01~F04 产品维度行（标记 + F02 验收 3/6 文案） | 修订 §16.2 表述为"未改动产品**结论**，仅同步已确认标记与 demand v0.2.0 对齐文案"；或把标记同步动作归因到需求阶段 |
| 2 | `architecture.md` §15 R-9「卡内验收标准里的 `[model_inferred M-0x]` 标记**仍保留**阶段 2 的写法」 | 实测卡内已全部改为 `[user_confirmed M-0x]`，`grep "\[model_inferred"` 仅命中 R-9 自身 | 删除/改写 R-9 该段（现描述已过时） |
| 3 | `prd/F03` 来源行与边界引用「边界 N-8」 | `demand.md` v0.2.0 只有 N-1~N-7，**不存在 N-8**；「不含多用户/权限边界」为需求外自行编号 | 将该条改为 demand 全局边界（或显式标注"需求外沿用项"），勿使用未定义编号 |
| 4 | `prd/F03` 来源行「澄清 M-6 / M-8 / M-5」 | `demand.md` 的 M-5 = 「改名的可见范围」，与 F03 只读边界无对应（F03 验收 5 的 `[user_confirmed M-05]` 指 prd 自身的 M-05 = 空态不可编辑）；同一行混用 demand 与 prd 两套 M 编号 | 统一编号口径（建议 demand 侧写 `§4.1 M-6/M-8`，prd 侧写 `M-05`），并在卡内注明两套编号区别 |
| 5 | `prd.md`「已确认项」表「demand 原文状态」列仍写 `demand.md 标为 model_inferred（未生效）` / 「待用户决策」 | `demand.md` v0.2.0 已把 I-6 与 P-1~P-4 回填为 D-14/D-7/D-6/D-8/D-5（`user_confirmed`），`prd.md` 自身页首也已声明"上游标注不一致已消除" | 同步该表该列（改为"已由 D-xx 回填"），消除 `prd.md` 内部的双真相 |
| 6 | `architecture.md` §8.4 回归锁锚点 `persist.test.js:233-241`（覆盖"自动标题 = 首条输入截断 40；全空白 → 「新对话」"） | 该测试实际为 `:233-244`，「新对话」兜底断言在 `:243`，`:241` 只是空行（pr-001 引用的 `:233-244` 才准确） | 校正 architecture §8.4 行号为 `:233-244` |
| 7 | `architecture.md` §1.1 声称 `index.html`（67 行）；§1.2 称前端只读表达唯一落点为 `app.js:165` | HEAD 下 `index.html` 实为 **70 行**；`app.js` 另有 `:164`（meta 文案 `chat.state === 'closed'`）与 `:510`（`closeCurrentChat` 幂等守卫 `chat.state === 'closed'`）两处 closed 引用（均非本次只读面） | 校正行数；将"前端只有一处判定"精确表述为"**只读面**判定唯一（标题门 + 关闭按钮共用 `isReadonly`），`:164`/`:510` 为文案与动作守卫、不属该面" |
| 8 | `status.md` 阶段 5 子状态「pr-001 待派发 / pr-002 排队」 | 验证时工作树已存在 `oamp/src/persist.js` +29 行未提交改动（`TITLE_MAX_MANUAL`/`readTitle`/`stmts.renameChat`/`renameChat`），且 `.pb-agents/worktrees/0014-pr-001-persist`、`0014-pr-002-web` 已存在 | 属阶段 5 进度同步问题，与阶段 2/3/4 产物判定无关；建议主 agent 刷新 `status.md` 子状态 |

---

## 下一迭代候选

- **`F03` 编号卫生**：幻影 `N-8` 与 M 编号口径混用（偏差 3/4）属可复现的追溯缺陷；建议在需求/规格模板中固化"引用上游编号前先核对其存在性"的检查项。
- **上游回填的传播一致性**：`demand.md` 确认值回填后，`prd.md` 的"已确认项"表未同步（偏差 5）——建议阶段推进时把"上游标注同步"作为阶段门禁的一条自动检查。
- **架构阶段写权限边界**：本次阶段 3 修改了产品维度行却自述未改（偏差 1/2）——建议 workflow-pb 明确"架构阶段可改 prd 的哪些段"，并让架构角色在越界声明中如实列出提交事实。
- **`upsertChat` 的静默失败写口**（`architecture.md` R-8 已记录）：无生产调用者但仍是导出项且 `changes=0` 不检查；本迭代未修复。若后续迭代要清理写口族，应一并处理。
- **`/api/messages` 与前端 `:510` 的只读面表达**（偏差 7）：服务端已统一为 `isReadonly`，前端关闭守卫仍只判 `closed`；本次不改，但若 F03 验收 3 要在前端层面严格成立，可考虑后续统一。

---

## 结论

**PASS**（6 项 pass、2 项 partial、0 项 fail、0 项 blocked；8 条偏差已显式记录）

**L1 复核结论**：独立复核确认 architecture「**本迭代无 L1 决策**」的判定**正确**——不涉及新技术栈、不改变既有模块职责（含被点名的 `isReadonly` 具名化 + `/api/messages` 409 调用改为行为保持型 extract-function，非职责变化）、不影响系统边界；该提取仅扩大改动爆炸半径，属 L2/L3 取舍（架构 §13 K-1 已给出可逆回退方案）。
