# 验证报告：PR-003 标题编辑态可见性修复（0014-chat-rename / 阶段 5 补丁验收）

**验证者身份**：前端样式修复的同级代码审查者——熟悉原生 DOM 事件序列（focus/blur/keydown 竞态）、CSS 层叠与优先级、以及"静态契约测试"这类以源码文本为断言对象的测试形态的评审者。

**产出物**：
- worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0014-pr-003-title-visibility`
- 分支/commit：`feat/0014-pr-003-title-visibility` @ `969c53e2f99b39feaaf32d1d90a989d28459a1a1`（base = `main` = `4736ccdddcbef3be8590592c866a40e380fcba3c`，即 HEAD 的父提交）

**验证标准来源**：
1. 主 agent 委托的 7 条验证标准（context）
2. `docs/iterations/0014-chat-rename/prs/pr-003-title-edit-visibility.md` 的 5 条验收标准

**验证日期**：2026-09-11

**独立性声明**：未接收执行过程上下文；未使用产出者的自述作为判定依据（背景段仅用于交叉核实，判定一律回到 `git` 产物与实测）。全程只读 worktree——验证前后 `git status --porcelain` 均为空（0 行）。

**验证环境**：macOS (darwin 24.6.0) / Node v22.15.0。
隔离环境：临时目录 `/tmp/oamp-pr003-verify.g8iGYQ`（`OAMP_SOCKET=/tmp/.../router.sock`、`OAMP_DB=/tmp/.../sql.db`）+ 随机端口 `47455` + headless Chromium 实测；主仓库运行中集群（router `46804` / web `:7788`）全程未触碰，验证结束时 web `:7788` 仍返回 200。

**隔离数据构造**：用被测代码 `src/persist.js#openDb` 建库建表，再直写 3 行 chat——`chat-editable`（completed）、`chat-archived`（completed + `archived_at` 非空）、`chat-closed`（closed）；未落任何真实会话数据。

---

## 逐项判定

### 标准 1：PR 卡验收逐条判定（附可定位证据）

#### 1.1 「编辑态互斥（浏览器实测）」：**pass**

真实 puppeteer 点击路径：打开 `chat-editable` → 点击 `#detail-title`。

| 时点 | `h1#detail-title` | `input#detail-title-input` |
|---|---|---|
| 点击前 | `display:block`，`class="editable"`，rect **112×28** | `display:none`，`class="detail-title-input hidden"`，rect 0×0 |
| 点击后 | `display:none`，`class="editable hidden"`，rect **0×0** | `display:block`，rect **260×23**，`focused:true`，`value="Alpha 编辑目标"`，`selectionStart/End=0/10`（全文预选） |

附带证据：`state.titleEdit === {"chatId":"chat-editable"}`；页面在编辑态下的匹配规则集合包含 `.detail-head h1.hidden { display: none; }`。类名同时含 `editable` 与 `hidden` 而 `display` 为 `none`，说明该规则在同等特异性（0,2,1）下按声明顺序胜出——与实现位置（`style.css:183`，位于 `.editable` 规则 `:170` 之后）一致。

#### 1.2 「退出编辑态恢复（三条路径各一次）」：**pass**

三条路径均以真实键盘/鼠标事件触发，退出后逐项探针：

| 路径 | 触发方式 | 退出后 `h1` | 退出后 `input` | 权威标题（`GET /api/chats/chat-editable`） | 左栏 active 项 |
|---|---|---|---|---|---|
| Enter 保存 | 键入 `Alpha 重命名一` → `keyboard.press('Enter')` | `display:block`，`text="Alpha 重命名一"`，rect 112×28 | `display:none`，rect 0×0，`focused:false` | `Alpha 重命名一` | `Alpha 重命名一` |
| 失焦保存 | 键入 `Alpha 重命名二` → 真实点击 `#messages`（`activeElement` 变为 `BODY`） | `display:block`，`text="Alpha 重命名二"`，rect 112×28 | `display:none`，rect 0×0 | `Alpha 重命名二` | `Alpha 重命名二` |
| Esc 取消 | 键入 `Alpha 不该保存` → `keyboard.press('Escape')` → 再点击 `#messages` 强制失焦 | Esc 后立刻 `display:block`，`text="Alpha 重命名二"`（保持原值）；失焦后不变 | Esc 后立刻 `display:none`（`value` 残留 `Alpha 不该保存` 但不显示），失焦后不变 | `Alpha 重命名二`（未变） | `Alpha 重命名二` |

Esc 路径附加强证据：`page.on('request')` 监听整个 Esc 用例期间，`POST */rename` 请求数 = **0**；`state.titleEdit` 在 Esc 后为 `null`，随后的失焦未产生补提交（D-11「Esc 优先于失焦」）。

#### 1.3 「既有断言零删改 + 仅追加 1 条」：**pass**

- `git diff -U0 main...HEAD -- oamp/test/web.test.js` 输出仅 1 个 `+` 块（`@@ -1028,0 +1029 @@`），无 `-` 块；
- `:1023-1028` 的 0014（pr-002）静态契约 5 条断言逐字在位（`id="detail-title-input"` / `maxlength="100"` / `isReadonly` / `titleEdit` / `.detail-title-input`），新断言紧随其后落在 `:1029`；
- `:415-431` 回归锁不在本 PR diff 范围内（diff 仅含 `:1029` 一行）。

#### 1.4 「全仓串行测试全绿」：**partial**

- 「全绿 / 无 skip / 无 only」子项 **通过**：`cd oamp && node --test --test-concurrency=1 test/*.test.js`
  - 第 2 次（TAP 日志留档）：`# tests 226 / # pass 226 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0`，`exit=0`
  - 第 3 次（TAP 日志留档）：同一结果，`exit=0`；`test/` 全目录 `skip|only` 匹配数 = 0
  - 单文件 `node --test test/web.test.js`：`# tests 33 / # pass 33 / # fail 0`
- 「227」子项 **不通过**：PR 卡写「既有 226 + 新增 1 = 227 绿」，实际总用例数 = **226**（base 与 HEAD 相同）。原因可定位：新增断言写进了既有用例体内（`web.test.js:1029`，位于 `test('Web：前端契约——轮询消失…')` 内），`git diff` 中无新增 `test(` 行 ⇒ 用例级 +0、断言级 +1。属验收文案计数错误，而非实现缺陷。
- 附：第 1 次全仓运行（与 headless Chromium + 隔离 web 服务并发）为 `226 tests / 1 fail`，其累计 `duration_ms 772485` 是干净运行（`142262`）的 5.4 倍，判定为负载下的偶发；该次仅保留 `tail` 输出，**失败用例名未能捕获**（详见偏差记录 #4）。

#### 1.5 「只读与空态不受影响」：**pass**

| 场景 | 进入方式 | 点击标题前 | 点击标题后 |
|---|---|---|---|
| 空态 | 刷新后未选对话 | `h1 display:block`，`class=""`，`text="选择或新建一个对话"`；input `hidden` | 完全不变；`state.titleEdit === null` |
| 已关闭 | 主列表点击 `chat-closed` | `h1 display:block`，`class=""`（无 `.editable`），`text="Gamma 已关闭"` | 完全不变 |
| 已归档 | 切到「归档」筛选后点击 `chat-archived` | `h1 display:block`，`class=""`，`text="Beta 已归档"` | 完全不变 |

三种场景 `h1` 均不含 `hidden` 类，`.detail-head h1.hidden` 规则未被误加。新规则只对"含 `hidden` 类的 `.detail-head` 内 h1"生效，空态 h1 无该类名，故不会误伤。

### 标准 2：修复有效性（自己实测，隔离环境 + 真实浏览器）：**pass**

见 1.1 / 1.2 的全部探针数据。隔离性复核：服务由 `OAMP_SOCKET=/tmp/oamp-pr003-verify.g8iGYQ/router.sock`、`OAMP_DB=/tmp/oamp-pr003-verify.g8iGYQ/sql.db`、端口 `47455` 启动，服务日志仅 `WEB_READY url=http://127.0.0.1:47455` 与 SQLite 实验性告警，**无任何 error 行**；`GET /api/chats` 返回的正是注入的 3 条隔离数据，可证未读到主仓库库。

### 标准 3：对照（因果隔离）：**pass**（两条独立对照，均为实测）

**对照 A — 静态断言转红（临时副本，worktree 未改动）**
- 副本路径 `/tmp/oamp-pr003-verify.g8iGYQ/control/oamp`（`cp -R` 自 worktree）。
- 保留规则：`node --test --test-name-pattern='前端契约——轮询消失' test/web.test.js` → `# tests 1 / # pass 1 / # fail 0`。
- `sed -i.bak '/\.detail-head h1\.hidden/d' web/style.css` 后重跑 → `not ok 1 - Web：前端契约——轮询消失…`，`failureType: 'testCodeFailure'`，`code: 'ERR_ASSERTION'`，`error: '编辑态 h1 应真正隐藏（.hidden 对 h1 无既有规则，须独立规则）'`，`location: .../test/web.test.js:1000:1`（断言在 `:1029`）。
- ⇒ 该新断言与 `style.css` 中这一行严格绑定，删规则即红。

**对照 B — 编辑态 h1 恢复可见（活体 CSSOM，因果点对点）**
- 在服务中的真实页面上，编辑态（`class="editable hidden"`，`h1 display:none`，rect 0×0，匹配规则 `[".detail-head h1.hidden { display: none; }"]`）→ 经 `document.styleSheets[*].deleteRule()` 移除该规则 1 条（类名状态不变）→ `h1 display:block`，rect **112×28**，与 `input display:block` **同时可见** = 原始缺陷复现；匹配规则集合变为 `[]`。
- ⇒ 该规则是"编辑态互斥"的**唯一**使能条件，排除"别处已有规则生效"或"浏览器默认行为"等其他解释。

### 标准 4：既有断言零删改（`git diff -U0 -- oamp/test/web.test.js` 删除行 = 0）：**pass**

在 worktree 根执行 `git diff -U0 main...HEAD -- oamp/test/web.test.js`，`-` 行（排除 `---` 头）计数 = **0**，diff 仅 `@@ -1028,0 +1029 @@` 一行新增。

### 标准 5：测试（`node --test test/web.test.js` 与全仓串行）：**pass**

- `node --test test/web.test.js`：`# tests 33 / # pass 33 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0`（`duration_ms 50629`）。
- 全仓串行（`--test-concurrency=1`）：两次独立运行均 `226 / 226 pass / fail 0 / skipped 0 / todo 0`，`exit=0`（TAP 日志留档于隔离目录，已随清理删除；结论数据见 1.4）。
- 与委托预期一致（226 全绿）；PR 卡的「227」为题面计数错误，见 1.4 与偏差记录 #1。

### 标准 6：边界（改动仅 2 文件 / +2 行）：**pass**

- `git show --stat 969c53e` → `oamp/test/web.test.js | 1 +`、`oamp/web/style.css | 1 +`，`2 files changed, 2 insertions(+)`。
- `git diff --name-only main...HEAD` → 恰 2 条路径，与 PR 卡「文件范围」逐条一致，无第三个文件。
- `git status --porcelain`（验证前 / 验证后）→ 空；worktree 内无未跟踪文件，验证者未写入被验产物。
- 新增内容：`style.css:183` `.detail-head h1.hidden { display: none; }`（含说明注释）；`web.test.js:1029` 单条 `assert.match`。无 JS / 接口 / 数据层改动，与 PR 卡「仅样式与测试断言」陈述一致。
- 背景核实：`git show main:oamp/web/style.css` 中 `.hidden` 相关规则仅 `.detail-title-input.hidden`（base:182）与 `.mention.hidden`（base:273）⇒「h1 的 `hidden` 类无对应规则」成立，本 PR 正是补该缺口。

### 标准 7：清理（隔离环境）：**pass**

- `hub stop pr003-verify-web` → `Stopped ... exited, uptime=16m51s`；`lsof -nP -iTCP:47455` → no listener；`pgrep -fl 'oamp.js web start --port 47455'` → none。
- 隔离目录与哨兵文件已删除：`/tmp/oamp-pr003-verify.g8iGYQ`（含 `sql.db`、`seed.mjs`、`control/`、两份 TAP 日志）、`/tmp/oamp-pr003-tmproot.txt` 均 `No such file or directory`。
- 浏览器侧：`close all` 释放本人创建的 3 个受管标签页（headless，非用户浏览器）。
- 主仓库集群未受影响：router `46804`、web `46829(:7788)` 仍在运行，`:7788` 返回 200；`/Users/chenchiyuan/projects/agents/oamp/.runtime/` 与 `oamp/data/sql.db` 未被本验证读写（验证全程的 `OAMP_SOCKET` / `OAMP_DB` 均指向 `/tmp`）。
- 遗留说明（非本人创建，按边界纪律未删除）：`/tmp/oamp-pr003-runner.mjs`（2882 B，mtime `2026-09-11 17:03:55`，内容自述为「PR-003 浏览器实测的隔离运行时」），系实现阶段遗留，见下一迭代候选。

---

## 汇总

- pass: **6**
- fail: **0**（无需返工）
- partial: **1**（标准 1 内「全仓串行」子项：全绿成立、PR 卡「227」计数不成立）
- blocked: **0**（无缺信息项）

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定（本次 partial 的实质要求"全绿"已满足），但应在下一迭代同步文档。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| 1 | PR 卡验收 4：「既有 226 + 新增 1 = 227 绿」 | 全仓实际 **226** 用例全绿；新增断言落在既有用例体内（`web.test.js:1029`），用例数不变（断言 +1、用例 +0） | 按实现更新 PR 卡措辞为「226 用例全绿（断言 +1，用例数不变）」，避免下游以 227 作为门禁阈值 |
| 2 | `oamp/web/style.css:182` 行内注释「本仓 `.hidden` 只对 `.mention` 生效（:255）」 | `.mention.hidden` 实际位于 `style.css:273`（base 与 HEAD 均如此，`:255` 为陈旧引用） | 预存在偏差（pr-002 引入，与本 PR 无关，故未计入本次判定）；建议下一迭代顺手改正行号，或不处理 |
| 3 | 背景段：「h1 的 `hidden` 类无对应 CSS 规则」 | 核实成立：base 中 `.hidden` 规则仅 `.detail-title-input.hidden`（:182）与 `.mention.hidden`（:273） | 无需处理（实现与陈述一致） |
| 4 | 交付前提「全仓串行 226 全绿」 | 第 1 次全仓运行（与 headless Chromium + 隔离 web 服务并发）出现 `226 tests / 1 fail`；其累计 `duration_ms 772485` vs 干净运行 `142262`（5.4×），随后两次干净运行均 `226/226` 全绿、`exit=0` | 判为负载下偶发。因第 1 次仅保留 `tail`，**失败用例名未能捕获**，本报告不对其根因下结论；建议下一迭代以 TAP 全量日志复现并定位耗时敏感用例（见下一迭代候选 #1） |

---

## 下一迭代候选

> 不在当前验收标准范围内，供主 agent 与 planner 决策是否纳入。

1. **全仓串行测试的偶发性（建议优先）**：并发负载下出现 1 例失败（用例名未捕获），干净环境两连绿。建议给全仓测试加 `--test-reporter=tap` 全量留档 + 失败重跑计数，或收敛各用例的墙钟敏感阈值；当前"全绿"结论在单次运行下不可复现地依赖机器负载。
2. **实现阶段遗留的 /tmp 脚本**：`/tmp/oamp-pr003-runner.mjs`（2882 B）未随实现完成清理，属"仓库外遗留物"。建议在 workflow 的实现阶段收尾清单里加入"清理 /tmp 隔离脚本"，或在验证交付定义中显式区分"仓库外临时物"。
3. **同一特异性规则的顺序脆弱性**：`.detail-head h1.hidden` 与 `.detail-head h1.editable` 同 specificity（0,2,1），当前仅靠声明顺序取胜（`:170` vs `:183`）。若后续有人上移 `.hidden` 规则，编辑态互斥会**静默失效**（现象与本次缺陷同形）。可选加固：并入 `.detail-head h1` 块、改用 `h1[hidden]` 属性选择器，或加一条注释锚定顺序约束。本迭代验收标准未要求，仅记录。
4. **静态契约测试的形态局限**：新断言以正则匹配 `style.css` 源码文本，属"钉子"而非"行为验证"——它能防回归，但无法发现"规则写对、被更高优先级覆盖"的情形（本次靠浏览器实测才排除了该类风险）。若后续 0014 之后还有样式类补丁，建议在静态契约旁补一条兜底的浏览器/几何断言。

---

## 结论

**PASS**

逐项判定：6 pass / 1 partial / 0 fail / 0 blocked。partial 的唯一子项是 PR 卡验收 4 的「227」计数（实质要求"全仓串行全绿、无 skip/only"已由两次干净运行满足，实测总数为 226），属文档计数错误而非实现缺陷，已记入偏差记录 #1。修复在隔离环境经真实浏览器三路径实测有效，并经两条独立对照证明因果关系（删规则 ⇒ 静态断言红 / h1 与 input 同时可见），可直接继续流程。

**阻塞返工项**：无。
