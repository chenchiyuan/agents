# pr-002 任务图（新建协议产物 `docs/worktrees/README.md`）

**来源 PR**：`prs/pr-002-worktrees-protocol-artifact.md`（`batch` = 1；`depends_on` = `pr-001-addressing-contract-and-host-skill.md`）
**架构依据**：`docs/iterations/0020-session-workspace-addressing/architecture.md` **v1.1.4** —— §3.3（`P-01`）、§3.5（零改动清单）、§3.6（检索式判据行）、§4.1（文件与头部）、§4.2（内容骨架）、§4.3（真源 / 实例化投影）、§4.4（三层分层）、§4.5（落点语义区分句）、§8.1-A（协议产物节条款全文与 `判断方式：` 行）
**功能卡**：`prd/F03-protocol-artifact-existence.md`、`prd/F04-artifact-content-skeleton.md`、`prd/F05-location-semantics-disambiguation.md`、`prd/F07-address-declaration-layering.md`、`prd/F21-specline-form-constraints.md`
**唯一改动文件（施工面）**：`docs/worktrees/README.md`（**新建**；`docs/worktrees/` 目录随之新建）
**任务总数**：**1**（单文件单次原子写入；拆法说明与被否决的拆法见 §4）

---

## 0. 判定体例（先读）

1. **条目内容不复述**：凡 `architecture.md` v1.1.4 已成段 / 成表的措辞与结构（头部字段名、节序、表格列名、五类信息模板、三层分层、落点语义承接行形态），本文件一律只写「见 §X」或**逐字引用该节原句**，**不复述、不自行判定落点**。施工与验收均以 architecture 对应节为唯一真源。
2. **「可裸判定」的定义**：不依赖判断者主观尺度——文本存在性、与指定真源逐字 / 逐句对照、`grep` 命中数、`git diff` 面、逐项对照表。
3. **每条验收标准都带追溯来源**（本任务图以验收标准表的「追溯来源」列承载）；未在 architecture v1.1.4 / 本 PR 文件 / 对应功能卡中出现的判据不写。
4. **检索式纪律**：本文件写出的每条检索式**均已实跑**（基线检出 = 本 PR worktree `.pb-agents/worktrees/0020-pr-002`，分支 `feat/0020-pr-002-protocol-artifact`；实测值见 §1）。产物本体尚不存在 ⇒ 针对产物的检索式在 §1 中标为「落盘后期望」，其**行为等价性**已用同类既有文件实跑（§1 的 R14 行为核对行）。
5. **优先级语义**：只表示建议执行次序，**不是可选项**。
6. **独立性读法**（引 PR 文件「验收标准」段末注记的口径）：本 PR 的验收条目在其 `depends_on` **全部合并之后**独立判定；`depends_on` = `pr-001`，其被引用的两文件版本行与三个规范节在本 PR 落地时**均已存在**（实测见 §1 的 R5~R9）⇒ **A10 / A11 属「依赖序内可判」条目**。
7. **不写入任何 PR 之外的文件**：本任务图只新增 `prs/pr-002-tasks.md`（**规划产物**；先例 = 同目录已存在的 `pr-001-tasks.md` / `pr-004-tasks.md`）。`prs/pr-*.md`、`architecture.md`、`prd/*.md`、规范 / SKILL / 脚本 / 代码 / `.gitignore` 零改动。

---

## 1. 基线与检索式实测（本文件全部检索式的实跑值）

基线检出 = 本 PR worktree（分支 `feat/0020-pr-002-protocol-artifact`；`<base>` = 该分支与 `iteration/0020-session-workspace-addressing` 的 merge-base，实测 `6a99505`，即 `pr-001` / `pr-004` 合并后的提交）。

| # | 检索式（相对检出根） | 基线实测 | 落盘后期望 |
|---|---|---|---|
| R1 | `ls docs/worktrees` | `No such file or directory`（目录未建） | 输出**恰 1 行** `README.md` |
| R2 | `git ls-files docs/worktrees` | 0 行 | **1 行**：`docs/worktrees/README.md` |
| R3 | `git ls-files --error-unmatch docs/worktrees/README.md` | **失败**（`did not match any file(s) known to git`，rc=1） | **成功**（rc=0；需纳入提交） |
| R4 | `git ls-files docs/ds/README.md`（先例被跟踪性的实测） | 1 行 | 不变 |
| R5 | `grep -n '^\*\*版本\*\*' roles/workflow-pb/workflow-pb.md` | **1 行**，读作 `**版本**: 0.10.0` | 不变；且与产物 §2 的声明一致 |
| R6 | `grep -n '^\*\*版本\*\*' .claude/skills/workflow-pb/SKILL.md` | **1 行**，读作 `**版本**: 1.13.0（对应规范 workflow-pb v0.10.0）` | 不变；且与产物 §2 的声明一致 |
| R7 | `grep -c '^### 规则 H' roles/workflow-pb/workflow-pb.md` | **1** | 不变（产物 §3 引用不悬空） |
| R8 | `grep -c '^### 规则 D' roles/workflow-pb/workflow-pb.md` | **1**（更名后的标题） | 不变 |
| R9 | `grep -c '^### 协议产物：工作区间与生效规范' roles/workflow-pb/workflow-pb.md` | **1** | 不变 |
| R10 | `git worktree list` | 5 项，**无 `docs/worktrees/` 前缀项** | 不变 |
| R11 | `git diff --name-only <base>..HEAD` | （施工前）空 | **恰 1 行**：`docs/worktrees/README.md` |
| R12 | `git diff <base>..HEAD -- .gitignore roles/workflow-pb/workflow-pb.md .claude/skills/workflow-pb/SKILL.md` | （施工前）空 | **空**（三面零改动） |
| R13 | `grep -c '^\*\*状态\*\*：' docs/ds/README.md` | **1**（先例头部用全角冒号） | 不变（见 §5-MI-02） |
| R14 | 正则行为核对：`grep -cE 'git +[-a-z]'` | 对 `非 git 共享资源` / `.gitignore` 两行 = **0**；对 `git -C <地址>` = **1**（隔离实跑） | 产物上 = **0**（无命令形态） |
| R15 | `grep -c '\*\*P-01\*\*' docs/iterations/0020-session-workspace-addressing/architecture.md` | **1**（定位 `§3.3` 的 `P-01` 行） | 不变 |
| R16 | `grep -c '^\*\*5 节，顺序与' docs/iterations/0020-session-workspace-addressing/architecture.md` | **1**（定位 `§4.2` 的骨架块首行） | 不变 |

> **定位式的写法注记（实跑所得）**：R15 / R16 一类**定位式**刻意不含行首竖线交替写法（本环境实测：以行首竖线开头的交替式在被按扩展正则解读时会退化为「空交替」，命中全文件 998 行 —— 同一式经内置检索工具为 1 命中）。故本文件给出的定位式一律取**不含行首竖线**的形态；判定式则一律取上表形态。
> R1~R10 / R13~R16 为**基线实跑值**；R11 / R12 的「落盘后」列是 `git diff` 面的期望形态；针对产物的部分（R1~R3 的期望列、R11~R14 的产物侧）在施工后复跑即得。

---

## 2. 任务列表

### T1 — 新建 `docs/worktrees/README.md`（头部 + 节骨架；全文一次写入）

| 项 | 内容 |
|---|---|
| **目标文件** | `docs/worktrees/README.md`（**新建**；`docs/worktrees/` 目录随之新建）——**本 PR 唯一改动文件** |
| **内容形态** | 头部与节骨架**一律逐字采用** `architecture.md` v1.1.4 §4.1（文件与头部）、§4.2（内容骨架）——**字段名 / 节序 / 列名 / 取值见该两节**（定位式 R15 / R16），本任务图**不复述**；全文**无命令块、无动态地址**（§4.2「形态决策」段） |
| **内容真源** | 五类信息模板与「非第二真源」口径 = §8.1-A；落点语义承接行 = §4.2 的 §4 块（其真源 = §4.5 的规范侧真源）；真源 / 投影关系 = §4.3；三层分层 = §4.4 |
| **追溯来源** | architecture v1.1.4 §3.3-`P-01` 行原句：<code>\| **P-01** \| `<项目根>/docs/worktrees/README.md`（**新建**，被 git 跟踪；不改 `.gitignore`） \| 新增 \| …。**全文无命令块、无动态地址**（F04 验收 5、F07 验收 3） \| F03 / F04 / F05 / F21 \|</code>（该行完整原文见 R15 命中行）；§4.1 落点行原句：「**落点**：`<项目根>/docs/worktrees/README.md`（**新建**，被 git 跟踪）。**仅此一份产物**，不新增第二个文件」；本 PR「文件范围」行；功能卡 F03 验收 1~5 / F04 验收 1~6 / F05 验收 1~4 / F07 验收 1、3 / F21 验收 1、5 |
| **前置依赖** | **无**（任务级）；**PR 级**前置 = `pr-001-addressing-contract-and-host-skill.md`（见 §3） |
| **优先级** | P0（本 PR 唯一任务） |

**验收标准**（全部可裸判定；`<产物>` = `docs/worktrees/README.md`）

| # | 验收标准（可测试） | 追溯来源 |
|---|---|---|
| **A1** | **产物存在且落点正确**：`<产物>` 存在；`ls docs/worktrees` 输出**恰 1 行**（`README.md`）——无任何工作区目录、无运行态文件（基线 R1 = 目录不存在） | F03 验收 1；F04 验收 1；pr-002「产物存在且落点正确」；architecture §4.1 落点行原句（「**仅此一份产物**，不新增第二个文件」） |
| **A2** | **被跟踪（随检出即得）**：`git ls-files --error-unmatch docs/worktrees/README.md` 成功（基线 R3 = 失败）｜R2 落盘后恰 1 行 | F03 验收 2；pr-002「随检出即得」条给出的裸判据；architecture §3.6「检索式判据（可裸判定）」行 |
| **A3** | **零改动面 / 唯一新增文件**：`git diff --name-only <base>..HEAD` **恰 1 行** = `<产物>`；`git diff <base>..HEAD -- .gitignore` **空**；`git diff <base>..HEAD -- roles/workflow-pb/workflow-pb.md .claude/skills/workflow-pb/SKILL.md` **空**；改动清单**不含任何 `.sh` / 可执行脚本 / 其他新建文件**（本 PR 的「不涉及（零改动面）」清单各面 0 hunk） | F03 验收 3；F21 验收 1；pr-002「形态与体例」；architecture §3.5（零改动清单）、§3.6（核对步骤①） |
| **A4** | **不扩展规则 F 闭集例外**：由 A3 的「规范文件 0 hunk」直接推得 —— 规则 F 的「**例外（闭集，仅两项）**」句与例外条数均未被改动（本 PR 零改动规范文件 ⇒ 条数不可能变化，故不另作点数） | F03 验收 4；architecture §3.5；规则 F 原句（`**例外（闭集，仅两项）**：① …② …`） |
| **A5** | **头部与节骨架逐字采用 §4.1 / §4.2**：把 `<产物>` 的头部块与节骨架块同 §4.1 / §4.2 的对应块**逐字对照**（字段名 / 节序 / 表格列 / 取值形态以该两节为唯一真源，本文件不复述） | pr-002「文件范围」行；architecture §4.1、§4.2（定位式 R15 / R16） |
| **A6** | **五类信息逐类可读**：按 §8.1-A 的模板顺序逐类核对 `<产物>`，五类**逐类**可在产物中读到对应陈述（缺任一类即不通过）；其中工作区间类的**三子项**（会话 / 迭代工作区落点、PR worktree 落点与层级、工作区的范围）逐项存在 | F04 验收 1~4；F03 验收 5；architecture §8.1-A（五类信息段）、§4.2（骨架块） |
| **A7** | **落点语义一行承接、不构成第二处定义**：`<产物>` 的承接行为「**一行** + 一个 `（真源：规范 §协议产物）` 指针」形态（形态见 §4.2 的 §4 块与 §4.5 的「产物侧承接行」）；三处语义**不在产物内**形成第二处完整定义 | F05 验收 1、2；architecture §4.5、§8.1-A（落点语义段） |
| **A8** | **非第二真源**：把 `<产物>` 中每条**规范性**陈述逐条回指规范条款（`roles/workflow-pb/workflow-pb.md`）——出现无对应条款的约束即不通过；产物 §5 含非第二真源自述句 | F04 验收 6（MI-08 引用形态判据）；F03 验收 5；architecture §4.3、§8.1-A（「**非第二真源**」段） |
| **A9** | **无脚本 / 钩子 / 守卫 / 命令块**：① 全文逐行读判——不出现脚本、钩子定义、守卫逻辑、供复制的命令块（权威判据原文）；② 机械辅助 = 全文 **0 个围栏代码块**（逐行判定：无任何行以三连反引号开头）且 `grep -cE 'git +[-a-z]' <产物>` = **0**（无命令形态；该式的行为核对见 R14）；③ 不引入 hook / 守卫 / 检测器 / 启动前准入门 | F04 验收 5；F21 验收 5、1；pr-002「形态与体例」；architecture §4.2「形态决策」段（原句：「§1~§5 **不出现任何 `git …` 命令**」） |
| **A10** | **静态声明正确（版本一致）**：提取 `<产物>` §2 的两处版本号，与 R5 / R6 的读出值**逐字相等**（R5 = 1 行读作 `0.10.0`；R6 = 1 行读作 `1.13.0`）；判据不作词形枚举，只作「产物值 ↔ 文件头部版本行值」的一致比对 | F03 验收 5；F04 验收 2；F07 验收 1；pr-002「静态声明正确」条（含其给出的核对检索式 `grep -n '版本' roles/workflow-pb/workflow-pb.md .claude/skills/workflow-pb/SKILL.md`） |
| **A11** | **引用不悬空（依赖序内可判）**：`<产物>` §3 / §4 引用的规范节在本 PR 落地时**均已存在且唯一**——R7 = 1、R8 = 1、R9 = 1 | pr-002 `depends_on` 三条文本级证据；architecture §3.1 的 `N-04`（规则 D 更名）、`N-05`（规则 H 新增）、`N-06`（协议产物新增） |
| **A12** | **不含动态地址（跨检出一致）**：在两个不同检出中 `<产物>` **逐字节相同**（`git diff` 无差异）——判定口径 = 「产物内容不随会话变化」（**不做词形枚举**）；辅助读法 = `<产物>` 中 `<仓库主工作区>` / `<会话工作区>` / `{迭代ID}` 读作**占位符**，**不出现任何一次运行的具体绝对路径** | F07 验收 3（裸判据原文）；F05 验收 4；pr-002「不含动态地址」条（原句：「判据 = 两个不同会话的检出中该文件**逐字节相同**（`git diff` 无差异）」） |
| **A13** | **落点语义禁令可核**：`ls docs/worktrees` 无工作区目录（同 A1 的 `ls` 面）+ `git worktree list` 输出**无 `docs/worktrees/` 前缀项**（基线 R10 已满足，落盘后须保持） | F05 验收 3；architecture §3.6（检索式判据行）、§8.1-A 判断方式④ |

**停手上报条件（不自行决断）**

- 若 A5 逐字对照时发现 §4.1 头部块的字段分写标点与先例 `docs/ds/README.md` 的实测形态不同（R13）⇒ **不自行选择**：按 pr-002 明文「一律逐字采用 §4.1」执行，并按 §5-MI-02 上报主 agent（本任务图不替上游裁定）。
- 若 A10 / A11 的 R5~R9 实跑值与 §1 表不符（即 `pr-001` 落地形态变化）⇒ 不改本任务图、不改产物声明的指向，上报主 agent（属 PR 级前置变化）。

**明确不做（越界即 A3 / A9 不通过）**：不新建第二个文件（含任何记录类文件）；不改 `.gitignore`；不改 `roles/workflow-pb/workflow-pb.md`、`.claude/skills/workflow-pb/SKILL.md`（属 `pr-001`）；不改两侧 `memory.md` 与 `data/skill-optimization-*.md`（属 `pr-003` / `pr-004`）；不改 `tools/**`、`oamp/**`、`principles/**`、任何执行角色文件；不写入 `docs/iterations/**` 的其他任何文件（本 PR 施工面只有 `<产物>`）；不引入 hook / 守卫 / 检测器 / 启动前准入门；不把规范条款整段重述进产物（A7 / A8）。

---

## 3. 依赖图摘要

```mermaid
graph LR
  T1["T1 新建 docs/worktrees/README.md（头部 + 节骨架）"]
```

- **任务总数**：**1**；**任务级依赖边**：**0**；**无环**（单节点，不存在环的构造可能）。
- **最长链 / 关键路径**：**T1**（链长 1，即本 PR 的全部工作量）。
- **与 PR 依赖图的关系**：本 PR 的 `depends_on` = `pr-001-addressing-contract-and-host-skill.md`（**PR 级**前置，非任务级边）——方向为「真源 → 实例化投影」（architecture §4.3 的箭头 `SPEC --> ART`），**反向无边**（论证见 PR 文件 `depends_on` 段末「反向无边（无环论证）」）⇒ 全图 `pr-001 → pr-002` 无环。
- **PR 级前置对验收的影响**：A10 / A11 为「依赖序内可判」条目（判定时点 = `depends_on` 全部合并之后）；A1~A9、A12、A13 在 `pr-001` 已合并的现场即可判（实测见 §1）。
- **可立即开工**：**T1**（无任务级前置）。

---

## 4. 粒度与覆盖说明

**为何是 1 个任务（含被否决的拆法）**

- **下限 = 「能独立验收」**：本 PR 的施工面是**一个文件的一次原子创建**——F03 验收 1（产物存在）与 F04 验收 1（内容可读）都以整个文件为对象，任一子部分单独存在都不构成可判定的交付。
- **被否决的拆法 ①（按节拆：静态声明部分 / 引用与承接部分）**：两半都不能独立判 A1、A2、A3、A5（骨架对照需全节序）、A12（跨检出一致需全文），会产生**假依赖**（后半对前半无内容依赖，却无法独立验收）⇒ 违反粒度下限。
- **被否决的拆法 ②（单列「收口核对」任务）**：本 PR 无第二个文件面可作跨文件收口；A3 / A4 / A13 的核对判据（diff 面、`ls` / `git worktree list`）是**同一变更的属性**，属该任务的验收面而非独立工作单元（同 `pr-004-tasks.md` §4 的裁定：「不上拆出「回归核对」任务（核对属阶段 5/6 的独立验证，非实现任务）」）。
- **不设「提交 / 纳入版本控制」任务**：A2 的被跟踪性由本任务的落地动作（纳入提交）满足，非独立工作单元。

**覆盖对照（本 PR 验收条 → 任务验收编号，逐项可核对）**

| 本 PR 「验收标准」条目（含括号内卡号） | 任务验收编号 |
|---|---|
| 产物存在且落点正确（F03 验收 1、F04 验收 1） | **A1**（+ A13 的 `ls` 面） |
| 随检出即得（F03 验收 2 / 3 / 4） | **A2**、**A3**、**A4** |
| 5 类信息逐类可判（F04 验收 1~5、F21 验收 5） | **A5**、**A6**、**A7**（对应 F04 验收 4）、**A9** |
| 非第二真源（F04 验收 6） | **A8** |
| 落点语义区分（F05 验收 1~4） | **A7**、**A12**、**A13** |
| 不含动态地址（F07 验收 3） | **A12** |
| 静态声明正确（F03 验收 5 / F04 验收 2 / F07 验收 1） | **A10**、**A11** |
| 形态与体例（F21 验收 1 / 5） | **A3**、**A9** |

| 功能卡 | 任务验收编号 |
|---|---|
| F03 | A1、A2、A3、A4、A10、A11 |
| F04 | A5、A6、A8、A10 |
| F05 | A7、A12、A13 |
| F07 | A10、A12 |
| F21 | A3、A9 |

**文件范围 → 任务**：`docs/worktrees/README.md` → **T1**（唯一）。本 PR 的「不涉及（零改动面）」清单（逐项内容见本 PR「文件范围」段与 architecture §3.5）由 **A3 / A4** 的 diff 面核对覆盖（本 PR 只 1 个新增文件 ⇒ 其余面必然 0 hunk）。

**本任务图不含实现代码、不含架构决策**；全部验收标准均可追溯到 architecture v1.1.4 / 本 PR 文件 / 对应功能卡。

---

## 5. `[model_inferred]` 清单

| # | 内容 | 依据 / 需主 agent 确认的点 |
|---|---|---|
| **MI-01** | **A9 的机械辅助判据形态**（0 围栏代码块 + `grep -cE 'git +[-a-z]'` = 0）是把 F04 验收 5 的语义判据与 §4.2「不出现任何 `git …` 命令」转成裸判据的形式。 | architecture / PR 只给语义判据与「不出现任何 `git …` 命令」的表述，未给检索式；若主 agent 认为「仅人工读判」已足，删该辅助式不改变 A9 的通过条件。 |
| **MI-02** | **A5 的「逐字采用」粒度**：§4.1 称头部「照抄 `docs/ds/README.md` 先例，4 行」，而其块内字段分隔用**半角** `: `；先例实测用**全角** `：`（R13 = 1）。本任务图按 pr-002 明文（「一律逐字采用 §4.1」）取「**与 §4.1 块逐字**」读法。 | 两种读法（对 §4.1 块逐字 / 对先例逐字）会产出不同字节；本文件不替上游裁定，按 PR 明文取前者并上报。 |
| **MI-03** | **A12 的「两个不同检出」取法**：architecture / PR 未指定第二检出。本任务图读作「任一含本 PR 提交的检出」（合并后 = 任一 `iteration/0020-session-workspace-addressing` 检出；亦含验收时由 `git worktree add` 临时建立的检出），判定时点 = `depends_on` 全部合并之后。 | 该读法决定 A12 的可执行时点与操作面；不含任何「未声明的外部条件」（PR「独立性读法」注记的适用口径）。 |
| **MI-04** | **A2 的强 / 弱形态关系**：F03 验收 2 原文含「并在一次新建工作区中确认该文件随检出存在」，而 pr-002 给出的**裸判据**是 `git ls-files --error-unmatch`。本任务图以 PR 明文裸判据为**通过条件**，强形态读作同一事实的说明（不另立判据、不新增工作单元）。 | 若主 agent 要求实跑「新建工作区检出」作为 A2 的必要条件，需在验收时点增加一次 `git worktree add`（属验收动作，不改任务图）。 |
| **MI-05** | **A4 的判据形式**：以「规范文件 0 hunk ⇒ 例外清单未被改动」替代 F03 验收 4 的「读例外清单 → 例外数变为 3 或以上即不通过」。 | 本 PR 零改动规范文件，条数不可能变化；若主 agent 要求保留「点数」形式的独立判据，可在验收时补读规则 F 的「例外（闭集，仅两项）」句。 |

---

## 6. 循环依赖上报

**无**。任务级依赖边 0 条（单节点）；PR 级依赖图 `pr-001 → pr-002`、`pr-001 → pr-003`、`pr-004 → pr-003` 无环（反向无边论证见 PR 文件 `depends_on` 段末）。

---

## 7. 疑问 / 越界声明

1. **§4.1「照抄先例」与先例实测标点不同**：见 **§5-MI-02**（证据 = R13）。本任务图按 pr-002 明文取「与 §4.1 块逐字」读法；**不修改上游任何产物**。
2. **本任务图的写入位置**：写入 `prs/pr-002-tasks.md`（**规划产物**），与本 PR 的施工面 `docs/worktrees/README.md` 不同；先例 = 同目录已存在的 `pr-001-tasks.md` / `pr-004-tasks.md` ⇒ 与 pr-002 备注③「本 PR 不写入 `docs/iterations/**`」不冲突（该备注约束的是**施工产物**）。
3. **未自行判定任何落点**：头部字段名 / 节序 / 表格列名 / 五类信息模板 / 落点语义承接行形态 / 三层分层，全部以 architecture 对应节（§4.1 / §4.2 / §4.3~§4.5 / §8.1-A）为唯一真源；本文件只给「逐字对照 / 检索式 / diff 面」的可裸判定形式。
4. **未修改任何上游产物**：只新增本文件；未执行 git 写命令；未跑测试；未派生其他记录类文件。
