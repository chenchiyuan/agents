# pr-001-addressing-contract-and-host-skill

## 上下文摘要

把「会话必须在自己的会话工作区里启动」换成「会话声明工作区绝对地址并按地址显式寻址」：规范 v0.9.0→v0.10.0 与 SKILL v1.12.0→v1.13.0 同步改写规则 C/D、新增规则 H、协议产物与约束主体两节、补写隔离边界声明，并承接 0019 的 D-1 偏差。

## 涉及功能点

- F01（零前置会话启动：任意位置启动 + 工作区由会话自建，不等候不请示）
- F02（显式寻址纪律：文件写入用绝对路径、git 写操作用 `git -C <地址>`、零工具依赖）
- F03（协议产物存在与落点：规范侧声明 `docs/worktrees/README.md` 的落点与被跟踪——真源面）
- F04（产物内容骨架：规范侧给出「必须覆盖 5 类信息」的模板与「非第二真源」分工）
- F05（落点语义澄清：规范侧持有区分句真源，产物侧只作一行承接）
- F06（工作区地址确定性解析：解析链不读 `pwd`、可被简报字段显式覆盖）
- F07（地址声明三层分层：投影 1 = `status.md` 字段、投影 2 = 简报字段；源在 pr-002）
- F08（简报两级寻址纪律 + `## 文档路径协议` 基准句改写）
- F09（硬停面收窄为「工作区无法确定或无法创建」一处 + 删除 cwd 启动门 + 输出不含祈使句）
- F10（主工作区定位重述：可作启动位置、不得作工作区、写入仍受规则 F 约束）
- F11（supersede 逐句显式声明：被取代清单 + 保留清单，落在 v0.10.0 变更说明）
- F12（已知代价显式化：cwd 不在隔离范围、越界不可被 git 强制、只能事后核查）
- F13（机制骨架不变：worktree 唯一形态 / 每迭代一个 / 迭代分支 / PR 子层 / 并发算法 / 七字段）
- F14（阶段 5 子 agent 以简报给定的 PR worktree 绝对地址为准；旧「被派发时所在的工作区」口径删除）
- F15（0019 E 项承接三分类：保留 E1~E4/E6/E7/E9~E13、改写 E5/E8、新增两条）
- F16（0018 活指令失效登记 + 恢复派发覆盖句；0018 文件零改动）
- F17（D-1 承接：`### 隔离边界声明` 补章节级 `判断方式：` 行）
- F19（约束主体显式分离：「对人」零前置段与「对会话」三项纪律段分写）
- F20（做事时检测：简报带 `检测项` 一行 + 执行者自检；不设启动前准入门）
- F21（规范形态约束：零 hook / 守卫 / 工具依赖；本轮新增或改写的每条条款自带 `判断方式：`）

## 文件范围

- `roles/workflow-pb/workflow-pb.md`（**修改**，v0.9.0 → v0.10.0）：architecture §3.1 的 `N-01`~`N-21` 全集——头部版本行 + 新增 `## v0.10.0 变更说明`（N-01，`:73` 之后 / `:75` 之前）；`### 规则 C`（`:149-164`，N-02 / N-03）；`### 规则 D` 更名与全文（`:165-184`，N-04）；新增 `### 规则 H`（插入点 = `### 规则 G` 末行 `:206` 之后、既有 `### 隔离边界声明` `:208` 之前，N-05）；新增 `### 协议产物`（N-06）；新增 `### 约束主体`（N-07）；`### 隔离边界声明` 补写三条（`:210-216` / `:228` / 节末，N-08 / N-09 / N-10）；`### 规则 F` 判断方式行（`:200`，N-11）；`## 文档路径协议` 基准句（`:461`，N-12）；`### 状态追踪协议` 格式块（`:557` 之后）+ `### 启动工作流` 步骤 2（`:330`，N-13 的第二半 = `**工作区地址**` 的写入时点）；`### 派发执行角色时的 brief 构建`（`:353` 之后 2 行 + 节末 1 句，N-14 / N-15）；`### 阶段 5` 步骤 2（`:384`，N-16）；`### 启动工作流` 首段（`:325-327`）与 4.5 步（`:333`，N-17 / N-18）；`### 阶段回退` 唯一例外句（`:425`，N-19）；`### 迭代分支合并进 main` 第 3 步前置动作（`:447`，N-20）；`### 隔离边界声明` 的 `config / hooks / remotes` 行**保留**（`:215`，N-21）
- `.claude/skills/workflow-pb/SKILL.md`（**修改**，v1.12.0 → v1.13.0）：architecture §3.2 的 `K-01`~`K-09`，**逐号**（编号 → 行号 → 语义，供阶段 6 逐 hunk 归类）：`K-01`（`:44`）CRITICAL 语义换向；`K-02`（`:136`）Step 0 第 1 步；`K-03`（`:98`）Tools 首项；`K-04`（`:125`）Important facts #6 术语口径；`K-05`（`:289` 之后）新增 `工作区地址：` 一行；`K-06`（`:174`）Step 5 步骤 2 落点；`K-07`（`:216` 之后）补 1 行文档协议基准引用；`K-08`（`:374-386`）Safety 改写 2 条 + 新增 1 条；`K-09`（三处版本引用：description「（v0.9.0）」/ `:30` `**版本**: 1.12.0…v0.9.0` / `:60` Purpose）同步为 v0.10.0 / 1.13.0。`K-10` 的保留面（`:118-131` 其余 8 条 Important facts、`:194-221` §对外协议其余、`:234-273` §阶段执行卡片、`:311-337` §用户决策点与暂停格式）**逐字不动**
- **本 PR 的改动面与 `pr-002` / `pr-003` / `pr-004` 无任何文件交集**

**不涉及（零改动面，出自 architecture §3.5；越界即 F13 验收 3 / 验收 5 不通过）**：仓库根 `.gitignore`（4 行）；`tools/**`；`oamp/**`；`roles/` 下 11 个非 `workflow-pb` 目录（10 个执行角色 + `_template`）；`principles/**`；`docs/iterations/0017-*/**`、`0018-*/**`、`0019-*/**`；`workflow-pb.md` 的 `## 阶段定义`、`## PR 文件格式规范`、`## 验证目标`、`## 历史记录协议`、`## 角色读取指南`、`## 原则`、`## 何时更新本文件`、v0.2.0~v0.9.0 变更说明、`### 约束生效范围`、并发槛位算法、状态符号与槛位状态取值定义；**以及 `docs/worktrees/README.md`（pr-002）、两侧惯例记录（pr-003）、`skill-optimization-v1.12.0.md`（pr-004）**。

## 验收标准

- [ ] **版本与变更说明（N-01；F11 验收 1~3 / F15 验收 1~4 / F16 验收 2）**：`roles/workflow-pb/workflow-pb.md` 头部 `**版本**: 0.9.0` → `0.10.0`，并在 `:73`（v0.9.0 段）之后、`:75`（`## v0.6.0 变更说明`）之前新增 `## v0.10.0 变更说明（相对 v0.9.0）`，六段齐备且顺序为：触发 / 方案 / **被取代清单（逐句）+ 保留清单** / 未改变的部分 / 生效范围 / 备案段；备案段**必须**含 ① 0019 E 项三分类（保留 E1~E4 / E6 / E7 / E9~E13、改写 E5 与 E8、**明确登记为新增两条**）与 ② `0018/status.md` 的「操作前置（宿主限制，必须由用户执行）」句**已失效**的登记；备案段不出现"0018 需回改"（F16 验收 5）
- [ ] **被取代清单 = §7.1 的 5+2 项，逐句且**不**把规则 C / 规则 D 读成整条废止（F11 验收 1~3）**：① 规则 D 判据 1/2/3 作为启动门；② 规则 C「会话必须运行在自己的会话工作区里」（`:151` 首句）；③ 规则 C「会话工作区与迭代分支在**会话启动之前**…随后会话在该工作区根目录内启动」（`:161` 时点 + 后半句）；④ 规则 D 硬停输出形态块（`:173` + `:176-181`，含「创建命令」）；⑤ `## 启动工作流` 首段（`:327`）；⑤′ `:167` / `:200` 的子 agent 口径；⑤″ `:333` 时点句与 `:447` 主体。保留清单照 §7.2 的 P1~P6 六组（每迭代一个 / 落点与命名 / PR worktree 子层 / 主工作区不作为工作区 / 阶段 1~6 检出迭代分支 / 规则 F 实质语义与闭集例外两项等）
- [ ] **规则 C（N-02 / N-03；F01 验收 3/5、F06、F10 验收 1、F13 验收 2⑤、T-16）**：改写段含四要素——会话**可在项目内任意位置启动（含仓库主工作区）**、开工时**解析并以绝对地址声明**其迭代工作区（不读取 `pwd`）、**工作区不存在时由会话自行建立**（建立成功即继续，不等候不请示）、`会话工作区 = 该迭代的迭代工作区` 的**一次**等同声明；`:151` 的 worktree/clone 声明句、`:153`（含「仓库主工作区不作为任何迭代的会话工作区」）、`:155`（父子层与「PR worktree 不得被当作会话工作区使用」）、`:157-159`（落点与命名）、`:161` 的命令形态**逐字保留**；判断方式行改为**地址锚定**——`git -C <该迭代工作区地址> branch --show-current` 输出 `iteration/{迭代ID}` 且 `git worktree list` 含该地址，**不出现 `pwd` / `--show-toplevel`**
- [ ] **规则 D（N-04；F09 验收 1/4/5/6、F01 验收 3、F06 验收 1~4、F12 验收 5、F20）**：标题 = `### 规则 D：启动与寻址契约（原「启动契约」改写，v0.10.0）`（**保留编号 D**）；正文 = 解析链（`git rev-parse --path-format=absolute --git-common-dir` 的**父目录** → 地址 = `<仓库主工作区>/.pb-agents/worktrees/{迭代ID}` → 简报「工作区地址」字段优先）/ 建立（**仅当地址不存在**，命令形态 = `git -C <仓库主工作区> worktree add .pb-agents/worktrees/{迭代ID} -b iteration/{迭代ID} main`，由**会话**执行）/ 建立成功与存在性依据（`git worktree list` 含该地址 + `git -C <地址> branch --show-current`）/ **唯一硬停面**（仅「本会话工作区无法确定或无法创建」）/ 硬停输出块（**三行事实陈述**，见 architecture §5.4；**不含可复制命令、不含指向人的动作要求**；不复用 `⛔ 需要你的决策`）/ 判断方式（地址可被第三者复算 + 写入路径均在地址之下 + 无第 5 步两条依据即违反）；`:167` 首句、`:169-171` 三条判据作为启动门的用法、`:173-181` 硬停块**不再存在**（被取代清单第 ①②③④ 项逐句对应）
- [ ] **规则 H（N-05；F02 验收 1~4、F20 验收 1/4）**：新增 `### 规则 H：显式寻址纪律（v0.10.0 新增）`，插入点 = `### 规则 G` 末行（`:206`）之后、既有 `### 隔离边界声明`（`:208`）之前（三节连续插入的第一条，位置口径见 N-06 条目）；含"一切文件写入以工作区地址下的绝对路径进行 / 一切 git 写操作用 `git -C <工作区地址>` / 不依赖 cwd、不依赖任何工具（无包装脚本、无钩子、无守护进程、无环境变量前置）"与**"不主张可以阻止跨工作区写入"**；紧跟一行 `判断方式：`（出现依赖 cwd 的裸 git 写命令、或出现"越界会被拦住"类断言即违反）
- [ ] **协议产物节（N-06；F03 验收 5、F04 验收 1~6、F05 验收 1~2、F21 验收 5）**：新增 `### 协议产物：工作区间与生效规范（`docs/worktrees/README.md`）（v0.10.0 新增）`，**紧邻 `### 规则 H` 之后**（= architecture **v1.1.1** §3.1「新增三节的位置口径（单一真源）」的生效表述）；三节**连续插入**在 `### 规则 G` 末行（`:206`）之后、既有 `### 隔离边界声明`（`:208`）之前，次序固定为 `### 规则 H` → `### 协议产物` → `### 约束主体`；「三节整体位于 `### 约束生效范围`（`:244`）之前」是该口径的**推论**，**不再作为独立口径复述**（D-10 已闭合）；含产物落点与**被跟踪**（随检出即得、不改 `.gitignore`、不扩展规则 F 闭集例外）/ **必须覆盖的五类信息（模板，顺序即此序）** / **地址声明三层分工**（源 = 产物静态声明；投影 1 = `status.md`；投影 2 = 简报）与"三层不一致以源为准并登记差异（载体不限）" / **硬约束：动态地址不得写进本产物** / **落点语义区分句真源**（T-15：`docs/worktrees/` = 协议文档位、`.pb-agents/worktrees/` = 工作区池、`docs/iterations/{迭代ID}/` = 迭代产物，三者互不替代，任何会话 / PR worktree 不得建在 `docs/worktrees/` 下）/ **非第二真源**（实例化投影，规范性陈述以引用形态出现）/ 不含脚本·钩子·守卫 + `判断方式：` 行（四步：五类信息是否覆盖 / 三处事实是否一致且产物不随会话变化 / 每条规范性陈述能否回指规范条款 / `git ls-files --error-unmatch` 成功且 `git worktree list` 无 `docs/worktrees/` 前缀项）
- [ ] **约束主体节（N-07；F19 验收 1~5）**：新增 `### 约束主体（v0.10.0 新增）`，紧邻协议产物节之后；**「对人」段四项零要求逐条**（不要求进入某目录 / 不要求执行任何命令 / 不要求预先创建工作区 / 不要求重开会话）；**「对会话」段三项纪律逐条**（① 声明地址 ② 按地址显式寻址 ③ 需恢复他人工作区内容时先取备份后操作）；两段**不混写在同一句内**；紧跟一行 `判断方式：`，且违反的判定方式指向**事后核查**而非阻止
- [ ] **隔离边界声明补写（N-08 / N-09 / N-10；F12 验收 1~4/6、F17 验收 1~3）**：① 「不覆盖」清单内追加 **cwd 一条**（cwd 从来不是隔离的一部分；隔离覆盖工作树 / 索引 / HEAD 三层；会话在哪儿启动与产物落在哪儿无关）；② 「已知代价」段（`:228`）追加**越界写入**段（跨工作区写入不可能被 git 强制 ⇒ 不会被阻止、只能事后核查；"未被阻止"不等于"条款作废"；**四步核查判据**：取 `status.md` 的 `**工作区地址**` 字段 → 取该次简报的 `工作区地址` 字段 → 取执行记录中每条写入目标路径 → 逐条判"是否位于声明地址之下"，区外路径若其之前有备份动作则不判违反）；③ 节末追加**一行章节级 `判断方式：`**（覆盖覆盖 / 不覆盖 / 非 git 共享资源 / 已知代价与事后核查四类结论）；**本节既有四类结论与既有语 `:210-228` 逐字不动**（只增不改）；④ `:215` 的 `config / hooks / remotes` 行（N-21）**逐字不动**
- [ ] **规则 F 判断方式主体口径（N-11；F14 验收 2、F13 验收 3）**：`### 规则 F` 判断方式行（`:200`）的「阶段 5 的子 agent 以其**被派发时所在的工作区**为准」→「以其**简报给定的 PR worktree 绝对地址**为准」；条款正文 `:192-198`（三类写操作点名 / 不可被 git 强制 / 闭集例外两项 / 先备份后操作）**逐字不动**；改动后 `grep -rn '被派发时所在' roles/workflow-pb/workflow-pb.md .claude/skills/workflow-pb/SKILL.md` **0 命中**
- [ ] **文档路径协议基准句（N-12；F08 验收 4）**：`:461`「**全部路径相对该会话所在的迭代工作区根目录判读**（见「提交管理约束·规则 C」）」→「**全部路径相对该会话声明的工作区地址判读**（见「规则 D」/「规则 H」）；相对路径不得作为唯一寻址方式」；`:459` 的目录约定句与目录树不变
- [ ] **status.md 格式新增字段 + 写入时点（N-13；F07 验收 1、F08 验收 1、F20 验收 3）**：① `### 状态追踪协议` 的格式块内在 `**迭代分支**: iteration/{迭代ID}` 行**之后**插入 `**工作区地址**: {该迭代的迭代工作区绝对地址，见「规则 D」}`；② **写入时点 = 「启动工作流」步骤 2（`:330`；创建 `status.md`、初始化阶段表时一并写入）**——取值 = 会话开工时按「规则 D」解析并声明的地址（简报的显式覆盖值优先），**不得推迟到 4.5**（否则阶段 1 期间投影 1 缺失，与 F08 验收 1 相撞）；**与 4.5 的分工 = 4.5 只写 `**迭代分支**` 字段、不写本字段**（与 N-18 逐字对应）；**不新增步骤号**；状态符号与槛位状态取值定义（`:583-596`）不变
- [ ] **brief 字段块新增两行 + 恢复派发句（N-14 / N-15；F08 验收 1~2、F14 验收 1、F20 验收 1、F16 验收 3/5）**：在 `工作流规范：{角色定义根}/workflow-pb/workflow-pb.md` 行（`:353`）之后、`当前迭代 ID：` 行之前插入 `工作区地址：<该次派发对应的绝对地址：阶段 1~4 为该迭代的迭代工作区；阶段 5 为该 PR 的 PR worktree>` 与 `检测项：本次一切写入以「工作区地址」为根、以绝对路径进行；git 写操作使用 git -C <工作区地址>` 两行；并在该节末追加**恢复派发**句（简报的「工作区地址」字段即覆盖被恢复迭代既有文本中的任何"须在该目录内启动 / 不得在该目录外推进"类前置；**被恢复迭代的既有文本不改写**）；该节其余字段行逐字不动
- [ ] **阶段 5 步骤 2（N-16；F14 验收 3、F13 验收 4）**：`:384` 的「**在该会话工作区内执行**，落点即该工作区内的子目录」→「**在该 PR worktree 的绝对地址下执行**（`git -C <该地址>`；该地址按派发简报的「工作区地址」字段解析），落点即该工作区内的子目录」；落点 / 分支名 / base / 解锁判据 / 并发槛位算法（`min(起始 + 累计释放次数 × 起始, 硬上限)`）与该节其余文字**逐字不动**
- [ ] **启动工作流首段与 4.5（N-17 / N-18；F09 验收 6、F08 验收 4、F01 验收 3、F13 验收 2②）**：`:327` 整段改写为「**本工作流的产物路径以该会话声明的工作区地址为根**（见「文档路径协议」/「规则 H」）。会话开工前按「规则 D」解析并声明该地址（缺省时建立）；本流程**不校验启动位置**，也不新增步骤号（步骤编号体系仍为 1~5 + 4.5）」，删去「在本会话的会话工作区内运行」与三条 git 原语校验动作序列；4.5 步删去"在**会话启动之前**完成"的时点与执行者预设，改为「迭代工作区与迭代分支由会话按其工作区地址建立（缺省时，见「规则 D」）——本步骤不执行 `git branch` / `git checkout`，只负责该字段的写入（**`**工作区地址**` 字段的写入归步骤 2，见 N-13**）。此后阶段 2~5 期间，**该迭代的工作区**（根目录）保持检出该分支」；**步骤号 1~5 + 4.5 与触发时机不变**。**4.5 的字段写入职责 = 只写 `**迭代分支**` 字段**（D-4 的闭合口径：architecture **v1.1.1** 的 §3.1-N-18 与 §8.1-M 已统一为同一措辞；`**工作区地址**` 的写入归**步骤 2**，见 N-13）——阶段 5 按此单一措辞落盘
- [ ] **术语口径（N-19；F09 验收 1 的唯一性可裸判定、F13 验收 7）**：`:425`「**唯一的硬停例外**」→「**唯一的流程结构错误中止**」，措辞取 architecture §8.1-N，并写明"与「规则 D」的工作区就绪性硬停、与「需要用户决策的情况」的暂停**三类并列、互不替代**"；`### 需要用户决策的情况`（`:427-433`）五类触发条件与 `⛔ 需要你的决策` 形态**逐字保留**
- [ ] **收口主体限定（N-20；F15 验收 2 的 E8 改写）**：`:447`「仍被**该会话工作区**检出」→「仍被**该迭代的工作区**检出」，`git -C <会话工作区> checkout --detach` → `git -C <该迭代的工作区地址> checkout --detach`；收口三步（`:443-445`）的动作顺序、触发时机与 commit message 格式**逐字不变**
- [ ] **SKILL 侧同步（K-01~K-09；architecture §8.2 措辞）**：`K-01`（`:44`）CRITICAL 换向（声明地址 + 显式寻址 + 唯一硬停 + 判据见规范 §规则 D / §规则 H，槽位保留）；`K-02`（`:136`）Step 0 第 1 步改为**工作区地址解析与声明**；`K-03`（`:98`）Tools 首项改为"启动时解析并声明其迭代工作区地址 + 角色来源解析"；`K-04`（`:125`）的「才硬停」→「才中止（流程结构错误）」；`K-05`（`:289` 之后）新增 `工作区地址：<绝对路径>` 一行（与规范 `:353` 之后新增行**逐字同构**）；`K-06`（`:174`）Step 5 步骤 2 改为按简报「工作区地址」字段解析并 `git -C <地址>` 创建（子层与分支命名、base 不变）；`K-07`（`:216` 之后）补一行"产物路径的判读基准 = 该会话**声明的工作区地址**（见规范 §文档路径协议），本 skill 不重复该基准的措辞"；`K-08`（`:374-386` Safety）改写启动校验条为"开工前必须已解析并声明工作区地址；无法确定或无法创建 → 唯一硬停（三行事实陈述，不含指向人的动作要求）"、**新增**"不得把寻址纪律表述为'越界会被阻止'——跨工作区写入不可被 git 强制，只能事后核查（见规范 §规则 F / §隔离边界声明）"、保留"派发子 agent 前必须已解析角色定义根路径"条；`K-09`（三处版本引用：description / `:30` / `:60`）
- [ ] **版本一致性可裸判定（K-09；F15 / F18）**：改动后 `grep -n 'v0\.9\.0' .claude/skills/workflow-pb/SKILL.md` **0 命中**（改动前实测 3 处 = `:4` / `:30` / `:60`）；`SKILL.md:30` 读作 `**版本**: 1.13.0（对应规范 workflow-pb v0.10.0）`；`roles/workflow-pb/workflow-pb.md` 头部读作 `**版本**: 0.10.0`
- [ ] **零改动面核对（F13 验收 1~7）**：以**本 PR 分支 base**（该 PR worktree 分支自 `iteration/0020-session-workspace-addressing` 切出时刻的 tip）为基线：`git diff --name-only <base>..HEAD` **仅**含 `roles/workflow-pb/workflow-pb.md` 与 `.claude/skills/workflow-pb/SKILL.md`（其余文件属 pr-002/003/004）；逐 hunk 可归类到 `N-01`~`N-21` / `K-01`~`K-09`，**无无法归类的 hunk**；§3.5 零改动清单逐项 0 hunk；**不得**用迭代级绝对基线 `main @ dbc99b5` 判定本 PR 的改动面（同波次 PR 先合并会产生假阳性）——该绝对基线只作 architecture §3.6 的**迭代级**回归对照（阶段 6），不作为本 PR 的验收判据；`workflow-pb.md:244-246` 的 `### 约束生效范围` 句**逐字未改**且全部新条款追加在其之前（F13 验收 6）；`SKILL.md` 其余 9 条 CRITICAL（`:36` `:38` `:40` `:42` `:46` `:48` `:50` `:52` `:54`）与 `K-10` 保留面、**Resources 其余行（`SKILL.md:365-372`）** 0 hunk
- [ ] **形态约束（F21 验收 1~4、F17 验收 4、F15 验收 2/5）**：本 PR 新增 / 改写的**每一条**条款（规则 C / 规则 D / 规则 H / 协议产物 / 约束主体 / 隔离边界声明章节级 / 各改写条款）均以「条款正文 + 紧跟一行 `判断方式：`」体例落盘（**不新增 `####` 子标题、不写成独立小节**）；不引入 hook / 守卫脚本 / 强制检测器 / 启动前准入门 / 工具依赖；改动文件清单不含任何 `.sh` 或可执行脚本
- [ ] **检索式判据（architecture §3.6）**：`grep -n 'pwd\|--show-toplevel' roles/workflow-pb/workflow-pb.md .claude/skills/workflow-pb/SKILL.md` **0 命中**（F09 验收 2）；`grep -rn '被派发时所在'` 上述两文件 **0 命中**（F14 验收 2）

> **独立性读法（阶段 4 主 agent 裁定，2026-09-12）**：本节全部验收条目的独立判定时点 = 本 PR 的 `depends_on` **全部合并之后**；「独立性」不得读作"任何时刻都不需要其他 PR"，也不得读作需要任何**未声明**的外部条件。依据：`roles/workflow-pb/workflow-pb.md:514-519` 的「依赖正确性验证·通过条件」只要求 `depends_on` 每条有证据 + 依赖图无环 + PR 间文件范围无重叠 + 无遗漏功能点；`:513` 的独立性字面口径按前述读法生效，阶段 6 复核时不再按字面复判。

## 参考资料

- `docs/iterations/0020-session-workspace-addressing/architecture.md` **v1.1.1**（本 PR 的直接验收基准；v1.1.0 → v1.1.1 的增量 = Gate 阶段 4 偏差 **D-4**（4.5 只写 `**迭代分支**` 字段、`**工作区地址**` 的写入归步骤 2）与 **D-10**（新增三节位置口径单一真源）的闭合，本 PR 的条款与验收标准**已按 v1.1.1 同步**）：§2.1~§2.8 全部出现点清单、§3.1（`N-01`~`N-21`）、§3.2（`K-01`~`K-10`）、§3.5 零改动清单、§3.6 回归核对组织与检索式判据、§5.4 硬停输出形态、§7.1/§7.2（被取代 5+2 项 / 保留 P1~P6）、§8.0 体例、§8.1（A/C/D/G/H/I/J/K/L/M/N 条款全文）、§8.2（K-01~K-09 措辞）、§8.3 变更说明六段组织、§9.1（0019 E 三分类）、§9.2（D-1 承接）、§11（T-01~T-16）、§12 一致性自查 22 项（含 Gate 偏差 D-4 / D-10 的闭合）
- `docs/iterations/0020-session-workspace-addressing/prd/F01~F21*.md`（逐卡「验收标准」与「架构落地」段；本 PR 覆盖 F01~F21 中除 F18 外的 20 张卡的规范 / SKILL 承载面）
- `docs/iterations/0020-session-workspace-addressing/status.md`（基线：迭代分支 base = `main` @ `dbc99b5`；§偏离记录 D-1 的活先例）
- `roles/workflow-pb/workflow-pb.md`（改动基线 v0.9.0 @ `dbc99b5`）：`:25`（版本行）/ `:65-73`（v0.9.0 说明段）/ `:137-206`（规则 A~G 与各自 `判断方式：` 行）/ `:208-228`（隔离边界声明）/ `:244-246`（约束生效范围）/ `:325-333`（启动工作流 + 4.5）/ `:341-367`（brief 构建）/ `:379-401`（阶段 5）/ `:418-433`（阶段回退 + 用户决策）/ `:440-455`（收口）/ `:457-461`（文档路径协议）/ `:545-596`（状态追踪协议）
- `.claude/skills/workflow-pb/SKILL.md`（改动基线 v1.12.0）：`:4` / `:30` / `:60`（版本三处）/ `:44`（CRITICAL）/ `:98`（Tools）/ `:125`（Important facts #6）/ `:136`（Step 0 第 1 步）/ `:174`（Step 5 步骤 2）/ `:216`（文档协议）/ `:289`（brief 字段块）/ `:374-386`（Safety）
- `docs/iterations/0019-worktree-isolation-protocol/clarifications/verify-stage6-20260912-200521.md`（偏差 D-1 原文要点；本迭代由 `N-10` 承接）
- `docs/iterations/0019-worktree-isolation-protocol/prs/pr-001-workflow-pb-workspace-isolation-protocol.md`（**粒度先例**：规范 + SKILL 同 PR 的论证方法与验收体例）

## depends_on

（无）

> **说明（含粒度判定与反向无边论证）**：① 本 PR 无对外依赖——全部被引用的对象（规范既有条款、`.gitignore`、`oamp/**`、前序迭代产物）在本 PR 落地时已存在；`docs/worktrees/README.md` 的**存在性**由本 PR 的 `### 协议产物` 条款作为本迭代变更集的一部分**声明**（真源 → 投影方向，见 architecture §4.3），不构成 `pr-002` → 本 PR 的边。
> ② **规范与 SKILL 必须同 PR（不是省事合并）**：F09 验收 2 的判据是**跨文件**判据（`grep 'pwd\|--show-toplevel'` 覆盖两文件 = 0 命中），拆开则任一 PR 都无法独立验收；SKILL 改写后的文本直接引用规范**新增**章节名（`§规则 H`）与**更名**后的 `§规则 D`，先落地的一侧必然留下悬空引用；且本迭代阶段 5 的宿主调度正由 SKILL 驱动，拆分会产生"规范已无启动门、SKILL 仍命令发现即停"的窗口期（两条活文本互相矛盾）。**先例**：`0005` / `0007` / `0008` / `0019` 四次协议自我修改迭代的规范 + SKILL 均判**单 PR**，其中 0019 的论证原文见 `docs/iterations/0019-.../prs/pr-001-...md` 的 batch 备注③；物理证据 = 提交 `c034dab` 的 6 文件同一提交。
> ③ **惯例记录不与规范同 PR（→ `pr-003`；独立性读法见「验收标准」段末注记）**：`roles/workflow-pb/data/workflow-pb-changelog.md`、两侧 `memory.md`、`data/skill-optimization-v1.13.0.md` 与规范 / SKILL **文件范围零交集**，且在其 `depends_on`（`pr-001` + `pr-004`）**全部合并之后**，其验收判据（体例 / 内容完整性 / 与本次改动的登记一致性）可逐条独立执行 ⇒ 按物理文件切分为 `pr-003` 并声明 `depends_on`（证据见 `pr-003`）。**与先例的差异如实登记**：`0005` / `0007` / `0008` / `0019` 把两侧惯例记录并入规范 PR（物理证据 = 提交 `c034dab` 的六文件同一提交）；本次以「PR 间文件范围无重叠」优先拆出，代价 = 两条已带文本级证据的依赖边（`pr-001 → pr-003`、`pr-004 → pr-003`）。

## batch

1

> **备注**：① 本 PR 与 `pr-002` / `pr-003` / `pr-004` 的文件范围**零交集**；`batch` 仅作人工速览分组（本迭代主交付 = 寻址契约 + 宿主 skill），调度依据是 `depends_on`。② **上下文摘要字数口径**：按全字符（Unicode，含标点与空格）≤ 200 计（沿用 0019 阶段 4 的裁定口径），本 PR 摘要实测 ≤ 200 字符。③ 本 PR 的验收标准**不引用** architecture §3.5 零改动清单之外的任何文件，阶段 5 施工与阶段 6 归类均以 architecture §3.1/§3.2 的 `N` / `K` 编号为唯一基准（编号空间与 0019 的 `W` / `S` 不共用）。
