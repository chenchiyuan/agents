# 工作流进度

**工作流**: workflow-pb v0.9.0（本迭代按规则 C/D 的**修正方向**运行：会话在仓库主工作区启动、以显式寻址操作本迭代工作区——见「偏离记录 D-1」）
**迭代**: 0020-session-workspace-addressing
**当前阶段**: 需求收敛（阶段 1）
**迭代分支**: iteration/0020-session-workspace-addressing（base = main @ `dbc99b5`，含 0017 与 0019 全部产出）
**会话工作区**: `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0020-session-workspace-addressing`
**状态**: 进行中
**history**: 开启
**前置**: 用户对迭代 0019 交付的 v0.9.0「启动契约」提出设计异议（原话见 §用户确认记录）⇒ 本迭代修正协议本身。

## 阶段状态

| # | 阶段 | 完成 | 已验证 | 备注 |
|---|---|---|---|---|
| 1 | 需求收敛 | ⏸ | ⬜ | 已派发 demand；输入 = 用户原话 + 主 agent 复盘 + v0.9.0 现状与 0019 验证记录 |
| 2 | 功能规格 | ⬜ | ⬜ | |
| 3 | 技术架构 | ⬜ | ⬜ | |
| 4 | PR 规划 | ⬜ | ⬜ | |
| 5 | PR 实现 | ⬜ | ⬜ | 逐 PR 状态见下 |
| 6 | 独立验证 | — | — | 按需触发，不计入线性进度 |

## PR 实现子状态（阶段 5 展开）

待阶段 4 产出 `prs/` 后初始化。

## 并发配置（阶段 5）

待阶段 4→5 入口初始化。

## 偏离记录

- **D-1（主 agent 主动登记）**：本会话 cwd = `/Users/chenchiyuan/projects/agents`（仓库主工作区，分支 `main`），**未**在其会话工作区内启动 ⇒ 按 v0.9.0 规则 D 的字面规定本应「硬停」。
  · 主 agent 判定不硬停的理由：① 规则 D 的 cwd 判据正是本迭代要修正的对象，硬停会使本迭代无法被启动（自指死锁）；② 用户已明确「不应该对使用者有任何强制」⇒ 降级为**显式寻址模式**运行；③ 该偏离同时作为本迭代需求的第一份实证（见 demand 输入）。
  · 风险控制：本迭代的一切读/写一律使用**绝对路径**（根 = 上方「会话工作区」）；每次派发的简报都携带「工作目录纪律」固定段；每阶段产物落盘后立即提交到 `iteration/0020-…` 分支。
- **D-2（前序同步项）**：迭代 0018 的 `status.md` §恢复设置 中「会话必须在其工作区内启动」的操作前置，待本迭代裁决后同步修正。

## 用户确认记录（Gate）

- **迭代 ID**：`0020-session-workspace-addressing`（主 agent 拟定并采用；用户未指定名称）。
- **需求方向（用户原话，逐字）**：
  > 我觉得是协议定义问题，我应该在项目中启动agent客户端都行。 worktree定义到指定路径就能工作，不用我cd 到对应目录，这是git协议的问题，定义好git的工作方式就行了，不应该对使用者有任何强制
  > （同义重申）我觉得是协议定义问题，我应该在项目中启动agent客户端都行。 worktree定义到指定路径就能工作，不用我cd 到对应目录，这是git协议的问题，定义好git的工作方式就行了，不应该对使用者有任何强制

## 更新日志

- 2026-09-12: 迭代启动。Step 0 前提声明——本宿主具备真实阻塞式人机交互通道（`ask`）；按 v0.9.0 规则 C 的命令形态创建迭代分支与工作区（`git worktree add .pb-agents/worktrees/0020-… -b iteration/0020-… main`）；会话在仓库主工作区启动（见偏离记录 D-1）。
- 2026-09-12: 创建 status.md + history.md → 派发 demand（第一轮），简报带「工作目录纪律」固定段与绝对路径。
