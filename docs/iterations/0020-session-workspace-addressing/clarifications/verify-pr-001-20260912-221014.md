# verify-pr-001-20260912-221014.md — `pr-001-addressing-contract-and-host-skill` PR 级独立验收

**验证者身份**：**契约审查者 + 同级文本审查者**（反射依据：被验证产出物 = 规范 / SKILL 的**文本实现**，属「文本 / 规范实现」类；契约审查者判「条款语义与判据齐备性」，同级文本审查者判「措辞与指定真源的逐字 / 措辞级等价性」。两名合成一个身份，对同一条款分别出结论，分歧处显式标注。）
**产出物**：PR worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0020-session-workspace-addressing/.pb-agents/worktrees/0020-pr-001`（分支 `feat/0020-pr-001-addressing-contract`，HEAD = `de5c0af`，base = `3d1d4c6`）；变更集 = `git diff 3d1d4c6..HEAD`
**验证标准来源**：主 agent 委托的 6 条验证标准 + `prs/pr-001-addressing-contract-and-host-skill.md`「验收标准」段 + `prs/pr-001-tasks.md` 的 T-01~T-22（内容真源 = `architecture.md` v1.1.4）
**验证日期**：2026-09-12
**独立性声明**：本报告只读 PR worktree 与两个产出物文件 + 只读 `git show/diff/status`；**未接收任何执行过程上下文**（未读产出者报告、未读讨论过程、未读前序轮次验证报告、未读主 agent 裁定理由）；未修改任何被验证文件，未执行 git 写命令。

---

## A. PR 文件验收标准（21 条，逐条判定）

### A-1 「版本与变更说明（N-01；F11 / F15 / F16）」：**pass**
- 判据①：`grep -n '^\*\*版本\*\*' roles/workflow-pb/workflow-pb.md` → `:25 **版本**: 0.10.0`。
- 判据②：`## v0.10.0 变更说明（相对 v0.9.0）` 存在于 `:75`，段构成 = 概述(`:77`) / 目的(`:79`) / 被取代清单(`:81-88`) + 保留清单(`:90`) / 取代的语义边界(`:92`) / 不改变的部分(`:94`) / 生效范围(`:96`) / 备案(`:98-101`)，与 `architecture §8.3 表 A`（6 段：①概述 ②目的 ③被取代+保留 ④不改变 ⑤生效范围 ⑥备案）逐段对应；`:74`（v0.9.0 段后的空行）处纯插入 28 行，落点 = §3.1-N-01 的「`:73` 之后、`:75` 之前」。
- 判据③：备案段三项登记齐备——0018 活指令失效登记(`:99`)、0019 E 项三分类承接(`:100`，含「**新增两条**」明确登记)、D-7 留痕指引(`:101`)；与 §9.1（保留 E1~E4/E6/E7/E9~E13、改写 E5/E8、新增①②）与 §9.2（D-1/D-7）逐条同源；`grep -n '回改'` → **0 命中**（不存在「0018 需回改」）。

### A-2 「被取代清单与保留清单（F11 验收 1~3）」：**pass**
- 判据①：被取代项 7 条（§7.1 的 5 + 补充 ⑤′/⑤″，逐项以 `### 规则 D` 判据 1/2/3、`### 规则 C` 首句、`:161` 时点句、硬停块、「启动工作流」首段、规则 D 句首 + 规则 F 判断方式、4.5 时点 + `:447` 主体 定位），保留项 P1~P6 逐条列出，与 §7.1 / §7.2 句级对照成立（全程不用行号）。
- 判据②：`**取代的语义边界**`(`:92`) 明写「**规则 C 与规则 D 都不整体废止**——两条规则仍在，只是绑定方式与判据形态换了」，与 §7.2 末段一致。
- 偏差：第 1、5 条引文中 `--show-toplevel` / `pwd` 两枚原语被转写（见偏差表 #3）——架构同时要求「逐句引 v0.9.0 原句」与「全文件 `pwd`/`--show-toplevel` 0 命中」，二者字面不可同时满足，实现选择后者（检索式判据）并保留其余引用文字。

### A-3 「规则 C（N-02 / N-03）」：**pass**
- 判据①：§8.1-C 四要素齐备——「可在**项目内任意位置启动**（含仓库主工作区）…不构成合规判据」(`:179`)、「开工时**解析并以绝对地址声明**其迭代工作区」(`:179`)、「**工作区不存在时由会话自行建立**，建立成功即继续推进（不等候、不请示）」(`:179`)、「会话工作区 = 该迭代的迭代工作区（本规范两词同指一个实体）」(`:179`)；`grep -c '两词同指一个实体'` → **恰 1**。
- 判据②：判断方式行(`:189`)= 「以**地址**为对象、可从任意位置复核——`git -C <该迭代工作区地址> branch --show-current` 输出 `iteration/{迭代ID}`，且 `git worktree list` 的输出含该地址」——逐字同 §8.1-C 末行。
- 判据③：`grep -n 'pwd\|--show-toplevel'` 全文件 → **0 命中**（基线 4 行 `:163/:169/:184/:327`）。

### A-4 「规则 D（N-04）」：**pass**
- 判据①：标题 `### 规则 D：启动与寻址契约（原「启动契约」改写，v0.10.0）`(`:191`) 逐字同 §8.1-D 首行；解析链 5 步(`:195-199`)、唯一硬停面(`:201`)、三行事实陈述块(`:204-207`)、不复用 ⛔(`:210`)、约束主体(`:212`)、判断方式(`:214`) 全部落盘。
- 判据②：硬停块 = 1 行标题 + 3 行事实陈述（`触发原因` / `当前状态` / `未完成事项`），与 §5.4 块逐字一致；块内无命令块（`git worktree add` 0 命中）、无指向人的动作要求（`请` / `建议你` / `你必须` 0 命中）、无 `⛔`。
- 判据③：旧启动门三条判据与旧硬停块在**活条款**中不再存在（`grep -rn '启动校验'` 两文件 → **0 命中**；`grep -rn '创建命令'` → 仅 `:85` 的**被取代清单引文**；`grep -rn '发现即停'` → `:67`（v0.9.0 历史变更说明，§3.5 要求历史记录不改）、`:79`/`:86`（变更说明的目的段与被取代清单引文）、`:201`（§8.1-D / §5.4 明文规定的停的时点）），四条残留均为架构明文要求保留 / 引用的位置。
- 判据④偏差点：`解析链` 行把 §8.1-D 的「不读取 `pwd`」转写为「**不读取会话的当前工作目录**」（见偏差表 #2）。

### A-5 「规则 H（N-05）」：**pass**
- 判据①：`### 规则 H：显式寻址纪律（v0.10.0 新增）` 位于 `:238`，前邻 `### 规则 G`（`:232` 起）、后邻 `### 协议产物`（`:244`）——按 §3.1「新增三节的位置口径」落位。
- 判据②：正文含「本纪律**不主张可以阻止跨工作区写入**」(`:240`)。
- 判据③：紧跟一行 `判断方式：`(`:242`)，其两条判据可裸判（依赖 cwd 的裸 git 写命令 / 「越界会被拦住」类断言即违反）。

### A-6 「协议产物节（N-06）」：**pass**
- 判据①：`### 协议产物：工作区间与生效规范（docs/worktrees/README.md）（v0.10.0 新增）`(`:244`) 紧邻规则 H 之后（`:238` → `:244` → `:256`）。
- 判据②：五类信息模板(`:246`，①工作区间定义 ②生效规范与版本 ③寻址纪律引用 ④落点语义一行 ⑤无脚本/钩子/守卫)、地址声明三层分工 + 以源为准 + 差异登记(`:248`)、动态地址不得写入产物(`:248`)、落点语义(`:250`)、非第二真源(`:252`) 逐项可读。
- 判据③：`判断方式：`(`:254`) 四步齐备且含 `git ls-files --error-unmatch docs/worktrees/README.md` 成功、`git worktree list` 无 `docs/worktrees/` 前缀项、两检出逐字节相同。
- 偏差点：§4.5 为「落点语义」段单列的 `判断方式：` 行未单独落地（其判据已由 §8.1-A 判断方式步骤 ④ 覆盖；§8.0 规定新增 4 处各一行判断方式）——见偏差表 #5。

### A-7 「约束主体节（N-07）」：**pass**
- 判据①/②：「对人」四项零要求(`:258`) 与「对会话」三项纪律(`:260`) 各自独立成段、不混写在同一句内。
- 判据③：`判断方式：`(`:262`) 指向事后核查（「两项不混写」「违反的判定方式指向事后核查而非阻止」），不出现「会被阻止」类断言。

### A-8 「隔离边界声明补写（N-08 / N-09 / N-10）」：**pass**
- 判据①：三段与 §8.1-G 逐字对照——cwd 一条(`:273`)、越界写入段(`:288`，含四步核查判据)、章节级 `判断方式：`(`:290`，覆盖四类结论)。
- 判据②：本节为**纯增不改**——hunk `@@ -216,0 +273,2 @@` 与 `@@ -229,0 +288,4 @@` 均为 `+` 行；基线 `:208-231` 全部非空行在实现后**逐字存在且次序不变**；`config / hooks / remotes` 一行（`N-21`，`:215` → `:271`）字节级相同。
- 判据③：章节级 `判断方式：` 覆盖四类结论，与子项判断方式行并列不互替（子项判断方式仍在表格单元格内）。
- 偏差点：cwd 条与前置 `config / hooks / remotes` 条之间被插入 1 空行（原清单为紧凑列表）——见偏差表 #11。

### A-9 「规则 F 判断方式主体口径（N-11）」：**pass**
- 判据①：`grep -rn '被派发时所在'` 两文件 → **0 命中**；判断方式行(`:230`) 主体改为「以其简报给定的 PR worktree 绝对地址为准」。
- 判据②：规则 F 正文与闭集例外两项**逐字不动**（基线 `:192-198` 全部非空行在实现后逐字存在；`@@ -200 +230 @@` 是本节唯一 hunk）。

### A-10 「文档路径协议基准句（N-12）」：**pass**
- 判据①：`:527` = 「**全部路径相对该会话声明的工作区地址判读**（见「规则 D」/「规则 H」）；相对路径不得作为唯一寻址方式。」（逐字同 §8.1-I）；`grep -n '相对该会话所在的迭代工作区根目录'` → 0 命中。
- 判据②：该节其余句（产物落点目录约定 `:525` + 目录树 `:529-544`）在 diff 中 0 hunk（基线 `:458-465` 除基准句外逐字存在）。

### A-11 「status.md 字段与写入时点（N-13）」：**pass**
- 判据①：`**工作区地址**: {该迭代的迭代工作区绝对地址，见「规则 D」}` 位于 `## 状态追踪协议（status.md）` 的 `### status.md 格式` 代码块**内**（`:617` 开栏、`:624` 字段行），紧跟 `**迭代分支**: iteration/{迭代ID}`(`:623`)——位置同 §8.1-J。
- 判据②：写入时点 = 「启动工作流」**步骤 2**(`:392`「创建…status.md，初始化阶段表，**并写入 `**工作区地址**`**」)；4.5(`:395`) **只写 `**迭代分支**` 字段**（不写 `**工作区地址**`）。
- 判据③：步骤号 1~5 + 4.5 未增删（`:391-401`），无新增步骤号行。
- 判据④：状态符号与槛位状态取值定义 0 hunk（基线 `:583-596` 逐字存在；该区间无 changed 行）。

### A-12 「brief 字段块新增行 + 恢复派发句（N-14 / N-15）」：**pass**
- 判据①：`工作区地址：<该次派发对应的绝对地址：阶段 1~4 为该迭代的迭代工作区；阶段 5 为该 PR 的 PR worktree>`(`:416`) 与 `检测项：本次一切写入以「工作区地址」为根、以绝对路径进行；git 写操作使用 git -C <工作区地址>`(`:417`) 位于 `工作流规范：`(`:415`) 之后、`当前迭代 ID：`(`:418`) 之前；该行与 SKILL 侧新增行**字节级同构**（见 A-17）。
- 判据②：检测项行与 §5.5「检测挂现场」行**逐字一致**（机器比对 True）。
- 判据③：恢复派发句(`:433`) 含「覆盖被恢复迭代既有文本中的任何…前置」与「**被恢复迭代的既有文本不改写**」两项，逐字同 §8.1-K 末块。
- 另：该节其余字段行 0 hunk（`@@ -353,0 +416,2 @@` 与 `@@ -368,0 +433,2 @@` 均为纯插入）。

### A-13 「阶段 5 步骤 2（N-16）」：**pass**
- 判据①：`:450` 以「**在该 PR worktree 的绝对地址下执行**（`git -C <该地址>`；该地址按派发简报的「工作区地址」字段解析）」表述（逐字同 §8.1-L）。
- 判据②：字符级 diff 显示本行**只**替换了「**在该会话工作区内执行**」这一片段；落点 / 分支名 / base / 解锁判据 / 并发槛位算法与其余文字逐字不动（算法区间 `:394-401` 无 changed 行）。

### A-14 「启动工作流首段与 4.5（N-13 / N-17 / N-18）」：**pass**
- 判据①：首段(`:389`) = 「**本工作流的产物路径以该会话声明的工作区地址为根**（见「文档路径协议」/「规则 H」）。会话开工前按「规则 D」解析并声明该地址（缺省时建立）；本流程**不校验启动位置**，也不新增步骤号（步骤编号体系仍为 1~5 + 4.5）。」——逐字同 §8.1-M 首段块，`grep '启动校验'` 0 命中。
- 判据②：4.5(`:395`) 只写 `**迭代分支**` 字段（`**工作区地址**` 归步骤 2）。
- 判据③：步骤号体系与触发时机不变。

### A-15 「术语口径（N-19）」：**pass**
- 判据①：`### 阶段回退` 内 `:491` = 「**唯一的流程结构错误中止**：…（与「规则 D」的工作区就绪性硬停、与「需要用户决策的情况」的暂停**三类并列、互不替代**。）」；`grep -c '流程结构错误中止'` → 2；`grep -n '唯一的硬停例外'` → 0 命中。
- 判据②：`### 需要用户决策的情况`(`:493-500`) 五类触发条件逐字保留（基线 `:427-433` 逐字存在，0 hunk）；`⛔ 需要你的决策` 形态在 SKILL `:331` 逐字保留（SKILL `:311-337` 全区间 0 hunk）。

### A-16 「收口主体限定（N-20）」：**pass**
- 判据①：`:513` = 「若 `iteration/{迭代ID}` 仍被**该迭代的工作区**检出，先执行 `git -C <该迭代的工作区地址> checkout --detach`」。
- 判据②：收口三步(`:509-511`) 的动作顺序、触发时机(`:508`) 与 commit message 格式(`:515`) 0 hunk。

### A-17 「SKILL 侧同步（K-01~K-09）」：**pass**
- 判据①：SKILL 侧 **11 个 hunk 全部可归类**：`:4`/`:30`/`:60` → K-09①；`:44` → K-01；`:98` → K-03；`:125` → K-04；`:136` → K-02；`:174` → K-06；`:216` → K-07（§文档协议）；`:289` → K-05；`:386→+2` → K-08。无无法归类的 hunk。
- 判据②：`K-10` 保留面 0 hunk——`:234-273`（阶段执行卡片）、`:311-337`（用户决策点）、`:118-131`（Important facts 其余 8 条）全区间逐字保留；`:194-221` 仅 `:216` 一处（K-07 的明文落点，§3.2-K-10 标注为「对外协议**其余**」）。
- 措辞：K-01(`:44`)、K-02(`:136`)、K-03(`:98`)、K-04(`:125`)、K-07(`:217`)、K-08(`:389-390`) 均与 §8.2 对应行逐字一致；K-05(`:290`) 与规范侧 §8.1-K 对应行字节级同构。

### A-18 「版本一致性可裸判定（K-09 版本引用部分）」：**pass**
- `grep -n 'v0\.9\.0' .claude/skills/workflow-pb/SKILL.md` → **0 命中**（基线实测 3 处 `:4`/`:30`/`:60`）。
- SKILL `:30` = `**版本**: 1.13.0（对应规范 workflow-pb v0.10.0）`；规范 `:25` = `**版本**: 0.10.0`；`K-09` ②③（新建 `data/skill-optimization-v1.13.0.md` + 两侧 `memory.md` 索引行）未出现在本 PR 变更集（归 pr-003 / O-04，非「无法归类的 hunk」）。

### A-19 「零改动面核对（F13 验收 1~7）」：**pass**
- `git diff --name-only 3d1d4c6..HEAD` **恰含** `roles/workflow-pb/workflow-pb.md` 与 `.claude/skills/workflow-pb/SKILL.md` 两个文件（base 校验：`3d1d4c6` 是 PR 分支 `merge-base`，且是 `iteration/0020-session-workspace-addressing` 切出时刻的 tip —— 其后继 `b3cc5e7` 为阶段 5 tasks 提交，非本 PR 面）。
- 逐 hunk 归类：规范侧 25 hunk → N-01(`:25`/`:74`)、N-02(`:149`/`:151`/`:161`)、N-03(`:161` 同 hunk)、N-04(`:165`/`:167`/`:169`/`:173`/`:176`/`:184`)、N-11(`:200`)、N-05+N-06+N-07(`:207`，26 行单 hunk，按节可归类)、N-08(`:216`)、N-09+N-10(`:229`)、N-17(`:327`)、N-13(`:330`/`:557`)、N-18(`:333`)、N-14(`:353`)、N-15(`:368`)、N-16(`:384`)、N-19(`:425`)、N-20(`:447`)、N-12(`:461`)；**N-21 保留 0 hunk**。无无法归类的 hunk。
- §3.5 零改动清单逐项 0 hunk：`.gitignore` / `tools/**` / `oamp/**` / `roles/` 其余 11 目录 / `principles/**` / `0017`·`0018`·`0019` 迭代目录 / 规范 `## 阶段定义`·`## PR 文件格式规范`·`## 验证目标`·`## 历史记录协议`·`## 角色读取指南`·`## 原则`·`## 何时更新本文件` / v0.2.0~v0.9.0 变更说明（`:45-113` 全行逐字存在，唯一落在 `:74` 的是纯插入点）/ `### 约束生效范围`（`:244-246` 逐字不动）/ 并发槛位算法（`:394-401`）/ 状态符号与槛位取值（`:583-596`）/ SKILL 其余 9 条 CRITICAL 与 Resources 其余行。
- `### 约束生效范围` 句逐字未改，且全部新条款在其之前（新三节 `:238`/`:244`/`:256`、隔离边界声明补写 `:273`/`:288`/`:290`，均早于 `:306`）。
- 未使用迭代级绝对基线 `main @ dbc99b5` 作本 PR 判据（本报告一律以 `3d1d4c6` 为基线）。

### A-20 「形态约束（F21 验收 1~4、F17 验收 4、F15 验收 2/5）」：**pass**
- 新增 / 改写的 4 处新条款均「正文 + 紧跟一行 `判断方式：`」且**不新增 `####` 子标题**：`grep -c '^#### '` 两文件 → 0 / 0；diff 中 `+####` 行 → 0。判断方式行数 8 → **12**（+4 = 规则 H / 协议产物 / 约束主体 / 隔离边界声明章节级，与 §8.0 一致）。
- 不引入 hook / 守卫脚本 / 强制检测器 / 启动前准入门 / 工具依赖：新增文本中 `无包装脚本、无钩子` 为声明句；`检测项` 为简报一行且唯一硬停面不含检测项（规则 D 仅两个触发条件）；diff 文件清单不含 `.sh` 或任何可执行脚本。

### A-21 「检索式判据」：**pass**

| 检索式 | 实测 | 期望 | 判定 |
|---|---|---|---|
| `grep -n 'pwd\|--show-toplevel' roles/workflow-pb/workflow-pb.md` | 0 | 0 | pass |
| 同上 `.claude/skills/workflow-pb/SKILL.md` | 0 | 0 | pass |
| `grep -rn '被派发时所在'` 两文件 | 0 | 0 | pass |
| `grep -n 'v0\.9\.0' SKILL.md` | 0 | 0 | pass |
| 版本行 | `0.10.0` / `1.13.0（对应规范 workflow-pb v0.10.0）` | 同 | pass |

---

## B. tasks 文件 T-01~T-22（逐条判定，含其实跑判据）

| 任务 | 判定 | 关键证据（实跑值） |
|---|---|---|
| **T-01** 规则 C 改写与判断方式地址锚定 | pass | 四要素齐备(`:179`)；判断方式地址锚定(`:189`)；`两词同指一个实体` = 1；P1~P5 保留句逐字存在（基线 `:153`/`:155`/`:157-159` 逐字命中）；本节 `pwd`/`--show-toplevel` 0；体例合规 |
| **T-02** 规则 D 更名与全文重写 | pass | 标题逐字；解析链 5 步 + 唯一硬停面 + 三行块 + 不复用 + 约束主体 + 判断方式齐备；块内 `git worktree add` 0、`请/建议你/你必须` 0、`⛔` 0；`启动校验` 0；`创建命令` 仅存于被取代清单引文 |
| **T-03** 新增规则 H | pass | `grep -c '^### 规则 H'` = 1；节序 `规则 G → 规则 H → 协议产物 → 约束主体 → 隔离边界声明` 成立；含「不主张可以阻止跨工作区写入」；`判断方式` 两条可裸判 |
| **T-04** 新增协议产物节 | pass | 节标题恰 1；五类信息 / 三层分工 / 动态地址不入产物 / 非第二真源可读；判断方式四步可裸判（含 `git ls-files --error-unmatch` 与 `git worktree list` 两条）；体例合规。范围注记：产物**存在性**的运行结果归 pr-002，本 PR 只判条款文本 —— 已按此口径判定 |
| **T-05** 新增约束主体 | pass | 对人四项 / 对会话三项逐项存在；两段独立成段不混写；判断方式指向事后核查 |
| **T-06** 隔离边界声明三段补写 | pass | 三段逐字对照 §8.1-G；本节**纯增不改**（两个 hunk 均 0 删除行）；`config / hooks / remotes` 行 0 hunk；章节级判断方式覆盖四类；既有子项判断方式行 0 hunk |
| **T-07** 规则 F 判断方式主体口径 | pass | 形态逐字同 §3.1-N-11；本行内 `被派发时所在` 0；规则 F 正文（含闭集例外）0 hunk |
| **T-08** 文档路径协议基准句 | pass | 逐字同 §8.1-I；`相对该会话所在的迭代工作区根目录` 0；其余句 0 hunk |
| **T-09** status.md 新增 `**工作区地址**` 字段 | pass | 逐字同 §8.1-J；位于格式块内、紧跟 `**迭代分支**`；状态符号与槛位取值 0 hunk；本节其余字段行 0 hunk |
| **T-10** 启动工作流首段 + 步骤 2 + 4.5 | pass | 三块逐字同 §8.1-M；4.5 与 §3.1-N-18 逐字一致（只写 `**迭代分支**`、不执行 `git branch`/`git checkout`、不写 `**工作区地址**`）；步骤号 1~5+4.5 不变；本节 `pwd`/`--show-toplevel` 0 |
| **T-11** brief 字段块两行 + 恢复派发句 | pass | 两行落位正确；`检测项` 与 §5.5 逐字一致；恢复派发句逐字同 §8.1-K 末块（含「不改写」）；其余字段行 0 hunk |
| **T-12** 阶段 5 步骤 2 | pass | 逐字同 §8.1-L；字符级 diff 仅替换执行位置片段，落点/base/解锁判据/并发算法 0 hunk；地址来源字段名 = `工作区地址` ✓ |
| **T-13** 术语口径 + 收口主体限定 | pass | `流程结构错误中止` = 2；五类触发条件 + `⛔` 形态 0 hunk；`:513` 主体 = 该迭代的工作区 + `git -C <该迭代的工作区地址>`；收口三步/触发时机/commit 格式 0 hunk |
| **T-14** 头部版本行 + 新增变更说明 | pass | `0.10.0`；6 段构成同 §8.3 表 A；段③ 与 §7.1/§7.2 句级对照成立；段⑥ 三项登记同 §9.1/§9.2 且无「0018 需回改」；段② 含三条一手反证；v0.2.0~v0.9.0 说明 0 内容行改动；读不出「整条废止」 |
| **T-15** K-01 | pass | 逐字同 §8.2-K-01；其余 9 条 CRITICAL 0 hunk；本行 `pwd`/`--show-toplevel` 0；引用节名 `§规则 D` / `§规则 H` 均在规范中真实存在 |
| **T-16** K-02 / K-03 | pass | 两处逐字同 §8.2；Step 0 第 2~4 步与 Tools 其余项 0 hunk；两处范围内 `pwd`/`--show-toplevel` 0 |
| **T-17** K-04 | pass | 逐字同 §8.2-K-04；`流程结构错误` 1 命中；与规范侧 N-19 同名不冲突；Important facts 其余 8 条 0 hunk |
| **T-18** K-05 / K-07 | pass | K-05 行与规范侧 §8.1-K 对应行**字节级同构**；K-07 为单行引用；两节其余内容 0 hunk。（判据「逐字对照 §8.2-K-05」的 `<绝对路径>` 占位简写与「与规范侧逐字同构」不可同时满足，实现取后者 —— 偏差表 #6） |
| **T-19** K-06 | pass | 改写句与 §8.2-K-06 语义一致（绝对地址 + `git -C <地址>` 创建）；分支名 / base 0 hunk；与 §8.1-L 表述同一事实无冲突。（「子层」落点路径字面被地址取代 = §8.2-K-06 明文要求 —— 偏差表 #8） |
| **T-20** K-08 | pass | §8.2-K-08 的两条指定文本均逐字落盘；新增条含「不可被 git 强制、只能事后核查」并引用真实节名；「派发子 agent 前…」条与 Safety 其余条 0 hunk |
| **T-21** K-09 三处版本引用 | pass | 三处逐字同 §8.2-K-09；`v0.9.0` 0 命中；`1.13.0` ↔ 规范 `0.10.0` 一致；②③ 面的跨 PR 归属按 §8.2-K-09 末段拆分，非「无法归类」 |
| **T-22** 跨文件收口核对 | pass | 改动面恰 2 文件；36 hunk 全部可归类；§3.5 清单逐项 0 hunk；三条检索式判据全 0；版本行合规；无 `+####`、无脚本、无 hook / 准入门；`### 约束生效范围` 逐字未改且在其之前；未用迭代级绝对基线 |

---

## C. 委托的 6 条验证标准

| 标准 | 判定 | 证据摘要 |
|---|---|---|
| **C-1 PR 验收标准逐条满足** | pass | 21 条全 pass（见 A 段） |
| **C-2 tasks T-01~T-22 逐条满足** | pass | 22 条全 pass（见 B 段）；全部检索式判据按 tasks §1 口径实跑 |
| **C-3 文件范围恰 2 个** | pass | `git diff --name-only 3d1d4c6..HEAD` = 2 文件；`--stat` = `roles/workflow-pb/workflow-pb.md`(+)/(-) 与 `.claude/skills/workflow-pb/SKILL.md`；无第三文件、无 `.sh` |
| **C-4 与 architecture v1.1.4 一致性** | pass | §8.1 各条款块（C/D/H/A/S/G/I/J/K/L/M/N）与实现逐字 / 措辞级等价（机器比对：H/A/S/I/J/K/L/M 与 G 三段、N-19 全文 True；C 仅 `git clone` 括注、D 仅 `pwd` 转写两处差异，均见偏差表）；§3.1 位置口径成立（H→协议产物→约束主体，窗口 `:206`→`:208` 对应实现 `:238`→`:264`）；§5.4 硬停形态逐字；§7 supersede：被取代 5+2 项已消失（含旧启动门三判据 / 旧硬停块 / 首段校验），保留 P1~P6 全在（逐字核验）；§3.5 零改动清单逐项 0 内容改动 |
| **C-5 工作目录纪律** | pass | 本 PR 改动**只**存在于自身分支 worktree；主工作区与会话工作区的两个文件版本行仍为 `0.9.0` / `1.12.0`，`git status` 对这两个文件 0 条（见文末三处自证） |
| **C-6 可施工性（自相矛盾 / 悬空引用）** | pass | 全部新增引用可解析：`§规则 D`(`:191`)、`§规则 H`(`:238`)、`§文档路径协议`(`:523`)、`§规则 F`(`:222`)、`§隔离边界声明`(`:264`)、`§协议产物`(`:244`)、`§阶段回退`(`:484`)、`§迭代分支合并进 main`(`:506`)、`§角色定义来源与部署`(`:292`)、SKILL 内部 `§ 角色文件来源与部署`(`:223`) 均存在；无新增节被引用而不存在；版本号两文件互相一致；supersede 后无「无主条款」（规则 C/D 明确不整体废止，被取代面有 P1~P6 承接）。两处前向引用（`data/skill-optimization-v1.13.0.md` → pr-003；`docs/iterations/0018-*/status.md` → 0018 分支）见偏差表 #9 / #10 |

---

## 汇总

- **pass: 49 / fail: 0 / partial: 0 / blocked: 0**（A 段 21 + B 段 22 + C 段 6）

## 偏差记录

| # | 规格 / 文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| 1 | §8.1-C：`git clone` 仅在"无法共享同一 `.git`"时（括注被省略） | 保留 v0.9.0 原文全形「（跨机器 / 跨用户 / 无写权限）」——与 §7.2/N-02「保留 `:151` worktree/clone 声明句（逐字）」一致 | 以 §7.2 保留清单 / v0.9.0 原文为准；§8.1-C 的括注简写宜在架构侧同步（不阻塞） |
| 2 | §8.1-D 首行 + §5.2 口径注记：明写「不读取 `pwd`」 | 转写为「**不读取会话的当前工作目录**、不使用启动目录作为输入项」 | 与 A-21/F09 验收 2 的「`pwd` 0 命中」判据相抵时的必然取舍；建议架构侧统一为「不读取当前工作目录」措辞 |
| 3 | §8.3 表 A 段③：被取代清单「逐句引 v0.9.0 原句」 | 第 1、5 条引文中 `--show-toplevel` / `pwd` 被转写（「顶层目录查询结果与本会话启动目录相等」） | 同上，属两条判据字面互斥；实现选择检索式判据；建议架构侧给出该二处的例外口径 |
| 4 | §8.0 术语等同声明：`会话工作区 = 该迭代的迭代工作区（本规范两词同指一个实体）；本规范不重命名既有标识` | 仅落第一句（§8.1-C 条款文本亦无第二句）；实质效果由 P2/P3 保留 `<会话工作区>` 占位符与规则 C 标题承载 | 以 §8.1-C（条款全文真源）为准；§8.0 括注的末句未逐字落地，建议架构侧收敛两处表述 |
| 5 | §4.5：落点语义块配一行专用 `判断方式：` | 未单独落地；同判据已由 §8.1-A 判断方式步骤 ④（`ls docs/worktrees` / `git worktree list` 前缀项）覆盖 | 以 §8.1-A 为条款全文真源、§8.0 的「4 处新增」为体例真源；建议架构侧标注 §4.5 判断方式行为 §8.1-A 的从属表述 |
| 6 | §2.4：「SKILL 字段清单与规范逐字同构 ⇒ 同 1 处新增，保持两侧同构」；T-11 验收 1「与 SKILL 侧新增行逐字同构」；§8.2-K-05 只给 1 行 | 规范侧新增 2 行（`工作区地址：` + `检测项：`，依 §8.1-K），SKILL 侧新增 1 行（依 §8.2-K-05）；`工作区地址：` 行两文件**字节级同构** | 以 §8.1-K / §8.2-K-05 / §3.2-K-05 为准（两侧**不再**逐字同构）；建议主 agent 裁定是否补 SKILL 侧 `检测项：` 行以恢复同构（涉及 F20 简报承载面） |
| 7 | §3.2-K-08 处置列「改写 2 条 + 新增 1 条」 | 基线 Safety **无**「启动校验」条（该三处 `:44`/`:98`/`:136` 归 K-01/K-02/K-03）；实现为 Safety 末尾新增 2 行，两条文本均逐字同 §8.2-K-08 | §8.2-K-08 的两条指定文本已逐字落盘、无残留启动校验条；建议架构侧校正 K-08 处置列条数口径 |
| 8 | T-19 验收 2「子层与分支命名、base 在 diff 中 0 hunk」 | 子层**落点路径字面**被「该 PR worktree 的绝对地址」取代（§8.2-K-06 明文要求）；「独立 worktree」子层声明、分支名 `feat/…`、base 均保留 | 以 §8.2（措辞真源）为准；子层落点命名在规范侧 P2 保留清单中仍逐字存续（规则 C 落点与命名列表） |
| 9 | 备案段 D-7 指引断言「两处 `§8.4` 引用**已**就地更正」，并指向 `data/skill-optimization-v1.13.0.md` | 本 PR HEAD 下 `skill-optimization-v1.12.0.md:13`/`:40` 仍为原形（O-03 归 pr-004）；该新记录文件不存在（归 pr-003） | 属 PR 文件已声明的跨 PR 归属（`K-09`②③ → pr-003；O-03 → pr-004）；建议由主 agent 在阶段 6 归类时确认时态表述 |
| 10 | 备案段引用 `docs/iterations/0018-*/status.md` §恢复设置 的「操作前置（宿主限制）」行 | 本 PR 分支树中不存在 `0018-*` 目录（该迭代产物仅存在于 `0018-chat-agent-subagent-protocol` 分支 worktree）；引文经核在该分支 `status.md:24` **逐字存在**且确已被本迭代取代 | 登记事实成立（F16 的对象即该分支产物）；建议在规范侧或阶段 6 注明该引用的归属分支，避免读者按本分支树解析失败 |
| 11 | §8.1-G：「在「不覆盖（逐项 + 负面约束）」清单内**追加一条**」 | 插入点落在基线 `:216`（清单末条后的空行）之后 ⇒ cwd 条与前三条之间多 1 空行（原清单紧凑、实现后为松散列表）；内容仍在同一清单内 | 纯格式差异，不影响判据；如需与前三行一致，可在下次编辑时去除该空行 |

**偏差合计：11 条**（无 fail / partial 级偏差；其中 #2 / #3 为架构内部两条判据字面互斥，实现取舍已在报告中说明）。

## 下一迭代候选

1. **两侧 brief 字段块同构恢复**：由架构 / 主 agent 裁定 SKILL 侧是否补 `检测项：` 行（消除偏差 #6 的口径分叉，并为 F20 的执行者自检提供双侧承载）。
2. **架构表述收敛**：把 §8.0（术语等同声明末句）、§2.4（两侧同构）、§3.2-K-08（处置条数）、§4.5（落点语义专用判断方式行）四处与 §8.1 / §8.2 相抵的表述收敛为单一权威（对应偏差 #4 / #5 / #6 / #7）。
3. **`pwd` / `--show-toplevel` 例外口径成文**：在架构或规范体例中写明「被取代清单的逐句引用」与「检索式 0 命中」冲突时的优先次序（对应偏差 #2 / #3），避免后续迭代重复处置。
4. **跨 PR 前向引用的登记形态**：为「本迭代另有归属的产物 / 记录」约定引用写法（标明归属 PR 或分支），避免读者按当前分支树解析失败（对应偏差 #9 / #10）。

## 结论

**PASS**

---

## 自证（三处 `git status --porcelain -uall`）

```
# 1) 报告所在处（会话工作区）
$ git -C /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0020-session-workspace-addressing status --porcelain -uall
?? docs/iterations/0020-session-workspace-addressing/clarifications/verify-pr-001-20260912-221014.md
?? docs/iterations/0020-session-workspace-addressing/clarifications/verify-pr-004-20260912-220525.md
（对照：写入本报告前为 1 行 = 同目录下 verify-pr-004-…md；本报告为本次验证唯一新增）

# 2) 主工作区
$ git -C /Users/chenchiyuan/projects/agents status --porcelain -uall
（0 行 —— 无任何改动）

# 3) 被验 PR worktree
$ git -C /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0020-session-workspace-addressing/.pb-agents/worktrees/0020-pr-001 status --porcelain -uall
（0 行 —— clean）
```

补充自证（工作目录纪律）：
- 主工作区 `roles/workflow-pb/workflow-pb.md:25` = `**版本**: 0.9.0`，`.claude/skills/workflow-pb/SKILL.md:30` = `**版本**: 1.12.0（对应规范 workflow-pb v0.9.0）` —— 本 PR 改动未泄漏至主工作区。
- 会话工作区同名两文件同为 `0.9.0` / `1.12.0`，且 `git status` 对该两文件 0 条 —— 本 PR 改动未泄漏至会话工作区。
- 只读操作清单：`git show` / `git diff` / `git log` / `git rev-parse` / `git merge-base` / `git worktree list` / `git ls-files` / 文件读 / 检索；**未执行任何 git 写命令**，未修改任何被验证文件。
