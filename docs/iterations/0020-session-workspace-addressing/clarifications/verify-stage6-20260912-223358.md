# verify-stage6-20260912-223358.md — 迭代 0020-session-workspace-addressing 阶段 6 最终独立验证

**验证者身份**（按角色反射规则「验证『流程协议本体改动 + 其全部阶段产物』」反射）：**发布前评审者（协议 / 需求 / 架构 / 测试四重视角）**——协议视角判「规范本体（`workflow-pb.md` v0.10.0）与宿主 skill（`SKILL.md` v1.13.0）的条款落位、判断方式与内部一致性」；需求视角判「`demand.md` E1~E17 与 `prd/F01~F21` 到实现的追溯」；架构视角判「阶段 3 落点（architecture v1.1.4）与阶段 4 PR 文件、最终实现的逐条相符」；测试视角判「被验收标准（含本迭代强制的并发调度真实执行证据三项与寻址契约机械面样本）是否被真实证据支撑」。

**产出物**：
- 迭代产物目录 `docs/iterations/0020-session-workspace-addressing/`（`demand.md` v1.0.0 / `prd.md` v0.2.0 + `prd/F01~F21` / `architecture.md` **v1.1.4** / `prs/`（4 PR + 4 tasks）/ `status.md` / `history.md` / `clarifications/`（5 份 Gate 报告 + 4 份 PR 级验收报告））
- 已合并实现（迭代分支 `iteration/0020-session-workspace-addressing`，本次判定 tip = **`9083c40`**，base = `main @ dbc99b5`）：`roles/workflow-pb/workflow-pb.md`（v0.10.0）、`.claude/skills/workflow-pb/SKILL.md`（v1.13.0）、**新建 `docs/worktrees/README.md`**、`roles/workflow-pb/data/workflow-pb-changelog.md`、`roles/workflow-pb/memory.md`、`.claude/skills/workflow-pb/data/skill-optimization-v1.13.0.md`（新建）、`.claude/skills/workflow-pb/memory.md`、`.claude/skills/workflow-pb/data/skill-optimization-v1.12.0.md`（D-7 两处就地更正）
- 工作区 `<WS>` = `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0020-session-workspace-addressing`

**验证标准来源**：主 agent 委托内联的 9 条标准（阶段 5 推进条件复核 / 需求到实现追溯 / 并发调度真实执行证据三项 / 实现与规范内部一致性 / 边界遵守 N1~N10 / 阶段 4 产物与最终实现一致性 / 搭置的需求变更报告 / 构造样本 / 台账一致性）+ 标准中明示的引用文件（`<WS>/roles/workflow-pb/workflow-pb.md` §验证目标·并发调度真实执行证据 / `prd/*` / `prs/*` / architecture §7 / status / history / 4 份 PR 级验收报告）。

**验证日期**：2026-09-12

**独立性声明**：未接收任何执行过程上下文——未读任何产出的「作者自述 / 讨论过程 / 主 agent 的裁定理由」作为判定依据；未修改任何被验证产出物与仓库文件（唯一写入 = 本报告自身，落在 `<WS>/docs/iterations/0020-session-workspace-addressing/clarifications/`，未提交、未触碰任何既有文件）。判定依据 = 产出物文本 + 只读 git 命令输出 + 标准 8 授权的 `/tmp` 构造样本。

**时点说明（对判定有影响，故显式记录）**：委托给定 HEAD = `52708e4`；验证期间迭代分支推进至 `9083c40`（`2026-09-12 22:26:27`，`git diff --name-status 52708e4 9083c40` = **仅 `docs/iterations/0020-session-workspace-addressing/status.md`**（8 插入 / 6 删除），无任何 PR 面文件改动）。本次判定一律以 `9083c40` 为准；凡受该推进影响的条目（标准 9 的台账读数）按新值判定。

**只读命令面**：`git diff/log/show/rev-parse/merge-base/branch/worktree list/ls-files/check-ignore/numstat` + `grep/read`；除标准 8 授权样本外**未执行任何 git 写命令**；未跑全量测试套件。

---

## 逐项判定

### 标准 1：阶段 5 推进条件复核 —— **pass**

| 子项 | 判定 | 证据 |
|---|---|---|
| ① 四个 PR 的文件范围与各自 PR 文件逐条相符 | pass | 逐合并核对（`git diff --name-status <merge>^1 <merge>`）：`ede5190`（pr-001）= `roles/workflow-pb/workflow-pb.md` + `.claude/skills/workflow-pb/SKILL.md` 恰 2 文件，与 pr-001「文件范围」两条一致；`6a99505`（pr-004）= `.claude/skills/workflow-pb/data/skill-optimization-v1.12.0.md` 恰 1 文件，与 pr-004 文件范围一致；`5835d80`（pr-002）= `A docs/worktrees/README.md` 恰 1 文件（PR 声明的唯一新建文件）；`52708e4`（pr-003）= `A skill-optimization-v1.13.0.md` + `M .claude/skills/workflow-pb/memory.md` + `M workflow-pb-changelog.md` + `M roles/workflow-pb/memory.md` 恰 4 文件，与 pr-003 文件范围四条一致。`numstat`：pr-002 `39/0`、pr-003 `34/0 · 1/0 · 59/0 · 1/0`、pr-004 `2/2`（就地更正，两行 1:1 替换）——与各 PR 文件「新建 / 追加 / 就地更正」的形态声明相符 |
| ② 合并后迭代分支除四 PR 面与迭代台账外无其他文件变更 | pass | `git diff --name-only main..9083c40` = **58 文件**：实现面 **8 件**（上列 8 个文件，逐件归属 pr-001/002/003/004）+ 本迭代台账 **50 件**（`docs/iterations/0020-session-workspace-addressing/**`：architecture/demand/history/prd.md/status + `prd/*`×21 + `prs/*`×8 + `clarifications/*`×16）。无第 9 个实现面文件 |
| ③ 四个 PR 均已合并（各 PR 分支提交均在迭代分支历史上） | pass | 4 个 merge commit 的第二父（`git log -1 --format=%P`）分别为 `de5c0af`/`b8c9525`/`dda13fb`/`e5baef0`，逐个 `git merge-base --is-ancestor <第二父> HEAD` **成立**（YES ×4）；PR 分支本身已删除（`git branch -a` 仅 `main` + `iteration/0018-*` + `iteration/0020-*`），但其提交均在迭代分支历史上 |
| ④ 无其他写入面（边界） | pass | `git diff --name-only main..9083c40 -- .gitignore tools/ oamp/ principles/ 'roles/*' ':!roles/workflow-pb'` = **空输出**；实现面清单**无任何非 `.md` 文件**（脚本 / 可执行文件 0） |

### 标准 2：需求到实现的追溯 —— **partial**（E 面 17/17 pass；F 卡面 20 pass + 1 partial）

**2-A `demand.md` E1~E17（原文 `demand.md:349-365`）逐条定位**（定位面 = 已合并实现的规范 / SKILL / 产物行，或本迭代真实证据）：

| # | E 项要点 | 判定 | 证据（可定位） |
|---|---|---|---|
| E1 | 任意目录启动 + 工作区自建 + 产物落迭代工作区 + 主工作区零改动 | pass | 规范 `:179`（可在项目内任意位置启动）、`:198`（建立仅当不存在，由会话执行，成功即直接继续）、`:392`（写入 `**工作区地址**`）；本迭代真实实例 = `status.md` §偏离记录 D-1 与 §运行形态段（会话在主工作区启动、WS 自建、主工作区 `status -uall` 为空）；**构造样本**（本报告附录 A）独立证实机械面 |
| E2 | 简报含工作区绝对地址，产物路径可据此解析 | pass | 规范 `:416`（`工作区地址：<…绝对地址…>`）；`SKILL.md:291` 同构一行；本迭代真实简报转引 `clarifications/round-1.md:13`（「工作目录纪律：…一切读与写均以绝对路径寻址 /Users/…/0020-…」） |
| E3 | `docs/worktrees/README.md` 存在、覆盖 5 类信息、无脚本、被跟踪 | pass | 文件存在（39 行）；`git ls-files --error-unmatch docs/worktrees/README.md` **exit 0**；节标题 5 个（`README.md:8/19/24/31/36`）；`grep -c '^```'` = 0、`grep -cE '/Users\|/home/\|/tmp/'` = 0 |
| E4 | 落点语义互不误用；`docs/worktrees/` 下无工作树 | pass | 规范 `:250`（三者互不替代 + 禁令）；`README.md:33-34`（一行承接）；`ls docs/worktrees` = 仅 `README.md`；`git worktree list \| grep -c 'docs/worktrees/'` = **0** |
| E5 | 不存在启动门（无 cwd 判据、无预先执行前置） | pass | `grep -n -- '--show-toplevel'` 两文件 = **0 命中**；`grep -nw pwd` 两文件 = **0 命中**；`grep '必须由用户执行\|用户需先\|请先 cd\|请重开会话'` = **0 命中** |
| E6 | 硬停触发面 = 工作区无法确定或无法创建；输出不含祈使句 | pass | 规范 `:201`（唯一硬停面 + 发现即停 + 不含指向人 / 用户的动作要求）；`:203-208` 三行事实陈述块（触发原因 / 当前状态 / 未完成事项），无命令块、无「请 / 你必须」 |
| E7 | 做事时检测可观察（简报含地址声明与检测项；执行记录含自检结果） | pass | 规范 `:416-417`（地址行 + `检测项：本次一切写入以「工作区地址」为根、以绝对路径进行；git 写操作使用 git -C <工作区地址>`）；执行记录侧自检结果 = `clarifications/round-1.md:13`、`verify-pr-001:205-215`、`verify-pr-003:134-146`、`verify-pr-004:97-116` 的三处 `git status` 自证（均含**地址 + 判定结论**，符 MI-06 载体口径） |
| E8 | 规范存在逐句 supersede 声明 | pass | 规范 `:81-88`（被取代清单 7 项：① 规则 D 判据 1/2/3 作启动门 ② 规则 C 首句 ③ 规则 C 时点句 ④ 规则 D 硬停块 ⑤ 启动工作流首段 + ⑤′/⑤″ 补充），句级粒度 |
| E9 | 主工作区可作启动位置 / 不作任何迭代工作区 / 写入仍受禁令 | pass | 规范 `:179`（含仓库主工作区）、`:181`（主工作区不作任何迭代的会话工作区，该行 diff 无 hunk）、`:224`（规则 F 正文，无 hunk）三句并存 |
| E10 | 三层声明可核查；产物不含动态地址 | pass | 规范 `:248`（源 / 投影 1 / 投影 2 + 以源为准 + 硬约束）；`README.md` 无绝对路径（占位符 `{迭代ID}`/`<会话工作区>`）；本次三处比对：源声明 `v0.10.0`/`v1.13.0` ↔ 实测 `workflow-pb.md:25` = `0.10.0`、`SKILL.md:30` = `1.13.0` 逐字一致；`status.md:7` 的地址与源规则推出的 `<仓库主工作区>/.pb-agents/worktrees/{迭代ID}` 一致 |
| E11 | 0019 E1~E4 在新模型下仍成立 | pass | 条款面：保留清单 P1~P5（规范 `:90`）+ 规则 C/D 的机制句（落点 / 父子层 / 迭代分支，diff 无 hunk）；真实证据：两波各 2 PR 并发（`status.md` PR 子状态表 `b3cc5e7` 的 pr-001‖pr-004、`5db313c` 的 pr-002‖pr-003 均同时「占用」）；提交只落各自分支（4 个 PR 分支提交名见标准 1③）；本报告附录 A 样本（两会话工作区，写其一，另一工作区 status / HEAD / 文件哈希前后逐字节相同） |
| E12 | 新增 / 改写条款自带「判断方式」 | pass | 规范 `判断方式：` 行实测 **12 处**（`:189` C / `:214` D / `:230` F / `:242` H / `:254` 协议产物 / `:262` 约束主体 / `:290` 隔离边界声明章节级 + 保留的 `:169` A / `:175` B / `:220` E / `:236` G / `:304` 角色来源），基线 8 处 → 新增 / 改写处均带 |
| E13 | 0019 E8 改写后仍成立（收口提交在主工作区；该迭代工作区前后始终检出迭代分支） | pass | 条款面：规范 `:513`（前置动作主体限定为「该迭代的工作区」+ `git -C <该迭代的工作区地址>`）；收口本身**尚未发生**（`main @ dbc99b5`、分支 tip `9083c40` 未合并）⇒ 运行实例待本迭代收口（另见偏差 D-1：卡要求的「待样本」登记缺失） |
| E14 | 显式声明 cwd 不在隔离内 + 越界不可被 git 强制、只能事后核查 + 给核查判据 | pass | 规范 `:273`（cwd 从来不是隔离的一部分）、`:288`（不可能被 git 强制 / 不会被阻止 / 「未被阻止」不等于「条款作废」+ 四步核查判据） |
| E15 | 0018 前置句登记失效 + 恢复派发由简报覆盖 | pass | 规范 `:99`（0018 活指令失效登记，指名到句）与 `:433`（恢复派发句）；`grep -n '回改'` = 0 命中；0018 文件零改动（标准 1 文件清单 0 命中） |
| E16 | `### 隔离边界声明` 含章节级判断方式行（D-1 闭合） | pass | 规范 `:290`（章节级 `判断方式：`，覆盖①覆盖 ②不覆盖 ③非 git 共享资源 ④已知代价与事后核查）；本节两个 hunk 均为纯新增（0 删除行） |
| E17 | `skill-optimization-v1.12.0.md` 不再出现「规范 §8.4」 | pass | `grep -rn '规范 §8\.4'` 三文件 = **0 命中**；`grep -c '8\.4'` 该文件 = **2**（`:13`、`:40`，均为 `architecture.md §8.4`）；`skill-optimization-v1.13.0.md:26` 有「更正登记」段 |

**2-B `prd/F01~F21` 验收标准逐卡判定**（判据 = 各卡「验收标准」段；证据取实现行 / 只读命令输出 / 本迭代记录）：

| 卡 | 判定 | 关键证据（不足处见偏差） |
|---|---|---|
| F01 零前置启动 | **pass** | `:179`（任意位置启动）、`:198`（会话自建 + 直接继续）、`:258`（对人零前置四项）、`:198`（「谁建立不作预设」）；验收 4 三段面齐备：文本 `:179/:392` + 真实（`status.md` D-1/运行形态）+ **构造样本（附录 A：工作区不存在 → 从任意位置建立 → 写入 → 主工作区与 peer 零改动）** |
| F02 显式寻址纪律 | pass | `:240`（绝对路径 + `git -C <地址>` + 不依赖 cwd/工具）、`:242`（判断方式：裸 git 写命令 / 「越界会被拦住」类断言即违反） |
| F03 产物存在与落点 | pass | 存在 + `ls-files --error-unmatch` exit 0 + `.gitignore` 零改动 + 规则 F「例外（闭集，仅两项）」` :228` + `:246`（协议先行）+ README 落在被跟踪 `docs/` 下 |
| F04 内容骨架 5 类 | pass | `README.md` §1（落点 / 子层 / 范围）/ §2（路径 + 版本）/ §3（三条引用）/ §4（一行 + 指针）/ §5（不含脚本、非第二真源）；逐条回指规范条款（`:179/:246/:250/:254`），无规范外新约束 |
| F05 落点语义澄清 | pass | 规范 `:250` + `README.md:33-34`；`ls docs/worktrees` 仅 1 行；`git worktree list` 无 `docs/worktrees/` 前缀项；产物无动态地址 |
| F06 地址确定性解析 | pass | `:193`（不读取 cwd / 不使用启动目录）、`:195`（`--git-common-dir` 父目录）、`:196`、`:197`（显式覆盖 + 进入声明链）、`:201`（失败面唯一）；**附录 A 实测**：同仓库 4 个不同位置求值结果**唯一相同**（`/private/tmp/verify020-sample/repo-main/.git`），父目录 = 仓库主工作区 |
| F07 地址声明三层 | pass | `:248`（源 / 投影 1 / 投影 2 + 以源为准 + 差异登记 + 动态地址不得入产物硬约束 + 与规则 A/B 不相撞理由）；`README.md` 不含动态地址（两检出一致性由「无动态地址」直接排除差异） |
| F08 简报寻址纪律 | pass | `:416`（地址行）、`:527`（基准句改写 + 相对路径不得作唯一寻址方式）+ `SKILL.md:217`（引用基准）；投影 2 的裁决规则指向源（`:248`） |
| F09 硬停收窄 | pass | `:201`（唯一硬停面 + 发现即停）、`:203-208`（无祈使句）、`:491`（原「唯一的硬停例外」已改称「唯一的流程结构错误中止」并与另两类并列）、`SKILL.md:44/98/136/389`（启动校验动作 0 残留；`grep '启动校验'` 两文件 = 0） |
| F10 主工作区定位 | pass | `:179` + `:181` + `:224` 三句并存；旧读法来源句（「这一条同时排除"在仓库主工作区里启动"」）已被取代清单收录并从条款删除 |
| F11 supersede 声明 | pass | `:81-88` 五项 + ⑤′/⑤″；`:90` P1~P6；`:92`「规则 C 与规则 D 都不整体废止」；与 F09 实态一致（无「声明已取代但旧门仍在」） |
| F12 已知代价 | pass | `:273` + `:288`（含四步判据、「未被阻止 ≠ 条款作废」）+ `SKILL.md:390`（不得表述为会被阻止）；规则 F 正文与例外集无 hunk |
| F13 机制骨架不变 | pass | 规范 25 个 hunk 逐条可归类 N-01~N-21（含 `:25`、`:75-101`、`:177-214`、`:238-262`、`:273-290`、`:389-433`、`:450`、`:491`、`:513`、`:527`、`:624`）；`## 阶段定义` / 并发槛位算法 / `## PR 文件格式规范` / 规则 A·B·E·G / `### 约束生效范围` 均 **0 hunk**；`oamp/**`、脚本文件 0 命中；CRITICAL 仅 K-01 一处改写 |
| F14 阶段 5 子 agent 口径 | pass | `:212`（以其简报给定的 PR worktree 绝对地址为准）、`:230`（判断方式主体口径）、`:450`（步骤 2 绝对地址 + `git -C`）；`grep '被派发时所在'` 两文件 = **0 命中**；样本面 = 4 份 PR 级验收报告（各含 worktree 绝对地址 + 分支 + HEAD + 三处 `git status` 自证）——构成**链式证据**（判断：成立；每份报告的被验工作区 / 主工作区均 clean，写入面仅报告自身且落 WS 约定落点） |
| F15 0019 E 项承接 | **partial** | 验收 1~4 pass（`:100` 保留 11 条 + 改写 E5/E8 + 明确登记「新增两条」）；验收 5：E12 pass、E11 pass（两波并发 + 附录 A 样本）、**E13 分支未闭合**——收口未发生，而卡内判定括注与「架构落地」段要求「若尚未发生，按 G2 口径在备案段登记为『待样本』」，实现面（规范 `:98-101` 备案段 + `changelog:56-60` 镜像）**无该登记**（全实现面 `待样本` / 等价表述 0 命中）⇒ 见偏差 D-1 |
| F16 0018 活指令失效登记 | pass | `:99`（指名到句的失效登记 + 不构成追溯）、`:433`（恢复派发覆盖句）、`:96`（生效范围）；0018 文件零改动 |
| F17 D-1 承接（章节级判断方式） | pass | `:290` 章节级行 + 覆盖四类 + 可由未参与讨论者判定；本节两个 hunk 纯新增（0 删除）；体例与既有 8 处同形 |
| F18 D-7 承接 | pass | `grep '规范 §8\.4'` = 0；该文件 diff 恰 2 行 1:1 替换（`2/2`，无新增段落）；`skill-optimization-v1.13.0.md:26` 更正登记段（改了哪两处 / 改成什么）；范围仅该文件 |
| F19 约束主体分离 | pass | `:258`（对人四项零要求）与 `:260`（对会话三项纪律）各为独立段落；`:262`（判断方式含「两段不混写在同一句内」）；违反归事后核查 |
| F20 做事时检测 | pass | ① 简报随带（`:416-417` 地址行 + 检测项行，条款面齐备）；② 执行者自检（载体口径 = MI-06「不限，但须含『地址 + 判定结论』」，落执行记录：`verify-pr-001:205-215` / `verify-pr-003:134-146` / `verify-pr-004:97-116` / `round-1.md:13` 均含地址与判定结论）；不设启动前准入门（`:201`/`:210`）；检测不引入工具（`:240`）；样本面 = 上述执行记录（本迭代简报的等价承载 = 「工作目录纪律」固定段，含地址与纪律） |
| F21 形态约束 | pass | 无 hook / 守卫 / 检测器 / 脚本（实现面 8 件全 `.md`，`oamp/**` 零改动）；判据手段全为读文本 + 检索 + git 原语；新增 3 条与改写 2 条条款均带 `判断方式：`（口径注记见偏差 D-4）；`README.md` 无脚本 / 命令块 |

### 标准 3：并发调度真实执行证据（工作流强制三项） —— **pass**

| 子项 | 判定 | 证据 |
|---|---|---|
| ① worktree 时间窗口重叠 | pass | (a) **单快照直证（第二波）**：`clarifications/verify-pr-002-20260912-142258.md` 的 A13 行原文「`git worktree list` 输出 5 项（主工作区 / 0018 / 0020 迭代工作区 / **0020-pr-002** / **0020-pr-003**）」——一次命令输出同时含两个 PR worktree；(b) **第一波（状态面 + 报告链）**：`status.md` 于 `b3cc5e7` 与 `aeb6d60` 版本的 PR 子状态表同时把 pr-001 与 pr-004 标 `⏸`（占用）且各自 worktree 分支列有名（`feat/0020-pr-001-addressing-contract` / `feat/0020-pr-004-d7-correction`）；两波并发上限成立（起始并发数 3 ≥ 2）；(c) **报告交叉观测**：`verify-pr-004-20260912-220525.md:5` 记录 pr-004 worktree 与其 HEAD `b8c9525`；`verify-pr-001-20260912-221014.md:4` 记录 pr-001 worktree 与 HEAD `de5c0af`，且其 WS `status` 输出**已含 pr-004 的报告文件**（`verify-pr-004-…md`）⇒ pr-004 的验证已完成而 pr-001 的验证仍在进行，两者 worktree 在 22:05~22:10 窗口内并存；(d) 合并顺序（`ede5190` → `6a99505`，后者后于前者）与 PR worktree「merge 后才清理」的规范约定一致，故并存窗口自派发（`b3cc5e7`）延伸至首次清理。**局限**：`history.md` 不可作为时间戳证据（见偏差 D-3）；第一波无单快照 `git worktree list` 输出，属链式证据（判断：该链足以成立） |
| ② 并发配置区块真实初始化与更新 | pass | 初始化：`status.md` 于 `3d1d4c6` 首次出现五字段且均有值（起始 3 / 硬上限 5（`2×起始-1`）/ 当前有效上限 3 / 累计槛位释放 0 / 已派发总数 2）；更新：`5db313c` 变为（当前有效上限 5 / 累计 2 / 已派发总数 4）；`9083c40` 维持 —— 即 `累计槛位释放次数` 真实经历 **0 → 2**（非「初始化后再未变化」）。证据命令：`git -C <WS> show <commit>:docs/iterations/0020-…/status.md \| grep -A7 '并发配置'`（逐提交实测） |
| ③ 爬升公式真实触发 | pass | `3d1d4c6`：`当前有效上限` = 3（= 起始）；`5db313c`：`当前有效上限` = **5**，且该行自带「按公式 `min(3 + 2×3, 5)` 重算，已达硬上限」——代入 `min(起始 + 累计×起始, 硬上限) = min(3+2×3,5) = min(9,5) = 5` **正确**；与 `已派发总数 4`、两波并发的调度现场一致。**注**：`9083c40` 未随第二波 merge 再次累加（应为 4），属台账缺口而非公式错误——见偏差 D-2（不影响本项判定：爬升已真实触发且算法正确） |

### 标准 4：实现与规范内部一致性 —— **pass**

| 子项 | 判定 | 证据 |
|---|---|---|
| ① 版本号一致 | pass | `workflow-pb.md:25` = `**版本**: 0.10.0`；`SKILL.md:30` = `**版本**: 1.13.0（对应规范 workflow-pb v0.10.0）`；`README.md:21-22` 声明二者为 v0.10.0 / v1.13.0（逐字与实测一致）；`grep -n 'v0\.9\.0' SKILL.md` = **0 命中** |
| ② 无悬空引用 | pass | 机械解析 SKILL.md / README.md 全部 `§…` 引用：SKILL 的 22 个引用中 15 个命中规范锚点、7 个为 SKILL 自身章节（「§ 对外协议」「§ 阶段执行卡片」「§ 用户决策点与暂停格式」「§ 角色文件来源与部署」「§六」）；README 的 4 个引用全部命中规范（`规则 H` / `规则 D` / `协议产物` / `规则 F` / `隔离边界声明`）；**两文件均无 MISSING-BOTH 项**。规范侧新引用「见规则 H」「见「协议产物」」「见「规则 D」/「规则 H」」「见「该迭代的工作区地址」」逐条存在 |
| ③ 无重复定义 | pass | SKILL `:105` 明写「隔离边界…见规范 §隔离边界声明（只引用，不重复定义）」；SKILL `:217` 明写「本 skill 不重复该基准的措辞」；README §3 四条全为「见规范 §…」引用形态、§5 自述非第二真源；规范 `:252` 定义「实例化投影」。SKILL §Brief 字段块与规范 §brief 字段块的**存在**为规范自身 v0.6.0 声明的「保持一致」设计（非本次新增重复），其**行数不一致**见偏差 D-4 |
| ④ 被取代的 v0.9.0 句子消失、保留项仍在（对照 architecture §7.1 / §7.2） | pass | 消失面：`--show-toplevel` / `pwd`（词）两文件 **0 命中**；「会话（主 agent 会话）**必须运行在自己的会话工作区里**」「**随后会话在该工作区根目录内启动**」「会话启动之前」仅存于 `:83-88` 的**被取代清单引用句**（成分合规：清单必须逐字引 v0.9.0 原文）；旧硬停块语句（「创建命令」「判定：cwd」）仅存于 `:85` 的清单引用句，**活条款中不存在**（`:201-208` 为三行事实陈述块）。保留面：architecture §7.2 P1~P6 逐条命中——「每迭代一个」（4 处）、「同一会话参与连续两个迭代时」（2）、「落点与命名」（2）、「不得被当作会话工作区使用」（2）、「仓库主工作区不作为任何迭代的会话工作区」（2）、「阶段 1~6 期间…保持在该分支上」（1）；规则 A/B/E/G 与规则 F 正文、`### 约束生效范围`、收口三步、并发算法、PR 七字段均 0 hunk |
| ⑤ 新增条款均带「判断方式」行 | pass | 新增 3 节：规则 H（`:242`）、协议产物（`:254`，四步判据）、约束主体（`:262`）；改写：规则 C（`:189`）、规则 D（`:214`）、规则 F 判断方式主体（`:230`）、隔离边界声明章节级（`:290`）。`grep -c '^判断方式'` 实测 12 处（基线 8 处 + 新增/改写 4 处） |
| ⑥ `docs/worktrees/README.md` 不重复定义、无命令块 / 动态地址 | pass | 39 行；`grep -c '^```'` = 0；`grep -cE '/Users\|/home/\|/tmp/'` = 0；唯一 `git -C` 出现于 `:26`（占位符 `<地址>` 的**内联引用**，非可复制命令块）；逐条规范性陈述回指规范条款（`:12-13`←规则 C；`:21-22`←协议产物②；`:26-29`←规则 D/H/F + 隔离边界声明；`:33-34`←`:250`；`:38-39`←协议产物⑤；无规范外约束） |

### 标准 5：边界遵守（N1~N10） —— **pass**

| 边界 | 判定 | 证据 |
|---|---|---|
| N1 不改机制骨架 | pass | 保留面逐条命中（见标准 4④）；规则 C/D 的机制句（worktree 唯一形态 / 每迭代一个 / 检出迭代分支 / PR 子层 / 分支名含编号）diff 无 hunk |
| N2 不改阶段序列 / 职责 / 推进条件 / 并发算法 / 七字段 | pass | `## 阶段定义`（`:142-158`）、并发槛位算法段、`## PR 文件格式规范`、规则 A/B/E 均 0 hunk（`git diff -U0` 逐 hunk 核对） |
| N3 不引入 hook / 守卫 / 强制检测器 / 启动前准入门 / 工具依赖 | pass | 实现面 8 件全 `.md`；`grep '启动校验'` 两文件 = 0；规范 `:240`「无包装脚本、无钩子、无守护进程、无环境变量前置」；`:271`「本协议不引入 hook 参与强制」（无 hunk）；`:201/:210` 无「检测不通过即停止」类条款 |
| N4 不改 `oamp/` | pass | `git diff --name-only main..9083c40 -- oamp/` = 空 |
| N5 不追溯已完成迭代（0018 例外登记） | pass | `:96` 生效范围「仅从下一个…迭代起生效，不追溯」；`docs/iterations/0018-*/**` 改动 0 命中；0018 只登记（`:99`）+ 恢复派发覆盖（`:433`） |
| N6 阶段 1 不决定机制细节 | pass | `demand.md` §4 明写「仍留白给阶段 3（N6）」；机制细节（字段名 / 解析命令 / 硬停形态 / 检测承载）均在 `architecture.md` §5.4/§5.5 与规范落位 |
| N7 不承诺阻止 | pass | `:240`「本纪律不主张可以阻止跨工作区写入」；`:288`「越界不会被阻止，只能在事后核查中发现」；`grep` 命中「会被拦住 / 会被阻止」的 3 处均为**禁止性**表述（列为违反 / 否定断言），无肯定断言 |
| N8 不为「仓库外任意 cwd」新增保证 | pass | 规范措辞限于「可在**项目内**任意位置启动」（`:179`）；无仓库外场景条款 |
| N9 不新增清理动作 | pass | 规则 G（`:232-236`）0 hunk；`:306-308` 无 hunk；收口三步与前置游离动作保留；实现面 diff 无清理类条款 |
| N10 不处理 0019 其余偏差（D-2/D-3/D-4/D-6） | pass | 0017/0018/0019 文件零改动；备案段只登记 D-1（经 E 三分类）/ D-7（留痕指引）；`grep -n 'D-2\|D-3\|D-4\|D-6'` 规范新增面 0 命中 |
| N11 / N12（后续新增） | pass | README 为实例化投影（`:252` + `README.md:39`）；`docs/worktrees/` 下无工作树（标准 2-A/E4）；无 `git worktree add` 指向该目录 |
| 零半成品 | pass | 实现面 8 文件 `TODO / TBD / stub / 占位符未 / 尚未实现` 0 命中（`workflow-pb.md` 的 5 处 `[架构待填]` 命中均为**既有阶段定义/历史段落**，非本次半成品）；新增 3 节与 4 份记录均内容完整 |

### 标准 6：阶段 4 产物与最终实现的一致性 —— **pass**

- 四个 PR 文件声明的验收标准在合并态成立：pr-001（版本行 / 被取代清单 / 规则 C·D·H / 协议产物 / 约束主体 / 隔离边界三段补写 / N-11~N-21 / 检索式 0 命中 / 零改动面）逐条核对通过（证据同标准 4）；pr-002（存在 + 跟踪 + 5 类 + 无命令块 + 无动态地址 + 版本声明 + 形态）通过；pr-003（changelog 七段构成 / memory 置顶插入且既有行逐字不动（`numstat` 删除列全 0）/ 新记录首行版本对 + 更正登记 / 零改动面）通过；pr-004（两处就地更正 + 无残留 + 范围限于该文件）通过。
- PR 文件引用的裁定编号可核对：`D-8`（`K-09` 跨 PR 归属拆分）在 `clarifications/verify-gate-stage4-r2-20260912-212007.md:137` 有原始登记；「独立性读法（阶段 4 主 agent 裁定，2026-09-12）」在 `status.md` §PR 验证 的主 agent 裁定段有留痕（含日期与理由三条）。
- 阶段 4 Gate 的推进方式存在一处**规范字面偏离**（r5 结论 FAIL 2 partial → 裁定「实质通过（附条件）」推进），已在 `status.md` 完整留痕——不改变本标准的通过判定（本标准的判据是「PR 文件验收标准在合并态成立」），另见偏差 D-8。

### 标准 7：搭置的需求变更报告 —— **pass**

`ls docs/iterations/0020-session-workspace-addressing/deferred-demand-changes.md` → **不存在**（No such file or directory）。按标准要求记为「**无搭置项**」。旁证：`clarifications/verify-gate-stage4-r4-20260912-214135.md:68` 亦记载「无搭置内容需透传：该文件不存在」。

### 标准 8：构造样本（唯一被授权的写动作） —— **pass**

**取舍**：采用**构造样本**（标准列举的第一路径），而非仅采信本迭代的活体证据——理由：本迭代按 v0.9.0「修正方向」运行，其执行记录不能直接证明 v0.10.0 的机械面；构造一次「目标工作区不存在 + 会话在任意位置启动 + 显式寻址写入 + 另一工作区零改动」是唯一能把契约机械面变成可观测事实的手段。样本在 `/tmp` 下自建自清，未触碰主工作区 / 会话工作区 / 迭代分支任何文件。

**样本设计与实测输出**（完整命令与输出见附录 A）：

| 待验证契约面 | 实测结果 |
|---|---|
| 会话可在项目任意位置启动 | 全部 git 写操作以 `cwd=/tmp`（仓库外）发起，命令形态 `git -C <绝对地址>` 成功（`worktree add` → 会话工作区 `0020-sample` + peer `0020-peer`） |
| 工作区不存在时由会话自建 | `git -C <repo-main> worktree add .pb-agents/worktrees/0020-sample -b iteration/0020-sample main` → rc=0，`git worktree list` 含该地址且分支 = `iteration/0020-sample` |
| 解析链不依赖 cwd（F06/E10） | 四个不同位置（仓库主工作区 / 会话工作区 / peer worktree / 项目内被忽略的非工作树目录）求值 `git rev-parse --path-format=absolute --git-common-dir` **结果唯一相同**，其父目录 = 仓库主工作区 |
| 显式寻址写入 + 提交只落自己分支 | 以绝对路径写入 `<WS>/.pb-agents/worktrees/0020-sample/docs/iterations/0020-sample/answer.md`，`git -C <该地址> add/commit` → 提交 `5909176` 出现在 `iteration/0020-sample`（`log --oneline` 可见），cwd 始终为 `/tmp` |
| 不污染其他工作区 | 主工作区与 peer worktree 的 `git status --porcelain -uall` **前后逐字节相同**（均空）、HEAD 与分支不变（`e2edfeb` `main` / `e2edfeb` `feat/0020-peer`）、`README.md` md5 不变；被写产物未出现在主工作树与 peer（`os.path.exists(<main>/docs)` = False） |
| 清理与无残留 | `worktree remove --force` ×2（rc=0）→ `git worktree list` 仅剩主工作区、`git branch --list` 仅 `main`；样本目录删除后 `ls /tmp \| grep -c verify020` = **0**；本仓库 `git worktree list` / `git branch` 与验证开始时一致（未受影响） |

**结论**：契约机械面**成立**；另**判定「三处 git status 自证」链为有效链式证据**——4 份 PR 级验收报告各自给出「报告落点（WS，含本报告文件）+ 主工作区（空）+ 被验 PR worktree（空）」三处 status 与 worktree 绝对地址 / 分支 / HEAD，可作为「写入目标 vs 简报地址」的链式核对依据（补充证据，非本项判定的替代）。

### 标准 9：台账一致性（status.md ↔ git 实测） —— **partial**

| 台账字段 | 判定 | 证据（git 实测） |
|---|---|---|
| 阶段状态表 | pass | 阶段 1~5 = ✅（与 4 个 merge commit、21 卡 + 4 PR 文件、architecture v1.1.4 齐备一致）；阶段 6 = ⏸（本次派发）；「已验证」列全 ⬜（本次即阶段 6，未收口） |
| PR 子状态表 | pass | 4 行：pr-001 ✅ `ede5190`、pr-002 ✅ `5835d80`、pr-003 ✅ `52708e4`、pr-004 ✅ `6a99505`——四个 hash **均为真实 merge commit 且第二父在迭代分支历史上**（标准 1③）；worktree 列 `(已清理)` 一致（`<WS>/.pb-agents/worktrees/` 目录为空） |
| 迭代分支字段 | pass | `iteration/0020-session-workspace-addressing（base = main @ dbc99b5，含 0017 与 0019 全部产出）`：分支存在、base = `dbc99b5`（reflog「Created from main」）、`main @ dbc99b5` 未合并该分支、`docs/iterations/` 下 0017 与 0019 目录均在 |
| 并发配置区块 | **partial** | 起始 3 / 硬上限 5（`2×起始-1` 正确）/ 当前有效上限 5（公式正确）/ 已派发总数 4 均与实测一致；**`**累计槛位释放次数** = 2 与「4 个 PR 均已释放」不一致**——同文件 PR 子状态表 4 行「已释放」、4 个 merge 均已在 git 落地，按规范「每当任一 PR 返回成功合并…加 1」应为 **4**（`当前有效上限` 因已达硬上限 5 而未受影响：`min(3+4×3, 5) = 5`）。见偏差 D-2 |
| 头部其他字段 | pass | `**工作流**: workflow-pb v0.9.0`、`**状态**: 进行中`、`**history**: 开启`、`**会话工作区**` 绝对路径——与实测一致（分支 tip 在 WS、WS 为 worktree 成员、history.md 存在）；该字段名非 v0.10.0 的 `**工作区地址**`，见偏差 D-5 |

---

## 汇总

- 标准 1：**pass** · 标准 2：**partial**（E 面 17/17 pass；F 卡 20 pass / 1 partial） · 标准 3：**pass**（3/3 子项）
- 标准 4：**pass** · 标准 5：**pass**（N1~N12 逐条） · 标准 6：**pass** · 标准 7：**pass**（无搭置项）
- 标准 8：**pass**（样本已执行 + 已清理 + 无残留） · 标准 9：**partial**（并发配置区块 1 字段）
- 计数：**pass 7 / fail 0 / partial 2 / blocked 0**（F 卡计数另计：F01~F21 = 20 pass / 1 partial / 0 fail）
- 偏差记录：**9 条**（D-1 ~ D-9；其中 D-1 为卡面 partial 的根因，D-2 为标准 9 的根因，其余为登记 / 一致性 / 过程留痕型）

## 偏差记录

| # | 规格 / 文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| **D-1** | `prd/F15-0019-e-items-carryover.md` 验收 5 的 E13 判定括注 + 同卡「架构落地」段：「若（收口）尚未发生，按 G2 的口径在备案段登记为『待样本』（不新增验证机制，不提前宣称成立）」 | 收口确实未发生（`main @ dbc99b5`、分支 tip `9083c40` 未合并），但**备案段无该登记**：`workflow-pb.md:98-101` 备案段仅 3 条（0018 失效登记 / 0019 E 三分类 / D-7 留痕指引）；`changelog:56-60` 镜像段同；全实现面检索 `待样本` / `尚未发生` / `收口实例` / `真实样本` **0 命中**。根因链：`architecture.md §9.1` 的 E8 行「本迭代落点」只列 `N-20` + 备案段「改写」行，未把该登记纳入落点 ⇒ 阶段 3 未列项 | 本迭代收口发生时（或在下一迭代的开篇台账）补登记：「E13（0019 E8 改写）条款落地成立；首份真实样本 = 0020 自身收口（合并提交落仓库主工作区 + 该迭代工作区在合并前后始终检出迭代分支）」；并在后续迭代把「备案段必须含待样本登记」写入 PR 文件验收标准或阶段 5 施工自检 |
| **D-2** | 规范 §阶段 5「每当任一并发中的 PR 返回『成功合并』或『失败/阻塞』判定，`累计槛位释放次数` 加 1，重新计算 `当前有效上限`」 | `status.md` §并发配置 `**累计槛位释放次数**` = **2**，而该 PR 子状态表 4 行均「已释放」、4 个 merge 均已在 git 落地（`ede5190`/`6a99505`/`5835d80`/`52708e4`）⇒ 应为 **4**；`9083c40`（第二波入账提交）未再更新该字段。`当前有效上限` 5 已达硬上限，故未影响实际调度 | 在（本次验证后）收口前的台账修正中把该字段改为 4（或登记「已达硬上限后不再累加」的口径并同步规范），并在阶段 5 收口动作里加一条「`累计槛位释放次数` = 已释放 PR 数」的机械核对 |
| **D-3** | 规范 §历史记录协议 / SKILL §history.md 更新时机：每次派发、收到报告、阶段推进核查 / Gate 确认 / 槛位释放爬升 / PR 失败或阻塞判定后**逐条追加** | `history.md` 仅 **54 行**，止于 `2026-09-12 20:36`「派发 demand（第三轮）」；阶段 1 收口及其后阶段 2~6 的全部事件（prd/architect/pr-planner 派发与报告、Gate r1~r5 裁定、4 个 PR 的派发与验收、槛位释放、merge、阶段 6 派发）**均未追加** | 恢复 history 追加（下一迭代起严格逐条），或在规范侧明确「history 的最小追加集（派发 / 收到报告 / 调度决策）」并把它列为阶段 6 强制核查项；本次因该缺口导致标准 3① 失去 `history.md` 时间戳证据面（已用 status 版本 + git log + 报告交叉观测替代） |
| **D-4** | `architecture.md §2.4`「SKILL 侧字段清单与规范**逐字同构** ⇒ 同 1 处新增」 | 规范 `:416-417` 新增 **2 行**（`工作区地址` + `检测项`）；`SKILL.md:291` 只新增 **1 行**（`工作区地址`，与规范同名字段行逐字同构）⇒ 两侧字段**集合**不再同构，宿主 skill 面缺 `检测项` 行（该分叉已由本迭代 PR 级验收 `verify-pr-001` 偏差 #6 登记） | 二择一并留痕：① SKILL §Brief 构建规则字段块补 `检测项` 行（保持两侧同构）；② 在 architecture / 规范侧改口径为「SKILL 只承载地址行」，避免「逐字同构」声明再次落空 |
| **D-5** | 规范 §状态追踪协议 status.md 格式块新增 `**工作区地址**` 字段；§协议产物判断方式② 要求三处（产物 / `status.md` / 简报）比对；§已知代价（越界写入）四步核查判据第 ① 步 = 取该会话 `status.md` 的 `**工作区地址**` 字段 | 0020 的 `status.md:7` 用改前字段名 `**会话工作区**`（无 `**工作区地址**` 行）。这与 v0.10.0「仅从下一个使用本工作流的迭代起生效，不追溯」**一致**（本迭代按 v0.9.0 运行），但使上述两条核查判据在**本迭代自身**无法直接执行（三层声明的迭代级投影在本迭代无落盘实例；简报侧承载为「工作目录纪律」固定段） | 无需返工；在下一迭代（首个按 v0.10.0 运行者）首份 `status.md` 中确认 `**工作区地址**` 字段落盘，并由该迭代阶段 6 按新判据复核一次 |
| **D-6** | 规范 v0.10.0 变更说明「具体改动（N-01~N-21 / K / P / O 逐条）」应逐条覆盖本次改动 | `changelog` 的 N-14 条目只登记「字段块新增 `工作区地址：<绝对路径>` 一行」，未单列同批新增的 `检测项：` 行（规范 `:417`）。该 hunk 仍可归属 F20 / T-05，可追溯性成立 | 在 changelog「具体改动」清单 N-14 条目补 `检测项` 行（或在架构 > 变更说明的映射表里显式登记该行归属），保持「逐 hunk 可归类」到条目级 |
| **D-7** | 验证报告命名体例 `verify-{timestamp}.md`（同批报告时间戳与事件时间一致） | `clarifications/verify-pr-002-20260912-**142258**.md` 文件名时间戳为 14:22:58，而同批报告为 `220525` / `221014` / `222337`，且该文件 mtime = 22:23、内容引 22:2x 现场 ⇒ 文件名疑为笔误 | 保留文件不动（不改历史），在后续迭代命名时与事件时间对齐；若需要索引一致性，在台账中以「报告实际落盘时间」为准 |
| **D-8** | 规范 §调度指南「Gate: 阶段 4 → 5 入口」：验证结论必须为 pass（每项有文件证据，不接受「基本通过」）；SKILL 同款「未通过：回到阶段 4 修订 PR 文件，不进入阶段 5」 | `verify-gate-stage4-r5-20260912-215004.md` 的结论为 **FAIL**（2 项非 pass：引用纪律 / 检索式唯一性），主 agent 裁定「**实质通过（附条件）**」并推进阶段 5（`status.md` §PR 验证 + 裁定段，理由三条已留痕） | 该偏离已透明留痕且实质面由 PR 级验收（49/49 · 29/29 · 14/14 · 全 pass）覆盖，无需返工；建议在下一迭代把「Gate 非 pass 时的裁定口径（可否附条件通过 + 必须留痕的字段）」写入规范，避免同类裁定每次靠个案解释 |
| **D-9** | 委托书给定的判定基准 HEAD = `52708e4` | 验证期间迭代分支推进为 `9083c40`（`22:26:27`，仅 `status.md`，8+/6−，无 PR 面改动） | 与本报告「时点说明」一致：本次判定以 `9083c40` 为准；后续核查请以同一 tip 或显式声明差异 |

## 下一迭代候选

1. **补登记 E13 的「待样本 → 首份真实样本」**：以 0020 自身收口现场作为 0019 E8 改写后的首份真实样本并登记（即 D-1 的处置）。
2. **并发配置字段的机械核对**：把「`累计槛位释放次数` = 已释放 PR 数」「`当前有效上限` = `min(起始+累计×起始, 硬上限)`」列为阶段 5 收口时的自检项（D-2）。
3. **history.md 的最小追加集**：定义并强制「每次派发 / 收到报告 / 调度决策」三条事件，或在阶段 6 强制核查 history 覆盖度（D-3；0019、0020 两迭代同族缺口）。
4. **两侧字段块同构口径收口**：决定 SKILL 是否补 `检测项` 行，或反向修正 architecture 的「逐字同构」声明（D-4）。
5. **变更说明的 hunk 级完整性**：`具体改动` 清单逐 hunk 覆盖（D-6）；报告命名 / 台账时间戳保持一致（D-7）。
6. **Gate 附条件通过的口径成文**：把「非 pass 项的裁定边界 + 必须留痕的字段（理由 / 转义务 / 阶段 6 复核点）」写入规范（D-8）。
7. **首个 v0.10.0 迭代的验证要点**：按新字段名 `**工作区地址**` 落盘 `status.md`、简报含 `工作区地址` + `检测项` 两行，并据规范 §已知代价四步判据 / §协议产物判断方式② 各复判一次（D-5）。

## 结论

**FAIL**

判定口径：按委托「全部条目 pass 才 PASS」。9 条标准中 **7 条 pass**、**2 条 partial**（标准 2 = E 面全通过但 F15 卡验收 5 的 E13 分支缺「待样本」登记，偏差 D-1；标准 9 = 台账并发配置区块 `累计槛位释放次数` 与 4 个已释放 PR 不一致，偏差 D-2）；**0 fail、0 blocked**。

需要主 agent 判断的处置：两项 partial 均为**登记 / 台账型缺口**（不涉及条款语义、边界、机制、并发执行真实性或版本一致性），且均可在收口前的台账动作或下一迭代开篇修正；条款与实现的实质面（8 件实现文件、21 张卡、E1~E17、9 条标准中的 7 条）全部有可定位证据支撑，**并发调度真实执行证据三项全部成立**。

---

## 附录 A：`/tmp` 构造样本（标准 8）——命令与输出（节选，逐条实测）

```
# 0) 建样本仓库（镜像真实仓库：工作区池被 .gitignore 忽略）
$ git init -q /tmp/verify020-sample/repo-main                                   [rc=0]
$ git -C /tmp/verify020-sample/repo-main commit -q -m 'init main'                [rc=0]   # README.md + .gitignore(.pb-agents/)

# 1) 会话在项目任意位置启动（cwd=/tmp，仓库外）→ 会话自建其迭代工作区 + 一个 peer worktree
$ (cwd=/tmp) git -C /tmp/verify020-sample/repo-main worktree add .pb-agents/worktrees/0020-sample -b iteration/0020-sample main   [rc=0]
$ (cwd=/tmp) git -C /tmp/verify020-sample/repo-main worktree add .pb-agents/worktrees/0020-peer  -b feat/0020-peer  main          [rc=0]
$ git -C /tmp/verify020-sample/repo-main worktree list
/private/tmp/verify020-sample/repo-main                                   e2edfeb [main]
/private/tmp/verify020-sample/repo-main/.pb-agents/worktrees/0020-peer    e2edfeb [feat/0020-peer]
/private/tmp/verify020-sample/repo-main/.pb-agents/worktrees/0020-sample  e2edfeb [iteration/0020-sample]

# 2) 规则 D 第 1 步：同仓库 4 个不同位置求值（cwd 无关性）
仓库主工作区             -> /private/tmp/verify020-sample/repo-main/.git   (rc=0)
会话工作区               -> /private/tmp/verify020-sample/repo-main/.git   (rc=0)
peer worktree            -> /private/tmp/verify020-sample/repo-main/.git   (rc=0)
项目内被忽略的非工作树目录 -> /private/tmp/verify020-sample/repo-main/.git   (rc=0)
结果集合唯一: True

# 3) 写入前基线（三处 status 均空；三者 HEAD/分支/文件哈希记录在案）
MAIN: status 空 | HEAD e2edfeb | branch main                | README.md md5 ad7e01c2…
WS  : status 空 | HEAD e2edfeb | branch iteration/0020-sample | README.md md5 ad7e01c2…
PEER: status 空 | HEAD e2edfeb | branch feat/0020-peer       | README.md md5 ad7e01c2…

# 4) 以绝对路径写入会话工作区 + git -C 提交（cwd 始终 = /tmp，仓库外）
$ (cwd=/tmp) git -C /tmp/verify020-sample/repo-main/.pb-agents/worktrees/0020-sample add -A && \
             git -C /tmp/verify020-sample/repo-main/.pb-agents/worktrees/0020-sample commit -q -m 'docs(0020-sample): 绝对寻址写入样本'   [rc=0]
$ git -C …/0020-sample log --oneline -2
5909176 docs(0020-sample): 绝对寻址写入样本
e2edfeb init main

# 5) 写入后：另两个工作区逐字节未变
主工作区 status 前后逐字节相同: True | peer 前后逐字节相同: True | 会话工作区发生变化: True
主工作树内是否存在被写产物: False | peer 内是否存在: False

# 6) 清理（自建自清）与无残留
$ git -C …/repo-main worktree remove --force .pb-agents/worktrees/0020-sample   [rc=0]
$ git -C …/repo-main worktree remove --force .pb-agents/worktrees/0020-peer     [rc=0]
$ git -C …/repo-main worktree list      → /private/tmp/verify020-sample/repo-main  e2edfeb [main]
$ git -C …/repo-main branch --list      → main
$ git -C …/repo-main status --porcelain -uall  → （空）
样本目录删除后：/tmp/verify020-sample 存在 = False；ls /tmp | grep -c verify020 = 0
（本仓库 git worktree list / git branch 在样本前后一致，未受影响）
```

## 附录 B：交付前三处（+ 现场）`git status --porcelain -uall` 自证（自证时刻 = **2026-09-12 22:35:31**，本报告落盘后实测；写入报告前的基线 = 22:33:58 同命令三处均为空 ⇒ 唯一新增项即本报告自身）

```
# 1) 会话工作区 <WS>（本报告为唯一新增，未跟踪；未提交）
$ git -C /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0020-session-workspace-addressing status --porcelain -uall
?? docs/iterations/0020-session-workspace-addressing/clarifications/verify-stage6-20260912-223358.md

# 2) 仓库主工作区（零改动）
$ git -C /Users/chenchiyuan/projects/agents status --porcelain -uall
（空）

# 3) PR 工作区现场：四个 PR worktree 均已由阶段 5 清理（`<WS>/.pb-agents/worktrees/` 为空目录）
$ ls -la <WS>/.pb-agents/worktrees
total 0 （仅 . / ..）
$ git -C <WS> worktree list
/Users/chenchiyuan/projects/agents                                                        dbc99b5 [main]
/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol 1a405a0 [iteration/0018-chat-agent-subagent-protocol]
/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0020-session-workspace-addressing 9083c40 [iteration/0020-session-workspace-addressing]
$ git -C <WS> branch --list
* iteration/0020-session-workspace-addressing
+ iteration/0018-chat-agent-subagent-protocol
+ main
$ ls /tmp | grep -c verify020   → 0（构造样本无残留）
```

---

验证者：发布前评审者（协议 / 需求 / 架构 / 测试四重视角）——独立验证，未接收执行过程上下文，未修改被验证产出物。
