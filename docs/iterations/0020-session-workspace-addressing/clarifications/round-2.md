# 澄清记录 · round-2（迭代 0020-session-workspace-addressing，阶段 1 需求收敛）

**caller**: demand
**时间**: 2026-09-12
**本轮性质**: 第二轮收敛 —— 接收 16 项裁决（其中 **P-1 为"升级"而非三选一**），逐条落为 `user_confirmed`；登记 P-1 升级引入的新冲突；产出 `demand.md` v0.2.0 + 一项待裁决（P-17）
**会话限制（硬性）**: 子 agent，无实时对话通道，**不能阻塞等待用户输入** ⇒ 本轮**零自问自答**；唯一未决推断（P-17）以提案形式转呈（`clarifications/demand-round-2-proposals.md`）
**本轮结论**: 16 项裁决**全部落定**（W1~W21 / N1~N11 / E1~E15 可交付）；P-1 升级引入一处**必须闭合的新冲突**（协议产物落 `<项目根>/.pb-agents/` 下 vs 该目录被 `.gitignore` 整体忽略），demand 已给出 3 选项 + 推荐，**不定案** ⇒ 阶段 1 推进条件第 2 项仍未满足。

---

## 一、本轮输入（逐条）

### 1.1 P-1（升级，原话逐字）

> 我建议是定义协议，比如参照之前的pb-agents协议，写入项目根目录的.pb-agents/ 下面。 定义好工作区间和规范就行了，不用脚本硬规范，做事的时候带检测就行。workflow-pb的一项原则就是协议先行

### 1.2 其余 15 项裁决

| P | 裁决 |
|---|---|
| P-2 + P-3 | 均 = ①（会话自建 + 人零前置；硬停只保留「工作区无法确定或无法创建」一个触发面；输出不含祈使句） |
| P-4 + P-14 + P-16 | 三项均按推荐（主工作区可作启动位置、不得作工作区；机制骨架不变；人零强制 / 会话有纪律显式分离） |
| P-5 + P-6 + P-7 + P-15 | 四项均按推荐（默认推导 + 可显式覆盖；地址声明落 `status.md` 字段 + 简报字段；简报两级寻址纪律 + 文档路径协议基准句改写；阶段 5 子 agent 以简报给定 PR worktree 绝对地址 + `git -C` 为准） |
| P-8 + P-9 + P-13 | 三项均按推荐（逐句显式 supersede；0019 E1~E13 三分类；越界不可阻止升级为显式已知代价） |
| P-10 + P-11 + P-12 | 三项均按推荐（0018 **不改文件**、在 v0.10.0 变更说明登记其前置句失效 + 恢复派发时简报覆盖；纳入 D-1；纳入 D-7 就地更正 + 新记录留痕） |

---

## 二、P-1 升级的读法（五要素 → W 项）

| 要素 | 原话片段 | 读法 | 落点 |
|---|---|---|---|
| V-1 协议先行 | 「workflow-pb的一项原则就是协议先行」 | 产出形态 = **先定义一份可被读到的声明式协议产物**，判据句从它派生；不是往规则 C/D 里再塞判据 | W3；产物形态与既有条款的分工留阶段 3 |
| V-2 落点 | 「参照之前的pb-agents协议，写入项目根目录的.pb-agents/ 下面」 | 既有惯例（`实测` F15）= `install-pb-agents.sh` 把 `roles/**`、`principles/` 部署到 `<目标项目>/.pb-agents/{roles,principles}/`，工作区池在 `<项目根>/.pb-agents/worktrees/` ⇒ 新产物落**同一层级**。**精确子路径与跟踪方式未定** | W4（层级已定）+ **P-17** |
| V-3 内容 | 「定义好工作区间和规范就行了」 | 「工作区间」= 各工作区的**落点与范围**（迭代工作区、PR worktree 的层级）；「规范」= 本项目生效的规范与其版本指针。内容域已定，字段形态留阶段 3 | W5 |
| V-4 不用脚本 | 「不用脚本硬规范」 | 不引入 hook / 守卫脚本 / 强制检测器；与 0019 P-11①（只做文本级条款）同源 | W6 = N3 |
| V-5 做事时检测 | 「做事的时候带检测就行」 | 检测点挂在**执行现场**（派发简报随带 + 执行者执行时自检），**不设启动前准入门**；与 P-2① 一致。承载形态留阶段 3 | W7 |

### 2.1 与 P-1 已确认方向的关系（保留，不推翻）

用户本轮**没有**推翻第一轮 P-1 的方向，两项并存：

- **保留**：显式寻址（绝对路径 + `git -C`）、不依赖 cwd、不依赖工具 → W2（另一份说明：`user_confirmed` 来源 = U-4 + 本轮原话未否定）。
- **新增**：契约必须**载体化**为一个声明式协议产物 → W3 ~ W7。
- 合起来：**「寻址契约」= 一份声明「工作区间 + 规范」的协议产物 + 会话按它显式寻址 + 做事时检测**。

### 2.2 受影响的编号与措辞调整（v0.1.0 → v0.2.0）

| 项 | 调整 |
|---|---|
| W 编号体系 | 由 v0.1.0 的「做什么」14 条（W1~W14，含混用的 P 项引用）**重建为 W1~W21**：W1~W7 = 第一轮 U 项 + 第二轮 V 项的落地；W8~W21 = P-2 ~ P-16 的逐条落地 |
| N3 | 显式并入"不用脚本硬规范"（原只写 hook / 访问控制 / 守护进程） |
| **N11** | **新增**：不把 `.pb-agents/` 下的协议产物写成**第二个规范真源**（生效性挂 P-17） |
| E 编号体系 | 由 v0.1.0 的 E1~E13 **重建为 E1~E15**：E3 新增（协议产物存在 + 内容 + 不含脚本）；E6 新增（做事时检测可观察）；E4 扩充（无启动前准入门）；原 E11（0018 活指令）→ E13，**判据按 P-10① 改写**为"已登记失效 + 简报覆盖"（因裁决不改 0018 文件） |
| 六维诊断 | 新增 M-9 / M-10 / M-11；新增 C-10 / C-11 / C-12；维度 6 由"全部未闭合"改为"除 P-17 外闭合" |
| 来源统计 | `user_confirmed` 增加"第二轮原话 1 段 + 要素 5 条 + 16 项裁决"；`[model_inferred]` **由 16 项收敛为 1 项（P-17）** |

---

## 三、新冲突的登记（P-1 升级引入）

### 3.1 冲突本体（C-10）

用户要求协议产物落在 `<项目根>/.pb-agents/` 下（V-2）；而该目录被 `.gitignore` **整体忽略** ⇒

- 产物**不会随 `git worktree add` 出现在任何新会话工作区里** ⇒ "会话在自己的工作区里读不到自己的协议"；
- 且**负例规则无法补救**：`gitignore(5)` 明文「It is not possible to re-include a file if a parent directory of that file is excluded. Git doesn't list excluded directories for performance reasons, so any patterns on contained files have no effect, no matter where they are defined.」（F14）⇒ 要"纳入跟踪"必须**改写**第 2 行，不是追加一行。

### 3.2 独立复核（本次亲自核对，非采信转述）

| # | 事实 | 核对方式 |
|---|---|---|
| F13 | `.gitignore:2` = `.pb-agents/`（第 4 行另有冗余的 `.pb-agents/worktrees/`）；`.gitignore` 被跟踪；**0020 工作区内不存在 `.pb-agents/`**；`git check-ignore -v` 对 `.pb-agents/`、`.pb-agents/roles/`、`.pb-agents/worktrees/` 三条路径均命中 `.gitignore:2` | `cat .gitignore`、`git check-ignore -v`、`ls .pb-agents`、`git ls-files --error-unmatch .gitignore` |
| F14 | 上引 `gitignore(5)` 原文（本机手册，退出码 0） | `man -P cat gitignore` |
| F15 | 既有 pb-agents 落点惯例：`install-pb-agents.sh` 部署 `roles/**` + `principles/` 到 `<目标>/.pb-agents/`；主工作区 `.pb-agents/` = `{principles,roles,worktrees}`；**工作区池锚定在主工作区**（各迭代工作区都在它下面） | `tools/install-pb-agents.sh`、`ls /Users/chenchiyuan/projects/agents/.pb-agents/` |
| F16 | 0019 已生效的「真源 = 被 git 追踪的内容」；`.pb-agents/` 被降级为"下游可选分发手段，不是运行前提"；明写"新工作区零安装动作即可读到完整角色定义" | 规范 `### 角色定义来源与部署`、`0019/demand.md` W8 / E9 |

### 3.3 三条次生问题（同源，合并为一项待裁决）

| # | 问题 | demand 的推荐（未定案） |
|---|---|---|
| C-10 | 落点与跟踪方式：主工作区本地声明（不跟踪）／纳入跟踪／不新增产物 | **纳入跟踪**（见 P-17 理由） |
| C-11 | 载体出现后"地址"有**三个落点**（产物 / `status.md` / 简报），谁是源？ | **分层**：产物 = 源（项目级静态声明）；`status.md` = 迭代级投影；简报 = 派发级投影；不一致时以产物为准并登记差异 |
| C-12 | 「规范定义落点」vs「产物声明工作区间」是否形成两处定义？（违反"约束只在本文件一处"原则） | 规范定义**模板与真源**、产物是其**实例化投影**，不替代、不并列；该分工在规范侧与产物侧各写一次 |

**为 P-10（0018 活指令）记一条同源约束**：P-17 无论选①还是②，产物都**只能承载静态内容**（工作区间规则 + 生效规范版本）；**本次运行的动态地址**仍落 `status.md`（迭代级）与简报（派发级）—— 若把动态地址写进被跟踪产物，每次会话都会产生一次仓库改动，与规则 A/B 的 PR 纪律相撞。

### 3.4 与 0019 W8 / §角色定义来源与部署 的一致性关系（P-17 必答项）

- **对象不同，不构成直接冲突**：W8 管"**角色定义从哪读**"；P-17 管"**工作区间与规范的声明落在哪**"。
- **取向必须一致**：0019 的核心取向是"**真源 = 随检出即得的被跟踪内容**"。若选①，本迭代就在**同一目录**下引入一个"不随检出即得的声明"，等于重建 0019 刚废除的模式；若选②，取向一致。
- **选②时的额外说明**：`.gitignore:2` 由 `.pb-agents/` 改写为 `.pb-agents/*` **不改变** W8 的任何结论（`roles/`、`principles/` 仍被忽略；`install-pb-agents.sh` 先 `rm -rf roles/ principles/` 再 copy，与 `.gitignore` 无关，其行为零变化）。

---

## 四、六维复检（第二轮）

| 维度 | 复检结论 |
|---|---|
| 1 · 明确性 | 除载体落点外闭合：M-1~M-6 由 16 项裁决闭合；M-7/M-8/M-10 为低风险推断（阶段 3 定形）；**M-9/M-11 未闭合 → P-17** |
| 2 · 逻辑闭环 | 主体闭合：三个第一轮缺口（静默写错 / 简报缺地址字段 / 宿主不可控）分别由 W14 / W10+W9 / W16 闭合；**第二轮的缺口④（契约无载体）主体闭合于 W3~W7，其落点张力 → P-17** |
| 3 · 边界完整性 | 用户明说 9 条（第一轮 4 + 第二轮 5）+ AI 推导 11 条（N1~N11）；N11 生效性挂 P-17 |
| 4 · 内部一致性 | C-1~C-9 已闭合；**C-10/C-11/C-12 未闭合**（同源 → P-17） |
| 5 · 可验证性 | E1~E15 全部通过裸判定；E4/E13 的判据已按第二轮裁决改写；刻意留白项如实标注 |
| 6 · 方案雏形 | 除 P-17 外闭合：W1~W21 全部取得用户雏形/裁决 |

**复检发现的新缺口**：仅一个（上节 3.1），已显性化并附提案。

---

## 五、来源标注统计（本轮）

| 类别 | 数量 | 说明 |
|---|---|---|
| `user_confirmed` | 第二轮原话 1 段 + 要素 5 条（V-1~V-5）+ 16 项裁决（P-1~P-16）+ 第一轮 U 项 6 条 + 边界 4 条 | 只含用户说出口的内容 |
| `实测 · 本次核对` | F13（`.gitignore` 与 `check-ignore`）、F14（`gitignore(5)` 原文）、F15（install 脚本与 `.pb-agents/` 布局）、F16（0019 W8 原文要点） | 本轮新增 4 条，均带命令或原文 |
| `[model_inferred]`（**未生效**） | P-17（含 C-10 / C-11 / C-12） | 由第一轮 16 项收敛为 1 项 |
| 参考视角（不进结论） | R-2 / R-3 / R-4（R-1 已升级为 W14） | 见 proposals 文件末节 |

---

## 六、未闭合声明与下一轮入口

1. **未闭合项**：**仅 P-17**（协议产物的精确落点与跟踪方式 + 三层落点的分层关系 + 与 0019 W8 的一致性关系）。
2. **下一轮（round-3）触发条件**：主 agent 以**真实阻塞**方式转呈 P-17 并取得用户回答后，派发 demand 第三轮。
3. **第三轮只做一件事**：把 P-17 落为 `user_confirmed`，定稿 W4 / W9 / N11 的措辞，升 `demand.md` v1.0.0 并给出阶段 1 三项推进条件的自检。
4. **本版可交付性**：`demand.md` 两段均有内容；`model_inferred` 仍未归零（P-17）⇒ **按阶段 1 推进条件第 2 项不可推进**；第 3 项（无活跃冲突）因 C-10/C-11/C-12 的待决状态而**有条件不满足**。

---

## 七、未越界声明

- 本轮**写入三个文件**（均以绝对路径寻址 `<WS>`）：`demand.md`（v0.2.0 重写）、`clarifications/round-2.md`（本文件）、`clarifications/demand-round-2-proposals.md`。
- **未修改**：`status.md`、`history.md`、`clarifications/round-1.md`、`clarifications/demand-round-1-proposals.md`（作为历史记录原样保留）；`roles/**`、`.claude/**`、`.pb-agents/**`、`tools/**`、**`.gitignore`**；任何代码（含 `oamp/**`）；未产出 `prd/**`、`architecture.md`、`prs/**`。
- **未在主工作区创建或修改任何文件**；读取 `0018/status.md` 使用的是 0018 工作区内的只读路径。
- **未执行任何 git 写命令**（`worktree add/remove`、`branch`、`checkout`、`commit`、`merge`、`stash`、`add -f` 一律未执行）；只读命令为 `git worktree list` / `git branch --list` / `git status --porcelain [-uall]` / `git rev-parse` / `git log --oneline -1` / `git check-ignore -v` / `git ls-files --error-unmatch`。
- **未改 `.gitignore`**：P-17 的选项② 涉及 `.gitignore:2` 的改写，本轮**只登记为待裁决代价**，未落地。
- **未运行测试或构建**；`/tmp` 下的探针目录（`/tmp/gitignore-probe-0020`）已删除，无残留。
- **未替用户做决定**：P-17 三个选项全部保留，未定案；未把任何推断写成 `user_confirmed`。
