# 阶段 4 产物 Gate 第四轮复验（r4）

验证者身份：发布 / 集成规划审查者，兼协议文本一致性审校者
产出物：`docs/iterations/0020-session-workspace-addressing/prs/`（4 个 PR 文件：`pr-001-addressing-contract-and-host-skill.md` / `pr-002-worktrees-protocol-artifact.md` / `pr-003-version-records-and-d7-trace.md` / `pr-004-d7-reference-correction.md`）
验证标准来源：主 agent 委托内联的 6 条标准（格式规范 / PR 粒度 / 依赖正确性 / 与 architecture v1.1.4 一致性 / 交付前置条件 / 格式硬冲突检查）
验证日期：2026-09-12
验证方式：只读。读产出物 + 对照基准（`architecture.md` **v1.1.4** 实测、`prd/F01~F21*.md`、`roles/workflow-pb/workflow-pb.md` @ base `dbc99b5`、`.claude/skills/workflow-pb/SKILL.md`、`roles/workflow-pb/data/workflow-pb-changelog.md`、两侧 `memory.md`、`data/skill-optimization-v1.12.0.md`、`docs/iterations/0019-*/architecture.md`）+ 只读 git 查询（`git log` / `git show` / `git ls-files` / `git diff --name-only`）。**未接收任何执行过程上下文**，**未读任何前序轮次验证报告**，**未修改任何被验证产出物**。

---

## 逐项判定

### 1. 格式规范：pass

证据：

- 四个 PR **各含且仅含**七个 `##` 级字段，顺序与 `roles/workflow-pb/workflow-pb.md:250-321` 的规范顺序一致：`上下文摘要` → `涉及功能点` → `文件范围` → `验收标准` → `参考资料` → `depends_on` → `batch`（实测各文件 `##` 标题行：pr-001 = 3/7/30/38/64/74/82；pr-002 = 3/7/15/21/34/42/48；pr-003 = 3/7/14/23/33/42/50；pr-004 = 3/7/11/17/26/35/42）。无第八个字段、无字段缺失。
- `## 上下文摘要` 正文按 Unicode 全字符（去首尾空白）计数：pr-001 = **142**、pr-002 = **131**、pr-003 = **147**、pr-004 = **165**，均 ≤ 200。
- `## depends_on` 字段全部存在；无依赖时按规定写作「（无）」（pr-001:76、pr-004:37）；有依赖时给出 PR 文件名 + 理由（pr-002:44、pr-003:44-45）。
- `## batch` 为单值（pr-001 = 1、pr-002 = 1、pr-003 = 1、pr-004 = 2）。
- `## 涉及功能点` 全部为 `prd/` 真实卡片的 F-ID（`F01`~`F21` 共 21 张实测存在），无自造 ID。

### 2. PR 粒度（逐 PR）：pass

证据（判据 = 规范 `:504-512` 的三条，独立性按委托给定读法「该 PR 的 `depends_on` 全部合并后可判」）：

- **pr-001**：逻辑原子性 = 一件可描述的事（「寻址契约改写 + 宿主 skill 同步」）；回滚只影响该契约的两个承载文件，与其余三 PR 文件范围零交集。可审查性 = 全部条目围绕同一语义换向（入口绑定 → 寻址绑定），无跨模块混合。规范 + SKILL 必须同 PR 的耦合论证在 pr-001:78-79 给出三条理由（跨文件 grep 判据 / 改写后引用新增与更名章节名导致悬空引用 / SKILL 驱动的阶段 5 窗口期），且与 0019 先例一致（实测 `c034dab` 为 6 文件同一提交）。独立性成立（其 `depends_on` 为空）。
- **pr-002**：逻辑原子性 = 新建**一份**产物 `docs/worktrees/README.md`（P-01）；回滚不影响任何既有功能。可审查性 = 单一文件、体例逐字采用 `architecture.md` §4.1/§4.2。
- **pr-003**：逻辑原子性 = 一次版本发布的**惯例留痕**（规范侧 changelog 条目 + 两侧 `memory.md` 索引行 + SKILL 侧新记录），四文件同体例、同为「记录 / 索引」性质，不写新规范条款（pr-003:54 自述）；回滚只回退留痕。可审查性 = 单一心智模型（现行惯例体例）。
- **pr-004**：逻辑原子性 = 两处引用就地更正（同一文件同一形态的两处）；不新增旁注、不改叙述结构、不批量统一体例。可审查性 = diff 仅两处引用句。

### 3. 依赖正确性：pass

**① 每条 `depends_on` 可在实际文件中核实**（逐条实测）：

- pr-002 → pr-001：① `roles/workflow-pb/workflow-pb.md:25` 实测 `**版本**: 0.9.0`；`.claude/skills/workflow-pb/SKILL.md:30` 实测 `**版本**: 1.12.0（对应规范 workflow-pb v0.9.0）`（PR 引用的字符串逐字相符）。② v0.9.0 的提交管理约束实测仅到规则 G（`### 规则 F` = `:192`、`### 规则 G` = `:202`，`:206` = 规则 G 的 `判断方式：` 行，`:208` = `### 隔离边界声明`），`§规则 H` / `### 协议产物` 确不存在 ⇒ 「悬空引用」证据成立。③ 插入点 `:206` 之后 / `:208` 之前 与 architecture §3.1 的「新增三节的位置口径（单一真源）」一致。
- pr-003 → pr-001：句级引用实测命中——`roles/workflow-pb/data/workflow-pb-changelog.md:15` = 「`.claude/skills/workflow-pb/SKILL.md` 同步（v1.11.0 → v1.12.0）：…」、`:22` = 「头部版本号 `0.8.0` → `0.9.0`，新增「v0.9.0 变更说明（相对 v0.8.0）」一节」（PR 引用串逐字相符；v0.9.0 条目 = `:3-30`）。
- pr-003 → pr-004：`data/skill-optimization-v1.12.0.md` 实测 `grep -n '8\.4'` **恰 2 行**（`:13` = 「- `:42` CRITICAL 语义反转（槽位保留，措辞见**规范 §8.4**）」；`:40` = 「…architecture.md` §5.1-W10 / §5.2-S1~S10 / §8.4」）⇒ 「登记陈述对象 = 两处待更正引用」的证据成立。
- pr-004 → （无）：其更正目标 `docs/iterations/0019-worktree-isolation-protocol/architecture.md:568` 实测 = `### 8.4 角色定义来源与部署（T-04 / T-11，F08）`，且该节内含 `:42` CRITICAL 的改写措辞（来源契约）⇒ 更正后指向真实存在且语义相符，零依赖成立（不经由本迭代任何 PR 产生）。

**② 无环**：三份 PR 文件的依赖图表述一致（`pr-001 → pr-002`、`pr-001 → pr-003`、`pr-004 → pr-003`；pr-001 与 pr-004 互不依赖），实测无环。

**③ 文件范围无真交集（逐文件核真交集）**：正向文件范围四组两两交集为空 —— pr-001 = {`roles/workflow-pb/workflow-pb.md`, `.claude/skills/workflow-pb/SKILL.md`}；pr-002 = {`docs/worktrees/README.md`}；pr-003 = {`roles/workflow-pb/data/workflow-pb-changelog.md`, `roles/workflow-pb/memory.md`, `.claude/skills/workflow-pb/data/skill-optimization-v1.13.0.md`, `.claude/skills/workflow-pb/memory.md`}；pr-004 = {`.claude/skills/workflow-pb/data/skill-optimization-v1.12.0.md`}。并集 = architecture §3.1~§3.4 的全部改动面（N-01~N-21、K-01~K-09、P-01、O-01~O-04），无遗漏、无重叠（`K-09` 的拆分归属在 pr-001:33 与 pr-003:29 表述一致，且与 architecture §8.2-K-09 末段一致）。

**④ F01~F21 全覆盖**：`涉及功能点` 并集 = 21/21（pr-001 = F01~F17、F19~F21；pr-002 = F03/F04/F05/F07/F21；pr-003 = F11/F15/F16/F18；pr-004 = F18），无缺项、无越界项。

### 4. 与 architecture.md v1.1.4 一致性：**partial**

**4a. 文件范围 / 验收标准与 §3 改写面清单一致：pass（本项内）**

- pr-001 承担 §3.1 的 `N-01`~`N-21` 与 §3.2 的 `K-01`~`K-09`（`K-10` 为保留面）；pr-002 承担 §3.3 的 `P-01`；pr-003 承担 §3.4 的 `O-01`/`O-02`/`O-04`；pr-004 承担 `O-03`。
- §3.5 零改动清单逐项出现在 pr-001:36 的「不涉及」清单（`.gitignore`（4 行，实测 `.gitignore` 恰 4 行）、`tools/**`、`oamp/**`、`roles/` 下 11 个非 `workflow-pb` 目录（实测 `roles/` 共 12 个目录 = `_template` + 10 执行角色 + `workflow-pb`）、`principles/**`、`0017/0018/0019` 迭代目录、规范保留节、v0.2.0~v0.9.0 变更说明、`### 约束生效范围`、并发槛位算法、状态符号与槛位状态取值定义）。
- 段序 / 段数**只引用不复述**：pr-001:40 明写「段构成（段序 / 段数 / 各段内容）一律以 §8.3 为唯一真源，本 PR 不复述」；pr-003:16/25 指向 §8.3 表 B、pr-003:26 指向「表 B 第 7 段 / 表 A 第 6 段」——与实测 §8.3（表 A = **6 段**、表 B = **7 段**，表 A ⑥ / 表 B ⑦ 段名均为「备案」）**一致**，无自行复述段序的冲突。
- 抽样逐条核对的结构类事实与 architecture **一致**：`:4`/`:30`/`:60`（≡ §3.2-K-09 与 §8.2-K-09 实测值）、`:425`（≡ §8.1-N）、`roles/workflow-pb/memory.md:8` 与 `.claude/skills/workflow-pb/memory.md:5`（≡ §3.4-O-02 与 §8.3 落点表，实测两文件行内容相符）、`工作流规范：` 之后 / `当前迭代 ID：` 之前（≡ §8.1-K；实测 brief 字段块 `:353` = `工作流规范：`、`:354` = `当前迭代 ID：`）、4.5 只写 `**迭代分支**`（≡ §8.1-M / §3.1-N-18）、`status.md` 字段行（≡ §3.1-N-13 / §8.1-J；实测格式块 `:554-559`、`**迭代分支**` 字段 = `:557`）、`git -C <该迭代的工作区地址> checkout --detach`（≡ §3.1-N-20；实测 `:447` 原句一致）、`N-21` 的 `config / hooks / remotes` 行（≡ §3.1-N-21；实测 `:215`）、§7.1「被取代 5+2 项」与 §7.2「P1~P6」（≡ 实测 §7.1 共 7 行 = ①~⑤ + ⑤′ + ⑤″、§7.2 = P1~P6）、`docs/worktrees/README.md` 五类信息 / 五节骨架（≡ §8.1-A 与 §4.2）、`docs/ds/README.md` 为头部与体例先例（实测该文件存在且 `git ls-files --error-unmatch` 成功）。

**4b. 结构类事实的「只引用不复述」：不成立（本项判 partial 的依据）**

1. **pr-001:57 对 `architecture.md` v1.1.4 `§8.2-K-09` 的描述与 architecture 自身不同**：PR 称「同一三处另见 …§8.2-K-09——其「原写 `:3` / `:58`」为该节自身的更正注记」。实测 v1.1.4 `§8.2-K-09` 明写「旧值与校正说明**只出现在 §12 的偏差闭合行（全文唯一一处）**」，且全文 `:3` / `:58` 仅出现于 §12（`:977`「`:3` 为 `description: |` 行、`:58` 为 `## Purpose` 标题行」）。同一结构（被替换的旧版本引用行号及其更正注记的落点）在 PR 与 architecture 出现**不同描述**。
2. **三处 PR 自行复述 `roles/workflow-pb/workflow-pb.md` 的行号且复述错误**：pr-001:62、pr-002:31-32、pr-003:30-31 均写「`:513` 的独立性字面口径按前述读法生效」。实测（base `dbc99b5` 与 HEAD 均同，`git diff --stat dbc99b5..HEAD -- roles/workflow-pb/workflow-pb.md` 为空 = 本迭代该文件零改动）：`:508` = 逻辑原子性、`:509` = 可审查性、**`:510` = 独立性判据**、`:512` = 「对于自动生成代码…」、`:513` = 空行。同句引用的 `:514-519`「依赖正确性验证·通过条件」（`:518` = 通过条件行）正确，`:513` 错误。

### 5. 交付前置条件：pass

证据：

- 逐条验收标准均为**可裸跑或可逐字对照**的判据：读落盘文本与 architecture 指定节对照（pr-001:40-59 全部指向 §3.1/§3.2/§5.4/§5.5/§7.1/§7.2/§8.1/§8.2/§8.3/§9.1/§9.2）、`grep` 类判据（`pwd\|--show-toplevel` 0 命中、`被派发时所在` 0 命中、`v0\.9\.0` 0 命中、`规范 §8\.4` 0 命中）、`git` 类判据（`git diff --name-only <base>..HEAD`、`git ls-files --error-unmatch`、`git worktree list`、`git -C … branch --show-current`）。
- 改动面判定的基线定义完整，无歧义残留：pr-001:58 明确以「该 PR worktree 分支自 `iteration/0020-session-workspace-addressing` 切出时刻的 tip」为基线，并显式排除迭代级绝对基线 `main @ dbc99b5`（只作阶段 6 的迭代级回归对照）。
- **是否存在需主 agent 先裁决的阻塞项：无**。唯一条件性上报条款（pr-004:20「若 0019 架构 §8.4 语义与 `:13` 所指不符则上报主 agent」）经核实**未触发**（0019 §8.4 = 角色定义来源与部署，其内即含 `:42` CRITICAL 改写措辞）；`K-09` 跨 PR 归属（pr-001:33 / pr-003:29）与 `F18` 署名移除（pr-001:57 末段）均为已裁定事项的登记，不构成待裁决项。
- 无搭置内容需透传：`docs/iterations/0020-session-workspace-addressing/deferred-demand-changes.md` 不存在（与本轮委托的已知事实一致）。

### 6. 格式硬冲突检查：pass

证据：

- 与 `## PR 文件格式规范`（`:250-321`）**无硬冲突**：七字段齐备；`depends_on` 空列表写作「（无）」；`涉及功能点` 为 F-ID 列表（括注形态与 0019 先例同构）；`batch` 为单值；`验收标准` 至少一条。
- PR 之间**无互斥声明**：依赖图三处表述一致；文件归属互指一致（pr-001:36 / pr-002:18-19 / pr-003:21 / pr-004:15 互为「属 `pr-00X`」且自述零交集）；`K-09` 拆分归属两处一致；`batch` 分组一致（batch 1 = pr-001/002/003，batch 2 = pr-004）。
- 「独立性」与规范 `:512` 字面要求的张力按委托给定的读法消解（`depends_on` 全部合并后可判），不计为冲突。

---

## 汇总

- pass: 5 / fail: 0 / partial: 1 / blocked: 0

---

## 偏差记录

| 规格 / 文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| pr-001:57「同一三处另见 `architecture.md` v1.1.4 §8.2-K-09——其「原写 `:3` / `:58`」为该节自身的更正注记」 | v1.1.4 `§8.2-K-09` 明写旧值「只出现在 §12 的偏差闭合行（全文唯一一处）」；全文 `:3` / `:58` 仅见 §12（`:977`）。PR 对 v1.1.4 该节内容的描述与 architecture 自身不符（陈旧括注，源自 v1.1.3→v1.1.4 的 r3 收敛） | 文本级一行：改述为「旧值仅见 §12 的『Gate r2 项 1 / 项 2 / 项 3 已闭合』行（§2.7 行 9 与 §8.2-K-09 均已不复现）」。判据本体（阶段 5 以实测 `:4`/`:30`/`:60` 为准）不受影响 |
| pr-001:62 / pr-002:31 / pr-003:30 引用 `roles/workflow-pb/workflow-pb.md:513` 为「独立性字面口径」 | 实测该行（base `dbc99b5` 与 HEAD 均同）为空行；独立性判据在 `:510`（`:508` 逻辑原子性 / `:509` 可审查性 / `:510` 独立性 / `:512` 对于自动生成代码…） | 三处把 `:513` 改为 `:510`。判据本体（按 `depends_on` 合并后可判）不受影响 |
| pr-003:36 引用 `roles/workflow-pb/data/workflow-pb-changelog.md:1-43` 标注「v0.9.0 条目 = 本 PR 追加条目的体例先例」 | 实测 v0.9.0 条目 = `:3-30`（`:1-2` 为文件标题、`:32` 起为 v0.8.0 条目）；§8.3 的段行号 `:5`/`:7`/`:17`/`:19`/`:21` 均落在该区间内 | 收窄为 `:3-30`，或改指 §8.3 表 B 后的「段序依据（changelog 侧）」行 |
| pr-001:47「本节既有四类结论与既有语逐字不动（**清单见 §7.2-P6 与 §3.5**）」 | §7.2-P6 与 §3.5 均不含 `### 隔离边界声明`（该节属 N-08 / N-09 / N-10 的改动面，不在零改动清单内）；「既有结论逐字不动」的权威表述在 §9.1-E10 与 §8.1-G | 把括注指向 §9.1-E10 与 §8.1-G，或删去括注（判据本体「只增不改」来自 §8.1-G，不受影响） |
| pr-001:42 括号「（N-02 / N-03；F01 / F06 / F10 / F13 / T-16）」 | architecture §3.1 的**卡片列**：N-02 = F01/F06/**F07**/F10、N-03 = F06/**F09**/F13 | 补 F07 / F09，或注明为「简化列举」。低影响：F07 已由 pr-001:50（N-13）承载、F09 已由 pr-001:43/53 承载，本 PR 的 `涉及功能点` 已含两张卡 |

> 说明：上述五条均为**文本级引用精度**问题，均不改变任何 PR 的**文件范围**、**依赖边**、**F 卡覆盖**与**验收判据的可执行性**；据本轮委托的判定口径（出现 partial 即 FAIL），条目 4 因第 1 / 第 2 条判 partial。

---

## 下一迭代候选

1. **把「PR 文件不得自行复述结构类事实」上升为规范级条款**（当前仅由主 agent 裁定与 architecture §8.3 括注承载，无规范条款）：本轮 r1~r4 的返工主因即「PR 侧自行复述段序 / 行号 → 与 architecture 收敛后的单一权威表述漂移」。候选落点 = `## PR 文件格式规范` 增一句判定（结构类事实只引用 `architecture.md` 的权威节，不复述），使后续迭代的同一问题可被裸判。
2. **`## PR 文件格式规范` 的「200 字以内」补计数口径**（0019 阶段 4 已作为 Gate 偏差 D7 登记、本迭代又一次以「沿用 0019 阶段 4 裁定口径」承载）：候选落点 = `roles/workflow-pb/workflow-pb.md:254` 的括注写为「200 字以内（按 Unicode 全字符计，含标点与空格）」，消除每轮重新裁定的成本。

---

## 结论

**FAIL**

（条目 4 = partial，其余 5 项 pass；偏差共 5 条，其中 2 条为条目 4 判 partial 的依据。全部偏差为文本级引用精度问题，无阻塞项。）

---

### 自证与边界声明

- 本报告写入 `<WS>/docs/iterations/0020-session-workspace-addressing/clarifications/verify-gate-stage4-r4-20260912-214135.md`（`<WS>` = `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0020-session-workspace-addressing`）；除本报告外未写任何文件，未写主工作区任何文件。
- 未修改任何被验证产出物；未执行任何 git 写命令（仅 `git log` / `git show` / `git ls-files` / `git diff` / `git branch --show-current` 等只读查询）；未运行测试。
- 未接收执行过程上下文；未读取 `clarifications/verify-gate-stage4-*` 任何前序轮次报告。
- 交付前自证：`git -C <WS> status --porcelain -uall` 仅出现本报告一行（`?? …clarifications/verify-gate-stage4-r4-20260912-214135.md`）。
