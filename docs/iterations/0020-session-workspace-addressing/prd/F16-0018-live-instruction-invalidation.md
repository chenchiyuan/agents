# F16：0018 活指令失效登记（不改其文件）

**功能 ID**: F16
**来源**: `demand.md` 第二段 §2 **W20**（`user_confirmed`：P-10① 按推荐）；效果 **E15**；实证 F11（0018 的状态是已恢复、待继续阶段 3，其 §恢复设置 已按 v0.9.0 落盘，含 `:24` 的操作前置）与冲突⑤
**迭代**: 0020-session-workspace-addressing
**产品对象（引用 `demand.md` 只读事实，非本阶段决策）**: 迭代 0018 的 `status.md` §恢复设置（含 `:24` 的"操作前置（宿主限制，必须由用户执行）：本迭代的恢复会话需以 cwd = 上述会话工作区启动；在此之前不得在该工作区外推进阶段 3"——**仍然会被人读到的活指令**）；既有「状态追踪协议」的恢复派发约定

---

## 用户价值

进行中的 0018 恢复会话不会照着一条已失效的前置句办事——失效被登记，恢复派发时由简报覆盖它。

## 验收标准

1. **不改 0018 的文件**（W20 / N5）：本迭代**不修改** 0018 分支上的任何文件。
   *判定*：取本迭代的改动文件清单 → 出现 `docs/iterations/0018-*/**` 即不通过。
2. **失效登记**（W20 / E15）：在 v0.10.0 的变更说明中**登记** `0018/status.md` 的操作前置句**已失效**。
   *判定*：读变更说明 → 找不到该登记、或登记未指明是 0018 的哪一句，即不通过。
3. **恢复派发时由简报覆盖该前置**（W20 / E15）：规范中写明——0018 恢复派发时，由**简报**覆盖该操作前置（以简报给定的绝对地址寻址，不要求会话在该目录内启动）。
   *判定*：读恢复派发相关条款 / 模板表述 → 该覆盖未写明即不通过。 〔承载形态 → T-04 / T-12〕
4. **与其他卡不冲突**：本卡不改动"会话工作区的现场保留"（既有条款，→ F13）与"不新增清理动作"（N9）。
   *判定*：读条款 → 出现对 0018 工作区的任何动作（清理、迁移、重建）即不通过。
5. **登记不构成追溯**（N5 / F13 验收 6）：登记只面向**下一次**对 0018 的恢复派发，不构成对已完成迭代的追溯改写。
   *判定*：读登记语句 → 出现"0018 需回改"的表述即不通过。

## 边界（不包含）

- 不含 0018 的恢复动作本身（何时恢复、由谁恢复是主 agent 的调度事项）。
- 不含 0018 文件的任何改动（W20 明定"不改文件"）。
- 不含登记与简报覆盖的形态（→ T-12 / T-04）。
- 不含 0019 其他偏差的处理（N10 → F15 边界）。
- 不含 0018 工作区的清理或迁移（N9）。

## 架构待填（阶段 3 填写）

- **T-12**：v0.10.0 变更说明的组织形态（0018 失效登记的落点与句形态）。
- **T-04**：恢复派发简报中覆盖该前置的字段 / 表述形态。
