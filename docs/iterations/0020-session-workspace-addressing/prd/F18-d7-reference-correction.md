# F18：D-7 承接：`§8.4` 引用就地更正

**功能 ID**: F18
**来源**: `demand.md` 第二段 §2 **W22**（`user_confirmed`：P-12① 按推荐）；效果 **E17**；实证 F10 与引自产物 G1（0019 阶段 6 遗留偏差 **D-7**：`skill-optimization-v1.12.0.md` 两处写作"措辞见**规范 §8.4**"，而 `§8.4` 实为 `architecture.md` 的章节号；其「下一迭代候选」第 8 项）
**迭代**: 0020-session-workspace-addressing
**产品对象（引用 `demand.md` 只读事实，非本阶段决策）**: 0019 的 `skill-optimization-v1.12.0.md` 记录文件（两处引用）与 `architecture.md §8.4`（被引用的真实来源）

---

## 用户价值

文档里指错地方的引用不会再把人带到一个不存在的章节——读者按引用去找，能找到真东西。

## 验收标准

1. **两处引用就地更正**（W22 / E17）：该记录文件中两处"规范 §8.4"更正为 **`architecture.md §8.4`**。
   *判定*（`demand.md` E17 给出的裸判据）：检索该文件 → 仍存在指向**不存在的规范章节**的"规范 §8.4"即不通过。
2. **就地更正**（W22）：在原处改写引用，**不新增旁注、不改叙述结构、不另起更正段落**。
   *判定*：取该文件的 diff → diff 范围超出这两处引用句（含新增更正说明段）即不通过。
3. **在新记录中留痕**（W22）：在本次迭代的**新记录**中留下该更正的痕迹（可追溯到"改了哪两处、改成了什么"）。
   *判定*：读本次迭代的新记录 → 找不到该留痕即不通过。 〔记录形态 → T-12〕
4. **范围限于这两处**（P-12① 的范围）：不做其他历史文件的引用核查、不批量统一引用体例。
   *判定*：取改动文件清单与 diff → 出现其他文件的引用改写即不通过。

## 边界（不包含）

- 不含其他历史文件 / 其他记录的引用核查（只做 D-7 登记的两处）。
- 不含引用体例的统一与规范（不做"顺手改进"，→ F13 验收 3）。
- 不含留痕的形态与落点（→ T-12）。
- 不含 0019 其余偏差（N10）。
- **不改规范 / SKILL 本体**（本卡对象是记录文件中的引用，不是规范条款）。

## 架构落地（阶段 3 已填，见 `architecture.md` §3.4-O-03/O-04 + §8.3 + §9.2；产品维度未改一字）

- **T-12 新记录组织形态**：本次迭代 SKILL 侧的新记录 = **新建** `.claude/skills/workflow-pb/data/skill-optimization-v1.13.0.md`（体例沿用 v1.12.0 记录：变更点 / 未改变的部分 / 一致性核对 / **更正登记** / 关联文件），并在 `.claude/skills/workflow-pb/memory.md` 追加索引行；**留痕落点 = 该记录的「更正登记」段**，句形态 = `更正登记：skill-optimization-v1.12.0.md 的 :13 与 :40 两处 §8.4 引用就地更正为 architecture.md §8.4（原写作"规范 §8.4"，而 §8.4 实为 0019/architecture.md 的章节号）。`（本卡验收 3）
- **两处就地更正的目标形态（实测已复核）**：`grep -n "8\.4" .claude/skills/workflow-pb/data/skill-optimization-v1.12.0.md` → 命中 **2 行**：`:13`「- `:42` CRITICAL 语义反转（槽位保留，措辞见**规范 §8.4**）」→ 就地改为 `architecture.md §8.4`；`:40`「- `docs/iterations/0019-…/architecture.md` §5.1-W10 / §5.2-S1~S10 / §8.4」（缩略 §8.4）→ 就地显式化为 `architecture.md §8.4`。**diff 范围仅这两处引用句**（不新增旁注、不改叙述结构、不另起更正段 ⇒ 本卡验收 2；不改其他文件、不批量统一引用体例 ⇒ 本卡验收 4）。
- **不改规范 / SKILL 本体（本卡边界）**：本卡对象是**记录文件**里的引用；规范侧无"规范 §8.4"式引用（规范章节号为 0.x 语义段名，不出现 `§8.4`）。
