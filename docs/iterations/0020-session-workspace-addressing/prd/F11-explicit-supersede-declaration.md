# F11：supersede 逐句显式声明

**功能 ID**: F11
**来源**: `demand.md` 第二段 §2 **W15**（`user_confirmed`：P-8① 按推荐）；效果 **E8**；第一段 C-1 / C-2 的闭合方式
**迭代**: 0020-session-workspace-addressing
**产品对象（引用 `demand.md` 只读事实，非本阶段决策）**: v0.9.0 既有句——规则 D 判据 1 / 2 / 3（作为启动门）、规则 C「会话必须运行在自己的会话工作区里」与「随后会话在该工作区根目录内启动」、规则 D 硬停形态块、启动工作流首段；以及**保留**项：每迭代一个工作区 / 落点 / PR worktree 子层

---

## 用户价值

读规范的人不会拿着已被废掉的旧句子当判据——哪句失效、哪句保留，写在明处，不用自己去比对版本。

## 验收标准

1. **被取代清单逐句完成**（W15 / E8）：规范中存在逐句显式 supersede 声明，逐项列出：① 规则 D 判据 1 / 2 / 3（**作为启动门**的用法）；② 规则 C「会话必须运行在自己的会话工作区里」；③ 规则 C「随后会话在该工作区根目录内启动」；④ 规则 D 硬停形态块；⑤ 启动工作流首段（"本工作流在本会话的会话工作区（见规则 C）内运行 + 启动时先做会话工作区校验"）。
   *判定*：与该 5 项 v0.9.0 原文**逐句对照** → 缺任一项、或清单粒度写成整节而非句子，即不通过。
2. **保留项逐条声明**（W15）：明确声明**保留**——每迭代一个工作区、工作区落点、PR worktree 子层（不含在本轮被取代的范围内）。
   *判定*：读清单的保留部分 → 缺任一条、或保留项与被取代项混写导致归属不清，即不通过。
3. **取代的语义边界写清**（W15）：被取代的是这些句子的**启动门 / cwd 判据用法**；其承载的实质语义（如"每迭代一个工作区"）由保留项存续，不得读成"整条规则被废止"。
   *判定*：读声明 → 出现"规则 C / 规则 D 整体废止"或"工作区机制取消"的读法即不通过。
4. **声明体可被任意位置启动的会话读到**：该声明随规范本体（被跟踪内容）分发。
   *判定*：与 F03 验收 2 同源（规范本体随检出即得），本卡不重复判据。
5. **实态与声明分离**：本卡只判"声明"，"规范里已不存在这些门"由 F09 验收 2~3 判定；两卡结论必须一致。
   *判定*：读 F09 与 F11 两处结论 → 出现"声明已取代但旧门仍在"的组合即不通过。

## 边界（不包含）

- 不含 supersede 的文本组织方式（清单位置、句级引用形态 → T-07）。
- 不含变更说明的位置与版本号（版本跃迁已在 `demand.md` 定：规范 v0.9.0 → v0.10.0、SKILL v1.12.0 → v1.13.0；变更说明组织形态 → T-12）。
- 不含 0019 E 清单的三分类（→ F15）；不含 0018 的失效登记（→ F16）。
- 不含"删除一切 cwd 启动门"的实态判定（→ F09）。
- 不含规则 C / 规则 D 之外的其他章节（如规则 F、规则 G 均不在被取代清单内）。

## 架构落地（阶段 3 已填，见 `architecture.md` §7（清单内容）+ §8.3（组织形态）+ §3.1-N-01；产品维度未改一字）

- **T-07 文本组织方式**：**落点 = `## v0.10.0 变更说明（相对 v0.9.0）` 内的一节**（规范头部，紧随 v0.9.0 段之后、`## v0.6.0 变更说明` 之前）；**位置 = 该变更说明的第 3 段**（紧随「方案」段、在「未改变的部分」段之前）；**句级引用方式 = 表内「v0.9.0 出处（章节 + 原句，逐字）」+ 处置 + 承载卡**，**引用原句逐字、不用行号**（行号随文件变化而失效，0019 的教训）；清单粒度 = **句级**（5 项逐句 + 2 项补充）。
- **T-08 落点与章节命名**：取代声明落在**变更说明**（不新开章节、不在规则正文里散写）；被取代清单的 5 项与保留清单的 6 组全文见 `architecture.md` §7.1 / §7.2；`roles/workflow-pb/data/workflow-pb-changelog.md` 同步追加 v0.10.0 条目（O-01）。
- **被取代 5 项（逐句）**：① 规则 D 判据 1/2/3 作为启动门的用法（`:169-171`）；② 规则 C「会话必须运行在自己的会话工作区里」（`:151` 首句）；③ 规则 C「在会话启动之前…随后会话在该工作区根目录内启动」（`:161` 时点 + 后半句）；④ 规则 D 硬停输出形态块（`:173` + `:176-181`，含「创建命令」行）；⑤ 启动工作流首段（`:327`）。**补充两项**：⑤′ 子 agent「被派发时所在的工作区」口径（`:167` / `:200`）；⑤″ 4.5 步的时点句（`:333`）与收口前置动作的主体（`:447`）。
- **保留 6 组（P1~P6）**：每迭代一个工作区 / 工作区落点与命名 / PR worktree 子层 / 主工作区不作工作区 / 阶段 1~6 检出迭代分支 / 规则 F·A·B·E·G 与收口三步·约束生效范围·阶段定义表·并发算法·PR 七字段·状态与历史格式（`architecture.md` §7.2）。
- **实态与声明分离（本卡验收 5）**：本卡判"声明"（清单 + 保留段）；"规范里已不存在这些门"由 **F09** 验收 2~3 判定（`grep 'pwd\|--show-toplevel'` → 0 命中）⇒ 两卡结论一致，不会出现"声明已取代但旧门仍在"。
- **取代的语义边界（本卡验收 3）**：声明中显式写明"被取代的是这些句子的**入口绑定（cwd 判据 + 启动门）与'由人在启动前把工作区准备好'的强制面**；工作区机制 / 每迭代一个 / 落点 / 父子层由保留项存续；**规则 C 与规则 D 都不整体废止**"（`architecture.md` §7.2 末段）。
