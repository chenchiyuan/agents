# F08：简报两级寻址纪律（绝对地址 + 基准句改写）

**功能 ID**: F08
**来源**: `demand.md` 第二段 §2 **W12**（`user_confirmed`：P-7① 按推荐）；效果 **E2**；实证 F8（简报模板**无**工作区地址字段，而「文档路径协议」原规定"全部路径相对该会话所在的迭代工作区根目录判读"——F4 事故的直接成因）
**迭代**: 0020-session-workspace-addressing
**产品对象（引用 `demand.md` 只读事实，非本阶段决策）**: 既有「派发执行角色时的 brief 构建」字段清单；既有「文档路径协议」的基准句"全部路径相对该会话所在的迭代工作区根目录判读"

---

## 用户价值

拿到简报的人不用猜"产物该放哪儿"——简报里就写着该工作区的绝对地址，所有产物路径照着它判读就行。

## 验收标准

1. **简报携带工作区绝对地址**（W12 / E2）：派发简报中出现该会话 / 该 PR 工作区的**绝对地址**。
   *判定*：任取一份派发简报，检索其中是否出现该工作区的绝对地址 → 未出现即不通过。
2. **产物路径以该地址为根**（W12 / E2）：简报里给出的产物路径**可据此解析**，不依赖读者猜测 cwd。
   *判定*：取同一份简报，把其中每条产物路径按声明的地址解析 → 全部可解析即通过；出现无法定位的路径（必须靠猜 cwd）即不通过。
3. **相对路径不得作为唯一寻址方式**（W12）：简报与条款中，相对路径**不得是唯一**的定位手段。
   *判定*：读条款 + 取一份简报核对 → 存在只能靠相对路径定位的产物即不通过。
4. **文档路径协议基准句改写**（W12）：基准句由"该会话所在的**迭代工作区根目录**"改为"**该会话声明的工作区地址**"。
   *判定*：读改动后的基准句 → 仍以"会话所在目录"为基准即不通过。
5. **两级分层的指代关系可读**（W12 / W11 关联）：简报是**投影 2**；与源（产物）不一致时**以源为准**（裁决规则见 F07 验收 2）。
   *判定*：读简报纪律条款中的指代关系 → 未指向 F07 的裁决规则即不通过（不由本卡重复定义裁决规则）。

## 边界（不包含）

- 不含字段的具体形态与在简报模板中的位置（→ T-04）。
- 不含简报模板的其余字段（既有条款不改，→ F13 验收 4）。
- 不含三层裁决规则本身（→ F07）；不含检测项的承载（→ F20）。
- 不含地址如何解析（→ F06）。
- 不含 SKILL 与规范之间谁为真源的取舍（→ T-09）。

## 架构待填（阶段 3 填写）

- **T-04**：简报字段形态（地址字段名与呈现位置）。
- **T-08**：基准句与简报纪律条款在规范与 SKILL 中的落点与章节命名。
- **T-09**：规范与 SKILL 的同步方式（何处为真源、何处只引用）。
