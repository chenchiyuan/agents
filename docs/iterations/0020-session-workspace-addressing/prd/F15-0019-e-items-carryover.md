# F15：0019 E 项承接三分类（保留 / 改写 / 新增）**〔登记项，非新增功能〕**

**功能 ID**: F15
**来源**: `demand.md` 第二段 §2 **W19**（`user_confirmed`：P-9① 按推荐）；效果 **E11 / E12 / E13**；引自产物 G1（0019 阶段 6 判定 PASS，其"下一迭代候选"链条）、G2（0019 E8 当时**条款落地成立、真实收口实例尚未发生**）
**迭代**: 0020-session-workspace-addressing
**产品对象（引用 `demand.md` 只读事实，非本阶段决策）**: 0019 的验收效果清单 E1~E13（只存在于 `0019/demand.md`，**不进规范文本**，F12）；本迭代的 supersede 清单（→ F11）

---

## 用户价值

上一个迭代的验收效果不会在这次改写里悄悄失效——保留哪些、改写哪些、新增哪两条，逐条有账可查。

## 验收标准

1. **保留项逐条**（W19）：声明 **E1~E4 / E6 / E7 / E9~E13 保留**（措辞不变）。
   *判定*：逐条列出保留清单并对照 0019 原文 → 缺任一条即不通过。
2. **改写项逐条**（W19 / E12 / E13）：① **E5** 改写为适用于**本次新增 / 改写的每一条条款**（每条自带"判断方式"）；② **E8** 改写为**主体 = "该迭代的工作区"**（不再是"会话工作区"这一 cwd 依赖表述）。
   *判定*：逐条对照改写后的条目 → 仍沿用旧主体（会话工作区 / 会话所在目录）即不通过。
3. **新增两条**（W19 / E1 / E2）：新增 ① **零前置可交付**（对应 E1）与 ② **简报可寻址**（对应 E2）两条效果。
   *判定*：读承接清单 → 任一条缺失即不通过。 〔`[user_confirmed MI-07]` 登记落点 = 在 v0.10.0 变更说明中**明确登记为两条**（与"保留 / 改写"并列），使"新增"这一动作可裸判定〕
4. **分类结论逐条可读**：保留 / 改写 / 新增三类逐条列出，无"未分类"的 0019 E 项。
   *判定*：把 0019 的 E1~E13 逐条对号入座 → 出现无归属项即不通过。 〔登记形态 → T-12〕
5. **改写后仍成立（实际成立性）**（E11 / E12 / E13）：
   - **E11**：0019 的 E1~E4 在新模型下仍成立——两会话互不改变对方工作区的 tracked / untracked 文件；一方切换分支不阻塞 / 不覆盖另一方；提交只出现在自己分支；并发产出各自完整找回。
     *判定*：取两会话的执行记录，逐条对照对方工作区内容、当前分支、提交归属（含未跟踪文件）。
   - **E12**：本次新增 / 改写的每一条条款都自带"判断方式"。
     *判定*：逐条读新增 / 改写条款，检查是否带 `判断方式`（未参与讨论者可据此判定违反 / 未违反）。
   - **E13**：收口时合并提交出现在**仓库主工作区**，而**该迭代的工作区**在合并动作前后**始终检出迭代分支**。
     *判定*：取收口记录，对照合并前后两处的检出分支（本迭代自身收口即是首份真实样本；若尚未发生，按 G2 的口径登记为"待样本"）。
6. **不新增功能**：本卡不引入任何 0019 效果之外的新能力；"新增两条"是效果清单的登记补齐，不是新功能点。
   *判定*：把"新增两条"回指 E1 / E2（已由 F01 / F08 承载）→ 出现新的能力要求即不通过。

## 边界（不包含）

- **不含 0019 其余偏差的处理**（N10：D-2 / D-3 / D-4 / D-6 本迭代不处理，已登记备查）。
- 不含 E8 所描述的收口动作本身的条款改动（既有条款不变，→ F13 验收 2 / 4）。
- 不含 0019 文件本身的修改（0019 是已完成迭代，N5；本卡只做**承接登记**）。
- 不含登记形态与落点（→ T-12）。
- 不含 0019 E 清单进入规范文本（F12：它不进规范；本卡的承接结论登记在本迭代的变更说明中，→ T-12）。

## 架构待填（阶段 3 填写）

- **T-12**：v0.10.0 / v1.13.0 变更说明的组织形态（含三分类承接清单的落点与形态）。
