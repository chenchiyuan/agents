# prd.md — 0020-session-workspace-addressing

**版本**: 0.2.0（第二轮修订版：8 项 MI 经用户裁决**全部按推荐**，卡内 `model_inferred` 归零；**架构维度全部留白**。版本轨迹：v0.1.0 首版拆卡 → **v0.2.0 本版**）
**迭代**: 0020-session-workspace-addressing
**创建日期**: 2026-09-12
**阶段**: 功能规格（阶段 2）
**状态**: 阶段 2 **已收敛**（`model_inferred` 残留为零）。产品维度锁定为 **21 张卡** = **18 个需求功能点（F01~F12、F14、F16~F20）** + **2 个保证项（F13 回归锁 / F21 形态约束）** + **1 个登记项（F15 0019 E 承接）**；**架构维度全部留白**（`[架构待填]` T-01~T-16）；**8 项 MI 判定口径已全部裁决**（转呈清单 `clarifications/prd-round-1.md`、裁决落盘 `clarifications/prd-round-2.md`），卡内标记已全部转为 `[user_confirmed MI-xx]`，受影响的 6 张卡（F01 / F04 / F07 / F09 / F15 / F20）已按裁决值改写
**输入合同**: `docs/iterations/0020-session-workspace-addressing/demand.md`（**v1.0.0 已收敛版**，2026-09-12；三轮共 17 项裁决全部 `user_confirmed`，`model_inferred` 归零。**W1~W23 为唯一功能来源**，N1~N12 为不做项，E1~E17 为验收标准的直接来源，C-1~C-13 为已闭合冲突）
**澄清依据（只读）**: `clarifications/round-1.md`、`round-2.md`、`round-3.md` 与 `demand-round-1/2-proposals.md`（供核对 W/N/E 的来路；**未作为需求来源**，需求来源一律是 `demand.md`）
**只读参考（本阶段未写入）**: `roles/workflow-pb/workflow-pb.md`（v0.9.0，本迭代的改动对象）、`.claude/skills/workflow-pb/SKILL.md`（v1.12.0，同）、`docs/iterations/0019-worktree-isolation-protocol/**`、`docs/iterations/0017-project-workspace/**`（前序证据；仅用于指认既有条款与既有产物的名称，**未在其中写任何内容**）
**本迭代的产品对象**: **流程规范本身**——`roles/workflow-pb/workflow-pb.md`（v0.9.0 → v0.10.0）与 `.claude/skills/workflow-pb/SKILL.md`（v1.12.0 → v1.13.0），**外加一份新增的声明式协议产物** `docs/worktrees/README.md`（W3~W7）。**不改 `oamp/` 产品代码**（N4）。
**本迭代的"用户"**: 未来在同一仓库里、**在任意位置**开起来的人和 agent —— 验收标准必须写成"谁、在什么条件下、看到什么，就能判真假"。

**术语口径**：功能卡的验收标准与边界一律使用**产品语言**——不出现要改哪个文件的哪一行、条款怎么写、检测命令怎么写、地址怎么解析（那些是阶段 3 的事）。**唯一的措辞例外**：`docs/worktrees/` 与 `docs/worktrees/README.md`（W4 / W5 已裁决的落点与文件名）、`.pb-agents/worktrees/`、`docs/iterations/{迭代ID}/`（W6 / W7 已定的工作区间与落点事实）、`status.md` / 简报（W11 已裁定的落点）、`git -C`（W2 已裁定的寻址形态）、`iteration/{迭代ID}`、规则 C / 规则 D / 规则 F / 启动工作流 / 文档路径协议 / `### 隔离边界声明`（既有交付物标识）、`pwd` / `--show-toplevel` / `git ls-files --error-unmatch` / `git worktree list`（**E3 / E4 / E5 由 `demand.md` 自己给出的裸判定判据**，引用它们不构成实现方式的选择）——均为 `demand.md` 已确认的产品对象、用户原话用词或既有交付物标识，保留可追溯性。**所有技术选型、数据模型、接口设计、命令与解析细节一律 `[架构待填]`（T-01~T-16，见文末）。**

---

## 功能点索引

| ID | 功能名 | 用户价值摘要 | 规格卡 | 来源条款 | 架构维度 |
|---|---|---|---|---|---|
| F01 | 零前置会话启动（任意位置启动 + 工作区自建） | 在项目里任意位置开起来就能开工——不用先建目录、不用先 cd，忘了也不会被挡在门外 | [F01](prd/F01-zero-precondition-startup.md) | W1；E1 | 待填 · T-03 / T-08 |
| F02 | 显式寻址纪律（绝对路径 + `git -C`） | 会话在哪儿启动都不影响产物落在哪儿——每条写入都能被指出落到了哪个绝对地址 | [F02](prd/F02-explicit-addressing-discipline.md) | W2；E1 | 待填 · T-08 |
| F03 | 协议产物存在与落点（`docs/worktrees/README.md`，随检出即得） | 会话在自己的工作区里就能读到"这项目的工作区怎么划、按哪版规范干活" | [F03](prd/F03-protocol-artifact-existence.md) | W3 / W4 / W5；E3 | 待填 · T-01 / T-08 |
| F04 | 产物内容骨架（必须覆盖 5 类信息） | 打开一份文件就知道工作区间在哪、按哪版规范干活、怎么寻址、别把三个近名目录搞混 | [F04](prd/F04-artifact-content-skeleton.md) | W6；E3 | 待填 · T-01 / T-02 / T-10 / T-14 |
| F05 | 落点语义澄清（三个近名落点互不替代） | 三个名字长得像的目录不会再被当成一回事——不会有人把工作树建进被跟踪的文档目录 | [F05](prd/F05-location-semantics-disambiguation.md) | W7；N12；E4 | 待填 · T-15 |
| F06 | 工作区地址确定性解析（默认推导 + 可覆盖） | 谁都能算出"我这个迭代的工作区在哪儿"——不靠猜、不靠问、不看当前目录 | [F06](prd/F06-determined-address-resolution.md) | W10 | 待填 · T-03 |
| F07 | 地址声明三层分层与一致性（源 + 两个投影） | 同一件事只在一个地方说了算——三处不一致时有明确裁决规则，而不是各读各的 | [F07](prd/F07-address-declaration-layering.md) | W11；E10 | 待填 · T-04 |
| F08 | 简报两级寻址纪律（绝对地址 + 基准句改写） | 拿到简报的人不用猜"产物该放哪儿"——简报里写着绝对地址，路径照着它判读 | [F08](prd/F08-brief-addressing-discipline.md) | W12；E2 | 待填 · T-04 / T-08 |
| F09 | 硬停面收窄与启动门删除 | 不该停的地方不许停；真停的时候也只是陈述事实，不命令人去做事 | [F09](prd/F09-hard-stop-narrowing.md) | W13；E5 / E6 | 待填 · T-06 / T-08 |
| F10 | 主工作区定位重述（可启动、不可作工作区） | 主工作区可以是我开客户端的地方，但不会是任何一个迭代的工地 | [F10](prd/F10-main-workspace-positioning.md) | W14；E9 | 待填 · T-08 |
| F11 | supersede 逐句显式声明 | 读规范的人不会拿着已被废掉的旧句子当判据——哪句失效、哪句保留，写在明处 | [F11](prd/F11-explicit-supersede-declaration.md) | W15；E8 | 待填 · T-07 |
| F12 | 已知代价显式化（不可阻止、只能事后核查） | 不会有人以为"写了寻址纪律就等于写错了会被拦住"——边界明说 | [F12](prd/F12-known-cost-explicit-declaration.md) | W16；E14 | 待填 · T-05 |
| F13 | 机制骨架不变（回归锁）**〔保证项〕** | 这次只换"听谁的、怎么找路"——工作区形态、每迭代一个、分支名这些骨架一根不动 | [F13](prd/F13-mechanism-skeleton-unchanged.md) | W17；N1 / N2 | 待填 · T-13 |
| F14 | 阶段 5 子 agent 的寻址主体口径 | 阶段 5 干活的子 agent 不会因为"我在哪儿"被判定违规——它按简报给的绝对地址干活 | [F14](prd/F14-phase5-subagent-addressing.md) | W18 | 待填 · T-04 / T-08 |
| F15 | 0019 E 项承接三分类（保留 / 改写 / 新增）**〔登记项〕** | 上一个迭代的验收效果不会在这次改写里悄悄失效——逐条有账 | [F15](prd/F15-0019-e-items-carryover.md) | W19；E11 / E12 / E13 | 待填 · T-12 |
| F16 | 0018 活指令失效登记（不改其文件） | 进行中的 0018 恢复会话不会照着一条已失效的前置句办事 | [F16](prd/F16-0018-live-instruction-invalidation.md) | W20；E15 | 待填 · T-12 |
| F17 | D-1 承接：隔离边界声明的章节级判断方式 | 隔离边界声明不再是"读完各人各有解释"的段落——章节自己带判断方式 | [F17](prd/F17-isolation-boundary-judgment-line.md) | W21；E16 | 待填 · T-11 |
| F18 | D-7 承接：`§8.4` 引用就地更正 | 文档里指错地方的引用不会再把人带到一个不存在的章节 | [F18](prd/F18-d7-reference-correction.md) | W22；E17 | 待填 · T-12 |
| F19 | 约束主体显式分离（人零强制 / 会话有纪律） | 规矩到底管谁写在明处——不拿"纪律"去要求人，也不拿"没强制"当会话的借口 | [F19](prd/F19-constraint-subject-separation.md) | W23 | 待填 · T-08 |
| F20 | 做事时检测（简报随带 + 执行者自检） | 检测发生在干活的现场而不是开工前的门禁——执行者顺手核一下地址 | [F20](prd/F20-in-flight-detection.md) | W9；E7 | 待填 · T-05 |
| F21 | 规范形态约束（零工具依赖 + 条款自带判断方式）**〔保证项〕** | 遵守这套规范不需要先装什么——规则写在文本里，读完就能自己判断 | [F21](prd/F21-specline-form-constraints.md) | W8；N3 | 待填 · T-14 |

**合计**：**21 张卡** = **18 个需求功能点（F01~F12、F14、F16~F20）** + **2 个保证项（F13 回归锁 / F21 形态约束）** + **1 个登记项（F15 承接登记）**。

- `demand.md` 第二段 §2 的 **W1~W23 全部覆盖，无漏项**（逐条覆盖点见「需求追溯 · W 条款」）；
- `demand.md` 第二段 §5 的 **E1~E17 全部有承载卡**（逐条见「需求追溯 · E 条款」），**无未承载项**；
- **无 `demand.md` 之外新增的功能点**——F13 / F15 / F21 不引入任何新能力，只承载 `demand.md` 已确认的 N1 / N2、W19、W8 / N3 条款（逐条见各卡「来源」与「疑问与越界」）。

---

## 本次迭代边界说明

### 包含（本迭代要产出的规范内容）

- **零前置会话启动**（W1 → F01）：会话可在项目内任意位置启动（含仓库主工作区）；人不承担 cd、不承担任何前置命令或前置目录要求；工作区不存在时由会话自己建立，建立成功即继续。
- **显式寻址纪律**（W2 → F02）：文件写入用工作区地址下的绝对路径、git 写操作用 `git -C <地址>`；不依赖 cwd、不依赖任何工具。
- **协议先行与产物**（W3 / W4 / W5 → F03）：先定义一份可被读到的声明式产物 `docs/worktrees/README.md`，落点 `<项目根>/docs/worktrees/`（**被 git 跟踪**，随检出即得）；**不改 `.gitignore`**；**不扩展规则 F 的闭集例外**（仍为 2 项）。
- **产物内容骨架**（W6 → F04）：必须覆盖 ①工作区间定义（会话 / 迭代工作区落点、PR worktree 落点与层级、工作区范围）②本项目生效的规范与版本 ③寻址纪律**引用**（不重述）④落点语义澄清的一行区分句 ⑤无脚本 / 钩子 / 守卫声明。组织形态 `[架构待填]`。
- **落点语义澄清**（W7 → F05）：`docs/worktrees/`（协议产物文档位置）≠ `.pb-agents/worktrees/`（工作区池）≠ `docs/iterations/`（迭代产物）；真源一处、产物以一行区分句承接；**不把 `docs/worktrees/` 用作任何工作区落点**（N12）。
- **地址确定性解析**（W10 → F06）：默认由「迭代 ID + 仓库主工作区」推出，不依赖 cwd、不依赖用户输入；允许显式输入覆盖。
- **地址声明三层分层**（W11 → F07）：源 = 协议产物（项目级静态声明）；投影 1 = `status.md` 字段；投影 2 = 简报字段。不一致时**以源为准**并登记差异；**动态地址只落后两者，不得写进协议产物**。
- **简报两级寻址纪律**（W12 → F08）：简报携带工作区**绝对地址**；产物路径以该地址为根；相对路径不得作为唯一寻址方式；文档路径协议的基准句改为"**该会话声明的工作区地址**"。
- **硬停面收窄**（W13 → F09）：唯一触发面 = 「本会话工作区无法确定或无法创建」；输出不含祈使句；删除一切以 cwd 为判据的启动门。
- **主工作区定位重述**（W14 → F10）：可作启动位置；不得作任何迭代的工作区；对它的写入仍受跨工作区写入禁止约束（闭集例外之外）。
- **supersede 逐句显式声明**（W15 → F11）：被取代清单 = 规则 D 判据 1/2/3（作为启动门）、规则 C「会话必须运行在自己的会话工作区里」与「随后会话在该工作区根目录内启动」、规则 D 硬停形态块、启动工作流首段；**保留**每迭代一个 / 落点 / PR worktree 子层。
- **已知代价显式化**（W16 → F12）：隔离不覆盖 cwd；跨工作区写入不可能被 git 强制 ⇒ 越界不会被阻止、只能事后核查发现，并给出核查判据；不得把"未阻止"读成"条款失效"。
- **机制骨架不变**（W17 / N1 / N2 → F13）：worktree 仍是唯一被规范化的形态；每迭代一个工作区、阶段 1~6 检出 `iteration/{迭代ID}`、PR worktree 为其子层、分支名含迭代编号全部保留；阶段序列 / 职责 / 推进条件 / 并发槛位算法 / PR 文件七字段格式零变化。
- **阶段 5 子 agent 寻址主体**（W18 → F14）：以简报给定的 PR worktree **绝对地址 + `git -C` 寻址**为准，不再引用"被派发时所在的目录"。
- **0019 E 项承接**（W19 → F15）：保留 E1~E4 / E6 / E7 / E9~E13；改写 E5（新条款自带判断方式）与 E8（主体改为"该迭代的工作区"）；新增零前置可交付与简报可寻址两条。
- **0018 活指令同步**（W20 → F16）：**不改 0018 的文件**；在 v0.10.0 变更说明中登记 `0018/status.md:24` 的操作前置句失效，并在 0018 恢复派发时由简报覆盖该前置。
- **D-1 承接**（W21 → F17）：为 `### 隔离边界声明` 补章节级 `判断方式：` 行。
- **D-7 承接**（W22 → F18）：`skill-optimization-v1.12.0.md` 两处"规范 §8.4"**就地更正**为 `architecture.md §8.4`，并在新记录中留痕。
- **约束主体显式分离**（W23 → F19）：对人零强制（无前置 / 无目录要求 / 无 cd / 无重开会话要求）；对会话有纪律（声明地址、显式寻址、越界须先备份），违反时被事后核查判定。
- **做事时检测**（W9 → F20）：检测挂在执行现场（派发简报随带 + 执行者执行时自检），**不设启动前准入门**。
- **形态约束**（W8 / N3 → F21）：不引入 hook / 守卫脚本 / 强制检测器 / 启动前准入门 / 任何工具依赖。

### 不包含（边界，源自 `demand.md` 第二段 §3 N1~N12）

- **N1 不改机制骨架**——落 F13 验收 1~2、本文件全局边界。
- **N2 不改阶段序列 / 阶段职责 / 推进条件 / 并发槛位算法 / PR 文件七字段格式**——落 F13 验收 4。
- **N3 不引入 hook / 守卫脚本 / 强制检测器 / 启动前准入门 / 任何工具依赖**——落 F21 验收 1~3、F09 验收 2~3、F20 验收 2。
- **N4 不改 `oamp/` 产品代码**——落 F13 验收 5、本文件全局边界。
- **N5 不追溯已完成迭代**；**唯一例外 = 0018**（进行中迭代，其 §恢复设置 是活指令），且**不改 0018 的文件**——落 F16、F13 验收 6。
- **N6 不在需求阶段决定机制形态细节**（产物组织形态与字段名、解析命令、检测项承载形态、硬停输出形态、supersede 文本组织方式）——**原样带进 `[架构待填]` T-01~T-16**，本阶段不自行增补实现级细节。
- **N7 不承诺"跨工作区写入会被阻止"**——落 F12 验收 2、5。
- **N8 不为"仓库外任意 cwd"新增保证**——落 F01 边界、F06 边界。
- **N9 不新增清理动作**（含不清理 0018 的工作区、不清理任何历史工作区）——落 F16 边界、F13 验收 6。
- **N10 不在本迭代处理 0019 的其余偏差**（D-2 / D-3 / D-4 / D-6）——落 F15 边界、F13 验收 3 的归类判据。
- **N11 不把 `docs/worktrees/README.md` 写成第二个规范真源**——落 F04 验收 6、F03 边界。
- **N12 不把 `docs/worktrees/` 用作任何工作区（含 PR worktree）的落点**——落 F05 验收 3、F03 边界。
- **全局**：不做 `demand.md` 之外的新增功能点；不修改 `demand.md` / `status.md` / `history.md` / `clarifications/` 下已有文件；不改任何规范 / SKILL / 脚本 / 代码 / `.gitignore`。

---

## 需求追溯（与 `demand.md` 一一对应）

### W 条款（做什么 → 承载卡）

| demand 条目 | 覆盖卡 | 覆盖点 |
|---|---|---|
| **W1** 人零前置：任意位置启动、人零前置、工作区不存在时会话自建并继续 | F01（验收 1~5）；F19（验收 1、4） | 启动位置无约束；人无前置动作；会话自建；零前置可交付（E1）；建立动作执行者中性化（C-3） |
| **W2** 显式寻址：写入用绝对路径、git 写操作用 `git -C <地址>`、不依赖 cwd 与工具 | F02（验收 1~4）；F14（验收 3） | 两处寻址形态；零工具依赖；不得声称"已阻止" |
| **W3** 协议先行：契约以声明式协议产物承载，判据句从它派生 | F03（验收 5）；F04（验收 1~5） | 产物为声明来源；内容覆盖 5 类信息 |
| **W4** 产物落点 `<项目根>/docs/worktrees/`（被跟踪）；不改 `.gitignore`；不扩规则 F 例外 | F03（验收 1~4、6） | 落点；被跟踪 / 随检出即得；`.gitignore` 零改动；例外仍 2 项 |
| **W5** 产物文件名 `docs/worktrees/README.md`（需求级判据基准） | F03（验收 1~2） | 存在性判据基准 |
| **W6** 产物内容必须覆盖 5 类信息（组织形态 `[架构待填]`） | F04（验收 1~6） | 逐类可判；引用不重述；不自行创立约束 |
| **W7** 落点语义澄清（三处近名、互不替代；真源一处、产物一行承接） | F05（验收 1~4） | 三处区分句；真源 / 承接关系；不作工作区落点（N12） |
| **W8** 不用脚本硬规范（无 hook / 守卫 / 强制检测器） | F21（验收 1~3、5） | 零工具依赖；产物内无脚本声明 |
| **W9** 做事的时候带检测（简报随带 + 执行者自检；不设准入门） | F20（验收 1~5） | 检测挂现场；可被观察到（E7）；不设准入门 |
| **W10** 工作区地址可确定性解析（默认推导 + 可显式覆盖） | F06（验收 1~3） | 不依赖 cwd / 用户输入；可覆盖；覆盖后进声明 |
| **W11** 地址声明三层分层（源 / 投影 1 / 投影 2）+ 以源为准 + 动态地址不落产物 | F07（验收 1~5）；F08（验收 5）；F20（验收 4） | 三层关系；裁决规则；硬约束；可核查（E10） |
| **W12** 简报两级寻址纪律 + 文档路径协议基准句改写 | F08（验收 1~4） | 简报绝对地址（E2）；路径以地址为根；相对路径不作唯一寻址；基准句改写 |
| **W13** 硬停面收窄（唯一触发面 + 输出不含祈使句 + 删除 cwd 启动门） | F09（验收 1~6）；F19（验收 5）；F21（验收 3） | 触发面；无准入门（E5）；输出形态（E6）；SKILL 同步 |
| **W14** 主工作区定位重述（可启动、不作工作区、写入仍受禁令） | F10（验收 1~5） | 三句同时可读（E9）；无冲突残留 |
| **W15** supersede 逐句显式声明（被取代清单 + 保留项） | F11（验收 1~4） | 逐句清单（E8）；保留项；与 F09 的分工 |
| **W16** 残留风险升级为显式已知代价（不覆盖 cwd、不可被 git 强制、事后核查判据） | F12（验收 1~5） | 三条声明（E14）；不得读成条款失效；不承诺阻止（N7） |
| **W17** 机制骨架不变（worktree 唯一形态 + 每迭代一个 + 检出迭代分支 + PR 子层 + 分支名含编号） | F13（验收 1~2、4） | 逐条保留；N1 / N2 零变化 |
| **W18** 阶段 5 子 agent 以简报给定的 PR worktree 绝对地址 + `git -C` 为准 | F14（验收 1~5） | 主体口径；旧口径删除；样本可判（F7） |
| **W19** 0019 E1~E13 三分类（保留 / 改写 E5、E8 / 新增两条） | F15（验收 1~5） | 分类逐条；改写后成立（E11 / E12 / E13） |
| **W20** 0018 活指令同步（不改文件 + 登记失效 + 简报覆盖） | F16（验收 1~4） | 零文件改动（E15）；登记；派发覆盖 |
| **W21** 承接 D-1：`### 隔离边界声明` 补章节级判断方式行 | F17（验收 1~4） | 章节级判断方式（E16）；D-1 闭合 |
| **W22** 承接 D-7：两处"规范 §8.4"就地更正为 `architecture.md §8.4` + 留痕 | F18（验收 1~4） | 更正（E17）；就地；留痕 |
| **W23** 约束主体显式分离（对人零强制 / 对会话有纪律） | F19（验收 1~5） | 两类主体分写；违反判定归事后核查（M-4） |

**结论**：W1~W23 全部有卡承载，**无漏项**；**无 `demand.md` 之外新增的功能点**。

### N 条款（不做什么 → 落入卡边界 / 验收）

| demand 条目 | 落入位置 |
|---|---|
| N1 不改机制骨架 | F13 验收 1~2；本文件全局边界 |
| N2 不改阶段序列 / 职责 / 推进条件 / 并发算法 / PR 七字段 | F13 验收 4；本文件全局边界 |
| N3 不引入 hook / 脚本 / 检测器 / 准入门 / 工具依赖 | F21 验收 1~3、5；F09 验收 2~3；F20 验收 2、5 |
| N4 不改 `oamp/` 产品代码 | F13 验收 5；本文件全局边界 |
| N5 不追溯已完成迭代（0018 例外且不改文件） | F16；F13 验收 6；本文件全局边界 |
| N6 不在需求阶段决定机制形态细节 | 「架构待填」T-01~T-16（原样带出）；各卡「架构待填」段 |
| N7 不承诺"跨工作区写入会被阻止" | F12 验收 2、5 |
| N8 不为"仓库外任意 cwd"新增保证 | F01 边界；F06 边界 |
| N9 不新增清理动作 | F16 边界；F13 验收 6 |
| N10 不在本迭代处理 0019 其余偏差（D-2/3/4/6） | F15 边界；F13 验收 3 的归类判据 |
| N11 产物不写成第二真源 | F04 验收 6；F03 边界 |
| N12 不把 `docs/worktrees/` 用作工作区落点 | F05 验收 3；F03 边界 |

### E 条款（裸判定效果 → 承载卡；**无未承载项**）

| demand 条目 | 承载卡（主 → 辅） | 判定方式（`demand.md` 已给出的裸判据） |
|---|---|---|
| E1 任意位置启动 + 工作区自建 + 产物落迭代工作区 + 主工作区零改动 | **F01 验收 4** → F02 验收 1~2 / F06 验收 1 / F09 验收 5 | 取一次启动的执行记录 + 两处 `git status` 前后对照 |
| E2 简报出现工作区绝对地址，产物路径可据此解析 | **F08 验收 1~2** → F07 验收 1 | 任取一份派发简报逐项核对 |
| E3 `docs/worktrees/README.md` 存在 + 覆盖 5 类信息 + 不含脚本 / 钩子 + 被跟踪 | **F03 验收 1~2** + **F04 验收 1~5** | 读文件 + `git ls-files --error-unmatch docs/worktrees/README.md` |
| E4 落点语义互不误用 + `docs/worktrees/` 下无工作树 | **F05 验收 1、3** | 读两处文本 + `ls docs/worktrees` + `git worktree list` 无 `docs/worktrees/` 前缀项 |
| E5 规范与 SKILL 不存在启动前准入门 / cwd 判据 / 用户前置 | **F09 验收 2~3、6** → F21 验收 3 | 检索 `pwd` / `--show-toplevel` 判据句 |
| E6 硬停触发面唯一 + 输出不含祈使句 | **F09 验收 1、4~5** | 构造"工作区无法创建"场景并读输出 |
| E7 做事时检测可被观察到（简报含地址与检测项；执行记录含自检结果） | **F20 验收 3** → F07 验收 1 | 任取一次派发简报 + 一次执行记录 |
| E8 规范中存在逐句显式 supersede 声明 | **F11 验收 1~3** | 与 v0.9.0 原文逐句对照 |
| E9 主工作区可启动 / 不作工作区 / 写入仍受禁令 | **F10 验收 4** | 读改动后文本逐句核对 |
| E10 三层声明可核查 + 不一致以产物为准 + 产物不含动态地址 | **F07 验收 2~4** | 三处文本比对 + 产物在两处检出中内容相同 |
| E11 0019 E1~E4 在新模型下仍成立 | **F15 验收 5** | 取两会话执行记录，逐条对照对方工作区内容 / 分支 / 提交归属 |
| E12 0019 E5 在新条款上仍成立（每条新增 / 改写条款自带判断方式） | **F15 验收 2、5** → F21 验收 4 | 逐条读新增 / 改写条款，检查 `判断方式` 行 |
| E13 0019 E8 改写后仍成立（合并出现在主工作区，该迭代的工作区前后始终检出迭代分支） | **F15 验收 2、5** | 取收口记录，对照合并前后两处的检出分支 |
| E14 规范显式声明"隔离不覆盖 cwd / 不可被 git 强制 / 事后核查"并给出核查判据 | **F12 验收 1~4** | 读规范文本 |
| E15 `0018/status.md:24` 前置句已登记失效 + 恢复派发简报覆盖 | **F16 验收 2~3** | 读 v0.10.0 变更说明 + 读恢复派发纪律表述 |
| E16 `### 隔离边界声明` 含章节级判断方式行（D-1 闭合） | **F17 验收 1、3** | 读该章节 |
| E17 不存在指向不存在规范章节的"规范 §8.4"引用（D-7 闭合） | **F18 验收 1** | 检索该记录文件 |

**结论**：E1~E17 **每条都有承载卡，无未承载项**。**E11 / E12 / E13 的判定对象是 0019 的验收效果清单本身**——它们由 `user_confirmed` 的 P-9① 裁决（W19）支撑，本阶段落为 **F15**，未自行增补需求条目。

### P 裁决（`user_confirmed`）→ 落卡

| 编号 | 裁决 | 落入位置 |
|---|---|---|
| P-1 | 升级：寻址契约载体化为声明式协议产物 | F03 / F04 / F05 / F07 / F08 / F20 / F21 |
| P-2 ① | 硬停只保留「工作区无法确定或无法创建」+ 输出不含祈使句 | F09 验收 1、4~5 |
| P-3 ① | 会话自建 + 人零前置 | F01 验收 1~3；F19 验收 1 |
| P-4 ① | 主工作区可作启动位置、不得作工作区 | F10 验收 1~4 |
| P-5 ① | 默认推导 + 可显式覆盖 | F06 验收 1~3 |
| P-6 ① | 地址声明落 `status.md` 字段 + 简报字段 | F07 验收 1；F08 验收 1 |
| P-7 ① | 简报两级寻址纪律 + 文档路径协议基准句改写 | F08 验收 1~4 |
| P-8 ① | 逐句显式 supersede | F11 验收 1~3 |
| P-9 ① | 0019 E1~E13 三分类 | F15 验收 1~3 |
| P-10 ① | 不改 0018 文件；登记失效 + 恢复派发简报覆盖 | F16 验收 1~3 |
| P-11 ① | 纳入 D-1：补章节级判断方式行 | F17 验收 1~3 |
| P-12 ① | 纳入 D-7：就地更正引用 + 新记录留痕 | F18 验收 1~3 |
| P-13 ① | 越界不可阻止升级为显式已知代价 | F12 验收 1~5 |
| P-14 ① | 机制骨架不变 | F13 验收 1~2 |
| P-15 ① | 子 agent 以简报给定 PR worktree 绝对地址 + `git -C` 为准 | F14 验收 1~3 |
| P-16 ① | 人零强制 / 会话有纪律显式分离 | F19 验收 1~5 |
| P-17 ④ | 产物落点改为 `docs/worktrees/`（被跟踪） | F03 验收 1~4；F05 验收 1~2；F07 验收 1、3 |

---

## 状态：`model_inferred` **残留为零**（8 项 MI 已裁决，见 `clarifications/prd-round-2.md`）

`demand.md` v1.0.0 的 `model_inferred` 已归零；本阶段于 v0.1.0 暴露的 8 项**不是需求结论**，而是"**验收标准的判定口径**"——它们按角色契约为 `model_inferred` 类标记（编号 `MI-xx`）随 `clarifications/prd-round-1.md` 转呈，**经用户裁决全部按推荐**（裁决落盘 `clarifications/prd-round-2.md`），卡内标记已全部转为 `[user_confirmed MI-xx]`：

| 编号 | 来源卡 | 裁决（`user_confirmed`，均按推荐） | 落定位置 |
|---|---|---|---|
| MI-01 | F01 验收 4 | E1 判定样本 = **构造 + 真实各半**（阶段 6 构造"工作区不存在 + 任意位置启动"样本；本迭代自身真实记录覆盖"任意位置启动 / 产物落迭代区 / 主工作区零改动"半句） | F01 验收 4 判定行 |
| MI-02 | F09 验收 4 | "不含祈使句" = 不出现**指向人 / 用户的动作要求**，只陈述事实 | F09 验收 4 判定行 |
| MI-03 | F09 验收 5 | 硬停时点 = **发现即停**（沿用 0019 MI-01 同族口径） | F09 验收 5 判定行 |
| MI-04 | F07 验收 2 | "差异登记"载体 = **不限**，须可被第三者读到"以源为准"的处置留痕；**不新增固定登记义务** | F07 验收 2 判定行；T-04 |
| MI-05 | F07 验收 3 | "产物不含动态地址" = 以**产物内容不随会话变化**为判据（两处检出比对），不做词形枚举 | F07 验收 3 判定行 |
| MI-06 | F20 验收 3 | E7 执行记录载体 = **不限，须含"地址 + 判定结论"两项** | F20 验收 3 判定行 |
| MI-07 | F15 验收 3 | W19"新增两条" = 在 **v0.10.0 变更说明中明确登记为两条**（与"保留 / 改写"并列） | F15 验收 3 判定行；T-12 |
| MI-08 | F04 验收 6 | "非第二真源" = **引用形态判据**（产物的规范性陈述均以指向规范条款的引用形态出现） | F04 验收 6 判定行 |

**本阶段收敛判据**：① 所有 W 有卡（W1~W23 逐条；无 W 条目但由 P-9① 支撑的 E11~E13 → F15）；② 无 `demand.md` 之外新增功能点；③ 架构待定项全部标注 `[架构待填]`（T-01~T-16，**继续留白**）；④ **`model_inferred` 残留为零**（8 项 MI 全部 `user_confirmed`）。

---

## 架构待填（**N6 原样带出，本阶段不自填**）

| 编号 | 待填内容 | 关联卡 |
|---|---|---|
| T-01 | 协议产物及其内容骨架的组织形态（章节顺序、字段名、是否用表格） | F03 / F04 |
| T-02 | 「工作区间定义」在产物中的呈现形态（落点 / 层级 / 范围的表达方式） | F04 |
| T-03 | 工作区地址的解析与建立的具体实现（命令序列、显式覆盖的输入形态） | F01 / F06 |
| T-04 | 三层声明的字段形态（`status.md` 字段名 / 简报字段名） | F07 / F08 / F14 / F16 |
| T-05 | 做事时检测项与事后核查判据的承载形态 | F12 / F20 |
| T-06 | 硬停输出的具体形态（含"不含祈使句"的措辞实现） | F09 |
| T-07 | supersede 的文本组织方式（清单位置、句级引用方式） | F11 |
| T-08 | 新增 / 改写条款在规范与 SKILL 中的落点与章节命名 | F01 / F02 / F08 / F09 / F10 / F14 / F19 |
| T-09 | 规范与 SKILL 的同步方式（何处为真源、何处只引用） | F01 / F08 / F09 |
| T-10 | 产物中「生效规范与版本」指针的写法 | F04 |
| T-11 | 既有章节的补写形态（`### 隔离边界声明` 章节级判断方式行的措辞与位置） | F17 |
| T-12 | v0.10.0 / v1.13.0 变更说明的组织形态（含 0018 失效登记、0019 E 三分类、D-7 留痕的落点） | F15 / F16 / F18 |
| T-13 | 回归锁（变更面闭合）的核对组织与基线 | F13 |
| T-14 | 产物中「无脚本 / 钩子 / 守卫」声明与「非第二真源」处置的句形态 | F04 / F21 |
| T-15 | 落点语义区分句的措辞与篇幅形态 | F05 |
| T-16 | 术语取舍（"会话工作区" vs "迭代工作区"，M-8）与术语表 | 全局 |

---

## 疑问与越界

1. **编号缺口（如实报告，未自行增补）**：`demand.md` 第二段 §2 的 W 清单共 23 条（W1~W23）；`§5` 的 E 清单 17 条（E1~E17）。**E11 / E12 / E13 在 W 清单中无逐条对应条目**（其依据是 P-9① 对 0019 E1~E13 的三分类裁决），本阶段落为 **F15〔登记项〕**——**未自行增补需求条目**；若主 agent 认为应回 demand 补 W 或降级为全局边界，删 F15 一张卡并同步索引 E→卡映射即可，其余 20 张卡不受影响。
2. **拆分粒度的非显然判断（供主 agent 复核）**：本阶段按**可独立失败的失败模式**拆卡，未按 W 编号机械一对一。四处非一对一：
   - W3 / W4 / W5 → **F03**（产物存在与落点），W6 → **F04**（内容骨架）——"文件在哪、被不被跟踪"与"文件里写了什么"是两个可独立失败的模式，E3 亦分两判；
   - W2 → **F02**（写操作形态），W10 → **F06**（地址推导规则）——"地址算得出来"与"写入是否按地址寻址"可独立失败；
   - W11 → **F07**（声明的落点与一致性），W12 → **F08**（简报如何携带 + 基准句）——E10 判前者、E2 判后者；
   - W13 → **F09**（改后的实态：无门 + 唯一硬停），W15 → **F11**（对旧句的显式取代声明）——E5/E6 判前者、E8 判后者；两卡在"规则 D 硬停形态块"处**存在判据重叠**，已按"F09 管实态、F11 管声明"分工写清，不构成双重约束。
3. **N 项未设独立卡**：N1~N12 中，N1 / N2 → F13（保证项），N3 → F21（保证项），N11 / N12 → F04 / F05 的验收与边界，其余（N4~N10）落各卡边界与本文件全局边界——**未为 N 项新增任何功能点**。体例先例：0017 F09 / F10、0019 F14 / F16。
4. **未发现 `demand.md` 内部矛盾**：W1~W23 / N1~N12 / E1~E17 / C-1~C-13 相互之间未发现冲突；三轮裁决（P-1~P-17）与第二段结论逐条一致。故本阶段**无"停下来报告矛盾"事项**。
5. **未在验收标准中引入架构决策**：全部卡的验收标准只写"谁、在什么条件下、看到什么"；命名、字段、命令、解析、措辞组织一律 `[架构待填]`（T-01~T-16）。措辞例外的范围见本文件顶部「术语口径」。
6. **`[UNVERIFIED]` 未当事实引用**：`demand.md` §7 登记的两项未实测项（仓库外任意 cwd 下主工作区的解析可行性；"同一分支已被别处检出"时的失败形态）本阶段**未据此新增任何保证或验收**（N8）。

---

## 未越界声明

本阶段**唯一写入**为 `docs/iterations/0020-session-workspace-addressing/prd.md`、`prd/F01~F21*.md`、`clarifications/prd-round-1.md`（v0.1.0 首版，全部为**新建**文件）与 `clarifications/prd-round-2.md`（v0.2.0 裁决落盘，**新建**文件）。**第二轮修订（v0.2.0）的写入范围**：`prd.md`（本文件）+ 6 张受影响卡（F01 / F04 / F07 / F09 / F15 / F20）的判定口径改写 + 新建 `clarifications/prd-round-2.md`；卡功能点、验收条目结构、边界与 `[架构待填]` 清单**均未增删**，只将 8 处 `model_inferred` 类标记替换为 `user_confirmed` 类标记（编号不变）并写入裁决值。

- **未修改** `demand.md` / `status.md` / `history.md` / `clarifications/` 下已有文件（`round-1~3.md`、`demand-round-1/2-proposals.md` 均未触碰）；
- **未触碰**任何规范 / SKILL / 脚本 / 代码：`roles/workflow-pb/workflow-pb.md`、`.claude/skills/workflow-pb/SKILL.md`、`docs/iterations/0019-*/**`、`docs/iterations/0017-*/**`、`oamp/**`、`tools/**`、仓库根 `.gitignore` 均只读参考，**未写入**；
- **未运行**任何测试或构建；**未执行**任何 git 写命令；
- **未写主工作区任何文件**（一切读写均为 `<WS>` 下的绝对路径）；
- 全部卡的产品维度未出现技术选型、数据模型或接口设计。
