# 独立验证报告 —— 阶段 4（PR 规划）产出物

**验证角色**：verifier
**验证时间**：2026-09-08
**迭代 ID**：0008-iteration-branch-workflow
**触发时机**：Gate: 阶段4→5入口（强制验证）
**产出物路径**：`docs/iterations/0008-iteration-branch-workflow/prs/pr-001-workflow-pb-iteration-branch-layer.md`

---

## 0. 搭置的需求变更/错误报告

检查 `docs/iterations/0008-iteration-branch-workflow/deferred-demand-changes.md` 是否存在：

```
$ ls docs/iterations/0008-iteration-branch-workflow/
architecture.md  clarifications/  demand.md  prd/  prd.md  prs/  status.md
```

**结论**：文件不存在。无搭置的需求变更/错误。

---

## 1. PR 粒度判断框架（判断"合并为单 PR 不拆分"这一决策本身是否成立）

**结论：pass**

pr-001 给出的三条理由逐一核实如下：

### 1.1 逻辑原子性 / 文件范围天然不可切分

pr-001 声称 D2（worktree base 显式化）与 D3（阶段 5 解锁判据改写）在 `workflow-pb.md` 第 266 行是"同一段落、同一句子"。经直接读取源文件核实为真：

`roles/workflow-pb/workflow-pb.md:266`（完整为一行，未换行）：
> "2. 对所有 `depends_on` 为空、或所依赖的 PR 均已**合并进主分支**的已解锁 PR，按当前有效上限取前 N 个……立即并发派发：为该 PR 创建独立 worktree 分支，在子 agent 里依次完成「派发 planner 产出该 PR 的 `prs/pr-{NNN}-tasks.md`」→「派发 dev 实现」→「验收」→「merge 主分支」。首批派发和后续补位派发共用同一条封顶规则……没有已解锁排队 PR 时，槛位空置，不因此放宽"合并进主分支才算解锁"这一条件去提前派发未解锁的 PR。"

该行同时包含：
- D3 范畴的解锁判据文本："所依赖的 PR 均已合并进主分支的已解锁 PR"、"合并进主分支才算解锁"
- D2 范畴的 worktree 创建描述："为该 PR 创建独立 worktree 分支"、"merge 主分支」"

证据确认：任何按 D2/D3 边界拆分 PR 的方案，都会导致同一行文本被两个 PR 的 diff 同时触碰，产生逻辑上无法独立审查、无法独立回滚的半改状态。**该理由成立，有代码级证据支撑，不是主观判断。**

### 1.2 可审查性 / D1-D4 生命周期叙事耦合

核实 `workflow-pb.md` 中「启动工作流」（第 210~221 行）与新增的「迭代分支合并进 main」小节确实紧邻同一「调度指南」章节，且架构文档 D1（创建）与 D4（合并）描述的是同一迭代分支对象从创建到销毁的完整生命周期，拆分成两个 PR 确实会造成评审时来回切换上下文。**理由成立。**

### 1.3 独立性 / D5 依赖 D1~D4 落地

D5（版本号/changelog）的验收判断（"版本号该不该跳 0.8.0"）确实在语义上依赖 D1~D4 是否已经落地成具体的协议文字变化——如果版本号先行、协议正文未改，会出现"文档自称 v0.8.0 但内容仍是 v0.7.0 行为"的自相矛盾状态。**理由成立。**

### 1.4 同类先例核实

`docs/iterations/0005-pr-concurrent-execution/prs/pr-001-workflow-pb-concurrent-dispatch-protocol.md` 与 `docs/iterations/0007-workflow-history-log/prs/pr-001-history-log-protocol.md` 均存在（已用 `ls` 核实文件真实存在），且均属于同一类"协议自我修改单 PR"迭代。引用真实、不是编造先例。

**PR 粒度判断框架整体结论：pass**——"合并为单 PR、不拆分"这一判断有充分的代码级证据支撑，符合逻辑原子性、可审查性、独立性三条判断依据。

---

## 2. 依赖正确性验证

### 2.1 depends_on 声明核实

**结论：pass**

pr-001 的 `depends_on` 为"（无）"。由于本迭代只有一个 PR，不存在需要核实的跨 PR 依赖边证据，也不存在依赖图成环的可能性（单节点图天然无环）。**通过。**

### 2.2 F01~F06 功能点覆盖完整性

**结论：pass**

对照 `prd.md` 功能列表索引（F01~F06）与 pr-001「涉及功能点」字段：pr-001 声明覆盖 F01~F06 全部六项，且在「验收标准」中逐条可追溯到 `architecture.md`「对 workflow-pb.md 的目标改动清单」§1~§11：

| architecture.md 条目 | 对应决策 | pr-001 验收标准是否覆盖 |
|---|---|---|
| §1 头部版本号 | D5/F06 | 是（第 37 行） |
| §2 新增 v0.8.0 变更说明 | D5/F06 | 是（第 38 行） |
| §3 规则 A 改写 | D2/F03 | 是（第 39 行） |
| §4 启动工作流步骤 4.5 + 迭代分支合并进 main 小节 | D1/D4/F02/F05 | 是（第 40、41 行） |
| §5 PR worktree 创建命令显式化 | D2/F03 | 是（第 42 行，见 2.3 节问题） |
| §6 解锁判据核心段落 | D3/F04 | 是（第 43 行，见 2.3 节问题） |
| §7 progress-observer 核实方式 | D3/F04 | 是（第 44 行） |
| §8 status.md 格式 | D3/D5/F04/F06 | 是（第 45 行） |
| §9 SKILL.md 同步（11 处+版本号） | D2/D3/D5/F03/F04/F06 | 是（第 48、49 行，见 2.4 节核实） |
| §10 workflow-pb-changelog.md | D5/F06 | 是（第 46 行） |
| §11 skill-optimization-v1.11.0.md 新建 | D5/F06 | 是（第 50 行） |

F01~F06 六个功能点均被 pr-001 引用且无遗漏。单 PR 场景下文件范围重叠、遗漏均无意义（无其他 PR 可比对），跳过该子项。

### 2.3 验收标准与 `workflow-pb.md` 实际文件内容的一致性核实 —— **发现两处不符**

这是本次验证中发现的实质性问题，直接回应任务里"是否存在验收标准与实际文件当前内容不符的情况"的要求。

**问题 1：第 268 行的"merge 主分支」"引用位置错误**

pr-001 第 42 行验收标准写道：
> "……第 268 行"merge 主分支」"→"merge 进当前迭代的迭代分支」""

用 `grep -n -o "主分支" roles/workflow-pb/workflow-pb.md` 核实全文 9 处 "主分支" 命中行号分布：`119, 121, 266(×3), 270(×2), 281, 458`——**不含 268**。直接读取第 268 行原文：

`roles/workflow-pb/workflow-pb.md:268`：
> "4. 某个 PR 阻塞或失败：只暂停依赖它的下游 PR，不影响其他无关 PR 的并发推进。**主 agent 不得对状态为失败/阻塞的 PR 执行 `git worktree remove`、`git branch -d` 或等价清理操作**……"

第 268 行内容与"merge 主分支"完全无关。真实的 "「merge 主分支」" 文本位于**第 266 行**末尾（同一行，与该行前半句"合并进主分支的已解锁 PR"共享同一物理行），已在本报告 1.1 节引用确认。

**根因**：该错误直接继承自 `architecture.md` 第 233~235 行（§5 小节）："- **位置**：第 268 行 / - **原文**：`……merge 主分支」`"——架构文档本身在这一处的行号标注就是错的，pr-planner 没有独立复核架构文档引用的行号是否与源文件当前实际内容一致，原样搬运了这个错误。

**问题 2：第 281 行"两处主分支"计数错误**

pr-001 第 43 行验收标准写道：
> "第 281 行「并发槛位算法」末段两处"主分支"→"迭代分支""

用 `grep -n -o "主分支" roles/workflow-pb/workflow-pb.md` 核实：第 281 行只命中 **1 次** "主分支"（`grep -c` 精确统计），原文：

`roles/workflow-pb/workflow-pb.md:281`：
> "……仍然只能分配给依赖已合并的排队 PR，**不改变「合并进主分支才算解锁」这一前提**。……不得因为槛位空闲而放宽解锁条件去提前派发依赖未合并的 PR。"

该行确实存在两处"需要编辑"的位置，但只有第一处（"合并进主分支才算解锁"）含字面"主分支"字样；第二处（"依赖未合并的 PR"→"依赖未合并进迭代分支的 PR"）是**插入新词**，不是"主分支"→"迭代分支"的替换。pr-001 的表述"两处'主分支'→'迭代分支'"不准确地把一次插入操作也算作了"主分支"替换。

**根因**：同样直接继承自 `architecture.md` 第 243~245 行（§6 小节第二条），架构文档原文本身把这两处编辑并列描述，但没有澄清"只有第一处含字面'主分支'"这一区别；pr-001 在转写为"两处主分支"时进一步固化了这个不精确的计数。

**影响评估**：两处问题均为可自行定位纠正的低风险问题——dev 在按验收标准第 42/43 行执行编辑时，由于第 266 行的完整编辑范围已经包含了"merge 主分支」"文本、第 281 行搜索"主分支"关键字仍能定位到唯一的真实编辑点，**实际编辑结果大概率不会出错**。但作为验收标准文档，其中出现与源文件当前内容不符的行号/计数陈述，违反 `workflow-pb.md` §验证目标 对验收标准"可被客观判断"的要求——如果 verifier（阶段 6）机械按行号核对，会在第 268 行找不到匹配文本从而误判为"未完成"，或反过来因为找不到而放过对第 266 行"merge 主分支」"的核查。

**结论：partial**——该子项判定为部分通过，需在进入阶段 5 之前修正这两处验收标准的行号/计数描述。

### 2.4 SKILL.md 11 处清单核实

**结论：pass**

用脚本抽取 `.claude/skills/workflow-pb/SKILL.md` 第 14/38/65/81/87/119/124/174/200/261/376 行原文，与 pr-001 及 `architecture.md` §9 表格逐条比对，11 处全部命中"主分支"字样且原文片段与 pr-001/architecture.md 描述一致（无遗漏、无多余、无行号错位）。版本号核实：`SKILL.md` 第 30 行 `**版本**: 1.10.0（对应规范 workflow-pb v0.7.0）`，与 pr-001 声称的"1.10.0→1.11.0"改动前状态一致。**通过。**

### 2.5 已修正行号引用的复核（F04 :24→:100，F06 45→54 行）

**结论：pass**

- **F04 progress-observer 引用**：pr-001 第 44 行验收标标准表述为"§可观测性·progress-observer 小节末尾新增一句……不改动 `progress-observer.md` 角色文件本身"，未在验收标准中直接引用 `progress-observer.md` 的行号（正确地避开了 architecture.md 曾经的错误引用 `:24`）。核实 `roles/progress-observer/progress-observer.md:100` 确实是"PR 依赖核实"示例表所在行（`git log main --grep="pr-001"` 找到 commit abc123`），与 architecture.md 修正记录一致。pr-001 采用的是修正后的正确理解，未复现 `:24` 的错误。**通过。**
- **F06 变更说明插入位置**：pr-001 第 38 行验收标准表述为"第 54 行空行之后、原「## v0.6.0 变更说明」标题之前"。核实 `workflow-pb.md` 第 53 行为 v0.7.0 变更说明正文结尾，第 54 行为空行，第 55 行为 `## v0.6.0 变更说明（相对 v0.5.0）`标题——与 pr-001 表述完全一致，采用的是修正后的正确版本（而非原稿错误的"第 45 行"）。**通过。**

---

## 3. 综合结论

| 验证项 | 结论 |
|---|---|
| PR 粒度判断框架（单 PR 不拆分是否成立） | **pass** |
| depends_on 声明核实（无依赖，是否合规） | **pass** |
| F01~F06 功能点覆盖完整性 | **pass** |
| 验收标准与 `workflow-pb.md` 实际内容一致性 | **partial**（两处行号/计数错误，见 2.3 节） |
| SKILL.md 11 处清单核实 | **pass** |
| 已修正行号引用复核（F04/F06） | **pass** |

### 整体结论：**partial**

**不满足直接进入阶段 5 的条件。** 需要退回阶段 4（pr-planner）对 `pr-001-workflow-pb-iteration-branch-layer.md` 做一次小范围修订，不涉及 PR 边界或依赖关系的重新规划：

**必须修正的具体问题**：
1. 第 42 行验收标准中"第 268 行"merge 主分支」"→"……"" 应改为"第 266 行同句"merge 主分支」"→"……""（该文本与该行前半句"合并进主分支"共享同一物理行，不是独立的第 268 行）。
2. 第 43 行验收标准中"第 281 行……两处"主分支"→"迭代分支""应改为准确描述：第 281 行末段有两处需编辑的位置，其中仅第一处（"合并进主分支才算解锁"）为字面"主分支"→"迭代分支"替换，第二处（"依赖未合并的 PR"→"依赖未合并进迭代分支的 PR"）为插入补充，不是术语替换。

以上两处修正均为验收标准文本的精确化,不改变 PR 粒度决策、不改变文件范围、不改变 depends_on/batch,预计可在阶段 4 内快速完成,无需重新触发架构阶段。修正后建议重新触发一次阶段 6 验证以确认。

---

## 4. 第二次验证（修正后复核）

**验证时间**：2026-09-08（pr-planner 修正后）
**验证范围**：针对第一次验证报告 §2.3 节发现的两处行号/计数错误,确认 pr-001 修正后的验收标准是否与源文件实际内容一致；快速复查全文其余验收标准条目是否有类似遗留问题。

### 4.1 第一次验证发现问题的修正情况

**问题 1：第 268 行"merge 主分支」"引用位置错误**

pr-001 修正后的第 42 行验收标准现为：
> "§调度指南·阶段 5（第 266 行,完整为一行）：该行内三处编辑点全部落在同一行——worktree 创建首次显式化为 `git worktree add <path> -b <pr分支名> iteration/{迭代ID}`；同句"合并进主分支"→"合并进当前迭代的迭代分支"；同句末尾"merge 主分支」"→"merge 进当前迭代的迭代分支」"；行尾"不因此放宽'合并进主分支才算解锁'……"一句同步替换"

用独立工具核实 `roles/workflow-pb/workflow-pb.md` 第 266 行实际内容：
```bash
$ grep -n "" roles/workflow-pb/workflow-pb.md | sed -n '266p'
266:2. 对所有 `depends_on` 为空、或所依赖的 PR 均已**合并进主分支**的已解锁 PR,按当前有效上限取前 N 个……立即并发派发：为该 PR 创建独立 worktree 分支,在子 agent 里依次完成……→「merge 主分支」。首批派发和后续补位派发……不因此放宽"合并进主分支才算解锁"这一条件去提前派发未解锁的 PR。
```

计数核实：
- `grep -o "主分支" roles/workflow-pb/workflow-pb.md:266` 命中 **3 次**（"合并进主分支"×2 + "merge 主分支」"×1）
- `grep -o "创建独立 worktree 分支" roles/workflow-pb/workflow-pb.md:266` 命中 **1 次**
- `grep -o "merge 主分支」" roles/workflow-pb/workflow-pb.md:266` 命中 **1 次**

**结论**：修正后的描述准确——第 266 行确实是"完整为一行"的长句,包含 worktree 创建描述、"合并进主分支"（2处）、"merge 主分支」"（1处）三组编辑点,全部落在同一物理行；pr-001 现在明确标注"第 266 行,完整为一行"并列举"该行内三处编辑点",与源文件实际情况完全一致。**第一次验证报告中指出的"第 268 行"错误已修正。**

**问题 2：第 281 行"两处主分支"计数错误**

pr-001 修正后的第 43 行验收标准现为：
> "第 270 行「为什么必须以'合并进主分支'作为解锁条件」标题及正文两处"主分支"→"迭代分支"；第 281 行「并发槛位算法」末段有两处编辑点：第一处"合并进主分支才算解锁"中字面"主分支"→"迭代分支"替换,第二处在"依赖未合并的 PR"之后插入"进迭代分支"（改为"依赖未合并进迭代分支的 PR"）,后者是插入补充语,不是"主分支"术语替换"

用独立工具核实 `roles/workflow-pb/workflow-pb.md` 第 270、281 行实际内容：
```bash
$ grep -n "" roles/workflow-pb/workflow-pb.md | sed -n '270p' | grep -o "主分支" | wc -l
       2
$ grep -n "" roles/workflow-pb/workflow-pb.md | sed -n '281p' | grep -o "主分支" | wc -l
       1
$ grep -n "" roles/workflow-pb/workflow-pb.md | sed -n '281p'
281:- 每次有槛位释放……仍然只能分配给依赖已合并的排队 PR,不改变「合并进主分支才算解锁」这一前提。……不得因为槛位空闲而放宽解锁条件去提前派发依赖未合并的 PR。
```

文本分析：
- 第 270 行含字面"主分支" **2 次**（标题"合并进主分支作为解锁条件" + 正文"合入 PR-A 之后的主分支"）——pr-001 表述"标题及正文两处"准确
- 第 281 行含字面"主分支" **1 次**（"合并进主分支才算解锁"），此外还有一处"依赖未合并的 PR"（需插入"进迭代分支"补充成"依赖未合并进迭代分支的 PR"）——pr-001 现在明确区分"第一处为'主分支'→'迭代分支'替换,第二处为插入补充语,不是'主分支'术语替换",与源文件实际情况完全一致

**结论**：修正后的描述准确——第 281 行确实只有 1 处字面"主分支"（不是第一次验证报告质疑的"两处主分支"计数错误）；pr-001 现在澄清了两处编辑点的性质区别（替换 vs 插入）,不再混淆计数。**第一次验证报告中指出的"两处'主分支'计数错误"已修正。**

### 4.2 全文其余验收标准条目快速复查（第 37~52 行,除已核实的 42/43）

用 grep 工具抽样核查几处关键引用的行号是否与源文件当前实际内容一致：

| pr-001 验收标准行号 | 引用的源文件位置/文本 | 核实结果 |
|---|---|---|
| 37 | workflow-pb.md 头部版本号 0.7.0→0.8.0 | `sed -n '1,30p' roles/workflow-pb/workflow-pb.md` 第 25 行：`**版本**: 0.7.0` ✓ |
| 38 | "第 54 行空行之后、原「## v0.6.0 变更说明」标题之前" | `sed -n '53,55p' roles/workflow-pb/workflow-pb.md`：53 行为 v0.7.0 正文结尾,54 行空行,55 行 `## v0.6.0 变更说明（相对 v0.5.0）` ✓ |
| 39 | §提交管理约束·规则 A 措辞改写 | 未在本次复查范围（第一次验证已判定 pass,本次聚焦行号问题） |
| 40 | §调度指南·启动工作流步骤 4.5 新增 | `sed -n '208,222p' roles/workflow-pb/workflow-pb.md` 显示第 216 行为步骤 5,第 210~215 行为步骤 1~4,**当前文件尚无步骤 4.5**（预期：改动后新增,当前验收标准与"待改写状态"一致）✓ |
| 41 | 新增「迭代分支合并进 main」小节 | 未在本次复查范围（新增小节,不是行号引用） |
| 44 | progress-observer 核实方式补充句 | 第一次验证已判定 pass（未复现 architecture.md `:24` 错误引用）✓ |
| 45 | status.md 头部字段区插入 `**迭代分支**` | `sed -n '414,426p' roles/workflow-pb/workflow-pb.md` 显示第 419~423 行为头部字段区,**当前无 `**迭代分支**` 字段**（预期：改动后新增）✓；第 458 行槛位状态列取值说明含"尚未合并进主分支"✓ |
| 48~49 | SKILL.md 版本号 1.10.0→1.11.0 及 11 处术语替换 | 第一次验证已判定 pass（11 处行号经独立核实全部命中）✓ |
| 50 | 新建 skill-optimization-v1.11.0.md | 未在本次复查范围（新建文件,不是行号引用） |

**结论**：快速抽样核查未发现新的行号/文本不符问题。pr-001 全文其余验收标准条目所引用的源文件位置（行号、文本片段）与当前实际内容一致,或为"改动后新增/改写"的预期状态（如步骤 4.5、status.md `**迭代分支**` 字段）。

### 4.3 PR 粒度判断框架与依赖正确性验证复查

第一次验证报告 §1（PR 粒度判断框架）与 §2.1/2.2/2.4/2.5（depends_on、F01~F06 覆盖、SKILL.md 清单、已修正行号引用）的结论均为 **pass**,且两处修正均为验收标准文本的精确化改写,**不触碰 PR 边界决策、文件范围、depends_on、batch、功能点覆盖**这些已 pass 的验证项。快速复查：

- pr-001「上下文摘要」第 7~11 行"单个 PR、不拆分"理由仍为同一三条（文件范围不可切分/同类先例一致/SKILL.md 不可再拆）——**未变**
- pr-001「涉及功能点」F01~F06 全部六项——**未变**
- pr-001「文件范围」六份文件清单——**未变**
- pr-001「depends_on」（无）——**未变**
- pr-001「验收标准」条目数量 16 项（第 37~52 行）——**未变**（只是第 42/43 两行的文字描述精确化,条目本身的功能覆盖范围不变）

**结论**：第一次验证已判定 pass 的验证项（PR 粒度判断框架、depends_on、F01~F06 覆盖、SKILL.md 清单、已修正行号引用）在本次修正中未被改动,仍维持 **pass** 结论。

---

## 5. 第二次验证综合结论

| 验证项 | 第一次验证结论 | 第二次验证结论 |
|---|---|---|
| PR 粒度判断框架（单 PR 不拆分是否成立） | **pass** | **pass**（未改动） |
| depends_on 声明核实 | **pass** | **pass**（未改动） |
| F01~F06 功能点覆盖完整性 | **pass** | **pass**（未改动） |
| 验收标准与 `workflow-pb.md` 实际内容一致性 | **partial**（两处行号/计数错误） | **pass**（两处问题均已修正,抽样核查其余条目无遗留问题） |
| SKILL.md 11 处清单核实 | **pass** | **pass**（未改动） |
| 已修正行号引用复核（F04/F06） | **pass** | **pass**（未改动） |

### 整体结论：**pass**

**满足进入阶段 5 的条件。**

pr-planner 针对第一次验证报告 §2.3 节发现的两处验收标准问题做了精确修正：
1. 第 42 行：原"第 268 行'merge 主分支」'"改为"第 266 行,完整为一行……该行内三处编辑点",与源文件实际内容（第 266 行确实是包含三处编辑点的长句）完全一致。
2. 第 43 行：原"第 281 行……两处'主分支'→'迭代分支'"改为"第 281 行……有两处编辑点：第一处……字面'主分支'→'迭代分支'替换,第二处……插入补充语,不是'主分支'术语替换",与源文件实际情况（第 281 行只有 1 处字面"主分支",另有 1 处插入编辑点）完全一致。

快速复查全文其余验收标准条目,未发现新的行号/文本不符问题遗留。第一次验证已判定 pass 的验证项（PR 粒度判断框架、depends_on、F01~F06 覆盖、SKILL.md 清单、已修正行号引用）在本次修正中未被改动,仍维持 pass 结论。

**pr-001 现在满足 workflow-pb.md §验证目标 对"验收标准与实际文件内容一致性"的要求,可以进入阶段 5（任务拆解）。**
