验证者身份：workflow-pb 协议同级代码审查者（熟悉 workflow-pb.md 协议文字改写模式、SKILL.md 与规范文件的同步惯例，能判断术语替换是否机械出错或产生语义断裂）
产出物：`.pb-agents/worktrees/agents-pr-001-0008` 中 commit `0bee7eb` 涉及的 6 个文件——
  - `roles/workflow-pb/workflow-pb.md`
  - `roles/workflow-pb/data/workflow-pb-changelog.md`
  - `roles/workflow-pb/memory.md`
  - `.claude/skills/workflow-pb/SKILL.md`
  - `.claude/skills/workflow-pb/data/skill-optimization-v1.11.0.md`
  - `.claude/skills/workflow-pb/memory.md`
验证标准来源：`docs/iterations/0008-iteration-branch-workflow/prs/pr-001-workflow-pb-iteration-branch-layer.md` §验收标准（第 37~52 行，16 条）
验证日期：2026-09-08

---

## 逐项判定

### 1（第 37 行）：头部版本号 `0.7.0` → `0.8.0`

pass
证据：`roles/workflow-pb/workflow-pb.md:25` 现为 `**版本**: 0.8.0`（改动前 `0bee7eb~1` 该行为 `0.7.0`，`git show 0bee7eb -- roles/workflow-pb/workflow-pb.md` 确认此为本次改动引入）。

### 2（第 38 行）：新增「v0.8.0 变更说明」节，四段齐全，生效范围写明不追溯 0005/0007

pass
证据：`workflow-pb.md:55~63`：
```
55: ## v0.8.0 变更说明（相对 v0.7.0）
57: **新增迭代分支层**：……
59: **目的**：main 应始终只包含已完成、已验证的迭代产出；……
61: **不改变的部分**：PR 文件七字段格式；阶段数量（仍为 6 个）；任何执行角色文件本身；并发槛位算法公式……
63: **生效范围**：仅从下一个使用本工作流的迭代起生效，不追溯 0005/0007。
```
触发/方案/不改变的部分/生效范围四段均存在，位置在第 54 行空行之后、原「## v0.6.0 变更说明」标题之前（现文件因插入内容整体下移，v0.6.0 节现位于第 65 行，紧接 v0.8.0 节之后，符合"之前"的位置关系）。生效范围原文与验收标准要求逐字一致。

### 3（第 39 行）：规则 A 三处编辑（主分支或迭代分支不得提交 / 判断方式改写 / 新增一句限制）

pass
证据：`workflow-pb.md:129,131`：
```
129: 从阶段 4（PR 规划）完成之后，任何代码变更必须在独立 worktree 分支上进行，不得直接在主分支（main/master）或当前迭代的迭代分支上提交。
131: 判断方式：每次提交所在分支不得为 `main`/`master`，也不得为当前迭代的迭代分支本身；所有变更必须通过 PR 合并进当前迭代的迭代分支。
```
三处编辑点（"或当前迭代的迭代分支上提交"新增、判断方式改"合并进主分支"为"合并进当前迭代的迭代分支"、新增"也不得为当前迭代的迭代分支本身"限制）全部落实，与验收标准原文逐字比对一致。

### 4（第 40 行）：启动工作流步骤 4.5

pass
证据：`workflow-pb.md:226`：
```
226: 4.5. 阶段 1（需求收敛）通过推进条件后，立即执行 `git branch iteration/{迭代ID} main` 创建迭代分支，随后 `git checkout iteration/{迭代ID}` 切换工作区；在 status.md 头部写入 `**迭代分支**: iteration/{迭代ID}`。此后阶段 2~5 期间主 agent 根目录保持在该分支上。
```
`git branch` + `git checkout` + status.md 字段写入三个动作齐全，紧跟在步骤 4「从阶段 1 开始」之后，编号为 4.5，与验收标准要求一致。

### 5（第 41 行）：新增「迭代分支合并进 main」小节

pass
证据：`workflow-pb.md:333~344`：
```
333: ### 迭代分支合并进 main
335: 阶段 6（独立验证）对本迭代最终产物判定 pass 之后，主 agent 依次执行：
336: 1. `git checkout main`
337: 2. `git merge --no-ff iteration/{迭代ID}`
338: 3. `git branch -d iteration/{迭代ID}`
340: commit message 格式：`merge: iteration {迭代ID} {该迭代一句话目标} into main`。
342: 若步骤 2 报告冲突：停止推进，向用户上报（结构性错误，类比"PR 依赖图有环"，不在调度层绕开）。
344: 合并完成后，status.md 的 `**迭代分支**` 字段改为 `iteration/{迭代ID}（已合并）`。
```
触发时机、三步 git 命令、commit message 格式、冲突处理、字段更新五个子要素全部落实，与验收标准原文逐字比对一致。

### 6（第 42 行）：阶段 5 第 266 行三处编辑点全部落在同一行

pass
证据：改动前（`0bee7eb~1:roles/workflow-pb/workflow-pb.md` 第 266 行）：
```
266: 2. 对所有 `depends_on` 为空、或所依赖的 PR 均已**合并进主分支**的已解锁 PR，……为该 PR 创建独立 worktree 分支，在子 agent 里依次完成……→「验收」→「merge 主分支」。……没有已解锁排队 PR 时，槛位空置，不因此放宽"合并进主分支才算解锁"这一条件去提前派发未解锁的 PR。
```
改动后（`workflow-pb.md` 第 277 行，因前文插入内容整体下移 11 行，但仍是单独一整行，未被拆分）：
```
277: 2. 对所有 `depends_on` 为空、或所依赖的 PR 均已**合并进当前迭代的迭代分支**的已解锁 PR，按当前有效上限取前 N 个……立即并发派发：为该 PR 创建独立 worktree 分支（`git worktree add <path> -b <pr分支名> iteration/{迭代ID}`，base 为当前迭代的迭代分支，不是 main），在子 agent 里依次完成「派发 planner 产出该 PR 的 `prs/pr-{NNN}-tasks.md`」→「派发 dev 实现」→「验收」→「merge 进当前迭代的迭代分支」。……没有已解锁排队 PR 时，槛位空置，不因此放宽"合并进迭代分支才算解锁"这一条件去提前派发未解锁的 PR。
```
四处编辑点核对：
1. worktree 创建首次显式化为 `git worktree add <path> -b <pr分支名> iteration/{迭代ID}` — 落实，且补充说明"base 为当前迭代的迭代分支，不是 main"，语义清晰无歧义。
2. 同句"合并进主分支"→"合并进当前迭代的迭代分支" — 落实。
3. 同句末尾"merge 主分支」"→"merge 进当前迭代的迭代分支」" — 落实（原文"merge 主分支」"改为"merge 进当前迭代的迭代分支」"，动词"进"补充使语义更通顺，未破坏原意）。
4. 行尾"不因此放宽'合并进主分支才算解锁'……"一句同步替换为"迭代分支" — 落实。
未发现机械替换产生语法或语义错误；整句读下来逐句连贯，"base 为当前迭代的迭代分支，不是 main"这一插入的括号说明进一步降低了歧义，是对原文的合理增强而非偏离。

### 7（第 43 行）：第 270/281 行两处措辞区分字面替换与插入补充语

partial（子项拆分：两条判据均落实到位，但行号标注与实际改动后行号不符，需澄清是否构成实质问题）

**子项 A（原第 270 行标题及正文两处"主分支"→"迭代分支"）**：pass
证据：改动前 `0bee7eb~1` 第 270 行：
```
270: **为什么必须以"合并进主分支"作为解锁条件，不是"dev 自称完成"**：……PR-B 的 worktree 分支必须是从"已经合入 PR-A 之后的主分支"拉出来的……
```
改动后（现文件第 281 行，同样因前文插入下移）：
```
281: **为什么必须以"合并进迭代分支"作为解锁条件，不是"dev 自称完成"**：每个 PR 在独立 worktree 分支上工作，如果 PR-B 依赖 PR-A 的产出，PR-B 的 worktree 分支必须是从"已经合入 PR-A 之后的迭代分支"拉出来的，否则 PR-B 的子 agent 在自己的分支里根本看不到 PR-A 的代码。这是技术约束，不是可选的保守做法。
```
标题"为什么必须以……"中的"主分支"→"迭代分支"，正文"已经合入 PR-A 之后的主分支"→"……迭代分支"，两处均为字面替换，落实且语义通顺，未产生断句错误。

**子项 B（原第 281 行「并发槛位算法」末段两处编辑点）**：pass
证据：改动前 `0bee7eb~1` 第 281 行：
```
281: - 每次有槛位释放……不改变「合并进主分支才算解锁」这一前提。**没有已解锁且排队中的 PR 时，释放出的槛位保持空置，不触发任何派发，不得因为槛位空闲而放宽解锁条件去提前派发依赖未合并的 PR。**
```
改动后（现文件第 292 行）：
```
292: - 每次有槛位释放……不改变「合并进迭代分支才算解锁」这一前提。**没有已解锁且排队中的 PR 时，释放出的槛位保持空置，不触发任何派发，不得因为槛位空闲而放宽解锁条件去提前派发依赖未合并进迭代分支的 PR。**
```
第一处"合并进主分支才算解锁"中字面"主分支"→"迭代分支"：落实，是纯字面替换（"合并进"三字保持不变，仅替换宾语）。
第二处"依赖未合并的 PR"之后插入"进迭代分支"：核对原文为"依赖未合并的 PR"，改后为"依赖未合并进迭代分支的 PR"——插入位置正确（在"未合并"和"的 PR"之间插入"进迭代分支"），是插入补充语而非替换已有的"主分支"字样（原句此处根本没有"主分支"二字），验收标准对"这两处编辑性质不同"的区分描述准确，dev 的实现也确实按这个区分正确操作，未把插入误做成替换。

**判定说明**：验收标准原文写"第 270 行"和"第 281 行"，均为改动前基线文件的行号；改动后因第 226 行新增步骤 4.5（+1 行）与第 55~63 行新增 v0.8.0 变更说明节（+9 行，含空行）共上移约 10~11 行，导致改动后实际行号变为 281 和 292。PR 文件第 42 行已预先声明"实际行号以改动时最新文件为准"，第 43 行本条虽未重复该免责声明，但该声明适用于整份验收标准，不构成 dev 实现的偏差，只是行号引用方式在文档间存在一次性偏移，两处内容改动本身完全落实、语义正确。此项判定 partial 仅因"第 270/281"字面行号与当前文件行号不一致需要澄清，不影响内容改动是否完成的判定——内容层面应为 pass。

### 8（第 44 行）：可观测性·progress-observer 小节末尾新增一句

pass
证据：`workflow-pb.md:304`：
```
304: **核实项**：……当前迭代存在迭代分支时，核实合并状态的 git log 命令以迭代分支为基准（如 `git log iteration/{迭代ID} --grep="pr-001"`），而非 main。
```
新增句紧跟在"核实项"段落末尾，示例命令与验收标准原文一致。核对 `progress-observer.md` 角色文件本身：
```bash
git diff 0bee7eb~1 0bee7eb -- roles/progress-observer/progress-observer.md
```
无输出，确认该角色文件本身逐字未变，符合"不改动 progress-observer.md 角色文件本身"的要求。

### 9（第 45 行）：status.md 格式新增迭代分支字段 + 第 458 行槛位状态列取值说明替换

pass
证据：`workflow-pb.md:443~448`（status.md 格式定义块）：
```
443: **工作流**: workflow-pb v{版本}
444: **迭代**: {编号-迭代名}
445: **当前阶段**: {阶段名}（阶段 {#}）
446: **迭代分支**: iteration/{迭代ID}
447: **状态**: {进行中 / 等待确认 / 已完成 / 已终止}
448: **history**: 开启
```
`**迭代分支**` 字段准确插入在「当前阶段」与「状态」之间，符合验收标准位置要求。

槛位状态列取值说明（现文件第 483 行，原基线第 458 行，因前文插入下移 25 行）：
```
483: **槛位状态列取值**：`占用`……/ `排队(依赖未满足)`（依赖的 PR 尚未合并进当前迭代的迭代分支）/ `排队(等待槛位)`……/ `已释放`……
```
"尚未合并进主分支"→"尚未合并进当前迭代的迭代分支"替换落实，与验收标准原文一致。行号偏移原因同第 7 条说明，属于文档引用行号随内容增长自然偏移，非实现偏差。

### 10（第 46 行）：`workflow-pb-changelog.md` 新增 `## v0.8.0（日期）` 条目

pass
证据：`roles/workflow-pb/data/workflow-pb-changelog.md:3~26`，条目包含：
```
3: ## v0.8.0（2026-09-08）
5: **触发**：……
7: **方案**：……（含五点具体机制）
14: **未改变的部分**：PR 文件七字段格式；阶段数量（仍为 6 个）；执行角色本身；并发槛位算法公式（起始并发数/爬升公式/硬上限不变，只是判据的检查对象从 main 变为迭代分支）。
16: **生效范围**：仅从下一个使用本工作流的迭代起生效，不追溯 0005/0007。
18: **具体改动**：（8 条列表）
```
触发/方案/具体改动清单三段齐全；未改变部分明确列出"PR 文件七字段格式、阶段数量、执行角色本身、并发槛位算法公式"四项，与验收标准逐字对照一致（验收标准原文即为这四项）。

### 11（第 47 行）：`roles/workflow-pb/memory.md` 新增索引行，格式与既有条目一致

pass
证据：`roles/workflow-pb/memory.md:8`：
```
8: - **v0.8.0（2026-09-08）**：新增独立于 main 的中间分支层"迭代分支"（`iteration/{迭代ID}`）……根因和决策过程见 `data/workflow-pb-changelog.md`。
```
与既有条目（如第 9 行 v0.7.0、第 11 行 v0.5.0）格式对比：均为 `- **v{版本}（日期）**：{一句话摘要}。根因和决策过程见 \`data/workflow-pb-changelog.md\`。` 的固定模式，新增行格式与既有条目风格无法区分，新条目插入在列表最前（v0.8.0 在 v0.7.0 之前），符合"最新版本在前"的既有排列顺序。

### 12（第 48 行）：`SKILL.md` 版本号 `1.10.0`→`1.11.0`，description 中 `v0.7.0`→`v0.8.0`

pass
证据：`.claude/skills/workflow-pb/SKILL.md`：
```
4: 启动并驱动 pb 产品研发工作流（v0.8.0）。……
30: **版本**: 1.11.0（对应规范 workflow-pb v0.8.0）
58: 接收迭代 ID，按 workflow-pb v0.8.0 规范调度 6 个阶段……
```
全文检索 `grep -n "v0\.7\.0" .claude/skills/workflow-pb/SKILL.md` 无命中，确认无遗留的 v0.7.0 引用（对应 dev 提交信息中提到的"修正一处遗留的 v0.7.0 规范引用"）。

### 13（第 49 行）：`SKILL.md` 全文 11 处"主分支"字样全部替换，替换后无残留

pass
证据：`grep -c "主分支" .claude/skills/workflow-pb/SKILL.md` 返回 `0`，全文零残留。

逐一核对 11 处原行号（14/38/65/81/87/119/124/174/200/261/376）与改动后内容，`git show 0bee7eb -- .claude/skills/workflow-pb/SKILL.md` 完整 diff 显示：
| 行号 | 改动内容 | 结果 |
|---|---|---|
| 14 | "合并进主分支"→"合并进当前迭代的迭代分支" | 落实 |
| 38 | 同上（CRITICAL 声明） | 落实 |
| 65 | "阶段 5 所有 PR 合并进主分支"→"……迭代分支" | 落实 |
| 81 | 对抗惯性表同上 | 落实 |
| 87 | 判断锚点·成功标准同上 | 落实 |
| 119 | "PR-B worktree 必须从已含 PR-A 代码的主分支拉出"→"……迭代分支拉出" | 落实 |
| 124 | "合并进主分支才算解锁"→"……迭代分支才算解锁" | 落实 |
| 174 | "解锁条件是合并进主分支……从已含 PR-A 代码的主分支拉出"→双处均替换为"迭代分支" | 落实 |
| 200 | "'合并进主分支'原则"→"'合并进当前迭代的迭代分支'原则" | 落实 |
| 261 | "merge 进主分支后重新扫描依赖图"→"merge 进当前迭代的迭代分支后重新扫描依赖图" | 落实 |
| 376 | Safety 中"阶段 5 解锁条件是'合并进主分支'"→"……'合并进当前迭代的迭代分支'" | 落实 |

11 处逐一核实，均按 architecture.md §9 表格的原文/改后文字精确落地，用词略有差异（"当前迭代的迭代分支" vs 单独"迭代分支"）属于同义表述，未产生语义偏差。全文交叉检索确认不再残留裸露的"合并进主分支"/"主分支拉出"表述。

### 14（第 50 行）：新建 `skill-optimization-v1.11.0.md`，记录 11 处替换清单及版本号升级原因

pass
证据：`.claude/skills/workflow-pb/data/skill-optimization-v1.11.0.md` 全文 39 行，包含触发/根因/方案/具体改动（含 11 行替换对照表，行号与替换前后文本）/未改变的部分/一致性核对/关联文件七个部分。11 处替换表格（第 13~25 行）与实际 SKILL.md 改动逐一核对一致，无遗漏无多余。版本号升级原因在"触发"和"方案"两段中说明（main 应始终代表已验证产出，新增迭代分支层需要 SKILL.md 同步表述）。

### 15（第 51 行）：`.claude/skills/workflow-pb/memory.md` 新增索引行，格式与既有条目一致

pass
证据：`.claude/skills/workflow-pb/memory.md:5`：
```
5: - [v1.11.0 优化记录](data/skill-optimization-v1.11.0.md) — 新增"迭代分支"中间层……与 workflow-pb.md v0.8.0 保持一致
```
与既有条目（第 6 行 v1.10.0、第 7 行 v1.1.0）格式对比：均为 `- [v{版本} 优化记录](data/skill-optimization-v{版本}.md) — {一句话摘要}` 模式，新条目插入在列表最前，格式与既有条目风格无法区分。

### 16（第 52 行）：workflow-pb.md 与 SKILL.md 全文交叉检索无重复定义/表述不一致

pass
证据：
```bash
grep -n "主分支" roles/workflow-pb/workflow-pb.md .claude/skills/workflow-pb/SKILL.md
```
结果：`SKILL.md` 零命中；`workflow-pb.md` 仅一处命中（第 129 行）：
```
129: 从阶段 4（PR 规划）完成之后，任何代码变更必须在独立 worktree 分支上进行，不得直接在主分支（main/master）或当前迭代的迭代分支上提交。
```
该处"主分支（main/master）"是刻意保留的正式术语（指代 main/master 分支本身，与"迭代分支"并列作为两种不同的禁止直接提交对象），不是遗留未替换的"合并进主分支"/"从主分支拉出"表述，符合验收标准里"（历史 changelog 段落除外）"及 pr-001-tasks.md AC-T17-1 "不存在任何一处仍将'合并进主分支'/'从主分支拉出'用作 PR 合并目标或 worktree base 的表述"的排除范围——此处不是合并目标或 worktree base 语境。

三大主题交叉核对：
- **PR 合并目标**：`workflow-pb.md:277` "merge 进当前迭代的迭代分支" vs `SKILL.md:261` "merge 进当前迭代的迭代分支后重新扫描依赖图"——一致。
- **worktree base**：`workflow-pb.md:277` "为该 PR 创建独立 worktree 分支（`git worktree add <path> -b <pr分支名> iteration/{迭代ID}`，base 为当前迭代的迭代分支，不是 main）" vs `SKILL.md:119` "PR-B worktree 必须从已含 PR-A 代码的迭代分支拉出"——一致，均指向迭代分支而非 main。
- **阶段 5 解锁判据**：`workflow-pb.md:277,281` "合并进当前迭代的迭代分支"才算解锁 vs `SKILL.md:38,81,124,174,376` "合并进当前迭代的迭代分支"才解锁——一致，无一份文件仍停留在"主分支"表述。

未发现两份文件对上述三个主题存在冲突表述或重复定义窗口。

---

## 汇总

- pass: 15 项
- fail: 0 项
- partial: 1 项（第 43 行/第 7 条，内容改动本身完全落实，仅因验收标准引用的行号在改动后随前文插入内容自然偏移，与当前文件行号不一致，需澄清是否构成本条判定标准的实质缺陷）
- blocked: 0 项

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| PR 验收标准第 43 行写"第 270 行"「为什么必须以'合并进主分支'作为解锁条件」标题及"第 281 行「并发槛位算法」末段"，均为改动前基线文件（v0.7.0，600 行版）的行号 | 因第 40/41 条验收标准对应的改动（新增步骤 4.5 共 1 行、新增 v0.8.0 变更说明节共 9 行含空行）落地在这两处引用行号之前，改动后实际行号分别变为第 281 行和第 292 行 | 该偏差不影响内容判定（子项 A、B 均已核实落实且语义正确），仅是验收标准文档本身引用了"改动前的行号"这一表述方式的固有局限；PR 文件第 42 行已对同类问题做过"实际行号以改动时最新文件为准"的免责声明，但该声明未覆盖到第 43 行——建议后续迭代的 PR 验收标准统一在"引用具体行号"的条目上加注"改动前基线行号，实际行号随本 PR 内其他改动的插入顺序变化"，避免验证者需要额外推导行号偏移量才能确认判定 |

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制。不在当前验收标准范围内，供主 agent 和 planner 决策是否纳入下一迭代。

- **v0.8.0 迭代分支机制本身尚无真实多 PR 并发执行验证**：本迭代（0008-iteration-branch-workflow）修改协议文字时，`0008-iteration-branch-workflow` 自身仍在使用发明本机制之前的单层结构执行（未观察到本迭代自己创建了 `iteration/0008-iteration-branch-workflow` 分支的证据），与 v0.7.0 变更说明中记录的"协议自我修改类迭代通常无法用自身验证新协议"的先例模式相同。建议下一个真正使用 workflow-pb v0.8.0 的迭代，在阶段 6 强制核查：迭代分支是否被真实创建、PR worktree 是否真的以迭代分支为 base、迭代分支合并进 main 的三步 git 命令是否被真实执行（而非仅协议文字层面自洽），类比已有的"并发调度真实执行证据"先例检查项。
- **`workflow-pb.md:129` 的"主分支（main/master）"与"迭代分支"并列表述，长期可能产生认知负担**：规则 A 现在要求判断"当前提交分支既不是 main/master 也不是当前迭代的迭代分支"，两个独立的禁止对象共存于一句话中；随着未来协议演进（如迭代分支概念被进一步拆分或引入更多中间层），这句话可能需要再次扩展。当前不构成问题，仅记录为潜在的复杂度增长点。

## 结论

**PASS**

15 项 pass、0 项 fail、1 项 partial（partial 项的两个内容子项均已核实落实且语义正确，partial 判定仅源于验收标准行号引用与改动后实际行号存在自然偏移，属于文档表述层面的可澄清点，不构成实现缺陷）、0 项 blocked。dev 的实现在协议文字改写、术语替换、changelog/memory 索引同步、SKILL.md 交叉一致性四个维度均满足 PR-001 验收标准，未发现机械替换导致的语法或语义错误，未发现两份核心文件对"PR 合并目标/worktree base/阶段5解锁判据"的表述冲突。
