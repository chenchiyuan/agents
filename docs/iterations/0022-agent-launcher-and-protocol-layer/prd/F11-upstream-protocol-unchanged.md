# F11：不改 omp / harness 源码与协议 〔保证项〕

**功能 ID**: F11
**来源**: `demand.md` 第二段 §3 **N2**（`user_confirmed`：A7 保留）；事实 **F1~F6**（RPC 所需能力经实测已具备：版本协商 / 受理与终态 / 三类增量 / 双向 / 反向请求 / 审批门上浮）
**迭代**: 0022-agent-launcher-and-protocol-layer
**产品对象（引用 `demand.md` 只读实测，非本阶段决策）**: RPC 面既有能力（F1 ready 帧与协议协商、F2 受理 ≠ 完成、F3 三类增量、F4 双向、F5 反向请求、F6 审批门上浮）

---

## 用户价值

只在 hub 这一侧加东西——agent 那边不用换版本、不用改协议，现有环境直接可用。

## 验收标准

1. **omp / harness 侧源码与协议零改动**（N2）：本迭代的改动不触及 omp / harness 的源码与协议；所需能力**消费既有能力即可**（F1~F6 已实测具备）。
   *判定*：查本迭代的改动范围 → omp / harness 侧源码与协议零改动；本迭代用到的能力均可回指到 F1~F6 的既有实测。
2. **不向 omp 提新能力需求**（N2）：本迭代不要求 omp 侧新增协议能力或字段。
   *判定*：查本迭代的产物与依赖 → 不含"需 omp 侧新增能力 / 字段"的前置项。

## 边界（不包含）

- 不含 omp / harness 的任何源码与协议改动。
- 不含向 omp 提出新能力 / 新字段需求。
- 不含对 omp 版本的变更要求。
- 不含 RPC 协议面本身的实测（→ 移交阶段 3 承接项 M-1~M-5；那是"读既有能力"的实测，不是"改 omp"）。

## 架构待填（阶段 3 · **已回填**）

> **回填说明**：本节原为留白（阶段 2 体例）。阶段 3（技术架构）按 `docs/iterations/0022-agent-launcher-and-protocol-layer/architecture.md` 落定，结论均在该文件的对应小节内可核。上方「越界自查」是**阶段 2 的历史自查记录**，其中「已落留白」一类表述由本节取代；本卡的产品维度（用户价值 / 验收标准 / 边界）未被阶段 3 触碰。

**阶段 3 结论：本卡在架构面零改动**——omp / harness 侧源码与协议零改动；本方案消费的每一项能力都可回指既有实测（F1~F6 + M-1~M-5），无「需 omp 侧新增能力 / 字段」的前置项（architecture §6 F11 / §5.4）。

## 越界自查

- 本卡为**保证项**：只承载 `demand.md` N2 的既有边界，**未引入任何新能力、未新增功能点**。
- 体例先例：0021 F12（上游协议零改动）——同为 N 项落卡。
