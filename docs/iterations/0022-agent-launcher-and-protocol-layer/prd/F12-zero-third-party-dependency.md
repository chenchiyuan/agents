# F12：不引入第三方依赖（保持零依赖）〔保证项〕

**功能 ID**: F12
**来源**: `demand.md` 第二段 §3 **N10**（`user_confirmed`：A7 保留）；产品既有约束（oamp README：零依赖 Node.js v22 ESM，`实测`）
**迭代**: 0022-agent-launcher-and-protocol-layer
**产品对象（引用 `demand.md` 只读实测，非本阶段决策）**: oamp 的既有零依赖约束（README）

---

## 用户价值

不为这次改动引入新的外部包——依赖面不扩大，装起来还是那么轻。

## 验收标准

1. **不新增第三方依赖**（N10）：本迭代引入的代码不依赖任何第三方包。
   *判定*：查依赖清单 / 安装产物 → **无新增第三方依赖项**。
2. **保持既有零依赖约束**（README）：实现仍以既有运行时自足（Node.js v22 ESM），不引入构建工具或运行时依赖。
   *判定*：核对实现所用能力的来源 → 均来自既有运行时与项目自身代码。

## 边界（不包含）

- 不含新增任何第三方包、构建工具或运行时依赖。
- 不含运行时版本的变更（既有版本约束不动）。
- 不含"把既有自研代码替换成第三方实现"这类等价变更（同样扩大依赖面）。

## 架构待填（阶段 3 · **已回填**）

> **回填说明**：本节原为留白（阶段 2 体例）。阶段 3（技术架构）按 `docs/iterations/0022-agent-launcher-and-protocol-layer/architecture.md` 落定，结论均在该文件的对应小节内可核。上方「越界自查」是**阶段 2 的历史自查记录**，其中「已落留白」一类表述由本节取代；本卡的产品维度（用户价值 / 验收标准 / 边界）未被阶段 3 触碰。

**阶段 3 结论：本卡在架构面零改动**——4 个新建文件只用 `node:*` 内置模块；`package.json`（`dependencies = {}`）零改动，既有 `test/hygiene.test.js` 的零依赖断言继续成立（architecture §9.1 / §9.3）。

## 越界自查

- 本卡为**保证项**：只承载 `demand.md` N10 的既有边界，**未引入任何新能力、未新增功能点**。
- 体例先例：0021 F11（无历史台账）——同为 N 项落卡。
