# F13：不实现 OAMP Router / 实例间消息层 〔保证项〕

**功能 ID**: F13
**来源**: `demand.md` 第二段 §3 **N11**（`user_confirmed`：J1 范围 + A7 保留 + 登记⑤「相邻 / 重叠登记」）
**迭代**: 0022-agent-launcher-and-protocol-layer
**产品对象（引用 `demand.md` 只读实测，非本阶段决策）**: `docs/multi-omp-agent-protocol.md`（**方案建议态，自身标注「待实现阶段确认」**；主题 = **实例间消息层（OAMP Router）**：link / register / heartbeat / message / ack；其 §4 `instances[]` 字段集与本迭代 L1 profile **概念相邻**，其 §10 / §14.1 的「adapter 层」命名与本迭代 L2 相撞）

---

## 用户价值

这次只管"怎么把 agent 起起来、怎么跟它说话"——不管 agent 之间怎么互相发消息，范围不发散。

## 验收标准

1. **不实现 OAMP Router / 实例间消息层**（N11）：本迭代不实现该文的 Router 与实例间消息面（投递 / 注册 / 心跳 / 租约等）。
   *判定*：查本迭代产物 → 无 Router / 消息投递 / 注册 / 租约相关实现。
2. **L1 profile 与该文 `instances[]` 不做合并**（N11 / 登记⑤）：本迭代的启动数据与该文 §4 的 `instances[]` 配置**不承诺合并**（仅保留字段可对齐的余地，归属不同层：启动面 vs 消息面）。
   *判定*：查启动数据的定义 → 未与 `instances[]` 合并为一个配置；两者可各自读到。
3. **不改该文**（登记⑤）：本迭代不修改该文档的内容与定位（其"方案建议、待实现阶段确认"的状态不变）。
   *判定*：查本迭代的改动范围 → 该文档零改动。

## 边界（不包含）

- 不含 Router / 实例间消息层的任何实现（不因"命名相邻"而顺手实现）。
- 不含与该文 `instances[]` 配置的合并承诺（N11）。
- 不含对该文档的修改或对其结论的采纳（它自身是"方案建议，待实现阶段确认"）。
- 不含 `adapter` 命名相撞的处置（命名属阶段 3 / 4 的形态与实现面，不是产品行为）。

## 架构待填（阶段 3 · **已回填**）

> **回填说明**：本节原为留白（阶段 2 体例）。阶段 3（技术架构）按 `docs/iterations/0022-agent-launcher-and-protocol-layer/architecture.md` 落定，结论均在该文件的对应小节内可核。上方「越界自查」是**阶段 2 的历史自查记录**，其中「已落留白」一类表述由本节取代；本卡的产品维度（用户价值 / 验收标准 / 边界）未被阶段 3 触碰。

**阶段 3 结论：本卡在架构面零改动**——`src/router.js` 的 `VALID_TYPES`（封闭 4 类）不动；L1 profile 与该文 §4 的 `instances[]` **不合并**（两者可各自读到）。另：命名冲突（该文 §10 / §14.1 的「adapter 层」指**消息面**）已由架构阶段处置——本迭代统一用「协议层 / 协议实现」称呼，**不新造同名层名**（architecture §7 T-01 / §9.3）。

## 越界自查

- 本卡为**保证项**：只承载 `demand.md` N11 的既有边界，**未引入任何新能力、未新增功能点**。
- 本卡**不选边站**：L1 profile 与该文 `instances[]` 不构成矛盾（同名字段与命名相邻，非冲突），故不向上游报告矛盾。
