# F10：服务边界不变（仅本机监听 / 零鉴权 / 不做多用户）〔保证项〕

**功能 ID**: F10
**来源**: `demand.md` 第二段 §3 **N8**（`user_confirmed`：A7「N1~N11 全部保留」；沿用 0021 合同的既有边界）
**迭代**: 0022-agent-launcher-and-protocol-layer
**产品对象（引用 `demand.md` 只读实测，非本阶段决策）**: N8 原文（服务仍 `127.0.0.1`、零鉴权、不做多用户 / 跨机——既有安全边界）

---

## 用户价值

这次不把服务开到局域网或公网去——本机和以前一样，别人连不上也不需要登录。

## 验收标准

1. **服务边界不变**（N8）：服务仍**仅在本机（`127.0.0.1`）监听**、**零鉴权**、**不做多用户 / 跨机接入**。
   *判定*：从另一台机器 / 局域网访问 → 仍不可达；访问服务仍不需要任何凭据。
2. **本迭代不触碰接入面**（N8）：本迭代的改动不改变监听地址、不新增鉴权、不引入多用户 / 跨机接入。
   *判定*：查本迭代的改动范围 → 监听地址与鉴权相关行为零改动。

## 边界（不包含）

- 不含鉴权、多用户、跨机接入的任何新增（N8 封死）。
- 不含服务监听的其它变更（本迭代不改服务边界）。
- 不含与宿主 / 协议运行相关的其它服务面（若涉及，属阶段 3 的形态决策，不是本卡的验收对象）。

## 架构待填（阶段 3 · **已回填**）

> **回填说明**：本节原为留白（阶段 2 体例）。阶段 3（技术架构）按 `docs/iterations/0022-agent-launcher-and-protocol-layer/architecture.md` 落定，结论均在该文件的对应小节内可核。上方「越界自查」是**阶段 2 的历史自查记录**，其中「已落留白」一类表述由本节取代；本卡的产品维度（用户价值 / 验收标准 / 边界）未被阶段 3 触碰。

**阶段 3 结论：本卡在架构面零改动**——不触碰监听地址 / 鉴权面；本迭代新增的「协议指定」是**进程内配置**（`config.json` + env + `agent start` flag），**不是**新接口、不改服务边界（architecture §6 F10 / §9.3）。

## 越界自查

- 本卡为**保证项**：只承载 `demand.md` N8 的既有边界，**未引入任何新能力、未新增功能点**。
- 体例先例：0021 F10（服务边界不变）——同为 N 项落卡。
