# verify-pr-005-20260914 — 阶段 5 次波独立验收报告（pr-005）

验证者身份：**协议层实现审查者 + 同级代码审查者（Node ESM / 异步子进程实现）**。
反射依据：产出物 = ① 三个新建生产模块（`protocol.js` 标准面与唯一注入点 / `rpc-client.js` 帧机 / `oneshot-client.js` 一次性执行）+ ② 一个已合并模块的加性修改（`launcher.js` 的 argv 构造入参）+ ③ 一个自证用例文件（`protocol-layer.test.js`）；验证标准全部为**可机核判据**（import 白名单 / grep 集合 / argv 逐字 / 逐帧映射 / 测试全绿）⇒ 身份落在「协议实现审查者」（帧面与门回路、生命周期语义）+「同级代码审查者」（ESM 循环导入、窄接口、零第三方依赖、零消费方）。

产出物：

- 代码（`git -C <PR worktree> diff fa2acd6..ed01648`）：
  - `oamp/src/protocol.js`（新，125 行）
  - `oamp/src/rpc-client.js`（新，552 行）
  - `oamp/src/oneshot-client.js`（新，246 行）
  - `oamp/src/launcher.js`（改，15+/9−）
  - `oamp/test/protocol-layer.test.js`（新，617 行）
- 规格：`prs/pr-005-protocol-layer-and-injection-entry.md`（迭代工作区最新版）+ `prs/pr-005-tasks.md`（T1~T6）+ `clarifications/2026-09-14-stage5-wave2-verdicts.md`（W2-A / MI-1~MI-4 / 矛盾①）+ `architecture.md` §3.3 / §5.1 / §5.2 / §5.3 / §5.4 / §5.6 / §5.7 + 既有代码基线 `oamp/src/agent.js:189-199`、`oamp/src/context-pool.js:203-227`。

验证标准来源：主 agent 派单的 7 条验证标准（下 §A）＋ pr-005 PR 文件「验收标准」14 条（下 §B）＋ W2 用户裁决（`2026-09-14-stage5-wave2-verdicts.md`）。

验证日期：2026-09-14

**上下文使用声明**：派单中携带了提交标识（`1082748` / `ed01648` / 基线 `fa2acd6`）与「主 agent 授权扩展」等字样。该信息**仅用于定位产出物与确定 diff 基准**，未作为判定依据；本报告所有结论均来自产出物本身与下述命令输出，未采信执行过程的解释。本报告为本次验收的**唯一写入**，未修改任何被验证产物（`git status --porcelain` 在验收全程为空）。

---

## A. 派单验证标准逐项判定

### A1. PR 文件「验收标准」全部满足 —— **pass**

证据：下 §B 逐条判定 14/14 全部 `pass`，每条附可定位证据（文件:行号 / 命令输出 / 断言输出）。

### A2. 改动面恰为 5 个文件，且未触碰清单外文件 —— **pass（附偏差 R2）**

证据 1（改动面清单，逐字）：

```
$ git -C <PR worktree> diff --name-status fa2acd6..ed01648
A	docs/iterations/0022-agent-launcher-and-protocol-layer/prs/pr-005-tasks.md
M	oamp/src/launcher.js
A	oamp/src/oneshot-client.js
A	oamp/src/protocol.js
A	oamp/src/rpc-client.js
A	oamp/test/protocol-layer.test.js
```

代码面 = **5 个文件**（4 新建 + `launcher.js` 加性修改）＝ 派单要求；第 6 项 `prs/pr-005-tasks.md` 是 **planner 产物**，非实现变动面：

```
$ git log --oneline --diff-filter=A -- docs/.../prs/pr-005-tasks.md
7e5f2be docs(0022): 补入 pr-005 任务图（阶段 5 次波 planner 产物）
```

`7e5f2be` 位于基线 `fa2acd6` 与 dev 提交 `1082748` 之间（`git log --oneline` 次序：`ed01648 / 1082748 / 7e5f2be / fa2acd6`），故被 `fa2acd6..ed01648` 一并计入。dev 提交自身面：`1082748` = 4 新建文件、`ed01648` = 3 文件（`launcher.js` / `oneshot-client.js` / `protocol-layer.test.js`）。该口径差异记入**偏差 R2**。

证据 2（禁改清单逐项未触碰）：上表集合外**零条目** ⇒ `oamp/src/acp-client.js` / `context-pool.js` / `agent.js` / `config.js` / `oamp/web/**` 及**任何既有测试文件**均未出现。`git status --porcelain` 输出为空（工作树干净、改动已提交）。

证据 3（`launcher.js` 修改面为纯加性）：`git diff fa2acd6..ed01648 -- oamp/src/launcher.js` 共 3 个 hunk，全部落在 `buildArgv` 文档与取参、`--approval-mode` 追加条件、`spawnAgent` 透传：

- `:109` `export function buildArgv(profileKey, { model, roleFile, tools, prompt, approval } = {})`
- `:114` `const approvalSpec = approval === undefined ? profile.approval : approval;`
- `:130-131` `if (approvalSpec !== null && (approvalSpec.appliesWhen === 'always' || toolsOn)) { args.push('--approval-mode', approvalSpec.mode); }`
- `:135` `if (profile.input === 'positional' && prompt !== undefined && prompt !== null && prompt !== '') args.push(prompt);`
- `:150` `export function spawnAgent(profileKey, { model, roleFile, tools, prompt, approval, stdin = 'pipe' } = {})` / `:152` 透传 `approval`

`PROFILES` 数据段（`:14-90`）未出现在任何 hunk 中 ⇒ pr-001 的 profile 数据行零改动。既有调用方行为不变，另有实证：全仓对 `buildArgv` / `spawnAgent` 的调用点仅存在于本 PR 的三个新模块与新增测试（`oamp/src/oneshot-client.js:11,117`、`oamp/src/rpc-client.js:9,122`、`oamp/test/protocol-layer.test.js:15,...`）⇒ **`launcher.js` 既有消费方为零**，`undefined` 路径逐字等价。

### A3. 零消费方：新面之外无生产者引用 —— **pass**

证据（逐字输出）：

```
$ grep -rn "rpc-client\|oneshot-client" oamp/src
oamp/src/protocol.js:10:import { createOneshotSession } from './oneshot-client.js';
oamp/src/protocol.js:11:import { createRpcSession, rpcCapabilities } from './rpc-client.js';

$ grep -rn "from './protocol.js'\|from './rpc-client.js'\|from './oneshot-client.js'" oamp/src
oamp/src/protocol.js:10  (oneshot-client)
oamp/src/protocol.js:11  (rpc-client)
oamp/src/oneshot-client.js:10:import { CAPABILITY_KEYS, ProtocolError } from './protocol.js';
oamp/src/rpc-client.js:8:import { CAPABILITY_KEYS, ProtocolError, readApprovalToolName } from './protocol.js';
```

命中集合恰 ⊆ `{oamp/src/protocol.js}`（第一条）与 ⊆ 本 PR 三个新模块自身（第二条），与 PR 验收 11 逐字一致；`context-pool.js:7` 对 `acp-client` 的既有命中不属本 PR 判定面（pr-003）。

扩展扫描（超出判据面、用于排除漏判）：对 `oamp/**` 与 `omp/**` 全量字符串检索 `rpc-client` / `oneshot-client` / `protocol.js`，排除本 PR 四个文件后**零命中**。

### A4. W2-A 落地（`approval` 三态 + oneshot 调用层合成三组合 + 与 `agent.js:192-199` 对齐）—— **pass**

证据 1（`buildArgv` 三态语义，实测）：

```
$ node --input-type=module -e "…buildArgv…"
oneshot tools on + approval undefined(=>profile yolo): ["-p","--no-session","--approval-mode","yolo","p"]
oneshot tools off + approval undefined:               ["-p","--no-tools","--no-session","--approval-mode","yolo","p"]
oneshot approval object:                              ["-p","--no-session","--approval-mode","always-ask","p"]
oneshot approval null + tools on:                     ["-p","--no-session","p"]
rpc default (unchanged path):                         ["--mode","rpc","--no-skills","--no-rules","--no-session","--model","m","--approval-mode","always-ask"]
```

三态逐字：`undefined` ⇒ 取 `profile.approval`（`launcher.js:114`，`omp:oneshot` 数据行 `:52` `{mode:'yolo',appliesWhen:'always'}` 未改）；`null` ⇒ 不追加（`:130` 判空短路）；对象 ⇒ 用传入值（`always-ask` 实测生效）。

证据 2（oneshot 调用层合成三组合，逐字）：

```
$ node --input-type=module -e "<复刻 oneshot-client.js:95-97 的合成式>"
tools=false perm=allow => ["-p","--no-tools","--no-session","p"]
tools=false perm=deny  => ["-p","--no-tools","--no-session","p"]
tools=true  perm=deny  => ["-p","--no-session","--approval-mode","always-ask","p"]
tools=true  perm=allow => ["-p","--no-session","--approval-mode","yolo","p"]
tools=true  perm=ask   => ["-p","--no-session","--approval-mode","yolo","p"]
```

与生产实现同源（`oamp/src/oneshot-client.js:95-97`）：

```js
const approval = toolsOn
  ? { mode: spec.permission === 'deny' ? 'always-ask' : 'yolo', appliesWhen: 'tools-on' }
  : null;
```

证据 3（与既有生产语义逐字对齐）：`oamp/src/agent.js:193` `if (!toolsOn) args.push('--no-tools');`、`:198` `if (toolsOn) args.push('--approval-mode', ctx.permission === 'deny' ? 'always-ask' : 'yolo');` ⇒ **tools 关 ⇒ 无该 flag；tools 开 + `deny` ⇒ `always-ask`；其余 ⇒ `yolo`** 三组合逐字一致，无第二读法（旧措辞「profile 数据定档 = `--approval-mode yolo`」未在实现中残留：`PROFILES['omp:oneshot'].approval` 仅在 `approval === undefined` 的既有路径取值，而 oneshot 调用层恒显式传值）。

证据 4（用例侧独立复现）：`oamp/test/protocol-layer.test.js` 的 ⑤ 组两条用例以 `buildArgv` 的 `approval` 入参表达期望值并断言 `--approval-mode` 段位与值：`node --test test/protocol-layer.test.js` ⇒ **22 tests / 22 pass / 0 fail**。

### A5. D-7′ 防护（未传提示词时 argv 末位不得为 `undefined` / 字面 `"undefined"`）—— **pass**

证据 1（实测）：`buildArgv('omp:oneshot', { prompt: undefined })` ⇒ `["-p","--no-tools","--no-session","--approval-mode","yolo"]`，末位 = `"yolo"`（`typeof === 'string'`）；`prompt: ''` 同形；`prompt: 'hello'` ⇒ 末位 `"hello"`。

证据 2（守卫实现）：`oamp/src/launcher.js:135` 的空值三态短路（`!== undefined && !== null && !== ''`），`:133-134` 注明 D-7′ 依据。

证据 3（用例断言）：`protocol-layer.test.js` ⑤ 组 `noPrompt` 与 `withPrompt.slice(0,-1)` 逐位相等 + `!noPrompt.some(arg => arg === undefined || arg === 'undefined')`；另「非字符串提示词」被 `oneshot-client.js:109-111` 在 spawn 前拒绝（用例实测 `readLog().argv` 为空，即**未 spawn**）。

### A6. 全量测试绿 + 既有 29 个测试文件零改动 —— **pass（附偏差 R5）**

证据 1（测试面计数）：`ls oamp/test/*.test.js | wc -l` = **30**（= 29 既有 + `protocol-layer.test.js`）；`oamp/test/helpers/` 仍为 2 个（`fake-node.js` / `harness.js`），零新增、零修改。

证据 2（全量命令，四次运行摘要）：

| 运行 | 命令 | 结果 |
|---|---|---|
| 1 | `node --test test/*.test.js`（cwd `oamp/`） | `# tests 357 / # pass 357 / # fail 0 / # skipped 0` |
| 2 | 同上 | `# tests 357 / # pass 356 / # fail 1`（`test/status.test.js:133` 用例、断言 `:157`） |
| 3 | 同上 | `# tests 357 / # pass 357 / # fail 0 / # skipped 0` |
| 4 | 同上 | `# tests 357 / # pass 357 / # fail 0 / # skipped 0`（并命中 4 条 `protocol-layer` 用例名 ⇒ 新用例确在全量面内） |

证据 3（唯一 fail 的归属判定）：失败断言 = `oamp/test/status.test.js:157` `assert.ok(Date.parse(r2[3]) > Date.parse(r1[3]), 'last_heartbeat 第二轮应严格推进（仅自然推进）')` —— 时序敏感断言。`node --test test/status.test.js` 单独连跑 3 次 = 3×（5 tests / 5 pass / 0 fail）。该文件不在本 PR 改动面内（§A2 清单），且本 PR 对生产模块的新增 import 为零（§A3）⇒ **不可能**由本 PR 引入；运行 2 发生在同机并发执行其它验证任务期间，判为负载抖动。记入**偏差 R5**。

证据 4（既有测试面零改动）：`git diff --name-status fa2acd6..ed01648` 的 `oamp/test/` 条目 = `A oamp/test/protocol-layer.test.js` **唯一一条** ⇒ 既有 29 个 `.test.js` 修改 0 个，`helpers/` 0 个。

证据 5（定向命令）：`node --test test/protocol-layer.test.js test/hygiene.test.js` ⇒ `25 tests / 25 pass / 0 fail`；其中 `hygiene.test.js` 单独 = `3 tests / 3 pass / 0 fail`（含 `dependencies === {}` 与凭据词扫描，扫描面覆盖三新文件文本）。

### A7. 无架构决策泄漏 —— **pass（附偏差 R3 / R4）**

证据 1（能力位键集原样，MI-A-6 六键不增不减）：`oamp/src/protocol.js:23` `CAPABILITY_KEYS = ['streaming','thinking','approvalGate','hostTools','introspection','queueControl']` —— 与 `architecture.md` §5.1 / §5.7 逐字相同；两实现的能力位表分别为 `oamp/src/rpc-client.js:31-41` 与 `oamp/src/oneshot-client.js:19-33`，逐键对照 §5.7 表：rpc = `yes/yes/yes/no/yes/yes`、oneshot = `degraded/no/no/no/no/no`，**逐字一致**。

证据 2（签名零扩展）：门面签名 `protocol.js:73`（5 参数，与 §5.1 同形，`profiles` 位保留未用并注明理由）；会话面 `rpc-client.js:534-550` / `oneshot-client.js:230-245`（`prompt/cancel/close` + `capabilities/capabilityNotes/pid/contextId`，与 §5.1 会话对象清单同构）；`onDelta` 四个 kind（`chunk/thinking/tool_call/tool_output`，`rpc-client.js:21` + `:272-292`）与 §5.1 枚举一致；`ProtocolError` 五码（`protocol.js:14-20` + `rpc-client.js:27` 域集合）。

证据 3（无新增配置键 / env / 目录 / 依赖）：新增文件全部平铺 `oamp/src/`（无新目录）；`oamp/package.json:14` `"dependencies": {}` 未变（该文件不在 diff 清单内）；模块内无新 env 读取（`launcher.js:143` 仍只读 `OAMP_OMP_BIN`）。

证据 4（未落定取值 2 处，登记为偏差）：`protocol.js:79-81` 的 acp 分支 `capabilities()` ⇒ `null`；`rpc-client.js:547-550` / `oneshot-client.js:243-245` 的 `contextId` ⇒ `null`。二者均不属「能力位键集 / 签名」的扩展，但取值本身在规格中未落定 ⇒ 记入 **R3 / R4**。

---

## B. PR 文件「验收标准」14 条逐条判定

| # | 验收标准（摘要） | 判定 | 可定位证据 |
|---|---|---|---|
| 1 | 三新文件存在且零第三方依赖：① `rpc/oneshot` import ⊆ `node:*` ∪ `{./protocol.js, ./launcher.js}`；② `protocol.js` 非受限方，其 import ⊆ `node:*` ∪ 三实现模块且**必含**三个实现；③ 无第三方裸包名、`dependencies` 仍 `{}` | **pass** | `rpc-client.js:8-9`（`./protocol.js` + `./launcher.js`）；`oneshot-client.js:10-11`（同）；`protocol.js:9-11`（`./acp-client.js` + `./oneshot-client.js` + `./rpc-client.js` —— 三实现齐备，无 `./launcher.js` 亦无第三方）；`oamp/package.json:14` = `{}`；三文件均无 `node_modules` 依赖 |
| 2 | 标准面四动作 + 三类增量 + 两类反向请求 + 能力位齐备（§5.1）；`prompt` 返回 `{text, model, stop_reason, usage, pid?}` | **pass** | `rpc-client.js:534-550`（四动作 + `capabilities`/`capabilityNotes`/`pid`/`contextId`）；`rpc-client.js:299-302`（返回载荷四字段 + `pid`）；`oneshot-client.js:230-245` / `:194-199`（同形）；`onDelta` 四 kind（`rpc-client.js:21,272-292`）；两类反向请求形状：`hooks.onApproval` 承接 `rpc-client.js:306-331`，`onHostUi` 按 §5.1 保留形状且本迭代不接线（`hostTools:'no'`） |
| 3 | 能力位声明面（本 PR 侧）：六键齐全、三态、非 `yes` 必有非空 note；`rpc.thinking==='yes'`、`oneshot.streaming==='degraded'`、`oneshot.approvalGate==='no'` | **pass** | `rpc-client.js:31-41,44-57`；`oneshot-client.js:19-33,69-82`（note 只对非 `yes` 键赋值，故 oneshot 六键 note 齐全、rpc 仅 `hostTools` 有 note）；用例 ① 断言逐键 + 三条逐字断言——`node --test test/protocol-layer.test.js` 22/22 pass |
| 4 | 解析链四档 + 选择域封闭 `{rpc, acp}`；越界值响亮失败；`oneshot` 不在域 | **pass** | `protocol.js:59-63`（角色级 > 折叠值 > 内置 `rpc`）；`:39-40`（选择域 = `{rpc,acp}`、默认 `rpc`）；`:46-51`（空即未设；域外抛 `OAMP 配置错误: … 仅支持 rpc/acp（当前值 …）`）；用例 ② 四组输入 + 四组越界输入（`oneshot` 两种承载面各判一次）实测通过；`config.json` 侧越界报错属 pr-001 已交付面（不在本 PR diff，符合 PR 文件登记①） |
| 5 | 默认走 rpc + 指定即生效（argv 观测），两者不回落；只判「选择与进程面」 | **pass** | 默认：用例 ②③ 断言 fake bin 记录 argv == `buildArgv('omp:rpc', {model, roleFile, tools:{mode:'allow'}})` 且 `slice(0,2) === ['--mode','rpc']`、不含 `--thinking`；指定 `acp`：argv[0] === `'acp'` 且不含 `--mode`；`protocol.js:86-88`（`rpc → createRpcSession` / 否则装配 `AcpClient`，无回落分支） |
| 6 | RPC 帧映射逐条（三类增量含空 delta 跳过 / `tool_output` 拼接取不到即跳过 / 终态字段来源 / 非终态不结算 / 受理不结算 / 忽略面 / 未知帧不崩） | **pass** | 实现：`rpc-client.js:21`（三类 kind 表 + 空 delta 短路 `:276`）、`:278-284`（`tool_execution_update` → `partialResult.content[].text` 拼接）、`:286-303`（`isTerminal!==true` 不结算；`stopReason`/`usage` 取末条 assistant）、`:253-258`（`prompt` 受理只置 `accepted`）、`:396-407`（忽略面 switch 默认分支）。用例 ④ 六条独立断言（含 `response{success:false}` 两码归类、`rpc_chunk` 两片重组等价单帧）全部通过 |
| 7 | 门承接（RPC 侧）：恰一次 / `title` 首行取工具名 / 二元 options / 回执逐字 / 非门 `cancelled` / 展示类不回执 | **pass** | `rpc-client.js:336-338`（双重过滤 `method==='select'` + `readApprovalToolName` 前缀判定）、`:24`（非门交互类集合 `select/confirm/input/editor`）、`:341-343`（回 `cancelled:true`）、`:345`（展示类与未知不回执）；`protocol.js:26,32-36`（自持同源原语，含多行 `title` 首行提取）。用例 ④ 三条：恰一次 + `options == [{optionId:'Approve'},{optionId:'Deny'}]` + 回执 `{type:'extension_ui_response',id:'g-1',value:'Approve'}`；三帧门 = 3 次回调；非门两帧 = 2 条 `cancelled`；展示类 `d-*` 零回执 |
| 8 | 取消与关闭语义：`cancel()` 无参 `{type:'abort'}` 幂等 → 宽限 → kill；`close()` 关 stdin → 短宽限 → kill 且容忍晚到帧 | **pass** | `rpc-client.js:508-518`（`cancel`：`abortSent` 幂等 + `CANCEL_GRACE_MS=2000` + `kill`）、`:521-530`（`close`：`closed` 幂等 + `CLOSE_GRACE_MS` + `kill`）、`:392-394`（`if (closed) return` ⇒ EOF 后帧一律不被承接）、`:445-449`（`kill`：`SIGTERM` → 500ms `SIGKILL`）。用例 ④ 实测：abort 帧逐字 `{type:'abort'}` 且重复调用仅 1 帧；`close()` 双调用后静置 300ms，fake 在 EOF 后吐门/增量/未知帧，`replies` 长度为 0 且进程无未捕获异常 |
| 9 | oneshot 无会话语义：argv（`-p` 末位位置参数 / 工具关 `--no-tools` / 角色 `--append-system-prompt` / `--approval-mode` 按调用层语义 W2-A）；两轮两进程不续接；行流 `kind`+`line` 形状与 200 行截断「与今天逐字一致」；能力位；无 shell 执行器 | **pass（行流形状按 W2-B MI-2 判；字面差异记偏差 R1）** | argv：用例 ⑤ 三条用例（`buildArgv` 期望值逐位 + `--approval-mode` 段值 + `--no-tools` 同向 + 末位位置参数）；两轮两进程：用例 ⑤ 断言 `entries.length === 2`、`pid` 互异、第二轮 argv 不含第一轮提示词、`session.contextId === null`、结算后 `session.pid === null`；行流：`oneshot-client.js:160-176`（去 ANSI → 空行跳过 → 200 行上限 → 恰一次截断），payload 形状 `{kind:'chunk', text, stream}`（MI-2）；截断 note 文案 `oneshot-client.js:172` 与 `agent.js:242` **逐字相同**（`明细行数超上限（200），后续行不再逐条上报`）；能力位见 #3；shell 执行器：`grep -nE "execSync\|spawnSync\|sh -c\|/bin/sh\|exec\(" oamp/src/oneshot-client.js oamp/src/rpc-client.js` **零命中**；另「模块内不出现 flag 字面量」实测 `grep -nE "['\"]--[a-z]"` 两模块零命中 |
| 10 | 三实现与 `protocol.js` 互有 import 可加载（无 TDZ）；实现侧对 `ProtocolError`/`CAPABILITY_KEYS` 的引用只在函数体/类成员内 | **pass** | 冒烟：`node -e "import('./src/protocol.js')"` ⇒ `loaded ok, keys: CAPABILITY_KEYS,ProtocolError,createProtocolLayer,readApprovalToolName`；静态核对：两实现模块顶层只有 `node:` 与模块 import、常量字面量（`rpc-client.js:19-41`、`oneshot-client.js:14-33`），对 `CAPABILITY_KEYS` 的求值在 `:46/:71`（函数体内），对 `ProtocolError` 的 `new` 全部在函数体内 |
| 11 | 零消费方 / 行为零变更：两条 grep 集合约束 + `git diff --stat` 只含 **5** 个文件 | **pass（diff 计数口径见偏差 R2）** | 见 §A3 逐字输出；`git diff --stat fa2acd6..ed01648` 代码面 5 文件（4 新 + `launcher.js`），第 6 项为 planner 文档提交（§A2 证据 1） |
| 12 | 既有测试面零改动零回归：29 个修改 0 个；`node --test oamp/test/*.test.js` 全绿 | **pass（附偏差 R5）** | 见 §A6：30 个测试文件、357 tests、3/4 次全绿；唯一 fail 为既有 `status.test.js:157` 时序断言（单跑 3/3 通过、与本 PR 零交集） |
| 13 | 静态卫生不因新增模块变红（`hygiene.test.js` 全绿） | **pass** | `node --test test/hygiene.test.js` ⇒ 3 tests / 3 pass / 0 fail（扫描面覆盖 `src/**` 平铺 `.js` 七词 + `dependencies === {}`） |
| 14 | 服务边界与上游零改动：diff 不含 `oamp/src/web.js` 监听/鉴权面、`oamp/API.md` / `oamp/llms.txt` / `omp/**`；无「需 omp 新增字段」前置项 | **pass** | `git diff --name-status fa2acd6..ed01648` 六条中零命中上述路径；帧面判据全部回指 `clarifications/probes/m*-output.txt`（M-1/M-2/M-2b/M-3/M-4/M-5），实现内无新增 omp 字段依赖（`PROTOCOL_VERSION=2`、`maxReassembledFrameBytes=67108864` 均取自实测 ready 帧） |

---

## C. 汇总

- 派单验证标准：**7 / 7 pass**（fail 0）
- PR 文件验收标准：**14 / 14 pass**（fail 0）
- 未判 / 阻塞（blocked）：**0**
- 偏差记录：**5 条**（R1 文档字面滞后；R2 diff 计数口径；R3 acp 分支 `capabilities()` 取值未落定；R4 会话 `contextId` 归属；R5 既有 `status.test.js` 时序断言抖动）
- 唯一写入 = 本报告；被验证产物零修改（`git status --porcelain` 全程为空）；未执行任何 git 写操作

## D. 偏差记录

| # | 规格描述 | 实现实际 | 建议处理 |
|---|---|---|---|
| R1 | PR 文件「验收标准」第 9 条与「文件范围」oneshot 条目字面：行流 `stdout` / `stderr` 的 `kind` + `line` 形状「与今天逐字一致」（PR 文件内另处写「`kind` 取值保持既有的 `stdout`/`stderr` + `line` 形状（零行为变更口径）」） | 实现按 **W2-B MI-2（用户已裁决）** 落为 `onDelta({kind:'chunk', text, stream:'stdout'|'stderr'})`（`oneshot-client.js:174-176`），`{kind,line}` 原形由消费层（pr-003）转译；截断事件形状与文案与 `agent.js:242` 逐字相同 | 阶段 6 同步 PR 文件字面（或就地注明「MI-2 覆盖」），避免二次判读；pr-003 验收需显式断言转译后的 `{kind:'stdout'|'stderr', line}` 形状 |
| R2 | 派单与 PR 文件均口径为「`git diff` 只含 **5** 个文件」 | `git diff --name-status fa2acd6..ed01648` 实为 **6** 项：多出 planner 文档提交 `7e5f2be`（`prs/pr-005-tasks.md`，非实现变动面）；dev 提交自身面 = 4 新建（`1082748`）+ 3 文件（`ed01648`，含 `launcher.js` 加性修改） | 阶段 6 复核 diff 基准时显式排除文档提交（或以「代码面」计数 / 以 `1082748^..ed01648` 为 dev 面基准），避免「多一个即 fail」的字面误判 |
| R3 | `architecture.md` §5.1 规定门面 `capabilities()` 返回「常驻协议的能力位（声明面）」；PR 文件把 acp **会话对象**的声明面外观归 pr-003，但**门面 acp 分支**的取值未落定 | `protocol.js:79-81`：`protocol === 'rpc' ? rpcCapabilities() : null`（新增用例显式断言 `layer.capabilities() === null`，把该取值固化进测试） | pr-003 落地时明确该分支契约（透传 `AcpClient` 能力位，或确认 `null` 语义并在 pr-003 验收写死）；当前无消费方，暂不构成行为风险 |
| R4 | `architecture.md` §5.1 把 `session.contextId` 列为「既有审计面」（`context_id = ctx-<pid>-<generation>` 不变） | 两实现的会话 `contextId` 恒 `null`（`rpc-client.js:547-550` / `oneshot-client.js:243-245`），审计 id 由消费层自持（`context-pool.js:133` 公式） | pr-003 接线时**不得**把 L2 会话作为审计 id 来源；建议在 pr-003 验收中对「审计 `context_id` 仍由池层生成」加显式断言，防止静默退化 |
| R5 | 「`node --test oamp/test/*.test.js` 全绿」 | 4 次全量运行中 1 次失败于 `oamp/test/status.test.js:157`（`last_heartbeat 第二轮应严格推进` 的严格不等断言，时序敏感）；该文件不在本 PR 改动面内，单文件连跑 3 次 3/3 通过，失败发生于同机并发执行其它验证任务期间 | 阶段 6 复跑时记录该抖动；如需根治（放宽为 `≥` 或注入时钟）属**既有测试面**小改，建议另行立项，不并入 0022 |

## E. 下一迭代候选

1. **pr-003 接口收口**（承接 R3 / R4）：明确门面 acp 分支 `capabilities()` 的取值契约；在 pr-003 验收中显式断言「审计 `context_id` 由池层按既有公式生成」与「行流 `{kind,line}` 由消费层转译」，把两处跨 PR 边界从「注释约定」升级为「可执行断言」。
2. **阶段 6 文档同步**（承接 R1 / R2 与 W2-C 矛盾①）：同步 PR 文件 oneshot 行流字面（MI-2）、`architecture.md` §3.4 流 2 / §5.6 表中 `options:[{option_id}]` 字样（用户已裁决按 `optionId`，`2026-09-14-stage5-wave2-verdicts.md` W2-C）；并在 diff 计数口径上统一「代码面 5 文件 / 含文档提交 6 项」的表述。
3. **既有测试稳定性硬化**（承接 R5，可选、独立立项）：`status.test.js:157` 的严格推进断言在高负载下的抖动。
4. **rpc 分片重组边界用例**（可选、非验收缺口）：现用例覆盖「两片完整重组」与「单帧等价」，可补「重复 index / 缺片 / 超 `maxReassembledFrameBytes` 丢弃」三类异常路径的独立断言（实现已按丢弃不崩处理，`rpc-client.js:352-372`）。

## F. 结论

**PASS**

- 派单 7 条验证标准与 PR 文件 14 条验收标准全部满足，未发现 fail 项，未发现 blocked 项。
- 改动面封闭（代码面恰 5 文件）、零消费方、既有测试面零改动、全量测试绿（含新增 22 条用例）、静态卫生绿、互有 import 可加载、W2-A 与 D-7′ 两项裁决/防护均落地且有实测证据。
- 5 条偏差均为「文档字面滞后」或「跨 PR 未落定取值 / 既有测试抖动」，**不影响本 PR 的验收标准成立**；其中 R1 / R3 / R4 建议在 pr-003 与阶段 6 收口时闭合。
