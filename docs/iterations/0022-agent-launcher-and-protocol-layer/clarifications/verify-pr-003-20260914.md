# pr-003 独立验证报告（消费层切到标准面：acp 对齐 + 默认 rpc + 一次性路径迁移 + B-17 零侵入断言）

**验证者身份**：同级代码审查者（主身份 —— 判定消费层切换后的行为等价性、契约边界与改动面封闭）／协议实现审查者（次身份 —— 判定标准面对齐与 acp 行为零变更）／测试工程师（次身份 —— 判定 B-17 三条断言的强度与全库绿度）。

**复述**：本次验证 `pr-003` —— 把生产消费层（`oamp/src/{context-pool,agent}.js` + 其唯一实现 `acp-client.js`）切到 L2 标准面、常驻链路默认走 `rpc`、一次性路径迁移到 `oneshot-client`、并新建 B-17 零侵入机械断言。**产出物** = PR worktree 分支 `feat/0022-pr-003-protocol-layer-and-consumption-cutover` 的 `b54f143..92a1bbb`（3 个提交）；**验证标准** = 主 agent 委托的 7 条 + PR 文件「验收标准」16 条 + 「文件范围 / 零改动」段 + 「择一判定声明」 + `prs/pr-003-tasks.md` T1~T8（69 条子判据）；**身份** = 同级代码审查者（主）/ 协议实现审查者 / 测试工程师。

**产出物清单**
- 代码：`git -C <PR worktree> diff b54f143..92a1bbb`（HEAD `92a1bbbd2ac341d255d2327cbf8e0db2d5f3d4d0`，base `b54f143`）；`--name-status` = 9 路径：`oamp/src/acp-client.js`(M) / `oamp/src/context-pool.js`(M) / `oamp/src/agent.js`(M) / `oamp/test/tool-permission.test.js`(M) / `oamp/test/confirmation-roundtrip.test.js`(M) / `oamp/test/web.test.js`(M) / `oamp/test/zero-intrusion.test.js`(A) / `oamp/README.md`(M) + `docs/.../prs/pr-003-tasks.md`(A，= PR 文件 §0.1 自述「不计入源码/测试/文档面」的阶段产物，见偏差 D-2)
- 规格：`prs/pr-003-protocol-layer-and-consumption-cutover.md`（验收标准 16 条 + 文件范围 8 项 + 零改动段 + 择一判定声明）、`prs/pr-003-tasks.md`（T1~T8 + §0.3 事实锚点 A1~A18 + §0.5 中间态红面）、`clarifications/2026-09-14-stage5-wave3-verdicts.md`（W3-MI-1~5 / 上报①~③ / 主 agent 补充约束 1~6）、`architecture.md` §3.3 / §5.1 / §5.6 / §5.7 / §9.2 / §9.4.2
- 只读对照面（本 PR 零改动，作为标准面真源与既有行为基线）：`oamp/src/{launcher,config,protocol,rpc-client,oneshot-client}.js`、`oamp/test/{acp-daemon,context-pool,project-workspace,call-protocol,protocol-layer,hygiene,omp-executor}.test.js`、`oamp/test/helpers/**`

**验证标准来源**：主 agent 委托（7 条）+ PR 文件「验收标准」（16 条）+「文件范围 / 零改动（防夹带）」段 +「择一判定声明（D-6）」+ `prs/pr-003-tasks.md` T1~T8（69 条子判据）+ `clarifications/2026-09-14-stage5-wave3-verdicts.md`（W3-MI-1~5、上报①~③ 的处置、主 agent 补充约束 1~6）。

**验证日期**：2026-09-14

**独立性声明**：本报告未接收、未使用任何执行过程上下文（不采信「已跑过全绿」「已完成」「已 stop 进程」等自述）。全部判定基于对上述产出物本身的读取、对标准面/基线的只读翻查，以及**本次独立实跑**（全量套件、定向套件、4 组一次性探针、2 组机制反例）。**未修改任何被验证产物**：PR worktree `git status --porcelain` 为空（写入前后各核一次）；未执行任何 git 写操作（仅 `git diff` / `git log` / `git status` / `git rev-parse` 只读命令）。本报告为本次验证的唯一入库写入；实跑产生的临时件全部落在 `os.tmpdir()` 下的 `mkdtemp` 目录（fake bin / argv 日志 / CLI stderr 临时文件），不触碰仓库与迭代目录。已结束本次实跑拉起的全部 router / agent / fake web 进程。

---

## 0. 本次实跑基线（判定所依据的观测面）

| # | 命令 | 结果 |
|---|---|---|
| R1 | `git -C <PR worktree> status --porcelain` | 空（工作树 clean，改动均已提交） |
| R2 | `node --test oamp/test/*.test.js`（31 个文件） | `# tests 361 / # pass 361 / # fail 0 / # cancelled 0 / # skipped 0`（68.9s） |
| R3 | `node --test test/{zero-intrusion,tool-permission,web,confirmation-roundtrip}.test.js` | `# tests 87 / # pass 87 / # fail 0`（63.8s） |
| R4 | `node --test test/{acp-daemon,call-protocol,context-pool,hygiene,omp-executor,project-workspace}.test.js` | `# tests 62 / # pass 62 / # fail 0`（24.2s） |
| R5 | 探针①（真 agent + fake bin，argv 观测） | 默认 ⇒ `["--mode","rpc","--no-skills","--no-rules","--no-tools","--no-session","--model","deepseek/deepseek-v4-flash"]`；`OAMP_PROTOCOL=acp` ⇒ `["acp","--no-skills","--no-rules","--no-tools","--no-session","--model",…]` |
| R6 | 探针②（CLI 档位，真 agent + fake bin） | `--protocol acp` ⇒ argv[0..1] = `acp,-…`；`--protocol rpc` ⇒ `["--mode","rpc",…]`；`--protocol acp` + `OAMP_PROTOCOL=rpc` ⇒ `acp`（角色级压过 env） |
| R7 | 探针③（rpc 形态 stub bin 走真 agent + 假 web 节点，一轮含三类过程帧） | `kindCounts={"started":1,"thinking":1,"chunk":1,"tool_call":1,"tool_output":1}`；`task.result state=completed text="hello" context_id="ctx-42065-1" pid=number`；非终态 `agent_end{isTerminal:false}` 未结算 |
| R8 | 探针④（rpc 门回路，`FAKE_SCRIPT=gate`） | `confirmation_request` 恰 **1** 条；字段 = `["agent_id","chat_id","confirmation_id","created_at","kind","options","title","tool"]`；`tool="bash"`（多行 title 摘取）；`options=[{"option_id":"Approve"},{"option_id":"Deny"}]`；裁决后仍 1 条（无重复入队）、该轮 `state=completed` |
| R9 | 机制正例对照（把同一组判据作用于 `protocol.js`） | import=true / newImpl=true / protoLiteral=true（判据有灵敏性，非恒真） |
| R10 | 机制反例（把同一组判据 + 快照比对作用于**内存中变异**的消费层文本） | B-17① import / ① newImpl / ② literal 三式 **DETECTED**；③ 快照比对 **DETECTED**（AssertionError） |
| R11 | CLI 退出码 | `--protocol bad` → 2（`--protocol 取值非法: "bad"`）；`--protocol`（缺值）→ 2；`--bogus 1` → 2；`--tools maybe` → 2（既有体例不变）；`--protocol acp`（无 router）→ 1（取值被接受，进入连接阶段） |

---

## A. 主 agent 委托的 7 条标准 —— 逐条判定

### A-1 PR 文件「验收标准」全部满足 —— **pass**
证据：下 §B 16 条逐条判定，全部 pass（每条的实跑/读码证据见该行）；无 fail / partial / blocked。

### A-2 改动面恰为 8 个文件；`project-workspace.test.js` 与其余禁改面零改动 —— **pass**
证据 1（正命中面）：`git diff --name-status b54f143..92a1bbb` = §0 的 9 路径，其中源码/测试/文档 = **8 项**（PR 文件「文件范围」逐条对应），第 9 项 `prs/pr-003-tasks.md` 是阶段产物（PR 文件 §0.1 自述不计入）。
证据 2（禁改面逐条 = 空）：`git diff --name-only b54f143..92a1bbb -- oamp/test/project-workspace.test.js oamp/src/{launcher,config,protocol,rpc-client,oneshot-client,web}.js oamp/web oamp/API.md oamp/llms.txt omp oamp/bin oamp/package.json oamp/test/helpers oamp/test/{acp-daemon,context-pool,call-protocol,protocol-layer,hygiene}.test.js` → **无输出**（零改动）。
证据 3（既有测试面改动封闭）：全部测试面改动 = 本 PR 的 3 个既有文件 + 1 个新文件；pr-002 固定的 4 文件（`acp-daemon` / `context-pool` / `project-workspace` / `call-protocol`）与 pr-005 的 `protocol-layer.test.js` 全部零 diff ⇒ 委托要求的「`project-workspace.test.js` 必须零改动」成立。

### A-3 「指定即生效」（D-1）—— **pass**
证据（R5，端到端运行时子进程 argv，非 L2 单测）：默认档（不做任何协议指定）⇒ 被启动子进程 argv 首两段 = `["--mode","rpc"]`；`OAMP_PROTOCOL=acp` ⇒ argv 首段 = `"acp"`；两次运行生产消费层源码逐字节相同 ⇒ **不回落**（argv 可观测面直接证否回落）。
证据（R6，角色级档位）：`--protocol acp` / `--protocol rpc` 逐档生效；`--protocol acp` 同时 `OAMP_PROTOCOL=rpc` 时仍起 `acp`（角色级 > env，与 §5.3 解析链一致）。
证据（文件面）：`oamp/src/acp-client.js:165-169` 的 argv 经 `buildArgv('omp:acp', …)` 产出，文件内 `const args = ['acp'` 手工拼装已消失；`oamp/src/agent.js:681-690` 的 `makeLayer` 把 `protocol`（角色级）/`configProtocol`（env 折叠值）交给唯一注入点。
证据（测试面）：`oamp/test/web.test.js` 用例「执行路径判定」在 `protocol: null`（不注入任何协议键）下断言 `daemonArgv.slice(0,2)` deepEqual `PROFILES['omp:rpc'].modeArgs` 且 `!daemonArgv.includes('acp')`（R3 绿）。

### A-4 B-17 存在且三条断言实际生效（附反例证据）—— **pass**
证据 1（在场且非空）：`oamp/test/zero-intrusion.test.js`（72 行，新增），三条 `test(...)` 分别承载 §3.3 判据 ①②③：① 三文件对 `from …/{acp,rpc,oneshot}-client.js` 与 `new (AcpClient|RpcClient|Oneshot*)` 两条 `assert.doesNotMatch`；② 三文件对 `['"](rpc|acp|oneshot)['"]` 一条 `assert.doesNotMatch`；③ 三文件「起始读取基线快照 → 以默认（rpc）与 `OAMP_PROTOCOL=acp` 各起一次**真实 agent**（REGISTERED → SIGINT → 退出 0）→ 再读并逐字节比对」。R3 实跑三条全 `ok`（`ok 85/86/87`），全量 R2 亦 `ok 359/360/361`。
证据 2（灵敏性 / 反例，R9+R10）：把同一组正则作用于 `src/protocol.js` ⇒ `import=newImpl=protoLiteral=true`（判据不是恒真）；把同一组判据与快照比对作用于**内存中注入**了 `"acp"` 字面 + `import { AcpClient }` + `new AcpClient({})` 的消费层文本 ⇒ 三式与③快照比对**全部 DETECTED**（AssertionError）⇒ 三条断言在变异下确实报红，非空断言、非空跑。
证据 3（定位不越界）：文件头自述「只承载三条机械判据，不承载行为断言」，实际断言集与之相符；只 import `node:*` + `./helpers/harness.js`（既有 helper，零新增/零修改，见 A-2 证据 2 的 `oamp/test/helpers` 零 diff）。

### A-5 acp 行为零变更；`optionId` 为钩子入参键名 —— **pass**
证据 1（行为面逐块核对，`git diff -U2 b54f143..92a1bbb -- oamp/src/acp-client.js`）：改动块 = 文件头注释 / import / 能力位三常量 + 两 getter / 删本地 `AcpError` 类 / `start()` 的 argv 改经 `buildArgv` / `onChunk`→`onDelta` / 全部 `AcpError`→`ProtocolError` / `dispose()`→`close()` / 门名原语改引用 `protocol.js` 同源实现。**未出现任何 hunk 落在两道门（base `:434-575`）、`_chunkHandler` 分支条件、冻结计时（`_pausedTurns`）/ `_waitQuiescence` / 六个常量**：`@@ -364,5 +389,5 @@` 与 `@@ -672,5 +697,5 @@` 之间的 base 区间（`:365-671`）无任何改动块，而门逻辑位于该区间内。
证据 2（流式增量语义）：`_chunkHandler` 仍只接受 `update.sessionUpdate === 'agent_message_chunk'`（`oamp/src/acp-client.js:226-233`），文本增量经 `onDelta({kind:'chunk', text})` 逐块原样产出；`agent_thought_chunk` **不被转发**——由新增用例「pr-003/B-16」实测（fake 同时下发一帧 `agent_thought_chunk` + 两帧 `agent_message_chunk`，断言 deltas **恰为** 两条 chunk）⇒ 印证能力位 `thinking:'no'`。
证据 3（错误码五值）：`grep -oE "ProtocolError\('[a-z_]+'"` 实测 —— `acp-client.js`：`context_crashed`×9 / `permission_denied`×2 / `timeout`×1（+ `model_unavailable` 经 `_request` 的 `errorCode` 位，`:275`）；`context-pool.js`：`context_crashed`×6 / `context_busy`×1 ⇒ 域仍恰五值，逐字不变（无新增/改名码）。
证据 4（`optionId` 键名）：`grep -n "option_id" oamp/src/{acp-client,rpc-client,protocol,oneshot-client}.js` → **零命中**；钩子返回值判定与门选项比对均用 `optionId`（`acp-client.js:520-524` / `:595` / `:601`，`rpc-client.js:319-330`）。`option_id` 仅出现在**信封 wire 面**（`agent.js:258-265` 的 `readConfirmationOptions` 把 ACP 选项映射为 `{option_id,label?}`；`agent.js:302` 读回信封 2 的 `option_id`；`web.js:1279-1311` 的 HTTP body 字段）—— 这是 §5.3 信封 1/2 的既定契约（W3 补充约束 2 所指「钩子入参选项键名 = `optionId`」在钩子面成立）。

### A-6 全量 `node --test test/*.test.js` 全绿；既有测试文件除本 PR 4 个外零改动 —— **pass**
证据：R2 = **361 / 361 pass / 0 fail**（31 个文件 = 29 既有 + pr-005 新增 `protocol-layer.test.js` + 本 PR 新增 `zero-intrusion.test.js`，与 PR 验收标准第 14 条的计数口径逐条吻合）；`hygiene.test.js`（零依赖 + 凭据词）在该次运行中绿（F12 承载）；测试面文件改动清单见 A-2 证据 3（仅 3 既有 + 1 新增）。

### A-7 无第二套档位语义 —— **pass**
证据 1（消费层零档位字面）：`grep -nE "approval-mode|always-ask|yolo" oamp/src/{agent.js,context-pool.js,web.js}` → **零命中**。
证据 2（档位合成单点）：一次性档位在 L2 合成 —— `oamp/src/oneshot-client.js:94-96` `toolsOn ? {mode: spec.permission === 'deny' ? 'always-ask' : 'yolo', appliesWhen:'tools-on'} : null`，经 L1 的 `approval` 入参落 argv（flag 字面量仍单点在 `launcher.js`）。
证据 3（消费层只传档位、不合成）：`agent.js:717-718` 只把 `permission` / `tools` 交给 `makeLayer(...).createEphemeral()`；`context-pool.js:190-200` 只把 `permission` 用于「仅 allow 档注入钩子」的门判（既有语义），不产出任何 argv。

---

## B. PR 文件「验收标准」16 条 —— 逐条判定

| # | 验收摘要 | 判定 | 可定位证据 |
|---|---|---|---|
| 1 | D-2 机械可核（消费层零 import / 零协议取值） | **pass**（口径按 W3 上报① 修正） | `grep -rn "acp-client\|rpc-client\|oneshot-client" oamp/src` 命中 3 文件：`acp-client.js:1`（文件头自指注释）、`protocol.js:6,9,10,11`（**唯一注入点的 import，判据适用范围之外**）、`rpc-client.js`（4 处注释）；**生产消费层三文件零命中**（B-17① 固化 + R9/R10 灵敏性）；`grep -nE "'rpc'\|'acp'\|'oneshot'" oamp/src/{agent.js,context-pool.js,web.js}` → 零命中。字面口径不可达的原因见偏差 D-1 |
| 2 | D-3 机械可核（切协议只改注入配置 ⇒ 消费层 diff 为空） | **pass** | B-17③ 运行时快照比对：起过「默认 rpc」与「`OAMP_PROTOCOL=acp`」两种注入配置后，三文件逐字节等于起始快照（R3 `ok 87`）；本轮 4 组探针（R5/R6/R7/R8）也均在**未改动任何消费层文件**的前提下切换档位（`git status --porcelain` 前后皆空） |
| 3 | D-1 指定即生效 + 不回落 | **pass** | R5 / R6（argv 首段可观测面）；`acp-client.js:165-169`、`agent.js:681-690` |
| 4 | 默认走 rpc + 选择域封闭 + 可切回（N3 / N9） | **pass** | 默认 rpc = R5 + `web.test.js` 用例断言；选择域恰 `{rpc,acp}` = `protocol.js` 折叠 + `agent.js:555-561` 的 `isSelectableProtocol` 经唯一注入点校验 + pr-005 `protocol-layer.test.js`「选择域封闭」用例（R2 绿）；**无切换入口**：`grep -rn "protocol" oamp/src/web.js oamp/web/app.js` 仅命中信封字段 `protocol:'oamp/1'`（无协议档位参数）；切到 acp 后该轮确走 acp = R5/R6；档位只在进程启动解析（`agent.js:679-690`）⇒ 切换 = 重起实例 ⇒ 既有会话不再被续接，「上下文已重置」提示路径（`context-pool.js:12-13,241` 的 `RESET_TEXT` / `_notice`）**零 diff** |
| 5 | acp 标准面外观（能力位 + `onDelta` + `ProtocolError`） | **pass** | 新增用例「pr-003/B-16」（`tool-permission.test.js`）六键键集 deepEqual `CAPABILITY_KEYS`、取值 ∈ 三态、非 yes 键 note 非空、`capabilities` 逐字 = `{streaming:yes, thinking:no, approvalGate:yes, hostTools:no, introspection:yes, queueControl:no}`、`onDelta` 增量逐块到达、`ProtocolError` 实例 + `name` + code ∈ 五值（R3 绿） |
| 6 | acp 不退化、不补能力（F06 1/2/3，**本 PR 判主面**） | **pass** | 经 agent 的行为回归 = R4（`acp-daemon` / `context-pool` / `project-workspace` / `call-protocol` + `omp-executor` 共 62/62，均为 acp 注入下的既有语义）+ R2 全量；7 字段信封 / 三事件通知 / 消息与状态流转 = `confirmation-roundtrip` 的跨进程用例（R3 绿，用例断言集未改动，见 D-3）；「不补能力」= R5 证据 2（thought 帧不转发） |
| 7 | 门承接（消费层侧，F05 1~4） | **pass** | **acp 侧**：`confirmation-roundtrip.test.js` 的 T1①~③ 与跨进程用例（7 字段信封、`options` 恒二元、`tool` 由既有原语从多行 title 提取、裁决后条目消失、挂起期间轮次不超时）；**rpc 侧**：R8 探针实测 —— 恰 1 条信封、字段集合 = 7 字段 + `kind`、`tool="bash"`、`options=[Approve,Deny]`、裁决后该轮 `completed`、无重复入队 ⇒ 两实现**各跑一次**的要求满足 |
| 8 | 非门反向请求处置（经消费层的观察面） | **pass** | L2 面 = pr-005 `protocol-layer.test.js`「非门交互类回 `{cancelled:true}`；展示类不回执」（R2 绿）；消费层可见面 = R7 —— 该轮同时含 `notice{text:'unknown-frame'}` 与 `available_commands_update` 等非门帧，轮次仍正常终态、task.update 类型集合无多无少 |
| 9 | 一次性路径迁移后既有可见面零回归（F07 / 主 agent 裁定 1） | **pass** | 行流形状 / 200 行截断 / 超时三拍 = `oneshot-client`（pr-005 自证 R2 绿）+ `omp-executor` / `web` / `project-workspace`（R3/R4 绿），其中 `web.test.js` 的 `messages.length === 4`（过程不入库）与 SSE 序列用例逐字保留；`oamp/src/web.js:1628` 的 `stdout` 累积分支零 diff（A-2 证据 2）且被上述一次性用例触发；一次性 argv = R3 中 `web.test.js` 的 `-p` / 末位「项目块 + \n\n + 原文」断言 + `confirmation-roundtrip` 的 `--approval-mode` 取 `PROFILES['omp:oneshot'].approval.mode`；`runShellTask` 与 `!` 路径 = agent.js 的 base `:401-583` 区间**无改动块**（diff hunk 间隔证明）；两轮不续接 = pr-005「连续两轮 = 两次独立子进程」（R2） |
| 10 | 三类增量在管道面被承接（F08 1/2 消费侧，本 PR 判辅面） | **pass** | R7：rpc 一轮内 `thinking` / `tool_call` / `tool_output` 逐类可数（各 1）；受理回包不判轮次结束 —— 该轮 `response{command:'prompt',success:true}` 先于增量到达，而 `task.result` 只在终态 `agent_end{isTerminal:true}` 后产生；非终态 `agent_end{isTerminal:false}` 未结算（同轮只产生 1 条 `task.result`）；`agent.js:356` 的 `onDelta → sendUpdate('working',{kind,text})` 为纯原样上送（不聚合、不落库） |
| 11 | 过程增量不入库、记录条数不变 | **pass** | `web.test.js` 两个用例显式断言 `detail.messages.length === 4`（注释「过程增量不入库（该轮仍恰 2 行）」）——含 SSE 过程增量用例（R3 绿）；消费层侧 R7 全轮仅产出 `task.update` 帧，无 persist 调用面（增量路径无 `persist` 引用） |
| 12 | CLI `--protocol` | **pass** | R11（`bad`→2、缺值→2、未知参数→2，文案体例同既有）+ R6（生效 + 角色级优先）+ 不指定 ⇒ `null` 交解析链（`agent.js:567,594-601`） |
| 13 | D-2 / D-3 可执行证据 + 本 PR 4 文件全绿 | **pass** | R3 = 87/87（`zero-intrusion` 3 + `tool-permission` / `web` / `confirmation-roundtrip` 全部 ok） |
| 14 | 全库测试面全绿（31 文件） | **pass** | R2 = 361/361/0 fail；文件数 31（`ls test/*.test.js | wc -l`） |
| 15 | 服务边界与上游零改动（F10 / F11） | **pass** | A-2 证据 2（`oamp/src/web.js` 整体零 diff ⇒ 监听/鉴权面零改动；`oamp/API.md` / `oamp/llms.txt` / `omp/**` 零 diff）；本迭代消费的 rpc 能力均有 pr-005 的 M-1~M-5 实测回指，无「需 omp 新增字段」前置项 |
| 16 | README 与实现一致（B-10） | **pass** | `oamp/README.md:89`（`OAMP_PROTOCOL` 行）、`:4`（四键表述）、`:302`（`--protocol rpc\|acp` 示例）、`:141/:199/:215/:306`（常驻路径表述改为「默认 `omp --mode rpc`，可切回 `acp`」）；`grep -n "三键" oamp/README.md` → **零命中**；示例 JSON 含 `"protocol": "rpc"`（`:314`） |

---

## C. `prs/pr-003-tasks.md` T1~T8 逐条判定（69 条子判据）

| 任务 | 子判据数 | 判定 | 关键证据 |
|---|---|---|---|
| T1（`acp-client.js` 对齐 + `tool-permission` 同批） | 12 | **pass** | argv 真源迁移（`grep "const args = \['acp'"` 零命中；R5/R3 全序 deepEqual `buildArgv('omp:acp',…)`，次序 = modeArgs→skills→rules→tools→session→model→roleFile→approval，与 base 手工拼装逐位一致）；能力位（A-5/B-5）；`onDelta`（`grep -n onChunk oamp/src` 零命中）；`AcpError` 全清（`grep -rn AcpError oamp/src oamp/test` → 仅 `protocol.js:13` 注释里的历史沿革表述）+ 五码域；门名原语同源（`protocol.js:26` 前缀 + `readApprovalToolName` 具名导出，`acp-client.js` 自持实现与 `APPROVAL_MESSAGE_PREFIX` 已删）；`close()` 在场（`acp-client.js:317-319`，语义 = 既有 `kill()`、幂等、不触发 `onExit`）；构造 10 键逐字（`bin/model/cwd/tools/roleFile/permission/auditContext/logger/onExit/onPermissionRequest`）；T1 改动面 = 该文件 + `tool-permission.test.js`（A-2） |
| T2（`context-pool.js` 消费注入的会话工厂） | 11 | **pass** | `grep -n "acp-client" oamp/src/context-pool.js` 零命中；构造面 = `createResident`（`context-pool.js:27-29`），旧键 `bin/cwd/tools/roleFile` 全清；钩子装配 = `onPermissionRequest` + `onApproval` 同体（`:190-212`）、仅 `permission==='allow'` 注入、附加 `{chatId,agentId,origin}`、`onExit` 实体、`contextId` 惰性 getter（`:201-212`）；审计面 `ctx-<pid>-<generation>`（`:117-119`）+ `pid`/`model`（`:121-128`）逐字未动；`onDelta` 透传（`:170`）、`grep onChunk` 零命中；释放路径 `this.client?.close()`（`:151`）；严格不动清单（`QUEUE_LIMIT=8` `:10`、同键 FIFO `:155-180`、LRU `:83-91`、`_failSession` 三码豁免 `:233`、`sentTurns` `:113,166-172`）逐条核对未动；R7/R8 经该池的真进程运行通过 |
| T3（`agent.js` 接线 + 一次性走 L2 + `--protocol`） | 10 | **pass** | 注入点接线（`:681-700`，resident 六键与 §5.2 逐条对应、`model` 建键轮解析 `:679,720-722`）；CLI（A-3/R6/R11）；`runOmpTask` 瘦身（`grep "'-p'\|\"-p\""` 零命中；`createEphemeral → prompt → 上报` `:199-243`；上报面形状 = `{kind:'stdout'\|'stderr', line}` + `{event:'truncated', note}` 原样透传）；一次性 argv = profile（R3 的 `web.test.js` / `confirmation-roundtrip` 断言）；`runDaemonTask` 增量按 kind 原样上送（`:356`）；严格不动（`runShellTask` / `heartbeatPlan` / 重连面所在 base 区间无 diff 块；`raiseConfirmation` 体仅 JSDoc 注释 1 行变更 → 偏差 D-3）；静态面（`grep -nE "'rpc'\|'acp'\|'oneshot'"` 零命中）；行为回归 = R4；管道面一次性证据 = R7（与 T3 验收 9 逐条对齐：三类逐类可数 / 受理不结算 / 非终态不结算 / 非门帧零新增分支） |
| T4（`confirmation-roundtrip.test.js` 构造改造 + acp 注入） | 6 | **pass** | 三处池构造改为 `createProtocolLayer(...).createResident` 注入（`:322,:358,:390` 一带；旧键 `bin/cwd` 全清）；跨进程 env 载体显式 `OAMP_PROTOCOL:'acp'`（`:265` 一带）；一次性档位取 `PROFILES['omp:oneshot'].approval.mode`；其余断言逐块核对未动；本文件绿（R3）；改动面封闭（A-2） |
| T5（`web.test.js` 默认路径反转 + acp 注入） | 6 | **pass** | 默认路径断言**语义反转**为 `--mode rpc`（`protocol: null` 注入面 + `PROFILES['omp:rpc'].modeArgs` 期望 + `!includes('acp')`，未以「注入 acp 后断言 acp」替代）；ACP-only 桩用例显式注入 `OAMP_PROTOCOL:'acp'`（model 审计用例 + SSE 用例）；SSE 用例 `kind` 判据面注释明确；`-p` / `!` 路径判据保留；本文件绿（R3）；改动面封闭 |
| T6（`zero-intrusion.test.js` 新建） | 8 | **pass** | A-4 全部证据（三条机制 + 灵敏性 + 反例 + 定位不越界 + 零第三方依赖 + hygiene 绿 + 改动面） |
| T7（`README.md`，B-10） | 7 | **pass** | B-16 行证据（env 表 / 四键 / `--protocol` 示例 / 常驻表述 / `grep 三键` 零命中 / `API.md`、`llms.txt` 零改动 / 改动面封闭） |
| T8（收口核验） | 9 | **pass** | 验收 1 = R2；验收 2 = R3；验收 3 = A-1 证据 1+2；验收 4 = B-2 行；验收 5 = R5/R6；验收 6 = A-2；验收 7 = A-2 证据 2 + 参考资料回指；验收 8 = B-4 行的 grep（口径性假阳性见偏差 D-4）；验收 9 = `git status --porcelain` 空 + 3 个提交均为常规提交（无 `--no-verify` 迹象，提交体含完整「为什么」） |

---

## 汇总

- 主 agent 委托标准：**pass 7 / fail 0 / partial 0 / blocked 0**
- PR 文件「验收标准」：**pass 16 / fail 0**（含「择一判定声明」遵守：本 PR 判 F02 验收 1/2 的辅面、F06 验收 1 的主面、F08 验收 1/2 的辅面，判定面与声明一致）
- `pr-003-tasks.md`：**pass 69 / fail 0**（T1 12 / T2 11 / T3 10 / T4 6 / T5 6 / T6 8 / T7 7 / T8 9）
- 实跑：全量 **361/361**（31 文件，0 fail）；本 PR 4 文件 **87/87**；相关既有 6 文件 **62/62**
- 偏差记录：**7 条**（均为口径 / 文字级，无一条构成行为缺陷或边界越界）
- 观察项：1 条（不构成判定依据）
- 结论：**PASS**

---

## 偏差记录

| # | 规格描述 | 实现实际 | 建议处理 |
|---|---|---|---|
| D-1 | PR 验收 1 字面：「`grep -rn "acp-client\|rpc-client\|oneshot-client" oamp/src` 的**命中集合 = {`oamp/src/protocol.js`}**」 | 实测命中 3 文件：`acp-client.js:1`（文件头自指注释）、`rpc-client.js`（4 处注释，pr-005 零改动面）、`protocol.js`（唯一注入点的 import，属**应当**命中）。字面口径不可达（自指 / 注释命中）——已由 `clarifications/2026-09-14-stage5-wave3-verdicts.md` 上报① 裁决为「生产消费层三文件零 import / 零构造」，实现按修正口径落地并由 B-17① 固化 | 判 **pass**（口径修正已裁决且被固化为可执行断言）。建议下一迭代直接把 PR 文件/architecture 的该条判据写成「生产消费层三文件零命中」，避免每次验收都要重述自指命中 |
| D-2 | PR 文件「文件范围」= **8 项** | `git diff --name-status` 为 9 路径（多出 `prs/pr-003-tasks.md`）。该文件是 PR 文件 §0.1 自述「不计入源码 / 测试 / 文档面」的阶段产物，且与 pr-001 / pr-002 / pr-005 的体例一致（pr-004 的任务图未入库属彼时偏差） | 判 **pass**。建议沿用 pr-002 验收报告的口径：改动面 = 8 项文件范围 + 1 项阶段产物，收口时统一登记 |
| D-3 | PR 文件「严格不动」清单含 `raiseConfirmation`（`:319`）/ `readConfirmationOptions`（`:303`）、T3 验收 6 同述 | 两函数**函数体逐字未动**，但 `raiseConfirmation` 的 JSDoc 内 1 行文字由「AcpClient 在钩子返回 Promise 期间冻结轮次计时」改为「L2 的会话实现在钩子返回 Promise 期间冻结轮次计时」；另 `CONFIRMATION_TITLE_MAX` 的同行注释同款文字变更 | 判 **pass**（注释级、零行为影响，且属于「AcpClient 不再被消费层直引」的必要表述同步）。建议下一迭代把「严格不动」清单显式标注为**行为面**判据，或在清单内注明「注释/spec 表述可同步」 |
| D-4 | T8 验收 8 / N3 字面：「`oamp/web/**` 与 `oamp/src/web.js` 零协议取值命中」 | `oamp/web/index.html:93` 含 `class="oneshot"`（一次性勾选项的 **CSS 类名**，非协议取值）；该文件本 PR 零改动（pr-004 已合并面）；`oamp/src/web.js` 与 `oamp/web/app.js` 对 `'rpc'\|'acp'\|'oneshot'` 零命中（`web.js` 仅信封字段 `protocol:'oamp/1'`） | 判 **pass**（假阳性）。建议下一迭代把该判据限定为「引号包裹的协议取值字面」，或显式排除 HTML class / CSS 选择器 |
| D-5 | 标准面回收动作更名 `close()`（W3 补充约束 4 授权）后，文件内术语应统一 | `oamp/src/acp-client.js:135 / :293 / :294 / :705` 仍保留私有字段 `disposed` 与注释「主动 kill/dispose」；字段语义与 `close() → kill() → disposed` 一致（无行为差），仅术语未同步 | 判 **pass**（文字级）。建议下一迭代把注释改为 `kill/close`，保持术语单点一致 |
| D-6 | D-3 要求「三档任一」切档后消费层 diff 为空 | B-17③ 的切档注入面用的是 env 档（`OAMP_PROTOCOL=acp`）；config.json 档未在该测试内命中，角色级档由本报告 R6 探针覆盖，三档解析链由 pr-005 用例覆盖 | 判 **pass**（三档生效面均有证据，仅分布在不同观测面）。建议下一迭代若要求同一测试内三档齐测，再扩展 B-17③ 的 configs 数组（不改文件范围） |
| D-7 | 委托标准 3 表述「默认档起子进程 ⇒ **argv 首段**为 `--mode rpc`」 | 实测 argv[0]=`--mode`、argv[1]=`rpc`（首**两**段构成 mode 段；PR 文件原文口径为「argv 含 `['--mode','rpc']`」，与实测一致） | 判 **pass**。建议后续派单文案统一为「argv 前两段 = `['--mode','rpc']`」 |

**观察项（不构成偏差，不作为判定依据）**
- O-1：本报告 R5/R6 探针在「任务在飞 + fake bin 永不完成握手」场景下对 agent 发 SIGINT，3s 内未退出而由探针降级 SIGKILL（`exit={code:null,signal:'SIGKILL'}`）。该场景无基线对照（探针构造物），且既有测试面中 agent 的优雅退出（SIGINT → 0）在 R2/R3 全程保持绿，故不作为本 PR 的判定依据，仅登记备查。

---

## 下一迭代候选

1. **口径回填**：把 PR 文件 / `architecture.md` §9.4.2 B-17 的 D-2 判据从「全 `oamp/src` 命中集合」改写为「**生产消费层三文件零 import / 零构造**」，并把 N3 判据补上「排除 HTML class 假阳性」限定（承接 D-1 / D-4；PR 文件已登记「§9.4.1 清单回填归主 agent」）。
2. **入库 F04 消费侧管道面证据**：本 PR 以一次性探针（R7）+ 阶段 6 承接「三类增量逐类可数 / 受理不结算 / 非终态不结算」。建议下一迭代新增 rpc 形态 stub 的入库用例（本迭代文件范围不允许追加）。
3. **术语与判据精度**：「严格不动」清单标注为行为面（D-3）；`acp-client.js` 的 `disposed` 注释统一为 `kill/close`（D-5）。
4. **阶段 6 真实进程面**：本迭代（含本 PR）的协议行为证据全部来自 fake bin；建议阶段 6 以真实 `omp` 跑一轮 rpc 常驻（含思考 / 工具增量）与一轮 acp 常驻，闭合「fake 与真实实现的等价性」风险。
5. **测试确定性**：本次收口的提交 `92a1bbb` 已修掉一处跨进程帧读取竞态（`tool-permission` 的 `session/cancel` 帧）。建议下一迭代把「读 fake 子进程日志前先等帧落地」的体例抽成 helper 约定，避免同类竞态在新用例中复现。
6. **`architecture.md` §9.4.1 测试面计数**：PR 文件「测试面口径登记」已实测为 7 个 ACP-only 桩文件（含 `call-protocol`）而文档写 6 个；回填归主 agent，本报告仅复核该登记与实测一致。

---

## 结论

**PASS**

主 agent 委托的 7 条标准全部 pass（fail 0 / partial 0 / blocked 0）；PR 文件「验收标准」16 条全部 pass；`pr-003-tasks.md` 69 条子判据全部 pass；全量套件 361/361、本 PR 4 文件 87/87、相关既有 6 文件 62/62 全绿；改动面封闭（8 项文件范围 + 1 项阶段产物，`project-workspace.test.js` 与全部禁改面零 diff）；D-1（默认 rpc / 指定 acp / 角色级优先、均不回落）、D-2（消费层零协议知识与零协议字面）、D-3（切档不动消费层源码）三条机械判据均有**本次独立实跑**证据，B-17 三条断言经正例对照与内存变异反例证明非空、非恒真；acp 行为零变更（两道门 / `_chunkHandler` / 冻结计时 / 常量所在区间无 diff 块，错误码仍恰五值，钩子面键名为 `optionId`）。7 条偏差记录均为口径或文字级，无一条阻塞验收关闭。

（本报告为本次验证的唯一入库写入；未修改任何被验证产物；未执行任何 git 写操作。）
