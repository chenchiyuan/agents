# verify-pr-002-20260914 —— pr-002「既有 harness 测试面按 L1 profile 显式化（协议注入固定为 `acp`）」次波独立验收

**验证者身份**：反射身份 —— 产出物 = ①**测试面代码改动**（4 个既有 harness 测试文件的 diff）②**规格与任务图**（`prs/pr-002-test-face-profile-pinning.md` / `prs/pr-002-tasks.md`）③**用户裁决**（W2-A）。
- 主身份 = **测试工程师 / 同级代码审查者**（测试面改动的验收者：判定注入完整性、期望值真源、断言强度、实跑绿度）；
- 次身份 = **计划审查者**（任务图 T1~T5 与 PR 验收标准的对位、口径落地）。
- 复述任务：验证 **pr-002 的测试面改动（commit `3dc14f7`）** 是否满足 **任务派发的 7 条标准（PR 验收 1~6 + 改动面 + 零改动 + 真源 + W2-A + 注入完整性 + 合跑全绿）**，身份 = **测试工程师（主）/ 计划审查者（次）**。

**产出物**
- 代码：`git -C /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0022-agent-launcher-and-protocol-layer/.pb-agents/worktrees/0022-pr-002-test-face-profile-pinning show 3dc14f7`
  - `HEAD = 3dc14f78ed31fe75deb14150db9222fd97b018d6`，父 = `192a255cac293c28d33b4c5627037d6ec2128997`，分支 = `feat/0022-pr-002-test-face-profile-pinning`，工作区 `git status --porcelain` 为空。
  - 改动面 = `oamp/test/{acp-daemon,context-pool,project-workspace,call-protocol}.test.js` + `docs/.../prs/pr-002-tasks.md`。
- 规格：`docs/iterations/0022-agent-launcher-and-protocol-layer/prs/pr-002-test-face-profile-pinning.md`（文件范围 + 验收标准 1~7 + D-6 择一判定声明）、`prs/pr-002-tasks.md`（T1~T5 + §0.3 事实锚点 A1~A14 + §5 疑问①/MI-1~MI-4）、`clarifications/2026-09-14-stage5-wave2-verdicts.md`（W2-A / 附带登记 1~3）。
- 只读对照面（未改）：`oamp/src/launcher.js`（PROFILES / buildArgv 真源）、`oamp/src/config.js`（`OAMP_PROTOCOL` 键与选择域）、`oamp/src/{agent,context-pool,web}.js`（惰性判据面）。

**验证标准来源**：`pr-002-test-face-profile-pinning.md`「验收标准」1~6 + 「文件范围/零改动（防夹带）」段 + `pr-002-tasks.md` T1~T5 硬契约（§0.4）+ `clarifications/2026-09-14-stage5-wave2-verdicts.md` W2-A 与附带登记 1（`buildArgv` 允许引用）。
**验证日期**：2026-09-14

**独立性声明**：本报告未接收任何执行过程上下文（不采信「已跑过全绿」「已完成」等自述）；全部判定基于对 `3dc14f7` 产出物本身的读取与本机定向实跑。**未修改任何被验证产物**（本报告为本验证者唯一写入；PR worktree 内未落任何文件，`git status --porcelain` 空）。

**范围口径（两处，先声明后判定）**
1. 派发标准写「PR 验收 1~6」——按 `pr-002-tasks.md` §3 对位表的编号（1 注入 / 2 profile 真源 / 3 行流零改动 / 4 生产零改动 / 5 惰性 / 6 合跑全绿）；PR 的第 7 条（**全库既有面** `node --test oamp/test/*.test.js`）按派发约束「全库由主 agent 收口时统一跑」**不在本次判定范围**，见「下一迭代候选」e。
2. `docs/.../prs/pr-002-tasks.md` 属任务图产物，按 PR 文件与 `pr-002-tasks.md` §0.1 末行**已声明口径不计入改动面**（见偏差 D-1）。

---

## 逐项判定

### 判定 1 · PR 验收 1（4 个文件的所有 agent 启动点均显式传入 `OAMP_PROTOCOL: 'acp'`）—— **pass**

证据（逐启动点核，检索式 = 每个 `startAgent(` / `startFlaggedAgent(` 的 env 载体）：

| 文件 | 启动点 | env 载体（行号 = 改后文件） | 注入 |
|---|---|---|---|
| `acp-daemon.test.js` | `setup()` → `startAgent('dev-1')` | `:253` | ✓ |
| | `startFlaggedAgent('pb-dev')` | `roleEnv`（`:508`）经 `...roleEnv`（`:513`） | ✓ |
| | `startFlaggedAgent('pb-planner')` | `roleEnv` 经 `:519` | ✓ |
| | `startFlaggedAgent('pb-dev-deny')` | `roleEnv` 经 `:533` | ✓ |
| | 匿名 `startAgent('dev-1')` | `:522` | ✓ |
| | perm 审计 `startFlaggedAgent('pb-dev')` | `:705` | ✓ |
| `context-pool.test.js` | `setup()` → `startAgent(instanceId)` | `:210` | ✓ |
| | `startAgent('dev-2')` | `:274` | ✓ |
| `project-workspace.test.js` | `setup()` → `startAgent(agentId)` | `:232` | ✓ |
| `call-protocol.test.js` | `setup()` → `startAgent(agentId)` | `:301` | ✓ |

- 检索实证：`grep -n "OAMP_PROTOCOL" <4 文件>` = 8 命中，全部落点是 agent 启动 env（`acp-daemon` 4 / `context-pool` 2 / `project-workspace` 1 / `call-protocol` 1），与 `pr-002-tasks.md` A1（10 处启动点 / 8 个 env 载体）逐点吻合（详见判定 11）。
- 全量锚定 `startAgent(` / `startFlaggedAgent(` / 内联 `spawn(..., [BIN,'agent','start',...])`：4 文件内不存在第 11 处 agent 启动点（唯一内联 spawner 是 `acp-daemon:473-476` 的 `startFlaggedAgent` 定义，其 `envExtra` 已在上表 4 个载体中覆盖）。
- 注入值合法性：`oamp/src/config.js:49-58` 的 `readProtocol` 选择域 = `{rpc, acp}`，越界值响亮失败 ⇒ 字面 `'acp'` 合法；4 个文件实跑全绿（判定 12）反证该键被真实解析且无配置错误。

### 判定 2 · PR 验收 2（argv 期望值按 `PROFILES` / profile 期望值断言；四条既有硬约束逐字保持）—— **pass**

- **真源引用在场**：3 个含 argv 断言的文件均 `import { PROFILES, buildArgv } from '../src/launcher.js'`（`acp-daemon:18`、`context-pool:18`、`project-workspace:20`）；`call-protocol` 零 argv 断言（判定 12 已核），无需引用。
- **模式记号取 profile**：`acp-daemon:23-24` / `context-pool:23-24` / `project-workspace:25-26` 的 `ACP_MODE = PROFILES['omp:acp'].modeArgs[0]`、`ONESHOT_MODE = PROFILES['omp:oneshot'].modeArgs[0]`；全部筛选/等待/查找点位（`acp-daemon:547/549/551/553/563/578/585/594`、`context-pool:522/539`、`project-workspace:1010`）均改用该两常量。
- **全量期望值经 `buildArgv` 产出**（W2-A 附带登记 1 允许）：`acp-daemon:568-572` 匿名常驻 argv 的 `assert.deepEqual` 期望值 = `buildArgv('omp:acp', { model: 'deepseek/deepseek-v4-flash' })`；`context-pool:540` 的四个固定 flag = `buildArgv('omp:acp').filter((f) => f.startsWith('--no-'))`；`project-workspace:1013` 的末位 = `buildArgv('omp:oneshot', { prompt }).at(-1)`。三者经读 `oamp/src/launcher.js:14-138` 逐段核对，派生值与改动前的字面期望逐位相等（`['acp','--no-skills','--no-rules','--no-tools','--no-session','--model','deepseek/deepseek-v4-flash']` / 四 flag 集 / positional 末位）。
- **零本地复写期望数组**：`grep -n "no-skills|no-rules|no-session|deepEqual|buildArgv(|modeArgs|PROFILES\[|launcher\.js"` 于 4 文件 → 无 `['acp','--no-skills',...]` 形态字面期望数组残留（改动前 `acp-daemon:562` 的那一条已被 `buildArgv` 取代）；残留的 `'--no-tools'` / `'--append-system-prompt'` / `'--approval-mode'` 均为 **flag 名查找键**（`indexOf` / `includes`），非期望数组。
- **四条硬约束逐字保持**（`acp-daemon` 主用例）：
  1. 常驻首段 = `ACP_MODE`（`:547/551/553` 的 `waitFor` + `:563/567/578` 的 `find`）；
  2. 工具开关同向：`:558` tools on ⇒ `!includes('--no-tools')`、`:565` tools off ⇒ `includes('--no-tools')`、`:580` 一次性 tools on ⇒ `!includes('--no-tools')`、`:587` 一次性 tools off ⇒ `includes('--no-tools')`、`:596` deny 档一次性 tools on ⇒ `!includes('--no-tools')`；
  3. 角色注入：`:555-557`（`ROLE_FILE_DEV` + `path.isAbsolute`）、`:566`（`ROLE_FILE_PLANNER`）、`:579`（一次性 `ROLE_FILE_DEV`）；
  4. 档位：`:559` 常驻 tools on = `PROFILES['omp:acp'].approval.mode`（= `always-ask`）、`:581` 一次性 allow = `PROFILES['omp:oneshot'].approval.mode`（= `yolo`）。
- **唯一真源无第二条读径**：`grep "from '.*launcher\.js'"` 于 `oamp/` 全域 → 命中仅本 PR 的 3 个测试文件，`oamp/src` / `oamp/bin` / `oamp/web` **零消费方** ⇒ 不存在「测试与生产各有一条 argv 推导」的平行真源。

### 判定 3 · PR 验收 3（行流与终态语义零改动）—— **pass**

- 承载面在场：`acp-daemon.test.js:388` `test('E2E：E-4 SSE——终态 message(out) 之前收到 ≥2 个 task_update 且文本递增')` —— diff 未触碰该用例（`git diff` 无其行）。
- 实跑通过（判定 12）：`acp-daemon` 7/7 绿（含 E-4）、`context-pool` 16/16 绿（含 `F08/§9.1：一次性 executor=omp（-p）与 shell 路径不回归`，`:511` 起的行流断言 `stdout` 逐行回流仍断）。
- 语义未弱化：`context-pool:522` 仅把筛选记号换成 profile 常量，其后的 `one-shot answer` 行流断言与 `shell-ok` 断言逐字未动。

### 判定 4 · PR 验收 4（生产源码零改动）—— **pass**

- 命令：`git -C <PR worktree> diff --name-only fa2acd6..3dc14f7` →
  `docs/iterations/0022-agent-launcher-and-protocol-layer/prs/pr-002-tasks.md`、`oamp/test/acp-daemon.test.js`、`oamp/test/call-protocol.test.js`、`oamp/test/context-pool.test.js`、`oamp/test/project-workspace.test.js`（5 项，无第 6 项）。
- `git -C <PR worktree> diff --name-only fa2acd6..3dc14f7 -- oamp/src oamp/test/helpers` → **空输出**（`oamp/src/**` 与 `oamp/test/helpers/**` 零 diff）。
- `git -C <PR worktree> diff --stat fa2acd6..3dc14f7` → 仅上述 5 文件（4 测试 + 1 任务图），`oamp/package.json` / `oamp/web/**` / `oamp/bin/**` / `omp/**` 零改动。

### 判定 5 · PR 验收 5（注入在切换落地前为惰性）—— **pass**

- `grep "config\.protocol|=== 'acp'|=== 'rpc'|'oneshot'|OAMP_PROTOCOL"` 于 `oamp/src/agent.js`、`oamp/src/context-pool.js`、`oamp/src/web.js` → **No matches found**（比 PR 文件的两条检索式更严：额外并入 `OAMP_PROTOCOL` 字面，仍零命中）⇒ 三文件既无协议取值分支、也无该键读取。
- 交叉基线：`OAMP_PROTOCOL` 全仓命中仅 `oamp/src/config.js:117,144-145`（键的产出面，`pr-001` 交付）与测试面；`launcher.js` 零 src 消费方（判定 2）⇒ 本 PR 合并本身不改变任何运行行为（`pr-002-tasks.md` A8/A9/A10 的基线在本 PR 合并时点成立）。

### 判定 6 · PR 验收 6（4 个文件单独与合并跑均全绿）—— **pass**

实际命令与输出（cwd = PR worktree 根，`node v22.15.0`）：

```
$ node --test oamp/test/acp-daemon.test.js oamp/test/context-pool.test.js oamp/test/project-workspace.test.js oamp/test/call-protocol.test.js
1..55
# tests 55
# suites 0
# pass 55
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 24750.17675

$ node --test oamp/test/acp-daemon.test.js      → # tests 7  # pass 7  # fail 0
$ node --test oamp/test/context-pool.test.js    → # tests 16 # pass 16 # fail 0
$ node --test oamp/test/project-workspace.test.js → # tests 21 # pass 21 # fail 0
$ node --test oamp/test/call-protocol.test.js   → # tests 11 # pass 11 # fail 0
```

单跑合计 7+16+21+11 = **55**，与合跑 55 逐数吻合（无套件级跳过、无 todo、无 cancelled）。

### 判定 7 · 改动面不超文件范围（`diff --name-only fa2acd6..3dc14f7` 只含 4 个测试文件，任务图文件按已声明口径不计入）—— **pass**

- 4 个测试文件全部在 PR 文件「文件范围」段声明之内（`acp-daemon` / `context-pool` / `project-workspace` / `call-protocol`），无第 5 个源码/测试文件。
- 第 5 项为 `prs/pr-002-tasks.md`（任务图，+225 行，纯新增文档），按 `pr-002-tasks.md` §0.1 末行「本阶段产物自身不计入源码/测试面」与派发口径不计入 —— 但仍登记为偏差 D-1（口径提示，非越界）。

### 判定 8 · 生产零改动（`oamp/src/**` 与 `oamp/test/helpers/**` 零 diff）—— **pass**

同判定 4 的第二条命令，`diff --name-only … -- oamp/src oamp/test/helpers` 输出为空。另核：`harness.js` 的 `buildEnv` / `startAgent` / `startRouter` 未被改动，注入一律走各文件既有的 `envExtra` / 局部 env 常量（`roleEnv`）通道 —— 与 `pr-002-tasks.md` §0.4-5（不新增 helper、不改 harness）一致。

### 判定 9 · 期望值真源 = `oamp/src/launcher.js`；不得存在测试内手写的平行期望数组（逐处核）—— **pass**

逐处核（4 文件中每一处「期望 argv / 期望记号」）：

| 处 | 文件:行 | 期望值形态 | 真源 |
|---|---|---|---|
| 常驻模式记号 ×8（waitFor/find） | `acp-daemon:547,551,553,563,567,578,585,594` | `ACP_MODE` / `ONESHOT_MODE` | `PROFILES[...].modeArgs[0]` |
| 常驻 tools-on 档位 | `acp-daemon:559` | `PROFILES['omp:acp'].approval.mode` | profile |
| 匿名常驻全量 argv | `acp-daemon:568-572` | `buildArgv('omp:acp', {model})` | `buildArgv` |
| 一次性 allow 档位 | `acp-daemon:581` | `PROFILES['omp:oneshot'].approval.mode` | profile |
| 一次性 deny 档位 | `acp-daemon:595` | 字面 `'always-ask'` | **调用层**（W2-A，见判定 10） |
| 一次性 tools-off 无档位 | `acp-daemon:588` | `!includes('--approval-mode')` | **调用层**（断言在场性，非取值） |
| §6.6 常驻记 + 四固定 flag | `context-pool:539,540` | `ACP_MODE` / `buildArgv('omp:acp').filter(--no-)` | profile / `buildArgv` |
| F08 一次性筛选记号 | `context-pool:522` | `ONESHOT_MODE` / `!ACP_MODE` | profile |
| F07 一次性筛选 + 末位 | `project-workspace:1010,1013-1015` | profile 常量 + `buildArgv('omp:oneshot',{prompt}).at(-1)` 与业务字面**双断言** | profile / `buildArgv` + 业务内容 |

结论：**无任何处**以本地复写的「一致期望数组」替代 profile 真源；唯二的调用层字面（`:595` 的 `'always-ask'`、`:588` 的「无档位」）由 W2-A 裁决直接要求（判定 10），不属「平行期望数组」。

### 判定 10 · W2-A 口径落地正确（一次性档位不取 profile 的 `yolo` 作 deny 档期望；体现调用层语义：tools 关 ⇒ 无该 flag；deny ⇒ always-ask；其余 ⇒ yolo）—— **pass**

三条分支逐条核（`clarifications/2026-09-14-stage5-wave2-verdicts.md` W2-A）：

| W2-A 分支 | 断言处 | 实际写法 | 判定 |
|---|---|---|---|
| tools 关 ⇒ **不追加** `--approval-mode` | `acp-daemon:588` | `assert.ok(!anonOneShot.includes('--approval-mode'), '… 档位由调用层按 permission × 工具开关合成，见 src/agent.js 一次性路径')` | ✓ 调用层语义 |
| `permission=deny` ⇒ `always-ask` | `acp-daemon:595` | 期望值 = 字面 `'always-ask'`（**未**取 `PROFILES['omp:oneshot'].approval.mode` 的 `yolo`），断言文案显式标注「一次性档位由调用层合成（tools on + permission=deny ⇒ always-ask），不取 profile 值」 | ✓ 正是 W2-A 的禁止项被正确规避 |
| 其余（tools on + allow）⇒ `yolo` | `acp-daemon:581` | 期望值 = `PROFILES['omp:oneshot'].approval.mode`（当前 = `'yolo'`） | ✓ 值正确（见偏差 D-2 的口径说明） |

- 对照组正确性：`oamp/src/launcher.js:52` 的 `omp:oneshot` = `approval: { mode: 'yolo', appliesWhen: 'always' }` 与 `oamp/src/agent.js` 一次性路径的 `toolsOn ? (permission==='deny' ? 'always-ask' : 'yolo')` 在 **deny 档**发散 ⇒ 若本 PR 把 deny 档期望写成 profile 派生，则「生产零改动 × 全绿」两个硬约束不可能同时成立。本 PR 未犯此错。
- 一次性段序同样未被错误「全量 deepEqual」化：`omp:oneshot` 的段序（`--no-tools` 在 `--no-session` 前）与生产相反，本 PR 对该路径只用「模式记号 + 档位 + tools 方向」的**分项断言**，未引入必然变红的全量比对。

### 判定 11 · 注入完整性（4 文件覆盖实测的 10 个 agent 启动点 / 8 个 env 载体）—— **pass**

- `OAMP_PROTOCOL` 命中数 = **8**（`acp-daemon` 4 / `context-pool` 2 / `project-workspace` 1 / `call-protocol` 1），与 `pr-002-tasks.md` A1 的 8 个 env 载体逐一对应（`grep -c` 实测）。
- 覆盖 **10 个启动点**：`roleEnv` 一处覆盖 `acp-daemon` 的 3 个 `startFlaggedAgent`（dev / planner / deny），另加 `acp-daemon` 的 `setup` / 匿名 / perm 审计 3 处 = 6，`context-pool` 2，`project-workspace` 1，`call-protocol` 1 ⇒ 10（判定 1 表格逐点在场）。
- 反查无遗漏：4 文件内不存在第 11 处 agent 启动点（含内联 spawn），亦不存在 env 载体未注入而仍启动 agent 的路径。

### 判定 12 · 4 个测试文件合跑全绿 —— **pass**

见判定 6（合跑 55/55 pass、fail 0；单跑亦全绿）。`call-protocol` 的 diff 仅 1 行（`:301` 注入），无任何断言改动；其 ACP-only 桩前提由该注入显式固定（组 F 的 `call_update.kind ∈ {chunk, stdout, stderr}` 断言逐字未动，`pr-002-tasks.md` T4 验收 2/3 成立）。

---

## 汇总

| 判定 | 结论 |
|---|---|
| 1 · PR 验收 1（注入面完整） | pass |
| 2 · PR 验收 2（真源 + 四硬约束） | pass |
| 3 · PR 验收 3（行流/终态语义零改动） | pass |
| 4 · PR 验收 4（生产源码零改动） | pass |
| 5 · PR 验收 5（注入惰性） | pass |
| 6 · PR 验收 6（单跑 + 合跑全绿） | pass |
| 7 · 改动面不超文件范围 | pass |
| 8 · `oamp/src/**` + `oamp/test/helpers/**` 零 diff | pass |
| 9 · 期望值真源 = `launcher.js`，无平行期望数组 | pass |
| 10 · W2-A 口径落地 | pass |
| 11 · 注入完整性 10 启动点 / 8 载体 | pass |
| 12 · 4 文件合跑全绿 | pass |

- **pass = 12 / fail = 0 / partial = 0 / blocked = 0**
- 分母说明：PR 验收 7（全库既有面 `node --test oamp/test/*.test.js`）按派发口径不计入本次判定（见「范围口径 1」与「下一迭代候选」e）。
- **D-6 择一判定**：本报告只判 F06 验收 1 的**辅面（测试面前置）** —— 固定「常驻链路 = acp」这一回归判据的假设；**不对 acp 行为本身下任何断言**（生产零改动，主面归 `pr-003`）。同一条验收不互推。

## 偏差记录

| # | 规格描述 | 实现实际 | 建议处理 |
|---|---|---|---|
| D-1 | PR 文件「文件范围」只列 4 个测试文件；「零改动（防夹带）」段要求改动面封闭 | `fa2acd6..3dc14f7` 的 `diff --name-only` 另含 `docs/.../prs/pr-002-tasks.md`（+225 行，任务图，纯新增） | **已声明口径内，不判 fail**。建议主 agent 在阶段 5 收口的 diff 清单里把「任务图/文档产物」与「防夹带零改动面（`oamp/src/**`、`oamp/test/helpers/**`、其余 25 个 `*.test.js`）」分列，避免混判；`pr-002-tasks.md` §0.1 末行已自证该口径。 |
| D-2 | W2-A：「档位取值（…其余 ⇒ `yolo`）由**调用层**决定，**不由 profile 数据**决定」 | `acp-daemon:581`（一次性 tools-on + allow 档）的期望值取 `PROFILES['omp:oneshot'].approval.mode`，而非调用层字面 `'yolo'` | **值等价（当前 profile 值即 `'yolo'`），判定 pass**。该写法同时满足 PR 验收 2（引用 profile）与 W2-A（值正确），可视为「allow 档 profile 与调用层必须同值」的交叉校验；代价是 pr-003/pr-005 若改动 `omp:oneshot.approval.mode` 会连带触发该断言变红。建议 pr-003 明确是否**刻意保留**该交叉绑定（若是，视为信号；若否，改为调用层字面并注释）。**不阻塞本 PR。** |
| D-3 | `pr-002-tasks.md` T2 验收 2：「四个固定 flag（`--no-skills`/`--no-rules`/`--no-tools`/`--no-session`）与 `--model` 值断言逐条保持」 | `context-pool:540` 改为 `buildArgv('omp:acp').filter((f) => f.startsWith('--no-'))` 推导（当前派生集 = 同 4 flag；`--model` 与取值断言 `:543-544` 保持字面） | **判定强度等价、无弱化**，pass。登记为「形态替换（真源化）而非削弱」。风险提示：若未来 `omp:acp` profile 新增任何 `--no-` 前缀 flag，该循环会自动扩大到新 flag —— 属**期望收紧**（更严），不会造成假绿。 |

## 下一迭代候选

- a. **architecture §9.4.1 清单回填（6 → 7 文件）**：PR 文件「D-1 登记」已给出正确口径（`tool-permission` / `confirmation-roundtrip` / `web` 随实现改造归 `pr-003`），回填由主 agent 处置；阶段 6 核查该项是否已同步（否则架构文档与实测再次漂移）。
- b. **pr-001 遗留 D-7′ 的防护义务**：`input:'positional'` 且未传 prompt ⇒ argv 末位 `undefined`。本 PR 的 `project-workspace:1013` 仅在**已传 prompt** 的路径取 `.at(-1)`，构成一个使用面证据点。pr-003 / pr-005 若引入「不传 prompt 的一次性路径」，需显式防护；建议阶段 6 核查。
- c. **W2-A 调用层档位语义在生产侧的复现验收**：本 PR 只固定测试面（未触生产），deny ⇒ `always-ask` / tools-off ⇒ 无档位 的**调用层实现**须由 pr-003（或 pr-005）独立按其验收标准验收；本报告的 pass 不可替代该主面判定。
- d. **D-2 的交叉绑定归属**：需在 pr-003 明确「allow 档期望 = profile 值」是否保留（见偏差 D-2），避免后续 PR 把该断言变红误读为回归。
- e. **PR 验收 7（全库既有面）**：本次按派发口径未跑，需主 agent 收口时以 `node --test oamp/test/*.test.js` 补判并记录「无非本 PR 引入的失败」。
- f. **一次性路径段序差异（A6）未在本 PR 引入判别面**：`launcher.js` 的 `--no-tools` 早于 `--no-session` vs 生产相反。本 PR 刻意只做分项断言（判定 10），段序一致性留待 pr-003/pr-005 的真源统一时判 —— 建议在 pr-003 brief 中显式确认该差异为「已知且可接受」。

## 结论

**PASS**

- 12 项逐条判定全部 pass，fail = 0、partial = 0、blocked = 0；偏差记录 3 条（D-1 已声明口径提示 / D-2 值等价的口径交叉绑定 / D-3 形态替换等价），**均不构成 fail 或阻塞**。
- 核心判据成立：注入面完整（10 启动点 / 8 载体）、期望值单一真源（`oamp/src/launcher.js`，`buildArgv` 允许引用且无平行数组）、生产零改动（`oamp/src/**` + `oamp/test/helpers/**` 零 diff）、注入惰性（三生产文件零协议取值分支）、W2-A 口径正确落地（deny 档不取 profile `yolo`）、4 文件单跑与合跑均全绿（55/55）。
- 本次判定限于**辅面（测试面前置）**；F06 验收 1 的主面（acp 行为不退化）仍归 `pr-003`。
