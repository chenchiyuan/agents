# 阶段 3 澄清记录（architect · 第 2 轮）— 0022-agent-launcher-and-protocol-layer

**角色**: architect（阶段 3 · 技术架构）·第 2 轮
**日期**: 2026-09-14
**迭代**: 0022-agent-launcher-and-protocol-layer
**工作区**: `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0022-agent-launcher-and-protocol-layer`（本轮一切写入均以该地址为根的**绝对路径**）
**本轮性质**: **裁决回收与收口**——只把用户裁决落进 v0.1.0，**不做新架构决策、不新增功能点、不增删组件、不改 L2 决策结论、不触碰功能卡产品维度**
**主输入**: `clarifications/2026-09-14-architect-round1-verdicts.md`（用户裁决原件）
**辅输入**: `architecture.md` v0.1.0（786 行）、`clarifications/2026-09-14-architect-round1.md`（第 1 轮记录）
**产出**: `architecture.md` **v0.2.0** + `prd/F02-protocol-layer-and-injection-entry.md` 的「架构待填」T-03 行状态同步 + 本记录
**会话限制**: 本阶段仍运行在**无实时对话通道**的子 agent 中 ⇒ **零自问自答**；本轮**未产生新问点**（转呈件为空，§5）。

---

## §1 裁决回收逐项对照（verdicts §「architect 第 2 轮的处理清单」1~6）

| # | 清单项 | 裁决要求 | 本轮落点（`architecture.md` 内节号） | 状态 |
|---|---|---|---|---|
| 1 | §4.1 两条 L1 的「裁决」行 | 由 `⏳ 待主 agent 确认` → `✅ 用户确认（2026-09-14，采纳推荐项①）`，**保留备选与否决理由** | §4.1 L1-1 / L1-2 的「裁决」行（两行同文，按裁决原文落字）；「备选」行**逐字保留不动**；§4.1 引言补一行裁决来源与"采纳推荐项①" | ✅ |
| 2 | §12.1 七项 `[model_inferred]` | 改为 `[user_confirmed]`，**保留推断原文 + 裁决结论** | §12.1 标题与导语改写为"已裁决"；表由 4 列扩为 **5 列**：第 2~4 列**保留推断原文 / 缘由 / 后果（已不发生，留档）**，第 5 列为新增的**裁决结论（`user_confirmed` · 2026-09-14）**，MI-A-1~MI-A-7 逐项 ✅ 采纳 | ✅ |
| 3 | §9.4 测试面 | 补入「新增零侵入机械断言测试」（三条判据固化），**标注为新增测试、与既有最小更新并列** | §9.4 拆为 **9.4.1 既有测试面 · 最小更新（B-11~B-15，6 文件）** 与 **9.4.2 新增测试（B-16 协议层用例 / B-17 零侵入机械断言）**；9.4 导语写明两组**并存、互不替代** | ✅ |
| 4 | §12.2 三条疑问收口 | ①/② 采纳 architect 判断；③ 转已裁决项 | §12.2 改写为「3 条已裁定 + 1 条无冲突声明」：① `API.md` / `llms.txt` **零改动**（配置面≠HTTP 接口面）；② `web/style.css` **登记为实现阶段确认**；③ **已裁决 = 新增机械断言测试**（落 B-17）；④ 保留"无根本冲突"声明 | ✅ |
| 5 | 文首状态行 | 版本 → v0.2.0；标注 L1 已确认 / `model_inferred` 归零 | 文首 `**版本**` → **0.2.0**；新增 `**本轮收口**` 行；§0 状态表 §4 / §12 两行同步；「阅读顺序」行的「2 条待确认」→「2 条已确认」 | ✅ |
| 6 | 落点可追溯 | 至少 §5.5 / §5.6 / §5.7 / §5.2 与 MI-A-1~MI-A-7 逐项对应 | 正文落点标记见 §2；另有 §5.1、§5.3、§7 T-03、§9.2 B-7 的补充落点 | ✅ |

**不做项核对（裁决明文禁止）**：未改动功能卡产品维度（功能 ID / 用户价值 / 验收标准 / 边界）；未增删组件；未变更任何 L2 决策结论（L2-1~L2-13 逐字未动）；未新增功能点。

---

## §2 `[user_confirmed MI-A-x]` 正文落点索引（可追溯）

| # | 采纳内容 | 正文落点（v0.2.0） |
|---|---|---|
| MI-A-1 | 过程增量只扩展 `kind` 取值，**不新增 SSE 事件类型** | §5.5「kind 取值域」行 |
| MI-A-2 | 过程展示 = 既有流式气泡内部分区，不做折叠 / 配色 / 开关 | §5.5「呈现（T-06）」行 |
| MI-A-3 | 门的呈现 = `tool` 名作主标签、`title` 原样多行作详情 | §5.6「呈现（T-08）」行 |
| MI-A-4 | rpc profile **不传 `--thinking`**（E3 成立的**必要条件**） | §5.2 `'omp:rpc'` 的 `thinking: null` 行 |
| MI-A-5 | oneshot `streaming` = **`degraded`** | §5.7 能力位表 `streaming` 行（oneshot 列） |
| MI-A-6 | 能力位最终集合 = M6 候选**六项原样**（不增不减） | §5.1 `CAPABILITY_KEYS` 注释；§5.7 硬约束注释；§7 T-05 引用 §5.7 |
| MI-A-7 | 新增 `agent start --protocol` flag（角色级档位） | §5.3「解析链」行；§7 T-03 落点列；§9.2 B-7 变更项 |

---

## §3 L1 收口（两条均已确认，落字范围最小）

| # | L1 决策 | 裁决 | 本轮落字 |
|---|---|---|---|
| L1-1 | 协议注入点的落点与配置载体 | ✅ 用户确认（2026-09-14，**采纳推荐项①**）：注入点 = 新建 `src/protocol.js` 的 `createProtocolLayer()`；载体 = **一个字符串**（协议名）；解析链 = 角色级 `--protocol` > env `OAMP_PROTOCOL` > `config.json` 第 4 键 `protocol` > 内置 `rpc`；**不引入协议参数对象** | §4.1 L1-1「裁决」行；备选①~④与否决理由**逐字保留**；影响面（含 `oamp/API.md` / `oamp/llms.txt` 零改动）不变 |
| L1-2 | L2 标准面被写定为一份接口契约，并成为消费层的唯一依赖面 | ✅ 用户确认（2026-09-14，**采纳推荐项①**）：会话四动作 + 三类增量回调 + 两类反向请求回调 + 能力位写定为 §5.1 的一份形状；`ProtocolError`（码值沿用既有五值）与能力位键集（M6 六候选）下沉该模块；`AcpClient` 变为"实现标准面的一个实现"（**行为零变更**）；已否决分发函数 / 每能力小接口 / `EventEmitter` / AsyncIterator 形态 | §4.1 L1-2「裁决」行；「备选」行**逐字保留**；影响面补入 §9.4 B-16 / B-17（测试面新增两项） |

**L1 与"未实施"的边界**：两条 L1 由**未确认**转为**生效架构契约**，但阶段 3 **仍不写任何代码**（§13）；实现属阶段 5。

---

## §4 收敛自检（本轮完成定义三项）

| # | 完成定义 | 结论 | 依据 |
|---|---|---|---|
| V1 | **L1 决策经用户确认** | ✅ | §4.1 两条「裁决」行 = `✅ 用户确认（2026-09-14，采纳推荐项①）`；原件 = `clarifications/2026-09-14-architect-round1-verdicts.md` |
| V2 | **所有功能卡有技术路径** | ✅ | §6「功能卡 ↔ 技术路径映射」F01~F13 = **13/13**（本轮未改；L1 确认后 F01 / F02 / F03 的路径由"待确认"转为"生效"） |
| V3 | **无架构内部冲突** | ✅ | ① 全文检索"待主 agent 确认 / 未确认 / 待转呈"**仅剩归零说明行与历史留档**，无生效的待决标记（§4.1 / §5.3 / §7 T-03 / §12.1 / §12.2 已同步）；② `[user_confirmed MI-A-x]` 七项**均有正文落点**（§2 表）；③ §9.4 B-17 与 §3.3 判据、§6 F03、§8 C4、§11 R2 **四处引用一致**；④ `model_inferred` 归零与 §12.1 标题一致 |

**未触发**：L2 决策结论变更、组件增删、功能点新增、功能卡产品维度改动（均为裁决明文禁止项）。

---

## §5 转呈件与越界声明

**转呈件：空。** 本轮只回收第 1 轮已裁决项；2 条 L1 + 7 项 MI + §12.2-3 全部有裁决原文 ⇒ **无新的 `[model_inferred]`、无新的 `[待裁决]`**。

**越界声明**：
- 本轮写入（均为 `<工作区地址>` 下的**绝对路径**）：`docs/iterations/0022-agent-launcher-and-protocol-layer/architecture.md`（v0.2.0）、`docs/iterations/0022-agent-launcher-and-protocol-layer/prd/F02-protocol-layer-and-injection-entry.md`（**仅**「架构待填」T-03 行的 L1-1 状态与 MI-A-7 标记；产品维度未触碰）、本记录。
- **未修改** `demand.md` / `prd.md` / 其余 12 张功能卡 / `status.md` / `history.md` / `clarifications/` 下既有文件（第 1 轮记录与裁决原件**全程只读**）。
- **未修改**任何产品代码（`oamp/**` 全程只读）；**未运行**测试套件 / 构建 / 格式化 / 探针；**未执行**任何 git 写命令。
- **未新增**架构决策：本轮所有改动均可回指 `verdicts` 的裁决原文，或 architect 第 1 轮已在 §12.2 登记的判断。
