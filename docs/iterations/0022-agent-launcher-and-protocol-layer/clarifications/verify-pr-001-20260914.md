# verify-pr-001-20260914 — pr-001 独立验收报告

**验证者身份（反射结果）**：同级代码审查者 —— Node.js ESM / 子进程启动与 argv 构造方向（能判断 profile 表 → argv 单一推导的边界与坑），并兼任测试面审查者（判断「既有测试面最小更新」是否越界、被改测试是否仍绿）。
反射依据：产出物 = 代码实现（新建模块 + 既有配置模块增量 + 测试增量） + 其规格符合性，主委托面为「实现是否满足验收标准 / 改动面是否封闭 / 是否越界」。

**复述**：本次验证 `pr-001`（`oamp/src/launcher.js` 新建 / `oamp/src/config.js` 第 4 键 / `oamp/test/config-file.test.js` 增量，提交 `263c787`，父 `e3f8a62`）；验证标准 = 主 agent 委托的 5 条标准 + `prs/pr-001-launcher-and-protocol-config.md` 的「验收标准」（13 条）+ `prs/pr-001-tasks.md` T1~T6 逐条；身份 = 同级代码审查者。

**独立性声明**：本报告只使用产出物本身（代码、diff、规格文件、architecture 摘录）与验证标准。未接收、未使用任何实现过程上下文（谁实现的、用什么方法、设计动机）。

**产出物**：
- `263c787` 的完整 diff（`git show 263c787`）：`oamp/src/launcher.js`（新增 150 行）、`oamp/src/config.js`（+16）、`oamp/test/config-file.test.js`（+16）
- 规格：`prs/pr-001-launcher-and-protocol-config.md`（验收标准 + 文件范围）、`prs/pr-001-tasks.md`（T1~T6）、`architecture.md` v0.2.0 §3.3 / §5.2 / §5.3 / §9.1~§9.5 / §12.1

**验证标准来源**：主 agent 委托（5 条）+ PR 文件「验收标准」（13 条）+ `pr-001-tasks.md` T1~T6（6 条）

**验证日期**：2026-09-14

**验证环境**：PR worktree `feat/0022-pr-001-launcher-and-protocol-config`，`HEAD = 263c787d641aa58db8f12273f7a4c79675d87848`，`git status --porcelain` = 空（0 项）；Node `v22.15.0`。
行为验证一律用一次性脚本（`/tmp/verify-pr001.mjs`、`/tmp/verify-pr001-config.mjs`、`/tmp/verify-pr001-positional.mjs`，未入库，不修改被验证产物）。

---

## 逐项判定

### A. 委托标准（5 条，汇总计数以此为准）

#### A-1 该 PR 文件「验收标准」全部满足 —— **pass**

依据：PR 文件 13 条验收标准逐条判定见 §B，全部命中；关键命令实跑输出：

```
$ git show --stat 263c787
 oamp/src/config.js            |  16 +++++
 oamp/src/launcher.js          | 150 ++++++++++++++++++++++++++++++++++++++++++
 oamp/test/config-file.test.js |  16 +++++
 3 files changed, 182 insertions(+)

$ node --test oamp/test/config-file.test.js oamp/test/hygiene.test.js      # cwd = PR worktree
# tests 14
# pass 14
# fail 0
（分跑：config-file.test.js 11/11 全绿；hygiene.test.js 3/3 全绿）
```

行为面独立复跑（一次性脚本，非测试文件）关键输出：

```
PROFILES keys = ["omp:rpc","omp:acp","omp:oneshot","claude:acp","codex:acp"]
每份 profile 顶层字段数 = 13  extra=[]  missing=[]        （§5.2 字段集，无多无少）
omp:rpc argv = ["--mode","rpc","--no-skills","--no-rules","--no-session","--model","m1","--append-system-prompt","/r.m","--approval-mode","always-ask"]
omp:acp argv = ["acp","--no-skills","--no-rules","--no-session","--model","m1","--append-system-prompt","/r.m","--approval-mode","always-ask"]
rpc.rest === acp.rest : true                              （唯一差异段 = modeArgs）
--thinking 检索：5/5 份 profile 均 false
tools: off ⇒ ["acp","--no-skills","--no-rules","--no-tools","--no-session"]
       allow ⇒ 无 --no-tools / 无 --tools
       list ⇒ ["acp",...,"--tools=read,exec","--no-session","--approval-mode","always-ask"]
approval: acp 工具关 ⇒ 无 --approval-mode；工具开 ⇒ --approval-mode always-ask
          oneshot 工具关 / 工具开 ⇒ 均 --approval-mode yolo
prompt: oneshot 末位 = "PROMPT-XYZ"；rpc/acp argv 内 prompt 零残留（contains=false）
bin 解析链：env⇒/tmp/fakebin ｜ profile⇒claude ｜ 无值⇒omp
spawn 实测：子进程 argv === buildArgv 输出（deepEqual true）；exit 0
config.protocol：env⇒acp ｜ file⇒acp ｜ 皆缺⇒rpc ｜ env>file⇒rpc ｜ env 空白⇒回落 file=acp
非法值：OAMP_PROTOCOL='bogus' ⇒ Error: OAMP 配置错误: OAMP_PROTOCOL 仅支持 rpc/acp（当前值 "bogus"）
        config.json {"protocol":"bogus"} ⇒ Error: OAMP 配置错误: protocol 仅支持 rpc/acp（当前值 "bogus"）
```

#### A-2 改动面不超过该 PR「文件范围」 —— **pass**（含 1 条口径差异，见偏差记录 D-2）

```
$ git -C <PR worktree> diff --name-only e3f8a62..263c787
docs/iterations/0022-agent-launcher-and-protocol-layer/prs/pr-001-tasks.md
oamp/src/config.js
oamp/src/launcher.js
oamp/test/config-file.test.js

$ git -C <PR worktree> diff --name-status e3f8a62..263c787
A  docs/.../prs/pr-001-tasks.md      ← 阶段 5 计划产物（commit 3cfee8f，先于实现提交）
M  oamp/src/config.js
A  oamp/src/launcher.js
M  oamp/test/config-file.test.js

$ git show --stat 3cfee8f
 .../prs/pr-001-tasks.md | 216 +++++++++++++++++++++
 1 file changed, 216 insertions(+)
```

判定依据：**源码面与测试面三文件与 PR「文件范围」逐项一致，零夹带**（无 `router.js` / `cluster-config.js` / `cluster.js` / `status.js` / `task.js` / `web.js` / `API.md` / `llms.txt` / `package.json` / `omp/**` 改动；`omp/` 目录在本仓库不存在）；第 4 个路径是阶段 5 的任务图产物，`pr-001-tasks.md` §0.1 与 T6 验收 2 明确将其排除在判定面外（该文件本身是本次验证的标准来源之一），故判 pass，但字面读法下的差异已单列偏差记录。

#### A-3 零消费方（`launcher.js` 落地后无任何生产引用） —— **pass**

```
$ 全仓扫描（PR worktree，跳过 docs/.git/node_modules/.runtime/data）：
  模式 launcher\.js | OAMP_PROTOCOL
  → 命中仅 6 处：oamp/src/config.js:18,144,145（新增键自身）
                 oamp/test/config-file.test.js:139,141,145（新增断言）
  → oamp/src/launcher.js 之外，全仓无 "launcher.js" 引用（含 src/bin/web/test/根配置）
```

`oamp/src/launcher.js` 文件头注释自述「不被任何既有模块 import」；实跑 `import { PROFILES, buildArgv, resolveBin, spawnAgent } from './oamp/src/launcher.js'` 可取用（导出面可用），反向无边。`config.protocol` 无生产读者：全仓对 `.protocol` 的其余命中均为既有 OAMP 消息信封 `protocol: 'oamp/1'`（`agent.js:164`、`router.js:27`、`web.js:1681` 等），与配置键无关。

#### A-4 测试面：仅 `config-file.test.js` 被改；其余既有测试文件零改动；被改测试仍绿 —— **pass**

```
$ git diff --name-only e3f8a62..263c787 | 测试文件筛选
oamp/test/config-file.test.js      ← 唯一被改测试文件
（其余既有测试文件零改动；未新增测试文件）

$ printf '%s\n' oamp/test/*.test.js | wc -l    → 29
$ printf '%s\n' oamp/test/**/*.js oamp/test/*.js | sort -u | wc -l  → 31（含 helpers/harness.js、helpers/fake-node.js）

$ node --test oamp/test/config-file.test.js → 11 tests / 11 pass / 0 fail
$ node --test oamp/test/hygiene.test.js     →  3 tests /  3 pass / 0 fail
```

`config-file.test.js` 的增量 diff 只增不删（commit 全量 `182 insertions(+)`，零 `-` 行）：既有全对象 `assert.deepEqual` 补 `protocol: 'rpc'`（`:47`），另加两条新用例（`:136-148`）。`hygiene.test.js` 的扫描面 = `bin/` + `src/` 平铺 `*.js` + `package.json`（`hygiene.test.js:26-39`），必然覆盖新增的 `launcher.js`，3/3 绿 ⇒ 新文件无凭据词、`dependencies` 仍为空。

#### A-5 无架构决策泄漏（新依赖 / 新目录 / 参数对象等） —— **pass**（含 2 条已登记的推断项，见偏差记录 D-3 / D-4）

```
$ oamp/package.json（零改动，未列入 diff）→ "dependencies": {}
  对应断言：hygiene.test.js "package.json dependencies 为空" → ok
$ 新目录：diff --name-status 仅在既有 oamp/src/ 新增单个文件，无新目录
$ 新增配置文件 / JSON：无（profile = 进程内字面量，§4.2 L2-13）
$ 配置载体形态：config.protocol 为字符串 'rpc' | 'acp'，非参数对象（§5.3「不引入参数对象」）
$ 新增 env 键：仅 OAMP_PROTOCOL（§5.3 / §5.2 明定）；新增 profile 字段：0（字段集 = §5.2 十三项）
$ launcher.js 内 cluster / instances / roles 读取：0 / 0 / 0 命中
$ launcher.js 内 stdout.on / stderr.on：0 / 0 命中（接线归实现方，§9.5 不入本 PR）
$ launcher.js 文件头 import：仅 node:child_process（唯一 import）
$ oamp/src/config.js 文件头 import：node:url / node:fs / node:path（无 ./xxx.js；import( / require( 各 0 命中）
```

对照 architecture §9.5「明确不在本迭代」（不改 omp/harness、不新增依赖/目录/进程/持久化/UI 载体、不合并 `instances[]`、不重构既有模块其它部分）：实现零命中。`launcher.js` 的导出集合与 spawn 的 stdin 选项、argv 次序属 `pr-001-tasks.md` §5 已登记的 `[model_inferred]`（MI-1/MI-2/MI-4），非静默泄漏，已单列偏差记录供主 agent 裁决。

---

### B. PR 文件「验收标准」13 条逐条判定（A-1 的展开）

| # | 验收摘要 | 判定 | 可定位证据 |
|---|---|---|---|
| 1 | `launcher.js` 存在；import ⊆ `node:*`；`dependencies` 仍 `{}`（F12 1/2） | pass | 文件存在（150 行）；`launcher.js:7 import { spawn } from 'node:child_process'` 为唯一 import；`oamp/package.json` `"dependencies": {}` 且未列入 diff；`hygiene.test.js` 14 号用例 ok |
| 2 | profile 表含三项 + `claude:*` / `codex:*` 结构项；字段集与 §5.2 逐项一致 | pass | 键集 = `omp:rpc / omp:acp / omp:oneshot / claude:acp / codex:acp`；5 份 profile 顶层字段各 13、`extra=[] missing=[]`；取值：rpc/acp = `input:'protocol'`、`session:false`、`skills:false`、`rules:false`、`tools:{mode:'off'}`、`approval:{mode:'always-ask',appliesWhen:'tools-on'}`、`thinking:null`、`model:null`、`roleFile:null`、`cwd:null`（deepEqual 通过）；claude/codex 仅表内字段，无探测 / 无接线 / 本 PR 无相关证据文件 |
| 3 | 新增宿主 = 新增 profile 数据（同一入口产出 argv） | pass | 表内 `claude:acp` / `codex:acp` 行由同一 `buildArgv` 入口产出 argv：`["--acp","--no-skills","--no-rules","--no-session","--model","m","--append-system-prompt","r","--approval-mode","always-ask"]`（与 omp 行走同一段代码：`buildArgv` / `resolveBin` / `spawnAgent` 内零 `host` 分支、零 `'omp'` 字面量比较，`host` 仅作为 5 处 profile 数据字段出现，行号 14/29/44/60/75）；新增宿主不需改 `launcher.js` 之外的任何源码（本 PR 的 `config.js` 改动属第 4 键 `protocol`，与本条无关；「协议层与消费层零改动」由 pr-005 / pr-003 在同一判据面复核） |
| 4 | `modeArgs` 是三份 profile 唯一的 argv 差异段 | pass | `omp:rpc=['--mode','rpc']`、`omp:acp=['acp']`、`omp:oneshot=['-p']`；去掉首段后 `rpc.rest === acp.rest` → true；oneshot 与 acp 的其余段差异 = `skills/rules true`（不追加二 flag）+ `approval.appliesWhen='always'`，属字段集推导结果，非独立 argv 差异段 |
| 5 | `omp:rpc` 的 argv 不含 `--thinking`（D-R3 / MI-A-4） | pass | `buildArgv('omp:rpc',{...})` 元素检索 `thinking` → false；表侧 `thinking:null` 且实现 `if (…) --thinking` 逻辑不存在（源码零 `--thinking` 字面量） |
| 6 | 工具与档位语义三条（D-R5 / §5.2） | pass | `off ⇒ --no-tools`；`allow ⇒ 不追加`；`list ⇒ --tools=read,exec`（白名单偏好）；`appliesWhen='tools-on'` ⇒ 工具关无 `--approval-mode`、工具开追加 `always-ask`；`appliesWhen='always'`（oneshot）⇒ 恒追加 `yolo`。与既有 argv 逐字对照：`acp-client.js:137-145`（`acp`/`--no-skills`/`--no-rules`/`[--no-tools]`/`--no-session`/`[--model]`/`[--append-system-prompt]`/`[--approval-mode always-ask]`）与 `agent.js:192-199`（`-p`/`--no-session`/`[--no-tools]`/`[--model]`/`[--append-system-prompt]`/`[--approval-mode yolo]`）—— flag 名与档位值逐字一致 |
| 7 | 提示词承载两态（`input` / M-3） | pass（含边界缺陷，见 D-7） | oneshot：末位 = `"PROMPT-XYZ"` 逐字（`buildArgv(...).at(-1) === 'PROMPT-XYZ'`）；rpc/acp：argv 内 prompt 零残留（`includes('PROMPT-XYZ') === false`）；子进程实测 oneshot 收到末位位置参数 `P`。边界：`buildArgv('omp:oneshot', {})` 末位为 `undefined`（子进程实收字面 `"undefined"`）——不在本条判据面内（判据以「传入非空提示词」为前提），单列偏差 / 候选 |
| 8 | `bin` 解析链 = `env.OAMP_OMP_BIN` > `profile.bin` > `'omp'` | pass | 三档实跑：env ⇒ `/tmp/fakebin`；profile（`claude:acp`）⇒ `claude`；删掉 `bin` 后 ⇒ `omp`；既有 `OAMP_OMP_BIN` 注入语义不变（`agent.js:31` 的同名语义） |
| 9 | `config.js` 第 4 键三档解析 + 非法取值响亮失败 | pass | 独立实跑：`{OAMP_PROTOCOL:'acp'}⇒'acp'`；file `{"protocol":"acp"}⇒'acp'`；皆缺 `⇒'rpc'`；env 胜出 `⇒'rpc'`；`'bogus'` ⇒ `Error: OAMP 配置错误: OAMP_PROTOCOL 仅支持 rpc/acp（当前值 "bogus"）`；file `'bogus'` ⇒ 同类抛错（体例同 `config.js:112` 的 `OAMP_RECONNECT 仅支持 0/1`）；空串 / 纯空白视为未提供 ⇒ 回落 file 值（实测 `'  '` ⇒ `'acp'`） |
| 10 | `config.js` 仍是叶子模块 | pass | 文件头 import 三行全为 `node:`（`node:url` / `node:fs` / `node:path`）；无 `./xxx.js`；无动态 `import(`；不引用 `launcher.js` |
| 11 | 服务边界与配置面不扩大（F10 1/2） | pass | diff 不含 `oamp/src/web.js`、不含 `oamp/API.md` / `oamp/llms.txt`、不含监听地址 / 鉴权相关代码；`protocol` 仅经 `config.json` + env 抵达，无新 HTTP 接口 |
| 12 | 启动数据不与 `instances[]` / 集群配置合并（F13 2） | pass | `launcher.js` 内 `cluster` / `instances` / `roles` 命中各 0；`oamp/src/cluster-config.js` 零改动（不在 diff）；`git rev-parse 263c787:oamp/src/cluster-config.js` = `e3f8a62:oamp/src/cluster-config.js` = `468d8cc6a4f2844523dc91956b6cf6f82586fe05`（逐字同一 blob） |
| 13 | 命令全绿且改动面封闭 | pass | `node --test oamp/test/config-file.test.js oamp/test/hygiene.test.js` ⇒ 14 tests / 14 pass / 0 fail；`oamp/test/*.test.js` = 29（28 个零改动 + 本 PR 改的 1 个）；`oamp/test/` 下 `.js` = 31；源码与测试面只含三文件（字面读法的第 4 路径属阶段产物，见 D-2） |

### C. `pr-001-tasks.md` T1~T6 逐条判定

| 任务 | 判据要点 | 判定 | 证据 |
|---|---|---|---|
| T1（profile 数据面） | 1 文件/import 面；2 键集；3 字段集 13 项；4 取值逐字；5 oneshot `skills/rules=true`；6 claude/codex 仅结构；7 新增宿主=加一行；8 不与集群合并；9 表可被外部 import | pass | 见 B-1 / B-2 / B-3 / B-12；`oneshot` argv 无 `--no-skills`/`--no-rules`（实测 false/false）；`import { PROFILES }` 外部可用 |
| T2（`buildArgv`） | 1 modeArgs 唯一差异；2 无 `--thinking`；3 tools 三值；4 approval 两档 × 工具开关四组合；5 提示词两态；6 其余 flag 段与调用参数；7 返回值不含 bin；8 调用签名；9 argv 次序 | pass | 见 B-4 / B-5 / B-6 / B-7；次序实测 = `modeArgs → --no-skills → --no-rules → [--no-tools\|--tools=] → --no-session → [--model] → [--append-system-prompt] → [--approval-mode] → [positional]`，与 A6（`acp-client.js`）次序逐位一致；返回值首元素为 mode 段（非可执行名）；未知 `profileKey` ⇒ `OAMP 配置错误: 未知 profile "nope"`（响亮失败） |
| T3（bin 链 + spawn 封装） | 1 bin 三档；2 argv 全出自 T2；3 cwd 基准；4 stdin 两态；5 不接线 stdout/stderr | pass | 三档实测见 B-8；`spawnAgent` 体内零 flag 字面量（唯一 flag 字面量全部在 `PROFILES` 与 `buildArgv`，行号 114/115/117/122/123/124/127），子进程实测 argv 与 `buildArgv` deepEqual；`cwd:null` ⇒ 子进程 cwd = 进程 cwd（`/private/tmp`），非 null ⇒ 用该值（实测 `/tmp` 生效）；`stdio:['pipe'｜'ignore','pipe','pipe']` 两态均 exit 0（oneshot `ignore` 无悬挂阻塞）；`stdout.on`/`stderr.on` 0 命中 |
| T4（config 第 4 键） | 1 三档；2 越界抛错；3 既有 10 键逐字不变；4 空串体例；5 叶子约束；6 配置面而非接口面 | pass | 见 B-9 / B-10 / B-11；返回对象 11 键 = 既有 10 键 + `protocol`，既有键取值实测 `socketPath/heartbeatIntervalMs:10000/heartbeatIdleMs:60000/heartbeatTimeoutMs:30000/hbLogWindowMs:60000/reconnect:true/reconnectMaxMs:10000/dbPath/defaultModel:'deepseek/deepseek-v4-flash'/contextMax:8` 全部在场且由既有用例（`config-file.test.js:34-47` 全对象 deepEqual）守护 |
| T5（测试最小更新 + 命令全绿） | 1 全对象断言增补；2 优先级/回落断言；3 非法取值断言；4 命令全绿；5 测试面无越界 | pass | `config-file.test.js` diff 只增不删（`+16`，零删除行）；新增 `:136-142` 三档解析与 `:144-148` 越界抛错；命令 14/14 绿；测试面仅此一文件 |
| T6（收口核验） | 1 命令全绿；2 改动面封闭；3 F10；4 F12；5 F13；6 零消费方；7 提交卫生 | pass | 见 A-2 / A-3 / A-4 / B-11 / B-12；`git status --porcelain` = 0 项（改动已提交）；`git rev-parse --git-common-dir`/`hooks` 下无非 `.sample` 钩子、`core.hooksPath` 未设 ⇒ 本仓无钩子可绕过（`--no-verify` 无观测差异）；`oamp/package.json` 零改动 + `dependencies {}`（`hygiene.test.js` ok） |

---

## 汇总

- pass: **5** 项（A-1 ~ A-5；展开面：PR 验收 13/13 pass、T1~T6 6/6 pass）
- fail: **0** 项
- partial: **0** 项
- blocked: **0** 项

---

## 偏差记录

> 实现与现有规格 / 文档不一致之处。不影响本次验收判定。

| # | 规格 / 文档描述 | 实现实际 | 建议处理 |
|---|---|---|
| D-1 | architecture §9.4.1「既有测试面 · 最小更新（6 文件）」未列 `oamp/test/config-file.test.js` | 本 PR 修改了该测试文件（+16 行，只增不删），依据 = PR 文件「文件范围」第 3 项 | 按实现更新 §9.4.1（补入该文件的「最小更新」行）；`pr-001-tasks.md` §5 登记②已记录该口径差异，无需返工 |
| D-2 | PR 文件验收 13 字面：「`git diff --stat` 只含本 PR 的三个文件」 | `diff --name-status e3f8a62..263c787` 返回 4 个路径，第 4 个 = `docs/.../prs/pr-001-tasks.md`（阶段 5 计划产物，commit 3cfee8f，先于实现提交） | 主 agent 明确「阶段产物不计入改动面」的永久口径（`pr-001-tasks.md` §0.1 / T6 验收 2 已如此声明）；本报告按该口径判 pass |
| D-3 | architecture §5.2 未给 argv flag 次序（既有 A6 与 A7 次序本身不一致） | 实现取 A6（acp）次序为唯一规范序；oneshot 侧 `--no-tools` 相对 `--no-session` 的位置与 `agent.js:192-199` 现状相反 | 主 agent 裁决 `pr-001-tasks.md` §5 MI-2（建议 pr-003 落地前裁决；M-3 已证 flag 集合与顺序无关，风险限于「逐字复现既有 argv」的断言面） |
| D-4 | architecture §5.2 只钉 `PROFILES` 与字段语义，未给导出形态 / spawn stdio 契约 | 实现导出 `PROFILES` / `buildArgv` / `resolveBin` / `spawnAgent`，spawn 支持 `stdin` 选项（默认 `'pipe'`） | 主 agent 确认 `pr-001-tasks.md` §5 MI-1 / MI-4（直接影响 pr-002 的「直接引用 `PROFILES`」与 pr-003 / pr-005 的启动面契约） |
| D-5 | architecture §5.2 的 `omp:oneshot` 行以 `…` 省略 `skills` / `rules` 取值 | 实现取 `skills:true` / `rules:true`（不追加 `--no-skills` / `--no-rules`，复现既有一次性 argv） | 主 agent 确认 `pr-001-tasks.md` §5 MI-3（取 `false` 会改变一次性 argv 行为） |
| D-6 | §5.3 解析链第四档「角色级 `--protocol`」；`pr-001-tasks.md` §5 MI-5 假定 `config.json` 的 `protocol` 为**顶层**键，而既有配置文件风格是 `data`/`defaults`/`context` 分段 | 实现按顶层键读取（`config.js:117 readProtocol(parsed.protocol, 'protocol')`） | 主 agent 裁决 MI-5（PR 验收 9 字面亦为顶层，建议维持并同步 README 文档面 B-10） |
| D-7 | 规格未定义「`input:'positional'` 且未传 prompt」的行为 | `buildArgv('omp:oneshot', {})` ⇒ argv 末位为 `undefined`；子进程实收字面字符串 `"undefined"`（实测 `["-p","--no-tools","--no-session","--approval-mode","yolo","undefined"]`，exit 0） | 下一迭代加固（未传 prompt 时不追加位置参数，或按响亮失败体例抛错）；本 PR 判据面以「传入非空提示词」为前提，故不改本次结论 |

---

## 下一迭代候选

- **位置参数空值防护**：`launcher.js:129`（`if (profile.input === 'positional') args.push(prompt)`）在 `prompt === undefined` 时把 `undefined` 推入 args，Node 将其字符串化为 `"undefined"` 传给子进程 —— 一次性路径若无提示词会静默多出一个垃圾位置参数（证据见 D-7）。
- **MI-1 / MI-2 / MI-3 / MI-4 / MI-5 待裁决**：`pr-001-tasks.md` §5 登记的五项 `[model_inferred]`（导出形态、argv 次序、oneshot 的 `skills/rules`、spawn stdin 选项、`config.json` 键位置）应在 pr-002 / pr-003 落地前由主 agent 裁决，否则下游断言会以未确认形态为真源。
- **`approval` 的 caller 覆写面无承载位**：既有一次性路径的 `deny ⇒ always-ask` 由外部条件决定（`agent.js:198`），而 `buildArgv(profileKey, {model, roleFile, tools, prompt})` 无 approval 覆写参数（`pr-001-tasks.md` §5 登记③）—— pr-003 / pr-005 需前置处理或扩展签名。
- **`tools.mode='list'` 空列表边界**：`tools:{mode:'list'}` 未带 `list` 时产出 `--tools=`（空 csv）；语义为「白名单偏好」不承诺精确集合，但空参形态是否合法未在规格面界定。
- **`config.json` 配置风格一致性**：新增第 4 键走顶层，与既有 `data` / `defaults` / `context` 分段风格并存，后续键位增加时建议统一（`architecture §8` 配置面口径）。

---

## 结论

**PASS**

说明：委托的 5 条验证标准全部 pass，PR 文件 13 条验收标准与 `pr-001-tasks.md` T1~T6 逐条 pass，fail = 0。上表 7 条偏差与 5 条下一迭代候选已显式记录，不阻塞本 PR 的验收关闭；其中「位置参数空值防护」为下一迭代应处理的实现缺陷，其余为规格未定项的裁决输入。

（本报告为唯一写入；未修改任何被验证产物，未执行任何 git 写操作。）
