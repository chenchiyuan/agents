# 澄清记录 · 2026-09-14-prd-round1（迭代 0022-agent-launcher-and-protocol-layer，阶段 2 功能规格）

**caller**: prd
**时间**: 2026-09-14
**本轮性质**: 阶段 2 拆卡时产生的 **4 项 `model_inferred` 判定口径推导**——**不是**需求结论（`demand.md` v1.0.0 的 `model_inferred` 与 `[待裁决]` 均已归零），而是拆卡过程中暴露的"**验收标准的判定口径存在多解**"。本文件供**主 agent 真实阻塞式转呈用户裁决**（本宿主无直接对话通道，**严格禁止自问自答**）。
**本轮不需要**需求层的重新澄清：U-1~U-5 / J1~J4 / A1~A7 / W1~W8 / N1~N12 / M1~M7 / E1~E7 / R1~R7 之间未发现矛盾或不清晰（见 `prd.md`「疑问与越界」6）。
**状态**: **4 项全部待确认**。用户裁决后，卡内标记按 `[model_inferred MI-xx]` → `[user_confirmed MI-xx]` 同步；若被驳回，按裁决改写对应验收 / 边界并重走 Verify。**在裁决前，本阶段不视为收敛。**

---

## 一、转呈清单（建议按组提问，4 项可并为 3 组）

### 组 A：可测面落在哪一层（F01 / F04）

| 编号 | 来源卡 | 待确认 | 本阶段采用值 | 备选读法 | 不问清会怎样 |
|---|---|---|---|---|---|
| **MI-01** | F04 验收 1（W3 / F3） | 三类增量"**被承接（不被丢弃）**"在**哪一层**可判 | **① 内部会话语义面**（增量的承接面）；**控制台展示面另由 F08 承载** | ② 以"控制台可见"为判（与 F08 重复，F04 失去独立可测性）；③ 不作规定 | 不落口径则 F04 与 F08 的判据面互相覆盖——"链路能拿到增量"与"控制台展示增量"两件事无法各自判定 |
| **MI-02** | F01 验收 1、2（W1 / U-2） | 「**统一起会话入口**」怎么算"统一" | **① 同一入口可服务多个宿主 + 启动信息以数据形态承载**（不在会话语义内写死） | ② 只有一个代码入口即可（弱化为形态约束，"可扩展"失去可测面）；③ 不作规定 | U-2 的核心诉求是"接入不同客户端"，口径不落则 F01 的验收只能写成描述而非规格 |

### 组 B：清单在验收中的定位（F09）

| 编号 | 来源卡 | 待确认 | 本阶段采用值 | 备选读法 | 不问清会怎样 |
|---|---|---|---|---|---|
| **MI-03** | F09 验收 3（M6） | M6 的**候选能力位清单**（`streaming` / `thinking` / `approvalGate` / `hostTools` / `introspection` / `queueControl`）在验收中的定位 | **① 可表态检查点**（每项都可被协议表态）；**不是最终集合门槛**（最终集合与签名留阶段 3 → T-05） | ② 把候选清单当作硬性最终集合（与 M6「最终集合 `[架构待填]`」相冲突，等于把阶段 3 的形态决策提前锁死）；③ 不列入验收（能力位声明无法验收） | ② 越界替阶段 3 做决策；③ 则 F09 不可判定 |

### 组 C：降级 / 无语义的可判形态（F07）

| 编号 | 来源卡 | 待确认 | 本阶段采用值 | 备选读法 | 不问清会怎样 |
|---|---|---|---|---|---|
| **MI-04** | F07 验收 1（W6 / M5 / J4） | 「**无会话语义**」怎么判 | **① 产物 / 日志显示走 oneshot + 无会话建立与续接行为** | ② 只判"不产生会话记录"（可能被读成"恰好这一轮没产生"）；③ 不作规定 | 不落口径则"不会假装自己有会话"只剩口号，阶段 6 无法判定 |

---

## 二、拆分计划与执行结果（W → 卡映射，供核对）

派发时对齐的拆解计划：按 `demand.md` 第二段 §2 的 **W1~W8** 逐条原子化为功能卡，粒度判据 = **可独立测试 + 可独立交付**；**M 条按形态归属分配到卡**；**N 条按"是否属跨切面边界保持"决定是否单独落卡**。

| demand 条目 | 落卡 | 是否一对一 | 拆分理由 |
|---|---|---|---|
| W1 | F01 | 一对一 | 启动数据化 + 统一入口 + 新增宿主扩展面同属一个可独立失败的行为面（E7） |
| W2 | F02 + F03 | **一对二** | M2（入口唯一 / 解析链 / 选择域，配置面的可观测行为）与 W8（消费层不构造 / 不选择、切换零 diff，DI 语义契约）可独立失败 |
| W3 | F04 + F05 | **一对二** | "链路能拿到增量 + 轮次终态"与"门接得对（一条 / 不重不欠）"在两处验收面（E3 vs E4 + M4），可独立失败 |
| W4 | F05 | 一对一 | — |
| W5 | F06 | 一对一 | — |
| W6 | F07 | 一对一 | — |
| W7 | F08 | 一对一 | — |
| W8 | F03 | 一对一（判据 D-1~D-3） | — |
| M1 | F01 / F02 / F03（需求级分工）+ T-01（形态留白） | 不设卡 | 三层是**形态**（形态细节 `[架构待填]`），其需求级分工由三张卡分别承载 |
| M6 | F09（形态约束卡） | 不设卡于 M6 本身 | 能力位声明机制横跨 F04~F07，需单一承载点 |
| M7 | —— | **无卡** | 阶段 3 的前置要求（非用户可见产品行为），登记在 `prd.md`「移交阶段 3 的承接项」 |
| N1 / N3 / N4 / N5 / N6 / N7 / N9 / N12 | 落入相关卡的验收与边界 | 不设卡 | 与所属功能同判据面（体例同 0021 的 N4~N7） |
| N2 / N8 / N10 / N11 | **F11 / F10 / F12 / F13**（保证项） | 独立落卡 | 跨切面边界保持、可独立判定（体例同 0021 F10~F12、0020 F17 / F19 / F20） |

**执行结果**：13 张卡 = 9 个需求功能点（F01~F09）+ 4 个保证项（F10~F13）；`prd.md` 与 `prd/` 目录一一对应。

---

## 三、需求空缺与拆分判断（**需求层事实，非 MI**）

| # | 事项 | 本阶段处置 | 回退口 |
|---|---|---|---|
| 1 | **W1 括注的 profile 内容维度清单**（宿主 / bin 解析 / argv / env / cwd / 模型 / 角色 / 工具 / 权限）来源是 kickoff §五 的「分层建议」——`demand.md` §1 明定该节按「**非结论**」处理；A6 又记录用户对 profile 字段集「都没有额外想法」 | **只把「启动信息数据化 + 统一入口」作为验收面**（U-2 / J1 / A2 直接支撑）；该清单**仅登记为内容维度参考**，其形态留 T-02；**未据此新增任何验收条目** | 若认为清单本身应作为验收阈值 → **回 `demand.md` 明确**，本阶段不自行拍板（`demand.md` 只读） |
| 2 | **`demand.md` 未规定过程增量的透出粒度与呈现形态**（M3 明标 `[架构待填]`，A6：用户无预设想法） | 本阶段只判"轮次结束前在既有对话详情内可见 + 不入库 + 不做开关"（E3 / J3 / A3/P4 有明文），粒度与形态落 T-06 / T-07 | 若认为粒度本身属需求 → 回 `demand.md` 补口径 |
| 3 | **`demand.md` 未规定 RPC 门在确认栏的呈现形态**（M4 明标 `[架构待填]`，A6） | 本阶段只判"字段集合与语义等价、值域随协议声明、一次调用一条"（A3/P5 / E4 有明文），呈现形态落 T-08 | 同上 |
| 4 | **拆分未按 W 编号机械一对一**：W2 → F02 + F03；W3 → F04 + F05；M1 / M7 不设卡 | 理由见上表与 `prd.md`「疑问与越界」3 | 若认为某处应合并 / 再拆 → 仅影响该组卡的编号与索引表，**不影响 W / E 覆盖完整性** |
| 5 | **E6（验收证据要求）与 M7（阶段 3 首步实测）不构成产品行为** | E6 → 由 F04 / F06 的"真实进程"判定行承接（证据产出与核查属阶段 5 / 6）；M7 → `prd.md`「移交阶段 3 的承接项」 | 体例先例：0021 的 U-6（流程约束、非产品需求，无承载卡） |

**W→卡 与 E→卡 映射的完整性**：`prd.md`「需求追溯」已逐条给出 W1~W8 / N1~N12 / M1~M7 / E1~E7 / R1~R7 的落地位置，**无未承载项**；本阶段**未自行增补任何需求条目**。

---

## 四、已按 `demand.md` 明确口径落卡、**不列为 MI** 的相邻事项（供核对，避免误当成遗漏）

| 事项 | 依据 | 落卡 |
|---|---|---|
| 默认走 rpc + 解析链（角色级 > 全局默认 > 内置 `rpc`）+ 选择域 = {`rpc`,`acp`} | W2 / M2 / E1 / A2 | F02 验收 1~3 |
| 不做动态切换 / per-chat / per-request / 控制台可切 | N3 / A2 | F02 验收 4 |
| 切换即新会话、不迁移在飞上下文（沿用既有 `context_reset` 提示） | N9 / R7 | F02 验收 5 |
| D-1 指定即生效（不回落到内置默认）/ D-2 消费层不构造不选择 / D-3 切换零 diff | W8 / A1（用户改写口径） | F03 验收 1~3 |
| 零侵入判据适用范围 = 生产消费层；测试面最小更新、不纳入判据 | A5 / R2 | F03 验收 4 |
| 旧口径（源码出现协议名 / "只留判据②"）作废 | 登记⑥ / A1 | F03 卡首「旧口径作废声明」 |
| 三类增量映射（文本 / 思考 / 工具）+ 轮次终态（受理 ≠ 完成） | W3 / F2 / F3 | F04 验收 1~3 |
| 门 → 既有 7 字段信封 → 收件箱 → 既有裁决回路 | W4 / M4 / F12 | F05 验收 1 |
| 一次受门禁调用恰一条确认项 + 裁决后条目消失 | E4 / F16 | F05 验收 2 |
| 字段与语义等价、值域随协议声明（RPC 恒二元 `Approve` / `Deny`），不要求逐字等价 | A3/P5 / R4 | F05 验收 3 |
| `tool` 名复用既有提取原语（与 RPC `title` 同源） | A3/P5 / F15 | F05 验收 4 |
| acp 既有行为不退化 + 可切回 | W5 / E5 / J2 | F06 验收 1、3 |
| acp 不补思考 / 工具输出增量，能力位 `thinking=false` | N12 / A4 | F06 验收 2 |
| oneshot 无会话语义 + 能力显式降级 | W6 / M5 / A3/P3 | F07 验收 1~2 |
| shell 执行器保持独立、不纳入协议层 | N7 / J4 | F07 验收 3 |
| 轮次结束前可见思考与工具增量 + 落在既有对话详情 + 不新增载体 | W7 / M3 / E3 / A3/P4 | F08 验收 1~2 |
| 不入库、不改「一次问答恰两条记录」 | E3 / J3 / N4 | F08 验收 3 |
| 不做透出开关 / 过滤 / 分级 | N5 / A3/P4 | F08 验收 4 |
| 逐协议显式声明能力位 + 缺能力显式降级 | M6 / A3/P3 | F09 验收 1~2 |
| `steer` 仅作能力位声明、不做产品功能 | N6 / F4 | F09 验收 4 |
| 服务边界不变（仅 `127.0.0.1` / 零鉴权 / 不做多用户） | N8 | F10 验收 1~2 |
| 不改 omp / harness 源码与协议 | N2 | F11 验收 1~2 |
| 不引入第三方依赖（保持零依赖） | N10 | F12 验收 1~2 |
| 不实现 OAMP Router / 消息层；profile 与其 `instances[]` 不做合并 | N11 / 登记⑤ | F13 验收 1~3 |
| 已知代价 R1 / R5 / R6 / R7 为已接受项，不派生功能点 | `demand.md` §6 | 各卡边界（F06 / F08 / F01 / F02） |

---

## 五、未越界声明

- 本文件与 `prd.md`、`prd/F01~F13*.md` 是该阶段**唯一写入**（全部为**新建**文件，均位于本迭代工作区内：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0022-agent-launcher-and-protocol-layer/docs/iterations/0022-agent-launcher-and-protocol-layer/`）。
- **未修改** `demand.md` / `status.md` / `history.md` / `clarifications/` 下已有文件（`2026-09-14-kickoff-discussion.md`、`2026-09-14-demand-round1.md`、`2026-09-14-demand-round1-verdicts.md`、`2026-09-14-demand-round2.md`、`briefs/` 均未触碰）。
- **未触碰**任何产品代码与规范：`oamp/**`、`roles/**`、`.claude/**`、`docs/iterations/0021-*/**`、`docs/iterations/0020-*/**` 仅作只读参考，**未写入**。
- **未运行**任何测试、构建、格式化或服务；**未执行**任何 git 写命令；**未写主工作区任何文件**（一切读写均为 `<工作区地址>` 下的绝对路径）。
- 本阶段**未做任何架构决策**：技术选型、数据模型、接口设计、承载形态、字段与实现细节一律以 `[架构待填]`（T-01~T-11）留白。
- **未发现 `demand.md` 内部矛盾**：故**未写入** `deferred-demand-changes.md`，也无"停下来报告矛盾"事项。
- **`model_inferred` 不自我宣布生效**：4 项 MI 全部待主 agent 转呈用户裁决；裁决前卡内保持 `[model_inferred MI-xx]` 标记。
