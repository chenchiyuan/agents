# 0022 阶段 5 · 末波任务图 `[model_inferred]` 与上报处置记录

**日期**: 2026-09-14（主 agent 阻塞式取得）
**转呈来源**: `prs/pr-003-tasks.md`（5 项 MI + 3 项上报）
**性质**: `user_confirmed`

---

## W3-MI · 5 项 —— **全部采纳**

| # | 采纳内容 |
|---|---|
| MI-1 | 池的注入位形参名/形状 = `createResident({chatId, agentId, role, hooks})`（取 `protocol.js` 门面同名成员） |
| MI-2 | 档位门「仅 allow 档注入 + 附加会话身份」同样适用于 rpc 分支消费的 `hooks.onApproval` |
| MI-3 | 池的会话可用性判据改按池自身状态（`this.client` 在场 + `closed`），不再读实现私有成员 `dead`/`disposed`；失效一律由 `_failSession` 收尾 |
| MI-4 | B-17③ 的机械化形态 = 运行时快照比对（测试起始读三文件为基线，起过两种注入配置后比对），不内嵌基线哈希 |
| MI-5 | `web.test.js`「执行路径判定」用例的常驻轮次形态：放宽容忍承接 argv 判据 |

## W3-ESC · 3 项上报的处置 —— **全部按建议**

| # | 处置 |
|---|---|
| 上报① | D-2 判据字面口径修正为「**生产消费层三文件零 import / 零构造**」（原字面含自指 / 注释命中，不可达）；依据 = architecture §9.4.2 B-17 的精确表述 |
| 上报② | `web.test.js`「执行路径判定」两条断言互斥 ⇒ **保留 argv 判据**；文本判据由既有用例（`project-workspace.test.js:232/:1021-1022`）承接；**不新增文件、不扩文件范围** |
| 上报③ | F04 验收 1/2 的消费侧管道面证据 = 一次性脚本（不入库）+ 阶段 6 真实进程证据；**不追加入库文件面**（超本 PR 8 项范围） |

## 主 agent 补充约束（非用户决策点）

1. **文件范围锁定为 pr-003 文件所列 8 项**；`project-workspace.test.js`（pr-002 已合并面）**不得修改**——上报②的「承接」按「既有覆盖已在场、无需改动」理解。
2. 钩子入参选项键名 = `optionId`（D-w2-1 已裁决）。
3. **W2-A 不得在本 PR 引入第二套档位语义**（一次性档位已由 pr-005 的 `oneshot-client` 合成）。
4. `acp-client.js` 补 `close()`（§5.1 四动作 + 池释放路径必需，属本 PR 文件范围）。
5. 本 PR 是最后一个 PR，**必须跑一次全量套件**（T8）并在报告中给出输出。
6. D-6 择一判定声明：F02 验收 1/2 判辅面；F06 验收 1 判主面；F08 验收 1/2 判辅面。
