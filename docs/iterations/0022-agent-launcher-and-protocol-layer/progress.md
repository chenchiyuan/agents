# progress.md — 0022-agent-launcher-and-protocol-layer

**快照序次**: 第 3 次（progress-observer，整体覆盖写入）
**观测时刻**: 2026-09-14 18:11:27 ~ 18:12:28 +0800
**观测基线**: 迭代分支 `iteration/0022-agent-launcher-and-protocol-layer` HEAD = `7651040`（工作区 `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0022-agent-launcher-and-protocol-layer`，`git status --porcelain` 为空）
**方法限定**: 只使用只读 git 命令（`log` / `cat-file -p` / `branch -a` / `worktree list` / `ls-tree` / `rev-list` / `merge-base` / `show` / `grep`）+ 产物文件存在性与文件内容读取 + 文件 mtime。**未采信 status.md / history.md 的自我声明作为结论**；每条结论后附一手记录。**未修改任何被核实产物，未执行任何写入性 git 命令**；本文件是本轮唯一写入。
**参考基线**: 代码库根 `/Users/chenchiyuan/projects/agents`（共享 `.git`）；迭代分支相对 `main` 的 `merge-base` = `71d5920`（= main 顶端）⇒ 迭代分支领先 main **24 个提交**且**尚未合并回 main**；迭代目录在 `main` 上 0 个文件。

---

## 1. 阶段完成状态

| # | 阶段 | 声明状态 | 一手证据（只读核实） | 核实结论 |
|---|---|---|---|---|
| 1 | 需求收敛 | ✅ | `demand.md` 在场且入库（引入提交 `a4d3e2f`，现处 HEAD 树）；文件头 `**版本**: v1.0.0` 与声明版本一致 | 产物在场 ✔（内容质量不在本角色判定面） |
| 2 | 功能规格 | ✅ | `prd.md`（文件头 `**版本**: 0.2.0`）+ `prd/` 下 **13** 个 `.md`（与「13 张卡」声明数一致） | 产物在场且计数相符 ✔ |
| 3 | 技术架构 | ✅ | `architecture.md`（文件头 `**版本**: 0.2.0`）；`clarifications/probes/` 目录存在（仅核存在性，内容未核，见 §6-6） | 产物在场 ✔ |
| 4 | PR 规划 | ✅ | `prs/` 下 5 个 PR 文件 + 5 个 `*-tasks.md`（`pr-001..005`）全在场且入库；Gate 报告两份：`verify-stage4-gate-20260914.md` 结论 **FAIL**（fail 2，均在 pr-003 的 A1/A2）、`verify-stage4-gate-r2-20260914.md` 结论 **PASS**（30 项全 pass / partial 0 / fail 0） | 与声明「首轮 FAIL ⇒ 重划后第二轮 PASS 30/30」逐字相符 ✔ |
| 5 | PR 实现 | ✅ 5/5 合并 | 5 条 merge 提交齐且**均为真双亲 merge**（§附录-1）；5 份验收报告齐且结论均 **PASS**（§2/§3）；合并后全量测试绿色一项见 §5-5 | 主体相符 ✔（一项声称缺完成态一手记录） |
| 6 | 独立验证 | ⏸ 已派发 | `clarifications/verify-stage6-20260914.md` **不存在**（工作区与 HEAD 树均无）；`history.md` 无阶段 6 派发条目（末条为 `18:16:30` 槛位释放） | **进行中 / 派发时刻无一手记录**（见 §5-4） |

阶段 5 三个波次的裁决文件（`2026-09-14-stage5-wave1|2|3-verdicts.md`）、pr-planner 第 1~4 轮（`2026-09-14-pr-planner-round1..4.md`）、5 份 `verify-pr-00X-20260914.md` 均在 `clarifications/` 下真实在场。

---

## 2. PR 依赖核实（每条 `depends_on` 对 git 真合并记录）

| PR | PR 文件 `depends_on` 声明 | 依赖的合并记录（git 提交时刻） | 本 PR 合并记录 | 判定 |
|---|---|---|---|---|
| pr-001 | （无） | — | `2a2d979` @ `16:03:39`（第二亲代 `263c787`） | 相符 ✔ |
| pr-002 | `pr-001` | `2a2d979` @ `16:03:39` | `4777bf0` @ `16:53:15`（第二亲代 `3dc14f7`） | 依赖先于本 PR 合并 **40m36s** ✔ |
| pr-003 | `pr-005` / `pr-002` / `pr-001` | `b54f143` @ `16:53:15` / `4777bf0` @ `16:53:15` / `2a2d979` @ `16:03:39` | `a975ce8` @ `18:09:13`（第二亲代 `92a1bbb`） | 三条依赖**全部**先于本 PR 合并 ✔ |
| pr-004 | （无）；另登记「验收时序边 `pr-004 ⤳ pr-003`，不入 `depends_on`」 | 该时序边要求：端到端形态复核晚于 pr-003 合并 | `f1097ae` @ `16:03:39`（第二亲代 `28acecb`）；pr-003 合并 @ `18:09:13` | 无合并前置 ✔；时序边亦满足（晚 2h05m）✔ |
| pr-005 | `pr-001` | `2a2d979` @ `16:03:39` | `b54f143` @ `16:53:15`（第二亲代 `ed01648`） | 依赖先于本 PR 合并 **49m36s** ✔ |

补充一手证据：每个 merge 的**第二亲代即该 PR 分支顶端提交**（`263c787` / `28acecb` / `3dc14f7` / `ed01648` / `92a1bbb`），且 `git merge-base --is-ancestor <tip> iteration/0022-…` 五条**全部为 yes** ⇒ 合并确实把该 PR 分支的内容带入迭代分支，非「空合并 / 声称合并」。
依赖图（`pr-001 → {pr-002, pr-005} → pr-003`、`pr-004` 独立）与 status.md 文字、5 个 PR 文件声明三方一致；全迭代未出现「依赖未满足即合并」的条目。

---

## 3. PR 实现状态核实（worktree / 分支 / 已合并 / 产物在场）

| PR | 分支 | worktree | 分支是否已合并 | 清理状态 | 核心产物在场证据（迭代分支树） |
|---|---|---|---|---|---|
| pr-001 | `feat/0022-pr-001-launcher-and-protocol-config` | 无 | 是（`2a2d979`，tip `263c787`） | 已清理 | `oamp/src/launcher.js`（引入提交 `263c787`）；`oamp/src/config.js` 第 4 键 `protocol`（`:117` / `:144-145` 三档解析链） |
| pr-002 | `feat/0022-pr-002-test-face-profile-pinning` | 无 | 是（`4777bf0`，tip `3dc14f7`） | 已清理 | `OAMP_PROTOCOL` 注入落在 `oamp/test/acp-daemon.test.js` / `context-pool.test.js` / `project-workspace.test.js` / `call-protocol.test.js` |
| pr-003 | `feat/0022-pr-003-protocol-layer-and-consumption-cutover` | 无 | 是（`a975ce8`，tip `92a1bbb`） | 已清理 | `oamp/test/zero-intrusion.test.js`（新增，引入提交 `7c123ad`）；`oamp/src/agent.js` 的 `--protocol` 角色级档（`AGENT_FLAGS:549`、`:598` 非法取值、`:683` 层序） |
| pr-004 | `feat/0022-pr-004-stream-kind-partition-ui` | 无 | 是（`f1097ae`，tip `28acecb`） | 已清理 | `oamp/web/app.js` 过程分区面（`state.stream.{text,thinking,tool}` `:32`、`renderStreamPart` `:419`/`:429`、`stream-${slot}` 容器 `:632`） |
| pr-005 | `feat/0022-pr-005-protocol-layer-and-injection-entry` | 无 | 是（`b54f143`，tip `ed01648`） | 已清理 | `oamp/src/protocol.js` / `rpc-client.js` / `oneshot-client.js`（引入提交 `1082748`）；`oamp/test/protocol-layer.test.js`；`launcher.js` 的 `approval` 入参与 D-7′ 守卫（`:135`） |

清理核实（一手记录）：
- `git branch -a` 全量输出 = `iteration/0022-agent-launcher-and-protocol-layer` / `main` / `remotes/origin/main`；`git branch -a --list '*feat*'` **空** ⇒ 5 个 PR 分支均已删除 ✔
- `git worktree list` = **2 条**（主工作区 `/Users/chenchiyuan/projects/agents` @ `71d5920 [main]`、迭代工作区 @ `a975ce8 [iteration/0022-…]`）；磁盘 `.pb-agents/worktrees/` 下**仅剩迭代工作区一个目录** ⇒ 5 个 PR worktree 均已清理 ✔
- 附带偏差登记：`git worktree list` 显示的迭代 HEAD 为 `a975ce8`（未随 `7651040` 刷新仅为 worktree 元数据滞后，实际 `git rev-parse HEAD` = `7651040`，工作树 clean）

---

## 4. 并发度分析

### 4.1 三个波次的在飞时间窗（history 条目标注时刻 ‖ git 提交时刻）

| 波次 | PR | planner 派发 | dev 派发 | dev 报告 | verifier 报告 | 合并（git 提交时刻） |
|---|---|---|---|---|---|---|
| 首波（`batch=1`） | pr-001 | 15:37:40 | 15:45:00 | 15:48:42 | 16:02:20 | `2a2d979` @ **16:03:39** |
| 首波（`batch=1`） | pr-004 | 15:37:40 | 15:45:00 | 15:55:27 | 16:03:36 | `f1097ae` @ **16:03:39** |
| 次波（`batch=2`） | pr-002 | 16:05:30 | 16:16:00 | 16:24:00 | 16:33:00 | `4777bf0` @ **16:53:15** |
| 次波（`batch=2`） | pr-005 | 16:05:30 | 16:16:00 | 16:46:00（含定点修复 `ed01648`） | 16:52:00 | `b54f143` @ **16:53:15** |
| 末波（`batch=3`） | pr-003 | 16:55:00 | 17:09:00 | 18:07:00 | 标注 18:15:30（报告件 mtime **18:08:55**，见 §5-3） | `a975ce8` @ **18:09:13** |

`batch` 字段取自 5 个 PR 文件正文（pr-001=1 / pr-002=2 / pr-003=3 / pr-004=1 / pr-005=2），与「首波/次波/末波」的划分逐条一致。

### 4.2 并发真实性（交错证据：同一时间窗内两个 PR 各自独立推进）

- **首波**：两条 verifier 派发条目**同一时刻**（`15:56:43` ×2）；两条 dev 报告相隔 **6m45s**（pr-001 `15:48:42` ↔ pr-004 `15:55:27`），即 pr-001 dev 完成时 pr-004 dev 仍在写码 ⇒ 并发，非串行。
- **次波**：pr-002 的 dev 报告（`16:24:00`）与验收 PASS（`16:33:00`）发生时，**pr-005 仍在 dev 修复轮次**（`16:46:00` 才报完成）⇒ 实测交错。
- **末波**：仅 pr-003 一个 PR（其三条依赖已全部满足），结构性串行。
- 门槛管理亦有记录：`槛位释放` 决策三次（`16:04:30` 释放 0→2；`16:53:00` 释放 2→4；`18:16:30` 释放 4→5），与合并次数一致。

### 4.3 「可并发但闲置」PR（本角色重点项）：**0 个**

- 解锁 → 派发时延（依赖合并提交时刻 → 该 PR 的 planner/dev 派发条目）：次波 `pr-001` 合并 `16:03:39` → `16:05:30` 派发 = **1m51s**（其间含 `16:05:07` Gate 裁决与任务图落盘）；末波 `pr-005`/`pr-002` 合并 `16:53:15` → `16:55:00` 派发 = **1m45s**。首波两个「无依赖」PR 在同一时刻（`15:37:40`）即被派发。
- 逐条核查：全迭代**不存在**「`depends_on` 已满足但无 worktree / 无派发记录」的 PR。第 2 次快照登记的「`pr-003` 依赖未满足（阻塞为预期）」现已被真实解锁并完成。

### 4.4 并发上限利用（事实陈述，不含建议）

- status.md「并发配置（阶段 5）」五字段与合并事实一致：起始并发数 `3` / 硬上限 `5` / 当前有效上限 `5` / 累计槛位释放次数 `5` / 已派发总数 `20`；**被后续合并真实更新过**——有效上限由 `15:37:30` 的 `3` 在 `16:04:30`（首波两 PR 合并、释放 0→2）变为 `5`（触硬上限），末次释放至 `5` 发生在 `18:16:30`；与 history 的三次槛位释放记录逐条相符 ✔（派发总数一项另有明细偏差，见 §5-2）。
- 实测同时在飞 PR 数上限 = **2**（最大反链 = `{pr-001, pr-004}` 与 `{pr-002, pr-005}`）；**起始并发数 3 与有效上限 5 均从未成为约束**，空闲槛位 3 属结构性空置（不存在可补位对象）。

### 4.5 观察项（非不一致）

`pr-002` 验收 PASS（`16:33:00`）到合并（`16:53:15`）间隔 **20m15s**，与 pr-005 同批合并。因 pr-003 的另一条依赖 pr-005 直到 `16:52:00` 才 PASS，pr-003 在 `16:52` 之前无论如何不能启动 ⇒ 该等待**未延长关键路径**。

---

## 5. 发现的不一致（逐条，只列事实不判严重性）

1. **`history.md` 缺 3 条派发条目**：存在「收到报告 · verifier」（`16:33:00` pr-002、`16:52:00` pr-005）与「收到报告 · dev」（`16:46:00` pr-005 定点修复），但 **无对应「派发 · verifier」条目（2 条）与「派发」定点修复条目（1 条）**；status.md 明细声明这些均已派发。⇒ 派发事实可推定（报告在场），**派发时刻无法从一手记录核实**。
2. **status.md「已派发总数 20」的明细与 `history.md` 实际条目分布不一致**（两边合计同为 20，但类别错位）：
   - 明细声称 = 首波 planner×2+dev×2+verifier×2（6）+ 次波 planner×2+dev×2+verifier×2+定点修复×1（7）+ 末波 planner×1+dev×1+verifier×1（3）+ pr-planner×2 + progress-observer×2 = 20
   - history 实际 = 首波 6 + **次波 4**（planner×2+dev×2；verifier×2 与定点修复×1 无条目）+ 末波 3 + **pr-planner 3 条**（`15:06:30`/`15:19:57`/`15:33:00`）+ **阶段 4 Gate verifier 2 条**（`15:14:50`/`15:26:40`，明细未列）+ progress-observer 2 = 20
   - 另：pr-planner 第 4 轮（`16:37:00` 报告，`2026-09-14-pr-planner-round4.md` 在场）同样**无派发条目**。
3. **`history.md` 末两条条目标注时间晚于本机当前时间与对应一手时间戳**：条目标注 `18:15:30`（verifier 报告）/ `18:16:30`（槛位释放），而本机 `date` = `18:11:27`~`18:12:28`、`clarifications/verify-pr-003-20260914.md` mtime = `18:08:55`、merge `a975ce8` 提交时刻 = `18:09:13`、末次提交 `7651040` 提交时刻 = `18:10:50`、`history.md` mtime = `18:10:28` ⇒ 标注时间**超前 4~7 分钟**。同类现象第 2 次快照已登记（「文内时间戳为写入方自述，非文件系统时间」），本次量级更大；**合并时刻一律以 git 提交时间为准**。
4. **阶段 6「已派发」无一手记录**：status.md「当前阶段 = 独立验证（阶段 6）· 已派发」+ 更新日志末条，但 `history.md` 末条为 `18:16:30` 槛位释放，**无阶段 6 派发条目**；`verify-stage6-20260914.md` 在工作区与 HEAD 树均不存在 ⇒ 「已派发」目前只有 status.md 一处自述。
5. **「合并后全量 361/361 绿」缺完成态一手记录**：`history.md` 末条原文为「**合并后全量套件实跑中**」，而 status.md 阶段 5 备注与「更新日志」、以及提交 `7651040` 的 message 均写「合并后全量 **361/361**」。可回溯的最强一手证据是 dev 在 **PR 分支**上的 `361/361`（`18:07:00` 报告，提交 `7c123ad`/`92a1bbb`，**早于** `a975ce8` 合并）；verifier 的 `361/361`（`verify-pr-003`）基线亦为 `b54f143`（合并前）。⇒ 「合并后」这一时点的绿色无独立记录（本角色未复跑，原因见 §6-1）。
6. **（正向闭合，登记以免遗漏）** 第 2 次快照登记的「status.md / history.md 未提交 git（`git status` 显示 ` M`）」已闭合：两文件已在 `7651040` 入库，当前 `git status --porcelain` **为空**。
7. **其余声称逐条核实一致（列出以免遗漏）**：5 条 merge 真双亲 ✔；5 个 PR 分支与 worktree 已清理 ✔；5 份验收报告结论 PASS 与计数（pr-001 委托 5 条 / pr-004 6 条 / pr-002 12 条 / pr-005 14 条 / pr-003 16 条 + 任务 T1~T8 69 条判据）与 status.md 逐条相符 ✔；阶段 4 首轮 FAIL（pr-003 A1/A2）⇒ 第二轮 PASS 30/30 ✔；status.md 登记的开放偏差在产物中真实存在——`architecture.md:704` §9.4.1 标称「既有测试面 · 最小更新（6 文件）」且 6 行清单中**不含** `oamp/test/config-file.test.js`（D-7）✔、`launcher.js:135` 的 `prompt` 守卫在场（D-7′ 已闭合）✔、`oamp/test/status.test.js:157` 确为时序敏感断言 `assert.ok(Date.parse(r2[3]) > Date.parse(r1[3]))`（D-w2-3）✔；`optionId` 在 `agent.js` 钩子面出现（D-w2-1 的裁决口径已落地）✔。

---

## 6. 无法核实项

1. **「合并后 361/361」的合并时点绿色**：无 CI / 回放记录，`history.md` 无完成条目。本角色**未独立复跑**全量套件——理由：观测期间阶段 6 验证正在同一棵工作树上做进程级探针，复跑会引入负载抖动与端口/桩进程竞争，可能污染该验证的一手证据。⇒ 登记为未核实，而非不一致。
2. **各 PR 验收判据的实质正确性**：只核实了报告件在场、结论行、汇总计数与偏差表条数，**未逐条重判**（重判属 verifier 职责面，不在本角色范围）。
3. **用户裁决的真实性**（阶段 1/2/3 的 `user_confirmed`、阶段 5 三波的 `[model_inferred]` 裁决）：唯一记录是 `clarifications/*-verdicts.md`，本身为执行方落盘文件，非独立信道。
4. **`history.md` 时间标签的绝对时刻**：可作为先后顺序参考，不可作为时刻证据（§5-3）；全迭代的「派发/报告时刻」均只有该单一信道，合并时刻有 git 提交时间可交叉。
5. **阶段 6 终态**：观测时该验证进行中，`verify-stage6-20260914.md` 未落盘，其结论/偏差/下一迭代候选均不可核实。
6. **`clarifications/probes/` 内探针脚本与原始输出**：仅核目录存在，未核内容与可复现性。
7. **「已派发总数 / 累计槛位释放次数」的绝对值**：只能由 `history.md` 条目与 status.md 自述交叉；除合并提交可核（3 次释放 ↔ 3 批合并）外，派发条目数本身仍是执行方自述文件。

---

## 附录 · 任务特别核实项逐条判定

| # | 特别核实项 | 判定 | 一手证据 |
|---|---|---|---|
| 1 | 5 条 merge 是否真实存在且为真双亲 merge | **是** | `git cat-file -p` 每条各两行 `parent`：`2a2d979`→`e3f8a62`+`263c787`；`f1097ae`→`2a2d979`+`28acecb`；`4777bf0`→`c66f0d4`+`3dc14f7`；`b54f143`→`4777bf0`+`ed01648`；`a975ce8`→`b41933c`+`92a1bbb`；5 条均在 `iteration/0022-…` 的 `git log` 上，且每个第二亲代为对应 PR 分支顶端 |
| 2 | 每个 PR 核心产出文件是否真在迭代分支上 | **是** | `oamp/src/protocol.js`（`1082748`）/ `rpc-client.js`（`1082748`）/ `oneshot-client.js`（`1082748`）/ `launcher.js`（`263c787`）/ `oamp/web/app.js`（pr-004 增量面在场）/ `oamp/test/zero-intrusion.test.js`（`7c123ad`）均在 `git ls-tree -r iteration/0022-…` 中，且引入提交为 PR 分支提交 |
| 3 | 5 个 `feat/0022-pr-00X-*` 分支与 worktree 是否确已清理 | **是** | `git branch -a --list '*feat*'` 空；`git branch -a` 仅 3 条（迭代 / main / origin/main）；`git worktree list` 仅 2 条；磁盘 `.pb-agents/worktrees/` 仅剩迭代工作区目录 |
| 4 | 并发真实执行证据（首/次波交错 + status 五字段被真实更新） | **是** | 交错：首波两 verifier 同刻派发（`15:56:43` ×2）、两 dev 报告相隔 6m45s；次波 pr-002 报告/验收期间 pr-005 仍在 dev（`16:46` 才完成）。配置更新：有效上限 `3`（`15:37:30`）→ `5`（`16:04:30`）、释放次数 `0→2→4→5`，与三批合并的 git 时刻 `16:03:39` / `16:53:15` / `18:09:13` 一一对应；实测同时在飞 PR ≤ 2（上限从未成为约束） |
| 5 | 迭代目录在 git 中的跟踪与提交情况 | **已全部入库** | 迭代目录磁盘 **68** 文件 = worktree 内 `git ls-files` **68**；`git status --porcelain` 空；迭代分支领先 main 24 提交，迭代目录在 main 上 0 文件（未合并 main）；末次提交 `7651040`（`18:10:50`）含 `verify-pr-003-20260914.md` + `history.md` + `status.md` |
| 6 | 任何「status.md 声称 vs 一手记录」差异 | **6 条已列入** | 见 §5 第 1~6 条（含 1 条正向闭合 × 明细错位 × 未来时间标签 × 阶段 6 无记录 × 合并后绿色无完成记录） |

---

**下次快照触发条件（按工作流契约）**：阶段 6 验证结论落盘后（`verify-stage6-20260914.md` 出现）应再采一次，以核实阶段 6 的声称与迭代收口（合并回 main、工作区清理）的 git 一手状态。
