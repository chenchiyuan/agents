# 需求合同：agent 启动服务与协议层（agent launcher and protocol layer）

**迭代**: 0022-agent-launcher-and-protocol-layer
**阶段**: 1（需求收敛）
**产出者**: demand
**版本**: v1.0.0（**已收敛：第 1 批 4 项裁决（J1~J4）+ 第 2 批 9 组裁决全部 `user_confirmed`；`model_inferred` 归零；`[待裁决]` 归零**）
**配套记录**: `clarifications/2026-09-14-kickoff-discussion.md`（起点材料）、`clarifications/2026-09-14-demand-round1.md`（第 1 轮提案 / 待裁决 / 方案雏形询问全文）、`clarifications/2026-09-14-demand-round1-verdicts.md`（用户裁决原件）、`clarifications/2026-09-14-demand-round2.md`（本轮回收过程与最终六维诊断）

---

## 文档状态

**第一段（澄清依据）完整**；**第二段（需求结论）可交付**。

标注体系（全文一致）：

| 标记 | 含义 | 是否生效 |
|---|---|---|
| `user_confirmed` | 用户原话或用户裁决直接支撑 | **生效** |
| `实测` | 本会话亲读源码 / 读探针脚本 / 此前主 agent 真实探针得到的事实 | 事实 |
| `[架构待填]` | 需求已定「必须有什么」，形态 / 选型 / 签名留阶段 3 | 需求部分生效 |
| ~~`[model_inferred]`~~ | **本版归零**：第 1 轮 6 项提案全部被裁决回收（P1 按用户改写重写口径，P2~P6 采纳） | — |
| ~~`[待裁决]`~~ | **本版归零**：V1 = (a)、V2 = (a) | — |

**会话限制（硬性）**：demand 运行在**无实时对话通道**的子 agent 中，**零自问自答**。第 1 轮 13 个问点（6 提案 + 2 待裁决 + 5 方案雏形询问）经主 agent 真实阻塞式转呈用户，用户逐项裁决（记录：`clarifications/2026-09-14-demand-round1-verdicts.md`）。第 2 轮**只做回收与闭合，不新增问点**。

**本迭代对象（产品）**：`oamp`（本机 Web 控制台 + HTTP/SSE 接口面 + 集群 / agent 运行时）。术语：「hub 侧」= `oamp` 的 Web 控制台与 SSE/HTTP 接口面。**本迭代不改 omp / harness 侧源码与协议**（N2）。

**版本轨迹**：v0.1.0（第 1 轮：草稿 v1 + 六维诊断 + 6 项 `[model_inferred]` 提案 + 2 项 `[待裁决]` + 5 项方案雏形询问）→ **v1.0.0（本版：第 2 批 9 组裁决落定，全文件收敛）**。

---

# 第一段 · 澄清依据

## 1. 起点材料清单（读取范围与相关性判断）

| 材料 | 读取范围 | 相关性判断 |
|---|---|---|
| `clarifications/2026-09-14-kickoff-discussion.md` | 全文 | **主起点材料**（用户原话 + 探针证据 + 现状代码事实 + 第 1 批裁决 + 主 agent 分层建议 + 风险表）；§二 / §三 按一手证据引用，**§五 按「非结论」处理**（其三条零侵入判据草案**已被用户改写口径取代**，见 §5 登记⑥），§六 按「待落定议题」处理 |
| `clarifications/2026-09-14-demand-round1.md` | 全文 | 第 1 轮产出（一手事实复核 F7~F19、六维诊断、提案 P1~P6、待裁决 V1/V2、方案雏形询问 Q1~Q5、登记①~⑤）；**本轮回收对象** |
| `clarifications/2026-09-14-demand-round1-verdicts.md` | 全文 | **第 2 批用户裁决（本轮主输入）**：P1 口径改写 / P2~P6 采纳 / V1=(a) / V2=(a) / Q1~Q5 回应 / N1~N11 保留 |
| `/tmp/oamp-rpc-probe.mjs`、`/tmp/oamp-rpc-gate-probe.mjs` | 全文 | 主 agent 探针脚本（未落仓库）；其 argv 面证据构成 F6b |
| `oamp/README.md` + `oamp/src/*`、`oamp/web/*`、`oamp/test/*` | 相关章节 / 符号级 | **现状事实来源**（F7~F19） |
| `docs/iterations/0021-confirmation-inbox-and-event-push/demand.md` | 全文 | 「不退化」的基线（确认收件箱与权限门语义来源）；F19 佐证其成果已在本工作区可见 |
| `docs/multi-omp-agent-protocol.md` | 全文（605 行） | **自身标注「方案建议，待实现阶段确认」**；主题是**实例间消息层（OAMP Router）**，与本迭代（启动面 + 协议面）不同层。两处相邻已登记（登记⑤）：其 §4 `instances[]` 字段集与本迭代 L1 profile 概念相邻，其 §10 / §14.1 的「adapter 层」命名与本迭代 L2 相撞。本迭代按 **N11** 划边界，不做合并 |

## 2. 用户原始表述（逐字）

> omp 支持 rpc 模式：RPC 模式 — `omp --mode rpc`。启动特点：stdio 上的原生 JSON-RPC 服务器，先发 ready 帧再接受命令；是常驻服务，客户端关闭 stdin / 断开 / 进程中断才退出。`@file` 启动参数被拒绝，输入必须走协议。适合场景：把 omp 嵌入自研应用/服务，通过程序化协议驱动完整代理能力。
>
> 相比目前的启动，rpc 模式似乎更适合。我现在需要系统架构封装几个层面：
>
> 1. agent 的启动服务，我的希望之后能接入不同的客户端，比如 omp，claude，codex。此服务封装不同 agent 的启动，对外提供统一的接入。
> 2. agent 通讯协议支持，之后可以选择多协议。也就是有协议层封装，走什么协议都可以指定。比如我希望现在接入 rpc 协议（omp 客户端支持的 rpc 协议），hub 与 agent 之间通过 rpc 服务通讯（默认）。希望以后的修改对协议指定是零侵入的。
> 3. 请确认：rpc 协议的信息传递是否是双向；是否是流式。我的目标是希望看到 agent 执行过程的信息，也就是 hub 侧能展示 agent 的部分思考/运行过程。
>
> 后续追加指令：「请先与我沟通方案，然后使用 workflow-pb 的工作流开发」

**从原话片段直接读出的需求级约束**：

| # | 原文片段（逐字） | 直接读出的约束 |
|---|---|---|
| U-1 | 「rpc 模式似乎更适合」+「stdio 上的原生 JSON-RPC 服务器，先发 ready 帧再接受命令；**是常驻服务**」 | 常驻链路从 `omp acp` 换为 `omp --mode rpc`（**常驻**语义对齐） |
| U-2 | 「agent 的启动服务…之后能接入不同的客户端，比如 omp，claude，codex。此服务封装不同 agent 的启动，对外提供统一的接入」 | 需要**独立的启动服务层**：把「怎么起一个 agent 进程」与「与它通讯」解耦，并以宿主（客户端）为单位可扩展 |
| U-3 | 「agent 通讯协议支持，之后可以选择多协议…有协议层封装，走什么协议都可以指定…希望以后的修改对协议指定是零侵入的」 | 需要**协议层**（可插拔）+ **协议指定**的可配置面 + **零侵入**（判据口径由用户在裁决中改写为依赖注入，见 A1 / W8） |
| U-4 | 「请确认：rpc 协议的信息传递是否是双向；是否是流式。我的目标是希望看到 agent 执行过程的信息，也就是 **hub 侧能展示 agent 的部分思考/运行过程**」 | 目标 = **hub 侧可见执行过程**（思考 / 运行增量）；前两问已由实测闭合（F1~F5） |
| U-5 | 「请先与我沟通方案，然后使用 workflow-pb 的工作流开发」 | **流程约束**（本次"怎么跑"），**非产品需求** ⇒ 不进第二段（登记④） |

## 3. 用户裁决（第 1 批 4 项 + 第 2 批 9 组，逐条 `user_confirmed`）

### 3.1 第 1 批（开工前裁决，J1~J4）

记录原件：`clarifications/2026-09-14-kickoff-discussion.md` §四。

| 项 | 用户裁决（逐字要点） | 落入位置 |
|---|---|---|
| J1 | **本迭代范围**：L1 启动服务（profile）+ L2 协议层 adapter 接口 + omp 的 rpc 适配器落地（含权限门 / 思考 / 工具增量映射）；**claude 与 codex 只留 profile 结构与能力位，不做真实接入** | W1 / W2 / W3 / W4 / N1 / R6 |
| J2 | **协议切换策略**：**rpc 默认 + acp 保留（双轨）**——默认走 rpc，配置可切回 acp 兜底；两条链路都要维护与测试 | W2 / W3 / W5 / N3 / R1 |
| J3 | **过程可视化范围**：**实时展示 + 不入库**——思考 / 文本 / 工具参数与输出的增量走运行时 SSE 通道展示（等价现有 `task_update` 语义），不落 SQLite，不改「一次问答恰两条记录」边界 | W7 / N4 / E3 |
| J4 | **一次性路径**：**纳入为 oneshot 适配器**——把 `omp -p` 一次性执行建模为 L2 的 oneshot 适配器（无会话语义）；shell 执行器保持独立、不纳入协议层 | W6 / N7 / E5 / M5 |

### 3.2 第 2 批（第 1 轮转呈件裁决）

记录原件：`clarifications/2026-09-14-demand-round1-verdicts.md`（主 agent 阻塞式取得，2026-09-14 10:56）。

| 项 | 用户裁决 | 落入位置 |
|---|---|---|
| **A1**（P1 零侵入判据） | **用户改写了口径**（原话逐字）：「这里应该使用**依赖注入**模式，指定协议（此协议如果已经实现），通讯层就应该使用该协议。」⇒ 零侵入判据**按依赖注入语义重写**：**注入点决定协议实现**、**消费层不自行构造 / 选择协议实现**、**切换协议只改注入配置且消费层代码不变**；「**指定已实现的协议即生效**」是**硬要求**（不是尽力而为）。**kickoff §五 的三条判据原文未被采纳**（旧措辞不作为结论），「只留判据②」亦未被接受 | W8 / E2 / M1 |
| **A2**（P2 协议指定粒度与入口，采纳） | 协议指定 = **配置面 + 角色级覆盖 + 全局默认 + 内置 `rpc`**；**选择域 = 常驻会话协议（`rpc` / `acp` 二选一）**，`oneshot` **不在选择域**；**不做** per-chat / per-request 指定；**不做**控制台可切 | W2 / M2 / N3 |
| **A3**（P3~P6 四项全部采纳） | **P3** L2 需求级语义面 = 会话四动作（开会话 / 提示 / 取消 / 关闭）+ 三类增量回调（文本 / 思考 / 工具）+ 两类反向请求回调（审批门 / 宿主 UI）+ **能力位显式声明**（缺能力必须显式降级）；**P4** 展示落点 = 复用按对话的运行时通道（等价 `task_update` 语义），**不新增 UI 载体、不做透出开关 / 过滤策略、不入库**；**P5** 确认面口径 = **字段集合与语义等价、值域随协议能力声明**（RPC 恒二元 `Approve` / `Deny`），**不要求跨协议逐字等价**；**P6** 验收证据 = **rpc 真实 omp 链路端到端一条**（思考增量可见 + 受门禁调用一次上浮与裁决 + 不入库核查）**+ acp 兜底链路回归一条** | W3 / W4 / W7 / M3~M6 / E3~E6 |
| **A4**（V1 acp 是否能力对齐） | 选 **(a) 仅保留 + 显式声明降级**：acp **保持现状**（**不补**思考 / 工具输出增量），能力位声明 `thinking=false`；「不退化」的基线 = 0021 之后的 acp 行为 | W5 / E3 / E5 / N12 |
| **A5**（V2 零侵入判据适用范围） | 选 **(a) 仅生产消费层**：判据只约束 `src/` 中**协议层（协议实现与注入点）以外的生产源码**；**测试面**按 profile 期望值断言（6 个测试文件的 argv 断言作既有回归资产，做**最小更新**），**不纳入**判据 | W8 / E2 / R2 |
| **A6**（Q1~Q5 方案雏形询问） | 用户回应 **「都没有额外想法」** ⇒ **第六维无缺口**。对 profile 字段集与能力位清单、增量粒度与展示形态、RPC 二元门呈现、acp / oneshot 降级表现、零侵入验收判法**均无预设想法**，按已采纳提案与后续架构阶段处理。**该回应不构成对任何具体机制 / 参数的用户确认** ⇒ 相关形态一律标 `[架构待填]` | M3~M6 / §6 维度 6 |
| **A7**（N1~N11 边界） | **全部保留** ⇒ 11 条边界由 `[model_inferred]` 转为 `user_confirmed` | N1~N11 |

## 4. 一手事实（`实测`）

### 4.1 探针实测（主 agent 真实执行 `omp --mode rpc`；脚本 `/tmp/oamp-rpc-probe.mjs`、`/tmp/oamp-rpc-gate-probe.mjs`）

| # | 事实 | 对需求的作用 |
|---|---|---|
| F1 | ready 帧 0.70s 到达（含 `protocolVersion` / `supportedProtocolVersions` / `maxFrameBytes`）；`negotiate_protocol{protocolVersion:2}` 成功 | RPC 面有**版本协商** ⇒ L2 需按协商结果声明能力 |
| F2 | `prompt` 返回 `{command:"prompt", success:true}` = **受理**，完成信号是 `agent_end{isTerminal}` | 「一轮结束」不能取受理回包（L2 轮次语义必须等终态事件） |
| F3 | 流式三类增量：`text_delta`×138、`thinking_delta`×120（首个 2.71s）、`toolcall_delta`×23 + `tool_execution_update`×2 | 直接支撑 W3 / W7：**思考与工具输出可被 hub 消费**，无需改 omp |
| F4 | 双向：流式中发 `get_state` 得 `response`；流式中发 `steer` 产生第二个 `agent_start` / `turn_start` | RPC 具备运行中干预能力（→ N6 仅作能力位声明） |
| F5 | 反向请求：`extension_ui_request`（带 `id`，需 `extension_ui_response` 回执）；文档另有 `host_tool_call` → `host_tool_result`、`host_uri_request` → `host_uri_result` | L2 必须提供**反向请求回调**（P3，A3 采纳）；宿主工具面 [架构待填] |
| F6 | **审批门上浮**：`extension_ui_request{method:"select", title:"Allow tool: bash\nCommand: echo hello-gate", options:["Approve","Deny"]}` → host 回 `{type:"extension_ui_response", value:"Approve"}` → 工具执行 | RPC 的门 = **单门 + 二元选项**（对照 F16）⇒ W4 的映射目标与「不重不欠」判据（E4） |
| F6b | gate 探针 argv = `--mode rpc --model … --approval-mode always-ask --thinking off` 正常启动并上浮门 | RPC 面**接受 `--approval-mode`** ⇒ 既有「档位在客户端侧实现」的模型可平移到 RPC |

### 4.2 本次核对（本会话亲读 oamp 源码；路径相对工作区根）

| # | 事实 | 证据位置 |
|---|---|---|
| F7 | **argv 构造与协议语义同处一个类**：`omp acp` 的 argv 在协议客户端类内部拼装 | `src/acp-client.js:137-144`（`['acp','--no-skills','--no-rules', (--no-tools)?, '--no-session', (--model)?, (--append-system-prompt)?, (--approval-mode always-ask)?]`） |
| F8 | **同一 argv 知识第二次落笔**：一次性路径 argv 在任务面另拼一份 | `src/agent.js` `runOmpTask()`：`['-p','--no-session', (--no-tools)?, (--model)?, (--append-system-prompt)?, toolsOn ? ('--approval-mode' + (permission==='deny'?'always-ask':'yolo')) : 无]` |
| F9 | **思考增量被客户端显式丢弃**：`prompt()` 的 `_chunkHandler` 只接受 `agent_message_chunk` | `src/acp-client.js` `_chunkHandler`（起点材料记为 :203） |
| F10 | **消费层直接 import 协议客户端**（今天不存在协议注入点），协议参数一路穿透 | `src/context-pool.js` 文件头 `import { AcpClient, AcpError } from './acp-client.js'`；构造参数 `bin / cwd / roleFile / tools / permission` |
| F11 | **三条执行路径分散在任务面** | `src/agent.js` `runDaemonTask()` / `runOmpTask()` / `runShellTask()` + `runTask()` 分派 |
| F12 | **确认面信封 7 字段** + 全局 SSE `confirmation` 帧 + 两条路由 | `src/web.js`：`GET /api/confirmations`（重建面，不发帧）、`POST /api/confirmations/<id>/decision`（原子取出；选项不在集合内 → 400）、`handleDeliver` 的 `confirmation_request` / `confirmation_cancelled` 分支；字段 = `confirmation_id / chat_id / agent_id / tool / title / options / created_at` |
| F13 | 通知事件类型是**封闭 3 类** | `web/notify.js:12` `['chat_completed','chat_failed','confirmation_required']` |
| F14 | **过程增量的既有通道 = 按对话的 `task_update`（运行时、不入库）** | `src/web.js` `handleDeliver` 的 `task.update` 分支（`{kind, text, line}` → SSE `task_update`）；agent 侧 `src/agent.js` `onChunk` → `{kind:'chunk'}` |
| F15 | **ACP 审批门的工具名提取原语已存在，与 RPC 实测的 `title` 同源**（omp dist 同一处产出 `Allow tool: <name>\n…`） | `src/acp-client.js:23` `APPROVAL_MESSAGE_PREFIX = 'Allow tool: '`、`:30-34` `readApprovalToolName()`、`:620-623` 审计 `option` |
| F16 | **ACP 两道门 vs RPC 一道门**：ACP 有结构化 `session/request_permission`（带 `option` / `toolCallId`，另有「一次放行抵扣一次审批门」的凭据模型）与审批门（无 `toolCallId`，工具名取自 message 首行）；RPC 实测只有 `extension_ui_request` select 一道 | `src/acp-client.js:434`、`:466-475`、`:521-570`；对照 F6 |
| F17 | **argv 字面量断言散布 6 个测试文件**（测试面自身携带协议知识） | `test/acp-daemon.test.js`（:539-588）、`test/context-pool.test.js`（:531-534）、`test/tool-permission.test.js`（:329-332、:502-527）、`test/web.test.js`（:557-574）、`test/project-workspace.test.js`（:1003）、`test/confirmation-roundtrip.test.js`（:703-711） |
| F18 | 测试面规模：**30 个测试文件**；`web.test.js` 1640 行、`project-workspace.test.js` 1329、`call-protocol.test.js` 1094、`tool-permission.test.js` 996、`confirmation-roundtrip.test.js` 741、`acp-daemon.test.js` 740 | `oamp/test/` 计数 |
| F19 | **0021 的成果已在本工作区可见**（确认收件箱三栏、`/api/confirmations`、`notify.js` 三事件、`confirmation-*.test.js`）⇒ 「不退化」有真实基线可对照 | `src/inbox.js`、`src/web.js`、`web/index.html`、`web/app.js`、`web/notify.js`、`test/confirmation-*.test.js` |

### 4.3 事实 → 需求的直接后果（`实测` 支撑的推导，非产品判断）

- **F7 + F8 + F10 + F11** ⇒ U-2 / U-3 的痛点实测成立：换协议要同时动三处（argv / 消费层 import / 任务面分派），且 argv 知识有两份；**F10 尤其点明今天不存在协议注入点** —— 这不是"想优化"，是**接入成本线性增长** + **协议实现无法被替换**。
- **F9** ⇒ U-4 的目标在 **acp 链路**上今天**物理上不可达**（思考在客户端被丢弃）⇒ U-1 不只是启动方式偏好，它同时是**可观测性的前置条件**；默认走 rpc（J2 / A2）正是为此，acp 侧不承诺对齐（A4）。
- **F15 + F6** ⇒ 起点材料 **R1 的严重度下调**（登记②）：RPC 的 `title` 形状与 ACP 审批门**同源**，`tool` 名提取可复用既有原语；剩余差异只在**选项值域**。
- **F16** ⇒ RPC 是**单门二元**：一次受门禁调用在 RPC 下只产生一条上浮请求 ⇒ W4 的「不重不欠」是**可在验收层直接数出来**的判据（E4）。
- **F14 + F12 + F19** ⇒ 过程增量与确认面**都已存在挂靠点**：本迭代是"接线 + 抽象"，不是"新造语义" —— 这支撑 N2（不改 omp）。
- **F17 + F18** ⇒ 起点材料 **R4 可量化**（登记③），且「零侵入」判据的适用范围成为必须裁决的口径问题 —— **已由 A5 裁决闭合**（仅生产消费层；测试面最小更新）。

## 5. 登记（因裁决 / 事实产生的结构化修订，非冲突）

| # | 登记内容 | 依据 |
|---|---|---|
| ① | **L2 的三个 adapter 不在同一维度上**：`rpc` / `acp` 是**互斥的常驻会话协议**（协议"选择"发生在这一维），`oneshot` 是**执行方式**（无会话语义，由「一次性」语义选中，与常驻协议并存）。第二段的 W2 / M2 / N3 按此措辞，避免后续阶段把"协议"混为三选一 | 用户原话第 1、2 点（U-2/U-3 说的是"协议"）+ J4（一次性建模）+ F8/F11（一次性路径 argv 与常驻路径本就分离）+ A2（选择域裁决） |
| ② | **起点材料 R1（RPC 审批门信息形状不同）严重度下调**：`tool` 名提取原语已存在且与 RPC 的 `title` 同源（F15），剩余差异仅在**选项值域**（二元 vs 结构化） | F6 + F15 + F16 |
| ③ | **起点材料 R4 量化**：既有测试面本身携带协议知识（6 个文件含 argv 字面量断言，F17；测试面 30 文件 / 头部 700~1640 行，F18）⇒ 抽象层移动代码必然触发测试最小更新 | F17 / F18 + A5 |
| ④ | **U-5 是流程约束**（"怎么跑"），不进第二段的「做什么」 | 体例同 0021 的 U-6；主 agent 在起点材料中已按此定性 |
| ⑤ | **起点材料的相邻 / 重叠登记**：`docs/multi-omp-agent-protocol.md`（方案建议态）的 §4 `instances[]` 配置与本迭代 L1 profile 概念相邻，其 §10 / §14.1 已使用「adapter 层」这一命名并与本迭代 L2 相撞。本迭代**不实现**其 Router / 消息层，且 L1 profile 与其 `instances[]` **不承诺合并**（归属不同层：启动面 vs 消息面） | 该文全文 + J1 范围 + U-2/U-3 |
| ⑥ | **「零侵入」判据口径改写登记**：第 2 批裁决 A1 把判据口径从「源码里出现不出现协议名」（kickoff §五 草案）**改写为依赖注入语义**——注入点决定协议实现、消费层不自行构造 / 选择协议实现、切换只改注入配置且消费层代码不变。据此：kickoff §五 三条判据原文与「只留判据②」两个旧口径**均作废**；W8 的 D-1~D-3 判据由新口径推导（并由 A5 限定适用范围） | A1 + A5；旧措辞不作为结论 |
| ⑦ | **边界编号顺延登记**：A4（V1=(a)）直接产生一条新的「不做什么」，记为 **N12**；**N1~N11 的原编号与内容不变**，仅来源标注由 `[model_inferred]` 转为 `user_confirmed`（A7）。N12 不改变任何既有编号的指代 | A4 + A7 |

## 6. 六维诊断（收敛后 · 第 2 轮）

| 维度 | 本轮诊断结论 |
|---|---|
| **1 · 明确性** | **无新缺口**。第 1 轮 4 处缺口全部闭合：①「零侵入」→ W8 的 D-1~D-3 机械判据（A1，旧口径作废）；②「指定协议」的粒度与入口 → M2（配置面 + 角色级覆盖 + 全局默认 + 内置 `rpc`；选择域封闭；不做 per-chat / 控制台可切，A2）；③「hub 侧展示过程」的落点 → M3（复用按对话运行时通道，不新增载体、不做开关，A3/P4）；④「协议层」与「一次性 / shell 执行方式」混谈 → 登记①（A2 落定选择域）。残留的形态性未定项（透出粒度、呈现形态、能力位最终集合与签名、降级表现、宿主工具面）**不是需求歧义**，已统一标 `[架构待填]`，且其需求级约束来自已采纳的 A3 提案原文 |
| **2 · 逻辑闭环** | **无新缺口**。why → what 反推逐条闭合：接入成本（V1）→ W1 / W2 / W8；可观测性（V2）→ W3 / W7 / W5；形态匹配（V3）→ W3 作默认链路；不退化（V4）→ W4 / W5 / W6。**第 1 轮唯一脱节点已闭合**：「看到执行过程」与「acp 物理上看不到思考」（F9）之间的落差，由 **A4 = (a)** 明确定性为「目标在**默认 rpc 链路**达成，acp 为兜底、不承诺能力等价，其差异以能力位 `thinking=false` 显式声明」⇒ 不再是脱节，而是**用户已裁决的能力刻度**。反向检查：无多余条目（每条 W 都能追到 V1~V4 之一） |
| **3 · 边界完整性** | **无新缺口**。N1~N11 由用户逐条**保留**（A7）⇒ 全部 `user_confirmed`，且每条保留「为什么这次不做」；A4 直接产生 N12（不为 acp 补齐增量）。**AI 推导且未获确认的边界数量 = 0** |
| **4 · 内部一致性** | **本轮新闭合一处，无残留矛盾**。① **DI「指定即生效」× A4「acp 不补能力」**：一致——`acp` 是**已实现**的协议（J2 保留），指定它即生效；其能力差异以能力位显式声明，不构成"指定不生效"；② **注入点 × N3「不做动态切换」**：一致——注入发生在**起会话时**，切换 = 下一次会话改注入配置（在飞上下文不迁移，N9）；③ **「指定未实现的协议」这一括注路径**：本迭代**不存在合法入口**——选择域 = {`rpc`, `acp`} 且两者本次均落地（J1 + J2 + A2），越界值处理属既有配置校验范畴，不进需求结论；④ 不入库 × 「一次问答恰两条记录」：一致（J3 明文保留；F14 的既有通道本就是运行时事件）；⑤ rpc 默认 × 既有 `permission` 两档：一致（**F6b 证明 RPC 面接受 `--approval-mode`**，档位仍在客户端侧）；⑥ 一次调用一条确认项 × RPC 单门：一致（F16）；⑦ DI 判据适用范围（A5 仅生产消费层）× 测试面 argv 断言（F17/F18）：一致——测试面明确不纳入判据 |
| **5 · 可验证性** | **无新缺口**。E1~E7 全部过裸判定测试（第二段 §5 每条给「一个没参与讨论的人，不问作者即可判真假」的方式）。第 1 轮两处「依赖裁决」的标注已回收：**E3 收窄到默认 rpc 链路**（A4），**E2 的作用范围 = 生产消费层**（A5）。E2 的判据改为 D 判据口径后仍可机械复核：改注入配置 → `git diff` 消费层为空；被指定协议确实生效（行为可观测） |
| **6 · 方案雏形** | **无新缺口**。Q1~Q5 已覆盖第 1 轮全部 8 条「做什么」条目，用户明确回应「都没有额外想法」（A6）⇒ 该维不再跑出问点；且第 2 轮发生口径变化的两处（W2 的指定粒度与入口、W8 的判据口径）**均由用户本人给出**（A2 / A1 原话），不是 AI 单方面补写。A6 的回应**不被解释为对任何具体机制 / 参数的用户确认**，相关形态一律 `[架构待填]`（记录见 round2 §2） |

**停止条件达成**：连续一轮六维诊断（本轮）跑不出新缺口，且**无活跃冲突**（登记①~⑦ 均为派生修订、事实澄清或裁决落地，不是两条裁决相冲突）⇒ 需求收敛完成，本文件为可交付版本。

## 7. 来源标注统计（本版）

| 类别 | 数量 | 说明 |
|---|---|---|
| `user_confirmed` | 原话 5 段（U-1~U-5）+ **第 1 批裁决 4 项**（J1~J4）+ **第 2 批裁决 9 组**（A1~A7，其中 A3 含 P3~P6 四项，A7 含 N1~N11 十一条） | 用户说出口的内容或其直接裁决 |
| `实测` | F1~F6b（探针）+ **F7~F19**（本次核对，均带文件 + 符号 / 行号） | 本会话亲读源码 / 读探针实测 |
| `[model_inferred]` | **0** | 第 1 轮 6 项提案全部回收：P1 → 按 A1 改写口径重写（登记⑥），P2~P6 → A2 / A3 采纳 |
| `[待裁决]` | **0** | V1 → A4 = (a)；V2 → A5 = (a) |
| `[架构待填]` | **6 处** | F5 宿主工具面；M1 三层形态细节；M3 透出粒度与呈现形态；M4 门的呈现形态；M5 oneshot 降级表现；M6 最终能力位集合与签名 |
| 参考视角（不进结论） | 0 新增 | 第 1 轮 5 条仅存于 `clarifications/2026-09-14-demand-round1.md` §8，**不进入本合同的任何一段**；其中 ②③ 已分别被 A4 吸收 / 取代，⑤ 的实质已由 M7 以需求级要求承接 |
| 待实测（交阶段 3 首步） | 5 项（M-1~M-5） | 见 `clarifications/2026-09-14-demand-round1.md` §9；由 M7 承接 |

## 8. 未闭合声明

- **无活跃冲突**：J1~J4 与 A1~A7 之间无矛盾；登记①~⑦ 均为派生修订、事实澄清或裁决落地。
- 起点材料 **R1 已降级**（登记②）、**R4 已量化**（登记③）、**§五 判据草案已作废**（登记⑥）；R2 / R3 / R5 以第二段 §6 的 R1~R7 形态落定（R3 转 M7 待实测）。
- 阶段 1 三项推进条件（`roles/workflow-pb/workflow-pb.md` §阶段定义「推进条件」列）逐项核查：
  1. **两段均有内容 ✅**
  2. **所有 `model_inferred` 结论经用户确认 ✅**（`model_inferred` 归零；第 1 轮 6 项提案 + 11 条边界 + 2 项待裁决全部经用户裁决落定）
  3. **无活跃冲突 ✅**
- **本版可作为阶段 2（功能规格）的启动依据**。

---

# 第二段 · 需求结论

> 本段全部条目带来源标注，**无未生效项**。`[架构待填]` 表示需求已定「必须有什么」，形态 / 选型 / 签名留给阶段 3。

## 1. 目标与价值（为什么）

| # | 驱动 | 现状痛点（`实测`） | 推导出的需求 |
|---|---|---|---|
| V1 | **接入成本**（U-2 / U-3） | 单协议 + argv 与协议语义耦合同一个类 + 三条执行路径分散 + argv 知识两份 + 消费层直接 import 协议实现（F7 / F8 / F10 / F11）⇒ 换协议要同时动三处，且协议实现不可替换 | W1（启动服务层）+ W2（协议层与注入点）+ W8（零侵入契约） |
| V2 | **可观测性**（U-4） | 思考增量在客户端被显式丢弃（F9）⇒ 「hub 侧看到执行过程」在 acp 链路**今天物理上不可达** | W3（rpc adapter 映射思考 / 工具增量）+ W7（实时展示、不入库）+ W5（acp 兜底，不承诺能力等价） |
| V3 | **形态匹配**（U-1） | rpc 是常驻 stdio 服务、双向、流式（F1~F5 实测）⇒ 比一次性启动更适合被 hub 长期驱动 | W3 作为**默认**链路（J2 / A2） |
| V4 | **不退化**（J2 / J3 / J4） | 0021 的确认收件箱与通知语义建立在本链路上（F12 / F13 / F19） | W4（权限门跨协议映射）+ W5 + W6 |

> **价值**：agent 接入从"每接一个宿主改三处"变为"加一份 profile 数据"；hub 从"只看结果"变为"看得到思考与工具过程"（默认链路）；协议从"写死在客户端类里"变为"注入点决定、一处配置可切、消费层零改动"。

## 2. 做什么

| # | 条目 | 来源 |
|---|---|---|
| W1 | **启动服务层（L1）**：把「怎么起一个 agent 进程」**数据化**为 profile（宿主 / bin 解析 / argv 构造 / env / cwd / 模型 / 角色 / 工具 / 权限），对外提供统一起会话入口；**新增宿主 = 新增 profile 数据**（claude / codex 本迭代只留 profile 结构与能力位） | `user_confirmed`（J1 + A2 + U-2） |
| W2 | **协议层（L2）**：一个内部会话语义接口 + 可插拔协议实现；**协议指定入口唯一**（注入点，落在配置面；解析链 = 角色级 > 全局默认 > 内置 `rpc`）；**选择域 = 常驻会话协议（`rpc` / `acp`）**，`oneshot` 不在其中 | `user_confirmed`（J1 + J2 + U-3 + **A2**） |
| W3 | **omp 的 rpc 适配器落地（默认链路）**：映射**思考 / 文本 / 工具参数与输出**的增量，以及工具审批门；「一轮结束」以终态事件为准（不取受理回包） | `user_confirmed`（J1 + A3 + F2 / F3 / F6） |
| W4 | **权限门跨协议映射**：把 RPC 的工具审批门接进**既有的确认面**（7 字段信封 → 收件箱 → 裁决回路），一次受门禁调用**一条确认项（不重不欠）**；**口径 = 字段集合与语义等价、值域随协议能力声明**（RPC 恒二元 `Approve` / `Deny`；`tool` 名复用既有提取原语），**不要求跨协议逐字等价** | `user_confirmed`（J1 + **A3/P5** + F6 / F12 / F15 / F16） |
| W5 | **acp 协议实现保留（兜底链路）**：既有行为**不退化**（确认面语义 / 消息与状态流转 / 三事件通知）；注入配置可切回；**保持现状，不补思考 / 工具输出增量**，能力位声明 `thinking=false` | `user_confirmed`（J2 + A3 + **A4** + F9 / F19） |
| W6 | **oneshot 适配器**：把 `omp -p` 一次性执行建模为 L2 的**无会话语义**适配器 | `user_confirmed`（J4） |
| W7 | **过程可视化**：思考 / 文本 / 工具增量经**运行时 SSE 通道实时展示**（等价现有 `task_update` 语义），落在**既有对话详情**内，**不新增 UI 载体、不做透出开关 / 过滤策略**、**不落 SQLite** | `user_confirmed`（J3 + U-4 + **A3/P4** + F14） |
| W8 | **零侵入契约（依赖注入口径）**：协议实现以**注入**方式提供给通讯层；**指定已实现的协议即生效**（被注入的实现就是被使用的实现，硬要求）；消费层不自行构造 / 不自行选择协议实现；切换协议只改**注入配置**一处，消费层代码不变 | `user_confirmed`（U-3「零侵入」+ **A1 用户改写判据口径为依赖注入**；判据形式按该口径推导、范围由 A5 限定） |

### W8 的可机械复核判据（口径来源 = A1；适用范围 = A5：`src/` 中协议层（协议实现与注入点）以外的**生产**源码）

| 判据 | 内容 | 机械复核方式 |
|---|---|---|
| **D-1 注入点决定实现** | 协议实现的选取发生在且仅发生在**注入点**（注入配置 / 解析链）；通讯层实际使用的实现 = 注入点给出的实现（**指定即生效**，不是尽力而为，也不回落到内置默认） | 向注入点指定 `acp`（已实现）起一轮 → 该轮确实走 acp 实现；指定 `rpc` → 确实走 rpc。**指定后仍走内置默认即为不通过** |
| **D-2 消费层不自行构造 / 选择** | 生产消费层中不存在具体协议实现的构造、导入，也不存在按协议取值的分支选择（协议实现的知识不落在消费层） | grep 生产消费层源码：无具体协议实现的符号导入 / 构造；无按协议值的分支 |
| **D-3 切换只改注入配置** | 切换协议 = 改**一处注入配置**（解析链 = 角色级 > 全局默认 > 内置 `rpc`）；消费层源码 **diff 为零** | 切换前后 `git diff` 看生产消费层：为空即通过 |

## 3. 不做什么

> 第 1 批 / 第 2 批裁决覆盖全部 12 条（A7 全部保留 + A4 直接产生 N12），每条都给「为什么这次不做」。

| # | 边界 | 为什么这次不做 | 来源 |
|---|---|---|---|
| N1 | 不做 **claude / codex 的真实接入与实测**（只留 profile 结构与能力位） | 本迭代的验证载体是 omp 的 rpc / acp 双链路；未实测宿主的能力位属推测，按最小边界留在后续迭代 | J1 + A7 |
| N2 | **不改 omp / harness 源码与协议** | RPC 所需能力（流式 / 双向 / 门 / 版本协商）经实测已具备（F1~F6），消费既有能力即可 | A7 + F1~F6 |
| N3 | 不做**同对话内的动态协议切换**，也不做 per-chat / per-request 指定、不做控制台可切 | 协议是进程级语义，切换即失忆（N9）；需求只要求"可指定"且入口唯一（A2）；动态面会引入新 UI/API 与在飞状态迁移 | J2 + **A2** + A7 |
| N4 | 不做过程增量的**持久化 / 回放 / 检索**，不改「一次问答恰两条记录」 | 用户裁决：实时展示 + 不入库 | J3 + A7 |
| N5 | 不做过程展示的**开关 / 过滤 / 分级偏好** | 未被提出；先有信号再谈调优（体例同 0021 的 N4）；A3/P4 明文本迭代不做 | A3 + A7 |
| N6 | 不把 RPC 的 `steer`（运行中插话）做成本迭代的**产品功能**（仅作能力位声明） | 用户目标是"看到"而非"干预进程"；插话会牵动轮次语义与 UI，未提出 | U-4 + A7 + F4 |
| N7 | 不把 **shell 执行器纳入协议层** | 用户裁决：shell 保持独立、不纳入协议层 | J4 + A7 |
| N8 | 不改变**服务边界**（仍 `127.0.0.1`、零鉴权、不做多用户 / 跨机） | 沿用 0021 N1 的既有边界；本迭代不触碰接入面 | A7（0021 合同 N1） |
| N9 | 不做协议切换时的**在飞上下文迁移**（切换即重置，沿用既有 `context_reset` 提示语义） | 跨协议会话不可互换（ACP session ≠ RPC 会话），无迁移路径；切换是运维动作 | A7 + F10 |
| N10 | **不引入第三方依赖**（保持 oamp 零依赖） | oamp 既有约束（README：零依赖 Node.js v22 ESM） | A7 + `实测` README |
| N11 | **不实现 `docs/multi-omp-agent-protocol.md` 的 OAMP Router / 实例间消息层**；L1 profile 与该文 §4 的 `instances[]` 配置**不做合并**（仅保留字段可对齐的余地） | 该文自身标注「方案建议，待实现阶段确认」，主题是**消息面**（agent 间投递 / 注册 / 租约）而非启动面与协议面；并入会使本迭代范围发散（登记⑤） | J1 + A7 + 登记⑤ |
| N12 | **不为 acp 链路补齐思考 / 工具输出增量**，其能力位显式声明 `thinking=false` | 用户裁决 V1 = (a)：acp 仅保留、不承诺与 rpc 能力等价；「不退化」的基线 = 0021 之后的 acp 行为（本就不展示思考） | **A4** |

## 4. 大概怎么做（**仅需求层形态**；技术选型 / 模块划分 / 签名留阶段 3）

| # | 形态 | 来源 |
|---|---|---|
| M1 | **三层分工**：L1 = 启动服务（profile 数据）；L2 = 协议层（协议实现 + 注入点 + 能力位声明）；L3 = 消费层**只依赖被注入的 L2 接口**。形态细节 `[架构待填]` | J1 + **A1** + U-2/U-3 + F7/F8/F10/F11 |
| M2 | **协议指定入口唯一 = 注入点（配置面）**：解析链 = 角色级配置 > 全局默认 > 内置 `rpc`（体例对齐既有模型解析链）；**选择域 = 常驻会话协议（`rpc` / `acp` 二选一）**，`oneshot` **不在该选择域内**（由「一次性」语义选中）；**不做** per-chat / per-request 指定、**不做**控制台可切 | J2 + **A2** + 登记① |
| M3 | **过程增量**：思考 / 文本 / 工具增量走运行时 SSE、落在既有对话详情、不入库；透出粒度（原样 vs 聚合）与呈现形态 `[架构待填]`（用户无预设想法，A6） | J3 + **A3/P4** + F14 + A6 |
| M4 | **确认面**：RPC 门 → **既有 7 字段信封**（`confirmation_id / chat_id / agent_id / tool / title / options / created_at`）→ 收件箱；裁决沿用 `confirmation_id` + `option_id` + 文本的既有回路；**一次工具调用一条**；值域随协议声明（RPC 恒二元 `Approve` / `Deny`，`tool` 名复用既有提取原语）；门的呈现形态 `[架构待填]`（用户无预设想法，A6） | J1 + **A3/P5** + F12 / F15 / F16 + A6 |
| M5 | **oneshot 适配器**：无会话语义；**能力位显式降级**（门 / 流式等能力差异以声明表达，不静默缺失）；"降级怎么表现" `[架构待填]`（用户无预设想法，A6） | J4 + **A3/P3** + F8 + A6 |
| M6 | **能力位显式声明**：机制 = 逐协议显式声明、缺能力**显式降级**；候选清单按已采纳提案原文 = `streaming` / `thinking` / `approvalGate` / `hostTools` / `introspection` / `queueControl`（用户对清单无预设想法，Q1 → A6）；**最终集合与签名 `[架构待填]`** | **A3/P3** + A6 + F2 / F3 / F5 / F16 |
| M7 | **阶段 3 首步必须先做 RPC 协议面实测**（会话模型 / 取消与超时语义 / argv 面完整性 / 宿主工具面） | F1~F6（覆盖面缺口）+ R3；清单见 `clarifications/2026-09-14-demand-round1.md` §9 |

## 5. 达到什么效果（**全部过裸判定测试**）

| # | 效果表述 | 裸判定方式（一个没参与讨论的人，不问作者即可判真假） | 来源 |
|---|---|---|---|
| E1 | 默认配置下起一轮对话，被启动的 omp 进程走 **rpc 模式** | 观察进程 argv（或既有 argv 日志）：含 `rpc` 模式参数即真 | J1 / J2 + A2 |
| E2 | **切换协议只改一处注入配置**，生产消费层代码 **diff 为零**；且**被指定的协议实现确实生效**（指定 `acp` 时该轮走 acp、指定 `rpc` 时走 rpc，不回落默认） | 改注入配置 → `git diff` 看生产消费层（为空）；再以被指定协议各起一轮，观测实际生效的实现是否等于被指定者 | **A1（D-1 / D-3）** + A5（范围） |
| E3 | **默认 rpc 链路**下一轮执行**期间**（轮次结束前）hub 控制台可见**思考增量**与**工具参数 / 输出增量**；同一轮结束后 **SQLite 仍只有 in / out 两条记录**（过程不入库）。**acp 链路不在本项范围**（N12） | 盯控制台看过程是否在轮次结束前实时出现；轮次结束后查库条数 | J3 + **A3/P6** + **A4** + F3 / F14 |
| E4 | 一次受门禁工具调用在 RPC 链路下**恰好产生一条确认项**（条目数 = 1，不重不欠）；裁决后该轮据此继续或中止，且条目消失 | 数确认栏条目数（= 1）+ 看轮次是否推进 / 中止 + 条目是否消失 | J1 + F16 + F12 + A3 |
| E5 | **acp 兜底链路**：改注入配置切回 acp 后，既有行为与 0021 一致（确认收件箱 7 字段语义、三事件通知、消息与状态流转）；**不承诺思考 / 工具输出增量**（能力位 `thinking=false`）；**oneshot 路径**经无会话语义适配器，`!` shell 路径行为不变 | 按 0021 既有验收同法复核一遍（不退化）；acp 下无思考增量**不算失败**（该能力位已声明 false）；一次性路径的产物 / 日志显示走 oneshot；shell 路径行为不变 | J2 / J4 + **A4** + **A3/P6** + F19 |
| E6 | **验收证据**齐备：rpc 真实 omp 链路端到端一条 + acp 兜底链路回归一条 | 两条证据都可查（真实进程，非 fake 桩） | **A3/P6** |
| E7 | **新增一个宿主（claude / codex）只需新增 profile 数据**，L2 / L3 源码零改动 | 加一份 profile → `git diff` 看 L2 / L3 是否为空 | U-2 + J1 |

## 6. 已知代价与风险

| # | 代价 / 风险 | 性质 |
|---|---|---|
| R1 | **双轨维护成本**：两条链路都要维护与测试，协议相关行为面与测试面近似翻倍 | **用户已选择**（J2）；缓解口径 = acp 侧只做协议适配、**不补能力**（**A4**），把 0021 的重测试面当作护栏而非改造对象 |
| R2 | **既有测试面冲击**：argv 字面量断言散布 6 个文件（F17），抽象层会移动代码并触发测试最小更新（F18：30 文件 / 头部 700~1640 行） | **必要动作且刻度已定**：**A5 = 仅生产消费层纳入判据**，测试面按 profile 期望值断言做**最小更新**；不更新则既有门禁不可能通过 |
| R3 | **RPC 会话模型未实测**（一个进程能否承载多会话、`abort` / 超时的确切语义、`host_tool_call` 是否需实现）：F1~F6 只覆盖单会话单轮 | 结构性风险 ⇒ **M7** 把实测列为阶段 3 首步，否则接口可能按错误假设定型 |
| R4 | **RPC 门是二元选项**（`Approve` / `Deny`），相对 ACP 的结构化选项是**能力收窄**；`title` 在 RPC 下含多行（`Allow tool: bash\nCommand: …`） | 必然差异 ⇒ 口径已定（**A3/P5**：字段与语义等价、值域随协议声明）；呈现形态 `[架构待填]`（A6） |
| R5 | **过程增量帧量大**（实测单轮 266 帧 `message_update` / 约 3 秒）⇒ 浏览器渲染与传输成本；本迭代不做节流（N5） | 已知代价；若体感不佳再谈策略（属下一迭代的输入） |
| R6 | **未实测宿主的 profile 字段与能力位可能返工**（claude / codex 只留结构，N1） | **用户已接受范围**（J1）；返工成本落在后续迭代 |
| R7 | **协议切换使在飞上下文失忆**（N9）：用户可能感知为"对话突然不记得前文"（沿用既有 `context_reset` 提示） | 已知代价；N9 边界 |

---

**结论**：本版（v1.0.0）为**阶段 1 的可交付需求合同** —— 两段完整、`model_inferred` 归零、`[待裁决]` 归零、无活跃冲突；阶段 1 三项推进条件全部满足，可进入阶段 2（功能规格）。
