# F04 · 阶段6验证报告强制置顶呈现 deferred-demand-changes.md

**来源**：demand.md 做什么#3；结论 #7

## 用户价值

用户读完阶段6独立验证报告，第一时间就能看到"这次迭代搭置了哪些需求层问题留给下一迭代"，不会被淹没在一堆技术验证细节里而错过关键的决策信号。

## 验收标准

1. `workflow-pb.md` "验证目标"章节中关于 `deferred-demand-changes.md` 的描述，明确要求：该文件存在时，其内容必须**原文摘录**（不是转述或总结）并**置顶或显著呈现**在验证报告中。
2. 文字明确排除"脚注式一笔带过"这类弱化呈现方式——即包含足够具体的否定性表述，禁止把这部分内容放在报告末尾或不显眼位置。
3. `SKILL.md` 的 Step 6 章节同步该强制呈现要求，口径与 `workflow-pb.md` 一致。

## 边界（不包含）

- 不改变 `deferred-demand-changes.md` 本身的写入时机和格式（仍由阶段4/5执行角色发现即直接写入）。
- 不改变 verifier 是否对搭置内容做二次判断——demand.md 现有机制是 verifier 只透传原文、不对内容本身做需求层面的二次判断，本次不变。

## model_inferred

无。
