# pr-008-tasks.md — pr-008 内部任务列表（过程证据与摩擦搭置的核对补齐，F18）

**迭代**: 0029-hub-client-session-and-duplex ｜ **阶段**: 5（PR 实现）｜ **PR 文件**: `prs/pr-008-friction-log-completion.md`
**PR worktree 分支**: `feat/0029-pr-008-friction-log-completion` ｜ **base = `cfb6736`**（= 迭代分支 tip，含 pr-002 合并）｜ **任务总数**: **4**（T1~T4）｜ **依赖图**: **无环**（见 §2）
**性质**: 本 PR 内部任务列表（子 agent 内部步骤，**不是**全局任务图），供 dev 消费
**输入真源**: PR 文件（7 条验收标准 / 1 个过程契约卡 F18）+ `prd/F18-process-evidence-and-friction-log.md`（验收 1~5、边界）+ `prd/F19-hub-usage-doc-fix.md` + `demand.md:48`（D-17）+ `workflow-pb.md` §文档路径协议（`deferred-demand-changes.md` 格式）+ 体例先例 `docs/iterations/0028-role-model-binding/prs/pr-007-execution-gap-record.md` + 代码/文档实读（§0.3 逐条带 `文件:行号`）

> **本 PR 零代码改动**（PR 文件明文排除 `oamp/**`）⇒ 全部判据是**文档面核对**：`grep` 计数 + `git diff` 形态 + 逐条自检表。**不新增测试、不新建文件**（AC5）。

---

## 0. 范围、文件面与事实锚点

### 0.1 本 PR 文件范围（唯一可写面）

| # | 文件 | 动作 | 内容（任务归属） |
|---|---|---|---|
| 1 | `docs/iterations/0029-hub-client-session-and-duplex/deferred-demand-changes.md` | 改（**只在文件末尾追加 1 条记录**，0 行删除） | 新增 `DC-10 · 摩擦（度量口径）`：三要素齐备 + 台账口径指向（**T3**） |
| 2 | `docs/iterations/0029-hub-client-session-and-duplex/prs/pr-008-friction-log-completion.md` | 改（仅「验收证据」段） | 五类载体原样落盘（**T4**） |

> 本文件 `prs/pr-008-friction-log-completion-tasks.md` 是本阶段产物，不计入改动面（与 PR 文件同目录，随 PR 分支提交）。

### 0.2 非目标（零改动清单 / 防夹带）

- **零改动**（PR 文件「排除」+ AC6 明文）：`status.md`（主 agent 滚动维护的派发台账）、`history.md`、`demand.md`（红线）、`architecture.md`、`prd/**`（只读）、`oamp/**`（本 PR 零代码）、`roles/**`、`docs/iterations/**` 下的其它任何文件、`docs/worktrees/**`、仓库根配置。
- **不改既有 `DC-*` 条目任何一个字符**（AC4：`--numstat` 删除行数 = 0）——包括其分类标题、三字段值、`---` 分隔符与文件头注。
- **不新建**：文件、目录、表格格式、表头、新编号前缀（沿用 `DC-` 且顺序续号 `DC-10`）、新字段名（沿用既有三字段）。
- **不回填台账**：`status.md` 的派发台账由**主 agent** 滚动维护（PR 文件明文；F18 验收 1/2/3 的载体）——本 PR 只**核对**口径指向是否可读，不写台账任何一行。
- **不做**（F18 边界）：不把摩擦当阻塞理由（不暂停 / 不回退 / 不改需求）；不新增过程度量工具；不为摩擦条目给出解决方案或修复计划；不改写既有 skill 文档（F19 归 pr-007）。

### 0.3 实读事实锚点（2026-09-16 实读，base `cfb6736`；判据基础）

| # | 事实 | 位置 |
|---|---|---|
| **A1** | 目标文件共 **95 行**、**10 条记录**（`DC-01`~`DC-09`，含 `DC-08a`）、**9 个 `---` 分隔符**；文件**结尾无尾随分隔符**（末条 `DC-09` 以"合规性说明"行收尾）⇒ 追加点 = 文件末，须自带 `---` 前缀 | `deferred-demand-changes.md:1-95` |
| **A2** | 头注（`:3-6`）声明用途 / 格式（`## {日期} · {阶段}` + 三字段模板）/ 处置原则（**不回退、不暂停**）/ 格式迁移说明 | `:1-6` |
| **A3** | 每条记录的字段行逐字为 `**编号**` / `**问题**` / `**为什么判定为需求层面问题 / 为什么记录于此**` / `**本迭代如何处理**`（第二条字段名含" / 为什么记录于此"后缀——**引用与 `grep` 时必须用这个全称**） | `:10-13`（DC-01 样例）等 10 处 |
| **A4** | **AC1 的 F-8 条款已在场**：`DC-03`（分类标题 `## 2026-09-16 · **摩擦（结果到手）**`）的问题句含"hub skill 的「序列 1」只教…未提 `--mode block` / `cli task watch`"，"为什么"列逐字含 `` `skill/hub.md` 未教正确用法 ``，**处置列逐字含 `⇒ **D-17 / 新增卡 F19**（本迭代内修复）`** ⇒ 无需新增即可满足 AC1，但须取证 | `:26`、`:29`、`:30`、`:31` |
| **A5** | 三字段全文命中数：`为什么判定为需求层面问题`（全称）= **10**、`本迭代如何处理` = **10**，与 10 条记录一一对应；`见上文` = **0**、`同上` = **0** ⇒ AC2 的"三要素齐备 + 无指代"在现有内容上成立 | 实测 `grep -c` |
| **A6** | 处置列（`:58`/`:67`/`:76`/`:94` 等）含"回退 / 暂停 / 改需求"字样的共 **6 行**（`:22`/`:40`/`:58`/`:67`/`:76`/`:94`），**全部是否定式陈述**（"不回退不暂停""不改需求""不回退、不重规划"）⇒ AC3 的字面 grep 会命中，但**语义上无一条把摩擦当阻塞理由**（判据见 §6 MI-P2） | 实测 `grep -n` |
| **A7** | 台账口径**已在 `status.md` 可读**：`## 派发台账`（`:46`）、`**口径**：…每行额外记录**结果到手方式**（自动送达 / 主动查询 / 阻塞等待）`（`:51`）、列头 `时点 / 角色（节点） / 用途 / call_id / 终态 / 实报 model / 结果到手方式`（`:53`）；表中**已含 3 行实测派发** | `status.md:46-59` |
| **A8** | 台账列头**不含**"是否需盯守"与"耗时"两列（F18 验收 2 的 D-13 三问里只落了"结果到手方式"）——该缺口的载体是 `status.md`（主 agent 维护、本 PR 排除）⇒ **只上报，不改**（见 §7 疑问 2） | `status.md:51-53`、`prd/F18` 验收 2 |
| **A9** | D-17 / F19 实体存在：`demand.md:48`（D-17 表行，`user_confirmed`）；`prd/F19-hub-usage-doc-fix.md`（标题即"hub 使用面文档修复（暴露现成等待原语）"） | `demand.md:48`、`prd/F19-*.md:1-3` |
| **A10** | 体例先例（0028 同卡）：核对结论写入 **PR 文件的「验收证据」段**（命令 + 原样输出 + 逐条自检表），目标文件的改动形态 = **段内追加 / 整段新增**，"既有条目的四要素与「证据」原文零删除"（先例实测 32 行新增 / 0 行删除） | `docs/iterations/0028-role-model-binding/prs/pr-007-execution-gap-record.md` |
| **A11** | 本仓无测试面（0 个 `.test.js`）⇒ 本 PR 的判据 = 文档 grep/diff 实跑，无测试可跑 | 实测 |
| **A12** | 基线 commit = `cfb6736`；`status.md` 表的 `truncated` 列与 `结果到手方式` 列**并列存在两张表**（`:49-53` 与 `:53-57` 一带）——**本 PR 不整理**（改 `status.md` 即越界） | 实测（`sed -n '46,53p' status.md`） |

### 0.4 本 PR 内冻结契约（改什么、怎么改、什么不许动）

1. **改动形态**〔追溯：AC4/AC5；A1/A10〕：**只在文件末尾追加一条记录**，自带 `---` 分隔符与前导空行（A1：文件末无尾随分隔符）；**0 行删除、0 行修改**（既有 95 行逐字保留）。
2. **新记录的编号与分类标题**〔追溯：AC5；A3〕：`**编号**：DC-10`（顺号续编）；分类标题沿用既有体例 `## 2026-09-16 · **摩擦（度量口径）**`（既有分类标题格式 = `## {日期} · **{类型}（{侧面}）**`）。
3. **新记录的三字段齐备**〔追溯：AC2/AC3〕：必须逐字出现三个字段行 `**问题**` / `**为什么判定为需求层面问题 / 为什么记录于此**`（**全称**，A3）/ `**本迭代如何处理**`；**不得**用"见上文""同上"式指代；处置列**不得**以"暂停 / 回退 / 改需求"为处置动作（内容契约见下）。
4. **新记录的内容契约**（三要素各自的**必备要素**，措辞可打磨，事实不得增删）：
   - **问题**：`status.md` §派发台账 的 D-13 度量口径（角色 / 用途 / `call_id` / 终态 / **结果到手方式**）只声明在 `status.md`（口径行 `status.md:51`、列头 `:53`），**本搭置文件内没有指向行** ⇒ 阶段 6 独立验证核对"口径是否已登记"时须跨文件检索，"摩擦记录"与"度量台账"的维护边界也无法从本文件读出。
   - **为什么判定为需求层面问题 / 为什么记录于此**：属**过程记录面的完整性缺口**（F18 验收 2/3 的载体归属问题），不是 hub 能力缺陷；F18 边界禁止新增过程度量工具、要求记录落在既有文件里 ⇒ 只能在既有搭置文件内补一条**指向**。
   - **本迭代如何处理**：本 PR 补该指向行（指向 `status.md` §派发台账）；**台账本体由主 agent 滚动维护，本 PR 不回填台账任何一行**（口径与列头均不动）；本迭代按已落盘需求继续。
5. **AC1 不新增条目**〔追溯：AC1；A4〕：F-8 条款（"既有 skill/hub.md 未教等待原语"）已由 **`DC-03`** 承载且处置指向 `D-17 / F19` ⇒ **不得**为满足 AC1 再写一条重复记录（重复即"同一事实落两处"）。
6. **不碰台账**〔追溯：AC6/AC7；A7/A8〕：`status.md` 零改动（含 A12 的双表并存现状、A8 的缺列现状）；台账口径的核对结论与 grep 原文**写进 PR 文件的「验收证据」段**，不写进 `status.md`。
7. **命名面守门**〔追溯：AC5/AC6〕：`git diff --name-only` 恰为**两行**（目标文件 + 本 PR 文件）；`git status --short` 无新增未跟踪文件（除本 tasks 文件）。
8. **红线**〔追溯：AC5；PR 文件「排除」〕：`demand.md` 零改动（`git diff -- demand.md` 为空）；既有 `DC-*` 行逐字保留（唯一改动 = 末尾新增）。

### 0.5 PR 验收标准 → 任务映射（7 条 AC 全覆盖，无孤儿任务、无无主 AC）

| AC | 验收标准（PR 文件原文摘要） | 服务任务 |
|---|---|---|
| AC1 | 摩擦类条目**至少**含"既有 `skill/hub.md` 未教等待原语"（F-8），处置指向 D-17 / F19 | **T1**（取证：`DC-03` 已在场） |
| AC2 | 每条摩擦条目三要素齐备且可核对；不出现"见上文""同上"式指代 | **T2** |
| AC3 | 处置一栏不出现"暂停 / 回退 / 改需求"形态的处置（摩擦不当阻塞理由） | **T2**（口径见 §6 MI-P2） |
| AC4 | 只增不改：既有 `DC-*` 行原文逐字保留；`--numstat` 删除行数 = **0** | **T3** |
| AC5 | 不新建文件、不新建表格格式（沿用既有表头与 `DC-` 前缀体例）；不改 `demand.md` | **T3** |
| AC6 | `git diff --name-only` 仅含该文件与本 PR 文件**两行**；不含 `oamp/**`、`roles/**`、`status.md`、`history.md` | **T3** |
| AC7 | 台账与度量口径**已登记（只核对、不回填）**：文件内可读到口径指向（指向 `status.md` §派发台账）；本 PR 不代主 agent 回填台账 | **T1**（现状核对）+ **T3**（补指向行，口径见 §6 MI-P1） |

**T4** 不单独服务某一条 AC，而是 **AC1~AC7 的全部证据载体**（PR 文件「验收证据」段要求的五类输出即其判据）；因此上表每条 AC 的"服务任务"集合与该任务的证据落点共同构成完整映射，无孤儿任务。

---

## 1. 任务列表

### T1: 条目清单与分类 + F-8 条款取证 + 台账口径现状核对

- **服务哪条 AC**: AC1、AC7（只核对部分）
- **描述**: 清点目标文件的全部记录并分类（摩擦类 / 非摩擦类），逐条定位 F-8 条款，并核对 `status.md` 台账口径的现状（供 T3 的指向行取得精确锚点）。
- **文件/锚点**: 只读 `deferred-demand-changes.md:1-95`（A1~A6）、`status.md:46-59`（A7/A8/A12）、`demand.md:48`（A9）、`prd/F19-*.md:1-3`。
- **步骤**: ① `grep -n '^## ' <目标文件>` 取全部记录标题；② 逐条判"摩擦类 / 非摩擦类"；③ `grep -n 'skill/hub.md' <目标文件>` + 读 `DC-03` 三字段；④ 核对 `status.md` 的口径行与列头行号。
- **验收判据（可执行，全部保留原始输出）**:
  1. **清单**：`grep -n '^## ' docs/iterations/0029-hub-client-session-and-duplex/deferred-demand-changes.md` 输出 **10** 行标题（`DC-01`~`DC-09` 对应的 10 个 `## {日期} · {类型}` 块，含 `DC-08a` 那一块）；`grep -c '^\*\*编号\*\*'` = **10**。
  2. **分类表**：给出"编号 → 分类标题 → 摩擦类？"三列表；**摩擦类 = `DC-02`（摩擦·可观测性）/ `DC-03`（摩擦·结果到手）/ `DC-05`（摩擦·追溯）/ `DC-08`（期望偏差·严重，按摩擦处置）/ 新增 `DC-10`**；其余（`DC-01` 错误 / `DC-04` 事实修正 / `DC-06` 事实 / `DC-07` 流程偏差 / `DC-08a` 主 agent 侧修正 / `DC-09` 阶段入口）标为非摩擦并给出判定依据（标题类型 + 是否把摩擦当阻塞理由）。
  3. **AC1 取证**：`grep -n 'skill/hub.md' docs/.../deferred-demand-changes.md` ⇒ 命中行位于 `DC-03`；`sed -n '29,31p'` 原样输出的问题句含"未提 `--mode block` / `cli task watch`"、**处置行含 `D-17` 与 `F19`**（两条都必须在同一行可见）。
  4. **D-17 存在性**：`grep -n 'D-17' docs/.../demand.md` ⇒ 命中 `:48`（`user_confirmed` 行）；`ls docs/.../prd/F19-hub-usage-doc-fix.md` 存在 ⇒ 处置指向的实体真实存在（不是悬空引用）。
  5. **AC7 现状**：`grep -n '口径\|结果到手方式' docs/.../status.md` ⇒ 口径行 `:51`、列头行 `:53` 命中；**`grep -c '结果到手方式\|派发台账' docs/.../deferred-demand-changes.md` = 0**（即指向行缺席的现状事实——这是 T3 补充的依据）。
  6. **基线干净**：`git -C <worktree> rev-parse --short HEAD` = `cfb6736`；`git -C <worktree> diff --stat cfb6736 HEAD -- docs/iterations/0029-hub-client-session-and-duplex/deferred-demand-changes.md` ⇒ **空**（改动尚未发生）。
- **前置依赖**: 无
- **优先级**: P0

---

### T2: 三要素自检表 + 处置形态核查

- **服务哪条 AC**: AC2、AC3
- **描述**: 对全部记录（含 T3 将新增的 `DC-10`，本任务先做既有 10 条的基线）逐条核对三字段与处置形态，产出可核对的自检表。
- **文件/锚点**: 只读 `deferred-demand-changes.md`；字段名以 A3 的**全称**为准（第二条字段含" / 为什么记录于此"）。
- **步骤**: ① 逐条抽取三个字段行；② 统计命中数；③ 抽取全部处置行并按形态分类；④ 记录原始 grep 输出。
- **验收判据（可执行）**:
  1. **AC2 字段齐备**：`grep -c '^\*\*问题\*\*' <文件>` / `grep -c '^\*\*为什么判定为需求层面问题 / 为什么记录于此\*\*' <文件>` / `grep -c '^\*\*本迭代如何处理\*\*' <文件>` ⇒ 三个数**相等**且 = 记录条数（既有 10 条 ⇒ 10/10/10，实测已核对；T3 后 ⇒ 11/11/11）；逐条自检表 = "编号 → 三字段各 ✓/✗"，**✗ = 0**。
     > 模式里的 `\*\*` 必须转义（BRE 下 `^\*\*X\*\*` 才是"行首两个星号"）；**期望计数为 0 的 `grep -c` 会以退出码 1 结束，这是"未命中"的正常返回、不是错误**（脚本里不要据此判失败）。
  2. **AC2 无指代**：`grep -c '见上文' <文件>` = **0** 且 `grep -c '同上' <文件>` = **0**（原始输出）。
  3. **AC3 处置形态**：逐条列出 10 条处置行的**首句**并标注形态，分类枚举必须只出现 ∈ {**按已落盘需求继续** / **转设计输入** / **主 agent 当场修正** / **事实追加** / **已由用户裁决升格为交付项**} 之一；**判定：0 条的处置动作是"暂停 / 回退 / 改需求"**。
  4. **AC3 字面命中需并列呈现**（口径见 §6 MI-P2）：`grep -n '^\*\*本迭代如何处理\*\*' <文件> | grep -E '回退|暂停|改需求'` 的原始输出（既有 **6 行**，实测已核对）**必须原样入证**，并附一句语义判定（这些是"未发生"的否定式陈述，不是处置动作）——不得只给结论、不给出原始命中行。
  5. **自检表覆盖新增条目**：T3 完成后重跑本条，条数由 10 → **11**，`DC-10` 行三字段全 ✓ 且处置形态合格（T2 的判据在 T3 后必须复跑，见 §3）。
- **前置依赖**: T1（清单与分类是逐条自检的输入）
- **优先级**: P0

---

### T3: 末尾追加 `DC-10`（度量口径指向）+ 只增不改与命名面守门

- **服务哪条 AC**: AC7（补齐）、AC4、AC5、AC6
- **描述**: 在目标文件**末尾**追加一条 `DC-10 · 摩擦（度量口径）`（三字段齐备，内容按 §0.4 契约 4；指向 `status.md` §派发台账 并明写不回填），随后做只增不改与改动面守门核查。
- **文件/锚点**: `deferred-demand-changes.md` 文件末（A1：末尾无尾随分隔符 ⇒ 追加块以 `---` 开头）；编号顺续 `DC-10`（A3 体例）。
- **步骤**: ① 追加 `---` + 空行 + 分类标题 + 三字段块（§0.4 契约 4 的三段内容）；② 跑 T2 判据全量复跑；③ 跑守门命令族（§4.3）。
- **验收判据（可执行，全部保留原始输出）**:
  1. **AC7 指向在场**：`grep -n 'DC-10' <文件>` 命中编号行；`sed -n '<新增块范围>p' <文件>` 原样输出中**同时**出现 `status.md`、`派发台账`、`结果到手方式` 三个锚点，且含"**不回填**"字样。
  2. **AC2/AC3 复跑**：三字段计数 11/11/11（T2 判据 1）、`见上文`/`同上` 仍为 0（T2 判据 2）、处置形态合格（T2 判据 3）——**新增条目自身也必须是摩擦类且三要素齐备**。
  3. **AC4 只增不改**：`git -C <worktree> diff --numstat cfb6736 HEAD -- docs/iterations/0029-hub-client-session-and-duplex/deferred-demand-changes.md` ⇒ 形如 `<N>\t0\t<路径>`，**第二列（删除）必须为 0**；`git -C <worktree> diff -U0 cfb6736 HEAD -- <该文件>` 的输出中**不得出现任何 `-` 起始行**（只有 `+` 行与 hunk 头）。
  4. **AC5 不新建**：`git -C <worktree> status --short` ⇒ 只出现目标文件（`M`）与 PR 文件（`M`）；**无 `??` 未跟踪项**（本 tasks 文件已提交/将提交，见 §5）；`git -C <worktree> diff cfb6736 HEAD -- docs/iterations/0029-hub-client-session-and-duplex/demand.md` ⇒ **空**。
  5. **AC5 格式沿用**：新增块的分类标题匹配既有体例（`^## 2026-09-16 · \*\*摩擦（度量口径）\*\*$` 形式，一处 `##`、无 `###` 新层级）；未新增字段名（`git diff` 的 `+` 行中的 `**…**：` 字段名集合 ⊆ {`编号`,`问题`,`为什么判定为需求层面问题 / 为什么记录于此`,`本迭代如何处理`}）。
  6. **AC6 命名面**：`git -C <worktree> diff --name-only cfb6736 HEAD` ⇒ **恰好两行**：`docs/iterations/0029-hub-client-session-and-duplex/deferred-demand-changes.md` 与 `docs/iterations/0029-hub-client-session-and-duplex/prs/pr-008-friction-log-completion.md`；**不含** `oamp/**`、`roles/**`、`status.md`、`history.md`。
  7. **红线**：`git -C <worktree> diff cfb6736 HEAD -- docs/iterations/0029-hub-client-session-and-duplex/status.md docs/iterations/0029-hub-client-session-and-duplex/history.md` ⇒ **空**。
- **前置依赖**: T2（新增条目必须按 T2 已确立的口径自检，并复跑其判据）
- **优先级**: P0

---

### T4: 验收证据落盘（PR 文件「验收证据」段）

- **服务哪条 AC**: AC1~AC7 的证据载体齐备（**P1 ≠ 可选**：全部 AC 通过才算本 PR 完成）
- **描述**: 把 T1~T3 的原始输出按 PR 文件「验收证据」段要求的五类载体回填。
- **文件/锚点**: `prs/pr-008-friction-log-completion.md` 的 **「验收证据」** 段（只改该段，**不改七字段**）；体例对齐 A10 的先例（命令 + 原样输出 + 逐条自检表；**不依赖 `/tmp` 中间产物**——先例的返工教训）。
- **步骤**: ① 汇总五类载体；② 逐条粘贴（命令与输出原样，不二次加工）；③ 自查 AC1~AC7 的对应关系；④ 提交本 tasks 文件与 PR 文件。
- **验收判据（可执行）**:
  1. 「验收证据」段含 PR 文件原文列举的**五类**：① 摩擦条目清单与三要素自检表；② F-8 那一条的原文与处置指向；③ `--numstat` 的删除行计数；④ `--name-only` 原文；⑤ 台账口径指向行的 `grep` 输出。
  2. **可复现性**：证据段内所有命令**自带 `cd` 目标与绝对路径**（先例体例），且**不引用 `/tmp/**`**（先例 pr-002 的两次返工即因"证据依赖 /tmp"而重做）——`grep -c '/tmp/' <PR 文件>` 在证据段内 = **0**。
  3. AC1~AC7 每条都能在该段指到对应原始输出（缺一即 T4 未完成）。
  4. `git status --short` 除目标文件、PR 文件、本 tasks 文件外**无其它改动**。
- **前置依赖**: T3
- **优先级**: P1

---

## 2. 依赖图

```
T1 ──> T2 ──> T3 ──> T4
```

边（逐条，均为真实约束；共 3 条）：
- `T1 → T2`：逐条自检表以 T1 的清单与分类为输入（否则无法回答"哪些条目是摩擦类"）。
- `T2 → T3`：T3 新增的 `DC-10` 必须按 T2 已确立的字段口径与处置形态自检，且 T3 判据 2 要**复跑 T2 的全量判据**（10 → 11 条）。
- `T3 → T4`：证据段必须引用改动后的实测输出（numstat / name-only / grep）。

**无环**：拓扑序 `T1 < T2 < T3 < T4` 满足全部边方向。

**最长依赖链（本 PR 内部任务图的关键路径，4 节点）**：`T1 → T2 → T3 → T4`（唯一链，故 T1~T4 全部在关键路径上）。

---

## 3. 执行顺序（dev 单次调用 ≤ 30 分钟上限制下的增量策略，见 DC-06/DC-08）

**顺序**：`T1 → T2 → T3 → T4（可复跑 T2 判据）`。

**每次调用产出的可验证增量**：
| 调用 | 产出增量 | 独立判据 |
|---|---|---|
| 1 | 清单 + 分类表 + F-8 取证 + 台账口径现状（`DC-10` 缺席事实） | §T1 判据 1~6 |
| 2 | 三要素自检表（10 条）+ 处置形态清单 | §T2 判据 1~4 |
| 3 | `DC-10` 追加块 + 全量复跑 + 守门输出 | §T3 判据 1~7 |
| 4 | PR 文件「验收证据」段五类载体 | §T4 判据 1~4 |

**若单次调用未跑完**：按上表在**任务边界**停下；注意 **T3 一旦开始追加就必须跑完守门核查**（半追加状态会让 AC4/AC6 的判据失去意义）。

---

## 4. 验证配方（全部为只读命令 + 一次追加；无测试可跑，A11）

工作目录 = PR worktree 根；下列命令**逐条原样入证**。

### 4.1 清单与分类（T1）

```bash
D=docs/iterations/0029-hub-client-session-and-duplex
grep -n '^## ' $D/deferred-demand-changes.md          # 期望 10 行记录标题
grep -c '^\*\*编号\*\*' $D/deferred-demand-changes.md  # 期望 10
grep -n 'skill/hub.md' $D/deferred-demand-changes.md   # 期望命中 DC-03 一带
sed -n '26,31p' $D/deferred-demand-changes.md          # DC-03 原文（标题 + 三字段）
grep -n 'D-17' $D/demand.md                            # 期望 :48
ls $D/prd/F19-hub-usage-doc-fix.md
```

### 4.2 三要素与处置形态（T2；T3 后复跑）

```bash
D=docs/iterations/0029-hub-client-session-and-duplex
grep -c '^\*\*问题\*\*' $D/deferred-demand-changes.md
grep -c '^\*\*为什么判定为需求层面问题 / 为什么记录于此\*\*' $D/deferred-demand-changes.md
grep -c '^\*\*本迭代如何处理\*\*' $D/deferred-demand-changes.md
grep -c '见上文' $D/deferred-demand-changes.md ; grep -c '同上' $D/deferred-demand-changes.md
grep -n '^\*\*本迭代如何处理\*\*' $D/deferred-demand-changes.md    # 逐条处置行（供形态分类）
grep -n '^\*\*本迭代如何处理\*\*' $D/deferred-demand-changes.md | grep -E '回退|暂停|改需求'   # 字面命中（须原样入证 + 语义判定）
```

### 4.3 只增不改与改动面守门（T3）

```bash
D=docs/iterations/0029-hub-client-session-and-duplex
git -C <worktree> diff --numstat cfb6736 HEAD -- $D/deferred-demand-changes.md     # 期望 "<N>\t0\t<路径>"
git -C <worktree> diff -U0 cfb6736 HEAD -- $D/deferred-demand-changes.md           # 不得出现 '-' 起始行
git -C <worktree> diff --name-only cfb6736 HEAD                                    # 期望恰两行
git -C <worktree> status --short                                                   # 无 '??' 未跟踪项（除本 tasks 文件的提交时点）
git -C <worktree> diff cfb6736 HEAD -- $D/demand.md $D/status.md $D/history.md      # 期望空
grep -n 'DC-10' $D/deferred-demand-changes.md
tail -n 12 $D/deferred-demand-changes.md
```

### 4.4 台账口径指向核对（T1 的 AC7 现状 / T4 的证据 ⑤）

```bash
D=docs/iterations/0029-hub-client-session-and-duplex
grep -n '派发台账\|口径\|结果到手方式' $D/status.md
sed -n '46,53p' $D/status.md
grep -c '结果到手方式\|派发台账' $D/deferred-demand-changes.md   # T1 时点期望 0；T3 后期望 ≥1（指向行）
```

---

## 5. 证据载体与落盘

- **最终证据载体**：`prs/pr-008-friction-log-completion.md` 的 **「验收证据」** 段（PR 文件自身在文件范围内，且原文即要求"本 PR 执行时填写"）。
- **不依赖 `/tmp`**：本 PR 的判据全部是**就地可重跑的命令**（grep/diff/sed）⇒ 证据段内联命令 + 原样输出，**不在 `/tmp` 留中间产物**（先例 pr-002 因"证据依赖 /tmp"两次返工的教训）。
- **不得**：新建文档 / 新截图 / 把证据写进 `status.md`、`history.md`、`deferred-demand-changes.md` 的既有段落（后者只允许末尾追加 `DC-10` 一条）。
- **提交面**：目标文件（追加 `DC-10`）、PR 文件（证据段）、本 tasks 文件三者提交在 PR 分支 `feat/0029-pr-008-friction-log-completion` 上。

---

## 6. model_inferred 验收标准（需主 agent 确认，逐条列出）

- **[model_inferred] MI-P1（AC7 的"文件内"指向何处）**：本任务列表把 AC7 的"**文件内**可读到……口径指向"读作**目标文件（`deferred-demand-changes.md`）内需有指向行**，故 T3 追加 `DC-10` 承载该指向。
  - 为什么需要推导：AC7 原文"文件内可读到'派发台账逐行含 D-13 三问……'的口径指向（指向 `status.md` §派发台账）"可有两种读法：**(a)** 口径本体已在 `status.md` 可读（当前事实，A7），本 PR 只需核对；**(b)** 目标文件内需出现该指向。AC6 要求 `--name-only` 为"该文件 + 本 PR 文件**两行**"、AC5 的括注又强调"沿用既有表头与既有 `DC-` 前缀体例"，两处都指向"目标文件应有一次追加" ⇒ 取读法 (b)。
  - 两种读法的**判据差**：读法 (a) 下 `--name-only` 只有一行（PR 文件），AC6 的"两行"须按**子集**判据复核；读法 (b)（本次采用）下两行自然成立，且 `--numstat` 有非空的"新增 N / 删除 0"，符合 PR「验收证据」段要求的两项计数。**若主 agent 裁定 (a)**，回退口径 = 删除 `DC-10` 追加块（成本 1 处删除块），此时 AC6 按子集判据复核。
- **[model_inferred] MI-P2（AC3 的判定口径）**：AC3 按**语义**判定（处置动作不属于"暂停 / 回退 / 改需求"），**字面 grep 命中须并列呈现**而非据此判负。
  - 为什么需要推导：AC3 原文是"处置一栏**不出现**'暂停 / 回退 / 改需求'**形态的处置**"，而既有 6 条处置行含这些字样（A6），且**全部是否定式**（"不回退不暂停""不改需求""不回退、不重规划"）——它们描述"没有发生什么"，不是处置动作本身；同时 AC4 禁止删除/改写既有行，两者叠加 ⇒ 字面读法自相矛盾，只能取语义读法。
  - 判据落地：T2 判据 3（形态分类）承担判定，T2 判据 4 要求把字面命中的原始行**原样入证**（供阶段 6 独立复核），并在证据段附一句语义说明。

**无其他推导项**：AC1/AC2/AC4/AC5/AC6 的判据均可逐字回指 PR 文件、`prd/F18` 或 §0.3 的事实锚点。

---

## 7. 循环依赖与疑问/越界

### 循环依赖
**无**（见 §2 的 3 条边与拓扑序 `T1 < T2 < T3 < T4`）。

### 疑问 / 越界（**不改 PR 文件的七字段**，只上报）

1. **AC1 在现状下**已由 `DC-03` 满足**（不必也无法"补齐"）：`DC-03` 的问题句即"hub skill 的「序列 1」只教派发→`calls get`，未提 `--mode block` / `cli task watch`"（事实 F-8），处置列逐字含 `D-17 / 新增卡 F19`（A4）。本任务列表因此**不新增** F-8 的重复条目（契约 5），T1 只做取证。若主 agent 期望把 F-8 单列成一条**新**记录（而非复用 `DC-03`），那是新增第二处真源，须由主 agent 裁决。
2. **F18 验收 2 的 D-13 三问在台账上尚未落全（载体是 `status.md`，本 PR 不改）**：`status.md` 台账列头 = `时点 / 角色（节点） / 用途 / call_id / 终态 / 实报 model / 结果到手方式`（A7/A8），**缺"是否需盯守"与"耗时"两列**（F18 验收 2 要求三问：① 是否需额外盯守 ② 结果如何到手 ③ 耗时）。该台账由**主 agent** 滚动维护，且 AC6 明文排除 `status.md` ⇒ 本 PR 只上报、不修改；请主 agent 决定是否在本迭代内补列（属 `status.md` 改动）。
3. **简报提到的 `DC-13` 不存在**（同前几个 PR 的观察）：`deferred-demand-changes.md` 当前为 `DC-01`~`DC-09`（含 `DC-08a`），无 `DC-13`。本 PR 是纯文档核对（无 30 分钟切分压力），引用偏差不影响切分。
4. **简报工作区地址与实际一致**：`…/worktrees/0029-pr-008-friction-log-completion` 存在，`git branch --show-current` = `feat/0029-pr-008-friction-log-completion`（本次无笔误，无需纠偏）。
5. **粒度/决策记录（本 PR 未写 `roles/planner/data/`）**：本次按"核对类 / 自检类 / 改动类 / 落盘类"四段切分（判据载体不同：清单 grep、自检表、守门命令族、证据段），未按文件切分；按 planner 角色定义本应记入 `data/`，但本 PR 文件范围不含（PR 明列排除 `roles/**`）⇒ 记录在此，不越界写 `roles/`。
6. **架构信息无缺口**：F18 是过程契约卡（`prd/F18` 明写"无待填项"），AC1~AC7 均可回指 PR 文件、`prd/F18` 验收 1~5/边界、`workflow-pb.md` §文档路径协议 与 0028 的体例先例；两条需要推导的口径（MI-P1/MI-P2）已列 §6 等确认。
7. **本 PR 的"证据不自证"风险已规避**：`DC-10` 的追加点在**文件末尾**（A1：末尾无尾随分隔符）⇒ 追加不会触及既有 95 行中的任何一行，AC4 的"删除行数 = 0"由追加位置结构性保证，而不靠"小心不要改到"。
