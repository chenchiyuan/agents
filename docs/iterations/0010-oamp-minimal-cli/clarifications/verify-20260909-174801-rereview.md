# 阶段 6 收尾复查报告（V1 fail 项返工复核） — 0010-oamp-minimal-cli

验证者身份：同级代码审查者 + 测试工程师（对 dedd519 修复提交做定向复查：断言方向正确性 + 稳定性回归 + README 陈旧措辞清理）
产出物：提交 `dedd519`（`oamp/test/status.test.js` + `oamp/README.md`，2 文件 19+/12-）
复查标准：主 agent 委托三项（断言方向 / 隔离 ≥10 次 + 完整 ≥3 次全绿 / README 陈旧声明清除）
复查日期：2026-09-09
复查方式：只读提交 diff 与文件当前态；隔离与完整套件真实连跑；未使用执行过程上下文。

---

## 逐项判定

### R1 用例 #1 断言方向性错误已消除 —— pass

证据（读提交 dedd519 与当前 `oamp/test/status.test.js:85-119`）：

- 新增注释明示竞态语义：「基准快照 base 须在 runStatusCli 之前取得（T_base < T_cli）…不得用更晚的 queryStatus 快照作下限去比较更早的 CLI 输出」。
- 结构改动：`const base = await queryStatus(...)` 移至 `runStatusCli` **之前**（原晚快照 `snap` 已删除）；:109 断言改为 `Date.parse(row[3]) >= node.last_heartbeat`，其中 `node` 来自 **T_base 的 base**（早于 CLI 读取 T_cli）。
- 正确性论证：注册表 last_heartbeat 单调不减（registry.js heartbeat 仅向前推进），CLI 于 T_cli 的输出值 ≥ T_base 的基准值恒成立 → 断言无竞态窗口，必然而非偶然成立。
- 副作用确认：session/state 断言同步改用 base（测试期无同 id 重注册、会话稳定，断言语义不受影响）；无其他用晚快照比较早输出的断言残留（通读用例 #1 全段）。

### R2 稳定性回归 —— pass

证据（真实连跑）：

- `node --test test/status.test.js` 隔离连跑 **12 次 → 12/12 全绿、0 fail**（修复前基线：10 次中 9 fail）。
- `npm test` 完整套件连跑 **3 次 → 每次 46/46 全绿、0 fail**（`# tests 46 / # pass 46 / # fail 0` ×3）。

### R3 README 陈旧声明清除 —— pass

证据（读当前 `oamp/README.md` + dedd519 diff）：

- 「当前状态」块改写为终态：「CLI 分发、Router/agent 运行时与 status 只读查询已全部落地可用——三个命令端到端可执行（复现步骤见下方 E2 手测）」（原"首个落地 PR…模块尚未实现…预期行为，非故障"已删）。
- E2 手测引导注改写：「完整 E2 复现…按下列步骤直接执行即可观察到 online/心跳/kill→offline」（原"骨架…随后续 PR 落地…届时按本步骤"已删）。
- 协议速览末行改为「详细协议契约…已随实现落地；设计依据见 architecture.md」（原"随后续 PR 代码与文档落地"已删）。
- 陈旧短语扫描（尚未实现/随后续/骨架/首个落地 PR）零命中。

---

## 汇总

- pass: 3 项（R1 断言方向、R2 稳定性回归、R3 README 清理）
- fail: 0 项
- partial: 0 项
- blocked: 0 项

## 偏差记录

无（dedd519 与首轮报告偏差记录 R1/R2 完全对应，无新偏差）。

## 下一迭代候选

无（V1 fail 项已闭环；README 陈旧偏差已随 R2 同步）。

## 结论

**PASS**（V1 返工验证通过：status.test.js #1 竞态断言已改为前置基准快照下限且 12/12 隔离 + 3×46/46 完整套件全绿；README 陈旧声明同步清除。阶段 6 原 fail 项闭环，迭代收尾验证结论由 FAIL 转为通过；是否合入 main 仍由用户决策，D-4。）
