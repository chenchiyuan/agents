# verify-20260909-173742 — PR-004 message-delivery-contract 独立验收报告

验证者身份：同级代码审查者 + 协议契约测试验收者（产出物 = node:test 契约测试载体 + 假节点 helper + 跨 PR src 行为修复；验证标准 = 行为契约/错误映射/信封语义，故按"能审查测试是否真实驱动协议语义、能审查 src 修复是否破坏既有行为"的评审者身份判定）
产出物：`.pb-agents/worktrees/0010-pr-004-delivery/`（相对基线 a462477）
- oamp/test/helpers/fake-node.js
- oamp/test/delivery-contract.test.js
- oamp/src/router.js + oamp/src/registry.js（Q-1 裁决跨 PR 竞态修复 diff）
- docs/iterations/0010-oamp-minimal-cli/prs/pr-004-tasks.md
验证标准来源：
1. `docs/iterations/0010-oamp-minimal-cli/prs/pr-004-message-delivery-contract.md` 验收标准 5 条
2. `prd/F07-message-delivery-contract.md`（W6/P-01/E1 扩展）+ P-09
3. `architecture.md` §4.4/§4.5/§4.6/§5.5/§6.4/§10.1、D10/D11/D12（pr-002 已合并语义）
验证日期：2026-09-09
验证方式：只读产出物与标准；`node --test`/`npm test` 各连跑 2 次；git diff 对照基线逐行核查；未使用任何执行过程上下文（tasks.md 修订记录仅作为"裁决存在性"的文档证据引用，判定以实码+实测为准）。

## 逐项判定

### V1 测试全绿且稳定含回归 —— pass
- `oamp/` 下 `node --test test/delivery-contract.test.js`：第 1 次 `# tests 12 / # pass 12 / # fail 0`（duration 1702ms）；第 2 次 `12/12 pass`（1720ms）。
- `npm test`（=`node --test test/*.test.js`）：第 1 次 `# tests 41 / # pass 41 / # fail 0`；第 2 次 `41/41 pass`。连跑稳定，无 flake。
- 基线核对（实测逐文件）：cli 8 / hygiene 3 / router-registry 8 / agent-heartbeat 6 / event-log 4 = **29 项全绿**；+ delivery-contract 12 = 41，与 tasks.md"29 基线 + 12 新增 = 41"总量一致。pr-002 三卡（router-registry 8 / agent-heartbeat 6 / event-log 4）在含新载体全量跑中均绿——零回归。
- 无残留：仓库内无 `.runtime/`、系统临时目录无 oamp-test 残留、测试进程全部干净退出。

### V2 闭环全链路（PR 验收 1 / F07-1/4）—— pass
- 测试 `F07-1/4：双假节点闭环 register→send→deliver→ack 全链路 + 心跳继续`（delivery-contract.test.js:60）驱动：A/B 双假节点 register（断言响应含 instance_id/session_id(UUID 形态)/state=online/lease_timeout_ms=300/last_heartbeat=number，:71-79）→ A send（断言 result `{accepted:true, message_id, status:"delivered"}`，:88-92）→ Router stdout 先 `MESSAGE_DELIVERED message_id=… from=dev-a to=dev-b` 后 `MESSAGE_ACKED message_id=… instance=dev-b`（:97-98）→ 经 queryStatus 轮询断言 A/B last_heartbeat 均推进（:101-110，3s 内多周期命中）——闭环无错误且心跳不被消息操作打断。
- 方法面最小集（F07 验收 4）：register/heartbeat/send/deliver/ack 均在闭环中实际驱动；deregister 由假节点 `stop()` 于 teardown（`t.after(() => a.stop())`）触发——fake-node.js stop() 内 `client.deregister()`（fake-node.js:71-76）。实测通过。

### V3 message_id 端到端贯通与重复 id（PR 验收 2 / F07-2/P-09）—— pass
- 测试 `F07-2/P-09：message_id 端到端贯通`（:100）：send 提交 id → B deliver 到达信封 `message_id === 提交值`（:112-113）→ Router `MESSAGE_ACKED message_id=<同一 id>`（:117）→ 自动 ack 后对同一 id 再 ack → `err.dataCode === UNKNOWN_MESSAGE`（:121）——ack 已消费 pending 的确定性正断言，id 三段可关联。
- 测试 `重复 send 同 message_id：透传可区分，非幂等去重`（:282）：两次 send 同一 id 均 `{accepted:true, status:"delivered"}`（:295-305），B 收两次 deliver 同 id（:310-311），Router MESSAGE_ACKED 恰两条（:312 `waitRouterLine(…, 2)`）——D11 无去重状态机、Map 覆盖语义实测保持。

### V4 错误映射 / 信封校验 / 钩子接管（PR 验收 3/4 / F07-3 + §4.3/§4.5/§4.6/§5.5）—— pass
- 错误映射六码全测（均断言 `err.dataCode === <机器码>`，:52-58 expectRejected 统一校验）：
  - `UNREGISTERED`（:157 未注册裸连接 send）、`AGENT_NOT_FOUND`（:174 未注册目标 + deregister 后目标同码）、`AGENT_OFFLINE`（:191 租约超时判 offline 后 send，先等 `AGENT_OFFLINE instance=dev-b` 事件锚点再断言 status 快照 state=offline）、`UNKNOWN_MESSAGE`（:206 未知 id ack）、`INVALID_MESSAGE`（:215 10 个子例：坏 protocol/缺/空/>64/含控制字符 message_id/空白/空 to/非法 content_type/body 非字符串/缺 payload）、`INVALID_SENDER`（:238 自报伪造 from）。
  - 实码侧一一对应（router.js:174-196 dispatch 顺序 = UNREGISTERED→INVALID_MESSAGE→INVALID_SENDER→AGENT_NOT_FOUND→AGENT_OFFLINE；registry.js:157-163 resolvePendingAck 三态）。错误码以 `data.code` 字符串承载（rpc.js ERR 冻结表 :8-18），符合 §4.2/§4.3。
- 信封校验顺序：INVALID_MESSAGE 用例目标 dev-b 未注册仍恒得 INVALID_MESSAGE——验证 §4.5"信封合法检查先于目标查找"。Router 代填 from/created_at 由 F07-3 用例实测。
- 钩子接管：测试 `受理与 ack 分离`（:251）B onDeliver 返回 false → 150ms 短窗内 Router stdout 匹配 MESSAGE_ACKED 数为 0（:270-274），手动 `ack(msgId)` 返回 `{acked:true, status:"accepted"}`（:277-279）——deliver 受理与 ack 分离语义（D12/§10.1）。实码侧 node-client.js:77-101：respond.ok({received:true}) 先行、onDeliver 返回 false 则跳过自动 ack。
- 附注：PR 验收 4 条文中"目标未注册 send → UNREGISTERED"与 architecture §4.3/F07 语义不符（目标不存在 → AGENT_NOT_FOUND；UNREGISTERED 仅指发送方）——tasks.md O-1 已记录，测试按 architecture/F07 语义实现；判定以架构语义为准（见偏差记录 1）。

### V5 真实 agent 受理（PR 验收 5 / §6.4/D12/§10.1）—— pass
- 测试 `真实 agent 受理`（:307）：harness startAgent 子进程 `node bin/oamp.js agent start real-1`（缩短 env + 临时 socket）→ Router `AGENT_REGISTERED instance=real-1`（:312）→ 假节点 A send 投递 → send result `{accepted:true, status:"delivered"}`（真实节点回传输应答后 Router 同步代理才完成，:322-323）→ Router `MESSAGE_DELIVERED` + `MESSAGE_ACKED message_id=<同一 id> instance=real-1`（:325-326）→ agent stdout `MSG_RECEIVED message_id=… from=dev-a size=\d+` 且**全文不含秘密正文**（:329-334，隐私/日志卫生断言）→ 第二次 send 亦成功（真实节点仍存活，:337-338）→ SIGINT 退出码 0 无 signal + Router `AGENT_DEREGISTERED instance=real-1`（:339-343）。
- 实码侧：agent.js 用 NodeClient（未注入 onDeliver → 默认自动受理）；node-client.js:83-87 MSG_RECEIVED 只打 message_id/from/size 不回显正文；agent.js SIGINT 路径 deregister → exit 0。真实节点与假节点共享同一协议实现（fake-node.js import 同一 createClient）——结构一致性由共用客户端保证。实测通过。

### V6 跨 PR src 修复正确性与回归安全 —— pass
- diff 范围（git diff a462477..worktree）：registry.js +5 行（新增 `clearPendingDelivery` 定向删除单条 pending + 导出）；router.js +17/−6（message.send 段）。
- 修复内容（router.js:208-222）：`recordPendingDelivery` 由 `await deliverTo` **之后**移至**之前**（同步于 dispatch 内）；deliverTo 抛错分支新增 `clearPendingDelivery(full.message_id)` 后再回 `AGENT_OFFLINE`。
- 竞态机制核查：目标 NodeClient 自动受理在传输应答后**同一 tick** 内发 message.ack（node-client.js:96-108 run() 异步立即执行），ack 帧可与 deliver 响应同 chunk 到达 Router；旧代码 recordPending 在 `await deliverTo` 的 microtask 续段之后，同 chunk 内先被分发的 ack 查 pending 无锚点 → UNKNOWN_MESSAGE。新代码 pending 在 dispatch 同步段已就绪，ack 恒可关联——修复方向正确且为最小序变更。
- 语义保持核查：① D11——`recordPendingDelivery` 仍为 `pendingDeliveries.set`（registry.js:152-154），重复 message_id Map 覆盖语义不变（测试"重复 send 同 message_id"实测绿）；② D10——失败分支定向清理本次 message_id，不留脏 pending；③ MESSAGE_DELIVERED 事件与 `{accepted:true,status:"delivered"}` 响应仍在投递成功后发出（router.js:224-225），对外时序不变；④ deliverTo 5s 超时亦落入 catch → 清理，无悬挂 pending。
- 回归安全：npm test 41/41 ×2，含 pr-002 router-registry 8 / agent-heartbeat 6 / event-log 4 三卡全绿——无回归。修复文件即 pr-002 已合并文件，属已裁决的跨 PR 例外（tasks.md 修订记录，先例 = pr-002 cli.js 修正），非越界改动。

### V7 文件范围 —— pass
- git status（基线 a462477 = pr-002 merge）：`M oamp/src/registry.js`、`M oamp/src/router.js`（Q-1 裁决例外，仅该两文件相关行）；`?? docs/iterations/0010-oamp-minimal-cli/prs/pr-004-tasks.md`、`oamp/test/delivery-contract.test.js`、`oamp/test/helpers/fake-node.js`。共 5 路径，其余 src 与 pr-001/pr-002 已合并文件全部无 diff（`git diff --stat` 仅 2 文件 16+/6−）。
- fake-node.js 仅 import pr-002 `src/node-client.js`（无 node: 内置、无第三方）；delivery-contract.test.js 只 import node:test/node:assert/node:net + helpers + src/rpc.js（ERR/RpcPeer 供裸连接用例，与 harness queryStatus 同型先例）。package.json `dependencies: {}` 零第三方（hygiene 测试亦断言）。
- 覆盖完整性：tasks.md 声明的 Q-1 例外（pr-004 契约测试暴露 ack 序竞态 → 主 agent 裁决跨 PR 最小修正）与实测 diff 一致。

## 汇总

- pass: 7 项（V1~V7 全 pass）
- fail: 0 项（需返工）
- partial: 0 项
- blocked: 0 项

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| # | 规格/文档描述 | 实际行为/实现 | 建议处理 |
|---|---|---|---|
| 1 | PR-004 文件验收第 4 条写"目标未注册/offline 时 send → UNREGISTERED/AGENT_OFFLINE" | architecture §4.3/§4.4 + F07 卡：目标不在注册表 → `AGENT_NOT_FOUND`；`UNREGISTERED` 仅指发送方连接未注册；offline → `AGENT_OFFLINE`。测试按架构语义实现（tasks.md O-1 已记录此措辞偏差） | merge 后由主 agent 修订 PR-004 文件第 4 条措辞（或补 O-1 引用），避免后续验收误读 |
| 2 | tasks.md"既有基线 = 5 卡 29 用例（cli 8 / hygiene 4 / router-registry 8 / agent-heartbeat 6 / event-log 7）"——括号内合计 33，与总量 29 自相矛盾 | 实测逐文件：cli 8 / hygiene 3 / router-registry 8 / agent-heartbeat 6 / event-log 4 = 29（与 41−12 一致；hygiene/event-log 两卡数字标错） | 更正 tasks.md 基线每卡用例数（hygiene 3、event-log 4） |
| 3 | architecture §5.5 首句"每次 deliver 发出**且目标传输层应答后**，Router 记 pendingDeliveries" | Q-1 修复后实现为 deliver **发出前**记录 pending（失败回滚）。tasks.md 修订记录称"架构文档无需改动"，但 §5.5 字面行文已落后于实现 | 更新 architecture §5.5 记录时机行文（投递前记录 + 失败回滚），或在该节标注 Q-1 裁决 |
| 4 | PR-004 文件"本 PR 只新增测试载体，不改任何 src 文件"、文件范围 2 文件 | Q-1 裁决后实际含 pr-002 router.js/registry.js 跨 PR 修正（16+/6−） | PR-004 文件补记 Q-1 裁决结果（或 merge 时在 PR 记录/提交信息注明——tasks.md 修订记录已要求提交信息注明，文档行文滞后于裁决） |

## 下一迭代候选

- clearPendingDelivery 失败回滚分支（router.js:218-222，deliverTo 抛错路径）无测试**直接**覆盖——现有 AGENT_OFFLINE 用例走"投递前状态检查拒绝"分支，非投递中断路径。可加故障注入（投递瞬间断连或 stub deliverTo）单测闭合该分支。
- §4.3 的 `STALE_SESSION` / `INVALID_ACK_STATUS` 两个 ack 错误码无契约测试覆盖（本轮验收清单未要求）；如需完整错误面可作 F07 后续补充。
- 极端组合边角（文档化即可，D11 语义内）：同 message_id 第二次 send 覆盖 pending 后投递失败 → clearPendingDelivery 会连带删除第一次投递的 pending 锚点（Map 单条目语义下二者不可区分，且第二次失败时目标连接已断、其 ack 无现实到达路径）。无需行为变更，建议在 §5.5 补一句说明。
- 跨 PR 修复随本 PR merge 进入迭代分支时，需确认与 pr-002 基线合并不冲突（本 worktree 基于 a462477 直接修改，无并行分支改动）——merge 时走常规流程即可。

## 结论

PASS（7 项全 pass，0 fail；偏差 4 条均已显式记录，不阻塞；PR 验收 5 条逐条有对应通过证据，可 merge）

## 证据索引

- 测试运行：delivery-contract 12/12 ×2（1702/1720ms）、npm test 41/41 ×2（2277/2274ms），逐文件基线 cli 8/hygiene 3/router-registry 8/agent-heartbeat 6/event-log 4 全绿
- git：HEAD=a462477，branch=feat/0010-pr-004-delivery；diff 仅 registry.js(+5)/router.js(+17/−6)；untracked 3 文件
- 代码锚点：router.js:173-247（message.send/ack 分发）、registry.js:151-167（pending 三函数）、node-client.js:76-108（deliver 自动受理）、log.js（event 永不节流/只节流 HEARTBEAT）、harness.js（startRouter/startAgent/queryStatus/stopAll）
- 交付清单：fake-node.js（工厂 startFakeNode + onDeliver/记录面/stop 生命周期）、delivery-contract.test.js（12 用例，覆盖闭环/message_id 贯通/payload 保真/六错误码/钩子接管/重复 id/真实 agent）
