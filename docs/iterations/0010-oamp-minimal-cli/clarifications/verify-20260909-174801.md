# 阶段 6 独立验证报告 — 0010-oamp-minimal-cli（迭代收尾）

验证者身份：同级代码审查者 + 测试工程师（产出物 = 全量 Node.js 实现 + 需求/规格/架构/PR 文档链；标准含裸判定效果条款 E1~E4 与可执行验收——两者都需要"能看代码、能跑测试、能抓时序"的身份）
产出物：
- `oamp/` 全量实现（package.json / bin/oamp.js / src×9 / test 6 卡 + helpers×2 / README / .gitignore，22 文件全部已提交）
- `docs/iterations/0010-oamp-minimal-cli/`（demand/prd/prd×8/architecture/prs×4+4/status/history/clarifications）
验证标准来源：
1. demand.md 效果条款 E1~E4（裸判定）与 W1~W6
2. prd.md + prd/*.md（F01~F08 验收，含 M-01~M-03）
3. workflow-pb 阶段 6 验证目标 + 前几轮报告"下一迭代候选"收尾项（F08-1 git 实查、E1 全链路）
验证日期：2026-09-09
验证方式：只读产出物与标准；未使用任何执行过程上下文（git log/status 仅作终态卫生证据）。真实启动 Router+agent 进程做 E2/E3/F08-1 实查并在验证后 SIGINT 清理（无残留进程/产物，见 V6 证据）。

---

## 逐项判定

### V1 E1 全链路 npm test 全绿稳定 —— fail（测试套件不稳定：status.test.js 用例 #1 存在可复现时序竞态）

证据（可复查）：

- **名义全绿**：`oamp/` 下 `npm test` 首两次 46/46 全绿（`# tests 46 / # pass 46 / # fail 0`）；测试数分布与委托预期一致（cli 8 / hygiene 3 / router-registry 8 / agent-heartbeat 6 / event-log 4 / status 5 / delivery-contract 12 = 46，逐文件独立运行核对）。
- **不稳定（fail 证据）**：累计 18 次完整 `npm test` 运行中 1 次失败（`not ok`，套件级 ~5.6% flake）；`node --test test/status.test.js` 隔离运行 10 次 → **1 通过 / 9 失败（90% flake）**；其余 6 个测试文件各自隔离连跑 5 次全部稳定（cli/hygiene/router-registry/agent-heartbeat/event-log/delivery-contract 均 5/5）。
- **失败定位**：status.test.js 用例 #1「F05-1/2 + §7.3/D8：多节点表格」行 74；断言 **status.test.js:109** `assert.ok(Date.parse(row[3]) >= node.last_heartbeat, '输出 last_heartbeat 不早于快照时点（心跳自然推进）')` → `ERR_ASSERTION`，expected true / actual false（failureType testCodeFailure）。
- **根因（时序竞态，非运行时缺陷）**：用例先 `runStatusCli`（status.test.js:88，子进程 CLI 读取注册表于时点 T_cli）后 `queryStatus`（:97，测试进程内快照读取于 T_snap > T_cli）；:109 用 **更晚** 快照的 `last_heartbeat` 作为下限去比较 **更早** CLI 输出的时间戳。注册表 `last_heartbeat` 单调不减（registry.js heartbeat 仅向前推进），T_cli→T_snap 窗口内任一心跳被处理即令快照值更新 → 断言必然失败。测试 env 心跳 interval=50ms ×2 节点（harness SHORT_ENV），窗口内落入心跳概率高 → 高频偶发。断言方向只有"窗口内零心跳"时才成立——该断言无法稳定验证它声称的"心跳自然推进"语义。实现侧行为正确（真实运行输出时间戳与注册表一致，见 V2）。
- **影响**：demand E1「npm test 全绿」作为裸判定无法被稳定复现 → E1 验收不成立；V1 判定 fail。运行时实现未发现缺陷（E2 实跑正常），返工面 = 单条测试断言（status.test.js #1），见下一迭代候选 R1。

### V2 E2 手测主链路可复现（含 status 与 offline）—— pass

证据（真实进程复现，缩短 env interval=400ms / timeout=1500ms / 窗口=2000ms，默认 socket）：

- Router `ROUTER_READY socket=…/oamp/.runtime/router.sock` 就绪行出现（README E2 步骤 1 ✓）。
- agent dev-1/dev-2 各自 `AGENT_START` → `REGISTERED instance=… session=<UUID> lease_timeout_ms=1500`（步骤 2 ✓）。
- `oamp status`：对齐表格 `instance_id/session_id/state/last_heartbeat` 四字段，两节点 `online`（步骤 4 ✓；四字段与注册快照一致）。
- Router 终端事件：`AGENT_REGISTERED` ×2 + `HEARTBEAT`（节流）；~2.5s 后再查 status 两节点仍 `online` 且 `last_heartbeat` 推进（21.6s 区间，心跳持续可见 ✓）；期间实际心跳 ~17 次（400ms×2 节点×~2.7s）仅日志 4 条 HEARTBEAT（窗口 2s/节点 → ≤⌈T/W⌉+1=2/节点，节流生效，F06-3/M-03 语义满足）。
- kill -9 dev-2（无 deregister）→ 约 timeout(1.5s)+sweep 后 Router 打 `AGENT_OFFLINE instance=dev-2`，再 `oamp status` 该行 `offline`（墓碑保留、last_heartbeat 停在其最后心跳；步骤 5 ✓，F04-2/3、F05-5）。
- SIGINT dev-1 → agent 打 `DEREGISTERED`（Router 同步打 `AGENT_DEREGISTERED`），进程退出无悬挂（F03-3；真实跨 shell 无法精确取退出码，退出码 0 由 agent-heartbeat.test.js 6/6 稳定覆盖）；SIGINT Router → `ROUTER_STOPPING` → 进程退出、socket unlink。
- 手测全链路与 README E2 步骤逐一对应并可复现 → E2 pass。

### V3 E3 双节点并发 online —— pass

证据：

- 真实进程（V2 同一运行）：dev-1 与 dev-2 同时 online、各自独立 session、各自心跳互不影响，同一 Router 维护（status 两行同时 online、Router 日志两节点 HEARTBEAT 交替）。
- 自动化独立覆盖：router-registry.test.js（含并发注册/同 id 唯一性/替换）与 agent-heartbeat.test.js 隔离 5/5 稳定；delivery-contract.test.js 双假节点闭环中亦为双节点并发（12/12 稳定）。
- 复现独立于任何既有单次运行结论 → E3 pass。

### V4 E4 卫生 + F08-1 git 实查（产物真实产生、被忽略、退出后清理）—— pass

证据：

- **F08-1 真产物实查**：真实启动 Router（默认 socket）→ 产物真实产生于 `oamp/.runtime/router.sock`（stat perms=600，F02-7 同证）→ `git check-ignore -v` 对**实际产物路径**命中 `oamp/.gitignore:1:.runtime/`、exit 0；SIGINT 优雅退出后 socket 已 unlink（`ls` 确认 .runtime/ 为空，仅残留被忽略的空目录）；`git status --porcelain oamp/` 零输出（产物不入库、无未跟踪残留）。此前 verify-20260909-163542/173644 的 O-3 候选（`--no-index` 模拟 → 真产物实查）正式闭环。
- **凭据扫描（E4 后半）**：`grep -rniE '\b(token|api_key|secret|password|credential|authorization|private_key)\b'` 扫 bin/ src/ package.json → 零命中；全树扫描仅 README 红线声明文本与协议"无 token 鉴权"说明（设计上属 F08-3 声明，不在扫描范围），`test/` 命中仅为模式定义（hygiene.test.js 排除自身）。hygiene.test.js 3/3 稳定通过（含 .gitignore 含 `.runtime/` 断言）。
- **README 红线声明（F08-3）**：「卫生红线声明」节声明两条红线（运行时产物不入 git / 零凭据字段），与实现一致。
- **测试卫生**：全部自动化测试 socket 位于 `os.tmpdir()` mkdtemp（harness），实跑确认未触碰仓库 `.runtime/`。
- **E4 pass**（git 侧 + 扫描侧 + 文档侧三面均满足）。

### V5 需求→规格→架构→PR→实现全链追溯一致（W/E ↔ F ↔ 文档 ↔ 代码）—— pass（附 2 条 README 陈旧偏差，见偏差记录）

证据：

- **demand ↔ prd**：prd.md「需求追溯」表 W1↔F01、W2↔F02、W3↔F03、W5↔F02/F04/F05/F06、W6↔F07、E1~E4↔对应卡、N1~N8↔边界，全覆盖且无 prd 外新增功能；八卡与 W/E 逐条对应（读 prd.md 核对）。
- **prd ↔ architecture**：F01~F08 卡"架构维度（阶段 3 已补全）"标注与 architecture.md 决策一一对应（抽查 D1/D2/D4/D6/D7/D8/D9/D10/D11/D12/D14/D15/D16/D17 与 AR-01~AR-12 补全对照表、§9 F08-1 指派阶段 6 一致）。
- **architecture ↔ PR**：prs/ 四份文件七字段齐全，涉及功能点引用与文件范围此前逐 PR verifier 核验（verify-20260909-161944 Gate：F01~F08 全覆盖、22 文件零重叠）。
- **文档 ↔ 代码（抽查）**：registry.js（latest-wins 替换/同 id 唯一、heartbeat 仅 live+同 session 更新、findExpired 纯判定、pendingDeliveries、snapshot 4 字段）↔ §5.2~5.6；router.js（7 方法分发、chmod 0600、ROUTER_READY/AGENT_REPLACED 关旧连接、租约主动扫描 clamp、ROUTER_STOPPING）↔ §4/§5/§8；node-client.js（唯一客户端、register/heartbeat 通知/deregister、deliver 自动受理 `{received:true}`+自动 ack accepted）↔ §6/D12；config.js（env 名与默认 10000/30000/60000、包根锚定 .runtime/router.sock）↔ §7.2；status.js（padEnd 四列表、router.status 无身份要求、UTC ISO、不可达退出 1 stderr 报错 stdout 空）↔ §4.4/§7.3/M-02；log.js（每节点滑窗只节流 HEARTBEAT）↔ §8.2。全部一致。
- **W1~W6 命令面实跑**：router/agent/status 三形态、0600、≥2 节点、lease→offline、status+事件日志、最小消息闭环（delivery-contract 12 用例含 F07-1/2/3、真实 agent 受理用例）均被真实运行/测试覆盖（V1~V3 证据）。
- **status.md 与 git 实际状态一致**：status.md 未提交 diff 恰为阶段 5→6 过渡（阶段表/并发配置 4 释放 4 派发/PR 子状态四 PR 已合并 (已清理)），与 git 实际完全吻合；git log 含基线（9c95bd8）+ 文档同步 ×3 + 4 个 merge（36b6d67→a462477→78f8182→d24754a，顺序与 depends_on 一致）+ 收尾同步（b04d72e），main..HEAD 首父链验证。
- 偏差见下（README 陈旧声明不构成 W/E↔F↔architecture↔prs↔代码映射断裂，F01-8/F08-3 载体经 V2/V4 实跑成立）。

### V6 迭代状态卫生（status.md 真实、分支已提交、无残留进程/产物）—— pass

证据：

- 分支 `iteration/0010-oamp-minimal-cli`；未提交项 = 仅 `docs/iterations/0010-oamp-minimal-cli/status.md`（阶段 5→6 状态过渡，主 agent 进行中、属预期）+ `docs/.DS_Store`（macOS Finder 杂项、未跟踪、**位于迭代范围外** docs/ 根，观察项非判定项）。`oamp/` 22 文件全部已提交（`git status --porcelain oamp/` 零输出）。
- 无残留进程：`pgrep -fl oamp.js` 零结果（E2/E3/F08-1 实跑后均已 SIGINT 清理）。
- 无残留运行时产物：`.runtime/` 仅空目录（socket 已 unlink、目录被 gitignore）。
- 无残留 PR worktree/分支：`git worktree list` 仅主工作区 + 既有 0004 迭代 worktree（非本迭代遗留）；`git branch -a` 无 0010 PR 分支。
- history.md 存在（history 开启与 status.md 头部一致），事件记录按协议维护。
- status.md 阶段表/并发配置/PR 子状态与 git log 实际一致（见 V5）。

---

## 汇总

- pass: 5 项（V2、V3、V4、V5、V6）
- fail: 1 项（V1——E1 npm test 不稳定，status.test.js #1 时序竞态，需返工）
- partial: 0 项
- blocked: 0 项

## 偏差记录

> 实现与现有规格/文档不一致处。不影响追溯判定，但应在合入前/下一迭代同步。

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| README「当前状态」块（oamp/README.md:8-10）：声称"本仓库首个落地 PR 交付 CLI 分发骨架…Router/agent/status 完整能力随后续 PR 落地。本 PR 阶段执行 `router start`/`agent start`/`status` 会得到'模块尚未实现'的明确提示（预期行为，非故障）" | 四 PR 已全部合并，三命令真实可用（V2 实跑）；执行不会得到"模块尚未实现"提示——该声明描述的是 PR-001 合并时点的历史状态 | 按实现更新 README（删除"当前状态"陈旧块或改写为最终交付状态）；README 自 PR-001 后未被任何 PR 触碰（文件范围不含 README），此陈旧为跨 PR 累积，建议合 main 前随返工一并同步 |
| README E2 手测节引导注（README:15-17）"完整 E2 复现步骤骨架…Router/agent 完整能力随后续 PR 落地，届时按本步骤即可完整执行"；协议速览末行（README:60-61）"详细协议契约…随后续 PR 代码与文档落地" | 能力已落地、步骤已可完整执行（V2 实测 5 步逐一复现） | 移除"骨架/随后续 PR 落地"限定语；契约细节现由 architecture.md §4 与契约测试承载，措辞同步 |
| F08 卡/README 红线声明 vs `docs/.DS_Store` | 仓库 docs/ 根存在未跟踪 .DS_Store（Finder 生成，非 oamp/ 产物，git status 噪声） | 不属 oamp/ 卫生范围；如介意可纳入根 .gitignore（当前根 .gitignore 仅 .idea/、.pb-agents/）——由主 agent/用户决定，不阻塞本迭代 |

## 下一迭代候选

- **R1（本 fail 的返工方向，必做）**：status.test.js #1（:74-117）断言 :109 存在读偏斜竞态——用 T_cli 的 CLI 输出比较 T_snap（更晚）的注册表快照，50ms 心跳×2 节点落入窗口即红（隔离 9/10 失败、完整套件 1/18 失败）。返工方向：把比较基准改为 CLI 读取**之前**的快照（断言输出不早于基准），或改为断言"输出 ≤ 后续快照且单调"的等价无竞态形式；改后需连跑验证（建议 ≥10 次隔离 + 数次完整套件）。运行时实现未发现缺陷，勿动 registry/router 逻辑。
- **R2**：README 陈旧声明清理（见偏差记录两行）——顺 R1 返工一并落地即可，无需单开 PR。
- **R3**：其余 6 个测试文件各自隔离 5/5 稳定、唯 status.test.js 有此竞态——R1 修复后建议把"status.test.js 隔离连跑 ≥10 次"加入该迭代回归验证清单，防止同类读偏斜断言再次引入。
- **观察（非问题）**：真实 E2 运行中 .runtime/ 目录在 Router 退出后留空壳（socket 已删、目录被忽略）——符合"产物 unlink + gitignore"设计，无需处理。
- **观察（非问题）**：agent SIGINT 的真实退出码在跨 shell 验证中无法精确采集（wait 127 为跨 shell 伪值），退出码 0 语义由 agent-heartbeat.test.js 稳定覆盖，无需补证。

## 结论

**FAIL**（V1 fail：E1「npm test 全绿稳定」不成立——status.test.js 用例 #1 时序竞态致套件间歇性失败，需返工后重新验证；运行时实现与其余验收 V2~V6 全部 pass，返工面收敛于单条测试断言）

注：按 workflow-pb 阶段 6 出口，本报告为迭代收尾独立验证结论；是否合入 main 由用户决策（D-4，不自动合入）。
