# Verify 报告：PR-003 status 只读查询（阶段 5 验收）

**验证者身份**：同级代码审查者 + 行为测试工程师（产出物 = 代码实现 status.js + 进程级测试 status.test.js，验证标准含可运行行为验收——反射为"有资格评审该 Node 模块、能判断 RpcPeer/config 消费模式是否踩坑、能运行并核对进程级行为"的双重身份）
**产出物**：
- `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0010-pr-003-status/oamp/src/status.js`
- `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0010-pr-003-status/oamp/test/status.test.js`
- `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0010-pr-003-status/docs/iterations/0010-oamp-minimal-cli/prs/pr-003-tasks.md`
**验证标准来源**：PR 文件 `prs/pr-003-status-query.md` 验收 4 条、`prd/F05-status-readonly-query.md`（验收 1-5 + M-02 user_confirmed）、`architecture.md` §7.3/§3.1/§5.2/§4.4、tasks.md T1~T3 细化标准
**验证日期**：2026-09-09
**独立性声明**：未接收任何执行过程上下文；仅读取产出物 + 标准文档；未修改被验证文件（smoke 用 /tmp 脚本，已删除）。

## 执行环境

- Node v22.15.0；验证在 PR-003 worktree 内执行（产出物所在分支基于 `iteration/0010-oamp-minimal-cli`，HEAD = PR-002 merge 提交 a462477）。
- git 核查：`git status --porcelain` 仅 3 个未跟踪文件 = `oamp/src/status.js`、`oamp/test/status.test.js`、`docs/.../prs/pr-003-tasks.md`——文件范围与 PR 声明精确一致，既有文件（cli.js/config.js/rpc.js/harness 等）零改动。

## 逐项判定

- **V1 测试全绿含回归（tasks T3-1/T3-2）**：pass
  证据：
  1. worktree `oamp/` 下 `node --test test/status.test.js` → 5/5 pass（5 条 F05 用例全绿）。
  2. `npm test`（`node --test test/*.test.js`）→ 34/34 pass：既有 29 基线（cli/router-registry/agent-heartbeat/event-log/hygiene，PR-002 收官基线）仍全绿 + status 新增 5 条全绿。回归面无损。

- **V2 输出格式（表头/字段/排序/ISO 时间，§7.3/D8 + F05-1/2）**：pass
  证据：
  1. 自动化用例 1：stdout 首行经 `HEADER_RE = /^instance_id\s{2,}session_id\s{2,}state\s{2,}last_heartbeat$/` 断言；数据行恰 2 行且按 instance_id 升序（dev-2 先于 dev-1 启动，输出 dev-1 在前 → 证明按 instance_id 排序而非注册序）；session_id 经 UUID_RE（36 字符）且与 router.status 快照一致；state=online 与快照一致；last_heartbeat 经 ISO_RE（`Z` 后缀毫秒）且 ≥ 快照时点。
  2. 手工 smoke（真实 Router+双 agent、默认 socket）：实际输出 `instance_id  session_id  state  last_heartbeat` 对齐表格，行序 dev-1→dev-2，心跳为 `2026-09-09T09:36:01.211Z`（UTC ISO-8601 毫秒 Z）。
  3. status.js L19-22 `COLUMNS` 四列与 §7.3 字段一致；L44 `cellOf` last_heartbeat 经 `toIso`（`new Date(ms).toISOString()`）外显；L47-55 `renderTable` padEnd 对齐 + 客户端侧自足排序（复制 `[...nodes]` 排序，不依赖 Router 序）。

- **V3 Router 不可达明确失败（M-02/F05-3，PR 验收第 2 条）**：pass
  证据：
  1. 自动化用例 2：不存在 socket → 子进程退出码 1；stdout 为空（不输出表头/空表格冒充成功）；stderr 含 `无法连接 oamp router`、含 socket 路径、含 `router 未运行`。
  2. 手工 smoke 第 6 步（Router SIGINT 退出 unlink 后）：退出码 1、stdout 空、stderr 含完整 socket 路径与 `无法连接 oamp router（socket=…；router 未运行？先执行 oamp router start）`。
  3. status.js L79-86 connect 失败分支：仅写 stderr、返回 1；L96-101 查询失败分支同样仅 stderr + 返回 1（快速失败不静默）。

- **V4 只读无副作用（F05-4，PR 验收第 3 条）**：pass
  证据：
  1. 静态全文走查 status.js（120 行）：网络动作仅 `net.connect`（L58）+ `RpcPeer.request('router.status', {}, {timeoutMs:2000})`（L94）；不调用 register/heartbeat/deregister/send/notify，无任何写方法；成功与失败路径均 `peer.close()`（L99/L103）+ socket 销毁；import 面仅 `node:net` + `./config.js` + `./rpc.js`（§3.1 ST-->CFG/RP 引用边，无越界 import）。
  2. 自动化用例 3：连续两次 status 子进程，两轮输出 (instance_id/session_id/state) 集合与行序完全一致、last_heartbeat 第二轮 ≥ 第一轮；两轮之间及之后的 router.status 快照条目集合/session/state 不变；agent 未退出且查询后心跳继续推进。
  3. 手工 smoke：两次连续 status 仅 last_heartbeat 推进（id/session/state 一致，`TWO_RUNS same id/session/state: true`）。

- **V5 kill→offline 反映与零节点形态（F05-5/E2 + §7.3 零节点，PR 验收第 4 条）**：pass
  证据：
  1. 自动化用例 4：agent SIGKILL（无 deregister）→ Router 打 `AGENT_OFFLINE` 行 → status 子进程输出该行 state=offline、保留原 session_id 墓碑（§5.4）、退出码 0（offline 为合法状态非错误）。
  2. 手工 smoke 第 4 步：SIGKILL dev-1 后 status 显示 dev-1 offline（session 保留）、dev-2 仍 online。
  3. 自动化用例 5（零节点）：真实 Router 运行零注册 → 退出码 0、stdout 仅表头一行无数据行、stderr 空——与 M-02 失败空态（stdout 全空 + 退出 1）可区分；status.js L109 `nodes = Array.isArray(...) ? ... : []` + `renderTable([])` 走 `Math.max(header.length, ...[])` 宽度路径，输出表头 + 换行。

- **V6 与 architecture/cli 分发一致性（§7.1/§7.2/§4.4，tasks T1-1/T3-7）**：pass
  证据：
  1. default 导出形态：`export default async function status(restArgs)` 返回数值退出码 0/1（L72-119），与 cli.js `loadAndRun` 约定（`await entry(restArgs)`；`typeof result === 'number' ? result : 0`）精确对齐；cli.js status 分支 `loadAndRun('./status.js','status', argv.slice(1))` 只读消费、本 PR 零改动（git 核查无 cli.js diff）；多余位置参数经 `void restArgs` 按 O-2 惯例忽略。
  2. socket 定位：消费 `loadConfig(process.env).socketPath`（§7.2，`OAMP_SOCKET` 可覆盖、默认 `<包根>/oamp/.runtime/router.sock` 与 cwd 无关）；手工 smoke 中 Router 从 worktree 根目录 cwd 启动、status 从 oamp/ cwd 执行，双方经无 OAMP_SOCKET env 命中同一默认 socket → 包根锚定确认；socket 权限 0600（smoke stat 600）。
  3. RpcPeer 用法：`new RpcPeer(socket, { idPrefix:'status' })` + `request('router.status', {}, {timeoutMs: REQUEST_TIMEOUT_MS})` 与 rpc.js 构造/请求签名一致（rpc.js L152-153/L168-170）；status 不注册身份即查询，符合 §4.4 "router.status 不需要注册身份"；D17 三处请求上限之一 status 2s 已落（L13 `REQUEST_TIMEOUT_MS = 2000`）；连接防悬挂定时器 2s（L14，node-client 同值先例）。
  4. Router 侧 `router.status` 快照投影（§4.4 result 4 字段按 instance_id 排序）由 pr-002 已合并 router.js/registry.js 提供——34/34 全绿含 router-registry 8+ 用例，查询-响应链路实测成立。

## 汇总

- pass: 6 项（V1~V6 全部）
- fail: 0 项（需返工）
- partial: 0 项
- blocked: 0 项

## 偏差记录

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| architecture.md §7.3 不可达 stderr 示例为英文 `cannot reach oamp router at <path>（router 未运行？）` | 实际输出中文：`oamp: status 失败: 无法连接 oamp router（socket=<path>；router 未运行？先执行 oamp router start）`；与 pr-002 已合并 agent.js L48 同场景文案同款（已 grep 证实） | 非阻塞。planner 已在 tasks.md MI-1/Q-1 显式提请主 agent 确认，跨模块中文一致性优先于 §7.3 英文字面示例。建议主 agent 确认后把 §7.3 示例文案同步为实际中文形态（文档对齐实现），或裁决维持英文示例并在文档注明实现取中文。M-02 硬验收语义（退出 1 + stderr 含 socket 路径 + 不静默空结果）不受影响 |

## 下一迭代候选

- **Q-1（承 tasks.md MI-1，主 agent 待确认项）**：上表偏差 1 的措辞裁决——确认取中文后需同步 architecture.md §7.3 示例文案，避免文档/实现措辞漂移。
- **O-2 超时分支无进程级测试载体**：status 请求 2s 上限（D17）与连接防悬挂 2s 定时器已存在（L13/L14 静态证实），但"Router 存活而响应超时"无现实触发源，以代码走查覆盖、无独立用例——planner 已注记；如需故障注入测试留未来迭代。
- **O-3（阶段 6 边界）**：README E2 手测步骤补充 status 手测 + F08-1 `git check-ignore` 实查未放入本 PR（文件范围仅 src/test 两文件，planner 已显式标注 O-3）；建议阶段 6 或下一迭代落实，避免 README 与三命令形态脱节。
- **观察（非问题）**：status.js 额外导出 `renderTable`（命名导出）供潜在复用，当前测试走 stdout 解析而非 import 它——不是死代码（导出即测试/复用面），但如后续无人消费可考虑收敛为模块私有，YAGNI 留痕。

## 结论

**PASS**（6 项判定全部 pass、0 fail、0 partial、0 blocked；偏差 1 条已记录且不阻塞 merge 判定）

按契约回报：报告路径 + 结论 PASS + fail 0 + partial 0 + 偏差记录 1 条。
