# verify-20260909-161944.md — 阶段 4→5 入口独立验证（PR 规划产物）

验证者身份：**冲刺/提交规划审查者（PR 规划评审员）**——产出物为 PR 规划文件（提交单元划分 + 依赖图），此类产出物的核心风险是"提交边界模糊、功能点漏引用、文件范围重叠、依赖证据失实、依赖成环"，故以"能核查依赖图真实性、能审计每份 PR 规划文件的格式与证据链"的规划审查者身份逐项验证。不使用执行过程上下文，不修改任何被验证产出物。

产出物：
- `docs/iterations/0010-oamp-minimal-cli/prs/pr-001-project-skeleton-cli-hygiene.md`
- `docs/iterations/0010-oamp-minimal-cli/prs/pr-002-protocol-runtime-registry-heartbeat.md`
- `docs/iterations/0010-oamp-minimal-cli/prs/pr-003-status-query.md`
- `docs/iterations/0010-oamp-minimal-cli/prs/pr-004-message-delivery-contract.md`

验证标准来源：
1. `.pb-agents/roles/workflow-pb/workflow-pb.md`（v0.8.0）「PR 文件格式规范」七字段 + 阶段定义表阶段 4 输出与推进条件
2. `docs/iterations/0010-oamp-minimal-cli/prd.md` + `prd/*.md`（F01~F08）
3. `docs/iterations/0010-oamp-minimal-cli/architecture.md`（§3.2 文件树、模块引用/数据流/测试组织章节）

验证日期：2026-09-09

---

## 逐项判定

### S1 每个 PR 文件含七字段：上下文摘要/涉及功能点/文件范围/验收标准/参考资料/depends_on/batch —— pass

证据（逐文件核对 `## ` 二级标题）：
- pr-001：上下文摘要 / 涉及功能点 / 文件范围 / 验收标准（4 条）/ 参考资料 / depends_on（无）/ batch=1——七字段齐全。
- pr-002：上下文摘要 / 涉及功能点（F02/F03/F04/F06）/ 文件范围（10 文件）/ 验收标准（4 条）/ 参考资料 / depends_on（pr-001，含 3 条理由）/ batch=2——七字段齐全。
- pr-003：上下文摘要 / 涉及功能点（F05）/ 文件范围（2 文件）/ 验收标准（4 条）/ 参考资料 / depends_on（pr-001 + pr-002，各含理由）/ batch=3——七字段齐全。
- pr-004：上下文摘要 / 涉及功能点（F07）/ 文件范围（2 文件）/ 验收标准（5 条）/ 参考资料 / depends_on（pr-002，3 条理由）/ batch=3——七字段齐全。
- 字段顺序与规范一致；depends_on 无依赖时 pr-001 写「（无）」，符合规范要求。

### S2 文件命名 pr-{NNN-描述}.md，NNN 三位有序从 001 递增 —— pass

证据：
- 目录实查（`prs/` glob 结果）：`pr-001-project-skeleton-cli-hygiene.md`、`pr-002-protocol-runtime-registry-heartbeat.md`、`pr-003-status-query.md`、`pr-004-message-delivery-contract.md`。
- NNN 三位、含 `-描述`、从 001 起连续递增、无跳号、无重号；四个文件均在 `prs/` 目录下，满足「PR 文件前置」的路径协议。

### S3 全部 prd 功能点 F01~F08 被至少一个 PR 引用；无 prd 之外的新增范围 —— pass

证据（「涉及功能点」字段全集）：
- F01 → pr-001；F02 → pr-002；F03 → pr-002；F04 → pr-002；F05 → pr-003；F06 → pr-002；F07 → pr-004；F08 → pr-001。8/8 全覆盖，无遗漏。
- 无 prd 之外的新增范围：四份 PR 的文件范围合计 22 个文件，逐一与 prd.md「包含」清单（W1~W6/E1~E4）及 architecture.md §3.2 文件树对照，全部落在迭代已锁范围内；PR 上下文摘要中的验收主题（注册字段/并发/同 id 替换/offline 重注册/deregister/0600/SIGINT/心跳节流/四字段表格/不可达失败/消息闭环/卫生红线）均可在 F01~F08 卡验收标准中找到对应编号（抽查 pr-001 的 F01 验收 2/4/5/6 与卡内编号一致、F08 验收 2/3 与卡内一致；pr-002 引用的 F02-2/4/6/7/8、F03-1~4、F04-2/4、F06-1~4 均存在于对应卡；pr-003 引用的 F05-1~5 与 M-02 存在；pr-004 引用的 F07-1~4 与 P-09 存在）。
- 说明（记录为偏差 D4，不构成判定变化）：pr-001 上下文摘要明确声明 F01 验收 1/3/7/8「最终可复现依赖后续 PR 合并后的整体状态」、F08-1 git 侧 check-ignore「留阶段 6」——这两处跨 PR/跨阶段验收归属与 architecture.md §9「git 侧核查由阶段 6 独立验证按 F08-1 执行」一致，是架构层的既定安排，功能点引用本身未被破坏。

### S4 各 PR 文件范围按 architecture.md §3.2 文件树声明，PR 间零重叠 —— pass

证据（architecture.md §3.2 文件树 22 个文件 ↔ 四份 PR 文件范围并集逐项比对）：
- 树 9 个 `src/` 模块全覆盖且互斥：cli.js/config.js（pr-001）、rpc.js/registry.js/log.js/router.js/node-client.js/agent.js（pr-002）、status.js（pr-003）。
- 树 `test/` 全覆盖且互斥：helpers/harness.js（pr-002）、helpers/fake-node.js（pr-004）、cli.test.js/hygiene.test.js（pr-001）、router-registry.test.js/agent-heartbeat.test.js/event-log.test.js（pr-002）、status.test.js（pr-003）、delivery-contract.test.js（pr-004）。
- 树根文件全覆盖且互斥：package.json/bin/oamp.js/.gitignore/README.md（pr-001）。
- 每 PR 文件数合计 8+10+2+2 = 22 = 树文件总数；无任何文件出现在两个 PR、无任何树外文件进入 PR 范围（如 pr-004 只含 fake-node.js + delivery-contract.test.js 两个测试文件，未声明任何 src 文件——与其摘要「本 PR 只新增测试载体，不改任何 src 文件」自洽）。
- 附注：pr-002 文件范围虽承载消息域实现代码（router.js message.* 分发、registry.js pendingDeliveries、node-client.js send/ack），但其与 pr-004 的文件集合零交集，判定不受影响（归属讨论见偏差 D2）。

### S5 depends_on 每条依赖有可核实的证据（architecture.md 模块引用/数据流/测试组织章节），证据能定位到章节 —— partial

证据（4 条依赖边逐一核查）：
- 通过部分：
  - **pr-002 → pr-001**：理由② bin/oamp.js + cli.js `router start`/`agent start` 分发分支属 pr-001，证据可定位 architecture.md §3.2（可执行入口形态 + 模块布局）、§7.1（分发表）、§10.2（「node:test 测试一律 node bin/oamp.js … 子进程方式拉起」+ harness 职责）——真实存在且可核实；理由③ package.json `scripts.test="node --test test/"` 为 §10.2 运行方式与 F01-1 npm test 载体入口——真实存在。
  - **pr-003 → pr-001**：bin/oamp.js + cli.js `status` 分发分支（§7.1 分发表「oamp status」行）、`OAMP_SOCKET` env 作用对象「全部」（§7.2 配置面表）、status 定位 Router（§7.3）——真实存在且可核实。
  - **pr-003 → pr-002**：①§3.1 组件图存在 `ST --> RP` 引用边（status 消费 rpc.js，rpc.js 属 pr-002）——图实查确认；②`router.status` 方法由 router.js 分发、registry.js 快照投影（§4.4/§4.6）；③status.test.js 复用 harness.js/agent.js/租约扫描（§10.2/§5.6）——真实存在。
  - **pr-004 → pr-002**：①§10.1「假节点 = test/helpers/fake-node.js：复用 src/node-client.js」——文件级引用证据；②契约测试对手方为 router.js 消息分发与 registry.js pendingDeliveries（§4.4/§5.5）、harness 拉起 Router（§10.2）；③真实 agent 受理经 agent.js/node-client.js（§10.1）——真实存在。
- 不通过部分（证据引用失实，2 处）：
  - **pr-002 depends_on 理由①** 声称「模块引用证据 = brief 与 §3.1/§3.3 数据流『config 被 router/agent/status 消费』」——该语句**不存在于** architecture.md §3.1/§3.3：§3.1 组件图（第 54 行 `CFG["src/config.js…"]`）无任何指向/出自 CFG 的边；§3.3 数据流①注册/②心跳/③消息/④status/⑤退出均无 config 环节；全文检索「消费」零命中。config 的消费关系仅能间接支撑：§3.2 文件树注释「config.js = 默认值 + env 读取 + 数值校验」+ §7.2 表 env 作用对象列（agent/Router/全部）。「brief」亦不属于可核实的章节证据。
  - **pr-003 depends_on 理由①（pr-001 边）** 以「模块引用证据 = 『config 被 router/agent/status 消费』」支撑 status.js 消费 config.js——同一不存在语句；该边可核实证据实为 §7.2「OAMP_SOCKET 作用对象 = 全部」+ §7.3 定位 + §3.2 config.js 职责注释。
- 判定说明：4 条依赖边**均成立**且各自保有至少一条真实可定位的证据（上述通过部分），故依赖关系本身可信；但其中 2 条边的理由引用了规格文档中不存在的表述，属证据引用准确性与「证据能定位到章节」要求的部分不满足——不判 fail（边成立、无环、调度不受影响），记 partial，需修正引用或由架构文档补 config 消费边（见偏差 D1）。

### S6 依赖图无环 —— pass

证据：
- 依赖边全集：pr-002 → pr-001；pr-003 → pr-001；pr-003 → pr-002；pr-004 → pr-002。共 4 条有向边、4 个节点。
- 拓扑序存在：pr-001 → pr-002 → {pr-003, pr-004}；pr-003 与 pr-004 均只挂在 pr-002 之后、互相独立，无反向边、无环。
- batch 分组（1 / 2 / 3+3）与依赖结构一致（batch 非调度依据，仅佐证划分者意图无冲突）。

### S7 每 PR 验收标准可独立判断、不依赖其他 PR 合并才能判断（可审查/独立性） —— pass

证据：
- **pr-001**：验收 1 显式声明「无需任何本 PR 之外的文件存在」并在命令中只跑本 PR 的两个测试文件（`node --test test/cli.test.js test/hygiene.test.js`）；验收 2 行为抽查（空/未知子命令、缺 instance-id、-h/--help）覆盖的均为不触发未落地模块的误用路径（上下文摘要已声明 cli.js 分发分支延迟加载、模块缺失时仍可执行错误路径——pr-001 内实现的约束，非后续 PR 依赖）；验收 3/4（package.json/.gitignore/README 内容核查）只查本 PR 文件。四条在 pr-001 合并时点均可独立判定。F01 验收 1/3/7/8 的完整复现被显式声明「不在本 PR 独立验收」（见 S3 说明），未混入本 PR 验收标准。
- **pr-002**：验收 1 的 `npm test`/`node --test test/` 在 pr-002 合并时点只发现已合并的 pr-001 测试 + 本 PR 三个测试文件（status.test.js/delivery-contract.test.js 尚未存在，不参与），其余验收均针对本 PR 文件与其声明的 pr-001 依赖（子进程入口）。无任何验收项引用 pr-003/pr-004 文件。
- **pr-003**：验收需真实 Router + agent（pr-001/pr-002，已在 depends_on 声明），本 PR 只新增 status.js/status.test.js 消费既有栈，合并 pr-001+pr-002 后即可判定。
- **pr-004**：验收只依赖 pr-002（NodeClient/harness/router.js 消息分发，已声明），本 PR 只新增两个测试载体文件，判定不依赖 pr-003。
- 各 PR 验收标准均为「本 PR 文件 + 已声明依赖合并后」即可判定的形态，无「等后续 PR 合并才能判定」的验收项混入。

### S8 与 architecture.md 的一致性：PR 划分未引入 architecture.md 未定义的新组件/文件 —— pass

证据：
- 四份 PR 文件范围并集 = architecture.md §3.2 文件树的精确 22 文件（见 S4 逐项比对），零新增、零遗漏——没有出现树外文件（如额外测试文件、新 src 模块、配置文件）。
- 无新增组件：src 模块全集（cli/config/rpc/registry/router/node-client/agent/status/log）与架构 §3.2/§11 奥卡姆检验的组件清单一致；测试文件 ↔ 功能卡映射（architecture.md §10.2 表）与 PR 归属一一对应（cli→F01、router-registry→F02、agent-heartbeat→F03+F04、status→F05、event-log→F06、delivery-contract→F07、hygiene→F08）。
- 跨 PR 共栖（pr-002 携带消息域实现代码）不构成新组件——代码落在架构已定义文件上，PR 内已披露理由（见偏差 D2）。

---

## 汇总

- pass：7 项（S1/S2/S3/S4/S6/S7/S8）
- fail：0 项（需返工）
- partial：1 项（S5——4 条依赖边成立，但 2 处证据引用失实）
- blocked：0 项（缺信息无法判定）

## 偏差记录

> 规划产出物与规格文档不一致/引用失实处。不影响结论判定，供主 agent 决定是否返工或开新迭代。

| 规格/文档描述 | 规划产物实际表述 | 建议处理 |
|---|---|---|
| architecture.md §3.1/§3.3 不含「config 被 router/agent/status 消费」任何语句（§3.1 组件图 CFG 节点无出入边、§3.3 数据流①~⑤无 config 环节、全文「消费」零命中；config 消费仅能由 §3.2 职责注释 + §7.2 env 作用对象表间接推出） | pr-002 depends_on 理由①、pr-003 depends_on 理由① 将该语句作为模块引用证据引用，pr-002 明确标注其位于「§3.1/§3.3 数据流」 | 二选一：PR 文件修正为引用 §3.2/§7.2 实际内容（并删除不可核实来源「brief」）；或 architecture.md §3.1 补 config 消费边（§3.1 图/§3.3 增补一句「router/agent/status 经 config.js 读取 env 配置」）。建议后者——架构文档的 config 消费边缺位是根因 |
| workflow-pb 格式规范「上下文摘要：…200 字以内」 | pr-002 上下文摘要显著超长（单行被截断至 768 字符仍未结束，含边界说明/实现约束大段内容）；pr-001/pr-003 摘要亦近或超 200 字 | 若保留 200 字约束，后续 PR 规划收敛摘要；或将约束放宽并写明容纳边界说明的位置——由工作流 owner 决定，不阻塞本次 |
| F07 卡（含运行时实现语义：send 同步代理/pendingDeliveries/ack 校验/D10~D12）在 prd 中为独立功能点 | pr-002 文件范围承载 F07 运行时实现代码（router.js message.* 分发、registry.js pendingDeliveries、node-client.js send/ack、log.js MSG 事件）但其「涉及功能点」仅列 F02/F03/F04/F06；F07 只被纯测试 PR-004 引用 | 维持现状（两 PR 均已显式披露「同文件共栖、无法文件级拆分」理由，与零重叠约束自洽）或 pr-002「涉及功能点」补注 F07（实现侧）。记录供主 agent 知悉阶段 5 验收 pr-002 时消息域代码无本 PR 测试覆盖（见下一迭代候选 C1） |
| F08-1（git check-ignore 命中实际产物）与 F01-1（E1 全链路 npm test 全绿）/F01-7/8（长驻/README 手测可复现）为 prd 验收标准 | 不落任何 PR 验收标准：F08-1 由 architecture.md §9 显式指派阶段 6 独立验证，F01-1/7/8 在 pr-001 中声明「最终可复现依赖后续 PR 合并后的整体状态」 | 与 architecture §9 一致、非矛盾；但需主 agent 确保阶段 6（或迭代收尾）验证清单显式包含 F08-1 的 git 实查与全链路 E1 npm test，否则这两条 prd 验收将无验收归属（见下一迭代候选 C2） |

## 下一迭代候选

- C1（F07 实现与验收分离的风险）：pr-002 合入迭代分支时，消息域运行时代码（send/deliver/ack 分发、pendingDeliveries、NodeClient 自动受理）零测试覆盖——其验收载体 delivery-contract.test.js 属 pr-004，错误只在 pr-004 暴露。若 pr-004 失败/阻塞，迭代分支将持有未经验证的消息域代码。建议：接受该依赖设计（prd F07 本就是契约测试形态）或未来在含共栖代码的 PR 中加一条该功能点关键路径冒烟验收。
- C2（F08-1 与 E1 全链路的验收归属）：F08-1（git check-ignore）与 F01 验收 1/7/8 的完整复现无 PR 归属、依赖阶段 6/收尾兜底。建议主 agent 在阶段 6 验证委托中显式列出 F08-1 git 实查（需真实运行 Router 产生产物）与全链路手测/E1 验证。
- C3（pr-004 对 pr-001 的传递依赖）：pr-004 的测试经 harness.js 以 `node bin/oamp.js …` 子进程方式执行，间接必需 pr-001 的 bin/oamp.js/cli.js/package.json（type=module），但仅列直接依赖 pr-002。当前 pr-004→pr-002→pr-001 链已保证合并顺序、无调度风险；若未来 harness 拆分或 CLI 入口变更，注意把该传递依赖显式化。

## 结论

**PASS**（fail 0 项；partial 1 项——S5 证据引用失实已附偏差 D1 与修正建议，不阻塞进入阶段 5；依赖图、文件范围、功能点覆盖、验收独立性全部通过）
