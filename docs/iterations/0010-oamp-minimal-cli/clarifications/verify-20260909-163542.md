# Verify — PR-001 project-skeleton-cli-hygiene（阶段 5 验收）

```
验证者身份：同级代码审查者 + 冲刺计划审查者（复合）
  反射依据：产出物 = 代码实现（oamp/ CLI 骨架 + 卫生基线 + 两张 node:test 卡）+
           任务图（pr-001-tasks.md）。代码实现 → 同级代码审查者（CLI 语义/退出码/流路由/
           测试真伪/模块加载）；任务图 → 冲刺计划审查者（范围一致性/依赖图/无越界）。
产出物：
  - /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0010-pr-001-skeleton/oamp/
    （package.json、bin/oamp.js、src/cli.js、src/config.js、.gitignore、README.md、
      test/cli.test.js、test/hygiene.test.js）
  - /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0010-pr-001-skeleton/docs/
    iterations/0010-oamp-minimal-cli/prs/pr-001-tasks.md
验证标准来源：
  1. docs/iterations/0010-oamp-minimal-cli/prs/pr-001-project-skeleton-cli-hygiene.md
     验收标准 4 条（scripts.test 已按修订为 glob 形态，以当前文件内容为准）
  2. prd/F01-cli-entry-subcommand-dispatch.md、prd/F08-hygiene-runtime-artifacts-credentials.md
     （M-01/M-02/M-03 已 user_confirmed）
  3. architecture.md §3.2（文件树）/§7.1（argv 分发）/§7.2（配置面）/§9（卫生）
验证日期：2026-09-09
```

## 逐项判定

### V1 测试全绿（node --test 双文件 + npm test）：pass

证据：
- `oamp/` 下 `node --test test/cli.test.js test/hygiene.test.js` → **11/11 pass / 0 fail / 0 cancelled**，进程退出码 0（TAP 尾部 `# tests 11 # pass 11 # fail 0`；`node_test_exit=0`）。
- `npm test`（即 `node --test test/*.test.js`）→ 同 **11/11 pass**，退出码 0（`npm_test_exit=0`）。
- cli.test.js 9 条用例覆盖空参/未知子命令/router 缺子动词/agent 未知子命令/agent start 缺参/-h/--help/package.json 载体；hygiene.test.js 3 条覆盖 .gitignore 规则/凭据扫描/dependencies 为空。无任何本 PR 之外文件参与（worktree 中无 helpers/、无其他测试文件）。

### V2 CLI 误用行为（空参/未知/非法/缺 instance-id → exit 2 + stderr 用法 + 不挂起；-h/--help → exit 0 + stdout）：pass

证据（`node bin/oamp.js …` 直跑 + cli.test.js 断言双重复核）：
- 空参 → exit **2**；stdout 空；stderr `错误: 未指定命令` + 用法（src/cli.js:46 `argv.length===0` → usageError）。
- 未知子命令 `bogus` → exit **2**；stderr 含 `未知命令: bogus` + 用法（cli.js:78；测试用例断言通过）。
- 非法形态：`router` 缺子动词 → exit **2**，stderr `错误: 缺少 router 子命令`（cli.js:56-58）；`agent foo` → exit **2**，stderr `错误: 未知的 agent 子命令: foo`（cli.js:65-67）。
- `agent start` 缺 instance-id → exit **2**；stderr 精确含 `missing required argument: instance-id`（cli.js:72，与 §7.1 原文逐字一致）+ 用法；stdout 空。
- `-h` / `--help` → exit **0**；stdout 含 `用法:` 与三个命令形态；stderr 空（cli.js:52-54）。
- 不挂起：cli.test.js 全部用例经 `spawnSync(…, timeout: 5000)` 且断言 `result.error === undefined`；直跑各路径即时返回（探针 wall time 毫秒级）。

### V3 模块缺失路径（router start / agent start <id> / status → 明确报错 + 非零退出 + 不挂起 + 不静默）：pass

证据（前台 `timeout 5` 探针，超时即 124 = 挂起，均未触发）：
- `router start` → exit **1**，stderr `oamp: router start 模块尚未实现（./router.js 不存在；随后续 PR 落地）`，stdout 空，探针 0.13s 内干净退出。
- `agent start dev-1` → exit **1**，stderr 同型 `./agent.js 不存在`，干净退出。
- `status` → exit **1**，stderr 同型 `./status.js 不存在`，干净退出。
- 实现机制：cli.js 仅 `await import(moduleFile)` **动态**加载（cli.js:32），`ERR_MODULE_NOT_FOUND` 分支显式报"模块尚未实现"（cli.js:34-35）；模块缺失退出码 1 与 tasks.md T3-AC7 / MI-3 锁定的取值一致。错误路径在模块文件不存在时仍可加载执行（实现约束满足）。
- 说明：首次探针曾误报 HANG，为探针未 `wait` 回收僵尸进程导致 `kill -0` 误判的假阳性；前台 timeout 复测三路径均干净退出，无挂起。

### V4 文件/命令核查（package.json 字段 / .gitignore / README D15 / 零 YAML）：pass

证据：
- **package.json**（实读 + cli.test.js 载体断言）：`name=oamp`、`private=true`、`type=module`、`bin={oamp:"./bin/oamp.js"}`、`engines.node=">=22"`、`scripts.test="node --test test/*.test.js"`（glob 形态，与修订一致）、`dependencies={}`（零运行时依赖）。
- **bin/oamp.js**：首行 `#!/usr/bin/env node`；文件模式 `-rwxr-xr-x`（100755，chmod +x 生效）；内容仅 shebang + `import { main }` + `process.exitCode = await main(process.argv.slice(2))`，无分发逻辑（§3.2"仅转发"）。
- **.gitignore**：单行 `.runtime/`（无 node_modules——零依赖无此必要）。
- **README.md**：章节顺序按 D15——快速开始（同时给 `npm link` 与 `node bin/oamp.js` 两种方式）→ E2 手测步骤（三终端骨架，含 ROUTER_READY 就绪行/kill 后等租约超时见 offline，并注明"Router/agent 完整能力随后续 PR 落地"）→ 环境变量参数表（四 env + 默认值 10000/30000/60000 与 socket 默认路径）→ 协议速览 → 卫生红线声明（两条红线，F08-3）。
- **零 YAML**：`find oamp/ -name '*.yaml' -o -name '*.yml'` 空结果（exit 0 无输出）；运行参数全部走 env（README 明示"无 YAML 配置面"）。
- **零额外文件**：oamp/ 下恰为 PR 文件范围 8 个文件（glob 实查：package.json/README.md/.gitignore/bin/oamp.js/src/{cli,config}.js/test/{cli,hygiene}.test.js），无越界产物。

### V5 卫生（凭据字段名静态扫描零命中 / cli.js、config.js 无越界 import）：pass

证据：
- **凭据扫描**：独立 grep（`\b(token|api_key|secret|password|credential|authorization|private_key)\b`，大小写不敏感）扫 oamp/ 全树——命中仅在 `README.md:57/66/67`（F08-3 红线声明文本，**不在** F08-2/§9 扫描范围 bin/、src/、package.json 内）与 `test/hygiene.test.js:14-17`（模式定义，设计上排除 test/）。`bin/`、`src/`、`package.json` 零命中；hygiene.test.js 对应断言通过。
- **cli.js 越界 import**：全文件**零静态 import**（唯一加载为 cli.js:32 动态 `import(moduleFile)`，moduleFile 为运行时字符串，不可能静态引用未落地的 router.js/agent.js/status.js）；亦不 import config.js（§3.1"cli.js 仅做子命令分发、不读配置"）。
- **config.js 叶子**：仅 `import { fileURLToPath } from 'node:url'` + `import path from 'node:path'`（config.js:6-7），无任何 src/ 内 import（§3.2 叶子约束）。
- **config.js 行为（§7.2 运行核查，补充证据）**：env 全缺省 → socket 默认 `<oamp 包根>/.runtime/router.sock`（经 `import.meta.url` 推导，与 cwd 无关——直跑输出锚定 worktree 内 oamp/ 目录）、数值默认 10000/30000/60000；env 覆盖生效（`OAMP_SOCKET`/三数值各自覆盖）；非法值（`'abc'`/`'0'`）→ 抛错且错误信息含 env 名与"正整数"要求（`OAMP 配置错误: OAMP_HEARTBEAT_INTERVAL_MS 需为正整数…`），快速失败。导出形态 `loadConfig(env)` + 默认实例 + 四字段（socketPath/heartbeatIntervalMs/heartbeatTimeoutMs/hbLogWindowMs）与 MI-2 接口约定一致。
- **git 侧忽略（F08-1，O-3 移交项）**：worktree 内 `git check-ignore -v --no-index .runtime/router.sock` → 命中 `oamp/.gitignore:1:.runtime/`，exit 0（`--no-index` 未创建任何文件，无副作用）。

### V6 tasks.md 与 PR 文件范围一致性（任务图未引入范围外工作）：pass

证据：
- 任务图 T1~T7 涉及文件集合 = PR 文件范围 8 文件（T1→package.json；T2→config.js；T3→cli.js；T4→bin/oamp.js；T5→cli.test.js；T6→hygiene.test.js；T7→.gitignore+README.md）；T8 为集成验收、无新增文件。范围声明明示"不实现 pr-002/003 模块（router.js/agent.js/status.js 本 PR 不创建）"，与 worktree 实查一致（无这些文件、无 helpers/）。
- 依赖图（T1→T5/T6、T3→T4→T5/T6、T2/T7 并行支线、T5/T6/T7→T8 汇点）无环、无虚假串行；关键路径 T3→T4→T5→T8 标注正确；T2 为终端叶子任务（消费方 pr-002/003）标注不构成断链。
- 修订记录（scripts.test glob 裁决、T1-AC3/T5-AC4 同步）与实现一致：package.json `scripts.test="node --test test/*.test.js"`、cli.test.js 断言同值。
- MI-1~MI-4 生效约定与实现吻合（config 验收载体为运行核查——本报告已执行；模块缺失退出码 1；非法形态退出 2；cli.test 含非法形态用例）。
- worktree `git status --short` 仅见 `?? oamp/` 与 `?? pr-001-tasks.md`，无范围外新增/修改。

## 汇总

- pass: 6 项
- fail: 0 项（需返工）
- partial: 0 项（部分需返工）
- blocked: 0 项（缺信息无法判定）

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在 merge 前处理或由主 agent 决策。

| 规格/文档描述 | 实际状态 | 建议处理 |
|---|---|---|
| workflow-pb 提交管理 Rule A：变更必须提交在独立 worktree 分支上（每次提交为可工作状态）；merge 前置 = PR 产出物已纳入版本库 | worktree 分支 `feat/0010-pr-001-skeleton` HEAD = `ee0f7fb`（= 迭代分支尖），**分支上零个 PR 提交**；`git status --short` 显示 `oamp/`（8 文件）与 `pr-001-tasks.md` 全部未跟踪（`??`）。实现文件在工作区齐全且全部判定 pass，但尚未提交 | merge 前由主 agent/dev 将 8 个 oamp 文件 + pr-001-tasks.md 提交到 `feat/0010-pr-001-skeleton` 分支，再合并进迭代分支（流程状态偏差，非实现缺陷） |

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制。不在当前验收标准范围内，供主 agent 和 planner 决策。

- **空字符串 instance-id 语义未定义**：`oamp agent start ""` 中 `""` 不等于 `undefined`，cli.js 不判空，将落入"模块尚未实现"路径退出 1 而非用法错误 2（§7.1 仅定义"缺 instance-id"）。建议 pr-002/003 落地 agent.js 后由模块侧校验（instance-id ≤64 可打印字符，§4.6 register 校验规则），届时 cli 是否需补空串拦截由消费方裁决。
- **多余位置参数透传**（O-2）：`router start extra` / `status extra` 的语义在 §7.1 未定义，本 PR 透传不校验；pr-002/003 各自定义命令自身参数处理时需对齐——tasks.md 已记录，维持跟踪即可。
- **F08-1 git 侧真产物核查**（O-3）：本报告以 `git check-ignore --no-index` 对模拟路径验证了忽略规则命中；待 Router 真实运行产生 socket 产物后的阶段 6 复查（需真实 git 状态 + 后续 PR 落地 Router）可作为正式闭环项。

## 结论

**PASS**（所有条目 pass，无 fail / partial / blocked）

偏差记录 1 条（提交状态，流程性偏差，不阻塞本验收判定；merge 前须补提交）。报告全程未接收执行过程上下文，仅依据产出物 + 验证标准判定。
