# 阶段 6 · 迭代终态独立验证报告

**验证者身份**：**多智能体权限面独立鉴定人**（反射结果：本次产出物 = 「agent↔web 权限挂起/裁决链路 + 控制台第三栏 + 通知面」的终态代码与迭代文档；验证标准 = PR 粒度框构 / 依赖正确性 / 并发真实执行证据 / 12 卡覆盖 / M4 双向硬义务 / 端到端可用性 / 回归与洁净。⇒ 反射出的身份 = **同时能在 ACP 协议层、Node 进程间链路层、浏览器前端层与 PR 规划层做交叉取证的资深评审者**——不是通用检查员，而是「知道 ACP `session/request_permission` + `elicitation/create` 双门模型的坑、也知道 PR 粒度评判陷阱」的人）

**产出物**：
1. 迭代产物目录 `docs/iterations/0021-confirmation-inbox-and-event-push/`（`demand.md` v1.1.0 / `prd.md` + `prd/F01~F12`（12 卡）/ `architecture.md` / `prs/pr-0NN-*.md`（4 PR + 4 tasks）/ `status.md` / `history.md` / `progress.md` / `clarifications/**`）
2. 代码终态 = 迭代分支 `iteration/0021-confirmation-inbox-and-event-push` tip **`697176f`**，4 个 merge commit：`1f7eceb`(pr-003) / `c84233a`(pr-001) / `017961a`(pr-004) / `697176f`(pr-002)
3. 上游合同：`demand.md`（15 项裁决全 `user_confirmed`）+ `prd/F01~F12.md`

**验证标准来源**：主 agent 委托（A~G 七组逐项判定）＋ `demand.md` 第二段 §2/§4/§5（W/M/E）＋ `prd/*.md` 各卡「验收标准 / 边界 / 架构落定」。

**验证日期**：2026-09-13（本报告所有命令与实测均在 **21:24–21:37 +0800** 窗口内、工作区 `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0021-confirmation-inbox-and-event-push`、迭代分支 `697176f` 上执行）

**需求变更搭置**：**已确认 `docs/iterations/0021-confirmation-inbox-and-event-push/deferred-demand-changes.md` 不存在**（`test -e` → NOT-EXISTS；`find . -name deferred-demand-changes.md` → 零命中）⇒ **本迭代无需求层搭置，无搭置内容可摘录**。

**独立性声明**：本报告未接收任何执行过程上下文；产出物内的自评文字（`history.md` / `status.md` / 既有 `clarifications/verify-*.md`）一律**未作为判定依据**，仅用于事后对照「实现与既有记录是否一致」。全部判定建立在：**我亲自执行的命令输出**、**我自建的探针实测**（`/tmp/m4-probe.mjs`、真实 router/web/agent 进程链、真实浏览器驱动）、**被测文件的原文行号**。

**验证方法（可复查）**：
- **M4 双向**：自建 `/tmp/m4-probe.mjs`——直接构造仓库内 `AcpClient`（`bin=/Users/chenchiyuan/.bun/bin/omp` v18.0.11、`tools:true` ⇒ argv 含 `--approval-mode always-ask`），包住实例的 `_handleMessage` / `_write` 记录 **ACP 帧级收发**，以「模型侧工具结果 + 磁盘副作用」为判据，审计行仅记录不判定。4 个场景各跑一次：`allow`(bash) / `deny`(bash) / `write`(Approve) / `write-deny`(Deny)。
- **端到端**：真实进程链 router（`OAMP_SOCKET=/tmp/e2e-0021/router.sock`）+ web（`--port 7799`；未触碰本机既有 7788 实例）+ agent（`e2e-dev` 与 `pb-dev`，`--role dev --tools on --permission allow`，真实 omp）+ 真实 Chromium（`browser` 工具）。
- **清理**：4 个 hub 进程已 stop（`e2e0021-router` / `-web` / `-agent` / `-agent-dev` / `-sse` 全部 exited）、浏览器 tab 已释放（`Released 1 managed tab`）、`lsof -iTCP:7799` → **端口已释放**；`/tmp/e2e-0021`、`/tmp/m4-probe-run`、`/tmp/m4-probe.mjs` 于报告落盘后删除（见 §清理）。

---

## 逐项判定

### A. PR 粒度判断框构（4 个 PR × 逻辑原子性 / 可审查性 / 独立性）

三点 diff 取证（`merge-base(merge^1,merge^2)` = 分支起点；`merge^2` = 分支 tip；`merge^1..merge` = 合并结果的第一父 diff）：

| PR | 分支 base | tip | merge | 首父 diff | 涉及卡 |
|---|---|---|---|---|---|
| pr-001 | `10cbca6` | `dd7badd` | `c84233a` | 4 文件 / **+861 −34** | F02 F04 F05 F12 |
| pr-002 | `c84233a` | `4d16ba4` | `697176f` | 5 文件 / **+1090 −12** | F02 F04 F05 |
| pr-003 | `10cbca6` | `0d9cbc2` | `1f7eceb` | 10 文件 / **+949 −29** | F01~F08 F10 F11 F12 |
| pr-004 | `1f7eceb` | `a431225` | `017961a` | 9 文件 / **+1172 −8** | F01 F03 F06 F07 F08 F09 |

- **pr-001 逻辑原子性：pass**
  证据：分支仅 4 个提交（`27eef14` 任务图 / `dd59d31` 主实现 / `1f11b59` 第 2 轮 / `dd7badd` 第 3 轮），改动集中在**一个关注点**——`AcpClient` 的「判定可挂起」语义：`oamp/src/acp-client.js:434-437`（`session/request_permission` 改为 `void this._handlePermissionRequest(message)` 异步化）、`:449-470`（`_handlePermissionRequest` await 判定后才 `_respond`）、`:484-501`（`_permissionDecision` 返回域扩为 `{deny, optionId}`）、`:548-570`（`_askHook` + `_pauseTurnTimer`/`_resumeTurnTimer` 冻结轮次计时）。M4 答复链路修复与挂起语义同属一条不可分割的因果链（挂起成立 ⇔ 应答值必须可回显），非「顺带加的功能」。
- **pr-001 可审查性：pass**。4 文件 / +861 −34，其中新断言 487 行集中在 `oamp/test/tool-permission.test.js`；`git diff c84233a^1 c84233a -- oamp/test/acp-daemon.test.js` = **1 行改动**（argv 断言 `yolo`→`always-ask`）。无跨模块散点。
- **pr-001 独立性：pass**。`depends_on` 为空；merge base = `10cbca6`（阶段 4 Gate 提交），与 pr-003 同点起飞；实际合并于 `c84233a`（20:45:46），此前分支已被独立验证（委托以外的既有报告不作为判据；我的判据 = 它在终态里真实工作，见 E/F 实测）。
- **pr-002 逻辑原子性：pass**。改动 = `oamp/src/context-pool.js`（+10：`onPermissionRequest` 透传与**唯一档位判定点**，见 `context-pool.js:222-232`）+ `oamp/src/agent.js`（+111：`raiseConfirmation` / `settleConfirmation` / `cancelPending` / 钩子注入 `/agent.js:705` / pending 表 `/agent.js:694`）。同一关注点：**agent 进程内的信封 1·2·3 收发**。
- **pr-002 可审查性：pass**。5 文件 / +1090 −12（`oamp/test/confirmation-roundtrip.test.js` 741 行新建为主）。文件面比 PR 文档声明多 1 个（`oamp/test/acp-daemon.test.js`），**由 Q7 裁决显式授权**（`clarifications/pr002-round-1-verdicts.md:39`「`oamp/test/acp-daemon.test.js` 由本裁决显式加入 pr-002 文件面（第 5 个文件）」）⇒ 计偏差（文档未回填），不计 fail。
- **pr-002 独立性：pass（有条件）**。`depends_on: pr-001`，且该依赖在**代码级成立**（见 B-1）；base = `c84233a`（pr-001 的合并点）⇒ 顺序化，无冲突。
- **pr-003 逻辑原子性：pass**。改动 = web 进程承载确认面（`oamp/src/inbox.js` 新建 42 行 + `oamp/src/web.js` +134 + 派生面 `API.md`/`llms.txt` + 5 个测试文件同步）。单一关注点：**在途表 + 两条新路由 + 信封消费 + 全局广播**。
- **pr-003 可审查性：pass**。10 文件 / +949 −29；`git diff 1f7eceb^1 1f7eceb -- oamp/API.md oamp/llms.txt` 仅 98 行（新增 §3.20/§3.21 + §4.2 事件集合），既有 19 条路由正文零改写（`api-routes.test.js` / `project-workspace.test.js` / `call-protocol.test.js` 的条数与登记集合断言同步）。
- **pr-003 独立性：pass**。`depends_on` 为空；merge base = `10cbca6`；与 pr-001 同在首波并各自独立合并（`1f7eceb` 早于 `c84233a` 3.5 小时）。
- **pr-004 逻辑原子性：pass**。改动 = 控制台第三栏 + 通知面（`oamp/web/notify.js` 新建 108 行、`index.html` +10、`app.js` +162、`style.css` +81）。单一关注点：**前端呈现与投递**。
- **pr-004 可审查性：pass**。9 文件 / +1172 −8（`inbox-console.test.js` 364 + `notification-scope.test.js` 247 为新建；两者均在 Q6 裁决授权范围内，`clarifications/pr004-round-1-verdicts.md:57-70`）。
- **pr-004 独立性：pass（有条件）**。`depends_on: pr-003`，代码级成立（见 B-2）；base = `1f7eceb`（pr-003 的合并点）。

### B. 依赖正确性

- **B-1 `pr-002 → pr-001` 的代码级证据：pass**
  **基线反证**（`git show 854766c:oamp/src/acp-client.js`，即分支起点前的共享祖先）：`:397-400` 为**同步**判定 + 立即回包——
  ```
  397: if (message.method === 'session/request_permission') {
  399:   const allow = this._permissionDecision(message, toolCall) !== 'deny';
  400:   this._respond(message.id, { outcome: { outcome: 'selected', optionId: allow ? 'allow_once' : 'reject_once' } });
  ```
  且 `:415-423` 的 `_permissionDecision` 只识别 `'allow'|'deny'` 两个字符串，**钩子返回 Promise 时返回 `undefined`** ⇒ `undefined !== 'deny'` ⇒ **立即 allow**。⇒ 基线状态下 pr-002 注入的「未结算 Promise」钩子无法产生挂起，且基线 argv (`:123`) 为 `'deny' ? always-ask : yolo` ⇒ `allow` 档走 `yolo`。
  **终态一致性**：`acp-client.js:434-437` 已异步化，`:449-470` 在判定结算后才 `_respond`，`:483-501` 识别 `{optionId}` 与 Promise。⇒ 依赖声明的「共享接口（钩子返回值域）+ 共享符号（`_handleServerRequest`/`_permissionDecision`）」**逐条对上**。
- **B-2 `pr-004 → pr-003` 的代码级证据：pass**
  **基线反证**：`git show 854766c:oamp/src/web.js | grep -c confirmations` → **0**；基线 `STATIC_FILES`（`:410-422`）**无** `'/notify.js'` 条目。
  **终态一致性**：`web.js:1258`（`GET /api/confirmations`）、`:1275`（`POST /api/confirmations/:confirmation_id/decision`）、`:1608`（`transport.publishGlobal({type:'confirmation', data: entry})` 为全局 `confirmation` 帧唯一发布点）、`:435`（`'/notify.js': 'web/notify.js'`）。⇒ pr-004 消费的三样东西（两条路由 / 一类全局帧 / 静态白名单）在基线**全部不存在**，依赖为真。
- **B-3 依赖图无环：pass**。声明 `pr-001→pr-002`、`pr-003→pr-004`；两条边互不相交，拓扑上为两个长度 2 的链，无环；合并顺序 `1f7eceb`(13:21) → `c84233a`(20:45) → `017961a`(20:50) → `697176f`(21:23) 与其 `depends_on` 完全一致（每个被依赖 PR 的合并时间早于依赖者）。
- **B-4 PR 间文件范围无重叠（按 PR 文档「文件范围」字段）：pass**
  逐对比较 4 份 PR 的声明范围：pr-001{acp-client, tool-permission.test, acp-daemon.test} / pr-002{context-pool, agent, confirmation-roundtrip.test, context-pool.test(条件)} / pr-003{inbox, web, API.md, llms.txt, 5 测试} / pr-004{web/notify, index.html, app.js, style.css, inbox-console.test} ⇒ **两两交集为空**。
  ⚠ **实际改动面另有 1 处重叠**（`oamp/test/acp-daemon.test.js` 同时出现在 pr-001 与 pr-002 的首父 diff：pr-001 `1 insertion 1 deletion`，pr-002 `29` 行）——**顺序化、无冲突**（pr-002 的 base `c84233a` 已含 pr-001 的改动），且 pr-002 侧由 Q7 授权。⇒ 记为偏差 D-3，**不判 fail**（判定对象是 PR 规划期的文件范围字段，即阶段 4 产物）。
- **B-5 `prd/*.md` 12 张卡 → PR 映射（逐卡，无幻影引用）：pass**

| 卡 | 引用它的 PR（来源：各 PR「涉及功能点」段） | 是否至少被 1 个 PR 引用 |
|---|---|---|
| F01 | pr-003, pr-004 | ✅ |
| F02 | pr-001, pr-002, pr-003 | ✅ |
| F03 | pr-003, pr-004 | ✅ |
| F04 | pr-001, pr-002, pr-003 | ✅ |
| F05 | pr-001, pr-002, pr-003 | ✅ |
| F06 | pr-003, pr-004 | ✅ |
| F07 | pr-003, pr-004 | ✅ |
| F08 | pr-003, pr-004 | ✅ |
| F09 | pr-004 | ✅ |
| F10 | pr-003 | ✅ |
| F11 | pr-003 | ✅ |
| F12 | pr-001, pr-003 | ✅ |

  **无幻影引用**：上表每个「卡→PR」对都对应到 §D 的**文件:行级落点**（逐卡证据见下）；反向亦无遗漏（4 个 PR 声明的 15 个引用全部命中 12 卡之内，未出现 `demand.md` 之外的新卡）。

### C. 并发调度真实执行证据（3 项）

- **C-1 worktree 时间窗口重叠（≥2 个 PR 分支在磁盘上同时存在过）：pass（首波有据；次波无据，见偏差 D-4）**
  一手证据链（全部来自 git 对象库，非文档叙述）：
  - **首波 `pr-003 ∥ pr-001`**：两分支同 base `10cbca6`；pr-003 分支提交 `a5b2043@12:30:45`→`0d9cbc2@12:53:56`；pr-001 分支提交 `27eef14@12:30:45`→`dd59d31@12:57:51`（其后 `1f11b59@13:26:51`）。pr-003 分支于 `13:21:40` 合并，**pr-001 分支其时仍然存活**（直到 `20:45:46` 才合并）⇒ 在 **[12:30:45, 13:21:40] 区间内 `feat/0021-pr-003-*` 与 `feat/0021-pr-001-*` 两个分支引用同时存在于 `.git`**（`git reflog --all` 的 merge 记录分别给出两分支名与删除时刻；`git log <merge>^2` 给出各自分支上的提交时间戳）。
  - **旁证（更长窗口）**：pr-004 分支的首个提交 `cce5f8b@13:27:19`，与 pr-001 分支（存活至 20:45:46）在 **[13:27:19, 20:45:46]** 内共存；同期两分支各自产生提交（`76e33d5@20:09:41` / `dd7badd@20:10:34`，相隔 53 秒）。
  - **两波两两合并提交相隔 4 分钟**（`0d9cbc2@12:53:56` 与 `dd59d31@12:57:51`）⇒ 两个工作树确实在**同一时间窗内各自推进**。
- **C-2 并发配置区块真实初始化与更新：pass**
  `git log --follow -p -- status.md` 显示 **5 次真实写入**：
  - `10cbca6`(12:24:40) **初始化**五字段：起始并发数 3 / 硬上限 5（`2×起始-1`）/ 当前有效上限 3（=起始）/ 累计槛位释放次数 0 / 已派发总数 0；
  - `7e979ac`(13:21:39) 更新已派发总数 → 6；
  - `59f9cba`(20:05:41) 更新有效上限 3→**5**、释放次数 0→**1**、派发 9；
  - `267bc21`(20:50:30) 释放 2、派发 11；
  - `d2c4b25`(20:56:02) 释放 3、派发 12。
- **C-3 爬升公式真实触发：pass**
  `59f9cba` 明确写出 `当前有效上限 = min(3 + 1×3, 5) = 5`（当时释放 1 次）⇒ 公式被**真实代入并重算**，结果为 `min(6,5)=5` = 硬上限。当前工作区版本（21:2x，主 agent 未提交的更新）进一步写为 `5（= min(3 + 4×3, 5) = 5，**已触硬上限**；历次释放：pr-003 / pr-001 / pr-004 / pr-002 各 1 次）` + `累计槛位释放次数 4` ⇒ **公式计算与「已触硬上限」的判定依据均被如实记录**，且与我独立核算的 `min(3+4×3,5)=5` 一致。

### D. 12 张功能卡覆盖复核（产品维度，逐卡文件:行级证据）

- **F01 待确认 inbox（第三栏的存在与跨对话集中）：pass**
  落点：`oamp/web/index.html:62-67`（`<section id="inbox">` 位于 `.sidebar` 与 `.detail` 之间）；`oamp/web/style.css` 三列规则；`oamp/web/app.js:626-627`（重建面）、`:639+`（条目渲染）、`:673-674`（移除）；服务端 `oamp/src/inbox.js:1-42`。
  实测：浏览器 DOM 直接子节点 = `["ASIDE.sidebar","SECTION.inbox","SECTION.detail"]`，`#inbox` 渲染矩形 `x=320,w=320,h=852`（三栏并列）；**无刷新**新增：页面加载后由全局 `confirmation` 帧驱动追加，栏内文本由「待确认 0 / 暂无待确认项」变为「待确认 1 / `e2e-dev · chat-cc3c37c6-…` / `null · echo E2E-UI-…` / Allow once … Always reject」；两条并存时栏内同时列出（并发两条 SCOPE-A/SCOPE-B 均在同一栏内可见）。验收 4（可辨识来源对话）：条目首行渲染 `e2e-dev · chat-214c3513-421b-4a29-ba38-3fa05bc8644e` ⇒ 可读出所属对话 ✅。
- **F02 确认源上浮：pass**
  落点：`oamp/src/context-pool.js:222-232`（上浮钩子**唯一注入点**，`permission === 'allow'` 才注入，`deny` 档恒 `null`）；`oamp/src/agent.js:314-336`（`raiseConfirmation` 组信封 1，含 `options` 原样映射 `:302-311`）；`oamp/src/acp-client.js:449-470`（先上浮、后回包）。
  实测（真实 omp 全链）：`allow` 档发起 bash ⇒ `GET /api/confirmations` 出现条目且**该调用未被自动放行**（工作目录 `ls` 为空，命令在裁决前未执行）；条目 `options` = `[{option_id:allow_once,label:Allow once},{allow_always},{reject_once,Reject},{reject_always}]`——与 omp 在 `session/request_permission` 里给出的四项**逐字一致**（同一组四项也由我的 M4 探针在帧级记录到 `options:["allow_once","allow_always","reject_once","reject_always"]`）；非门禁操作（纯文本消息 / hub 调用）零新增条目；`deny` 语义由 `context-pool.js:229` 的档位判定保证（deny 档不注入 ⇒ `acp-client.js:463-469` 的自动拒绝三步原样生效）。
- **F03 栏内裁决交互：pass**
  落点：`oamp/web/app.js:682-694`（点选即 POST、200/404 即移出、不等 SSE）、`:639-671`（渲染选项按钮 + 文本输入）；`oamp/src/web.js:1275-1341`（服务端校验 option_id 合法性 + 文本追加派发）。
  实测：真实鼠标点击 `Reject`（`page.mouse.click(544.7,185.7)`）⇒ 栏内 `#inbox-item` 数 **2→0**（条目立即消失）；`{"option_id":"reject_once","text":"e2e 拒绝理由：不允许动工作目录"}` 提交后该文本**作为该对话的一条 `chat` 输入落库并派发**（`GET /api/chats/<id>` 的消息列表出现 `in | e2e 拒绝理由：…`）⇒ **选项与文本一并提交**（非两段式）✅。
- **F04 裁决回路：pass**
  落点：`oamp/src/web.js:1295-1341`（`inbox.take()` 原子取出 → `sendControlNotice({kind:'confirmation_decision'…})`）；`oamp/src/agent.js:340-352`（`settleConfirmation` 按 `confirmation_id` 结算）；`oamp/src/acp-client.js:459-460`（`{optionId}` 回 `_respond`）。
  实测：① **放行** `allow_once` ⇒ 聊天推进到 `completed`、模型侧报出**真实工具输出** `E2E-0021-1789306165`（退出码 0）；② **拒绝** `reject_once` ⇒ 该聊天的 `out` 消息 = `工具调用被 permission 策略拒绝（permission=deny）`，`error=permission_denied`，状态 `failed`，且 **marker 文件未生成**（`/tmp/e2e-0021/work/` 空）；③ **作用域**：同 agent 两对话各挂一条（`cfm-335788c7…` SCOPE-A / `cfm-3d1ae3f3…` SCOPE-B），裁决 A 后 **A 的聊天 completed、B 仍在 `working` 且 B 的条目仍在栏内** ✅。
- **F05 未裁决时的挂起语义：pass**
  落点：`oamp/src/inbox.js`（**零定时器 / 零 TTL**，`take()` 即删）；`oamp/src/acp-client.js:576-608`（`_pauseTurnTimer` / `_resumeTurnTimer` 冻结并恢复轮次计时，按深度支持同轮重叠挂起）；`oamp/src/agent.js:353-365`（失效清扫而非超时收尾）。
  实测：条目在裁决前**持续停留**（多条消息间隔数十秒后重新查询仍在，轮次未推进、命令未执行）；全站无策略配置面——`grep -rn "策略|policy|默认选项|自动放行|timeout" oamp/web/notify.js oamp/web/app.js` **零命中**，`git diff 1f7eceb 017961a -- oamp/web/index.html` 显示本次仅新增 inbox 骨架与 `<script src="/notify.js">`，**未新增任何配置控件**。
- **F06 在途项的可见性：pass**
  落点：`oamp/web/app.js:166-181`（全局 `confirmation` 帧去重追加）、`:204` 与 `:542`（每次 `onopen` 重新对齐）、`:626-627`（`GET /api/confirmations` 全量重建）；服务端 `oamp/src/inbox.js:22-29`。
  实测：① **刷新**：`tab.goto('http://127.0.0.1:7799/')` 重建 JS 上下文后重新进入项目 ⇒ 条目**仍在**且 `data-confirmation` 与刷新前**同为 `cfm-8dcbf2cf-8d61-4933-a0ea-4ff544290338`**，文本（来源 / 操作 / 选项集合）逐字一致；② **只重建在途项**：经由 HTTP 在带外裁决掉两条后刷新 ⇒ 栏内 `#inbox-item` 计数 **2→0**，已裁决项**不回流**（服务端 `GET /api/confirmations` 返回 `{"confirmations":[]}`）✅。
- **F07 通知事件类型与产生时机（封闭 3 类）：pass**
  落点：`oamp/web/notify.js:12`（`EVENT_TYPES = Object.freeze(['chat_completed','chat_failed','confirmation_required'])` 为唯一真源）；`oamp/web/app.js:181`（确认类）/ `:199`（终态类派生）；`oamp/src/web.js:1482` + `:1608`（两类事实帧的唯一发布点）。
  实测：浏览器实测 `window.oampNotify.EVENT_TYPES` = 恰好这 3 项；时序上浏览器通知实际出现且**仅出现这 3 类标题**（见 F08）；**不含 `call_completed`**：一次 `POST /api/calls`（`mode:block`，`call_id=task-f680c24d-b06d-4352-bd50-5dba1bcd81c0`，终态 `completed`）在**同一页面、同一记录器**下产生 **0 条通知**（`window.__notes === []`），而同一记录器此前刚记录到真实聊天类通知（正向对照）✅；`grep -rn "call_completed" oamp/` 在生产代码中零命中（仅 `web.js:76` 的注释说明）。
- **F08 通知投递（首个通道 = 浏览器通知）：pass**
  落点：`oamp/web/notify.js:60-80`（`deliver`：`Notification.permission !== 'granted'` 直接静默返回 false，`new Notification(...)` + `onclick`）、`:84-95`（`requestPermission` 仅在手势内调用，至多一次）、`oamp/web/app.js:1120`（首次点击时请求权限）。
  实测（`overridePermissions(origin,['notifications'])` + 包裹真实构造器记录）：三类事件**各触发一次浏览器通知**——`需要你确认`（`e2e-dev 请求执行 null：echo E2E-SCOPE-A-…`，两条各一次，**重建不重复**：刷新前后 `confirmation_required` 未再触发）、`对话已完成`（两条，对应 `chat-214c3513…` 与纯文本 `chat-eaafc9e1…`）、`对话失败`（两条，对应被拒的 `chat-6b58dc56…` 与 UI 点击拒绝的 `chat-cc3c37c6…`）；通知在**页面停在项目/工作区视图、并不停留在该对话时**照常出现 ✅；页面加载期 `Notification.permission` 为 `default` 且 `__notes` 为空（未在加载时请求/投递）。
- **F09 通知服务与通道分离：pass**
  落点：`oamp/web/notify.js` 的 `service` 段（`:16-56`）与 `channel` 段（`:58-95`）；
  实测（静态分片计数）：`awk '/service 层/,/channel 层/' | grep -c Notification` = **0**；`awk '/channel 层/,0' | grep -cE "chat_completed|chat_failed|confirmation_required|call_completed"` = **0** ⇒ 两侧零命中，扩展/替换通道不动事件类型契约（F09 验收 2 的可验证形状成立）。
- **F10 服务边界不变（保证项）：pass**
  实测：`lsof -nP -iTCP:7799 -sTCP:LISTEN` = `node … TCP 127.0.0.1:7799 (LISTEN)`（仅回环）；`oamp/src/web.js:1737` = `server.listen(port, '127.0.0.1', resolve)`（构造处未改）；零鉴权（全程无任何凭据即可读写）；`oamp/package.json` `dependencies = {}`（**零新依赖**）；投递路径在**页面内** Notification API，服务端**零新增出网路径**（`notify.js` 全文无 `fetch`/`WebSocket`/Service Worker）⇒「边界只约束服务本身」与实现一致。
- **F11 无确认项历史台账（保证项）：pass**
  落点：`oamp/src/inbox.js` 导出面**恰 5 个函数**（`add/list/take/remove/size`，`:13/22/27/35/40`），无 `history/export/decided` 类入口；`take()` 原子删除（`:27-33`）。
  实测：裁决后 `GET /api/confirmations` 立即为 `[]` 且**不回流**（刷新后仍为 0）；`oamp/src/persist.js` **零改动**（`git diff --name-only 854766c..HEAD -- oamp/src/persist.js` 为空）；无新表 / 无迁移。
- **F12 上游协议零改动（保证项）：pass**
  实测：`git diff --name-only main..HEAD` 的全部 70 个文件中，`grep -E '^(omp|harness|roles|\.pb-agents|\.claude)'` → **NONE**（改动全部落在 `oamp/**` 与 `docs/iterations/0021-*/**`）；ACP 侧仅「何时应答」与「应答值」两处变化，方法名与报文形状不变（`session/request_permission` / `elicitation/create` 原文未改）；`oamp/src/router.js` 零改动（复用既有 `notice` 类型 + 新增 `kind`）。

### E. M4 硬义务终态复核（真实 omp，双向，审计行不作判据）

探针：`/tmp/m4-probe.mjs`（构造仓库内 `AcpClient`，`bin=/Users/chenchiyuan/.bun/bin/omp` v18.0.11，`cwd=/tmp/m4-probe-run`）。**判据 = 模型侧工具结果 + 磁盘副作用**；`TOOL_APPROVED` / `TOOL_DENIED` 仅记录、未参与判定。

| 场景 | 门 | 钩子回值 | ACP 回包（帧级实测） | **模型侧工具结果** | **磁盘副作用** |
|---|---|---|---|---|---|
| bash + 放行 | ACP 权限门 | `{optionId:'allow_once'}` | `{"outcome":{"outcome":"selected","optionId":"allow_once"}}` | `status:completed`，内容 = `M4-ALLOW-1789306063202` + `Wall time: 0.03 seconds`（**真实输出，非 denied**）；模型复述逐字一致 | 命令真实执行 |
| bash + 拒绝 | ACP 权限门 | `{optionId:'reject_once'}` | `{"outcome":{"outcome":"selected","optionId":"reject_once"}}` | 轮次以 `permission_denied` 终止；tool_call 帧停在 `pending`（**从未执行**） | `markerCreated=false` |
| write + 放行 | 工具审批门 | `{optionId:'Approve'}` | （elicit 应答 `accept/Approve`） | `in_progress: Writing 22 bytes…` → `completed: Successfully wrote 22 bytes to …`；模型读回内容逐字节一致 | 文件存在且内容 = `M4-WRITE-…` |
| write + 拒绝 | 工具审批门 | `{optionId:'Deny'}` | （elicit 应答 `accept/Deny`） | **`Tool call denied by user: write`**（模型侧确实拿到 denied，且模型自述「未用 bash 绕过」） | 文件不存在；后续 `read` 报 `Path not found` |

- **E-1「放行 ⇒ 模型侧工具结果 = 工具真实输出且副作用落盘」：pass**（bash 与 write 两路均成立）
- **E-2「拒绝 ⇒ 模型侧确实 denied 且未执行」：pass**（两路均成立；write 路给出模型侧显式 denied 文本，bash 路给出轮次终止 + 工具帧从未执行 + 磁盘无痕）
- **E-3「审计行不得作判据」：pass**（判据全部取自 ACP 帧内容与文件系统；本报告未引用任何 `TOOL_APPROVED/TOOL_DENIED` 作为通过依据）
- **E-4「不经 ACP 权限门的写类工具（`write`）不再被静默拒绝」：pass**（`write` 经 `elicitation/create` 的工具审批门上浮，钩子按请求自身选项回 `Approve` ⇒ 文件真实写入；回 `Deny` ⇒ 明确拒绝且无副作用）
  ⚠ 附注（非 fail，见偏差 D-1）：该结论成立的前提是**裁决回值取自该请求自身的选项集合**（审批门 = `Approve|Deny`）；若回值是与该门不相容的 optionId（如 `allow_once`），`acp-client.js:568-575` 的保守语义会判为 `Deny`——这是设计意图（审批门绝不默认放行），但**两型门的选项词汇不同**（`allow_once…` vs `Approve/Deny`）这一点值得在文档中显式登记。

### F. 端到端可用性（真实进程链复现）

拓扑（全部真实进程，`OAMP_SOCKET=/tmp/e2e-0021/router.sock`、`OAMP_DB=/tmp/e2e-0021/oamp.db`、web `--port 7799`，**未触碰本机既有 7788 实例**）：`router` + `web` + 两个 `agent`（`e2e-dev` / `pb-dev`，`--role dev --tools on --permission allow`，真实 omp）+ 真实 Chromium。

- **F-1「`allow` 档触发确认 → 控制台第三栏出现该条 → 点选裁决 → agent 继续 / 中止」全链：pass**
  证据链（同一次会话内，逐段可复查）：
  1. `POST /api/messages`（bash 命令含唯一 token）⇒ `GET /api/confirmations` 由 `[]` 变为含 1 条（`cfm-034bf052…`），条目含 `chat_id/agent_id/tool/title/options/created_at`；
  2. 全局订阅 `GET /api/events` 收到 **恰一帧** `event: confirmation`（`data` 与列表元素同形状），且该帧只在**首次入表**时出现（重建面 `GET /api/confirmations` 不发帧）；
  3. 浏览器：项目视图切到工作区后三栏并列，条目**无需刷新**出现；点击 `Reject` ⇒ 栏内该条立即消失；
  4. agent 侧后果：`chat-cc3c37c6…` → `state=failed`，`out` 消息 `工具调用被 permission 策略拒绝…` `error=permission_denied`；放行路径（`cfm-9f19d518…` + `allow_once`）⇒ `chat-e6c55823…` → `completed`，模型报出真实输出 `E2E-0021-1789306165`；
  5. 通知：`需要你确认` / `对话已完成` / `对话失败` 三类各在浏览器实际出现。
- **F-2「hub 调用面完成不产生通知」：pass**
  `/api/messages` 与 `/api/calls` 两条路径的对照实测：`POST /api/calls`（`mode:block`）终态 `completed` 后，`GET /api/events` **无任何**该 `chat_id` 的全局 `chat_state` 帧（SSE 日志逐帧核对：调用发生前后仅 `agent_online pb-dev` 一行，无新 `chat_state`），浏览器记录器 `__notes === []`；同一记录器在同一页面对消息驱动终态**有**通知 ⇒ 排除「记录器失效」的可能。

### G. 回归与仓库洁净

- **G-1 全量回归 `node --test oamp/test/*.test.js`（迭代分支终态 `697176f`）：pass（含 1 次不可复现失败，见下）**
  共 **9 次**运行：
  - 第 1 次（**与本机真实 omp 探针并发**）：`tests 332 / pass 330 / **fail 2**` ——**未捕获到失败用例名**（当时只取了尾部输出），此后 8 次均未复现；
  - 第 2~9 次：`tests 332 / **pass 332 / fail 0**`（`duration ≈ 61s`），其中第 7~9 次为**完全无并发负载**的干净跑（连跑 3 次全绿）。
  ⇒ 结论：**终态全量 332/332 绿（8/9 次）**；1 次 2 条失败既未复现也未能定位 ⇒ 记入偏差 D-5 与下一迭代候选（不得声称「无 flake」）。
- **G-2 `git status --porcelain` / `git worktree list`：pass（有一处并发写入，非缺陷）**
  - `git worktree list` → 恰 2 条：`/Users/chenchiyuan/projects/agents d84b2be [main]` 与 `…/.pb-agents/worktrees/0021-confirmation-inbox-and-event-push 697176f [iteration/0021-…]`；`.pb-agents/worktrees/` 下**仅此一个**目录；`.git/worktrees/` 下**仅此一个**元数据目录；`.pb-agents/worktrees/*` 无残留 PR 工作树 ⇒ **4 个 PR 的 worktree/分支已清理干净**（`git branch -a` 仅 `iterations/0021…` 与 `main`）✅。
  - `git status --porcelain` → ` M docs/…/history.md`、` M docs/…/progress.md`、` M docs/…/status.md`（**均为 docs-only**，且**是我验证期间主 agent 正在写入的阶段 6 台账**：第一次读取时只有 `history.md` 一项，其后变为 3 项；`status.md` 的未提交 diff 正是把「阶段 5 进行中 → 阶段 5 完成 / 阶段 6 进行中」与并发计数 3→4 更新到位）。⇒ **产品代码零未提交改动**，迭代终态代码 = `697176f` 可复现。
  - 报告自身为 `clarifications/` 下的新增未跟踪文件（本报告落点，允许的写入）。
- **G-3 仓库根未被改动：pass**
  `git -C /Users/chenchiyuan/projects/agents status --porcelain` 初次执行时仅剩 `?? xd-browser-open.json`（**我自己**在一次失败的 shell 重定向尝试中误建的临时文件）⇒ **已 `rm -f` 删除**，复检干净；仓库根 `main` = `d84b2be`，本轮验证**未对其做任何写入**。

---

## 汇总

| 判定 | 计数 | 明细 |
|---|---|---|
| **pass** | **33** | A×4（pr-001/002/003/004）+ B×5（B-1~B-5）+ C×3（C-1~C-3）+ D×12（F01~F12）+ E×4（E-1~E-4）+ F×2（F-1/F-2）+ G×3（G-1~G-3） |
| **fail** | **0** | — |
| **partial** | **0** | — |
| **blocked** | **0** | — |

> 计数口径（逐条判定条目）：A = 4 条（pr-001/002/003/004）；B = 5 条（B-1~B-5）；C = 3 条（C-1~C-3）；D = 12 条（F01~F12）；E = 4 条（E-1~E-4）；F = 2 条（F-1/F-2）；G = 3 条（G-1~G-3）⇒ **合计 33 条，全部 pass**。
> 另设 1 条**非 A~G 的集成前置事实**（§结论下方「集成前置事实」），**不计入上述计数**。

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。**不影响本次验收判定**，但应在下一迭代中同步文档或评估是否调整实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| **D-1** | `architecture.md` §7 T-02：条目一行 = 「工具名 + `title`」；§7 T-09：通知正文模板 = `${agent_id} 请求执行 ${tool}：${title}` | **ACP 权限门来源的确认项 `tool` 恒为 `null`**（`agent.js:330` 取 `toolCall.toolName`，而 omp 的 `session/request_permission` 报文只带 `title`/`kind`，不带 `toolName`；工具审批门来源才有 `toolName='write'`）。⇒ 第三栏渲染出字面量 **`null · echo E2E-…`**（浏览器 DOM 实测 `inbox-request` 文本），浏览器通知正文为 **`e2e-dev 请求执行 null：echo …`**（实测 `window.__notes[0].body`）。**用户可见面缺陷** | 按实现调整二者之一：① `agent.js` 的 `raiseConfirmation` 对权限门来源用 `toolCall.kind`/`title` 派生可读工具名（或省略该字段），② 或 `notify.js:41` 的正文模板不再引用 `tool`；随后把「工具名取值口径」回填 `prd/F01` T-02 与 `architecture` §7 T-02/T-09 |
| **D-2** | `prd/F03` 验收 4「裁决完成后该条从栏内消失」（未限定裁决发生的位置） | 条目移除只发生在**本页提交返回 200**（`app.js:691`）或**重建**（首屏 / 每次 `onopen`，`app.js:626-627`）时。**经 API 在带外裁决**（另一标签页 / 脚本）时，其它标签页的栏内会**残留陈旧条目**直到刷新或重连；点击该陈旧条目才因 404 被移除（`app.js:693`） | 低severity。可在裁决路径补一帧全局 `confirmation_resolved`（或复用既有失效帧）驱动跨页移除；或在 `prd/F03` 边界里显式写明「裁决面 = 当前控制台页」 |
| **D-3** | 4 份 PR 文档的「文件范围」字段 | 实际改动面有 3 处超出/交叉：① `oamp/test/acp-daemon.test.js` **同时**出现在 pr-001（+1/−1）与 pr-002（29 行）的合并 diff（pr-002 侧由 **Q7 裁决**授权，`pr002-round-1-verdicts.md:39`）；② pr-002 实际 5 文件（声明 4 + Q7 授权的 1）；③ pr-004 实际 9 文件（声明 5 + **Q6 裁决**授权的 `src/web.js` / `llms.txt` / 新建 `test/notification-scope.test.js`，`pr004-round-1-verdicts.md:57-70`） | 按实现回填 PR 文件范围（或加「授权扩范围」小节），使阶段 4 产物与阶段 5 实现对齐；对下游「文件范围无重叠」的自动化核对有意义 |
| **D-4** | `status.md` 并发配置：`首波可并发 = {pr-001, pr-003}，次波 = {pr-002, pr-004}`；`history.md` 亦有「pr-002/pr-004 并行」叙述 | **首波并发有一手证据**（两分支 12:30:45–13:21:40 共存）；**次波无任何磁盘/引用级证据，且 git 时间线与「并行」相反**——pr-004 分支于 `20:50:30` 合并（其 tip 提交 `20:33:41`），而 pr-002 分支的 base 是 `c84233a`(`20:45:46`)、首个提交为 `20:52:03` ⇒ 若存在重叠，窗口 ≤ 4 分 44 秒且不可证。 | 台账按事实改写为「首波并发执行；次波顺序执行（pr-002 在 pr-004 合并后起飞）」；若确须并行，下一迭代把「依赖解锁后立即起飞」写进调度约束 |
| **D-5** | `history.md`「oamp 全量 **322/322 两次连跑均全绿**（无 flake）」（pr-002 验收记录） | 我实测 **9 次**：332 用例，**8 次 332/332 绿**，**1 次 330/332（2 条失败，用例名未捕获；此后 8 次不可复现，含 3 次零并发负载连跑全绿）**；该次运行的唯一环境差异 = 同时有真实 omp 探针占用 CPU/网络（`[INFERENCE]`：并发负载相关，未证实） | 不改变「终态可绿」的结论；下一迭代把这条 flake 定位并钉死（建议在测试用 fake ACP 的静默窗口/计时器处加确定性等待），并停止在台账中写「无 flake」这类绝对断言 |
| **D-6** | `oamp/src/web.js:435` 的 `STATIC_FILES` 白名单 + `API.md` / `llms.txt` 派生面 | 与实现一致，但 `history.md` 之外无独立记录说明「新增 2 条路由 ⇒ `llms.txt` 必须重生成」这一**漂移锁**的触发条件 | 可忽略（既有测试 `api-routes.test.js` / `call-protocol.test.js` 已锁死）；仅登记以便下一迭代复用 |
| **D-7** | `status.md` 工作区版的「已派发总数 23（… 阶段 6：verifier×1 已计入 verifier 计数）」 | 该计数把**本次验证自身**计入「已派发」，而「已派发」的历史口径是「阶段 5 派发」。自指计数使该字段在阶段 6 期间不收敛（我读到的两次快照：committed 版 12 → 工作区版 23） | 低severity。阶段 6 的派发单列一行，或把字段改名为「阶段 5 已派发总数」以固定口径 |

---

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制。不在当前验收标准范围内，供主 agent 与 planner 决策。

1. **`tool` 字段取值口径（对应 D-1）**：真正修一次需动 `agent.js` 的一个字段推导 + 通知文案，代价小、用户可见收益大（当前第三栏与浏览器通知都会出现字面量 `null`）。
2. **跨标签页陈旧条目（对应 D-2）**：需要一条「裁决完成」的全局帧；在此之前「多页同时打开」是可达的陈旧态。
3. **合并 main 的常量冲突（见「集成前置事实」）**：`oamp/src/agent.js` 超时常量块需保留 main 侧 30 分钟口径，同时保留本迭代新增的 `CONFIRMATION_TITLE_MAX`。
4. **超时口径的覆盖关系**：main `d84b2be` 把 agent 侧 omp 超时上限提到 30 分钟，而 `web.js` 的 `RECONCILE_TTL_DEFAULT_MS` 亦为 30 分钟——其注释已自陈由「覆盖有余」变为「持平」，需评估是否需要抬高 TTL。
5. **`prd/F01` 验收 4 的呈现措辞**：实现渲染为 `agent_id · chat_id/title` 前缀（Q6 的 D-2 已登记），建议按实现回填规格措辞。
6. **测试 flake（对应 D-5）**：把 332 用例中与时序窗口相关的用例钉成确定性，消除 1/7 的失败率。
7. **通知噪声**：`confirmation_required` 在并发多条确认时逐条弹出（本次实测同时 2 条），文案 80 字符截断且不合并（T-10 的 YAGNI 决策合理）；若下一迭代引入批量确认，应重新评估 T-10。
8. **`F01` 栏内排序 / 分组 / 条数上限**：仍按 prd 既有裁定保持出界（回退口 = 回 `demand.md` 补一条 W）。

---

## 集成前置事实（不属 A~G，但决定「合并进 main」这一步能否直接执行）

**任务要求判定「迭代可收口并合并进 main」。除 A~G 外，我额外做了一次只读的合并预演：**

```
$ git --version → 2.39.5
$ git merge-tree --write-tree --name-only main HEAD
1916cd120b1df961f1f552e896b895d22502e23b
oamp/src/agent.js
Auto-merging oamp/src/acp-client.js
Auto-merging oamp/src/agent.js
CONFLICT (content): Merge conflict in oamp/src/agent.js
Auto-merging oamp/src/web.js
exit=1
```

- **事实**：迭代分支的 merge-base 是 `854766c`，而 **main 已前进 1 个提交 `d84b2be`**（「omp 单轮超时默认与上限 5 分钟 → 30 分钟」，2026-09-13 用户指示），其改动落在 `oamp/README.md` / `oamp/src/acp-client.js` / `oamp/src/agent.js` / `oamp/src/web.js`，与本迭代改动的文件**重叠 3 个**，其中 `agent.js` 产生**内容冲突**。
- **冲突内容（`git show 1916cd1:oamp/src/agent.js` 第 28~35 行原文）**：
  ```
  <<<<<<< main
  const DEFAULT_OMP_TIMEOUT_MS = 1800000; // omp（LLM）任务默认：给足推理时间
  const MAX_TIMEOUT_MS = 1800000;
  =======
  const DEFAULT_OMP_TIMEOUT_MS = 300000; // omp（LLM）任务默认：给足推理时间
  const MAX_TIMEOUT_MS = 600000;
  const CONFIRMATION_TITLE_MAX = 120; // §5.3 信封 1（pr-002）：title 截断 120 字符（沿用 acp-client 的 TOOL_TITLE_MAX 口径）
  >>>>>>> HEAD
  ```
  ⇒ 冲突范围 = **同一常量块（2 行取值 + 1 行新增常量）**，与本迭代的挂起/裁决语义无关；解决方式为「取 main 的 30 分钟取值 + 保留本迭代新增的 `CONFIRMATION_TITLE_MAX`」。
- **对本判定的影响**：这**不是**本迭代产物的缺陷（迭代分支在其 base 上自洽、终态 332/332 绿、四项 PR 验收均通过），但**集成动作不能免人工**。因此：「迭代可收口」= 是；「合并进 main」= **可合并，但必须处理 1 处内容冲突（3 行）后 `git merge` 才能完成**。
- 该事实在 §结论 中同样标注，避免下游把 PASS 误读为「`git merge` 一定零冲突」。

---

## 结论

**PASS**（33 条判定：**33 pass / 0 fail / 0 partial / 0 blocked**）

- A（PR 粒度 4 条）pass · B（依赖正确性 5 条）pass · C（并发真实执行证据 3 条）pass（C-1 首波有据、次波无据已登记）· D（F01~F12 = 12 条）pass · E（M4 双向 4 条）pass · F（端到端 2 条）pass · G（回归与洁净 3 条）pass。
- **偏差记录 7 条**（其中 D-1 为用户可见面缺陷、D-5 为未复现的测试失败，其余为文档/台账口径）；**下一迭代候选 8 条**。
- **偏差不阻塞本次收口**：按 verifier 契约，偏差被显式记录即满足交付条件；是否开启新迭代处理由主 agent/用户决定。
- ⚠ **合并动作附条件**：迭代分支合入当前 `main`（`d84b2be`）时 **`oamp/src/agent.js` 存在 1 处内容冲突（3 行，见「集成前置事实」）**——结论为 PASS 指的是**迭代本身可收口**，不含「零冲突合并」这一强断言。

---

## 清理与残留

| 资源 | 处置 |
|---|---|
| hub 进程 `e2e0021-router` / `e2e0021-web` / `e2e0021-agent` / `e2e0021-agent-dev` / `e2e0021-sse` | **已全部 stop**（`exited`，uptime 5m41s~6m18s） |
| 端口 7799 | `lsof -nP -iTCP:7799 -sTCP:LISTEN` → **无输出（已释放）**；本机既有 7788 实例**全程未被触碰** |
| 浏览器 | `browser close --all --kill` → `Released 1 managed tab` |
| `/tmp/e2e-0021/`（socket、db、事件日志、token、工作目录） | **已 `rm -rf`**（复检：`No such file or directory`） |
| `/tmp/m4-probe-run/`（探针工作目录与产物） | **已 `rm -rf`**（复检：`No such file or directory`） |
| `/tmp/m4-probe.mjs`（自建探针） | **已删除**（复检：`No such file or directory`；脚本内容已在本报告 §E 完整描述，可重建） |
| 仓库根 `/Users/chenchiyuan/projects/agents` | 我误建的 `xd-browser-open.json` **已删除**；除此之外零写入 |
| 迭代工作区 | 唯一写入 = 本报告文件 |
