# pr-003 独立验证报告（0021 阶段 5）

**验证者身份**：熟悉 oamp web 进程 / SSE 键空间（`chat:` / `call:` / `call-calls:` / 全局 `null`）/ 既有「漂移锁」体系的**同级服务端代码审查者**，兼 wire 层协议契约验证者（有能力读这份代码，也知道这类实时链路的坑：帧「恰一次」、键隔离、旧断言的等价强度）。

**产出物**
1. 代码实现：worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0021-confirmation-inbox-and-event-push/.pb-agents/worktrees/0021-pr-003-web-inbox-and-decision-api`，分支 `feat/0021-pr-003-web-inbox-and-decision-api`，HEAD `0d9cbc289892ed4c9f16d6a931f60e503aad01b4`。判定面 = `git diff iteration/0021-confirmation-inbox-and-event-push...HEAD`（merge-base `10cbca6308a3b0d300a932bfd3b7f9f3ca624e2e`）。注：迭代分支在此 PR 分叉后另有 4 个 docs-only 提交（`124b048` / `f9c2806` / `a779ebd` / `bd39abb`，只动 `docs/iterations/0021-.../` 的 4 个记录文件），三点 diff 已正确排除；在本 PR 的 9 个文件上三点 diff 与两点 diff **逐字相同**（`diff <(git diff --name-status A...HEAD) <(git diff --name-status A..HEAD)` 仅差那 4 个 docs 文件）。
2. PR 契约：`docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-003-web-inbox-and-decision-api.md`
3. 任务图：`docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-003-tasks.md`
4. 背景规格（只读参考）：`docs/iterations/0021-confirmation-inbox-and-event-push/architecture.md`、`prd/F*.md`

**验证标准来源**：主 agent 委托指定的 A（PR 验收标准 12 条）/ B（`pr-003-tasks.md` T1~T6 全部验收标准）/ C（C1~C3 裁决口径）/ D（4 条硬性红线）。

**验证日期**：2026-09-13

**验证方法（全部为本次自测取证；未使用产出物的自评叙述作为判据）**

| 编号 | 手段 | 说明 |
|---|---|---|
| E1 | `node --test oamp/test/confirmation-inbox.test.js oamp/test/api-routes.test.js oamp/test/project-workspace.test.js oamp/test/call-protocol.test.js oamp/test/web.test.js` | 授权清单（单轮 84/84 绿） |
| E2 | 同 5 文件逐文件单独跑 + `node --test oamp/test/hygiene.test.js` | 逐文件通过数 |
| E3 | `node --test oamp/test/*.test.js` 共 7 轮 | 完整既有套件 |
| E4 | `git archive <base> \| tar -x -C /tmp` 抽 base 完整树，跑完整套件 6 轮 | 基线对照（判断偶发红是否本 PR 引入） |
| E5 | **自建探针 1/2/3**（真 Router 子进程 + 真 `oamp web start` + 真 HTTP/SSE + wire 层 `notice` 信封投递，逻辑自写、不复用产出物测试） | 帧级判据（恰一帧 / 重建零帧 / 重复投递不发帧 / 两链路同形 / 取消零帧 / 错误契约 / 文本落地 / 静态面 / 退化入参） |
| E6 | 模块级直调 `inbox.js` + `createApiRoutes` / `projectRoutes` / `matchRoute` / `renderLlmsTxt` | 在途表 5 函数语义、登记面形态、投影与匹配器 |
| E7 | `git show <base>:... \| diff` 与 hunk 行号区间分析 | 零改动清单与文件范围（机械判定，不靠阅读印象） |
| E8 | 把 `oamp/` 复制到 `/tmp` 后**重跑生成脚本** `node scripts/gen-llms-txt.mjs` 并 `cmp` | `llms.txt` 逐字节相等（不在被验 worktree 内写任何文件） |

被验 worktree 全程只读：未在其中 `commit`/`checkout`/`stash`/写文件；`/tmp` 下写探针与临时树。本报告是本人对迭代工作区的唯一写入。

---

## 逐项判定

### A. PR 文件「验收标准」12 条

**A1. `GET /api/confirmations` 形态 / 空态 / 不 404 / 不查 Router、不读库 / 元素 7 字段 — pass**

- 证据（E5）：空态 `GET /api/confirmations` → `200 {"confirmations":[]}`（无 404）。投递 1 条 `confirmation_request` 后 → `{confirmations:[{confirmation_id, chat_id, agent_id, tool, title, options, created_at}]}`，字段集合实测恰为 7 个：`["agent_id","chat_id","confirmation_id","created_at","options","title","tool"]`。
- 证据（E7，`oamp/src/web.js:1260-1263`）：handler 体只有 `sendJson(res, 200, { confirmations: inbox.list() })`——无 Router/DB 依赖、无帧发布。
- 证据（E1/E2）：`api-routes.test.js` 7/7、`project-workspace.test.js` 21/21 绿（空依赖 `createApiRoutes({})` 纯构造仍可构造该表项）。

**A2. `confirmation_request` → 入表 + 全局恰一帧（同形状）+ 重复投递不重发 — pass**

- 证据（E5 探针 1）：投递后 `GET /api/confirmations` 含该条；全局流（`GET /api/events`）`confirmation` 帧计数 = **1**；帧 `data` 与列表元素**逐字相等**（`JSON.stringify(frame.data) === JSON.stringify(list[0])` → `true`），键集合同 7 字段。
- 幂等：同一 `confirmation_id` 再投 2 次（其一改 `title='MUTATED'`）后，全局 `confirmation` 帧计数仍 = **1**，列表仍为首条原值（`title` 未被覆盖）。
- 跨来源幂等（自加）：同一 id 换 `agent_id='other-agent'` 再投 → 帧计数仍 = 1，条目仍为首次登记值。
- 证据（E7，`oamp/src/web.js:1588-1600`）：发帧点唯一 = `if (inbox.add(entry)) transport.publishGlobal({type:'confirmation', data: entry})`——幂等由 `add()` 的返回值把关。
- 定向面（自加，T3.1③）：以该条的 `chat_id` 订阅 `GET /api/stream?chat_id=…`，收到的 `confirmation` 帧数 = **0**（该流事件类型集合为空）。

**A3. 重建面不发任何帧 — pass**

- 证据（E5 探针 1）：连续 3 次 `GET /api/confirmations`（前后各留 250ms 观察窗）→ 全局流**帧总数增量为 0**（不只是 `confirmation` 类型为 0，是任何类型都没有）。
- 证据（E5 探针 2）：带查询参数的 `GET /api/confirmations?status=decided&limit=1&q=all` → 帧增量 0，且响应与无参调用逐字相同（无过滤面 / 无历史读取入口）。

**A4. R-2 合法裁决 → 200 `{confirmation_id, accepted:true}` + 立即移出（二次 404）+ 回传 `confirmation_decision` — pass**

- 证据（E5 探针 1）：`POST /api/confirmations/vfy-cfm-2/decision {option_id:'allow_always', text:'  理由文本  '}` → `200 {"confirmation_id":"vfy-cfm-2","accepted":true}`；紧随其后的列表只余未裁决的 `vfy-cfm-1`（该条已移出）。
- 第二次提交同一 id → `404 {"code":"NOT_FOUND"}`。
- 回传：发出方（wire 层 fake node）收到**恰 1 条** `notice`，body = `{kind:'confirmation_decision', confirmation_id:'vfy-cfm-2', option_id:'allow_always', text:'理由文本', chat_id:'chat-172f16c5-…'}`——`confirmation_id` 同值、`option_id` = 用户所选、`text` 为 trim 后值。

**A5. 错误契约（400 保留在途 / 404 同码不区分） — pass**

- 证据（E5 探针 1）：`{}` / `{option_id:42}` / `{option_id:'not_in_options'}` / `{option_id:''}` 四种入参 → 全部 `400 INVALID_PARAM`，且**每种之后**列表仍含该条（`still_in_flight:true`）。
- 从未存在的 id → `404 NOT_FOUND`；已裁决的 id 再提交 → `404 NOT_FOUND`（同一码，不区分）。
- 边界（自加，E5 探针 3）：无 body → `400 INVALID_PARAM` + 条目保留；非法 JSON → `400 INVALID_PARAM` + 条目保留；2 MiB 超限体 → `413 PAYLOAD_TOO_LARGE` + 条目保留；三种情况下 web 进程均存活（后续请求正常，`getExit()` 为 `null`）。

**A6. 文本非空白 ⇒ 追加输入并派发；空白不追加 — pass**

- 证据（E5 探针 1）：裁决带文本后，`GET /api/chats/<chat_id>` 的 `direction='in'` 条数 1 → **2**，末条 `text='理由文本'`；发出方收到 `task.request`，其 body `prompt='理由文本'`、`executor='omp-daemon'`、`chat_id` 为该对话。
- 空白：`text:'   '` → `200 {accepted:true}`，`in` 条数增量 **0**、`task.request` 增量 **0**，条目同样移出在途表。
- 复用方式见 C2（未抽取 `POST /api/messages` 内核）。

**A7. `confirmation_cancelled` → 移出在途表 + 不产生任何通知类广播 — pass**

- 证据（E5 探针 1）：投递后条目从列表消失（轮询确认），**全局流帧总数增量 = 0**。
- 边界（E5 探针 2）：对**从未在途**的 id 发 `confirmation_cancelled` → 无副作用、帧增量 0、列表不变。

**A8. 无历史台账（结构保证）+ `persist.js` 零改动 — pass**

- 证据（E6）：`Object.keys(inbox)` = `["add","list","remove","size","take"]`，5 个导出**全是函数**——无 `history` / `export` / `decided` 类入口。
- 证据（E5 探针 1/2）：已裁决条目在任何列表 / 任何查询参数下都不返回；`take()` 即删（`take` 后 `list()` 为空、再次 `take` 返回 `null`）。
- 证据（E7）：`git diff --name-status` 中**不存在** `oamp/src/persist.js`（零改动；同批未被改动的还有 `router.js` / `transport.js` / `cluster-config.js`）。

**A9. 三条漂移锁转绿且未削弱断言 — pass**

- 漂移锁②（`llms.txt` 逐字节）：E8 在 `/tmp` 复制树内重跑 `node scripts/gen-llms-txt.mjs`，输出「接口 **21** 条，**2837** 字节」，`cmp` 与仓库文件**逐字节相等**，`sha256 = 8b970e4681b8708c60b829e2d5dac62660f834caf32aec1811c3721940ebbada`（改前 = 改后）。标题行为 `## 接口（21 条）`。
- 漂移锁③（API.md ⇄ 登记集合双向覆盖）：`api-routes.test.js` 内该用例绿（E1/E2，7/7）；E6 实测两条新 `docLink` = `API.md#320-get-apiconfirmations` / `API.md#321-post-apiconfirmationsconfirmation_iddecision`，与新增小节标题一致。
- 「既有 19 条条目逐字不改」：E7 机械比对 `/tmp/API-old.md` vs 新文件——19 条 `| N | … |` 表行**逐字相同且同序**（新文件只是追加第 20/21 行）；19 个既有 `### 3.x` 小节标题为新增小节的**严格前缀**（追加 `### 3.20` / `### 3.21`）。
- 三测试文件硬编码断言同步：`api-routes.test.js`（`EXPECTED_SIGNATURES` 全量 `deepEqual` 列表 +2；标题 19→21；写接口 `danger` 计数 7→**8**）、`project-workspace.test.js`（`EXPECTED_ROUTE_SIGNATURES` 全量 `deepEqual` +2；`routes.length` 19→**21**；`slice(-6)` 6 元 `deepEqual`；两处标题；`danger` 7→8）、`call-protocol.test.js`（`routes.length` 19→21）。三者均为**等价强度**的清单 / 条数比较（详见 D3）。

**A10. `publishState` 既有 `chat:<id>` 逐字不变 + 新增全局广播使终态可观测 + 三面口径一致 — pass**

- 「既有发布逐字不变」：E7 ——`oamp/src/web.js:1473` 与 base 的对应行**逐字相同**（`transport.publish(chatId, { type: 'chat_state', data: { chat_id: chatId, state } });`），新增的只是紧随其后的一行 `transport.publishGlobal(...)`；`publishState` 的全部既有调用点未改。
- 「终态在全局链路可观测」+「每次调用追加一次」：E5 探针 1 实测——某对话经 3 次派发，全局流上该 `chat_id` 的 `chat_state` 帧 = **6** 帧（每次派发 `working` + `completed` 各一），其中**含 `working`**（非终态）⇒ 全局广播**按调用追加**，不按终态过滤。
- 「两链路同形」：同一轮次里，全局 `chat_state` 帧增量 **2** = 定向 `chat:<id>` 帧增量 **2**；`data` 键集合恰为 `["chat_id","state"]`；全局帧的每个 `data` 都能在定向帧集合中找到逐字相等者（`global ⊆ chat` 为 `true`）。
- 「三面一致」：API.md §4.2 标题「（**4** 类事件）」+ 事件表 4 行（`agent_online` / `agent_offline` / `confirmation` / `chat_state`）+ 键隔离表述（显式点名 `message`/`task_update`/`notice` 只走 `chat:<id>`，`chat_state` 两处都发、不新增事件类型）；`web.test.js:1502` 排除清单 = `['message','task_update','notice']`；实现侧 `publishGlobal` 的 type 取值全集（E7 全文件检索）= `agent_online` / `agent_offline` / `confirmation` / `chat_state`。三面一致（详见 C1）。

**A11. `STATIC_FILES` 含 `/notify.js`；文件缺席 ⇒ 404，无通配 / 无目录枚举 — pass**

- 证据（E7，`oamp/src/web.js:429-430`）：新增 `'/notify.js': 'web/notify.js',`——STATIC_FILES 区段的 diff 只有新增行，**既有条目零改写**。
- 证据（E5 探针 1）：`web/notify.js` 在仓库中不存在（`fs.existsSync` = false），`GET /notify.js` → **404**、`GET /web/notify.js` → **404**、`GET /notify.js.map` → **404**；`GET /app.js` → 200 且与仓库文件**逐字相等**；`GET /` → 200。

**A12. 五测试文件全绿；`hygiene` 保持绿 — pass**

- E1（授权清单，单轮）：`# tests 84 / # pass 84 / # fail 0 / # cancelled 0 / # skipped 0`，无失败原文。
- E2（逐文件）：`confirmation-inbox.test.js` 6/6、`api-routes.test.js` 7/7、`project-workspace.test.js` 21/21、`call-protocol.test.js` 11/11、`web.test.js` 39/39（合计 84）；`hygiene.test.js` 3/3。
- 旁证（E3/E4）：完整既有套件在 PR head 上 7 轮中 4 轮、在 **base 完整树**上 6 轮中 1 轮，出现**同一处**偶发红 `status.test.js:133`（F05-4，「last_heartbeat 第二轮应严格推进」）——**与 pr-003 无关的既有计时脆弱性**，见「下一迭代候选 1」。该文件不在本项的 5 文件清单内，且 5 文件在上述所有轮次中始终全绿。

### B. `pr-003-tasks.md` T1~T6 全部验收标准

**T1（`inbox.js` 五函数）— 5/5 pass**

1. **pass** — E6 直调：`add(first)`→`true` 且入 `list()`；`add({...first, title:'覆盖尝试'})`→`false`、`list()[0].title` 仍为首条原值、`size()` 不变；`add({})`→`false`、`add(null)`→`false`（无 id 不入表）。
2. **pass** — `take('A')` 返回该条且 `list()` 中消失；再次 `take('A')`→`null`。
3. **pass** — `remove('B')` 后条目消失；`remove('nope')` 返回 `undefined`、不抛错；`size() === list().length` 为 `true`。
4. **pass** — 导出面恰 5 个函数 `["add","list","remove","size","take"]`，无 history/export/decided 类入口（见 A8）。
5. **pass** — `hygiene.test.js` 3/3 绿（E2）：`src/inbox.js` 无凭据类字段名命中；`package.json` dependencies 仍为空。

**T2（R-1 / R-2）— 6/6 pass**

1. **pass** — 空态 `200 {confirmations:[]}`（不 404）；有项时 7 字段齐备；handler 只依赖注入的 in-process inbox（`web.js:1260-1263`，无 Router/DB 调用）。
2. **pass** — E6：`routes.slice(-2)` = `['GET /api/confirmations','POST /api/confirmations/:confirmation_id/decision']`（表末位）；两条表项 8 个元数据字段（`ROUTE_META_FIELDS`）**全在**、`handler` 均为 `function`；`projectRoutes(...).danger` = `[false, true]`（POST 派生 `true`）；`matchRoute(routes,'GET','/api/confirmations').route.path` = `/api/confirmations`（未被贪婪参数吞并）；`matchRoute(routes,'POST','/api/confirmations/cfm-9/decision').params` = `{confirmation_id:'cfm-9'}`；`GET` 同路径 → `null`。
3. **pass** — 见 A4。
4. **pass** — 见 A5。
5. **pass** — 见 A6。
6. **pass** — E7 机械判定：web.js 的全部**删除行**只有 4 行（1 行路由表注释、1 行 `publishState` 表达式〔被改写为块，其中既有 publish 行保留〕、1 行 `handleDeliver` JSDoc、1 行 body 守卫表达式〔语义等价改写〕）；被改动的新行区间为 `14`、`22-23`、`41`、`429-430`、`1249-1339`、`1469-1472`、`1474-1475`、`1577-1578`、`1584-1608`，与旧文件行号带 `429-1240`（既有 19 条 handler 所在区段）**无任何重叠**；`POST /api/messages` handler 块（107 行）在新旧两版中**逐字相同**；`server = http.createServer(...)`（`web.js:1700`）与 `server.listen(port,'127.0.0.1')`（`:1728`）均落在改动区间之外。现有套件全绿（A12）。

**T3（3 个新 `notice` kind 的消费）— 5/5 pass**

1. **pass** — ①列表含该条；②全局**恰一帧** `confirmation`，`data` 与列表元素同 7 字段且逐字相等；③该对话的 `GET /api/stream?chat_id=…` 收到 **0 帧**（任何类型）。
2. **pass** — 同 id 重复投递（含改字段、换 `agent_id`）→ 帧数保持 1、列表保持首条原值。
3. **pass** — 3 次重建调用 → 全局帧总数增量 0。
4. **pass** — 取消后条目移出、全局帧总数增量 0。
5. **pass** — E5 探针 2：`notice{kind:'context_released'}` → 定向流收到恰 1 帧 `notice`，`data` = `{chat_id, kind:'context_released', text}`，全局增量 **0**；`context_reset` 同形同量（第 2 帧）。未知名 kind / 非法 JSON body → 双链路均 0 帧（与改动前的「静默忽略」一致）。代码面上旧守卫 `body && typeof body.chat_id === 'string'` 被 `if (!body) return;` 等价替换，其余旧 kind 分支逐字未动（`web.js:1580-1610`）。

**T4（`publishState` 全局广播 + 键隔离断言同步）— 4/4 pass**

1. **pass** — 见 A10（既有 `chat:<id>` 行逐字未变；定向流帧形态 `{chat_id,state}`、时序 `working → completed`）。
2. **pass** — E5 探针 1：全局流上该对话同时出现 `working` 与 `completed`（不依赖停留在该对话）。
3. **pass** — 全局流事件类型全集实测 = `["confirmation","chat_state"]`（另有 `agent_online`/`agent_offline`，由 E1 的 `web.test.js` 用例 81 覆盖：起停 agent 各收一帧）；**不含** `message` / `task_update` / `notice`；实现复用既有事实类型（无新增事件类型常量，`publishGlobal` 的 type 取值只有 4 个既有/新事实名）。
4. **pass** — `web.test.js:1502` 的否定断言仍为 `.some()` 全流扫描，排除清单**显式命名** `message` / `task_update` / `notice`（不是子集断言、未删除），该文件 39/39 绿。

**T5（派生面同步）— 4/4 pass**

1. **pass** — API.md：§3 标题 `## 3. 接口清单（21 条）`；清单表追加第 20/21 行；新增 `### 3.20` / `### 3.21` 两小节（`params` / `response` / `errors` 字段级齐备，含 `INVALID_PARAM` / `NOT_FOUND` 错误表）；既有 19 条**逐字不改**（A9 机械比对）。
2. **pass** — E8：`llms.txt` 重生成后逐字节相等（sha256 同上），标题 `## 接口（21 条）`。
3. **pass** — 三处硬编码断言同步且未削弱（A9 / D3）。
4. **pass** — API.md §4.2 事件表 4 行 + 标题「4 类事件」+ 键隔离表述与 T4 验收 3 的事件集合逐条对齐（`message`/`task_update`/`notice` 排除；`chat_state` 两处都发）。

**T6（`STATIC_FILES` + 收口回归）— 3/3 pass**

1. **pass** — `'/notify.js': 'web/notify.js'` 已登记（`web.js:430`）；文件缺席时 `GET /notify.js` = 404、`GET /web/notify.js` = 404、`GET /notify.js.map` = 404（无通配 / 无目录枚举 / 无路径拼接）。
2. **pass** — `GET /app.js` 与仓库文件逐字相等；`GET /` = 200；静态面既有条目零改写（新增行仅 2 行）。
3. **pass** — 见 A12（5 文件 84/84 + hygiene 3/3）。

### C. 主 agent 裁决口径

**C1（全局广播按调用追加、复用 `chat_state`、三面一致） — pass**

- 实现：`web.js:1472-1475` —— `publishState` 先写既有 `chat:<id>` 帧（逐字不变），再 `transport.publishGlobal({type:'chat_state', data:{chat_id, state}})`，**无终态过滤**（E5 探针 1 实测全局链路出现非终态 `working` 帧；3 次派发 = 6 帧）。
- wire 层事件类型全集：`agent_online` / `agent_offline` / `confirmation` / `chat_state`（实现侧全部 `publishGlobal` 调用点的 type 取值；E1 的 `web.test.js` 用例 81 覆盖前两者）。
- `API.md §4.2`：标题条数 `4 类事件`；事件表 4 行；键隔离表述已改写为「`message` / `task_update` / `notice` **永远只**发往对应 `chat_id` 的订阅者；`chat_state` **两处都发**——既有 `chat:<id>` 帧的形态与时机逐字不变，另在全局键上**追加**一帧（同一 `data` 形状，不新增事件类型）」。
- `oamp/test/web.test.js:1502-1503`：排除清单为 `['message','task_update','notice']`——**只**去掉 `chat_state`，三个既有类型仍显式排除，断言未删除、未退化为子集断言。
- 三面一致，判定 pass。

**C2（文本落地不抽取 `POST /api/messages` 内核） — pass**

- `POST /api/messages` 的 handler 块（以 `path: '/api/messages'` 为锚取 ±12/+95 行，共 107 行）在新旧两版中**逐字相同**（`diff` 空）。
- 新 handler 内自行编排既有原语：`db.projectByChat` → `db.insertInput` → `publishMessage` → `publishState` → `tasks.set` → `sendTask` → `scheduleReconcile`，失败分支走 `db.insertOutput` + `publishMessage` + `publishState`（`web.js:1310-1336`）——与 `§11.4`「既有 19 条路由 handler 零改动」一致。

**C3（`oamp/web/debug.js` 不得改动） — pass**

- `git diff --name-status base...HEAD` 中无 `oamp/web/debug.js`；该文件内容在 worktree 中仍为 base 版本（其 `EVENT_TYPES` 白名单 `oamp/web/debug.js:8` 未含 `confirmation`，按裁决属本 PR 范围外）。

### D. 硬性红线

**D1. `§11.4` 零改动清单未被改动 — pass**

- `persist.js` / `router.js` / `transport.js` / `cluster-config.js`：**均不在** `git diff --name-status` 的 10 个条目中（E7）。
- 静态面既有条目：STATIC_FILES 区段 diff **只有新增 2 行**（注释 + `/notify.js`），无删除行。
- 既有 19 条路由 handler：web.js 的全部删除行仅 4 行（见 T2.6），且被改动的新行区间与旧行号带 `429-1240` 无交集；`http.createServer` / `listen` 行未动。
- `/api/stream` 4 类事件：路由 handler 与文件头注释行（`web.js:13`）逐字未动；API.md §4.1 未动；E5 探针 2 实测定向流仍只出 `notice`（`context_released`/`context_reset`）形态，全局增量 0。
- 既有 19 条 API.md 条目正文：全部删除行只有 3 行（§3 标题条数行、§4.2 标题行、§4.2 键隔离句），无任何条目正文删除。

**D2. 文件范围只含 PR 文件列出的 9 个路径 — pass**

- 三点 diff 共 10 个条目：PR 文件列出的 9 个路径（`oamp/src/inbox.js` A、`oamp/src/web.js` M、`oamp/API.md` M、`oamp/llms.txt` M、`oamp/test/confirmation-inbox.test.js` A、`oamp/test/api-routes.test.js` M、`oamp/test/project-workspace.test.js` M、`oamp/test/call-protocol.test.js` M、`oamp/test/web.test.js` M）+ `docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-003-tasks.md`（planner 产出，按委托不计入判定）。**无越界文件**。

**D3. 授权测试的断言同步未削弱 — pass**

- `web.test.js`：否定断言保留 `.some()` 全流扫描 + 显式三类型排除（非子集断言、未删除）。
- `api-routes.test.js`：`EXPECTED_SIGNATURES` 为**全量 `deepEqual`**（21 项，追加 2 项，其余 19 项未动）；`/api/docs` 写接口 `danger` 计数断言 7→8（等强度计数）；标题断言 19→21；**新增** 4 条正向断言（§3.20/§3.21 小节标题与清单表行）。
- `project-workspace.test.js`：`EXPECTED_ROUTE_SIGNATURES` 全量 `deepEqual`（追加 2 项）；`routes.length` 19→21；`slice(-6)` 仍为 6 元 `deepEqual`（因新面追加末位，窗口内容按预期平移，末位语义仍由全量 `deepEqual` 更强地保证）；`danger` 7→8；标题 19→21。
- `call-protocol.test.js`：`routes.length` 19→21；API.md 行号锚点 5 组同步平移。**逐组实测**：`[628,898]→[630,900]`（+2）、`[937,962]→[1025,1050]`、`[1279,1386]→[1367,1474]`、`[1387,1412]→[1475,1500]`、`[1415,1534]→[1503,1622]`（+88），每组在旧 / 新文件下**逐行内容完全相同**（`identical=true`）⇒ 扫描窗口的语义覆盖未变，无「锚点漂移到错误区间」的削弱。
- 无任何断言被删除或改弱为「包含」类子集判断。

**D4. `llms.txt` 由生成脚本重生成而非手改 — pass**

- E8：把 worktree 的 `oamp/` 复制到 `/tmp` 后运行 `node scripts/gen-llms-txt.mjs`，产物与仓库文件 `cmp` 完全一致（sha256 `8b970e46…bada`，2837 字节，21 条）；生成器输出行例：「`llms.txt 已生成：…（接口 21 条，2837 字节）`」。
 - 非手改旁证：E6 直调 `renderLlmsTxt(projectRoutes(createApiRoutes({})))` 的渲染结果含两条新清单行（`- GET /api/confirmations — …` / `- POST /api/confirmations/:confirmation_id/decision — …`）与 `## 接口（21 条）` 标题，与仓库文件逐行一致（A9 的 sha256 字节比对为等价强度的独立佐证）。
- 说明：验证在 `/tmp` 副本内进行，被验 worktree 未被写入。

---

## 汇总

| 判定 | 条目数 |
|---|---|
| pass | **46** |
| fail | 0 |
| partial | 0 |
| blocked | 0 |

（A 12 项 + B 27 项〔T1:5 / T2:6 / T3:5 / T4:4 / T5:4 / T6:3〕+ C 3 项 + D 4 项 = 46 项，无漏项。）

## 偏差记录

> 实现与现有规格 / 文档不一致之处。**不影响**本次验收判定（每条均已在下面给出可复查位置）。

| # | 规格 / 文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| 1 | `architecture.md` §5.2（`architecture.md:374`）、§4.2 M-14（`:288`）、§11.2 B-6（`:796`）：全局订阅事件集合「**2 类 → 3 类**（+`confirmation`）」 | 全局链路实际为 **4 类**：`agent_online` / `agent_offline` / `confirmation` / `chat_state`（`web.js:1474` 每次 `publishState` 追加一帧全局 `chat_state`）。API.md §4.2 与 `web.test.js` 已按 4 类口径同步 | 按实现（= 主 agent C1 裁决口径）更新架构 §5.2 / M-14 / B-6 的条数表述，并把「`chat_state` 两处都发」写成显式结论（否则架构里留着一个与世界不符的「3 类」） |
| 2 | `architecture.md` §2.1 组件图（`:116`）：`web.js 路由表 19 + ★3 条` | 落地为 **21 条** = 19 + **2**（与 §4.2 M-4 / §5.1「新增 2 条 HTTP 接口」一致） | 架构组件图数字同步为 +2（若第 3 条原指静态白名单 `/notify.js`，应标注它不是路由表项） |
| 3 | `oamp/README.md:167`：`GET /api/events` =「全局 SSE：agent 上线 / 下线事件（`agent_online` / `agent_offline`）；无参数、不依赖对话」；`:176` 仍写「SSE 事件（共六类）」 | 全局链路另承载 `confirmation` 与 `chat_state`；README 未更新（README 不在架构 §1.5 D1~D3 的派生面清单内，也不在 PR 文件范围） | 下一迭代（或 pr-004 收口时）同步 README 的全局事件面描述，避免文档面自相矛盾（API.md 与 README 现在口径不同） |
| 4 | `API.md §4.1`（`API.md:994`）`notice` 行把 `kind` 枚举写作 `context_released / context_reset`；全文仅 §3.21 的正文一句提到 `confirmation_decision`（`API.md:954`） | agent→web 信封面新增 3 个 `notice` kind（`confirmation_request` / `confirmation_decision` / `confirmation_cancelled`），其中**两个在 API.md 无任何记载**。§4.1 的那一行描述的是 SSE 层 `notice` 帧（新 kind 不会被转成 SSE `notice` 帧，实测如此），故语义上仍准确，但读者无法从 API.md 得知信封 kind 全集 | 下一迭代在 API.md 适当章节补 agent⇄web `notice` 信封的 kind 清单，消除「§4.1 的 kind 枚举是否覆盖信封」的歧义（架构 B-10 只核查了 §7，§7 确实未列举 ⇒ 该缺口不在原核查面内） |
| 5 | `API.md §3.21` 语义段：`text` 去空白后非空 ⇒ 「另向该 `chat_id` 追加一条输入并派发」 | 当 `chat_id` 在库中**无对应项目行**时（如 web 重启换了 DB / 上游给了陌生 chat_id），该段代码**静默跳过**文本落地，接口仍返回 `200 {accepted:true}`（`web.js:1310`；探针 3 实测 `in_delta=0`、无 `task.request`） | 下一迭代二选一：在 §3.21 明示该回落（与「回传 best-effort」并列写出），或把「文本无处可落」显式化。**不构成本次失败**：A#6 的样本场景（chat 行存在）实测通过，且裁决本身确实已受理 |

## 下一迭代候选

> 本轮发现的优化点 / 边界 / 遗留限制，不在本次验收标准范围内。

1. **既有测试 `oamp/test/status.test.js:157` 的计时脆弱性会随机把完整套件染红**：断言「两次 `status` CLI 之间 `last_heartbeat` 必须**严格**推进」在并行负载下会因两次调用落在同一心跳 tick 内而失败（失败原文：`'last_heartbeat 第二轮应严格推进（仅自然推进）'`，`AssertionError`，`status.test.js:157`）。实测采样：**base 完整树 6 轮 1 红**、PR head 完整套件 7 轮 4 红、PR head 去掉新测试文件 3 轮 1 红 ⇒ 与 pr-003 无关的既有脆弱性，但在 CI 并行环境下会表现为随机红。建议改为「轮询等待心跳推进」而非严格大于。
2. **`oamp/web/debug.js:8` 的 `EVENT_TYPES` 白名单未含 `confirmation`**：调试台的全局订阅不会显示新帧（C3 已明确该文件不在本 PR 范围；planner 任务图 §5 疑问 3 亦登记）。
3. **既有前端对全局新帧静默忽略，pr-004 接线前功能不可见**：`oamp/web/app.js:128-157` 的全局订阅只为 `agent_online` / `agent_offline` 注册监听； `transport` 每帧都带 `event: <type>` 名（`transport.js:88`），EventSource 对「有名无监听器」的帧不触发回调 ⇒ 现有页面行为不受影响（架构 B-12 的复核点已证伪其风险），但 `confirmation` / 全局 `chat_state` 要等 pr-004 才有消费方。
4. **多标签页陈旧条目**（架构 §5.2 已明示的代价）：裁决不发帧 ⇒ 未提交裁决的另一个标签页会保留陈旧条目，直到刷新（重建）才消失。若后续要「跨标签页同步」，需另行设计事件。
5. **`publishState` 的全局广播覆盖全部状态（含 `working`）**：全局链路上的帧量随派发数线性增长（每轮 2 帧）。当前订阅者仅控制台页面，量级可控；若未来通知服务改为订阅全局流，需重新评估是否需要按状态过滤（注意：过滤会与 C1 的「每次调用追加一次」口径冲突，属产品决定）。

## 结论

**PASS**

- 46 项判定全部 pass，0 fail / 0 partial / 0 blocked。
- 5 条偏差记录均已定位到可复查的文档位置与代码行；偏差不阻塞本迭代交付（按 verifier 契约，偏差被记录即满足交付条件）。
- 唯一需要主 agent 知情的是「下一迭代候选 1」：完整套件在 base 上也存在同一处偶发红，**不是**本 PR 引入的回归，但会污染后续阶段的「全绿」判据。
