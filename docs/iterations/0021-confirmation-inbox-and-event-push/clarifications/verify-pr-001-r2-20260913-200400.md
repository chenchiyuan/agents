# pr-001 独立验证报告 · 第 2 轮复验（agent 侧权限挂起链路 + ACP 答复链路修复）

验证者身份：**ACP/JSON-RPC 协议集成的同级代码审查者 + 真实运行时验证者**（具备 omp dist 反查与帧级协议复现能力）。判断依据仅来自产出物本身与主 agent 指定的验证标准；**未接收、未使用任何执行过程上下文**——commit message、PR/任务图的结论句、dev 自建证据文件的结论一律只作为「被核查对象」，不作为判定依据。
产出物：
1. worktree `.pb-agents/worktrees/0021-pr-001-permission-suspend-and-reply-fix`，分支 `feat/0021-pr-001-permission-suspend-and-reply-fix`，HEAD `1f11b595e652ce66a93ef85ea2133b51ef8495a9`（**本轮改动面** = `git diff dd59d31...HEAD`）
2. `docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-001-agent-permission-suspend-and-reply-fix.md`
3. `prs/pr-001-tasks.md`
4. `clarifications/pr001-round2-verdicts.md`（C1~C6 + 附加交付项，验证标准来源）
5. `clarifications/verify-pr-001-20260913-131042.md`（首轮报告，仅用于定位本轮待复核项）
6. `clarifications/pr-001-m4-evidence.md`（本轮新增证据文件，按文件实际内容判定）
验证标准来源：主 agent 委托的 A（PR 验收 6 条）+ B（T1~T6）+ C（C1~C6）+ D（D1~D3）+ E（3 组回归 + 4 项内务）。
验证日期：2026-09-13

**环境**：真实 `omp` 18.0.11（`/Users/chenchiyuan/.bun/bin/omp` → `@oh-my-pi/pi-coding-agent/dist/cli.js`，19,803,745 B）；node v22.15.0；模型 `deepseek/deepseek-v4-flash`。

**本次全部为自建工具，未复用 PR 自带用例、未复用 dev 的 `/tmp/r2` 探针**（均在 `/tmp/r2verify`，未提交任何仓库；被验 worktree 全程只读）：

| 自建件 | 用途 |
|---|---|
| `/tmp/r2verify/fake-acp-scripted.mjs` + `fake-bin.sh` | **脚本化** fake ACP 服务端：按 `R2_SCENARIO` 指定帧序（含 `after:<label>` 触发关系），逐帧落盘两侧帧 |
| `/tmp/r2verify/probe-frames.mjs` | 帧级探针：16 个场景（S1~S16）× 5 种钩子模式（escalate / nohook / allow-then-deny / option-echo / option-illegal / suspend-*） |
| `/tmp/r2verify/omp-mitm.mjs` + `omp-wrap.sh` | 真实 omp 中间人 wrapper：双向透传并逐帧落盘 ⇒ **原始帧**证据（含 argv 逐字） |
| `/tmp/r2verify/probe-real.mjs` | 真实 omp 场景探针（write-allow / write-deny / write-nohook / bash-allow / bash-deny / concurrent / concurrent-slow / `--strip` / `--pre`） |
| `/tmp/r2verify/probe-raw.mjs` | 裸 ACP 驱动（不经 `AcpClient`、**不发 `session/cancel`**）：观测「拒绝」的模型侧文字 |
| `/tmp/r2verify/acl-pre.js` / `acl-post.js` | 源对照：`git show dd59d31:oamp/src/acp-client.js` 与本轮 HEAD 源（`md5` 与 worktree 文件一致：`70fb06064b3f20265285eaca30e10a9f`） |

---

## 逐项判定

### A. PR 文件「验收标准」6 条

- **A1（`tool-permission.test.js` 全绿；同步 `'allow'`/`'deny'` 两条既有用例逐字保留；`deny` 档三步不变）：pass**
  证据①：`node --test oamp/test/tool-permission.test.js` → `1..22 / # tests 22 / # pass 22 / # fail 0`（`/tmp/r2verify/reg-toolperm.txt`）。
  证据②（字节级，非人工比对）：脚本按 `test('…')` 块配对花括号提取块体，`tp-base.js`（`dd59d31`）× `tp-head.js`（HEAD）比对：
  `F05-2/§11.2：允许档恒回 allow_once…` base≡head（1623 B / 1623 B，**IDENTICAL**）；`F05-3/F05-4：拒绝档回 reject_once + 立即 session/cancel + 轮次 permission_denied…` base≡head（1322 B / 1322 B，**IDENTICAL**）。同一比对对 `27eef14` 侧亦为 IDENTICAL（跨两轮零改动）。
  证据③（`deny` 三步代码未动）：`_handlePermissionRequest` 的 deny 分支（`acp-client.js:452-459`）与 `dd59d31` 同构——`_respond(reject_once)` → `_permissionDenied = true` → `cancel()` → 恰一行 `TOOL_DENIED`；唯一差异是删除 pre 的 `this._approvedToolCallId = null`（见偏差 #2）。

- **A2（未结算 Promise 期间不回包；结算后 `optionId` = 钩子选定值）：pass**
  证据（自建帧级探针，钩子模式 `suspend-permission`；`timeoutMs=300`）：`S12_suspend_permission` —— 服务端请求发出于 `+0ms`，钩子 Promise 在 `+601ms` 结算，客户端应答帧落在 **`+601ms`**（即结算之后），挂起窗口内**零应答帧**；回包 `{"outcome":{"outcome":"selected","optionId":"allow_once"}}`。
  证据（非默认值回显，钩子模式 `option-echo`，同样先挂起 600ms）：回包 `{"outcome":{"outcome":"selected","optionId":"allow_always"}}` —— **原样回显钩子选定值，非恒为 `allow_once`**。
  证据：`S13_suspend_gate`（钩子对 `tool_approval` 挂起）——应答 `+601ms`，回包 `{"action":"accept","content":{"value":"Deny"}}`，`cancelCount=0`、`deadAfterPrompts=false`。

- **A3（非法 `optionId` 回落并记审计；fake 侧不出现「未知 option ID」类错误）：pass**
  证据（自建探针，钩子模式 `option-illegal`，返回 `{optionId:'allow_everything_forever'}`）：回包 `{"outcome":{"outcome":"selected","optionId":"allow_once"}}` —— 回落默认项且 ∈ 请求方 `options`；该轮 `stop_reason=end_turn` 正常结算；服务端**未记录任何 error 帧**（帧日志 `error` 全为 null）。
  旁证（omp 侧硬约束真实存在，本次直读 dist）：偏移 `15551720` 逐字 `let R=SEs.get(T.optionId);if(!R)throw new X(\`Tool permission response used unknown option ID: ${T.optionId}\`)`。
  证据（语义未回归）：`_permissionDecision` 的回退表达式在 `dd59d31` 与 HEAD **逐字相同**（函数体提取比对：`const optionId = known ? verdict.optionId : verdict.optionId.startsWith('reject') ? DEFAULT_DENY_OPTION : DEFAULT_ALLOW_OPTION;`）。

- **A4（挂起期间冻结轮次计时；裁决后按剩余时间恢复并正常结算）：pass**
  证据（`S12_suspend_permission`，`timeoutMs=300`，挂起 601ms）：`prompt()` 以 `stop_reason=end_turn` 正常结算（`elapsedMs=602`）、`cancelCount=0`（无 `session/cancel`）、`deadAfterPrompts=false`。若冻结失效，计时器应在 ~300ms 处 `cancel → 宽限 → kill`。
  证据（超时语义未被改写）：`§4.4/pr-001` 的对照用例 `L1-1/pr-001④对照` 在 HEAD 中存在且为套件成员（22/22 全绿），断言「无挂起仍 cancel → kill」。
  注：**重叠挂起**下冻结失效（深度计数缺陷），见偏差 #3——该帧序超出 A4/T4 的判据范围，且真实 omp 不可达。

- **A5（`tools=true` 且 `allow` ⇒ argv `always-ask`；`deny`/`tools=off` 形状不变；一次性路径仍 `yolo`）：pass**
  证据①（**真实 argv 逐字**，由自建 MITM wrapper 在 `exec` 前记录，各场景 `mitm-*.jsonl` 的 `meta` 行一致）：
  `["acp","--no-skills","--no-rules","--no-session","--approval-mode","always-ask"]`。
  证据②（HEAD 源码 `acp-client.js:136-138`）：`if (this.tools) args.push('--approval-mode', 'always-ask');` —— 两档（`allow`/`deny`）同值，`tools=false` / 匿名不追加。
  证据③（断言现状，`tp-head.js:473-500`）：allow 档 `assert.equal(allowArgv[allowIdx+1],'always-ask',…)`；deny 档 `assert.equal(denyArgv[…+1],'always-ask')`；`tools=false` 与匿名 `assert.ok(!…includes('--approval-mode'))` + `assert.ok(…includes('--no-tools'))`；该块在 `dd59d31`→HEAD **零改动**（唯一改动块见 E4）。
  证据④（**一次性路径零改动**）：`acp-daemon.test.js` 本轮**整文件未改**（`git diff dd59d31...HEAD -- oamp/test/acp-daemon.test.js` 输出为空）；`:554` 的 `yolo` 断言与 `27eef14` 侧**同一行 md5 相同**（`5ab73950571723b5b0dbdb43fed6f7e3`）。

- **A6（M4 硬义务：真实 omp 双向；审计行不作判据）：pass** —— 判据全部来自自建真实 omp 复跑（详见 D1/C6），未引用 PR 内任何叙述；全程未使用 `TOOL_APPROVED`/`TOOL_DENIED` 作证据（本轮全部 11 次真实 omp 运行均未读取审计日志）。

### B. `prs/pr-001-tasks.md` 的 T1~T6

**T1（argv 改档 + 两处断言同步）**
- T1.1（两档 `always-ask`；`tools=false`/匿名不含）：**pass** — 见 A5①②③。
- T1.2（常驻路径断言同步）：**pass** — `acp-daemon.test.js` 本轮整文件零改动，`:532` 已是 `always-ask`（`§4.4/L1-2②（pr-001）` 文案）；`node --test oamp/test/acp-daemon.test.js` → `# tests 7 / # pass 7 / # fail 0`。
- T1.3（一次性路径零改动；`agent.js` 不在 diff）：**pass** — `git diff --name-only dd59d31...HEAD` 仅 2 个文件（不含 `oamp/src/agent.js`）；`:554` 与 `27eef14` 逐字相同（md5 同）。

**T2（钩子返回域扩展）**
- T2.1（挂起期不回包，帧级）：**pass** — 见 A2（`+0ms` 请求 → `+601ms` 应答，窗口内零应答帧）。
- T2.2（结算后回显钩子 `optionId`）：**pass** — `allow_always` 原样回显（A2 证据②）。
- T2.3（同步 `'allow'`/`'deny'` 两条既有路径逐字不变）：**pass** — 见 A1 证据②③。
- T2.4（审计在裁决后写恰一行、`option` = 实际回显值）：**pass** — 代码序：`acp-client.js:468` `_respond(...)` 先执行，随后 `:476` `_audit('TOOL_DENIED'…)` / `:487` `_audit('TOOL_APPROVED'…)`；该段与 `dd59d31` 的 deny 侧、allow 侧审计顺序一致（函数体提取比对）；对应用例 `L1-2/pr-001②` 为套件成员且 22/22 绿（其块体跨两轮零改动）。
- T2.5（JSDoc 返回域改写，优先级语义保留）：**pass** — `acp-client.js:88`：`… (info) ⇒ 'allow'|'deny'|{optionId}|Promise<…>；给了则优先于 permission`（此前缀语义逐字保留），并新增第二种入参形态描述。

**T3（`optionId` 校验与回落）**
- T3.1（非法 → 回落 + 审计 = 回落值）：**pass** — 见 A3（回包 `allow_once`）；审计字段 `option` = 回显值由 `_audit(eventName, toolCall, option)` 签名保证（`optionId` 为已回落值）。
- T3.2（应答值恒合法，轮次不崩）：**pass** — 见 A3（无 error 帧、`end_turn`）。
- T3.3（合法 `{optionId}` 逐字回显）：**pass** — 见 A2 证据②（`allow_always`）。

**T4（冻结轮次计时）**
- T4.1（挂起 > `timeoutMs` 未被 cancel/kill）：**pass** — 见 A4（`cancelCount=0`、`dead=false`、未以 `timeout` 结算）。
- T4.2（裁决后按剩余时间恢复并正常结算；超时路径仍能 cancel→kill）：**pass** — `S12` 结算 `end_turn`（`elapsedMs=602`）；超时对照用例存在且绿。
- T4.3（冻结只作用于挂起路径）：**pass** — 同步 `'allow'`/`'deny'`（A1）与回落路径（A3）计时行为不变；`_pauseTurnTimer` 仅由 `_askHook` 在钩子返回 thenable 时调用（`acp-client.js:519-529`），非挂起路径零触碰。

**T5（M4 根因定位，证据落盘）**
- T5.1（按 D3→D1→D2 给单一结论，每条含成立/不成立的观测证据）：**pass** — `pr-001-m4-evidence.md` §1（argv 两侧）/§2（原始帧原文）/§3（四档表）/§4（dist 定位）/§5（修复前/后对照）齐备，且 §1 结论表明确写出 D3「两侧结果一致 ⇒ 不成立」、§3 明确写出 D2「四档逐档相同 ⇒ 不成立」。
- T5.2（D3 步以 daemon 真实 argv 复跑并记录 argv 逐字，判据「其中无 `--no-session`」）：**pass** — 证据 §1 给出**两份 argv 逐字列表**：含 `--no-session`（daemon 真实形态）与**不含** `--no-session`（wrapper strip 后 omp 实收），后者即判据要求的列表；本次由我**独立复跑同一侧**（`probe-real.mjs write-allow --strip`）→ wrapper 记录 omp 实收 argv `["acp","--no-skills","--no-rules","--approval-mode","always-ask"]`（确无 `--no-session`），且模型侧与副作用与该侧一致（见 D1）。
  说明：该判据的**前提**（daemon 路径不带 `--no-session`）与代码事实相反（`acp-client.js:126` 无条件 `args.push('--no-session')`，本次独立复核 argv 逐字含之），证据文件已如实并列两侧并显式指出该前提错误 ⇒ 判据的可达成部分已达成（属偏差 #4，主 agent 已登记）。
- T5.3（D1 证据 = 帧逐字比对 + omp 读取路径结论）：**pass** — §2 给出修复前/后的应答帧原文 `{"outcome":{"outcome":"selected","optionId":"allow_once"}}`；§4 给出 omp 取值路径逐字（偏移 `15551720` 的 `SEs.get(T.optionId)` 校验链）。**本次对 §4 的 6 处 dist 偏移逐字复核，全部命中**（见 T5.5 证据）。
- T5.4（D2 证据 = 0/1ms/100ms/1s 四档模型侧结果对照表）：**pass** — §3 给出完整四档表（修复前四档皆 `Tool call denied by user: write` + 文件未创建；修复后四档皆真实写入 + 文件存在）。
  独立佐证范围（如实声明）：我**未**重跑这四档延迟，但以帧级证据佐证其「与应答时延无关」的机理——结果只取决于审批门是否进入可裁决面（`_handleElicitationRequest` 的形状判定 + 钩子上浮），与应答时刻无关；本次自建探针在 `+0ms`/`+601ms` 两种应答时刻下对同一帧序得到同一应答值。
- T5.5（结论不以审计行为证据）：**pass** — §1~§5 的判据均为模型侧工具结果 / 副作用文件 / 原始帧；§6 把审计行单列且明示「不作判据」。本报告亦全程未使用审计行。dist 复核（本次直读 `/Users/chenchiyuan/.bun/install/global/node_modules/@oh-my-pi/pi-coding-agent/dist/cli.js`）：`14046764` `P=await A.select(I,["Approve","Deny"])…throw Error(\`Tool call denied by user: …\`)`；`13316196` `function aZn(e,t,n){let s=[\`Allow tool: ${e.name}\`]…`；`18049212` `let s=n?.elicitation?.form!=null;return{select:async(o,r,i)=>{if(!s)return;…`；`15541352` `bEs={bash:!0,edit:!0,delete:!0,move:!0}`；`18034948` `async function mba(e,t,n,s,o){let r={toolCallId:n.toolCallId,title:n.title,…}`（**无 `toolName`**）；`15551720` 未知 optionId 抛错。

**T6（M4 修复落地 + 双向真实 omp 验证）**
- T6.1（放行路径模型侧 = 工具真实输出）：**pass** — 自建真实 omp（argv 见 A5①）：模型侧原文 `工具已真正执行。原始输出逐字如下：\n\n```\nSuccessfully wrote 15 bytes to /tmp/r2verify/se-write-allow.txt\n````（不含 `denied by user`）；副作用文件 `/tmp/r2verify/se-write-allow.txt` **存在**、内容 `R2V-WRITE-ALLOW`。
- T6.2（拒绝路径模型侧确实 denied）：**pass** — `bash`：裸 ACP 驱动（不发 cancel）模型侧原文 `…工具返回逐字如下：\n\n```\nTool call rejected by user (bash)\n````，副作用文件**不存在**（见 C6）。
- T6.3（1+2 同时成立）：**pass** — 两侧均在本次独立复跑成立。
- T6.4（不回归 fake 层）：**pass** — 22/22、7/7（A1、T1.2）。
- T6.5（oamp 全量全绿）：**pass** — `node --test oamp/test/*.test.js` → `1..294 / # pass 294 / # fail 0`（**首轮即全绿**，无 flake，故未触发隔离复跑与对照树需求）。

### C. `clarifications/pr001-round2-verdicts.md` C1~C6

- **C1（写类工具经上浮可裁决）：pass**
  帧形状判据（自建探针，`S8`/`S13`）：审批门帧 `requestedSchema.properties.value.enum=["Approve","Deny"]` 且 `message` 首行 `Allow tool: <toolName>` ⇒ 走钩子。钩子入参实测（PR 用例与自建探针一致）：`{kind:'tool_approval', sessionId, toolCall:{toolName,title}, options:[{optionId:'Approve'},{optionId:'Deny'}]}` ⇒ pr-002 的 pending 表可获得工具名与选项集合。
  真实 omp（自建探针）：钩子调用逐字 `[{kind:'tool_approval', toolName:'write', toolCallId:null, verdict:'allow'}]`；帧原文 `{"method":"elicitation/create",…,"message":"Allow tool: write\nPath: /tmp/r2verify/se-write-allow.txt\nContent:\nR2V-WRITE-ALLOW",…}`（由 MITM 逐帧落盘）。
  放行 ⇒ 模型侧 `Successfully wrote 15 bytes…` + 副作用文件存在（T6.1）；拒绝 ⇒ 模型侧 `Tool call denied by user: write` + 副作用文件 `/tmp/r2verify/se-write-deny.txt` **不存在**（钩子调用 `verdict:'deny'`，轮次 `end_turn`）。**回退已闭合。**
- **C2（同一 toolCall 不重复提问）：pass**
  帧级（自建探针 `S1`/`S7`，钩子对审批门恒答 `deny`）：`gate_1_own` 回 `Approve` 且钩子**零调用**（凭据抵扣，`hookCalls` 中该门无对应项）；`gate_3_after_terminal` 在 call_A 终态后回 `Deny`（凭据已随终态失效）。
  真实 omp（`concurrent` 场景，同轮 bash + write）：帧序 `request_permission(bash)@2586 → REP allow_once@2586 → tool_call pending@2587 → elicitation 'Allow tool: bash'@2588 → REP Approve@2588 → bash in_progress@2620 → bash completed@2620 → elicitation 'Allow tool: write'@2621 → REP Deny@2621`；钩子调用恰 2 次（1 次 `permission` + 1 次 `tool_approval:write`）⇒ bash 自己的门被抵扣、**未重复上浮**；write 的门独立上浮。**与 C2 一致。**
- **C3（非交互安全默认）：pass**
  真实 omp（`write-nohook`，`onPermissionRequest=null`）：钩子调用 `[]`，帧应答 `{"action":"accept","content":{"value":"Deny"}}`，模型侧 `Tool call denied by user: write`，副作用文件未创建，`stop_reason=end_turn`（未崩在协议校验里）。
  帧级（自建探针 `S8`）：无钩子 ⇒ `Deny`；`S13`（钩子挂起后返回非显式放行值）⇒ `Deny`。
- **C4（消除误放行窗口；首轮偏差 #4 的确定性复现已消失）：pass**
  自建 fake 服务端 + 同型帧序（`S1`：`permission(call_A)→allow` → `gate_1_own` → `gate_2_unrelated` → `call_A` 终态 → `gate_3`），**修复前/后对照**（钩子对审批门恒 `deny`）：

  | 帧 | `dd59d31`（pre） | 本 PR HEAD（post） |
  |---|---|---|
  | `gate_1_own` | `Approve`（凭据） | `Approve`（凭据，钩子零调用） |
  | `gate_2_unrelated` | **`Approve` ← 无人工裁决（偏差 #4）** | **`Deny` ← 上浮后由钩子裁决** |
  | `gate_3_after_terminal` | `Deny` | `Deny` |

  并发变体 `S7`（两道门不等待彼此应答）同结论：pre `gate_2`=`Approve` / post `gate_2`=`Deny`。
  fake 层固定：HEAD 套件含 `M4/C2+C4` 用例（`tp-head.js:846+`）断言 `gate_2_unrelated` 必须为 `Deny`、`gateCalls=['write','bash']` ⇒ 若回退，套件即红。
- **C5（非审批 elicitation 仍 decline）：pass**
  帧级（自建探针 `S11`：`properties.q0`、`properties.value.type='boolean'` 两种形状，钩子恒 `allow`）：两类均回 `{"action":"decline"}`，**钩子零调用**（证明由形状决定而非钩子取值）。
  放行窗口内亦成立，且 **decline 不消费凭据**（`S16`：`permission_A→allow` → `other`(decline) → `gate_after_other` 回 `Approve` 且钩子零调用 ⇒ 凭据留给下一道审批门）。代码级一致：`_consumeApprovalGrant()` 仅在 `enum` 同时含 `Approve`/`Deny` 之后调用（`acp-client.js:552-558`）。
- **C6（`bash` 双向不回归）：pass**
  放行侧（真实 omp，argv 见 A5①）：模型侧原文 `执行了，只调用一次。工具返回的原始输出逐字如下：\n\n```\nR2V-BASH-ALLOW\n\n\nWall time: 0.03 seconds\n````；副作用文件 `/tmp/r2verify/se-bash-allow.txt` **存在**、内容 `R2V-BASH-ALLOW\n`；帧序含两道门（`request_permission` → `allow_once`；`elicitation 'Allow tool: bash'` → `Approve`）。
  拒绝侧：产品路径（`onPermissionRequest` 对权限门答 `deny`）→ `prompt()` 抛 `code='permission_denied'`，副作用文件 `/tmp/r2verify/se-bash-deny.txt` **不存在**，服务端未收到第二道门（帧序止于 `request_permission` → `reject_once` → `tool_call pending`）。产品路径的模型侧文字不可观测（拒绝语义 = 回包后立即 `cancel`），故以**裸 ACP 驱动**（不发 `cancel`，应答帧逐字相同 `reject_once`）补测：模型侧 `Tool call rejected by user (bash)`，副作用文件未创建。**双向均成立。**

### D. 首轮偏差闭合复核（独立重做）

- **D1（`write` 在 `allow` 档下的真实 omp 三支）：pass**
  自建探针 + 自建 MITM wrapper，`AcpClient` = 本 PR HEAD 源，argv = daemon 真实 argv（A5①）：

  | 分支 | 钩子调用（逐字） | 模型侧工具结果（原文节选） | 副作用文件 |
  |---|---|---|---|
  | 钩子放行 | `[{kind:'tool_approval',toolName:'write',toolCallId:null,verdict:'allow'}]` | `Successfully wrote 15 bytes to /tmp/r2verify/se-write-allow.txt` | `/tmp/r2verify/se-write-allow.txt` **存在**，内容 `R2V-WRITE-ALLOW` |
  | 钩子拒绝 | `[{kind:'tool_approval',toolName:'write',…,verdict:'deny'}]` | `Tool call denied by user: write` | `/tmp/r2verify/se-write-deny.txt` **不存在** |
  | **无钩子** | `[]` | `Tool call denied by user: write` | `/tmp/r2verify/se-write-nohook.txt` **不存在**；`stop_reason=end_turn`（轮次正常结算，未崩） |

  对照（`--pre`，即 `dd59d31` 源 + 同 argv + 同提示词）：钩子调用 `[]`、模型侧 `Tool call denied by user: write`、副作用文件 `/tmp/r2verify/se-write-allow-pre.txt` **不存在** ⇒ **首轮偏差 #1 的现象在 pre 源上确定性复现、在 HEAD 上消失**。
  argv 无关性（排除「探针 argv 产物」）：`write-allow --strip`（wrapper 丢弃 `--no-session`，omp 实收 argv 无 `--no-session`）→ 模型侧 `Successfully wrote 15 bytes…`、副作用文件存在、钩子调用 1 次 ⇒ 两侧一致。
  帧原文（MITM 落盘，`write` 场景**只有一道门**）：`{"method":"elicitation/create","params":{"mode":"form","sessionId":"…","message":"Allow tool: write\nPath: /tmp/r2verify/se-write-allow.txt\nContent:\nR2V-WRITE-ALLOW","requestedSchema":{"type":"object","properties":{"value":{"type":"string","enum":["Approve","Deny"]}},"required":["value"]}}}`。

- **D2（C4 的 fake 层复现，自建 fake 服务端，修复前/后对照）：pass**
  见 C4 表：同型序列（与首轮报告偏差 #4 同构）下，`gate_2_unrelated` pre=`Approve`（无人工裁决）→ post=`Deny`（上浮）；`hookCalls` pre=`[permission]`（审批门零调用）→ post=`[permission, tool_approval:write, tool_approval:bash]`。轮次两侧均 `end_turn`。
  源对照可信性：`acl-post.js` 的 md5 与被验 worktree 的 `oamp/src/acp-client.js` 完全一致；`acl-pre.js` 由 `git show dd59d31:oamp/src/acp-client.js` 直接导出。

- **D3（残余暴露面：凭据模型在「一次放行 → 该 toolCall 终态」窗口内的行为边界）：pass（分析穷举完成，结论明确；残余暴露见偏差 #1/#2）**
  帧级穷举（自建 fake，16 场景；钩子对审批门恒 `deny` ⇒ **任何 `Approve` 应答都必然未经人工裁决**）：

  | 帧序 | 结果（post） | 是否有无人工裁决的 `Approve` |
  |---|---|---|
  | S1 `permission_A→allow` → `gate_1_own` → `gate_2_unrelated` → `terminal(call_A)` → `gate_3` | `Approve`,`Deny`,`Deny` | 有 1 处（`gate_1_own`，属 C2 预期） |
  | S2 放行后**先**来无关门：`permission_A→allow` → `gate_B_unrelated` → `gate_A_own` → `terminal` | `Approve`,`Deny` | **有（`gate_B_unrelated` 抵扣了 call_A 的凭据）← 残余** |
  | S3 门早于权限门：`gate_early` → `permission_A` → `gate_late` | `Deny`,`Approve` | 有 1 处（`gate_late`，凭据正常） |
  | S4 多次放行 + 逆序门：`perm_A,perm_B` → `gate_B_own` → `gate_A_own` | `Approve`,`Approve` | **有 2 处（身份错配：B 的门吃掉 A 的凭据）← 残余** |
  | S5 终态先行：`perm_A` → `terminal(call_A)` → `gate_after_terminal` | `Deny` | 无 |
  | S6 轮末清零：轮 1 放行无终态 → 轮 2 来门 | `Deny` | 无（跨轮不残留） |
  | S7 并发双门（不等待彼此应答） | `Approve`,`Deny` | 有 1 处（自己的门；**同步抵扣保证恰一门**） |
  | S8 无权限门仅一道门（有/无钩子） | `Deny` | 无 |
  | S9 门先于权限门、且其后权限门放行 | `Deny`,`Approve` | 有 1 处（属 C2 预期） |
  | S10 多 toolCall 交错 + 残余凭据 | `Approve`,`Deny` | 有 1 处（自己的门）；无关门 `gate_C` 在上一个终态后仍为 `Deny` |
  | S11/S16 非审批 elicitation（窗口内） | `decline`（不消费凭据） | 无 |
  | S12/S13/S14 挂起（单门/挂起乱序） | 见 A2/A4 | 无 |
  | S15 `perm_A→allow` → `perm_B→deny` → `gate_C` | `Approve` ← **残余**（pre 为 `Deny`，见偏差 #1b） | **有** |

  **明确结论**：**帧级存在「无人工裁决的 `Approve`」**——只要审批门在与某次放行同一轮内、且该次放行所属 toolCall 尚未观测到终态之前到达，客户端就会以凭据抵扣它，**不看该门属于哪个工具**（wire 上无身份关联：`session/request_permission.params.toolCall` 不含 `toolName`——dist `mba()` 偏移 `18034948` 逐字；`elicitation/create.params` 不含 `toolCallId`——本次真实帧原文确认）。
  **真实 omp 路径下的可达性：未观测到，且受「轮内工具串行执行」约束**——实测证据：`concurrent-slow` 场景（同轮 `bash sleep 4` + `write`）帧序为 `…bash completed@6819 → elicitation 'Allow tool: write'@6819`，即第二道门在第一个工具**终态之后**才发出（`bash` 499ms/4s 运行期间无任何门）；`bash-allow` 场景 permission 应答与自身审批门之间亦严格串行（`request_permission@2891 → REP@2891 → elicitation@2893`）。故「放行后、终态前的窗口」在实测路径内**只包含该工具自身的审批门**，凭据必被正确的门抵扣。

### E. 回归与内务

- **E1 `node --test oamp/test/tool-permission.test.js`：pass** — `# tests 22 / # pass 22 / # fail 0`。
- **E2 `node --test oamp/test/acp-daemon.test.js`：pass** — `# tests 7 / # pass 7 / # fail 0`。
- **E3 `node --test oamp/test/*.test.js`（oamp 全量）：pass** — `1..294 / # pass 294 / # fail 0`，**首轮即全绿**（无 `EADDRINUSE` 类 flake）⇒ 未触发「隔离复跑 + 未改动对照树」流程（该流程仅在出现时序类 flake 时必需）。
- **E4 既有断言逐字未改：pass** — 块级字节比对（`dd59d31`→HEAD）：唯一变化的既有块是 `M4：第一道门放行 ⇒ …`，且改动为**纯增补**（`await c3.prompt(…)` → `const orphanResult = …; assert.equal(orphanResult.stop_reason,'end_turn',…)`），**未删除/未削弱**任何断言；其余 17 个既有块 IDENTICAL。同步 `'allow'`/`'deny'` 两条用例 IDENTICAL（A1）。一次性 `yolo` 断言（`acp-daemon.test.js:554`）与 `27eef14` 同行 md5 相同（A5④）。
- **E5 本轮改动文件范围：pass** — `git diff --stat dd59d31...HEAD` = `oamp/src/acp-client.js | 87 +, 22 -`、`oamp/test/tool-permission.test.js`，共 2 文件（`oamp/test/acp-daemon.test.js` 本轮零改动）；不含 `oamp/src/agent.js`、`oamp/web/**`、`src/web.js`。
- **E6 仓库根内务：pass** — `/Users/chenchiyuan/projects/agents`：`git status --porcelain -uall` 输出为空（`.pb-agents/` 已在 `.gitignore:2,4`，本报告写入不污染仓库）。
- **E7 被验 worktree 只读纪律：pass** — 全程未 `commit`/`checkout`/`stash`；worktree `git status --porcelain -uall` 输出为空；所有探针/对照树/产出均在 `/tmp/r2verify`，未向任何仓库写入（本报告为唯一写入，且落在迭代工作区 `clarifications/`）。

---

## 汇总

- pass: **46 项**
- fail: **0 项**（无返工项）
- partial: **0 项**
- blocked: **0 项**

分组：A 6/6；B 24/24（T1×3、T2×5、T3×3、T4×3、T5×5、T6×5）；C 6/6；D 3/3；E 7/7（3 组回归 + 4 项内务）。

**「是否仍有『无人工裁决的 `Approve』」的明确回答**：**帧级——有**（帧序 S2 / S4 / S15 可构造：任一道审批门只要在「某次放行 → 该次放行所属 toolCall 终态」窗口内到达，就会被凭据抵扣，**不校验该门属于哪个工具**）；**真实 omp 路径——未观测到**（实测轮内工具串行执行，窗口内只出现该工具自身的门；见 D3）。该残余属**同一批改动内新引入的凭据模型固有限制**，已登记为偏差 #1，不阻塞本轮验收（C2/C4 的字面契约均已满足）。

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| **1（低-中）凭据与工具身份无绑定** | C2 要求「判定必须基于**该次放行的显式关联**」（`pr001-round2-verdicts.md` C2） | 实现以 `_approvalGrants`（toolCallId 的 FIFO）+「先到先抵扣 + 终态/轮末失效」为模型；**抵扣时不校验门属于哪个工具**。帧序 S2（放行后先来无关门）⇒ 无关门 `Approve`；S4（两次放行 + 逆序门）⇒ 身份错配；即「A 的放行被 B 的门吃掉」。真实 omp 未观测到（轮内工具串行）。 | 若需身份级保证：① 推动上游让 `elicitation/create` 携带 `toolCallId`（当前 wire 无此字段，客户端不可达），或 ② 改为「每个审批门都必须有人工裁决」（放弃 C2 的免重复提问），或 ③ 在 pr-002 的 pending 表侧记录「哪次放行对应哪道门」并在协议层加固。三者均在 pr-001 文件范围外，建议开下一迭代处理 |
| **2（低）拒绝分支不再清空已有凭据** | `dd59d31` 的 `_handlePermissionRequest` 拒绝分支含 `this._approvedToolCallId = null`（清空凭据）；本轮 PR 语义未声明该行为变化 | 本轮删去该行（只保留 deny 三步），先前放行的凭据在拒绝后仍留在队列：帧序 S15（`perm_A→allow` → `perm_B→deny` → `gate_C`）post=`Approve` / **pre=`Deny`**（我已实测两侧）。真实 omp 下 `deny` 立即 `cancel()` ⇒ 轮次随后结算并在 `finally` 清空凭据，故不可达 | 若认同「B 的拒绝不应牵连 A 的放行」，需在 PR/架构中显式登记该语义变化；否则恢复「拒绝即清空」。建议随偏差 #1 一并在下一迭代统一凭据模型 |
| **3（低）重叠挂起时冻结失效（深度计数缺陷）** | architecture §3.1 L1-1 / T4：「挂起期间轮次计时冻结」 | `_pauseTurnTimer` 在 `entry.timer == null`（已暂停）时**提前返回**，第二次重叠挂起不递增 `_pausedTurns` ⇒ 先结算的一方 `_resumeTurnTimer` 把深度降到 0 并重启计时器 ⇒ 轮次在另一挂起仍未裁决时超时。实测（自建 fake，两个服务端请求同时挂起，`timeoutMs=300`，权限门 900ms 结算 / 审批门 400ms 结算）：`prompt()` **`code='timeout'`，`elapsedMs=704`**（≈400+300），而另一挂起 901ms 才结算；对照组（单挂起）无超时（`elapsedMs=602`）。代码与 `dd59d31` 相同（r2 仅把调用点抽为 `_askHook`），但 r2 把挂起面扩到工具审批门 ⇒ 暴露面变大。真实 omp 下两个服务端请求严格串行 ⇒ 未观测到 | 修正深度计数（区分「计时器未建立」与「已暂停」两态：已暂停时仅递增深度），或改为引用计数 + 显式 `paused` 标志。属 acp-client.js 文件面，可由下一迭代承接 |
| **4（中，主 agent 已登记为偏差 #2）架构 §9.4.3 D3 行前提误述** | architecture §9.4.3：「受控实测用的 argv 含 `--no-session`…而 **daemon 常驻路径不带** `--no-session`」 | `acp-client.js:126` **无条件** `args.push('--no-session')`，构造入参无 argv 覆盖位；本次自建 MITM wrapper 记录 daemon 路径 argv 逐字 **含** `--no-session`。本轮 `pr-001-m4-evidence.md` §1 已如实并列两侧并指出该前提错误（该文件同时满足 T5.2 的可达成判据） | 阶段 6 汇总时回填 `architecture.md` §9.4.3 的 D3 行前提（「D3 不成立」的结论本身正确）。**不阻塞** pr-001 |

---

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制。不在当前验收标准范围内，供主 agent 与 planner 决策。

- **审批门 ↔ toolCall 的身份绑定**：与偏差 #1/#2 同源。当前 wire（dist `mba()` 丢弃 `toolName`；`elicitation/create` 无 `toolCallId`）使身份级关联在客户端不可达；需要上游协议改动或产品侧取舍（每个门都上浮 vs 免重复提问）。
- **重叠挂起下的冻结深度计数**：与偏差 #3 同源，修复点在 `acp-client.js` 的 `_pauseTurnTimer`/`_resumeTurnTimer`（建议补一条「两个服务端请求同时挂起」的 fake 层用例；若判定该帧序在真实 omp 不可达，也可只补注释说明）。
- **`permission='deny'` 档的模型侧可见性**：拒绝语义「回包后立即 `session/cancel`」使模型永远看不到拒绝原因（只能借裸 ACP 驱动观测到 `Tool call rejected by user (bash)`）；若 F04 需要「模型知道为什么被拒」，需新的裁决—回传时序设计。（首轮候选，未变化。）
- **`ask` 等非审批 elicitation 的处置面**：当前恒 `decline`，且模型一调用即以 `stopReason='cancelled'` 结束轮次（首轮实测）。本轮 `_handleElicitationRequest` 已引入上浮分支，但只覆盖 `Approve|Deny` 形状；是否把 `ask` 形状纳入上浮面仍是未决产品问题。（首轮候选，未变化。）
- **轮内工具串行执行的显式结论入库**：本次以 `concurrent-slow`（`sleep 4`）取得「第二道门在第一个工具终态之后才发出」的直接帧证据；建议将该事实（及其对凭据窗口可达性的约束）写入 architecture 的 M4/时序小节，避免后续迭代重复论证并发场景。

---

## 结论

**PASS**（46 项全 pass，无 fail / partial / blocked）

支撑要点：首轮判为「高」的行为回退（非 `bEs` 写类工具在 `allow` 档被无人工介入地静默拒绝）**已闭合**——`write` 经审批门上浮（钩子入参含工具名与选项集合），钩子放行 ⇒ 模型侧 `Successfully wrote …` + 副作用文件落盘，钩子拒绝 ⇒ 模型侧 `Tool call denied by user: write` + 文件未创建，无钩子 ⇒ 保守拒绝且轮次正常结算；pre 源（`dd59d31`）在同一探针下确定性复现旧行为（钩子零调用 + 恒 denied），构成因果对照。首轮已通过的判据未回归：挂起不回包 / `optionId` 回显 / 非法 `optionId` 回落 / 冻结计时 / `bash` 双向真实 omp，均由**本次自建的帧级探针与自行搭建的真实 omp MITM 探针**独立复现（未复用 PR 用例与 dev 探针）；`M4/C2+C4` 的确定性复现已消失（fake 层 pre/post 对照 + 套件固定）；既有两条同步用例与一次性 `yolo` 断言字节级未改；三组回归首轮全绿（22/22、7/7、294/294）；改动面恰 2 文件，仓库根与被验 worktree 均干净。

需主 agent 决策（**本报告不构成合并许可**）：偏差 #1（凭据与工具身份无绑定 ⇒ 帧级仍存在无人工裁决的 `Approve`）与偏差 #3（重叠挂起冻结失效）均为**不在本轮验收标准字面范围内**的残余项，按 verifier 契约「偏差被显式记录即满足交付条件」处理；若合并门槛含「不得残留任何无人工裁决的放行路径」，则该项判定权在主 agent，不在本报告。
