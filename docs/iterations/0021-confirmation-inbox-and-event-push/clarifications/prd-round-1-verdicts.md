# 阶段 2 · MI 裁决记录（MI-01~MI-05）

**方式**: 主 agent 真实阻塞式转呈（`ask`）。prd 首轮列出 5 项 `[model_inferred MI-xx]`（未确认前不得作为已生效验收口径），用户逐项裁决如下（全部采纳 prd 推荐）：

| 编号 | 待确认 | 用户裁决 |
|---|---|---|
| MI-01 | 刷新 / 断线重连重建在途列表时，`confirmation_required` 是否重复触发通知 | **① 重建不重复通知** |
| MI-02 | 栏内条目是否必须可辨识来源对话 | **① 必须可辨识所属对话** |
| MI-03 | 「不做历史台账」的判定面 | **① 只判用户可见面**（不判数据层） |
| MI-04 | E5「内容一致」是否含栏内相对顺序 | **① 不判顺序**（只判条目存在 + 内容一致） |
| MI-05 | E1/E2/E5 的判定样本来源 | **① 构造 + 真实各半** |

## 同时处置的 prd 报告级事项（主 agent 裁定，非用户决策点）

| # | prd 提出 | 主 agent 裁定 |
|---|---|---|
| 1 | **需求空缺**：`demand.md` 未规定栏内**排序 / 分组 / 条数上限**；prd 未新增（既不写验收也不塞 `[架构待填]`），仅在 F01 边界显式出界 | **维持出界**（最小边界原则：未提出的不新增）⇒ 登记为**下一迭代候选**，不回退阶段 1 |
| 2 | 本迭代自身执行方式（hub 后台派发 + `allow` 档）在无人值守时会产生挂起（R1，用户已接受），是否可作 E2 的「真实半句」样本 | 与 **MI-05 裁决（构造 + 真实各半）** 合并处理：真实样本**允许**取自本迭代自身执行记录，但须在验证报告里标明来源与场景 |

## 记录时点

- 时标：2026-09-13（本地时钟）
