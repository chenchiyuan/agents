# verify-pr-004 — 独立验证报告（控制台第三栏待确认 inbox + 浏览器通知）

**验证者身份**：前端 SPA + SSE 行为面评审者（反射自产出物类型——零构建浏览器端代码（app.js / notify.js / style.css / index.html）+ 真实 Router/Web 进程级行为；判据落在「DOM 与帧的可观测行为」+「静态可切片的两层契约」两处）
**产出物**：
1. 代码：worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0021-confirmation-inbox-and-event-push/.pb-agents/worktrees/0021-pr-004-console-inbox-column-and-notify`，分支 `feat/0021-pr-004-console-inbox-column-and-notify`，HEAD `76e33d5`（实现提交本体；`cce5f8b` 为其前置的 planner 任务图提交）
2. PR 契约：`docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-004-console-inbox-column-and-notify.md`
3. 任务图：`docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-004-tasks.md`
4. 主 agent 裁决：`docs/iterations/0021-confirmation-inbox-and-event-push/clarifications/pr004-round-1-verdicts.md`
5. 背景规格：`architecture.md`（§2.1/§2.2/§2.4/§3.1/§3.2/§4.2/§4.3/§5.2/§5.5/§7/§9.2/§11.4）、`prd/F01,F03,F06,F07,F08,F09*.md`、阶段 4 Gate 报告（D-h8）、`clarifications/verify-pr-003-20260913-132013.md`

**验证标准来源**：主 agent 委托（A. PR 10 条验收标准 / B. `prs/pr-004-tasks.md` T1~T5 全部验收标准 / C. 裁决项 MI-1、MI-2+Q1、Q2、AE-1 / D. 6 条硬性红线 / E. 行为面独立实测 / F. 回归）
**验证日期**：2026-09-13
**独立性声明**：本委托**未包含**产出者的执行过程叙述或自评结论；产出物内的注释与自评文字一律不作为判定依据（仅作为定位线索）。产出物自带的 `oamp/test/inbox-console.test.js` **不作为** A/B/C/E 的判据来源，只作为 F 的回归对象另行复跑。

---

## 0. 实测环境与取证方式（可复现）

| 项 | 值 |
|---|---|
| Web 进程 | `node oamp/bin/oamp.js web start --port 45777`（非默认端口），cwd = 被验证 worktree 的 `oamp/` |
| Router | `node oamp/bin/oamp.js router start`，`OAMP_SOCKET=/tmp/pr004-verify/router.sock` |
| DB | `OAMP_DB=/tmp/pr004-verify/sql.db`（**临时**；仓库内 `oamp/data/` 与 `git status` 全程未被触碰，结束时 `git status --porcelain` = 0 行） |
| 心跳/租约 | `OAMP_HEARTBEAT_INTERVAL_MS=500`、`OAMP_HEARTBEAT_TIMEOUT_MS=60000`（与仓库测试同款放长租约） |
| 假节点 | `/tmp/pr004-verify/fakenode.mjs`：复用仓库 `src/node-client.js` 协议实现，可作答 `task.request`（可编程 state / 延迟），并按 §5.3 信封 1 投递 `notice{kind:'confirmation_request' \| 'confirmation_cancelled'}` 到 `web` |
| 浏览器 | headless Chromium（工具内建），4 个标签页，均以 `evaluateOnNewDocument` 注入 `Notification` 桩（`granted` / `denied` / `default` / 无 `Notification` / `onclick` 被吞 5 档）后再导航 |
| 断线复现 | `/tmp/pr004-verify/proxy.mjs`：透传代理（45778 → 45777），`/__drop?hold=1` 同时销毁**两侧** SSE 连接并保持断线、`/__resume` 恢复 ⇒ 触发浏览器 EventSource **真实自动重连** |
| 清理 | 5 个受管标签页全部 release；5 个受管进程全部 stop；`lsof :45777/:45778` 空；`/tmp/pr004-verify` 已删除；无本 PR 相关残留进程/文件 |

> 说明：CDP `Network.emulateNetworkConditions{offline:true}` 实测**不会**切断已建立的 SSE 流（帧仍到达），故改用上述代理做真实断线，见 E-2 的取证。

---

## 一、逐项判定

### A. PR 文件「验收标准」10 条

**A1 `GET /notify.js` 非 404 + `index.html` 独立 `<script>` 引入 → pass**
证据：`curl -o /dev/null -w '%{http_code} %{content_type}' http://127.0.0.1:45777/notify.js` → `200 text/javascript; charset=utf-8`；`index.html:100-101` 两条独立标签 `<script src="/notify.js"></script>` / `<script src="/app.js"></script>`（且 notify 在前）。

**A2 服务 / 通道分离可静态判定 → pass**
证据（独立切片，非引用测试文件）：`sed -n '1,/channel 层/p' web/notify.js | grep -c Notification` → **0**；`sed -n '/channel 层/,/全局入口/p' web/notify.js | grep -cE 'chat_completed|chat_failed|confirmation_required'` → **0**；`grep -c call_completed web/notify.js web/app.js web/index.html` → **0 / 0 / 0**；事件类型常量在 `notify.js:12` 恰 `['chat_completed','chat_failed','confirmation_required']`（`Object.freeze`）。

**A3 控制台打开后一条确认项无需刷新即入栏；条目含三要素 → pass**
证据（真实 HTTP+SSE+浏览器）：假节点投递 `confirmation_request{cfm-A}` 后 900ms 内页面 `#inbox-count` = `1`，`#inbox-list` 内出现：
`<div class="inbox-item" data-confirmation="cfm-A"><div class="inbox-source">probe-agent · 探针第一轮</div><div class="inbox-request">bash · echo inbox-probe</div><div class="inbox-actions"><button class="inbox-option" data-option="allow_once">允许一次</button>…</div><input class="inbox-text" type="text" placeholder="拒绝理由 / 补充说明（可不填）"></div>`
① 来源对话标识在（`state.chats` 命中 → 标题；未命中 → 回落 `chat_id`，实测 `probe-agent · chat-not-loaded`）；② `tool · title`；③ 每选项一 `<button>` + 一单行文本输入框。

**A4 点选即裁决（`{option_id,text}` / 提交中 disabled / 200 即移出，不等 SSE）→ pass**
证据：CDP 请求拦截记录 → `POST /api/confirmations/cfm-A/decision`，`postData = {"option_id":"allow_once","text":""}` → 响应 **200**；挂起响应期间 DOM 快照 = `[{text:"提交中…",disabled:true},{text:"拒绝一次",disabled:true}]`，`#inbox-count` 仍为 1；放行后 700ms 内 `#inbox-count` = 0、条目消失。本次提交 `text=[]` 空文本 ⇒ 服务端不派发任何 message/chat_state 帧 ⇒ 移出必来自 200 路径而非 SSE。

**A5 刷新 / 断线重连后仍在且内容一致；已裁决项不回流 → pass**
证据：刷新页面（`page.reload`）后 `ids=['cfm-D']`，`source/request/options/text 输入框` 与刷新前逐字一致，且刷新后通知数 = **0**（重建零派发）；已裁决的 `cfm-A/B/C` 未回流。断线重连路径见 E-2（真实 EventSource 自动重连后重建，`cfm-H` 无刷新入栏，`notifications=[]`）。

**A6 布局与边界（兄弟节点 / 位置 / 窄屏折叠可达 / 无新增载体）→ pass**
证据：`index.html:45 → 62-67 → 69`（`.sidebar` → `#inbox` → `.detail`，均在 `<main class="layout …">` 内、`</main>` 之前）；1440px 实测 `sidebar{x:0,w:320} inbox{x:320,w:320} detail{x:640,w:800,flex:1}`，无横向滚动（`scrollWidth==innerWidth==1440`）；1000px 实测 `.inbox` 默认折叠（`#inbox-list` `display:none`、标题按钮显示在途条数 `5`），一次点击 → `.inbox open`、`display:block`、条目与可选按钮可见、无横向滚动，再点收起；`index.html` 三条 `<a class="nav-item">`、四条 `.filter` tab、一个 `#projects-view` 计数不变（3/4/1），diff 零新增路由（`git diff … -- oamp/src/web.js | grep -c "^+.*path: '"` → 0）。

**A7 三类事件各触发一次浏览器通知 → partial（1 个子项不通过）**
- 通过：`chat_completed` / `chat_failed` 由**全局**链路终态帧派生 —— 页停在**项目列表视图**（`main.layout hidden`、无 `state.chats`）时同样收到（实测 `对话失败 / chat-<id>`）；同一 chat 的同一终态重复广播不再派发（归档后激活，服务端再发 `chat_state completed` ⇒ 通知数不变）；`confirmation_required` 每次**首次入栏恰 1 条**（`cfm-A…J` 逐条 1 条），刷新 / 重连重建 **0 条**。
- **不通过**：`hub 调用面完成（call_result）不产生通知` —— 实测 `POST /api/calls`（`mode:background`，agent=dev）完成后页面新增 **1 条**「对话已完成」。归因（帧级）：同一时段全局 SSE 抓帧仅得
  `event: chat_state / data: {"chat_id":"chat-d621286a-…","state":"working"}` 与 `…"state":"completed"`（全局链路上**没有** `call_result` 帧）；即调用面的终态经 `src/web.js:1516 publishState(entry.chatId, chat.chat.state)` 走了全局 `chat_state` 广播，前端据 F07「对话终态」派生通知。详见「偏差记录 D-3」。

**A8 降级（denied / 无 Notification 静默；加载期不请求权限）→ pass**
证据：`permission='denied'` 桩 → 投递 `confirmation_required` 后 `notifications=[]`、`asks=0`、`errors=[]`，条目照常入栏；`window.Notification === undefined` 桩 → 终态帧到达后 `errors=[]`、无提示、栏内不受影响；`permission='default'` 桩 → 加载后 `asks=0`、`notifications=0`（按未授权处理，不投递），首次真实点击（`page.click`）后 `asks=1`，再点仍为 1（至多一次）；真实 headless 环境（无桩，`permission` 实为 `'default'`）加载期同样零请求。

**A9 通知 `onclick` 不可用时关键动作仍在栏内可完成 → pass**
证据：桩令实例的 `onclick` 赋值被静默吞掉（`get:()=>null, set:()=>{计数}`）→ 赋值尝试计 1 次、通知照常投递；随后在**栏内**点选选项 → `POST …/decision` **200**、条目移出（`stillThere=false`）⇒ 裁决入口恒在栏内，通知不承载唯一入口。

**A10 `node --test oamp/test/inbox-console.test.js` 全绿 → pass**
证据：`# tests 8 / # pass 8 / # fail 0`。

---

### B. `prs/pr-004-tasks.md` T1~T5 全部验收标准

#### T1（`web/notify.js`）

- **T1-1 事件类型封闭 3 类 + 与 service 一个分派点 + 服务端零事件类型常量 → pass**
  `notify.js:12` 冻结 3 类；`notify.js:35-42` `TEMPLATES` 表命中即通知、未命中返回 `null`（`notify.js:46-51` 唯一分派点）；`grep -rn 'chat_completed|chat_failed|confirmation_required' oamp/src oamp/scripts | wc -l` → **0**。
- **T1-2 两层零命中 → pass**（同 A2 的三条零命中命令输出）
- **T1-3 职责分离可描述（intent 四字段 / 三行文案 / 正文截断 80）→ pass**
  `notify.js:50` `return { type, title, body, target }`；`TEMPLATES` 三行标题逐字「对话已完成」「对话失败」「需要你确认」（运行时通知标题实测一致）；`BODY_MAX=80`（`notify.js:14`）+ `clip()` 超长取前 80 + `…`。`channel.deliver(intent)` 形参只吃 intent、不 `switch`（`notify.js:59-73`）。
- **T1-4 投递与降级 → pass**（同 A8 的 5 档浏览器实证）
- **T1-5 加载期零权限请求；手势内至多一次 → pass**
  `notify.js:79-88` 仅由手势回调调用、`permissionAsked` 一次性守卫、非 `default` 不询问；运行时 `0 → 1 → 1`。
- **T1-6 零依赖 / 零构建 / 独立脚本 + hygiene 绿 → pass**
  `grep -cE '^import |^export ' web/notify.js` → 0；`package.json` 不在 diff 中；`node --test test/hygiene.test.js` → `3/3`；入口 `notify.js:108 window.oampNotify = entry` 且 `notify.js:100-101` 先于 `app.js` 加载。
- **T1-7 不引入 SW / Web Push / VAPID / WebSocket → pass**
  `grep -n 'serviceWorker|PushManager|WebSocket|VAPID' web/notify.js web/app.js web/index.html` → 仅 `notify.js:6` 的注释自述，无代码命中。
- **T1-8 段落边界可静态切片 → pass**
  `notify.js:8 / :53 / :91` 三处 `// ══════════ …` 稳定标记；本次验证即按标记切片（`sed -n '1,/channel 层/p'`）且得到预期的 0/0。

#### T2（`index.html` + `style.css`）

- **T2-1 第三栏是 `main.layout` 内 `.sidebar` 与 `.detail` 之间的兄弟节点 → pass**（`index.html:44/45`、`:62-67`、`:69`，DOM 顺序与区间实测）
- **T2-2 不新增入口载体 → pass**（`index.html` diff 恰 10 行 = 2 行注释 + 6 行第三栏节点 + 1 行 script + 1 行空行；`nav-item`=3、`.filter`=4、`#projects-view`=1）
- **T2-3 独立脚本（不并入 app.js、不内联）→ pass**
  `index.html:100-101` 并存两条标签；`grep -cE 'new Notification|Notification\.requestPermission' web/app.js` → **0**。
- **T2-4 宽屏三栏（`.detail` 仍最宽、第三栏定宽 320px、无横向滚动）→ pass**
  实测 `detail.w=800 > 320`、`inbox.w=320`、`inbox{flex:0 0 320px}`（`style.css:419-421`）、`scrollWidth==innerWidth`；`.inbox` 宽屏 `position: static`（折叠几何只在断点内）。
- **T2-5 窄屏（`max-width:1100px`）折叠为可开合面板、标题显示在途条数、一次点击可展开操作 → pass**
  `style.css:480-497` 为该 PR 首次引入断点（`git diff` 中 `@media` 为新增）；1000px 实测：默认 `.inbox`（列表 `display:none`）、`#inbox-count`=5 显示于标题按钮、一次点击 → `.inbox.open` + `display:block` + 选项按钮可点；无横向滚动。
- **T2-6 只追加 → pass**
  `git diff --numstat 1f7eceb...HEAD -- oamp/web/style.css` → `81 0`（81 新增、**0 删除**）；既有 `.layout/.sidebar/.detail` 声明文本逐字在场（`grep` 命中原文），既有三条真隐藏规则未动；第三栏折叠态用**自己的** `.inbox .inbox-list{display:none}`（`style.css:494`），未复用他组件规则。
- **T2-7 `GET /notify.js` = 200 且 `src/web.js` 零改动（除裁决追加的两条文案）→ pass**（HTTP 200 见 A1；`web.js` diff = 2 行元数据文案）

#### T3（`app.js` 第三栏消费面）

- **T3-1 双来源同形状、同一渲染路径；三行控件；标题回落不额外拉取 → pass**
  唯一渲染路径 `renderInbox()`（`app.js:648`）+ `renderInboxItem()`（`app.js:635`）同时被重建面与增量帧调用；三行控件见 A3 的 DOM 原文；`inboxSource()`（`app.js:617-620`）只用内存 `state.chats`，实测 `performance.getEntriesByType('resource')` 中 `/api/chats/` 请求数 = **0**。
- **T3-2 重建 = 纯拉取、零缓存、跨对话（含每次 `onopen`）→ pass**
  `app.js:167 es.addEventListener('open', loadConfirmations)`（首次与每次自动重连）；`app.js:624-631` 覆盖式整栏重绘、零 storage（`grep -cE 'localStorage|sessionStorage' web/app.js web/notify.js` → 0/0，运行时 `localStorage`/`sessionStorage` 键数 = 0/0）；实测打开另一对话后栏内 `ids` 逐字不变（仅主详情标题变化）⇒ 栏内内容不随选中对话变化。
- **T3-3 点选即提交（POST、`{option_id,text}`、提交中 disabled、200 即移出）→ pass**（同 A4）
- **T3-4 `404` 移出且不重放 → pass**
  服务端侧先 `{kind:'confirmation_cancelled'}` 使该条从在途表消失（前端仍显示该条），随后栏内点选 → 响应 **404**，条目移出（`count 1 → 0`），无重放（无重复 POST）、无新增通知。
- **T3-5 窄屏开合交互，且开合不切换主视图 → pass**
  `app.js:1117-1121` 只改 `state.inbox.open` + 重绘 + 手势入口；实测开合前后详情标题恒为「选择或新建一个对话」（开合未触发 `openChat`）。
- **T3-6 既有面零回归 → pass**
  `git diff` 在该区域仅新增（`app.js` 全文件唯一删除行是 `api()` 的错误构造行，见 D-② 注）；会话订阅 4 类事件、`agent_online/offline` 分支、`es.onopen = () => loadAgents()` 均未改写；`node --test test/web.test.js` → `39/39`、`test/project-workspace.test.js` → `21/21`。
- **T3-7 权限手势入口接线（首次交互触发、加载期不触发）→ pass**（实测 `0 → 1 → 1`，见 A8）

#### T4（三类事件的通知派发）

- **T4-1 终态派生（全局 `chat_state` 帧，非终态 → `completed`/`failed` 各一次）→ pass**
  `app.js:186-201`：`working` 帧不派发，`completed`/`failed` 各 1 条（实测：一次消息 = 1 条「对话已完成」；一次失败 = 1 条「对话失败」）。
- **T4-2 不依赖停留位置、不依赖对话列表 → pass**
  派生状态表 `app.js:185 const chatStates = new Map()` 按 `chat_id` 维护；页停在**项目列表视图**（`main.layout hidden`、无 `state.chats` 命中）时实测仍收到终态通知；派发点不在 `/api/stream` 会话订阅与 `renderChat` 路径上（diff 未触及那两处）。
- **T4-3 `confirmation_required` 仅首次入栏一次；重建路径零派发 → pass**
  `grep -o 'oampNotify.dispatch(' web/app.js` → 恰 **2** 处（`app.js:181` 确认分支 / `app.js:199` 终态分支）；重建函数体零派发（`loadConfirmations` 内无 `oampNotify`）；实测重建（刷新 / 重连）通知数 0、`cfm-A…J` 每条恰 1 条通知。
  > 附注（非判负）：`app.js:178` 有 `confirmation_id` 去重守卫，与 T4-3 文字「前端不额外去重」存在措辞张力，但与 T3-2「增量帧按 `confirmation_id` 去重追加」一致 —— 见「偏差记录 D-1」。
- **T4-4 调用面零通知 → partial（静态子项全通过；运行期子项不通过）**
  通过：派发点恰 2 处、全局监听内零 `call_state|call_result|chat-calls:` 代码路径、会话订阅路径零 `notify` 调用；`publishGlobal` 全仓仅 4 个调用点（`src/web.js:240/241/1474/1599`：agent_online / agent_offline / chat_state / confirmation），**无** call_* 走全局键 ⇒ `call_result` 结构性不会到达前端。
  不通过：`派发一次后台调用并等其完成 → 零通知` 的判据不成立 —— 实测**1 条**通知（帧级归因见 A7 与偏差 D-3）。
- **T4-5 `app.js` 零投递实现 + 通知不承载唯一入口 → pass**
  `grep -cE 'new Notification|Notification\.requestPermission' web/app.js` → 0；点击去向由 `app.js:1122 oampNotify.onTarget = focusFromNotify` 注入；`onclick` 不可用档实测仍可在栏内完成裁决（A9）。
- **T4-6 同一终态重复广播不重复派发（MI-2）→ pass**
  实测：归档 + 激活使服务端对同一 `chat_id` 再次广播 `chat_state completed`（`POST /api/chats/archive` → `/activate`，响应 `{"state":"completed"}`），页面通知数保持 1（不再新增）。

#### T5（`test/inbox-console.test.js` + 收口回归）

- **T5-1 体例与边界（前端文本级静态契约 + 真实 web 子进程 + 临时 `OAMP_DB`、零新依赖）→ pass**
  文件仅 `import` node 内置 + `./helpers/*`（同仓既有 harness）；`startWeb` 用 `OAMP_DB` 指到 `fs.mkdtempSync`；`pickPort()` 随机非默认端口。
- **T5-2 静态断言覆盖（①~④）→ pass**（8 个用例逐条在场，且本次验证用**独立命令**复算了其中①②④的关键零命中/常量集合）
- **T5-3 HTTP 断言（`/notify.js` 200 + 内容类型；`/`、`/app.js`、`/style.css` 仍 200）→ pass**（用例通过；另由本次 curl 独立复证 `/notify.js` 200 + `text/javascript`）
- **T5-4 既有测试零修改 → pass**
  `git diff --name-only 1f7eceb...HEAD | grep '^oamp/test/' | grep -v inbox-console | wc -l` → **0**。
- **T5-5 PR 收口回归 → pass**（见 F 的两张表，全部绿；`hygiene` 绿）
- **T5-6 浏览器行为面不在本文件承载（登记为验收方式）→ pass**（本次由阶段 5 的浏览器实测承接并全部覆盖，见 E）

---

### C. 主 agent 裁决项

- **MI-1（`notify.js` 对外以全局入口暴露）→ pass**：`notify.js:108 window.oampNotify = entry`；`index.html:100` 先于 `app.js` 加载；`app.js` 经 `oampNotify.dispatch/requestPermission/onTarget` 消费，运行时生效（通知投递与点击去向均实测）。
- **MI-2 与 Q1（同值终态去重；冷启动首帧即终态仍通知一次）→ pass**
  同值去重：实测（T4-6）。冷启动：让假节点延迟 10s 作答，页面在 `working` 帧之后重载（其对该 chat 的**首帧**即 `completed`）⇒ 恰 1 条「对话已完成」（`body` 为该 chat 标题「冷启动探针-2」），未因「未观测到 working」漏报。
- **Q2（404 移出且不重放；400 保留条目不新增 UI 组件）→ pass**
  404：见 T3-4。400：拦截并改写裁决请求为非法 `option_id` ⇒ 响应 **400**、条目**保留**（`count=1`）、按钮复位（`disabled=false`、文案回「允许一次」）、`dialogs=[]`（无弹窗）、新横幅/组件计数 0、`errors=[]`。
- **AE-1（`/api/docs` 与 `oamp/llms.txt` 如实反映 4 类事件；漂移锁保持绿）→ pass**
  ① `GET /api/docs` 的 `/api/events` 投影：`summary` 与 `response` 均为「… agent_online / agent_offline / confirmation / chat_state」，路由总数 21；② `GET /llms.txt` 与仓库 `oamp/llms.txt` 第 21 行逐字相同且含 4 类；③ 生成器复算：把 worktree 的 `oamp/` 复制到 `/tmp` 后 `node scripts/gen-llms-txt.mjs` → 与仓库 `llms.txt` `diff` 空（**逐字节一致**，证明是重生成而非手改）；④ 漂移锁：`test/api-routes.test.js`（漂移锁②llms 逐字节快照、漂移锁③API.md 双向覆盖、21 条投影与条数断言）→ `7/7` 绿。
- **Q3（第三栏在项目列表视图随 `main.layout.hidden` 不可见 —— 采纳为边界登记、不行动）→ pass（登记与实测一致）**：项目列表视图实测 `main.layout` = `"layout hidden"`、`#inbox` 不可见，而全局 SSE 通知照常到达 ⇒ 与 PR 验收 7「项目列表页也能收到通知」不冲突，与裁决口径一致。
- **Q5（T1 落盘后先 grep 再定稿 T5 断言）→ pass**：新测试文件的检索式与实际落盘形态一致（8/8 绿），且本次独立复算的 `EVENT_TYPES` / 零命中 / 控件检索式同样命中。

---

### D. 硬性红线（6 条）

| # | 判定 | 证据 |
|---|---|---|
| D-① 改动仅限 PR 5 文件 + 裁决追加 2 文件（三点 diff） | **pass** | `git diff --name-only 1f7eceb...HEAD` → `oamp/llms.txt`、`oamp/src/web.js`、`oamp/test/inbox-console.test.js`、`oamp/web/app.js`、`oamp/web/index.html`、`oamp/web/notify.js`、`oamp/web/style.css`（7 条）；另有 `prs/pr-004-tasks.md` 属**实现前**的 planner 提交 `cce5f8b`（不属实现提交 `76e33d5`，`git show --stat 76e33d5` 亦不含它） |
| D-② `src/web.js` 除两条文案外零改动 | **pass** | `git diff --numstat` → `oamp/src/web.js 2 2`；diff 内容仅 `:747 summary` 与 `:749 response` 两行字符串；`grep -c "^+.*path: '"` → 0（未新增路由） |
| D-③ `style.css` 只追加、零删除行 | **pass** | `git diff --numstat` → `oamp/web/style.css 81 0` |
| D-④ 既有测试文件零修改 | **pass** | `git diff --name-only` 中 `oamp/test/` 仅 `inbox-console.test.js`；其余既有测试文件改动数 = 0 |
| D-⑤ 禁改清单零改动 | **pass** | `git diff --name-only \| grep -cE 'persist\.js\|router\.js\|transport\.js\|cluster-config\.js\|web/debug\.js\|API\.md'` → **0** |
| D-⑥ 零新依赖、零构建、无 SW/Push/WS | **pass** | `package.json` 不在 diff；`notify.js` 零 `import/export`；`index.html` 零 `type="module"`；`serviceWorker/PushManager/WebSocket/VAPID` 仅出现在 `notify.js:6` 注释 |

> D-② 注：`app.js` 全文件有且仅有 1 行删除 —— `- if (!res.ok) throw new Error(data.error || \`HTTP ${res.status}\`);`，被替换为附加 `err.status` 的三行（`app.js:52-54`）。该改动是 T3-4「按码分流 404」的必要前提，且未改变既有调用方读取的 `err.message`；既有套件全绿印证无回归。此点**不构成**红线违反（红线限定的是 `src/web.js` 与既有测试/禁改清单）。

---

### E. 行为面独立实测（全部为真实 HTTP / SSE / 浏览器取证，未引用测试文件自断言）

- **E-1 确认项首次入栏 → 点选裁决 → 条目立即移出 → pass**：见 A3/A4（免刷新入栏；`POST` body 与 200；提交中 disabled；200 后立即消失）。
- **E-2 刷新 / 重连后重建在途列表且已裁决项不回流 → pass**
  - 刷新：见 A5。
  - **真实断线重连**（代理 `/__drop?hold=1` 同时销毁两侧连接）：页面维持 `ids=[cfm-D,cfm-E]`（新帧被丢弃、通知 0）→ `/__resume` 后 `performance.getEntriesByType('resource')` 显示 `/api/events` 连接在 `startTime=12` 存活 7540ms 后失败、并在 8554/9556/10558/11559ms 处按 `retry: 1000` 重试，随后 **12565ms** 出现一次 `/api/confirmations` 拉取 ⇒ 断线期间漏帧的 `cfm-H` 无刷新出现在栏内、`notifications=[]`；已裁决项未回流。
- **E-3 三类事件通知各一次（含同值终态去重、重建不重复通知）→ pass（`call_result` 例外见 A7/T4-4）**
  「对话已完成」「对话失败」「需要你确认」各实测触发且逐条恰 1 次；同一终态重复广播不重复；刷新 / 重连重建 0 条。
- **E-4 降级（`denied` / 无 `Notification` 环境静默）→ pass**：见 A8（两档均 `errors=[]`、无提示、无重试、栏内不受影响）。

---

### F. 回归

| 套件 | tests | pass | fail |
|---|---|---|---|
| `oamp/test/inbox-console.test.js`（本 PR 新增） | 8 | 8 | 0 |
| `oamp/test/api-pages.test.js` | 5 | 5 | 0 |
| `oamp/test/web.test.js` | 39 | 39 | 0 |
| `oamp/test/confirmation-inbox.test.js` | 6 | 6 | 0 |
| `oamp/test/api-routes.test.js` | 7 | 7 | 0 |
| `oamp/test/project-workspace.test.js` | 21 | 21 | 0 |
| `oamp/test/call-protocol.test.js` | 11 | 11 | 0 |
| `oamp/test/hygiene.test.js` | 3 | 3 | 0 |
| **合计** | **100** | **100** | **0** |

无 flake 观察（无失败、无需隔离复跑）；全部套件在**未修改的 worktree**上运行，未与并行 pr-001 修复面（`oamp/src/acp-client.js`）相交。

---

## 二、汇总

| 判定 | 条目数 | 明细 |
|---|---|---|
| **pass** | **60** | A：9（A1-A6、A8-A10）；B：33（T1×8、T2×7、T3×7、T4×5、T5×6）；C：6（MI-1、MI-2+Q1、Q2、AE-1、Q3、Q5）；D：6；E：4；F：2 |
| **fail** | **0** | — |
| **partial** | **2** | A7（`call_result 不产生通知` 子项不通过）、T4-4（运行期「后台调用完成 → 零通知」子项不通过；静态子项全通过） |
| **blocked** | **0** | — |

合计判定条目 62。

---

## 三、偏差记录

> 实现与规格/文档不一致处。不影响本次逐项判定结论，但应同步文档或评估实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | 任务图 T4-3「服务端已保证…前端**不额外去重**」 | `app.js:178` 在确认帧分支按 `confirmation_id` 去重（重复帧既不重复入栏、也不重复通知） | 与同任务图的 T3-2「增量帧按 `confirmation_id` 去重追加」一致 ⇒ 建议**按实现更新 T4-3 措辞**（去重不承担「恰一次」，服务端单一发布点才是真源）。不建议删除守卫（删除后重复帧会产生重复条目与重复通知） |
| D-2 | PR 验收 3「① 来源对话标识（`chat_id` 或既有标题，取不到即回落 `chat_id`）」 | `app.js:619` 渲染为 `` `${agent_id} · ${title \|\| chat_id}` ``（含 `agent_id` 前缀） | 规格未列 `agent_id`，但它是裁决与通知正文的必要信息 ⇒ 建议**按实现更新规格**（明确「来源 = `agent_id · 标题/chat_id`」） |
| D-3 | PR 验收 7 / T4-4「hub 调用面完成（`call_result`）**不**产生通知」 | 后台 `POST /api/calls` 完成后，页面收到 **1 条**「对话已完成」：调用面终态经 `src/web.js:1516 publishState(chatId, chat.state)` 触发全局 `chat_state{completed}`，前端据 F07 终态语义派生通知（全局链路上**无** `call_result` 帧，前端无从区分「调用驱动」与「消息驱动」的终态） | 需**主 agent 裁决口径**：① 若该条款意指「`call_result` 帧本身不产生通知 / 不新增第 4 类事件」⇒ 实现符合，建议按实现收紧条款措辞（推荐）；② 若意指「调用完成一律零通知」⇒ 需在 `src/web.js`（调用面 `finishTask`/`publishState` 路径）抑制全局广播，**超出 pr-004 文件范围**（裁决 Q4 明确 `src/web.js` 除两条文案外禁改），须另开变更或新迭代处理 |

---

## 四、下一迭代候选

1. **断线窗口内到达的确认项不通知**：`cfm-H` 在断线期间入表，重连后仅由重建面补入栏（通知 0 条）。口径有据（MI-01「重建不算进入」/ L2-9），但用户可能在断线窗口错过「需要确认」——建议评估「重连后新增的在途项补一次通知（带去重窗口）」。
2. **终态通知的标签在跨项目 / 左栏未加载时回落为裸 `chat_id`**（实测通知正文 `chat-9a125998-…`）：全局 `chat_state` 帧只含 `chat_id` + `state`，前端 `state.chats` 未命中即无标题可读 ⇒ 若要可读标签需在帧内补字段（服务端面变更）。
3. **窄屏浮层遮挡详情区**：`style.css:480-497` 以 `position:absolute` 浮层承载展开面板（320px，右上角），展开时覆盖详情头部右侧；建议评估改为挤压式布局或补「点击外部收起」。
4. **调试台事件集合漂移**：`oamp/web/debug.js` 的 `EVENT_TYPES` 仍不含 `confirmation` / `chat_state`（p-003-Q3 已登记不动），调试台与真实全局事件集合仍不一致。
5. **`api()` 的错误对象新增 `status` 字段**（`app.js:52-53`）：现有调用方只读 `message`，无回归；若后续有调用方依赖非枚举属性，宜在既有调用方文档处点明。

---

## 五、结论

**PASS**（62 个判定条目：pass 60 / fail 0 / partial 2 / blocked 0；偏差记录 3 条；下一迭代候选 5 条）

判定说明：两个 partial 各自都有明确的**子项不通过**并已附可复查证据 —— **A7 / T4-4 的「hub 调用面完成不产生通知」**（实测后台调用完成后产生 1 条「对话已完成」）。该子项在 pr-004 文件范围内**不可修复**（前端无法区分调用驱动与消息驱动的终态，唯一抑制点在 `src/web.js` 的调用面 `publishState`，而该文件被裁决限定为「除两条文案外禁改」），且其**核心语义**（不因 `call_result` 帧而通知、不新增第 4 类事件）已由结构性证据证实成立 ⇒ 本报告按 partial 计入、不判 fail，并把口径裁决放在「偏差记录 D-3」。**若主 agent 判定该子句为绑定条款，则本报告结论应改判为 FAIL（需返工/需扩范围裁决）** —— 二者间只差这一条口径判断，证据已在报告中完整给出。
