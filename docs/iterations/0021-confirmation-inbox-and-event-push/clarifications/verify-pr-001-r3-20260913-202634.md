# pr-001 独立验证报告 · 第 3 轮复验（agent 侧权限挂起链路 + ACP 答复链路修复）

验证者身份：**ACP/JSON-RPC 协议集成的同级代码审查者 + 真实运行时验证者**（具备 omp dist 反查、帧级协议复现与真实 omp 受控复跑能力）。判断依据仅来自产出物本身与主 agent 指定的验证标准；**未接收、未使用任何执行过程上下文**——commit message、PR/任务图的结论句、dev 自建证据文件的结论一律只作为「被核查对象」，不作为判定依据。
产出物：
1. worktree `.pb-agents/worktrees/0021-pr-001-permission-suspend-and-reply-fix`，分支 `feat/0021-pr-001-permission-suspend-and-reply-fix`，HEAD `dd7baddff86b7279a330d444449180b347c4c7b5`（**本轮改动面** = `git diff 1f11b59...HEAD`；全量面 = `git diff 27eef14...HEAD`）
2. `docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-001-agent-permission-suspend-and-reply-fix.md`
3. `docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-001-tasks.md`
4. `clarifications/pr001-round2-verdicts.md`（C1~C6 + 附加交付项，验证标准来源）
5. `clarifications/verify-pr-001-20260913-131042.md`（首轮报告）与 `clarifications/verify-pr-001-r2-20260913-200400.md`（第 2 轮报告）——**仅用于确定本轮应复核哪些条目与偏差 #3/#2 是什么**，两报告的任何结论均未被引用为通过证据
6. `clarifications/pr-001-m4-evidence.md`（R2 补落的 M4 证据文件，按文件实际内容独立核查）
验证标准来源：主 agent 委托的 A（PR 验收 6 条）+ B（T1~T6 全部验收标准）+ C（C1~C6）+ D（本轮变更面 D1~D3）+ E（3 组回归 + 4 项内务）
验证日期：2026-09-13 ｜ **环境**：真实 `omp` 18.0.11（`/Users/chenchiyuan/.bun/bin/omp` → `dist/cli.js`，**19,803,745 B**，本次实测复核）；node v22.15.0；模型 `deepseek/deepseek-v4-flash`

**被验 worktree 全程只读**（未 commit/checkout/stash；`git status --porcelain -uall` 为空）；全部探针/对照树/产物落在 `/tmp/r3verify`，未向任何仓库写入（本报告是唯一写入，落在迭代工作区 `clarifications/`）。

**本轮自建工具**（全部在 `/tmp/r3verify`，未复用 PR 自带用例、未复用前两轮任何探针）：

| 自建件 | 用途 |
|---|---|
| `fake-acp.mjs` | **脚本化** fake ACP 服务端：15 个命名场景（重叠挂起 / 拒绝清凭据 / 跨轮 / C2-C4 窗口 / 非审批 elicit / 超时对照…），逐帧落盘两侧帧 + 绝对时间戳 |
| `probe-frames.mjs` | 帧级探针（7 场景）：D1a/D1b/D1c/D2/D3a/D3b/D3c |
| `probe-frames2.mjs` | 帧级探针（8 场景）：A2/A3/A4/A4b/T2.4 审计/C2+C4 窗口/C3/C4 残余面/C5 |
| `omp-wrap.sh` | temp argv wrapper：逐字记录 daemon 路径 argv 后 exec 真实 omp（`R3_STRIP_NO_SESSION=1` 丢弃 `--no-session`） |
| `omp-mitm.mjs` | temp 中间人 wrapper：双向**按完整行**逐帧落盘真实 omp 帧（argv 一并记录） |
| `probe-real.mjs` | 真实 omp 产品路径探针：bash-allow / bash-deny / write-allow / write-deny / write-nohook / write-allow-delay1000 / bash-allow-strip |
| `probe-raw2.mjs` | 裸 ACP 驱动（不经 `AcpClient`、**不发** `session/cancel`）：观测 bash 拒绝的**模型侧**文字 |
| `blocks/compare.mjs` | 测试块字节比对器：按 `test('<name>')` 花括号配对提取块体 + md5 比对 |
| `clients/acp-client-{head,pre,r1}.js` | 源对照：HEAD（md5 `03094461b8a5d4baadf4c8beae65cbff`，与被验 worktree 文件一致）、`1f11b59`（第 2 轮，`70fb06064b3f20265285eaca30e10a9f`）、`dd59d31`（首轮，`be83310360d9a09bfc628d4ab09dee70`） |

**本轮改动面**（`git diff 1f11b59...HEAD`，逐行读过）：`oamp/src/acp-client.js` +18/−5（① `_request` 的 `entry` 增 `paused:false`；② 拒绝分支清空 `_approvalGrants`；③ `_pauseTurnTimer` 增「已冻结只递增深度」分支；④ `_resumeTurnTimer` 的恢复前提改为 `entry.paused`）；`oamp/test/tool-permission.test.js` +96（fake 新增 `suspend_pair`/`suspend_pair_hang` 两场景 + 两条新用例）。

---

## 逐项判定

### A. PR 文件「验收标准」6 条

- **A1（`tool-permission.test.js` 全绿；同步 `'allow'`/`'deny'` 两条既有用例逐字保留；`deny` 档三步不变）：pass**
  证据①：`node --test oamp/test/tool-permission.test.js` → `1..24 / # tests 24 / # pass 24 / # fail 0`（`/tmp/r3verify/reg-toolperm-pass.txt`；另有 1 次全文件运行出现 1 例失败，未归因本 PR，见 E1 的 flake 分析）。
  证据②（**字节级**，自建块比对器）：`1f11b59` 与 HEAD 的 22 个既有 test 块**全部 IDENTICAL**（`identical: 22 / changed: 0 / added: 2`）；其中
  `F05-2/§11.2：允许档恒回 allow_once…` 1538 B ≡ 1538 B **IDENTICAL**；`F05-3/F05-4：拒绝档回 reject_once + 立即 session/cancel + 轮次 permission_denied…` 1223 B ≡ 1223 B **IDENTICAL**。
  证据③（`deny` 三步帧级）：自建 fake（`single_perm`）+ 钩子返回 `'deny'` → 帧序原文 `… server_request_reply P_single result={"outcome":{"outcome":"selected","optionId":"reject_once"}} → prompt_result → SESSION_CANCEL`，`prompt()` 以 `error.code='permission_denied'` 结算（`/tmp/r3verify/out2/single_perm-7-frames.jsonl`）。代码侧差异仅新增 `_approvalGrants.length = 0` 一行（`git diff 1f11b59...HEAD -- oamp/src/acp-client.js`）。

- **A2（未结算 Promise 期间不回包；结算后 `optionId` = 钩子选定值）：pass**
  证据（自建帧级探针 `A2_echo_suspend`，HEAD 源 + 自建 fake，`timeoutMs=300`）：请求帧 `session/request_permission` 于 `t=0` 发出，钩子 Promise 在 **458 ms** 后结算 ⇒ 挂起窗口内 `replyCount=0`、`cancels=0`、`dead=false`；结算后应答帧原文 `{"outcome":{"outcome":"selected","optionId":"allow_always"}}`（`sinceGateMs=458`），即**原样回显钩子选定值**而非恒 `allow_once`；轮次 `end_turn`。

- **A3（非法 `optionId` 回落并记审计；fake 侧不出现「未知 option ID」类错误）：pass**
  证据（`A3_illegal_option_*`）：钩子返回 `{optionId:'allow_everything_forever'}` → 回包 `optionId='allow_once'`；`{optionId:'buffalo_forever'}` → 同样 `allow_once`；两次 `replyOptionIdInRequestOptions=true`（应答值 ∈ 请求方 `options`）、客户端 error 帧数 `0`、轮次 `end_turn`（未崩在协议校验里）。
  旁证（omp 侧硬约束实存，本次直读 dist 偏移 `15551720`）：`let R=SEs.get(T.optionId);if(!R)throw new X(\`Tool permission response used unknown option ID: ${T.optionId}\`)`。

- **A4（挂起期间冻结轮次计时；裁决后按剩余时间恢复并正常结算）：pass**
  证据①（单次挂起 > `timeoutMs`，`A4_single_freeze`）：挂起至 **708 ms**（`timeoutMs=300`）时 `cancels=0`、`dead=false`、`replies=0`；裁决后 `end_turn`、全程 `cancels=0`。
  证据②（按剩余预算恢复，`A4b_single_remaining_budget`）：挂起 250 ms 后裁决、轮次不结算 ⇒ `settledBeforeRelease=false`，随后在 **559 ms** 处以 `code='timeout'` 结算 + 1 次 `SESSION_CANCEL`（= 250 挂起不计预算 + ~300 剩余预算）。
  证据③（超时语义未被改写，`A4_timeout_control`）：无挂起 + 服务端不结算 ⇒ **352 ms** 时 `timeout` + 1 次 `session/cancel`。
  证据④（重叠挂起，本轮重点）：见 D1。

- **A5（`tools=true` 且 `allow` ⇒ argv `always-ask`；`deny`/`tools=off` 形状不变；一次性路径仍 `yolo`）：pass**
  证据①（**真实 argv 逐字**，本次共 13 次真实 omp 运行，wrapper 在 exec 前记录，全部一致）：`["acp","--no-skills","--no-rules","--no-session","--approval-mode","always-ask"]`（`/tmp/r3verify/real/argv.log`、`argv-raw.log`、`mitm-bash2.jsonl` 的 meta 行）。
  证据②（HEAD 源码 `acp-client.js:141`）：`if (this.tools) args.push('--approval-mode', 'always-ask');` —— 两档同值；`tools=false`/匿名不追加。
  证据③（套件断言现状，`tool-permission.test.js:502-528`）：allow 档 `assert.equal(allowArgv[allowIdx+1],'always-ask',…)`；deny 档 `assert.equal(denyArgv[…+1],'always-ask')`；`tools=false` 与匿名均 `assert.ok(!…includes('--approval-mode'))`，且 `assert.ok(offArgv.includes('--no-tools'))`；该块在 `1f11b59`→HEAD **零改动**（A1 证据② 的 22 个 IDENTICAL 块之一）。
  证据④（**一次性路径零改动**）：`oamp/test/acp-daemon.test.js` 在 `1f11b59` 与 HEAD 之间 **整文件 md5 相同**（`6550e32162c853b17d7e9bda50c45183`）⇒ `:554` 的 `yolo` 断言逐字未改；`:532` 为 `always-ask`（R1 引入）。

- **A6（M4 硬义务：真实 omp 双向；审计行不作判据）：pass** —— 判据全部来自**本次自建的真实 omp 复跑**（结果见 T6/C1/C6），全程未使用 `TOOL_APPROVED`/`TOOL_DENIED` 作任何结论依据。

### B. `prs/pr-001-tasks.md` 的 T1~T6

**T1（argv 改档 + 两处断言同步）**
- T1.1（两档 `always-ask`；`tools=false`/匿名不含）：**pass** — 见 A5①②③。
- T1.2（常驻路径断言同步）：**pass** — `acp-daemon.test.js:532` 逐字为 `assert.equal(devArgv[devArgv.indexOf('--approval-mode') + 1], 'always-ask', '§4.4/L1-2②（pr-001）：…')`；`node --test oamp/test/acp-daemon.test.js` → `# tests 7 / # pass 7 / # fail 0`。
- T1.3（一次性路径零改动；`agent.js` 不在 diff）：**pass** — `:554` 逐字 `assert.equal(devOneShot[…+1], 'yolo', '§4.4/pr-007②：allow 档一次性 argv 应含 --approval-mode yolo')`，该文件跨 `1f11b59`→HEAD 零改动（md5 相同）；`git diff --name-only 27eef14...HEAD` 不含 `oamp/src/agent.js`。

**T2（钩子返回域扩展）**
- T2.1（挂起期不回包，帧级）：**pass** — 见 A2（窗口内 `replyCount=0`，应答落在结算之后）。
- T2.2（结算后回显钩子 `optionId`）：**pass** — `allow_always` 原样回显（A2）。
- T2.3（同步 `'allow'`/`'deny'` 两条既有路径逐字不变）：**pass** — A1 证据②（1538 B / 1223 B IDENTICAL）。
- T2.4（审计在裁决后写恰一行、`option` = 实际回显值）：**pass** — 自建探针 + 自建 logger 记录：同步 `'allow'` → 恰 1 行 `{name:'TOOL_APPROVED', option:'allow_once', tool_call_id:'call_S', source:'acp_permission'}`；同步 `'deny'` → 恰 1 行 `{name:'TOOL_DENIED', option:'reject_once', …}`，回包分别为 `allow_once`/`reject_once`；代码序 `acp-client.js:462` `_respond(...)` 先于 `:466`/`:477` 的 `_audit(...)`。
- T2.5（JSDoc 返回域改写，优先级语义保留）：**pass** — `acp-client.js:88`：`(info) ⇒ 'allow'|'deny'|{optionId}|Promise<…>；给了则优先于 permission`（前缀语义逐字保留），并新增第二形态说明（`:89`）。

**T3（`optionId` 校验与回落）**
- T3.1（非法 → 回落 + 审计 = 回落值）：**pass** — A3（两例均回落 `allow_once`）；`_audit(eventName, toolCall, option)` 的第三参即已回落值（`acp-client.js:477`）。
- T3.2（应答值恒合法，轮次不崩）：**pass** — A3（应答值 ∈ `options`、客户端 error 帧 0、`end_turn`）。
- T3.3（合法 `{optionId}` 逐字回显）：**pass** — A2（`allow_always`）。

**T4（冻结轮次计时）**
- T4.1（挂起 > `timeoutMs` 未被 cancel/kill）：**pass** — A4①②，以及 D1 的重叠场景（挂起 1208 ms > 3×`timeoutMs` 仍零 `cancel`、`dead=false`）。
- T4.2（裁决后按剩余时间恢复并正常结算；超时路径仍能 cancel→kill）：**pass** — A4②（559 ms 处按剩余预算到点）与 A4③（352 ms 到点 + cancel），以及 D1c。
- T4.3（冻结只作用于挂起路径）：**pass** — 同步 `'allow'`/`'deny'`（A1）、回落路径（A3）计时行为未变；`_pauseTurnTimer` 仅由 `_askHook` 在钩子返回 thenable 时调用（`acp-client.js:527-535`），非挂起路径零触碰；对照用例 `A4_timeout_control` 观测到 cancel。

**T5（M4 根因定位，证据落盘）** —— 对象为 `clarifications/pr-001-m4-evidence.md`（本次按文件实际内容核查；每条结论均由本人在 `dd7badd` 侧重新取证）
- T5.1（按 D3→D1→D2 给单一结论，每条含成立/不成立的观测证据）：**pass** — 文件 §1 结论表逐字「两侧结果一致 ⇒ 排除『探针 argv 产物』」；§3 逐字「四档内部结果**逐档相同** ⇒ **D2（…）不成立**」；§4 末段逐字「**由 ①③→ 根因**：M4 症状 `Tool call denied by user: bash` 由**第二道审批门**产生（不是权限门…）」并给出 D1 方向的排除链（§2.2 的应答帧原文 + §4⑧ 的 `SEs.get(T.optionId)` 校验链）。**注**：D1 方向未以「D1：不成立」标签行显式写出，而是以「帧原文 + omp 取值路径比对」体现（记入偏差 #4，低）。
- T5.2（D3 步以 daemon 真实 argv 复跑并记录 argv 逐字，判据「其中无 `--no-session`」）：**pass** — 文件 §1 并列两侧 argv 逐字；本次**独立复跑两侧**：含 `--no-session` 侧（daemon 真实形态）与 strip 侧（wrapper 丢弃后 omp 实收）模型侧结果与副作用一致（含 `--no-session` 侧 2 次运行、strip 侧 1 次运行，均：bash 真实执行 + `se-bash-allow.txt` 落盘）。判据字面（「其中无 `--no-session`」）与代码事实相反（`acp-client.js:140` 无条件 `args.push('--no-session')`；本次 13 次实跑记录全部含之），文件已如实指出该前提错误 ⇒ 可达成部分已达成（记入偏差 #3，主 agent 已登记为偏差 #2）。
- T5.3（D1 证据 = 帧逐字比对 + omp 读取路径结论）：**pass** — 本次自建 MITM 落盘的**真实帧原文**：`{"jsonrpc":"2.0","id":0,"method":"session/request_permission","params":{"sessionId":"…","toolCall":{"toolCallId":"call_00_Ins5t5dR5VjVlLPc5EG80382","title":"…","kind":"execute","status":"pending","rawInput":{"command":"…"},"content":[…],"locations":[]},"options":[{"optionId":"allow_once",…}]}}` —— 实测 `JSON.stringify(frame).includes("toolName") === false`（与文件 §4⑦ 的 `mba()` 结论一致）；客户端应答帧逐字 `{"id":0,"result":{"outcome":{"outcome":"selected","optionId":"allow_once"}}}`、`{"id":1,"result":{"action":"accept","content":{"value":"Approve"}}}`（`/tmp/r3verify/real/mitm-bash2.jsonl`）。
- T5.4（D2 证据 = 0/1ms/100ms/1s 四档模型侧结果对照表）：**pass** — 文件 §3 给出完整四档表（修复前四档皆 `Tool call denied by user: write`；修复后四档皆真实写入）。**独立佐证范围（如实声明）**：本次未重跑全部四档，而以两端端点 + 机理两条独立证据佐证——(i) 真实 omp 下审批门应答延迟 0 ms（`write-allow`）与 1000 ms（`write-allow-delay1000`）结果**逐字相同**（均 `Successfully wrote 14 bytes…` + 文件存在）；(ii) 自建 fake 下同一帧序在不同应答时刻（0 / 458 / 601 / 1208 ms）得到同一应答值与同一轮次结局。
- T5.5（结论不以审计行为证据）：**pass** — 文件 §1~§5 判据均为模型侧结果 / 副作用文件 / 原始帧；§6 单列审计行且明示「不作判据」。本报告亦全程未使用审计行判定 M4。

**T6（M4 修复落地 + 双向真实 omp 验证）**
- T6.1（放行路径模型侧 = 工具真实输出）：**pass** — 真实 omp（argv 见 A5①），`bash`：模型侧原文 `原始输出逐字如下：\n\n```\nR3-BASH-ALLOW\n\n\nWall time: 0.03 seconds\n```…`（不含 `denied by user`）；副作用文件 `/tmp/r3verify/se-bash-allow.txt` **存在**，内容 `R3-BASH-ALLOW\n`。
- T6.2（拒绝路径模型侧确实 denied）：**pass** — 两条独立证据：(a) `write`（非 `bEs`）产品路径：模型侧原文 `写入被拒绝。工具返回的原始输出逐字如下：\n\n```\nTool call denied by user: write\n```…`，副作用文件 `/tmp/r3verify/se-write-deny.txt` **不存在**，`stop_reason=end_turn`；(b) `bash` 产品路径以 `permission_denied` 结算且 `se-bash-deny.txt` **不存在**，模型侧文字不可观测（拒绝语义 = 回包后立即 `cancel`），故以**自建裸 ACP 驱动**（逐字相同应答帧 `reject_once`、**不发** cancel）补测：模型侧原文含 `| 返回 | \`Tool call rejected by user (bash)\` |`，副作用文件**不存在**。
- T6.3（1+2 同时成立）：**pass** — 两侧均在本报告独立复跑成立（13 次真实 omp 运行）。
- T6.4（不回归 fake 层）：**pass** — 24/24、7/7（A1、T1.2）。
- T6.5（oamp 全量全绿）：**pass** — `node --test oamp/test/*.test.js` → `1..296 / # tests 296 / # pass 296 / # fail 0`（**首轮即全绿**，无 `EADDRINUSE` 类 flake ⇒ 未触发隔离复跑与对照树归因流程）。

### C. `clarifications/pr001-round2-verdicts.md` C1~C6

- **C1（写类工具经上浮可裁决）：pass**
  真实 omp（MITM 落盘，`write` 场景**只有一道门**）：帧原文 `{"method":"elicitation/create","params":{"mode":"form",…,"message":"Allow tool: write\nPath: /tmp/r3verify/se-write-allow.txt\nContent:\nR3-WRITE-ALLOW","requestedSchema":{"type":"object","properties":{"value":{"type":"string","enum":["Approve","Deny"]}},"required":["value"]}}}`；钩子入参实测 `{kind:'tool_approval', toolName:'write', toolCallId:null, options:['Approve','Deny']}`（含工具名与选项集合 ⇒ 可供 pr-002 的 pending 表承接）。放行 ⇒ 模型侧 `Successfully wrote 14 bytes …` + 文件存在（内容 `R3-WRITE-ALLOW`）；拒绝 ⇒ 模型侧 `Tool call denied by user: write` + 文件不存在。**首轮偏差 #1 的行为回退在本轮未回归。**
- **C2（同一 toolCall 不重复提问）：pass**
  真实 omp（`bash`，MITM 帧序）：`session/request_permission@…` → 客户端回 `{"outcome":{"outcome":"selected","optionId":"allow_once"}}` → 立即 `elicitation/create('Allow tool: bash…')` @ **同一次运行** → 客户端回 `{"action":"accept","content":{"value":"Approve"}}` —— 而钩子**只被调用 1 次**（`hookCalls` 仅 `kind:'permission'`）⇒ 该 toolCall 自己的门由本次放行凭据抵扣、未重复上浮。
  帧级（自建 fake `c4_window`，钩子对审批门恒 `deny`）：`gate_1_own = Approve` 且钩子零调用；随后 `gate_2_unrelated = Deny`（钩子上浮，`toolName:'write'`）、`gate_3_after_terminal = Deny`（终态后凭据失效）。套件另有 `M4/C2+C4` 用例固定该行为（`tool-permission.test.js:944-979`，24/24 绿）。
- **C3（非交互安全默认）：pass**
  帧级（自建 fake `only_gate`，`onPermissionRequest=null`）：应答 `{"action":"accept","content":{"value":"Deny"}}`、轮次 `end_turn`（未崩在协议校验里）。
  真实 omp（`write-nohook`，`onPermissionRequest=null`）：`hookCalls=[]`、模型侧原文 `write 被拒绝，文件未创建。原始输出逐字如下：…Tool call denied by user: write…`、副作用文件不存在、`stop_reason=end_turn`。
- **C4（消除误放行窗口；首轮偏差 #4 的确定性复现已消失）：pass**
  自建 fake + R1 报告偏差 #4 的**同型帧序**（`permission_A→allow` → `gate_1_own` → `gate_2_unrelated` → `call_A` 终态 → `gate_3`），钩子对审批门恒 `deny`（⇒ 任何 `Approve` 都必然未经人工裁决）：

  | 帧 | R1 源 `dd59d31`（本次独立复跑） | 本 PR HEAD `dd7badd` |
  |---|---|---|
  | `gate_1_own` | `Approve`（单槽凭据） | `Approve`（凭据抵扣，钩子零调用） |
  | `gate_2_unrelated` | **`Approve` ← 无人工裁决（首轮偏差 #4）** | **`Deny` ← 上浮后由钩子裁决** |
  | `gate_3_after_terminal` | `Deny` | `Deny` |
  | 钩子调用 | 仅 `[permission]`（审批门零调用） | `[permission, tool_approval:write, tool_approval:bash]` |

  fake 层固定：`M4/C2+C4` 用例断言 `gate_2_unrelated` 必须为 `Deny` + `gateCalls=['write','bash']`（套件 24/24 绿）。
  **残余暴露面（本次独立取证，见偏差 #5）**：把「无关同型门」置于该 toolCall **自身门之前**时（`permission_A→allow` → `gate_unrelated_first` → `gate_own_second` → 终态 → `gate_after`），HEAD 实测 `gate_unrelated_first = Approve` 且该门**未被上浮**（钩子零调用），`gate_own_second = Deny`。该变体不在 C4 所指的复现帧序内（R1 报告的复现含 `gate_1` 先行），故不影响本项判定，但如实登记。
- **C5（非审批 elicitation 不变）：pass**
  自建 fake + 自建探针（钩子**恒返回 `'allow'`**，以排除「钩子没给值」的解释）：`ask_dialog`（`properties.q0.oneOf`）→ `{"action":"decline"}`；`confirm_boolean`（`properties.value.type='boolean'`）→ `{"action":"decline"}`；`hookCalls=[]`（由形状决定而非钩子取值）；轮次 `end_turn`。套件 `M4/C5` 用例同结论且绿。
- **C6（`bash` 等 `bEs` 工具双向不回归）：pass**
  放行侧（真实 omp，argv 见 A5①）：MITM 帧序 `request_permission → allow_once → tool_call pending → elicitation 'Allow tool: bash' → Approve → tool_call in_progress（rawOutput `R3-BASH-ALLOW\n`）→ completed`；模型侧原文含 `R3-BASH-ALLOW` + `Wall time: 0.03 seconds`；副作用文件存在（`R3-BASH-ALLOW\n`）；全程无 `session/cancel`。
  拒绝侧：产品路径 `permission_denied` + 文件不存在；裸 ACP 驱动模型侧 `Tool call rejected by user (bash)` + 文件不存在。**双向均成立。**

### D. 本轮变更面重点复核（主 agent 指定）

- **D1（重叠挂起期的冻结语义）：pass**
  自建脚本化 fake + 自建帧级探针，`timeoutMs=300`，三个帧序：

  | 场景（帧序） | 挂起期（> `timeoutMs`） | 先结算其一后（再 > `timeoutMs`） | 全部结算后 |
  |---|---|---|---|
  | **D1a 两道权限门**（P1/`call_X` + P2/`call_Y` 不等应答连发） | 607 ms：`session/cancel`=0、`dead=false`、`prompt()` 未结算、应答帧=0 | 1208 ms：`cancel`=0、`dead=false`、未结算（仅 P1 应答帧 1 条） | `end_turn`（总 1209 ms）；仅 305 ms 时两门齐发，P1 应答落在 **911 ms**、P2 应答落在 **1512 ms** |
  | **D1b 权限门 + 工具审批门**（P1 + elicitation('Allow tool: write') 不等应答连发） | 606 ms：同上（`cancel`=0、`dead=false`、未结算、0 应答） | 1207 ms：`cancel`=0、未结算 | `end_turn`（总 1208 ms） |
  | **D1c 两者同时挂起后先后结算、该轮不结算**（剩余预算对照） | — | 250 ms 处先结算其一：`settled=false`、`cancel`=0 | 559 ms 处以 `code='timeout'` 结算 + 1 次 `session/cancel`（≈250 挂起不计预算 + ≈300 剩余预算） |

  帧时间线原文（D1a：`/tmp/r3verify/out/ov_perm_perm-1-frames.jsonl`，`at` 相对首帧）：
  `305 in prompt` / `305 out P1 session/request_permission toolCallId=call_X` / `305 out P2 session/request_permission toolCallId=call_Y` / `911 in server_request_reply P1 {"outcome":{"outcome":"selected","optionId":"allow_once"}}` / `1512 in server_request_reply P2 {…}` / `1513 out prompt_result end_turn` —— **全程无 `SESSION_CANCEL` 帧**。
  **修复前对照（同一探针、同一 fake、源 = `1f11b59`）**：D1a/D1b 在「先结算其一」后 **`settled=true`、`cancels=1`、`code='timeout'`（≈900 ms，即 600 挂起 + 300 剩余）**，而另一等待仍未裁决 ⇒ 偏差 #3 的失效形态确定性复现，在 HEAD 上消失。四个判据（无 `session/cancel`、`dead=false`、未以 `timeout` 结算、先结算方不解冻）全部由本次实测支撑。

- **D2（拒绝分支清凭据）：pass**
  自建帧序（`reject_clear`）：`permission(P_A/call_A)` → 钩子 `'allow'` → `permission(P_B/call_B)` → 钩子 `{optionId:'reject_once'}` → 与 A 无关的同型审批门 `G_unrelated`（`Allow tool: write`）。钩子对审批门恒返回 `'deny'`（⇒ 若凭据残留，该门会被静默 `Approve` 且钩子零调用）。

  | 源 | `G_unrelated` 应答 | 该门是否上浮（钩子调用） | 轮次 |
  |---|---|---|---|
  | HEAD `dd7badd` | **`{"action":"accept","content":{"value":"Deny"}}`** | **是**（钩子入参 `kind:'tool_approval', toolName:'write'`） | `permission_denied`（拒绝档三步） |
  | `1f11b59`（第 2 轮，偏差 #2 形态） | **`{"action":"accept","content":{"value":"Approve"}}` ← A 的残留凭据** | **否**（钩子仅 `call_A`/`call_B` 两次调用） | `permission_denied` |
  | `dd59d31`（首轮，单槽凭据） | `Deny` | 否（单槽在拒绝时被 `null` 清空） | `permission_denied` |

  ⇒ HEAD 与首轮在「拒绝即作废既有凭据」语义上一致，第 2 轮引入的残留窗口**已闭合**；拒绝三步（回 `reject_once` → `session/cancel` → `permission_denied`）由帧序确认未变。

- **D3（跨轮无残留）：partial** —— 三个子项中两项通过、一项不通过：
  - **D3-1（上一轮挂起后收尾 → 再起一轮无挂起：计时器行为正常、无提前 cancel）：pass** — 自建 probe：轮 1（挂起 150 ms 后裁决）= `end_turn`；轮 2（服务端不结算、无挂起）在 150 ms 检查点 `settled=false`、`cancel` 增量=0；随后在 **301 ms** 处以 `timeout` 结算，且出现 1 次 `session/cancel`（既不提前、也不被吞）。
  - **D3-2（凭据不跨轮残留）：pass** — 轮 1 放行 `call_C1` 但**未观测到终态**即收尾（`end_turn`）；轮 2 的无关审批门 `G_round2` 实测 `Deny` 且钩子被调用（`kind:'tool_approval', toolName:'write'`）⇒ 轮末 `finally` 的凭据清空生效（`acp-client.js:238`）。
  - **D3-3（上一轮遗留的暂停态不得影响下一轮）：fail** — 构造「轮 1 在挂起未决时被服务端结算」（轮 1 的钩子 Promise 永不结算，轮次仍 `end_turn`，进程存活）后，轮 2 的挂起在 200 ms 后裁决：观测窗 1408 ms 内 `settled=false`、轮 2 内 `session/cancel`=**0**（预期应在 ≈200+300 ms 处以 `timeout` 结算 + cancel）。机制（读码 + 实测对照）：`_pausedTurns` 只在 `_askHook` 的 `finally` 里递减，轮次在挂起未决时收尾不会把它归零 ⇒ 轮 2 的 `_pauseTurnTimer` 把深度推到 2，`_resumeTurnTimer` 递减到 1 后直接 `return`，**计时器不再装回** ⇒ 该轮失去超时上界。同一帧序在 `1f11b59` 上结果相同（**非本轮引入**）；真实可达性本轮未能建立（需要「轮次在审批门未决时结算」，而真实 omp 的 tool call 会等待裁决；`kill()/dispose()` 路径下客户端已 `dead` ⇒ 无法再起一轮）。

### E. 回归与内务

- **E1 `node --test oamp/test/tool-permission.test.js`：pass（含 flake 归因）** — 规范运行 `1..24 / # tests 24 / # pass 24 / # fail 0`（`/tmp/r3verify/reg-toolperm-pass.txt`）。
  但本报告在一次全文件运行中观测到 **1 例失败**：`not ok 5 - F05-3/F05-4：拒绝档回 reject_once + 立即 session/cancel + 轮次 permission_denied（会话保留）`，`error: '拒绝后必须立即发出 session/cancel'`（`tool-permission.test.js:447`，`/tmp/r3verify/reg-toolperm-flake.txt`）。**归因（未改动对照树 + 隔离复跑 + 机理）**：
  (i) **对照树**：`/tmp/r3verify/flake-pre/`（`src/acp-client.js` = `1f11b59` 源，md5 `70fb06064b3f20265285eaca30e10a9f`；`test/tool-permission.test.js` = `1f11b59` 测试文件；失败用例块与 HEAD **字节相同**）——全文件运行 **11 次 0 失败**。
  (ii) **隔离复跑**：HEAD 全文件 **14 次 / 1 失败**；HEAD 单用例过滤（`--test-name-pattern='F05-3'`）**30 次 0 失败**；PRE 侧同一过滤 **30 次 0 失败**；全量套件（E3）**1 次 296/296**。
  (iii) **机理（源无关）**：该用例在 `prompt()` 拒绝后**立即**读 fake 帧日志断言其中已有 `session/cancel`，而自建探针实测两侧帧序**完全同形**——fake 先写 `prompt_result`（解除 `prompt()` 阻塞），**其后**才收到/落盘客户端的 `SESSION_CANCEL`（HEAD：`304 in server_request_reply → 304 out prompt_result → 304 in SESSION_CANCEL`；PRE：`305 / 305 / 305`；`/tmp/r3verify/out2/single_perm-7-frames.jsonl` 与 `/tmp/r3verify/out2-pre/single_perm-2-frames.jsonl`）⇒ 断言与 fake 落盘存在**观测时序竞争**。
  (iv) 本轮改动面不可能改变该窗口：拒绝分支的两处写（`_respond(reject_once)` → `cancel()`）顺序未变，R3 新增的 `_approvalGrants.length = 0` 在 `cancel()` **之后**执行，且该路径无挂起（无钩子 ⇒ 不触碰 `_pauseTurnTimer`）。⇒ 判定为**既有测试的观测竞争（非本轮引入）**，记入偏差 #6。
- **E2 `node --test oamp/test/acp-daemon.test.js`：pass** — `1..7 / # tests 7 / # pass 7 / # fail 0`。
- **E3 `node --test oamp/test/*.test.js`（oamp 全量）：pass** — `1..296 / # tests 296 / # pass 296 / # fail 0`（单次运行即全绿，未触发 E1 的观测竞争；该测试文件作为子进程参与本次全量运行）。
- **E4 既有断言逐字未改：pass** — 块级字节比对（`1f11b59`→HEAD 的 `tool-permission.test.js`）：22 个既有块**全部 IDENTICAL**、新增 2 块、**0 个既有块被改动**；两条同步 `'allow'`/`'deny'` 用例 1538 B / 1223 B 逐字相同；一次性 `yolo` 断言所在的 `acp-daemon.test.js` 整文件 md5 跨轮相同。
- **E5 本轮改动文件范围：pass** — `git diff --name-only 1f11b59...HEAD` 仅 `oamp/src/acp-client.js` 与 `oamp/test/tool-permission.test.js`（`--shortstat`：2 files changed, 109 insertions(+), 5 deletions(-)）；不含 `oamp/src/agent.js`、`oamp/test/acp-daemon.test.js`、`oamp/web/**`。全量面（`27eef14...HEAD`）恰为 PR 文件声明的 3 个文件。
- **E6 仓库根内务：pass** — `/Users/chenchiyuan/projects/agents`：`git status --porcelain -uall` 输出为空；被验 worktree：`git status --porcelain -uall` 输出为空。
- **E7 被验 worktree 只读纪律：pass** — 全程未 `commit`/`checkout`/`stash`/`worktree remove`；所有探针、对照源、帧日志、副作用文件均在 `/tmp/r3verify`；本报告是唯一写入，且落在迭代工作区 `clarifications/`。

---

## 汇总

- pass: **45 项**
- fail: **0 项**
- partial: **1 项**（D3——三个子项中「上一轮遗留的暂停态不得影响下一轮」不通过）
- blocked: **0 项**

分组：A 6/6 pass；B 24/24（T1×3、T2×5、T3×3、T4×3、T5×5、T6×5）；C 6/6 pass；D 3 项（2 pass / 1 partial）；E 7/7（3 组回归 + 4 项内务）。

**本轮变更面（主 agent 指定的 D1~D3）闭合情况**：**D1 闭合**（重叠挂起深度冻结，两种重叠形态 + 先结算方不解冻 + 剩余预算续计，四判据全部实测；修复前对照确定性复现）、**D2 闭合**（拒绝即作废全部既有凭据；`1f11b59` 的残留 `Approve` 消失，`dd59d31` 语义恢复）、**D3 未完全闭合**（D3-1/D3-2 通过；D3-3「跨轮残留暂停态」在 fake 帧序下不通过，见偏差 #1）。

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| **1（中，本轮新发现）跨轮残留暂停态吞掉下一轮的超时上界** | D3「跨轮无残留——上一轮遗留的暂停态…不得影响下一轮」 | `_pausedTurns` 仅在 `_askHook` 的 `finally` 递减（`acp-client.js:527-535`），轮次在挂起未决时收尾不会归零：轮 2 若再有挂起，`_pauseTurnTimer` 把深度推到 2，`_resumeTurnTimer` 递减到 1 后 `return`（`:585-593`）⇒ 计时器不再装回，该轮**永不超时**。自建 fake 实测：轮 2 挂起 200 ms 结算后 1408 ms 观察窗内 `settled=false`、`cancel`=0（`/tmp/r3verify/out2/round_residual-*.json`）；同帧序在 `1f11b59` 上同样失败 ⇒ **非本轮引入**。真实可达性未建立（需「轮次在门未决时结算」；`kill/dispose` 路径下客户端已 `dead`，无法再起一轮） | 开下一迭代处理：轮次边界（`prompt()` 的 `finally` 或 `_request` 结算）重置冻结深度/`paused` 态，并补一条 fake 层用例固定「轮末仍有未结算挂起 → 下一轮挂起仍受剩余预算约束」 |
| **2（低，既有）`kill()`/stdin end 后写帧触发未捕获 stream error** | 无显式规格；`_write` 的守卫为 `!this.child.stdin.destroyed` | `kill()` 会 `stdin.end()` 但 `destroyed` 仍为 false、`this.dead` 尚未置位 ⇒ 此后到达的裁决仍会 `_respond` 写入，触发异步 `'error'`（`ERR_STREAM_WRITE_AFTER_END`）——本次探针在「裁决与 `kill()` 相邻」时**两次终止了探针进程**（`acp-client-pre.js:330` / `acp-client.js:333` 同一行为）。可达路径：`context-pool` 在挂起期间 `dispose()` 后仍有迟到裁决到达 | 属 `acp-client.js` 文件面但与本轮改动面无关（pre/HEAD 同形）；建议开下一迭代评估「`kill()` 置 `dead`/关闭写通道」或为 stdio 挂 `'error'` 处理 |
| **3（中，承接主 agent 已登记的偏差 #2）任务图 T5.2 判据前提与代码事实相反** | `pr-001-tasks.md` T5.2 判据「记录实际 argv 逐字，**其中无 `--no-session`**」（前提：daemon 常驻路径不带该 flag） | `acp-client.js:140` 无条件 `args.push('--no-session')`；本次 13 次真实运行记录的 daemon 路径 argv 逐字**全部含** `--no-session`（`["acp","--no-skills","--no-rules","--no-session","--approval-mode","always-ask"]`）。`pr-001-m4-evidence.md` §1 已如实并列两侧并显式指出该前提错误 | 维持主 agent 既定处置：阶段 6 汇总时回填 `architecture.md` §9.4.3 的 D3 行前提（「D3 不成立」的结论本身正确）；并把「证据落盘路径 + 判据前提须与代码事实一致」写入任务图规范。**不阻塞** pr-001 |
| **4（低）T5.1 的 D1 方向判定未显式成行** | T5.1 要求「按 D3 → D1 → D2 给单一结论，每条方向给出『成立 + 观测证据』或『不成立 + 排除证据』」 | `pr-001-m4-evidence.md` 中 D3（§1 结论表）与 D2（§3「四档逐档相同 ⇒ D2 不成立」）有标签化结论；D1 方向由 §2.2 的应答帧原文 + §4⑧ 的 `SEs.get(T.optionId)` 校验链**隐含排除**，但无「D1：不成立」字样 | 下一迭代补写为显式结论行（或在该文件的结论表增一行），便于后续复核快速对位 |
| **5（低，承接 R2 报告的偏差 #1）凭据与工具身份无绑定 ⇒ 帧级仍存在「无人工裁决的 `Approve`」窗口** | C2「判定必须基于**该次放行的显式关联**」 | 凭据为「toolCallId FIFO + 先到先抵扣」，抵扣时不校验门属于哪个工具。本次独立实测（HEAD，自建 fake）：`permission_A→allow` → **先**来无关同型门 `gate_unrelated_first` ⇒ 应答 **`Approve` 且钩子零调用**；其后 call_A 自身的门反而走上浮（`Deny`）。wire 上无身份可依（真实帧实测 `session/request_permission.params.toolCall` **不含 `toolName`**、`elicitation/create` 不含 `toolCallId`）。本次**未复核**真实 omp 的轮内工具串行性（R2 曾以 `concurrent-slow` 取证），故该残余在真实路径的可达性本轮未独立确认 | 若需身份级保证需上游协议改动或产品取舍（每个门都上浮 vs 免重复提问），超出 pr-001 文件范围；建议随下一迭代的凭据模型统一处理，并独立复现「轮内工具串行性」后入库 |
| **6（低，既有测试的观测竞争）`F05-3/F05-4` 用例在 `prompt()` 拒绝后立即断言帧日志** | 无显式规格（测试健壮性） | 该用例（块与 `1f11b59` **字节相同**）在 `prompt()` 以 `permission_denied` 拒绝后立刻读 fake 帧日志断言其中已有 `session/cancel`；而 fake 先写 `prompt_result`（解除阻塞）、**其后**才收到并落盘客户端的 `SESSION_CANCEL`（两侧帧序同形，见 E1(iii)）⇒ 存在观测竞争。本报告实测：HEAD 全文件 14 次中 **1 次失败**（`not ok 5`，断言 `tool-permission.test.js:447`），未改动对照树 11 次 0 失败、两侧单用例过滤各 30 次 0 失败 | 属既有测试的健壮性问题，不在本轮改动面；建议下一迭代把该断言改为「等待帧到达」（`waitFor`）而非立即读文件；同时说明「全量套件 296/296」与「单文件偶发」并不矛盾 |

---

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制。不在当前验收标准范围内，供主 agent 与 planner 决策。

- **轮次边界的冻结深度重置（与偏差 #1 同源）**：修复点在 `acp-client.js` 的 `prompt()` 收尾 / `_request` 结算处；建议同时补一条 fake 层用例（「轮末仍有未结算挂起 → 下一轮挂起仍受剩余预算约束」）。
- **`kill()`/dispose 后的写通道治理（与偏差 #2 同源）**：`_write` 的守卫以 `stdin.destroyed` 判活，与「已 `end()` 但未 destroy」的现实不符；评估置 `dead` 或挂 stdio `'error'` 处理。
- **审批门 ↔ toolCall 的身份绑定（与偏差 #5 同源）**：需要上游协议改动（让 `elicitation/create` 携带 `toolCallId`）或产品侧取舍；同时建议把「轮内工具串行执行」这一事实独立复现并写入 architecture，避免后续迭代重复论证并发场景。
- **`permission='deny'` 档的模型侧可见性（首轮/R2 候选，未变化）**：拒绝语义「回包后立即 `session/cancel`」使模型看不到拒绝原因（本次仍需借裸 ACP 驱动观测 `Tool call rejected by user (bash)`）；若 F04 需要「模型知道为什么被拒」，需要新的裁决—回传时序设计。
- **`ask` 等非审批 elicitation 的处置面（首轮/R2 候选，未变化）**：当前恒 `decline`（本次已由自建探针在两种非审批形状上复核），是否纳入上浮面仍是未决产品问题。
- **任务图的证据类判据显式化（承接偏差 #3/#4）**：把「证据落盘路径」「每条根因方向的显式结论行」「判据前提须与代码事实一致」写成任务图硬判据。
- **既有用例的观测竞争治理（与偏差 #6 同源）**：`F05-3/F05-4` 的「立即读帧日志」断言应改为等待帧到达；凡「先断言进程外事实」的同类用例建议统一 `waitFor` 形态，避免单文件运行偶发红。

---

## 结论

**PASS**（45 项 pass / 1 项 partial / 0 fail / 0 blocked）

支撑要点：本轮变更面两处修复均由**本次自建证据**独立确认——(i) **重叠挂起冻结**：两种重叠帧序（权限门×2、权限门+审批门）在挂起 > 3×`timeoutMs` 期间零 `session/cancel`、`dead=false`、`prompt()` 未以 `timeout` 结算，先结算其一方后仍保持冻结，全部结算后正常结算/按剩余预算到点；同一探针在同一 fake 下对 `1f11b59` 确定性复现失效（先结算方解冻 ⇒ ~900 ms 超时），构成因果对照。(ii) **拒绝即清凭据**：`放行 A → 拒绝 B → 无关同型门` 帧序下 HEAD 答 `Deny` 且该门上浮（钩子被调用），`1f11b59` 则为 `Approve` 且钩子零调用，`dd59d31` 亦为 `Deny` ⇒ 第 2 轮引入的残留窗口闭合、首轮语义恢复。前两轮已通过项未回归：挂起不回包/`optionId` 回显/非法回落/单次挂起冻结与剩余预算/同步两条用例逐字/一次性 `yolo` 断言逐字/`M4/C2+C4` 窗口修复/C5 非审批 decline，均由本次自建帧级探针或自建真实 omp 探针独立复现；M4 硬义务在真实 `omp` 18.0.11 上**双向**成立（bash 放行 ⇒ 模型侧真实输出 + 副作用文件；bash 拒绝 ⇒ `permission_denied` + 文件不存在，裸 ACP 驱动侧模型文字 `Tool call rejected by user (bash)`；write 放行 ⇒ `Successfully wrote 14 bytes` + 文件；write 拒绝 ⇒ `Tool call denied by user: write` + 文件不存在），且全程未以审计行作判据；三组回归规范运行 24/24、7/7、296/296（全量单次全绿；单文件曾 1 次触发既有用例的观测竞争，已归因并隔离，见 E1），改动面恰 2 文件，仓库根与被验 worktree 均干净。

需主 agent 决策（**本报告不构成合并许可**）：**D3-3 / 偏差 #1**（跨轮残留 `_pausedTurns` 使下一轮失去超时上界）是本轮**未闭合**的唯一子项——它在 `1f11b59` 上同样存在（非本轮引入），且本轮未能建立其在真实 omp 上的可达性（需「轮次在门未决时结算」）。按 verifier 契约「偏差被显式记录即满足交付条件」处理；若合并门槛含「跨轮不得残留任何影响下一轮的暂停态」，则该子项的判定权在主 agent，不在本报告。偏差 #2（`kill()` 后写帧崩溃面）与 #5（凭据无身份绑定）同为既有/承接项，记录即满足交付条件。
