# pr-001 独立验证报告（agent 侧权限挂起链路 + ACP 答复链路修复 / M4）

验证者身份：**ACP/JSON-RPC 协议集成的同级代码审查者（具备 omp dist 反查能力）+ 真实运行时验证者**——判断依据全部来自产出物本身与主 agent 指定的验证标准；未接收、未使用任何执行过程上下文（产出物内的自评文字、commit message 的结论句一律不作为判定依据，只作为「被核查对象」）。
产出物：
1. worktree `.pb-agents/worktrees/0021-pr-001-permission-suspend-and-reply-fix`，分支 `feat/0021-pr-001-permission-suspend-and-reply-fix`，HEAD `dd59d314313083b4a2a6bb0e6d7af5fd29c8f7d6`（三点 diff 基 `27eef14`；3 文件 / +346 −34）
2. `docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-001-agent-permission-suspend-and-reply-fix.md`
3. `docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-001-tasks.md`
4. 背景规格（只作对照）：`architecture.md`、`prd/F02,F04,F05,F12*.md`、`clarifications/arch-round-1-verdicts.md`
验证标准来源：主 agent 委托的 A（PR 验收 6 条）+ B（T1~T6 全部验收标准）+ C（C1~C8 硬性核查）+ D（3 组回归）。
验证日期：2026-09-13

**环境**：真实 `omp` 18.0.11（`/Users/chenchiyuan/.bun/bin/omp` → `@oh-my-pi/pi-coding-agent/dist/cli.js`，19,803,745 B）；node v22.15.0；实测模型 `deepseek/deepseek-v4-flash`。
**本次使用的临时探针（全部在 `/tmp`，未提交任何仓库）**：

| 文件 | 用途 |
|---|---|
| `/tmp/pr001-verify/omp-wrap.sh` | temp wrapper：记录 argv 后 exec 真实 omp；`PR001_STRIP_NO_SESSION=1` 时丢弃 `--no-session` |
| `/tmp/pr001-verify/probe-acpclient.mjs` | 用**本 PR 的 `AcpClient`** 起真实 omp（allow / deny / ask / write 四场景） |
| `/tmp/pr001-verify/probe-raw.mjs` | 裸 ACP 驱动（不经 `AcpClient`），隔离 `cancel()` 副作用观察「拒绝」的模型侧结果 |
| `/tmp/pr001-verify/probe-tools.mjs` | clientCapabilities 与 approval-mode 的 A/B 实测 |
| `/tmp/pr001-verify/probe-frames.mjs` + `fake-acp2.mjs` | 自建帧级 fake ACP 服务端（独立于 PR 自带用例） |
| `/tmp/pr001-verify/probe-credential.mjs` + `fake-acp.mjs` | 凭据 `_approvedToolCallId` 覆盖面确定性复现 |

---

## 逐项判定

### A. PR 文件「验收标准」6 条

- **A1（`tool-permission.test.js` 全绿；同步 `'allow'`/`'deny'` 两条既有用例逐字保留；deny 档三步不变）：pass**
  证据①：`node --test oamp/test/tool-permission.test.js` → `1..18 / # pass 18 / # fail 0`（输出 `/tmp/reg-toolperm.txt`）。
  证据②：字节比对（脚本逐行提取 base `27eef14` 与 head 的同名用例块）：`F05-2/§11.2：允许档恒回 allow_once…` base:252-286 ≡ head:292-326 **IDENTICAL**；`F05-3/F05-4：拒绝档回 reject_once + 立即 session/cancel + 轮次 permission_denied…` base:315-345 ≡ head:355-385 **IDENTICAL**。两条用例的断言文本（含 `{outcome:{outcome:'selected',optionId:'allow_once'}}` / `'reject_once'` / 审计恰一行 / `permission_denied`）零改动。

- **A2（未结算 Promise 期间不回包；结算后 `optionId` = 钩子选定值）：pass**
  证据（自建帧级探针，非 PR 自带用例）：`node /tmp/pr001-verify/probe-frames.mjs` 场景 `hang_and_echo`（客户端 = 本 PR 的 `oamp/src/acp-client.js`，服务端 = `/tmp/pr001-verify/fake-acp2.mjs`）：
  `repliesBeforeSettle: 0`；释放后 `replyCountAfterSettle: 1`，帧原文 `{"outcome":{"outcome":"selected","optionId":"allow_always"}}`；服务端帧日志 `prompt_received at=305ms → server_request_reply at=607ms`（应答发生在 Promise 结算之后），`turn_settled at=607ms`。回显值为钩子显式选定值，非 `allow_once`。

- **A3（非法 `optionId` 回落并记审计；fake 侧不出现「未知 option ID」类错误）：pass**
  证据（同探针）：`illegal_allow`（`{optionId:'allow_everything_forever'}`）→ 帧 `optionId='allow_once'`、`error=null`、轮次 `end_turn` 正常结算；`illegal_reject`（`{optionId:'reject_forever'}`）→ 帧 `optionId='reject_once'` 且 `prompt()` 抛 `permission_denied`。全部应答值 ∈ 请求方 `options` 集合。
  旁证（omp 侧硬约束真实存在）：`cli.js` 偏移 15551xxx `let R=SEs.get(T.optionId); if(!R) throw new X(\`Tool permission response used unknown option ID: ${T.optionId}\`)`。

- **A4（挂起期间冻结轮次计时；裁决后按剩余时间恢复并正常结算）：pass**
  证据（同探针）：`frozen_timer`（`timeoutMs=500`）——挂起至 1400ms 时 `cancelDuringHang=false`、`deadDuringHang=false`（若冻结失效，计时器早在 ~500ms 处触发 `cancel`）；释放后轮次 `end_turn` 正常结算（`elapsedMs=1604`，服务端 `server_request_reply at=1704` → `turn_settled_after_hold at=1907`）。
  对照（超时语义未被改写）：`timeout_control`（无挂起、轮次不结算）→ `errorCode='timeout'`（elapsed 1103ms）、服务端观测到 `session_cancel_received at=705ms`。

- **A5（`tools=true` 且 `allow` ⇒ argv `always-ask`；`deny`/`tools=off` 形状不变；一次性路径仍 `yolo`）：pass**
  证据①（真实 argv 逐字，由 wrapper 记录 `/tmp/pr001-verify/argv.log`）：`acp` `--no-skills` `--no-rules` `--no-session` `--approval-mode` `always-ask`。
  证据②：`tool-permission.test.js` 中 `assert.equal(allowArgv[allowIdx+1], 'always-ask', …)`；`acp-daemon.test.js:532` 断言已同步为 `always-ask`（diff hunk 唯一改动行）。
  证据③：`acp-daemon.test.js:554` 一次性路径 `assert.equal(devOneShot[…], 'yolo', …)` base 与 head **逐字相同**（两侧 550-558 行原文比对一致）。`tools=false`/匿名实例「不含 `--approval-mode`」的既有断言在 diff 中未变。

- **A6（M4 硬义务：真实 omp 双向；审计行不作判据）：pass** ——详见 C1（本报告以独立复跑为准，未引用 PR 内任何叙述）。

### B. `prs/pr-001-tasks.md` 的 T1~T6

**T1（argv 改档 + 两处断言同步）**
- T1.1（两档均 `always-ask`；`tools=false`/匿名不含）：**pass** — 证据同 A5①②；`tool-permission.test.js` 中「tools=false ⇒ 不追加」「匿名 ⇒ 不追加」断言在 head 中原样保留（块比对只差标题行与 allow 档断言值）。
- T1.2（常驻路径断言同步 `always-ask`）：**pass** — `git diff 27eef14...HEAD -- oamp/test/acp-daemon.test.js` 仅 1 hunk（行 532）；`node --test oamp/test/acp-daemon.test.js` → `# pass 7 / # fail 0`。
- T1.3（一次性路径零改动；`agent.js` 不在 diff）：**pass** — `git diff --name-only 27eef14...HEAD` 不含 `oamp/src/agent.js`；`:554` 逐字保持 `yolo`。

**T2（钩子返回域扩展）**
- T2.1（挂起期不回包，帧级）：**pass** — 见 A2。
- T2.2（结算后回显钩子 `optionId`）：**pass** — 见 A2（`allow_always` 原样回显）。
- T2.3（同步 `'allow'`/`'deny'` 两条既有路径逐字不变）：**pass** — 见 A1 的字节比对。
- T2.4（审计行在裁决后写恰一行、`option` = 实际回显值）：**pass** — 证据：`acp-client.js:456-459` 在 `this._respond(...)` 之后才 `this._audit('TOOL_APPROVED', toolCall, optionId)`；`tool-permission.test.js` head:556-597 的用例断言 `approved[0].fields.option === 'allow_always'` 且套件 18/18 通过；自建探针的帧回显值与之一致。
- T2.5（JSDoc 返回域改写，优先级语义保留）：**pass** — diff 第 74 行：`⇒ 'allow'|'deny'|{optionId}|Promise<…>；给了则优先于 permission`。

**T3（`optionId` 校验与回落）**
- T3.1（非法 → 回落 + 审计 = 回落值）：**pass** — 见 A3。
- T3.2（应答值恒合法，轮次不崩）：**pass** — `illegal_allow` 轮次 `end_turn`；两个场景服务端均未记录任何 error 帧。
- T3.3（合法 `{optionId}` 逐字回显）：**pass** — `hang_and_echo` 帧原文即证据。

**T4（冻结轮次计时）**
- T4.1（挂起 > `timeoutMs` 未被 cancel/kill）：**pass** — 见 A4。
- T4.2（裁决后按剩余时间恢复并正常结算；超时路径仍能 cancel→kill）：**pass** — `frozen_timer` 结算为 `end_turn`；`timeout_control` 观测到 `session/cancel`。
- T4.3（冻结只作用于挂起路径）：**pass** — 同步 `'allow'`/`'deny'` 路径（A1 用例）与响应回落路径（A3）计时行为不变；`_pauseTurnTimer` 只在钩子返回 thenable 时调用（`acp-client.js:477`）。

**T5（M4 根因定位，产出可复跑证据）**
- T5.1（按 D3→D1→D2 给单一结论，每条含成立/不成立的观测证据）：**partial** — 结论完整且方向正确（D1/D2/D3 三者「不成立」+ 真实根因为第二道审批门），但只以结论句出现在 commit `dd59d31` 的 body；D2 的观测证据没有逐档记录，D3 的 argv 证据缺失（见 T5.2）。
- T5.2（D3 步以 daemon 真实 argv 复跑并「记录实际 argv 逐字，其中无 `--no-session`」）：**partial** — (i) 判据要求的 argv 列表在**全部 pr-001 产出物中不存在**（worktree 仅 3 个改动文件，无证据落盘）；(ii) 该判据的前提与代码事实相反：`acp-client.js:126` 无条件 `args.push('--no-session')`，实测 daemon 路径 argv 逐字含 `--no-session`（见 C6），故按字面**不可复现**；(iii) 其意图（排除探针 argv 干扰）成立，且本次独立复跑确认「带 / 不带 `--no-session`」两侧结果一致（见 C1）。
- T5.3（D1 证据 = 帧逐字比对 + omp 读取路径结论）：**pass** — commit body 逐字给出修复前应答帧 `{outcome:{outcome:'selected',optionId:'allow_once'}}` 并与 omp 读取路径（`result.outcome` → `SEs.get(optionId)`）作了比对；omp 侧读取路径本次由 dist 反查复核一致（`cli.js` 偏移 15551xxx）。
- T5.4（D2 证据 = 0/1ms/100ms/1s 四档模型侧结果对照表）：**partial** — 结论句「delays 0/1/100/1000ms 四档结果不变」在 commit body 中，但**对照表本身不在任何产出物中**；本次亦未复跑四档（D2 已被根因定位取代，非本轮验收判据）。
- T5.5（结论不以审计行为证据）：**pass** — 结论所引证据为模型侧结果与原始帧；`TOOL_APPROVED`/`TOOL_DENIED` 未被用作任何结论依据。

**T6（M4 修复落地 + 双向真实 omp 验证）**
- T6.1（放行路径模型侧 = 工具真实输出）：**pass** — 见 C1①。
- T6.2（拒绝路径模型侧确实 denied）：**pass** — 见 C1②。
- T6.3（双向同时成立才算通过）：**pass** — 两侧均在本报告独立复跑成立。
- T6.4（不回归 fake 层：两个测试文件全绿）：**pass** — `tool-permission` 18/18，`acp-daemon` 7/7。
- T6.5（oamp 全量 `node --test oamp/test/*.test.js` 全绿）：**pass** — 第 1 次运行 289/290（唯一失败 `project-workspace.test.js:1166` 为 `EADDRINUSE: 127.0.0.1:41684`，并行测试端口冲突）；第 2 次全量运行 **290/290 pass / 0 fail**（`/tmp/reg-oamp-all2.txt`），且该文件隔离复跑 21/21 全绿 ⇒ 判定为环境性冲突，不归因本 PR。

### C. 硬性核查项

- **C1（M4 双向真实 omp 复跑；审计行不作判据）：pass**
  **使用的 argv 逐字**（wrapper `/tmp/pr001-verify/omp-wrap.sh` 记录）：`acp`、`--no-skills`、`--no-rules`、`--no-session`、`--approval-mode`、`always-ask`（= `AcpClient.start()` 对 `tools=true` 的**真实 daemon argv**）。
  ① **放行侧**（`onPermissionRequest → 'allow'`；探针 `/tmp/pr001-verify/probe-acpclient.mjs`，输出 `/tmp/pr001-verify/out-allow.json`）：
  - 模型侧结果原文（节选）：`命令已真实执行（\`bash\` 工具），以下是收到的**原始输出**，逐字粘贴：\n\n```\nPR001-PROBE\n\n\nWall time: 0.03 seconds\n```` —— **含工具真实输出、不含 `denied by user`**。
  - 副作用文件核对：`/tmp/pr001-verify/side-effect-allow.txt` **存在**，内容 `PR001-PROBE` ⇒ 工具确实执行。
  - 帧面：服务端先发 `session/request_permission`（options = `[allow_once, allow_always, reject_once, reject_always]`），客户端回 `{outcome:{outcome:'selected',optionId:'allow_once'}}`；随后 omp 发**第二道门** `elicitation/create`（`requestedSchema.properties.value.enum = ["Approve","Deny"]`），客户端回 `{action:'accept', content:{value:'Approve'}}`。
  ①'（`--no-session` 对照，排除 D3 干扰）：同一探针在 wrapper 丢弃 `--no-session` 后复跑 → 模型侧同样返回 `PR001-PROBE`，副作用文件 `side-effect-allow-strip-strip.txt` **存在**、内容 `PR001-PROBE`。**两种 argv 结果一致 ⇒ D3（探针 argv 产物）不成立。**
  ② **拒绝侧：两条独立证据同时成立**
  - (a) 产品路径（`AcpClient`，`onPermissionRequest → 'deny'`，输出 `/tmp/pr001-verify/out-deny.json`）：`prompt()` 以 `code='permission_denied'` 结算；服务端只收到 1 条 `session/request_permission`（未进入第二道门）；客户端回 `reject_once`；副作用文件 `side-effect-deny.txt` **不存在**。**模型侧文字不可观测**——产品拒绝语义是「回包后立即 `session/cancel`」，轮次在模型产出前即被取消（如实记录，不代以审计行）。
  - (b) 裸 ACP 驱动（`/tmp/pr001-verify/probe-raw.mjs`，**不发 `cancel`**，其余应答帧逐字相同）：模型侧原文 `对第一次调用（执行你给的命令）：\n```\nTool call rejected by user (bash)\n```\n对第二次调用…\n```\nTool call rejected by user (bash)\n````，`tool_call_update status=failed`，副作用文件 `side-effect-raw-reject.txt` **不存在** ⇒ 模型侧确实得到 denied 且工具未执行。
  - (c) 产品路径上的「模型侧 denied」正面观测（**额外发现，见偏差 #1**）：以 `write` 为目标工具时，omp **不发** `session/request_permission`（`hook: []`）、只发第二道门；本 PR 客户端因无放行凭据答 `{action:'accept',content:{value:'Deny'}}`，模型侧原文 `Tool call denied by user: write`，轮次正常 `end_turn`，副作用文件未创建。
  - 审计行（`TOOL_APPROVED`/`TOOL_DENIED`）在上述全部判定中**未被使用**。
  - 根因链条与 omp dist 逐字吻合：审批门 `let P; try{P=await A.select(I,["Approve","Deny"])}catch…; let F=P==="Approve"; … if(!F) throw Error(\`Tool call denied by user: ${this.tool.name}\`)`（`cli.js` 偏移 14046783 附近）——原 M4 症状 `Tool call denied by user: bash` 由该门产生，而非权限门（权限门的拒绝文案是 `Tool call rejected by user (…)`，偏移 15551300 附近）。第二道门的能力门控 `let s=n?.elicitation?.form!=null; … select:async(o,r,i)=>{if(!s)return; …}`（偏移 18049221 附近）⇒ 未声明 `elicitation.form` 时 `select` 返回 `undefined`，`F` 恒为 false。**根因定位成立。**

- **C2（既有同步用例逐字未改）：pass** — 见 A1 的字节比对（两区块 IDENTICAL）。逐 hunk 核对 `git diff 27eef14...HEAD -- oamp/test/tool-permission.test.js`：涉及既有断言的 hunk 只有 2 处（argv 断言值 `yolo`→`always-ask`、R-12 的 `clientCapabilities` 期望值），均不在上述两条用例内。

- **C3（一次性路径零改动）：pass** — `acp-daemon.test.js:554` 的 `assert.equal(devOneShot[devOneShot.indexOf('--approval-mode') + 1], 'yolo', '§4.4/pr-007②：allow 档一次性 argv 应含 --approval-mode yolo')` 在 base 与 head 逐字相同；`oamp/src/agent.js` **不在**三点 diff 内（`git diff --name-only` 校验）。

- **C4（文件范围 = 3 个文件）：pass** — `git diff --name-only 27eef14...HEAD` 输出且仅输出：`oamp/src/acp-client.js`、`oamp/test/acp-daemon.test.js`、`oamp/test/tool-permission.test.js`。

- **C5（越界与回滚核实）：pass** — 仓库根 `/Users/chenchiyuan/projects/agents`：`git status --porcelain -uall` 输出为空；`git diff HEAD -- oamp/src/acp-client.js` 输出为空。被验证 worktree：本次全程只读（测试写入均在 `os.tmpdir()`/`/tmp`），`git status --porcelain -uall` 为空。

- **C6（`--no-session` 的 argv 事实 ⇒ 架构 §9.4.3 D3 行是否误述）：pass（核查完成；结论：D3 行为误述）**
  代码级证据：`oamp/src/acp-client.js:126` `args.push('--no-session');` —— 位于 `start()` 的**无条件**语句序列中（`if (!this.tools) args.push('--no-tools')` 之后、`if (this.modelArg)` 之前），无任何条件包裹；构造函数入参只有 `bin`，**无 argv 覆盖位**（对比 `bin`/`modelArg`/`roleFile`/`tools`/`permission`）。
  运行时证据：以 wrapper 记录 daemon 路径真实 argv（`/tmp/pr001-verify/argv.log`）＝ `acp --no-skills --no-rules --no-session --approval-mode always-ask` —— **含 `--no-session`**。
  结论：架构 §9.4.3 表格 D3 行「受控实测用的 argv 含 `--no-session` …，而 **daemon 常驻路径不带 `--no-session`**」与代码事实**相反**，属**误述**；该行给出的处置动作（「用 daemon 的真实 argv（不带 `--no-session`）重跑」）在实现上不可达成。其结论「D3 不成立」本身正确，仅前提描述错误。

- **C7（`clientCapabilities.elicitation.form` 的副作用与判据安全性）：pass（三项各自给出所需结论与证据）**
  ① **是否使其他 UI/工具变为可用：是（实测 + 代码级）**
  A/B 实测（`/tmp/pr001-verify/probe-tools.mjs`，仅 `initialize` 的 `clientCapabilities` 不同，其余 argv 相同）：
  - `caps = {}` → 模型自报工具：`read, bash, edit, eval, glob, grep, task, hub, todo, web_search, write, ast_edit, debug, lsp, inspect_image, browser`（**无 `ask`**）
  - `caps = {elicitation:{form:{}}}` → `read, bash, edit, **ask**, eval, glob, grep, task, hub, todo, web_search, write, ast_edit, debug, lsp, inspect_image, browser`（**多出 `ask`**）
  代码级证据链（`cli.js`）：`initialize` 存下 `this.#a = e.clientCapabilities`（偏移 18052xxx）→ 会话工厂 `{interactivePrompts: this.#a?.elicitation?.form!=null}`（偏移 18060764 / 18061438 / 18061824）→ `createSession({…, interactivePrompts: n?.interactivePrompts, …})`（偏移 18117439）→ `canPromptUser: e.interactivePrompts ?? e.hasUI ?? !1`（偏移 15886245）→ `ask` 工具注册门 `static createIf(e){return e.canPromptUser??e.hasUI?new eG(e):null}`（偏移 14352949）。SDK 注释亦载明：`interactivePrompts`「A human can answer synchronous prompts even without a terminal UI (e.g. ACP client rendering elicitation forms). **Enables `ask`**…」。
  同一门控函数 `let s=n?.elicitation?.form!=null;` 同时把 `select/confirm/input/askDialog/editor` 从 no-op 变为可用（偏移 18049221 附近）——即工具审批门依赖的 `select` 正是同一开关；`fs.*`/`terminal` **未**被声明（V-10③ 口径成立）。
  受控实测之一（`ask` 是否真能用）：模型被要求调用 `ask` → omp 发出 `elicitation/create`（`requestedSchema.properties = {q0:{…oneOf:[A,B]}, q0__other:{…}}`），**未**抛「requires interactive mode」⇒ 该工具在 ACP 下确实可执行。
  ② **`_approvedToolCallId !== null` 是否可能错误放行无关的 `Approve|Deny` 型 elicitation：会，窗口内可确定性复现**
  凭据时机（`oamp/src/acp-client.js`）：**置位** `:458`（放行裁决分支，`toolCall.toolCallId` 为非空字符串；拒绝分支 `:451` 置 `null`）；**失效** `:385`（该 toolCall 观测到终态 `completed|failed` 且 id 相等）、`:228`（`prompt()` 的 `finally`，轮末）、`:451`（拒绝）。
  确定性复现（自建 fake 服务端 `/tmp/pr001-verify/fake-acp.mjs` + `probe-credential.mjs`，输出 `/tmp/pr001-verify/credential-replies.json`）：同一轮内 `permission(call_A) → allow` 后依次发三门 —— `gate_1`（call_A 自己的门）→ **`gate_2`（与 call_A 无关的同型门）** → `call_A` 终态 → `gate_3`。结果：`gate_1 = accept/Approve`；**`gate_2 = accept/Approve`（无任何人工裁决）**；`gate_3 = accept/Deny`（终态后凭据已清）。
  真实 omp 可达性：omp 的 `Approve|Deny` `select` 在 dist 中**只有 1 处**（工具审批门，偏移 14046783），但该门对**未经过 `session/request_permission` 的工具同样会发**——权限包装只覆盖 `bEs={bash:!0,edit:!0,delete:!0,move:!0}`（偏移 15541352），而审批门条件 `R.required = g.length>0 || m.policy==="prompt" && (h||!T)` 与 `bEs` 无关（`write` 实测即为无权限请求而发门）。⇒ 一个「已放行但尚未见终态」的工具 + 另一个非 `bEs` 受门工具并发发门时，后者会被静默放行。**结论：会误放行（窗口 = 放行裁决 → 该 toolCall 终态；触发条件 = 窗口内出现另一道门）。**
  ③ **非审批类 elicitation 是否一律 `decline`：是** —— 代码：`:504-508` 仅当 `requestedSchema.properties.value.enum` 同时含 `Approve` 与 `Deny` 才走 accept，其余 `this._respond(message.id, {action:'decline'})`。确定性复现：`gate_4`（askDialog 形状，`properties.q0`）→ `decline`；`gate_5`（`properties.value.type==='boolean'` 的 confirm 形状）→ `decline`。真实 omp 复现：要求模型调用 `ask`（`AcpClient` 客户端，输出 `/tmp/pr001-verify/out-ask-acpclient.json`）→ omp 发 `elicitation/create`（`elicitEnum=null`，形状为 askDialog）→ 客户端回 `{action:'decline'}`，轮次以 `stopReason='cancelled'` 结束。

- **C8（被改写的既有断言未削弱）：pass**
  原断言（base:521）：`assert.deepEqual(init.params.clientCapabilities, {}, 'V-10③：声明能力会把文件写入 / 终端委托给客户端 → 工具 failed');`
  新断言（head:565-569）：`assert.deepEqual(init.params.clientCapabilities, { elicitation: { form: {} } }, 'M4：omp 的第二道审批门经 elicitation form select 询问；fs.* / terminal 仍不声明（V-10③：声明即把文件写入/终端委托给客户端）');`
  判定：原用例的判定意图 = 「不得声明 `fs.*` / `terminal`」；新断言仍对**整个 `clientCapabilities` 对象**做 `deepEqual`，对象中只允许 `elicitation.form` 一个键 ⇒ 任何 `fs.*`/`terminal` 声明依旧令断言失败，**意图保留且更严格**（由「必须为空」收紧为「必须恰为 `{elicitation:{form:{}}}`」）。
  同文件其他既有断言是否被顺带削弱：**否**。head 中 `clientCapabilities` 断言只有这一处（`clientCapabilities` 在 `/tmp/tp-head.js` 仅命中 565 行）；argv 用例块 base:389-416 vs head:429-456 逐行集合比对，差异仅为标题行与 allow 档断言值（`'yolo'` → `'always-ask'` + 原因文案），`deny` 档 `always-ask`、`tools=false` 不追加、匿名不追加三条断言**逐字保留**。

### D. 回归测试

- **D-1 `node --test oamp/test/tool-permission.test.js`：pass** — `1..18 / # pass 18 / # fail 0`（`/tmp/reg-toolperm.txt`）。
- **D-2 `node --test oamp/test/acp-daemon.test.js`：pass** — `1..7 / # pass 7 / # fail 0`（`/tmp/reg-daemon.txt`）。
- **D-3 `node --test oamp/test/*.test.js`（oamp 全量）：pass** — 首轮 `1..290 / # pass 289 / # fail 1`；次轮 `1..290 / # pass 290 / # fail 0`。首轮唯一失败原文：`oamp: web start 运行失败: listen EADDRINUSE: address already in use 127.0.0.1:41684`（`oamp/test/project-workspace.test.js:1166`，`failureType: testCodeFailure`）；该文件隔离复跑 `21/21 pass` ⇒ 环境性端口冲突，非本 PR 归因。

---

## 汇总

- pass: **38 项**
- fail: **0 项**（无返工项）
- partial: **3 项**（T5.1 / T5.2 / T5.4 —— 均为「T5 根因定位的**原始证据未落盘**」，非功能缺陷）
- blocked: **0 项**

分组：A 6/6 pass；B 24 项（21 pass / 3 partial）；C 8/8 pass；D 3/3 pass。

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| **1（高）** | PR 上下文摘要：`allow` 档「上浮给人裁决」；架构 §3.1 L1-2②：改 `always-ask` 否则「没有可上浮的请求」 | omp 在 `always-ask` 下对 tier ≥ `write` 的工具**一律**叠加审批门，而本 PR 只对「刚通过 `session/request_permission` 的工具」答 `Approve`；**不在 `bEs={bash,edit,delete,move}` 的写类工具**永远无凭据 ⇒ 恒答 `Deny`。实测 `write`：无权限请求、无任何上浮通知、模型侧得 `Tool call denied by user: write`、文件未创建；改动前（`yolo` 档）同一调用**正常执行**（实测 `Successfully wrote 11 bytes…`，副作用文件存在）。**本 PR 引入的行为回退，且 `pr-002` 只注入 `onPermissionRequest`，不覆盖该门** | 合并前裁决：① 把审批门一并纳入可裁决面（`_handleElicitationRequest` 需能上浮而非硬拒），或 ② 限定 `always-ask` 的适用范围使其不波及 `bEs` 之外的写类工具；至少须在 PR 上下文摘要 / 架构「必然变更点」中显式登记该回退与其归属 PR |
| **2（中）** | 架构 §9.4.3 D3 行：「daemon 常驻路径**不带** `--no-session`」 | `acp-client.js:126` 无条件 `args.push('--no-session')`；构造函数无 argv 覆盖位；实测 daemon 路径 argv 逐字含 `--no-session` | 修订 `architecture.md` §9.4.3 的 D3 行前提描述（其「D3 不成立」的结论本身正确）。本 PR commit body 已给出正确表述，仅架构文档未同步 |
| **3（中）** | 任务图 T5：「产物 = 根因结论 + 三类证据（argv/帧/时序）」；判据明确要求「argv 列表」与「0/1ms/100ms/1s 四档对照表」 | 三类证据仅以**结论句**出现在 commit `dd59d31` body；worktree 内无任何证据文件（3 个改动文件全为代码/测试），**argv 列表与四档对照表在整个 pr-001 产出物中不存在** | 补落一份 `clarifications/pr-001-m4-evidence.md`（argv 逐字 + 帧原文 + 四档表）；或在下次同类任务图中把「证据落盘路径」写成硬判据 |
| **4（低）** | `acp-client.js:494-501` 注释的设计假设：审批门「**只**在该 toolCall 刚被放行、进入执行前发出」 | 该假设不成立：审批门对**任何** tier ≥ `write` 的工具都会发出（`write` 实测无权限请求即发门）。凭据为「最后写入者胜」的单槽 `_approvedToolCallId`，清除时机仅三处（`:385` 终态、`:228` 轮末、`:451` 拒绝）⇒ 在「放行 → 终态」窗口内，任何 `Approve|Deny` 型 elicitation 都被答 `Approve`（确定性复现：无关门 → `accept/Approve`） | 若采纳偏差 #1 的处理方式，需一并重设该凭据模型（例如改为 elicitation 与 toolCall 的显式关联/握手，而非单槽凭据） |
| **5（低）** | PR 代码注释：`elicitation.form`「只开放『向客户端提问』通道，不委托 `fs.*` / `terminal`」 | 除审批门与 `ctx.ui.*` 桥外，还使 omp 的 **`ask` 工具对模型可见**（A/B 实测：`{}` → 16 工具无 `ask`；`{elicitation:{form:{}}}` → 17 工具含 `ask`）；模型真调用时本 PR 客户端因 schema 形状不匹配一律 `decline`，轮次以 `stopReason='cancelled'` 结束 | 在 PR/架构「必然变更点」中登记该连带面；并决定 `ask` 的 elicitation 是否纳入上浮面（见下一迭代候选 #1） |

---

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制，不在当前验收标准范围内，供主 agent 与 planner 决策。

- **`ask` 工具的 elicitation 处置**：当前恒 `decline`，且模型一调用即令轮次以 `stopReason='cancelled'` 结束（实测）。若要支持「就地提问」，需为上浮面新增 `ask` 形状分支（`properties.q0__other` + `oneOf`）。
- **审批门覆盖率与上浮面的一致性（与偏差 #1 同源）**：需要一次产品决策——「受门禁调用」是否包含 `bEs` 之外的 tier ≥ `write` 工具；若包含，`_handleElicitationRequest` 的硬拒语义必须改为上浮。
- **凭据模型加固（与偏差 #4 同源）**：单槽 `_approvedToolCallId` → 与 `toolCall` / elicitation 请求显式关联，消除窗口内误放行。
- **多工具并发挂起语义**：本 PR 的挂起是「每请求一个 Promise + `_pausedTurns` 深度计数」，但**并发**多个 `session/request_permission`（同一条 assistant 消息内多个 `bash`/`edit`）的帧级行为本次未验证；建议下一轮补 fake 层并发用例与真实 omp 双调用实测。
- **`permission='deny'` 档的模型侧可见性**：产品拒绝语义为「回包后立即 `session/cancel`」，模型侧永远看不到拒绝原因（本轮只能借裸 ACP 驱动观测到 `Tool call rejected by user (bash)`）；若 F04 需要「模型知道为什么被拒」，需要新的裁决—回传时序设计。

---

## 结论

**PASS**（所有条目 pass 或 partial，无 fail；partial 3 项，blocked 0 项）

支撑要点：M4 硬义务在真实 `omp` 18.0.11 上**双向**成立（放行 ⇒ 模型侧得工具真实输出 + 副作用文件落盘；拒绝 ⇒ 模型侧得 denied + 工具确实未执行），根因（第二道审批门受 `clientCapabilities.elicitation.form` 门控）由 omp dist 代码逐字证实；挂起链路与冻结计时的四项帧级判据已由**独立自建探针**复现（未依赖 PR 自带用例）；文件范围、一次性路径与既有断言逐字未动均经字节级核对。

需主 agent 决策（**本报告不构成合并许可**）：**偏差 #1（高）** 是本 PR 引入的、未被任何下游 PR 承接的行为回退（非 `bEs` 的写类工具在 `allow` 档被无人工介入地静默拒绝）。若合并门槛含「不引入未登记的行为回退」，则该项应视为返工项；判定权在主 agent，不在本报告。偏差 #2~#5 按上表建议在下一迭代同步。
