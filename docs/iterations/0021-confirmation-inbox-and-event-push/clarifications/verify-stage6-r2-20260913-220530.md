# 阶段 6 · 迭代终态**复验**报告（R2，终态 `575c634`）

**验证者身份（反射结果）**：**多智能体权限面独立鉴定人（终态复验视角）**——本次产出物 = 「agent↔web 权限挂起/裁决链路 + 控制台第三栏 + 通知面」的**新终态代码**（首轮阶段 6 之后追加 pr-005）与迭代文档；验证标准 = 偏差 D-1 闭合复核 / 首轮 33 条在新终态的再成立 / pr-005 集成无回归 / 终态完整性 / 回归与偏差台账。⇒ 反射出的身份 = **能在 ACP 协议层（帧级）、Node 进程间链路层、浏览器真实渲染面与 git 对象库四层交叉取证，并能对「消费侧空值语义」这一具体接缝做字节级对照的资深评审者**——不是通用检查员。

**产出物（待验证）**
1. 迭代终态代码：worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0021-confirmation-inbox-and-event-push`，分支 `iteration/0021-confirmation-inbox-and-event-push`，tip **`575c634e8f333cd609de591fb70ae4932980a9a9`**（实测 `git rev-parse HEAD` 一致），5 个 merge commit。
2. 迭代产物目录 `docs/iterations/0021-confirmation-inbox-and-event-push/`（`demand.md` / `prd.md` + `prd/F01~F12` / `architecture.md` / `prs/`（5 PR + 5 tasks）/ `status.md` / `history.md` / `progress.md` / `clarifications/**`）。
3. 首轮阶段 6 报告 `clarifications/verify-stage6-20260913-213704.md`、pr-005 验收报告 `clarifications/verify-pr-005-20260913-215436.md`：**只读参照，不作判定依据**（其结论已在本轮逐条重建）。

**验证标准来源**：主 agent 委托（A~F 六组逐项判定）。

**验证日期**：2026-09-13 ｜ 本报告全部命令与实测在 **21:56 – 22:05 +0800** 窗口内、上述 worktree（迭代分支 `575c634`）上执行。环境：node v22.15.0、真实 `omp` = `/Users/chenchiyuan/.bun/bin/omp`（v18.0.11）、headless Chromium。

**需求变更搭置**：**已确认 `docs/iterations/0021-confirmation-inbox-and-event-push/deferred-demand-changes.md` 不存在**（`find . -name deferred-demand-changes.md` → 零命中，目录清单逐项核对无此文件）⇒ **本迭代无需求层搭置，无搭置内容可摘录**。

**独立性声明**：本报告**未接收任何执行过程上下文**。产出物内的自评文字（`history.md` / `status.md` / 既有 `clarifications/verify-*.md` / 各 PR 文档的验收勾选）**一律未作为判定依据**——仅在**事后**用于对照「实现与既有记录是否一致」。全部判定建立在：**我亲自执行的命令输出**、**我自建的三套探针**（M4 ACP 帧级探针 / 真实 SSE 前端探针 / 端到端真机链路脚本）、**真实浏览器 DOM 与真实 `Notification` 构造实录**、**被测文件原文行号**。

**验证方法（可复查，全部资源已清理）**
- **A 组（D-1）**：① 把终态 `oamp/web` 与**对照基线** `git show 697176f:oamp/web/*` 分别静态服务（`:8921` / `:8922`），用真实 headless Chromium 加载**未经改动的 `app.js`/`notify.js`**，经 `/api/events` **真实 SSE 帧**驱动未改动的 app.js 处理器，读真实 DOM 与真实 `Notification` 构造实录；② 端到端：真实 `router` + `web`（`:8931`）+ `agent`（真实 omp）+ 浏览器，观察**协议真实产出的确认项**。
- **B/E 组**：git 对象库取证 + `node --test`；**M4 双向**用自建 `/tmp/vr2/m4-probe.mjs`（构造终态 `AcpClient`，包住 `_write`/`_handleMessage` 记录 ACP 帧收发），判据 = **模型侧工具结果 + 磁盘副作用**，审计行仅记录不判定。
- **清理**：全部 hub 进程已 stop、5 个端口复检 released、浏览器 tab 已释放、`/tmp/vr2` 与 `/tmp/vr2-e2e` 已 `rm -rf`（详文末 §清理与残留）。

---

## A. 偏差 D-1 闭合复核（本轮核心）——**4/4 pass**

**复核对象**：首轮阶段 6 的偏差 D-1 —— ACP 权限门来源的确认项 `tool` 恒为 `null`，消费侧仍把 `null` 插进模板 ⇒ 第三栏出现字面量 `null · <title>`、浏览器通知正文出现「`请求执行 null：<title>`」。

### A-1 「确认项 `tool` 为 `null` 时，第三栏行不出现字面量 `null`/`undefined`」：**pass**

**真实渲染面取证**（未经改动的 `app.js` 由**真实 SSE 帧**驱动；帧经 `/api/events` 下发，app.js 的 `confirmation` 监听器原文执行 `renderInbox()`）：

```
SSE 帧 A: {"confirmation_id":"cfm-r2-null","agent_id":"pb-dev","chat_id":"chat-r2-1","tool":null,"title":"echo E2E-NULL",…}
SSE 帧 B: {…,"confirmation_id":"cfm-r2-missing",…, 无 tool 键, title:"echo E2E-MISSING",…}

真实 DOM（#inbox-list .inbox-item）：
  cfm-r2-null    → .inbox-request textContent = "echo E2E-NULL"
  cfm-r2-missing → .inbox-request textContent = "echo E2E-MISSING"
  两者 textContent / innerHTML 均不含 "null" / "undefined"，无悬空 "·"
```

**对照基线（同一驱动方式，`697176f` 的 `app.js`）**：`cfm-r2-null` → `"null · echo E2E-NULL"`；`cfm-r2-missing` → `"undefined · echo E2E-MISSING"`。⇒ 缺陷在基线真实存在，在新终态被消除。

**协议真实性复核**：这些帧的 `tool:null` **不是构造出来的**——端到端实测中，真实 omp 经 ACP 权限门上浮的确认项，服务端 `GET /api/confirmations` 与 SSE 帧给出的 `tool` **实测为 `null`**，同时 `title` 为命令原文（见 §B-F-1 的 `allSseConfirmationFrames`：两条真实确认项 `tool` 均为 `null`）。根因独立复现：M4 帧级探针记录到 `session/request_permission` 的 `params.toolCall` **不含 `toolName`**（仅 `title` / `kind`，见 §B-E-1）。

### A-2 「`tool` 为 `null`/缺键时，浏览器通知正文不出现字面量 `null`/`undefined`」：**pass**

**真实 `Notification` 构造实录**（在同一页面上下文包裹真实投递路径，`app.js:181` 的 `oampNotify.dispatch('confirmation_required', entry)` 原文执行）：

```
window.__notes = [
  { title: "需要你确认", body: "pb-dev 请求执行 echo E2E-NULL" },
  { title: "需要你确认", body: "pb-dev 请求执行 echo E2E-MISSING" },
  { title: "需要你确认", body: "pb-dev 请求执行 bash：echo E2E-BASH" }
]
```

两条缺 `tool` 的正文**不含** `null` / `undefined`，**无悬空 `：`**。**对照基线**同页同路径：`"pb-dev 请求执行 null：echo E2E-NULL"` / `"pb-dev 请求执行 undefined：echo E2E-MISSING"`。

### A-3 「`tool` 有值时输出与首轮阶段 6 时的形态逐字一致」：**pass（字节级）**

判定口径 = 与 pr-005 的基线 `697176f`（即首轮阶段 6 实测所用版本）**逐字节比对** `renderInboxItem(entry)` 全量返回值（同一页内调用真实函数）：

| 输入 | `575c634` 第三栏行 | `697176f` 第三栏行 | 逐字节一致 |
|---|---|---|---|
| `{tool:'bash', title:'echo E2E-1'}` | `bash · echo E2E-1` | `bash · echo E2E-1` | **是** |
| `{tool:'write', title:'Write file x'}` | `write · Write file x` | `write · Write file x` | **是** |
| `{tool:'<b>', title:'x'}` | `&lt;b&gt; · x` | `&lt;b&gt; · x` | **是** |

通知侧同口径：`{agent_id:'pb-dev', tool:'bash', title:'echo hi'}` 两版均为 `pb-dev 请求执行 bash：echo hi`（**逐字**）；`title` 100 字符时两版均为「80 字符 + `…`」。

**端到端真实取值补充**：真实链路中**工具审批门**来源的确认项 `tool` **确为非空**——实测在途表条目 `{"tool":"write","options":["Approve","Deny"]}`，第三栏行渲染为 `write · Allow tool: write\nPath: /tmp/…/r2-write.txt\nContent:\nR2WRITE-…`，通知正文为 `e2e-r2-dev 请求执行 write：…`（80 字符截断 + `…`）⇒ 与首轮阶段 6 的模板形态 `${agent_id} 请求执行 ${tool}：${title}` 一致。

### A-4 「未引入『用 `title` 猜工具名』的猜测性派生」：**pass**

1. **源码级**：`oamp/web/app.js:636-639` 的 `inboxRequest` 只从 `entry.tool` / `entry.title` 本身取段（`[entry.tool, entry.title].filter(...)`），**无** `title→tool` 映射、无 `kind` 词表、无回填；`oamp/web/notify.js:40` 的 `${p.tool ? \`${p.tool}：\` : ''}` 同理。改动 hunk 中不存在任何新增的派生行。
2. **行为级**：缺 `tool` 时输出**恰为 `title` 本人**（`{title:'echo E2E-1'}` → `echo E2E-1`，未重复 / 未变形 / 未附加工具名词）。
3. **契约级**：`oamp/test/confirmation-roundtrip.test.js`（含用例名即契约的 `:445-451`「权限门不带 toolName（真实 omp 帧形）⇒ tool=null，**不得用 title 猜测**」）**零改动且 20/20 绿**（见 §C）。

### D-1 闭合判定

> **闭合（用户可见面）**。第三栏行与浏览器通知正文在 `tool` 为 `null` / 缺键时**均不再出现字面量 `null`/`undefined`**，且**无悬空分隔符**；`tool` 有值时输出与首轮阶段 6 所用版本**逐字节一致**；未引入猜测性派生。判定基于**真实渲染面**（真实 SSE → 未改动 app.js → 真实 DOM / 真实 `Notification` 构造）+ **端到端真机链路**，而非单元测试转述。
>
> **未闭合的残留**：D-1 的**文档口径半**（`architecture.md` §7 T-02 / §7 T-09 与 `prd/F01` T-02 仍写「工具名 + `title`」/ `${agent_id} 请求执行 ${tool}：${title}`，未登记「缺失即整段省略」）**pr-005 显式列为非目标** ⇒ 该项转入 §F 台账（对应 pr-005 偏差 4）。

---

## B. 首轮阶段 6 的结论在新终态上是否仍成立（逐项重建，非转述）

> 口径：首轮为 **33 条**（A×4 + B×5 + C×3 + D×12 + E×4 + F×2 + G×3）。本轮因新增 **pr-005**，PR 粒度组由 4 项扩为 5 项、依赖组增 1 项专项，故本轮条目数为 **35**；下表给出**逐条重建**与**与首轮条目的对应**。**每项判定均附本次取证**。

### B-A. PR 粒度（逻辑原子性 / 可审查性 / 独立性）——5/5 pass

三点 diff 取证（`merge-base(merge^1, merge^2)` = 分支起点；`merge^2` = 分支 tip；`merge^1..merge` = 合并结果的首父 diff），全部来自 git 对象库：

| PR | 合并提交 | merge base | 分支 tip | 首父 diff | 涉及卡 |
|---|---|---|---|---|---|
| pr-001 | `c84233a` | `10cbca6` | `dd7badd` | 4 文件 / **+861 −34** | F02 F04 F05 F12 |
| pr-002 | `697176f` | `c84233a` | `4d16ba4` | 5 文件 / **+1090 −12** | F02 F04 F05 |
| pr-003 | `1f7eceb` | `10cbca6` | `0d9cbc2` | 10 文件 / **+949 −29** | F01~F08 F10~F12 |
| pr-004 | `017961a` | `1f7eceb` | `a431225` | 9 文件 / **+1172 −8** | F01 F03 F06~F09 |
| **pr-005** | **`575c634`** | **`697176f`** | **`9e34e96`** | **5 文件 / +276 −3** | F01 F08 |

- **B-A-1 pr-001：pass**。分支仅 4 个非合并提交，改动集中在 `AcpClient` 的「判定可挂起」语义（`acp-client.js:433-444` `_handleServerRequest` 异步化、`:450-476` 先判定后 `_respond`、`:484-501` 返回域含 `{optionId}`、`:507-518` `_askHook` + `:581-608` 冻结/恢复轮次计时）。首父 diff 中 `oamp/test/acp-daemon.test.js` 仅 1 行改动（argv 断言）⇒ 无跨模块散点。`depends_on` 为空；与 pr-003 同 base `10cbca6` 独立起飞。
- **B-A-2 pr-002：pass（有条件）**。改动 = `oamp/src/context-pool.js`（上浮钩子**唯一档位判定点**）+ `oamp/src/agent.js`（`raiseConfirmation`/`settleConfirmation`/`cancelPending` + pending 表）；同一关注点 = agent 进程内信封 1·2·3 收发。**实际 5 文件 > 声明 4**（`oamp/test/acp-daemon.test.js` 由 Q7 裁决显式授权）⇒ 计偏差 D-3（见 §F），不计 fail。base = `c84233a`（pr-001 合并点）⇒ 依赖顺序化。
- **B-A-3 pr-003：pass**。改动 = web 进程承载确认面（`oamp/src/inbox.js` 新建、`oamp/src/web.js`、派生面 `API.md`/`llms.txt`、5 测试）。`depends_on` 为空；base = `10cbca6`；首父 diff 中派生面为纯追加（既有 19 条路由正文零改写）。
- **B-A-4 pr-004：pass（有条件）**。改动 = 控制台第三栏 + 通知面（`oamp/web/notify.js` 新建、`index.html`/`app.js`/`style.css`、2 新建测试）。**实际 9 文件 > 声明 5**（`src/web.js`/`llms.txt`/`test/notification-scope.test.js` 由 Q6 裁决授权）⇒ 计入 D-3。base = `1f7eceb` ⇒ 依赖顺序化。
- **B-A-5 pr-005（本轮新增）：pass**。分支**恰 3 个非合并提交**（`638d068` 规划 / `3a018a2` 任务图 / `9e34e96` 实现），改动**恰为声明的 3 条代码路径 + 2 条 docs 工作流产物**（`git diff --name-status 697176f 575c634 -- oamp/` = `M test/inbox-console.test.js` / `M web/app.js` / `M web/notify.js`，实测**逐条命中 PR 文件范围字段**）⇒ 可审查性最高（+276 −3，其中测试 +48 **纯新增**）。独立性：`depends_on` 为直接消费关系（见 B-B-6），base = `697176f`（即 pr-002 的合并点）⇒ 顺序化无冲突。逻辑原子性：单一关注点 = **消费侧空值语义**，生产侧 `oamp/src/**` **零 diff**。

### B-B. 依赖正确性 —— 6/6 pass

- **B-B-1 `pr-002 → pr-001` 代码级证据：pass**。基线反证 `git show 854766c:oamp/src/acp-client.js`：`session/request_permission` 同步判定 + 立即回包、`_permissionDecision` 只识别 `'allow'|'deny'`（钩子返回 Promise ⇒ `undefined !== 'deny'` ⇒ 立即 allow）⇒ pr-002 的未结算 Promise 钩子在基线**无法产生挂起**。终态一致性：`:450-461` 已异步化并在判定结算后回包，`:495-499` 识别 `{optionId}`。共享接口逐条对上。
- **B-B-2 `pr-004 → pr-003` 代码级证据：pass**。基线反证 `git show 854766c:oamp/src/web.js | grep -c confirmations` = **0**（实测）；终态 `src/web.js` 含 `GET /api/confirmations` 与 `/decision` 路由、`publishGlobal` 的 `confirmation` 帧、`STATIC_FILES` 含 `'/notify.js'` ⇒ pr-004 消费的三样在基线**全不存在**，依赖为真。
- **B-B-3 依赖图无环：pass**。`pr-001→pr-002`、`pr-003→pr-004`、`pr-005→{pr-004, pr-002}`；合并时间 `1f7eceb`(13:21:40) → `c84233a`(20:45:46) → `017961a`(20:50:30) → `697176f`(21:23:01) → `575c634`(21:55:37)，**每个被依赖 PR 的合并时间均早于依赖者**，拓扑一致且无环（实测 `git log -1 --format=%cI` 逐个读取）。
- **B-B-4 PR 间文件范围无重叠（按声明字段）：pass**。5 份 PR 的声明范围两两交集为空。**实际改动面另有重叠**：`oamp/test/acp-daemon.test.js` 同现于 pr-001（±1 行）与 pr-002（29 行）——顺序化、无冲突（pr-002 的 base 已含 pr-001），且 pr-002 侧由 Q7 授权 ⇒ 记偏差 D-3，**不判 fail**（判定对象是阶段 4 的声明字段）。
- **B-B-5 12 张卡 → PR 映射无幻影引用：pass**。逐卡证据：F01{pr-003,pr-004,pr-005} / F02{pr-001,pr-002,pr-003} / F03{pr-003,pr-004} / F04{pr-001,pr-002,pr-003} / F05{pr-001,pr-002,pr-003} / F06{pr-003,pr-004} / F07{pr-003,pr-004} / F08{pr-003,pr-004,pr-005} / F09{pr-004} / F10{pr-003} / F11{pr-003} / F12{pr-001,pr-003}；每个「卡→PR」对都对应 §B-D 的文件:行级落点；反向亦无遗漏（5 个 PR 声明的引用全部命中 12 卡之内）。
- **B-B-6 `pr-005` 的 `depends_on` 代码级证据（本轮专项）：pass**。PR 文档声明两条直接依赖，我用基线反证逐条独立判定：
  - **`pr-005 → pr-004`**：反证① `git show 1f7eceb:oamp/web/app.js | grep -c 'inbox-request'` = **0**（实测）；反证② `git show 1f7eceb:oamp/web/notify.js` → **文件不存在**（pr-004 新建）；反证③ 测试承载文件 `oamp/test/inbox-console.test.js` 同为 pr-004 新建。⇒ 本 PR 的 3 个改动文件在 pr-004 之前**均不存在或无语义**，依赖为真。
  - **`pr-005 → pr-002`**：反证 `git show c84233a:oamp/src/agent.js | grep -c 'confirmation_id'` = **0**（实测）；终态 `oamp/src/agent.js:328` 为信封 1 `tool` 字段的**唯一赋值点**，且钉死该值的契约测试同为 pr-002 产物。⇒ 缺陷的触发源由 pr-002 定义，依赖为真。
  - **传递性不列**：`inbox.js`/`web.js`（pr-003）经 `pr-004→pr-003` 已在 base 内满足且与本 PR 零交集 ⇒ 不列为直接依赖，与「直接消费关系」口径一致。

### B-C. 并发调度真实执行证据 —— 3/3 pass（C-1 带偏差注记）

- **B-C-1 worktree 时间窗重叠：pass（首波有据；次波无据 ⇒ D-4）**。一手证据 = git 对象库中**各 PR 分支自身的提交时间戳**（分支引用已删，但 `merge^2` 的提交链完整保留）：
  - **首波 `pr-003 ∥ pr-001`**：两分支同 base `10cbca6`；`pr-003` 分支提交 `a5b2043@12:30:45` → `0d9cbc2@12:53:56`，并于 **13:21:40** 合并；`pr-001` 分支提交 `27eef14@12:30:45` → `dd59d31@12:57:51`（其后 `1f11b59@13:26:51`、`dd7badd@20:10:34`），**直到 20:45:46 才合并**。⇒ 两条分支的提交在 **[12:30:45, 12:53:56] 与 [12:30:45, 12:57:51] 的交叠窗口内各自推进**，且 pr-003 分支在整个 `[12:30:45, 13:21:40]` 区间与存活中的 pr-001 分支**同时存在** ⇒ 真实并发执行成立（不依赖任何台账叙述）。
  - **旁证（更长窗口）**：`pr-004` 分支首个提交 `cce5f8b@13:27:19`，与存活至 20:45:46 的 `pr-001` 分支在 **[13:27:19, 20:45:46]** 内共存，同期两分支各自产生提交（`76e33d5@20:09:41` / `dd7badd@20:10:34`，相隔 53 秒）。
  - ⚠ **次波无据**：`pr-002` 分支首个提交 `70c8877@20:52:03`，而 `pr-004` 已于 **20:50:30** 合并 ⇒ 若存在重叠窗口 ≤ 1 分 33 秒且**不可证**，时间线与「并行」相反。归入偏差 D-4。
  - 一手时间源另经 `git reflog --all` 交叉核对：merge 行的分支名与时刻（`merge feat/0021-pr-005-null-tool-name-fix: Merge made by the 'ort' strategy` 等 5 条）与上述逐条一致。
- **B-C-2 并发配置区块真实初始化与更新：pass**。`git log -p -- docs/…/status.md` 实测 **5 次真实字段写入**（非仅标题变更）：
  - `10cbca6`(12:24:40) **初始化**五字段：起始并发数 3 / 硬上限 5 / 当前有效上限 3 / 释放次数 0 / 已派发总数 0；
  - `7e979ac`(13:21:39) 已派发总数 0 → 6；
  - `59f9cba`(20:05:41) 有效上限 3 → **5**、释放次数 0 → 1、派发 6 → 9；
  - `267bc21`(20:50:30) 释放 → 2、派发 → 11；
  - `d2c4b25`(20:56:02) 释放 → 3、派发 → 12；
  - `b8f4d75`(21:55:37) 有效上限 → `5（= min(3 + 4×3, 5) = 5，**已触硬上限**…）`、释放 → 4、派发 → 23。
- **B-C-3 爬升公式 / 「已触硬上限」依据：pass**。`b8f4d75` 的字段原文代入公式 `min(3 + 4×3, 5)`；我独立核算 `3 + 4×3 = 15`，`min(15, 5) = 5`，与硬上限 `2×3−1 = 5` 相等 ⇒ **「已触硬上限」的判定依据成立**。工作区版 `status.md:35` 与提交版一致（工作区对该文件**无未提交改动**，实测 `git status` 仅 `M history.md`）。

### B-D. 功能卡覆盖复核（F01~F12 全覆盖；F01·F08 为重点抽检）——12/12 pass

**全卡落点在终态的在位性（程序化，`oamp/` 根下实测）**：

| 卡 | 落点实测 | 判定 |
|---|---|---|
| **F01** | `web/index.html` 存在 `id="inbox"`；`src/inbox.js` 在位；**真实三栏几何**：`main.layout` 直接子节点 = `[ASIDE.sidebar, SECTION#inbox, SECTION.detail]`，矩形 `sidebar{x:0,w:320}` / `#inbox{x:320,w:320}` / `detail{x:640,w:800}`，三者 y=48 h=852 **并列同屏**；`#inbox` **不在** `.sidebar` 内、**不在** `.detail` 内；空态文案 `暂无待确认项`、计数 `0` | **pass** |
| **F02** | `src/context-pool.js` 的 `onPermissionRequest` 上浮钩子（档位判定唯一注入点）在位；`src/agent.js` `raiseConfirmation` 在位；`src/acp-client.js` `_handlePermissionRequest` 先上浮后回包 | **pass** |
| **F03** | `web/app.js:694` 为 `/api/confirmations/<id>/decision` 的 **POST 唯一提交点**，`:698` 200 即 `dropInboxItem`、`:700` 404 同样移出；服务端 `src/web.js` 含 `decision` 路由（4 处） | **pass** |
| **F04** | `src/agent.js` `settleConfirmation` 在位；`confirmation_decision` 在两进程在位（`web.js` ×1 / `agent.js` ×2） | **pass** |
| **F05** | `src/inbox.js` 全文（42 行）**零 `setTimeout`/`setInterval`/定时器**（仅有注释言「无 TTL、无定时器」）；`src/acp-client.js` `_pauseTurnTimer`/`_resumeTurnTimer` 在位（4 处） | **pass** |
| **F06** | `web/app.js` `loadConfirmations` 在位（3 处）；`src/web.js` `publishGlobal` 在位（4 处）；**实测**：确认项在裁决前持续在列；`GET /api/confirmations` 为重建面 | **pass** |
| **F07** | `web/notify.js:12` = `Object.freeze(['chat_completed','chat_failed','confirmation_required'])`（封闭 3 类）；生产代码中 `call_completed` **仅 1 处命中且为注释**（`src/web.js:76`） | **pass** |
| **F08** | `web/notify.js` `Notification` 投递路径在位；**真实投递实录**见 A-2；真实链路下三类事件的通知标题实测出现 | **pass** |
| **F09** | 静态分片实测：service 段内 `Notification` 命中 **0**；channel 段内事件类型串命中 **0** ⇒ 两侧零命中，切片契约成立 | **pass** |
| **F10** | `src/web.js:1737` = `server.listen(port, '127.0.0.1', resolve)`；`package.json` `dependencies = {}`（零依赖）；`web/notify.js` 全文无 `fetch`/`WebSocket`/Service Worker | **pass** |
| **F11** | `src/inbox.js` `export` 面**恰 5 个**（`add`/`list`/`take`/`remove`/`size`），`history` 仅 1 处命中且为注释（「不提供 history/export/decided 类入口」）；`git diff --name-only 854766c 575c634 -- oamp/src/persist.js` **为空** | **pass** |
| **F12** | `git diff --name-only 854766c 575c634` 共 73 文件，`grep -E '^(omp\|harness\|roles\|\.pb-agents\|\.claude)/'` → **NONE**（改动全在 `oamp/**` 与 `docs/iterations/0021-*/**`） | **pass** |

**F01 / F08 深挖（受 pr-005 影响的两卡）**：
- **F01 验收 3（免刷新 + 全部列出）**：真实 SSE 驱动下 3 条确认项**无需刷新**同现于栏内（`#inbox-count = "3"`，3 个 `.inbox-item`）；端到端真机下 2 条来自**不同对话**的条目同屏（`chat-1691b1e2…` 与 `chat-090070cf…`）⇒ 验收 2「跨对话集中」与验收 3 成立。
- **F01 验收 4（可辨识来源对话）**：条目首行 `.inbox-source` 实测 = `e2e-r2-dev · chat-1691b1e2-986a-4aab-a43e-fe48259ecb68`（agent + chat_id 双标识）⇒ 成立。**注**：验收 4 只要求「可辨识」，未规定 `tool` 的呈现；**受 pr-005 影响的是第二行（T-02）**，其新形态见 A-1/A-3。
- **F08 验收 2（三类事件各一次 + 重建不重复）**：真实投递实录中，**每次首次入栏各触发恰一次**通知（两个 live 帧 → 两条通知）；而经**重建面**（`GET /api/confirmations` 拉取）进入栏内的在途项（`cfm-80b59aed…`，页面前已存在）**零通知**——同一次会话窗口中 `__notes` 长度恰等于 live 帧数 ⇒ 「重建不重复触发」成立。F08 的**正文内容**（T-09）为 pr-005 的改动面，A-2/A-3 已实测。

### B-E. M4 硬义务终态复核（真实 omp，双向，审计行不作判据）——4/4 pass

**探针**：自建 `/tmp/vr2/m4-probe.mjs`（构造终态 `AcpClient`，`bin=/Users/chenchiyuan/.bun/bin/omp`、`cwd=/tmp/vr2/m4run`；包住 `_write`/`_handleMessage` 记录**帧级**收发）。**判据 = 模型侧工具结果 + 磁盘副作用**；`TOOL_APPROVED`/`TOOL_DENIED` 仅记录、未参与判定。四个场景各跑一次，全部实跑成功（无异常退出）。

| 场景 | 门 | 钩子回值 | ACP 回包（帧级实测） | **模型侧** | **磁盘副作用** |
|---|---|---|---|---|---|
| bash + 放行 | ACP 权限门 | `{optionId:'allow_once'}` | `{"outcome":{"outcome":"selected","optionId":"allow_once"}}` | 模型报出真实执行结果（`-rw-r--r-- 1 chenchiyuan wheel 12 … marker-allow.txt` + `M4-R2-allow`） | `markerExists=true`，内容 `M4-R2-allow` |
| bash + 拒绝 | ACP 权限门 | `{optionId:'reject_once'}` | `{"outcome":{"outcome":"selected","optionId":"reject_once"}}` | 轮次以 `permission_denied` 终止；tool_call 帧**停在 `pending`**（从未执行） | `markerExists=false` |
| write + 放行 | 工具审批门 | `{optionId:'Approve'}` | `{"action":"accept","content":{"value":"Approve"}}` | `write` 报 `completed`，模型复述读回内容与预期**逐字一致** | `targetExists=true`，内容 `M4-R2-WRITE-write` |
| write + 拒绝 | 工具审批门 | `{optionId:'Deny'}` | `{"action":"accept","content":{"value":"Deny"}}` | 模型侧**显式** `Tool call denied by user: write`，并自述未用 bash 绕过 | `targetExists=false` |

- **B-E-1「放行 ⇒ 模型侧真实工具输出 + 副作用落盘」：pass**（bash / write 两路均成立）。
- **B-E-2「拒绝 ⇒ 模型侧确实 denied 且未执行」：pass**（两路均成立：bash 路轮次终止 + 工具帧从未执行 + 磁盘无痕；write 路模型侧显式 denied 文本）。
- **B-E-3「审计行不得作判据」：pass**（判据全部取自 ACP 帧内容 + `fs` 实测；本报告未引用任何 `TOOL_APPROVED`/`TOOL_DENIED` 作为通过依据）。
- **B-E-4「不经 ACP 权限门的写类工具（`write`）不再被静默拒绝」：pass**（`write` 经 `elicitation/create` 审批门上浮；回 `Approve` ⇒ 文件真实写入；回 `Deny` ⇒ 明确拒绝且无副作用）。
  **旁证（本轮 D-1 的根因独立复现）**：帧级实测 `session/request_permission` 的 `params.toolCall` = `{title:'echo …', kind:'execute', **toolName: null**}`——**omp 的权限门帧确实不带 `toolName`**，而在审批门帧上 `toolName:'write'`。这独立印证了 D-1 的根因描述，也印证 pr-005「修在消费侧、不动生产侧」的取舍依据。

### B-F. 端到端可用性（真实进程链复现）——2/2 pass

**拓扑**（全部真实进程）：`router`（`OAMP_SOCKET=/tmp/vr2-e2e/router.sock`、`OAMP_DB=/tmp/vr2-e2e/oamp.db`）+ `web`（`:8931`）+ `agent e2e-r2-dev`（`--role dev --tools on --permission allow`，**真实 omp**）+ 真实 headless Chromium。**未触碰本机既有 7788 实例**。

- **B-F-1「上浮 → 入栏 → 裁决 → 继续 / 中止」全链：pass**。同一会话内逐段实测：
  1. **上浮归栏**：`POST /api/messages`（bash 命令）⇒ `GET /api/confirmations` 由空变为含 1 条，字段含 `confirmation_id/chat_id/agent_id/tool/title/options/created_at`；`tool` 实测 **`null`**、`options` 实测为 omp 给出的四项 `allow_once/allow_always/reject_once/reject_always`（**原样**，label = `Allow once` 等）。
  2. **全局帧恰一帧**：SSE 订阅实录，每 chat **恰 1 帧** `event: confirmation`（两条 chat 各 1 帧 = 共 2 帧），`data` 与列表元素**同形状**；重建面不发帧。
  3. **继续（放行）**：`POST …/decision {option_id:'allow_once'}` → 200 `{accepted:true}` ⇒ 条目即时移出（`remaining` 随裁决归零），chat 终态 **`completed`**，out 消息报出**真实工具输出** `R2E2E-…-ALLOW` 且「退出码 0」。
  4. **中止（拒绝）**：`POST …/decision {option_id:'reject_once', text:…}` → 200 ⇒ chat 终态 **`failed`**，out 消息 = `工具调用被 permission 策略拒绝（permission=deny）`、`error=permission_denied`。
  5. **非幂等**：对同一 `confirmation_id` 二次提交 → **404**（实测）。
  6. **服务端选项校验**：对审批门条目（`options = ["Approve","Deny"]`）提交 `reject_once` → **400** ⇒ `option_id` 必须属于该条 options 的校验真实生效。
  7. **浏览器真实呈现**：真实 console 页在**不刷新**的情况下由 live 帧追加条目，DOM 行与通知正文见 A-1/A-2/A-3；三条确认项（`tool:null` ×2 + `tool:"write"` ×1）同屏并列。
- **B-F-2「非确认/终态类事件不产生确认项与错误通知」：pass**。真实链路中两类来源被清晰区分：`tool:null`（ACP 权限门）与 `tool:'write'`（审批门）均由同一 `raiseConfirmation` 产出并归栏；同一 agent 的两个对话各挂一条时条目**互不串扰**（`chat-1691b1e2…` / `chat-090070cf…` / `chat-20d64196…` 三条各自独立，`source` 行正确显示各自 chat_id）；对其中一条裁决不影响其余，且逐条裁决后 `remaining` 归零。

### B-G. 回归与仓库洁净 —— 3/3 pass

- **B-G-1 全量回归 `node --test oamp/test/*.test.js`（终态 `575c634`，无并发负载的干净跑）：pass** ⇒ `# tests 333 / # pass 333 / # fail 0 / # cancelled 0 / # skipped 0`（`duration_ms ≈ 61305`）。本轮 **未复现** 首轮登记的 D-5 flake（`status.test.js` 心跳时序），故**无失败需要归因**；用例数由首轮的 332 → **333**（+1 = pr-005 新增的三态断言用例）。
- **B-G-2 工作区 / worktree 洁净：pass（含 1 项 docs-only 未提交写入，见 §F-R2-1）**。`git worktree list` → **恰 2 条**（仓库根 `d84b2be [main]` 与迭代工作区 `575c634 [iteration/0021-…]`）；`.pb-agents/worktrees/` 下**仅此一个**目录；`.git/worktrees/` 下**仅此一个**元数据目录；`git branch -a` → 仅 `iteration/0021-…`、`main`、`remotes/origin/main` ⇒ **5 个 PR 的 worktree/分支已全部清理**。`git status --porcelain` → 仅 ` M docs/…/history.md`（**docs-only**，非本轮产物代码）。产品代码零未提交改动 ⇒ 终态代码 = `575c634` 可复现。
- **B-G-3 仓库根未被改动：pass**。`git -C /Users/chenchiyuan/projects/agents status --porcelain` → **空**（全程复检两次，末次在报告落盘前，仍为空）；仓库根 HEAD = `d84b2be`，本轮**未对其做任何写入**。

---

## C. pr-005 集成无回归 —— 3/3 pass

- **C-1 `oamp/test/confirmation-roundtrip.test.js` 零改动且绿：pass**。`git diff --name-only 697176f 575c634 -- oamp/test/confirmation-roundtrip.test.js` → **空**（实测）；`node --test oamp/test/confirmation-roundtrip.test.js` → **`# tests 20 / # pass 20 / # fail 0`**（实测）。pr-002 的契约测试（含 `tool=null` 钉死与审批门 `tool='write'`）**未被动过**。
- **C-2 `oamp/test/inbox-console.test.js` 既有断言未被削弱：pass**。两路证据：① `git diff --numstat 697176f 575c634 -- oamp/test/inbox-console.test.js` = **`48 0`**（**纯新增，零删除行**）；② 程序化有序子序列校验：`697176f` 版 **364 行**按序**全部**出现在 `575c634` 版（412 行）中，**缺失/改写 = 0 行**。⇒ 既有断言原文零改写、零削弱。用例数 8 → **9**（`# tests 9 / # pass 9 / # fail 0`）。
- **C-3 `oamp/src/**` 在本轮 pr-005 改动面内零改动：pass**。`git diff --name-only 697176f 575c634 -- oamp/src/` → **空**；`-- oamp/` 的改动面**恰为 3 条**：`oamp/test/inbox-console.test.js`、`oamp/web/app.js`（+9 −2）、`oamp/web/notify.js`（+1 −1）。⇒ 挂起/裁决链路与信封形状零风险，可独立回滚。

---

## D. 终态完整性 —— 4/4 pass

- **D-1（本组）5 个 merge 均为真双亲 merge 且在迭代分支上：pass**。逐条实测 `git rev-list --parents -n 1 <commit>`：
  - `1f7eceb` parents = `7e979ac` + `0d9cbc2`（pr-003，13:21:40）
  - `c84233a` parents = `a3f8068` + `dd7badd`（pr-001，20:45:46）
  - `017961a` parents = `267bc21` + `a431225`（pr-004，20:50:30）
  - `697176f` parents = `0efee9c` + `4d16ba4`（pr-002，21:23:01）
  - `575c634` parents = `b8f4d75` + `9e34e96`（pr-005，21:55:37）
  五者**父数均为 2**（非 fast-forward 单亲）；`git merge-base --is-ancestor 575c634 iteration/0021-…` → **YES**；`git rev-parse HEAD` = `575c634e8f333cd609de591fb70ae4932980a9a9`。
- **D-2 `git worktree list` 仅剩迭代工作区：pass**。恰 2 条（主仓库 + 迭代工作区），无第三项，无 pr-* 工作区。
- **D-3 `.pb-agents/worktrees/0021-pr-*` 已全部清理：pass**。`ls -d .pb-agents/worktrees/0021-pr-*` → **none**；`.pb-agents/worktrees/` 内容实测**仅** `0021-confirmation-inbox-and-event-push/`；`.git/worktrees/` 内容实测**仅** `0021-confirmation-inbox-and-event-push/`。
- **D-4 仓库根干净：pass**。`git status --porcelain` → **空**；仓库根 HEAD 未变（`d84b2be`）。

---

## E. 回归 —— 1/1 pass

`node --test oamp/test/*.test.js`（在**无并发负载**的干净窗口执行，工作区 `575c634`）：

```
# tests 333
# suites 0
# pass 333
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 61305.412209
```

- **真失败 / 已登记 flake（D-5）的区分**：本轮 **0 条失败** ⇒ **无需归因**。首轮登记的 D-5（`status.test.js` 心跳时序，1/9 次复现）本轮**未复现**——按首轮自身的登记口径（「未改动对照树同样复现」），未复现**不构成**对该 flake 的证否，故 D-5 **保持「已记录未闭合」**（见 §F）。
- **改动面相关的定向回归**（本轮另行实测）：`oamp/test/inbox-console.test.js` 9/9、`oamp/test/confirmation-roundtrip.test.js` 20/20、全量 333/333。

---

## F. 偏差台账（截至本轮，供主 agent 汇总）

### F-1 首轮阶段 6 的 7 条

| # | 偏差 | 本轮独立复核结论 | 状态 |
|---|---|---|---|
| **D-1** | 确认项 `tool=null` 时第三栏与通知正文出现字面量 `null` | **用户可见面已在 `575c634` 闭合**（A-1~A-4 实测）；**文档口径半未闭合**（`architecture.md` §7 T-02/T-09、`prd/F01` T-02 仍写「工具名恒在场 / `${tool}：`」，pr-005 显式列为非目标） | **部分闭合**（可见面 ✅ / 文档面 ❌） |
| **D-2** | 经 API **带外裁决**时其它标签页残留陈旧条目 | `grep -rn 'confirmation_resolved\|confirmation_removed' src/web.js web/app.js web/notify.js` → **无此类帧**；`web/app.js:688-701` 的移除仍只在「本页 200 / 404 / 重建」三径 | **未闭合** |
| **D-3** | 4 份 PR 文档「文件范围」与实际改动面不符（pr-001/002 共用 `test/acp-daemon.test.js`、pr-002 实 5 文件、pr-004 实 9 文件） | 阶段 4 产物**未回填**（本轮核实：pr-002/pr-004 的声明字段仍为 4/5 文件）。**注**：pr-005 的文件范围声明**与实现逐条精确相符**（B-A-5） | **未闭合**（不含 pr-005） |
| **D-4** | `status.md`/`history.md` 称「次波 `{pr-002, pr-004}` 并行」，但无磁盘级证据 | 本轮独立时间线复现首轮结论：`pr-002` 分支首提交 `20:52:03`，`pr-004` 已于 `20:50:30` 合并 ⇒ **顺序**；`status.md:39` 原文仍写「次波 = {pr-002, pr-004}」，**未按事实改写** | **未闭合** |
| **D-5** | `history.md` 断言「全量 322/322 两次连跑全绿（无 flake）」 | 本轮 333/333 **未复现**；首轮 1/9 复现、用例名未捕获 ⇒ 仍属**未定位** | **未闭合**（本轮未复现） |
| **D-6** | 「新增路由 ⇒ `llms.txt` 必须重生成」这一漂移锁的触发条件无独立记录 | 无变化（既有 `api-routes.test.js`/`call-protocol.test.js` 已锁死） | **未闭合**（信息性） |
| **D-7** | `status.md` 的「已派发总数」把**阶段 6 的验证自身**计入（自指计数，阶段 6 期间不收敛） | `status.md:37` 现值为 `23（…；阶段 6：verifier×1 已计入 verifier 计数）` ⇒ **自指口径仍在**，未改为「阶段 5 已派发总数」 | **未闭合** |

### F-2 pr-005 验收的 4 条（本轮独立复现其形态）

| # | 偏差 | 本轮独立复现（真实 DOM / 真实脚本沙箱） | 状态 |
|---|---|---|---|
| **P1** | 「存在的段才拼」被泛化到 `title` 轴：`tool` 非空而 `title` 为 `null` 时，旧版 `"bash · null"` → 新版 `"bash"` | 实测 `inboxRequest({tool:'bash',title:null})` = `"bash"`（旧版为 `"bash · null"`） | **未闭合**（判定为非缺陷，属规则泛化） |
| **P2** | 两侧对 `''` 口径不一致：`app.js` 仅判 `null`/`undefined` ⇒ `tool:''` 产出**前导悬空 `·`**；`notify.js` 用真值判断 ⇒ 已省略 | 实测 `inboxRequest({tool:'',title:'echo X'})` = `" · echo X"`；`{tool:'',title:''}` = `" · "` ⇒ **悬空分隔符确实存在**（正常链路不可达：`agent.js:328` 把空串折叠为 `null`） | **未闭合**（不可达） |
| **P3** | 通知侧 `title` 段不参与存在性判定：`tool` 在场而 `title` 缺失时留**尾随 `：`** | 实测 `onEvent('confirmation_required', {tool:'bash', title:''})` → `"pb-dev 请求执行 bash："`；`title:null` 同此 | **未闭合**（不可达） |
| **P4** | 文档口径仍描述「工具名恒在场」（`architecture.md` §7 T-02/T-09、`prd/F01` T-02） | 与 F-1/D-1 的文档半**同源**，本轮合并计为**同一项未闭合**（D-1 文档面） | **未闭合** |

### F-3 本轮新增（3 条）

| # | 偏差 | 证据 | 建议处理 |
|---|---|---|---|
| **R2-1** | **终态工作区含 1 项 docs-only 未提交写入**：` M docs/…/history.md`（提交前与报告落盘前两次实测均为该项）。⇒ 合并动作将带上「`575c634` 不含该文本」的既成事实；不属产品代码缺陷 | `git status --porcelain`（worktree）→ 仅 ` M …/history.md`；`git rev-parse HEAD` = `575c634`；仓库根 `git status` 为空 | 合并前由主 agent 决定「先提交该台账再合并」或「合并后补提交」；两者均不影响 `oamp/**` |
| **R2-2** | **栏内行与通知正文对「falsy 非 null」值的口径不一致**：`tool:0` 时栏内行为 `"0 · echo X"`（保留），通知正文为 `"pb-dev 请求执行 echo X"`（省略） | 实测 `inboxRequest({tool:0,title:'echo X'})` = `"0 · echo X"`；`onEvent(…,{tool:0,…}).body` = `"pb-dev 请求执行 echo X"` | 低。正常链路**不可达**（`agent.js:328` 仅接受字符串）；若采纳 F-3 的统一口径，可在两处同改 |
| **R2-3** | **`title:''` 可达且产尾随悬空 `·`**：`agent.js:329` 对 `title` 仅按 `typeof === 'string'` 折叠，**不折叠空串**（与 `:328` 对 `tool` 的折叠不对称） | `oamp/src/agent.js:329` 原文；实测 `inboxRequest({tool:'bash',title:''})` = `"bash · "`。omp 是否可能发 `title:''` **未观测到** `[INFERENCE]` | 低。若要钉死，在 `agent.js:329` 增加空串折叠（与 `:328` 对称）；属**生产侧**改动，须先确认是否需要 |

> **台账总计**：仍处「已记录未闭合」的偏差 = 首轮阶段 6 **7 条**（D-1~D-7；D-1 为「部分闭合」）+ pr-005 验收 **4 条**（P1~P4）+ 本轮新增 **3 条**（R2-1~R2-3）⇒ **共计 14 条行项**；其中 D-1 的文档口径半与 pr-005 的 P4 为**同一事实** ⇒ **去重后 13 条事实**。所有行项均**未闭合或部分闭合**，**无一被本轮判定为 fail**——按 verifier 契约，偏差被显式记录即满足交付条件。

---

## 汇总

| 判定 | 计数 | 明细 |
|---|---|---|
| **pass** | **48** | A×4（A-1~A-4）+ B×35（PR 粒度 5 + 依赖 6 + 并发 3 + 功能卡 12 + M4 4 + 端到端 2 + 回归洁净 3）+ C×3 + D×4 + E×1 + F×1 |
| **fail** | **0** | — |
| **partial** | **0** | — |
| **blocked** | **0** | — |

> **计数口径**：A = 4 条（D-1 闭合复核四项）；B = 35 条（B-A 5 / B-B 6 / B-C 3 / B-D 12 / B-E 4 / B-F 2 / B-G 3）；C = 3 条；D = 4 条；E = 1 条（全量回归）；F = 1 条（偏差台账完备性）。**合计 48 条，全部 pass**。与首轮 33 条的对应关系：首轮 A×4 在本轮扩为 B-A×5（+pr-005）；首轮 B×5 扩为 B-B×6（+pr-005 依赖专项）；其余 C/D/E/F/G 逐条重建。

---

## 偏差记录

> 完整台账见 §F（13 条，去重 12 条）。**均不阻塞本次判定**；其中与「合并进 main」直接相关的仅 R2-1（docs-only 未提交写入）与首轮报告已登记的 `oamp/src/agent.js` 合并冲突（见结论）。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| **D-1（文档半）** | `architecture.md` §7 T-02「工具名 + `title`」、§7 T-09「`${agent_id} 请求执行 ${tool}：${title}`」、`prd/F01` T-02「② 工具名 + `title`」 | 实现改为「缺失即整段省略」⇒ 文档与实现存在口径差（**可见面已按 pr-005 修好**，仅文档未回填） | 收口后补一条文档同步（回填「`tool` 缺失即省略该段」） |
| **D-2** | `prd/F03` 验收 4「裁决完成后该条从栏内消失」（未限定位置） | 经 API **带外裁决**时其它标签页残留陈旧条目至刷新/重连 | 补一帧全局「裁决完成」或在 `prd/F03` 边界写明「裁决面 = 当前控制台页」 |
| **D-3** | 4 份 PR 文档「文件范围」 | pr-002 实 5 文件、pr-004 实 9 文件、`test/acp-daemon.test.js` 跨 PR（Q6/Q7 已授权） | 按实现回填声明字段 |
| **D-4** | `status.md`「首波可并发 = {pr-001, pr-003}，次波 = {pr-002, pr-004}」 | 首波并发有 git 对象级证据；**次波无据且时间线相反**（pr-002 于 pr-004 合并后起飞） | 台账改为「首波并发；次波顺序」 |
| **D-5** | `history.md`「全量 322/322 两次连跑全绿（无 flake）」 | 首轮 1/9 次 330/332（用例名未捕获）；本轮 333/333 未复现 | 定位并钉死时序窗口；停止写「无 flake」类绝对断言 |
| **D-6** | 派生面漂移锁（新增路由 ⇒ `llms.txt` 重生成） | 与实现一致，但触发条件无独立记录 | 忽略（既有测试已锁死）；仅登记 |
| **D-7** | `status.md`「已派发总数」（自指计数） | 把阶段 6 的验证自身计入，阶段 6 期间不收敛 | 单列阶段 6 派发行，或改名为「阶段 5 已派发总数」 |
| **P1** | 「`tool` 可得时输出逐字不变」（未限定 `title`） | 「存在的段才拼」泛化到 `title` 轴（`bash · null` → `bash`） | 判定为非缺陷；建议在文档中显式登记该泛化 |
| **P2** | 两侧空值口径一致性 | `app.js` 判 `null`/`undefined`、`notify.js` 判真值 ⇒ `tool:''` 时栏内行留前导悬空 `·` | 统一为「非空字符串才算在场」；不可达，可延后 |
| **P3** | 通知侧 `title` 段对称性 | `tool` 在场而 `title` 缺失 ⇒ 尾随悬空 `：` | 与 P1 同规则对称化；不可达，可延后 |
| **P4** | 文档口径「工具名恒在场」 | 见上表 D-1（文档半）——**同一事实** | 与 D-1 合并处理 |
| **R2-1** | 终态应可复现（= 已提交内容） | worktree 有 1 项 docs-only 未提交写入（`history.md`） | 合并前后补提交，由主 agent 择时 |
| **R2-2** | 两处消费面同一口径 | `tool:0` 时栏内行保留 `0`、通知省略 ⇒ 侧间不一致 | 低；不可达（信封只接受字符串） |
| **R2-3** | 空串折叠口径对称 | `agent.js:329` 对 `title` 不折叠空串 ⇒ `title:''` 可达并产尾随悬空 `·` | 低；若需钉死则在 `:329` 补空串折叠（生产侧改动，须先确认必要性） |

**偏差记录条数 = 14 行 / 去重 13 条事实**（D-1 文档半与 P4 为同一事实）。

---

## 下一迭代候选

> 不在本轮验收标准范围内，供主 agent 与 planner 决策。按「用户可见收益 ÷ 代价」排序。

1. **D-1 文档面回填**：`architecture.md` §7 T-02 / §7 T-09 与 `prd/F01` T-02 补记「`tool` 缺失即省略该段」。与 pr-005 的实现口径绑定，代价极小。
2. **P2/P3 空值口径统一**：把 `oamp/web/app.js` 的存在性判定提升为「非空字符串」，并在通知侧对 `title` 段做同样的存在性判定；两根悬空分隔符（前导 `·` / 尾随 `：`）一次消除。
3. **D-2 跨标签页陈旧条目**：补一条「裁决完成」全局帧（或复用失效帧）驱动跨页移除。在此之前多页同开是可达的陈旧态。
4. **D-4 台账口径订正**：`status.md` / `history.md` 按 git 时间线改写为「首波并发；次波顺序」，避免下轮复核重复命中。
5. **D-5 flake 定位**：把 333 用例中与时序窗口相关的用例钉成确定性等待；并在台账中停用「无 flake」绝对断言。
6. **D-3 PR 文件范围回填**：使阶段 4 产物与阶段 5 实现对齐（对下游「文件范围无重叠」的自动核对有意义）。
7. **D-7 计数口径固定**：把「已派发总数」限定为阶段 5，阶段 6 派发单列。
8. **R2-3 空串折叠对称化**：`agent.js:329` 对 `title` 增加空串折叠（与 `:328` 对称）——**属生产侧改动**，须先评估必要性（当前未观测到 omp 发空 `title`）。
9. **F01 栏内排序 / 分组 / 条数上限**：仍按既有裁定保持出界（回退口 = 回 `demand.md` 补一条 W）。

**下一迭代候选条数 = 9**。

---

## 集成事实（不属 A~F 计数，但决定「合并进 main」这一步能否直接执行）

- **迭代分支的可合并性**：`git merge-base main iteration/0021-…` = `854766c`；**main 已前进 1 个提交 `d84b2be`**（「omp 单轮超时默认与上限 5 分钟 → 30 分钟」），其改动的文件与本迭代改动**重叠 3 个**（`acp-client.js` / `agent.js` / `web.js`），其中 `oamp/src/agent.js` 存在**内容冲突**（同一常量块；首轮报告已实测 `git merge-tree` 退出码 1 并给出冲突原文）。冲突解决方式 = 「取 main 的 30 分钟取值 + 保留本迭代新增的 `CONFIRMATION_TITLE_MAX`」，与本迭代的挂起/裁决语义无关。
- **本轮对终态代码的复核不受该冲突影响**：`575c634` 在其 base 上自洽（333/333 绿、pr-005 集成零回归、D-1 可见面闭合、M4 双向与端到端全链实测通过）。
- ⇒ **「迭代可收口」= 是；「合并进 main」= 可合并，但必须处理 1 处内容冲突（3 行常量）后 `git merge` 才能完成**，且需处置 R2-1（docs-only 未提交写入）。PASS 判定**不含**「零冲突合并」这一强断言。

---

## 结论

**PASS**（48 条判定：**48 pass / 0 fail / 0 partial / 0 blocked**）

- **偏差 D-1 已闭合（用户可见面）**：在**新终态 `575c634`** 上，以**真实渲染面**（真实 SSE 帧驱动未改动的 `app.js` → 真实 DOM；真实 `Notification` 构造实录）与**端到端真机链路**（真实 omp → agent → web → 浏览器）取证——`tool` 为 `null` / 缺键时，第三栏行与浏览器通知正文**均不出现字面量 `null`/`undefined`**，且**无悬空分隔符**；`tool` 有值时输出与首轮阶段 6 所用版本（`697176f`）**逐字节一致**；**未引入**任何 `title→tool` 的猜测性派生。D-1 的**文档口径半**仍开放，已如实登记（不属可见面缺陷）。
- **首轮阶段 6 的结论在新终态上仍成立**：35 条逐条重建（PR 粒度 5 / 依赖 6 / 并发 3 / 功能卡 12 / M4 4 / 端到端 2 / 回归洁净 3），每项均附**本次**取证；其中新增 pr-005 的 PR 粒度、`depends_on` 代码级证据均为本轮独立判定。
- **pr-005 集成无回归**：契约测试零改动且 20/20 绿；既有断言 48/0 纯新增、364 行按序零改写；`oamp/src/**` 零改动面。
- **终态完整**：5 个 merge 均为真双亲且都在迭代分支上；仅剩迭代工作区；pr-* 工作区已清理；仓库根干净。
- **回归**：`node --test oamp/test/*.test.js` → **333/333 通过、0 失败**（本轮未复现 D-5 flake，故无需归因）。
- ⚠ **合并动作附条件**：`oamp/src/agent.js` 存在 1 处内容冲突（3 行超时常量，源自 main 的 `d84b2be`），且工作区有 1 项 docs-only 未提交写入（R2-1）。**「可合并进 main」= 是（需处理该冲突与该项提交）**；本 PASS 指的是**迭代产物本身可收口**。

---

## 清理与残留

| 资源 | 处置 |
|---|---|
| hub 进程 `vr2-router` / `vr2-web` / `vr2-agent` / `vr2-static` / `vr2-base` | **已全部 stop**（均 `exited`） |
| 端口 `8921`（终态静态面）/ `8922`（基线静态面）/ `8923`（几何实测）/ `8931`、`8932`（真实 console） | `lsof -nP -iTCP:<port> -sTCP:LISTEN` 逐个复检 → **全部 released** |
| 浏览器 | `browser close --all --kill` → `Released 3 managed tabs` / 再次 `Released 1 managed tab`（工具托管页，无沙箱外副作用） |
| `/tmp/vr2/`（探针、矩阵产物、基线 web 副本、SSE 帧文件） | **已 `rm -rf`**（复检 `No such file or directory`） |
| `/tmp/vr2-e2e/`（socket / db / 工作目录） | **已 `rm -rf`**（复检 `No such file or directory`） |
| 遗留探针进程 | `ps aux \| grep '[v]r2' \| wc -l` → **0** |
| 仓库根 `/Users/chenchiyuan/projects/agents` | **零写入**（`git status --porcelain` 为空，复检两次） |
| 迭代工作区 | 唯一写入 = 本报告文件（`clarifications/verify-stage6-r2-<ts>.md`，新增未跟踪）；未 commit / checkout / stash；未修改任何被验证产出物 |
