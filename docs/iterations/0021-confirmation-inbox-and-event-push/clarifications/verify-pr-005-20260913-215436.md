# verify-pr-005 — pr-005（确认项 `tool` 为 `null` 的消费侧最小修复）独立验证报告

**验证者身份（反射结果）**：浏览器端消费侧渲染 / 通知面实现审查者（front-end consumer-side rendering & notification reviewer）——具备 ① 真实 DOM 渲染实测能力（headless Chromium + 真实 `app.js` / `notify.js` 经典脚本加载）、② node:test / node:vm 契约测试与变异（mutation）敏感性分析能力、③ 前端输出字节面（`innerHTML` / `textContent` / 转义）比对能力。本类产出物（消费侧空值语义 + 断言承载）的判定权在「产出文本」而非实现形式，故验证以**产出文本的可复查实测**为准。

**产出物（待验证）**
1. 代码实现：worktree `…/0021-confirmation-inbox-and-event-push/.pb-agents/worktrees/0021-pr-005-null-tool-name-fix`，分支 `feat/0021-pr-005-null-tool-name-fix`，HEAD `9e34e96e0c03eb8b1f5ffc2b9b759539b82b949c`（实测 `git rev-parse` 与简报一致），改动面 = `git diff 697176f...HEAD`
2. PR 契约：`docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-005-null-tool-name-fix.md`（8 条验收标准）
3. 任务图：`docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-005-tasks.md`（T1~T3 验收 + §4 C1~C10）
4. 需求源头（只读参照）：`prd/F01-confirmation-inbox-column.md`、`prd/F08-notification-delivery.md`；既有契约只读参照：`oamp/test/confirmation-roundtrip.test.js:445-451`

**验证标准来源**：主 agent 委托书 A(8) + B(T1~T3) + C(4 红线) + D(5 行为面) + E(4 回归)；产出物内自评文字一律不作为判定依据。

**验证日期**：2026-09-13 ｜ **环境**：node v22.15.0，headless Chromium（真实加载 worktree 内 `oamp/web/*.js`，静态服务 `127.0.0.1:8907`）

**独立性声明**：本次验证只接收产出物路径与验证标准；未使用任何执行过程上下文（也未使用产出物内自评）。被验 worktree 全程只读：未 commit / checkout / stash；`git status --porcelain` 验证后仍为空。临时资源全部置于 `/tmp` 并已清理（见文末）。

---

## A. PR 文件 8 条验收标准（逐条判定）

| # | 验收标准 | 判定 | 证据 |
|---|---|---|---|
| A1 | 缺 `tool` 不产伪值（栏内行）：`null` / 无键 ⇒ `echo E2E-1`；`{tool:null,title:null}` ⇒ 空串；均不含 `null`/`undefined`、无悬空 `·` | **pass** | 真实 DOM（HEAD 页 `http://127.0.0.1:8907/index.html`，调用页面内真实 `window.renderInboxItem` 后读 `.inbox-request`）：`tool:'bash'`→`"bash · echo E2E-1"`；`tool:null`→`"echo E2E-1"`；**无 `tool` 键**→`"echo E2E-1"`；`{tool:null,title:null}`→`""`（DOM `<div class="inbox-request"></div>`）。自有 20 格矩阵探针（`/tmp/pr005-probe.mjs`，自研大括号配对提取 `escapeHtml` / `inboxRequest` 后 `node:vm` 执行）全格 `hasLiteralNullOrUndefined=false`、三态 `danglingDot=false`。对照 base 同页：`null · echo E2E-1` / `undefined · echo E2E-1` / `null · null`（缺陷确实存在，本 PR 将其消除）。 |
| A2 | `tool` 可得时栏内逐字不变（`bash · echo E2E-1`） | **pass** | 真实 DOM 的 `.inbox-item` `outerHTML` 在 HEAD 与 base 两页对 `{tool:'bash',title:'echo E2E-1',…}` **逐字相同**（两页实测字符串一致：`…<div class="inbox-request">bash · echo E2E-1</div>…`）。矩阵探针：`tool=bash,title=echo E2E-1` / `tool=html,title=echo E2E-1` 等「两段皆在场」格 `identical=true`。 |
| A3 | 转义面不变：`title='<img src=x onerror=1>'` ⇒ 含 `&lt;img`、不含 `<img` | **pass** | HEAD 页实测 DOM `innerHTML` = `<div class="inbox-request">&lt;img src=x onerror=1&gt;</div>`（`<`、`>` 均被转义；`textContent` 呈字面文本）。附加探针：`tool='<b>'` ⇒ `&lt;b&gt; · x`（工具段同样转义）；`tool='a"b<c>&d'` ⇒ `a"b&lt;c&gt;&amp;d · t'q`，与 base 同格 `identical=true`。 |
| A4 | 缺 `tool` 不产伪值（通知正文）：`null` / 无键 ⇒ `pb-dev 请求执行 echo hi`；不含 `null`/`undefined`，无悬空 `：` | **pass** | 真实页 `window.oampNotify.service.onEvent('confirmation_required', …).body`：`tool:null`→`"pb-dev 请求执行 echo hi"`；无键→同值；输出无 `null`/`undefined`、无 `：`。base 同页对照：`"pb-dev 请求执行 null：echo hi"` / `"pb-dev 请求执行 undefined：echo hi"`。自有 notify 矩阵探针（`/tmp/pr005-notify-probe.mjs`，两个独立 vm 沙箱分别执行 HEAD / base 的 `notify.js`）复现同结论，全格 `hasLiteralNullOrUndefined=false`。 |
| A5 | `tool` 可得时通知正文逐字不变（含 80 字符截断 + `…`） | **pass** | HEAD `{agent_id:'pb-dev',tool:'bash',title:'echo hi'}` ⇒ `"pb-dev 请求执行 bash：echo hi"`，与 base **逐字相同**；`title='x'×100` ⇒ `"pb-dev 请求执行 bash：<80×x>…"`（探针计数 `longTitleXCount=80`，`identical=true`）。notify 矩阵中「`tool` 非空」4/4 格 `identical=true`（`nonEmptyToolMismatches=0`）。既有断言（base 编号 `:188` / `:202`，现 `:195` / `:209`）原文未改写且绿。 |
| A6 | 新增断言是行为断言（执行被测路径，非源码文本匹配） | **pass** | 变异敏感性实测（把被测实现换成旧值/坏值后新用例必须失败）：① base 版 `app.js`+`notify.js` ⇒ **fail**（`web/app.js 应含顶层函数 inboxRequest`）；② 仅 base `app.js` ⇒ fail；③ 仅 base `notify.js` ⇒ fail，报 `expected: 'pb-dev 请求执行 echo hi' actual: 'pb-dev 请求执行 null：echo hi'`；④ 值级变异（把 `inboxRequest` 的 `.filter((s) => s !== null && s !== undefined)` 改成 `.filter(() => true)`）⇒ fail，报 `expected: 'echo E2E-1' actual: 'null · echo E2E-1'`。被测路径确为执行形态：`node:vm.runInNewContext` 提取函数体后调用（`oamp/test/inbox-console.test.js:94-98`、`:243-280`）。 |
| A7 | 零回归 | **pass** | `node --test oamp/test/inbox-console.test.js` → **9 pass / 0 fail**；`node --test oamp/test/confirmation-roundtrip.test.js` → **20 pass / 0 fail** 且该文件 `git diff --name-only 697176f -- …` 为空；`node --test oamp/test/*.test.js` → **333 pass / 0 fail**（无新增失败，亦无失败需归因 flake，见 E 组）。 |
| A8 | 信封零改动（改动面恰 3 条路径） | **pass** | `git diff --name-only 697176f HEAD -- oamp/` 输出**恰三行**：`oamp/test/inbox-console.test.js`、`oamp/web/app.js`、`oamp/web/notify.js`；`git diff --name-only 697176f HEAD -- oamp/src/` 为**空**。注：全量 `git diff --name-only 697176f...HEAD` 另含 2 条 `docs/**` 工作流产物（PR 契约 + 任务图，状态 `A`），按任务图 §4-C8 / §5 疑问 7③ 的判据面收敛（代码面 `-- oamp/`）判定，记录于本报告证据栏而非 fail。 |

---

## B. `prs/pr-005-tasks.md` T1~T3 全部验收标准（逐条判定）

### T1（消费侧空值语义落地）

| # | 验收标准 | 判定 | 证据 |
|---|---|---|---|
| T1.1 | 通知正文：`tool` 缺失不产伪值 | **pass** | 同 A4（真实页 + notify 矩阵探针）。 |
| T1.2 | 通知正文：`tool` 可得时逐字不变（含 80 字符截断） | **pass** | 同 A5；既有断言 `:195` / `:209` 未改写且绿。 |
| T1.3 | 栏内行：`tool` 缺失不产伪值（三态） | **pass** | 同 A1（真实 DOM 三态 + 20 格矩阵）。 |
| T1.4 | 栏内行：`tool` 可得时逐字不变 | **pass** | 同 A2。 |
| T1.5 | 转义面不变（每段先 `escapeHtml` 再拼） | **pass** | 同 A3；实现 `oamp/web/app.js:634-639` 为 `segs.map(escapeHtml).join(' · ')`，判据以输出为准（`&lt;img`/不含 `<img`）。 |
| T1.6 | 两处产出均可被「执行」而非文本匹配；`inbox-request` 字面量仍留在 `renderInboxItem` 体内 | **pass** | `oamp/web/app.js:638` 顶层纯函数 `inboxRequest(entry)`（单参、参数表无嵌套括号 ⇒ 既有 `fnBody` 正则可提取）；`<div class="inbox-request">` 包装仍在 `renderInboxItem` 体内（`oamp/web/app.js:647`）。既有断言 `oamp/test/inbox-console.test.js:333` `assert.match(item, /inbox-request/)` 未改写且绿 ⇒ 相容性约束（C4）成立。 |
| T1.7 | 两处同一口径（`null` \| `undefined` 视为缺失；空串不特判） | **pass** | 两处实现：「键缺失 / `null` ⇒ 省略」（`app.js:637` 的 `s !== null && s !== undefined`；`notify.js:40` 的真值判断）。空串不特判与信封层折叠（`oamp/src/agent.js:328` 把非字符串/空串折叠为 `null`）一致；消费侧空串行为差异见**偏差记录 2 / 3**（不在验收面，且非回归）。 |

### T2（新增三态行为断言）

| # | 验收标准 | 判定 | 证据 |
|---|---|---|---|
| T2.1 | 新增断言实际执行被测路径，三态期望值可区分 | **pass** | 同 A6 四项变异实测（旧实现必失败、值级破坏必失败）；用例内含显式可区分性断言 `assert.notEqual(rows[0], inboxRequest({tool:'bash',…}))`（`:260`）与 `assert.notEqual(bodyOf({…tool:'bash'}), bodyOf(base))`（`:279`）。 |
| T2.2 | 断言点覆盖「两处 × 三态 + 两处逐字不变 + 转义面」 | **pass** | `oamp/test/inbox-console.test.js:243-280`：栏内行三态（`:247-252`）+ 悬空 `·` / 字面量否定（`:254-258`）+ 转义两格（`:263-266`）；通知三态（`:271-273`）+ 字面量 / 悬空 `：` 否定（`:275-278`）。 |
| T2.3 | 既有断言原文零改写 | **pass** | `git diff --numstat 697176f...HEAD -- oamp/test/inbox-console.test.js` = **48 / 0**（纯新增，零删除行）；另有程序化校验：base 版 365 行**逐行按序全部出现在** HEAD 版中（`missing/rewritten: 0`）；用例数 8 → 9。`oamp/test/confirmation-roundtrip.test.js` 零 diff。`notificationStub`（`:80-90`）与 `fnBody`（`:57-62`）等既有 harness 未被触碰。 |
| T2.4 | 落盘形态对齐后定稿（检索式先校验命中） | **pass** | 新断言用的检索式 / 函数名与落盘实现一致（`fnBody(src,'inboxRequest')` 命中 `app.js:638`；`loadNotify` 命中 `notify.js` 的 `window.oampNotify`），隔离运行 `--test-name-pattern "消费侧空值语义"` = **1 pass / 0 fail**；未出现「检索式未命中」类失败（对照：该文件其余 8 条既有用例同批全绿）。 |
| T2.5 | `node --test oamp/test/inbox-console.test.js` 全绿 | **pass** | 9 pass / 0 fail（实测输出 `# tests 9 / # pass 9 / # fail 0`）。 |

### T3（收口回归与边界审计）

| # | 验收标准 | 判定 | 证据 |
|---|---|---|---|
| T3.1 | 改动面恰三条**代码**路径 | **pass** | 同 A8（`-- oamp/` 判据面）。 |
| T3.2 | 契约测试零改动且全绿 | **pass** | `git diff --name-only 697176f -- oamp/test/confirmation-roundtrip.test.js` 为空；该文件 **20 pass / 0 fail**。钉死点均在：`:449` `assert.equal(env1.tool, null)`（用例名 `…不得用 title 猜测`）、`:459` `env1.tool === 'write'`、`:419` 信封键集合含 `tool`。 |
| T3.3 | 回归无新增失败（扣除已登记 flake） | **pass** | `node --test oamp/test/*.test.js` → **333 pass / 0 fail**。本轮**未复现** D-5（`status.test.js` 心跳时序）flake，故无失败需要归因；改动面相关文件（`inbox-console` / `confirmation-roundtrip` / `confirmation-inbox`）全绿。 |
| T3.4 | 「修在消费侧」的反证 | **pass** | `git diff --name-only 697176f -- oamp/src/` 输出为空 ⇒ 信封取值口径未被松动，既有 `tool=null` 契约完好。 |
| T3.5 | PR 八条验收逐条对位且有可复现证据 | **pass** | 本报告 A 组 A1~A8 逐条给出命令 / DOM 实测 / 前值→后值对照。 |

---

## C. 红线（4 条）

| # | 红线 | 判定 | 证据 |
|---|---|---|---|
| C1 | 改动面 `oamp/**` 恰 3 条路径；`oamp/src/**` 与 `confirmation-roundtrip.test.js` 零改动 | **pass** | `git diff --name-only 697176f HEAD -- oamp/` = 3 条；`-- oamp/src/` 为空；`-- oamp/test/confirmation-roundtrip.test.js` 为空。 |
| C2 | 既有断言原文零改写、未削弱（尤 `fnBody` / `inbox-request` 字面量依赖与 `notificationStub` 断言） | **pass** | numstat **48 / 0**；base 365 行按序全在场（`missing/rewritten: 0`）；`fnBody` 正则（`:57-62`）与 `notificationStub`（`:80-90`）未改；依赖 `inbox-request` 字面量的 `:333` 与 `tool='bash'` 正文断言 `:195` / `:209` 原文在场且通过（9/9）。 |
| C3 | 零新依赖 / 零构建（不得引入 import/export、不得新增运行时依赖） | **pass** | `oamp/web/app.js`、`oamp/web/notify.js` 内 `^\s*(import\|export)\s` **无命中**（仍为零模块语法经典脚本）；`git diff --name-status 697176f...HEAD` 全为 `A`(2 条 docs 工作流产物) / `M`(3 条代码)，无新增文件；`package.json` 不在 diff；测试仅沿用既有 `node:test` + `node:vm` + harness 体例（文件头 `:10-17` 既有 import 未增改）。 |
| C4 | 仓库根与被验 worktree 干净 | **pass** | worktree `git status --porcelain` 为空（验证前后均空）；仓库根 `/Users/chenchiyuan/projects/agents` `git status --porcelain` 为空（0 条）。本报告唯一写入目标（迭代工作区 `clarifications/`）不在仓库根受控面内被改动的既定文件之列。 |

---

## D. 行为面独立实测（自有方式，非 PR 自带测试）

**实测手段**：① 真实 headless Chromium 分别加载 worktree HEAD 与 `697176f` base 的 `oamp/web`（静态服务），在页面上下文调用**页面内真实**的 `window.renderInboxItem` / `window.inboxRequest` / `window.oampNotify.service`，读取真实 DOM（`innerHTML` / `textContent`）与真实返回值；② 自研 `node:vm` 探针（自写大括号配对提取器，**不复制**测试文件的 `fnBody`）对 HEAD 与 base 实现做 20 格（栏内行）/ 16 格（通知）矩阵比字节。

| # | 判据 | 判定 | 证据（HEAD / base 对照） |
|---|---|---|---|
| D1 | 栏内行不得出现字面量 `null`/`undefined`、不得留悬空 `·` | **pass** | HEAD 真实 DOM：`tool:null`→`echo E2E-1`；无键→`echo E2E-1`；双 `null`→``（空）。base 同页：`null · echo E2E-1`、`undefined · echo E2E-1`、`null · null`。矩阵 20 格 `hasLiteralNullOrUndefined=false`；三态 `danglingDot=false`。 |
| D2 | `tool` 有值时输出与 base **逐字相同** | **pass**（判据边界见右） | 「两段皆在场」全部格**逐字节相同**：真实 DOM `tool='bash'` 的 `.inbox-item` `outerHTML` 两页一致；矩阵 `identical=true`（`bash · echo E2E-1`、`&lt;b&gt; · echo E2E-1`、含 `<img>` 转义档等）；notify 侧「`tool` 非空」4/4 格 `identical=true`。**边界**：`tool` 非空但 **`title` 缺失**（`null`/无键）的 4 格与 base 不同（HEAD 得 `bash`，base 得 `bash · null`）——这是 PR 文件范围明示的「**存在的段才拼**」对 `title` 轴的同一化泛化，非 `tool` 轴漂移，判决见**偏差记录 1**。 |
| D3 | HTML 转义面不变 | **pass** | HEAD `innerHTML`：`&lt;img src=x onerror=1&gt;`（`<`/`>` 均转义）；`tool='<b>'`→`&lt;b&gt; · x`；引号 / `&` 档 `a"b&lt;c&gt;&amp;d · t'q` 与 base 同格 `identical=true`。转义函数本体零改动（`git diff` 中 `escapeHtml` 不在增删侧，仅行号平移 `:1041`→`:1048`）。 |
| D4 | 通知正文同样三态（不得 `null`/`undefined`、不得留悬空 `：`） | **pass** | HEAD：`tool:null`→`pb-dev 请求执行 echo hi`；无键→同值；`tool:''`→同值；输出无 `null`/`undefined`、无 `：`。base：`…null：echo hi`、`…undefined：echo hi`、`… ：echo hi`（悬空冒号）。 |
| D5 | 缺 `tool` 时是**省略该段**，而非用 `title` 猜工具名（不得引入猜测性派生） | **pass** | ① 源码级：改动 hunk 中 `tool` 段只由 `entry.tool` / `p.tool` 本身决定（`app.js:637`、`notify.js:40`），**无任何** `title`→`tool` 的映射 / 回填 / `kind` 词表派生（diff 中不存在此类新增行）；② 行为级：缺 `tool` 时输出**恰为 `title` 本人**（`{title:'echo E2E-1'}`→`echo E2E-1`，未重复 / 未变形 / 未加工具名词）；③ 契约级：`oamp/test/confirmation-roundtrip.test.js:445-451`（「不得用 title 猜测」）**零改动且绿**，信封侧亦零改动（`oamp/src/**` 零 diff）。 |

---

## E. 回归（4 组）

| # | 命令 | 判定 | 结果 |
|---|---|---|---|
| E1 | `node --test oamp/test/inbox-console.test.js` | **pass** | `# tests 9 / # pass 9 / # fail 0`（既有 8 + 新增 1） |
| E2 | `node --test oamp/test/confirmation-roundtrip.test.js`（零改动且绿） | **pass** | `# tests 20 / # pass 20 / # fail 0`；`git diff --name-only 697176f -- <该文件>` 为空 |
| E3 | `node --test oamp/test/confirmation-inbox.test.js` | **pass** | `# tests 6 / # pass 6 / # fail 0`（该文件不在改动面） |
| E4 | `node --test oamp/test/*.test.js` | **pass** | `# tests 333 / # pass 333 / # fail 0`（无失败 ⇒ **本轮无需归因** D-5 flake；`status.test.js` 心跳时序 flake 本轮未复现，与「未改动对照树同样复现」的登记口径不冲突） |

---

## 汇总

| 判定 | 计数 |
|---|---|
| **pass** | **38**（A 8 + B 17〔T1 7 + T2 5 + T3 5〕 + C 4 + D 5 + E 4） |
| fail | 0 |
| partial | 0 |
| blocked | 0 |

---

## 偏差记录

| # | 规格描述 | 实现行为 | 建议处理 |
|---|---|---|---|
| 1 | PR 文件 验收 2 / 任务图 T1 验收 4：「`tool` 可得时输出逐字不变」（未限定 `title` 是否在场）；`tool` 轴的“逐字不变”语义即本 PR 的最小面承诺 | 实现按 PR 文件范围写的规则「**存在的段才拼**」泛化到了 `title` 轴：`tool` 非空而 `title` 为 `null` / 键缺失时，HEAD 得 `bash`，base 得 `bash · null`（4 格；矩阵探针 `tool=bash,title=null` / `tool=bash,title=missing` / `tool=html,title=null` / `tool=html,title=missing`）。该行为**不在 PR 8 条验收的任何一条**（各条均固定 `title` 在场），且消除了 `title` 轴的同类伪值 | 判定为**非缺陷**：这是 PR 文件范围明示的同一化规则（消除伪值）而非 `tool` 轴漂移，且无既有断言 / 契约覆盖 `title` 缺失的渲染；建议在 PR 文件或架构口径中把该泛化显式登记为「两段皆按存在性拼装」，避免下轮验收重复讨论 |
| 2 | 任务图 C7：「`''` 已被信封层折叠为 `null` ⇒ 消费侧不必特判（特判亦无害）」 | 侧间不一致：`oamp/web/notify.js:40` 用真值判断，`tool:''` 与缺省同处理（`pb-dev 请求执行 echo hi`）；`oamp/web/app.js:637` 仅判 `null`/`undefined`，`tool:''` 被当作在场段产出**前导悬空 `·`**（真实 DOM 实测 `" · echo E2E-1"`）。base 行为相同（`identical=true`）⇒ **非回归**，且 `oamp/src/agent.js:328` 已把空串折叠为 `null`，正常链路不可达 | 不在本 PR 验收面，无需返工；建议下轮把两侧对 `''` 的口径统一（或在 `inboxRequest` 一并按「非空字符串才算在场」判） |
| 3 | 同一「存在的段才拼」规则在通知侧未对称落地 | `notify.js` 的 `title` 段由 `clip(p.title)` 统一产出（非字符串 ⇒ 空串）且无条件拼接：`tool='bash'` 而 `title` 缺失时产出 `pb-dev 请求执行 bash：`（尾随 `：` 无内容）。与 base **逐字相同**（`identical=true`）⇒ 非回归，亦不在 PR 验收 4/5（验收 4 只管 `tool` 缺失态） | 记录为已知边界；若采纳偏差 1 的「显式登记」，应同时说明通知侧 `title` 段不参与存在性判定，或列为下一迭代候选 1 |
| 4 | 文档口径仍描述「工具名恒在场」：`architecture.md` §7 T-02 / §7 T-09（`${agent_id} 请求执行 ${tool}：${title}`）与 `prd/F01-confirmation-inbox-column.md` T-02「② 工具名 + `title`」 | 实现改为「缺失即整段省略」⇒ 两者存在口径差；PR 文件已把文档回填列为**非目标**（交「随后」的文档同步），故未越界 | 提醒：阶段 6 复审若以 `architecture.md` / `prd` 原文为标准判定 D-1 闭合，将再次命中此差异；建议收口后补一条文档同步（不在本 PR 文件面） |

---

## 下一迭代候选

1. **两侧空值语义对齐**：把 `oamp/web/app.js:637` 的存在性判定从「非 `null`/`undefined`」提升为「非空字符串」，与 `oamp/web/notify.js:40` 的真值口径一致，消除 `tool:''` 的前导悬空 `·`（偏差记录 2）。
2. **通知侧 `title` 段的对称化**：`tool` 在场而 `title` 缺失时不留尾随 `：`（偏差记录 3），与栏内行「存在的段才拼」同规则。
3. **D-1 的文档面回填**：`architecture.md` §7 T-02 / §7 T-09 与 `prd/F01` T-02 补记「`tool` 缺失即省略该段」（偏差记录 4）——与阶段 6 复审口径绑定。
4. **D-5 flake 定位**：`status.test.js` 心跳时序 1/9 复现、未改动对照树同样复现（本轮 333/333 未复现）；建议把相关时序窗口钉成确定性等待，并停止在台账中写「无 flake」。
5. **（契约变更，须先改文档）** 若产品要在栏内显示真实工具名，需先在 `prd/F01` T-02 回填口径并解冻 `confirmation-roundtrip.test.js:445-451` 的 `tool=null` 钉死断言，再动 `oamp/src/agent.js` —— 本 PR 已显式排除，不在缺陷修复范围。

---

## 验证手段自身的边界（诚实声明）

- 行为面以**真实浏览器加载真实脚本**为准（含真实 DOM 解析 / 序列化），并另以自研 `node:vm` 探针做 20+16 格矩阵的字节级对照；两者的 `notify.js` 沙箱均为「经典脚本 + 桩 `Notification`」，未走真实系统弹窗（系统通知不经 `Notification` 权限面则不可实测，且属 T-11 既有降级口径，不在本 PR 面）。
- 栏内行的端到端链路（`/api/confirmations` → `renderInbox`）未在真实 oamp 服务上重启验证：本 PR 未触碰该链路的拉取 / 裁决 / 状态面（`oamp/src/**` 零 diff、`app.js` 改动仅为第二行文本段），且渲染函数已在真实 DOM 中按真实调用形态实测 ⇒ 该链路无回归风险面。
- 未使用任何执行过程上下文；未修改被验证产出物；报告内每条判定均可按所列路径 / 命令复现。

---

## 结论：**PASS**

**回报主 agent**：报告路径 = `docs/iterations/0021-confirmation-inbox-and-event-push/clarifications/verify-pr-005-20260913-215436.md`；结论 **PASS**；fail = 0、partial = 0、blocked = 0（pass = 38）；**偏差记录 4 条**；**下一迭代候选 5 条**；「**既有断言未被削弱**」= **是**（numstat 48/0 纯新增 + base 365 行按序全在场 0 处改写/缺失 + `confirmation-roundtrip.test.js` 零 diff + 9/9、20/20 绿）；「**未引入猜测性派生**」= **是**（改动 hunk 中无 `title`→`tool` 映射；缺 `tool` 时输出恰为 `title` 本身；`confirmation-roundtrip.test.js:445-451` 零改动且绿）。

**清理结果**：`/tmp/pr005base/`（base 静态服务根）、`/tmp/pr005-mutant/`（变异沙箱副本）、`/tmp/pr005-probe.mjs`、`/tmp/pr005-notify-probe.mjs`、`/tmp/pr005.diff`、`/tmp/pr005-matrix.txt` 全部删除（复核无残留）；两个临时静态服务（`:8907` HEAD / `:8908` base）已 `hub stop`（均 `exited`）；headless 浏览器两个 tab 为工具托管页、未产生沙箱外副作用。他人遗留的 `/tmp/pr005*` 文件（非本次验证创建）**未触碰**。被验 worktree 与仓库根全程零写入（验证后 `git status --porcelain` 均为空）。
