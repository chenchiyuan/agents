# verify-pr-004-r2 — 独立验证报告（pr-004 第 2 轮：调用面驱动的 chat 状态不进全局链路）

**验证者身份**：进程级 SSE/HTTP 行为面评审者（反射自产出物类型——服务端状态发布路径改动（`oamp/src/web.js` 的 `publishState` 调用点）+ 新建的进程级契约断言；判据落在「两条 SSE 链路上的帧原文」与「浏览器端通知派发行为」两处，而非实现自述）
**产出物**：
1. 代码：worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0021-confirmation-inbox-and-event-push/.pb-agents/worktrees/0021-pr-004-console-inbox-column-and-notify`，分支 `feat/0021-pr-004-console-inbox-column-and-notify`，HEAD `a431225`（本轮改动面 = `git diff 76e33d5...HEAD`；全量面 = `git diff 1f7eceb...HEAD`）
2. PR 契约：`docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-004-console-inbox-column-and-notify.md`
3. 任务图：`docs/iterations/0021-confirmation-inbox-and-event-push/prs/pr-004-tasks.md`
4. 主 agent 裁决：`docs/iterations/0021-confirmation-inbox-and-event-push/clarifications/pr004-round-1-verdicts.md`（§一 MI-1/MI-2、§二 Q1~Q5、§四 **Q6**）
5. 需求源头：`docs/iterations/0021-confirmation-inbox-and-event-push/prd/F07-notification-event-types.md`（验收 3 = N5，`user_confirmed`）

**验证标准来源**：主 agent 委托（A. PR 10 条验收 / B. `prs/pr-004-tasks.md` T1~T5 全部验收标准 / C. 裁决项 MI-1、MI-2+Q1、Q2、Q3、Q5、AE-1、Q6 / D. 新断言文件非空断言核查 / E. 行为面独立实测 / F. 回归与内务）
**验证日期**：2026-09-13
**独立性声明**：本委托**未包含**产出者的执行过程叙述或自评结论。上一轮验收报告 `verify-pr-004-20260913-202425.md` 仅用于确定「待复核清单」（2 个 partial + D 项），其中每一条结论均由本次在 `a431225` 上**重新取证**。产出物内的注释、提交信息、自评文字一律不作为判定依据（仅作定位线索）；产出物自带的 `oamp/test/notification-scope.test.js` **不作为** A/B/C/E 的判据来源，它在本次验证中的角色是：① F 的回归对象；② D 的被反证对象。

---

## 0. 实测环境与取证方式（可复现）

| 项 | 值 |
|---|---|
| 独立行为探针 | `/tmp/pr004-r2/probe.mjs`（自写，**未复用**被测测试文件的断言）：真实 Router 子进程 + `oamp web start --port 45888` + 脚本级假节点（UDS `NodeClient`，`instance_id=pb-dev`，心跳 500ms，收到 `task.request` 即回 `task.result`）；手工解析两条 SSE 的**原始帧文本** |
| 浏览器链路 | `oamp web start --port 45890` + 控制面 45891（假节点投递 `notice{kind:'confirmation_request'}` / 切换 `task.result` 的 state）+ 透传代理 45892（`/__drop?hold=ms` 真实掐断 TCP，触发浏览器 EventSource 自动重连）；headless Chromium，`Notification` 以桩注入（`granted` / `denied` / `default` / 无 / `onclick` setter 被吞 5 档），1440×900 与 1000×800 两档视口 |
| DB / socket | 全程 `OAMP_DB=/tmp/pr004-r2/sql.db`、`OAMP_SOCKET=/tmp/pr004-r2/router.sock`（临时）——仓库内 `oamp/data/` 与 `.runtime/` 全程未被触碰 |
| 反证副本 | `/tmp/pr004-r2/{fixed,neg}/oamp`（`cp -R` 自 worktree；`neg` 仅把 `oamp/src/web.js` 换成 `git show 76e33d5:oamp/src/web.js`，其余逐字相同；两份副本的测试环境完全一致） |
| 清理 | 两个 hub 常驻进程已 stop；浏览器标签已 release；`/tmp/pr004-r2`、`/tmp/pr004-probe` 已删除；端口 45888/45889/45890/45891/45892 复检**全部空闲**；无本探针残留进程；被验证 worktree `git status --porcelain -uall` = **空**（全程只读，未 commit/checkout/stash/worktree） |

> 环境一处必须说明的坑（影响反证的判读，已排除）：只复制 `oamp/` 到 `/tmp` 会让 `roleFromInstanceId('pb-dev')` 因缺同级 `roles/dev/dev.md` 而返回 null（`src/role-binding.js:18-25,45-53` 的 `roleRoot = PKG_ROOT/..`），`POST /api/calls` 会以「agent 不可用」404 失败——这是**副本环境缺失**，与实现无关。上述 `fixed`/`neg` 两份副本的对照运行时设 `OAMP_ROLE_ROOT=/Users/chenchiyuan/projects/agents`，两侧同条件。

---

## 一、逐项判定

### A. PR 文件「验收标准」10 条

- **A1 `GET /notify.js` 非 404 + `index.html` 独立 `<script>` 引入 → pass**
  证据：`curl -o /dev/null -w '%{http_code} %{content_type}' http://127.0.0.1:45890/notify.js` → `200 text/javascript; charset=utf-8`（同时 `/`→200 text/html、`/app.js`→200、`/style.css`→200 text/css）；`oamp/web/index.html:100-101` 为两条独立标签，`/notify.js` 在 `/app.js` **之前**（非内联、非合并）。

- **A2 服务 / 通道分离可静态判定 → pass**
  证据（独立切片，非引用测试文件）：`awk '/channel 层/,/全局入口/' web/notify.js | grep -cE 'chat_completed|chat_failed|confirmation_required'` → **0**；`awk 'NR<53' web/notify.js | grep -c Notification` → **0**；`grep -c call_completed web/{notify.js,app.js,index.html,style.css}` → **0/0/0/0**；`web/notify.js:12` `const EVENT_TYPES = Object.freeze(['chat_completed','chat_failed','confirmation_required'])`（恰 3 类，冻结）。

- **A3 控制台打开后一条确认项无需刷新即入栏；条目含 ①来源对话标识 ②工具名 + 动作描述 ③每选项一按钮 + 一文本框 → pass**
  证据（真实浏览器 + 真实 SSE + 假节点按 §5.3 信封 1 投递）：页面打开后（`?project=…`，1440px）`#inbox-count` = `4`；随后投递 `cfm-R2-E`/`cfm-R2-F` **未刷新**，`#inbox-count` 4 → **6**，栏内新增两条。条目 DOM 原文（浏览器实测快照）：
  `{"id":"cfm-R2-E","source":"pb-dev · chat-R2-E","request":"bash · echo E","opts":["允许一次|allow_once","拒绝一次|reject_once"],"placeholder":"拒绝理由 / 补充说明（可不填）"}`
  ⇒ ① 来源标识在场（未加载的对话回落 `chat_id`；左栏已加载则用标题，渲染路径 `app.js:617-620` **只用内存 `state.chats`，零额外拉取**）；② `tool · title`；③ 每选项一按钮 + 一个单行文本输入框。（本判据允许「构造样本」，本次即构造样本。）

- **A4 点选即裁决：`{option_id,text}`、提交中 `disabled`、`200` 即移出（不等 SSE） → pass**
  证据：在页面内把 `fetch` 对 `/decision` 的**响应**延迟 1500ms，点击 `cfm-R2-E` 的首个选项按钮 → 中途快照 `{"id":"cfm-R2-E","labels":["提交中…","拒绝一次"],"disabled":[true,true]}`、`#inbox-count` 仍为 6；放行后（`200`）→ `#inbox-count` = 5、该条消失，同批其它条目未受影响；服务端在途表复读 `GET /api/confirmations` = `[A,B,C,D,F]`（**E 已不在**）。
  ⇒ 请求体形态：`app.js:685-692` 提交 `{ option_id: button.dataset.option, text: textInput.value }`，且服务端只有在 `option_id` 属于该条选项集合时才回 200 并把条目移出在途表（`src/web.js:1299-1306` 的 400 分支为反证）⇒ 被受理的 body 必为合法 `option_id`（`[INFERENCE]`：本轮未逐字抓取请求体，但 200 + 在途表移除是该推断的可复查依据）。
  ⇒ 移出不由 SSE 引发：裁决路由在正常路径下**不发布任何帧**（`src/web.js:1289-1341` 无 `transport.publish*` 调用），移出只能来自 `200`。

- **A5 刷新 / 断线重连后仍在且内容一致；已裁决项不回流 → pass**
  证据（两条路径都实测）：
  ① **刷新**：`page.reload()` 后栏内 6 条逐字一致（`source` / `request` / `opts` / `placeholder` 逐字段相同，快照对比 base vs afterReload），且 `window.__notes` = **0**（重建零派发）；已裁决的 `cfm-R2-C`/`cfm-R2-E`/`cfm-R2-L` 未回流。
  ② **真实断线重连**（代理 `/__drop?hold=6000` 掐断 9 条 TCP，含 SSE）：`EventSource` 进入 error（`esErr` 6 → 12、`readyState` 0 ↔ 1）并在阻塞窗口后**自动重连**（`esOpen` 2 → 4，`t≈8.7s`），随即出现一次新的 `/api/confirmations` 拉取（`nFetch` 2 → 3）；断线窗口内投递的 `cfm-R2-U` **无刷新**出现在栏内（`hasU: true`），且 `notes` = **0**（重建不通知）；重连后新帧 `cfm-R2-V` 正常增量入栏并派发 **1** 条通知。全程 `__errs` = `[]`。

- **A6 布局与边界（兄弟节点 / 位置 / 窄屏折叠可达 / 无横向滚动 / 未新增入口载体） → pass**
  证据：`oamp/web/index.html:44`（`<main class="layout hidden">`）→ `:45`（`.sidebar`）→ `:62`（`<section id="inbox" class="inbox">`）→ `:69`（`.detail`）→ `:98`（`</main>`），DOM 顺序与区间实测；1440px 几何 `sidebar{x:0,w:320} inbox{x:320,w:320} detail{x:640,w:800}`（`.detail` 仍最宽、第三栏定宽 320px），`scrollWidth == innerWidth == 1440`（无横向滚动）；1000px：`#inbox-list` `display:none`（默认折叠）、标题按钮显示在途条数 `1`，**一次点击** → `.inbox open` + `display:block`，条目选项可点并完成裁决（`#inbox-count` 1 → 0，服务端在途表清空），`scrollWidth == innerWidth == 1000`（无横向滚动）；1440px 下同一条目 `display:block`（宽屏不折叠）。未新增载体：`git diff 1f7eceb...HEAD -- oamp/web/index.html` 仅 `+10/-0`（第三栏 6 行节点 + 2 行注释 + 1 行 script + 1 空行），无新页面 / 新路由 / 顶栏抽屉 / 第 4 个 filter tab（`web.js` diff 中 `^+.*path: '` 命中 0）。

- **A7 三类事件各触发一次浏览器通知；终态帧派生；`confirmation_required` 仅首次入栏一次、重建不重复；hub 调用面完成不产生通知 → pass（本轮两个子项均成立）**
  - `chat_completed`：一次消息驱动轮次 → 恰 **1** 条「对话已完成」（body = 该 chat）；**停在其他对话甚至项目列表页也能收到**：以 `/`（项目列表视图，`main.layout` = `layout hidden`、`#inbox` 不可见）为停留位置发起一次消息轮次 → 仍恰 **1** 条「对话已完成」。
  - `chat_failed`：控制面令假节点回 `state:'failed'` → 实测 `chat.state = failed`、out 记录 `error = PROBE_FORCED_FAILURE`，页面恰 **1** 条「对话失败」。
  - `confirmation_required`：仅**首次入栏**时通知一次（两条新确认项 = 2 条通知），刷新 / 重连重建路径 **0** 条（A5 的两处 `notes: []`）。
  - **hub 调用面完成（`call_result`）不产生通知**：调用面（`POST /api/calls`，`mode=background`）派发 → `GET /api/calls/<id>` 终态 `completed` 且 `chat.state = completed` → 页面 `newNotesAfterCall` = **`[]`**（其前一轮消息驱动已产生 1 条，调用面轮次**未新增任何通知**）。服务端侧同见 E-①。

- **A8 降级（`denied` / 无 `Notification` 静默；加载期不请求权限） → pass**
  证据：`permission='denied'` 桩 → 一条新确认项入栏后 `notes=[]`、`asks=0`、`errors=[]`（栏内不受影响）；`window.Notification = undefined` 桩 → 一条新确认项 + 一次消息终态后 `notes=[]`、`errors=[]`、栏内照常；`permission='default'` 桩 → 加载后 `asks=0`（**不主动请求**），首次真实点击（栏内选项按钮）后 `asks=1`，再次点击后仍为 `1`（至多一次）；全程零未捕获异常。

- **A9 通知 `onclick` 不可用时关键动作仍在栏内可完成 → pass**
  证据：桩把实例 `onclick` 的 setter 吞掉（getter 恒 `null`）→ 赋值尝试计数 `onclickSets=1`、通知照常投递（`notes=["需要你确认"]`）；随后在**栏内**点选该条选项 → 条目移出、`GET /api/confirmations` 在途表中消失（裁决在栏内完成）⇒ 通知不承载唯一入口。

- **A10 `node --test oamp/test/inbox-console.test.js` 全绿 → pass**
  证据：`# tests 8 / # pass 8 / # fail 0`。

### B. `prs/pr-004-tasks.md` T1~T5 全部验收标准

#### T1（`web/notify.js`）

- **T1-1 事件类型封闭 3 类 + 常量与 service 一个分派点一一对应 + 服务端零事件类型常量 → pass**：`notify.js:12` 冻结 3 类；`notify.js:35-42` `TEMPLATES`（表查不到 ⇒ 返回 `null` 不通知，`notify.js:46-51` 为唯一分派点）；`grep -c call_completed` 在 `web/*` 与 `src/` 均 0。
- **T1-2 两层零命中（可静态判定） → pass**：见 A2 的切片命令输出（service 段 `Notification` 0；channel 段事件类型字符串 0）。
- **T1-3 职责分离可描述（`intent = {type,title,body,target}` / 三行文案 / 正文截断 80） → pass**：`notify.js:49-51` 返回四字段；`TEMPLATES` 三行标题逐字「对话已完成」「对话失败」「需要你确认」（浏览器实测通知标题一致）；`BODY_MAX=80`（`notify.js:14`）+ `clip()`；`channel.deliver(intent)` 形参只吃 intent、不 `switch`（`notify.js:59-73`）。
- **T1-4 投递与降级 → pass**：见 A8（5 档浏览器实证中的 denied / 无 Notification / granted 三档）。
- **T1-5 加载期零权限请求；手势内至多一次 → pass**：`notify.js:79-88`（`permissionAsked` 一次性守卫、非 `default` 不询问、仅由手势回调调用）；运行时 `asks` = `0 → 1 → 1`。
- **T1-6 零依赖 / 零构建 / 独立脚本 + hygiene 绿 → pass**：`grep -cE '^(import|export) ' web/notify.js` → **0**；`index.html` 零 `type="module"`；`package.json` 不在 diff；`window.oampNotify`（`notify.js:108`）先于 `app.js` 加载；`hygiene` 3/3 绿。
- **T1-7 不引入 Service Worker / Web Push / VAPID / WebSocket → pass**：`grep -nE 'serviceWorker|PushManager|WebSocket|VAPID' web/*.js src/web.js` 除 `notify.js:6` 的注释自述外**无命中**。
- **T1-8 段落边界可静态切片 → pass**：`notify.js` 三处 `// ══════════ …` 稳定标记（service / channel / 全局入口）；本次切片命令即按标记执行并得到预期 0/0。

#### T2（`index.html` + `style.css`）

- **T2-1 第三栏 = `main.layout` 内 `.sidebar` 与 `.detail` 之间的兄弟节点 → pass**：见 A6 的行号与区间证据。
- **T2-2 不新增入口载体 → pass**：`index.html` 全量 diff = `+10/-0`（仅第三栏节点 + 注释 + script）；无新页面 / 路由 / 抽屉 / 第 4 个 tab。
- **T2-3 独立脚本（不并入 `app.js`、不内联） → pass**：`index.html:100-101` 两条标签并存；`grep -cE 'new Notification|Notification\.requestPermission' web/app.js` → **0**。
- **T2-4 宽屏三栏（`.detail` 仍最宽 / 第三栏定宽 320px / 无横向滚动） → pass**：1440px 实测 `detail.w=800 > inbox.w=320`，`style.css:419-421` 定义第三栏、`.detail` 未被改写；`scrollWidth == innerWidth`。
- **T2-5 窄屏（`max-width: 1100px`）折叠为可开合面板、标题显示在途条数、一次点击可展开并操作 → pass**：`style.css:480` 为该 PR 引入的断点；1000px 实测（折叠 → 一次点击展开 → 完成一次裁决 → 无横向滚动，见 A6）。
- **T2-6 只追加（既有 `style.css` 规则零改写；折叠态用本栏自己的规则） → pass**：`git diff --numstat 1f7eceb...HEAD -- oamp/web/style.css` → `81 0`（零删除行）；折叠态用 `.inbox .inbox-list{display:none}`（`style.css:494`）/`.inbox.open .inbox-list{display:block}`（`:496`），未复用他组件 `.hidden` 规则；既有前端断言（`project-workspace` 21/21、`api-pages` 5/5）保持绿。
- **T2-7 `GET /notify.js` 为 200（`STATIC_FILES` 由 pr-003 登记） → pass**：HTTP 200 + `text/javascript`（A1）。括号内「本 PR 对 `src/web.js` 零改动」的表述已被裁决 Q4 / Q6 显式扩范围推翻（见偏差 D-4）——**主判据（200）成立**，判定不因此降级。

#### T3（`app.js` 第三栏消费面）

- **T3-1 双来源同形状、同一渲染路径；三行控件；标题回落不额外拉取 → pass**：唯一渲染路径 `renderInbox()`（`app.js:648`）+ `renderInboxItem()`（`app.js:635`）同时服务重建面与增量帧；三行控件见 A3 的 DOM 原文；`inboxSource()`（`app.js:617-620`）只读内存 `state.chats`。
- **T3-2 重建 = 纯拉取、零缓存、跨对话（含每次 `onopen`） → pass**：`app.js:167` `es.addEventListener('open', loadConfirmations)`（首次与每次自动重连）、`app.js:1139` boot 首屏拉取；`loadConfirmations()`（`app.js:624-631`）覆盖式整栏重绘、零派发；`grep -cE 'localStorage|sessionStorage' web/app.js` → **0**；实测刷新 / 真实断线重连两条路径都重建成功（A5）。
- **T3-3 点选即提交（`POST` / `{option_id,text}` / 提交中 `disabled` / `200` 即移出） → pass**：见 A4。
- **T3-4 `404` 移出且不重放 → pass**：把裁决请求的 `confirmation_id` 改写为不存在者 → 服务端 `404`，条目**移出**栏内，且裁决请求计数恰 **1**（无重放）；零弹窗（`dialogs: []`）。
- **T3-5 窄屏开合交互，且开合不切换主视图 → pass**：`app.js:1117-1121` 只改 `state.inbox.open` + 重绘 + 手势入口；1000px 实测展开后 `page.click` 仍作用于第三栏条目，主详情区标题未被切换（见 A6 快照）。
- **T3-6 既有面零回归 → pass**：会话订阅 4 类事件、`agent_online` / `agent_offline` 分支、`es.onopen = () => loadAgents()` 在本轮 diff 中零改动；`web.test.js` 39/39、`project-workspace.test.js` 21/21、`api-pages.test.js` 5/5 绿。
- **T3-7 权限手势入口接线（首次交互触发、加载期不触发） → pass**：实测 `asks` = `0 → 1 → 1`（A8）。

#### T4（三类事件的通知派发）

- **T4-1 终态派生（全局 `chat_state` 帧，非终态 → `completed`/`failed` 各一次） → pass**：`app.js:186-201` 的全局 `chat_state` 分支；浏览器实测 completed / failed 各恰 1 条（A7）。
- **T4-2 不依赖停留位置、不依赖对话列表 → pass**：派生状态表 `app.js:185 const chatStates = new Map()` 按 `chat_id` 维护；项目列表视图（`main.layout hidden`、无 `state.chats`）下实测仍收到 1 条「对话已完成」；派发点不在 `/api/stream` 会话订阅与 `renderChat` 路径上。
- **T4-3 `confirmation_required` 仅首次入栏一次；重建路径零派发 → pass**：派发点恰 2 处（`app.js:181` 确认分支 / `app.js:199` 终态分支）；`loadConfirmations` 体内零 `oampNotify`；实测每条恰 1 条、刷新 / 重连重建 0 条。
  > 措辞张力（非判负，已由裁决落为偏差 D-1）：`app.js:178` 存在按 `confirmation_id` 的去重守卫，与文字「前端不额外去重」不一致；但该守卫**只强化**「仅首次一次」，且与 T3-2「增量帧按 `confirmation_id` 去重追加」一致 ⇒ 判 pass + 记录偏差。
- **T4-4 调用面零通知（静态 + 运行期） → pass（本轮重点，两个子项均成立）**
  - 静态：派发点恰 2 处；全局监听内零 `call_state` / `call_result` / `chat-calls:` 代码路径；`publishGlobal` 全仓 4 个调用点（`src/web.js:245/246` agent_online/offline、`:1482` chat_state、`:1608` confirmation）——**无** `call_*` 类型走全局键。
  - 运行期：`POST /api/calls`（background）派发并等其终态 `completed`（`chat.state=completed`）⇒ 页面**零**新增通知（`newNotesAfterCall = []`），全局链路该轮次零 `chat_state` 帧（E-① 的帧级证据）。
- **T4-5 `app.js` 零投递实现 + 通知不承载唯一入口 → pass**：`grep -cE 'new Notification|Notification\.requestPermission' web/app.js` → **0**；`app.js:1122 oampNotify.onTarget = focusFromNotify` 注入点击去向；`onclick` 不可用档实测仍在栏内完成裁决（A9）。
- **T4-6 同一终态重复广播不重复派发（MI-2） → pass**：一次消息轮次后 1 条通知；随后 `POST /api/chats/archive` → `POST /api/chats/<id>/activate`（响应 `{"state":"completed"}`，服务端**再次**广播同值终态 `chat_state`）⇒ 页面通知数**不变**（仍 1 条）。

#### T5（`test/inbox-console.test.js` + PR 收口回归）

- **T5-1 体例与边界（前端文本级静态契约 + 真实 web 子进程 + 临时 `OAMP_DB` + 零新依赖） → pass**：该文件只 `import` node 内置与 `./helpers/*`（同仓既有 harness）；`OAMP_DB` 指向 `fs.mkdtempSync` 临时目录；端口随机非默认；运行期不写仓库 `oamp/data/`。
- **T5-2 静态断言覆盖 ①~④ → pass**：8 个用例；本次用**独立命令**复算了其中①②④的关键零命中与常量集合（A2、T1-1、T2-3、T3-1 的证据命令）。
- **T5-3 HTTP 断言（`/notify.js` 200 + 内容类型；`/`、`/app.js`、`/style.css` 仍 200） → pass**：用例通过；另由本次独立 curl 复证（A1）。
- **T5-4 既有测试零修改 → pass**：`git diff --name-only 1f7eceb...HEAD -- 'oamp/test/*'` → 仅 `inbox-console.test.js`、`notification-scope.test.js`（两份均为本 PR **新建**）。
- **T5-5 PR 收口回归 → pass**：见 F（10 文件 104/104/0；`hygiene` 绿）。
- **T5-6 浏览器行为面不在本文件承载（登记为验收方式） → pass**：本次由独立浏览器实测承接（A3~A9、E-③）。

### C. 主 agent 裁决项

> 委托正文列出的 C 项为 7 条（Acceptance 写「C(6 项)」）；本报告按**正文列举的 7 条**逐条判定。

- **MI-1（`notify.js` 对外以全局入口暴露给 `app.js`） → pass**：`web/notify.js:108 window.oampNotify = entry`（零模块语法，`notify.js:100-101` 先于 `app.js` 加载）；运行时生效——通知实际投递、`onTarget` 点击去向注入（`app.js:1122`）。
- **MI-2 + Q1（同值终态去重；冷启动首帧即终态仍通知一次） → pass**
  同值去重：T4-6（archive→activate 重播同值终态，通知数不变）。
  冷启动：页面加载前该对话已 `completed`（先在另一标签页完成轮次），页面加载后**首次**观测到的该 chat 帧即终态（由 activate 重播触达） ⇒ 恰 **1** 条「对话已完成」，body = 该对话标题（未因「没观察到 `working`」漏报）。
- **Q2（`404` 移出且不重放；`400` 保留条目不新增 UI 组件） → pass**
  `404`：裁决请求被改写为不存在的 `confirmation_id` → 条目移出、裁决请求计数 1（不重放）、零弹窗。
  `400`：把裁决 body 的 `option_id` 改写为非法值 → 条目**保留**在栏内、按钮复位（`disabled` 全 `false`、文案回「允许一次 / 拒绝一次」）、`dialogs: []`、`errors: []`、无新横幅 / 新组件。
- **Q3（第三栏在项目列表视图下不可见 —— 采纳为边界登记、不行动） → pass（与实测一致）**：以 `/`（无 `?project=`）进入项目列表视图，实测 `main.layout = "layout hidden"`、`#inbox` `offsetParent` 为 null（不可见），而同一停留位置下消息驱动的终态**仍**触发 1 条通知 ⇒ 「项目列表页也能收到通知」由全局 SSE 驱动，与第三栏可见性无耦合，与裁决口径一致。
- **Q5（T1 落盘后先 grep 校验命中，再定稿 T5 的断言检索式） → pass**：新测试的检索式与实际落盘形态一致（8/8 绿），且本次用独立命令复算的关键检索式（`EVENT_TYPES` 常量集合、两处零命中、`/notify.js` 标签、`onopen` 重建点）同样命中。
- **AE-1（`/api/docs` 投影与 `oamp/llms.txt` 如实反映 4 类事件；漂移锁保持绿） → pass**
  ① `GET /api/docs` 的 `/api/events` 投影：`summary` = `全局事件订阅（SSE：agent_online / agent_offline / confirmation / chat_state）`、`response` = `SSE 事件流（text/event-stream）：agent_online / agent_offline / confirmation / chat_state`，路由总数 21；
  ② `GET /llms.txt` 第 21 行与仓库 `oamp/llms.txt:21` **逐字相同**且含 4 类；
  ③ 把 worktree 的 `oamp/` 整树复制到 `/tmp` 后 `node scripts/gen-llms-txt.mjs` → 产物与仓库 `llms.txt` **`diff` 为空（逐字节一致）**，证明是重生成而非手改（全量 diff 该文件恰 `+1/-1` 行）；
  ④ 漂移锁：`api-routes.test.js` 7/7 绿（含 llms 逐字节快照与 API.md 双向覆盖）。
- **Q6（调用面驱动的 chat 状态变化不进全局链路；既有 `chat:<id>` 定向帧逐字不变；消息驱动终态仍全局可观测） → pass（三条判据全部闭合）**
  · 实现所选口径（须写明）：**「调用面驱动的 chat 状态变化完全不进全局链路」**——调用面派发处的 `working` 与终态、以及调用面首项派发失败分支的读库值，均以 `LOCAL_ONLY` 关闭全局广播（`src/web.js:78` 常量、`:1081`、`:1105`、`:1525`）。
  · 判据 ①（调用面驱动的 chat 状态变化不进全局 `chat_state` 链路）：`publishState` 增加默认等价的 `opts` 参数，`opts.global === false` 时**只跳过全局广播、定向帧先发**（`src/web.js:1479-1483`，行序：`transport.publish(chatId, …)` → `if (opts.global === false) return;` → `transport.publishGlobal(…)`）；运行期以独立探针复现：`POST /api/calls`（background）派发 → 终态 `completed`、`chat.state=completed` → 全局 `GET /api/events` 在该窗口**零** `chat_state` 帧（`baseline idx=3 → 现 3 帧，命中 0`，窗口内全局帧类型集合为 `[]`）。
  · 判据 ②（既有 `chat:<id>` 定向帧形态与时机逐字不变）：同一场景下 `GET /api/stream?chat_id=` 上的帧**原文**为
    `event: chat_state\ndata: {"chat_id":"chat-addc2fa0-…","state":"working"}` 与 `…{"state":"completed"}`，键集合恰 `["chat_id","state"]`，`working` 在前、终态在后（与 pr-003 既有语义一致）；消息驱动路径的定向帧同样照常（未受影响）。
  · 判据 ③（消息驱动 `POST /api/messages` 的对话终态仍在全局链路可观测，防修复过头）：修后两次独立对照均通过——新对话的消息轮次在全局链路得到 `working` + `completed`；**同一对话**（即先前走过调用面轮次的那个 chat）再走消息驱动，全局链路**仍**得到 `working` + `completed` ⇒ 抑制是按**条目**（`entry.call`）而非全局静音。
  · 前端面：调用面终态**不产生**通知、消息面终态**产生一次**通知（浏览器实测，见 E-③）。

### D. 新增断言文件非空断言核查（`oamp/test/notification-scope.test.js`）

- **D 结论：该文件的断言为**非空 / 非恒真**，且在修复前会红 → pass**

  证据（反证法，**未改 worktree**；在 `/tmp` 副本上操作）：
  1. `/tmp/pr004-r2/neg/oamp` = worktree 的 `cp -R`，仅把 `oamp/src/web.js` 换为 `git -C <worktree> show 76e33d5:oamp/src/web.js`（`diff` 复验「identical-to-76e33d5: yes」）；
  2. `/tmp/pr004-r2/fixed/oamp` = 同一 `cp -R` 的**未改动**副本（仅 `web.js` 与 `neg` 不同，其余逐字节相同）；
  3. 两份副本在**完全一致**的环境下运行同一文件：
     - `neg`（修复前）：`not ok 1`，失败原文 = `调用驱动的 chat 不得进全局 chat_state 链路：[{"type":"chat_state","data":{"chat_id":"chat-75a69ef5-…","state":"working"}},{"type":"chat_state","data":{"chat_id":"chat-75a69ef5-…","state":"completed"}}]` ⇒ **修复前全局链路上确实出现调用面驱动的 `working` 与 `completed` 两帧**，断言①被真实反例打红；
     - `fixed`（`a431225`）：`ok 1` / `ok 2`，`# pass 2 / # fail 0`。
  4. 断言集覆盖 ①②③ 三元判据（逐条对应）：① = 用例 1 的 `assert.deepEqual(globalAfter, [])`（全局链路 slice 为**空数组**的严格相等，而非「未断言」）；② = 同一用例的 `chatStates.length >= 2` + `Object.keys(e.data).sort() === ['chat_id','state']` + 首帧 `working` / 末帧 `completed`（定向链路**必须**非空且形态逐字）；③ = 用例 2 的 `states.has('working') && states.has('completed')`（消息驱动终态**必须**在全局链路出现，防修复过头）。三条断言都带**先证明轮次真的走完终态**的前置（`GET /api/calls/:id` 终态 + `chat.state` 落库 + `assert.deepEqual(globalAfter, [])` 之外还断言定向链路收到 ≥2 帧）⇒ 不存在「零帧也通过」的空断言面。
  5. 附带核验（非判据）：`neg` 与 `fixed` 的用例 2（③ 对照）**都通过**，说明该文件的 ③ 断言不是「跟着 ① 一起红」的伪控制项。

### E. 行为面独立实测（自建探针，非运行被测测试文件）

- **E-① 调用面（`POST /api/calls`，background）→ 全局零 `chat_state` 帧、定向仍有 `chat_state`（含终态） → pass**
  环境：Router 子进程 + `oamp web start --port 45888` + 假节点（心跳 500ms），临时 socket/DB。
  帧级证据（探针原文）：基线 `global.frames.length = 3`（此前的消息驱动轮次）→ 派发调用 → 终态 `completed`、`chat.state = completed` → 全局窗口内新增帧数 **0**（`S2-Q6① ... 命中 0；该窗口全局帧类型=[]`）；定向链路 `S3-Q6②` 原文 = `event: chat_state | data: {"chat_id":"chat-addc2fa0-…","state":"working"} ;; event: chat_state | data: {"chat_id":"chat-addc2fa0-…","state":"completed"}`。
- **E-② 消息驱动（`POST /api/messages`）的对话终态仍在 `GET /api/events` 出现（防修复过头） → pass**
  证据：`S4-Q6③`（新对话）：全局帧原文 = `working` + `completed`；`S5`（**同一对话**，先调用面后消息面）：全局帧 states = `["working","completed"]`。
- **E-③ 前端（`app.js` / `notify.js`）对调用面终态零通知、对消息面终态一次通知 → pass**
  证据（headless Chromium，`Notification` 桩 `granted`）：消息驱动轮次 → `notes = [{"title":"对话已完成","body":"chat-667e76e5-…"}]`（**1** 条）；随后调用面轮次（`GET /api/calls/<callId>` = `completed`、`GET /api/chats/<chatId>.chat.state` = `completed`）→ `newNotesAfterCall = []`、`notes` 仍 1 条 ⇒ 调用面终态**零**通知。补充：项目列表视图下消息面终态仍产生 1 条通知；`chat_failed` 轮次产生 1 条「对话失败」。无覆盖缺口。

### F. 回归与内务

**F-1 回归（10 个文件，逐个文件运行，未修改的 worktree 上执行）**

| 套件 | tests | pass | fail |
|---|---|---|---|
| `oamp/test/notification-scope.test.js`（本轮新增） | 2 | 2 | 0 |
| `oamp/test/inbox-console.test.js`（本 PR 新增） | 8 | 8 | 0 |
| `oamp/test/confirmation-inbox.test.js` | 6 | 6 | 0 |
| `oamp/test/web.test.js` | 39 | 39 | 0 |
| `oamp/test/api-routes.test.js` | 7 | 7 | 0 |
| `oamp/test/project-workspace.test.js` | 21 | 21 | 0 |
| `oamp/test/call-protocol.test.js` | 11 | 11 | 0 |
| `oamp/test/call-console.test.js` | 2 | 2 | 0 |
| `oamp/test/api-pages.test.js` | 5 | 5 | 0 |
| `oamp/test/hygiene.test.js` | 3 | 3 | 0 |
| **合计** | **104** | **104** | **0** |

另：单条命令 `node --test <上述 10 个文件>` 的一次性运行同样为 `# tests 104 / # pass 104 / # fail 0`（两种方式结果一致）。`notification-scope.test.js` 在未改动 worktree 上连续 **3/3** 通过（无 flake 观察）。**未**与并行 pr-001 面（`oamp/src/acp-client.js`）相交。

**F-2 六条红线**

| # | 判定 | 证据 |
|---|---|---|
| ① 三点 diff 文件面 | **pass** | 本轮 `git diff --numstat 76e33d5...HEAD` → `oamp/src/web.js 13 4`、`oamp/test/notification-scope.test.js 247 0`（**新增**）——恰为裁决 Q6 授权的两个面；全量 `git diff --name-status 1f7eceb...HEAD` → 7 个 `oamp/` 文件 + `prs/pr-004-tasks.md`（后者为**实现前** planner 提交 `cce5f8b`，不属实现提交）；无第三个文件被本轮触碰 |
| ② `src/web.js` 仅本轮的 `publishState` / 调用点改动 | **pass** | 全量 diff 共 4 处：`:78` 新增常量；`:1081`/`:1105` 传 `LOCAL_ONLY`；`:1479-1483` `publishState` 增参；`:1525` `finishTask` 按 `entry.call` 开关。**零**路由表新增（`^+.*path: '` 命中 0）、零既有 handler 行为改动；`:749/:751` 两条文案为裁决 Q4 授权项 |
| ③ `style.css` 只追加 | **pass** | `git diff --numstat 1f7eceb...HEAD -- oamp/web/style.css` → `81 0`（零删除行） |
| ④ 既有测试文件零修改 | **pass** | `git diff --name-only 1f7eceb...HEAD -- 'oamp/test/*'` → 仅两份**新建**文件 |
| ⑤ 禁改清单零命中 | **pass** | `git diff --name-only 1f7eceb...HEAD \| grep -cE 'persist\.js\|router\.js\|transport\.js\|cluster-config\.js\|web/debug\.js\|API\.md\|package\.json'` → **0** |
| ⑥ 零新依赖 / 零构建 | **pass** | `package.json` 不在 diff；`web/notify.js` 零 `import`/`export`；`index.html` 零 `type="module"`；`serviceWorker`/`PushManager`/`WebSocket`/`VAPID` 无代码命中（仅 `notify.js:6` 注释） |

**F-3 内务**：仓库根 `git status --porcelain -uall` = **空**；被验证 worktree `git status --porcelain -uall` = **空**（HEAD `a431225`，全程只读）；探针资源已全部清理（端口 45888/45889/45890/45891/45892 复检空闲、`/tmp/pr004-r2` 与 `/tmp/pr004-probe` 已删、无残留进程、浏览器标签已释放）。注：`/tmp/pr004-*`（无 `-r2`/`-probe` 后缀）为**上一轮**验证者留下的临时文件，非本次产物，未代为删除。

---

## 二、汇总

| 判定 | 条目数 | 明细 |
|---|---|---|
| **pass** | **57** | A：10（A1~A10，其中 A7 的两个子项均成立）；B：34（T1×8、T2×7、T3×7、T4×6、T5×6）；C：7（MI-1、MI-2+Q1、Q2、Q3、Q5、AE-1、Q6）；D：1；E：3（E-①/E-②/E-③）；F：2（F-1 回归、F-2 六红线+F-3 内务） |
| **fail** | **0** | — |
| **partial** | **0** | 上一轮的两个 partial（A7 / T4-4 的「hub 调用面完成不产生通知」子项）本轮**均已闭合**，无残留部分项 |
| **blocked** | **0** | — |

合计判定条目 **57**。

**Q6 三条判据闭合状态**：① 闭合（静态 + 探针帧级）｜② 闭合（探针帧原文逐字）｜③ 闭合（两次独立对照，含同对话回归）——**三条全部闭合**。
**新断言文件非空断言**：**已被证明非空**（`/tmp` 副本上把 `web.js` 回退到 `76e33d5` ⇒ 断言①以真实反例打红；同条件下 `a431225` 绿）。

---

## 三、偏差记录

> 实现/裁决与既有规格措辞不一致处。不影响本次逐项判定结论。

| # | 规格/文档描述 | 实现（或裁决）实际行为 | 建议处理 |
|---|---|---|---|
| D-1（承上轮，未闭合） | 任务图 T4-3「服务端已保证…前端**不额外去重**」 | `web/app.js:178` 在确认帧分支按 `confirmation_id` 去重（重复帧不重复入栏、也不重复通知） | 按实现更新任务图措辞（去重不承担「恰一次」，服务端单一发布点才是真源）。裁决已登记「留阶段 6 汇总」 |
| D-2（承上轮，未闭合） | PR 验收 3「① 来源对话标识（`chat_id` 或既有标题，取不到即回落 `chat_id`）」 | `web/app.js:619` 渲染为 `` `${agent_id} · ${title \|\| chat_id}` ``（含 `agent_id` 前缀） | 按实现更新规格（明确「来源 = `agent_id · 标题/chat_id`」）。裁决已登记「留阶段 6 汇总」 |
| D-3（上轮 FAIL 根因，**本轮闭合**） | PR 验收 7 / T4-4「hub 调用面完成（`call_result`）**不**产生通知」 | `src/web.js` 的 `publishState` 增 `opts`（默认等价），调用面驱动路径传 `LOCAL_ONLY`：`working` 与终态均不进全局链路；定向帧逐字不变 | **无需处理**。本轮以帧级 + 浏览器级证据确认闭合（E-①、E-③），上轮「实现/文档不一致」的登记可作废 |
| D-4（本轮新增） | 任务图 §0「非目标」与 §4.2-2 谓「`oamp/src/web.js` 整体不在本 PR 文件范围（含 … `publishState` 的全局广播）」；T2-7 谓「本 PR 对 `src/web.js` 零改动」；§0 文件面表列 5 条 | 裁决 Q4 显式追加 `src/web.js`（两条元数据文案）与 `llms.txt`；裁决 Q6 再次扩为 `publishState` 的选项参数 + 调用面路径的全局广播开关，并**授权新建** `oamp/test/notification-scope.test.js` | 按裁决更新任务图的任务图面（文件范围表 → 7 项 + 新建测试文件；T2-7 括号句改写为「`STATIC_FILES` 登记由 pr-003 落盘；`src/web.js` 的本 PR 改动面 = 裁决 Q4/Q6 两条」）。与 D-1/D-2 同批在阶段 6 汇总 |
| D-5（本轮新增，措辞级） | 任务图 §1 T5 的文件范围只列 `oamp/test/inbox-console.test.js` | 本 PR 实为**两个**新建测试文件（另加 `notification-scope.test.js`，由 Q6 授权） | 同上，并入 D-4 一并更新 |

---

## 四、下一迭代候选

1. **新建测试的假节点无心跳，依赖 3s 租约窗口（本轮新发现）**：`test/notification-scope.test.js` 的 `startEchoAgent` 不传 `heartbeatMs`，而 `LEASE_ENV = { OAMP_HEARTBEAT_TIMEOUT_MS: '3000' }`；`POST /api/calls` 必须在注册后 3s 内发生，否则 Router 判离线 ⇒ 用例以无关原因（`404 agent 不可用`）变红。实测路径下该窗口有约 2.5s 余量（`T+431ms` 即派发），但机器重载时风险真实存在。建议：给假节点加心跳（`heartbeatMs: 1000`）或进一步放长租约。
2. **断线窗口内到达的确认项不通知**（承上轮）：断线期间入表的项，重连后仅由重建面补入栏（0 条通知）。口径有据（MI-01「重建不算进入」），但用户可能错过「需要确认」；建议评估带去重窗口的补通知。
3. **终态通知的正文在对话未加载时回落为裸 `chat_id`**（承上轮，本轮再次实测到：`对话已完成#chat-ee42…`、`对话失败#chat-d206…`）。全局 `chat_state` 帧只含 `{chat_id, state}`，前端无从取标题；若要可读标签需在帧内补字段（服务端面变更）。
4. **窄屏浮层遮挡详情区**（承上轮）：`style.css:480-497` 以 `position:absolute` 浮层承载展开面板；本轮仅验证「折叠→一次点击可展开并操作」，未评估遮挡与「点击外部收起」。
5. **调试台事件集合漂移**（承上轮）：`oamp/web/debug.js` 的 `EVENT_TYPES` 仍不含 `confirmation` / `chat_state`（p-003-Q3 已登记不动），与真实全局事件集合不一致。

---

## 五、结论

**PASS**（57 个判定条目：pass 57 / fail 0 / partial 0 / blocked 0；偏差记录 5 条；下一迭代候选 5 条）

判定说明：本轮的核心待复核缺口——PR 验收 7 / T4-4 的「hub 调用面完成（`call_result`）不产生通知」——在 `a431225` 上以三条独立证据闭合：① 服务端帧级（调用面轮次在 `GET /api/events` 上零 `chat_state` 帧、定向链路逐字不变、消息面仍全局可观测）；② 断言级（新文件在回退 `web.js` 的副本上以**预期反例**打红，在同条件修复版上绿）；③ 端到端浏览器级（调用面终态零通知、消息面终态一次通知）。上一轮已通过的面（A1~A6、A8~A10、T1~T3、T5、MI-1、MI-2+Q1、Q2、Q3、Q5、AE-1、六条红线、回归）在本轮**逐项重新取证后无回归**。偏差记录中的 D-1/D-2/D-4/D-5 均为**规格措辞与已裁决实现不一致**，按 verifier 契约不阻塞交付，建议随阶段 6 汇总更新文档。
