# 澄清记录 · prd-round-1（迭代 0021-confirmation-inbox-and-event-push，阶段 2 功能规格）

**caller**: prd
**时间**: 2026-09-13
**本轮性质**: 阶段 2 拆卡时产生的 **5 项 `model_inferred` 判定口径推导**——**不是**需求结论（`demand.md` v1.1.0 的 `model_inferred` 与 `[待裁决]` 均已归零），而是拆卡过程中暴露的"**验收标准无法唯一判定之处**"。本文件供**主 agent 真实阻塞式转呈用户裁决**（本宿主无直接对话通道，**严格禁止自问自答**）。
**本轮不需要**需求层的重新澄清：U-1~U-6 / P1~P10 / T-0~T-3 / W1~W6 / N1~N7 / M1~M6 / E1~E5 / R1~R3 之间未发现矛盾或不清晰（见 `prd.md`「疑问与越界」5）。
**状态**: **5 项全部待确认**。用户裁决后，卡内标记按 `[model_inferred MI-xx]` → `[user_confirmed MI-xx]` 同步；若被驳回，按裁决改写对应验收 / 边界并重走 Verify。**在裁决前，本阶段不视为收敛。**

---

## 一、转呈清单（建议按组提问，5 项可并为 3 组）

### 组 A：通知的触发与去重（F06 / F08）

| 编号 | 来源卡 | 待确认 | 推荐值 | 备选读法 | 不问清会怎样 |
|---|---|---|---|---|---|
| **MI-01** | F08 验收 2（E3）、F06 边界 | 刷新 / 断线重连会**重建在途列表**（T-3 裁决），而 `confirmation_required` 的产生时机是"确认项**进入 inbox**"（M5）——**重建**算不算"进入"？即：重建时是否**重复触发**通知 | **① 只在确认项首次进入 inbox 时通知一次；重建不重复通知** | ② 每次重建都重新通知一次（每次刷新都弹一遍）；③ 不规定，由实现自行决定 | ②会让"刷新一次弹一次"，与注意力驱动的初衷（减少噪声）相反，且已处理项可能被反复提醒；③则同一产品的通知行为不可预期，阶段 6 无法判定 |

### 组 B：栏内可见性的判定口径（F01 / F06）

| 编号 | 来源卡 | 待确认 | 推荐值 | 备选读法 | 不问清会怎样 |
|---|---|---|---|---|---|
| **MI-02** | F01 验收 4（W1） | `demand.md` 说的是"**跨对话集中**列出"，但没说每条**是否必须标示来源对话**——不标示也能"集中"，只是用户不知道是哪条对话在等 | **① 必须可辨识**（最小 = 能读出所属对话） | ② 不要求，栏内只列待裁决内容本身；③ 要求但不设最小口径 | ②使"跨对话集中"退化为无法归属的列表（同一时刻多条待裁决时无法知道该回到哪个对话看上下文）；③不可判定。本条是 W1 的验收口径，需明确后才能写成可测断言 |
| **MI-04** | F06 验收 3（E5） | E5 说"条目**内容与刷新前一致**"——**内容**是否包含条目在栏内的**相对顺序 / 位置**不变 | **① 只判"条目存在 + 内容（请求方 / 请求的操作 / 可选项集合）一致"，不判顺序** | ② 顺序也须一致（等于隐式要求一个稳定的排序规则）；③ 不规定 | ②会在未定义排序规则的前提下引入一条不可判的断言（`demand.md` 全篇未规定栏内排序）；③留给判定者主观解释 |

### 组 C：边界的判定面与判定样本（F11 / F02 / F06）

| 编号 | 来源卡 | 待确认 | 推荐值 | 备选读法 | 不问清会怎样 |
|---|---|---|---|---|---|
| **MI-03** | F11 验收 1（N2） | "不做确认项的持久化历史 / 审计台账"的**判定面**：只判**用户可见面**（无历史查看入口 + 已裁决项不在栏内留存），还是同时判**数据层不持久化** | **① 只判用户可见面**，不裁数据层实现 | ② 同时判数据层（等于对实现方式下隐性约束，属阶段 3 领地）；③ 不判，只写在边界里 | ②会把 N2 从"不做可见的历史功能"扩成一条实现约束（越界到架构维度）；③则 F11 不可判定，N2 只能是口号 |
| **MI-05** | F02 验收 1、F06 验收 1（E1 / E2 / E5） | E1 / E2 / E5 的**判定样本来源**：本迭代自身的执行记录未必出现"人在场裁决一次真实确认"以及"刷新 / 断线重连"的场景 | **① 构造 + 真实各半**：阶段 6 构造一次"受门禁调用 + 栏内裁决 + 刷新 / 断线重连"的样本（覆盖交互与重连半句）；本迭代自身的执行记录覆盖"真实运行中不出现无人在场放行"半句 | ② 全部用构造样本；③ 只判本迭代的真实执行记录，交互与重连半句记为 partial | E1 / E2 / E5 是本迭代三条核心裸判定。不定口径则阶段 6 只能读到"条款文字正确"，无法对"人真的能裁决""刷新后真的还在"作出判定 |

---

## 二、已按 `demand.md` 明确口径落卡、**不列为 MI** 的相邻事项（供核对，避免误当成遗漏）

| 事项 | 依据 | 落卡 |
|---|---|---|
| 三栏并存（对话列表 \| 待确认 inbox \| 对话详情）+ 不做其它入口载体 | W1 / P3② / N7 | F01 验收 1、F01 边界 |
| 跨对话集中 + 免刷新可见 + 多条全部列出 | W1 / E1 | F01 验收 2~3 |
| 受门禁调用上浮 + 选项集合来自请求本身 | W3 / M1 / P2 | F02 验收 1~2 |
| `allow` 档上浮给人裁决 / `deny` 档仍自动拒绝（不上浮、不产生条目） | M1 / T-1 第 2 批裁决 / R3 | F02 验收 4 |
| 选项为主、文本为辅、一并提交、裁决后消失 | W2 / P4 / P10-E2 | F03 验收 1~4 |
| 文本是否必填 / 空白可否提交 | M6 明列为架构待填 | F03 边界 → T-04 |
| 裁决回传请求方 + 据此继续或中止 | M2 / E2 | F04 验收 1~2 |
| 默认阻塞无上限 + 无自动裁决策略 + 无超时退化 | M3 / E4 / N6 / R1 / R2 | F05 验收 1~3 |
| 刷新 / 断线重连后未裁决项仍在栏内 + 内容一致 + 只重建在途项 | W6 / E5 / T-3 第 2 批裁决 / N2 范围澄清 | F06 验收 1~4 |
| 事件类型封闭 3 类（`chat_completed` / `chat_failed` / `confirmation_required`）+ 产生时机 | W5 / M5 / P6 | F07 验收 1~2 |
| 不含 `call_completed`（hub 调用面完成不通知） | N5 | F07 验收 3 |
| 首通道 = 浏览器 Notification API + 权限已授予时触发 + 不依赖停留 | W4 / M4 / E3 / P5 | F08 验收 1~3 |
| 权限未授予时的降级形态 | M6 明列为架构待填 | F08 边界 → T-11 |
| 不做通知分级 / 免打扰 / 声音 | N4 | F08 边界 |
| 服务与通道分离 + 扩展通道不改事件类型契约 | W4 / M4 / P5 | F09 验收 1~2 |
| 服务边界不变（仅 `127.0.0.1` / 零鉴权 / 不做多用户）+ 只约束服务本身 | N1 / T-2 第 2 批裁决 | F10 验收 1~2 |
| 不做已裁决项的历史 / 审计台账（只覆盖已裁决项） | N2 / T-3 范围澄清 | F11 验收 2 |
| 不改 omp / harness 侧协议 + 确认源复用既有原语 | N3 / F1 / F2 | F12 验收 1~2 |

---

## 三、需求空缺与拆分判断（**需求层事实，非 MI**）

| # | 事项 | 本阶段处置 | 回退口 |
|---|---|---|---|
| 1 | **栏内排序 / 分组 / 条数上限**：`demand.md` 未规定 | **不新增**，只在 F01 边界显式出界（既非架构待填、也不构成验收缺口——"所有在途项可见"不依赖排序） | 若认为需要规定 → **回 `demand.md` 补一条 W**，本阶段不自行拍板 |
| 2 | **拆分未按 W 编号机械一对一**：W2 → F03 + F04；W4 → F08 + F09；W5 → F07（合并）；W6 → F06（合并） | 四处理由见 `prd.md`「疑问与越界」2 | 若认为某处应合并 / 再拆 → 仅影响该组卡的编号与索引表，不影响 W / E 覆盖完整性 |

**W→卡 与 E→卡映射的完整性**：`prd.md`「需求追溯」已逐条给出 W1~W6 / N1~N7 / M1~M6 / E1~E5 / R1~R3 的落地位置，**无未承载项**；本阶段**未自行增补任何需求条目**。

---

## 四、未越界声明

- 本文件与 `prd.md`、`prd/F01~F12*.md` 是该阶段**唯一写入**（全部为**新建**文件，均位于本迭代工作区内）。
- **未修改** `demand.md` / `status.md` / `history.md` / `clarifications/` 下已有文件（`raw-request.md`、`demand-round-1-proposals.md`、`demand-round-1-verdicts.md`、`hub-execution-log.md` 均未触碰）。
- **未触碰**任何产品代码与规范：`oamp/**`、`roles/**`、`.claude/**`、`docs/iterations/0017-*/**`、`docs/iterations/0020-*/**` 仅作只读参考，**未写入**。
- **未运行**任何测试、构建或服务；**未执行**任何 git 写命令；**未写主工作区任何文件**（一切读写均为 `<工作区地址>` 下的绝对路径）。
- 本阶段**未做任何架构决策**：技术选型、数据模型、接口设计、承载形态、字段与实现细节一律以 `[架构待填]`（T-01~T-16）留白。
- **`model_inferred` 不自我宣布生效**：5 项 MI 全部待主 agent 转呈用户裁决；裁决前卡内保持 `[model_inferred MI-xx]` 标记。
