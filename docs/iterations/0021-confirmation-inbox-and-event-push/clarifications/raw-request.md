# 用户原始表述（逐字，2026-09-13）

新增需求：

1，agent中需要用户确认的单独一栏列出来，有交互可以确认选项，输入答案，按照

2，任务完成/中间确认等事件需要推送（web push）。 （事件类型可以定义，目前就定义会话完成，回传用户需求确认的事情）

这次依然需要使用workflow-pb的工作流，但是agent任务请使用hub api执行，记录下执行过程。接口文档：http://127.0.0.1:7788/docs

## 主 agent 的读法与边界（待阶段 1 收敛，不构成结论）

- 第 1 条字面结尾的「按照」为断句残留，字面不可解 ⇒ 必须在阶段 1 向用户澄清该条的确切语义。
- 第 2 条「web push」为歧义点（浏览器 Web Push / Notification API / 仅服务端推送通道），事件类型「会话完成」「回传用户需求确认的事情」是给出的首批类型。
- 第 3 条是**执行方式约束**（本迭代的流程如何跑）：workflow-pb 工作流 + 角色任务经 hub API 派发（`POST /api/calls`）+ 记录执行过程；而非产品需求。其可执行性已由迭代 0018 交付的调用面提供。
