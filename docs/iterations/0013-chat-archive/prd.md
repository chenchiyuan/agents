# prd.md — 0013-chat-archive

**版本**: 0.1.0
**迭代**: 0013-chat-archive
**创建日期**: 2026-09-11
**阶段**: 功能规格（阶段 2）
**状态**: 阶段 2 产物 + 阶段 3 架构回填（2026-09-11）。产品维度（功能点、验收标准、边界）已锁定为 5 个功能点且**未改动**；架构维度 AR-01~AR-17 已由阶段 3 全部落定（逐条摘要见文末「架构维度落定」，完整推导见 `architecture.md`）；`[model_inferred]` 5 项已于 2026-09-11 全部确认（M-01~M-05，5/5），无残留。功能卡的产品结论段不含任何技术选型；本次的技术选型、数据模型与接口设计集中在各卡的「架构维度」段与文末「架构维度落定」段。
**输入合同**: `docs/iterations/0013-chat-archive/demand.md`（v0.2.0 已确认，2026-09-11；A-1~A-10 为唯一功能来源，N-1~N-9 为不做项，E-1~E-8 为验收标准的直接来源）
**背景参考**: `docs/iterations/0012-roles-agent-cluster/prd.md` 与 `prd/F01-*.md`（**仅作卡片格式参照**，未写入任何功能结论）
**术语口径**：功能卡的验收标准与边界一律使用产品语言（进行中 / 已完成 / 失败 / 已关闭）。追溯表、拆分理由与架构待填项中出现的 `working` / `completed` / `failed` / `closed` 系对 `demand.md` 原文与 0011 既有规范的引用，仅为可读性与可追溯性，不构成本阶段对数据模型或状态取值的任何决策。

---

## 功能点索引

| ID | 功能名 | 用户价值摘要 | 规格卡 | model_inferred | 来源条款 | 架构维度（阶段 3 已填） |
|---|---|---|---|---|---|---|
| F01 | 一键归档（入口、范围、确认、结果可见性） | 主列表堆满历史对话时，一次操作把所有"已经结束的"收起来，不误伤进行中的对话 | [F01](prd/F01-bulk-archive-entry-scope.md) | M-01、M-05（已确认） | A-1、A-8、A-9 | AR-01、AR-02、AR-03 |
| F02 | 归档语义（独立标记、归档时间、只读、历史保留） | 归档后"收起来但没丢"——历史随时能翻，又不会被误发消息打断，原状态仍分得清 | [F02](prd/F02-archive-marker-readonly.md) | — | A-2、A-5（前半） | AR-04、AR-05、AR-06、AR-07 |
| F03 | 归档分类视图（标签页、内容、排序、主列表排除、空态） | 收起来的对话集中在一处，最近收起的排最前，每项旁边就有捞回入口 | [F03](prd/F03-archive-tab-view.md) | M-02（已确认） | A-3、A-10 | AR-08、AR-09、AR-10 |
| F04 | 归档列表容量（全部可达、加载更多） | 归档再多也找得到，不会"明明归档了却翻不出来" | [F04](prd/F04-archive-list-capacity.md) | M-03（已确认） | A-7 | AR-11、AR-12 |
| F05 | 单个激活（恢复可续、置顶、状态还原、上下文不延续提示） | 一键把某段对话捞回主列表最上面、马上能接着聊，并明确知道它已不记得此前内容 | [F05](prd/F05-single-activate-restore.md) | M-04（已确认） | A-4、A-5（后半）、A-6 | AR-13、AR-14、AR-15、AR-16 |

**合计**：5 个功能点。`demand.md` 第二段 §1 的 A-1~A-10 全部覆盖，无漏项、无 demand 之外的新增功能点（逐条见「需求追溯」）。

---

## 本次迭代边界说明

### 包含

- **一键归档**（A-1 / A-8 / A-9 → F01）：过滤栏右侧「归档全部」入口；范围 = 所有未归档且非进行中的对话（已完成 / 失败 / 已关闭）；已归档跳过；一次轻确认；批量结果可见且不回滚已成功项。
- **归档语义**（A-2 / A-5 前半 → F02）：独立归档标记 + 本次归档时间；归档不改变对话原状态；归档对话只读（拒收新输入）、历史完整可读；归档状态持久化（重启不变）；归档释放常驻上下文。
- **归档分类视图**（A-3 / A-10 → F03）：过滤栏「归档」标签页；只列已归档、主列表不再显示；归档时间倒序（同时间按对话标识倒序稳定排序）；每项含标题 / agent / 归档时间 / 原状态标识 / 独立可点的「激活」按钮；空态文案。
- **归档列表容量**（A-7 → F04）：归档视图可访问全部已归档对话；较大首屏 + 底部继续加载；续页保持同一排序；末尾无死控件。
- **单个激活**（A-4 / A-5 后半 / A-6 → F05）：移除归档标记、立即恢复可继续对话、移出归档视图并置顶 All 列表；状态还原（已关闭 → 已完成并清除关闭时间）；不恢复上下文；给出"不再记得此前内容"的显式提示；非归档的已关闭对话仍不可重开。

### 不包含（边界，源自 demand 第二段 §2 N-1~N-9）

- **N-1 不做对话删除** —— 落 F01 / F02 / F05 边界（归档不是删除，数据保留且可恢复）。
- **N-2 不做自动归档策略**（按时间 / 数量阈值自动触发）—— 落 F01 边界。
- **N-3 不做批量激活** —— 落 F03 / F05 边界。
- **N-4 不做上下文还原**（激活不恢复被释放的常驻上下文）—— 落 F02 / F05 边界与 F05 验收 5。
- **N-5 不新增通用"重开对话"能力**（可重开仅通过"归档 → 激活"路径）—— 落 F05 验收 8 + F05 边界。
- **N-6 不新增归档专属搜索 / 时间过滤维度** —— 落 F03 / F04 边界。
- **N-7 不做归档项的手动排序 / 排序自定义** —— 落 F01 / F03 边界。
- **N-8 不做多用户 / 权限** —— 全局边界 + 各卡边界（沿用 0011 既有边界）。
- **N-9 不做历史数据迁移**（库中已有已关闭对话视为"未归档"，保持现状）—— 落 F01 / F02 边界。
- **全局**：不做 `demand.md` 之外的新增功能点（删除、自动归档、批量激活、上下文还原、通用重开、归档专属过滤、排序自定义、多用户权限、数据迁移均已列入对应卡边界）。

---

## 需求追溯（与 demand.md 一一对应）

### A 条款（做什么）

| demand 条目 | 覆盖功能卡 | 覆盖点 |
|---|---|---|
| A-1 一键归档入口与范围 | F01（验收 1~4） | 入口文案与位置；范围 = 未归档且非进行中；已归档跳过；主列表只剩进行中对话的后果显式化 |
| A-2 归档语义（释放上下文 / 打标记与归档时间 / 只读可查看历史） | F02（验收 1~7） | 独立标记 + 原状态保留；归档时间记录与刷新；只读；历史完整；持久化；上下文释放的可观察后果 |
| A-3 归档分类视图 | F03（验收 1~7） | 标签页入口；内容互斥；归档时间倒序（同时间按对话标识倒序）；项构成；独立可点激活按钮；时间展示可区分先后 |
| A-4 单个激活 | F05（验收 1~3、5、7） | 移除标记并从归档视图消失；立即恢复可继续对话；置顶 All；不恢复上下文；单条操作 |
| A-5 状态还原规则 | F02（验收 1）+ F05（验收 4） | 归档不改变原状态；激活保持原状态；已关闭 → 已完成并清除关闭时间 |
| A-6 上下文不延续的显式提示 | F05（验收 6） | 激活后打开对话可见"此后不再记得此前内容"说明；回复不含归档前内容 |
| A-7 归档列表容量 | F04（验收 1~4） | 全部可达；首屏覆盖较大范围；继续加载；续页排序一致 |
| A-8 批量归档结果可见性 | F01（验收 6、7） | 结果反馈不静默；失败不回滚；失败项留主列表可重试 |
| A-9 一键归档二次确认 | F01（验收 5） | 一次轻确认；确认后执行、取消不执行；文案不显示条数 |
| A-10 归档页空态 | F03（验收 6） | 无归档时显示"还没有归档的对话" |

### N 条款（不做什么 → 落入卡边界）

| demand 条目 | 落入位置 |
|---|---|
| N-1 不做对话删除 | F01 / F02 / F05 边界 |
| N-2 不做自动归档策略 | F01 边界 |
| N-3 不做批量激活 | F03 / F05 边界 |
| N-4 不做上下文还原 | F02 / F05 边界；F05 验收 5 |
| N-5 不新增通用重开 | F05 验收 8 + F05 边界 |
| N-6 不新增归档专属过滤维度 | F03 / F04 边界 |
| N-7 不做手动排序 / 排序自定义 | F01 / F03 边界 |
| N-8 不做多用户 / 权限 | 本文件「不包含」+ F01 / F03 / F05 边界 |
| N-9 不做历史数据迁移 | F01 / F02 边界 |

### E 条款（裸判定效果 → 一致性映射）

| demand 条目 | 映射到的验收标准 | 可判定性说明 |
|---|---|---|
| E-1 主列表不再有非进行中对话 + 归档页倒序 | F01 验收 2、3、6 + F03 验收 2、3 | 拆分：**动作范围与结果反馈**（F01）与**归档视图的列出与排序**（F03）是两个独立失败模式，分别断言 |
| E-2 归档对话可打开、历史完整可读 | F02 验收 5 | 逐条比对归档前后消息序列 |
| E-3 归档对话无法继续对话 | F02 验收 3 | 发送后无新输入记录、有拒绝提示 |
| E-4 激活三步（归档页少一条 / All 多一条 / 发消息有回复） | F05 验收 1、2、3 | 三步分别独立断言 |
| E-5 进行中对话归档后仍在主列表且状态未变 | F01 验收 2 | 归档前后对同一条进行中对话做对照断言 |
| E-6 重启后归档状态与归档时间不变 | F02 验收 6 | 重启前后归档视图内容一致性 |
| E-7 归档数超默认单页上限仍可访问全部 | F04 验收 1、3 | 归档 60+ 条后逐页浏览到末条 |
| E-8 激活后回复不再记得归档前内容 + 同级提示 | F02 验收 7 + F05 验收 5、6 | 拆分：**上下文确已释放**（F02，经激活后观察）与**激活后的界面提示**（F05 验收 6）分别断言 |

### D 决策条款与 M 澄清结论

| 条目 | 处理方式 |
|---|---|
| D-1 归档范围（除进行中外全部） | F01 验收 2、3 |
| D-2 归档 = 关闭上下文 + 分类标记；只读；单个激活 = 恢复可续 | F02 验收 1、3、7；F05 验收 1、2 |
| D-3 左栏顶部「归档」标签页 | F03 验收 1 |
| D-4 归档时间倒序 | F03 验收 3 |
| D-5 做法 A：独立标记，不新增状态取值；激活时已关闭 → 已完成并清关闭时间 | F02 验收 1；F05 验收 4 |
| D-6 可重开范围收窄仅限归档 → 激活路径 | F05 验收 8 |
| D-7 首屏较大 limit + 加载更多 | F04 验收 1~3 |
| D-8 一次轻确认（不显示条数） | F01 验收 5 |
| D-9 激活后置顶主列表 | F05 验收 3 |
| D-10 批量部分失败：不回滚 + 显式提示 | F01 验收 6、7 |
| D-11 一键归档入口 = 过滤栏右侧，「归档全部」 | F01 验收 1 |
| D-12 激活后提示"此后不再记得此前内容" | F05 验收 6 |
| D-13 归档项 = 归档时间 + 独立可点激活按钮 | F03 验收 4、5 |
| D-14 做法 A 即用户确认的机制（无额外机制想法） | F02 验收 1 |
| M-1 归档为独立标记（非新增 state 取值） | F02 验收 1 |
| M-2 一键归档按钮位于过滤栏右侧 | F01 验收 1 |
| M-3 归档项显示标题 + agent + 归档时间（+ 原状态 badge） | F03 验收 4 |
| M-4 激活按钮独立可点、不触发行打开详情 | F03 验收 5 |
| M-5 归档页空态"还没有归档的对话" | F03 验收 6 |
| M-6 需要一次轻确认 | F01 验收 5 |
| M-7 批量部分失败不回滚但必须显式提示 | F01 验收 6、7 |
| M-8 主列表不显示已归档对话 | F03 验收 2 |
| M-9 归档页必须能访问全部已归档对话 | F04 验收 1 |
| M-10 较大首屏 + 加载更多（沿用现有分页协议） | F04 验收 2、3 |
| M-11 激活后置顶 | F05 验收 3 |

**结论**：5 张卡覆盖 A-1~A-10 全部功能点；N-1~N-9 全部落入对应卡边界或全局边界；E-1~E-8 全部映射为可判定验收；D-1~D-14 与 M-1~M-11 全部有落点，**无 demand.md 之外新增的功能能力**。

---

## 拆分理由（原子粒度判断）

- **F01 独立成卡，且 A-9 / A-8 不单独成卡**：F01 的失败模式是"按钮不在 / 范围错（误伤进行中或漏掉已关闭）/ 没有确认 / 失败无声"，观察面是一次点击前后两个列表的差异。A-9（二次确认）是这次点击的前置步骤、A-8（结果可见性）是这次点击的收尾——两者都无法脱离这一次点击被独立交付与独立验收，拆开会得到两张没有自身触发入口的卡，故并入 F01 作为验收 5 与验收 6/7。
- **F02 与 F05 分拆（归档与激活）**：归档是"进入只读"、激活是"退出只读并恢复可续"，两者互为反面但独立可测、独立可交付——归档有效而激活无效（或反之）都是可独立出现的失败。A-5（状态还原规则）横跨两卡：其"归档不改变原状态"只能由归档动作后观察到（落 F02 验收 1），"激活后保持原状态 / 已关闭例外"只能由激活动作后观察到（落 F05 验收 4）；硬塞进一张卡会让另一半失去独立验收条件。
- **F03 与 F04 分拆（归档视图与容量）**：A-3 的失败模式是"内容 / 顺序 / 入口 / 项构成不对"，3 条数据即可测；A-7 的失败模式是"超过单页上限后归档项看不见"，必须 60+ 条数据才能测。两者测试前置条件与失败模式都不同，故按独立验收拆开。与此对照，A-10（空态）与 A-6（激活后提示）**未**单独成卡：空态是归档视图在"零条"时的展示分支、提示只在激活动作之后才有意义，两者都没有独立于所属功能的交付物，故分别落 F03 验收 6 与 F05 验收 6。
- **F03 与 F05 未合并**：F03 判"归档视图列什么、怎么排、怎么进入"，F05 判"点激活之后发生什么"。归档视图正确但激活无效、或激活有效但归档视图列错，都是可独立出现的失败。
- **F01 与 F02 分拆（批量动作与单条语义）**：F01 判"谁被归档、由什么触发、结果如何反馈"，F02 判"被归档后这条对话处于什么状态"。批量执行成功但单条语义错（例如归档后仍能发消息）是典型可独立出现的失败。
- **未单立"既有契约同步"卡**：`demand.md` §3 列出的已知受影响契约（测试断言、README 中"closed 不可重开"表述收窄）属实现的已知成本，不构成独立功能能力，列为本文件「架构待填」的全局项 AR-17，不另立卡片（另立会构成 demand 之外的新增功能点）。

---

## model_inferred 列表（已全部确认 5/5，2026-09-11；无残留）

| 编号 | 来源卡 | 推导内容 | 推导依据 | 确认状态 |
|---|---|---|---|---|
| M-01 | F01 验收 8 | 一键归档在"零可归档项"（全部已归档，或只剩进行中对话）时，操作结束后仍必须给出结果反馈（0 条），不得静默无响应 | A-8 要求"操作结束后必须给出结果反馈、不允许静默"，但未单独覆盖零条分支；把"零条也反馈"提升为必须项属产品逻辑推导 | **已确认**（2026-09-11） |
| M-02 | F03 验收 7 | 归档时间的展示格式沿用既有列表的时间展示方式，只要求"用户可读、足以区分先后" | M-3 / D-13 只要求归档项"显示归档时间"，未定展示格式；沿用既有格式是产品一致性推导，不引入新格式设计 | **已确认**（2026-09-11） |
| M-03 | F04 验收 5 | 归档视图已无更多可加载时，不再提供继续加载入口 | A-7 只要求"可访问全部"，未说明加载到底后的控件行为；"不留死控件"是该要求的最小完备形式 | **已确认**（2026-09-11） |
| M-04 | F05 验收 6 | A-6 提示的展示时机与持续范围——**用户确认取值：在对话头部显示提示，直到该对话产生新的回答后自动消失** | D-12 只说"激活后打开该对话时给出说明"，未说明是一次性还是持续；该取值由用户于 2026-09-11 确认 | **已确认**（2026-09-11） |
| M-05 | F01 验收 6 | 一键归档执行结束后，主列表立即反映归档结果，无需用户手动刷新页面 | E-1 的观察方式是"点击并确认后，主列表不再出现任何非进行中的对话"，隐含结果即时可见；demand 未显式说明刷新时机 | **已确认**（2026-09-11） |

> 上述 5 项均为**验收标准判定面的推导**，不改变任何功能范围；已于 2026-09-11 经用户**全部确认（5/5）**，卡内标记同步改为 `[user_confirmed M-xx]`，本文件无 model_inferred 残留。

---

## 架构维度落定（阶段 3 已填，2026-09-11）

**总览**：AR-01~AR-17 全部落定。完整推导见 `architecture.md`（§3 数据层与迁移 / §4 写口与只读 / §5 接口 / §6 前端 / §7 契约同步 / §10 逐条落定），逐卡落定见 `prd/F01`~`prd/F05` 各自文末的「架构维度」段。**产品维度未改动**；本次未引入任何新模块 / 新进程 / 新传输 / 新第三方依赖（L1 决策：无）。

### 阶段 2 原始待填项（追溯保留）

| 编号 | 来源卡 | 待填内容 |
|---|---|---|
| AR-01 | F01 | 归档动作的接口形态与批量编排（单条 / 批量；逐条调用 / 后端一次编排；接口命名与请求响应字段） |
| AR-02 | F01 | 批量结果的可见性承载（成功 / 失败条数与失败项清单的返回形态与展示位置） |
| AR-03 | F01 | 一键归档按钮在过滤栏右侧的呈现承载（既有前端静态结构的扩展方式） |
| AR-04 | F02 | 归档标记与归档时间的持久化承载（字段命名、类型、未归档项默认取值、写入时机） |
| AR-05 | F02 | 数据层显式迁移写法（现状无迁移机制，对已存在库新增字段必须显式迁移；幂等保证） |
| AR-06 | F02 | 只读判定路径的落实（归档标记只读 vs 既有 closed 只读的落点与优先级） |
| AR-07 | F02 | 上下文释放的触发方式（复用既有释放通道的具体时机与形式） |
| AR-08 | F03 | 归档列表的查询维度（"是否归档"如何进入列表查询；默认排除已归档与归档视图只取已归档的落点） |
| AR-09 | F03 | 归档标签页的承载方式（过滤标签集合的扩展方式与选中态表达） |
| AR-10 | F03 | 激活按钮与行点击的交互隔离方式（不触发行打开详情的交互承载） |
| AR-11 | F04 | 每页条数与翻页参数形态（首屏 / 续页取值、沿用既有分页协议还是新增） |
| AR-12 | F04 | 归档视图的加载状态维护（已加载范围、是否还有更多、与筛选条件的联动） |
| AR-13 | F05 | 激活的接口形态（复用既有状态变更路径 vs 新增能力） |
| AR-14 | F05 | 状态还原的落点（closed → completed 且清除关闭时间的判定与写入落点，及与既有状态流转的收窄关系） |
| AR-15 | F05 | 置顶的实现位置（"最近更新时间前移"的写入位置与对既有排序的影响） |
| AR-16 | F05 | 上下文不延续提示的承载（与既有上下文释放提示的关系、同级说明的复用方式、呈现位） |
| AR-17 | 全局 | `demand.md` §3 已知受影响契约的同步更新（既有测试的列名 / 字段白名单 / 前端静态与接口契约断言；README 中"closed 不可重开"表述按 N-5 收窄） |

> 其中 AR-04 / AR-05 直接对应 `demand.md` 第二段 §3 的"数据层必须显式迁移"与"归档标记独立于 state"；AR-01 / AR-07 / AR-11 对应 §3 中"批量归档的编排与接口命名 / 字段名属架构决策，本文件不预设"。本阶段严格未预设任何一项。

### 阶段 3 落定摘要（逐条）

| AR | 落定内容（一句话） | 详见 |
|---|---|---|
| AR-01 | 归档 = **后端一次编排的批量端点** `POST /api/chats/archive`（无请求体，范围由服务端按 A-1/D-1 计算；内部逐条 `UPDATE`，成功项不回滚，失败项可重试）；**不提供单条归档端点**（产品无触发点） | `architecture.md` §5.1 / F01 |
| AR-02 | 响应 `{ archived, failed, failed_ids }`（三键恒存在；零条也反馈）；界面承载 = 既有 `#hint` 行；**不发 SSE 事件**（结果可见性由响应 + 前端重载承载） | §5.1 / §6.2 / F01 |
| AR-03 | `web/index.html` 的 `.filters` 内新增 `<button id="btn-archive-all" class="archive-all">归档全部</button>`，`margin-left:auto` 抵到同一行最右；点击后 `window.confirm('将归档全部非进行中的对话，是否继续？')` | §6.1 / F01 |
| AR-04 | `chats.archived_at INTEGER`（**可空**，`NULL` = 未归档；标记与归档时间同址）+ `chats.context_released INTEGER NOT NULL DEFAULT 0`（归档置 1 / 输出置 0 / 激活不改） | §3.1 / F02 |
| AR-05 | `openDb` 内 `migrate(db)`：以 `pragma_table_info('chats')` 的**列存在性**为守卫，缺列则 `ALTER TABLE chats ADD COLUMN …`（幂等、新库 no-op、新库列序与迁移库一致、既有行 `NULL`/`0`）；**不引入 `user_version`** | §3.2 / F02 |
| AR-06 | 判定点唯一：`POST /api/messages` 的 409 分支扩为 `archived_at IS NOT NULL`（优先）与 `state === 'closed'`（既有，文案不变）两条独立路径；DB 层另有 `archiveChat`/`activateChat` 的结构性守卫 | §4.4 / F02 |
| AR-07 | 复用既有 `sendControlNotice(agentId, { kind:'context_release', chat_id })`，对每个成功归档的 chat 逐条发送；agent 集 = `chats.agent_id ∪ DISTINCT messages.agent_id`；best-effort；不新造消息类型 | §4.3 / F02 |
| AR-08 | CTE 加第 6 槽 `archived`（缺省 `0` = 主列表排除已归档；`1` = 归档视图只取已归档）+ 互斥谓词 + 条件排序键（归档视图 `archived_at DESC, chat_id DESC`）；`countChats` 共用同一 FROM 与参数 ⇒ `total` 恒一致 | §3.3 / F03 |
| AR-09 | 过滤栏第 4 颗**静态** `.filter[data-filter="archived"]`（与既有三颗同构），选中态沿用 `.filter.active`；空态「还没有归档的对话」 | §6.1 / F03 |
| AR-10 | 归档行 `.meta` 内新增 `button.activate`，绑定 `e.stopPropagation()`；行级 `openChat` 绑定不变；时间用既有 `fmtTime(c.archived_at)` | §6.2 / F03 |
| AR-11 | 首屏 = 续页 `limit=200`（`ARCHIVE_PAGE_SIZE`，既有 API 上限）+ `offset` 递增；**沿用既有分页协议，不新增后端形态** | §5.2 / F04 |
| AR-12 | `state.archive = { chats, total, loading }`；已加载范围 = `chats.length`、还有更多 = `chats.length < total`（total 同源 `countChats`）；无更多则不渲染「加载更多」 | §6.2 / F04 |
| AR-13 | **新增** `POST /api/chats/<chat_id>/activate`（200 `{chat_id,state}` / 404 / 409「未归档」），与既有 `<id>/close` 同构；**不引入通用 PATCH/PUT** | §5.3 / F05 |
| AR-14 | **一条** `UPDATE`：`SET archived_at=NULL, state=CASE WHEN state='closed' THEN 'completed' ELSE state END, closed_at=CASE WHEN state='closed' THEN NULL ELSE closed_at END, updated_at=? WHERE chat_id=? AND archived_at IS NOT NULL`；`WHERE` 把 N-5 的收窄例外落成结构性保证 | §4.2 / F05 |
| AR-15 | 置顶 = 同一语句内的 `updated_at = ?`（激活时刻）⇒ 命中主列表既有 `ORDER BY updated_at DESC, chat_id DESC`；**不新增排序字段、不改排序规则** | §4.2 / F05 |
| AR-16 | 说明条位于消息区顶部（对话头部）、复用既有 `.notice-bar`；驱动源 = 服务端 `chats.context_released`（归档置 1、`insertOutput` 置 0 ⇒ 产生新回答后自动消失）；既有 SSE notice 链路保持原样 | §6.4 / F05 |
| AR-17 | 同步清单：`persist.test.js`（列名 +2 / 写口白名单 +2 并排除 `listArchivable` / 详情键 +2 / 两处 `deepEqual` +2 键 / 新增迁移与双视图用例）、`web.test.js`（既有断言不变 + 新端点与静态契约用例）、`README.md`（UI / API 表 / `closed` 收窄例外 / 归档释放与激活不恢复） | §7 |

---

## 疑问与越界

1. **未发现 `demand.md` 内部矛盾**：A 条款、N 条款、D 决策、M 澄清结论之间一致；C-1（0011 closed 终态 vs 可激活）已在需求阶段以 D-6 / N-5 的收窄例外解决，本阶段据其写成 F05 验收 8，未做任何调整。
2. **demand 中带有实现指向的措辞已按产品可观察行为改写**：A-2 ①"复用现有 `context_release` 通道"、A-7 的"较大首屏 limit（API 上限 200）+ offset 翻页"、A-9 的"原生 confirm"均在卡片中被改写为可观察的产品行为（释放上下文的可观察后果、全部可达与继续加载、一次轻确认），机制层面分别标为 AR-07 / AR-11 与 F01 验收 5 的形态描述，**未在卡内写死接口形态或平台 API**。
3. **A-5 被拆分到两张卡**：归档不改状态（F02 验收 1）与激活状态还原（F05 验收 4）分属两个动作的观察面，追溯表已显式标注为"A-5 前半 / 后半"，请主 agent 知悉这是拆分而非遗漏。
4. **M-04（A-6 提示的展示时机）已确认（2026-09-11）**：D-12 未说明提示是一次性还是持续显示；用户确认取值为"在对话头部显示提示，直到该对话产生新的回答后自动消失"，F05 验收 6 已按此定稿。
5. **F01 验收 3 显式化了 demand §4.2 记录的范围后果**（一次归档全部后主列表只剩进行中的对话）：这是需求合同要求写明、避免用户事后意外的内容，不是本阶段新增的行为要求。
6. **F03 验收 5 与 F05 的接口边界**：归档视图中的「激活」按钮存在性（F03）与激活执行后的结果（F05）被分开验收——按钮不可点属于 F03 失败，点了没反应属于 F05 失败，两者是不同失败模式；如主 agent 认为应合并，请指出。
7. **未越界声明（阶段 3 更新）**：未修改 `demand.md`、未修改 `oamp/**`（代码零改动）；`roles/**` 除本角色自己的决策记录（`roles/architect/data/0013-chat-archive-arch-decision-notes.md`，新增）外未改动（角色定义零改动）；5 张功能卡的产品结论段未改动，技术选型 / 数据模型 / 接口设计只写入各卡的「架构维度」段与 `architecture.md`；AR-01~AR-17 已全部落定（见文末「架构维度落定」）。**遗留待主 agent 知悉（均为 L2 取舍，不阻断实现）**：① `chats.context_released`——为承载 F05 验收 6 的提示持续期而新增的列（`architecture.md` §13 K-2 / §15 R-4）；② 「按列存在性守卫的幂等迁移、不引入 `user_version`」（§13 K-1）。
