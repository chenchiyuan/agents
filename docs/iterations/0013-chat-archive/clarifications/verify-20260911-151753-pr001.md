# 独立验收报告 — PR-001（数据层：归档列 + 幂等迁移 + 双视图查询 + 三个新写口）

**验证者身份**：同级代码审查者（SQLite 数据层 / 持久层契约方向）——有资格评判 `node:sqlite` 建库与迁移写法、单语句查询语义、写口守卫的人。验证者只接收「产出物 + 验证标准」，未接收任何执行过程上下文。
**产出物**：`.pb-agents/worktrees/0013-pr-001-persist`（分支 `feat/0013-pr-001-persist`，HEAD = `243e113`；base = `iteration/0013-chat-archive` 的 `47ea70c`）
**验证标准来源**：主 agent 委托的 7 条验证标准（§验证标准 1~7）；PR 卡的 8 条验收；`architecture.md` §3.2（M-1~M-5）/ §3.3（V-1~V-5）/ §7.1
**验证日期**：2026-09-11
**验证方式**：只读被验 worktree；所有独立探针与真实旧库副本落在 `/tmp/verifier0013-real/`，未写入 worktree，未触碰主仓库 `oamp/data/sql.db`（验证前后 md5 恒为 `b020c4fd4c3e63424a98e01573b24393`）

---

## 逐项判定

### 标准 1：PR 卡 8 条验收逐条判定 — **pass**

| # | 验收条目 | 判定 | 可定位证据 |
|---|---|---|---|
| 1 | 迁移幂等与新旧库同构（M-1/M-2/M-3） | **pass** | `src/persist.js:42-52`（MIGRATIONS + 按列存在性守卫的 `migrate`）、`:141`（`exec(SCHEMA)` 之后、`stmts` 之前调用）；SCHEMA 新列声明在 `closed_at` 之后（`:20-22`）。单测 `test/persist.test.js:84-142` 用 7 列旧 SCHEMA 建库并插行，断言列序 = `CHATS_COLUMNS_9`（`:80-82`）、旧行 `archived_at === null` / `context_released === 0`、`close()` 后再开列集不变、新建库与迁移库列序逐位相同。**另经独立真实旧库实测（见标准 2）** |
| 2 | `archiveChat` 双守卫 | **pass** | 语句 `:168-171`（`WHERE chat_id = ? AND archived_at IS NULL AND state != 'working'`），包装 `:234-236`（`changes > 0`）。单测 `:454-479` 覆盖 working → `false` 且两列不变、已完成 → `true` 且 `archived_at` = 传入毫秒 / `context_released = 1` / `state` 不变、重复归档 → `false` 且时间不刷新、未知 chat → `false`。**独立探针复现一致** |
| 3 | `activateChat` 一条语句三件事 | **pass** | 语句 `:174-181`（一条 UPDATE：清 `archived_at` + `CASE WHEN state='closed' THEN 'completed'` + `CASE` 清 `closed_at` + `updated_at = ?`；`WHERE chat_id = ? AND archived_at IS NOT NULL`），包装 `:238-240`。单测 `:481-514` 覆盖未归档 → `false` 且整行 `deepEqual` 不变、closed 来源 → 三件事齐备且 `context_released` 保持 1、completed 来源 → 状态保持、重复激活 → `false` 且 `updated_at` 不再前移。**独立探针复现一致（含 failed 来源）** |
| 4 | 双视图互斥与 `total` 一致性 | **pass** | `LIST_WITH` 第 6 槽 `:64`、`LIST_FROM` 互斥谓词 `:71`、条件排序键 `:196`、`readArchived` `:104-112`、参数数组一次构造并同值喂给两条语句 `:264-266`。单测 `:547-579`。**独立探针覆盖 4 状态 × 2 视图 + 6 类非法值（见标准 3）** |
| 5 | 归档视图分页 | **pass** | 单测 `:581-595`（60 条、`limit 20` 三页、并集 60 无重复、`total` 恒为 60、顺序 = 归档时间倒序）。**独立探针另用 65 条 × `limit 20`/`limit 7` 两种切分复现（见标准 3）** |
| 6 | `listArchivable()` 范围 | **pass** | 语句 `:183-185`（`WHERE archived_at IS NULL AND state != 'working' ORDER BY chat_id`），包装 `:242-244`。单测 `:516-532`（completed/failed/closed 入选，working 与已归档排除，归档一条后候选集即时收缩）。**独立探针以全表 ground truth 逐行比对 `listArchivable()` 输出，完全相等** |
| 7 | 既有契约同步（AR-17） | **pass** | 逐条见标准 5，四处逐字断言均按 §7.1 更新且无削弱 |
| 8 | 测试全绿 + 范围外零改动 | **pass**（附一条观察） | `node --test test/persist.test.js` → 36/36 pass；`node --test --test-concurrency=1 test/*.test.js` → 210/210 pass、exit 0（127s）。`git diff --name-status 47ea70c..243e113` = 2 个 oamp 文件（`src/persist.js` M、`test/persist.test.js` M）+ 1 个 docs 文件。**实施提交 `243e113` 自身 `--stat` 恰为这 2 个 oamp 文件**；多出的 `prs/pr-001-...-tasks.md` 属规划者提交 `d8aa6d3`（先于实施提交落在同一分支），非本 PR 的实现改动。其余 19 个既有测试文件、`web.js`、`web/**`、`README.md` 零改动 |

### 标准 2：迁移幂等性（真实旧库实测） — **pass**

**方法**：`cp -p oamp/data/sql.db /tmp/verifier0013-real/sql.db`（真实库含 **40 条 chats / 86 条 messages**，初始列序为 7 列旧形态），用独立脚本 `probe.mjs` 调被验 `openDb` 做三次开库/关库，逐次取全库快照（`sqlite_master` 对象、`pragma_table_info`、全表行 dump、`PRAGMA user_version`、`journal_mode`）与文件 md5 比对。

| 子项 | 结论 | 证据 |
|---|---|---|
| ① 打开后列补齐为 9 列且与新建库列序一致 | **pass** | 旧库补列后 `['chat_id','title','agent_id','state','created_at','updated_at','closed_at','archived_at','context_released']`，与新建库 `pragma_table_info` 逐位相同；新增两列确为末尾追加 |
| ② 既有行 `archived_at IS NULL` / `context_released = 0` | **pass** | 40 行全部满足（对全表逐行判定，非抽样） |
| ③ 旧数据零变化 | **pass** | chats 行（除新增两列）与 messages 全表 dump 与迁移前逐字节相等；行数 40/86 不变 |
| ④ 二次、三次打开无写入 | **pass** | 首次 open+close 后 md5 = `a10eb2418c5413ca16495022d750bbd3`；第二次、第三次 open+close 后 md5 完全一致（首次变化来自补列，属预期） |
| ⑤ 未引入 `user_version` / 迁移表 | **pass** | `sqlite_master` 对象清单（type+name）迁移前后完全一致：4 个对象（chats / messages / idx_chats_updated / idx_messages_chat_time）；`PRAGMA user_version` 前后均为 0 |
| 附加 | **pass** | 主仓库 `oamp/data/sql.db` 验证前后 md5 恒为 `b020c4fd4c3e63424a98e01573b24393`（未被触碰） |
| 边界（额外） | **pass** | 零字节库文件 → 正常建成 9 列；**部分迁移自愈**：只补了 `archived_at` 的中间态库 → 再 open 恰好补 `context_released`，既有行 `archived_at = 4242` 保留、`context_released = 0`；全量列已存在的库再开无副作用（`probe4.mjs`） |

### 标准 3：双视图正确性 — **pass**

**方法**：独立探针（`probe2.mjs`）自造混合数据（未归档 working/completed/failed/closed + 5 条已归档含两条同 `archived_at`），不使用被验单测的任何断言。

| 子项 | 结论 | 证据 |
|---|---|---|
| 缺省列表排除已归档 | **pass** | 缺省 `listChats()` 返回 4 条未归档，`archived_at` 全为 `null`，无一条已归档泄漏 |
| 归档视图只取已归档且 `archived_at DESC` | **pass** | `archived:1` 返回 5 条，全为已归档；顺序 `arch-tie-b > arch-tie-a`（同 `archived_at` 时 `chat_id DESC` 兜底）→ `arch-closed > arch-failed > arch-completed`，与 `archived_at` 降序完全一致 |
| `countChats` 与 SELECT 的 `total` 一致 | **pass** | 每页 `total` 恒为视图全量（65），与 `limit` 无关；大 `limit` 下 `total === chats.length`；`:264-266` 证实二者共用同一参数数组 |
| `state` 与 `archived` 正交 | **pass** | 四状态 × {缺省 / `archived:0` / `archived:1`} 全测：缺省 ≡ 显式 `0`；主列表与归档集合**互斥**（无交集）；各查询 `total` 与返回集一致 |
| 非法 `archived` 抛错 | **pass** | `2 / -1 / '1' / 1.5 / true / {}` 六类全部抛 `查询参数非法: archived 需为 0|1`；`undefined`/`null`/`0` 缺省为 0（`:104-112`） |
| 分页无重叠无遗漏 | **pass** | 65 条归档分别按 `limit 20`（4 页）与 `limit 7`（10 页）翻完：并集 65、去重后 65、跨页 `total` 恒 65；`offset` 越界返回空数组 |
| 附加：`updated_at` 前移即置顶 | **pass** | 激活后该项出现在缺省主列表首位（V-3 / F05-3 的落点） |

### 标准 4：三个新写口语义 — **pass**

| 子项 | 结论 | 证据 |
|---|---|---|
| `archiveChat` 双守卫 | **pass** | working → `false` 且 `archived_at`/`context_released`/`state`/`updated_at` 全不变；已完成 → `true` 且 `archived_at` = 传入毫秒、`context_released = 1`、`state`/`updated_at`/`created_at` 不变；重复归档 → `false` 且原归档时间保留；未知 chat → `false` |
| `activateChat` 一条 UPDATE | **pass** | 未归档 → `false` 且整行完全不变；未归档且已 closed 的对话 → `false` 且 `state` 仍 `closed`（N-5 结构性收窄成立）；closed 来源 → `archived_at = null`、`state = 'completed'`、`closed_at = null`、`updated_at` = 激活时刻、`context_released` 保持 1；completed/failed 来源 → 状态保持、`closed_at` 保持 `null`；重复激活 → `false` 且 `updated_at` 不再前移；未知 chat → `false` |
| `listArchivable` 范围 | **pass** | 以独立直读 SQL 的 ground truth（`archived_at IS NULL AND state != 'working'`）与 `listArchivable()` 输出逐项比对，**完全相等**；归档一条后其立即从候选集消失 |

### 标准 5：契约测试同步 — **pass**

`git diff 47ea70c..243e113 -- oamp/test/persist.test.js` 的**全部**既有断言改动（共 5 处，无其他）：

1. 列名断言 `:71-73` → 追加 `archived_at`,`context_released`（顺序敏感，与 §7.1 一致）
2. 写口白名单 `:173-176` → 排除名单加 `listArchivable`，期望集 = `['activateChat','archiveChat','closeChat','insertInput','insertOutput','startupSweep','upsertChat']`
3. 详情 chat 键白名单 `:368-370` → 追加 `archived_at`,`context_released`
4. `upsertChat` 逐字 `deepEqual` `:383-385` → 追加 `archived_at: null, context_released: 0`
5. `closeChat` 逐字 `deepEqual` `:406-408` → 同上

**未削弱既有断言**：diff 中不存在任何「放宽（`deepEqual` → `ok`/`includes`）、删除断言、调小断言范围」的改动；`:48-57` 的「重复 openDb 不抛错」既有用例在补列后仍绿（36/36 已证）。新增用例位于 `:80-142`（迁移）、`:454-479`（归档）、`:481-514`（激活）、`:516-532`（候选集）、`:534-545`（释放位）、`:547-579`（双视图）、`:581-595`（归档分页）。

### 标准 6：测试与范围外零改动 — **pass**

- `cd oamp && node --test test/persist.test.js` → `# tests 36 / # pass 36 / # fail 0`，exit 0（324ms）
- `cd oamp && node --test --test-concurrency=1 test/*.test.js` → `# tests 210 / # pass 210 / # fail 0`，exit 0（126.6s）
- 改动范围：`git diff --name-status 47ea70c..243e113` 仅 `oamp/src/persist.js`、`oamp/test/persist.test.js` 两个 oamp 文件（+1 个规划者 docs 文件，见偏差 D-1）；worktree 工作区干净（`git status --porcelain` 为空）

### 标准 7：反向核对（因果隔离） — **pass（已做，5 处变异）**

在 `/tmp/verifier0013-real/mutants/` 复制被验源码后做点变异（**未改动被验 worktree**），逐个观察可观测行为：

| 变异 | 基线行为 | 变异后行为 | 结论 |
|---|---|---|---|
| M1 去掉 `if (!existing.has(column))` 迁移守卫 | 新建库正常打开 | 首次 `openDb` 抛 `duplicate column name: archived_at` | 守卫是幂等性的承载点 |
| M2 去掉 `LIST_FROM` 的归档互斥谓词 | 缺省列表只含未归档 | 缺省列表泄漏已归档（`total` 由 1 → 2） | 谓词是双视图互斥的承载点 |
| M3 去掉 `archiveChat` 的 `archived_at IS NULL AND state != 'working'` | working → `false` 且不变 | working 被归档（`archived_at` 写入、`context_released = 1`） | 双守卫是范围兜底的承载点 |
| M4 去掉 `activateChat` 的 `archived_at IS NOT NULL` | 未归档 closed → `false` 且状态不变 | 未归档 closed 被重开为 `completed`（N-5 破防） | 结构性收窄的承载点 |
| M5 排序键退化 `CASE` → `c.updated_at` | 归档视图按 `archived_at DESC` | 归档视图按 `updated_at` 排序（`a@100` 与 `b@90` 顺序颠倒） | 条件排序键是 F03-3 的承载点 |

---

## 汇总

- **pass：7 项**（标准 1~7 全部通过；标准 1 内含 8 条 PR 验收，8/8 pass）
- **fail：0 项**
- **partial：0 项**
- **blocked：0 项**
- **偏差记录：1 条**（不阻塞交付）

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| 委托标准 6 表述为「`git diff --name-only` 仅 2 个 oamp 文件」，PR 卡验收 8 表述为「本 PR 内 … 零改动」 | 以提交 `243e113` 为界，改动恰为 `oamp/src/persist.js` + `oamp/test/persist.test.js` 两个文件；但以 base `47ea70c` 为界还包含规划者提交 `d8aa6d3` 新增的 `docs/iterations/0013-chat-archive/prs/pr-001-...-tasks.md`（非实现改动） | 按实现/现状更新：后续 PR 验收标准区分「分支差异」与「实施提交差异」，避免把规划者产物计入实施范围；本次无需返工 |

---

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制。不在当前验收标准范围内，供主 agent 和 planner 决策是否纳入下一迭代。

- **迁移用例的旧库保真度**：`persist.test.js:84-142` 的合成旧库只建 `chats`（无 `messages` 表与两个索引），真实旧库的「消息零变化」只在本次独立探针中被覆盖。建议补一条以旧版完整 SCHEMA（含 messages + 两索引）建库的迁移用例，或引入一份小体积 legacy fixture。
- **迁移中断自愈无单测**：M-5 的「两条 ALTER 之间中断」路径（只补了一列）已由独立探针证通过，但仓库测试未覆盖。建议补一条「部分迁移库 → openDb 补齐全」的用例，把自愈行为固化为回归保护。
- **`archived` 参数的类型收窄需在 pr-002 接线时对齐**：`readArchived` 严格只接受 `0|1`（数字），字符串 `'1'` 会抛错（`persist.js:104-112`）。`web.js:376-389` 对 `limit/offset` 走 `num()` 转换，pr-002 透传 `archived` 时必须同样转换（不可直接传 query string），并确认 `?archived=abc` 落在 400 分支。
- **归档视图无索引（V-4 既定取舍）**：条件排序键无法走索引，当前仅靠 `chats` 全表扫描 + `countChats` 全量计数。个人控制台量级无收益，但归档数显著增长（数千级）时需按 architecture §15 R-2 再评估。
- **`listChats` 列表项不含 `context_released`**（`persist.js:193`）：pr-002 前端若要展示归档提示条需走详情口（architecture §6.4 的落点），此处保持一致即可，勿在列表口额外加列。

---

## 结论

**PASS**

判定依据：7 条验证标准全部 pass，PR 卡 8 条验收 8/8 pass；最高风险项（真实旧库上的迁移幂等与数据不变）经**独立实测**确认；5 处关键实现经变异核对确认是行为的实际承载点，而非装饰性代码。偏差 1 条为验证标准表述与分支提交结构的差异，不构成返工项。
