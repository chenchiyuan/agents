# 工作流进度

**工作流**: workflow-pb v0.8.0
**迭代**: 0018-chat-agent-subagent-protocol
**当前阶段**: 阶段 2 已完成（**已暂停**；恢复后从阶段 3（技术架构）继续）
**迭代分支**: iteration/0018-chat-agent-subagent-protocol（tip 含 `028ca8a`（外部会话的 0017 收尾提交）+ 本迭代阶段 1~2 文档提交）
**基线**: `428e672`（原 iteration/0017-project-workspace 的 tip，现已并入 main `03a2f00`）
**状态**: **已暂停**（用户裁决：先做 0019（agents 框架并行会话工作区隔离协议）——「这个需要迭代优化 workflow-pb 流程，优先级最高」）
**history**: 开启

## 阶段状态

| # | 阶段 | 完成 | 已验证 | 备注 |
|---|---|---|---|---|
| 1 | 需求收敛 | ✅ | ⬜ | demand.md v1.2.0 已收敛；P-1~P-11 与 Q-1 全部经用户逐条裁决；`model_inferred` 归零 |
| 2 | 功能规格 | ✅ | ⬜ | prd.md v0.2.0 已收敛；16 张卡（F01~F13 + 保证项 F14~F16）；MI-01~MI-08 经用户裁决「全部按推荐」并落卡；卡内 `[model_inferred]` 零残留；T-01~T-17 架构待填 |
| 3 | 技术架构 | ⬜ | ⬜ | **等待恢复**（0019 完成后启动） |
| 4 | PR 规划 | ⬜ | ⬜ | |
| 5 | PR 实现 | ⬜ | ⬜ | 逐 PR 状态见下 |
| 6 | 独立验证 | — | — | 按需触发，不计入线性进度 |

## PR 实现子状态（阶段 5 展开）

待阶段 4 产出 `prs/` 后初始化。

## 并发配置（阶段 5）

待阶段 4→5 入口初始化。

## 待确认项

- [ ] **C-1（工作区共享冲突，已登记；用户已知晓）**：外部会话在同一工作区完成迭代 0017 收口（阶段 6 PASS → 合并 main `03a2f00` → 删除 0017 分支），其收口提交 `028ca8a` 落在本迭代分支上且**不在 main**。主 agent 的处置：不改写任何 ref、不删除他人提交、不替 0017 做提交决策。用户裁决（本轮）：这正是需要 0019 解决的根因。
- [ ] **C-2（0017 两处残留的最终去向；2026-09-12 18:20 按 progress-observer 独立核实修正）**：① `docs/iterations/0017-project-workspace/history.md`（外部会话 17:42:52 收口记录）；② `docs/iterations/0017-project-workspace/clarifications/verify-stage6-20260912-173934.md`（工作区版本曾比 `028ca8a` 中版本第 160 行多一处 `:695-699` → `:695-698` 修正）。**实际情况（修正原表述）**：主 agent 执行 `git stash push` 时已无内容可保存——外部会话已抢先把两处残留提交进 main（`9ede9ea`，17:46:02）；实测 `git stash list` 为空、`refs/stash` 不存在，**恢复 0018 时无需 stash pop**。工作区版本的 `:695-698` 修正仅存于 `/tmp/pb-0017-residual/`（3 文件备份，mtime 17:42~17:44）。0017 收尾产物以 main 中的版本为准；其提交版内部的一字符自相矛盾见 0019 台账 D-4 留痕。
- [ ] **C-3（0017 遗留缺陷 D-2，用户裁决：不纳入本迭代）**：`/api/chats`、`/api/messages` 路由登记元数据缺 `project_id` —— 不在本迭代完成。**归属（2026-09-12 18:20 更新）**：用户裁决由 **0019 完成后起的单独小迭代承接**，三处 status（0017 / 0018 / 0019）互相引用留痕；0018 恢复时不承接该项（除非用户另行指示）。
- **无其他用户待确认项**（P-1~P-11、Q-1、MI-01~MI-08 均已裁决并落盘）。

## 用户确认记录（Gate）

- **阶段 0**：迭代 ID = `0018-chat-agent-subagent-protocol`；0017 前置处理 = 「0018 从 0017 迭代分支起」。
- **阶段 1 第二轮（P-1~P-11）**：P-1 = A（参照 harness `task` 工具契约）；P-2 = A 并追加身份规则（用户原话：「同hub和agent之前的约定， 同chat下，同名agent是一个agent，共享上下文。 不同chat下agent是不同agent」）；P-4 = A（`task_id` 升格为 HTTP 一等调用资源，解除 0015 N5 但限「按调用 id 读自己发起的调用」）；P-3/P-5/P-6/P-7/P-8/P-9/P-10 = 全部按推荐；P-11 = 「都没想过，交给架构阶段」。
- **阶段 1 第三轮（Q-1）**：= ①（`one_shot` 保留为显式例外，登记差异 ⑲）。
- **阶段 2（MI-01~MI-08）**：全部按推荐采纳（八项验收口径定案）。
- **范围项（C-3）**：D-2 不纳入本迭代。
- **流程项（新需求）**：并行会话各用独立 worktree/clone、不得共用同一工作树 → 迭代优化 workflow-pb，优先级最高 → 落地方式 = 「先做 0019（独立迭代），暂停 0018」。

## 更新日志

- 2026-09-12: 迭代启动（Step 0 前提声明：本宿主具备真实阻塞式人机交互通道 `ask`）。
- 2026-09-12: 阶段 0 用户裁决两条（迭代 ID、0017 基线处理）→ 派发 demand（第一轮）。
- 2026-09-12: 阶段 1 三轮收敛：v1.0.0（待确认版）→ P-1~P-11 用户裁决 → v1.1.0 → Q-1 用户裁决 ① → v1.2.0（`model_inferred` 归零）→ 阶段 1 推进条件通过 ✅。
- 2026-09-12: 创建迭代分支 `iteration/0018-chat-agent-subagent-protocol`（base = `iteration/0017-project-workspace` @ 428e672）→ 阶段 2 派发 prd + 自动派发 progress-observer。
- 2026-09-12: progress-observer 独立核实发现外部会话在同一工作区完成 0017 收口（`028ca8a` 落在本分支、main 得 `03a2f00`、0017 分支已删）→ 主 agent 以零丢失方式处置（备份 → `checkout -m` → 并集解冲突 → 恢复残留）并切回本分支。
- 2026-09-12: 阶段 2 两轮：prd v0.1.0（16 张卡 + 8 项 MI 待裁决）→ 用户裁决「全部按推荐」+ D-2 不纳入 → prd v0.2.0 收敛（`model_inferred` 零残留）→ 阶段 2 推进条件三项通过 ✅。
- 2026-09-12: 用户提出新需求（并行会话工作区隔离，优先级最高）并裁决「先做 0019、暂停 0018」→ 本迭代置**已暂停**；阶段 1~2 产物提交进 `iteration/0018-chat-agent-subagent-protocol`；0017 两处残留具名 stash 保存；工作区切至 0019。
