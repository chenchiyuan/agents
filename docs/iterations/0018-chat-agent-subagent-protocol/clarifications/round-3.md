# 澄清记录 · round-3（迭代 0018-chat-agent-subagent-protocol，阶段 1 需求收敛·Q-1 归零收口）

**caller**: demand
**时间**: 2026-09-12
**本轮性质**: 第三轮（收口轮）——接收用户对 **Q-1** 的裁决，把 `demand.md` 由 v1.1.0 收敛为 **v1.2.0**：`model_inferred` **归零（无残留待确认项）**，阶段 1 推进条件三项全部满足
**裁决通道**: 主 agent 真实阻塞式提问（原文转达）
**基线**: `iteration/0017-project-workspace`（阶段 0 裁决 G-2）

---

## 一、本轮输入（读取范围）

| # | 材料 | 用途 |
|---|---|---|
| 1 | 用户对 **Q-1** 的裁决（主 agent 原文转达：「**①**」） | Q-1 落定的唯一依据（`user_confirmed`） |
| 2 | `docs/iterations/0018-chat-agent-subagent-protocol/demand.md`（v1.1.0） | 收敛对象 |
| 3 | `docs/iterations/0018-chat-agent-subagent-protocol/clarifications/round-2.md` | 上一轮未闭合项（Q-1）的来源 |
| 4 | `docs/iterations/0018-chat-agent-subagent-protocol/clarifications/recon-20260912.md`（F-17 锚点复核） | 复核 `one_shot` 路径事实（`web.js:697-701` → `executor:'omp'` → `omp -p --no-session`，不共享上下文） |

**未读**（与本轮无关）：`oamp/src/**` 全文、`oamp/API.md` 全文、`oamp/test/**`。

---

## 二、Q-1：提案 → 裁决 → 落定

### 2.1 提案（`demand-round-1-proposals.md` 之外的 round-2 遗留项；提案内容见 v1.1.0 §6.1-A 点 6 与文末「待确认项」）

- **问题**：既有 `one_shot:true` 路径（**每次调用独立进程、不共享上下文**，F-17）与 W8 身份规则（同 chat 同名 **共享上下文**）的关系。
- **提案选项**：
  - ① `one_shot` 保留为**「调用方主动放弃上下文延续」的显式例外**，并在 `oamp/API.md` 差异清单登记（demand 倾向）；
  - ② 新调用面**不提供** `one_shot`（保留既有 HTTP 面行为不变，仅新面不暴露）；
  - ③ 彻底废除 `one_shot`。
- **为何上一轮未落定**：P-2 裁决原文只定义了"同 chat 同名共享上下文"这一条身份规则，**未对既有 `one_shot` 模式表态**；按"以 `user_confirmed` 原文为准、不外推"的要求，不擅自归类。

### 2.2 用户裁决（原文转达，`user_confirmed`）

> **①**

### 2.3 落定结论（写入 `demand.md` v1.2.0）

| 落点 | 内容 |
|---|---|
| §2.3（新增） | 第三轮已裁决项：Q-1 选 ①，`one_shot` 保留为显式例外，落差异 **⑲**；未选 ②③ |
| §4.3 | 用户明说边界补入"`one_shot` 保留为显式例外（Q-1 裁决 ①，落差异 ⑲）" ⇒ 明说边界 **9 条** |
| §6.1（表格新增行） | Q-1 裁决行：原文「①」+ 落地结果（差异 ⑲ / §6.1-A 点 7 / `model_inferred` 归零）+ "与原倾向一致" |
| §6.1-A（新增点 7） | `one_shot` 路径的地位：显式例外 + 差异 ⑲；W8 默认语义（`omp-daemon` 路径）与 `one_shot` 例外地位**不再冲突**；未选 ②③（**未写成任何形式的落定结论**） |
| §6.2 | Q-1 转换行由「仍为 `model_inferred`」改为「`user_confirmed`（列为差异 ⑲）」；标题改为 `v1.0.0 → v1.2.0，含第三轮` |
| 结论段 §3 差异清单 | **新增 ⑲**（编号顺延，不复用 ①~⑱）：`one_shot:true` = 调用方主动放弃上下文延续的显式例外 |
| 附一 | `model_inferred` 行 → **「无（归零）」**；`user_confirmed` 行补入 Q-1 裁决；`派生` 行的编号范围由 ②~⑱ 收窄为 **②~⑰**（⑱/⑲ 由 P-3 / Q-1 裁决直接给出） |
| 附二 | B-5 参考视角**关闭**（标注"已由 Q-1 裁决落定，其内容仍不作为结论"） |
| 文末「待确认项」 | 由 Q-1 单项改为 **「无」** + 三项自检表 |
| 文档状态头 | v1.1.0 → **v1.2.0**；新增「本版（v1.2.0）相对 v1.1.0 的变更」；v1.1.0 变更清单转为「保留备查」 |

### 2.4 本轮**未**新增 W / N / E 条目的理由

Q-1 的裁决**确认既有行为**（把 `one_shot` 定性为显式例外）并落入差异清单 ⑲：

- 它没有引入新范围（不新增调用面能力）；
- 它没有引入新边界（`one_shot` 路径本身**既存且不变**，只是被定性并登记）；
- 它没有产生新的可验证效果（该路径的既有行为不变，E1~E11 的判定对象不受影响）。

⇒ 按"不为凑数新增条目"的边界纪律，W1~W8 / N1~N21 / E1~E11 **一律不变**；其裁决痕迹通过 §2.3、§6.1、§6.1-A 点 7、差异 ⑲、附一/附二/待确认项六处落定。

---

## 三、六维诊断对最终草稿复跑（v1.2.0）

| 维度 | 结论 |
|---|---|
| 明确性 | 跑不出新缺口：M-1~M-12 唯一填法、全部 `user_confirmed` |
| 逻辑闭环 | 跑不出新缺口：4 处缺口已落定；**Q-1 裁决同时消除了 W8 与 `one_shot` 之间的最后一处潜在冲突**（§6.1-A 点 7） |
| 边界完整性 | 跑不出新缺口：用户明说边界 **9 条**；N1~N21 全部经 P-2~P-10 覆盖；Q-1 不新增 N（确认既有行为 + 差异 ⑲） |
| 内部一致性 | 无活跃冲突：C-1~C-8 全部已决（与 v1.1.0 相同，Q-1 未引入新冲突） |
| 可验证性 | 跑不出新缺口：E1~E11 全部裸判定；E1 的差异清单范围更新为 ①~⑲ |
| 方案雏形 | **仍闭合**：7 条 W 用户明确回应 + W8 用户自述机制；参考视角 B-1~B-5 未进入结论段，其中 B-5 已因 Q-1 裁决关闭 |

**停止条件**：满足（连续一轮跑不出新缺口 + 无活跃冲突 + 无残留待确认项）。

---

## 四、来源标注统计（v1.2.0）

| 类别 | 数量 / 内容 | 说明 |
|---|---|---|
| `user_confirmed` | §1.1 原话 + S-1~S-6 + G-1/G-2 + P-1~P-11 裁决 + P-2 身份规则原文 + **Q-1 裁决 ①** + M-1~M-12 + W1~W8 + N1~N21 + E1~E11 | 全部机制层结论均已确认 |
| `model_inferred` | **无（归零）** | Q-1 已落定为 `user_confirmed` |
| `派生`（可回退） | 2 条：差异 ②~⑰ 的编号与归类；role 推导空值形态 | 留痕 §6.3 |
| `侦察`/`实测` | F-1~F-17 + §3.2 三面契约 | 均带 file:line 锚点 |
| `参考视角` | B-1~B-4 保留；B-5 已关闭 | 均未进入结论段 |
| `[UNVERIFIED]` | 侦察 §D.1~D.7 | 原样登记 |
| `[架构待填]` | 结论段 §3 末段（`one_shot` 例外地位的**登记形态**亦为留架构项，但已不再是待决需求问题） | 留阶段 3 |

---

## 五、阶段 1 推进条件自检（三项）

| # | 推进条件 | 结论 | 证据 |
|---|---|---|---|
| 1 | `demand.md` 两段均有内容 | **满足** | 第一段 = §1 用户表述 / §2 决策记录（含 §2.3 第三轮）/ §3 现状事实与三面覆盖表 / §4 六维诊断 / §5 澄清方式 / §6 裁决落定；第二段 = §1 做什么 / §2 不做什么 / §3 大概怎么做 + 差异清单 / §4 达到什么效果 / §5 为什么这么做 / §6 六维收敛自检 |
| 2 | 所有 `model_inferred` 经用户确认（归零） | **满足** | 附一 `model_inferred` 行 = **无（归零）**；文末「待确认项」= **无**；Q-1 已由第三轮裁决 ① 转为 `user_confirmed`（差异 ⑲） |
| 3 | 无活跃冲突 | **满足** | C-1~C-8 全部已决（C-1/C-8 ← P-2；C-2 ← P-4；C-3 ← P-6；C-4 ← P-5；C-5 ← P-9；C-6/C-7 = 既定约束）；Q-1 裁决消除 W8 与 `one_shot` 的最后一处潜在冲突 |
| （附） | 第六维仍闭合 | **满足** | P-11 裁决（7 条 W 均有明确回应）+ W8 用户自述机制；无新增缺口 |

---

## 六、本轮未做的判断（边界纪律）

未写 prd/architecture/prs；未改 `status.md`/`history.md`/`recon-20260912.md`/`round-1.md`/`round-2.md`/`demand-round-1-proposals.md`；未改任何代码；未运行测试或构建；未执行 git 命令；未写 `.pb-agents/` 下任何文件；**未把 ②③ 两个未被选择的选项写成任何形式的落定结论**（仅在 §6.1 与 §6.1-A 点 7 记录"未选择 ②③"这一事实）。
