# pr-004 独立验收报告（阶段 5 末波 · 合并前门禁）

- **验证者身份**：`verifier` 0.2.0 —— 第三方独立验证者。**未接收执行过程上下文**（未读 dev 会话记录 / 未读 dev 自跑结论 / 未读 history:// 与 agent:// 产物；仅接收产出物路径 + 验证标准）。
- **反射身份**：合并前代码评审者（验证对象 = 已实现并提交的前端页面 + 新增测试文件）。
- **产出物**：
  - PR worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol/.pb-agents/worktrees/0018-pr-004-console-call-page`
  - 分支 `feat/0018-pr-004-console-call-page`；base `7e351fa`；实现提交 `d84dba1`（`da6fa63` = 任务图文档）
- **验证标准来源**：`prs/pr-004-console-call-page.md` 验收标准 8 条 + `prs/pr-004-tasks.md` T1~T7 判据 + `architecture.md` §6 / §12.2 组 K + 主 agent 派发的检测项 A~F。
- **验证日期**：2026-09-13
- **方式声明**：全部判据**自行实测**（含真起服务与真起浏览器）；全程只读；**未修改任何被验证文件**；**未执行 git 写命令**；除本报告外**未写任何文件**；未做变异实验。
- **环境**：Node v22.15.0（darwin arm64）。

---

## 逐项判定

### A. PR 文件「验收标准」8 条

#### A1 — `GET /calls` / `GET /calls.js` 的 200 + content-type（真起服务实测）**pass**

自身 smoke（真 Router 子进程 + `oamp web start` 子进程，端口 **48123**）：

```
GET /calls     → HTTP/1.1 200 OK   content-type: text/html; charset=utf-8        body=1371 B
GET /calls.js  → HTTP/1.1 200 OK   content-type: text/javascript; charset=utf-8  body=7088 B
```

白名单由 pr-003 提供、本 PR 零触碰：`git show 7e351fa:oamp/src/web.js | grep -c "'/calls'"` = **1**（`/calls` 行在 base 已存在；§ 另一行为 `'/calls.js'`），且 `git diff --name-only 7e351fa..HEAD -- oamp/src/web.js` 为空。
承载体即本 PR 新增的两文件（base 上文件不存在 ⇒ `serveStatic` 的 `fs.readFile` ENOENT 分支 → `res.writeHead(404)`，`src/web.js:398-402`）。

#### A2 — `index.html` 新入口 + 既有 3 占位项与 `/docs`、`/debug` 逐字未变 **pass**

```
$ git diff -U0 7e351fa..HEAD -- oamp/web/index.html
@@ -17,0 +18 @@
+        <a class="nav-item" href="/calls">调用</a>
```
base 与 HEAD 的 `nav-item` 行逐行比对：第 13~17 行（`Workspace`(active span) / `Agents` / `Tasks` / `/docs` 文档 / `/debug` 调试）**逐字不变**，仅第 18 行新增。
`grep -n 'href="/calls"' oamp/web/index.html` = **1 行**（18）。
`git diff 7e351fa..HEAD -- oamp/test/api-pages.test.js` = **0 行**（该文件内容与 base 完全一致）。
`node --test oamp/test/api-pages.test.js` → **tests 5 / pass 5 / fail 0**（含既有顶栏静态契约与 `/docs`、`/debug`、`/docs.js`、`/debug.js`、`/api-pages.css` 可达性）。

#### A3 — roster 六列 + 取数 `GET /api/calls` + 5 s 轮询（核常量与单定时器）**pass**

- 表头 6 列（真浏览器 DOM 实测）：`["调用 id","agent","状态","开始时间","结束时间","模型"]`。
- `calls.js:78` `const res = await fetch('/api/calls');`；`calls.js:8` `const ROSTER_REFRESH_MS = 5000;`；`calls.js:178` `setInterval(loadCalls, ROSTER_REFRESH_MS);`；`grep -c 'setInterval' oamp/web/calls.js` = **1**（单定时器）。
- 运行期节奏实测（页内包装 `window.fetch` 计数 11.2 s）：命中 `/api/calls` **2 次**，相邻间隔 **4998 ms**。
- 零新推送机制：`grep -c 'new EventSource'` = 1（仅按调用订阅处），无第二套轮询/推送。

#### A4 — 点击行订阅按调用流 + 三类事件渲染 + 切换关闭旧订阅 **pass**

实现核对：`calls.js:133` `new EventSource(`/api/calls/${encodeURIComponent(callId)}/stream`)`；`calls.js:140` 对 `CALL_EVENT_TYPES = ['call_state','call_update','call_result']` 逐名 `addEventListener`；`unsubscribe()`（`close()`）在 `selectCall` 内先于新订阅构造（`calls.js:126-131`）。零 `chat_id=` / `/api/stream` / `/api/events`（检索零命中）。
**真浏览器实测见 E4/E5/E6**（真实点击 → 真实 SSE → 真实事件）。

#### A5 — 不呈现无数据源项（检索式零命中）**pass**

```
$ grep -nEi 'token|cost|steer|cancel|isolated|effort|usage' oamp/web/calls.html oamp/web/calls.js
exit=1（无输出）
```
两文件分别复查亦零命中。

#### A6 — `app.js` / `style.css` / `api-pages.css` 零改动 **pass**

`git diff --name-only 7e351fa..HEAD -- oamp/web/` = `calls.html`、`calls.js`、`index.html`（三文件均不在其中）。
补充：新页面复用的类**全部存在**于既有样式表 —— `.badge-{submitted,working,completed,failed,idle}`、`.muted`、`.hint`/`.hint.error`、`.topbar`/`.brand*`（`style.css`）与 `.sse-log`/`.sse-entry`/`.route-path`/`.api-table`/`.api-page`（`api-pages.css`）；页面骨架与 `docs.html`/`debug.html` 同款（`header.topbar` + `main.api-page`）。零新 CSS 文件成立且样式实际生效。

#### A7 — README 控制台节 +1 行且 `## 协议速览` 逐字未变 **pass**

```
$ git diff -U0 7e351fa..HEAD -- oamp/README.md
@@ -128,0 +129 @@ oamp web start [--port 7788] …
+- **顶栏入口 · 调用**：「调用」→ `/calls`（调用 roster：…六列；进入取一次 + 5 s 轮询刷新，点击某行按该调用订阅 SSE 进度）
```
`-` 行 = **0**、`+` 行 = **1**（单 hunk）。行号 129 落在 `## Web 控制台（demo）`(120) 与下一 `##`(149) 之间；`grep -n '/calls' oamp/README.md` = **1 行**。
`## 协议速览`(103~111) 区间未被 diff 触及，7 方法面逐字在场：`agent.register` / `agent.heartbeat` / `agent.deregister` / `message.send` / `message.deliver` / `message.ack` / `router.status`。
`node --test --test-name-pattern '协议方法面仍 7 个' oamp/test/project-workspace.test.js` → **pass 1 / fail 0**。

#### A8 — `call-console.test.js` 自带 harness、无新共享 helper、全绿 **pass**

```
$ node --test oamp/test/call-console.test.js
# tests 2   # pass 2   # fail 0
```
- import 仅 `node:test` / `node:assert/strict` / `node:child_process` / `node:fs` / `node:os` / `node:path` / `node:url` + 既有 `./helpers/harness.js`（`startRouter`/`waitFor`/`stopAll`/`buildEnv`）；`startWeb`/`pickPort`/`read` 为**文件内局部定义**。
- `git diff --name-only 7e351fa..HEAD -- oamp/test` = 恰 `oamp/test/call-console.test.js`；`-- oamp/test/helpers` = **0 行**；`-- oamp/test/api-pages.test.js` = **0 行**。

---

### B. 测试非空洞性 **pass**（附判据；未做变异实验）

| 用例 | 观测类型 | 判据 | 结论 |
|---|---|---|---|
| 用例 2「新页面 HTTP 可达性与响应类型」 | **运行期** | 真起 Router（临时 socket）+ `oamp web start` 子进程（随机端口）+ 临时 `OAMP_DB`，`fetch` 断言 `status`/`content-type`/体长。**反空洞性可判据**：base `7e351fa` 上两目标文件不存在 ⇒ `serveStatic` ENOENT → 404（`src/web.js:398-402`），该用例在 base 上**必然失败** ⇒ 非恒真。另在独立端口 48123 亲自复现同一事实（200 + 两类 content-type + 1371/7088 B） | 非空洞 |
| 用例 1「调用页静态契约」 | **源码文本级**（声明式静态契约，`architecture §12.2 组 K` 明列此形态） | 逐条判据均可伪且**无自证**：`const ROSTER_REFRESH_MS = 5000;`（:8）、`setInterval(loadCalls, ROSTER_REFRESH_MS)`（:178）、`fetch('/api/calls')`（:78）、EventSource 模板串（:133）、三事件名 `'call_state'`/`'call_update'`/`'call_result'` 的**引号形式唯一出现点**为 `CALL_EVENT_TYPES` 数组（:9）——文件头注释为**无引号**形式，故断言不落在注释上；`doesNotMatch`（零无数据源列 / 非全局流）为真实否定断言。无 `assert.ok(true)`、无自证式期望 | 非空洞（但**不与运行期行为绑定**：重构保行为也可能使其失败，属静态契约固有性质，已登记为下一迭代候选 3） |

---

### C. 既有面零回归 **pass**（逐轮报数 + flake 纪律）

| 轮次 | 命令 | 结果 |
|---|---|---|
| R1 | `cd <PW>/oamp && npm test` | tests **274** / pass **273** / fail **1** —— **失败用例名未捕获**（仅取到尾窗输出），如实登记为未归因 |
| R2 | 同上（`grep '^not ok'` 无输出） | 274 / **274** / 0 |
| R3 | 同上 | 274 / **274** / 0 |
| R4 | 同上 | 274 / **274** / 0 |
| 并发加压轮 | 两套 `npm test` **同时**跑 | A: 274/274/0 ；B: 274/274/0 |
| flake 单跑 | `cd <PW>/oamp && node --test test/status.test.js`（安静环境） | tests **5** / pass **5** / fail **0** |

**flake 纪律结论**：既有 flake 为 `oamp/test/status.test.js F05-4`（负载下偶发）。R1 的 1 次失败在后续 **4 轮（含双并发加压）均未复现**，安静环境单跑该文件 5/5 绿 ⇒ **不存在可复现回归**；R1 失败**无法归因到具体用例**（不予类比推断）。本轮变更面相关用例（`api-pages` 5/5、`call-console` 2/2、`project-workspace` 方法面 1/1）无一失败。

---

### D. 变更面闭合 **pass**

```
$ git diff --name-only 7e351fa..HEAD
docs/iterations/0018-chat-agent-subagent-protocol/prs/pr-004-tasks.md
oamp/README.md
oamp/test/call-console.test.js
oamp/web/calls.html
oamp/web/calls.js
oamp/web/index.html
```
= 恰 **5 个 PR 文件 + `prs/pr-004-tasks.md`**（6 路径，无第六个业务路径）。
`git diff --stat 7e351fa..HEAD` = 554 insertions / **0 deletions**（README +1、call-console.test.js +118、calls.html +39、calls.js +178、index.html +1、pr-004-tasks.md +217）。
`git show HEAD:oamp/package.json | jq .dependencies` = **`{}`**；`package.json` / `config.json` / `bin/**` / `API.md` / `llms.txt` 均在 diff 之外（`wc -l` = 0）。无新增配置键（前端 `calls.js` 零 env / 配置读取；`src/**` 零改动）。
`git status --porcelain -uall`（PR worktree）= **空**。

---

### E. 真实页面核对（F13 的非自动化项）**pass**

载体：真实 `oamp router start` + `oamp web start --port 48123`（临时 `OAMP_DB` / 临时 socket）+ 真实 `oamp agent start pb-dev`；真实 `POST /api/calls`（`mode=background`）产生 roster 行；headless Chromium 驱动 `/calls`。

| # | 核对项 | 观测证据 |
|---|---|---|
| E1 | **页内三块齐备** | `blocks = {roster:true, progress:true, hint:true}`（`table.api-table tbody#call-rows` / `#call-progress` / `#call-hint`） |
| E2 | **表头 6 列 + 真实行** | 表头 = `[调用 id, agent, 状态, 开始时间, 结束时间, 模型]`；真实两行六列全有值（`state=completed`、时间已格式化、`model=deepseek/deepseek-v4-flash`） |
| E3 | **null 占位「—」** | 刚创建（进行中）行：`rowAtSelect = [task-2b4c…, dev, working, 2026/9/13 00:53:32, —, —]`（`ended_at`/`model` 为 `null` → 「—」）；同一行 14.75 s 后 `rowNow = […, completed, …, 2026/9/13 00:53:34, deepseek/deepseek-v4-flash]` |
| E4 | **点击后进度区更新** | 真实 `row.click()` → `created = ['/api/calls/task-2b4c…/stream']`；`#call-progress` 头部由「点击上方任一行…」变为「调用进度：`task-2b4c…` —」；`#call-log` 增至 18 条（call_update 逐行 + call_result 全文） |
| E5 | **三类事件渲染（含进行中增量）** | 第二组采样：badge 取值集合 = `['—','working','completed']`（`call_state` 驱动 `working`）；`#call-log` 条目数序列 `0→27→89→141→192→245→257`（`call_update` 增量）；终态 `completed` + `call_result` 全文。**旁证**：同一时刻 roster 行仍为 `working` 而进度区已 `completed` ⇒ 进度区确由 SSE 驱动，非 5 s 轮询拷贝 |
| E6 | **切换选中项关闭旧订阅** | 切前 `created=1 / closed=0` → 切后 `created=2 / closed=1`（closed 恰为**旧** URL）→ 再切 `created=3 / closed=2`；`source !== null` ⇒ 任一时刻恰 1 个活动订阅 |
| E7 | **错误路径（404）** | `selectCall('task-does-not-exist-verifier')` → 服务端 404 → `#call-hint` = 「订阅中断（该调用可能已不存在）——浏览器将自动重试。」class=`hint error`；切回真实行后 hint 清空 |
| E8 | **既有样式实际生效** | 复用类全部命中既有 CSS（见 A6）；页面骨架与 `docs.html`/`debug.html` 同款 |

---

### F. 端口与现场清理 **pass**

- **我用端口段**：`48000–48499`（实测只占 **48123**）；**未占用 7788**（该端口由**既有**主仓 cluster 进程 PID 31625 持有，非我起、全程未触碰）。
- 测试自带段：`call-console.test.js` 声明并使用 **49000–49499**（源码注释与实现一致）；pr-005 段 49500–49999，无交叠。
- 收尾：hub 托管三进程（router / web / agent）全部 `stop`；`lsof -nP -iTCP:48123` **空**；`pgrep -fl oamp-ver18p4` **空**；临时目录 `/tmp/oamp-ver18p4-GZOL4d` 已删除并复查不存在；浏览器会话 `close`（释放 1 tab）。
- 残留 `oamp.js` 进程仅为**既有**主仓 cluster（tmux `oamp-cluster`，主仓路径 + 7788），与本验收无关。
- 未写任何文件（除本报告）；未修改被验证文件；未执行 git 写命令。

---

## 汇总

**pass 14 / fail 0 / partial 0 / blocked 0**（A1~A8 = 8 + B/C/D/E/F = 6）。

---

## 偏差记录

| # | 偏差 | 证据 / 判据 | 阻塞 |
|---|---|---|---|
| 1 | README「13 条」HTTP 接口计数未随 pr-003 后的真实 19 条更新 | 已裁定 4（本迭代不改计数）；pr-004 对 README 仅 +1 行，计数行未被 diff 触及；T5 验收 4 明写「不改任何计数行」 | 否 |
| 2 | 5 条 `[model_inferred]`（已裁定 7 批准）与实现一致，逐条核验 | ①新入口追加在既有两条实入口之后（`index.html:18`）②表头中文列名 `调用 id/agent/状态/开始时间/结束时间/模型`（浏览器 DOM 实测）③`null` 占位「—」（E3 实测）④`call_update` 取 `text`，否则 `` `${line}\n` ``（`calls.js:159`）⑤README 新增独立 bullet（`README.md:129`，`-` 0 行 / `+` 1 行） | 否（登记） |
| 3 | **终态调用点击后进度区不回填**：点击**已完成**行 → 进度区 badge 停留「—」、日志为空（既有 SSE 抽象不补发、页面不补取 `GET /api/calls/:call_id`）。PR 验收 4 字面（订阅 + 渲染**到达**的事件）已满足；此即 `pr-004-tasks.md` 上报事项 a 的架构留白 | 实测：选中已 `completed` 的行后 400 ms 内 `#call-state` 文本 = 「—」、`#call-log` 条目 = 0 | 否（登记为已知体验缺口） |
| 4 | 已裁定读法登记（主 agent 已裁定、按权威读法判 pass）：F10 验收 3 重启语义、`callSchemas`/`call.terminal`、§11 逐字零改写、GET 裸 10 键信封（SSE `call_result` 11 键）、`web.js` 注释「11 键」之差 | 本 PR 改动面**不触及**上述任一条（`src/**` 零改动、无新路由/事件/信封） | 否 |
| 5 | R1 全量套件 1 次**未归因**失败 | R1 `tests 274 / pass 273 / fail 1`（失败名未捕获）；R2~R4 + 双并发加压轮均 **0 fail**；`status.test.js` 安静单跑 5/5。不为 R1 做归因推断 | 否 |
| 6 | 状态覆盖范围说明（非偏差）：本轮真实 E2E 观测到的调用均为 `completed`（真实模型返回真实文本），未观测到 `failed` 终态渲染 | E2/E3/E5 的 row/ badge 观测值集合仅含 `submitted/working/completed` | 否 |

---

## 下一迭代候选

1. **（体验）终态调用回填**：点击已完成行时补取 `GET /api/calls/:call_id`（或 `/transcript`）回填终态文本，消除「点已完成的调用看到空进度区」（对应偏差 3 / `pr-004-tasks.md` 上报事项 a）。
2. **（文档）README HTTP 接口计数漂移**：「13 条」vs 真实 19 条，pr-003 与 pr-004 均不承接，建议独立收尾动作（对应偏差 1）。
3. **（可测性）组 K 静态契约断言升级**：当前为源码文本级；若需抗重构，可改为对 `/calls` 响应体做断言（运行期可达面），而非读 `web/calls.html` 文本（对应 B 用例 1 的固有性质）。
4. **（自动化边界）面内 roster 行与进行中增量的核对方式**：本轮已用 headless 浏览器 + `EventSource` 计数 + DOM 采样完成该「人工/浏览器核对项」，可作为后续若要求自动化的既有起点。

---

## 结论

**PASS**

8 条 PR 验收标准与 B/C/D/E/F 全部 **pass**（14/14），0 fail、0 partial、0 blocked；6 条偏差中 5 条为已裁定/登记性、1 条为状态覆盖说明，均不阻塞合并。

**报告自检**：每项判定均附可定位命令或运行期观测证据；无 fail/partial（无需补充失败证据）；偏差表含处置与阻塞判定；下一迭代候选 4 条。**漏项回补**：A1~A8 + B~F 全覆盖，无漏项。
