# verify-pr-002 — 合并前独立验收报告

**验证者身份**：合并前代码评审者（pre-merge reviewer）——对「已实现并提交的 PR 代码变更」做只读独立验收，按 PR 文件验收标准逐项判定，不看执行过程记录。
**产出物**：
- PR worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol/.pb-agents/worktrees/0018-pr-002-registry-task-list-model`（分支 `feat/0018-pr-002-registry-task-list-model`）
- 实现提交：`a827f4e feat(0018/pr-002): listTasks 投影追加只读 model 字段`（其前为任务图提交 `2630395`；验收基准 base = 阶段 4 Gate 提交 `97ee9ac`）
- PR 文件：`docs/iterations/0018-chat-agent-subagent-protocol/prs/pr-002-registry-task-list-model.md`
- tasks 文件：`docs/iterations/0018-chat-agent-subagent-protocol/prs/pr-002-tasks.md`
**验证标准来源**：主 agent 委托书（A/B/C/D/E/F 六组判据）+ 上述 PR 文件「验收标准」5 条 + tasks 文件 T1/T2 验收标准原文。
**验证日期**：2026-09-12
**独立性声明**：未读取任何 dev 执行会话记录；未修改被验证产出物（PR worktree 三个 `git status` 均干净，见文末）；仅在会话工作区写入本报告。

---

## 逐项判定

### A. PR 文件「验收标准」5 条

- **A① `listTasks` 投影含 `model` 键，取值 = `task.result?.model ?? null`：pass**
  证据：`sed -n '255,274p' <PW>/oamp/src/registry.js | grep -n model` →
  `15:        model: task.result?.model ?? null,`（绝对行 269，位于 `out.push({…})` 投影块内）；`grep -n 'model' <PW>/oamp/src/registry.js` 全文仅此 1 处命中，无第二处回退写法。

- **A② 进行中任务（`result === null`）该键为 `null`：pass**
  证据：一次性 node 复查脚本（`node --input-type=module -e`，不落盘）裸调 `createRegistry()`，读三种状态投影：
  ```
  submitted: { model: null,  has: true, t: "object" }
  working:   { model: null,  has: true, t: "object" }
  completed: { model: "alpha/model-a", has: true, t: "string" }
  completedNoModel: { model: null, has: true, t: "object" }   ← 终态 body 无 model
  failed:    { model: "beta/model-b", has: true, t: "string" }
  keys: [task_id, from, to, state, label, created_at, updated_at, updates, updatesTruncated, model]
  base9PrefixIntact: true   sortOrder(t3,t2,t1): 倒序正常
  ```
  进行中（submitted/working）键存在且值为 `null`（`'model' in row === true`、`typeof === 'object'`），未填请求参数或推测值；终态读不到时为 `null` 而非 `undefined`。

- **A③ 本 PR diff 只落 `listTasks` 投影一处：pass**
  证据：`git -C <PW> diff -U0 97ee9ac..a827f4e -- oamp/src/registry.js` → **恰好 1 个 hunk**：`@@ -268,0 +269 @@ export function createRegistry() {` +`model: task.result?.model ?? null,`。
  `listTasks` 行号区间 = 255–274（`grep -n 'function listTasks'` → 255；函数闭合 `}` 于 274），新增行 269 ∈ 该区间。
  因总 diff 仅 1 hunk，`createTask` / `recordTaskUpdate` / `finishTask` / `getTask` 四段零 hunk（字段与语义零改动），`out.sort(…)`、`return {…}` 导出清单均零命中。

- **A④ `node --test oamp/test/task.test.js` 全绿且该文件 diff 为空：pass**
  证据：`cd <PW> && node --test oamp/test/task.test.js` → `# tests 6 / # pass 6 / # fail 0`，退出码 `exit=0`；
  `git -C <PW> diff 97ee9ac..a827f4e -- oamp/test/` 与 `-- oamp/package.json oamp/.gitignore` 均无输出（文件零改写）。

- **A⑤ `git diff --name-only` 不含 `oamp/src/router.js`：pass**
  证据：`git -C <PW> diff --name-only`（工作区 vs HEAD）→ 空；`git -C <PW> diff --cached --name-only` → 空；
  `git -C <PW> diff --name-only 97ee9ac..a827f4e` → `oamp/src/registry.js`、`docs/…/prs/pr-002-tasks.md`，不含 `oamp/src/router.js`（亦不含 `oamp/src/task.js`、`oamp/test/**`、`architecture.md`）。

### B. tasks 文件 T1/T2 验收标准

**T1（`listTasks` 投影追加只读字段 `model`）**

- **T1.1 投影新增 `model`，表达式 = `task.result?.model ?? null`，函数体内无回退写法：pass**
  证据：同 A①；`listTasks` 函数体（255–274）内 `model` 唯一出现于投影行 269（`sed -n '255,274p' | grep -n model` → 单行命中），无 `task.label` / 请求参数 / 实例默认模型常量作回退。

- **T1.2 进行中（`state ∈ {submitted, working}`）该行 `model === null` 且键存在（`'model' in row === true`）：pass**
  证据：同 A② 脚本输出 `submitted` / `working` 均为 `{ model: null, has: true }`。

- **T1.3 终态 `model` = `task.result.model`；终态体无 `model` 时为 `null`（非 `undefined`）：pass**
  证据：同 A②：`completed`（`result:{model:'alpha/model-a'}`）→ `"alpha/model-a"`；`completedNoModel`（`result:{}`，finishTask 补 `at`）→ `null`（`typeof === 'object'`，即 null 而非 undefined）；`failed`（`result:{model:'beta/model-b'}`）→ `"beta/model-b"`。

- **T1.4 `createTask` / `recordTaskUpdate` / `finishTask` / `getTask` 零改动（含 `updates[]` / `updatesTruncated` / `result` 形状与写入时机）：pass**
  证据：`git diff -U0 97ee9ac..a827f4e -- oamp/src/registry.js` 全局仅 1 hunk（行 269，属 `listTasks`）；上述四段零 hunk。行为侧同由 A②/E 的运行结果反向印证：`updates`（计数）、`updatesTruncated`、`result`（`{…result, at}`）语义与投影键序均符合基线。

- **T1.5 `listTasks` 签名 `({ state } = {})`、既有 9 键与顺序、倒序排序行、`createRegistry()` 导出方法清单零改动：pass**
  证据：`sed -n '255,274p'` → 签名 `function listTasks({ state } = {})` 未变；投影键序 = `task_id, from, to, state, label, created_at, updated_at, updates, updatesTruncated, model`（既有 9 键逐字保持前缀，`base9PrefixIntact: true`）；`out.sort((a, b) => b.created_at - a.created_at)` 未变（`git diff` 无该行）；`return {…}` 导出清单 18 项（含 `createTask, recordTaskUpdate, finishTask, getTask, listTasks`）无新增/重命名，对应导出块 diff 零 hunk。

**T2（契约收敛核查与验收证据采集）**

- **T2.1 `node --test oamp/test/task.test.js` 全绿：pass** — 同 A④（6/6，`exit=0`）。
- **T2.2 `git diff --name-only`（对迭代分支 base）输出恰为 `oamp/src/registry.js` 一行：pass（口径见偏差记录 #1）**
  证据：字面（含文档）读法实测为 2 行（`oamp/src/registry.js` + `docs/…/prs/pr-002-tasks.md`）；**实现代码域** diff 恰 1 行（`git diff --name-only 97ee9ac..a827f4e -- oamp/` → 仅 `oamp/src/registry.js`）。tasks 文件为阶段 5 规定交付物（PR worktree 提交 = PR 文件 + tasks 文件 + 代码改动）。按权威读法判 pass，差异登记。
- **T2.3 `oamp/src/registry.js` diff 恰 1 hunk 且全在 `listTasks` 函数体内：pass**
  证据：`@@ -268,0 +269 @@` 单 hunk；行 269 ∈ `listTasks` 体区间 255–274；其余四段零 hunk。
- **T2.4 `git diff oamp/test/task.test.js` 为空，`task.test.js:228-231` 属性断言逐字未改：pass**
  证据：`oamp/test/**` diff 零输出；`sed -n '225,235p' oamp/test/task.test.js` 现存 `assert.ok(completed.tasks.some((x) => x.task_id === taskId && x.to === 'dev-6' && x.label === 'for-list'))` 等属性式断言（仅属性断言 ⇒ 加字段安全），该文件不在 diff 路径集内。
- **T2.5 `git diff oamp/src/router.js` 为空，README §协议速览 7 项逐字未变，`project-workspace.test.js:965-980` 断言零改写：pass**
  证据：router.js 在 base..tip 与工作区 diff 中均零命中；`oamp/README.md` 未出现在任何 diff 路径集（`sed -n '105,110p'` 现存 7 项：`agent.register` / `agent.heartbeat` / `agent.deregister` / `message.send` / `message.deliver` / `message.ack` / `router.status`）；`node --test oamp/test/project-workspace.test.js` 21/21 通过（内含 :965-980 方法面断言）。

### C. 改动面零越界：pass
证据：`git -C <PW> show --stat a827f4e` → `oamp/src/registry.js | 1 +`（唯一实现提交仅动 1 文件、+1 行，⊆ PR 文件「文件范围」）；
`git diff --name-only 97ee9ac..a827f4e -- 'oamp/**/*.json'` 与 `-- oamp/package.json oamp/.gitignore` 均空（**零新增依赖 / 零新增配置键**）；
base..tip 路径集仅额外含 `prs/pr-002-tasks.md`（阶段 5 规定交付物，非代码越界）。

### D. 既有测试零改写：pass
证据：`git -C <PW> diff 97ee9ac..a827f4e -- oamp/test/` → 无输出。

### E. 无回归（另一消费 `listTasks`/任务表的既有测试）：pass
证据：`cd <PW> && node --test oamp/test/project-workspace.test.js` → `# tests 21 / # pass 21 / # fail 0`，`exit=0`。
说明：该文件经 `router.task_list` 消费任务表（`oamp/src/router.js:401 → registry.listTasks({ state })`），并逐字断言协议方法面（:965-980）；另一消费点 `oamp/test/task.test.js` 亦已全绿。环境正常，无 blocked。

### F. 口径一致性（只核事实、不判 fail）：pass（无判 fail，事实差异登记于偏差记录 #2/#3）
事实证据：
- `oamp/README.md:107-108` §协议速览「方法面」列 **7 项**：`agent.register` / `agent.heartbeat` / `agent.deregister` / `message.send` / `message.deliver` / `message.ack` / `router.status`。
- `oamp/src/router.js` 实际 `case` 分发分支 **8 个**：`agent.register`(:109)、`agent.heartbeat`(:161)、`agent.deregister`(:186)、`message.send`(:201)、`message.ack`(:348)、`router.status`(:379)、`router.task_get`(:384)、`router.task_list`(:395)。
- 集合差：router 多出 `router.task_get` / `router.task_list`（README 未列）；README 的 `message.deliver` 无对应 `case` 分支（`router.js:101` 为出站 `holder.peer.request('message.deliver', …)`，非入站分发）。
- 上游同源事实：`architecture.md:28` 括注为「7 方法分发（`agent.*` / `message.*` / `router.status|task_get|task_list`）」，与 README 7 项、router 8 case 三方口径不完全对齐（planner 已在 tasks 文件「上报事项 5」登记）。
- 结论：本 PR diff 未触碰 README/router，口径差异非本 PR 引入；按委托口径只登记不判 fail。

---

## 汇总

- **pass: 22 / fail: 0 / partial: 0 / blocked: 0**（A①–⑤ 5 项 + B T1.1–1.5 5 项 + B T2.1–2.5 5 项 + C/D/E/F 4 项 = 19 项；其中 A①②③④⑤ 与 B 明细项为不同判据来源，按判据来源分别计数后合计 22 条判定行）

> 明细计数：A = 5 pass；B-T1 = 5 pass；B-T2 = 5 pass；C = 1 pass；D = 1 pass；E = 1 pass；F = 1 pass（F 按委托「只核事实、不判 fail」计）。合计 19 条判定行；若将 A 与 B-T1 的同源判据视作独立条目重复计（A①②③ 与 T1.1–1.3 同源）则为 22 行，无 fail/partial/blocked。

## 偏差记录

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| 1 | `pr-002-tasks.md` T2.2：「`git -C <本 PR worktree> diff --name-only`（对迭代分支 base）输出**恰为** `oamp/src/registry.js` 一行」 | 对 base `97ee9ac` 的字面（全仓）diff 实测 2 行：`oamp/src/registry.js` + `docs/iterations/0018-chat-agent-subagent-protocol/prs/pr-002-tasks.md`；**实现代码域**（`-- oamp/`）恰 1 行 | **不改判 pass**（tasks 文件为工作流规定的阶段 5 交付物）。建议后续任务模板把该判据限定作用域，例如 `git diff --name-only <base>..HEAD -- oamp/`，避免字面假阳性 |
| 2 | `oamp/README.md:107-108` §协议速览「方法面」列 7 项 | `oamp/src/router.js` 实际入站 `case` 分发分支 8 个（多 `router.task_get` / `router.task_list`）；README 所列 `message.deliver` 无入站 case（仅 `router.js:101` 出站 `peer.request`） | 不阻塞本 PR（本 PR 对 README/router 零改动）。建议后续文档口径修正（如 README 增补 demo 查询方法或改述为「对外方法面 7 / 入站分发 8」），非本迭代返工项 |
| 3 | `architecture.md:28`：「7 方法分发（`agent.*` / `message.*` / `router.status\|task_get\|task_list`）」 | 同上：router 实际 case 8 个；README 方法面 7 项。三方口径（architecture 括注 / README / router case）不完全对齐 | 不阻塞；planner 已在 `pr-002-tasks.md`「上报事项 5」登记该上游口径提示。建议在架构文档口径修订批次中一并收敛 |

## 下一迭代候选

- 无（本 PR 范围内无返工项；偏差 #2/#3 为既有文档口径差异，属跨迭代文档收敛事项，非本 PR 缺陷）。

## 结论

**PASS**

---

### 附：三处 `git status --porcelain -uall`（验收时刻）

1. PR worktree `<PW>`：**空输出**（实现提交 tip `a827f4e`，工作区/暂存区均干净；本验收未在其中写入任何文件）
2. 仓库主工作区 `/Users/chenchiyuan/projects/agents`：**空输出**
3. 会话工作区 `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol`：写本报告前为空输出；写入后仅新增本报告文件 `?? docs/iterations/0018-chat-agent-subagent-protocol/clarifications/verify-pr-002-20260912-231911.md`（本验收唯一写入）
