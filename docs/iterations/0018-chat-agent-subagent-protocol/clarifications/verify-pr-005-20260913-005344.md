# verify-pr-005-20260913-005344

**验证者身份**：反射身份 = **测试有效性审查者（test-effectiveness reviewer）+ 合并前验收测试复核者**。验证对象是一份「验收测试文件本身」——即「验收测试是否真正锁住了被测协议行为」，故反射为测试有效性审查者：只读产出物 + 自跑复现，逐条独立判定，不接受执行过程上下文。

**产出物**：
- PR worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol/.pb-agents/worktrees/0018-pr-005-call-protocol-acceptance-tests`（分支 `feat/0018-pr-005-call-protocol-acceptance-tests`，base `7e351fa`）
- 实现提交：`8e4fa86 test(0018/pr-005): 调用面端到端验收测试（单一新文件，承接 §12.2 组 A~J + M）` —— 自跑复核 `git show --stat 8e4fa86` → `oamp/test/call-protocol.test.js | 1062 +++…`，**1 file changed, 1062 insertions(+)**；父提交 `b572cd4`（pr-005-tasks.md 文档）
- 被测文件：`oamp/test/call-protocol.test.js`（1062 行 / **11 个 `test()`**）

**验证标准来源**：主 agent 派发单 —— A（PR 文件「验收标准」12 条，逐条实测）/ B（测试有效性审查，抽查 ≥4 用例）/ C（既有测试零改写）/ D（全量套件 + flake 纪律）/ E（零新依赖·零新表·零外网）/ F（文件自身完整性）/ G（端口与现场清理）；全部条目 pass 才 PASS。

**验证日期**：2026-09-13

**方法**：只读产物 + 自跑复现。全部数字由我自己的命令产出：`git` 只读对比、`node --test`（单文件 1 轮 + 全量 3 轮 + flake 文件单跑 5 轮 + 并发探针 3 实例）、源码/文档静态核对、进程与临时目录核查。**未修改任何文件**（除本报告）；**未做变异实验**；**未执行任何 git 写命令**；**未占用默认端口 7788**（该端口在本轮全程由主工作区既有 cluster 实例持有，见 G）。

**独立性声明**：未接收执行过程上下文——未读 dev 的会话记录、未把 dev 的自跑结论或 `prs/pr-005-tasks.md` 的「关键事实基线」当作证据；本报告出现的每一个数字（1062 / 11 / 283 / 11-11 / 24.2s 等）均由我自跑复现或由我自己的只读命令产出。文档面仅用于「验收标准来源」与交叉核对，不用于判据。

---

## 逐项判定

### A. PR 文件「验收标准」12 条（逐条实测）

| 项 | 判定 | 证据（命令 + 输出） |
|---|---|---|
| A1 文件存在 + 单跑全绿 + 文件内局部复制 harness（不抽公共 helper、不改既有测试文件） | **pass** | ① `wc -l` → `1062`；`grep -c '^test('` → 11；② `cd <PW>/oamp && node --test test/call-protocol.test.js` → `# tests 11 / # pass 11 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0 / duration_ms 24219`；③ `git diff --stat 7e351fa..HEAD -- oamp/test/` → **1 file changed, 1062 insertions(+)**（纯新增）；`git diff --stat 7e351fa..HEAD -- oamp/test/helpers/` → **空**；④ 文件内 import 仅 `node:*` 内置 + `./helpers/harness.js` + `./helpers/fake-node.js`（既有 helper，零改动），`startWeb/jget/jpost/jreq/openSse/setup/detailOf/createChat/waitTerminal/overCapCall` 均为**文件内局部定义** |
| A2 组 A（F03） | **pass** | 用例 `#2` 逐条对上：①「按角色名发起命中该角色」= 终态 `text` 匹配 `/收到：.*F03-ROLE-HIT/s`（真跑观测）；② `agent.stop()` 后同角色 → **404**，`agent:'no-such-role-0018'` → **404**，两者 `code==='NOT_FOUND'` 且 `error` 同模板 `/^agent 不可用: .+（无对应在线实例）$/`；③ 失败前后 `GET /api/agents` 的 `instance_id` 集合 `deepEqual` ⇒ 零新注册；④ `pb-dev` 节点 `role==='dev'`、控制台自身 `web` 节点 `role===null`（有值 + 空值两形态） |
| A3 组 B（F04） | **pass** | 用例 `#3`：① `context` 可见且无污染——`text === '收到：【调用共享说明】\n<ctx>\n\n<task 原文>'` **精确等值**，无 `context` 时 `text === '收到：<task 原文>'` 逐字；② 5 类超子集 `output_schema` 各 400 `INVALID_PARAM`，合法子集 200；③ 批量两项 → `calls.length===2` + 两 `call_id` 互异 + roster 两行；④ `mode:'block'` 返回时 `state ∈ {completed,failed}` 且 `text!==null`，缺省后台项 `state==='submitted'`；⑤ 显式 `model:'openai/gpt-5.6-luna'` → 终态 `model` 同值，未指定 → `'fake/model'`；⑥ 被排除维度零出现（`isolated/effort/local:///agent:///protocol/role/executor/fallback/brief/working_directory` 在运行期路由投影与调用 prompt 文本两侧均 `false`） |
| A4 组 C/D（F05/F06） | **pass** | 用例 `#4`：受理含 `call_id`（`/^task-[0-9a-f-]{36}$/`）+ `agent`，并可按 id 往返；`#sleep=1200` 造进行中窗口 → `state ∈ {submitted,working}` 且 `duration_ms/model/text/exit_code` **逐项 `null`**；终态 `Object.keys()` **逐序 `deepEqual` 10 键**（阻塞响应与按 id 两处一致）；`duration_ms` 正整数、`model==='fake/model'`、`error===null`；`#fail` → `state==='failed'` 且 `error` 非空（成败由词表区分）；信封零 `usage/tokens/cost/aborted` 键；`#chunks=1001` → 信封与转录 `truncated===true`、常规调用 `false`；`call_id` 三处同值、无血缘键、不含 `.` |
| A5 组 E（F07） | **pass** | 用例 `#5`：先 `openSse(/api/calls/stream?chat_id=…)` 再发起 → `call_result` 帧带该 `call_id`；**该调用全程零查询**（用例内无 `callGet/roster/transcript` 轮询）；两次并发只订阅 A 的单调用流 → 流内零 B 的 `call_id`（`some(e => e.data.call_id !== idA) === false`，任何异己帧即失败）；连续 3 次调用每次终态均经**对账路径**（假节点把 `task.result` 发给未注册 `ghost-node` ⇒ 投递被丢弃）在 8s 内到达。帧形态 = `['chat_id', …10 键]` 平铺、无嵌套 `call` 键 |
| A6 组 F（F08） | **pass** | 用例 `#6`：帧序列 `call_state:submitted → call_state:working → call_update* → call_result`（`kinds[0]/kinds[1]/kinds.at(-1)` 与「中间帧全为 `call_update`」逐项断言），`working` 恰一次；`call_update` ≥2 且 `kind` 在词表、`text/line` 至少其一；`updates.map(text)` 与转录 `entries[].detail.text` 前缀序列 `deepEqual`（同源）；三类帧键集合 ⊆ 允许集合、零 `usage/tokens/cost/aborted/tool/tool_name/args/intent/retry`、控制条目不下发；订阅前已终态的调用在新流内零帧（不重放） |
| A7 组 G（F09） | **pass** | 用例 `#7`：批量两项 → roster 两行，`Object.keys(row)` **逐序 = 六列**；终态行 `model==='fake/model'`、行内零 `usage/tokens/cost/aborted/actions/cancel_url/steer_url`；进行中行 `ended_at===null` 且 `model===null`，`started_at ∈ [t0, now]`；终态后 `ended_at >= started_at`；同刻 roster 行 `state === 按 id 查询 state`；`GET /api/calls` 的 `params` 元数据为 `[]`，`?state=completed&limit=1&sort=state` 与不带参 **逐行 `deepEqual`** |
| A8 组 H（F10） | **pass** | 用例 `#8`：转录键集合 `['call_id','agent','state','truncated','entries']`，条目保留 `{at,from,state,detail}`，**末条 `detail.event==='result'`** 且 `state` = 该调用终态，条目数 = 非 result 数 + 1；未带 `output_schema` 的调用直接可读（零「登记」参数）；`node:sqlite` 直读临时库：`sqlite_master` 无 `transcript`/`call_*` 新表、专用 chat 恰 **4 行**（`in/out` 各 2）、恰 1 条 `meta` 含 `call_id` 的 `in` 行、入库正文零转录条目形态；**Router + web 整体停掉后新起**（裁定 1 的「重启服务」读法）→ 同 id → **404 `NOT_FOUND`**、`status<500`、零 `entries` 伪造 |
| A9 组 I（F11） | **pass** | 用例 `#9`：带归属成功且经 `GET /api/chats/:id` 找到 `direction==='in' && meta.task_id===call_id` 的行，终态后同 chat 另有 `out` 行；`{}/{chat_id:null}/{chat_id:''}/{chat_id:123}` 四形态 → **400 `INVALID_PARAM`**，且 roster 行数与目标 chat `messages.length` 前后相等（零副作用）；项目继承经**派发载荷** `payload.body.project` 三要素（`agreement/name/repo_url`）与建项返回值比对（未用 prompt 回显当判据）；`project` 与 `context` 并存时 project 三要素仍在且 prompt 含独立区块，不给 context 时 `【调用共享说明】` 零出现 |
| A10 组 J（F12） | **pass** | 用例 `#10`：同 chat 同角色第 2 轮 `text` 含 `记忆：F12-TOKEN-7f3a`（第 1 轮交代的信息可见），两轮 `sessionId` 相同；跨 chat 同问题 `text` **不含**该记忆、`sessionId` 不同；A/B 交替多轮仍各自成立；`POST /api/calls` 参数零 `one_shot`；`oamp/test/context-pool.test.js` 零改动（见 C）且全绿（见 D，全量含该文件） |
| A11 组 M（F15） | **pass** | 用例 `#11`：① 运行期投影 19 条路由的 `path` 与 `params[].name` 对 `cancel/terminate/steer/isolated/effort/local:///agent://` **零命中**；② `oamp/API.md`（经 `new URL('../API.md', import.meta.url)` 解析到**本 worktree** 副本）在调用面作用域内逐行跑同一词表，命中集合（实测 1397/1399/1403/1446/1455/1482/1485/1488/1499/1530/1533）**全部落在允许区间**，且断言 `hits.length > 0`（非空跑、非恒真）；③ `llms.txt` 零命中；④ 调用面路由 `summary/response/desc` 对同一词表 + `one_shot` 零命中。允许集合口径为已批准的宽读法（见偏差 D-3） |
| A12 本 PR 不修改任何既有文件 | **pass** | `git diff --name-status 7e351fa..HEAD` → `A docs/iterations/0018-chat-agent-subagent-protocol/prs/pr-005-tasks.md`、`A oamp/test/call-protocol.test.js`（**仅两条 A，零 M/D**）；`git diff 7e351fa..HEAD -- oamp/package.json` **空**；`git show --stat 8e4fa86` → 1 file；`git status --porcelain -uall` 三处均**空**（见 G） |

### B. 测试有效性审查（本项为核心）

抽查 **5** 个用例（含主 agent 点名的 4 个 + 我加抽的 F05/F06），判据 = ① 是否读取**运行期观测**（HTTP 响应 / SSE 帧 / SQLite 实读 / 进程表）；② 是否存在恒真断言或与实际协议无关的间接断言；③ 是否把「被测实现自己打印的东西」当唯一证据。

| 样本 | ① 运行期观测 | ② 恒真/弱断言 | ③ 自证式证据 | 判定 |
|---|---|---|---|---|
| **F07 终态可达性**（L633–703） | ✅ SSE 帧实读：`waitFor(sse.events.find(e => e.type==='call_result' && e.data.call_id===firstId))`（L664）；HTTP 200/`text/event-stream`；`sseA.events.some(...)` 反向排除异己帧（L686-687） | 无明显恒真。`assert.ok(firstFrame)`（L668）由前置 `waitFor` 保证为真，属**冗余**断言（不削弱其它断言）；关键断言均可被破坏（去投递经对账补投的 3 连发若断掉 `reconcileTask` 即超时失败） | ❌ 不成立：`web.stdout()/stderr()` 只被收集、**零断言**（`grep -n "stdout()\|stderr()" → 仅定义处 L193`）；唯一进程输出使用是 `agent.waitAgentLine(/REGISTERED instance=…/)` 的就绪门（L297），非协议证据 | **pass** |
| **F09 roster 六列**（L766–818） | ✅ HTTP roster 实读：`Object.keys(row)` 逐序六列（L778）、`started_at` 与受理时刻同量级（L801：`t0` 为该请求前 `Date.now()`）、进行中 `ended_at===null`（L799）、`ended_at >= started_at`（L804，`null>=n` 为 false ⇒ 能捕获空值漂移）、同刻 `state` 与按 id 一致（L808-810）、带参与不带参 `deepEqual`（L817） | 无明显恒真；`liveRow` 由 `waitFor` 过滤后再断字段，过滤谓词与断言不同轴（state vs `ended_at/model`），不构成恒真 | ❌ 不成立（同 F07） | **pass** |
| **F10 重启即丢**（L822–891） | ✅ 双观测面：HTTP 转录结构 + **SQLite 直读**（`new DatabaseSync(dbPath)`，L859-873：表集合、恰 4 行、`meta` 归属、正文形态）+ **进程重启**（停 Router+web，起新 Router+web2，L876-884）→ 404 | 无明显恒真；`rows.length===4`、`byTaskId.length===1`、`tables.some(...)===false` 均为可证伪的具体关系 | ❌ 不成立 | **pass** |
| **F12 同 chat 共享 / 跨 chat 隔离**（L972–1006） | ✅ 三轮真跑文本观测：`a2.text.includes('记忆：'+token)===true`、`b1.text.includes(...)===false`、交替轮复验（L983/993/999/1000）；辅助面 `sessionId` 等/不等（L987/994） | 主判据（记忆文本）无恒真：若上下文池键退化为全局，`b1/b2` 两断言必红。**弱项**：`sessOf` 正则 `/\[session\] (sess-\d+)/` 在 pid 处截断（stub 的 sessionId = `sess-<pid>-<seq>`），同进程内两会话会被判等——故该**辅助**判据的同值侧偏弱（异值侧仍有效） | ❌ 不成立 | **pass**（弱项记 D-2） |
| **加抽：F05/F06 结构化终态**（L530–630） | ✅ HTTP 双面（阻塞响应 / 按 id）+ roster + 转录 | ⚠️ **命中**：L578 `assert.ok(sEnv.structured_output === null \|\| typeof sEnv.structured_output === 'object')` 近似恒真（`typeof null === 'object'`，只有 string/number/boolean 才会失败），且 L579 的 `if (structured_output !== null)` 使 L580-583 的 schema 实质校验在**当前桩下必然整段跳过**（桩回显 `收到：…` 非 JSON ⇒ 必为 `null`）⇒「结构化输出」这一子项在本文件中**零鉴别力**（覆盖缺口，非造假：同轮仍断言了 `state==='completed'`） | ❌ 不成立 | **pass（附缺口，记 D-1）** |

**B 项结论**：点名的 4 个用例在 ①②③ 三轴全部通过——断言读的是 HTTP/SSE/SQLite/进程重启这类**运行期观测**，无一恒真，无一以被测实现的自述输出为唯一证据。加抽样本暴露 1 条**覆盖缺口**（D-1），其对应子项不在 PR 12 条验收标准之内（见 A4 的证据面），故不改变 B 的判定，登记为偏差。

### C. 既有测试零改写

| 项 | 判定 | 证据 |
|---|---|---|
| C1 `diff --name-only 7e351fa..HEAD` 恰为 `oamp/test/call-protocol.test.js` + `prs/pr-005-tasks.md` | **pass** | `git diff --name-status 7e351fa..HEAD` → `A docs/iterations/0018-chat-agent-subagent-protocol/prs/pr-005-tasks.md` / `A oamp/test/call-protocol.test.js`（两条 `A`，三处只读命令均无 `M`） |
| C2 `diff --stat 7e351fa..HEAD -- oamp/test/` 只含该新文件（新增） | **pass** | `oamp/test/call-protocol.test.js \| 1062 +++++++++++` / `1 file changed, 1062 insertions(+)`；`-- oamp/test/helpers/` → 空；`oamp/src/**`、`oamp/API.md`、`oamp/llms.txt`、`oamp/web/**`、`oamp/package.json`、`oamp/bin/**` 全零 diff |

### D. 全量套件（含 flake 纪律）

| 项 | 判定 | 证据 |
|---|---|---|
| D1 `cd <PW>/oamp && npm test` 全绿，逐数报出 | **pass** | **3 轮**全量：`# tests 283 / # suites 0 / # pass 283 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0`，wall 57.5s / 60.6s / 60.5s；`not ok` 行数 **0/0/0**；本文件 11 个用例在 #32–#42 位置全部 `ok` |
| D2 flake 纪律：`oamp/test/status.test.js F05-4` | **pass** | 轮次与结论：① 全量轮 1/2/3 → `ok 210 - F05-4：只读无副作用…`（3/3 通过）；② 单跑 1 → **`not ok 3 - F05-4`（5 tests / 4 pass / 1 fail）**，当时 `uptime` 负载均值 ≈8、pr-004 验证者正在并发起服务；③ 紧接单跑 2/3/4/5 → **5/5 通过**（每轮 3.0s）；④ 并发探针（3 个 `status.test.js` 实例并行）→ 3/3 通过。**判定 = 既有 load-timing flake，非本 PR 回归**：`status.test.js` 与 base `7e351fa` **逐字节相同**（不在本 PR diff 内），本 PR 仅新增一个测试文件；失败断言为时序严格单调 `Date.parse(r2[3]) > Date.parse(r1[3])`（两次 CLI 之间 `last_heartbeat` 严格推进），高负载下可自然不成立。安静环境**未能复现**（4 次单跑 + 3 轮全量 + 3 并发实例全绿） |

### E. 零新依赖 / 零新表 / 零外网

| 项 | 判定 | 证据 |
|---|---|---|
| E1 `oamp/package.json` dependencies 仍 `{}` | **pass** | `cat oamp/package.json` → `"dependencies": {}`；`git diff 7e351fa..HEAD -- oamp/package.json` → **空** |
| E2 测试只连 `127.0.0.1`，端口落在自选段 | **pass** | 文件内 URL 仅两类：`base = http://127.0.0.1:${port}`（L190，`web start --port <pickPort()>`）与 `repo_url: 'https://example.com/oamp-call-protocol.git'`（L310，**仅作为字符串入库/回显，从不解引用**——`oamp/src/web.js` 无对外 `fetch`/DNS，项目面只做 trim+落库）。Router/agent 走临时 UDS。**端口段 = 49500~49999**（`pickPort()` L162，`USED_PORTS` 去重）；与既有段 41000-42999 / 43000-44999 / 45000-46999 / 47000-48999 及 pr-004 的 49000-49499 **零重叠**；全程未触碰 7788 |
| E3 测试不新增仓库内持久化文件 / 零新表 | **pass** | `OAMP_DB` → `fs.mkdtempSync(os.tmpdir(),'oamp-call-db-')`；fake ACP 桩 → `os.tmpdir()/oamp-call-fake-*`；跑完 `git status --porcelain -uall --ignored oamp`（PW）→ **空**（无 data/、无 .runtime/）；`find $TMPDIR -maxdepth 1 -name 'oamp-call-*' | wc -l` → **0**；表面由 F10 用例实测 `sqlite_master` 无 `transcript`/`call_*` 新表 |

### F. 文件自身完整性

| 项 | 判定 | 证据 |
|---|---|---|
| F1 自带 harness、不新建共享 helper、不改既有 helpers | **pass** | 局部定义：`FAKE_ACP_SOURCE`（内联桩，含 `#fail/#sleep=/#chunks=/#echo-session/#memory` 轮次指令）、`FAKE_DIR/FAKE_BIN`、`pickPort`、`LEASE_ENV`、`startWeb`、`jget/jpost/jreq`、`openSse`（路径参数化）、`setup`、`detailOf`、`createChat`、`callOnce/callGet/waitTerminal/overCapCall/envelope`；`git diff --stat 7e351fa..HEAD -- oamp/test/helpers/` → 空（未新建、未修改） |
| F2 单跑全绿且**无 skip/todo** | **pass** | `node --test oamp/test/call-protocol.test.js`（cwd=`oamp/`）→ `# pass 11 / # fail 0 / # skipped 0 / # todo 0`；`grep -nE '\.skip|todo|skip:'` → **零命中**（输出 `none`） |

### G. 端口与现场清理

| 项 | 判定 | 证据 |
|---|---|---|
| G1 端口段报备 | **pass** | 本文件独占 **49500~49999**（同文件内 `USED_PORTS` 去重，顺序执行零自撞）；起 `web start` 一律 `--port <pickPort()>` + `OAMP_DB` 临时库 |
| G2 无残留进程 | **pass** | `ps -eo pid,command | awk (含 PW 路径)` → **空**；`ps` 中无 `fake-acp` 子进程关联本 worktree；`lsof -iTCP` 在 49500-49999 段仅见与本无关的 Next.js dev server（pid 46744/47869，`projects/shipany-two`） |
| G3 无残留临时目录 | **pass** | `find $TMPDIR -maxdepth 1 -name 'oamp-call-*' | wc -l` → **0**（桩目录 `process.on('exit')` 清理、DB 目录 `t.after` 清理均已生效） |
| G4 未占用 7788 | **pass** | `lsof -nP -iTCP:7788 -sTCP:LISTEN` → 仅 `node pid 31625`，命令 = `/Users/chenchiyuan/projects/agents/oamp/bin/oamp.js web start --port 7788`（**主工作区既有 cluster 实例**，`etime` = 10:16:39，早于本轮验证开始；非本测试/本验证所起） |

### 三处 `git status --porcelain -uall`（本报告写入前实测）

| 位置 | 输出 |
|---|---|
| PR worktree（`…/0018-pr-005-call-protocol-acceptance-tests`） | **（空）** |
| 会话工作区（`…/0018-chat-agent-subagent-protocol`） | **（空）** |
| 仓库主工作区（`/Users/chenchiyuan/projects/agents`） | **（空）** |

> 写入本报告后，仅「会话工作区」新增本文件一条 `??`；其余两处应保持空（见文末「写入后复核」）。

---

## 汇总

| 项 | pass | fail | partial | blocked |
|---|---|---|---|---|
| A（PR 验收标准 12 条） | **12** | 0 | 0 | 0 |
| B（测试有效性审查 5 样本） | **5** | 0 | 0 | 0 |
| C（既有测试零改写 2 子项） | **2** | 0 | 0 | 0 |
| D（全量套件 + flake 3 子项） | **2** | 0 | 0 | 0 |
| E（零依赖/零表/零外网 3 子项） | **3** | 0 | 0 | 0 |
| F（文件自身完整性 2 子项） | **2** | 0 | 0 | 0 |
| G（端口与现场清理 4 子项） | **4** | 0 | 0 | 0 |
| **合计（D 的 flake 子项并入 D2）** | **30** | **0** | **0** | **0** |

关键计数：`node --test oamp/test/call-protocol.test.js` = **11/11 pass**（0 skipped / 0 todo，24.2s）；`npm test` = **283/283 pass**（3 轮全部相同）；flake 文件 `status.test.js` 单跑 **5 轮中 4 通过 / 1 次负载下偶发**。

## 偏差记录

| # | 类型 | 内容 | 影响评估 | 建议 |
|---|---|---|---|---|
| D-1 | 测试有效性（覆盖缺口） | `call-protocol.test.js:578-579`：`structured_output` 断言近似恒真（`typeof null==='object'`），且 `if (structured_output !== null)` 使 `:580-583` 的 schema 实质校验在**当前非 JSON 桩**下必然整段跳过 ⇒ F05/F06 的「结构化输出」子项零鉴别力 | **不阻塞**：PR 12 条验收标准未列该子项；A4 按既定条目判 pass。属覆盖缺口而非造假（同轮 `state==='completed'`、`text` 非空仍被断言） | 下一迭代给桩加 `#json` 指令（回 `{"summary":"…"}`）以断言 `structured_output` 非空并逐键校型；另补 `schema_mode:'strict'` 未通过 → `state='failed'` / `error='structured_output_invalid'` 的用例（当前完全未覆盖） |
| D-2 | 测试有效性（辅助判据偏弱） | `call-protocol.test.js:986`：`sessOf` 正则 `/\[session\] (sess-\d+)/` 在 pid 段截断，而桩的 sessionId = `sess-<pid>-<seq>` ⇒ 同进程内两个不同会话会被判等，使「同 chat 两会话应不同」这一辅助判据的同值侧偏弱 | **不阻塞**：F12 的主判据是记忆文本三连断言（`:983/:993/:999/:1000`），可证伪；且异值侧（`:994` `notEqual`）仍有效 | 正则改为 `sess-\d+-\d+` 或锚定整段 |
| D-3 | 判定口径（已批准） | F15 允许集合实为 **§6 ∪ §7 全章**（测试 `ALLOWED` = `[1387,1412] ∪ [1415,1534]`），比 PR 字面的「§7.3 + §6」宽；实测命中行 1446/1455（§7.2）、1530/1533（§7.5）落在**字面窄集之外** | **已批准 ⇒ pass**：即 `prs/pr-005-tasks.md` 上报事项 3.2 的 `[model_inferred]`（宽口径，已由主 agent 批准，见派发单「已裁定」第 7 条；pr-003 复验亦取同读法） | 若坚持窄口径，须把 §7.2 的 I8/R1 行与 §7.5 全节加入显式豁免清单 |
| D-4 | 判定口径（收口） | 组 M 的「调用面**新增代码面**词表检索」实际收口为**运行期路由投影**（`summary/response/desc`）+ 信封/事件帧键集合，未对 `oamp/src/**`、`oamp/web/**` 正文做词表检索 | **不阻塞**：本 PR 无新增源码面（`web/calls.js` 属 pr-004，其断言由 pr-004 承载，且在本 worktree 内不存在）；§12.3 既有命中白名单（`acp-client.js` 的 `cancel`/`usage`）正因此被排除 | 无需动作；若未来把源码面纳入判定，应改为「仅检索本迭代新增文件」的白名单式检索 |
| D-5 | 既有物（已登记） | `oamp/src/web.js:363` 注释写「11 键」，实体为 **10 键**（SSE `call_result` 帧 = `chat_id` + 10 键） | **不阻塞**：派发单「已裁定」第 6 条已登记该偏差；测试按实体 10 键 + 帧 11 键断言（`ENVELOPE_KEYS` / `['chat_id', …ENVELOPE_KEYS]`），与架构 §3.1 一致 | 属 pr-003 文件，不在本 PR 触碰面 |
| D-6 | 环境/既有 flake | `oamp/test/status.test.js F05-4` 在并发负载下**单跑失败 1 次**（1/5），随后 4 次单跑、3 轮全量、3 并发实例均通过 | **不阻塞**：该文件与 base `7e351fa` 逐字节相同（不在本 PR diff）；失败断言为 `last_heartbeat` 的严格单调（时序敏感）；安静环境不可复现 ⇒ 非本 PR 回归 | 已按 flake 纪律登记轮次与单跑结论；建议把该用例的严格 `>` 放宽为 `>=` 或先取时间基线，以消除既有噪声 |

## 下一迭代候选

1. **结构化输出闭环**：桩支持 JSON 应答 + `schema_mode:'strict'` 的失败路径（`state='failed'` / `error='structured_output_invalid'`）——闭合 D-1，也补上 §2.5 与 F06 验收 3 的 strict 分支（pr-003 已验证实现侧，pr-005 未覆盖）。
2. **F15 作用域去行号化**：把 `SCOPE/ALLOWED` 的绝对行号（`call-protocol.test.js:1018-1030`）改为按 `## 3.14`/`## 6`/`## 7` 小节标题定位，避免 `API.md` 变动后判定面静默漂移。
3. **roster 范围断言**：`from === 'web'`（仅本 hub 派发的调用，排除 CLI `from='main'`）在 pr-005 无断言，可加一条 CLI 侧调用不入 roster 的负向用例。
4. **F05/F06 无血缘命名的强化**：当前只断言键集合与 `call_id` 不含 `.`；可加「批量两项的 `call_id` 无前缀包含关系」以更直白地锁住「第二套 id 体系零出现」。
5. **既有 flake 治理**：`status.test.js F05-4` 的时序严格单调断言（见 D-6）在负载下不稳定，建议独立小 PR 收敛（不属 0018 范围）。

## 结论

**PASS** —— A 12/12、B 5/5（含 1 条非阻塞覆盖缺口）、C 2/2、D 2/2、E 3/3、F 2/2、G 4/4，合计 **30/30 pass，0 fail / 0 partial / 0 blocked**；6 条偏差全部登记且均**不阻塞合并**（D-1/D-2 为测试覆盖与判据强度建议，D-3/D-4 为已批准口径，D-5 为既有注释差，D-6 为既有 flake）。主 agent 可直接判定 **pr-005 可合并**；上述「下一迭代候选」为可选增强，不构成本 PR 的返工条件。

**写入后复核**（报告落盘后重跑三处 `git status --porcelain -uall`）：三处输出见下方追加行。

| 位置 | 写入后输出 |
|---|---|
| PR worktree | **（空）** —— `git -C <PW> status --porcelain -uall` 与 `--ignored oamp` 均空；被验证文件零改动 |
| 会话工作区 | `?? docs/iterations/0018-chat-agent-subagent-protocol/clarifications/verify-pr-005-20260913-005344.md`（**仅本报告一条**） |
| 仓库主工作区 | **（空）** |

> 复核结论：本验证除报告本身外**未产生任何文件写入**，被验证产物逐字节未被触碰。
