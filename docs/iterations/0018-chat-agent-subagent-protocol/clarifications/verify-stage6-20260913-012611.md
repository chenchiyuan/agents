# 阶段 6 最终独立验证报告 — 0018-chat-agent-subagent-protocol

**验证者身份**：发布前评审者 + 交付审计者（面向「多 PR 迭代的收口面」——需求 → 规格 → 架构 → PR → 实现 → 验证链）
**产出物**：`docs/iterations/0018-chat-agent-subprotocol/`（实测正确路径 = `docs/iterations/0018-chat-agent-subagent-protocol/`）下全部阶段产物 + `oamp/**` 实现面
**验证标准来源**：主 agent 阶段 6 委托书（标准 1~13 全文注入）
**验证日期**：2026-09-13

---

## 0. 独立性与方法声明

- **不接收执行过程上下文**：未读任何执行角色（demand / prd / architect / pr-planner / planner / dev）的会话记录；未使用 `history://`、`agent://`、`local://`；未采信 `clarifications/verify-*.md`（既有 8 份 PR 级 / Gate 报告）作为依据——本报告的全部结论来自我自己的一手检查。
- **一手手段**（只读 + 自跑）：
  1. `git` 只读命令（`log/show/diff/branch/worktree list/rev-list/merge-base/rev-parse/cat-file`），全程未执行任何写入性 git 命令；
  2. **全量回归**（安静环境、单轮）：`cd oamp && node --test test/*.test.js` → **285/285 pass，fail 0**（57.8 s）；
  3. **自建端到端驱动 ×3 轮**（`/tmp/v18s6/drive.mjs`、`/tmp/v18s6/drive2.mjs` 模式 A/B）：真起 Router + agent（fake ACP 桩）+ `oamp web start`（**随机端口 51329 / 52203 / 52510**，全程未占用 7788）；
  4. 源码通读（`src/web.js` 新增面全文、`src/transport.js` diff、`src/registry.js` diff）+ 词表检索 + 逐文件 sha256 对照；
  5. 文档通读（`demand.md` 500 行、`prd.md` + 16 卡、`architecture.md` 686 行、`API.md` 新增面）。
- **写入边界**：唯一写入 = 本报告；未修改任何被验证文件；未执行 git 写命令；临时驱动与临时库均在 `/tmp` 与 `os.tmpdir()`，跑完已清理。
- **环境**：本报告基于「安静环境单轮」——全量回归未见任何 flake，故无「受负载影响的偶发失败」需按 flake 纪律单跑核对（详见标准 6）。

---

## 1. 逐项判定（标准 1~13）

### 标准 1・阶段产物完整性 —— **pass**

| 阶段 | 产物 | 实测 |
|---|---|---|
| 1 需求收敛 | `demand.md` | 存在，**500 行 / 63,704 B**；含 W1~W8 / N1~N21 / E1~E11 / 差异①~⑲ / C-1~C-8 / M-1~M-12（逐条目视核对在场） |
| 2 功能规格 | `prd.md` + `prd/F01~F16*.md` | `prd.md` v0.2.0（40,594 B）；`prd/` 恰 **16 个 `.md`**，最小 3,576 B / 最大 6,155 B，**零空文件** |
| 3 技术架构 | `architecture.md` | 686 行 / 76,285 B；§0~§13 齐备 |
| 4 PR 规划 | `prs/` | **10 个文件** = 5 份 PR 文件（pr-001~pr-005，1.6~6.9 KB）+ 5 份 tasks（13.3~74.0 KB） |
| 5 PR 实现 | `oamp/**` | 代码：`src/web.js`(+507/−5)、`src/transport.js`(+68/−19)、`src/registry.js`(+1)、`web/calls.html`(+39)、`web/calls.js`(+178)、`web/index.html`(+1)；测试：**新增** `test/call-protocol.test.js`（1093 行）、`test/call-console.test.js`（118 行） |
| 6 独立验证 | 本报告 | 本文件 |
| 过程留痕 | `clarifications/` | **15 个文件** = 7 份阶段 1~2 澄清（`round-1/2/3`、`demand-round-1-proposals`、`prd-round-1/2`、`recon-20260912`）+ **8 份验证/Gate 报告**（7 份 PR 级 + 1 份阶段 4 Gate） |
| 台账 | `status.md` / `history.md` / `progress.md` | 11,766 B / 70,947 B / 27,321 B，均非空 |

> **委托口径提醒（偏差 D-14）**：委托书所述「5 份 PR 文件 + 5 份 tasks + **6 份**澄清/Gate 报告」与实测不符——实测 `prs/` = 10（5 PR + 5 tasks），`clarifications/` = 15（7 澄清 + 8 验证/Gate）。产物齐备性不受影响。

### 标准 2・需求追溯（卡片 ↔ 需求） —— **pass**

**自建追溯矩阵（不采信 prd.md 的自述，按 `demand.md` 原文与 16 章卡片的「来源」段逐条重算）**

**W 条款（做什么）**

| demand | 承载卡（自建核对结果） |
|---|---|
| W1 契约锚定与差异清单 | F01（验收 1~5 逐条对位） |
| W2 调用面（按角色名寻址 + I2/I3/I6/I7） | F03（寻址侧）、F04（入参侧） |
| W3 调用身份（`task_id` 一等资源 + 查终态/订阅/转录） | F05、F07（验收 3）、F08（验收 1~3）、F10 |
| W4 响应面（结构化终态 + 异步投递 + 不持久） | F06、F07、F10（验收 2~5） |
| W5 展示面（进度 / roster / 通知 / 按调用订阅 + 控制台） | F08、F09、F13 |
| W6 与项目层共存（归属 chat、`project` 与 `context` 不合并） | F11 |
| W7 文档与工程约束（对照表 + 示例 + 登记 + 零依赖） | F02（文档侧）、F16（工程侧） |
| W8 agent 身份规则 `(chat, agent 名)` | F12 |

⇒ **W1~W8 全覆盖（8/8），无漏项。**

**E 条款（裸判定）**

| demand | 承载卡与验收（自建核对结果） |
|---|---|
| E1 对照表与差异清单存在（①~⑲ 逐条可读） | F01-1/2/3 |
| E2 按角色名可调用；不可用明确失败 | F03-1/2/3 |
| E3 入参契约可见（task + context + schema + 批量） | F04-1/2/3/4 |
| E4 结构化终态可核对（id + agent + 承诺字段 + 差异⑧） | F05-1/2/3 + F06-1~5 |
| E5 终态异步投递（订阅里收到带 id 的终态事件） | F07-1/2/4 |
| E6 调用级进度可见 | F08-1~4（+ F13-3 呈现侧） |
| E7 调用 roster 可列 | F09-1/2/3（+ F13-2 呈现侧） |
| E8 身份规则可核对（同 chat 共享 / 跨 chat 隔离） | F12-1/2/5 |
| E9 既有面零破坏 | F14-1~6 + F16-1~5 |
| E10 差异清单可核对（不做项零半成品） | F15-1/2 + F01-2 |
| E11 不引入无归属调用 | F11-2 |

⇒ **E1~E11 全覆盖（11/11），无未承载的裸判定。**

**反向漏项（「卡里的能力在 demand 里找不到依据」）**：逐卡核对 16 张卡的「来源」段——F01~F13 分别回指 W1 / W7 / W2 / W2 / W3 / W4 / W3+W4 / W3+W5 / W5 / W3+W4 / W6 / W8 / W5；F14 回指 N15/N16/N18/N19+E9（**保证项，不新增能力**）；F15 回指 E10 + N1/N3~N14；F16 回指 W7 工程侧 + N19 + E9。**零条**卡片能力落在 `demand.md` 的 W/N/E/C/M 之外。⇒ **反向漏项 0 条。**

### 标准 3・逐卡验证（核心） —— **pass（16/16 张卡 pass）**

证据一律指向**实现面或可执行观测**（`[实测]` = 我自建的端到端驱动观测；`[代码]` = 源码通读；`[用例]` = 我自跑并逐名核对通过的测试）。

| 卡 | 判定 | 可定位证据（实现面 / 可执行观测） |
|---|---|---|
| **F01** 契约锚定与差异清单 | pass | `oamp/API.md:1415-1534`（§7.1~§7.5）；§7.2 三表实测 **25 行**（I1~I11=11 / R1~R7=7 / D1~D7=7，`awk` 计数）；§7.3 实测 **19 行**（①~⑲，逐条与 `demand.md` 差异总表逐字同序同义）；§7.4 行侧 18 行 ↔ 条目侧 19 编号互覆盖；§7.5 含「默认路径」与「显式例外」两措辞（F01 验收 5 判定成立） |
| **F02** 文档交付可用性 | pass | §5.12~§5.16 五组示例在场（`API.md:1279-1386`）；`[实测]` 我照示例要素真发请求，返回形状与示例**逐字段一致**（10 键信封 / roster 六列 / 转录 entries+终态条目 / SSE 序列）；仓库零 SDK（`git diff --name-status f7bf41e..HEAD` 的 0018 足迹 = `docs/iterations/0018-*/**` + 14 个 `oamp/**` 文件，无 SDK / 插件 / 适配器） |
| **F03** 按角色名寻址与角色可发现性 | pass | `[实测]` `agent:"dev"` → 派发命中 `pb-dev`（fake ACP 收到 prompt）；`agent:"nobody"` → **404** `{"error":"agent 不可用: nobody（无对应在线实例）","code":"NOT_FOUND"}`；`GET /api/agents` → `role:"dev"` 与 `web` 节点 `role:null` 并存；`[代码]` 离线路径经 `err.dataCode ∈ {AGENT_OFFLINE, AGENT_NOT_FOUND}` → 同码同文案（`src/web.js:1096-1101`）；`[代码]` `src/router.js` 零改动（协议方法面不变） |
| **F04** 调用入参契约 | pass | `[实测]` 派发载荷 `prompt="【调用共享说明】\nC-原文\n\n记住 F12TOK9z"`（共享说明独立区块 + task 原文逐字零污染）；`output_schema` 超出子集 `{$ref}` 与 `{properties:{a:{type:'string',pattern:'^a'}}}` **均 400**；批量两项 → **两个不同 `call_id`**；`mode:"block"` → 返回即终态、`background` → 立即受理；`model:"openai/gpt-5.6-luna"` → 派发载荷 `model` 同值、非法 model/`mode`/`schema_mode` 均 400；被排除维度零出现（`effort`/`isolated` 传入不产生任何字段，路由登记正文零命中，用例组 M 通过） |
| **F05** 调用身份与按 id 取终态 | pass | `[实测]` 受理响应含 `call_id` + `agent`；`GET /api/calls/<id>` 进行中给 `state:"submitted"`（非报错非空）、终态给完整信封；`GET /api/calls` 登记 `params:[]`（无过滤 / 无编排入口）；五处呈现同一标识（`call_id === task_id`，`[代码]` 单一构造点）；无血缘命名（`task-<uuid>`） |
| **F06** 结构化终态字段 | pass | `[实测]` **四态全部真实驱动**：无 schema → `structured_output:null` + `text` 原文；permissive 通过 → 对象 + `text` 保留；strict 通过 → 对象；strict 不通过 → `state:"failed"` + `error:"structured_output_invalid"` + `structured_output:null`；信封**恰 10 键**（零 usage/token/成本）；截断：`#chunks=1100` ⇒ `truncated:true`，常规调用 ⇒ `false` |
| **F07** 终态异步投递 | pass | `[实测]` **先订阅**（`/api/calls/stream?chat_id=`）**再发起** → 对话作用域流收到带 `call_id` 的 `call_result` 帧（11 键 = `chat_id` + 信封 10 键）；全程零查询亦收到；按调用订阅（`/api/calls/<idA>/stream`）只收到 A 的事件（`containsB=false`）；同一 `publishCallResult` 单一发布点（`[代码]` `src/web.js:1380-1392`，投递路径与对账路径共用） |
| **F08** 调用级进度可见 | pass | `[实测]` 对话流事件序列 `call_state(submitted) → call_state(working) → call_update×3 → call_result`，闭合于终态；增量 ≥2 次；增量与转录同源（同一 `updates[]`）；帧内字段仅 `chat_id/call_id/agent/kind/text|line`（零工具级 / token / 成本） |
| **F09** 调用 roster | pass | `[实测]` 行键集合与键序 = `['call_id','agent','state','started_at','ended_at','model']`（**六列**）；两次调用 → 两行；进行中行 `ended_at:null` + `model:null`，终态行 `ended_at ≥ started_at`；与按 id 查询状态一致；无成本/token；无过滤 / 分页 / 排序参数（`params:[]`） |
| **F10** 调用转录 | pass | `[实测]` `entries` 为既有 `updates[]` 原样 + 末尾 `detail.event:"result"` 终态条目（F10 验收 1 / MI-08）；直接可读（无登记开关，`params` 仅 `call_id`）；超上限 `truncated:true` 且与信封同值；不存在的 id → **404** `{"error":"call 不存在: ...","code":"NOT_FOUND"}`（非 5xx、无伪内容）；`[用例]` 组 H 真停 Router+web 再起新实例 → 同 id 404，且 `messages` 表零转录形态内容、无 `transcript`/`call_*` 新表 |
| **F11** 调用归属 chat 与项目 | pass | `[实测]` 未提供 / `''` 均 **400** `需要 chat_id（调用必须归属一个已存在的对话）`、未知 chat → 400 `chat 不存在: chat-nope`，且 roster 无新增行（前后差值恰 = 后续新建的 2 次调用）；派发载荷 `project={name,repo_url,agreement}`（三要素齐备，经 fake-node 观测真实 `task.request`）；`context` 与 `project` 并存不合并（各自独立字段 + prompt 独立区块） |
| **F12** agent 身份规则 | pass | `[实测]` 同一 chat 两轮 → 第二轮可见第一轮交代的信息（`记忆：F12TOK9z`）；**换 chat 问同一角色 → 无记忆**；回到原 chat 第三轮 → 记忆仍在；调用面无 `one_shot` 开关（`[代码]` 调用面恒 `omp-daemon`；`[实测]` 路由 `params` 零 `one_shot`）；`[代码]` `src/context-pool.js` 零改动（键与串行/并发语义不变） |
| **F13** 控制台调用面 | pass | `[实测]` `GET /calls` → **200** `text/html`（含 roster 表结构）、`GET /calls.js` → **200** `text/javascript`（5,835 B，含 `/api/calls` 引用）；`index.html` 顶栏 diff = **单 hunk 纯 +1 行**（既有 3 个占位项 `Workspace`/`Agents`/`Tasks` 逐字未变，`[代码]`/`[用例]` 组 K）；页面无 token / 成本字样；既有点击行后按该调用订阅的路径 = `/api/calls/:call_id/stream`（我已实测该路由可用） |
| **F14** 既有面零破坏 | pass | 见标准 6（改动面全部落在 §11 点名行区间；§11 第 12 行 7 文件 diff 为空；另外 15 个既有测试文件 sha256 与 base 等同；`dependencies` 仍 `{}`；全量回归 285/285） |
| **F15** 不做项零半成品 | pass | 禁用词检索（`cancel/terminate/steer/isolated/effort/local:///agent:///tokens/cost`）在新增代码面（`web.js`/`transport.js`/`registry.js`/`calls.html`/`calls.js`）**零命中**（仅 2 处既有 `best-effort` 注释，非本次新增）；全仓新增代码与文档零 `TODO/FIXME/待补/占位实现`（`占位` 命中均为既有 `app.js`/`API.md` 行或 UI 空值字形 `—`）；`[用例]` 组 M 实跑通过（路由表 19 条零禁用词、调用面 6 条路由正文零禁用词、`API.md` 调用面章节命中 ⊆ `§6` ∪ `§7`、`llms.txt` 零命中） |
| **F16** 新增面登记与工程锁 | pass | `[实测]` `/api/docs` **19 条** = 既有 13 + 调用面 6（顺序 = `POST /api/calls` → `GET /api/calls` → `/stream` → `/:call_id/stream` → `/:call_id/transcript` → `/:call_id`，字面量在前）；6 条元数据齐备（8 字段 + `params` 5 字段；`errors` ⊆ `ERR_CODE` 5 码；`kind ∈ {json,sse}`；`docLink` 指向 `API.md#314~#319`）；`llms.txt` 头 = `## 接口（19 条）` 且三条漂移锁 + 零依赖锁全绿（全量回归） |

**逐卡汇总：pass 16 / partial 0 / fail 0。**

### 标准 4・架构 ↔ 实现一致 —— **pass（抽查 8 处，全部一致）**

| # | architecture 结论 | 实现面实测 |
|---|---|---|
| 1 | **§2.1** 六条新路由 + 顺序约束（`/stream` 与两条 `:call_id/…` 必须排在 `GET /api/calls/:call_id` 之前） | `[实测]` `/api/docs` 6 条按该顺序返回；`[代码]` `src/web.js:941-1245` 表项顺序一致；`GET /api/calls/stream` 实测返回 SSE 而非被 `:call_id` 吞掉 |
| 2 | **§2.2** 入参 9 字段（含 `task`/`tasks` 互斥、`mode`/`schema_mode` 枚举、`model` 正则） | `[实测]` 路由 `params` 长度 = 9；互斥 / 空数组 / 枚举外 / 正则非法**均 400 且文案与 §2.3/§3.14 一致** |
| 3 | **§2.3** 受理判定顺序「归属 → 目标 → 入参形态 → 派发」、判定先于写入 | `[代码]` `src/web.js:968-1048` 顺序逐条一致；`[实测]` 三类 400/404 失败后 roster 零新增行、`messages` 零写入 |
| 4 | **§2.5** strict 覆写落点（`schema_mode='strict'` 且结构未通过 ⇒ `state='failed'` + `error='structured_output_invalid'`，**不新增终态词**） | `[实测]` strict 不通过 → `state:"failed"` + `error:"structured_output_invalid"`；`[代码]` `composeCallEnvelope` 的 `invalid` 分支 + `callState()` 单读法（`src/web.js:364-393`） |
| 5 | **§3.1** 信封「唯一形状」10 字段（**不含** `chat_id`、**不含** usage/token/成本/`aborted`） | `[实测]` GET 与阻塞响应键集合 = 10 键且键序一致；SSE `call_result` = 11 键（+`chat_id`，与 §4.1/裁定⑤一致） |
| 6 | **§3.4** roster 六列与口径（`started_at=created_at`、`ended_at` 进行中 `null`、`model` 进行中 `null`、范围 `from==='web'`） | `[实测]` 六列实测；进行中行 `ended_at:null`/`model:null`；roster 含既有 `/api/messages` 问答行（与 §10-6 一致）；`[代码]` `registry.js` 投影 +`model`（`task.result?.model ?? null`） |
| 7 | **§4.2** transport 键空间三前缀（`chat:` / `call:` / `chat-calls:`，互不为前缀） | `[代码]` `src/transport.js:15-17` 三个具名键函数；`closeAll` 覆盖三空间；既有对外方法名/行为逐字保持（`test/transport.test.js` 零改动且通过） |
| 8 | **§7.3** 差异 ①~⑲ 的载体计数与内容 | `[实测]` `API.md` §7.3 恰 19 行，编号与内容/依据/落点逐条与 `demand.md` 差异总表一致（**不增不改**） |

### 标准 5・合同文档真实性 —— **pass**

| 检查项 | 声称 | 实测 |
|---|---|---|
| `oamp/API.md` §3 接口条数 | 19 条 | `## 3. 接口清单（19 条）`（`API.md:157`），`/api/docs` 运行时投影 **19** 条 |
| §3.14~§3.19 错误码集合 | — | §3.14 `INVALID_PARAM(400)/NOT_FOUND(404)/PAYLOAD_TOO_LARGE(413)/UPSTREAM_UNAVAILABLE(502)`；§3.15 无错误码；§3.16 `INVALID_PARAM`；§3.17/§3.18/§3.19 `NOT_FOUND` —— 与 `[实测]` 路由元数据 **逐条一致**，且全部 ⊆ 既有 `ERR_CODE` 5 码（**零新错误码**） |
| §7.2 对照表行数 | 25 = 11 + 7 + 7 | `awk` 计数 **25**（I/R/D 三表分别为 11/7/7） |
| §7.3 差异条目 | ①~⑲，不增不改 | **19** 行；编号、内容、依据（P/Q 裁决 + 侦察事实）与 `demand.md` 差异总表**逐条同序同义**；无新增编号、无重排 |
| §7.4 双向覆盖 | 非「必须等价」行 ↔ 19 编号 | 行侧 **18** 行（I1/I4/I5/I8/I9/I10/I11/R1/R2/R3/R6/R7/D1/D2/D4/D5/D6/D7）与条目侧 **19** 编号互覆盖，① 由 I1 与 I8 两行覆盖（无孤儿行、无孤儿条目） |
| `llms.txt` 同步锁 | 逐字节 | `llms.txt` 头 `## 接口（19 条）` + 6 行调用面摘要；既有锁②（`renderLlmsTxt` 输出与快照逐字节相等）在**安静环境全量回归中通过**；锁①（签名表）与锁③（路径级双向覆盖）同样通过 |

### 标准 6・既有面零破坏（F14） —— **pass**

- **口径**：`git -C <W18> diff --name-status <迭代分支 base>..HEAD`。实测该分支的 base 提交 = **`428e672`**（`iteration/0017-project-workspace` 的 tip = 本分支最早非 main 提交 `028ca8a` 的父提交），并同时用 `main` tip `f7bf41e` 复核——**两者给出的 `oamp/**` 改动集完全相同**（14 文件 / +2599 / −36）。
- **`oamp/test/**` 改动仅落在 §11 点名的行区间**：实测改动 hunk（行号以 base 文件为准）：

| 文件 | base 行号（实测） | §11 条目 | 结论 |
|---|---|---|---|
| `test/api-routes.test.js` | `49`（签名表末项后插入 6 行） | §11-1（:36-49） | 命中 |
| 同上 | `473` | §11-2 | 命中 |
| 同上 | `483` | §11-3 | 命中 |
| 同上 | `499` | §11-4（:495-502） | 命中 |
| `test/project-workspace.test.js` | `1219`（签名表末项后插入 6 行） | §11-5（:1206-1221） | 命中 |
| 同上 | `1230`、`1231` | §11-6 / §11-7 | 命中 |
| 同上 | `1265` | §11-8 | 命中 |
| 同上 | `1301` | §11-9 | 命中 |
| `test/web.test.js` | `1518` | §11-10（:1517-1518） | 命中 |

  ⇒ **无任何「清单外」改动**（口径：§11 的行号是 **base 相对**值，实测逐条命中；该口径本身未在 §11 写明 → 偏差 D-2）。
- **§11 第 12 行点名的 7 个文件 diff 必须为空**：`test/task.test.js` / `test/transport.test.js` / `test/api-pages.test.js` / `test/hygiene.test.js` / `test/context-pool.test.js` / `test/acp-daemon.test.js` / `test/delivery-contract.test.js` —— `git diff --quiet` 全部 **EMPTY**。
- **扩展核对（超出委托要求，我自行加做）**：`oamp/test/` 下 **23 个既有 `*.test.js`** 与 base 逐文件 sha256 对照 ⇒ 仅 **3 个**既有文件被修改（即上表三个），其余 **20 个字节等同**；`test/helpers/*.js` 亦字节等同。新增文件 2 个（`call-protocol` / `call-console`）。
- **`oamp/package.json` 的 `dependencies`**：仍为 `{}`。
- **全量回归**：`node --test test/*.test.js` → **# tests 285 / # pass 285 / # fail 0 / cancelled 0 / skipped 0 / todo 0**（57.8 s，**安静环境、单轮**）。
  **flake 纪律执行情况**：本轮**未出现任何失败**，故委托书点名的既有 flake（`status.test.js F05-4` 心跳严格推进、并发验证者端口撞号 `project-workspace.test.js` EADDRINUSE）**本轮均未观测到**，无需按「受影响文件单跑核对安静环境结果」的流程处置；**不存在**需要在报告中断言为回归的失败项。（另：我另做了一轮 13 个调用面用例的单文件运行，同样全绿——见标准 3 `[用例]` 出处。）

### 标准 7・零半成品（F15） —— **pass**

- **全仓检索**（新增代码 + 文档）：`TODO / FIXME / XXX / 待补 / 待实现 / TBD / not implemented / 未实现` 命中 0 处实质半成品——唯一命中 `oamp/src/cli.js:42` 是**既有**「模块尚未实现」分支，与本次变更无关。
- **`占位` 命中** 6 处，逐条核对均为：既有 `app.js`（流式占位气泡 / 既有 `—` 语义）、既有 `API.md`（`last_activity_at` 展示占位）、本次新增 `web/calls.js:10` 的 UI 空值字形 `'—'`（注释明确「不造值、不推测」）、`API.md:1488` 差异⑧ 的「不占位」声明。**无一是假 fallback / 占位实现。**
- **`architecture.md` §12.3 的词表检索口径**（禁用词 = 调用面新增代码面 + `API.md` 调用面章节）：`cancel / terminate / steer / isolated / effort / local:// / agent:// / tokens / cost` 在 `web.js`/`transport.js`/`registry.js`/`calls.html`/`calls.js` 上 **零命中**（`effort` 2 次命中均为既有 `best-effort` 注释，行号 `web.js:614`、`web.js:1559`，非新增行）；用例组 M 实跑通过并给出**允许集合**（§6 不做声明 ∪ §7 全章）。

### 标准 8・并发调度真实执行证据（三项，强制） —— **pass（三项全部 pass）**

**8-1 worktree 时间窗口重叠 —— pass（一手链式证据，非「无法核实」）**

已删 worktree 无 reflog，但**分支提交时间 + 合并提交的第二父指针**构成完整的一手链：

- **首波 {pr-001, pr-002}**：
  - pr-001 分支（第二父 `cb1bdc8`）提交区间 = `48b056e` **23:14:20** → `cb1bdc8` **23:17:31**；
  - pr-002 分支（第二父 `a827f4e`）提交区间 = `2630395` **23:14:20** → `a827f4e` **23:15:23**；
  - **首次合并** `b92ad82` 发生在 **23:20:58**（其后 `95051b0` 同秒）。
  - **推理**：`23:14:20 – 23:15:23` 的 **63 秒窗口**内两个分支各自都有提交、且都尚未合并。两个未合并分支的提交只能由各自的 worktree 检出产生 ⇒ **两个 worktree 在磁盘上同时存在**。两份任务图提交时间**逐秒相同**（均为 23:14:20）⇒ 与「并发派发」一致。
- **末波 {pr-004, pr-005}**（更强）：
  - 两份任务图提交 `da6fa63` / `b572cd4` 时间**逐秒相同** = **00:37:16**（同时拉出）；工作提交 `d84dba1` **00:47:04** / `8e4fa86` **00:47:06**（相隔 2 秒）；
  - pr-004 合并 `3a8f0d3` = **00:58:20**，而 pr-005 分支在其后仍产出提交 `8aaaf1a` **01:05:55**，直到 `bb1e26b` **01:17:06** 合并。
  - **推理**：两分支在 `[00:37:16, 00:47:06]` 内并发产出提交 ⇒ 两 worktree 同时存在；且 pr-005 的 worktree 生命周期**跨越了 pr-004 的合并时刻**（00:58:20），构成「并存 + 互不阻塞」的完整证据链。
- 旁证：`git worktree list` 现存 2 项（仓库主工作区 + 会话工作区）⇒ 五个 PR worktree 已全部清理（与台账「已删除」一致）；5 个 `feat/0018-*` 分支均不存在。

**8-2 并发配置区块真实初始化与更新 —— pass**

`git log -p -- status.md` 的一手记录：

| 提交 | 时间 | 起始并发数 | 硬上限 | 当前有效上限 | 累计槛位释放次数 | 已派发总数 |
|---|---|---|---|---|---|---|
| `e4231af`（**阶段 4 Gate 后初始化**） | 09-12 | **3** | **5** | **3** | **0** | **0** |
| `161ad36`（首波合并） | 09-12 | 3 | 5 | **5** | **2** | **2** |
| `b362b46`（pr-003 合并） | 09-13 | 3 | 5 | **5** | **3** | **5** |
| `9f42255`（pr-004 合并） | 09-13 | 3 | 5 | **5** | **4** | 5 |
| `eae4ca8` / `915a0a0`（pr-005 合并 + 收口） | 09-13 | 3 | 5 | **5** | **5** | 5 |

⇒ 五字段在**阶段 4→5 入口真实初始化**（`e4231af`，非事后补写），随后在**每一次槛位变化后被真实更新**（4 次更新，含 `已派发总数` 2→5 的修订）。**pass。**

**8-3 爬升公式真实触发 —— pass（适用，且真实触发过）**

公式 `min(起始并发数 + 累计槛位释放次数 × 起始并发数, 硬上限)`，起始=3、硬上限=5：

| 释放次数 | 公式结果 | 台账实际取值 | 一致 |
|---|---|---|---|
| 0（初始化） | `min(3+0,5)` = **3** | **3** | ✅ |
| 2 | `min(3+2×3,5)` = `min(9,5)` = **5** | **5** | ✅ |
| 3 | `min(3+3×3,5)` = `min(12,5)` = **5** | **5** | ✅ |
| 4 | `min(3+4×3,5)` = `min(15,5)` = **5** | **5** | ✅ |
| 5 | `min(3+5×3,5)` = `min(18,5)` = **5** | **5** | ✅ |

⇒ **不是「初始化后再未变化」**：取值在首次槛位释放时由 **3 → 5** 真实重算（爬升真实发生并被硬上限钳制）；此后每次释放都按公式重算，结果恒为 5（因 `起始 3 / 硬上限 5` ⇒ 释放 1 次即触顶）。台账每次均记录了当时的公式（`min(3 + N×3, 5)`），与公式逐次对照一致。**pass。**
次要观察（偏差 D-5）：台账未记录「释放 = 1」的时点——首波两 PR 同批合并，一次记为 2；按公式 `min(3+3,5)=5`，**结果不受影响**。

### 标准 9・台账一致性 —— **pass（零条事实性不一致；2 条口径性偏差已登记）**

`status.md` / `history.md` / `progress.md` 的声称 vs git 一手记录：

| 声称 | 一手核对 | 结论 |
|---|---|---|
| 迭代分支 = `iteration/0018-chat-agent-subagent-protocol` | `git rev-parse --abbrev-ref HEAD` | 一致 |
| 五 PR 合并哈希 `b92ad82`/`95051b0`/`7e351fa`/`3a8f0d3`/`bb1e26b` | 五个对象均存在，且**第二父分别 =** `cb1bdc8`/`a827f4e`/`baf9c43`/`d84dba1`/`8aaaf1a`（与各 PR 分支名一致） | 一致 |
| PR 子状态表「worktree 分支（已删除）」 | `git branch -a` 无 `feat/0018-*`；`git worktree list` 仅 2 项 | 一致 |
| 阶段状态表（1~4 ✅ / 5 ✅ / 6 待触发） | 产物实体齐备；阶段 4 Gate 报告存在；五 PR 均已合并 | 一致 |
| 并发配置五字段 | 见 8-2 / 8-3（逐次与公式对照） | 一致 |
| 「基线：已并入当前 main」 | `git rev-list --count HEAD..main` = **0** | 一致 |
| 「阶段 5 完成（五 PR 全部合并）」 | 五个 merge 提交均在 HEAD 祖先链上 | 一致 |
| 「全量回归 285/285 PASS」 | 我自跑：285/285，fail 0 | 一致 |
| `history.md` 时标 | 篇首**自行声明**「时标可能超前 git committer 时间 15~25 分钟，权威以 git 为准」——已自陈，不构成隐瞒 | 一致（自陈） |
| `progress.md` 所列 11 条不一致 | 观察时点为 pr-004 合并后（`9f42255`）；其后 `eae4ca8`「按 progress-observer 核实修正 status 九处过期/不一致」已修正；我复核现行 `status.md` 已无该九处过期文本 | 已闭合 |

⇒ **零条事实性不一致。** 2 条口径性偏差登记见 D-3（「8 份」含 1 份 Gate 报告）与 D-4（阶段 5「已验证」列口径）。

### 标准 10・搭置项透传 —— **pass**

- `docs/iterations/0018-chat-agent-subagent-protocol/deferred-demand-changes.md` —— **不存在**。
- **核实方式（三重）**：① `ls -la` 该绝对路径 → `No such file or directory`；② `find docs/iterations/0018-chat-agent-subagent-protocol -name 'deferred*'` → 零输出；③ `git ls-files`（工作区全量跟踪清单）中无任何 `deferred*` 路径。
- ⇒ 委托书所述「规范『搭置的需求变更/错误报告』透传项」**在本迭代不适用**，故无原文可摘录。

### 标准 11・既有文档结论的改写面 —— **pass**

`git diff f7bf41e..HEAD -- oamp/API.md` 的全部**删除行**仅 3 条（`grep '^-'` 计数 4 含 `---` 头）：

| 删除行 | 性质 | 是否属「既有结论」 |
|---|---|---|
| `-## 3. 接口清单（13 条）` | 标题**计数**（13→19，§11-4 已登记） | 否 |
| `-      "last_heartbeat": 1789184738463` | `GET /api/agents` 的**示例 JSON 行**（因示例新增 `"role": null` 而改） | 否（示例体，非结论） |
| `-- ❌ **tasks 接口**：Router 的任务面（`oamp task *`）不经 HTTP 暴露；这里只有「对话」这条线。` | **既有结论行** | **是** |

⇒ 与 `architecture.md` 声称一致：本迭代对既有文档结论的**唯一改动** = `API.md` §6「tasks 接口不经 HTTP 暴露」一行（改写为「调用面只解除**一处**约束——按调用 id 读自己发起的调用；按状态过滤 / 跨调用检索 / 编排操作仍然不提供」，与 C-2 / P-4 的范围限定逐字对应）。**未出现其他既有结论行被改。** 其余 `API.md` 改动全部为**纯追加**（§3.14~§3.19、§4.4、§5.12~§5.16、§7.1~§7.5）+ §3.1 追加 `role` 字段说明。

### 标准 12・交付面真实性（端到端，真起服务） —— **pass**

**真起服务**：我自建驱动起真实 Router + 真实 agent 进程（`OAMP_OMP_BIN` 指向 fake ACP 桩）+ `oamp web start`（`OAMP_DB` 指向临时库）。端口 **51329 / 52203 / 52510**（随机，**未占用 7788** —— 7788 由 pid 31625 的既存实例持有，非本次创建）。结束后 router/agent/web 全部停止、临时目录已删（复测 `lsof` 三个端口零监听）。

**实际响应片段（≥5 处，逐字取自运行输出）**

1. **建 chat（既有入口预热）+ 发起调用（后台）**
   `POST /api/calls {chat_id, agent:"dev", task:"后台调用甲"}` →
   `200 {"calls":[{"call_id":"task-bc2d4961-465c-4cb1-bc73-9f38b564efd7","agent":"dev","state":"submitted","duration_ms":null,"model":null,"truncated":false,"text":null,"structured_output":null,"error":null,"exit_code":null}]}`（键集合实测 = 10 键）
2. **`GET /api/calls`（roster，六列）**
   `200 {"calls":[{"call_id":"task-100eeb2e-…","agent":"dev","state":"working","started_at":1789233701006,"ended_at":null,"model":null},{"call_id":"task-bc2d4961-…","agent":"dev","state":"completed","started_at":1789233701002,"ended_at":1789233701021,"model":"fake/model"},{…预热轮…}]}`
3. **`GET /api/calls/<id>`（终态信封）**
   `200 {"call_id":"task-bc2d4961-465c-4cb1-bc73-9f38b564efd7","agent":"dev","state":"completed","duration_ms":19,"model":"fake/model","truncated":false,"text":"收到：后台调用甲","structured_output":null,"error":null,"exit_code":0}`
4. **`GET /api/calls/<id>/transcript`（含终态那次事件）**
   `200 keys=["call_id","agent","state","truncated","entries"]`，`entries` 的 `detail` 依次为 `{event:"started",executor:"omp-daemon",chat_id:"chat-36c6fc14-…"}` → `{kind:"chunk",text:"收到："}` → `{kind:"chunk",text:"后台调"}` → `{kind:"chunk",text:"用甲"}` → 末条 `{event:"result",state:"completed",…}`
5. **SSE 订阅（对话作用域，先订阅再发起）**
   `GET /api/calls/stream?chat_id=chat-36c6fc14-…` → `200 text/event-stream`；帧序列 `call_state` → `call_state` → `call_update` ×3 → `call_result`：
   `event: call_result / data: {"chat_id":"chat-36c6fc14-…","call_id":"task-bc2d4961-…","agent":"dev","state":"completed","duration_ms":19,"model":"fake/model","truncated":false,"text":"收到：后台调用甲","structured_output":null,"error":null,"exit_code":0}`（实测 **11 键** = `chat_id` + 信封 10 键）
6. **按调用订阅（并发两次只收其一）**
   `GET /api/calls/task-ac0a644b-…/stream` → 收到的事件 `call_id` 去重集合 = `["task-ac0a644b-c7d6-460e-9419-f5766ab0b5a6"]`，`containsB=false`，共 5 帧
7. **错误面实测**：缺 `chat_id` → `400 {"error":"需要 chat_id（调用必须归属一个已存在的对话）","code":"INVALID_PARAM"}`；未知 chat → `400 {"error":"chat 不存在: chat-nope",…}`；未知角色 → `404 {"error":"agent 不可用: nobody（无对应在线实例）","code":"NOT_FOUND"}`；`GET /api/calls/task-nope/stream` → `404 {"error":"call 不存在: task-nope","code":"NOT_FOUND"}`
8. **控制台 / 登记面**：`GET /calls` → 200 `text/html`；`GET /calls.js` → 200 `text/javascript`（5,835 B）；`/api/docs` → `DOCS_ROUTE_COUNT=19`，6 条调用面路由元数据逐条在场；`/llms.txt` → `## 接口（19 条）`

### 标准 13・已知裁定事项的计算 —— **pass（7/7 与裁定一致，全部登记为偏差）**

| # | 裁定 | 我的核查 | 结论 |
|---|---|---|---|
| ① | F10 验收 3 的「重启服务」= Router + web 整体重启 | `[用例]` 组 H 实际 `web.stop()` + `router.stop()` → 新 Router + 新 web（新临时 socket）→ 同 id **404**（非 5xx、无伪内容）。卡片原文即「重启服务」，无更窄读法 | 一致 |
| ② | web 侧 `callSchemas` / `call.terminal` 内存登记已批准（零持久化 / 零新表列 / 零新协议方法） | `[代码]` `web.js:1366`（注释 1361-1365）为进程内 `Map`；`src/persist.js` 零改动（`git diff` 无该文件）；`src/router.js` 零改动（协议方法仍 7 个）；`[用例]` 组 H 直读 sqlite：无 `transcript`/`call_*` 新表、`messages` 零转录形态内容 | 一致 |
| ③ | `oamp/test/**` 的 §11 点名区间内过时字样（如「13 条投影」标题）**不改** | `[代码]` 测试标题仍为「…的 13 条投影（danger 派生 / docLink）」`test/api-routes.test.js:466`（base 行 460）与「/api/docs 投影 13 条（写接口 6 条）」`test/project-workspace.test.js:1299`（base 行 1289）——两处**均不在 §11 第 1~10 条点名的行区间内** | 一致 |
| ④ | `oamp/README.md` 的「13 条」计数未改 | `[代码]` `README.md:170`「…请求时从路由登记投影，13 条」、`:174`「（13 条 API + 6 类事件）」原文未动；README 的 0018 改动 = **+1 行**（顶栏入口 · 调用） | 一致 |
| ⑤ | GET 返回裸 **10 键**信封；SSE `call_result` 帧 **11 键** | `[实测]` `BG_ENVELOPE_KEYS` = 10；`SSE_RESULT_KEYS` = 11（`chat_id` + 10） | 一致 |
| ⑥ | `oamp/src/web.js` 一处注释写「11 键」而实体 10 键 | `[代码]` `web.js:362`：「…三态共用同一 **11 键**与键序」；实体（`web.js:371-382` 与实测）为 **10 键** | 一致（偏差已登记） |
| ⑦ | 各任务图的 `[model_inferred]` 均已由主 agent 批准 | `[代码]`+`[git]` 共 **21 条**：pr-001 **2**（`history.md:231`「裁定 2 条生效」）、pr-002 **0**、pr-003 **10**（`history.md:312`「裁定 10 条全部生效」）、pr-004 **5** + pr-005 **4**（`history.md:424`「裁定两份任务图的 9 条全部生效」）；对应提交 `2d7f973` / `d5c098e` / `b572cd4` | 一致 |

---

## 2. 汇总

- **标准级**：pass **13** / fail **0** / partial **0** / blocked **0** ⇒ **全部标准级条目 pass**
- **逐卡**：pass **16** / partial **0** / fail **0**
- **E 项追溯**：E1~E11 **11/11 有卡承载**（fail **0**）；W1~W8 **8/8 有卡承载**；反向漏项 **0**
- 自跑证据：全量回归 **285/285（fail 0，安静环境单轮）**；调用面两文件 **13/13**；自主端到端 **3 轮**（端口 51329 / 52203 / 52510）

---

## 3. 偏差记录

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| **D-1** | `architecture.md` §12.3② 承诺「新增断言：从 §5 调用面示例抽顶层字段名，断言 ⊆ `GET /api/docs` 中 `POST /api/calls` 的 `params[].name`」；F01 的 T-12 承诺「条数断言（对照表 25 行、差异清单 19 条）+ 覆盖关系集合断言，落 `call-protocol.test.js`（组 L/M）」 | 两条自动化断言**均未落地**（`grep '5.12\|curl -d\|示例'` 在两个新测试文件零命中；无 25/19 行数断言）。这两个验收面目前**仅靠人工核对**（我已独立核完：25/19/覆盖表均成立） | 下一迭代补 2 条文档面断言；或修正 §12.3 表述为「人工核对项」（不阻塞 F01/F02 验收——两卡验收标准为文档面，已全部满足） |
| **D-2** | `architecture.md` §11 的「位置（既有测试/产物）」列给出行号（如 `:36-49`、`:473`） | 行号是 **base 文件相对**值（实测逐条命中）；清单未写明该口径，易被读成「迭代分支当前行号」（后者会整体偏移 +1~+10） | §11 加一句口径说明 |
| **D-3** | `status.md` 阶段 6 备注：「PR 级验证报告 **8 份**已存在（含两轮复验）」 | `clarifications/` 下 8 份 `verify-*` 中 **1 份是阶段 4 Gate 报告**（`verify-stage4-gate-20260912-231000.md`），严格 PR 级 = **7 份**（pr-001/002/003/003-r2/004/005/005-r2） | 改述为「PR 级 7 份 + 阶段 4 Gate 1 份」 |
| **D-4** | `status.md` 阶段状态表：阶段 5「完成 ✅ / **已验证 ⬜**」 | 阶段 5 的 5 个 PR **全部**有 PR 级验证报告且 PASS，但该表未定义「已验证」是**阶段级**还是**含 PR 级**（阶段 4 该列为 ✅，其验证载体是阶段 Gate 报告） | 明确该列口径（若指阶段级 → 现状成立；若含 PR 级 → 应置 ✅） |
| **D-5** | 并发配置台账按「每次槛位释放」记录 `累计槛位释放次数` 与 `当前有效上限` | 缺「释放 = **1**」的独立记录（首波 pr-001 / pr-002 同批合并，`161ad36` 一次记为 2）。按公式 `min(3+3,5)=5`，**结果不受影响** | 台账补一句「释放 1 未单独记录」的口径说明 |
| **D-6** | `src/web.js:362` 注释「受理 / 进行中 / 终态三态共用同一 **11 键**与键序」 | 实体信封 = **10 键**（`call_id`/`agent`/`state`/`duration_ms`/`model`/`truncated`/`text`/`structured_output`/`error`/`exit_code`） | 已裁定不改（裁定⑥）；下一迭代候选 |
| **D-7** | `oamp/README.md:170`「…13 条」、`:174`「（13 条 API + 6 类事件）」 | 实现为 **19 条** API | 已裁定不改（裁定④）；下一迭代候选 |
| **D-8** | `oamp/test/api-routes.test.js:466`（base 460）与 `oamp/test/project-workspace.test.js:1299`（base 1289）的测试标题含「13 条投影」「投影 13 条（写接口 6 条）」 | 实际 19 条 / 写接口 7 条 | 已裁定不改（裁定③，维持「§11 逐字零改写」的机械判据）；下一迭代候选 |
| **D-9** | 终态信封字段数 | GET/阻塞响应 = **10 键**；SSE `call_result` 帧 = **11 键**（+`chat_id`，§4.1 已规定平铺） | 已裁定（裁定⑤）；两处均已文档化（§3.19 / §4.4），无需动作 |
| **D-10** | 「转录不持久 / 零新存储」 | web 侧存在**进程内** `callSchemas` Map（含 `call.terminal`）用于终态后按同一 schema 复算 `structured_output`；零持久化、零新表列、零新协议方法、零新依赖 | 已批准（裁定②）；已文档化（§7.3 第 ⑩ 条「数据库零新表零新列」） |
| **D-11** | F10 验收 3「重启服务后先前调用的转录不可读」 | 实测口径 = Router + web **整体重启**（卡片原文即「重启服务」） | 已裁定（裁定①）；与用例组 H 的实现一致 |
| **D-12** | F13 验收 3「某调用进行中时该面能反映其状态与增量」 | 无自动化断言（架构 §12 T-12 已声明为「人工/浏览器核对」）；本次验证我做了**页面可达性 + 页内要素 + 订阅路由实测**，**未做浏览器级动态核对** | 接受该承载方式；建议下一迭代为该验收留一次浏览器核对留痕 |
| **D-13** | 各任务图 `[model_inferred]` 验收口径 | 共 **21 条**（pr-001 2 / pr-003 10 / pr-004 5 / pr-005 4 / pr-002 0），经主 agent 逐批裁定生效（`history.md:231/312/424`） | 已批准（裁定⑦）；无需动作 |
| **D-14** | 委托书：「5 份 PR 文件 + 5 份 tasks + **6 份**澄清/Gate 报告」 | 实测：`prs/` = **10**（5 PR + 5 tasks）；`clarifications/` = **15**（7 阶段 1~2 澄清 + 8 验证/Gate 报告） | 属委托口径笔误，以实测为准（不影响产物齐备性判定） |

**偏差合计：14 条**（其中 6 条为「已裁定 / 已批准」类，1 条为委托口径笔误，7 条为待处理口径 / 落地缺口）。

---

## 4. 下一迭代候选

1. `oamp/README.md:170/174` 的「13 条」计数订正为 19 条（与 D-7 同源）。
2. §11 区间内的过时测试标题 / 注释字样订正（「13 条投影」/「投影 13 条（写接口 6 条）」/「末位新增 GET /api/docs」/「既有 11 条 + 末位两条项目面」）。
3. `src/web.js:362` 注释「11 键」→「10 键」（与 D-6 同源）。
4. 补两条文档面自动化断言：F01 的「§7.2 = 25 行 / §7.3 = 19 条 / 覆盖关系互覆盖」；F02 的「§5 调用面示例顶层字段 ⊆ `POST /api/calls` 的 `params[].name`」。
5. `status.md` 口径订正（PR 级报告计数、阶段 5「已验证」列定义）。
6. `architecture.md` §11 增加行号口径说明（base 相对）；未来可改为「语义锚点 + 行号」双列，避免行号漂移。
7. `transport.close(chatId)` 只关 `chat:<id>` 键、不关 `chat-calls:<id>` 键（当前 `web.js` 无任何调用方 ⇒ **无可观测影响**；如未来引入按对话关闭迁移订阅的调用方，需一并覆盖）。
8. 为 F13 验收 3（进行中增量在页面内的动态呈现）留一次浏览器级核对留痕。

---

## 5. 附录：三处 `git status --porcelain -uall` 与清理

1. **迭代会话工作区** `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol`：**空输出**（本报告写入前实测；写入本报告后为单行 ` M …/clarifications/verify-stage6-20260913-012611.md`，即本文件自身）。
2. **仓库主工作区** `/Users/chenchiyuan/projects/agents`：**空输出**（无跨工作区写入痕迹）。
3. **PR 子工作区**：`git worktree list` 仅 2 项（仓库主工作区 + 本会话工作区）⇒ 五个 PR 子工作区**均已删除、不存在**（与台账一致）。

**端口与清理**：本次验证使用端口段 **51329 / 52203 / 52510**（随机挑选，均在 51000~52999）；**未占用 7788**（7788 由 pid 31625 的既存 web 实例持有，早于本次验证）。验证结束后 router / agent / web 进程全部停止，临时 socket 目录、临时 `OAMP_DB` 目录与 `/tmp` 驱动脚本已清理；复测三端口零监听。

**写入边界**：本次唯一写入 = 本报告；未修改任何被验证文件；未执行任何 git 写命令。

---

## 6. 结论

**PASS**

- 13 条标准级条目**全部 pass**（fail 0 / partial 0 / blocked 0）；
- 16 张功能卡**全部 pass**（F14~F16 保证项同样 pass）；
- W1~W8 全覆盖、E1~E11 全覆盖、**无 demand 之外新增功能点**；反向漏项 0；
- 全量回归 **285/285（fail 0）**，本轮无 flake；既有面零破坏（改动面全部落在 §11 点名区间、7 个定点文件 diff 为空、依赖仍 `{}`）；
- 零半成品；合同文档计数与错误码集合与实现逐项一致；
- **并发调度三项证据全部成立**（worktree 时间窗口重叠有明确重叠区间、并发配置真实初始化并 4 次更新、爬升公式真实触发 `3→5`）；
- 搭置项文件不存在（三重核实）；既有文档结论改写面与架构声称一致（唯一 = tasks 一行）；
- 交付面**真起服务端到端跑通**（8 处实际响应片段），已清理且未占用 7788；
- 14 条偏差**全部已登记**（含 7 条待处理口径 / 落地缺口），**无一阻塞本迭代关闭**；下一迭代候选 8 条。
