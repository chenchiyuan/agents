# pr-005 修复后闭合复验报告（R2 / 合并前门禁）

- **验证者身份**：`verifier`（独立第三方）——反射身份：**测试有效性审查者（复核轮）**（产出物 = 修复后的验收测试文件，验的是「断言是否真驱动」）
- **产出物**
  - PR worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol/.pb-agents/worktrees/0018-pr-005-call-protocol-acceptance-tests`（下称 `<PW>`）
  - 分支：`feat/0018-pr-005-call-protocol-acceptance-tests`，tip `8aaaf1a`（链 `8e4fa86` 实现 → `8aaaf1a` 定向修复）
  - 被验文件：`oamp/test/call-protocol.test.js`（1093 行）+ `docs/iterations/0018-chat-agent-subagent-protocol/prs/pr-005-tasks.md`
- **验证标准来源**：主 agent 派发单（核心 = 上轮基线 `clarifications/verify-pr-005-20260913-005344.md` 的偏差 **D-1 / D-2** 是否闭合 + 无回归；条目 R1~R6，全部 pass 才 PASS）
- **验证日期**：2026-09-13
- **独立性声明**：未接收执行过程上下文——未读 dev 会话记录、未把 `prs/pr-005-tasks.md` 的「关键事实基线」或 dev 自跑结论当证据。本报告所有数字均由**我自己**的命令产出（git 只读对比、`node --test` 自跑、变异实验、源码静态核对、进程/监听核查）。**未修改任何被验证文件**；**未执行任何 git 写命令**；**未占用 7788**（该端口全程由主工作区既有 cluster 实例持有）。

---

## 逐项判定

### R1. 偏差 D-1 已闭合（结构化输出断言真实驱动且具鉴别力）—— **pass**（4/4 子项）

| 子项 | 判定 | 证据（我自跑 / 自读） |
|---|---|---|
| ① 自跑全绿、零 skip/todo | **pass** | `cd <PW> && node --test oamp/test/call-protocol.test.js` → `1..11 / # tests 11 / # pass 11 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0`，`duration_ms 24282.9`（wall 24.34s），退出码 0 |
| ② 无恒真形态、无必跳过分支 | **pass** | 当前 L581–L617 全文重写：旧的 `assert.ok(sEnv.structured_output === null \|\| typeof … === 'object')` 与 `if (sEnv.structured_output !== null) { … }` **均已删除**（见 `git show 8aaaf1a -- oamp/test/call-protocol.test.js`，+48/−17）。对全文件检索 `structured_output === null \|\|`、`typeof … === 'object'`、`.skip`、`.todo`、`TODO/FIXME` → **0 命中**；TAP 计数 skipped 0 / todo 0 |
| ③ 四态断言均取自运行期 HTTP 观测 | **pass** | 四态各自的期望值全部与**真实 HTTP 响应**比较，未被任何条件包裹：<br>④ 无 schema（L585–589）：`#json={"summary":"F06-JSON-摘要"}` 使桩回**可解析 JSON 原文**，仍断言 `nEnv.structured_output === null` + `nEnv.text === okJson`（负向基线：文本可解析也不许顺带解析）；<br>① permissive 通过（L591–597）：`deepEqual(pEnv.structured_output, { summary: 'F06-JSON-摘要' })` + `text` 保留原文 + `error === null`；<br>② strict 通过（L599–604）：同上（strict 不误伤合法结构）；<br>③ strict 不通过（L605–617）：`state === 'failed'`、`error === 'structured_output_invalid'`、`structured_output === null`、`text` 保留 `{"summary":123}`，且**按 id 信封**（`GET /api/calls/:call_id`）四项逐一同值。<br>数据来源：`callOnce` = `POST /api/calls`（block 终态信封）、`callGet/waitTerminal` = `GET /api/calls/:call_id`；测试侧不自行复算期望对象（期望值写成测试常量 `{summary:'F06-JSON-摘要'}`，非由响应派生） |
| ④ 鉴别力判定（变异实验） | **pass** | 见下 |

**④ 变异实验（已声明：临时副本，结束前删除）**

- 位置：`<PW>/oamp/test/.mut-r1/repo/`（**点号前缀**临时副本，含完整 repo 树——仅 `oamp/` 副本不自足：`oamp/src/role-binding.js:26` 会取 `<repo>/roles/<role>/<role>.md`，故首版副本出现 `agent 不可用: dev（无对应在线实例）`，改为整树副本后控制组通过）。实验方式：副本内改 `src/web.js`，跑 `node --test --test-name-pattern='组 C/D' test/call-protocol.test.js`（控制组 = 未变异副本 → `1..1 / pass 1 / fail 0`）。
- **仓库内文件零改动**：变异只发生在 `.mut-r1/` 副本；副本已 `rm -rf` 删除（`ls -d` → `No such file or directory`），实验后 `<PW>` 的 `git status --porcelain -uall` 为空。

| 变异 | 期望退化 | 实测结果（我自跑） |
|---|---|---|
| M1 `extractStructuredOutput(text) { return null; }` | ① 解析路径失效 | **FAIL** @ L595：`permissive 校验通过 ⇒ structured_output = 解析出的对象`，`+ null − { summary: 'F06-JSON-摘要' }`（`deepStrictEqual`） |
| M2 `validateAgainstSchema(value, schema) { return true; }` | 校验恒通过 | **FAIL** @ L609：`strict 校验不通过应 failed`，实得信封 `state:"completed"`、`structured_output:{"summary":123}`、`error:null` |
| M3 strict 覆写退化 `const invalid = false;` | strict 失败态不再产生 | **FAIL** @ L609（同断言）：实得 `state:"completed"`、`structured_output:null`、`error:null` |
| M4（加做）去掉「无 schema 不解析」守卫（`extracted`/`structured` 无条件取值） | ④ 负向基线失效 | **FAIL** @ L588：`不带 output_schema 时 structured_output = null（文本可解析也不例外）` |

**判据（口述版）**：四个态的期望值两两不同（`null` / 对象 / `'failed'`+`structured_output_invalid`），且全部由 `#json=` 指令驱动的**真实 HTTP 观测**产生——`extractStructuredOutput`、`validateAgainstSchema`、strict 覆写任一退化，都至少命中一条上述断言（M1~M4 已逐一实证）。D-1 的「恒真 + 必跳过」两个病灶均已消失。

### R2. 偏差 D-2 已闭合（sessOf 正则）—— **pass**（2/2 子项）

| 子项 | 判定 | 证据 |
|---|---|---|
| ① 同进程内不同会话可区分 | **pass** | 我的核实方式：读桩的 id 形态 `oamp/test/call-protocol.test.js:102` = `'sess-' + process.pid + '-' + sessionSeq`（**同进程内 pid 相同、seq 不同**）；再用 `node -e` 对同形态 id 跑新旧正则：<br>老 `/\[session\] (sess-\d+)/` → `["sess-12345","sess-12345"]` **判等（缺陷复现）**；<br>新 `/\[session\] (sess-\d+-\d+)/` → `["sess-12345-1","sess-12345-2"]` **可区分**；<br>对「同一 session（真泄漏）」两文本 → 新正则仍判等 ⇒ `assert.notEqual` 仍能抓住跨 chat 共用会话。即：**异值侧鉴别力恢复，同值侧告警能力保留** |
| ② 主判据（记忆文本三连断言）未删弱 | **pass** | 修复 diff 仅动 `sessOf` 一行 + 一句注释（`git show 8aaaf1a`），主判据原样保留：L1013–1014 `a2.text.includes('记忆：'+token) === true`、L1022 `b1… === false`、L1028–1031 `a3 true / b2 false`。实测：组 J 在 3 轮全量 + 单文件轮中均 `ok` |

### R3. 无回归（全量套件 + flake 纪律）—— **pass**（3/3 子项）

| 轮次 | 命令 | 计数 | 结果 |
|---|---|---|---|
| 全量轮 1 | `cd <PW>/oamp && npm test` | `# tests 283 / # pass 282 / # fail 1 / # skipped 0 / # todo 0`，60.5s | **exit 1**：`not ok 185 - F08：新结构不重建且数据保留；删库重启 = 新建空库可用`（`test/project-workspace.test.js:1166`），原因 `listen EADDRINUSE 127.0.0.1:41267` |
| 全量轮 2 | 同上 | `# tests 283 / # pass 283 / # fail 0 / # skipped 0 / # todo 0`，60.6s | **exit 0**，`^not ok` 命中 0 |
| 全量轮 3 | 同上 | `# tests 283 / # pass 282 / # fail 1 / # skipped 0 / # todo 0`，58.1s | **exit 1**：`not ok 174 - F03：缺参 / 空值 400 + 文案…`（同一文件 `project-workspace.test.js:662`），原因 `listen EADDRINUSE 127.0.0.1:42016` |
| 安静单跑 A/B | `node --test test/project-workspace.test.js` | **21 / 21 pass / fail 0**（12.8s；13.0s） | 两次单跑均全绿 ⇒ 上述两次失败**安静环境不可复现** |
| 观测探针 | 23 个 base 既有文件（排除 call-protocol）`node --test <files>` | `# tests 272 / # pass 272 / # fail 0`，60.5s | 不加载新文件时该轮无撞号（1 轮，非统计结论） |
| 点名 flake | `status.test.js F05-4`（`ok 210 - F05-4：只读无副作用…`） | 轮 1 / 2 / 3 全部 `ok`（另 `F05-4` 变体 #89/#223/#277 亦全 `ok`） | **本轮未触发**；仍按纪律给出单跑数据（上表） |

**flake 纪律结论**：轮 1 与轮 3 的失败**均非点名 flake F05-4**，而是 `project-workspace.test.js` 的 `EADDRINUSE`（41267 / 42016）；两次均落在**既有文件**的随机端口段——`project-workspace.test.js:121-123` 与 `web.test.js:117-119` 的 `pickPort()` 逐字相同（`41000 + Math.floor(Math.random() * 2000)`，两文件彼此不感知 ⇒ 并发时互相撞号），且两文件 **sha 与 base `7e351fa` 逐字节相同**（`36b89d81…` / `4b5d35ac…`），本 PR 的 diff 不含它们。失败**安静环境不可复现**（单跑 21/21 ×2）⇒ 判为**既有并发端口撞号 flake，非本 PR 回归**（登记为偏差 R2-D-1）。新文件自身端口段独占且去重（L164–172 `49500~49999` + `USED_PORTS`），**不可能**落到 41xxx，故非其自身缺陷；其对全量的影响仅是使 41xxx 取号文件的并发波次改变（24 文件的 suite）。

**本文件（新文件）在 3 轮全量的表现**：`ok 38/39/40/41/42` = 组 G/H/I/J/M 三轮全 `ok`，`not ok` 无一命中本文件。

### R4. 变更面不变 —— **pass**（3/3 子项）

| 子项 | 判定 | 证据 |
|---|---|---|
| `diff --name-status 7e351fa..HEAD` 恰两条 A | **pass** | `A docs/iterations/0018-chat-agent-subagent-protocol/prs/pr-005-tasks.md`、`A oamp/test/call-protocol.test.js`（`--stat`：2 files changed, 1434 insertions(+), 0 deletions） |
| 既有文件零改动 | **pass** | `status.test.js` sha `537925b0…`＝base 同值；`project-workspace.test.js` `36b89d81…`＝base；`web.test.js` `4b5d35ac…`＝base；`oamp/test/helpers/` 对 base diff 文件数 = **0** |
| `oamp/package.json` dependencies 仍 `{}` | **pass** | 实读 `"dependencies": {}`（且 package.json 不在 diff 内） |

### R5. 未新增共享 helper / 零新依赖 / 零外网 / 零仓库内持久化 —— **pass**（4/4 子项）

| 子项 | 判定 | 证据 |
|---|---|---|
| 未新增共享 helper | **pass** | import 面仅 9 个 `node:` 内建（test/assert/strict/child_process/crypto/fs/os/path/sqlite/url）＋既有 `./helpers/harness.js`、`./helpers/fake-node.js`；`startWeb/jget/jpost/jreq/openSse/setup/…` 均在**本文件内**局部定义（沿用既有体例），helpers 零改动 |
| 零新依赖 | **pass** | 见 R4；无 devDependencies 新增 |
| 零外网 | **pass** | 全文件仅两处 URL 字面量：`http://127.0.0.1:${port}`（自起 web 的 loopback，动态端口）与 `repo_url: 'https://example.com/oamp-call-protocol.git'`（**作为 JSON 载荷数据**写入项目 fixture，从不请求）。所有 `fetch` 目标均为 `web.base`（本机动态端口）；零真实 omp（`OAMP_OMP_BIN` 指向自建桩）、零真实 LLM、零 tmux |
| 零仓库内持久化 + 临时目录清理 | **pass** | 临时物全在 `os.tmpdir()`：`oamp-call-fake-*`（L153–161，`process.on('exit')` 删）、`oamp-call-db-*`（L305–311，`t.after` 删）；4 轮全量 + 变异实验后 `<PW>` 无残留（`oamp/` 仍只有 `bin/src/test/web/scripts/API.md/README.md/llms.txt/package.json/.gitignore`，无 `data/`、`.runtime/`）；`git status --porcelain -uall` 为空（见文末复核） |

### R6. 上轮 pass 项抽查（4 条，含组 G/H/I/J）—— **pass**（4/4）

| 抽查条目 | 判定 | 证据 |
|---|---|---|
| 组 G（F09，L796–850） | **pass** | 断言实物未改（本 PR 只动 L576–617 与 L1016）：批量两项 = 两行、行键集合与键序 = §3.4 六列、进行中 `ended_at===null`/终态 `ended_at ≥ started_at`、roster 与按 id 的 `state` 同值、行内零 `usage/tokens/cost/aborted/actions/cancel_url/steer_url`、参数元数据 `[]` 且带参请求与不带参逐行相同。实测 3 轮均 `ok 38` |
| 组 H（F10，L852–922） | **pass** | 转录末尾终态条目（`entries.at(-1).detail.event==='result'`）、键集合 §3.5、重启（新 Router+新 web，同 `OAMP_DB`）后同 id → `404` 且 `<500` / `code='NOT_FOUND'` / 无 `entries`、超上限 `truncated===true` 与信封同值、`messages` 表专表直读 4 行且零转录条目形态。实测 3 轮均 `ok 39` |
| 组 I（F11，L924–1000） | **pass** | 归属经 `messages[].meta.task_id` 核对、缺/空/非字符串 `chat_id` 一律 `400 INVALID_PARAM` 且 roster 行数与 `messages` 数不变、项目上下文三要素经派发载荷（非 prompt 回显）核对、`project` 与 `context` 并存不合并。实测 3 轮均 `ok 40` |
| 组 J（F12，L1002–1039） | **pass** | 主判据三连（L1013/1022/1028-1031）+ 修正后的 `sessOf` 辅助判据 + 无 `one_shot` 开关；另见 R2。实测 3 轮均 `ok 41` |

---

## 汇总

| 条目 | 子项数 | pass | fail | partial | blocked |
|---|---|---|---|---|---|
| R1 D-1 闭合 | 4 | 4 | 0 | 0 | 0 |
| R2 D-2 闭合 | 2 | 2 | 0 | 0 | 0 |
| R3 无回归（含 flake 纪律） | 3 | 3 | 0 | 0 | 0 |
| R4 变更面不变 | 3 | 3 | 0 | 0 | 0 |
| R5 helper/依赖/外网/持久化 | 4 | 4 | 0 | 0 | 0 |
| R6 上轮 pass 抽查 | 4 | 4 | 0 | 0 | 0 |
| **合计** | **20** | **20** | **0** | **0** | **0** |

**关键计数**：新文件单跑 = **11/11 pass**（skipped 0 / todo 0，24.3s）；`npm test` = **283 tests**，轮 1 **282 pass/1 fail(EADDRINUSE)**、轮 2 **283/283**、轮 3 **282/1(EADDRINUSE)**；变异实验控制组 1/1 pass，M1~M4 全部按预期 FAIL。

**端口段（我全程观测）**：本 PR 新文件独占 `49500–49999`（随机 + 进程内去重，L164–172）；pr-004 占 `49000–49499`；既有文件占 `41000/43000/45000/47000` 各 2000 段。本轮两次 flake 的端口 = **41267 / 42016**（既有 41xxx 段）。**7788 全程由主工作区既有 cluster 持有，我未占用**（`lsof -iTCP -sTCP:LISTEN` 仅显示既有 `oamp web --port 7788` PID 31625；我的 4 轮日志中 `7788` 命中 **0**）。现场核查：我的测试进程全部退出，41xxx/49xxx **无残留监听**。

**三处 `git status --porcelain -uall`（报告写入前）**：主工作区 `/Users/chenchiyuan/projects/agents` → **空**；会话工作区 `…/worktrees/0018-chat-agent-subagent-protocol` → **空**；PR worktree `<PW>` → **空**。

## 偏差记录

| ID | 类别 | 事实与证据 | 是否阻塞 | 建议 |
|---|---|---|---|---|
| R2-D-1 | 既有 flake（环境/端口分配） | 全量轮 1、轮 3 各 1 次 `EADDRINUSE`（41267 / 42016），失败点均在 `project-workspace.test.js`（F08 L1166 / F03 L662）；该文件与 `web.test.js` 的 `pickPort()` 逐字相同（`41000 + rand*2000`，互不感知），两文件 sha 与 base 逐字节相同；安静单跑 21/21 pass ×2 ⇒ **不可复现，非本 PR 回归**；本 PR 新文件端口段 49500–49999 与该段无交集，机制在 base 已存在 | **否**（不阻塞合并） | 下一迭代把既有测试端口分配集中化/去重（如复用 call-protocol 的 `USED_PORTS` 或由 runner 分配），或 `npm test` 降并发；已按 flake 纪律登记轮次与单跑结论 |
| R2-D-2 | 既有物（上轮 D-5 结转） | `oamp/src/web.js:363` 注释写「11 键」而实体 10 键（帧 `chat_id`+10）；本轮文件按实体键序断言（`ENVELOPE_KEYS` 10 键 / 帧 11 键），与架构 §3.1 一致 | **否**（不在本 PR 触碰面） | 由 pr-003 注释修正承接 |

登记偏差 **2** 条，均**不阻塞**；无新的断言强度/覆盖缺口。

## 下一迭代候选

1. **既有测试端口分配治理**（承 R2-D-1）：`pickPort()` 的随机段在 `project-workspace.test.js` / `web.test.js` 重复实现，是 suite 级偶发失败的唯一已定位来源。
2. **结构化输出再加一态**：可补 `permissive + 校验不通过 ⇒ structured_output=null 且 state 仍 completed（error 不写成 structured_output_invalid）`，把「permissive 与 strict 的分野」直接锁进断言（当前四态未覆盖 permissive-fail 分支）。
3. **`sessOf` 断言由「辅助」升格**：可对同一 chat 的连续三轮断言 `sessOf` 全等 + 跨 chat 全不等，进一步减少对记忆文本的单一依赖。

## 结论

**PASS** —— R1~R6 **20/20 sub-item pass，0 fail / 0 partial / 0 blocked**；偏差 D-1（恒真 + 必跳过）与 D-2（pid 段截断）经**自跑 + 变异实验**闭合（M1~M4 逐一实证断言可被证伪），主判据未删弱，变更面恰两条 A、既有文件零改动、零新依赖/外网/持久化。两条登记偏差均为**既有**非阻塞项（端口撞号 flake、既有注释差）。主 agent 可直接判定 **pr-005 可合并**。
---

## 附：三处 `git status --porcelain -uall`

**报告写入前**：主工作区 `/Users/chenchiyuan/projects/agents` → **空**；会话工作区 `…/worktrees/0018-chat-agent-subagent-protocol` → **空**；PR worktree `<PW>` → **空**。

**报告写入后复核**：主工作区 → **空**；会话工作区 → 仅 `?? docs/iterations/0018-chat-agent-subagent-protocol/clarifications/verify-pr-005-r2-20260913-011613.md`（= 本报告，授权写入）；PR worktree `<PW>` → **空**。即：复验过程对三处工作区**零污染**，唯一新增文件是本报告。
