# prd-round-2 — 0018-chat-agent-subagent-protocol（阶段 2 · 裁决落盘）

**caller**: `prd`
**迭代**: 0018-chat-agent-subagent-protocol
**日期**: 2026-09-12
**阶段**: 2（功能规格，第二轮修订完成）
**用途**: 记录用户对 **MI-01~MI-08** 的裁决原文与逐条落定、主 agent 对 4 项提请的裁定结论与落点、以及 C-3 范围裁定（0017 遗留缺陷 D-2 不纳入本迭代）
**配对文件**: 转呈清单 `clarifications/prd-round-1.md`（本轮提问）；`prd.md`（索引，v0.2.0 已收敛）；`prd/F01~F16*.md`（逐卡落地）
**状态**: **已收敛**——卡内与索引中 `model_inferred` 残留为零

---

## 1. 用户裁决（原文照录）

**裁决来源**：主 agent 以真实阻塞式提问取得，原文转达：

> **MI-01~MI-08：全部按推荐采纳**（用户原话：「全部按推荐」）。

**裁决方式**：一次成组裁决（8 项均按 `prd-round-1.md` 给出的推荐值采纳，**无一项被驳回、无一项被改写**）。

**C-3（范围项，用户裁决）**：**不纳入本迭代**——0017 遗留缺陷 D-2（既有路由登记元数据缺项目归属）**不在 0018 完成**；本迭代范围保持 W1~W8 不变，**不新增任何功能点、不新增 W 项**。

---

## 2. MI-01~MI-08 逐条落定（推荐 = 裁决 = 定案）

| 编号 | 议题 | 用户裁决 | 定案口径（= 推荐值） | 卡内落地位置 | 状态 |
|---|---|---|---|---|---|
| **MI-01** | 角色不可用的失败口径 | **按推荐（A）** | 三种情形（不存在 / 离线 / 无法按角色名寻址）统一为**一类 4xx 明确失败**、不静默 200、**不新增错误码族**；无 role 实例保持 `派生` 空值口径 | F03 验收 2（正文补「三种情形**不作区分**、不新增错误码族」） | 已确认 |
| **MI-02** | 批量提交的调用粒度 | **按推荐（A）** | **每项 = 一个独立调用**（N 项 ⇒ N 个调用 id、N 行 roster、N 个终态） | F04 验收 3 + 判定（两个互不相同的调用 id）；F05 验收 1 + 判定；F09 验收 1 + 判定（两项 ⇒ 两行） | 已确认 |
| **MI-03** | roster 作用域与 N17 的边界 | **按推荐（A）** | roster = **展示面**（无按状态过滤 / 无编排操作 / 非任务管理面）；「读自己发起的调用」按**范围限定**理解，**不引入鉴权可见性隔离** | F09 验收 1 / 5 与边界；F13 验收 2；F05 边界引用 | 已确认 |
| **MI-04** | 无归属调用的拒绝口径 | **按推荐（A）** | **未提供与提供空值一律拒绝**（同类 4xx，沿既有统一错误契约）；**不设**服务端兜底默认 chat | F11 验收 2 + 判定（两形态同判、同一结果） | 已确认 |
| **MI-05** | 阻塞模式的时限与断连口径 | **按推荐（A）** | **不设人为上限**（阻塞至终态）；客户端断连 / 放弃**不终止**调用，调用仍在后台完成 | F04 验收 4 + 判定（无超时降级、无「超时失败」；断连后终态仍可经订阅或按 id 取得）；F07 边界不重复断言 | 已确认 |
| **MI-06** | 终态「截断标记」的判定对象 | **按推荐（A）** | 标记 = 本次调用交付的**输出或过程记录**是否被既有上限截断——**任一触顶即为真**；数据来源留架构（T-02 / T-08） | F06 验收 5（正文改为「输出或过程记录」「两者都未触顶时为假」） | 已确认 |
| **MI-07** | roster 的起止时间口径 | **按推荐（A）** | 开始 = 调用**被受理**的时间；结束 = 进入**终态**的时间；进行中结束为空；结束 ≥ 开始 | F09 验收 2；F13 验收 2 | 已确认 |
| **MI-08** | 转录内容范围与超上限表现 | **按推荐（A）** | 转录覆盖**从发起到终态**的过程记录（**含终态那次事件**）；超上限**明确呈现「被截断」**——不报错、不静默少给 | F10 验收 1 / 4 | 已确认 |

**逐卡标记转换**：卡内原 `[model_inferred MI-xx]` 已全部转为 `[user_confirmed MI-xx]`（F03 / F04 / F05 / F06 / F09 / F10 / F11 / F13 共 8 张卡、12 处标记）；其余 8 张卡（F01 / F02 / F07 / F08 / F12 / F14 / F15 / F16）本就无 MI 标记，产品结论未改一字。

---

## 3. 主 agent 对 4 项提请的裁定与落点

| # | 提请 | 裁定（主 agent） | 落点 |
|---|---|---|---|
| ① | F14~F16 保证项的定位（保留为独立卡 vs 降级为全局边界） | **保留为独立保证项卡**（不降级；体例与 0017 F09 / F10 一致） | `prd.md`「疑问与越界」5 已标注裁定；F14 / F15 / F16 三卡原样保留 |
| ② | 三处 E 覆盖缺口的承载方式（转录 / I6·I7 / 控制台面） | **承载方式可接受**——**不新增裸判定**，按报告的方式定案 | `prd.md`「疑问与越界」3 已标注裁定；F10 卡内 E 覆盖说明已补「主 agent 已裁定该承载方式可接受」；F04 验收 4 / 5、F13 保持原承载 |
| ③ | roster 与 N17「不做全量任务列表」的边界口径 | **按最窄读法落卡，以 MI-03 = A 定案** | `prd.md`「疑问与越界」1、2 已标注裁定；F09 验收 1 / 5 与 F13 验收 2 |
| ④ | 两条 `派生` 口径的回退口 | **原样保留**（回退口继续留在 F01 边界与 F03 验收 4） | F01 边界（差异编号归类可重排）；F03 验收 4（无 role 实例空值形态可收紧） |

---

## 4. C-3 范围裁定：0017 遗留缺陷 D-2 不纳入本迭代

- **裁定内容**（主 agent 转达用户裁决）：0017 遗留缺陷 **D-2**（既有 `/api/chats`、`/api/messages` 路由登记元数据缺 `project_id`）**不在 0018 完成**；本迭代范围**保持 W1~W8 不变**，**不新增任何功能点、不新增 W 项**。
- **落点**：
  - `prd.md`「本次迭代边界说明 / 不包含」新增一条显式边界（→ F16 边界）；「疑问与越界」新增第 8 条。
  - **F16 边界**新增显式排除项：本卡只覆盖本次**新增**调用面的登记义务，既有路由的元数据不因本迭代被补齐或改动（F16 验收 1 的「元数据齐备」**只针对本次新增的路由 / 事件**）。
  - **F14**（既有面回归）不因 D-2 改写：既有面的「零破坏」判定不包含「修复既有缺项」，D-2 的既存状态既不算回归红、也不因本迭代被顺带修复。
- **影响面**：零——本裁定只是把范围钉死，未改动任何功能卡的验收标准与边界表述（除 F16 边界新增的排除句）。

---

## 5. 收敛自检（第二轮）

| 项 | 结论 | 依据 |
|---|---|---|
| 所有功能点有独立卡片 | ✅ | `prd/` 下 16 个 `.md`（F01~F13 + F14~F16），每张含功能 ID / 来源 / 用户价值 / 验收标准（含判定）/ 边界 / 架构待填 |
| 无 `demand.md` 外新增功能 | ✅ | W1~W8 全覆盖、零新增功能点；卡内引用仅 W1~W8 / E1~E11 / N1~N21 / T-01~T-17；C-3 裁定未引入新范围 |
| 架构待定项已标注 `[架构待填]` | ✅ | 16 张卡均含「架构待填」段；索引 T-01~T-12（demand §3 原文）+ T-13~T-17（本次清点）；F12 的「无待填」为诚实结论（N16 既有实现已符合） |
| **`model_inferred` 零残留** | ✅ | 16 张卡中 `model_inferred` 命中数均为 **0**；索引中无任何未确认项（`model_inferred` 仅作为「已归零 / 残留为零」的陈述出现） |
| 8 项 MI 全部落卡 | ✅ | 见 §2 表，逐条给出卡内落地位置与定案口径 |
| 主 agent 4 项裁定全部落盘 | ✅ | 见 §3 表 |
| C-3 范围裁定落盘 | ✅ | 见 §4 |

---

## 6. 未越界声明（第二轮）

- **本阶段（第二轮）唯一写入**：`prd.md`（版本 0.1.0 → **0.2.0**；状态头、MI 判定口径表、疑问与越界的裁定标注、边界说明新增 C-3 一条）、`prd/F03/F04/F05/F06/F09/F10/F11/F13/F16` 的标记与口径修订（其余 7 张卡未改）、本文件 `clarifications/prd-round-2.md`（新文件）。
- **未修改**：`demand.md`、`status.md`、`history.md`、`clarifications/prd-round-1.md` 与 `clarifications/` 下阶段 1 的既有文件（`round-1.md`、`round-2.md`、`round-3.md`、`demand-round-1-proposals.md`、`recon-20260912.md`）。
- **未触碰**：`oamp/**`、`.pb-agents/**`、`roles/**`、仓库其它 docs；**未写** `architecture.md` / `prs/`。
- **未执行**：git 命令、测试、构建。
- **未做架构决策**：8 项 MI 均为**产品维度判定口径**（失败形态、粒度、作用域、时间取点、呈现口径），不含技术选型、数据模型、接口设计、路由形态、字段命名与存储形态；架构维度仍一律 `[架构待填]`。
