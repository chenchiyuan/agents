# verify-pr-003-r2-20260913-002937

**验证者身份**：合并前代码评审者（复核轮 / pre-merge code reviewer, re-review）——验证对象 = 已修复并提交的 PR 代码变更 + 契约文档变更，故反射为「合并前代码评审者」：只接产出物（分支 tip `baf9c43` 的工作树）与验收标准，只读 + 自跑，逐条独立复核。

**产出物**
- PR worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol/.pb-agents/worktrees/0018-pr-003-call-http-surface-and-contract-docs`
- 分支 tip：`baf9c43`（链：`d1bc97d` 实现 → `f771ae5` 修复轮 → `baf9c43` 文档收尾）
- 修复轮 diff（我自读 git 取得）：`f771ae5` = `oamp/src/web.js` + `oamp/API.md`（28+/12-、10+/10-）；`baf9c43` = `oamp/API.md`（2+/1-）——**两轮均未触碰 `oamp/test/**`**。

**验证标准来源**：本轮派发单 R1~R7（含「上轮基线」`clarifications/verify-pr-003-20260913-000547.md` 的 2 条 fail 与 7 条已裁定读法）。

**验证日期**：2026-09-13

**方法**：只读产物 + 自跑。全部数字与引用由我本次命令产出：源码/文档静态核对、`node --test`（三个改动文件 + 全量 2 轮 + `status.test.js` 单跑）、自写端到端 smoke（真起 Router + agent(fake ACP) + `web start`，**OS 随机分配端口** + 临时 `OAMP_SOCKET` + 临时 `OAMP_DB`；fake ACP 由我自写，按 prompt 内标记 `<<JSON_OK>>` / `<<JSON_BAD>>` / `<<PLAIN>>` / `<<FAIL>>` / `<<HANG>>` 编排应答），共 **31 项断言 + 2 轮漂移窗口探针**。
**独立性声明**：未接收执行过程上下文——未读 dev 会话记录、未把 dev 自证或 `history.md` 结论当证据；修复说明里的每个数字（四处一致、exit_code 四态、272/272）都由我重新复现。

---

## 逐项判定

### R1. 上轮 fail A20（终态漂移）已闭合——自行复现性实证

| 项 | 判定 | 证据（我自跑） |
|---|---|---|
| R1a strict + 校验不通过 ⇒ 四处终态一致 | **pass** | 阻塞调用（`POST /api/calls` + `mode:"block"` + `schema_mode:"strict"` + `{type:object,properties:{ok:{type:boolean}},required:[ok]}`，应答体 `{"ok":"nope"}`）**同一 `call_id=task-e503b06b…`**：① 阻塞响应 `state=failed`；② `GET /api/calls/<id>` `state=failed`（键序逐字 10 键）；③ roster 行 `state=failed`（`ended_at=1789230350497 ≥ started_at=1789230350486`）；④ `/transcript` 外层 `state=failed`。四处 `state` 采样 `["failed","failed","failed","failed"]`；四处 `error` 均 `structured_output_invalid`；`structured_output` 均 `null`；`text` 均非空（`{"ok":"nope"}`，转录侧在同一条末条条目 `detail.text`） |
| R1b 反向对照：permissive 未通过 ⇒ 四处 `completed` | **pass** | 同 schema 同应答体、`schema_mode:"permissive"`：`{block:completed, byId:completed, roster:completed, transcript:completed}`，两处 `structured_output=null`（退回文本不报错） |
| R1c 反向对照：strict 通过 ⇒ 四处 `completed` 且 `structured_output` 为对象 | **pass** | 应答体 `{"ok":true,"n":3}`：四处均 `completed`，阻塞响应与按 id 的 `structured_output={"ok":true,"n":3}`、`error=null`（覆写不误伤） |
| R1d 附加：漂移窗口探针（后台模式高频采样） | **pass** | 后台 strict 未通过调用，12~16 次采样（roster 与信封各一次 HTTP）：**稳态定格 roster=failed / 信封=failed / 转录=failed / error=structured_output_invalid**；探针未观察到「信封 failed 而 roster completed」的采样对。唯一瞬态见偏差 1 |

### R2. 上轮 fail D2（`exit_code` 文档）已闭合

| 项 | 判定 | 证据 |
|---|---|---|
| R2a daemon 成功 ⇒ `0` | **pass** | `GET /api/calls/<id>`：`{"state":"completed",…,"exit_code":0}`（`<<PLAIN>>` 路径，text=`plain text answer`，error=null） |
| R2b daemon 执行失败 ⇒ `null`（且有 `error`） | **pass** | fake ACP 对 `session/prompt` 回 JSON-RPC error 触发执行侧失败：`{"state":"failed","error":"context_crashed","text":"ACP error: fake boom","exit_code":null}` |
| R2c shell 失败 ⇒ 真实退出码 | **pass** | 经既有 `/api/messages` 发 `!exit 3`：`{"state":"failed","exit_code":3,"error":null,"model":null,"text":null}`（**error 为 null**，与 §3.19 `error` 行新增表述一致） |
| R2d 一次性 ⇒ `0` | **pass** | `one_shot:true` 经既有面派发：`{"state":"completed","exit_code":0}` |
| R2e 四处示例核对 + 「不得再出现 completed + `exit_code:null`」 | **pass** | 我实测：daemon 成功 = 0 ⇒ §3.16（`API.md:759`，call_result 帧）、§5.13（`:1320`）、§5.14（`:1332`）、§5.16（`:1381`）四处示例均为 `completed` + `"exit_code":0`，与实测一致；§5.12（`:1298`/`:1302`）与 §3.14 示例（`:686`）为受理态 `submitted` + `null`，亦与实测（受理态 `exit_code=null`）一致。机械扫描：`API.md` 中 `"exit_code": null` 共 **3 处**，其 state 分别为（`:686` 属 §3.14 的 `"state":"submitted"` 块、`:1298`、`:1302`）**全部非终态**；**`state ∈ {completed, failed}` 且 `exit_code:null` = 0 处** |

### R3. 文档订正项 R3a~R3d 已落实且与实现一致

| 项 | 判定 | 证据 |
|---|---|---|
| R3a §3.19「10 键」（`call_result` 帧 11 键另注） | **pass** | `API.md:882`：“信封的键集合是**封闭的 10 键**（上表）；SSE 的 `call_result` 帧 = 该信封 + `chat_id`（**11 键**，见 §4.4）”。实测 `GET /api/calls/<id>` 键集合 = 10 键且键序逐字 `[call_id, agent, state, duration_ms, model, truncated, text, structured_output, error, exit_code]`；§4.4 事件表（`:954`）`call_result` 行逐名列出 **11 个字段**（含 `chat_id`）。`API.md` 中「11 键」仅此 1 处且语义为帧 |
| R3b §3.14「零副作用」限定到两类 + 离线路径 Δmsg=2 实测 | **pass** | `API.md:705`：零副作用限定为「`INVALID_PARAM` 与「角色不可寻址」两类」，并显式写明「『角色可解析但当前离线』**不在**这条表述的范围内」。我实测三类：① 缺 `chat_id` → `400/INVALID_PARAM`，**Δroster=0 Δmsg=0**；② `agent:'no-such-role'`（角色文件不存在）→ `404/NOT_FOUND`，**Δroster=0 Δmsg=0**；③ `agent:'planner'`（`roles/planner/planner.md` 存在、无在线实例）→ `404/NOT_FOUND` 同文案「agent 不可用: planner（无对应在线实例）」，**Δroster=0 Δmsg=2**（1 `in` + 1 `out{dispatch_failed}`）——与文档的限定完全对应，第 ③ 类确属既有行为 |
| R3c §3.19 `error` 行覆盖 shell/一次性失败 | **pass** | `API.md:877`：“`failed` 且执行侧 / 校验侧给出机器可读原因时给出…**shell / 一次性失败以 `exit_code` 表达，此时 `error` 为 `null`**”；实测 shell 失败 `exit_code=3` + `error=null`（R2c），daemon 失败 `exit_code=null` + `error="context_crashed"`（R2b）——两种表达均被覆盖 |
| R3d §3.18 末条终态条目 `state` 与 `detail` 刻意区分 | **pass** | `API.md:828`：外层 `state` 含 strict 覆写、「与 §3.19 信封同真源」，`detail` 为执行侧原样终态体。实测同一条转录末条条目：`{"state":"failed","detail":{"event":"result","state":"completed","executor":"omp-daemon","text":"{\"ok\":\"nope\"}",…}}`——外层 `failed` / `detail.state` `completed`，与文档逐字对应 |

### R4. 无回归（含 flake 纪律）

| 项 | 判定 | 证据 |
|---|---|---|
| R4 全量 2 轮 + flake 单独重跑结论 | **pass** | ① 第 1 轮 `cd <PW>/oamp && node --test test/*.test.js` → `# tests 272 / # pass 272 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0`（EXIT=0，56.88s）；② 第 2 轮同命令 → **272/272 / fail 0**（EXIT=0，56.96s）。两轮 `^not ok` 命中数 = **0**，**`F05-4` 未出现失败**（两轮均 `ok`）。按简报「回归判定纪律」，我仍按要求给出安静环境单跑数据：`node --test test/status.test.js` → `# tests 5 / # pass 5 / # fail 0`，`ok 3 - F05-4：只读无副作用——连续两次 status 仅 last_heartbeat 自然推进、注册表不变`。**判定：本轮不存在该 flake，也就无从触发「安静环境复现」分支；除该用例外的 0 条失败 ⇒ 我未发现任何真回归**。（附诚实登记：我最初一次全量调用写错 cwd（落在 worktree 根，glob 未命中）→ TAP 输出 `# tests 0`，该次**判定为无效调用并弃用**，上列两轮为有效轮次。） |

### R5. 变更面闭合不变

| 项 | 判定 | 证据 |
|---|---|---|
| R5a `95051b0..HEAD` 文件集合闭包 | **pass** | `git diff --name-only 95051b0..HEAD` → 恰 7 条：`oamp/API.md`、`oamp/llms.txt`、`oamp/src/web.js`、`oamp/test/api-routes.test.js`、`oamp/test/project-workspace.test.js`、`oamp/test/web.test.js`、`docs/.../prs/pr-003-tasks.md` —— ⊆ PR 6 文件 + `prs/pr-003-tasks.md` |
| R5b `oamp/test/**` hunk 全落 §11 第 1~10 条 | **pass** | `git diff -U0 95051b0..HEAD -- oamp/test/` 的 hunk 头（**改动前行号**）逐条映射：api-routes `-49,0`→§11-1（36-49）、`-473`→§11-2、`-483`→§11-3、`-499`→§11-4（495-502）；project-workspace `-1219,0`→§11-5（1206-1221）、`-1230,2`→§11-6+§11-7（1230/1231）、`-1265`→§11-8、`-1301`→§11-9；web `-1518`→§11-10（1517-1518）。共 9 个 hunk，**区间外 0 个**。`f771ae5`/`baf9c43` 的 `--stat` 显示只改 `API.md`/`src/web.js` ⇒ **修复轮未新增任何测试改动、也未新增区间外改动** |
| R5c §11 第 12 行 7 个文件 diff 全空 | **pass** | `git diff --numstat 95051b0..HEAD -- oamp/test/<f>` 对 `task` / `transport` / `api-pages` / `hygiene` / `context-pool` / `acp-daemon` / `delivery-contract` 逐一 → **7 个文件均 0 行** |
| R5d `package.json` dependencies 仍 `{}` | **pass** | `git show HEAD:oamp/package.json \| jq -c .dependencies` → `{}` |
| R5e llms 重生成无新 diff | **pass** | `node oamp/scripts/gen-llms-txt.mjs` → 「接口 19 条，2569 字节」；随后 `git diff --name-only -- oamp/llms.txt` → **空**（逐字节同步） |

### R6. 修复未引入新漂移

| 项 | 判定 | 证据 |
|---|---|---|
| R6a `callState` 未影响既有 `POST /api/messages` 路径 | **pass** | 真实实测：`POST /api/messages`（`<<PLAIN>>` → daemon 成功）返回 `task_id`；`GET /api/calls/<id>` = **10 键且键序逐字** `[call_id,agent,state,duration_ms,model,truncated,text,structured_output,error,exit_code]`，`state=completed` / `structured_output=null` / `error=null` / `exit_code=0` / `text="plain text answer"`；roster 同行 `state=completed`。机理核对：`callSchemas` 仅登记带 `output_schema` 的调用（`web.js:1077`），`/api/messages` 不登记 ⇒ `callState` 走 `task.state` 回退（`web.js:388-390`），既有面无覆盖 |
| R6b roster `ended_at` 判据随新 state 后语义正确（终态 / 进行中各一例） | **pass** | 终态（strict 未通过、新 state=failed）：`{state:"failed",ended_at:1789230350497,started_at:1789230350486}` ⇒ `ended_at ≠ null` 且 `≥ started_at`；进行中（`<<HANG>>` 悬挂轮）：`{state:"working",ended_at:null,model:null}`，其转录 `state=working` 且 **不追加** 终态条目（过程条目数=1） |

### R7. 其余上轮 pass 项抽查（PR 15 条取 5 + B/C 各 1）

| 项 | 判定 | 证据 |
|---|---|---|
| R7a PR A1 六条路由 + 顺序 | **pass** | 实跑 `createApiRoutes({})`（19 条）：末位 6 条 = `POST /api/calls`、`GET /api/calls`、`GET /api/calls/stream`、`GET /api/calls/:call_id/stream`、`GET /api/calls/:call_id/transcript`、`GET /api/calls/:call_id`；可达性实测 `GET /api/calls/stream` → **200**（未被 `:call_id` 吞成 404） |
| R7b PR A2 元数据 8 字段 / params 5 字段 / kind / SSE 三类事件 | **pass** | 全 19 条实跑校验 `violations=[]`；调用面两条 SSE 的 `response` 均逐名列出 `call_state / call_update / call_result` |
| R7c PR A4 单一构造点 + 单一发布点 + 禁词 | **pass** | `composeCallEnvelope` 定义 **1 处**；`publishCallResult(` 调用点 **2 处**（`web.js:1421` 投递路径、`:1455` 对账路径）+ 定义 1 处（`:1380`）；`grep -nE 'usage\|tokens\|cost\|aborted' oamp/src/web.js` → **零命中**；`finishTask` 的「写 out → 推 `message` → 推 `chat_state`」三行在本区间 diff 中**零增删**（`git diff -U0 … \| grep '^[+-].*insertOutput({ chatId: entry\|publishMessage(entry.chatId\|publishState(entry.chatId)'` 无输出） |
| R7d PR A7 `STATIC_FILES` 两项 + `role` 追加 | **pass** | 源码注册 `/calls → web/calls.html`、`/calls.js → web/calls.js`；`GET /api/agents` → 节点键序 `[instance_id, session_id, state, last_heartbeat, role]`（前 4 键 = 既有 4 字段顺序，`role` 追加末位；`?state=online` 同键序），`pb-dev.role='dev'`。HTTP 侧 `/calls` 与 `/calls.js` 均 404 且 content-type `text/plain`、body `not found` ⇒ **白名单分支被走到**（区别于路由层 JSON 404），页面本体属 pr-004（源码注释声明）——见偏差 2 |
| R7e PR A11 llms 重生成 | **pass** | `oamp/llms.txt:11` = `## 接口（19 条）`；重生成输出「接口 19 条，2569 字节」且 `git diff` 空 |
| R7f B 组：三个改动测试文件全绿 | **pass** | `node --test test/api-routes.test.js test/project-workspace.test.js test/web.test.js` → `# tests 67 / # pass 67 / # fail 0 / canceled 0 / skipped 0 / todo 0` |
| R7g C 组：零新依赖 | **pass** | 同 R5d（`{}`）；全量 272 用例含 `hygiene.test.js`（零依赖锁）全绿 |

---

## 汇总

- **pass: 28 / fail: 0 / partial: 0 / blocked: 0**（28 项：R1 4 项、R2 5 项、R3 4 项、R4 1 项、R5 5 项、R6 2 项、R7 7 项）
- 自跑规模：端到端 smoke **31/31 断言 pass**（真起 Router + agent(fake ACP) + web，随机端口，未占用 7788）；漂移窗口探针 2 轮；全量 2 轮 272/272；`status.test.js` 单跑 5/5；三个改动文件 67/67。

---

## 偏差记录

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| 1 | F09 验收 3 / §3.15 / §3.19：「`state` … 两处不会漂移」（终态单一真源） | **稳态已完全一致**（四处 `state`/`error` 同值，见 R1a）。但存在**瞬时窗口**：`call.terminal` 由 `publishCallResult` 在 web 侧写入（`web.js:1387`），而 Router 的任务表在更早就已终态；两者之间的一个 UDS 往返内，若恰好读 roster/转录，会读到回退值 `completed`，随即收敛为 `failed`。我的探针（信封先采样、roster 后采样）观测到一枚 `[roster=completed, 信封=working]` 采样对（下一迭代即 `[failed,failed]`）；**未观测到**「信封=failed 且 roster=completed」的采样对 | **接受 + 登记**（上轮 fail 是**持久**漂移，本轮已消除；残留为有界瞬态，不阻塞合并）。下一迭代候选：让 `callState` 复用 `composeCallEnvelope(task, call).state` 派生（零写入、零滞后），或把 `call.terminal` 的写入提前到与任务表终态同一发布点 |
| 2 | PR 验收 A7「`STATIC_FILES` 白名单新增 `/calls`、`/calls.js` 两项」 | 白名单**已注册且分支可达**，但 `GET /calls` 与 `GET /calls.js` 实测 **404 `not found`**（`web/calls.html`、`web/calls.js` 不在本 PR 文件范围内，源码注释明写「页面本体属 pr-004；本 PR 内 serveStatic 走 ENOENT → 404」） | **接受 + 登记**（A7 验收要求的是「白名单新增两项」，已满足；404 是跨 PR 边界的刻意中间态）。下游若把 `/calls` 当可用入口会踩空 —— 下一迭代（pr-004）随页面本体消除 |
| 3 | §11 第 10 条 / `web.test.js:1518` 期望文本写作 `['instance_id','last_heartbeat','session_id','state']` | 运行期节点键序为 `[instance_id, session_id, state, last_heartbeat]`（`role` 追加末位）；既有断言用 `.sort()` 比较（旧新两版都是 `.sort()`），故**不构成漂移**；键序真源在 `router.js`（本 PR 零 diff） | **接受 + 登记**（顺序在改动前即如此，§11 文本只是列举集合、非键序断言）。零动作；如追求逐字，可在 §11 文本注明「集合，非键序」 |
| 4 | §3.19 `exit_code` 行新增类型 `number \| string \| null` 与「被信号终止时为信号名 / `killed`」 | 我实测覆盖了 **`0`（daemon 成功/一次性）**、**`3`（shell 非零）**、**`null`（daemon 失败）** 三态；**未覆盖**「被信号终止」（`agent.js` 的 `code === null ? (signal \|\| 'killed') : code` 分支） | **登记为未覆盖边界**（非 fail：类型扩写与实测三态一致，`string` 分支源自既有代码 `agent.js` 的 shell/一次性共用终态构造）。下一迭代候选：加一条 `kill -TERM` 用例覆盖信号名形态 |

---

## 下一迭代候选

1. **消除瞬态窗口**（偏差 1）：`callState` 改为 `call === null ? task.state : composeCallEnvelope(task, call).state` 式的纯派生，取消 `call.terminal` 这一写入型中间态 —— 零写入即零滞后，且与 §3.19 信封天然同源。
2. **补 `exit_code` 的信号形态覆盖**（偏差 4）：shell 路径发 `kill -TERM $$` 断言 `exit_code='SIGTERM'`（或 `killed`），把文档新增的 `string` 分支纳入证据。
3. **pr-004 边界收口**（偏差 2）：`web/calls.html` / `web/calls.js` 落地后，把 `/calls`、`/calls.js` 的 200 断言补进 `api-pages.test.js` 的既有占位项邻位。
4. **上一轮遗留的文档级候选（仍未处理，均不阻塞）**：`oamp/README.md` 的「13 条」计数（上轮裁定 ④）、`§7.2` 补英文原词锚点（裁定 ⑥）、`prs/pr-003-tasks.md` 的「重启 web 子进程」措辞（裁定 ①）、in/out 双向关联（偏差 8）——本轮复核确认这些面**零改动**，与裁定一致。

---

## 结论

**PASS**

- 28 项判定 **全部 pass**（0 fail / 0 partial / 0 blocked），4 条偏差均为**有界瞬态或跨 PR 边界**，无阻塞项。
- 上轮 2 条 fail **均已闭合且由我独立复现**：
  - **A20 终态漂移**：strict 未通过 ⇒ 阻塞响应 / 按 id / roster / 转录**四处一致 failed**（`error=structured_output_invalid`、`structured_output=null`、`text` 非空），反向对照（permissive 未通过、strict 通过）四处均 `completed` —— R1a~R1c。
  - **D2 `exit_code` 文档**：成功 `0` / daemon 失败 `null`（有 `error`）/ shell 失败 `3`（`error=null`）/ 一次性 `0` 四态实测与 §3.19 字段表 + §3.16/§5.13/§5.14/§5.16 四处示例逐条吻合，`completed + exit_code:null` 残留 **0 处** —— R2a~R2e。
- 无回归：全量 2 轮 **272/272**（`F05-4` 未失败，安静环境单跑亦 5/5，判定无回归）；变更面闭合不变（7 路径、测试 hunk 全落 §11 1~10、§11-12 七文件零 diff、`dependencies` 仍 `{}`、llms 重生成零 diff）。

---

## 环境自证

- **进程**：收尾复查 `pgrep -fl '0018-pr-003'` → **none**（smoke 与探针的 Router/agent/web 均已随 `finally` 退出）。默认端口 **7788 未被本次验证占用**：监听者是**既有**开发服务 `node …/projects/agents/oamp/bin/oamp.js web start --port 7788`（pid 31625，启动于 Sat Sep 12 14:36:40，从**主工作区**启动，非我创建、我未触碰）；本次 smoke 的 web 端口全部由 OS 随机分配（`listen(0)` 取空闲端口后立即释放）。
- **临时目录/脚手架**：`/tmp/vr2`（我的 smoke 与探针脚本、fake ACP）**已删除**（`ls /tmp/vr2` → No such file or directory）；临时 socket 与 `OAMP_DB` 目录由脚本自身 `finally` + harness 清理。
- **不写文件**：PR worktree、仓库主工作区、会话工作区**除本报告外零写入**（`llms.txt` 重生成经 `git diff` 证实逐字节无变化，非写入）；未执行任何 git 写命令。
- **三处 `git status --porcelain -uall`**（收尾快照）：
  - PR worktree（`…/0018-pr-003-call-http-surface-and-contract-docs`）：**空**
  - 仓库主工作区（`/Users/chenchiyuan/projects/agents`）：**空**
  - 会话工作区（`…/0018-chat-agent-subagent-protocol`）：仅新增本报告 `docs/iterations/0018-chat-agent-subagent-protocol/clarifications/verify-pr-003-r2-20260913-002937.md`
