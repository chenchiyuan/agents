# verify-pr-001 — PR 级独立验收报告（合并前门禁）

**验证者身份**：合并前代码评审者（pre-merge independent code reviewer）——对「已实现并提交的 PR 代码变更」反射出的角色；只读产物 + 自跑验证，不接收 dev 执行过程上下文。

**产出物**：
- PR worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol/.pb-agents/worktrees/0018-pr-001-transport-call-key-namespace`（分支 `feat/0018-pr-001-transport-call-key-namespace`）
- 实现提交：`cb1bdc8`（`transport: 键空间加命名空间前缀 + 按调用/按对话的调用两对方法`）；其上一条 `48b056e`（tasks 文件提交）；分支相对基线 `48b056e` 无其它提交。
- 改动文件（该提交全量）：`oamp/src/transport.js`（+68 / −19）

**验证标准来源**：主 agent 委托指定的 A~F 六组判据（PR 文件「验收标准」6 条 + `prs/pr-001-tasks.md` 内 T1/T2/T3 验收标准 + 改动面/测试零改写/无回归/自陈一致性）。

**验证日期**：2026-09-12

**验证方式**：只读代码 + 自跑命令。除 `node --test` 外，另以**真实 `node:http` SSE 客户端**（脚本经 stdin 管道执行，未落盘任何文件）对 33 条行为判据 + 8 条 `null` 键/`close` 边界判据做端到端核对。

---

## A. PR 文件「验收标准」6 条

### A1. `createSseTransport` 返回对象新增四个方法 —— **pass**
证据（`grep -n 'handleCallStream\|publishCall\|handleChatCallStream\|publishChatCall' oamp/src/transport.js`）：
```
75:  function handleCallStream(req, res, { callId }) {
80:  function handleChatCallStream(req, res, { chatId }) {
103:  function publishCall(callId, event) {
109:  function publishChatCall(chatId, event) {
140:    handleCallStream, publishCall, handleChatCallStream, publishChatCall,
```
4 处定义 + 末尾 return 对象第 140 行同时含 4 个键。运行时键集核对：`Object.keys(t).sort()` = `["close","closeAll","globalCount","handle","handleCallStream","handleChatCallStream","kind","publish","publishCall","publishChatCall","publishGlobal"]`（恰 11 键，无多余键）。

### A2. 三前缀互不为前缀 + 全局键仍 `null` —— **pass**
- 字面核对：`chat:` / `call:` / `chat-calls:` 两两之间无前缀关系（`chat:` 之后是 `-` 而非 `:`；`chat:` 长度更短但字符不同位 ≠ 前缀）。
- 运行时核对：`publishCall`、`publishChatCall`、`publish`、`publishGlobal` 四路互不串扰（见 A3/B2.3/B2.4/B3.5）。
- 全局键：`globalCount()` 在 8 个订阅（7 个字符串键 + 1 个 `null` 键）下返回 `1`；`handle({chatId:null})` 收 `publishGlobal` 而收不到 `publish('CH_A',…)`；`publish(null,ev)` 与 `publishGlobal(ev)` 均到 `null` 键。

### A3. `publishCall` 一次发布双键写入 —— **pass**
真实 `node:http` SSE 客户端证据（一帧 `event: call_result\ndata: {"chat_id":"CH","call_id":"C1","ok":true}`）：
- `publishCall('C1', {type:'call_result', data:{chat_id:'CH', call_id:'C1', ok:true}})` ⇒ `call:C1` 订阅者收到帧：`["event: call_result\ndata: {\"chat_id\":\"CH\",\"call_id\":\"C1\",\"ok\":true}"]`
- 同一调用 ⇒ `chat-calls:CH` 订阅者收到**逐字节相同**帧（两帧字符串相等判定 `true`）。
- 一次调用两个作用域各收一份，调用方仅发布一次（`publishCall` 函数体内两次 `publishTo`，无其它发布路径）。

### A4. 既有对外面逐字不变 —— **pass**
- 返回对象既有成员 `kind:'sse'` 与 `handle`/`publish`/`publishGlobal`/`globalCount`/`close`/`closeAll` 名称、签名、可调用性均不变（`kind` = `sse`；10 个非 `kind` 成员全为 function）。
- SSE 头：`{status:200, content-type:"text/event-stream; charset=utf-8", cache-control:"no-store", connection:"keep-alive"}`。
- 首帧：`await open(...)` 返回的 chunk 以 `"retry: 1000\n\n"` 起头（`RETRY_MS = 1000`，源码 `transport.js:57`）。
- keepalive：注入 `heartbeatMs: 30` 后流内出现 `: keepalive` 注释行；`closeAll()` 后长度稳定不再增长（心跳停止）。默认周期 `DEFAULT_HEARTBEAT_MS = 15000`（源码 `transport.js:11,19`）。
- 既有语义：`publish(chatId)` 仅到该 chat；`publishGlobal` 仅到全局；`close(chatId)` 仅结束该 chat；`close(null)` 仅结束全局且不影响 chat；`publish` 在 `close` 之后无写入（帧数 0）；订阅建立与断开清理沿用同一 `subscribe` 内核（三个键空间共用）。

### A5. `node --test oamp/test/transport.test.js` 全绿 **且** 该文件 `git diff` 为空 —— **pass**
- `cd <PW> && node --test oamp/test/transport.test.js` → `# tests 13 / # pass 13 / # fail 0 / # cancelled 0 / # skipped 0`（duration_ms 984.75）。
- `git -C <PW> diff --name-only` → 输出为空（工作树干净，`git status --porcelain -uall` 亦为空）。

### A6. `oamp/package.json` 的 `dependencies` 仍为空对象 —— **pass**
```
"dependencies": {}
```
`git diff cb1bdc8^ cb1bdc8` 文件集仅 `oamp/src/transport.js` ⇒ package.json 未被触碰。

---

## B. `prs/pr-001-tasks.md` 内 T1 / T2 / T3 验收标准

### T1（键空间前缀化，既有对外面不变）
| # | 判据 | 判定 | 证据 |
|---|---|---|---|
| B1.1 | 三个具名键函数 + 对应前缀 | pass | `transport.js:21-23`：`chatKey = (chatId) => \`chat:${chatId}\``；`callKey = (callId) => \`call:${callId}\``；`chatCallsKey = (chatId) => \`chat-calls:${chatId}\`` |
| B1.2 | 三前缀两两互不为前缀 | pass | 字符串核对无前缀关系；行为侧四路键互不串扰 |
| B1.3 | 全局键仍 `null` | pass | `globalCount()`=1/8 订阅；`handle({chatId:null})` 收 `publishGlobal` 不收 `publish('CH_A')`；`close(null)` 仅结束全局 |
| B1.4 | `handle`/`publish`/`close` 改用 `chatKey` 后对外行为不变 | pass | 同 chat 订阅者收 `publish` 帧；`close(chatId)` 只结束该 chat（其它 chat `ended=false`）；之后 `publish` 无写入（帧数 0） |
| B1.5 | 返回对象既有成员 + `globalCount` 只计 `null` 键 | pass | 键集恰 11；7 个 chat/call/chat-calls 订阅不计入，`globalCount()`=1 |
| B1.6 | 首帧 `retry: 1000` / 默认 15000 ms / `closeAll` 停心跳 | pass | 首帧逐字节 `retry: 1000\n\n`；源码常量 15000；`closeAll()` 后 chunk 长度冻结（151 字节不变） |
| B1.7 | 回归全绿 + 零新 import | pass | 13/13 pass；`grep -n '^import\|require(' oamp/src/transport.js` → 无匹配（`NO IMPORTS`） |

### T2（按调用 `handleCallStream` / `publishCall`）
| # | 判据 | 判定 | 证据 |
|---|---|---|---|
| B2.1 | 复用既有 `handle*` 内核：SSE 头 / 首帧 / 断开清理 / keepalive | pass | 头 `text/event-stream; charset=utf-8`；首帧 `retry: 1000`；`closeAll()` 后 `ended=true`；keepalive 行出现 |
| B2.2 | 一次发布同写两键；内容相同；无订阅者不抛错、不缓存、不补发 | pass | 两键帧逐字节相等；`publishCall('NOPE', …)` 正常返回无异常；后续未观察到补发 |
| B2.3 | 按调用键隔离（`C2` 收不到 `C1`；`call:` 不收 chat/全局帧） | pass | `call2` 无帧；`call1` 无 `task.update`、无 `presence` |
| B2.4 | 结构性防串键：`chatId='call:X'` 订阅者收不到 `publishCall('X', …)` | pass | `spoofChat`（`/api/stream?chat_id=call:X`）对 `publishCall('X', {data:{chat_id:'CH',call_id:'X'}})` 收到帧数 = 0 |
| B2.5 | 存在性 `grep` 可见两处定义 + 返回对象两键 | pass | 定义 `transport.js:75,103`；返回键 `:140` |
| B2.6 | 回归全绿 + 零改写 + 零 import | pass | 13/13；`git diff -- oamp/test/` 空；无 import |
| B2.7 | `[model_inferred]` `chatId` 取自 `event.data.chat_id` | pass | `publishCall('C1', {data:{chat_id:'CH',…}})` ⇒ `chat-calls:CH` 收到该帧 |

### T3（对话作用域 `handleChatCallStream` / `publishChatCall`）
| # | 判据 | 判定 | 证据 |
|---|---|---|---|
| B3.1 | 在 `chat-calls:<chatId>` 上建订阅并复用内核 | pass | 头 / 首帧 `retry: 1000` / 断开移除 / keepalive 均与 chat 路径一致 |
| B3.2 | `publishChatCall` 只写 `chat-calls:` | pass | `CH2` 收到、`CH` 未收到；`chat:` 与 `null` 键均未收到 |
| B3.3 | 「先订阅、再发起」成立 | pass | 先建 `chat-calls:CH_NOREPLAY` 订阅，再 `publishChatCall` ⇒ 恰收 1 帧 |
| B3.4 | 不补发（无缓存、无排队） | pass | 订阅建立前的 `C0` 帧未被后建订阅者收到（C0 帧数 0） |
| B3.5 | 作用域互不串扰（双向） | pass | `chat-calls:` 订阅者不收 `publish`/`publishGlobal`；`chat:`/`null` 订阅者不收 `publishChatCall`/`publishCall` |
| B3.6 | `closeAll()` 覆盖 `call:` / `chat-calls:`，之后四路发布均无写入 | pass | 四类订阅 `ended=true`；publish 前后长度 `[185,185] / [172,172] / [151,151] / [148,148]` |
| B3.7 | 存在性 `grep` 可见两处定义 + 返回对象两键 | pass | 定义 `transport.js:80,109`；返回键 `:140` |
| B3.8 | 回归全绿 + 零改写 + 零 import | pass | 13/13；test 目录 diff 空；无 import |

---

## C. 改动面零越界 —— **pass**
`git -C <PW> diff --stat cb1bdc8^ cb1bdc8`：
```
 oamp/src/transport.js | 87 ++++++++++++++++++++++++++++++++++++++++++++++++-----------
 1 file changed, 68 insertions(+), 19 deletions(-)
```
文件集 = `{oamp/src/transport.js}`，⊆ PR 文件「文件范围」（仅 `oamp/src/transport.js`）。零新增依赖（`package.json` 未触碰）、零新增配置项（无配置文件改动）。

## D. 既有测试零改写 —— **pass**
- `git -C <PW> diff --name-only 48b056e HEAD -- oamp/test/` → 空
- `git -C <PW> diff 48b056e..HEAD -- oamp/test/` → 空
- `oamp/test/transport.test.js` 行数 = 429（与基线一致）

## E. 无回归 —— **pass**
同 worktree 下两个受 transport 影响的既有测试文件均全绿：
- `node --test oamp/test/web.test.js` → `# tests 39 / # pass 39 / # fail 0`
- `node --test oamp/test/delivery-contract.test.js` → `# tests 12 / # pass 12 / # fail 0`

## F. 实现自陈与代码一致性 —— **pass**（差异登记见「偏差记录」）
源码中被重构抽出、且未被 `architecture.md` §4.2 命名的内部符号共 4 个：`chatKeyOrGlobal`、`subscribe`、`publishTo`、`closeKey`。逐一核实**不改变对外行为**：
- `subscribe`（`transport.js:42`）＝ 原 `handle` 函数体原样搬迁（头 / 注册 / 首帧 / `close` 清理 / 心跳全部逐字保留），三个键空间共用；`handle` 变为一行委托（`:71-73`）。
- `publishTo`（`:87`）＝ 原 `publish` 函数体原样搬迁（帧格式 `event: …\ndata: …\n\n`、无订阅者直接 return），`publish` 变为一行委托。
- `closeKey`（`:120`）＝ 原 `close` 函数体原样搬迁；`close` 变为一行委托。前缀化后 `closeAll` 必须改走 `closeKey`（否则会把已前缀键再次前缀化），属必需内部调整。
- `chatKeyOrGlobal`（`:25`）＝ `chatId === null ? null : chatKey(chatId)`，等价于既有「`null` 即全局键」约定。
行为等价由 33 + 8 条运行时判据与 13 条既有断言共同证明（无任何一条既有断言被修改）。

---

## 汇总

| 组 | 条目数 | pass | fail | partial | blocked |
|---|---|---|---|---|---|
| A（PR 验收 6 条） | 6 | 6 | 0 | 0 | 0 |
| B（T1 7 条 / T2 7 条 / T3 8 条） | 22 | 22 | 0 | 0 | 0 |
| C（零越界） | 1 | 1 | 0 | 0 | 0 |
| D（测试零改写） | 1 | 1 | 0 | 0 | 0 |
| E（无回归） | 1 | 1 | 0 | 0 | 0 |
| F（自陈一致性） | 1 | 1 | 0 | 0 | 0 |
| **合计** | **32** | **32** | **0** | **0** | **0** |

## 偏差记录

> 说明：以下均为「判据字面读法 vs 权威读法」的差异或派生偏差，**不改判 pass**。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| 1 | PR 文件 A3 与 tasks T2 字面表述「一次发布同时写入该调用的键与该调用所属对话的调用键」未规定 `chatId` 入参位置 | `publishCall(callId, event)` 的 `chatId` 取自 `event.data.chat_id`（`transport.js:105`） | tasks 已以 `[model_inferred]` 登记并经权威读法（§4.2 固定两参签名 + §4.1 三类事件 `data` 均含 `chat_id`）确认；建议在 pr-005 端到端断言中固定该取值口径，或后续在 architecture.md §4.2 补一句显式说明 |
| 2 | `architecture.md` §4.2 仅命名 `chatKey` / `callKey` / `chatCallsKey` 与 4 个新方法；「既有对外面逐字不变」字面可读作源码逐字不变 | 新增 4 个未命名内部符号 `chatKeyOrGlobal` / `subscribe` / `publishTo` / `closeKey`（原函数体搬迁 + 一行委托），对外名称/签名/语义/帧格式不变 | 无需返工；权威读法为「对外 API 契约不变、内部可重构」。建议在后续迭代文档中把「内部实现可重构」写入验收口径，避免同类假阳性 |
| 3 | 同上 | `publishGlobal` 内部由 `publish(null, event)` 改为 `publishTo(null, event)`（`:98`） | 等价（`chatKeyOrGlobal(null) === null`），不返工 |
| 4 | 同上 | `closeAll` 内部由 `close(chatId)` 改为 `closeKey(key)`（`:135`）——前缀化后必需，否则键会被二次前缀化 | 不返工；该改动正是 `closeAll` 覆盖新键空间的前提 |
| 5 | tasks 实现范围表标注 transport.js「阶段 3 实测 93 行」 | 实现后 142 行（+68/−19） | 非判据项；仅提示文档内行数为阶段 3 快照，后续引用勿当作当前值 |

## 下一迭代候选

1. `pr-005` 按 tasks「核对载体」条落新增面的端到端断言（`call:` / `chat-calls:` 键、`publishCall` 双键、先订阅再发起、`closeAll` 覆盖新键空间），把本报告的一次性临时验证固化为可重复证据（本 PR 明确「不新增测试文件」，非返工项）。
2. 如需严格锁定默认 keepalive 周期，可加一条注入式断言（现有测试仅在注入 `heartbeatMs` 下验证行为；默认 15000 ms 目前仅由源码常量佐证）。
3. 在 `architecture.md` §4.2 或 PR 模板中显式写明 `publishCall` 的 `chatId` 取值来源（`event.data.chat_id`），消除 `[model_inferred]` 残留。

## 结论

**PASS** —— 32/32 条目通过；0 fail；偏差记录 5 条（均为口径/内部重构，不阻塞本迭代关闭）。

---

### 附：三处 `git status --porcelain -uall`

- **PR worktree**（`.../0018-pr-001-transport-call-key-namespace`）：空（无输出）
- **仓库主工作区**（`/Users/chenchiyuan/projects/agents`）：空（无输出）
- **会话工作区**（`.../0018-chat-agent-subagent-protocol`）：
```
?? docs/iterations/0018-chat-agent-subagent-protocol/clarifications/verify-pr-002-20260912-231911.md
```
（唯一未跟踪项为并行验证者 pr-002 的报告；本报告写入后该目录将再增 `verify-pr-001-20260912-232019.md`）
