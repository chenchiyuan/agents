# verify-pr-003-20260913-000547

**验证者身份**：合并前代码评审者（pre-merge code reviewer）——验证对象 = 已实现并提交的 PR 代码变更 + 契约文档变更，故反射为「合并前代码评审者」：只读产物 + 自跑验证，逐条独立复核，不接受执行过程上下文。

**产出物**：
- PR worktree：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0018-chat-agent-subagent-protocol/.pb-agents/worktrees/0018-pr-003-call-http-surface-and-contract-docs`
- 实现提交：`d1bc97d feat(0018/pr-003): 调用面 HTTP/SSE 六条路由 + 契约文档 + 既有断言必变点`（6 files changed, 1083 insertions(+), 17 deletions(-)）
- 验证标准来源：主 agent 派发单（A 15 条 PR 验收 / B 独立性复核 / C 变更面闭合 / D 契约文档真实性 / E 无回归零越界 / F 已裁定事项核对 / G 零半成品检索）

**验证日期**：2026-09-13

**方法**：只读产物 + 自跑。全部证据由我的命令产出：源码/文档静态核对、`node --test`（三个文件 + 全量）、自写端到端 smoke（真起 Router + agent + web，随机端口 45807 / 46000+ / 47000+ / 47500+，临时 `OAMP_SOCKET` + 临时 `OAMP_DB`，**未占用默认端口 7788**，fake ACP 自写）。四轮 smoke 共 129 项断言。
**独立性声明**：未接收执行过程上下文——未读 dev 的会话记录、未把 dev 的验收报告或 `history.md` 的结论当作证据；下文出现的每一个数字（272 全量、7/21/39、19 条签名、129 项 smoke 断言等）均由我自跑复现。

---

## 逐项判定

### A. PR 文件「验收标准」15 条

| 项 | 判定 | 证据 |
|---|---|---|
| A1 六条新路由登记 + 顺序约束 | **pass** | ① `grep -n "path: '/api/calls" oamp/src/web.js` → 935 / 1112 / 1138 / 1160 / 1183 / 1211，依次 `POST /api/calls`、`GET /api/calls`、`GET /api/calls/stream`、`GET /api/calls/:call_id/stream`、`GET /api/calls/:call_id/transcript`、`GET /api/calls/:call_id`；② `createApiRoutes({})` 实跑：`routes.length=19`，末位 6 条即上列签名；③ 可达性实测：`GET /api/calls/stream` 返回 200（不是被 `:call_id` 吞成 404）、`GET /api/calls/<id>/transcript` 200（smoke） |
| A2 6 条表项元数据 8 字段 + params 5 字段 + errors ⊆ 5 码 + kind + SSE 列三类事件 | **pass** | `createApiRoutes({})` 实跑校验：`violations = []`（8 字段齐备、params 项 5 字段齐备、errors ⊆ `ERR_CODE` 五码）；`kinds = [json,json,sse,sse,json,json]`；两条 SSE 的 `response` 均含 `call_state / call_update / call_result` |
| A3 `POST /api/calls` 入参面齐备且只用既有 5 码 | **pass** | 9 个入参（`chat_id`/`agent`/`task`/`tasks`/`context`/`output_schema`/`schema_mode`/`mode`/`model`）齐备；全文件 `ERR_CODE` 引用无新码（`web.js:108-114` 冻结 5 码）；smoke 实测 22 条错误路径全部落在 400/404/413/502 四类 |
| A4 终态信封单一构造点、三处共用、键集合排除 usage/tokens/成本/aborted | **pass** | `composeCallEnvelope` 唯一构造点；实测阻塞响应 / `GET /api/calls/<id>` / SSE `call_result` 三者对同一次调用给出**完全一致**的 10 键与值（含 strict 覆写路径：三处一致 = true）；`grep -nE 'usage|tokens|cost|aborted' oamp/src/web.js` → **零命中** |
| A5 终态单一发布点覆盖投递与对账两路径 + `finishTask` 顺序零改 | **pass** | `grep -n 'publishCallResult' oamp/src/web.js` → 1366（定义）、1405（投递路径 `finishTask` 内）、1439（对账路径 `reconcileTask` 内）；实现为「追加」调用，`finishTask` 的「写 out → push message → push chat_state」三行顺序逐字未变（diff 中该三行无改动） |
| A6 API.md 签名集合双向覆盖 + §3 表头 + §3.14~§3.19 + §3.1 role + §4.4 + §5.12~§5.16 + §6 + §7 | **pass** | 归一化双向覆盖实跑：`registered=19 / documented=19`，`双向缺登记=[]`、`双向多出=[]`；`## 3. 接口清单（19 条）`；§3.14~§3.19 六节齐备；§3.1 含 `role` 字段（含 `null` 语义）；§4.4 事件表；§5.12~§5.16 五组示例；§6 追加 10 行；§7 全章（7.1~7.5） |
| A7 STATIC_FILES +`/calls` `+`/calls.js`；`GET /api/agents` 追加 role 且既有 4 字段不变 | **pass** | `STATIC_FILES` 416-418 行新增两项；smoke 实测 `GET /api/agents` → `pb-dev.role === 'dev'`、`dev-1.role === null`；节点键集合 = 既有 4 字段 + `role`；`?state=online` 同样带 role；既有 `?state=bogus` 仍 400 |
| A8 `api-routes.test.js` 四处必变（§11-1~4） | **pass** | diff hunk：`EXPECTED_SIGNATURES` 追加 6 条（13→19）、`danger).length, 6→7`、`接口（13 条）→19 条`、§3.14~§3.19 小节与清单行断言新增（14 行） |
| A9 `project-workspace.test.js` 五处必变（§11-5~9） | **pass** | diff hunk：签名表 13→19、`routes.length, 13→19`、`routes.slice(-2)→slice(-6)` 末位 6 条调用面、llms 表头 19 条、写接口计数 7 |
| A10 `web.test.js` 一处必变（§11-10） | **pass** | diff hunk @1518：键集合追加 `role` |
| A11 `llms.txt` 已重生成（§11-11） | **pass** | `node oamp/scripts/gen-llms-txt.mjs` → `接口 19 条，2569 字节`，随后 `git diff --stat` **无新变化**（快照逐字节同步） |
| A12 `npm test` 全绿且其余测试文件 diff 为空 | **pass** | 见 B3；`oamp/test/**` diff 仅落 3 个文件 |
| A13 零改动路径（router/agent/persist/context-pool/role-binding）+ 协议方法仍 7 | **pass** | `git diff --name-only d1bc97d^..HEAD` 不含任何 `src/` 其它文件；协议面未触 |
| A14 端到端 smoke：缺 `chat_id` → 400 INVALID_PARAM；`agent` 不存在的角色 → 404 NOT_FOUND；两者 roster 无新增行 | **pass** | 逐案例探针：`未提供 chat_id → 400/INVALID_PARAM Δroster=0`、`agent:'no-such-role' → 404/NOT_FOUND Δroster=0`（并实测 15 条错误路径 Δroster 全 0） |
| A15 既有 `/api/messages` 形态与失败兜底逐字不变 | **pass** | 实测：正常派发 200；离线目标 → **200 + warning 非空** 且对话侧补 `out{error:'dispatch_failed'}`（≥2 条）；已关闭对话 → 仍 **409**；`GET /api/chats/<id>` 仍 200 |
| **A16** roster 六列 + 倒序 + 零成本/token（补充覆盖项） | **pass** | 键集合逐字 `['call_id','agent','state','started_at','ended_at','model']`；`started_at` 单调不增；进行中 `ended_at=null`、`model=null`；终态 `ended_at >= started_at`、`model='deepseek/deepseek-v4-flash'`；无 usage/tokens/cost 列 |
| **A17** 按 id 与转录读取（补充覆盖项） | **pass** | `GET /api/calls/<id>` 返回**裸信封**（未包装，实测无外层键）；`/transcript` 五键 + 过程条目 + 末尾 `detail.event='result'`；未知 id → 404 `call 不存在: …`；进行中可读且不追加终态条目 |
| **A18** 两类 SSE 作用域与三类事件（补充覆盖项） | **pass** | 对话作用域可「先订阅、再发起」；实测序列 `["call_state","call_state","call_update","call_update","call_update","call_result"]`（submitted → working 至多一次 → ≥2 次增量 → 闭合于 call_result）；`call_result` 帧 = `chat_id` + 信封 10 字段平铺（11 键，不嵌套）；按调用作用域只收本调用事件；事件封闭为三类；缺 `chat_id`/空值 → 400；未知 call_id → 404 且不建订阅 |
| **A19** 既有面零回归（补充覆盖项） | **pass** | 全量 272/272（B3）；既有 `/api/messages`/`/api/chats`/`/api/agents`/`?state=` 行为实测未变；`transport/registry/router/agent/persist/context-pool/role-binding` 零 diff |
| **A20** 状态一致性：roster ↔ 按 id ↔ 转录（补充覆盖项） | **FAIL** | strict 校验失败路径：同一 `call_id` 上 **信封 `state=failed`**，而 **roster `state=completed`**、**转录 `state=completed`**。违反 F09 验收 3「roster 中某行的状态，与该调用按 id 查询到的状态一致」，并证伪 API.md §3.19「`state` 与 §3.15 的 `state` 同真源（同一任务记录），两处不会漂移」。复现见「偏差 2」 |

### B. 独立性复核

| 项 | 判定 | 证据 |
|---|---|---|
| B1 快照已同步 | **pass** | `cd <PW> && node oamp/scripts/gen-llms-txt.mjs` → 输出「接口 19 条，2569 字节」，紧接 `git -C <PW> diff --stat` 输出为空（无新变化） |
| B2 三个既有测试文件全绿 | **pass** | `oamp/test/api-routes.test.js` **7/7**、`oamp/test/project-workspace.test.js` **21/21**、`oamp/test/web.test.js` **39/39**，`fail 0 / cancelled 0 / skipped 0 / todo 0`（三文件合跑一次亦为 67 OK） |
| B3 全量回归全绿 | **pass** | `cd <PW>/oamp && node --test test/*.test.js` → **`1..272` / `# tests 272` / `# pass 272` / `# fail 0` / `# cancelled 0` / `# skipped 0` / `# todo 0`**（59982 ms，24 个测试文件） |
| B4 端到端验证（真起服务） | **pass** | 我自写 4 个 smoke（真起 Router + 3~4 个 agent（fake ACP）+ web，随机端口与临时 DB/socket），共 **129 项断言、3 轮 FAIL 全部定位为我的过严断言或真实缺陷**：后台/阻塞两模式、批量两项、共享说明装配、期望结构四态（命中 / 围栏剥离 / permissive 退回 / strict 失败）、`POST /api/calls`、`GET /api/calls` 六列、`GET /api/calls/<id>` 裸信封、`/transcript`（含 1001 条截断）、两个 SSE 端点的完整事件序列、`>1000` 条更新 → `truncated:true`（信封与转录同口径）、Router 重启后 404、DB 表/列零新增。进程与临时目录已清理（见「环境自证」） |

### C. 变更面闭合（§11 机械判据）

| 项 | 判定 | 证据 |
|---|---|---|
| C1 `diff --name-only` 恰 6 路径 | **pass** | `oamp/API.md`、`oamp/llms.txt`、`oamp/src/web.js`、`oamp/test/api-routes.test.js`、`oamp/test/project-workspace.test.js`、`oamp/test/web.test.js` |
| C2 `oamp/test/**` hunk 全部落在 §11 第 1~10 条点名区间 | **pass** | 全部 hunk 映射回**改动前**行号：api-routes 旧 49（§11-1 的 `EXPECTED_SIGNATURES` 数组末位）、旧 473（§11-2）、旧 483（§11-3）、旧 499（§11-4 的 495-502）；project-workspace 旧 1219（§11-5 数组末位）、旧 1230/1231（§11-6/7）、旧 1265（§11-8）、旧 1301（§11-9）；web 旧 1518（§11-10 的 1517-1518）。**名义区间与插入点的关系**：§11 的行号是改动前实测值，数组类条目的「追加」必然插在区间末行之后（新文件行号 = 旧行号 + 前序插入量，实测差值恰等于前序插入行数），故判为落在点名区间内 |
| C3 §11 第 12 行 5 组「已核对不受影响」文件 diff 为空 | **pass** | `git diff --numstat` 对 `task.test.js` / `transport.test.js` / `api-pages.test.js` / `hygiene.test.js` / `context-pool.test.js` / `acp-daemon.test.js` / `delivery-contract.test.js` 逐一 → `changed_files=0` |
| C4 `package.json` dependencies 仍 `{}` | **pass** | `git show HEAD:oamp/package.json \| jq .dependencies` → `{}` |

### D. 契约文档真实性（API.md）

| 项 | 判定 | 证据 |
|---|---|---|
| D1 §3 表头计数 = 登记数；签名集合双向覆盖 | **pass** | `## 3. 接口清单（19 条）`；归一化双向覆盖 `缺=[] / 多=[]` |
| D2 §3.14~§3.19 字段表 / 错误表与代码实际行为一致（抽查 ≥5 字段/错误） | **FAIL** | 抽查 24 项中 23 项一致（`chat_id` 四形态 400、`agent` 三情形同码同文案、`tasks` 互斥/空数组/项缺 task、`mode`/`schema_mode` 枚举、`output_schema` 子集（`$ref`/`anyOf`/`items`/真嵌套 properties/非法 type）、`context` 类型、`model` 正则、413 + `connection: close`、`task` 全空白、400/404/502 文案与状态码、`structured_output` 三态、转录追加规则、`truncated` 口径、六列与排序）。**不一致 1 项**：§3.19 字段表 `exit_code` 行「可得时给出；**常驻执行路径为 `null`**」↔ 实测 daemon **成功**路径返回 **`0`**（`agent.js:338` 的既有体含 `exit_code: 0`），仅失败路径为 `null`。§5.13/§5.14 的示例输出（completed 常驻路径写 `"exit_code":null`）同样不可复现，冲击「示例可粘贴」承诺。另 §3.19「`state` … 两处不会漂移」的断言被 A20 证伪（计入偏差 2） |
| D3 §7.2 三面对照表 25 行（11+7+7） | **pass** | 机械计数：`§7.2 行数 = 25 | I = 11 R = 7 D = 7`；编号序 `I1…I11,R1…R7,D1…D7` |
| D4 §7.3 差异 ①~⑲ 齐备且不新增编号 | **pass** | `§7.3 编号数 = 19 ①②③④⑤⑥⑦⑧⑨⑩⑪⑫⑬⑭⑮⑯⑰⑱⑲`（无 ⑳、无新增） |
| D5 §7.4 覆盖核对双向 | **pass** | 行侧：§7.2 非「必须等价」行 = 18，与 §7.4 列出的 18 行集合**完全相同**（`I1,I4,I5,I8,I9,I10,I11,R1,R2,R3,R6,R7,D1,D2,D4,D5,D6,D7`）；条目侧：§7.4 的 19 个编号全部出现在 §7.2「差异编号」列（编号集 = 19） |
| D6 §7.2 是否仍能逐条对上 harness 参照契约的不变量表述 | **pass（附重点偏差）** | 我通读 `omp://tools/task.md`（参照契约唯一真源）逐条比对：**25/25 行均能在参照契约中找到唯一对应不变量**（含 I1 发现优先级、I2 batch/context、I3 `outputSchema`+`schemaMode`、I6 阻塞 vs 后台 job、I7 模型优先级、I11 ACP 派发契约、R3 `history://`、R5 `yield`、R7 终态词、D3 完成通知、D5 取消、D6 帧订阅、D7 协同镜像），**故仍可逐条核对**。代价如实登记为偏差 4（8 行把英文锚点改为中文等价表述，关键词级检索性弱于 harness 原文） |

### E. 无回归 / 零越界

| 项 | 判定 | 证据 |
|---|---|---|
| E1 `95051b0..HEAD` 文件集合 ⊆ PR 文件范围 + `prs/pr-003-tasks.md` | **pass** | `git diff --name-only 95051b0..HEAD` → 7 个：6 个 PR 文件 + `docs/.../prs/pr-003-tasks.md` |
| E2 禁改面零命中 | **pass** | 同一命令不含 `oamp/README.md`、`roles/**`、`.claude/**`、`docs/**`（仓库文档）、`oamp/web/**`、`oamp/src/{transport,registry,router,agent,persist,context-pool,role-binding}.js` |
| E3 零新依赖 | **pass** | 同 C4；全量 272 用例含 `hygiene.test.js`（零依赖锁）全绿 |

### F. 已裁定事项的核对（按权威读法判 pass，全部登记为偏差）

| 项 | 判定 | 证据 |
|---|---|---|
| F1 F10 验收 3「重启服务」= 整体重启 | **pass** | 实测：Router 重启（同 socket 路径重起）后 `GET /api/calls/<id>` → **404** `{"error":"call 不存在: …","code":"NOT_FOUND"}`、`/transcript` → **404** 同款；重启前两者均 200；重启后既有 `GET /api/chats/<id>` 仍 200（零回归）。转录真源在 Router 任务表 ⇒ 该表现符合卡原文与架构 §3.4/§3.5 单真源 |
| F2 web 侧 `callSchemas` 的存在与影响面 | **pass** | 复核影响面：仅 `startWeb` 作用域的进程内 `Map`（`web.js:1348`），只登记带 `output_schema` 的调用、退出时 `clear()`；**无**持久化、**无**新表/列（实测 DB 表 = `chats/messages/projects/sqlite_sequence`，列与基线一致）、**无**新 UDS 方法、**无**新配置键、**无**新依赖 |
| F3 §11 区间内过时字样未改 | **pass** | §11 第 1~10 条点名区间内的既有断言文本除表内点名的字面量（13→19、6→7、键集合 +role）外零改写；第三处过时标题（`test` 用例名仍写「13 条」）保留未改 —— 属裁定结果，登记为偏差 5 |
| F4 GET 返回裸信封 | **pass** | 实测响应**无外层包装**，键集合/顺序 = `[call_id, agent, state, duration_ms, model, truncated, text, structured_output, error, exit_code]`。**但键数为 10 而非裁定文本的「11 键」** —— 见偏差 3 |
| F5 10 条 `[model_inferred]` 的实现一致性 | **pass** | 与调用面实现相关的可核对项实测一致：`model` 只报执行侧实报值（成功 = `deepseek/deepseek-v4-flash`；非终态 = `null`）、失败体 `model` 不冒充请求参数、`truncated` 单一口径 OR、`task_list` 投影 `model` 供 roster 使用。登记为偏差（口径沿用，无新增证据冲突） |

### G. 零半成品检索（F15 验收 1）

| 项 | 判定 | 证据 |
|---|---|---|
| G1 新增代码面零半成品 | **pass** | `grep -nE 'cancel\|terminate\|steer\|isolated\|effort\|usage\|tokens\|cost\|aborted\|TODO\|FIXME\|XXX\|HACK\|not implemented\|未实现\|排期\|占位' oamp/src/web.js` → 仅 607、1543 两处命中，均为**既有** `best-effort` 字样（`effort` 子串），与调用面新增代码无关；新增 6 条路由的 `path`/`params` 无 cancel/terminate/steer/isolated/effort 类入口 |
| G2 `API.md` 词表命中 ⊆ 允许集合（差异清单节 + §6） | **pass** | 全文半成品词表零命中（`TODO/FIXME/占位实现/未实现/排期`）；不变量词表命中集中在 §6（不做声明）与 §7（对照表 + 差异清单）；另有 §3.4/§3.5 两处 `best-effort`（既有文本，未修改）与 §3.12/§3.13 两处「展示占位即可」（既有文本，未修改，非本迭代新增面） |
| G3 零持久化 / 零编排入口 | **pass** | 转录不落库：MANY 用例产生 1001 条转录条目，DB 中该调用仅 1 条 `in` + 1 条 `out` 行（8 行 = 4 次调用 × 2），无逐条过程行；roster 无过滤/分页/排序参数（`params: []`） |

---

## 汇总

- **pass: 43 / fail: 2 / partial: 0 / blocked: 0**（共 45 项）
- fail 明细：**A20**（状态一致性：roster ↔ 按 id ↔ 转录）、**D2**（§3.19 `exit_code` 字段表行与实现不一致）

---

## 偏差记录

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| 1 | API.md §3.19 字段表：`exit_code`「可得时给出；常驻执行路径为 `null`」；§5.13/§5.14 示例输出 `"exit_code":null`（completed） | daemon **成功**路径返回 **`0`**（`agent.js:338` 既有成功体含 `exit_code: 0`，信封透传）；daemon 失败路径 `null`；shell/一次性路径为真实码。附带：F06 卡 T-13 与 architecture §3.2 的「daemon 路径无此字段」与同卡技术锚点 F-13「成功体含退出码」自相矛盾 | **改文档**（实现忠实透传真实值，符合 F06 验收 2 的「非零退出标记」语义）：§3.19 改为「成功 = 0；失败/不可得 = `null`」，并同步 §5.13/§5.14 示例输出。**下一迭代候选**（文档级，不阻塞代码合并） |
| 2 | API.md §3.19「`state` 与 §3.15 的 `state` **同真源**（同一任务记录），两处不会漂移」；F09 验收 3「roster 状态与按 id 一致」 | `output_schema` + `schema_mode:'strict'` + 校验不通过时：信封 `state=failed`（`composeCallEnvelope` 的 strict 覆写），而 roster（§3.15）与转录（§3.18）取任务表原值 `completed`。实测同 `call_id`：`信封=failed / roster=completed / transcript=completed`；三处信封（阻塞响应 / GET / SSE `call_result`）彼此一致 = true | **需裁定**：属真实功能缺陷（F09-3 判定失败）。两条路——① 让 roster/转录同样反映 strict 覆写后的终态（单一终态真源）；② 在文档显式记载该漂移并收窄 §3.19 的措辞（并说明 roster 反映执行事实、信封反映交付事实）。在裁定前不建议合并 |
| 3 | API.md §3.19「信封的键集合是**封闭的 11 键**（上表）」；主 agent 裁定 ⑤「GET 返回裸 11 键信封」 | 同节字段表列 **10** 个字段，实现返回 **10** 键（`call_id, agent, state, duration_ms, model, truncated, text, structured_output, error, exit_code`）。**11 键**只对 SSE `call_result` 帧成立（`chat_id` + 10）。architecture §3.1 与 PR 验收第 4 条列举的同样是 10 个字段名，且 §3.1 明写信封「**不包含** `chat_id`」 | **改文档**：§3.19 的「11 键」→「10 键」（`call_result` 帧另行说明为 11 键）。裁定 ⑤ 的「裸（不包装）」已满足，仅计数需订正 —— **下一迭代候选**（文档级） |
| 4 | architecture §12.3 / F15 验收 1 的词表检索；§7.2 应「逐条不变量语义对照」参照契约 | §7.2 为满足关键词检索，把 **8 行**的不变量英文锚点改为中文等价表述：I4 `effort`→「工作量档位」、I5 `isolated`→「隔离」、I8 `AgentOutputManager`→「调用命名与身份」、I9 `local://`+`ArtifactManager`→「共享文件系统根 + 产物管理器」、R1 `usage`/`tokens`/`exitCode`/`resolvedModel`→「一组字段 / 退出标记 / 生效模型」、R2 `agent://`→「产物 URI」、D1 `tokens`/`cost`→（省略）、D2 `cost`/`token`→「用量与开销」。**核对结论：25/25 行仍能在 `omp://tools/task.md` 找到唯一对应不变量，语义对照可逐条完成**；损失仅在关键词级可检索性（英文原词不再出现在 §7.2） | **接受 + 记重点偏差**（主 agent 已裁定 ⑥）。建议下一迭代在 §7.2 的「参照契约的不变量」列**加括号补英文原词**（如「工作量档位（`effort`）」），零语义变化即恢复关键词级可核对 —— **下一迭代候选** |
| 5 | §11「变更面闭合」逐字零改写口径 | `§11` 第 1~10 条点名区间内的三处**用例标题**仍含过时字样（如 `test('派生面 HTTP：/api/docs 的 13 条投影…')`、`test('API.md 同步：…§3 标题 13 条…')`、`test('F10：/api/docs 投影 13 条…')`），**未改** | **接受**（主 agent 裁定 ③：区间内过时字样不改是裁定结果）。登记，无动作 |
| 6 | pr-003-tasks.md 措辞「重启 web 子进程」 | 卡片 F10 验收 3 原文为「重启服务」；实现/架构真源 = Router 任务表 ⇒ 只重启 web 不丢属正常，Router 重启 → 404（已实测） | **接受**（主 agent 裁定 ①）；如需消除歧义可在 tasks.md 加一行注 —— 下一迭代候选（文档级，可选） |
| 7 | `POST /api/calls` 的 404 语义 | 「角色**可解析但离线**」路径实测 **Δmsg=2**（1 `in` + 1 `out{dispatch_failed}`）、Δroster=0；而 API.md §3.14 的「**零副作用**（…对话消息数不变）」按上下文只覆盖 INVALID_PARAM 与「角色**不可寻址**」两类（实测这两类 Δmsg=0/Δroster=0，**属实**） | **接受**（与 architecture §2.3 第 6 步「对话侧行为与既有逐字一致」一致）。建议把 §3.14 的「零副作用」措辞显式限定到「INVALID_PARAM 与角色不可寻址」两类，避免被读成全类 —— **下一迭代候选**（文档措辞） |
| 8 | F11 验收 1「该调用与所归属的 chat 关联可核对」 | `in` 行 `meta.task_id` = `call_id`（可关联，实测命中）；`out` 行 `meta = {context_id, pid}`，**无** `task_id`（既有 `insertOutput` 行为，本 PR 未改） | **接受**（关联核对路径成立）。登记为事实；若要求 in/out 双向关联，属既有 persist 面变更 → **下一迭代候选** |
| 9 | `oamp/README.md` 的「13 条」计数 | 未改（本 PR 未触碰 README） | **接受**（主 agent 裁定 ④，已登记为下一迭代候选） |

---

## 下一迭代候选

1. **订正 API.md §3.19 `exit_code` 行 + §5.13/§5.14 示例输出**（偏差 1）。
2. **订正 API.md §3.19 的「11 键」→「10 键」**（偏差 3）。
3. **裁定并修复/记载 strict 覆写导致的 state 漂移**（偏差 2；A20 fail 的根因）。
4. **§7.2 加括号补英文原词**，恢复关键词级可核对性（偏差 4；纯文档、零语义变化）。
5. **收窄 API.md §3.14「零副作用」措辞**到 INVALID_PARAM 与角色不可寻址两类（偏差 7）。
6. **`oamp/README.md` 的「13 条」计数漂移**（偏差 9，主 agent 已登记）；
7. （可选）`prs/pr-003-tasks.md` 的「重启 web 子进程」措辞与卡片对齐（偏差 6）。

---

## 结论

**FAIL**

- 45 项中 43 pass / **2 fail**（A20 状态一致性、D2 §3.19 字段表 `exit_code`），0 partial / 0 blocked。
- 15 条 PR 验收标准（A1~A15）**全部 pass**；两条 fail 均来自独立复核的加严检查（补充覆盖项 A16~A20 与契约文档真实性 D2），非 PR 自查项。
- 阻塞合并的核心项：**偏差 2（strict 路径下 roster/转录与信封的 state 漂移）**——它是可复现的真实功能缺陷（F09 验收 3 判定失败），需主 agent 裁定「改实现收敛终态真源」还是「改文档显式记载漂移」后再放行。
- 其余 8 条偏差均为**文档级**（计数/措辞/锚点），不阻塞代码合并。

---

## 环境自证（验证过程零残留、零越界）

- **进程**：`pgrep -fl '0018-pr-003'` → 无；未占用默认端口 7788（全部 smoke 用随机测试端口）；仓库既有 `oamp-cluster` 进程与 peer 的 pr-002 进程**未被触碰**。
- **临时目录**：4 轮 smoke 的临时 socket/DB 目录均由其自身 `finally` + harness `cleanup()` 删除；`/tmp/oamp-verify-pr003`（我的 smoke 脚手架）已删除。OS 临时目录下 `oamp-test-*` 为项目自带测试套件（`test/helpers/harness.js`）的产物、跨会话累积，非本次新增判定对象。
- **不写文件**：除本报告外，PR worktree / 仓库主工作区**零写入**；`oamp/llms.txt` 的重生成经 `git diff` 证实为逐字节无变化（非写入）。
- **三处 `git status --porcelain -uall`**：
  - PR worktree（`…/0018-pr-003-call-http-surface-and-contract-docs`）：**空**
  - 仓库主工作区（`/Users/chenchiyuan/projects/agents`）：**空**
  - 会话工作区（`…/0018-chat-agent-subagent-protocol`）：报告写入前**空**；写入后仅新增本报告 `docs/iterations/0018-chat-agent-subagent-protocol/clarifications/verify-pr-003-20260913-000547.md`
