# pr-001 任务图 — critical-path-constraint-and-gate-migration

**输入**：`architecture.md` §F01~§F04、`prd/F01~F04.md`、`prs/pr-001-critical-path-constraint-and-gate-migration.md`（19 条验收标准）
**范围**：本 PR 内部任务分解，非全局任务图

---

## 任务列表

### T1. pr-planner.md Strategy 新增「关键路径长度」小节

**描述**：在 `roles/pr-planner/pr-planner.md` Strategy 章节「PR 粒度判断锚点」之后、「并发可行性（推进条件）」之前，插入命名为"关键路径长度（Work 阶段硬约束）"的独立小节，含合并循环规则子节与"合并受阻：建议拆分为多个迭代"子节。

**验收标准**（覆盖 PR 验收项 1、2）：
- 小节存在且命名准确，明确定义（依赖图最长链节点数）、硬上限（3）、检查时机（Work 阶段）、超限处理规则（四步合并循环：计算关键路径→排序取候选对→合并前置检查→不违反则合并回到步骤1/违反则停止）、决策理由（L2，复用 `depends_on` 图结构不引入工时估算）
- 该小节包含子节"合并受阻：建议拆分为多个迭代"，明确停止动作、转出结论、拆分边界算法（以触发违反的相邻对为分割点、B 及其下游划入迭代 2）、落盘位置（`prs/split-suggestion.md`）、性质（建议而非自行执行）

**前置依赖**：无

**优先级**：P0

---

### T2. pr-planner.md Workflow §4/§5 新增关键路径压平段落与检查项

**描述**：在 `roles/pr-planner/pr-planner.md` Workflow §4（Work）既有 Thought-Action-Observation 循环之后追加"关键路径压平"段落；§5（Verify）新增关键路径检查项。

**验收标准**（覆盖 PR 验收项 3、4）：
- §4 Work 追加段落明确触发时机（全部 PR 文件 `depends_on` 写定后）、执行动作（计算关键路径长度、>3 时执行合并循环、合并会改 PR 文件需回本步骤重新 Action）、循环终止条件（≤3 或转出拆分建议）
- §5 Verify 新增检查项："关键路径长度 ≤3，或合并受阻已转出'建议拆分为多个迭代'结论（`prs/split-suggestion.md` 存在）——二者必居其一，不能关键路径 >3 且没有转出结论就直接交付"

**前置依赖**：T1（Workflow 段落文字需引用 T1 定义的合并规则与小节名称，避免用语不一致）

**优先级**：P0

---

### T3. pr-planner.md 成功标准/报告契约/红线/Tools边界/决策记录同步新增

**描述**：在 `roles/pr-planner/pr-planner.md` 的成功标准、报告契约、红线（绝不）、Tools and capability boundaries §不做什么、决策记录判断标准五处各新增一条。

**验收标准**（覆盖 PR 验收项 5、6、7、8、9）：
- 成功标准新增："关键路径长度 ≤3，或已转出'建议拆分为多个迭代'结论（`prs/split-suggestion.md`）"
- 报告契约新增第 5 项："关键路径压平结果：最终关键路径长度；是否发生合并（合并了哪些 PR）；若转出'建议拆分为多个迭代'结论，附 `prs/split-suggestion.md` 路径与摘要"
- 红线（绝不）新增："绝不为了让关键路径数字达标而强行合并违反原子性/可审查性的候选对"
- Tools and capability boundaries §不做什么新增："不自行创建新迭代目录或修改 demand.md——合并受阻时只产出'建议拆分'结论"
- 决策记录判断标准新增一条："触发过合并受阻转出'建议拆分'结论 → 记"

**前置依赖**：T1（这五处新增条目的措辞需与 T1 定义的小节名称、机制名称保持一致）

**优先级**：P1

---

### T4. pr-planner.md 版本升级 + changelog 新增记录

**描述**：`roles/pr-planner/pr-planner.md` 版本号 `0.2.0` → `0.3.0`，新增"v0.3.0 变更说明"章节（格式参照现有 v0.2.0 章节）；`roles/pr-planner/data/pr-planner-changelog.md` 新增一条 v0.3.0 变更记录。

**验收标准**（覆盖 PR 验收项 10、11）：
- `pr-planner.md` 版本号升至 `0.3.0`，新增"v0.3.0 变更说明"章节，含触发背景/新增了什么/目的/不改变的部分四段
- `data/pr-planner-changelog.md` 新增一条 v0.3.0 变更记录，包含触发背景（0025 迭代案例）、具体改动摘要、决策过程（若有非显然的架构选择）

**前置依赖**：T1、T2、T3（版本说明与 changelog 需完整反映 T1~T3 的全部改动内容，必须等三者内容确定后才能准确总结）

**优先级**：P1

---

### T5. 新建 prs/split-suggestion.md 格式规范参考文件

**描述**：在 `docs/iterations/0027-pr-planner-wave-cap/prs/split-suggestion.md` 新建格式规范参考文件（非运行时产物），包含四个章节。

**验收标准**（覆盖 PR 验收项 12）：
- 文件存在，包含四个章节：触发原因（合并循环停止处关键路径长度 + 候选对 + 违反锚点具体说明）、拆分建议（迭代 1 与迭代 2 各列功能点 F-ID 与覆盖 PR）、依据（分割点与划分算法说明）

**前置依赖**：无（格式定义可独立于 T1~T4 编写，内容来自 architecture.md §F01 第 2 步的既有格式模板，文件路径与 T1~T4 无重叠）

**优先级**：P2

---

### T6. workflow-pb.md 阶段定义表扩展 + 方案确认门章节替换 + 版本升级

**描述**：`roles/workflow-pb/workflow-pb.md` §阶段定义表第4行「推进条件」列从四项扩展为六项；原"§方案确认门（阶段3→4之间）"整节替换为新版"§方案确认门（阶段4→5之间）"；§调度指南「启动工作流」启动前询问措辞同步；版本号 `0.13.0` → `0.14.0`。

**验收标准**（覆盖 PR 验收项 13、14、15、17）：
- §阶段定义表第4行「推进条件」列包含六项（原四项 + 并发可行性检查全部通过 + 关键路径长度 ≤3 或已转出拆分建议，后者引用 `prs/split-suggestion.md` 文件存在作为判断信号）
- 原"§方案确认门（阶段3→4之间）"章节已被替换为新版"§方案确认门（阶段4→5之间）"，包含六个子项：触发时机、触发条件、前置说明、呈现内容（三部分）、通过条件、关闭情况
- §调度指南「启动工作流」章节的启动前询问措辞，"阶段3→4之间"已改为"阶段4→5之间"
- 版本号从 `0.13.0` 升至 `0.14.0`

**前置依赖**：无（workflow-pb.md 与 pr-planner.md 是两个独立文件，T6 不依赖 T1~T5 的具体文字内容，只需引用 `prs/split-suggestion.md` 这个文件名，属于既定契约，不需等 T1/T5 完成才能写）

**优先级**：P0

---

### T7. workflow-pb/data/formats.md 措辞同步

**描述**：`roles/workflow-pb/data/formats.md` 第 104 行左右 `**方案确认门**` 字段注释"供阶段3→4推进逻辑查询"改为"供阶段4→5推进逻辑查询"；扫描全文确认是否存在独立的"阶段4推进条件"描述段落，若存在则同步为六项内容或新增指向 workflow-pb.md 的说明性注释，若不存在则本项自动通过。

**验收标准**（覆盖 PR 验收项 16、18）：
- `roles/workflow-pb/data/formats.md` 第 104 行左右的"供阶段3→4推进逻辑查询"已改为"供阶段4→5推进逻辑查询"
- `formats.md` 中关于"阶段4推进条件"的描述（若存在）已同步为六项内容，或新增说明性注释指向 `workflow-pb.md` §阶段定义表第4行；若原本不包含该独立描述段落，则本项自动通过

**前置依赖**：T6（措辞同步的"六项"内容与"阶段4→5"表述需与 T6 改完后的 workflow-pb.md 保持一致，避免文本先行、被 T6 的最终表述覆盖导致不一致）

**优先级**：P1

---

### T8. architecture.md §F03/§F04 章节内容核对（架构决策留痕确认）

**描述**：确认 `architecture.md` 中 §F04 章节与 §F03 第 5 点（SKILL.md 同步清单表格）的既有内容满足 PR 验收标准要求（该文件在阶段3已产出，本任务是核对而非新增内容；若发现缺口需回报，不在本 PR 范围内修改架构决策本身）。

**验收标准**（覆盖 PR 验收项 19、20）：
- `architecture.md` §F04 章节存在，明确说明"F04 的技术路径无独立文本改动——通过 F03 第1步与 F03 第2步共同满足"，以及 `[架构待填]` 解答"复用阶段4推进条件核查结果，不独立重新执行"
- `architecture.md` §F03 第 5 点存在完整的 SKILL.md 同步清单表格，明确列出 frontmatter description / Step 0 启动询问措辞 / Gate 章节标题从"阶段3→4"改"阶段4→5"、Gate 章节位置迁移、Gate 章节内容同步为新版方案确认门五处改动

**前置依赖**：无（architecture.md 是阶段3既有产物，本任务只做核对，与 T1~T7 的实现顺序无关）

**优先级**：P2

---

## 依赖图

```
T1 ──┬──> T2 ──┐
     └──> T3 ──┴──> T4

T6 ──> T7

T5   （独立）
T8   （独立）
```

**最长依赖链（关键路径）**：T1 → T2 → T4（长度 3，含 T1、T2、T4 三个节点；T3 与 T2 同层，均依赖 T1，T4 依赖 T2 和 T3 两者）

无环：T1→T2、T1→T3、T2→T4、T3→T4、T6→T7，T5、T8 无入边无出边。全图为有向无环图。

**关键路径任务**：T1（关键路径长度定义与合并规则，是 T2/T3 措辞一致性的基础）→ T2（Workflow 段落落地）→ T4（版本与 changelog 总结，需等 T2、T3 内容确定）。

---

## model_inferred 验收标准

无——本任务图的全部验收标准直接摘自 `prs/pr-001-critical-path-constraint-and-gate-migration.md` 的 19 条原文表述（逐条分组归入 T1~T8），未新增或改写验收语言。

## 上报的循环依赖

无——依赖图验证为有向无环图（见上方「依赖图」小节的边列表与无环说明）。

## 疑问/越界

无——19 条验收标准均能在 architecture.md 对应章节找到具体技术路径依据，未发现信息不足以写出可追溯验收标准的情况。
