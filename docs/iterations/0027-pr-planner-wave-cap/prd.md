# prd.md — 0027-pr-planner-wave-cap

**版本**: 0.1.0
**迭代**: 0027-pr-planner-wave-cap
**创建日期**: 2026-09-15
**阶段**: 功能规格（阶段 2）
**输入合同**: `docs/iterations/0027-pr-planner-wave-cap/demand.md`（已收敛，v1.0.0）

**说明**：本次迭代修改的是 `pr-planner` 和 `workflow-pb` 两份角色/流程规范文件本身，不是业务代码。因此本迭代的"用户可见行为"是这些规范文档最终文本内容——每张卡的验收标准都可以通过直接阅读改动后的文件文本（以及用 0025 迭代真实依赖图做纸面推算）来核查，不需要运行程序或做用户操作测试。

---

## 功能点索引

| ID | 功能名 | 用户价值摘要 | 规格卡 | 来源条款 | 架构维度 |
|---|---|---|---|---|---|
| F01 | pr-planner 关键路径硬约束与合并压平规则 | 迭代产出的 PR 依赖图关键路径不会无限累加，不会再遇到"5 轮才能跑完一次迭代"的规划结果 | [F01](prd/F01-pr-planner-critical-path-constraint.md) | 做什么#1（前半）；D-1/D-2/D-3/D-5（合并排序规则部分） | 无待填项（复用现有 `depends_on` 依赖图，见 demand.md「大概怎么做」） |
| F02 | pr-planner 合并受阻转出"建议拆分为多个迭代"结论 | 迭代范围真的太大、无法靠合并 PR 压平关键路径时，用户能直接看到"拆成几个迭代、每个迭代含哪些功能点"的具体建议，不会被卡在一个无法执行的规划里 | [F02](prd/F02-pr-planner-split-iteration-suggestion.md) | 做什么#1（后半）；D-3/D-5（合并前置检查部分） | 无待填项 |
| F03 | workflow-pb 方案确认门位置迁移与呈现内容扩展 | 用户在唯一一次确认门里就能看到完整方案全貌（产品+架构+PR拆分摘要，含关键路径长度和若有的拆分建议），一次性拍板这次迭代最终跑几轮 | [F03](prd/F03-plan-gate-relocation-and-content.md) | 做什么#2（前半+呈现内容部分）；D-6 | 已补全（阶段3）：SKILL.md 同步位置清单见 `architecture.md` §F03 第 5 点 |
| F04 | workflow-pb 阶段4结构性自检前置于确认门 | 用户在确认门里看到的已经是结构正确的方案（无环、无文件冲突、并发可行、关键路径已压平），用户只需要做业务判断，不用先挑结构性错误 | [F04](prd/F04-stage4-structural-precheck.md) | 做什么#2（后半）；D-6 | 已补全（阶段3）：复用阶段4推进条件核查结果，不独立重新执行，见 `architecture.md` §F04 |

**合计**：**4 张功能卡**，覆盖 demand.md「做什么」全部 2 条（F01/F02 ← 做什么#1 的合并压平主路径与转出拆分建议两个判断分支；F03/F04 ← 做什么#2 的确认门位置内容与结构性自检两个维度）。

---

## 本次迭代边界说明

### 包含

- pr-planner 新增"关键路径长度"判断维度，Work（分组）阶段作为硬约束（关键路径 ≤3），超限时按合并排序规则循环合并压平（F01）。
- 合并前置检查违反原子性/可审查性锚点时，停止合并循环并转出含具体拆分建议的"建议拆分为多个迭代"结论（F02）。
- workflow-pb 方案确认门从"阶段3→4之间"挪到"阶段4→5之间"；呈现内容扩展为 prd.md + architecture.md + PR 拆分方案摘要（PR数、依赖图、关键路径长度、若有拆分建议）（F03）。
- 阶段4完成后的结构性自检（依赖图无环、文件范围不重叠、并发可行性、关键路径压平结果）在确认门之前自动跑完，只有结构正确的方案才进确认门（F04）。

### 不包含（对应 demand.md「不做什么」，已作为各卡边界项落实，不单独拆保证项卡）

- 不引入关键路径算法之外的新调度机制（时间估算/工时字段）——见 F01 边界说明。
- 不改变"并发可行性"逐对检查的判断逻辑和 Verify 阶段位置——见 F01/F04 边界说明。
- 不新增独立的暂停触发类型——见 F02/F03/F04 边界说明。
- 不改变 PR 文件七字段格式本身——见 F01 边界说明。
- 不追溯已完成迭代（如 0025）的历史产物——0025 仅作纸面推算验证用例，不涉及回写。
- 不设合并循环的次数上限——见 F01 验收标准第5条。
- 不用功能点数量作为触发拆分判断的信号——见 F02 边界说明。

以上 7 条"不做什么"未在 demand.md 中对应独立的产品行为，均以既有功能卡的边界约束形式承载，不新增额外的保证项卡。

### 架构维度处理（阶段3已补全）

原标注 `[架构待填]` 的 2 处，已在阶段3（技术架构）补全，详见 `architecture.md`：

- F03 验收标准3：SKILL.md 需要同步的具体位置清单已确定（frontmatter description、Step 0 询问文案、`Gate: 阶段3→4方案确认门` 章节标题+内容+位置迁移），见 `architecture.md` §F03 第 5 点；SKILL.md 本身不在本迭代工作区内，由主 agent 手动同步。
- F04 验收标准2：结构性自检确定为复用阶段4既有推进条件核查结果，不独立重新执行——阶段4推进条件表已扩展为六项（新增并发可行性、关键路径压平结果），见 `architecture.md` §F04。

---

## model_inferred 汇总（已确认，2026-09-15）

| 卡片 | 推导内容 | 推导依据 | 状态 |
|---|---|---|---|
| F03 | SKILL.md 需要同步的具体位置范围：要求"同步"但"不要求逐字相同"（口径一致即可，不逐点核对每处"阶段3→4"引用） | demand.md D-6 只说"方案确认门挪位"，未逐条列出 SKILL.md 内需要改动的具体章节/措辞位置 | `user_confirmed`（2026-09-15） |
| F04 | 结构性自检复用阶段4/pr-planner 已有检查结果做汇总呈现，不在确认门前独立重新执行一遍 | 从 demand.md「不做什么」第2条"不改变并发可行性检查的判断逻辑和 Verify 阶段位置"以及阶段4推进条件表原文内容重叠推导 | `user_confirmed`（2026-09-15） |

以上 2 项已经用户确认，按上述方向进入阶段3（技术架构）。

---

## 疑问/越界

无发现 demand.md 内部矛盾或不清晰之处。demand.md 两段结构完整、七项决策（D-1~D-7）均为 user_confirmed，澄清依据充分，功能规格拆解过程顺利。
