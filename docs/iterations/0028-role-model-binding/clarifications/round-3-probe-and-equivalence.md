# 澄清记录 · 第 3 轮（探针通道与等价性判据）

- **迭代**: 0028-role-model-binding
- **阶段**: 1（需求收敛，主 agent 内联执行）
- **时间**: 2026-09-15 20:47~20:49
- **caller**: demand
- **触发**：三个关键决策（D-8 / D-10 / D-13）的追问反射

---

## 追问反射与决策

| # | 决策点 | 结论 | 来源 |
|---|---|---|---|
| D-14 | 前置常驻探针如何在不违反 D-5 的前提下指定模型 | **开一条只用于探针的 per-call `model` 例外**；探针完成后关闭该通道，并把这次例外记入差距记录 | `user_confirmed` |
| D-15 | 常驻探针覆盖哪些后端 | **仅 gpt 与 grok**（deepseek 已有现行集群持续运行事实 + 一次性探针） | `user_confirmed` |
| D-16 | 派发产出被截断、需两步拼接时是否算等价性缺口 | **算缺口**，逐次记入差距记录（同类现象可聚合） | `user_confirmed` |
| D-17 | D-10 等价性判据的适用范围 | **阶段 2~6 全部角色派发**（不止阶段 5） | `user_confirmed` |
| D-18 | 是否纳入并发等价 | **纳入**：阶段 5 首次派发时显式取证（同一轮连续发起 ≥2 个 `calls create` + 分别取终态） | `user_confirmed` |
| D-19 | 第二集群起哪些窗口 | **全 10 角色**（完整启动路径，兼作 D-12 对照组载体） | `user_confirmed` |

## 本轮显式留痕的冲突（→ 差距记录首条）

**C-2：前置验证与真源唯一性的冲突**。D-8（前置验常驻路径）与 D-5（派发不带 `model`）在机制上互斥：常驻路径只能跑在 oamp 节点上，而让节点使用 gpt/grok 只有"派发时带 `model`"或"启动时带 `--model`"两条路。D-14 选择了范围最窄的口子（只限探针、限时关闭、强制留痕），本条冲突本身即差距记录的第一优先级条目。
