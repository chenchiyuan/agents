验证者身份：冲刺计划审查者（产出物为 10 份 PR 切分卡 + 依赖图；标准覆盖字段齐备、功能点覆盖、文件范围互斥、验收独立性、依赖证据、无环、并发窗口——对应冲刺计划审查，而非代码审查或架构评审）
产出物：`/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0028-role-model-binding/docs/iterations/0028-role-model-binding/prs/`（`pr-001-iteration-artifacts-commit.md` ~ `pr-010-post-merge-activation-evidence.md`，共 10 个文件）
验证标准来源：主 agent 委托的 7 条标准（七字段齐备 / 功能点全覆盖 / 文件范围零重叠 / 验收标准可独立判断 / 依赖证据充分 / 依赖图无环 / 并发可行性）；功能点对照只读参照 `prd.md` 功能点索引与 `prd/F01~F13*.md`
验证日期：2026-09-15

说明：委托中的阶段标签与工作流入口描述不进入判定。只对照产出物正文与指定标准。

## 委托方指定文件的原文摘录

来源：`docs/iterations/0028-role-model-binding/deferred-demand-changes.md`（文件存在，全文 114 行）。语义头：同时承载三类条目，二级标题分区、三级标题条目。

**计数**：二级分区 3 个；三级条目 14 条 = 需求变更 0 条 + 执行方式差距 10 条（G-1~G-10）+ 澄清期登记的冲突 4 条（C-1~C-4）。四要素口径 = 现象 / 差异 / 证据 / 影响。

---

### 分区 A · `## 需求变更`（文件第 10–12 行）

条目数：**0**。正文：

> （暂无。本迭代为对已落盘需求的确定性还原；执行角色若发现需求层问题，在此分区追加。）

四要素：不适用（无条目）。

---

### 分区 B · `## 执行方式差距`（文件第 16–88 行）

条目数：**10**。每条均具备四要素字段行（齐备 10/10）。逐条摘录标题 + 关键字段行：

**G-1 · 前置验证与真源唯一在机制上互斥**（第 18–23 行）· 四要素齐备
- **现象**：前置常驻验证（D-8）要求让节点用 gpt/grok 跑一次，而正式派发的真源契约（D-5）禁止携带 `model`；两者不能同时成立，必须开例外。
- **差异**：本地 subagent 派发无此问题——宿主工具在每次派发的参数里直接带模型，不存在"角色级真源 vs 派发级覆盖"的双层语义，也不需要在流程契约里为验证动作开口子。
- **证据**：`demand.md` D-5 / D-8 / D-14；探针调用 `task-bfd83f28-2078-434b-8a16-eac455d2e999`（pb-dev，实报 `openai/gpt-5.6-luna`）、`task-39204abd-38ab-4971-b785-3eed6b0bb835`（pb-verifier，实报 `powerby/grok-4.6`），两条均在 `--model` 显式携带下完成。
- **影响**：真源链上存在一条已关闭的例外；下迭代若要在绑定生效前做任何按模型区分的验证，都会再次撞上同一冲突，需要设计一条通用且可留痕的验证通道。

**G-2 · brief 全文注入在 hub 层 A 没有文件通道**（第 25–30 行）· 四要素齐备
- **现象**：workflow-pb 的 brief 契约要求把角色定义**全文逐行注入**（`roles/prd/prd.md` 约 5.2KB 多行文本 + 固定字段），而 hub 层 A 的 `--task` 只接受字面字符串：全仓检索确认没有任何 `@file` / stdin 读参路径（`@file` 仅存在于旧命令面 `oamp task send`）。
- **差异**：本地 subagent 走宿主工具的字符串参数，长文本无转义问题；hub 侧必须靠 shell 命令替换（`--task "$(cat <brief>)"`）传递，多行内容、反引号与 `$` 都处于转义风险中。
- **证据**：`oamp/sdk/cli.js`、`oamp/sdk/surface.js` 无文件读参分支；`grep '@file'` 仅命中 `src/cli.js:10`（旧 CLI 的 `task send`）；本次阶段 2 派发的 brief 长 5267 字符，实发成功（call `task-8e84f95f-b783-4477-8054-abf0b36f31ec`）。
- **影响**：每次派发的传参方式都是隐式约定，没有契约承载；一旦 brief 里出现未转义字符就会静默截断/变形，而产物错误要等到阶段验证才发现。

**G-3 · 派发必须 chat_id 前置，而建 chat 无独立端点**（第 32–37 行）· 四要素齐备
- **现象**：`api calls create` 强制要求已存在的 `chat_id`（`API.md:702`），但对话创建只发生在 `POST /api/messages`（新建必带 `project_id`）。⇒ 想要"一个迭代一个 chat"，就必须先发生一次消息型调用，而这个调用会真实占用一个角色节点。
- **差异**：本地 subagent 派发是一次调用即完成，没有"会话容器必须先存在"的前置步骤。
- **证据**：首次建 chat 由探针消息完成（`chat-6c89902c-0a9f-4513-a299-90a7f98ae611`，`message_id` `msg-9f62bf12-5733-4b82-ba13-f26c6ba7f679`）。
- **影响**：迭代的 chat 时间线必然以一次真实调用开头（本次是探针）；若迭代首步不允许产生调用，就需要另设建 chat 机制。

**G-4 · 已实现的等待能力被重新包了一层（本次执行偏差点，已被用户指出）**（第 39–44 行）· 四要素齐备
- **现象**：主 agent 首次派发时自行搭了 subprocess 包装 + 自写轮询循环去取终态，而 hub 层 A 已实现 `api calls create --wait`（缺省上限 30 分钟）与 `api stream call`（流式直到终态）。
- **差异**：本地 subagent 的"等待终态"由宿主工具天然承担，不会诱发自建轮询；hub 侧因为返回 `submitted` 信封，容易下意识补一层循环——这正是需要被契约约束的惯性。
- **证据**：本次执行记录（`subprocess` + `poll()` 自建循环，随后被用户指出）；对照 `oamp/sdk/cli.js:18/254`（`--wait` 缺省 1800000ms）与 CLI 的 `api stream call` 条目。
- **影响**：**纠正规范**——新建调用用 `api calls create --mode block --wait <ms>`（阻塞取终态），已存在调用用 `api stream call <call_id>`，不得自建轮询或包装脚本；否则每次演示都要多出一层未受契约覆盖的代码。

**G-5 · 终态信封被截断，全文必须两步拼接**（第 46–51 行）· 四要素齐备
- **现象**：`prd` 派发的终态信封 `truncated: true`，`text` 只有 **3632** 字符；该次产出的完整正文经 `api calls transcript` 拼回为 **7078** 字符（含报告契约 1~5 全文）⇒ 拿到全文必须由消费方执行"读信封 + 取转录 + 自行拼接"两步。
- **差异**：本地 subagent 的最终产出一次性完整交回宿主，不存在"信封截断 + 自行拼接过程记录"这一层；D-10 的信号 ③（无需人工搬运中间产物即可继续）在 hub 侧**首次真实角色派发即不成立**。
- **证据**：call `task-8e84f95f-b783-4477-8054-abf0b36f31ec` —— `hub api calls get` 的 `truncated: true` 与 `text` 长度 3632；`hub api calls transcript` 拼回 7078 字符（报告契约 1~5、架构待填 A-01~A-04、MI-1~MI-6 均在其中）。按 D-16 判定为等价性缺口。
- **影响**：阶段 2~6 的每一次派发都要多一步产出回收且不可省略；后续同现象条目按 D-16 聚合到本条。**聚合记录**：第 2 次同类现象出现在阶段 2 修订轮（call `task-ad4c21ee-3bff-45ba-9b20-b2d7c1dc8edb`，`prd`，119s，终态 `truncated: true`）。

**G-6 · 过程记录存在 1001 条上限（中段过程不可回溯）**（第 53–58 行）· 四要素齐备
- **现象**：`api calls transcript` 返回 `entries` 恰好 **1001** 条、响应顶层 `truncated: true`；首条为 `started`、末条为 `result`（时间戳跨度覆盖该次调用全程 366s），且**无逐条截断标记** ⇒ 被裁剪的是**中段**过程条目。本次过程构成为 `tool_call` 751 / `thinking` 209 / `chunk` 37 / `tool_output` 2 / `started` 1 / `result` 1。
- **差异**：本地 subagent 的过程记录由宿主完整保存，不经固定条数上限；hub 侧长任务的过程记录会丢中段——"过程可回溯"发生降级。
- **证据**：`hub api calls transcript task-8e84f95f-b783-4477-8054-abf0b36f31ec` 原始响应（1001 条 / 顶层 `truncated: true` / 首末条目完整）。
- **影响**：终态正文可恢复（见 G-5），但若某次派发的关键判断发生在被裁掉的中段，事后无法复现；对"验证产物质量靠过程证据"的场景是结构性限制。

**G-7 · `--wait` 单独不阻塞：必须配 `--mode block`**（第 60–66 行）· 四要素齐备（另有附注，不计入四要素缺项）
- **现象**：`hub api calls create … --wait 900000`（不带 `--mode`）**立即返回** `state: submitted`，不阻塞到终态。实现上 `--wait` 只是**本地等待上限**，仅对声明了 blocking 形态的条目生效（`sdk/cli.js:254`：`waitMs` 作为本地上限传入）；真正取终态要么加 `--mode block`，要么事后用 `api calls get` / `api stream call`。
- **差异**：本地 subagent 派发天然是"发起即等结果"的单一语义，没有"后台/阻塞两种形态 + 一个听起来像阻塞的选项名"这一层；hub 侧选项名（`--wait`）与实际行为不一致，极易误用——本次即误用一次。
- **证据**：派发命令 `api calls create --chat-id chat-6c89902c… --agent prd --task … --wait 900000` 返回 `{"state":"submitted", …}`（call `task-ad4c21ee-3bff-45ba-9b20-b2d7c1dc8edb`）；对照 `oamp/sdk/cli.js:18/254` 与 `API.md` §3.14 的 `mode` 字段说明（`background` 立即受理 / `block` 挂起至终态）。
- **影响**：派发规范需固定写法——正式角色派发用 `--mode block --wait`；需要并行发起时用 `background` + `api stream call` 分别等。
- **附注（非四要素）**：`--mode block` 下 `call_id` 只在终态返回时才可读，故"派发即登记台账"（F09 验收 4）在阻塞形态下没有直接可用值。

**G-8 · hub 有"确认收件箱 + 控制台通知"面；本地 subagent 无对应物（本次已采纳为规范）**（第 68–73 行）· 四要素齐备
- **现象**：hub 侧存在完整链路——上游发起 `notice{kind:'confirmation_request'}` → web 进程在途表（`src/inbox.js`）→ 全局 `confirmation` 帧 → 控制台通知（`web/notify.js` 的 `EVENT_TYPES` 含 `confirmation_required`）→ 用户在控制台裁决（`api confirmations decide`）→ 回传发起方。本次主 agent 以 `main` 身份经 `uds message.send` 上浮一条确认，Router 返回 `{"accepted":true,"status":"delivered"}`，`api confirmations list` 中立即可见。
- **差异**：本地 subagent 的"叫人回来判断"只能靠会话里的文本暂停，没有**独立通知面**，也没有"在途待裁决项"这一持久状态；hub 侧把"通知"与"裁决"拆成了两个独立动作（通知 ≥ 控制台提醒，裁决 = 显式选项回传）。
- **证据**：`cfm-f1296d32-8025-4ad8-a367-f08579e53517`（`request_kind: question`，`chat_id=chat-6c89902c…`，投递 `delivered`）；**回传闭环**：用户裁决后 web 回传 `notice{kind:'confirmation_decision', option_ids:['seen'], confirmation_id:cfm-f1296d32…}`（`message_id=ntc-fa1d54c8-f7f0-46a9-8d1b-846498b53055`，`from=web`）至 `main` 会话，裁决后在途表回空 `{"confirmations":[]}`；`oamp/src/inbox.js`（在途表 5 个导出函数）；`oamp/web/notify.js` 的 `EVENT_TYPES`。
- **影响**：**新增执行方式规范**——用户决策点走"双通道"：① hub 确认收件箱（通知 + 可裁决）；② 会话内暂停格式（保证判断内容本身有出处）。**裁决回传要求发起方保持在线身份**：一次性 CLI 调用收不到回传，故主 agent 需在会话内核持有常驻 `main` 会话。

**G-9 · 单次调用有 30 分钟节点侧硬上限，调用面无法覆盖**（第 75–81 行）· 四要素齐备
- **现象**：`oamp/src/agent.js:31-32` 写死 `DEFAULT_OMP_TIMEOUT_MS = 1800000` 与 `MAX_TIMEOUT_MS = 1800000`（30 分钟）；任务体支持 `timeout_ms`，但**调用面不传该字段**（`oamp/src/web.js` 组装任务时无 `timeout_ms`）⇒ 经 hub 派发的每一次执行都固定在 30 分钟。客户端 `--wait` 可设更长（本迭代已按用户要求改为 60 分钟），但它只约束"本 agent 等多久"，不约束节点侧的执行上限。
- **差异**：本地 subagent 没有"单次执行 30 分钟"这一硬限，长任务只受会话生命周期约束；hub 侧的长任务会在 30 分钟处被节点判超时，而调用面没有"这次允许跑 60 分钟"的入口。
- **证据**：`oamp/src/agent.js:31-32`（常量；旁证 `timeout_ms 需为 1~600000 正整数` 的校验文案）；`oamp/src/web.js` 无 `timeout_ms` 组装；`oamp/sdk/cli.js:18` 的 `--wait` 缺省 1800000 与节点缺省同值。本迭代 `architect` 派发（`task-191d2e14…`，21:09 起）工具调用已达 900+ 条，属该类长任务。
- **影响**：① 本迭代等待口径 = 客户端 60 分钟 + 节点侧实际 30 分钟天花板（两者不一致；超时后调用仍在服务端进行，客户端本地中止 ≠ 调用取消）；② 放开该上限必须改 `oamp/src/agent.js`，与本迭代 F13 验收 1 的冻结面冲突 ⇒ **登记为下一迭代候选**：`MAX_TIMEOUT_MS` 提升到 ≥60 分钟，并给调用面补 `timeout_ms` 透传入口。
- **实证（同条补记）**：`task-191d2e14-e852-412d-ab90-e6fc00092928` 于 21:09 发起，**21:39 被节点侧判 `failed`**（`error: timeout`、`duration_ms: 1802661`）。

**G-10 · 单一 chat 跨角色派发时，控制台的对话归属标签固定为首条消息的 agent**（第 83–88 行）· 四要素齐备
- **现象**：`chat.agent_id` 由**建 chat 的那条消息**决定（本迭代 = gpt 探针发给 `pb-dev`），此后同一 chat 内的所有派发（prd / architect / verifier…）都正确落在各自节点（消息级 `agent_id` 与调用 `agent` 字段均正确），但控制台的对话列表/对话头只显示 chat 级归属 ⇒ 显示为 `pb-dev`，与本迭代"多角色共用一 chat"的事实不一致；用户据此产生"是不是派错 agent 了"的疑问（本次由用户发现）。
- **差异**：本地 subagent 的派发在会话里逐条以工具调用呈现，目标角色与调用一一对应，没有"容器级归属"这一层；hub 侧 chat 是容器，其归属字段是单值且**无修改端点**（`api chats` 只有 list / get / close / archive / activate / rename，`rename` 只改 title）。
- **证据**：`hub api chats get chat-6c89902c…` → `chat.agent_id = pb-dev`，而同一 chat 内 `messages[].agent_id` = `pb-verifier` / `pb-prd` / `pb-architect`，`hub api calls list` 的 `agent` = dev / verifier / prd / architect；`oamp/sdk/surface.js` 的 chats 5 个端点中确无改归属项。
- **影响**：① 已用既有 `api chats rename` 把该 chat 标题改为「【0028 迭代 · 多角色】role-model-binding（阶段 2~6 共用同一 chat）」；② 人工巡查时**必须以消息级 / 调用级 agent 字段为准**；③ **下迭代候选**：控制台在对话内按消息级 agent 呈现，或支持多角色对话的归属表达。

---

### 分区 C · `## 澄清期登记的冲突`（文件第 92–114 行）

条目数：**4**。就地四要素齐备 **2/4**（C-1、C-4 齐备；C-2、C-3 为交叉引用，就地无四要素字段行）。

**C-1 · 默认模型 id 与 provider 清单不同名**（第 94–99 行）· 四要素齐备
- **现象**：`~/.omp/agent/config.yml` 的默认 `deepseek/deepseek-v4-flash:high` 在 `~/.omp/agent/models.yml` 的 deepseek provider 下**不存在同名 id**（清单里是 `deepseek-chat` / `deepseek-reasoner` / `deepseek-v4.1-flash`），靠 fuzzy match 才解析成功。
- **差异**：不涉及 hub 与 subagent 的差异，属 omp 环境配置面的事实。
- **证据**：一次性探针 `deepseek/deepseek-v4-flash:high` → `OK`（2.3s），即 fuzzy match 生效；`models.yml` 全文。
- **影响**：默认值不可静态校验；若哪天 fuzzy 规则收紧，全集群默认模型会一起失效，而配置文本看不出问题。

**C-2 · 前置常驻验证与真源唯一冲突**（第 101–103 行）· 就地四要素**不齐备**
- 全文：与 **G-1** 同一条冲突的澄清期登记，此处不重复现象描述；处置见 `demand.md` D-14。
- 就地无「现象 / 差异 / 证据 / 影响」四行。

**C-3 · 建 chat 无独立端点**（第 105–107 行）· 就地四要素**不齐备**
- 全文：与 **G-3** 同一条冲突的澄清期登记；处置见 `demand.md` D-21。
- 就地无「现象 / 差异 / 证据 / 影响」四行。

**C-4 · 主集群跑的是主工作区代码与配置，迭代改动只在 PR worktree / 迭代分支可见**（第 109–114 行）· 四要素齐备
- **现象**：在跑的主集群由主工作区的 `cluster.json` 与 `oamp/` 代码启动，迭代的改动落在 PR worktree → 迭代分支，主集群看不见；直接改主工作区又与 PR 流程相冲。
- **差异**：本地 subagent 在"一次会话、一个工作区"内工作，不存在"运行中的真实服务使用另一份检出"这一层。
- **证据**：`git worktree list`（主工作区 `main@162682d` 与迭代工作区并存）；`cluster.js` 的 tmux 窗口 cwd = 配置文件所在目录。
- **影响**：每次涉及"运行态行为"的迭代都要额外回答"实证跑在哪个实例、用哪份检出"，并为此付出一个第二集群的生命周期；本次处置见 D-13 与 D-20。

---

**摘录可数结论**：分区 3；条目 0+10+4=14；执行方式差距四要素 10/10 齐备；澄清期冲突就地四要素 2/4 齐备（C-2/C-3 交叉引用 G-1/G-3）。

## 逐项判定

- **1. 七字段齐备**：pass
  证据：10 个文件均含且仅按此顺序出现七个二级标题 `## 上下文摘要`、`## 涉及功能点`、`## 文件范围`、`## 验收标准`、`## 参考资料`、`## depends_on`、`## batch`，各节正文非空。
  - 上下文摘要：各文件第 3–5 行起均有非空段落（例：`pr-001` 第 5 行；`pr-010` 第 5 行）。
  - 涉及功能点：均为 `- Fxx` 列表（`pr-001` 第 9–10 行 F08/F13；`pr-006` 第 9–11 行 F05/F06/F07；`pr-008` 第 9–11 行 F09/F10/F11）。
  - 文件范围：均有至少一条路径（`pr-003` 第 13 行仅本 PR 文件；`pr-005` 第 13–14 行 `cluster.second.json` + 本 PR 文件）。
  - 验收标准：均 ≥1 条 checkbox（最少 `pr-009` 5 条，第 19–23 行；`pr-008` 10 条，第 21–30 行）。
  - 参考资料：均有路径/条款列表。
  - `depends_on`：无依赖者显式写「（无）」——`pr-001` 第 42 行、`pr-002` 第 35 行、`pr-003` 第 33 行、`pr-007` 第 39 行；有依赖者均有条目（见标准 5）。
  - `batch`：均为非空整数：pr-001/002/003 = 1；pr-005 = 2；pr-006 = 3；pr-007 = 4；pr-004 与 pr-008 = 5；pr-009 = 6；pr-010 = 7。

- **2. 功能点全覆盖**：pass
  证据：`prd.md` 第 34–50 行索引列出 F01~F13；`prd/` 目录 13 张卡文件名与卡头 `# F01`~`# F13` 一一对应。各功能点在 PR「涉及功能点」中的出现：
  | 功能点 | 出现位置 |
  |---|---|
  | F01 | `pr-003` 第 9 行 |
  | F02 | `pr-004` 第 9 行 |
  | F03 | `pr-002` 第 9 行 |
  | F04 | `pr-005` 第 9 行 |
  | F05 | `pr-006` 第 9 行 |
  | F06 | `pr-006` 第 10 行 |
  | F07 | `pr-006` 第 11 行 |
  | F08 | `pr-001` 第 9 行；`pr-010` 第 9 行 |
  | F09 | `pr-008` 第 9 行 |
  | F10 | `pr-008` 第 10 行 |
  | F11 | `pr-008` 第 11 行 |
  | F12 | `pr-007` 第 9 行 |
  | F13 | `pr-001` 第 10 行；`pr-009` 第 9 行 |
  无遗漏。F08/F13 被两份 PR 同时点名不构成漏覆盖。

- **3. 文件范围零重叠**：pass
  证据：各 PR「文件范围」声明的写入路径集合互斥（规范化后）：
  | PR | 写入路径 |
  |---|---|
  | pr-001 | `demand.md`、`prd.md`、`prd/F01~F13*.md`（13 卡）、`architecture.md`、`clarifications/round-1~4.md`、`history.md`、`status.md`、`prs/pr-001-*.md` |
  | pr-002 | `cluster.json`、`prs/pr-002-*.md` |
  | pr-003 | `prs/pr-003-*.md` |
  | pr-004 | `prs/pr-004-*.md` |
  | pr-005 | `<工作区>/cluster.second.json`、`prs/pr-005-*.md` |
  | pr-006 | `prs/pr-006-*.md` |
  | pr-007 | `deferred-demand-changes.md`、`prs/pr-007-*.md` |
  | pr-008 | `prs/pr-008-*.md` |
  | pr-009 | `prs/pr-009-*.md` |
  | pr-010 | `prs/pr-010-*.md` |
  无同一路径被两个「文件范围」同时声明。`status.md` 仅 pr-001 写入；pr-006/008/009/010 将其列为排除/主 agent 滚动维护。`deferred-demand-changes.md` 仅 pr-007 写入；pr-001 第 23 行排除声明与之对齐。`cluster.json` 仅 pr-002。
  `depends_on` 内无「由 pr-00X 写入」类排除声明（排除句均在「文件范围」节，不在 `depends_on`），故不存在 depends_on 排除句与文件范围的实质冲突。

- **4. 验收标准可独立判断**：partial
  证据：10 个 PR 均有 ≥1 条验收标准。逐 PR 拆开：
  **可通过本 PR 范围判定的（通过侧）**：
  - pr-001 第 27–30 行：tracked / 纯新增 / `--name-only` 只含上列路径 / 无 `??` ——判定对象即本 PR 文件范围。
  - pr-002 第 18–24 行：`cluster.json` 两键取值、8 角色无 `model`、`git diff main -- cluster.json`、加载检查 ——`cluster.json` 在本 PR 范围。
  - pr-003 第 17–24 行：三条回执五字段落在本 PR「验收证据」（第 39–41 行）；失败时在本 PR 登记「待 F12 收录」（第 21 行）不要求 pr-007 已完成才能判本条真伪。
  - pr-004 第 17–20、22–23 行：两条六字段回执、不同节点、实报后端、MI-2 补记、同属迭代 chat、不新建探针文件 ——均可落在本 PR 证据小节。
  - pr-005 第 22–27 行：tmux 双 session、`lsof :7789`、socket 落点、`api agents --port 7789` 10 实例、主集群零启停、启动记录写入本 PR。
  - pr-006 第 21–28 行：三条实报与 chat 归属写入本 PR「验收证据」（第 45–47 行）。
  - pr-007 第 20–27 行：判定对象 `deferred-demand-changes.md` 在本 PR 范围。
  - pr-008 第 21–25、27、29–30 行：通道/寻址/真源/台账/迭代 chat 归属/等价性三信号/并发取证 ——证据写入本 PR；核对对象为 `status.md` 与调用面，不是另一 PR 的写入面。
  - pr-009 第 22–23 行：`cluster.second.json` untracked 登记与 F13 边界条款可写在本 PR。
  - pr-010 第 20–24 行：重启命令/两条实报/收口提交可落本 PR 证据小节。
  **不能只看本 PR 范围判定真伪的（不通过侧）**：
  - `pr-004` 第 21 行「例外通道唯一性」：明文「逐条命令形态的核对结论取自 pr-008」。
  - `pr-005` 第 20–21 行：`diff cluster.json cluster.second.json` 恰 2 行、副本两处 `model`「与 pr-002 写入 … 逐字一致」——`cluster.json` 不在本 PR 文件范围。
  - `pr-008` 第 26 行「同址」：判定对象明文为「pr-006 的三条实报」。
  - `pr-008` 第 28 行「缺口登记」：要求「核对 pr-007 的记录中有对应条目（G-5）」。
  - `pr-009` 第 19 行「改动面清单」：`git diff --name-only main...iteration/0028-role-model-binding` 覆盖整条迭代分支，超出本 PR 唯一写入路径 `prs/pr-009-*.md`；上下文摘要第 5 行亦写「须在全部 PR 已合入迭代分支之后」。
  - `pr-009` 第 21 行：明文「复用 pr-004 落盘的两条常驻探针回执」。
  - `pr-010` 第 19 行「合并存在」：要求 main 上可读 `demand.md` / `prd/**` / `deferred-demand-changes.md` / `prs/*.md` 等全量迭代产物，超出本 PR 文件范围（仅 `prs/pr-010-*.md`）。
  混合情况，故 partial（不是程度副词：上列通过侧成立，不通过侧不成立）。

- **5. 依赖证据充分**：pass
  证据：`depends_on` 共 7 条边，每条含理由 + 可核对锚点，无「仅 PR 名」或空泛理由：
  1. `pr-004` 第 35 行 → `pr-008-dispatch-contract-audit.md`。理由：F02 验收 4 与 F09 验收 3 同源、全量命令形态由 pr-008 产出。锚点：`prd/F02-resident-backend-probes.md` 验收 4 原文；`prd/F09-dispatch-channel-and-source-of-truth.md` 验收 3。
  2. `pr-005` 第 38 行 → `pr-002-role-model-binding.md`。理由：副本定义为 `cluster.json` 逐字副本 + 两键差异，缺 pr-002 则两处绑定为空。锚点：`oamp/src/cluster.js:200-201`（`planWindows` 追加 `--model`）。
  3. `pr-006` 第 39 行 → `pr-005-second-cluster-bring-up.md`。理由：`--port 7789` 与第二集群库均为 pr-005 产物。锚点：`oamp/src/cluster.js:190`（`config.web.port`）；`oamp/src/config.js:154`（`dbPath` 按包根推导）。
  4. `pr-008` 第 42 行 → `pr-006-second-cluster-binding-evidence.md`。理由：F10 验收 2 的判定对象是 pr-006 三条 `call_id`。锚点：`architecture.md` §4 A-05 第 2 条（`chat_id` / `project_id` / 三条 `call_id` 记入该 PR 验收证据）。
  5. `pr-009` 第 36 行 → `pr-002-role-model-binding.md`。理由：F13 验收 1 路径清单首项即 `cluster.json`。锚点：`prd/F13-existing-surface-unchanged.md` 验收 1 原文。
  6. `pr-009` 第 37 行 → `pr-004-resident-backend-probes.md`。理由：F13 验收 2 复用 F02 两条回执、本卡不重复取证。锚点：`prd/F13` 验收 2 原文；`prd/F02` 验收 1 字段形态。
  7. `pr-010` 第 37–38 行 → `pr-001` 与 `pr-002`。理由：未跟踪文件不参与合并；重启后实报唯一来源是 `roles.<role>.model`。锚点：`git status --porcelain` 对迭代目录报 `??`；`prd/F08` 验收 1；`oamp/src/cluster.js:200-201`。
  pr-001/002/003/007 为「（无）」，无待证条目。

- **6. 依赖图无环**：pass
  证据：有向边（本文件 → 依赖文件）仅：
  - pr-004 → pr-008
  - pr-005 → pr-002
  - pr-006 → pr-005
  - pr-008 → pr-006
  - pr-009 → pr-002
  - pr-009 → pr-004
  - pr-010 → pr-001
  - pr-010 → pr-002
  唯一长链：`pr-009 → pr-004 → pr-008 → pr-006 → pr-005 → pr-002`。另有 `pr-009 → pr-002`、`pr-010 → pr-001|pr-002`。从任一节点沿边无法回到自身。pr-001/002/003/007 为源点或孤立点。无环。

- **7. 并发可行性**：pass
  证据：祖先 = 沿 `depends_on` 可达的依赖；后继 = 反向依赖。互不可达 = 彼此都不是对方祖先/后继。每个 PR 至少找到一个互不可达的其他 PR：
  | PR | 祖先 | 后继 | 一个互不可达例 |
  |---|---|---|---|
  | pr-001 | ∅ | pr-010 | pr-003 |
  | pr-002 | ∅ | pr-005, pr-006, pr-008, pr-004, pr-009, pr-010 | pr-001 |
  | pr-003 | ∅ | ∅ | pr-001（相对全图孤立，与任意其他 PR 互不可达） |
  | pr-004 | pr-008, pr-006, pr-005, pr-002 | pr-009 | pr-001 |
  | pr-005 | pr-002 | pr-006, pr-008, pr-004, pr-009 | pr-007 |
  | pr-006 | pr-005, pr-002 | pr-008, pr-004, pr-009 | pr-003 |
  | pr-007 | ∅ | ∅ | pr-002 |
  | pr-008 | pr-006, pr-005, pr-002 | pr-004, pr-009 | pr-010 |
  | pr-009 | pr-004, pr-008, pr-006, pr-005, pr-002 | ∅ | pr-007 |
  | pr-010 | pr-001, pr-002 | ∅ | pr-007 |
  并发重叠窗口存在：例如 batch 1 的 pr-001 / pr-002 / pr-003 两两互不可达；pr-007 与除自身外的全部节点互不可达，可与任一链并行。

## 汇总

- pass: 6 项
- fail: 0 项（需返工）
- partial: 1 项（部分需返工）
- blocked: 0 项（缺信息无法判定）

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| `pr-008` 验收第 28 行要求核对 `pr-007` 的 G-5 条目（F11 验收 2） | `pr-008` 的 `depends_on`（第 40–42 行）只列 `pr-006`，未列 `pr-007` | 重新评估实现：要么把该核对从 pr-008 验收中移出，要么补 `depends_on: pr-007`（由主 agent 决定） |
| `prd.md` 第 141 行：F08「要求全部 PR 已合入迭代分支、迭代分支已合入 main」；`pr-010` 第 19 行验收 1 列出 `deferred-demand-changes.md` 与 `prs/*.md` | `pr-010` `depends_on`（第 37–38 行）只列 pr-001 与 pr-002，未列 pr-003~pr-009 | 重新评估实现：收口 PR 的依赖边是否应覆盖「合并存在」所枚举的写入方 |
| `prd.md` 第 139 行：F05/F06 依赖 F03+F04 | F05/F06/F07 合在 `pr-006`，其 `depends_on` 只列 pr-005（F04）；F03 仅经 pr-005→pr-002 传递 | 按实现更新文档：写明 F03 为传递依赖，或在 pr-006 显式列出 pr-002 |
| F12 验收 4（`pr-007` 第 23 行）：C-1~C-4 逐条收录 | `deferred-demand-changes.md` C-2/C-3（第 101–107 行）以交叉引用收录，就地无四要素字段 | 按实现更新文档：允许「澄清期冲突」用交叉引用满足「收录」；或要求 C-2/C-3 就地补四要素 |

## 下一迭代候选

> 本轮发现的优化点、边界问题或遗留限制。不在当前验收标准范围内，供主 agent 和 planner 决策是否纳入下一迭代。

- 验收独立性与冻结/收口类 PR 的固有冲突：`pr-009` 的整分支 `git diff` 与 `pr-010` 的「合并后产物可读」在定义上跨文件范围；若下迭代仍用「只看本 PR 范围判定」作为切分门，需要单独口径，否则会稳定打出 partial。
- `pr-004` 把「例外通道唯一性」的全量核对外包给 `pr-008`，本 PR 只落一侧事实：同源判据拆到两张卡，验收条目与 `depends_on` 方向相反于「谁先完成」（pr-004 batch 与 pr-008 同为 5，但 004 依赖 008）。
- 节点 30 分钟硬上限（G-9）与控制台 chat 级归属单值（G-10）已在委托方指定文件中登记为下迭代候选，本报告不重复展开，仅指出其仍不在本 7 条切分标准范围内。
- C-2/C-3 就地四要素缺失（交叉引用）会在 F12 执行时被重新解释；切分阶段未要求执行 F12，但执行角色会撞上「收录」口径。

## 结论

PASS

注：偏差记录不影响结论判定——有偏差不等于 FAIL，偏差被记录即满足交付条件。本报告 7 条中无 fail、无不可绕过的 blocked；唯一 partial 已拆开通过侧/不通过侧。主 agent 可继续；若要求标准 4 升级为全员独立可判定，则需返工上述 7 条不通过侧验收条目，而非重做整张图。
