# F12 · 执行方式差距记录

**来源**：demand.md 做什么#5（"逐条记录执行方式差距"）；达到什么效果#5 / #6；决策 D-7（载体）/ D-11（分区 + 四要素）/ D-14（例外留痕）/ D-16（截断缺口）

## 用户价值

本迭代换了派发通道，凡是"和原来不一样的地方"都落成白纸黑字（现象 / 差异 / 证据 / 影响）——下一个迭代才有决策依据，而不是散落在会话记忆里。

## 验收标准

1. `docs/iterations/0028-role-model-binding/deferred-demand-changes.md` 存在（本迭代首次写入时建立该文件），且含**二级标题分区**「执行方式差距」（D-11）。
2. 该分区**每条含四要素**：**现象** / **差异** / **证据** / **影响**；其中「证据」字段含可复核的路径、命令或 `call_id`，不接受"见上文""同上"式指代（效果#5）。
3. **分区不语义混载**：既有「需求变更」条目沿用既有格式（含"为什么判定为需求层面问题"字段），「执行方式差距」条目**不得**被写成需求变更；两类条目在标题上即可分辨。**该文件已按三分区落地**：`## 需求变更` / `## 执行方式差距`（G-1~G-6）/ `## 澄清期登记的冲突`（C-1~C-4）[已确认 MI-6]。
4. **C-1 ~ C-4 收录**（效果#6）：四条澄清期冲突逐条收录，或对任一条给出"不成立"的理由且逐条可核对。四条内容以 `demand.md` §四 的表述为准（C-1 默认 id 靠 fuzzy 解析 / C-2 前置验证与真源唯一性机制互斥 / C-3 建 chat 无独立端点 / C-4 主集群与迭代改动的可见性错位）。
5. **D-14 例外留痕**：收录探针期 per-call `model` 例外——范围（只探针期）、时点、关闭状态各一条可核对表述（D-14 原文要求"把这次例外记入差距记录"）。
6. **等价性缺口收录**（D-16）：F11 验收 2 判定的缺口逐次（或按同类聚合）有对应条目；无缺口时以"无缺口"明确表述，不留空。
7. 本文件的写入**不修改 `demand.md`**，也不向其追加内容（红线）。

## 边界（不包含）

- 不修改 `demand.md`（红线）；不改 `status.md` / `history.md` / `clarifications/` 的既有内容。
- 不把差距记录当作本迭代的"需求变更申请"——记录即结论，处理留给下一迭代（D-7 语义）。
- 不要求为每条差距给出解决方案、修复计划或技术选型。
- 不要求收录与执行方式无关的既有遗留问题（本迭代未发现即不写）。
- 不承担等价性判据本身的判定（F11 判，本卡只承载记录）。

## 架构维度

无待填项（D-11 已定形态：二级标题分区 + 四要素；文件位置由工作流的文档路径协议给定）。

## model_inferred

- **已确认 · MI-6**（验收 3 的分区口径）：「执行方式差距」与「需求变更」两类条目不语义混载；该口径已由文件的三分区落地形态满足（R-7）。
