# 工作流进度

**工作流**: workflow-pb v0.8.0
**迭代**: 0015-hub-orchestration-open-api
**当前阶段**: PR 实现（阶段 5）
**迭代分支**: iteration/0015-hub-orchestration-open-api
**状态**: 进行中
**history**: 开启

## 阶段状态

| # | 阶段 | 完成 | 已验证 | 备注 |
|---|---|---|---|---|
| 1 | 需求收敛 | ✅ | ⬜ | demand.md v1.0.0（**用户改判收窄**：hub 不启动/不关闭 agent，只做连接与路由中心）；范围 = 只读 agent 列表 + 心跳两档 + 开放接口与 API.md；零待确认项 |
| 2 | 功能规格 | ✅ | ⬜ | 8 卡 F01~F08（列表面板/自动更新/心跳两档/接口面/全局事件流/统一错误/API 文档/连接语义回归）；MI-01~MI-08 + 两条口径裁决（租约联动、列表字段保留）已确认 |
| 3 | 技术架构 | ✅ | ⬜ | architecture.md v1.0.0（799 行，AR-01~AR-08 共 26 子项全填）；**L1-01 已确认**（协议两个可选字段：`next_interval_ms` + `lease_follows_interval`）；三条硬契约 = 心跳阈值联动公式 / 全局事件流 `/api/events` / 统一错误契约 `{error,code}`；R-1 裁决保持 60s |
| 4 | PR 规划 | ✅ | ⏸ | 3 PR（pr-001 心跳两档 / pr-002 开放接口+顶栏面板 / pr-003 文档）；**pr-001 ∥ pr-002 无依赖可并发** → pr-003 fan-in 依赖两者；文件范围互斥；G2+G3 合并为 pr-002 的裁决已评估（web.test.js 互斥下的唯一无环解） |
| 5 | PR 实现 | ⏸ | ⬜ | 首批并发派发：pr-001 ∥ pr-002 |
| 6 | 独立验证 | — | — | 按需触发，不计入线性进度 |

## PR 实现子状态（阶段 5 展开）

| PR 文件 | depends_on | 状态 | worktree 分支 | 已合并 | 槛位状态 |
|---|---|---|---|---|---|
| pr-001-heartbeat-two-tier.md | （无） | ✅ | (已清理) | ✅ | 已释放 |
| pr-002-open-api-and-agent-panel.md | （无） | ✅ | (已清理) | ✅ | 已释放 |
| pr-003-api-doc.md | pr-001, pr-002 | ⏸ | feat/0015-pr-003-api-doc | ⬜ | 占用 |

## 并发配置（阶段 5）

**起始并发数**: 3
**硬上限**: 5
**当前有效上限**: 3
**累计槛位释放次数**: 2
**已派发总数**: 3

> 本迭代存在**真实并发分支**（pr-001 与 pr-002 互不依赖）→ 阶段 6 必须核查「并发调度真实执行证据」三项。

## 待确认项

- [x] 需求澄清（用户 2026-09-11，含一次改判）：① **hub 不启动、不关闭 agent**（用户改判：职责简单，先单纯做连接/路由中心）→ 需求 1 取消、需求 2 的"一键关闭"取消 ② 保留：顶栏计数**可点击展开只读 agent 列表** ③ 心跳 = **空闲降频**（活跃 10s / 空闲 60s，任务时立即恢复）④ 开放接口 = **全能力接口化 + `oamp/API.md`**（含可粘贴示例）⑤ 双向 = 发送指令 + 订阅事件流；不纳入 tasks；不鉴权、仅同机

## 用户确认记录（Gate）

- **L1-01 协议可选字段增补**（`agent.heartbeat` 新增可选参数 `next_interval_ms`；`agent.register` 回包新增 `lease_follows_interval: true`）→ **2026-09-11 用户确认采纳**（主 agent 转呈并以「采纳（推荐）」选项拍板；用户原话选项 = 采纳）。
- **R-1 空闲档间隔**：保持 **60s**（不采用 65s；用户表述为"1 分钟"）→ 2026-09-11 用户确认。
- **需求收窄（2026-09-11 用户改判）**：hub 不启动、不关闭 agent（只做连接/路由中心）→ 需求 1 取消、需求 2 的"一键关闭"取消。
- **TC-05 tasks 接口不纳入 / TC-06 不鉴权仅同机 / TC-07 文档 = oamp/API.md** → 2026-09-11 用户确认。
- borderline 裁决（主 agent，2026-09-11）：统一错误契约 `{error,code}` 的对外响应体横切变更按 **L2** 处理（`error` 字段与既有文案逐字不变 + 纯附加字段 + F06 本身为用户确认需求）。

## 更新日志

- 2026-09-11: 迭代启动（四项需求：双连接方式 / agent 列表与一键关闭 / 心跳自适应 / 开放接口与文档）。前置：0014-chat-rename 已合并 main（226/226）。
- 2026-09-12 11:43:10: 首批并发（pr-001 ∥ pr-002）双双验收 PASS 并合并（迭代分支 235/235；pr-002 的 /api/agents 无参响应与旧版逐字节相同）；pr-003（文档）依赖满足，已派发。
