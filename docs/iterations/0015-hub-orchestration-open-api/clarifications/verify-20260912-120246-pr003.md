# verify-20260912-120246-pr003.md — PR-003（`oamp/API.md` 接口文档 + README 同步）独立验证

**验证者身份**（反射结果）：**接口文档评审者（主视角）+ 契约测试工程师（兼）**——
主视角是"专门评审对外 HTTP 契约文档的评审者"：判断文档能否让一个只读文档、不读源码的客户端接进来，逐字段描述是否与真实契约同形；
兼视角是契约测试工程师：不采信文档自述，一律用"隔离环境实跑 + 代码逐行对照"判定文档的每一条断言。
**不接收任何执行过程上下文**（未读取执行者报告、未使用其自述结论、未参考其测试结果）。

**产出物**：

- worktree：`.pb-agents/worktrees/0015-pr-003-api-doc`（分支 `feat/0015-pr-003-api-doc`，commit `7cbe43a`，`git status` 干净）
- `oamp/API.md`（新建，849 行）
- `oamp/README.md`（修改，307 行）
- 对照基准（只读）：`oamp/src/web.js`（771 行）、`oamp/src/transport.js`、`oamp/src/persist.js`、`oamp/src/registry.js`、`oamp/src/router.js`、`oamp/src/config.js`、`oamp/web/*`

**验证标准来源**：主 agent 委托 brief 的 8 条验证标准 + PR 卡 `prs/pr-003-api-doc.md` 的 7 条验收标准 + `architecture.md` §6.4 / §9.3 / §9.4

**验证日期**：2026-09-12

**验证环境（自建隔离，全程未触碰主仓库集群）**：

- 临时 `OAMP_SOCKET=/tmp/oamp-verify-pr003/router.sock`、`OAMP_DB=/tmp/oamp-verify-pr003/sql.db`、端口 `57298`/`57299`
- 进程：`oamp router start` + `oamp web start --port 57298` + `oamp agent start demo-1`（另起 demo-2/3/4 用于上下线事件）
- 示例逐条从 `API.md` 抽取执行，仅替换端口（文档自身声明的允许替换项）

---

## 逐项判定

### 标准 1：PR 卡验收逐条判定（附可定位证据）

- **AC1（`API.md` 存在 + README HTTP 段有可点开的链接）**：**pass**
  证据：`oamp/API.md` 存在（849 行）；`README.md:169` 在「HTTP API」块内写入 `**[API.md](API.md)**`；链接目标 `API.md` 相对 `oamp/` 解析存在（`fs.existsSync` = true）。

- **AC2（覆盖 10 条 API，每条含方法+路径、参数表、真实成功响应示例、全部 `code` 错误清单）**：**pass**
  证据（机械比对，可复跑）：
  - 路由集合：从 `web.js` 抽取 `req.method && p ===/startsWith/endsWith('/api/…')` 得 **10 条**；从 `API.md` §3 清单表抽取得 **10 条**；两集合归一化后**完全相等**（`/api/stream?chat_id=<id>` 归一为 `/api/stream`）。
  - 逐接口结构：10/10 个 `### 3.N` 小节均含「参数（或请求体）」块 + `**成功响应** \`200\`` + JSON/SSE 代码块 + `**错误**` 块。
  - 逐接口错误清单与代码推导集合**逐条相等，0 处不符**：3.1 `{INVALID_PARAM,UPSTREAM_UNAVAILABLE}`｜3.2 同｜3.3 `{NOT_FOUND,UPSTREAM_UNAVAILABLE}`｜3.4 同｜3.5 `{UPSTREAM_UNAVAILABLE}`｜3.6 `{NOT_FOUND,CONFLICT,UPSTREAM_UNAVAILABLE}`｜3.7 全 5 码｜3.8 `{INVALID_PARAM,PAYLOAD_TOO_LARGE,CONFLICT,UPSTREAM_UNAVAILABLE}`（**无 `NOT_FOUND`，与代码一致**：新 `chat_id` 直接新建）｜3.9 `{INVALID_PARAM}`｜3.10 `{}`。
  低severity 备注（见 D7）：`GET /api/chats/<chat_id>` 的路径参数以文字「参数：无（chat_id 在路径中，按 URI 解码）」表达，未做成 `类型/必填/默认` 表。

- **AC3（统一约定含 `{error, code}` + 5 行映射表 + "同类错误跨接口一致" + 与 `ERR_CODE` 一一对应）**：**pass**
  证据：`API.md:80` 为 `{ "error": …, "code": "NOT_FOUND" }` 形态 + `:83-86` 字段语义表；`API.md:92-96` 映射表 5 行；`API.md:98-100`"同类错误跨接口一致"段；机械比对：文档 5 键 == `web.js` 的 `ERR_CODE` 5 键 == `sendError` 调用点推导的 code→status 映射（`INVALID_PARAM`=400 / `NOT_FOUND`=404 / `CONFLICT`=409 / `PAYLOAD_TOO_LARGE`=413 / `UPSTREAM_UNAVAILABLE`=502），**逐键逐值相符**；5 个状态码全部在实跑中真实出现（400/404/409/413/502 各至少 1 次）。

- **AC4（4 类 chat 事件 + 2 类全局事件 + `retry: 1000` + 15s keepalive，与 publish 调用点逐条对照）**：**pass**
  证据：`API.md` §4.1 表 4 行（名/`data` 字段/触发时机/入库）+ §4.2 表 2 行（名/载荷/触发时机）+ 判定源/时延/播种基线/键隔离；机械比对文档事件名集合 `{message, chat_state, task_update, notice, agent_online, agent_offline}` **与 `transport.publish`/`publishGlobal` 的 `type:` 集合完全相等（6==6）**；`retry: 1000` 与 15s keepalive 与 `transport.js:9-10`（`RETRY_MS=1000`、`DEFAULT_HEARTBEAT_MS=15000`）逐值一致；实跑观测到 `retry: 1000` 首帧与 `: keepalive` 帧（见标准 3 证据）。

- **AC5（6 组示例原样复制可执行，唯一占位符为 `<chat_id>` 与示例实例名）**：**partial**
  **通过的子项**：① §5.1（`?state=online`）原样执行输出与文档同形；② §5.2（列表/搜索/归档视图）输出 `{"chats":[],"total":0,"limit":50,"offset":0}` 与文档逐字一致；④ §5.4 变体 A/B + shell 演示三例均返回 `{"chat_id":…,"task_id":"task-…","message_id":"msg-…","warning":null}`，与文档同形；⑤ §5.5 订阅帧**逐帧逐字节与文档期望一致**（`retry: 1000` → `message`(in) → `chat_state working` → `task_update stdout/line` → `message`(out) → `chat_state completed`）；⑥ §5.6 订阅端实收 `event: agent_online` → `event: agent_offline`，载荷与文档一致。附加示例 §5.7/§5.8/§5.9 亦全部实跑通过（§5.8 四条错误输出与文档**逐字节相同**）。
  **不通过的子项**：③ §5.3「读消息」——命令硬编码 `chat-demo-1`，而**文档中没有任何示例创建该对话**（§5.4 建的是 `chat-demo-shell`/`chat-demo-model`/`chat-demo-oneshot`），在干净环境按 §5 前置原样执行得
  `{"error":"chat 不存在: chat-demo-1","code":"NOT_FOUND"}` / `HTTP 404`，与文档「期望输出」的 200 + 两条消息**不符**。改读实际创建的 `chat-demo-shell` 则字段与文档**逐字段一致**（见标准 2④）。详见偏差 D6。

- **AC6（README 四处同步且与 `API.md` 不冲突）**：**pass**
  证据：`README.md:127`（顶栏可点击展开在线列表，含实例标识/在线状态/最后心跳时间、随上下线自动更新）｜`README.md:156`（`GET /api/agents` 行标注 `?state=online`）｜`README.md:165`（新增 `GET /api/events` 行）｜`README.md:167-169`（统一错误契约 5 码 + `[API.md](API.md)` 链接 + "10 条 API + 6 类事件"）｜`README.md:171-172`（SSE 事件 4 → 6 类，注明全局两类仅 `/api/events` 订阅者可见）｜`README.md:80`（空闲档 = 6 × 该值）、`:81`（基准阈值 + `max(基准, 2×通告值)` 抬升）。
  与 `API.md` 同路径同形态核对：README HTTP 表 10 行 == API.md §3 清单 10 条；两份文档的错误码集合同为 5 个、状态码同值；无相互矛盾表述（唯一用词差异见 D4）。README 环境变量两行与 `config.js:27` / `:118`（`HEARTBEAT_IDLE_FACTOR = 6`）、`registry.js:126-135`（`:132` 的 `max(baseTimeoutMs, 2 × announced)`）逐值一致。

- **AC7（E9 反向检索零命中：不存在 agent 启停接口/按钮、鉴权、跨机、tasks、客户端 SDK 条目）**：**pass**
  证据：`web.js` 路由集合 10 条中**无任何 agent 生命周期路径**（无 `/api/agents/<…>`、无 start/stop/kill）；`API.md` 全文按 `/api/agents/(start|stop|kill|close)`、`/api/(tasks|task)`、`Authorization`、`Bearer`、`api key`、cookie、`https://` 模式反向检索，**唯一命中均为显式"不做"声明行**（`API.md:62`、`:840`）；`README.md` Web 控制台段的按钮只有对话级三枚（「归档全部」`:128`、「关闭对话」`:129`、「激活」`:130`），无 agent 启停入口；`web/index.html`/`app.js` 中无启停控件（唯一 `agent` 匹配为 `@` 补全过滤 `app.js:747`）。
  解释边界（已在报告中显式声明而非默默选择）：`README.md` 保留了 0010 起的 `oamp task *` **CLI** 说明段，`API.md:842` 显式声明"Router 的任务面（`oamp task *`）不经 HTTP 暴露"。按 E9 原文口径（`demand.md:274`"在网页界面与接口清单里找不到任何启动 agent 或关闭/断开 agent 的入口、按钮或路径"）与架构 §9.4 检查项 4 的语义，这是 CLI 文档而非**接口条目**，不构成命中；若主 agent 采"README 全文不得出现 tasks 字样"的更严读法，则该项应改判 partial——**判定依据的歧义在此显式记录，由主 agent 决策**。

### 标准 2：文档与代码一致性（逐条）

- **① 接口数量与路径集合与 `web.js` 实际路由完全对齐（应为 10 条）**：**pass**
  证据：机械抽取两侧集合，归一后**完全相等**（10 == 10，无多无漏）。静态面 `/`、`/index.html`、`/app.js`、`/style.css` 在 `API.md:157` 的脚注中显式排除于错误契约之外，口径与代码一致（其中 `/index.html` 未逐一列举，属文档简化，不影响集合正确性）。

- **② 事件类型集合与 `web.js` 的 publish 点一致（应 6 类：chat 4 + 全局 2）**：**pass**
  证据：文档 §4.1 四类 + §4.2 两类；代码 `publishMessage`→`message`、`publishState`→`chat_state`、`notice`、`task_update`、`publishGlobal`→`agent_online`/`agent_offline`；集合**双向差集为空**；实跑另证两方向**键隔离**成立（全局订阅者只收到 `agent_*`，chat 订阅者只收到 chat 四类）。

- **③ 错误码与 `ERR_CODE` 映射一致（应 5 个）**：**pass**
  证据：`ERR_CODE` 5 键（`web.js:81-87`）== 文档 §2.2 映射表 5 行；每码对应状态码与 `sendError` 调用点推导值逐值相同；"成功响应不含 `code`"经全部 200 响应实测（`{"chat_id":…}`、`{"agents":…}`、`{"chats":…}` 等均无 `code` 键）。

- **④ 抽查 3 个接口的请求/响应字段描述与代码逐字段一致（含分页、`archived` 三取值、`?state=online`）**：**partial**
  **`GET /api/chats`（pass 部分）**：参数 8 项与 `persist.listChats` 一一对应；默认 `limit=50`/`offset=0`/`archived=0` 与 `LIMIT_DEFAULT`/参数默认值一致；`limit` 上限 200 与 `LIMIT_MAX` 一致；排序 `updated_at DESC`（`archived=0`）/`archived_at DESC`（`archived=1`）/同序 `chat_id DESC` 与 `persist.js:219` 的 CASE 表达式逐字一致；`total` = 过滤命中总数（`countChats` 同参）与实测一致（`limit=10` 时回显 `limit:10`）；`q` 的 `%`/`_`/`\` 转义与 `escapeLike` 一致；列表项 8 字段与 `listChats` SELECT 字段逐字相同（含仅列表项有的 `message_count`）。实跑校验：`limit=0/201/abc`、`offset=-1`、`from=abc`、`from>to`、`archived=2`、`state=bogus` 全部 400 `INVALID_PARAM` 且文案形态与文档一致。
  **`GET /api/chats/<chat_id>`（pass 部分）**：详情 9 字段 == `CHAT_COLUMNS`；消息 9 字段 == `MESSAGE_COLUMNS`；实跑输出与文档 §3.3 示例**逐字段同形**（含 `meta` 输入侧 `{task_id}`、输出侧 `null`、`direction` 仅 `in`/`out`、in 侧 `model`/`duration_ms` 恒 `null`）。
  **`POST /api/messages`（fail 部分）**：请求体 5 字段（`chat_id`/`agent_id`/`text`/`model`/`one_shot`）及必填与默认、`MODEL_RE` 形态、执行路径判定顺序（`!`→shell / `one_shot:true`→一次性 / 默认→常驻）**与代码逐条一致**；`warning` 语义（非空仍 200 + 落一条 `error='dispatch_failed'` 的 out 记录）与代码一致；但**响应字段 `message_id` 的文字说明与代码相反**——文档称"本次输入在对话库中的消息 id"（隐含整数库内 id），实测返回的是 `msg-<uuid>` 派发信封 id，库内 id 只出现在 `message` SSE 事件与详情接口。证据：同一对话两次发送，响应 `message_id` = `msg-1cf608f9-…` / `msg-ce456477-…`，而库内该两次输入的行 id 为 **8 / 10**；代码为 `web.js:695`（`messageId = 'msg-'+randomUUID()`）、`:716`（同一值作信封 id 传入 `sendTask`）、`:730`（同一值作响应 `message_id` 返回）。文档自身亦自相矛盾：示例 JSON 给的是 `msg-…`，紧邻的文字却说它是库内 id。详见偏差 D1。
  **`?state=online`（pass 部分）**：无参逐字透传（含 `offline` 墓碑）与 `?state=online` 过滤后**同形状**，实测两次响应字段集合相同；传 `bogus` → 400 且文案与文档逐字一致。
  **`archived`（fail 部分，措辞）**：文档 `API.md:213` 称"归档视图**三态**"，随后只列出 `0`/`1` 两个取值；代码 `readArchived` 只接受 `0|1`（其余 400）。取值集合本身描述正确，**"三态"一词与自身枚举项数矛盾**。详见偏差 D4。

### 标准 3：示例可跑（自建隔离环境逐条实跑）

**partial**（与 AC5 同一根因，两个入口）
证据（逐组）：§5.1 ✅、§5.2 ✅、**§5.3 ❌**（干净环境 404，见 D6）、§5.4 ✅（三例）、§5.5 ✅（帧序与载荷逐字节一致）、§5.6 ✅（实收 online/offline）、§5.7 ✅（四例返回与文档同形；`archived` 计数为 2 而非文档示例的 1，但文档已显式注明该值"随环境不同"）、§5.8 ✅（四条错误输出与文档逐字节相同）、§5.9 ✅（Node 客户端实跑打印 `基线在线: [ 'demo-1', 'web' ]` → `上线 demo-3` → `下线 demo-3`）、§5.10 为标注"伪代码"的接入闭环，非可执行示例（不计入）。
另证 SSE 契约：`content-type: text/event-stream; charset=utf-8`、`cache-control: no-store`、`connection: keep-alive` 与文档 §1.2 一致；`curl -N` 语义必要（`-s` 会缓冲）与文档说明一致。

### 标准 4：安全边界描述准确（仅同机 / 零鉴权 / 无 agent 启停接口）

**pass**
证据：① **仅同机** —— `lsof -nP -iTCP:57298 -sTCP:LISTEN` 输出 `TCP 127.0.0.1:57298 (LISTEN)`，无 `0.0.0.0`/`*` 绑定，与文档"只绑定 `127.0.0.1`、不做跨机接入、无 TLS"一致；`web.js` 的 `server.listen(port, '127.0.0.1', …)` 未改动。② **零鉴权** —— 全部请求未携带任何凭据头即成功；代码中无 cookie/token 校验点（反向检索零命中）。③ **无 agent 启停接口** —— 见 AC7。④ 文档额外声明的越权面（`!` 前缀在本机执行 shell）与 `web.js` 的 shell 路径（`{command:'/bin/sh', args:['-c', …]}`）一致，实测 `!echo hello-oamp` 成功执行。

### 标准 5：README 同步

**pass**（逐项证据见 AC6）
四项均到位：HTTP 表新增 `/api/events` 行 + `/api/agents` 标注 `?state=online` + 表后统一错误契约与 `API.md` 链接；Web 控制台段补顶栏列表一句；SSE 段 4 → 6 类；环境变量表两行说明与两档行为一致（空闲档 = 6 × interval；timeout 为基准阈值、已通告实例按 `2 × 通告值` 抬升）。另：`OAMP_WEB_TOPOLOGY_POLL_MS` 行（`:90`）默认值与 `web.js:46`（`TOPOLOGY_POLL_DEFAULT_MS = 2000`）一致。

### 标准 6：边界（改动仅 `oamp/API.md`（新建）+ `oamp/README.md`（+tasks 文件）；零新依赖）

**pass**
证据：`git show --stat 7cbe43a` = `oamp/API.md`（+849，新建）、`oamp/README.md`（+20/-6），**无第三个文件**；与迭代分支的差异文件集合为 `oamp/API.md`、`oamp/README.md`、`docs/…/prs/pr-003-api-doc-tasks.md`（后者属阶段 4 planner 产物，见提交 `8e6c9bf`，符合工作流阶段 5 输出契约）；`oamp/package.json` 未改动、`dependencies` 仍为 `{}`；worktree 内 docs 变更不含代码；`git status` 干净。

### 标准 7：反向核对（文档失真可被本验证方法发现的检出力）

**pass（含明确的方法局限声明）**
做法：把本验证中**可机械复跑**的层（路由集合 / `ERR_CODE` 键与状态码映射 / 逐接口错误码集合与状态码 / 事件名集合）实现为断言脚本，先对**原文跑基线**，再对 7 类人工注入的失真各跑一次，记录检出与否：

| 注入的失真 | 检出 | 证据 |
|---|---|---|
| §2.2 `CONFLICT` 状态码 409→400 | ✅ | `§2.2 CONFLICT 状态码 doc=[400] != 代码=[409]` |
| §3.7 表内 `CONFLICT` 状态码 409→400 | ✅ | `§3.7 CONFLICT 状态码 400 与代码 [409] 不符` |
| 删除 §3 清单的 `/api/events` 行 | ✅ | 路由集合不一致：仅代码 `['/api/events']` |
| 删除 §3.3 的 `UPSTREAM_UNAVAILABLE` 行 | ✅ | `§3.3 错误码集合 ['NOT_FOUND'] != 代码推导 [..., 'UPSTREAM_UNAVAILABLE']` |
| 事件名 `notice`→`noticex` | ✅ | 事件集合不一致（doc 含 `noticex`，代码不含） |
| §3.8 `text` 必填 是→否 | ❌ 未检出 | 参数表语义无机械断言 |
| 删除 §3.7 的 `title` 参数整行 | ❌ 未检出 | 同上 |

**基线（原文）在该脚本下零异常**——即本报告的"一致性结论"不依赖人眼印象，而是同一组断言的空结果。
**方法检出力结论**：① 路径集合 / 错误码 / 状态码 / 事件名四类失真**可被机械发现且可复跑**；② **参数表的语义列（必填 / 默认 / 类型）与散文性字段说明不可被机械发现**——只能靠"读表 ↔ 读代码"的人工比对，无红绿灯信号、不可复跑。本报告发现的 D1（`message_id` 语义写反）正落在该盲区：它由**代码逐行对照 + 实跑对账**发现，而非由断言脚本发现。⇒ 若该盲区被视为不可接受，处置见「下一迭代候选」N3。

### 标准 8：清理（自建隔离环境必须清理干净）

**pass**
证据：`hub stop` 逐个收停自建进程（`pr003-router`/`pr003-web`/`pr003-web-norouter`/`pr003-agent`/`pr003-events`/`pr003-stream`/`pr003-stream2`/`pr003-node-client`，demo-2/3/4 已自然退出）；`lsof -nP -iTCP:57298`、`57299` 均**无输出**（端口已释放）；`rm -rf /tmp/oamp-verify-pr003` 后复查该目录**不存在**；`ps aux | grep -c '[p]r003'` = **0**（无残留进程）。
**未触碰主仓库集群**：全部进程均带 `OAMP_SOCKET=/tmp/oamp-verify-pr003/…`、`OAMP_DB=/tmp/oamp-verify-pr003/…`；主仓库 `oamp/.runtime/router.sock`、`oamp/data/sql.db` 的时间戳均早于本次验证开始时刻。worktree 保持 `git status` 干净，未修改任何被验证产物。

---

## 汇总

- pass: **5** 项（AC 1/2/3/4/6/7 中除 AC5 外全通过；标准 4/5/6/7/8 全通过）
- fail: **0** 项
- partial: **3** 项（标准 1 = PR 卡 AC5；标准 2 = ④；标准 3）
- blocked: **0** 项

> partial 的构成（拆开看，不做"基本通过"）：**1 个真实缺陷**（§5.3 示例引用无生产者对话，D6）+ **1 个真实缺陷**（`message_id` 语义写反，D1）+ 1 处措辞失真（`archived` "三态"，D4）。三个 partial 均由这些**单点、可定位、可一句话修好**的问题触发；10 条接口覆盖、5 码映射、6 类事件、示例可跑、README 同步、改动边界六项核心契约**全部 pass**。

---

## 偏差记录

> 实现/代码与文档描述不一致之处。**不影响本次结论判定**（§5.3 与 `message_id` 两处已在 partial 中明示），逐条附建议处理方式供主 agent 决策。

| # | 文档描述（位置） | 实现实际行为（证据） | 建议处理 |
|---|---|---|---|
| D1 | `API.md:441`「`message_id`：本次输入在对话库中的消息 id（`msg-<uuid>` 是派发信封 id，与此处的库内 id 不同）」 | 响应 `message_id` **就是** `msg-<uuid>` 信封 id；库内消息 id 是整数，只出现在 `message` SSE 事件与 `GET /api/chats/<id>`。代码 `web.js:695`（生成 `msg-<uuid>`）→`:716`（作信封 id 发出）→`:730`（作响应字段返回）；实测 `resp.message_id=msg-1cf608f9-…` 而库内行 id = 8/10。文档示例 JSON 与自身文字相反 | **按实现改文档**：把该句改为"`message_id` = 派发信封 id（`msg-<uuid>`）；库内消息 id 见 `message` 事件的 `message.id` 或详情接口"。需同时修 §3.8 该 bullet（示例无需改） |
| D2 | `API.md:186`「数组含 `state: "offline"` 的墓碑项（**实例注销** / 判活超时后仍保留记录）」 | **优雅注销不留记录**：`registry.deregister` 走 `entries.delete(instanceId)`（`registry.js:96-103`，删除点在 `:100`）、`router.js:186-196` 的 `agent.deregister` 分支直调它；只有**判活超时**才经 `markOffline` 保留 `offline` 墓碑。实测：`demo-3` Ctrl-C 后无参列表**无该实例**；`demo-4` SIGKILL 后约 30s 出现 `state:"offline"` 且 `session_id` 保留 | **按实现改文档**：拆成两类分别描述（注销 = 记录消失；判活超时 = 保留 offline 墓碑），并告知客户端"无参列表无法区分『从未注册』与『已注销』" |
| D3 | `API.md:157`「（路径穿越仍返回 `403` 文本）」 | **不可达**：`serveStatic` 仅被 `/`（→`index.html`）、`/app.js`、`/style.css` 三个字面量调用（`web.js:733-740`），`!full.startsWith(WEB_ROOT)` 的 403 分支（`web.js:241-243`）在 HTTP 面无触发路径。实测 `/..%2f..%2fpackage.json` 与 `--path-as-is /../../package.json` 均返回 `404` + `{"error":"not found: …","code":"NOT_FOUND"}` | **按实现改文档**：删去该括注，或改为"非 API 面的静态资源不在错误契约范围内；路径穿越请求落 404 兜底"。**不建议**为迁就文档去改代码 |
| D4 | `API.md:213`「归档视图**三态**：`0` = 排除已归档、`1` = 只看已归档」 | 只枚举 2 个取值；代码 `readArchived` 仅接受 `0 \| 1`（`persist.js:105` 起），其余 400。实测 `archived=2` → 400 `查询参数非法: archived 需为 0\|1` | **按实现改文档**：去掉"三态"字样（改"两值：`0` / `1`，缺省 `0`"）。取值语义本身无需改 |
| D5 | `API.md` §4.2「**时延**：≤ 2s + 传输延迟（判定界 5s，余量 2.5×）」（与 `architecture.md` §4.2 同文） | 该上界只覆盖**轮询腿**：优雅注销路径总时延 ≤2s 成立；**判活超时**路径的总时延 = 租约阈值 + ≤2s（默认 `OAMP_HEARTBEAT_TIMEOUT_MS=30000`，故默认约 30s；轮询间隔 `web.js:46`，阈值 `config.js:21`）。实测 `demo-4` 于 03:58:51 被 SIGKILL，`agent_offline` 在约 30s 后才到达 | **按文档补一句限定**："≤2s 指拓扑轮询腿；实例被强杀时，事件还须等判活阈值（默认 30s）先判离线"。规格侧（架构 §4.2）同文，建议一并同步 |
| D6 | `API.md` §5 前言「除 `<chat_id>` 与示例实例名（`demo-1`）外，下列命令可直接复制执行」+ §5.3 命令 `…/api/chats/chat-demo-1` | 文档中**无任何示例创建 `chat-demo-1`**；干净环境原样执行得 404，与 §5.3「期望输出」（200 + 两条消息）不符。改读实际创建的 `chat-demo-shell` 后输出与文档逐字段一致 | **按实现改文档**：§5.3 改用 `chat-demo-shell`（§5.4 的 shell 演示已创建它），或把 §5.4 的演示前移到 §5.3 之前并加一句前置说明 |
| D7 | `API.md:258`「**参数**：无（`chat_id` 在路径中，按 URI 解码）」 | 路径参数 `chat_id` 的类型/必填未表格化（其余 4 个无入参接口用「参数：无」是准确简写，本条是路径参数） | **低优先**：若追求与其他接口同形，补一行 `chat_id \| string \| 是 \| — \| 目标对话`；不改亦不影响可用性 |

---

## 下一迭代候选

> 本轮发现的优化点、边界问题与遗留限制，不在当前验收标准范围内，供主 agent / planner 决策。

- **N1 文档一致性检查自动化**：本迭代 §9.4 的一致性检查项（路由 / 错误码 / 事件三清单 ↔ 代码）目前只在阶段 5/6 由人工比对执行。建议把它落成 `oamp/test/` 下的一条断言（路由集合、`ERR_CODE` 键与状态码、逐接口错误码、事件名四类，本报告标准 7 的脚本可直接移植），使文档漂移在 CI 即可被发现；否则每次迭代都要靠"有人认真读一遍"兜底。
- **N2 客户端可见的两种"下线"语义差异**：优雅注销（记录消失）与判活超时（保留 offline 墓碑）在无参列表里不可区分（D2）。若外部客户端需要区分，当前契约不提供依据——需评估是否加字段（属接口变更，另开迭代）。
- **N3 示例的"干净环境可复现"规程**：本轮暴露 §5.3 引用了无生产者的对话（D6）。建议文档类交付统一约定"示例按文档顺序可从空库复现"，并在 PR 卡验收里保留"逐条实跑"这一条（本轮证明它确实抓到了问题）。
- **N4 `message_id` 命名歧义**（D1）：响应字段与库内 id 同名不同物，是本轮唯一会让接入方写错代码的缺陷。若未来存在兼容窗口，可考虑在响应中显式给出库内消息 id（或改名），把歧义从文档层消灭。
- **N5 README 与 API.md 的边界表述收敛**：`README.md` 保留 `oamp task *` CLI 段，`API.md:842` 声明 tasks 不经 HTTP 暴露——两者不冲突但需读者自行拼合（AC7 的解释边界）。若后续迭代要求"README 也能被当作对外契约读"，建议在 README 的 CLI 段头部加一句"CLI 命令面不属于 HTTP API 面"。

---

## 结论

**PASS（无 fail）**

- 6 条核心契约（10 条接口不重不漏、5 码映射逐键一致、6 类事件与 publish 调用点相等、示例隔离环境实跑、README 四处同步、改动边界与零新依赖）**全部通过**，且每条都有可复跑的机械证据（标准 7 的断言脚本对原文零异常）。
- 3 项 partial 由**两处单点事实性错误**（§5.3 示例引用无生产者对话 D6；`message_id` 语义写反 D1）与**一处措辞失真**（`archived` "三态" D4）触发，均为文档侧、可一句话修正，不涉及实现改动。
- 7 条偏差已逐条记录并附建议处理方式；按 verifier 约定，**偏差被显式记录即满足交付条件**，是否返工由主 agent 决策。若主 agent 要求"文档零事实性错误"再合并，建议**先修 D1 与 D6**（前者会误导接入方写错代码，后者使 §5 的"可直接复制执行"承诺在干净环境不成立），D2/D3/D4/D5/D7 可合并后随下一迭代修。

**清理确认**：自建隔离环境（临时 socket/db + 57298/57299 端口 + 8 个受管进程 + `/tmp/oamp-verify-pr003`）**已全部清理**，主仓库集群未被触碰，被验证产物未被修改。
