# verify-20260912-111418-stage234.md — 阶段 2/3/4 独立验证

**验证者身份**（反射结果）：**迭代中期产物复合审查者——主视角为测试工程师**（能判断功能卡的验收标准是否可测试、是否可独立判定、是否覆盖需求条款），**兼架构评审者**（能判断组件是否过度设计、三条硬契约是否可在既有代码上落地、L1 分级是否成立），**兼发布计划审查者**（能识别依赖证据是否真实、文件范围是否互斥、依赖图是否有环、粒度是否成立）。

**产出物**：
- 阶段 2：`docs/iterations/0015-hub-orchestration-open-api/prd.md`（214 行）+ `prd/F01~F08*.md`（8 张卡）
- 阶段 3：`docs/iterations/0015-hub-orchestration-open-api/architecture.md`（v1.0.0，799 行）
- 阶段 4：`prs/pr-001-heartbeat-two-tier.md`（46 行）、`prs/pr-002-open-api-and-agent-panel.md`（58 行）、`prs/pr-003-api-doc.md`（42 行）

**验证标准来源**：主 agent 委托 brief 的 8 条验证标准（对照基准 = `demand.md` v1.0.0 与 `oamp/` 代码库现状）

**验证日期**：2026-09-12

**对照基准与取证口径（必读，影响全部行号引用）**：
- 代码库"现状"= **迭代分支 HEAD 内容**（`86cc8fd`）。核验方式：`git diff --numstat -- oamp/` 与 `git diff --summary -- oamp/` **均为空** ⇒ 工作区内容与 HEAD 逐字一致（`git status` 对 `oamp/src/web.js` / `oamp/src/transport.js` 标记 M，但内容零差异，属 stat/mtime 层面）。全部行号以该内容为准，并与产出物引用逐一对照。
- 行为实验：`/tmp/verify0015-exp.mjs`（进程内 import 真实 `src/transport.js` / `src/registry.js`，用桩 `res` 对象，15 项断言全部 PASS）。**未接触 `.runtime/`、`data/sql.db`、7788 端口、运行中的 10 角色集群；未写入仓库任何文件（除本报告）**。
- `docs/iterations/0015-hub-orchestration-open-api/deferred-demand-changes.md` **不存在**（已实测），故无搭置内容需原文透传。
- 需求条款基线：`demand.md` 第二段 W1~W7 / N1~N14 / E1~E9。

---

## 逐项判定

### 1. prd 覆盖性 —— **pass**

证据：
- **卡结构**：F01~F08 每张均含 `**功能 ID**`、`**来源**`（W/N/E/F 锚点）、`## 用户价值`、`## 验收标准`（每卡 ≥5 条，F03 8 条）、`## 边界（不包含）`（逐文件读取确认；例：`prd/F08-connection-semantics-unchanged.md:3-5,11-49`）。
- **W1~W7 全覆盖、无新增功能点**：`prd.md` 的需求追溯表给出映射，我逐条回归到卡：W3→F01、W4→F02、W5→F03、W6①⑤→F04、W6②③→F05、W6④→F06、W7→F07、W1+W2→F08 ⇒ W1~W7 并集完整、无遗漏；**反向**：8 张卡的"来源"行只引用 demand 的 W/N/E/M/C 条目，未出现 demand 之外的来源条款。
- **卡外技术面已显式声明来源**：`architecture.md:798`（§17 第 5 条）把 `HEARTBEAT_SENT` / `LEASE_ADJUSTED` / `HEARTBEAT_IDLE_DISABLED` / `?state=online` 逐条标注为"服务既有验收条款的观测面/口径落点，不是 demand 之外的功能能力"，与 F03 验收 1~3/7、F04 验收 2 一一对应 ⇒ 不构成新增功能点。
- **`[model_inferred]` 零残留**（机械核实）：`grep -rn '\[model_inferred' docs/iterations/0015-hub-orchestration-open-api/` **零命中**；`prd/F0*.md` 内连裸词 `model_inferred` 也零命中（`grep -rln` 只命中 `prd.md`/`architecture.md`/`history.md`/`demand.md` 的说明性文字）。8 项推导均在卡内改为 `[user_confirmed MI-0x]` 并在 `prd.md:153-166` 列出（F01 验收 2/5、F02 验收 1/2/5+边界、F05 验收 2、F06 验收 1/3 逐一核对存在）。

### 2. 无技术污染 —— **pass**

证据（机械扫描：取每卡 `## 架构维度` 之前的全部内容，匹配 `/api/`、`.js/.css/.html`、`#conn-status`、`#agent-panel`、`OAMP_`、`EventSource`、`SSE`、`registry`、`transport`、`sendError`、`ERR_CODE`、`JSON`、`HTTP`、`函数`、`接口路径`、`字段名`）：
- F01/F02/F03/F04/F05/F08 **零命中**。
- F06 唯一命中为其**显式排除项**：`prd/F06-...:26`「不含**错误响应体的具体字段名与状态码取值**——属架构维度」。
- F07 命中 3 处，均为**用户已确认的交付物标识**：`oamp/API.md`（TC-07/M-8 原文指定的文件）、"README 的 HTTP 段"（TC-07 原文）——非架构选型；且 `prd.md` 的「术语口径」已声明技术锚点的引用边界。
- 另注（不判为污染）：F01 验收 2 的判定句使用 `oamp status` 作为**对照工具名**（demand E1 原文的对照基准），F01 来源行与 `prd.md` 术语口径已显式锚定。
- 阶段 2 的占位事实可复核：`git show 097d8c7 -- prd/` 的卡内旧文本存在 `## 架构维度（\`[架构待填]\`）` + "以下均为阶段 3 待决策项"；阶段 3 提交 `bfd45df` 的删除行中这些占位逐条保留语义、被填充（见标准 3）。

### 3. architecture 补全 —— **pass**

证据：
- **AR-01~AR-08 及子项逐条可定位**：`architecture.md:638-652`（§11）表列出 AR-01 a~d / AR-02 a~c / AR-03 a~e / AR-04 a~d / AR-05 a~d / AR-06 a~c / AR-08 a~c，每行附 `详见` 章节引用（§3 / §4 / §5 / §6 / §7 / §9）。我在 `prd.md`（阶段 2 原始「架构待填列表」）独立数出**带字母子项恰 26 条**（AR-01 4 + AR-02 3 + AR-03 5 + AR-04 4 + AR-05 4 + AR-06 3 + AR-08 3 = 26，AR-07 为无字母的全局项）——与 `history.md:55`"已补全 26 条"**独立吻合**。所引章节实际存在且内容对应（抽查：AR-03→§3 全节 `:148-284`；AR-05→§4 全节 `:287-346`；AR-06→§5 全节 `:348-413`；AR-07→§9 `:572-609`）。
- **全文无 `[架构待填]` 残留**：`grep -rn '\[架构待填\]'` 在本迭代目录只命中 2 处，**均在输入/流水侧**：`demand.md:260`（阶段 1 自己列出的"待填清单"，属输入文档的合法内容）与 `history.md:55`（architect 报告摘要行）。`architecture.md` 与 `prd/*.md` **零命中**。
- **阶段 3 对 prd 的改动仅限架构维度**（`git log -p` 核实）：`git show bfd45df -- prd.md | grep '^-'` 删除行全部落在 ①文件状态行、②功能点索引表的"架构维度"列、③MI 列表的状态列与说明、④`## 架构维度（[架构待填]）` 占位段、⑤「疑问与越界」中被主 agent 裁决后的条目；`git show bfd45df -- prd/ | grep '^-'` 删除行全部是 MI 标注由 `[model_inferred MI-0x]` 改为 `[user_confirmed MI-0x]`（同句其余文字逐字不变）与各卡 `[架构待填]` 段。**无一行「用户价值/验收标准/边界」被删改**。
- **零越界写入**：`git show --stat 097d8c7 / bfd45df / 86cc8fd` 三提交的文件清单**只含 `docs/iterations/0015-*/**`**，`oamp/**`、`roles/**`、`demand.md` 零改动。
- **基线引用准确度抽查**（架构 §1.1 声称的行数/用例数）：`wc -l` 实测 `web.js 647 / registry.js 276 / transport.js 79 / agent.js 698 / node-client.js 228 / config.js 127 / app.js 784 / index.html 71` —— **全部与声称一致**；`oamp/test/*.test.js` = **20 个文件**、顶层 `test(` 计数 = **226** —— 与 `architecture.md:6` 声称的"20 个文件、226 用例"**独立吻合**。

### 4. L1 判定与留痕（独立复核） —— **partial**

拆开说：

- **4a. L1-01 的标注 = pass**。`architecture.md:613-636`（§10）把 D-01 标为 `**L1-01**`，`:700-719`（§14）给出完整 L1 条款：改了什么（`agent.heartbeat` 可选参数 `next_interval_ms` + `agent.register` 回包附加字段 `lease_follows_interval: true`）、属 L1 的理由（协议形态变更）、未采用的回退（方案 C 全局抬阈值）、"若只接受一半"的后果。**我判定该分级成立**：两处增补都落在 UDS 协议面（`router.js:159-171` 与 `:152` 的对外报文），而 router/agent/web 是各自独立部署、可独立重启的进程族，"协议形态"即系统整体边界；将其列为 L1（等主 agent 拍板）是保守且自洽的分级 —— 且架构自己以"L1 级代价"为由否决了 Router 主动广播方案（`architecture.md:307-309`），说明其分级尺度前后一致。
- **4b. 用户确认留痕 = partial**。留痕**存在但均为主 agent/执行角色的转述，缺独立事件记录**：
  - 存在：`architecture.md:3`（"L1 决策已确认（2026-09-11 用户决策；2026-09-12 主 agent 转达落盘）"）、`:700` 标题、`prd.md:7`、`status.md:20`（阶段 3 备注）、`history.md:53`（收到报告·architect 第 2 项"用户已确认采纳"）。
  - 缺失（可定位）：`history.md` 中**没有**任何 `调度决策 · Gate确认` 记录（协议把 `Gate确认` 列为合法事件对象——`workflow-pb.md:§历史记录协议`），`status.md` 的「待确认项」仅有一条需求澄清项（`[x]`），**无 L1-01 条目**。因此"用户于 2026-09-11 拍板采纳协议增补"这一事实，在迭代产物内**只能由同一工作流的自述证成**，无法独立复核。
  - 补充事实（对判定的降级说明）：`demand.md:117`（M-5）与 `:136` 明确记载"**不在需求层指定机制**"——用户把机制选型下放给架构阶段。这支持"协议字段增补落在被授权的边界内"，但不等于"用户对这个具体的协议增补拍过板"。
  - 判定：**缺一个主 agent 侧的 Gate 确认记录**（或一句"用户口头确认、无独立凭证"的显式声明）才能判 pass；现状态为 partial（留痕不完整，非缺失）。
- **4c. 是否存在其它未被标注的 L1 = 未发现需补标项（pass）**。我独立按 L1 口径（引入新技术栈 / 改变现有核心模块职责 / 影响系统整体边界）扫全部 18 条决策（§10 D-01~D-18）：
  - **重点核实项「`transport.js` 加全局键 + `web.js` 新增 `/api/events`」是否构成系统边界变更 → 我的判定：不构成。** 四条理由（均可定位）：
    1. **该能力的存在已是用户确认的需求**：`demand.md` W6③（"新增全局事件流：不依赖 `chat_id` 即可订阅，至少覆盖 agent 上线 / 下线"）+ D-5 + M-6；且 M-6 明写"**是否复用同一路径留架构**"——路径与复用方式是被用户显式下放给架构的，架构只在已授权的边界内做实现选择。
    2. **对外契约面零破坏**：既有 9 条路径的方法与成功路径语义零变更（`architecture.md:417-447` §6.1），监听地址 `127.0.0.1`、零鉴权、零跨机面逐字不变（§6.3 `:456-459`），无新进程 / 新端口 / 新传输实现（复用同一 `createSseTransport` 实例，全局键 = `null`）。
    3. **模块职责未改变**：`web.js` 原职责已含"查 `router.status`（`/api/agents`）+ 持有定时器（reconcile）+ 持有 transport"三类能力（`web.js:112-135,197-215,478-481`），新增的拓扑差值只是既有三类能力的组合（复用 `queryOnce` 与既有"env 只为测试压缩时间轴"的定时器范式），未迁移或替换任何既有职责；`transport.js` 的键结构、keepalive、清理、丢弃语义零改动（`transport.js:11-70`）。
    4. **与"真正会被判 L1 的替代方案"对比自洽**：Router 主动广播 = 新增协议通知方法 + 订阅登记 + Router 新职责，架构正是以该理由否决（§4.2 `:299-326`），反证其分级尺度未漂移。
  - **零新技术栈**：`oamp/package.json` 的 `dependencies` 保持为空（`hygiene.test.js:61-63` 有断言），新增代码全部落在既有 8 个 `src/` 文件 + 3 个前端文件（`architecture.md:122-132` §2.2）。
  - **零核心职责变更**：`registry.js` 仍是纯状态 + 纯判定（阈值公式只是既有 `findExpired` 内部替换，`registry.js:111-118`）；`router.js` 仍是"监听 + 分发 + 租约扫描"（`:105-193,:475-494`）；前端仍是"静态 DOM + 事件绑定 + 内存态"。
  - **borderline（记为偏差/候选，不判 fail）**：统一错误契约 `{error, code}` 是**对外 HTTP 响应体形状的横切变更**（覆盖全部错误出口），架构按 L2 处理（D-11）。我判定其不构成 L1，依据是 `error` 字段与全部文案逐字不变（`app.js:34-43` 实测：`api()` 只读 `data.error` 即 `throw new Error(data.error)`；`web.test.js` 全部错误断言基于 `.error` 字符串或状态码），`code` 为纯附加字段，且 F06 本身即用户确认需求（W6④）。若主 agent 对"对外响应体形状"采取更严口径，可补标；此项列入偏差与候选。

### 5. 三条硬契约可执行性（重点） —— **partial**

**① 心跳阈值联动 = pass**

- **协议字段落点可行**：`node-client.js:153-166` 现为 `tick()`+`setInterval(tick, intervalMs)`，改为"每跳前求值 + 自调度 `setTimeout`"是局部替换；`router.js:159-171` 的 heartbeat 分支只需透传 1 个参数；`registry.js:74` 的 `heartbeat({instanceId,sessionId,now})` 加可选入参即可。
- **"唯一落点 `registry.findExpired`"成立（机械核实）**：全仓 `findExpired` 引用只有 `registry.js:111`（定义）、`:263`（导出）、`router.js:477`（唯一调用方）——**测试文件零引用**，签名保持 `(now, baseTimeoutMs)` 两参不变 ⇒ 既有调用方零改动。
- **不变式与"前向生效"由行为实验证成**（`/tmp/verify0015-exp.mjs`）：以真实 `createRegistry()` 做时间推进式模拟（活跃档 10s 通告 10000 直到 T0=60s → 空闲档 60s 通告 60000，扫描每 750ms 一次）：
  - 基线（固定 30000 阈值）在 **t=90750ms 首次命中离线**（末次心跳 60000 + 阈值 30000 + 扫描粒度）⇒ 精确复现 `demand.md` C-2 的物理来源；
  - proposed 公式 `announced==null ? base : max(base,2×announced)` 在 **600s 观察窗内零命中** ⇒ "长间隔发生之前阈值就已放大"成立；
  - **未通告（旧 agent）⇒ 与基线逐字同判**（PASS）；
  - 不变式 `max(base,2i) ≥ 2i` 对 i ∈ {1,10000,30000,60000,600000} 恒成立 ⇒ `LEASE_ALARM` 的告警条件（`agent.js:664-669` 的 `lease < 2×interval`）在通告路径上不可能触发。
- **零重注册**：方案路径不触碰 `agent.register` / `agent.deregister` / `agent.replaced`（`router.js:109-188`），SIGINT 路径（`agent.js:598-622`）与重连退避（`:645-693`）逐字不动 ⇒ 会话身份保持；佐证事件 `HEARTBEAT_TIER` 带 `session`（§3.6 `:248-260`）可判定。
- **R-1 边界算术独立复核**：60s 间隔下 180s 窗口 idle 跳数 = `floor(180000/60000)` = **3**（实验 PASS），配合"进入空闲档那一跳记 `tier=active`、窗口左端取该跳之后"的实现契约 ⇒ E4"3 分钟 ≤3 次"可判定、与"保持 60s"的裁决相容。
- **观测面缺口的必要性成立**：`log.js:32-48` 的 `heartbeat()` 按 `key` 做滑窗节流（默认 `OAMP_HB_LOG_WINDOW_MS=60000`），agent 侧原本**零心跳记录** ⇒ 默认配置下 E4 的"心跳记录可直接计数"确实没有落点，`HEARTBEAT_SENT`（不节流，字段 `instance/tier/interval_ms`）是必要的最小修复（§3.6 `:248-260` 与 `architecture.md:682` 的奥卡姆行对照）。
- 兼容数值复核：默认活跃档 `max(30000,20000)=30000`（与迭代前逐字一致）；测试环境 `SHORT_ENV`(50/300) 活跃档 `max(300,100)=300`、空闲档 `max(300,600)=600` ⇒ 与 pr-001 验收"`threshold_ms = max(300, 2×next_interval_ms)`"一致，且 `agent-heartbeat.test.js:28` 的 `lease_timeout_ms=300` 断言（注册回包，取 `config.heartbeatTimeoutMs`）**无需改写**。

**② 全局事件流 = pass**

- **真实模块行为实验（15 项中 10 项针对本条，全部 PASS）**：以桩 `res` 驱动**未改动的** `createSseTransport`：
  - `handle(...,{chatId:null})` 与 `handle(...,{chatId:'chat-1'})` 并存；`publish(null,…)` 只达全局订阅者、`publish('chat-1',…)` 只达 chat 订阅者 ⇒ **键隔离成立**（F05 验收 4 的"结构性隔离"得到实测支持）；
  - 字符串 `'null'` 与 `null` 键**不冲突**（Map 严格键相等）；
  - keepalive 覆盖 `null` 键与 chat 键（`transport.js:21-27` 遍历全部 Set）；全局订阅者断开后 chat 键仍可投递（自清理只清本 Set）；`closeAll` 覆盖 `null` 键；
  - 现模块**无** `publishGlobal` / `globalCount` ⇒ 改动确为"+2 方法、键结构零改动"，与 §4.1/§4.3 一致。
- **判定源可执行**：`queryOnce(config.socketPath,'router.status',{})`（`web.js:112-135`）已是 `/api/agents` 的数据源（复用能力 B），`snapshot()` 4 字段含 `state`/`instance_id`/`last_heartbeat`（`registry.js:134-146`）⇒ 集合差值成立；`readPositiveMs`（`web.js:44-47`）可直接解析 `OAMP_WEB_TOPOLOGY_POLL_MS`；`url.searchParams` 已就位（`web.js:366-368`）⇒ 新路由零新解析逻辑。
- **≤2s 判定界成立**：默认 2000ms 轮询 vs MI-06 的 5s 界（2.5× 余量），且"仅存在全局订阅者时运行 / 首个订阅者播种基线不发事件 / Router 不可达该 tick 跳过不更新基线"三处语义都在 §4.2 明确到可实现的粒度（`createTopologyWatch` + 具名导出纯函数 `diffTopology` 可直接单测）。
- **既有按对话订阅零改动**：`/api/stream` 的"无 `chat_id` → 400"逐字保留（`web.js:621-629`，基线），既有 4 类事件与 `publish` 调用点不动 ⇒ 既有用例（`web.test.js:914-915`）零改写。
- 已声明残留风险（不阻断）：播种竞态（R-3，由 `API.md` 要求"订阅后先取 `/api/agents` 基线"缓解）、`?state=online` 与前端过滤是两处同谓词（R-10）。

**③ 统一错误契约 = partial**（机制 pass / 回填规模枚举 fail）

- **机制自洽且可执行的部分（pass）**：
  - 5 个码与状态码一一映射（`INVALID_PARAM`=400 / `NOT_FOUND`=404 / `CONFLICT`=409 / `PAYLOAD_TOO_LARGE`=413 / `UPSTREAM_UNAVAILABLE`=502），`sendError()` 为唯一构造点（`architecture.md:375-397` §5.3）；
  - `error` 为人类可读字符串且既有文案逐字不变 ⇒ 既有前端/调用方零改动（`app.js:34-43` 实测只读 `data.error`）；
  - **既有测试零改写可核实**：`web.test.js` 的全部错误断言都基于 `.error` 字符串或状态码（`:378,:463,:467,:471,:475,:484,:492,:1066,:1108,:1113,:1116,:1191,:1310,:1315,:1321,:1335,:1336,:1345,:1354`），**无一处对错误响应体做整体 `deepEqual`** ⇒ 新增 `code` 不命中任何既有断言；
  - 我逐一验证 5 个码能覆盖全部实际错误出口的语义类别（400/404/409/413/502 五类与代码中的 4xx/5xx 完全对应，无第 6 类）。
- **不通过的部分（partial 的来源）—— 回填规模数字与代码不符**：
  - 产出物称"**12 处**出口"（`architecture.md:392`（§5.3 步骤 2 行"共 12 处出口"）/ §11 AR-06 `:647` / §13 `:682` / §15.1 `:739` / `pr-002` 上下文摘要与验收 / `history.md:54`）。
  - 实测（基线内容机械计数）：`sendJson(res, <4xx|5xx>, {…})` **17 处**（`:397,:407,:417,:467,:471,:475,:496,:500,:510,:515,:524,:548,:552,:557,:564,:619,:621`）+ `sendJson(res, status, {error…})`（`readBody` 分支）**2 处**（`:490,:537`）= **19 处**错误出口。
  - `pr-003` 的证据段又枚举了 **14 处**（`:397/:407/:417/:471/:496/:500/:510/:515/:524/:548/:552/:557/:564/:621`），**既与"12"互相矛盾，也漏了 5 处**（`:467` `/activate` 未知 chat 404、`:475` activate 防御性 409、`:490` rename readBody 400/413、`:537` messages readBody 400/413、`:619` 路由兜底 404）。
  - **影响可判定**：F06 验收 1（同类错误跨接口响应一致）与验收 2（逐接口错误语义可枚举）要求 19 处全部同形；按"12 处"施工会漏 7 处，其中 `:467`（`/api/chats/<id>/activate` 未知 chat → 404）与 `:619`（未知路径/方法不匹配 → 404）是外部客户端**必然命中**的路径，可直接判 F06 验收 1 失败。故该项不是纯文字瑕疵，而是会传导到验收的偏差。
  - 因此本条判 **partial**：机制与"零改写"结论成立；"回填规模"枚举不成立。

### 6. prs 格式与依赖正确性 —— **pass**

证据：
- **七字段齐备**：三份 PR 文件的二级标题序列均为 `上下文摘要 / 涉及功能点 / 文件范围 / 验收标准 / 参考资料 / depends_on / batch`（`sed -n '/^## /p' prs/pr-00*.md` 逐文件核实）。
- **`depends_on` 证据真实**（逐条回代码核实，行号以基线为准）：
  - `pr-003 → pr-001`：证据"`config.js:19-24` 无 `heartbeatIdleMs`"✓（`:19-24` 就是 `NUMERIC_DEFAULTS`，仅 4 个键）；"`registry.js:111-118` `findExpired(now, timeoutMs)` 单参固定阈值"✓；"`README.md:80/81` 现文案"✓ 逐字（`:80` = "心跳周期（毫秒）；正整数"、`:81` = "租约超时（毫秒）；正整数；建议 ≥ 2×interval"）⇒ 依赖为真。
  - `pr-003 → pr-002`：证据的路由行号 `:378 / :383 / :403 / :413 / :434 / :463 / :483 / :521 / :530 + :619` 与基线 `web.js` **逐行对应**（实测：`:378` `/api/agents`、`:383` `/api/chats`、`:403` `/api/chats/`、`:413` `/close`、`:434` `/archive`、`:463` `/activate`、`:483` `/rename`、`:521` `/stream`、`:530` `/api/messages`、`:619` 兜底）✓；"现错误出口均为单字段体、无 `ERR_CODE`/`sendError`"✓；"`transport.js:12-60` 只有 chatId 键、无 `publishGlobal`/`globalCount`"✓（实验亦证实）；"README `:119-143` 现无'顶栏可点开列表'、`:154-162` 无 `/api/events` 行"✓ ⇒ 依赖为真（**唯一瑕疵**：同段又写"现 `:151-162`"，与 `:154-162` 轻微不一致，指向同一张表，不影响成立性）。
  - `pr-001` / `pr-002` 的 `（无）`：两条理由我独立复核成立 —— pr-001 只改"通告字段 → 阈值公式 → 跳间隔"，`registry.js:134-146` 的 4 字段投影与 `web.js:378-381` 的逐字透传均不在其改动面；pr-002 只消费既有 `queryOnce(router.status)` 与 4 字段快照，且 `startHeartbeat` 的固定数字入参行为被契约锁定（"通告一个与自身间隔同值的数"）⇒ 二者无消费关系，可并发。**不是把顺序偏好当依赖**。
- **文件范围无重叠**：pr-001{`config.js`,`registry.js`,`router.js`,`node-client.js`,`agent.js`,`test/agent-heartbeat.test.js`} ∩ pr-002{`transport.js`,`web.js`,`web/index.html`,`web/app.js`,`web/style.css`,`test/transport.test.js`,`test/web.test.js`} ∩ pr-003{`oamp/API.md`,`oamp/README.md`} = **∅** ✓；且与 `architecture.md:760-771`（§15.3）的组划分一致。
- **依赖图无环**：pr-001 ∥ pr-002 → pr-003（fan-in），无环 ✓。
- **F01~F08 全覆盖且无重复归属**：pr-001→F03/F08；pr-002→F01/F02/F04/F05/F06；pr-003→F07 ⇒ 并集 = 8 张卡、交集 = ∅（逐文件核对"## 涉及功能点"段）。
- **G2+G3 合并裁决成立（"是否真的只能合并"→ 是）**：在架构 §15.3 的硬约束下逐条验证——
  (a) `oamp/test/web.test.js` **既**承载后端用例段、**又**承载前端静态契约段（实测 `web.test.js:1001-1052`、`:1226-1243` 读 `web/app.js`/`index.html`/`style.css` 并断言前端符号）；
  (b) 若前端独立成 PR，其静态契约段只能留在同一文件 ⇒ 两个 PR 同时声明 `web.test.js` ⇒ 违反阶段 4 推进条件的"PR 间文件范围无重叠"；
  (c) 若把前端静态段放进后端 PR，则后端用例须断言 `#agent-panel` / `connectAgentEvents` 等前端产物 ⇒ 后端依赖前端，而前端 F02（自动更新）依赖后端的 `/api/events` ⇒ **成环**（裁决描述与代码事实一致）；
  (d) 唯一替代路径"新开前端契约测试文件"与架构 §9.1（"新增断言只落 `web.test.js`/`agent-heartbeat.test.js`/`transport.test.js`，其余 17 文件零改动"）冲突 ⇒ 在既定架构约束下**合并是唯一无环解**。
  复核结论：`pr-002` 的 8 条验收标准在其自身 worktree 内全部可判定（两个既有测试文件 + HTTP 实调 + 前端静态契约），合并未造成跨 PR 前置。

### 7. 验收标准可独立判断 —— **pass**

证据与口径说明：
- **pr-001（7 条）**：全部可在自身 worktree 判定 —— 纯函数三条边界（`heartbeatPlan`）、`node --test test/agent-heartbeat.test.js`、`LEASE_ADJUSTED`/`HEARTBEAT_TIER` 行断言、既有三个测试文件回归 + `git diff` 自查"既有用例逐字未改"。无跨 PR 前置 ✓。
- **pr-002（8 条）**：全部可在自身 worktree 判定 —— `node --test test/transport.test.js test/web.test.js`、`curl -N /api/events` 实调、`?state=online` 与无参形态对照、错误契约逐类触发、前端面板/自动更新的人工或脚本观察、`git diff` 自查既有断言未改。无跨 PR 前置 ✓。
- **pr-003（7 条）——"依赖 pr-001/pr-002 应如何判定"的说明**：其中 4 条（接口清单 ↔ `web.js` 路由集合逐条对照、错误章节 ↔ `ERR_CODE` 五键、事件清单 ↔ `publish`/`publishGlobal` 调用点、6 组示例原样可执行）以"pr-001 与 pr-002 合并后的代码形态"为判定基准，因此**不能**在 pr-003 的分支 base 早于二者合并时判定。判定结论：**仍属合规**，依据是 workflow 的阶段 5 解锁语义 —— `depends_on` 中每个 PR 必须**合并进迭代分支**后该 PR 才被派发（`workflow-pb.md:§阶段 5`"合并进迭代分支才算解锁"），即 pr-003 的 worktree base 必然已含 pr-001/pr-002 的产出 ⇒ **判定动作发生在派发之后、不需要等待任何并行中的 PR**，符合"可独立判断，不依赖其他 PR **完成**（在执行时仍未完成）才能判断"的语义。同时格式硬要求（"至少一条"）由验收 1（`oamp/API.md` 存在 + README HTTP 段有链接）单独满足，该条完全自洽。**建议**：在 pr-003 文件里显式标注"判定基准 = 合并 pr-001/pr-002 后的迭代分支"，避免被误读为需等待。此项记为偏差 9（表述性）。
- 补充：`pr-003` 的验收 6 涉及 README 环境变量表两行"描述 pr-001 引入的语义"，依赖方向为 pr-003 → pr-001（与 depends_on 一致），无环 ✓。

### 8. 回归影响面准确性 —— **partial**

拆开说：

- **pr-002 的"既有断言零删改" = pass（完整）**：
  - 错误体相关：`web.test.js` 全部错误断言基于 `.error` 字符串或状态码（行号见标准 5③），无整体 `deepEqual` ⇒ 新增 `code` 零命中；`:914-915`（`/api/stream` 缺 `chat_id` → 400）被方案完整保留 ⇒ 该"唯一疑似需改写"的用例零改写。
  - 前端静态契约的隐性约束被显式承接：`web.test.js:1005` 与 `:1051` 的 `assert.doesNotMatch(appJs, /POLL_MS/)`、`:1006` 的 `assert.doesNotMatch(appJs, /setTimeout\(tick/)` —— pr-002 验收 8 明写"不引入 `POLL_MS` 标识符与 `setTimeout(tick` 形态"；且方案新增的 `AGENT_PANEL_REFRESH_MS` 不含子串 `POLL_MS`、定时器形态为 `setInterval(loadAgents…)` ⇒ 可兼容。
  - `transport.test.js:78-89` 只做**子集**形状检查（`for (const name of ['handle','publish','close','closeAll'])`），无全键 `deepEqual` ⇒ `+publishGlobal`/`+globalCount` 不破 ✓。
- **pr-001 的回归清单 = partial（漏识别一条既有断言的约束面）**：
  - 已声明的回归面我全部复核成立：`router-registry.test.js`（未手工构造条目、无 `findExpired` 直接调用）✓；`agent-heartbeat.test.js` 既有 6 条用例的时序预算 — `F04-2` 的强杀判离线断言 `elapsed < 2500ms`（`:103`）在"空闲档通告 300ms ⇒ 生效阈值 600ms"下最坏约 900ms，余量充足 ✓；`F04-4` 的"心跳存活期不得 offline"在阈值 600ms > 空闲间隔 300ms 下成立 ✓；`F03-2` 的 `last_heartbeat` 推进（3000ms 预算）在空闲档 300ms 一跳下成立 ✓；`delivery-contract.test.js`（`HB_MS=50`，默认 300ms 阈值）与 `web.test.js` 假节点（`heartbeatMs=500`，`LEASE_ENV` 3000）及真实 agent（50/3000、50/300）——因契约规定"固定数字入参通告同值"，`max(base,2×announced)` 恒等 base ⇒ **阈值数值零变化**，故 `web.test.js` 全程不受影响 ✓（这也补上了 brief 所指"pr-001 声称既有 `web.test.js` 需全绿"的实际依据：pr-001 未点名该文件，其隐含保证来自"固定数字入参行为逐字不变"）。
  - **未识别项（partial 的来源）**：pr-001 新增的 **agent 侧不节流日志**会进入 `event-log.test.js` 的既有断言面 —— 该文件 `F06-2`（`:55-67`）对 agent **每一行** stdout 施加 `EVENT_LINE_RE = /^\[TS\] (router|agent) ([A-Z_]+)((?: [a-z_]+=[^ ]+)*)$/`。pr-001 的回归清单与 `architecture.md:584-590`（§9.2 回归锁）**均未提及这个文件**（§9.1 只说其余 17 文件"零改动"，但"零改动"≠"无约束"）。
  - 我判定其**可兼容**（新字段 `instance`/`session`/`tier`/`interval_ms` 全为小写无空格键值，行格式（`log.js:22-30` 的 `key=value` 拼装与 `log.js:11-14` 的 `formatValue` 引号规则）满足正则）⇒ **不判 fail**；但 `HEARTBEAT_IDLE_DISABLED` 的 `note`（含空格与中文；`formatValue` 加引号后值内空格仍破坏该正则）一旦在测试中触发即打破 `F06-2`（测试环境 Router 均为新版，该事件不会触发；既有 `LEASE_ALARM` 的 `note` 有同样性质，同样不触发）。风险可验证：跑 `node --test test/event-log.test.js` 即可。

---

## 汇总

- **pass: 5 项**（标准 1、2、3、6、7）
- **fail: 0 项**
- **partial: 3 项**（标准 4：4a pass / 4b 留痕 partial；标准 5：① pass / ② pass / ③ partial；标准 8：pr-002 pass / pr-001 partial）
- **blocked: 0 项**
- 偏差记录：**9 条**
- 另一条独立复核结论：`[架构待填]` 零残留（architecture.md 与 prd/*.md），残留的 2 处字面命中均在 demand.md（阶段 1 输入文档自身的"待填清单"）与 history.md（报告摘要行），非阶段 3 缺口。

## 偏差记录

| # | 规格/文档描述 | 实现/代码实际 | 建议处理 |
|---|---|---|---|
| D1 | "**12 处**错误出口回填 `sendError()`"（`architecture.md:392`/§5.3/§11 AR-06、`pr-002` 摘要与验收、`history.md:54`） | 基线 `web.js` 错误出口实测 **19 处**（17 处 `sendJson(res,4xx/5xx,{error})` + 2 处 `sendJson(res,status,{error})`） | **按代码更正为 19 处**（或改为"全部错误出口，以机械检索 `sendJson(res, [45]\d\d` 与 `sendJson(res, status` 为准"），并把它写进 pr-002 验收的可核对口径 |
| D2 | `pr-003` 的 depends_on 证据枚举 **14 处**出口行号 | 既与 D1 的 12 互相矛盾，又漏 `:467/:475/:490/:537/:619`（其中 `:467`、`:619` 是客户端必命中的 404 路径） | 与 D1 一并更正为完整清单（19 处）；该证据不改变依赖成立性 |
| D3 | `prd.md` N 追溯表：N14 "落…各卡边界" | F01~F08 实际**无任何卡**提及 N14（只在 `prd.md` 的全局「不包含」中） | 修正表述为"N14 → 全局边界（本文件「不包含」）"，或补进 F08 边界 |
| D4 | `pr-003` 同段内 README 位置引用出现"现 `:151-162`"与"`:154-162`" | 实测 HTTP 表数据行为 `:154-162`（`:151` 为 `HTTP API` 标题行） | 统一为 `:154-162` |
| D5 | `README.md:81` 文案"租约超时（毫秒）；建议 ≥ 2×interval" | 改造后该值语义变为**基准**阈值（已通告实例按 `max(基准, 2×通告)` 抬升；空闲档默认生效 120000） | 按 `architecture.md:592-599`（§9.3）同步 README 该行（"基准阈值"），否则文档与实现不一致 → 由 pr-003 承接 |
| D6 | `prd.md:7` 状态行仍写"**阶段**: 功能规格（阶段 2）" | 同文件已含阶段 3 的「架构维度落定」正文章节 | 标注为"阶段 2 产物（架构维度由阶段 3 于 2026-09-12 补全）"，避免版本语义混淆 |
| D7 | `architecture.md:584-590`（§9.2 回归锁）/ pr-001 回归清单 | 未识别 `event-log.test.js:55-67` 对 agent 每行日志格式的**既有断言**构成对新增日志字段的隐性契约（实测可兼容；`HEARTBEAT_IDLE_DISABLED` 的 `note` 若触发则不兼容） | pr-001 实施时显式把 `event-log.test.js` 纳入回归执行清单；新增日志字段保持"小写键 + 无空格值" |
| D8 | F06"所有接口统一错误契约" | 静态资源路径 `web.js:150/:155` 仍返回非 JSON（`403 forbidden` / `404 not found` 纯文本） | 架构 §6.1/§6.4 已把静态面排除在外；建议在 `API.md` 第 2 节明写"统一错误体仅适用 `/api/*`" |
| D9 | `pr-003` 验收 1~7 的判定基准 | 4 条验收依赖 pr-001/pr-002 的最终代码形态（虽由 depends_on 语义保证不阻塞） | 在 pr-003 文件显式标注"判定基准 = 合并 pr-001/pr-002 后的迭代分支" |

（另有 1 条 borderline，供主 agent 裁决，不单列行：统一错误契约属"对外响应体形状"的横切变更，架构按 L2 处理；我判定不构成 L1（`error` 与文案逐字不变 + 纯附加字段 + F06 本身为用户确认需求），若主 agent 取更严口径可补标 L1。位置：`architecture.md:625`（§10 D-11 行）。）

## 下一迭代候选

- **L1 留痕机制化**：在 `history.md` 增加 `调度决策 · Gate确认` 记录（用户原话/时间/确认项），使"L1 已确认"可独立复核；本轮仅四处转述（见标准 4b）。
- **日志体量**：`HEARTBEAT_SENT` 不节流 ⇒ 活跃档 6 行/分钟/实例（`R-7` 已声明）；实例数放大后建议评估"仅档位变化 + 每 N 跳"或缩短滑窗。
- **E4 判定对日志的依赖**：需求裸判定"心跳记录可直接计数"现在依赖 agent 侧新增日志；长期可考虑把档位纳入 `router.status` 快照（可查询接口）作为更稳的观测面。
- **web 进程第二条定时器**：拓扑轮询（2s）与对账轮询（5s/30s）并存；长期可合并为一条"Router 快照轮询"（`R-10` 的两处同谓词同理，但需先解耦 `@` 补全的离线灰显状态源）。
- **播种竞态（R-3）**：当前靠"订阅端先取基线"的文档约定缓解；若有第三方订阅者报告首条上线事件漏收，候选修法是首个订阅者到达时先发一条快照事件（本轮刻意不做）。
- **回填类验收的可核对化**：本轮的"12/14/19"分歧说明"用手写计数表达回填范围"不可靠；建议后续迭代的 PR 验收改用可机械检索的判据（如"`grep` 零命中旧构造形态"）。

## 结论

**PASS**（0 fail / 3 partial / 0 blocked；偏差 9 条已记录并逐条给出处理建议）

判读要点（供主 agent 直接决策）：
1. **可以继续进入阶段 5 的既有并发派发**（pr-001 ∥ pr-002 已派发）——三条硬契约中①②机制可执行且经行为实验证成，"心跳阈值联动"的不变式与"全局键隔离"均有实测支持。
2. **开工前需修正的只有一处硬事实**：D1/D2（错误出口 12/14 vs 实际 19）——建议主 agent 直接把"19 处（以机械检索为准，含 `:467/:619`）"作为 pr-002 的实施与验收口径下发给 dev，避免漏改导致 F06 验收 1 在 `/activate` 未知 chat 与未知路径上失败。
3. **D5（README 阈值语义）必须在 pr-003 落地时同步**，否则实现的语义变更会留在文档外（pr-003 的依赖方向已覆盖该风险）。
4. **D7（event-log.test.js 的行格式隐性契约）建议作为 pr-001 的回归执行项**，实测可兼容，但需在实施时真正跑一次该文件。
5. 标准 4b（L1 留痕）与标准 8（pr-001 回归清单缺 `event-log.test.js`）为 partial，均**不阻塞**本迭代闭环；若主 agent 要求，可在 history.md 补一条 Gate 确认记录、并在 pr-001 实施时补跑一个测试文件即可就地消解。
