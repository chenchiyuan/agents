# verify-20260912-114018-pr002 — PR-002 独立验收报告

**验证者身份**：同级代码审查者 + 端到端验收工程师（HTTP/SSE 接口契约与浏览器实测方向）
—— 本产出物 = 后端接口/事件流实现 + 前端只读面板 + 测试回填，需要同时具备"读得懂 SSE 键位与错误出口"与"敢于用真实进程 + 真实浏览器推翻自证"两种能力，故反射为二者的合体，而非通用检查员。

**产出物**：
- worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0015-pr-002-api-panel`（分支 `feat/0015-pr-002-api-panel`，commit `b562a26`，未提交改动 = 0）
- PR 卡 `docs/iterations/0015-hub-orchestration-open-api/prs/pr-002-open-api-and-agent-panel.md`
- 契约 `docs/iterations/0015-hub-orchestration-open-api/architecture.md`（硬契约 ②§4 / ③§5，AR-01/AR-02，§6/§7/§9）

**验证标准来源**：主 agent 委托的 10 条验证标准（逐条引用 PR 卡验收 + 架构 §4/§5/§6/§7 原文）

**验证日期**：2026-09-12

**独立性声明**：本次验证未接收任何执行过程上下文；所有判定仅基于产出物本身、委托给定的验证标准、以及我自行启动的隔离环境（`/tmp` socket + 随机端口 17788/17790 + 独立 DB）。隔离环境已于验证结束时清理（见标准 10）。

---

## 逐项判定

### 标准 1 · PR 卡验收逐条判定

PR 卡「验收标准」共 9 条，逐条判定如下。

**1.1 `node --test test/transport.test.js test/web.test.js` 全绿 + 既有断言逐字未改**：**pass**
- 证据（运行）：`cd oamp && node --test test/transport.test.js test/web.test.js` → `# tests 52 / # pass 52 / # fail 0`（含新增段：全局键 3 条、`/api/events`、`?state=online`、统一错误契约、前端静态契约）。
- 证据（零改写）：`git diff -U0 main...HEAD -- oamp/test/` 删除行数 = **0**；numstat `120  0  oamp/test/transport.test.js`、`293  0  oamp/test/web.test.js`（唯一新增 import 为新增行，非替换：`+import { diffTopology, createTopologyWatch } from '../src/web.js';`）。
- 证据（卡上点名的四段仍在）：`oamp/test/web.test.js:952` `/api/stream 缺 chat_id → 400`（原 `:911`）仍为 `assert.equal(missing.status, 400)`；`web.test.js:1154/1157/1591/1592` 的 `chat 已归档（只读），不接受新输入` 与 `:1376/1591` 的 `chat 已归档（只读），不可改名` 逐字在位。

**1.2 全局键隔离 / `globalCount` / `closeAll` 覆盖全局键**：**pass**
- 证据（单测）：`test/transport.test.js` 新增三条用例全绿，其中 `assert.ok(!chat.text.includes('agent_online'), 'chat 订阅者不得收到全局事件')` 与 `assert.ok(!glob.text.includes('按对话事件'), '全局订阅者不得收到 chat 事件')` 双向断言。
- 证据（运行时）：同时挂 `/api/stream?chat_id=chat-iso` 与 `/api/events`，起 `dev-b` + 发消息后，chat 流只出现 `message`/`chat_state`/`notice`，全局流只出现 `agent_online`（`dev-b`、`web`），两侧零串扰（日志见 `1789184027…`–`1789184032…` 段）。
- 证据（反向）：把 `publishGlobal` 改成遍历所有键广播后，用例 `全局键：publishGlobal 只到 /api/events 订阅者…` 立刻 `not ok`（AssertionError）——见标准 9。

**1.3 `GET /api/events` **：**pass**
- 200 + `content-type: text/event-stream; charset=utf-8` + `retry: 1000` 首帧，连接保持（`curl -N` 实测）。
- 起 `dev-a` ⇒ `event: agent_online / data: {"instance_id":"dev-a","last_heartbeat":…}`：注册时刻 `…971930`（`REGISTERED` 行）→ 事件到帧 `…973218` = **1.29s**（≤2s 轮询 + 传输）；停 ⇒ `event: agent_offline / data: {"instance_id":"dev-a"}`（仅一个字段，实测 `Object.keys` 恰为 `['instance_id']`）。
- 另一路实测：`dev-c` 注册→事件 1.74s、`dev-d` 0.24s；`dev-c` SIGINT 注销→`agent_offline` 1.9s（`DEREGISTERED` 行为触发源，非超时推断）。
- 播种：订阅后静置 6s（默认 2s 轮询 ⇒ 3 个 tick）零事件，随后 `agent_online` 才出现 ⇒ 首订阅只播种基线。
- Router 不可达期间的 tick：全局订阅者在 Router 被停掉后 100 余秒内**没有**收到 `agent_offline`（只有 keepalive），即查询失败不产生虚假上下线（架构 §16 R-4）。

**1.4 `GET /api/agents?state=online`**：**pass**（详见标准 4）

**1.5 统一错误契约**：**pass**（详见标准 3）

**1.6 接口面零缺口、零启停路径**：**pass**
- 对照表逐行实调（隔离环境，`node sweep.js`）：`GET /api/agents`、`/api/agents?state=online`、`GET /api/chats`、`GET /api/chats/<id>`、`POST …/rename`、`POST …/close`、`POST /api/chats/archive`、`POST …/activate`、`POST /api/messages`、`GET /api/stream`、`GET /api/events`、`/`、`/app.js`、`/style.css` 全部按表响应（含 409/200 各分支）。
- 反向检索：`web.js` 路由集合 = 10 条 API + 2 条静态 + 1 条兜底 404（`web.js:478/494/514/524/545/574/594/635/644/650/733/737/741`），**不存在** start/stop/kill/launch 路径；`web.js` 内无 `spawn`/`child_process`（唯一 `.exec(` 命中是 `web.js:664` 的 `@agent` 正则 `.exec(text)`）。

**1.7 前端面板**：**pass**（详见标准 5，含空态与零启停入口）

**1.8 前端自动更新**：**pass**（详见标准 5 ③ 与 1.8 补充）
- 面板**关闭**时计数也变：`dev-d` 注册 → 顶栏 `2 agents online` → `3 agents online`，**404ms**，无任何手动操作。
- 面板**展开**时：`dev-d` 行出现、SIGINT 注销后 1.6s 内行消失且计数回到 2。
- 其余条目不变：时间线中 `dev-b`/`web` 行持续在位，仅新增项出现/移除。
- `onopen ⇒ loadAgents()` 全量对齐（F02 验收 5）另做隔离实测：面板关闭、`focus` 事件计数 = 0 的条件下，杀掉 web 进程 → 期间直连 Router 注册 `dev-recon` → 重启 web ⇒ 页面计数在 **10.5s** 内自动由 `0 agents online` 变为 `1 agents online`，全程无点击 / 无焦点事件 / 无展开 ⇒ 该变化只能来自 `EventSource.onopen`（全局事件流重连 + 全量重取）。

**1.9 既有前端契约不破**：**pass**
- `git diff --numstat`：`oamp/web/app.js` = **103 增 / 0 删** ⇒ 既有行（`setConn` 的 `已连接 · N agents online` 文案、`loadAgents` 全量取数、`showMention` 灰显）逐字未动，仅新增函数与三处绑定行。
- `app.js` 中 `POLL_MS` 与 `setTimeout(tick` 形态检索：**零命中**（`oamp/test/web.test.js:1649` 的 `assert.doesNotMatch(appJs, /POLL_MS/)` 同时通过）。
- `style.css` 唯一删除行 = `.topright { margin-left: auto; }` → `.topright { margin-left: auto; position: relative; }`（有意变更，与架构 §7.1「定位锚点」一致）。

### 标准 2 · 全局事件流（判定源差值 / 键隔离 / 播种 / 自停）

**pass**
- `/api/events` 可用：`web.js:644-648`（`transport.handle(req,res,{chatId:null})` 先注册订阅者，再 `topologyWatch.ensureRunning()`，顺序保证 `globalCount() ≥ 1` 才起表）。
- `/api/stream` 的 chat_id 强制规则与 400 文案**逐字保留**：实测 `GET /api/stream` → `400 {"error":"需要 chat_id（不做全局订阅）","code":"INVALID_PARAM"}`；`GET /api/stream?chat_id=`（空值）同为 400；`web.js:638` 文案与基线 `git show main:oamp/src/web.js` 的 `:524` 逐字相同。既有用例 `web.test.js:952` 未被改写（标准 1.1）。
- 事件载荷：`agent_online` = `{instance_id, last_heartbeat}`（`last_heartbeat` 实测为 number，且与 `router.status` 同值）；`agent_offline` = `{instance_id}`。
- 判定源差值逻辑正确：
  - `diffTopology`（`web.js:161`）只把 `state === 'online'` 收进 `next`（离线墓碑不进基线），新增项带 `last_heartbeat`、消失项只带 `instance_id`；单测覆盖"心跳刷新不产生事件"。
  - 仅存在订阅者时运行：**运行时定量证据**——把 Router 换成一个"接受连接但永不响应"的沉默 UDS 服务，并在 web 侧用 `OAMP_WEB_TOPOLOGY_POLL_MS=50`：
    - 有 1 个 `/api/events` 订阅者时，web 进程（pid 2141）持有 **59** 个在途 UDS 连接（`lsof -a -p 2141 -U | tail -n +2 | wc -l`，≈ 3s 请求超时 / 50ms 间隔）；
    - 订阅者断开后 +3s / +6s / +9s 采样均为 **0**（tick 自停，不再拉 `router.status`）——这是"无订阅者零轮询"在真实进程上的直接观测，而非仅单测推断。
  - 首订阅播种不发事件：单测 + 上面的 6s 静置实测双重证据。
  - 查询失败不更新基线：见 1.3 的 Router 宕机段。
- 键隔离：`publishGlobal` 只写 `null` 键、`publish` 只写 chat 键（结构性，不靠过滤）；单测双向断言 + 运行时双向观测 + 反向变异（标准 9）三重证据。

### 标准 3 · 统一错误契约

**pass**
- 形状与映射（运行时实调，`node errors.js`，逐条打印状态码 / `connection` / body 键集合）：

| 触发 | HTTP | 键集合 | `code` | `error` |
|---|---|---|---|---|
| `/api/agents`（Router 不可达） | 502 | `["code","error"]` | `UPSTREAM_UNAVAILABLE` | `router 不可达或请求失败: connect ENOENT …` |
| `/api/chats/chat-nope` | 404 | `["code","error"]` | `NOT_FOUND` | `chat 不存在: chat-nope` |
| `/api/nope` | 404 | `["code","error"]` | `NOT_FOUND` | `not found: GET /api/nope` |
| `DELETE /api/agents`（方法不匹配） | 404 | `["code","error"]` | `NOT_FOUND` | `not found: DELETE /api/agents` |
| `/api/chats?limit=0` | 400 | `["code","error"]` | `INVALID_PARAM` | `查询参数非法: limit 需为 1..200 的整数（当前值 0）` |
| `POST /api/messages`（畸形 JSON） | 400 | `["code","error"]` | `INVALID_PARAM` | `请求体非法 JSON: …` |
| `POST /api/messages`（70KB 体） | 413 | `["code","error"]` | `PAYLOAD_TOO_LARGE` | `请求体过大（上限 65536 字节）`，且响应头 `connection: close` **确实存在** |
| `POST …/rename`（归档后） | 409 | `["code","error"]` | `CONFLICT` | `chat 已归档（只读），不可改名` |
| `POST /api/messages`（归档后） | 409 | `["code","error"]` | `CONFLICT` | `chat 已归档（只读），不接受新输入` |
| `POST …/activate`（未归档） | 409 | `["code","error"]` | `CONFLICT` | `chat 未归档，无法激活` |
| `/api/agents?state=offline`（Router 在线时） | 400 | `["code","error"]` | `INVALID_PARAM` | `查询参数非法: state 需为 online（当前值 "offline"）` |
| `GET /api/chats`（成功） | 200 | `["chats","limit","offset","total"]` | 无 `code` | — |

- 5 码一一映射（`web.js:81` 的 `ERR_CODE` 冻结表，5 键）全部实测命中，`code` 与状态码一一对应，成功响应不含 `code`。
- **回填完整性（独立 grep 核实，不采信执行方计数）**：
  - 基线 `git show main:oamp/src/web.js` 的 JSON 错误出口 = **19 处**：17 处 `sendJson(res, <4xx|5xx>, {…})`（行 397/407/417/467/471/475/496/500/510/515/524/548/552/557/564/619/621）+ 2 处 `sendJson(res, status, {…})`（`:490` /rename 读体、`:537` /messages 读体）。
  - worktree 现状：`sendError()` 调用点 **20 处**（`web.js:488,508,518,528,578,582,586,601,607,611,624,629,638,657,668,672,677,684,741,743`；`91` 为定义行）；**残留的 `sendJson(res, <4xx|5xx>, …)` = 0 处**；`sendJson(res, status, …)` 仅剩 `sendError()` 内部唯一构造点（`web.js:92`）。
  - 即 19 处全部改道 + 1 处新增（`?state=online` 非法值 400）= 20，**无遗漏 JSON 错误出口**。委托中提到的"执行方声称 17 处"是只数了"字面量状态码"的子集（17 处），未含 2 处经 `status` 变量的 `readBody` 分支；以实际出口点计应为 19（基线）/20（现状）——已记入偏差 D1。
- `error` 文案逐字不变：与基线逐一对照，含卡上点名的 `chat 已归档（只读），不接受新输入`、`需要 chat_id（不做全局订阅）`、`not found: <method> <path>`、`router 不可达或请求失败: …`（基线 `main:web.js:621` → worktree `web.js:743`，文案同字）。

### 标准 4 · `GET /api/agents?state=online`

**pass**
- 只返回在线：Router 上存在 `dev-a` 的 **offline 墓碑**时，无参响应 = `{"agents":[{"instance_id":"dev-a","session_id":…,"state":"offline","last_heartbeat":…}]}`（4 字段、墓碑保留），`?state=online` = `{"agents":[]}`。
- 同形状：`?state=online` 与无参响应顶层键、条目键集合完全一致（`Object.keys` 对照：顶层 `['agents']`，条目 `instance_id,last_heartbeat,session_id,state`）。
- 非法值 400：`?state=offline` → `400 INVALID_PARAM`；`?state=ONLINE`（大小写）→ 400；空值 `?state=` → 200（视为缺省）——见偏差 D2。
- **无参时响应形态与迭代前逐字一致（强证据）**：用 `git archive main oamp` 解出迭代前包，起第二台 web（端口 17790，同一 Router / 独立 DB），与 pr-002 web（17788）在同一毫秒窗口各取一次 `/api/agents`：
  - `BYTE-IDENTICAL: true`（响应体逐字节相同，含 `last_heartbeat` 原值）；
  - `content-type`、`cache-control` 头也相同。
  - 代码层同证：`sendJson(res, 200, { agents: r.nodes })` 与基线 `main:web.js:396` 逐字相同。

### 标准 5 · 浏览器实测（隔离环境 + 真实 Chromium，全部由我自行驱动）

**pass**（5 项全部通过）

**① 顶栏计数可点击 → 面板开合（点外 / Esc 关闭）**：**pass**
- 初始：`hidden=true, display:none`，`#conn-status` = `已连接 · 2 agents online`，`cursor: pointer`（`#conn-status.conn-ok{cursor:pointer}` 生效）。
- 点击 `#conn-status` ⇒ `hidden=false, display=block`，浮层 rect `{w:240,h:70,x:1109,y:40}`（`.topright` 相对定位 + 面板绝对定位生效）。
- 点击面板外（品牌区坐标 60,24）⇒ `hidden=true`；再次点击展开 ⇒ 按 `Escape` ⇒ `hidden=true`。四种状态均有 DOM 采样 + 截图（`panel-open.png` / `panel-open2.png`，视觉复核：面板浮层在计数下方，无按钮/链接）。

**② 面板列出在线实例 + 相对心跳时间**：**pass**
- 展开时行 = `dev-b | online 徽标 | 8s 前`、`dev-d | online | 0s 前`、`web | online | 2s 前`；条目数 3 = 顶栏 `已连接 · 3 agents online` = 在线实例数。
- `fmtAgo` 形态实测为 `Ns 前`（`0s 前`/`8s 前`），并随展开期 5s 刷新重算（同一行在 6s 后由 `0s 前` 变为 `6s 前`）。
- 每行三项字段 DOM 结构：`<span class="agent-id">dev-b</span><span class="badge badge-online">online</span><span class="agent-hb">8s 前</span>`（`innerHTML` 原样抓取）。
- 空集：Router 无实例时顶栏 `已连接 · 0 agents online`、面板单行 `暂无在线 agent`（`class="agent-row muted"`，实测色值 `rgb(107,114,128)` = `var(--muted)`，满足"一行灰字"）。

**③ 另一进程注册 / 注销 agent → 面板与计数数秒内自动更新**：**pass**
- 由浏览器脚本 `child_process.spawn` 另起真实进程（`node bin/oamp.js agent start dev-c/dev-d`，非页内模拟）：
  - 面板**关闭**时：注册 → 计数 `2 → 3`，耗时 **404ms**（无手动操作）；
  - 面板**展开**时：`dev-d` 行出现、计数 3；
  - 该进程 SIGINT 优雅注销（`DEREGISTERED` 日志为证）后，面板展开状态下 **1615ms** 内行消失、计数回到 `2 agents online`；
  - 时间线采样证实未命中条目的 `instance_id` 保持不变（仅新增/移除命中的那一项）。

**④ 断连时面板自动收起**：**pass**
- 面板展开状态下停掉 Router：页内 250ms 采样记录到 **5.5s** 时 `#conn-status` 由 `conn-ok`/`已连接 · 2 agents online` 变为 `conn-bad`/`Router 不可达：router 不可达或请求失败: connect ENOENT …`，且 `#agent-panel` 同时 `hidden=true`（由 `setConn(false) ⇒ closeAgentPanel()` 触发；展开期 5s 刷新是失败发现点）。

**⑤ 面板内零启停入口**：**pass**
- DOM 检索 `#agent-panel` 内 `button, a, input, select, textarea, [onclick], [role=button]` ⇒ **0 个**（空态与有数据态均为 0）；面板 `innerHTML` 仅由 `div/span` 组成，无链接、无按钮、无事件属性。
- 视觉复核（`inspect_image`）确认截图中面板无可点击控件。
- 接口面同证：路由集合中无 agent 启停路径（标准 1.6）。

### 标准 6 · 既有断言零删改

**pass**
- `git diff -U0 main...HEAD -- oamp/test/` → **删除行 0**（两个测试文件均如此）。
- numstat：`120 0 oamp/test/transport.test.js`、`293 0 oamp/test/web.test.js` —— 纯新增。
- 新增 import 行为**追加**而非替换（`git diff` 中该行以 `+` 出现，无对应 `-`）。
- 卡上点名的既有断言逐条在位（`web.test.js:952` 的 400、`:1154/1157` 与 `:1376/1377` 的 409 文案、`:1591/1592` 的新增错误契约段再次断言同文案）。

### 标准 7 · 测试（两个文件 + 全仓串行）

**partial**
- 通过部分：`node --test test/transport.test.js test/web.test.js` → **52/52 pass，0 fail**；全仓串行 `node --test --test-concurrency=1 test/*.test.js` → **235 tests / 235 pass / 0 fail / 0 cancelled / 0 skipped，退出码 0**，无任何用例失败。
- 未通过部分（计数预期）：委托标准写"期望 232"，实测 **235**。独立核对结论是"实现无缺陷、预期值失真"：
  - 基线（`git archive main oamp`）静态顶层 `test(` 计数 = **226**；worktree = **235**；差值 = **+9**，恰好等于新增用例数（`transport.test.js` 10→13，`web.test.js` 33→39）。
  - 静态计数与 `node --test` 上报的 235 完全一致 ⇒ 无用例被删、无重复计数、无静默跳过。
  - 故 235 = 226 + 9 自洽；"232"既不等于基线（226）也不等于现状（235），疑为委托侧预估残留。已记入偏差 D4。

### 标准 8 · 边界（改动文件范围 + 零新依赖）

**pass**
- PR-002 提交 `b562a26` 的 numstat 恰好覆盖允许集合：`oamp/src/transport.js`（17/3）、`oamp/src/web.js`（150/27）、`oamp/web/app.js`（103/0）、`oamp/web/index.html`（2/0）、`oamp/web/style.css`（22/1）、`oamp/test/transport.test.js`（120/0）、`oamp/test/web.test.js`（293/0），外加 `docs/…/pr-002-open-api-and-agent-panel-tasks.md`（162/0，即委托允许的 tasks 文件）。**无第 8 个代码文件**。
- 分支上其余 docs 改动来自更早的阶段 2/3 提交（`097d8c7`/`bfd45df`），不属于 PR-002 的改动面。
- 零新依赖：`oamp/package.json` 与 lockfile 相对 `main` **零 diff**；`dependencies` 仍为 `{}`；无 `node_modules`；`src/` + `test/` 全部 import 中第三方模块 = **0 个**（12 个 `node:` 内建 + 相对路径）。
- 前端唯一新增 import 行为相对路径 `../src/web.js`（测试侧具名导出单测用）。

### 标准 9 · 反向核对（因果隔离）

**pass**（做了 2 条变异，均在产出物副本 `/tmp/pr002-rev` 上进行，**未改动 worktree**）
- 变异 A「去掉键隔离」：`publishGlobal` 由 `publish(null, event)` 改为遍历所有订阅集合广播 ⇒ `test/transport.test.js` 用例 `全局键：publishGlobal 只到 /api/events 订阅者，chat 订阅者收不到（键隔离）` 立即 `not ok 1`（AssertionError）⇒ 该用例确实由键隔离因果驱动，而非恒真。
- 变异 B「去掉播种抑制」：`createTopologyWatch.tick()` 中 `if (!seeded) return;` 删除 ⇒ `test/web.test.js` 用例 `Web：createTopologyWatch——无订阅者零轮询 / 播种不发事件 / 订阅者归零后 tick 自停` 失败，失败断言正是 `首个订阅者播种基线不得发事件` ⇒ 播种抑制被该断言真实约束。
- 两条变异都是"只改一处实现 ⇒ 对应用例翻红"，且无其他用例连带失败（变异 A 运行 1 条、变异 B 运行 1 条，均 only that one）。

### 标准 10 · 清理

**pass**
- 我创建的隔离环境已全部移除：`/tmp/pr002-verify-60218`、`/tmp/pr002-verify-r2`、`/tmp/pr002-main`（基线包解包）、`/tmp/pr002-rev`（变异副本）、`/tmp/pr002-evidence.mjs`、`/tmp/pr002-verify-dir.txt`、`/tmp/pr002-full-suite.log` —— 复核 `ls -d /tmp/pr002-*` 已无属于本次验证的路径。
- 我启动的全部受管进程均为 `exited`：`pr002-router`(×2)、`pr002-web`/`pr002-web2`/`pr002-webbase`、`pr002-agent-a/-b/-cmp`、`pr002-sse/-sse2/-sse3`、`pr002-chatsse`、`pr002-silent`、`pr002b-router/-web/-web2/-agent`；端口 17788/17790 无 LISTEN 残留；无进程再引用 `/tmp/pr002-*`。
- **未触碰主仓库运行中的集群**：全程使用 `/tmp` 私有 socket 与独立 DB；验证结束时主集群 router/web/agent 三个进程仍在运行（pid 50674/50698/50708 存活）。
- 被验证 worktree 保持只读：`git status --short` 为空（无未提交改动）。
- 备注（非我产生、未删除）：`/tmp/pr002-test.diff`、`/tmp/pr002-verify/` 为 9 月 11 日遗留物，非本次验证创建。

---

## 汇总

- pass：9 项
- partial：1 项（标准 7 —— 全绿通过，但"期望 232"的计数预期与实测 235 不符；已核实为预期失真，非实现缺陷）
- fail：0 项
- blocked：0 项

---

## 偏差记录

> 实现/交付与既有规格或委托描述不一致之处。不影响本次验收判定（无 fail），但应同步文档或在下游决策。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D1 | `architecture.md` §5.3 步 2「共 **12 处**出口」、PR 卡摘要「12 处出口」；本次委托亦称执行方声称「**17 处**」 | 基线 JSON 错误出口实测 **19 处**（17 处字面量状态码 + 2 处经 `status` 变量的 `readBody` 分支，`main:web.js:397…621`）；PR-002 后 `sendError()` 调用点 **20 处**（19 回填 + 1 新增），残留 `sendJson(res,<4xx|5xx>,…)` = 0 | 按实测更新 `architecture.md` §5.3 与 PR 卡口径为「全部 4xx/5xx 出口，机械检索 `sendJson(res, [45]\d\d` 与 `sendJson(res, status`」，并把 20 写入可核对点。此项阶段 2/3 验收已记为 D1，但 `architecture.md` 正文尚未更正 |
| D2 | `architecture.md` §6.2「`state` 参数的校验：**仅接受 `online`；其它值 → 400**」 | `?state=`（空值）被当作"无参"处理 → 200 全量；`?state=offline` / `?state=ONLINE` 才 400。新增用例亦显式断言空值 200（`空值视为无参（不 400）`） | 二选一并写入文档：① 明确"空值等同缺省"（与 `qs.get('q') ?? undefined` 的既有约定一致，建议此项）；② 若坚持"其它值一律 400"，需改代码与用例 |
| D3 | `web.js` 头部注释「**全部 4xx/5xx 响应体** = {error, code}」 | `serveStatic` 的 403/404 为纯文本（`web.js:242` `'forbidden'`、`:247` `'not found'`），不过错误契约。`architecture.md` §6.1 已明确"静态面不在错误契约范围" | 收敛表述：把 `web.js` 头注释改为"全部 **API** 4xx/5xx"（或注明静态面例外）；`web.js:242` 的分支在现有 3 条固定静态路由下不可达，风险为零 |
| D4 | 委托标准 7「全仓串行（期望 **232**）」 | 实测 **235 / 235 pass / 0 fail**；基线静态计数 226 + 新增 9 = 235，静态与实际完全一致，删除行 0 | 更正验收计数来源（232 既不等于基线也不等于现状）；无需改实现 |
| D5 | 需求语义「在线 agent 列表 / agents online 计数」 | `/api/agents` 返回的 `router.status` 包含 web 进程自身（`instance_id="web"`，web 作为发送方注册），故顶栏计数与面板会把 `web` 计为一个"agent"（实测纯前端会话下为 `1 agents online`；`2 agents online` 中一个是 web）。该行为**迭代前已存在**（基线 web 的 `/api/agents` 与 pr-002 逐字节相同，已实测） | 非本 PR 引入；若产品语义要求"只列真正的 agent"，需另开决策：前端过滤 `instance_id === 'web'`，或在 `router.status` 侧区分客户端节点。当前至少应在 `API.md`（pr-003）中如实说明计数含 web 自身 |

---

## 下一迭代候选

- **Router 首订阅窗口的计数陈旧边界**：若首个 `/api/events` 订阅者建立时 Router 不可达，`prev` 保持 `null`，Router 恢复后的首次成功 tick 只**播种**（不发事件）——此窗口内新注册的 agent 不会产生 `agent_online`，在**面板关闭**且无焦点/无其他上下线事件时，顶栏计数可能陈旧到下一次变化。建议：在 `EventSource.onopen` 之外，追加"低频（如 30~60s）自愈对齐"或把 `ensureRunning` 的首次播种失败纳入重试计数。
- **`agent_online` 增量 upsert 的字段保真度**：事件路径构造的新条目 `session_id: null`（面板不展示该字段，故当前零可见影响），与 `GET /api/agents` 的 4 字段形态在内存中短暂不一致；若有后续消费方使用 `session_id`，建议 upsert 时保留服务端真值或补一次 `loadAgents`。
- **面板独占的"相对时间新鲜度"**：`fmtAgo` 只在面板展开期（5s 定时器）重算；面板关闭时 `last_heartbeat` 文本不被使用，故无影响，但若未来在文本中暴露心跳新鲜度需重新评估。
- **API.md / README 同步（pr-003）待办清单**（供下一位 owner 直接消费）：`/api/events` 的 2 类事件与载荷、`retry: 1000`/keepalive、`?state=online`（含空值口径 D2）、`GET /api/agents` 无参含 offline 墓碑（D5 需一并说明计数含 web 自身）、统一错误契约 5 码表与"同类错误跨接口一致"、静态面不在契约范围（D3）。
- **多标签页的轮询规模**：已验证拓扑轮询是**每 web 进程一条**（与订阅者数量无关，`globalCount ≥ 1` 即运行），当前无规模问题；若未来把 `chatId=null` 键复用于其他全局事件，需重新审视该判据。

---

## 结论

**PASS**

- 10 条验证标准：9 pass、1 partial（标准 7 的"232"计数预期失真，已核实为预期错误而非实现缺陷，且全仓 235/235 全绿、零删除）、0 fail、0 blocked。
- 三条硬契约（全局事件流 / 统一错误契约 / 接口面无启停路径）与 AR-01/AR-02 的前端面均有**运行时**证据（非仅单测），并附带两条反向变异证明因果隔离。
- 5 条偏差全部记录且均不阻塞交付：D1/D3/D4 为文档与计数口径问题，D2 为待确认的边界语义（建议按"空值=缺省"落文档），D5 为迭代前既有行为、本 PR 使其在 UI 上首次可见。
- 隔离环境已清理干净，被验证 worktree 保持只读，主仓库运行中的集群全程未受触碰。
