# verify-20260912-114015-pr001.md — PR-001（心跳两档与租约判活联动）独立验收

**验证者身份**（反射结果）：**同层代码审查者 + 分布式租约/心跳协议审查者**。反射依据：产出物是"UDS 协议可选字段增补 + 注册表租约判活公式 + 客户端自调度心跳"三者的联动实现，验证标准要求"真实环境复现两档行为"与"阈值公式行为实验"。该身份意味着：我读得懂协议面与租约判活的相互约束，能用真实进程独立复现心跳节奏/判活阈值/身份不变性，**不采信任何执行过程叙述**（本次委托未提供执行过程上下文，仅给产出物路径与标准）。

**产出物**：`.pb-agents/worktrees/0015-pr-001-heartbeat`（分支 `feat/0015-pr-001-heartbeat`，commit `8cee94e`；工作区 `git status --short` 为空 ⇒ 我只读、未改）
- 变更文件 7 个：`oamp/src/{agent,config,node-client,registry,router}.js` + `oamp/test/{agent-heartbeat,config-file}.test.js`
- PR 卡：`docs/iterations/0015-hub-orchestration-open-api/prs/pr-001-heartbeat-two-tier.md`

**验证标准来源**：主 agent 委托 brief 的 9 条验证标准；PR 卡『验收标准』7 条；对照基准 = `architecture.md` §3（含 §3.3 公式/§3.4 落点/§3.5 时序/§3.6 计数规程/§3.7 偏斜）、§14 L1-01（协议取值即实现契约）、§15.2 跨组契约 1~3、§16 R-1（计数边界裁决）。

**验证日期**：2026-09-12

**取证口径（必读）**
- 行为实验全部在**自建隔离环境**运行：socket 与 `OAMP_DB` 均落在 `fs.mkdtemp(os.tmpdir())` 的临时目录；**未接触主仓库 `.runtime/`、`data/sql.db`、7788 端口及运行中的集群**；worktree 零写入（唯一写入 = 本报告）。
- 实验脚本为**自建**（不复用实现方 `test/helpers` 的断言，仅在需要真实 CLI 进程时以 `bin/oamp.js` 启动被测包）：
  `/tmp/v0015/exp-{a,b1,b2,c,d,e,f,g,h}.mjs`，结果 JSON `/tmp/v0015/{b1,b2,c,e,f,g,h}-result.json`。
- 行号引用基准 = worktree HEAD（`8cee94e`）；`HEAD~1`（`406c466`）作为"迭代前"差分基准。
- **默认档 3 分钟窗口是真实运行**（exp-b2，实测 240s 生命周期），非公式替代；压缩档（50/300ms）用于高频复现两档切换与立即恢复。
- **PR 卡在本验证进行中被并发修订**：工作区 `prs/pr-001-heartbeat-two-tier.md` 与 `git show 8cee94e:…` 相差 1 行（验收 1.6 的措辞），两版均已对照，判定取**当前工作区版本**并在 1.6 中给出 diff。
- 偏差记录 **4 条**（D-1~D-4）；其中 D-4 属既有行为（迭代前后复现一致），仅登记不归因本 PR。

---

## 逐项判定

### 标准 1：PR 卡验收逐条判定 —— **partial**（1.6 部分不通过，其余 6 条通过）

**1.1 `heartbeatPlan` 两值 + 三边界（`< idleMs` / `=== idleMs` / `idleMs === null`）—— pass**
- 实现唯一落点 `oamp/src/agent.js:530-533`（`idleMs === null || idleForMs < idleMs ⇒ active`，否则 `idle`）。
- 我**直接 import 真实函数**独立调用（exp-f，非读测试）：`59_999/<10k,60k>` ⇒ `{"tier":"active","intervalMs":10000}`；`60_000` ⇒ `{"tier":"idle","intervalMs":60000}`；`MAX_SAFE_INTEGER/<10k,null>` ⇒ active；`idleForMs=0` ⇒ active。三个返回值键集完全相同（`{tier,intervalMs}`）⇒ 无第三档/退避。

**1.2 `node --test test/agent-heartbeat.test.js` 全绿 + 新增端到端档位段断言（50→300、≥3×空闲档无 `AGENT_OFFLINE`）—— pass**
- 全量套件：`node --test --test-concurrency=1 test/*.test.js` ⇒ `# tests 232 / # pass 232 / # fail 0`（也即覆盖该文件 12 例 = 既有 6 + 新增 6）。
- 我不满足于"用例绿"，用**自建隔离环境**独立复现同等行为（exp-b1，19/19 PASS）：`HEARTBEAT_TIER … tier=idle interval_ms=300` 出现；其后 `HEARTBEAT_SENT` 由 `interval_ms=50` 变 `300`（实测条目 `[03:34:00.698Z] agent HEARTBEAT_SENT instance=dev-1 tier=active interval_ms=300`，与 §3.6"进入空闲档那一跳记 active"一致）；900ms（=3×空闲档）窗口内 `tier=idle` 跳数 = 3、零 `AGENT_OFFLINE`、状态轮询恒 online。

**1.3 空闲档收到任务 ⇒ `interval_ms` 立即回 50；随后窗口 `interval_ms=50` 行 ≥4 —— pass**
- exp-b1：`message.send(task.request)` 后 **elapsed = 1ms** 即出现 `[03:34:05.117Z] … HEARTBEAT_SENT … interval_ms=50`；其后窗口内 `interval_ms=50` 计数 = 4（要求 ≥4）。恢复钩子落点 `agent.js:601-605,735-737`（`markActivity` → `resumeHeartbeat` → `client.startHeartbeat(planNext)`，首动作即"立即一跳"）。

**1.4 服务端直接证据：`LEASE_ADJUSTED` 且 `threshold_ms = max(300, 2×通告)`（通告 300 ⇒ 600）；空闲期间 `LEASE_ALARM` 零命中 —— pass**
- 实现 `oamp/src/router.js:170-179`（变化即打、无节流）。
- exp-b1 实测：`[03:34:00.386Z] router LEASE_ADJUSTED instance=dev-1 next_interval_ms=50 threshold_ms=300`、`[03:34:00.698Z] … next_interval_ms=300 threshold_ms=600`；`LEASE_ALARM` 零命中。
- exp-b2（默认档）：`next_interval_ms=10000 threshold_ms=30000`（= 基准，逐字不变）与 `next_interval_ms=60000 threshold_ms=120000`（= 2×）。

**1.5 身份不变：tier 行 `session=` 与 `AGENT_REGISTERED` 同值；空闲期间无 `AGENT_REGISTERED`/`AGENT_OFFLINE`/`agent.replaced` —— pass**
- exp-b1：`HEARTBEAT_TIER … session=6ff7ed17-…` 与 `AGENT_REGISTERED … session=6ff7ed17-…` 同值；`AGENT_REGISTERED`（dev-1）全程 = 1、`AGENT_OFFLINE` = 0、agent 侧 `REPLACED`=0 / `CONNECTION_LOST`=0；全程 `session_id` 快照不变。
- exp-b2（默认档 240s）：`AGENT_REGISTERED`=1、`AGENT_OFFLINE`=0、每 5s 轮询 `state==='online'` 且 `session_id` 恒等。

**1.6 兼容零破坏（既有 agent / 固定数字入参调用点）—— partial**

- **卡片在本验证进行中被并发修订**（`git diff` 单行改动，旧 → 新）：
  - 旧：`心跳无 next_interval_ms（既有 agent / 假节点 / web 常驻发送方的固定数字入参）⇒ 阈值回退基准 …，且不产生 LEASE_ADJUSTED`
  - 新：`既有 agent（心跳无 next_interval_ms）⇒ 阈值回退基准 …，且不产生 LEASE_ADJUSTED；假节点 / web 常驻发送方的固定数字入参 → 按契约通告与自身间隔同值的数（会各打 1 条 LEASE_ADJUSTED，但阈值与行为逐字不变）`
- **通过的子项**（按新版逐条实测）：
  1. 既有 agent（心跳体无该字段）⇒ 回基准 + 零 `LEASE_ADJUSTED`：exp-b2 实测 `legacy-a` 无 `LEASE_ADJUSTED`、31s（基准 30000 + 750ms 扫描）判 `AGENT_OFFLINE`；`legacy-c`（非法值 `0`）同样回基准、零 `LEASE_ADJUSTED`、同样 31s 判离线（fail-closed）。
  2. 固定数字入参"通告与自身间隔同值的数"：exp-g 实测 `startHeartbeat(50)` ⇒ `next_interval_ms=50`；`startHeartbeat(1000)` ⇒ `next_interval_ms=1000`；真实 `oamp web start`（隔离端口/socket）⇒ `next_interval_ms=10000`（= `web.js:324` 解析出的 `heartbeatMs`）。
  3. "各打 1 条 `LEASE_ADJUSTED`"：单个注册会话内每次通告值变化恰 1 条（`router.js:172-179`）。web 进程实测出现 **2 条同值行**，原因是它在派发触发时**并行注册了 2 次**（`AGENT_REGISTERED instance=web` ×2，时间戳相差 1ms）——该双注册在 **`HEAD~1`（迭代前）以同一脚本复现结果相同**（`registered=2`；旧版 0 条 `LEASE_ADJUSTED`，因该事件当时不存在）⇒ 属 web 懒初始化发送方的**既有竞态**，与本 PR 无关。
- **不通过的子项**："阈值与行为逐字不变"在**非默认 env** 下不成立：
  - 默认配置（`interval=10000/timeout=30000`）⇒ web 发送方 `threshold_ms=30000` = 基准，逐字不变 ✓；假节点 `50/300` ⇒ `300` = 基准 ✓。
  - 压缩 env（`interval=50/timeout=300`，`web.js:324` 因 `max(500, …)` 下界取 500）⇒ 实测 `LEASE_ADJUSTED instance=web next_interval_ms=500 threshold_ms=1000`，其**自身**判活阈值被抬到 1000 ≠ 基准 300；凡满足 `2 × heartbeatMs > heartbeatTimeoutMs` 的 env（含用户把 `OAMP_HEARTBEAT_TIMEOUT_MS` 设到 < 1000）皆如此。
- 判定：新版卡片修正了旧版的事实性枚举错误（旧版把固定数字入参说成"不发该字段"，与实现及 `architecture §3.4` 冲突）；剩余不一致仅在"阈值与行为逐字不变"这一绝对化措辞——**语义零破坏在默认配置下成立**，在窄带非默认 env 下该调用点自身的离线发现被拖长。故整条判 **partial**。偏差见「偏差记录」D-2。

**1.7 回归锁零修改全绿 + 既有用例逐字未改 + `snapshot()` 4 字段 —— pass**
- 见标准 6、标准 8 的证据（全量 232 绿；`agent-heartbeat.test.js` 既有 6 例与 `HEAD~1` **逐字节一致**；`snapshot()` 4 字段）。
- `oamp status`（真实 CLI，隔离 socket）输出表头 `instance_id session_id state last_heartbeat`，条目键集恰为 4 字段（exp-f）。

### 标准 2：协议面（`agent.heartbeat` 可选参数 / `agent.register` 回包）—— **pass**

- **`next_interval_ms` 语义与校验**：`registry.js:17`（`MAX_ANNOUNCED_INTERVAL_MS = 600000`）、`registry.js:20-21`（`Number.isInteger(v) && v>=1 && v<=600000`）、`registry.js:88`（非法/缺省 ⇒ 落 `null`＝未通告）；`router.js:165-169` 仅透传 `params.next_interval_ms`，不在 Router 侧另做语义分支。落点唯一、与 §15.2 契约 1 逐字一致。
- **非法/缺省 fail-closed 的行为实验**（exp-a，真实 `createRegistry`）：`0 / -1 / 1.5 / '300' / 600001 / null / undefined / NaN / Infinity / true / [300] / {}` 全部 `stored=null`，且 `last_heartbeat` **仍被刷新**（心跳仍然算数），`findExpired` 回基准 ⇒ fail-closed 方向正确；`600000` 合法、`600001` 非法。
- **`agent.register` 回包**：`router.js:155` 追加 `lease_follows_interval: true`；既有 5 字段（`instance_id/session_id/state/lease_timeout_ms/last_heartbeat`）取值未动 —— `git diff HEAD~1 HEAD -- oamp/src/router.js` 中 register 回包仅 `+` 注释与 `+lease_follows_interval: true` 两行；实机回包（exp-b2）`{"instance_id":"legacy-b","session_id":"7b61…","state":"online","lease_timeout_ms":30000,"last_heartbeat":1789184063299,"lease_follows_interval":true}`。
- `registry.snapshot()`（`registry.js:153-163`）仍只投影 4 字段 ⇒ 新增条目字段不外泄（exp-a 实测 `[{"instance_id":"x","session_id":"s-x","state":"online","last_heartbeat":2}]`）。

### 标准 3：阈值公式唯一落点 + 未通告逐字一致 + 通告 `max(基准, 2×通告)` + 非法 fail-closed —— **pass**

真实 `registry.js` 行为实验（exp-a，`/tmp/v0015/exp-a.mjs`）：
- **唯一落点**：全仓仅 `registry.js:130-133` 做阈值推导（`git diff` 中无第二处 `next_interval_ms` 参与判活；Router 侧的 `threshold_ms` 只用于日志字段 `router.js:177`）。
- **未通告 ⇒ 与迭代前逐字一致**：我把 `HEAD~1` 的 `registry.js` 抽到 `/tmp/v0015/registry-old.mjs` 做**差分**，在 `base∈{300,30000}` × `delta∈{0,1,299,300,301,599,600,601,1200,1201,30000,30001,119999,120000,120001}` 上逐点比较 `findExpired` ⇒ **IDENTICAL**（两组基全部一致）。
- **通告 ⇒ `max(基准, 2×通告)`**：默认配置 `announce=60000/base=30000` ⇒ `+119999` 未到期、`+120000` 未到期（严格 `>` 才到期）、`+120001` 到期；`announce=100/base=300` ⇒ 阈值兜底为 300（不被缩小）；`announce=1` ⇒ `max(base,2)`；`announce=600000` ⇒ 1200000（`+1200000` 未到期 / `+1200001` 到期）。
- **fail-closed**：非法通告视同未通告（见标准 2 的 12 组取值）。

### 标准 4：两档行为真实复现（自建隔离环境）—— **pass**

- **活跃档频率不变**：exp-b1 实测活跃跳 `meanΔ = 52.2ms`（配置 50ms，6 跳 `[53,52,52,52,52]`）。
- **空闲档 = 6× 活跃**：exp-b1 实测空闲跳 `meanΔ = 301.7ms`，`ratio = 5.78`（14 跳样本）；exp-b2 默认档实测 `deltas=[59999,60000]`（配置 60000ms ＝ 6×10000）。
- **空闲期间保持 online、零重注册、`session_id` 不变**：exp-b1（900ms 窗口 + 3.3s 观察）与 exp-b2（**真实 3 分钟窗口**，每 5s 轮询）均满足；`AGENT_REGISTERED`=1、`AGENT_OFFLINE`=0、`session_id` 恒等。
- **收到任务后立即恢复活跃档**：exp-b1 `elapsed=1ms`；且任务在飞（900ms shell 任务）期间不得跌回空闲档（`HEARTBEAT_TIER` 计数不变）。
- **§3.6 计数规程下「3 分钟 ≤3 跳」**：
  - 压缩档（50/300）：窗口左端取 `HEARTBEAT_TIER … tier=idle` 那条**之后**，900ms（=3×空闲档）内 `tier=idle` 跳数 = **3**；
  - 默认档（10000/60000，**实跑非推导**）：`tier=idle` 记录出现在 `03:35:23.294Z`，其后 180s 窗口内 `tier=idle` 跳数 = **3**，窗口内恒 online、零 `AGENT_OFFLINE`、零重注册、零 `LEASE_ALARM`；
  - 双证自洽：`180s / 60s = 3`，与实测一致。

### 标准 5：降级路径（旧 Router / 既有客户端）—— **pass**

- **旧 Router（注册回包无 `lease_follows_interval`）**：exp-c 自建"旧 Router"（进程内 UDS 服务端，**租约判活用迭代前实现的 `registry-old.mjs` 驱动固定阈值扫描**）+ 真实 agent 子进程（SHORT env），9/9 PASS：
  - `HEARTBEAT_IDLE_DISABLED instance=dev-1 note="router 未通告 lease_follows_interval…"` 出现（`agent.js:711-717`）；
  - 900ms 内 18 跳全部 `tier=active interval_ms=50`，零 `HEARTBEAT_TIER`（禁用空闲档）；
  - **register 请求恰 1 次**（不换会话）、agent 侧零 `CONNECTION_LOST`/`RECONNECT_WAIT`（未被判离线）、旧版固定阈值扫描对该实例**零命中**。
- **既有客户端（心跳体无 `next_interval_ms`）**：exp-b2 实测 `legacy-a` 无 `LEASE_ADJUSTED`、31s（基准 30000 + 750ms 扫描）判 `AGENT_OFFLINE`；同一时刻通告 60000 的 `legacy-b` 仍 online（阈值 120000）。两实例同基准对照，因果清晰。

### 标准 6：回归（全量 232 绿 + 既有测试删除行为 0）—— **pass**

- `cd oamp && node --test --test-concurrency=1 test/*.test.js` ⇒ `1..232 / # tests 232 / # pass 232 / # fail 0`（期望 232 ✓）。**该全量含 `event-log.test.js`**（4/4 单独复跑确认），因此阶段 2/3/4 验证提出的"pr-001 回归清单缺 `event-log.test.js`"这一 partial 在本轮被实测覆盖。
- **既有测试删除行为 0**：`git diff HEAD~1 HEAD -- oamp/test/agent-heartbeat.test.js | grep '^-[^-]'` 仅 1 行（`import` 行扩写）；既有 6 例函数体与 `HEAD~1` **逐字节 diff 为空**（唯一差异是新段前的 3 行分隔注释，位于既有用例之外）。`config-file.test.js` 删除行 = 0。
- **`config-file.test.js` 的改动属"变更必然影响面"而非削弱**：两处均为**新增**（`heartbeatIdleMs: 60000` 进入 `assert.deepEqual` 全量断言；`assert.equal(config.heartbeatIdleMs, 1234*6)`），因为该用例以整对象深比较断言 config 形状，新增派生键必然要求同步——断言强度**增加**，未删除或放松任何既有断言。

### 标准 7：新增 agent 侧事件行必须满足 `event-log.test.js` 行格式正则 —— **partial**

- 正则 `^\[…Z\] (router|agent) ([A-Z_]+)((?: [a-z_]+=[^ ]+)*)$` 逐行套用（exp-d）：
  - `HEARTBEAT_SENT instance=dev-1 tier=active interval_ms=50` ⇒ **MATCH**
  - `HEARTBEAT_TIER instance=dev-1 session=<uuid> tier=idle interval_ms=300` ⇒ **MATCH**
  - `HEARTBEAT_IDLE_DISABLED instance=dev-1 note="router 未通告 lease_follows_interval（判活阈值固定）：空闲档已禁用，保持活跃档"` ⇒ **NOMATCH**（`formatValue` 对含空格值加引号，`[^ ]+` 无法匹配引号内的空格）
- 一致性说明：同一形态在**迭代前已存在**（`LEASE_ALARM` 的 `note` 同为正则不可匹配，exp-d 对照亦 NOMATCH），故这是**既有约定的延伸**而非新增破坏；`event-log.test.js` 4/4 绿（其 F06-2 只覆盖正常路径，不触发该行）。判定为 partial（3 条新事件行中 2 条合规、1 条不合规）。

### 标准 8：边界（改动文件集 + 零新依赖）—— **pass**

- `git diff --name-only HEAD~1 HEAD` ⇒ 恰为 `oamp/src/{agent,config,node-client,registry,router}.js` + `oamp/test/{agent-heartbeat,config-file}.test.js`，7 个文件，无越界。
- 零新依赖：`package.json` 未被改动（`git diff --name-only … -- '*package.json'` 为空）；新增 import 仅 `node:net`/`node:fs`/`node:path` 与包内相对路径（`./helpers/*`、`../src/*`）。
- worktree 未被我的验证写入（`git status --short` 空）。

### 标准 9：反向核对（因果隔离，1~2 条）—— **pass**（实做 2 条，均在代码副本上做退化突变）

突变副本：`fs.cpSync(worktree/oamp → /tmp/v0015/mut{1,2})`，改动仅在副本内，worktree 零写入。判别配置取 `interval=100/timeout=300`（空闲 600ms **>** 基准阈值 300ms，具备判别力）：
- **反向核对-1（阈值公式退化为基准）**：把 `findExpired` 的公式强制为 `const timeoutMs = baseTimeoutMs;` ⇒ 同配置下 `AGENT_OFFLINE` **命中 2 次**、`state=offline`、空闲跳数为 0；而未突变基线同配置下 `offline=0 / state=online / sentIdle=3`、`LEASE_ADJUSTED … threshold_ms=1200`。⇒ 证明"空闲期间不被判离线"确实由 `max(基准, 2×通告)` 产生，而非扫描未跑或其他偶然因素。
- **反向核对-2（档位不切换）**：把 `heartbeatPlan` 改为恒返回 `active` ⇒ 档位事件 0、空闲跳 0、活跃跳 26；基线同配置下 `tierEvents=1 / sentIdle=3`。⇒ 证明降频与档位观测面确实由 `heartbeatPlan` 的判定驱动。

---

## 汇总

- 逐项判定项数：9 条委托标准（其中标准 1 展开为 7 个 PR 卡子项，单独列出）
- **pass：7**（标准 2、3、4、5、6、8、9）
- **partial：2**（标准 1 = 其中 PR 卡 1.6；标准 7 = 三条新事件行中的 `HEARTBEAT_IDLE_DISABLED`）
- **fail：0**
- **blocked：0**
- PR 卡 7 条验收子项：pass 6 / partial 1（1.6）/ fail 0

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定（无 fail），但应在下一迭代同步文档或评估是否调整实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | `architecture §3.7` + `event-log.test.js` 行格式正则要求事件行值为"无空格"的 `key=value`（`[a-z_]+=[^ ]+`） | 新事件 `HEARTBEAT_IDLE_DISABLED` 的 `note` 含空格与中文，被 `formatValue` 引号包裹（`agent.js:713-716`），正则不可匹配；`log.js` 的"值含空格即引号"与测试正则本就存在口径差，既有 `LEASE_ALARM` 同型（`agent.js:704`） | 按实现更新文档（在 §8.1 明确"含空格值引号包裹、解析方须支持引号"）**或**把告警 `note` 改为无空格字段（如 `reason=router_no_lease_follows` + 独立说明）；同时给 `event-log.test.js` 的正则加引号分支（开新迭代处理） |
| D-2 | PR 卡验收 1.6（**工作区已修订版**）："…固定数字入参 → 按契约通告与自身间隔同值的数（会各打 1 条 `LEASE_ADJUSTED`，但阈值与行为逐字不变）"；旧版（`8cee94e`）则称"固定数字入参 ⇒ 阈值回退基准且不产生 `LEASE_ADJUSTED`" | `node-client.js:159` 使固定数字入参**恒携带** `next_interval_ms` ⇒ 假节点与 web 常驻发送方**会**各产生 `LEASE_ADJUSTED`（默认配置阈值仍 = 基准；**非默认 env** 下不成立：压缩 env 实测 web `next_interval_ms=500 threshold_ms=1000 ≠ 基准 300`，凡 `2×heartbeatMs > heartbeatTimeoutMs` 皆如此）。无字段的真实旧客户端与旧/新卡片均一致 | 新版卡片已与实现对齐（"各打 1 条"按注册会话成立）；仅"阈值与行为逐字不变"的绝对化措辞需限定为"默认配置"，或架构侧裁决固定数字入参是否该发 `next_interval_ms` |
| D-3 | PR 卡『文件范围』未列 `oamp/test/config-file.test.js` | 实现确实修改了该文件（新增 2 行派生键断言），且委托标准 8 已显式允许 | 按实现更新 PR 卡文件范围（文档同步，无代码影响） |
| D-4 | 无规格条目（既有行为，非本 PR 引入） | `oamp web start` 在派发触发时对同一 `instance_id=web` **并行注册两次**（`AGENT_REGISTERED ×2`，时间戳相差 1ms，第二次按 latest-wins 顶替第一次）；`HEAD~1` 以同一脚本复现结果相同 ⇒ 与 PR-001 无关 | 记入下一迭代候选（web 懒初始化 `ensureSender` 的并发竞态）；本 PR 无需处理 |

---

## 下一迭代候选

- **事件行 `note` 字段的统一口径**：把"含空格/中文的值"从行格式契约中移除或显式纳入解析规范，避免出现第二处无法被现有正则解析的事件行（D-1）。
- **固定数字入参是否应通告**：当前 `startHeartbeat(number)` 会为自己声明租约阈值（`threshold = max(base, 2×number)`）。web 常驻发送方在默认配置下无影响，但在 `2×heartbeatMs > heartbeatTimeoutMs` 的环境（例如压缩 env 或用户把 `OAMP_HEARTBEAT_TIMEOUT_MS` 设到 <1000）下，其离线发现被自身通告拖长；若"发送方节点也须按配置阈值判活"是运维预期，需要架构级裁决（D-2）。
- **PR 卡回归清单补齐**：把 `event-log.test.js` 显式列入 PR-001 回归清单（本轮全量 232 已覆盖，清单层面仍是缺口）。
- **web 常驻发送方启动期双注册竞态**（D-4）：`ensureSender` 无并发保护，派发触发时对同一 `instance_id=web` 连发两次 `agent.register`（`HEAD~1` 同样复现）⇒ 每次触发多一条 `LEASE_ADJUSTED` 与一次 latest-wins 顶替；与 PR-001 无关，但会掩盖"每会话 1 条"的观测口径。
- **`registry.findExpired` 的阈值可观测性**：当前只有"通告值变化"才打 `LEASE_ADJUSTED`；若后续 E4/运维需要"每实例当前生效阈值"快照，可考虑在 `router.status` 之外补一个只读投影（本轮不做，仅记录）。

---

## 结论

**PASS**（fail = 0；2 项 partial 均为"文档/口径与实现不一致"或"既有约定的延伸"，不构成返工项；偏差 D-1~D-4 不阻塞本迭代闭环，其中 D-4 属既有行为）。

补充说明（供主 agent 决策，非判定）：
1. 两档机制、租约联动公式、身份不变性、降级路径均已在**自建隔离环境**用真实进程独立复现，且默认档 3 分钟窗口为**实跑**（`tier=idle` 3 跳）；反向突变实验证明上述行为确由被验证的两处落点产生。
2. 本 PR 的对外可观测面（UDS 协议 + `registry.snapshot()` 4 字段 + `oamp status`）未发生破坏性变化；唯一新增的"对外可见副作用"是日志行 `LEASE_ADJUSTED`（新事件，含固定数字入参调用点，见 D-2）与 `HEARTBEAT_SENT`/`HEARTBEAT_TIER`（agent 侧观测面）。
