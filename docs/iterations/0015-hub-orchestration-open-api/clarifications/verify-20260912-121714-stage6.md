# verify-20260912-121714-stage6.md — 0015 阶段 6（最终产物整体验收 + 并发调度真实执行证据三项核查）

**验证者身份（反射结果）**：迭代交付验收人 / 发布门禁评审者。本委托同时覆盖「产物质量」与「流程契约是否被真实执行」，故反射为两种视角的复合身份：
- **代码与文档质量评审者**——熟悉本仓库既有模式（UDS JSON-RPC 协议面、SSE 传输层、registry 快照 4 字段投影、sqlite 持久化、原生前端控制台），能判断实现是否真正满足三条硬契约，而不是"文档说满足"。
- **流程契约审计者**——对"并发是否真的发生过"这类流程事实不采信自我声明，只认 git 对象/引用一手记录、文件系统元数据与带时间戳的第三方报告。

**产出物**：
- 迭代分支 `iteration/0015-hub-orchestration-open-api`，HEAD `0db206e`（3 个 PR 全部已合并进迭代分支：`dcf0572` pr-001、`c5bc42b` pr-002、`a7e79cc` pr-003）
- docs 产物：`demand.md`(293 行) / `prd.md`(214)+`prd/F01~F08`(8 卡) / `architecture.md`(805 行) / `prs/pr-001~003` + `prs/pr-00{1,2,3}-*-tasks.md` / `status.md`(55) / `history.md`(145) / 既有 4 份 verify 报告
- 代码与文档：`oamp/src/{config,registry,router,node-client,agent,transport,web}.js`、`oamp/web/{index.html,app.js,style.css}`、`oamp/API.md`(855 行)、`oamp/README.md`(306 行)、`oamp/test/{agent-heartbeat,config-file,transport,web}.test.js`

**验证标准来源**：主 agent 委托的 A~I 九条；其中 B（并发三项）、C（依赖正确性）、D（PR 粒度）逐条对应 `workflow-pb.md` v0.8.0 §验证目标的三节原文。

**验证日期**：2026-09-12
**独立性声明（对执行过程上下文的处理）**：委托 brief 中携带的执行过程陈述——"首批 pr-001 ∥ pr-002 在同一轮响应内并发派发""两者 base 同为 `bfd45df`""pr-003 在其后"——**未被采信为证据**。报告里与之对应的三条结论（B① 的同基分叉、B② 的派发计数、C 的合并顺序）全部由 `git cat-file` / `git merge-base` / `git show <commit>:status.md` 与 `history.md`、两份 PR 验收报告的时间戳**独立重建**；重建结果与委托陈述一致，但依据不是委托陈述本身。

**边界与未触碰声明（含方法学偏差披露）**：
- 只读验证，唯一写入 = 本报告；未修改任何被验证产物（验证前后 `git status --short` 逐字相同：3 个与本迭代无关的既有已修改文件）。
- **未触碰主仓库运行中的集群**：全程**未在仓库根执行测试套件**（与标准 F 的字面要求不同 ⇒ 见 F 的显式披露与替代取证）；全量测试在 `/tmp` 克隆中运行；独立行为核查使用临时 `OAMP_SOCKET=/tmp/…/router.sock`、`OAMP_DB=/tmp/…/db.sqlite`、端口 17779。收口后复查：`oamp/.runtime/router.sock` 在、`oamp/.runtime/cluster/{pb-verifier,router}.log` **只增长未被截断**（活集群自身写入）、`127.0.0.1:7788` 仍由 pid 6583 LISTEN、`tmux ls` = `oamp-cluster 12 windows`。
- 隔离环境全部清理：`hub stop oamp15-{router,web,agent-a}`、浏览器 tab 已释放、`rm -rf /tmp/v0015-stage6`（复查不存在）、`pgrep -f v0015-stage6` = 0、端口 17779 无 LISTEN。
- 过程失误如实披露：早期一次 `oamp web start --help` 因该 CLI 不支持 `--help` 而尝试绑定默认 7788（被活集群占用，EADDRINUSE 退出）；一次 `oamp agent start --help` 误把 `--help` 当实例名起了一个节点（连接的是克隆目录下的 socket，未触及活集群），已在 30 秒内 `hub cancel`。两者均未写入主仓库 `.runtime/` 或 `data/`。

---

## 逐项判定

### A. 阶段 5 推进条件（逐 PR 核实：验收标准 / 无简报外改动 / PR 文件齐备）—— **partial**（9 个子项：pass 6 / partial 3 / fail 0）

#### A-1 pr-001（心跳两档）

**A-1a 验收标准——partial**（7 条卡片子项：pass 6 / partial 1；报告结论 PASS、0 fail）
证据：`clarifications/verify-20260912-114015-pr001.md` §汇总「pass 7 / partial 2 / fail 0」，其中标准 1 = PR 卡验收逐条，1.1~1.5、1.7 pass，**1.6 partial**。1.6 的不通过子项是卡片措辞的绝对化（"阈值与行为逐字不变"在 `2×heartbeatMs > heartbeatTimeoutMs` 的非默认 env 下不成立，压缩 env 实测 web 自身 `next_interval_ms=500 threshold_ms=1000 ≠ 基准 300`）；而该卡在验证进行中已被并发修订（`8cee94e:prs/pr-001-*.md` 与工作区版本差 1 行），修订后仍保留这句绝对化措辞。后由主 agent 裁决"以契约为准"并修正卡片（见 `history.md` 11:43:10 收到报告第 3 项）。

**A-1b 无简报外改动——partial**
- 事实：`git diff --stat bfd45df 8cee94e` = `oamp/src/{agent,config,node-client,registry,router}.js` + `oamp/test/{agent-heartbeat,config-file}.test.js`，而卡片『文件范围』只列了 6 项，**未列 `oamp/test/config-file.test.js`**（实改 +3 行）。
- 减轻事实：执行方主动上报越界（`history.md` 11:43:10"越界"项）；pr-001 验收报告的委托标准 8（改动文件集 + 零新依赖）**显式允许**并判 pass（报告 §标准 8）；改动方向是**断言强度增加**（`heartbeatIdleMs: 60000` 进入整对象 `deepEqual` + `assert.equal(config.heartbeatIdleMs, 1234*6)`，删除行 = 0），且属"config.js 新增派生键"的必然影响面（该用例以整对象深比较断言 config 形状）。
- 判定依据：卡片文件范围是阶段 5 简报的正式载体，§阶段 5 推进条件写的是"无简报外改动"，字面不成立；但"被授权 + 必要性 + 已披露"三点齐备 ⇒ 不作 fail，判 partial，并要求补卡片（偏差 D3，至今未补）。

**A-1c PR 文件齐备——pass**
证据：`prs/pr-001-heartbeat-two-tier.md` 七字段齐备（`上下文摘要/涉及功能点/文件范围/验收标准/参考资料/depends_on/batch`）；阶段 5 输出 `prs/pr-001-heartbeat-two-tier-tasks.md` 存在（commit `406c466`，parent = `bfd45df`）。

#### A-2 pr-002（开放接口 + 顶栏面板）

**A-2a 验收标准——pass**
证据：`verify-20260912-114018-pr002.md` §汇总「pass 9 / partial 1 / fail 0」；标准 1 的 9 条卡片子项 1.1~1.9 **全部 pass**（1.7/1.8 为浏览器实测，1.2 键隔离、1.3 `/api/events`、1.5 错误契约均 pass）。唯一的 partial 是**委托书写错计数预期**（"期望 232"vs 实测 235），报告已核实"静态计数 226+9=235 自洽、删除行 0"，与卡片验收标准无关。

**A-2b 无简报外改动——pass**
证据：`git diff --stat bfd45df b562a26` = `transport.js / web.js / web/{app.js,index.html,style.css} / test/{transport,web}.test.js` + `prs/pr-002-*-tasks.md`（阶段 5 契约产物），与卡片『文件范围』逐项一致，无第 8 个代码文件；`oamp/package.json` 与 lockfile 零 diff。

**A-2c PR 文件齐备——pass**（七字段齐备；`pr-002-*-tasks.md` 随 `b562a26` 提交）。

#### A-3 pr-003（API 文档）

**A-3a 验收标准——partial**
- 报告期事实：`verify-20260912-120246-pr003.md` 判定 AC5 partial + 标准 2④ partial + 标准 3 partial，拆开是**两处真实事实性错误**（§5.3 示例引用无生产者对话 `chat-demo-1`，干净环境原样执行 404，与文档期望 200 不符；`message_id` 语义写反——文档称"库内消息 id"，实测返回的是 `msg-<uuid>` 派发信封 id）**+ 一处措辞失真**（`archived`"三态"）。报告的结论行是 PASS（0 fail），但"验收标准全部通过"字面不成立。
- 返工事实：报告写于 **12:02:46**，返工提交 `e54cd40` 在 **12:06:29**（其后 3m43s），修 7 处（D1~D7）。
- **我的独立复核（逐条 diff 对代码）**：`oamp/API.md:445` 的 `message_id` 已改为"派发信封 id（`msg-<uuid>`）…不是库内消息 id"，与 `oamp/src/web.js` 的 `messageId = 'msg-'+randomUUID()` → 作信封 id 发 `sendTask` → 作响应字段返回（`web.js:695/716/730`）一致；§5.3 改用 `chat-demo-shell` 并加前置说明（该对话由 §5.4 的 shell 演示创建，§5.3 正文于 `API.md:603`、§5.4 于 `API.md:617`）；`archived` 改为"合法值只有 0/1"，与 `persist.js:105-113` 的 `readArchived` 一致（实测 `archived=2` → 400）；路径穿越括注改为"落 404 兜底"；离线墓碑拆为"判活超时保留 / 优雅注销删除"两类（与 `registry.js` 的 `deregister → entries.delete` 一致）；`/api/events` 时延补"≤2s 是轮询腿、强杀须先等租约阈值（默认 30s）"。⇒ **修正内容与代码一致，缺陷实质已消除。**
- 判定依据："验收标准全部通过（交叉引用 verify 报告）"这一条的证据链在**"返工后无独立复核"**这一环缺失（返工只有执行方自述 + 我的本次 diff 复核，无 verifier 复验报告）⇒ partial；同时如实说明：实质缺陷已由本次独立复核确认消除（见偏差 D5 的处理建议）。

**A-3b 无简报外改动——pass**（`git diff --stat c5bc42b e54cd40` = `API.md` + `README.md` + tasks 文件，与卡片一致；API.md 与 README.md 之外无第三个文件。）

**A-3c PR 文件齐备——pass**（七字段齐备；`pr-003-*-tasks.md` 见 `8e6c9bf`）。

---

### B. 并发调度真实执行证据（工作流强制三项）—— **pass 2 / partial 1**

> 前置事实（决定本迭代是否受本项约束）：`prs/` 阶段 4 产出 3 个 PR，其中 pr-001 与 pr-002 的 `depends_on` 均为 `（无）` ⇒ 存在真实并发分支，触发条件成立，三项必须核查。

#### B① worktree 时间窗口重叠 —— **pass**（附证据强度说明）

逐条一手证据：

1. **两条 PR 分支同基分叉（git 对象级）**：`git merge-base --all dcf0572^1 dcf0572^2` = `bfd45df`；`git merge-base bfd45df dcf0572^2` = `bfd45df`；`git merge-base bfd45df c5bc42b^2` = `bfd45df`。且 `git cat-file -p 406c466`（pr-001 分支首个提交）与 `git cat-file -p b562a26`（pr-002 分支唯一提交）的 parent **同为 `bfd45df`**。
   ⇒ 两个 worktree 分支都是在 `iteration/0015` 的 tip 尚为 `bfd45df` 时创建的，即落在 **11:05:21（`bfd45df` 提交）~ 11:08:45（`86cc8fd` 提交，tip 前移）** 这 3 分 24 秒窗口内（工作流的创建命令为 `git worktree add <path> -b <branch> iteration/{迭代ID}`，base = 创建时刻的 tip）。这是**创建区间重叠**的直接证据。
2. **两次独立验收在同一时刻各自确认自己的 worktree 在盘**：`verify-20260912-114015-pr001.md`（时间戳 11:40:15）的产出物 = `.pb-agents/worktrees/0015-pr-001-heartbeat`（分支 `feat/0015-pr-001-heartbeat`，commit `8cee94e`，`git status --short` 为空）；`verify-20260912-114018-pr002.md`（11:40:18）的产出物 = `.pb-agents/worktrees/0015-pr-002-api-panel`（分支 `feat/0015-pr-002-api-panel`，commit `b562a26`）。**两份报告相差 3 秒**，而 pr-001 的验收内含一次**实测 240 秒**的真实窗口实验（`verify-…-pr001.md` 取证口径第 4 条）。若两次验收是串行的，两次报告落盘间隔不可能只有 3 秒 ⇒ 两份验证在时间上重叠 ⇒ **两个 worktree 在 11:40 前后同时在盘**。
3. **旁证（同窗口第三处并发位点）**：`verify-…-pr001.md` 记载"PR 卡在本验证进行中被并发修订"，而该卡修订随 `bca9f5e`（11:43:10）提交 ⇒ 主仓库工作区在 11:40 与两个 worktree 同时活跃——结合同一轮响应内两条派发记录（`history.md` 11:08:45 一条 `派发 · dev（pr-001-heartbeat-two-tier ∥ pr-002-open-api-and-agent-panel）` 记录内并列两个 PR）。
4. **收尾对齐**：两次 merge 落在**同一秒** `2026-09-12 11:43:10 +0800`（`dcf0572`、`c5bc42b`）；`status.md` 两行均标 `(已清理)`；`git worktree list` 现状仅剩 0004 遗留目录（0015 的 3 个 worktree 均已清理）。

**证据强度说明（不遮掩）**：产出物中**不存在**一份包含两个 0015 worktree 同时出现的 `git worktree list` 输出或 `progress.md`（本迭代未派发 `progress-observer`）。因此 B① 的结论建立在"创建区间重叠（证据 1）+ 验收期同时在盘（证据 2）+ 同秒合并（证据 4）"的链式推断上，而不是单张快照。该推断链每一环都是一手记录，且相互独立；我认为足以判定"并发确实发生过，不是先后串行后误报"，但读者应知道它与 0005 报告设想的取证形态（worktree list 快照）不同。

#### B② 并发配置区块真实初始化与更新 —— **pass**

逐版本一手证据（`git show <commit>:status.md` 的 `## 并发配置（阶段 5）` 区块）：

| commit | 时刻 | 起始 | 硬上限 | 当前有效上限 | 累计槛位释放 | 已派发总数 |
|---|---|---|---|---|---|---|
| `86cc8fd`（阶段 4 规划 + 首批派发同轮） | 11:08:45 | 3 | 5 | 3 | 0 | 2 |
| `b749d4d`（两 PR 合并 + 派发 pr-003） | 11:43:11 | 3 | 5 | 3 | **2** | **3** |
| `0db206e`（pr-003 合并 + 派发阶段 6） | 12:09:34 | 3 | 5 | 3 | **3** | 3 |

⇒ 五字段**在阶段 4→5 入口被真实初始化**（全部有值），且**随后续释放被真实更新两次**（累计释放 0→2→3、已派发 2→3），不是"初始化后再未变化"。硬上限 5 = 2×3-1 与公式一致。

#### B③ 爬升公式真实触发 —— **partial**（触发条件成立、计数器被真实更新；`当前有效上限` 未按公式重算）

- 公式（`workflow-pb.md` §并发槛位算法）：`当前有效上限 = min(起始并发数 + 累计槛位释放次数 × 起始并发数, 硬上限)` = `min(3 + 累计×3, 5)`。
- 实际：`累计 = 2`（11:43:11）时公式值 = `min(9, 5)` = **5**，落盘值 = **3**；`累计 = 3`（12:09:34）时公式值 = **5**，落盘值 = **3**。⇒ **字段维持初始值不变，未按公式重算**。
- 通过的部分：爬升触发条件确实成立（两次槛位释放在同一时刻发生，累计 ≥1 即应重算）；`累计槛位释放次数` 与 `已派发总数` 两字段被真实、正确地更新（见 B② 表）。
- **是否影响派发：不影响**（这是关键判断，不是辩解）。重算后 `N = 当前有效上限 - 仍在执行中的 PR 数量`：11:43:10 释放后 `N = 5 - 0 = 5`，而当时"已解锁且排队中"的 PR **只有 pr-003 一个**（pr-001/pr-002 已合并，无其他 PR 存在）⇒ 取前 5 个 = 1 个 = 实际派发数；12:09:34 释放后排队 PR 为 0 ⇒ 槛位空置（工作流明令"没有已解锁且排队中的 PR 时保持空置，不放宽解锁条件"）。故本迭代**无法**通过派发结果观察到爬升差异，该偏差仅停留在账面。
- 判定依据：工作流把本项定义为"核实 `当前有效上限` 是否按爬升公式被正确重新计算，而不是维持初始值不变"——不满足；但触发条件成立且派发结果无影响 ⇒ partial + 偏差 D1（不阻塞交付，见偏差表建议）。

---

### C. 依赖正确性（pr-003 的 depends_on ↔ 已合并事实）—— **pass**

- **依赖与合并顺序一致（git 一手）**：`pr-003-api-doc.md` 声明 `depends_on = pr-001-heartbeat-two-tier.md + pr-002-open-api-and-agent-panel.md`；`git cat-file -p 8e6c9bf`（pr-003 任务图提交）的 parent = `c5bc42b`（**pr-002 合并提交**），而 `c5bc42b` 的 parent #1 = `dcf0572`（**pr-001 合并提交**） ⇒ pr-003 的 worktree 是从"pr-001 与 pr-002 都已并入迭代分支"之后的 tip 拉出的，正是 `depends_on` 所要求的前置条件。
- **每条依赖都有代码级证据（我复核）**：pr-003 → pr-001 的判据是 README 环境变量表两行描述 pr-001 引入的语义——`oamp/src/config.js:27,118`（`HEARTBEAT_IDLE_FACTOR = 6` / `heartbeatIdleMs = heartbeatIntervalMs × 6`）与 `oamp/src/registry.js:130-132`（`Math.max(baseTimeoutMs, 2 × announced)`）在本仓库**当前确实存在**，即依赖的对象是真实符号而非文档叙述顺序。pr-003 → pr-002 的判据是路由集合 / `ERR_CODE` / `publishGlobal` 调用点——`oamp/src/web.js:81-87`（5 键 `ERR_CODE`）、`:644`（`/api/events`）、`oamp/src/transport.js:68-76`（`publishGlobal`/`globalCount`）均存在。
- **方向与扇入无环**：pr-001 ∥ pr-002（`depends_on` 均为空，两条"无依赖"的代码级理由经阶段 2/3/4 验证独立复核成立）→ pr-003 fan-in，无环。
- **未被违反的解锁语义**：pr-003 的验收 4 条依赖"合并后代码形态"，而派发时其 base 已含两者 ⇒ 判定动作不需等待任何并行 PR。

---

### D. PR 粒度（逻辑原子性 / 可审查性 / 独立性）—— **pass**（含一处边界说明）

- **pr-001**：单一可描述的事——"心跳两档 + 租约阈值联动"。回滚它不影响 pr-002/pr-003 的任何文件（文件范围交集 = ∅）。验收全部可在自身 worktree 判定（纯函数边界、日志断言、三个既有测试文件回归）。**原子性/可审查性/独立性均成立**。
- **pr-003**：单一可描述的事——"接口文档 + README 同步"。文件仅 2 个（+tasks），完全不触碰代码，回滚零功能影响。**成立**。
- **pr-002（G2+G3 合并的合理性）**：把"开放接口面（F04/F05/F06）+ 顶栏面板（F01/F02）"放在一个 PR，是本迭代最粗的一颗粒度。我独立复核了 `pr-002-open-api-and-agent-panel.md` 给出的合并理由链，并回到代码验证：
  (a) `oamp/test/web.test.js` **同时**承载后端用例段与前端静态契约段（实测 `web.test.js:1622` 起的 0015 段同时读 `web/app.js`/`index.html`/`style.css` 并断言前端符号）；
  (b) 若前端独立成 PR，其静态契约段只能落在同一个 `web.test.js` ⇒ 两个 PR 同时声明该文件 ⇒ 违反阶段 4「PR 间文件范围无重叠」；
  (c) 若把前端静态段并入后端 PR，则后端用例要断言前端产物（`#agent-panel`/`connectAgentEvents`）⇒ 后端依赖前端，而前端 F02 依赖后端 `/api/events` ⇒ **成环**；
  (d) 唯一替代（新开前端契约测试文件）与 `architecture.md` §9.1「新增断言只落既有 3 个测试文件」冲突。
  ⇒ 在既定架构约束下**合并是唯一无环解**，且 pr-002 的 8 条验收在其自身 worktree 内全部可判定（pr-002 验收报告标准 7 已证）。**判定 pass，但如实标注**：pr-002 覆盖 5 张功能卡、869 行改动、跨后端与前端两层，"逻辑原子性"是靠"对外可观测面"这一条主线维持的，属于**可接受但偏粗**的边界；若下一迭代放宽 §9.1 的测试文件约束，应把它拆成"接口面"与"前端消费面"两颗。

---

### E. 最终产物一致性（抽查 3 处硬契约）—— **pass**（3/3）

#### E① 心跳阈值联动（`next_interval_ms` + `max(基准, 2×通告)`，唯一落点 `registry.findExpired`）—— **pass**

代码证据（迭代分支 HEAD）：
- 判活公式唯一落点：`oamp/src/registry.js:126-135`——`findExpired(now, baseTimeoutMs)` 内 `timeoutMs = entry.next_interval_ms == null ? baseTimeoutMs : Math.max(baseTimeoutMs, 2 * entry.next_interval_ms)`；全仓检索 `Math.max(`/`2 *` 的结果里，只有 `router.js:177` 出现同一公式（且**仅用于 `LEASE_ADJUSTED` 日志字段**，不参与任何判定），其余命中均为无关（`router.js:18` 的 RPC 超时、`agent.js:621/662/701` 的飞行计数/退避/降级判定）。
- 通告字段链：`config.js:27,118`（空闲档派生值 = 6×interval，非 env 旋钮）；`node-client.js:157-178`（自调度 `setTimeout` 链，`next_interval_ms` = 距下一跳间隔，同源不可漂移）；`registry.js:17-21`（`MAX_ANNOUNCED_INTERVAL_MS=600000` + 合法值 fail-closed 落 `null`）；`registry.js:88`（`heartbeat()` 只存合法通告）；`router.js:163-179`（透传 + 变化即打 `LEASE_ADJUSTED`）。
- **本次独立运行时证据（自建隔离环境，非复用被验方脚本）**：起真实 agent 后静置，agent 侧日志（`oamp15-agent-a`）在 **04:15:49**（最后一次活动 04:14:49 后恰好 60s）打出 `HEARTBEAT_TIER instance=demo-15a session=5cc3dacc-… tier=idle interval_ms=60000`，随后心跳 `HEARTBEAT_SENT … tier=active interval_ms=60000`；**同一毫秒** Router 侧打出 `LEASE_ADJUSTED instance=demo-15a next_interval_ms=60000 threshold_ms=120000`（= max(30000, 2×60000)），且在此之前 60s 内该实例 `AGENT_REGISTERED` 仅 1 次、零 `AGENT_OFFLINE`、session 不变；活跃档阶段（04:14:49~04:15:39）6 跳均为 `interval_ms=10000`、Router 侧 `threshold_ms=30000`（= 基准，逐字不变）。⇒ 两档切换、阈值联动、不误判离线、零重注册四项在同一时间线上相互印证。

**偏差登记（不改变本项判定）**：公式字面量在 `registry.js:132`（判活）与 `router.js:177`（日志）各出现一次，若公式演进会有静默漂移风险 ⇒ 偏差 D4。

#### E② 全局事件流（`/api/events` + 键隔离 + 仅有订阅者时轮询）—— **pass**

- 路由与注册顺序：`oamp/src/web.js:644-648`——`transport.handle(req,res,{chatId:null})` **先注册订阅者**再 `topologyWatch.ensureRunning()`（保证 `globalCount() ≥ 1` 才起表）。
- 键隔离（结构性，不靠过滤）：`oamp/src/transport.js:7,67-76`——`publishGlobal` = `publish(null, event)`、`publish` 只写 chat 键；`globalCount()` 读 `subscribers.get(null)`。
- 判定源与自停：`oamp/src/web.js:161-219`——`diffTopology` 只把 `state==='online'` 收进基线（离线墓碑不进），`tick()` 首尾两次 `globalCount() === 0 → stop()`，`prev === null` 时只播种不发事件，查询失败（Router 不可达）不更新基线。
- **本次独立运行时证据**：
  1. 键隔离实调：同时挂 `curl -N /api/events` 与 `curl -N /api/stream?chat_id=iso-1`，期间起停一个真实 agent（`demo-15b`）。全局流收到 `event: agent_online` + `event: agent_offline`（载荷形状与文档一致：online 带 `instance_id`+`last_heartbeat`，offline 只带 `instance_id`），chat 流**零事件**（仅 `retry: 1000` 与 `: keepalive`）；两向检索均无串扰。
  2. 播种：`demo-15a` 在我重启 Router 并重连后已在线，新订阅者建立时**未**为它补发 `agent_online`（基线播种抑制正确）。
  3. 界面消费方（浏览器实测，见 H）：面板关闭状态下计数由 `0 agents online` 变为 `1 agents online`（无任何手动操作）⇒ 事件流链路端到端生效。

#### E③ 统一错误契约（`{error, code}` 5 码 + `sendError` 唯一构造点）—— **pass**

- 代码证据：`oamp/src/web.js:81-87` `ERR_CODE` 冻结表恰 5 键（`INVALID_PARAM`/`NOT_FOUND`/`CONFLICT`/`PAYLOAD_TOO_LARGE`/`UPSTREAM_UNAVAILABLE`）；`:91-93` `sendError()` 是唯一错误体构造点；机械检索 `sendJson(res, [45]\d\d` **零命中**，`sendJson(res, status` 仅剩 `sendError` 内部一处；`sendError` 调用点 **20 处**（`web.js:488/508/518/528/578/582/586/601/607/611/624/629/638/657/668/672/677/684/741/743`），与 pr-002 验收报告的独立计数一致。
- **本次独立运行时证据（隔离环境逐类触发）**：未知路径 `GET /api/nope` → 404 `{"error":"not found: GET /api/nope","code":"NOT_FOUND"}`；方法不匹配 `DELETE /api/agents` → 404 同码；`?state=offline` → 400 `INVALID_PARAM`；`GET /api/stream`（缺 chat_id）→ 400 `INVALID_PARAM`，文案与迭代前逐字相同（"需要 chat_id（不做全局订阅）"）；未知 chat → 404 `NOT_FOUND`；畸形 JSON → 400 `INVALID_PARAM`；70KB 体 → 413 `PAYLOAD_TOO_LARGE` 且响应头带 `connection: close`；成功响应（`/api/agents`、`/api/chats`）**不含 `code`**。5 个码全部被真实触发，键集合恰为 `{error, code}`。

---

### F. 测试（迭代分支串行全量）—— **pass**（含方法学偏差披露）

**披露：我未在仓库根执行套件，改用等价替代取证。** 原因：仓库根正跑着 10 角色集群（`tmux oamp-cluster` 12 窗口，`oamp/.runtime/cluster/pb-*.log` 存在），而 `cluster-actions.test.js` 的 `LOG_DIR = <repo>/oamp/.runtime/cluster`（`oamp/test/cluster-actions.test.js:20`）是仓库内共享目录，且 `cluster.js` 的 up 会对已存在 `*.log` 做 truncate ⇒ 在仓库根实跑会**截断活集群日志**，与委托约束"不要触碰主仓库运行中的集群（`.runtime/`）"直接冲突。替代方案（两条，均在 `/tmp` 克隆内、互不污染）：

1. **干净克隆全量串行**（`git clone --local --branch iteration/0015-hub-orchestration-open-api` → `/tmp/v0015-stage6/clone`，HEAD `0db206e`）：
   `cd oamp && node --test --test-concurrency=1 test/*.test.js` ⇒ **`1..241` / tests 241 / pass 241 / fail 0 / cancelled 0 / skipped 0，退出码 0**，耗时 151.8s（日志 `/tmp/v0015-stage6/run1.log`，已随清理删除；关键末尾行为 `ok 241 - Web：前端静态契约——顶栏 agent 列表…`）。⇒ **本迭代分支自身的测试全绿，零失败。**
2. **已知非密闭的定向复现**（证明那 1 个 fail 的唯一成因）：在第二个克隆副本内预置活集群的同名产物 `oamp/.runtime/cluster/pb-verifier.log` 与 `router.log`（模拟运行中集群的文件），只跑 `node --test --test-concurrency=1 test/cluster-actions.test.js` ⇒ **12 pass / 1 fail**，失败用例 = `not ok 1 - up：session/窗口名与数量、每窗 -c、命令串、日志 truncate 与 git check-ignore（F06 验收 1/2/6）`，`error: '未启用角色无日志'`（日志 `/tmp/v0015-stage6/run-cluster-only.log`）。

**与 0013 既有判定的一致性（逐项对齐）**：

| 维度 | 0013 阶段 6 判定 | 0015 本次 | 一致性 |
|---|---|---|---|
| 失败用例 | `not ok 22 - up：session/窗口名与数量…（F06 验收 1/2/6）`，`AssertionError: '未启用角色无日志'` | **同一用例、同一断言文本**（编号 1 vs 22 取决于是否只跑该文件） | ✅ 同一根因 |
| 仓库内实跑 | 214/215（两次） | 未在仓库内实跑（见披露）；等价条件下复现 12/13 | ✅ 差值 1，且成因同 |
| 干净副本 | 215/215 | **241/241** | ✅ 全绿（用例数 215→226→241 为逐迭代累加） |
| 根因 | `LOG_DIR` 取仓内共享目录 + 断言要求该目录不存在 `pb-verifier.log`；活集群恰好产生该文件 | 同上（`cluster-actions.test.js:20` 未改，`git diff main..iteration -- oamp/test/cluster-actions.test.js oamp/src/cluster.js` 为空） | ✅ 同一根因 |
| 归因 | 与本迭代无关、非代码缺陷 | 同 | ✅ |

⇒ 结论：**本迭代未引入任何测试失败**；唯一 fail 是已被 0013、0014 两次记录的既知非密闭用例，本迭代第三次记录（偏差 D6）。

---

### G. 搭置文件（`deferred-demand-changes.md`）—— **pass**

`docs/iterations/0015-hub-orchestration-open-api/deferred-demand-changes.md` **不存在**（`ls` 报 No such file or directory；目录清单亦无该文件）。按工作流「搭置的需求变更/错误报告」条，本迭代**无搭置项需要透传**——不存在需要原文摘录的内容。是否要追溯到"用户改判收窄"这类需求变化：该变化发生在阶段 1 内且已由 `demand.md` §1.1/§2.1/§6.2 显式落盘（`user_confirmed` + 失效项表），属正常的需求收敛，不是搭置。

---

### H. 前端行为可达性（可观察判定面）—— **pass**

涉及用户可见行为的验收共 3 类，逐类核查"是否有可观察判定面"，并**自行用真实 Chromium 复做**（隔离环境：Router + web 于端口 17779，另起真实 `oamp agent start demo-15a`）：

| 用户可见行为 | 可观察判定面（DOM/文本/样式） | 我的实测结果 |
|---|---|---|
| 顶栏面板开合 | `#agent-panel` 的 `hidden` 类 → `display` 计算值 | 初始 `hidden=true/display:none`；点击 `#conn-status` ⇒ `hidden=false/display:block`（空态渲染 `暂无在线 agent`）；点面板外 ⇒ `hidden=true`；再开按 Esc ⇒ `hidden=true`（四态均取到计算样式） |
| 列表内容与字段 | `#agent-panel` 行 DOM（`agent-id`/`badge-online`/`agent-hb`） | 展开后 1 行：`demo-15a` + `online` 徽标 + `6s 前`；6 秒后同一行变为 `1s 前`（展开期 5s 刷新真实生效） |
| 自动更新（不手动刷新） | `#conn-status` 文案 + 行集合 | **面板关闭状态下计数由 `0 agents online` 变为 `1 agents online`**（agent 注册后，全程无点击/无焦点/无展开）⇒ 变化只能来自全局事件流 |
| 断线收起 | `#conn-status` 的 `conn-bad` 类 + 文案 + 面板 `hidden` | 停掉 Router 后：文案变为 `Router 不可达：router 不可达或请求失败: connect ECONNREFUSED …`、类名 `conn conn-bad`、面板 `hidden=true/display:none` |
| 零启停入口 | 面板内控件检索 + 路由集合检索 | 结构面：`index.html:20` 面板为静态空节点、`renderAgentPanel` 只产出 `div/span`；接口面见 I。既有 `web.test.js:1622` 起的静态契约用例断言了面板结构/开合/Esc/点外/断连收起/空态/5s 刷新/只列在线等 17 条 |

⇒ 每类行为都有**可观察判定面**（DOM 类名/计算样式/文本/行集合），且我以真实浏览器独立复现，不依赖被验方脚本。**偏差登记（不改变判定）**：该静态契约用例的标题含"零启停入口"，但用例体内**没有**任何针对"零启停"的断言（无 `doesNotMatch` 按钮/链接类语句）——标题覆盖度大于断言覆盖度，实际支撑来自结构性检索与运行时 DOM 检索 ⇒ 偏差 D8。

---

### I. 代码零启停校验（结构性成立，非文档声明）—— **pass**

在迭代分支 HEAD 上机械检索（builtin grep 全 `oamp/src/`，含 `spawn|child_process|process.kill|.kill(|execFile|SIGKILL|SIGTERM|SIGINT|agent_start|agent_stop|closeAgent|launch` 等）：

| 模块 | 命中 | 是否 agent 启停路径 |
|---|---|---|
| `oamp/src/web.js` | 启停类路径**零命中**；唯一命中是 `process.on('SIGINT', onSigint)`（web 进程**自身**的优雅退出，`web.js:768`） | **否** |
| `oamp/src/router.js` | 仅 `process.on('SIGINT'/'SIGTERM', shutdown)`（`router.js:534-535`，Router **自身**退出） | **否** |
| `oamp/src/transport.js` / `registry.js` / `node-client.js` / `rpc.js` | 零命中 | 否 |
| `oamp/src/agent.js` | `spawn` + `child.kill` ×4 —— 是 agent **自身**执行 shell/`omp` 任务时的子进程管理（`agent.js:181/354` 等） | 否（无"被 hub 启动/关闭"的入口） |
| `oamp/src/acp-client.js` / `context-pool.js` | `spawn`/`kill` —— agent 内部 ACP 子进程的生命周期（上下文池） | 否 |
| `oamp/src/cluster.js` | `spawnSync(tmux…)` / `kill-session` / `process.kill(pid,'SIGKILL')` —— **CLI 的 `oamp cluster up/down`**，用户终端显式调用 | 否（不在 hub 面，且 `demand.md` E9 的判据只覆盖网页界面与接口清单） |

补充两条结构性证据：
1. **RPC 方法面封闭**：`oamp/src/router.js` 的方法分派只有 8 个 —— `agent.register` / `agent.heartbeat` / `agent.deregister`（agent → Router 的注册与生命周期**通知**，由 agent 自己发起）/ `message.send` / `message.ack` / `router.status` / `router.task_get` / `router.task_list`。**没有** `agent.start` / `agent.stop` / `agent.kill` 之类方法。
2. **HTTP 路由面**：`web.js:644` 之前的路由集合 = 10 条 API + 2 条静态 + 1 条兜底 404，无 `agent` 生命周期路径；面板内无控件（H 表最后一行）。

⇒ "hub 不启动 / 不关闭 agent"是**结构性成立**（hub 的两个进程 `web.js`/`router.js` 内不存在任何 agent 生命周期路径），不是文档声明。`demand.md` §4 E9 的原始判据（"网页界面与接口清单（含 `oamp/API.md`）里找不到任何启动/关闭 agent 的入口、按钮或路径"）逐条满足（`API.md` 的全量反向检索见 pr-003 验收报告 AC7；其中 README 保留的 `oamp task *` **CLI** 段属命令面而非接口条目 —— 该读法边界见偏差 D9）。

---

## 汇总

- pass：**17** 项
- partial：**4** 项 —— ①A-1a（pr-001 验收：卡片 1.6 措辞绝对化，报告已判 partial）；②A-1b（pr-001 改动超出卡片文件范围）；③A-3a（pr-003 验收：报告期 2 处事实性错误已返工修正，但返工后无独立复核）；④B③（爬升公式未重算，派发无影响）
- fail：**0** 项
- blocked：**0** 项
- 偏差记录：**9** 条（D1~D9，见下表）

判定口径说明（避免"基本通过"式模糊）：A 按 3 个 PR × 3 条推进条件展开为 9 个子项；B 按工作流三条展开为 3 个子项；E 按三条硬契约展开为 3 个子项。partial 的每一处都在正文拆开了"哪部分通过、哪部分不通过"。

---

## 偏差记录

> 实现/交付与现有规格或文档不一致之处。不影响本次结论判定（无 fail），逐条附建议处理方式。

| # | 规格/文档描述 | 实现/交付实际行为 | 建议处理 |
|---|---|---|---|
| **D1** | `workflow-pb.md` §并发槛位算法：每次槛位释放都要"重新计算 `当前有效上限`"（本迭代公式值应为 5） | `status.md` 的 `当前有效上限` 三次提交均为 **3**（未重算）；`累计槛位释放次数` 正确更新 0→2→3。**派发结果不受影响**（释放时唯一排队的 pr-003 只有 1 个，任何 N≥1 都只派发 1 个；末次释放后队列为空，按规则应空置） | ①阶段 6 收口时把该字段按公式重算为 5 并在备注里写明"本迭代派发不受影响"；②工作流侧补一句"多 PR 迭代应保留每版 `status.md` 以支持事后核对"（本迭代正是靠 `git show <commit>:status.md` 才核出该偏差） |
| **D2** | `workflow-pb.md` §启动工作流：并发配置区块 `**已派发总数**`（初始 0） | 初始化当次（`86cc8fd` 11:08:45）即为 **2**——因为"初始化该区块"与"首批派发 pr-001 ∥ pr-002"发生在同一轮响应 | 工作流文本补一句"初始化与首批派发同轮时，该字段可直接写首批数量"，或改为"初始化后立即按实际派发次数更新"（纯文本澄清，无代码/流程影响） |
| **D3** | `prs/pr-001-heartbeat-two-tier.md`『文件范围』：6 个文件 | 实现多改了 `oamp/test/config-file.test.js`（+3 行，断言强度增加，删除 0 行）。执行方已上报、pr-001 验收的委托标准 8 已显式允许，但**卡片至今未补** | 按实现把该文件补进卡片『文件范围』（一行文档改动）；同时把"派生 config 键会波及 `config-file.test.js` 的整体深比较"写进架构 §9.2 回归锁清单 |
| **D4** | `architecture.md` §3「阈值公式唯一落点 = `registry.findExpired`」 | 公式字面量实际出现两处：`registry.js:132`（**判活**，唯一落点成立）与 `router.js:177`（**只作 `LEASE_ADJUSTED` 日志字段**，不参与判定） | 建议抽一个具名函数/常量（如 `leaseThresholdMs(base, announced)`）供两处共用，或补一条断言锁两处一致；若判定为可接受，则在 §3 注明"日志字段为同一公式的投影，需同步修改" |
| **D5** | 阶段 5 推进条件"该 PR 验收标准全部通过" | pr-003 的验收报告（12:02:46）判出 2 处事实性错误 + 1 处措辞失真；返工提交（12:06:29）逐条修正 7 处，但**返工后没有新的独立复核报告**（证据链在"复核"一环缺失） | 补一条 verifier 复核记录（可轻量：只覆盖 D1/D6 两处事实性错误的原文与实跑复现）；或在工作流补"返工 >= 1 处事实性错误时必须复核"的规则。本次我已独立 diff 复核确认 7 处修正与代码一致，但该复核不应替代流程内复核 |
| **D6** | `oamp/test/cluster-actions.test.js` 自述边界"零真实 tmux 会话 / 零网络" | `LOG_DIR = <repo>/oamp/.runtime/cluster` 是仓库内共享固定目录（`:20`），断言要求其中不存在 `pb-verifier.log`；活集群使该断言必失败，且 `cluster.js` 的 up 会 truncate 活集群日志 | **连续第 3 个迭代记录（0013 D5 / 0014 D7 / 本报告）⇒ 建议列为下一迭代 P0**：让 `LOG_DIR` 可注入（如 `OAMP_LOG_DIR`），一条改动同时消除"仓库内全量必 1 fail"与"测试静默截断活集群日志" |
| **D7** | `architecture.md` §3.7 / `event-log.test.js` 的行格式正则（值 `[^ ]+`） | 新事件 `HEARTBEAT_IDLE_DISABLED` 的 `note` 含空格+中文、被引号包裹 ⇒ 不满足该正则（既有 `LEASE_ALARM` 同型；测试环境不触发故全绿） | 按实现更新文档（写明"含空格值引号包裹、解析方须支持引号"）或把告警改为无空格字段（`reason=router_no_lease_follow`）；pr-001 验收报告 D-1 已提出，此处仅登记仍未处理 |
| **D8** | `oamp/test/web.test.js:1622` 用例标题含"（…零启停入口）" | 用例体内**无**任何"零启停"断言（无按钮/链接的 `doesNotMatch`）；该性质实际由结构检索 + 运行时 DOM 检索支撑 | 二选一：补一条断言（如 `assert.doesNotMatch(appJs, /<button/)` + 面板 HTML 生成模板不含 `a\|button\|input`），或把标题里的"零启停入口"改成指向接口面检索的表述。**同类的"标题覆盖度>断言覆盖度"检查建议推广到全仓用例** |
| **D9** | `demand.md` §4 E9 的检索范围 = "网页界面与接口清单（含 `oamp/API.md`）" | `oamp/README.md` 保留了 0010 起的 `oamp task *` **CLI** 说明段（`API.md:848` 已显式声明"Router 的任务面不经 HTTP 暴露"）。按 E9 原文与 `architecture.md` §9.4 检查项 4 的语义，CLI 文档不构成"接口条目"命中 ⇒ AC7 pass 成立 | 若主 agent 采"README 全文不得出现 tasks 字样"的更严读法，则 pr-003 的 AC7 应改判 partial —— **按现状我的判定是 pass，读法边界在此显式记录，由主 agent 决策**（建议：在 README 的 CLI 段头部加一句"CLI 命令面不属于 HTTP API 面"，把边界写进文档而不是留在解释里） |

---

## 下一迭代候选

- **`cluster-actions.test.js` 密闭化（P0，跨 3 迭代遗留）**：`LOG_DIR` 可注入 + 断言与活集群文件解耦。它同时影响"测试可信度"与"不干扰运行中集群"两条纪律，是本迭代唯一让全量套件在仓库内无法全绿的因素。
- **并发爬升的实测覆盖（P1）**：本迭代`当前有效上限`的偏差暴露了一件事——**只有一个排队 PR 时，爬升公式对派发结果没有任何可观测影响**，"并发调度真实执行证据"的第 3 项实际上无法被真实验证。建议下一迭代刻意构造"首批 ≥3 个无依赖 PR + 后续补位"，让爬升在派发数量上留下可核对痕迹（并把每版 `status.md` 作为证据保留）。
- **文档一致性断言落地（P1）**：把 pr-003 验收报告标准 7 的机械脚本（路由集合 / `ERR_CODE` 键与状态码 / 逐接口错误码集合 / 事件名集合）移植为 `oamp/test/` 下的一条用例，让文档漂移在测试层可发现（本迭代已验证该脚本对 7 类注入失真检出 5 类，盲区是参数表语义）。
- **示例"干净环境可复现"规程（P2）**：`API.md` §5.3 曾引用无生产者的对话（D6，已修）。建议文档类交付统一约定"示例按文档顺序可从空库复现"，并保留"逐条实跑"为验收项。
- **`message_id` 命名歧义（P2）**：响应字段 `message_id` 是派发信封 id（`msg-<uuid>`）而库内消息 id 是整数，同名不同物是唯一会让接入方写错代码的点（文档已澄清，字段名仍歧义）。
- **空值参数口径统一（P2）**：`?state=`（空）按缺省处理走 200，`?archived=`（空）走 400（`persist.js:105-113`）。文档各自准确，但跨参数不一致；建议统一为"空值=缺省"或"一律 400"并同步 `API.md`。
- **事件流首帧播种窗口的计数陈旧（P3）**：首订阅时若 Router 不可达，`prev` 保持 `null`，恢复后首个成功 tick 只播种不发事件 —— 该窗口内新注册实例不产生 `agent_online`，面板关闭时计数可能陈旧到下一次变化（pr-002 验收报告 N1 已提出）。
- **web 常驻发送方启动期双注册竞态（P3，既有）**：`ensureSender` 无并发保护，派发触发时对同一 `instance_id=web` 连发两次 `agent.register`（迭代前后同现象）⇒ 每次触发多一条 `LEASE_ADJUSTED`。
- **`registry.findExpired` 生效阈值的可观测性（P3）**：当前只有"通告值变化"才打 `LEASE_ADJUSTED`；如运维需要"每实例当前生效阈值"快照，可考虑在 `router.status` 之外补只读投影。

---

## 结论

**PASS**（0 fail；4 partial 均落在"文档/口径/证据链"层面，无功能性缺陷；9 条偏差已逐条记录并附处理建议）

供主 agent 直接决策的判读要点：

1. **可以合并进 main**：三条硬契约（①心跳阈值联动 ②全局事件流 ③统一错误契约）我全部做了独立代码检索 + 隔离环境运行时复现（含 `LEASE_ADJUSTED threshold_ms=120000`、键隔离实测、5 码逐类触发 + 413 的 `connection: close`），与 `architecture.md` 的契约面逐项一致；前端三类用户可见行为我用真实 Chromium 独立复现（开合/Esc/点外/空态/自动更新/断线收起），零启停为结构性与接口面双重成立。
2. **本迭代自身的测试全绿**：干净克隆串行全量 **241/241 / 0 fail**；唯一既知 fail（`cluster-actions.test.js` 的 `未启用角色无日志`）已用"预置活集群同名文件"的定向复现证明其成因与 0013/0014 完全一致，**非本迭代引入**（这也是我未在仓库根实跑的原因——会截断活集群日志，与委托约束冲突，替代取证已在 F 披露）。
3. **并发三项**：① worktree 时间窗口重叠 **pass**（两条 PR 分支同基 `bfd45df` 分叉 + 两份验收报告相隔 3 秒各自确认自己的 worktree 在盘 + 两次 merge 同秒；**无 worktree list 快照**，证据链为推断，强度已在 B① 明示）；② 并发配置区块初始化和更新 **pass**（三次提交的五字段值可核）；③ 爬升公式重算 **partial**（字段维持 3，公式值应为 5；**派发结果无影响**，因为释放时唯一排队 PR 只有 1 个）。
4. **需要主 agent 处理的三处**：D3（pr-001 卡片文件范围未补）、D5（pr-003 返工后无独立复核记录）、D1（`当前有效上限` 重算）。三者都是"改一行文档/补一条记录"量级，不构成返工；其中 D1 建议在收口时顺手改正，避免该字段带着错误值成为下一轮核对的基准。
5. **该偏差不妨碍本迭代闭环，但不宜再往后拖**：`cluster-actions.test.js` 的非密闭已被三个迭代连续记录（0013/0014/0015），下一次多 PR 迭代若要真实验证爬升公式，一个会与活集群日志碰撞的全量套件本身就是障碍。
