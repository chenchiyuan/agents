# PR-001 验收报告（独立验证）

验证者身份：同级代码审查者 + 集成测试工程师（反射自产出物类型——4 个可执行 shell 脚本 + CLI/JSON 接口契约；验证方式 = 逐字读码 + 隔离沙箱 E2E 实跑关键路径，不接收 dev 执行上下文）

产出物：`roles/cdp-debug-skill/scripts/` 下 4 个脚本（worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/agents-pr-001-0009`，分支 `pr-001-cdp-debug-skill`，HEAD `5754be1`）

验证标准来源：`docs/iterations/0009-cdp-debug-skill/prs/pr-001-scripts-deterministic-ops.md` 验收标准 8 条 + `prd/F05-scripts-resources.md` 主卡 6 条（合并映射）+ 主 agent 委托附加抽查项

验证日期：2026-09-08

验证方法说明：只读/无副作用命令直接在 worktree 内执行；所有写状态子命令（profile create/wipe、chromium ensure/install、cdp-browser start/stop）均在隔离沙箱执行——`CDP_DEBUG_HOME` 指向 `$TMPDIR` 下临时目录，CfT known-good JSON 与 mac-arm64 zip 均为本地 `file://` 伪造分发物（含服务器桩 exe，可应答 `/json/version`），全程零网络、零仓库内写入、零交付文件修改。

---

## 逐项判定

### 1. scripts/ 目录存在、4 脚本可执行；无参/子命令调用独立于 SKILL.md/references（F05-1；D4）：pass

证据：
- 目录与权限：`roles/cdp-debug-skill/scripts/` 含 `check-deps.sh`(341 行)、`chromium.sh`(522)、`cdp-browser.sh`(431)、`profile.sh`(193)，均 `-rwxr-xr-x`（`ls -la` / `file` 实测）。
- `bash -n` 全部通过（4 脚本零语法错误）。
- 全脚本 grep：无任何对 `SKILL.md`、`references/`、其他脚本之外的仓库文件的引用/依赖；4 脚本互相独立（cdp-browser/check-deps 各自内置同一份进程探测实现，注释明示"改动须同步两处"，非运行期依赖）。
- 所有子命令在隔离沙箱（无 SKILL.md/references 环境）实跑成功：见条目 2/4/5/6 证据。

### 2. check-deps.sh 无参 exit 0 + 合法 JSON，schema 与 D4 一致；三源解析 + scope 报告（F05-2/6；D4/D7）：pass

证据：
- worktree 根无参运行：`rc=0`，stderr 为空；`python3 -m json.tool` 判定合法 JSON；顶层键恰为 `checked_at / mcp / chromium / errors`；嵌套键逐一比对 D4 schema 完全一致：`mcp.playwright{configured,scope,command,args,endpoint_arg}`、`mcp.chrome_devtools{…}`、`chromium{installed,version,path,running,port,profile_dir}`；空态默认值正确（`configured:false`、`args:[]`、`errors:[]`、`port:null` 等）。
- 三源解析（隔离 fake `$HOME` + 临时 cwd 实测）：
  - 仅 user 源（`~/.claude.json` 顶层）：`chrome_devtools configured:true scope:user`，`endpoint_arg` 从 `--browser-url=http://127.0.0.1:9222` 正确解析；
  - user + project 源（`projects["<cwd物理路径>"].mcpServers`）：playwright `scope:project`，`--cdp-endpoint <value>` 空格分隔形态正确解析；
  - local 源（`<cwd>/.mcp.json`）命中同名 playwright：`scope:local`，遮蔽 project/user（与脚本头注释声明一致）；
  - 别名命中：server 名 `chrome-devtools-mcp` 亦识别为 `chrome_devtools`；
- 配置文件非法 JSON 时：`errors[]` 记录含路径的解析异常、其余源继续解析（实测 cdt 仍 configured:true）、exit 仍 0——符合 D4"errors[] 收集中配置源解析异常"契约。
- 帮助/非法参数：`-h/--help` exit 0；多余参数 exit 2（用法错误，非执行错误路径）。

### 3. check-deps 自身执行错误 exit≠0 + JSON error 字段；exit 0 仅表执行成功（D4）：pass

证据：
- 代码路径（可定位）：python 段顶层 `try/except`——异常时 `out["error"]="check-deps.sh 执行异常: …"` 输出 + `sys.exit(1)`；bash 侧兜底（`_py_rc≠0` 且无输出时）输出含 `"error": "python3 处理异常（exit N）…"` 的 JSON 并以 `exit $_py_rc` 退出（check-deps.sh 尾部 `_py_rc` 分支，实测文件中约 300-341 行）。两条路径均满足"exit≠0 + JSON error 字段"。
- exit 语义实测：依赖全缺时（真实本机无两 MCP、无 CfT）`rc=0` 且 JSON 合法——exit 0 仅表执行成功、与依赖状态无关（对照 D4 契约）。
- 说明：该自身错误分支的**动态强制触发**需使 `/usr/bin/python3` 不可执行/崩溃（需 root 或替换系统二进制），本验证未实际触发；分支为无歧义直白代码（except→error JSON→exit 1），与同构的 chromium.sh 自错误路径（实测触发：fresh home + 不可达 known-good → `{"command":"ensure","error":"CfT known-good 版本清单拉取异常…"}` + exit 1）逐字同构，判定 pass 不降级。

### 4. chromium.sh ensure/install/verify/status/path；幂等 ensure；版本不一致不自动升级（-f 强制）；glob 动态解析；manifest 落盘（F05-5；D2）：pass

证据（全部在隔离沙箱 E2E，本地伪造 CfT 分发：JSON `channels.Stable.version=152.0.7977.82`、zip 布局 `chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing`，`file://` URL，`CDB_KNOWN_GOOD_URL` 覆盖）：
- 5 子命令全部存在且行为正确：`ensure`（fresh → `action:installed`，exit 0）；`path`（输出单行绝对路径，exit 0；未装时无输出 exit 1）；`status`（JSON，`installed/version/path/manifest`）；`verify`（JSON，`exe_exists/executable/hash_matches`）；`install` 与 ensure 强制分支共用 do_install。
- glob 动态解析：伪造 zip 解包后脚本经 `*.app/Contents/MacOS/*` 与 `*/*.app` 双层 glob 命中可执行文件（安装落位路径 = `…/chromium/152.0.7977.82/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing`，未写死 zip 内部布局）。
- 幂等：同版本再 `ensure` → `action:noop`（不重下）；`-f` 亦不重复下载（代码：已装=known-good 分支在 force 判断之前）。
- 版本不一致：伪造 known-good 升到 152.0.7977.90 后 `ensure` → `action:version_mismatch` + installed vs known_good facts，exit 0，**不自动升级**（manifest 仍 v1）；`ensure -f` → `action:installed` + `previous_version:"152.0.7977.82"`，旧版本目录被清理（`chromium/` 下仅剩新版本目录）。
- manifest：`$CDP_DEBUG_HOME/chromium/.manifest.json` 恰含 `version/url/path/sha256/installed_at` 五字段（E2E 输出逐字段核对）。
- verify 事后校验：篡改已装 exe 后 `verify` → `hash_matches:false`（事实输出，语义留给调用方）。
- 离线路径：known-good 拉取失败但本地已装有效 → `action:noop offline:true` exit 0；未装 → error JSON exit 1，不产生半成品（无 manifest、无版本目录）。
- 自身执行错误契约（下载/解包异常→exit≠0 + error JSON）与条目 3 的 die() 路径一致，die 输出纯事实文案。

### 5. cdp-browser.sh start/stop/status/restart；--remote-debugging-port + --user-data-dir；端口/profile 参数或 env 覆盖；占用 profile 不强启；status --json（F05-3；D1/D3/D4）：pass

证据（E2E，mock chromium 服务器桩 exe 可应答 `/json/version`）：
- 启动参数：`ps` 实测启动进程 argv 含 `--remote-debugging-port=9332 --user-data-dir=<CDP_DEBUG_HOME>/profiles/main`（逐字符合 D1）；`started:true`、`reachable:true`、pid/port/profile_dir/binary/endpoint 事实齐备，exit 0。
- 默认端口 9222：无 --port 启动实测运行于 9222（`DEFAULT_PORT="${CDP_PORT:-9222}"` 静态 + ps 实证）。
- 覆盖优先级：CLI `--port 9332` 生效；env `CDP_PORT=9444 CDP_PROFILE=alt` 启动于 9444/profiles/alt（ps 实证）；代码顺序 env 赋默认 → CLI 覆盖。
- 幂等与单实例锁：同 profile 已有受管实例再 start → `started:false running:true` + note"未重复启动"；profile 被外部进程占用（实测 python 进程持 `--user-data-dir=<profile_dir>`）→ `started:false profile_in_use:true` + exit 1，**不强启**；释放后 start 成功。
- stop/restart：`stop --port/--profile` 精确停匹配实例（`stopped_pids` 输出）；无规格 stop 停全部；restart = stop+start 实测成功；停止后 `status` running:false。
- `status --json`：运行中输出 `{command,running:true,pid,port,profile_dir,endpoint}` 合法 JSON exit 0；未运行时 `running:false` 全 null exit 0。
- 缺二进制：manifest 缺失时 start → `started:false chromium_installed:false` + exit 1（确定性事实不强启）。
- 输入校验：非法端口/越界/非法 profile 名（`../x`）/非 status 用 `--json` 均 exit 2 带 stderr 文案。
- check-deps 与 cdp-browser 同一事实源核对：实例运行于 9336 时两脚本端口/profile_dir 报告一致。

### 6. profile.sh list/create/path/wipe；默认 main；path 绝对路径；wipe 需 --confirm/交互确认（D3）：pass

证据（隔离 `CDP_DEBUG_HOME` 实测全部子命令）：
- `list`：空时无输出 exit 0；创建后按字典序列出 `main`。
- `create`：缺省 name=main 创建 `<home>/profiles/main`（`existed:0`）；重复创建幂等（`existed:1`，不覆盖不报错）。
- `path`：缺省与具名均输出 user-data-dir **绝对路径**单行（`/…/home/profiles/main`），供 cdp-browser 消费（接口契约实测：profile.sh path 输出 === cdp-browser 启动时 --user-data-dir 值）。
- `wipe`：无 `--confirm` 且非交互终端 → 不执行删除、输出 `removed:false` + note、exit 1（目录仍在）；`wipe --confirm` → `removed:true`、目录删除、exit 0；目标不存在 → `removed:false` + note、exit 0；`rm -rf` 前有 `"$profiles_root"/*` 前缀双保险。
- 名称安全：`../evil`、`.hidden` 非法名 exit 2（防路径穿越/隐藏项）。
- 未知子命令/空参数 exit 2。

### 7. profile 落点默认 git 外；脚本不写仓库内路径、不改 .gitignore；仓库 git status 无可观察新增（F05-4；D3）：pass

证据：
- `CDP_DEBUG_HOME` 默认 `$HOME/.local/share/cdp-debug-skill`（4 脚本同一默认，静态）；profile/chromium/manifest 全部落于其下。
- 代码扫描：脚本内零 `gitignore`、零 `.git/`、零仓库路径引用；全部 `rm/mv/mkdir` 目标位于 `$CHROMIUM_DIR`（含 `.tmp`）与 `$profiles_root` 之下且受前缀防护。
- 全量 E2E（含 chromium 安装/升级/清理、cdp-browser 启停、profile wipe）后 worktree `git status --porcelain` 仅 `?? docs/iterations/0009-cdp-debug-skill/`（预存在的规划文档未跟踪，属预期，非脚本行为产物）；`git diff main...HEAD -- .gitignore` 为 0 行（未改 .gitignore）。

### 8. 4 脚本不输出页面证据/结论/依赖可用性语义判断（F05-6；协议检查项 #15；D4）：pass

证据：
- 对 4 脚本全部输出串（printf/echo/heredoc JSON/die 文案）逐条审查：均为事实字段（`installed/started/running/reachable/removed/hash_matches/configured/scope` 等）或纯执行结果/执行错误描述（"下载异常/解包异常/构建落位异常/manifest 写入异常/版本清单拉取异常"），零语义结论。
- 禁用词扫描：`可用/缺失/请注册/通过/失败` 仅出现在**注释与 usage 文档**中，且均为"本脚本**不**输出此类结论"的契约自述（check-deps.sh 头注、chromium.sh 头注、cdp-browser.sh 头注）；无一出现在输出路径。
- 收口提交 5754be1 实证：将 status 帮助文案"离线可用"等改为"纯本地只读，无需联网"等纯事实表述，提交信息自述对齐 PR-AC8。
- 就绪轮询输出 `reachable:false` 属操作结果事实（探测失败），非依赖可用性判定；语义路由留给 SKILL.md/agent 层（D4）。

### 9. 附加抽查（git log 分支归属 / diff 范围 / bash -n / sha256 决策点复核）：pass

证据：
- 分支与提交：worktree 当前分支 `pr-001-cdp-debug-skill`；`git log main..HEAD` 恰为 6 个提交（9c68344→5754be1），全部不在 main；每提交 diff 均只触及 `roles/cdp-debug-skill/scripts/` 下单脚本（9c68344 check-deps.sh / e0ee2c6 profile.sh / 4c2a4f5 chromium.sh / e27d77c cdp-browser.sh / edf6b2e check-deps.sh / 5754be1 chromium.sh）。
- `git diff main...HEAD`：仅 4 文件、1487 insertions、0 deletions、0 其他改动。
- `bash -n`：4 脚本零语法错误。
- **决策点复核（manifest sha256 = 已装可执行文件哈希，而非 zip 哈希）**：判定为**可接受的实现解释，不构成验收偏差**。理由：(a) D2 原文"下载后记录本地 sha256 至 manifest"未指明哈希对象；D2 自述 CfT 官方端点**无 sha256 字段**（E5），故不存在官方 zip 哈希可比对；(b) D2 校验信任边界 = 官方 JSON + googleapis HTTPS（不变），manifest sha256 的既定用途是事后完整性（verify 重算比对），对已装可执行文件哈希才可验证（zip 哈希在解包后无法复算）；(c) chromium.sh 头注显式记录该解释（"sha256 = 已装可执行文件哈希，供 verify 事后完整性比对"），非静默选择；(d) E2E 实证：manifest.sha256 与 exe 的 `shasum -a 256` 一致，篡改 exe 后 verify `hash_matches:false`。作为文档歧义已按意图澄清处理，建议（非必须）在 references 文档补一句说明。

---

## 汇总

- pass: 9 项
- fail: 0 项（需返工）
- partial: 0 项
- blocked: 0 项（缺信息无法判定）

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| D2"下载后记录本地 sha256 至 manifest"未指明哈希对象 | 实现记录**已装可执行文件**的 sha256（非 zip），脚本头注已显式说明，verify 以 exe 重算比对 | 属文档歧义的有据澄清、与用途自洽，非不一致；可选在 references/chromium-profile-guide 补一句说明（不阻塞） |
| （无其他不一致） | — | — |

## 下一迭代候选

- check-deps 对**字符串形态**配置条目（`"server": "整行命令"`，非 dict 形态）无法解析出 `endpoint_arg`（整行作为单一 args token，未按空格拆解）。dict 形态（`claude mcp add` 实际产物）全覆盖；低影响，可考虑对 string 条目按空格分词以完备 endpoint_arg。
- 探测逻辑在 check-deps.sh 与 cdp-browser.sh 各维护一份（注释互指"改动须同步两处"）。为保"同一事实源"而刻意为之，长期可考虑抽公共片段（本轮不改，避免扩大文件范围）。
- `chromium.sh` 首次 fetch 失败时会在 `$CDP_DEBUG_HOME/chromium/` 留下空 `.tmp/` 目录（fetch_known_good 先 mkdir 后 curl，失败路径不清理目录本身；无 manifest/无版本目录，非半成品）。cosmetic，可加 rmdir。
- `cdp-browser.sh status --json` 中 `pid` 为字符串而 `port` 为整数（无既定 schema，仅风格不一致）。如需机器消费可统一类型。
- 本机真实环境两 MCP 未配置、无 CfT 安装（check-deps 实测 `configured:false` / `installed:false`）——与 architecture.md E7 基线一致，非缺陷；F10 前置 Gate 上线后将有真实事实可用。

## 结论

PASS（9 项全部 pass，无 fail，无 partial，无 blocked）

偏差记录 1 条（sha256 决策点的文档歧义澄清，非不一致，建议处理 = 可选补文档）；下一迭代候选 4 条。主 agent 可直接按"合入 PR-001"推进，无需返工。
