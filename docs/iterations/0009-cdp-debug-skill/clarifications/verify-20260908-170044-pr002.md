# verify-pr002 — cdp-debug-skill SKILL.md + references 独立验收

```
验证者身份：SKILL.md / 领域知识文档独立评审者（规范合规审查 + 领域事实复核）
           —— 以 docs/skill-design-protocol.md v3.1.0、architecture.md（D1~D10/内容大纲/配额分配）、
           PRD 功能卡 F01~F04/F06~F13 为唯一判定标准；对文档引用的 scripts CLI/JSON 采用实跑取证，
           不采信文档自述；不接收 dev 执行上下文。
产出物：roles/cdp-debug-skill/SKILL.md + references/（5 文件）
        （worktree: .pb-agents/worktrees/agents-pr-002-0009，分支 pr-002-cdp-debug-skill，基点 7dfb9ad 含 pr-001）
验证标准来源：docs/iterations/0009-cdp-debug-skill/prs/pr-002-skillmd-and-references.md「验收标准」19 条
            + 相关 F 卡（F01~F04、F06~F13）
验证日期：2026-09-08
```

**交付面确认（先于逐条判定）**：
- `git log`：分支 pr-002-cdp-debug-skill 上 3 个提交（a3f7017 → 1e95812 → 01954b6），基点 7dfb9ad（pr-001 合并提交），3 提交均在本 PR 分支。✓
- `git diff --stat 7dfb9ad..HEAD`：6 个新增文件、+561 行，全部位于 `roles/cdp-debug-skill/`（SKILL.md + references/5）。零既有文件修改、零范围外文件。✓
- `roles/cdp-debug-skill/` 目录内：SKILL.md + references/（5 md）+ scripts/（4 sh，pr-001 交付）。无 `.mcp.json`（find 计数 0）、无 `schemas/`、无 assets/。✓
- 6 文件全文扫描：无 TODO/FIXME/TBD/占位符/待填 残留。✓
- 运行期事实（check-deps.sh 实跑）：两 MCP 当前 `configured=false` —— 与 SKILL.md Important facts「两个 MCP 当前均未配置，需用户手动注册」一致。✓

## 逐项判定

### AC1：Skill 三层形态 + frontmatter name + 无 .mcp.json — **pass**
证据：目录形态 = SKILL.md + references/（5）+ scripts/（4），三层成立；frontmatter L2 `name: cdp-debug-skill`（kebab-case）；`find roles/cdp-debug-skill -name ".mcp.json"` = 0；无 schemas/ 目录。git diff name-status 仅 6 新增文件（F01 验收 1、4；D8）。

### AC2：使用范围仅本仓库内、不声明 pb 生态横向能力、不改动宿主/异仓 — **pass**
证据：description 首行（SKILL.md L4）「仓库内能力定义，仅本仓库内使用」；Purpose L38「目录以 SKILL.md 交付……install-pb-agents.sh 只安装 roles/\<role\>/\<role\>.md——天然不随 .pb-agents 分发」——已实查 `tools/install-pb-agents.sh`（L46-53：仅 copy `<role>/<role>.md`，缺同名 .md 打印"跳过"），声明与脚本行为一致；mcp-registration.md L1「仅本仓库内使用、不引用 powerby 跨仓技能（C-4）」；division-of-labor.md L4-6 定位声明。frontmatter 无 style/principles 字段 → 无跨仓继承/加载语义。git diff 零范围外改动（F01 验收 3、5；F03 验收 3）。

### AC3：frontmatter 六必选字段 — **pass**
证据：SKILL.md L1-31 frontmatter 含 `name`（L2）、`description` 三段（L3-11）、`role.identity`（L4 精度人格：「极少数同时精通浏览器自动化与 CDP 协议……像航空排故工程师做记录那样」）、`role.relationship`（用户是决策者/你是证据闭环交付者）、`role.character`（证据驱动、克制、对凭据敏感）、`compatibility`（macOS arm64 / node >= 20.19（MCP 运行时下限）/ Claude Code / Chrome for Testing）。六字段齐全（F02 验收 2；对照 protocol §4.3.1 必选字段表）。

### AC4：正文 Section 顺序 + Safety/Success criteria — **pass**
证据：Section 头实测顺序 —— Purpose(L34) → Success criteria(L44) → Strategy(L54) → Tools and capability boundaries(L78) → Important facts and constraints(L98) → Workflow(L107) → Output format(L157) → Resources(L213) → Examples(L225) → Safety(L280)。与 protocol §4.2 顺序一致（Resources→Subtask 可选→Examples→Safety；Safety 收尾）；无独立 Subtask 节 = architecture 大纲 11 节形态（本 skill 无子任务编排需求）。Safety 与 Success criteria 两规定 Section 均存在且为实质内容（非空壳）。各 Section 均有实质填充、无引用悬空（F02 验收 3、4）。

### AC5：约束词分级 + 凭据三明治 — **pass**（附计数口径说明）
证据（独立 grep 计数，与 dev 自报一致：字面 CRITICAL 4 次 / 独立规则 3）：
- 独立 CRITICAL 规则 3 条：① 凭据不入 git+提交前脱敏 —— L42（Purpose 前置红线）与 L298（Safety 后置检查）**首尾各现一次**，同一规则两处出现（F02 验收 6 三明治 + 架构配额①「CRITICAL 凭据条款在 SKILL.md 首尾各现一次」明示）；② 共享实例前提 Form A 不成立不宣称登录态连续 —— L73（Strategy）；③ MCP 未配置/未就绪产出 blocked 不伪证 —— L123（Workflow 步骤 0）。
- 字面出现 4 次 = 3 独立规则。计数口径：F02 验收 5「CRITICAL ≤ 3 处」在架构中按**配额/独立规则**分配（3 条规则含三明治两现的凭据规则），protocol §4.4.1「宁可重复，不能遗漏。同一条关键规则至少出现两次」为三明治重复背书——若按纯字面计数，F02 验收 5+6 与 3 规则配额互相矛盾，规则计数是唯一自洽口径。交付与架构配额分配精确一致。
- MUST：Workflow 2（L113、L132）/ Output format 1（L209）/ Safety 1（L291）→ 每 Section ≤2 ✓；NEVER = 0 ✓。
- 每条 CRITICAL/MUST 均附「——后果」声明（protocol §9.1.1 原则一：L42/L73/L123/L298 不可逆泄漏/误归因/假证据污染；MUST 同理）。
- 首尾高注意力热区落位符合 protocol §4.4.3（Purpose 前置红线 / Safety 后置检查）。

### AC6：description 三段信息齐备 — **pass**
证据：SKILL.md description —— L5-7「做什么」：对真实页面自动操作断言（@playwright/mcp）、失败深诊（chrome-devtools-mcp console/网络/性能/内存）、登录态续调（CDP 独立 Chromium+profile 免重登），产出结构化证据+截图+三态结论供仓库内 reviewer/verifier/testing 复验；L8-9「何时用」：页面调试/自动操作断言/页面失败排查/登录态续调/组合任务；L10-11「否定边界」：纯测试框架与 CI/压测性能基准/视觉回归基线/pb-v1-brower 式评审任务/普通浏览取数（web-access/browse 语境）。与 architecture「description 构成」逐项一致（F03 验收 1）。

### AC7：description-eval-samples 样例集 — **pass**
证据：references/description-eval-samples.md —— should-trigger **6 条**（≥5）：主链路·自动操作断言 / 主链路·页面调试 / 诊断·失败排查 / 诊断·性能深诊 / 登录态续调 / 组合语境，覆盖主链路/诊断/登录态/组合全部触发面；should-not-trigger **7 条**（≥5）：N2 测试框架 CI / N2 压测 / N2 视觉回归 / N1 pb-v1-brower 式评审协议 / 普通浏览取数 / investigate 系统级调试 / 批量取数。每条附「依据」指向 description 正向/否定措辞，边界与 description 一致；文件自宣「样例仅供评审复用，命中率判定由评审离线执行，不随交付物声明已通过」（D10）。多数 should-not 样例直接落入否定边界字面条款（F03 验收 2；D10）—— 1 条边界口径微瑕见偏差记录 D-2。

### AC8：references/mcp-registration.md 注册文档完整性 — **pass**
证据（含一手复核，不采信文档自述）：
- 两 MCP 用途与关键配置项（§1/§2）：@playwright/mcp `--cdp-endpoint`（对应 env `PLAYWRIGHT_MCP_CDP_ENDPOINT`）、chrome-devtools-mcp `--browser-url`/`-u` + `--memory-debugging` + `--redact-network-headers` + `--no-usage-statistics` + `--no-performance-crux`。
- Form A 命令（L47-48）与 Form B（L59-60）：`claude mcp add <name> --scope project -- npx -y …` —— 已实查本机 `claude mcp add --help`：`--scope <scope> (local, user, project; default local)` 与 `--` stdio 分隔符语法成立。scope project + 精确落点余量声明（L69-71）与架构 U3 标注一致。
- 版本核实（L80-81）：`npm view @playwright/mcp@latest` = **0.0.80**（engines node>=18）、`npm view chrome-devtools-mcp@latest` = **1.8.0**（engines `^20.19.0 || ^22.12.0 || >=23`）—— 与文档完全一致；@latest 注册策略 + 排障 §7。
- 工具名核实（dev 可疑点独立复核）：`npm view @playwright/mcp@0.0.80 readme` 含 browser_navigate/browser_click/browser_type/browser_snapshot/browser_take_screenshot/browser_fill_form/browser_select_option/browser_wait_for **全部 8 个命名**；`npm view chrome-devtools-mcp@1.8.0 readme` 含 list_console_messages/get_console_message/list_network_requests/get_network_request/performance_start_trace/performance_stop_trace/performance_analyze_insight/take_heapsnapshot/navigate_page 及 heap 查询族（get_heapsnapshot_summary / get_heapsnapshot_retaining_paths / query_heapsnapshot_objects / compare_heapsnapshots）—— cheatsheet/文档工具名全部属实。
- N3 明示（L5）：不交付 `.mcp.json`、注册由用户手动完成、运行期不自动注册——照文档可完成手动注册（F04 验收 1、2、3、6；D5）。

### AC9：chain-cheatsheet 与 SKILL.md Workflow 一致 — **pass**
证据：§1 主链路操作序列（导航→快照→点击/填表→断言→截图）+ 失败转诊断 = Workflow 步骤 4/1 组合；§2 诊断顺序 console→网络→性能/内存、`--memory-debugging` 门槛、navigate 复现取证 = 步骤 5 及其注记；§3 登录态命令表（cdp-browser.sh/chromium.sh/profile.sh/check-deps.sh 子命令、`status --json` 字段 command/running/pid/port/profile_dir/endpoint、退出码 0/1/2、env 覆盖、CLI 优先）——与 4 脚本**实跑 usage 逐一核对一致**（见 AC15 取证明细）；与 SKILL.md 步骤 2/3、login_required、截图命名、常用顺序表述一致（F04 验收 4）。

### AC10：chromium-profile-guide（$CDP_DEBUG_HOME/profile/端口/脱敏/.gitignore 句）— **pass**
证据：§1 布局（chromium/\<version\>/ + `.manifest.json` version/url/path/sha256/installed_at + profiles/\<name\> + runs/）= D3 布局明细；§2 profile.sh 管理表与实跑 usage 一致（list/create/path/wipe、name 字符集 `[A-Za-z0-9][A-Za-z0-9._-]*` 缺省 main、wipe 需 --confirm、单实例锁、env CDP_DEBUG_HOME）；§3 端口 9222 + `--port`/`CDP_PORT`、`--profile`/`CDP_PROFILE`、status --json 字段（实跑核对）；§4 脱敏/隐私选项表与 mcp-registration 参数表一致；**L18 gitignore 明示句存在**：「若经 `CDP_DEBUG_HOME` 覆盖到仓库内路径，须用户**自行**在仓库 `.gitignore` 追加（skill 不代改仓库 `.gitignore`）」；§5 登录态证据默认落 `$CDP_DEBUG_HOME/runs/`（git 外）不入 git 视野（F05/F09/F13 文档面；D3/D5/D9）。

### AC11：division-of-labor 分工表 — **pass**
证据：三行分工表（gstack browse/$B、pb-v1-brower、investigate 各一行：职责现状 + 边界），与 F12 验收 1「每既有能力一行」一致；「无重复条款核对」4 条（浏览器驱动不重造 / console·网络取证不重复实现 / 系统化根因流程不重复 / 评审协议不重复）与 F12 验收 2 逐项对应；边界声明 N1/N2/N5/C-4-N6 与 F12 验收 3 逐字一致；SKILL.md Tools and capability boundaries 含分工摘要 + 指向该文件。点名 pb-v1-brower/gstack browse/investigate = F12 要求的**边界对象罗列**，文件明示「不构成对所列任何技能（含 powerby 跨仓技能）的引用、依赖或加载语义」，无 style.inherits/principles——不构成跨仓引用/加载（F12 验收 1~4；分工边界表内容）。

### AC12：Workflow 显式选路判据 + 组合编排 + 工具归属 — **pass**
证据：Strategy §2 选路表（三类任务特征 → 链路 → 承担工具，唯一映射）+ Workflow 步骤 1 同源判据；组合编排（步骤 1/3~5）：登录态先就绪再主链路/诊断、主链路断言失败→步骤 5 诊断接力，转诊断接力 MUST 在同一实例/上下文（L132）；Tools 表「链路/承担工具/覆盖能力/执行落位（Workflow 步骤 3/4/5）」+ L80「本 skill 不重复实现链路能力本身」——主链路指向 @playwright/mcp（步骤 4）、诊断指向 chrome-devtools-mcp（步骤 5）、登录态指向 scripts+CDP（步骤 2/3），不重复实现链路能力（F06 验收 1~4；F07 验收 1、4；F08 验收 1、3；D1）。Form A/B 语义在 Strategy/Important facts/步骤 0 三处一致。

### AC13：登录态链路条款 — **pass**
证据：Important facts —— CfT **独立 Chromium**（非本机 Google Chrome，C-6）、CDP `--remote-debugging-port=9222` 默认可换、独立 profile `$CDP_DEBUG_HOME/profiles/<name>` 默认 main、默认 git 外；Strategy L69「三层共享同一浏览器实例/上下文……连续 = 同一浏览器进程 + 同一 user-data-dir」+ CRITICAL L73「Form A 不成立时不得宣称登录态连续」；Form A 为登录态/跨层接力任务**要求**形态；步骤 3.3 未登录/登录态失效 → blocked（`login_required`）+ 恢复指引、不产伪证据；步骤 3.4 跨会话续调（profile 持久化，本 skill 不清理 profile）（F09 验收 1、2、5；D1/D3）。

### AC14：前置 Gate（步骤 0）四分支 + N3 — **pass**
证据：Workflow 步骤 0（L107-125）—— ①会话级目检工具面；②配置级无参运行 check-deps.sh 解析 JSON facts；③分流四分支全：就绪→正常选路（不遮蔽）；MCP 缺失/未加载 → blocked（`mcp_unconfigured`）附缺失明细，`next_step` 按子情形指向 mcp-registration.md 对应小节（configured=true 但工具面未加载 → 先指引重启宿主会话，不与"真未配置"混同）；Chromium 缺失 → 自助 `chromium.sh ensure` + `cdp-browser.sh start` → 复检，仍未就绪 → blocked（`chromium_not_ready`）+ `ensure -f` 恢复指引；需登录态/跨层接力但未按 Form A → blocked（`shared_instance_not_configured`）。N3 明示（L125：「不承诺运行期自动注册 MCP——路径 = 检查 + 指引 + blocked 结论，注册动作永远由用户手动完成」）+ CRITICAL L123（F10 验收 1~5；D7）。四分支的 blocked_reason 拼写与 Output format 枚举一致。

### AC15：Gate 描述与 pr-001 脚本真实 CLI/JSON 一致（实跑取证）— **pass**
证据（全部实跑，非文档自述）：
- `./check-deps.sh`（无参）：stdout JSON = `mcp.playwright/mcp.chrome_devtools{configured,scope,command,args,endpoint_arg}` + `chromium{installed,version,path,running,port,profile_dir}` + `errors`，exit=0（即便全部未配置）——与 SKILL.md 步骤 0.2、chain-cheatsheet、Resources 索引描述逐字一致。
- `./cdp-browser.sh status --json`：字段 command/running/pid/port/profile_dir/endpoint；usage 子命令 start/stop/status/restart、`--port`/`--profile`/env CDP_PORT/CDP_PROFILE/CDP_DEBUG_HOME、退出码 0/1/2（1=start 未产生受管实例、2=用法错误）——与 cheatsheet/guide/SKILL.md 全部引用一致。
- `./chromium.sh status` + `--help`：子命令 ensure|install|verify|status|path、`-f|--force` 语义（幂等 ensure/版本不一致需 -f 升级）、退出码 0/1/2 ——与 SKILL.md 恢复指引（`chromium.sh ensure -f`）、cheatsheet 退出码速记一致。
- `./profile.sh --help` + `list`：list|create|path|wipe、name 字符集与缺省 main、wipe 需 --confirm、退出码 0/1/2（1=wipe 未确认或路径异常）——与 guide/cheatsheet 一致。
- 全文 scripts 引用（Resources 索引 4 脚本、Workflow 步骤 0/2/3、Examples 1/2、cheatsheet/guide 命令表）无悬空/失实（F10 验收 4；D4/D7）。实测环境同时印证「两 MCP 未配置」事实句。

### AC16：Output format 内联产物协议 — **pass**
证据：Output format（L157-211）—— 目录布局（session.md / evidence.json / screenshot-\*.png / 可选 perf-trace.json / result.json）+ **evidence.json schema + result.json schema 完整内联**；无 schemas/ 目录（D8 无 schemas/）。evidence.json 键与 architecture D6 **逐键一致**（schema_version / target{url,title,captured_at} / console{errors,warnings,messages} / network{failed_requests,summary} / page{snapshot_path,assertions} / performance{trace_path,summary} / memory{heap_path,summary}）；result.json 键一致 + `status: passed|failed|blocked` + `blocked_reason` 五枚举拼写（mcp_unconfigured / chromium_not_ready / login_required / shared_instance_not_configured / target_error，SKILL.md 全文 grep 无变异拼写，与 D6 拼写逐一相同）+ next_step + sanitized。`artifact_root` 为调用方注入、缺省**拒绝执行并提示**（L155 步骤 6 + L172 语义声明）；任务结束返回绝对路径 + 产物清单（F11 验收 1；D6）。

### AC17：pb-v1 仅格式参考、无评审语义 — **pass**
证据：Output format L211「产物结构与命名对齐 pb-v1 findings/verify 风格——**仅格式参考**，不构成对 powerby 跨仓技能的任何引用/依赖（C-4/N6）；本产物协议是"调试闭环证据 + 三态结论"，**无 round/severity/findings 评审语义**，与 pb-v1-brower 的 review/verify/iterate 报告协议不重合」—— schema 内确无 round/severity/findings 字段（仅三态 status + chains_used/evidence_refs/sanitized）；division-of-labor「评审协议不重复」条款呼应（F11 验收 3、4；D6）。

### AC18：Safety 凭据保护条款 — **pass**
证据：Safety 节（L280-298）—— profile 不入 git（默认 $CDP_DEBUG_HOME 仓库外；覆盖到仓库内须自行 .gitignore，指向 chromium-profile-guide.md）；含凭据证据默认落 `$CDP_DEBUG_HOME/runs/`（git 外）根本不进 git 视野；console/网络证据提交前须脱敏；**三层防线**① `--redact-network-headers` 注册参数 ② runs/ git 外 ③ 脱敏清单（cookie/authorization/set-cookie/token 类头字段、URL query token/session 参数、console 凭据）+ 入库复核置 `result.json#sanitized=true`；`sanitized=false` 产物不允许进 git 路径；语义判断留 agent 层（scripts 只做确定性操作）；**覆盖声明**（F05 profile.sh wipe 需 --confirm / F09 登录态链路不清理 profile、登录态证据不入库 / F11 产物先 git 外、脱敏复核后才入库）；证书/密码输入值不采集进 evidence；验证清单 checklist 3 项 + 后置 CRITICAL（L298，与 L42 前置红线同源、三明治收尾）（F13 验收 1、3、4；D9）。

### AC19：Resources 索引 — **pass**
证据：Resources（L213-224）逐条索引 references/ 5 文件（mcp-registration / chain-cheatsheet / chromium-profile-guide / division-of-labor / description-eval-samples，各附读取时机）与 scripts/ 4 脚本（check-deps / chromium / cdp-browser / profile，各附子命令职责）——与 D8 布局文件清单逐一对应；9 个被引文件全部真实存在、交叉引用（SKILL.md↔references↔scripts、references 互指）无悬空（F04/F05 资源索引面）。

## 汇总

- pass: 19 项
- fail: 0 项（需返工）
- partial: 0 项
- blocked: 0 项

## 偏差记录

> 实现与现有规格/文档不一致处，不阻塞本次判定。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | protocol §9.1.1/检查清单 #16 字面「CRITICAL ≤ 3 处/Skill」 | 交付 SKILL.md 字面 CRITICAL 4 次 = 3 独立规则（凭据规则按 F02 三明治在 L42/L298 首尾两现，架构配额①明示该两现计一条配额） | 计数口径在规范链内自洽（规则计数），判定 pass；建议下一迭代在 protocol 或 F02 将「≤3 处」显式注释为「≤3 独立规则，三明治重复出现不额外计数」，消除外部复审核对歧义（不返工） |
| D-2 | description-eval-samples.md 自宣判定口径「should-not-trigger 样例应**落入 description 否定边界**（不适用段）而不命中」 | should-not 样例 #6（investigate 系统级 502 服务端日志根因）不落入否定边界任何字面条款，系「正向面缺席」命中（description 否定边界未列系统级/服务端根因调试）；其余 6 条均直接落入否定边界字面条款 | 不阻塞（≥5 达标）；建议下一迭代二选一：description 否定边界补「系统级/服务端根因调试（investigate 语境）」显式句，或将样例 #6 依据改为「非正向面命中」口径说明 |

（无实现与架构 D1~D10 / F 卡验收字面冲突的其他偏差。）

## 下一迭代候选

- **约束词计数口径规范化**：protocol §9.1.1「每 Skill ≤ 3 处」与 F02 验收 5+6 三明治并存时的计数语义（字面 vs 独立规则）宜在规范层显式化（关联偏差 D-1）。
- **description 离线 eval 未执行**：references/description-eval-samples.md 已交付（6+7 样例），D10/F03 §4 裸判定 2 的命中率判定仍待评审离线执行——样例集可直接复用；若 eval 出现 should-not 误命中，再评估 description 是否补 investigate/系统级显式否定句（关联偏差 D-2）。
- **架构 U3 实机验证**：`claude mcp add --scope project` 精确落点（预计 `~/.claude.json#projects[<path>].mcpServers`）尚未实测写入——用户完成 Form A 注册后可用 `claude mcp list` 复核，references/mcp-registration.md §5 已留余量措辞。
- **架构 U1 实机验证**：@playwright/mcp 带 `--cdp-endpoint` 的连接时机（启动即连 vs 工具首用懒连）官方无明文——SKILL.md 已采用「先起浏览器、再开/复用宿主会话」稳妥时序并明示不设硬前提，注册后实机验证可消除该不确定性。

## 结论

**PASS**（19 条全部 pass，0 fail / 0 partial / 0 blocked；偏差记录 2 条均不阻塞当前迭代关闭，由主 agent 决定是否开启下一迭代处理）。

要点：
- 交付面精确：3 提交均在本分支、基点 7dfb9ad、6 新文件 +561 行、无 .mcp.json/schemas/占位符、零范围外改动。
- 文档引用的 scripts CLI/JSON/退出码/环境变量经 4 脚本实跑逐一核对一致，无悬空/失实引用。
- MCP 版本（0.0.80/1.8.0）与工具命名（browser_\* 8 项、cdt console/network/perf/heap/navigate 全套）经 npm registry readme 一手复核属实。
- dev 自报可疑点独立复核结论：CRITICAL 字面 4 = 规则 3（三明治两现计一条，可接受，见 D-1）；@playwright/mcp 工具名与官方 README 一致。
