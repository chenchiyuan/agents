# tasks.md — 0003-workflow-scm

**版本**: 0.1.0
**迭代**: 0003-workflow-scm
**创建日期**: 2026-09-02
**对应阶段**: 任务拆解（Phase 4）

---

## 任务总览

| 任务 ID | 名称 | 优先级 | 前置依赖 | 预计工作量 |
|---|---|---|---|---|
| T01 | 创建 `roles/workflow-scm/workflow-scm.md` | P0 | 无 | 1-2 天 |
| T02 | 创建 `roles/commit-planner/commit-planner.md` | P1 | T01 | 1-2 天 |
| T03 | 修改 `roles/workflow-pb/workflow-pb.md` | P1 | 无（T01 推荐先完成） | 0.5-1 天 |

---

## 依赖图

```
T01
├── T02（依赖 T01：commit-planner.md 的输出格式契约引用 workflow-scm.md §文档路径协议 的 F03 规范）
└── T03（推荐在 T01 后：Phase 5 推进条件可直接引用 F06 AC#1 措辞，无硬依赖）
```

**最长依赖链**：T01 → T02（长度 2）
**关键路径任务**：T01
**并行机会**：T01 完成后，T02 与 T03 可并行执行

---

## T01：创建 `roles/workflow-scm/workflow-scm.md`

**覆盖功能**: F01（全部）、F03（格式规范放在此文件 §文档路径协议 章节）、F05（全部）、F06（AC#1、AC#2）

**工作描述**: 新建目录 `roles/workflow-scm/` 并在其中新建 `workflow-scm.md`。文件须包含以下所有章节：不变量声明、阶段定义、文档路径协议（含 PR 文件格式规范）、角色读取指南、验证目标。

---

### 验收标准

**AC-T01-1**：文件 `roles/workflow-scm/workflow-scm.md` 存在，可独立读取（无需其他文件补全理解）。
- 追溯：F01 AC#1

**AC-T01-2**：文件 frontmatter 满足以下所有条件：
- `name: workflow-scm`
- `description` 字段非空（非空字符串）
- 版本字段存在且值 ≥ `0.1.0`
- 创建日期字段存在
- 追溯：F01 AC#3、AC#4

**AC-T01-3**：文件包含独立的"约束声明"或"不变量"章节，其中包含以下两条规则，措辞可被客观判断是否违反：
- **规则 A**：从 `tasks.md` 产出（即 Phase 4 完成）之后，任何代码变更必须在独立 worktree 分支上进行，不得直接在主分支（main/master）上提交。
- **规则 B**：每个 worktree 变更必须对应一个 `prs/pr-{id}.md` 文件（按 F03 规范），merge 前该文件须存在。
- 追溯：F06 AC#1

**AC-T01-4**：约束声明章节中明确说明约束生效范围：仅从下一个使用 workflow-scm 的迭代起生效，不追溯已完成迭代。
- 追溯：F06 AC#2

**AC-T01-5**：文件包含阶段定义章节，描述提交规划阶段的职责、输入（`tasks.md`）、输出（`prs/` 目录）、推进条件。四项均非空。
- 追溯：F01 AC#2（阶段定义要求）

**AC-T01-6**：文件包含文档路径协议章节，明确 PR 文件的存放路径规则为 `docs/iterations/{迭代ID}/prs/pr-{NNN-描述}.md`，其中 `{NNN-描述}` 为数字加简短描述（如 `001-add-workflow-scm`）。
- 追溯：F03 AC#4；F01 AC#2（文档路径协议要求）；F03 model_inferred 确认（主 agent 2026-09-02：`{id}` 格式为 `NNN-简短描述`）

**AC-T01-7**：文档路径协议章节中定义 `prs/pr-{NNN-描述}.md` 的格式规范，包含以下所有字段的定义，缺一不可：
- **上下文摘要**：描述该 PR 解决什么问题，dev 读完后能独立工作
- **涉及 tasks**：来自 `tasks.md` 的任务 ID 无序列表（至少一个），每行一个 ID（如 `- T01`）
- **文件范围**：该 PR 预计涉及的文件路径或目录列表
- **验收标准**：merge 前须满足的可判断条件（至少一条）
- **参考资料**：可为空列表，但字段必须存在
- 追溯：F03 AC#2；架构 AR-03（1:N 映射内嵌于"涉及 tasks"字段，格式为无序列表）

**AC-T01-8**：文件包含角色读取指南章节，commit-planner 读此文件能找到：输入路径（`tasks.md` 来自何处）、输出路径（`prs/` 目录规则）、交付标准。三项均可从本文件独立读出。
- 追溯：F01 AC#2（角色读取指南要求）

**AC-T01-9**：文件包含"验证目标"章节，覆盖以下两类验证目标：
- **PR 粒度验证**：预估 diff < 400 行；验收标准可独立判断（不依赖其他 PR 完成）
- **文件范围验证**：PR 文件声明的文件范围与 tasks.md 对应任务的实际变更范围，采用静态声明对比（无重叠、无遗漏）
- 追溯：F05 AC#1；F05 model_inferred 确认（主 agent 2026-09-02：以 diff < 400 行为粒度标准；采用静态声明对比）

**AC-T01-10**：文件不包含执行细节（具体实现步骤属于执行角色文件范围）。判断标准：文件中不出现"第 N 步：""首先执行""运行命令"等指令式内容。
- 追溯：F01 AC#5

---

**前置依赖**: 无

**优先级**: P0

---

## T02：创建 `roles/commit-planner/commit-planner.md`

**覆盖功能**: F02（全部）、F03（AC#5，角色侧输出格式一致性）

**工作描述**: 新建目录 `roles/commit-planner/` 并在其中新建 `commit-planner.md`。文件须定义 commit-planner 执行角色的完整规范：输入/输出契约、执行模式、worktree/branch 创建规格、报告契约。输出格式须与 T01 的 workflow-scm.md §文档路径协议 中定义的 F03 格式规范一致。

---

### 验收标准

**AC-T02-1**：文件 `roles/commit-planner/commit-planner.md` 存在，可独立读取。
- 追溯：F02 AC#1

**AC-T02-2**：文件 frontmatter 满足以下所有条件：
- `name: commit-planner`
- `description` 字段非空
- 版本字段存在且值 ≥ `0.1.0`
- 创建日期字段存在
- 追溯：F02 AC#2、AC#6

**AC-T02-3**：文件明确声明输入契约：接收 `tasks.md` 路径和 `workflow-scm.md` 路径（两者均由调用方 brief 传入，不由角色自行推断）。
- 追溯：F02 AC#3；架构 AR-02 调度序列步骤 1

**AC-T02-4**：文件明确声明输出契约：产出 `prs/` 目录，路径规则为 `docs/iterations/{迭代ID}/prs/pr-{NNN-描述}.md`，与 F03 规范一致。
- 追溯：F02 AC#3；F03 AC#4；架构 AR-03

**AC-T02-5**：文件明确声明执行模式为全自动（不需要人工干预中间步骤）。
- 追溯：F02 AC#3

**AC-T02-6**：文件包含 worktree/branch 创建的规格说明，明确覆盖以下两点，表述无歧义：
- 分支命名规则（如分支名与 PR 文件名或迭代 ID 的对应关系）
- 创建时机（何时创建 worktree/branch）
- 追溯：F02 AC#4

**AC-T02-7**：文件包含报告契约：角色完成后须回报以下内容：
- `prs/` 目录文件列表（所有产出的 PR 文件路径）
- 每个 PR 的摘要（至少包含 PR 名和涉及 tasks 列表）
- 追溯：F02 AC#5；架构 AR-02 调度序列步骤 2

**AC-T02-8**：commit-planner.md 中声明的 PR 文件输出格式，与 T01（workflow-scm.md）定义的 F03 格式规范中所有必填字段一致（字段名称和含义对应）。判断方法：逐一对照 T01 的 AC-T01-7 字段列表，在 commit-planner.md 中均能找到对应的说明或引用。
- 追溯：F03 AC#5；架构 AR-03

---

**前置依赖**: T01（commit-planner.md 的输出格式契约须引用或对照 workflow-scm.md §文档路径协议 中的 F03 格式规范；T01 完成前无法完成 AC-T02-8 的验收）

**优先级**: P1

---

## T03：修改 `roles/workflow-pb/workflow-pb.md`

**覆盖功能**: F04（全部）、F06（AC#3）

**工作描述**: 编辑已有文件 `roles/workflow-pb/workflow-pb.md`。在阶段定义表中插入 Phase 5（提交规划）行，将原 Phase 5（后端实现）顺移为 Phase 6，原 Phase 6（独立验证）顺移为 Phase 7，并更新文件中所有阶段总数引用（"6 个阶段" → "7 个阶段"），包括 status.md 模板中的阶段表。Phase 5 推进条件须包含对 workflow-scm.md §不变量 两条规则的引用或说明。

---

### 验收标准

**AC-T03-1**：`roles/workflow-pb/workflow-pb.md` 阶段定义表中存在 Phase 5（提交规划）行，位于 Phase 4（任务拆解）之后、Phase 6（后端实现）之前（即表格第 5 行为提交规划）。
- 追溯：F04 AC#1

**AC-T03-2**：Phase 5 行包含所有列且均非空：
- 阶段编号：`5`
- 职责描述：描述将 `tasks.md` 转化为 PR/worktree 分组规划的职责
- 输入：`tasks.md`
- 输出：`prs/` 目录（含 Issues 分组 + PR 独立文件）
- 推进条件：包含以下两条可独立判断的条件，两条缺一不可：
  - "每个 tasks.md 中的任务都已归入某个 PR"
  - "每个 PR 文件满足 F03 规范（含上下文摘要/涉及 tasks/文件范围/验收标准/参考资料字段）"
- 追溯：F04 AC#2

**AC-T03-3**：Phase 5 推进条件中包含对 SCM 强制约束的引用或说明，使主 agent 仅读 workflow-pb.md 即可获知以下两条约束：
- 从 tasks.md 产出后，代码变更必须在独立 worktree 分支上进行，不得直接在主分支提交
- 每个 worktree 变更须对应 `prs/pr-{id}.md` 文件，merge 前须存在
- 追溯：F06 AC#3

**AC-T03-4**：原 Phase 5（后端实现）编号改为 `6`，其职责描述、输入、输出、推进条件内容与修改前逐字一致。
- 追溯：F04 AC#3、AC#5

**AC-T03-5**：原 Phase 6（独立验证）编号改为 `7`，其职责描述、输入、输出、推进条件内容与修改前逐字一致。
- 追溯：F04 AC#3、AC#5

**AC-T03-6**：文件开头"工作流包含 N 个阶段"描述从 `6` 更新为 `7`，与阶段表实际行数一致。文件内所有其他阶段总数引用（含 status.md 模板中的阶段表）均更新为 7 个阶段。
- 追溯：F04 AC#4

**AC-T03-7**：原 Phase 1–4 的职责描述、输入、输出、推进条件内容与修改前逐字一致（仅编号不变，无内容改动）。
- 追溯：F04 AC#5

**AC-T03-8**：`roles/dev/dev.md` 和 `roles/verifier/verifier.md` 未被修改（git diff 显示这两个文件无变更）。
- 追溯：F04 边界；F06 AC#4

---

**前置依赖**: 无（Phase 5 推进条件中的 SCM 约束措辞可直接来自 F06 AC#1；T01 推荐先完成以确认 §不变量 章节的准确名称，但不构成独立验收的硬依赖）

**优先级**: P1

---

## 验收前置检查

以下检查在任意任务验收时适用：

1. 所有新建文件的路径与 `architecture.md §二` 组件图声明的路径完全一致
2. `roles/verifier/verifier.md` 文件未被修改（F05 AC#2；F06 AC#4）
3. `roles/dev/dev.md` 文件未被修改（F04 边界；F06 AC#4）

---

## 循环依赖检查

依赖图为有向无环图（DAG）：

```
T01 → T02
（T03 无硬依赖）
```

无环，无需上报。
