# 独立验证报告 — 0011 阶段 5 补丁 PR-009（内置默认模型修订）

**验证者身份**：同级代码审查者（Node.js ESM 服务端 + 测试工程背景：能读懂 fake ACP 注入式测试的判别力、能识别"断言退化为恒真"的坑、熟悉配置默认值变更的爆炸半径判断）
**产出物**：
- `.pb-agents/worktrees/0011-pr-009-default-model/oamp/src/config.js`
- `.pb-agents/worktrees/0011-pr-009-default-model/oamp/test/config-file.test.js`
- `.pb-agents/worktrees/0011-pr-009-default-model/oamp/test/{context-pool,acp-daemon,web}.test.js`
- `.pb-agents/worktrees/0011-pr-009-default-model/docs/iterations/0011-chat-context-protocol/prs/{pr-001-config-surface-and-persistence.md,pr-001-tasks.md,pr-005-tasks.md}`

**验证标准来源**：主 agent 委托的 5 条验证标准（V1~V5，见下逐项）
**验证日期**：2026-09-10
**独立性声明**：未接收任何执行过程上下文；全部结论来自产出物读取 + 本地命令复现 + 自建对照实验（对照实验在 `/tmp` 的工作区副本上进行，**未修改任何被验证文件**）。

**产出物状态快照（用于复查）**：worktree 分支 `feat/0011-pr-009-default-model`（base = `d78d8c0`），改动为**未提交**工作区状态，共 8 个文件：1 src + 4 test + 3 prs 文档（`git status --porcelain` 无新增文件、无未跟踪文件）。

---

## 逐项判定

### V1 内置默认与优先级链：**pass**

| 检查项 | 命令 / 位置 | 观察 |
|---|---|---|
| 内置默认常量 | `oamp/src/config.js:16` | `const MODEL_DEFAULT = 'deepseek/deepseek-v4-flash';`（:15 带修订说明注） |
| 取值表达式 | `oamp/src/config.js:120` | `defaultModel: readNonEmptyString(env.OAMP_OMP_MODEL) \|\| file.model \|\| MODEL_DEFAULT` |
| `loadConfig({})` 无 env 路径 | `node -e "import('./src/config.js').then(m=>console.log(m.loadConfig({}).defaultModel))"` | `"deepseek/deepseek-v4-flash"` |
| 默认导出（真实 env 路径） | 同上，`m.default.defaultModel` | `"deepseek/deepseek-v4-flash"` |
| env 干扰已排除 | `env \| grep -iE "OAMP"` → 仅 `ANTHROPIC_MODEL`（非本模块键）；`test -f oamp/config.json` → `NO` | 环境与磁盘均无覆盖源，默认导出走的是真内置值 |
| 默认导出确为"解析"而非"硬编码常量" | `OAMP_OMP_MODEL=env/model-e node -e ...` | `"env/model-e"`（env 路径真实生效） |
| 优先级链 env > config.json > 内置 | 自建临时配置 `/tmp/cfgcheck/config.json`（`{"defaults":{"model":"file/model-f"}}`）：<br>① `loadConfig({})` → `deepseek/deepseek-v4-flash`<br>② `loadConfig({OAMP_CONFIG:file})` → `file/model-f`<br>③ `+ OAMP_OMP_MODEL:'env/model-e'` → `env/model-e`<br>④ `OAMP_OMP_MODEL:''`（空串）→ `file/model-f` | 四级逐键回落全部正确；空串 env 视为未提供（`readNonEmptyString` :38-40 会 trim）后继续向下一级取值 |
| 回归 | `node --test test/config-file.test.js` | `# tests 9 / pass 9 / fail 0` |

### V2 连带断言同步（含切换语义未被削弱）：**pass**

**同步面（逐一核对 `git diff`）**

| 位置 | 原值 | 现值 | 语义 |
|---|---|---|---|
| `test/context-pool.test.js:49`（fake spawnModel 兜底镜像） | gpt-5.6-luna | deepseek/…-flash | 镜像常量 |
| `test/context-pool.test.js:244`（E-1 第二轮回读 `model` 取自 ACP currentValue） | gpt-5.6-luna | deepseek/…-flash | 默认值断言 |
| `test/context-pool.test.js:417`（首轮 model） | gpt-5.6-luna | deepseek/…-flash | 默认值断言 |
| `test/context-pool.test.js:423`（失败轮仍只报 ACP 实报值） | gpt-5.6-luna | deepseek/…-flash | 默认值断言 |
| `test/context-pool.test.js:434`（**未指定轮次回到默认模型**） | gpt-5.6-luna | deepseek/…-flash | 默认值断言 |
| `test/context-pool.test.js:537`（§6.6 daemon `--model` 启动参数） | gpt-5.6-luna | deepseek/…-flash | 默认值断言 |
| `test/context-pool.test.js:430`（**切换目标**） | `deepseek/deepseek-v4-flash`（当时 = 默认） | `alpha/model-a` | **切换目标已改为异于默认** |
| `test/acp-daemon.test.js:46`、`test/web.test.js:42`（fake 兜底镜像） | gpt-5.6-luna | deepseek/…-flash | 镜像常量（见偏差 D-2） |

**"切换模型不重建进程"用例的判别力 —— 自建对照实验（工作区副本 `/tmp/pr009-ctrl/oamp`，被验证文件零改动）**

对照手法：只改副本中 fake ACP 的行为，制造"切换请求被接受但未真正生效"的情形，观察用例是否还能通过。

| 实验 | 变异 | 结果 | 说明 |
|---|---|---|---|
| A | fake ACP 进入 sticky（`set_config_option` 回 ok 但不改 `currentValue`），用例保持现状（目标 `alpha/model-a`） | **FAIL**：`expected 'alpha/model-a'` / `actual 'deepseek/deepseek-v4-flash'`（`mutA.test.js:429`） | 断言确实绑定"ACP 真正切换成功"，不是恒真 |
| B | 同 sticky，且把**请求目标与断言一并改回与内置默认同值**（=`'deepseek/deepseek-v4-flash'`，即变更前的形态） | **PASS** | 证实：若切换目标等于默认值，用例退化为"指定同值"，**即使切换完全没生效也照样通过**。当前改为 `alpha/model-a` 正是避免该退化 |
| D | fake ACP 静默忽略"回到默认"的对齐调用（仅当 `value === spawnModel`） | **FAIL**：`未指定轮次回到默认模型` → `expected 'deepseek/deepseek-v4-flash'` / `actual 'alpha/model-a'`（`mutD.test.js:435`） | "未指定轮次回到默认模型"断言（:434 新值）仍具判别力：能捕获 agent 未重新对齐默认模型的情形 |

结论：切换目标 `alpha/model-a` ≠ 默认值 `deepseek/deepseek-v4-flash`，**"切换模型不重建进程 + 切换不丢上下文"的语义未被削弱**；A/B 对照同时证明该取值选择是必要而非随意的。完整实验见本报告末「对照实验复现脚本」。

### V3 prs 三文档同步与修订注：**pass**

| 文件:行 | 内容要点 |
|---|---|
| `prs/pr-001-config-surface-and-persistence.md:23` | 验收 1 改为 `defaultModel = 'deepseek/deepseek-v4-flash'`，注："2026-09-10 用户修订，依据 V-13；gpt-5.6-luna 保留为可指定值" |
| `prs/pr-001-tasks.md:32` | 优先级表兜底值改为新值 + 同款修订注 |
| `prs/pr-001-tasks.md:83` | 验收取证语句改为新值 + 同款修订注 |
| `prs/pr-005-tasks.md:67` | T3 描述改为 "模型指定与默认 `deepseek/deepseek-v4-flash`（修订注）" |
| `prs/pr-005-tasks.md:74` | T3-2 ④ 模型链兜底值改为新值 + 修订注 |

`grep -rn "gpt-5.6-luna" docs/iterations/0011-chat-context-protocol/prs/` 的全部命中均为上述"保留为可指定值"说明注，**无遗留旧默认值**。

**面外确认（不计入本 PR 判定）**：主仓库迭代分支 HEAD `cb2dcb6`（"默认模型修订决策落地"）已更新 `architecture.md`（V-9/V-13、L1-5/L1-6、AR-13、§16.5 补丁记录）、`prd.md`、`prd/F06-model-selection-default.md`、`prd/F07-config-surface-data-location.md`、`oamp/README.md:87/:185`；本 PR 不负责这些文件，**未因它们作任何 fail 判定**。

### V4 测试全绿 + 零新依赖 + 文件范围：**pass**

| 检查项 | 命令 | 结果 |
|---|---|---|
| 指定单测 | `node --test test/config-file.test.js` | `# tests 9 / pass 9 / fail 0`（45.7ms） |
| 全量回归 | `npm test`（`node --test test/*.test.js`） | `# tests 152 / pass 152 / fail 0 / cancelled 0 / skipped 0 / todo 0`（37.3s） |
| 零新依赖 | `package.json` `dependencies: {}` 未改动；`git diff --name-only \| grep -E "package.json\|lock"` → 无命中 | 通过 |
| 文件范围 | `git diff --stat` | 8 文件 / +18 −17：`oamp/src/config.js`(+2−1，1 注 + 1 常量)、4 个 test 各 1~8 行常量/断言替换、3 个 prs 文档各 1~2 行。**无新增文件、无越界文件** |

一致性补充：`oamp/src` 全扫后，**默认模型的唯一定义点仍是 `config.js:16`**（`grep -rn "luna\|MODEL_DEFAULT\|5\.6" oamp/src oamp/bin oamp/scripts` → 仅 config.js 的注释行与定义行、`:120` 引用行；另有 `src/registry.js:1` 因 `§5.2~5.6` 章节号误命中，非模型引用）；前端 `oamp/web/index.html:57` 占位符为"模型（留空用默认）"、`oamp/web/app.js:415` 未填不携带 model → 前端无硬编码默认，无需改动。

### V5 边界与残留扫描：**pass**

`grep -rn "gpt-5.6-luna" oamp/src oamp/test oamp/web` 命中分类：

| 位置 | 性质 | 判定 |
|---|---|---|
| `src/config.js:15` | 修订说明注（"原值 openai/gpt-5.6-luna 保留为可指定值"） | 合理残留 |
| `test/persist.test.js:159/:163/:393` | `insertOutput(..., model: 'openai/gpt-5.6-luna', ...)` → 断言读回同值：**DB 往返夹具**，非默认值语义 | 合理残留 |
| `test/web.test.js:715/:744` | 对账补拉投递体里的 `model` 字段 → 断言 `out.model` 落盘同值：**回显夹具**，非默认值语义 | 合理残留 |
| `oamp/web/**` | 零命中 | — |

结论：**无"旧默认值仍作为默认值语义存在"的残留**；剩余 4 处均为说明注或与默认值无关的夹具字符串。

---

## 汇总

- pass: **5** 项（V1 / V2 / V3 / V4 / V5）
- fail: **0** 项
- partial: **0** 项
- blocked: **0** 项

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| **D-1**：worktree 内 `oamp/README.md:87/:182/:195` 仍写旧内置默认 `openai/gpt-5.6-luna` | 迭代分支 `cb2dcb6` 已把 README 改为 `deepseek/deepseek-v4-flash`（含"gpt-5.6-luna 为可选值"说明），但本 PR 分支 base = `d78d8c0`（早于 `cb2dcb6`），故 PR 工作区内呈现的是**继承来的旧文本**。已核实 `cb2dcb6` 改动的 5 个文件与本次 8 个改动文件**零交集**（`git merge-tree` 无冲突），合并不会回退 README | 无需处理——合入迭代分支后自然消除。仅作事实登记，避免 reviewer 在 worktree 内看到旧 README 误判为漏改 |
| **D-2**：`test/acp-daemon.test.js:46`、`test/web.test.js:42` 的 fake `spawnModel` 兜底常量被同步 | agent 在 daemon 路径**恒传** `--model`（`src/acp-client.js:80`，`modelArg` 由解析后的默认模型填充），故该兜底分支在真实用例中不被触发；改动性质是"镜像常量一致性维护"，非行为必需 | 保留（无害且更诚实）。若追求最小 diff，可在下轮清理该死兜底分支；不建议在本次补丁内追加改动 |
| **D-3**：架构 §7.1 描述模型解析链"每轮独立：payload.model > OAMP_OMP_MODEL > config.defaults.model > 内置默认" | 该链实际只覆盖 `omp-daemon` 路径（`src/agent.js:267` `task.model \|\| ctx.defaultModel`）；**一次性路径** `runOmpTask`（`src/agent.js:160`）仅在 payload 带 `model` 时才传 `--model`，未指定时用的是 CLI 自身默认，不落本迭代默认链 | 属**既有行为**、非本 PR 引入（本 PR 只改常量）。是否需要对文档口径或实现对齐，请主 agent 决策；已列入下一迭代候选 |

## 下一迭代候选

- **C-1 一次性路径的默认模型语义**：与 D-3 同源。若产品预期"未指定模型"在两种执行路径下语义一致，一次性路径需要显式注入 `defaultModel`；否则应在文档中明写"内置默认链仅对常驻上下文路径生效"。
- **C-2 默认值的单一真源**：新默认值当前散落在 `config.js:16` + 3 个测试文件 fake 的镜像常量（`context-pool:49`、`acp-daemon:46`、`web:42`）+ 5 处断言字面量；下一次改默认值需同步 ~9 处（本 PR 的 diff 正是这一成本的实证）。可考虑测试侧经 `loadConfig({}).defaultModel`（或导出常量）取值以消除镜像。
- **C-3 reasoning 模型静默期**：V-13（首 token ≈242s）与 pr-008 的等待计时/慢模型提示是否在真实 reasoning 模型下达到预期可见性，尚无真实场景复验记录（本轮验收标准不覆盖）。

## 结论

**PASS**（5 项全部 pass，无 fail、无 partial、无 blocked）

主 agent 可直接据此判定该补丁 PR 满足合并前置；偏差 D-1/D-2/D-3 均被显式记录，不阻塞本 PR 合入，其中 D-3 与 C-1 建议纳入下一迭代决策。

---

## 附：命令与对照实验复现脚本（供复查）

```bash
# 1) 内置默认 + env 路径（消除 env 干扰：先确认无 OAMP_* 环境变量、无 oamp/config.json）
cd .pb-agents/worktrees/0011-pr-009-default-model/oamp
env | grep -iE "OAMP" ; test -f config.json && echo YES || echo NO
node -e "import('./src/config.js').then(m=>{console.log(m.loadConfig({}).defaultModel, m.default.defaultModel)})"
OAMP_OMP_MODEL=env/model-e node -e "import('./src/config.js').then(m=>console.log(m.default.defaultModel))"

# 2) 优先级链（含空串回落）
mkdir -p /tmp/cfgcheck && printf '{"defaults":{"model":"file/model-f"}}' > /tmp/cfgcheck/config.json
node -e "import('./src/config.js').then(m=>{
  const f='/tmp/cfgcheck/config.json';
  console.log(m.loadConfig({}).defaultModel);                                   // 内置
  console.log(m.loadConfig({OAMP_CONFIG:f}).defaultModel);                      // 文件
  console.log(m.loadConfig({OAMP_CONFIG:f,OAMP_OMP_MODEL:'env/model-e'}).defaultModel); // env
  console.log(m.loadConfig({OAMP_CONFIG:f,OAMP_OMP_MODEL:''}).defaultModel);    // 空串→文件
})"

# 3) 测试
node --test test/config-file.test.js
npm test

# 4) 残留扫描
grep -rn "gpt-5.6-luna" oamp/src oamp/test oamp/web

# 5) 对照实验（在 /tmp 副本上做，产出物零改动）
rm -rf /tmp/pr009-ctrl && mkdir -p /tmp/pr009-ctrl && cp -R oamp /tmp/pr009-ctrl/oamp
#  mutA：在 §7.3/§7.4 用例内 setup(t) 追加 env { FAKE_ACP_STICKY_MODEL: '1' } → 期望 FAIL
#  mutB：同 mutA，并把该用例中“请求目标 + 断言期望”一并改回 'deepseek/deepseek-v4-flash'（=内置默认）→ 期望 PASS（证明“目标=默认”会退化）
#  mutD：fake ACP 的 set_config_option 分支追加——当 value === spawnModel 时静默忽略，并回 currentValue=session.model；
#        以 CTRL_IGNORE_DEFAULT=1 运行 → 期望在“未指定轮次回到默认模型”断言 FAIL
cd /tmp/pr009-ctrl/oamp
node --test --test-name-pattern="未知模型" test/mutA.test.js
node --test --test-name-pattern="未知模型" test/mutB.test.js
CTRL_IGNORE_DEFAULT=1 node --test --test-name-pattern="未知模型" test/mutD.test.js
```
