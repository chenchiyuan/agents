# 独立验证报告：PR-005 会话面清理 + 端到端契约验收 + 文档同步

**验证者身份**：同级代码审查者（跨进程集成测试方向）——有资格审这份 diff、清楚"端到端契约测试"最容易骗过自己的地方（自证式断言、只读测试不复现、把过程行当落盘证）；同时具备进程级集成调试与 SQLite 一手取证经验。

**产出物**：
- worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-005-cleanup/`（分支 `feat/0011-pr-005-cleanup`，HEAD `66bcd2c`，基线 = `514d299`，worktree 干净无未提交改动）
- `oamp/src/router.js`、`oamp/src/registry.js`（删除面）、`oamp/test/acp-daemon.test.js`（新建）、`oamp/README.md`（更新）、`docs/iterations/0011-chat-context-protocol/prs/pr-005-tasks.md`

**验证标准来源**：
1. `docs/iterations/0011-chat-context-protocol/prs/pr-005-cleanup-e2e-and-docs.md`（7 条验收标准）
2. `architecture.md` §9.3（删除清单 /「不动（回归边界）」）/ §12 AR-15 / §17（测试策略）/ §18 R-13、R-14 / §19 裁决 2、3、5
3. `prd/F08-baseline-compatibility-regression.md`（验收 3 新口径 + a~d 等价项 + 边界）
4. 委托方给的判定条目 V1~V9

**验证日期**：2026-09-10

**独立性声明**：本次验证未接收任何执行过程上下文（未读执行者自述报告，未采信提交信息中的自我声明）。所有结论来自产出物本身、独立运行的命令与**自建复现装置**。

---

## 0. 验证手法（不依赖被测测试文件）

| 手法 | 说明 |
|---|---|
| 自建 fake ACP | `/tmp/oamp-verify-pr005/fake-omp.cjs`——独立编写（不复制 `test/acp-daemon.test.js` 的 fake 源码），记忆语义（`已记录\|N` / `回忆值=N` / `无记忆`）与被测 fake 不同，回答切 **3 片**流式（被测为 2 片） |
| 自建 e2e 驱动器 | `/tmp/oamp-verify-pr005/repro.mjs`——起真实 `oamp router start` / `oamp agent start dev-1`（`OAMP_OMP_BIN` 注入自建 fake）/ `oamp web start --port <随机>`（临时 `OAMP_DB`），fetch 断言 REST + 手写 SSE reader + `node:sqlite` **直连库文件** |
| 独立复现结果 | `38/38 pass`（E-1~E-5、F08-1/2、F08-4 错误面与控制台） |
| 保留面行为核查 | `/tmp/oamp-verify-pr005/preserved.mjs`——`oamp task send/status/list` 真跑 + 关闭语义 409/只读，`8/8 pass` |
| 静态核查 | `git diff` 逐文件比对、全仓 grep（`oamp/src`、`oamp/test`、`oamp/web`、`oamp/bin`）、README 声明 ↔ 源码逐项对照 |

复现命令（产物未改动，装置在仓库外）：

```
cd <worktree>/oamp && node --test test/acp-daemon.test.js      # 连跑 3 次
cd <worktree>/oamp && npm test                                 # 全量
node /tmp/oamp-verify-pr005/repro.mjs                          # 自建独立复现
node /tmp/oamp-verify-pr005/preserved.mjs                      # 保留面 + 关闭语义
```

---

## 逐项判定

### V1 测试全绿（含回归）：**pass**

| 命令 | 结果 |
|---|---|
| `node --test test/acp-daemon.test.js`（第 1 次） | `# pass 5 / # fail 0`，10.45s |
| 同上（第 2 次） | `# pass 5 / # fail 0`，10.55s |
| 同上（第 3 次） | `# pass 5 / # fail 0`，10.34s |
| `npm test`（`node --test test/*.test.js`） | `1..145 / # tests 145 / # pass 145 / # fail 0`，24.55s |

证据：连跑 3 次无抖动；全量 145 与提交信息自述一致（**独立复算，非采信**）。运行后 `pgrep -fl "<worktree>/oamp/bin/oamp.js"` 无残留进程，worktree `git status --porcelain` 为空，`oamp/data/`、`oamp/.runtime/` 不存在（测试未污染仓库）。

### V2 删除面零残留：**pass**

- `grep -rn "chat_message|chat_get|chat_list|appendChatMessage|chatDetail|tasksByMessage|createChat" oamp/`（含 `*.js`/`*.html`/`*.css`/`*.json`/`*.md`）→ **零命中**。
- 全仓 grep 的命中全部落在 `docs/iterations/0010-*`、`docs/iterations/0011-*/{architecture.md,prs/*,clarifications/*}`——**文档对删除动作本身的描述**，不属删除面（删除面 = `src/`、`test/`、`web/`、`bin/`）。
- `git diff 514d299 HEAD -- oamp/src/registry.js oamp/src/router.js`：`0 insertions`，纯删除 134 行（registry 86 行 / router 48 行）——不存在"删一半"或"以新实现顶替旧实现"的隐性残留。
- 归属核查（MI-4 口径）：`getChat`/`listChats` 裸名仅存在于 `src/persist.js`（:141/:145 预编译语句、:191/:210 函数、:219 导出）并被 `src/web.js:203/302/320/330/389/423` 消费——库读口，合法保留；`registry.` 前缀零命中。
- 孤儿引用核查：`router.js` 头部 4 个 import（`isValidInstanceId`/`isValidMessageId`/`newSessionId`/`createRegistry`）删除后仍被消费（分别 3/5/3 处命中），无孤儿 import；`router.js:278` 仍用 `newSessionId()` 生成 `task-`，与 T1-6 声明一致。

### V3 保留面与理由：**pass**（含 1 条文案偏差，见偏差 #2）

保留面三处**逐字未动**（`git diff` 中零改动行，与 base `514d299` 逐字节比对一致）：

| 位置 | 现状 |
|---|---|
| `src/registry.js:177` | `function createTask({ taskId, from, to, now, label, messageId = null })` |
| `src/registry.js:185` | 任务条目 `message_id: messageId,` |
| `src/router.js:296` | `messageId: m.message_id,` |

理由已记录且可验证：架构 §9.3「不动（回归边界）」明列「Router 任务表与 `oamp task` CLI」；§16.4 排除「Router 任务表改造」。`router.task_get`/`task_list` 直接返回 registry 任务对象（`src/router.js:371-388`），删字段会改变 RPC 输出形状——**该事实独立成立**（见偏差 #2 对措辞的修正）。残留事实（字段失去唯一读者）已在提交信息中如实声明。

**行为级验证**（`preserved.mjs`，非仅静态）：真实 Router + agent 下 `oamp task send dev-1 '{"command":"echo","args":["task-ok"],"label":"verify"}'` → 返回 task_id；`oamp task status <id>` → `state: completed` + 明细含 `echo task-ok`；`oamp task list` 可用。删除 `tasksByMessage` 后 `oamp task` CLI 链路未回归。**8/8 pass**。

### V4 E-1 / E-2（上下文累积与隔离）：**pass**

独立复现（自建 fake + 自建驱动，未运行被测测试文件）：

```
PASS | E-1 轮1 得到 agent 回答 | 已记录|42
PASS | E-1 轮2 记得 42 | 回忆值=42
PASS | E-1 两轮 out.meta.context_id 相同且非空 | ctx-94501-1 vs ctx-94501-1
PASS | E-1 两轮 out.meta.pid 相同且非空 | 94501 vs 94501
PASS | E-2 新 chat id 不同
PASS | E-2 新 chat 无 42（上下文隔离） | 无记忆
PASS | E-2 新 chat 自身可累积（答 7） | 回忆值=7
PASS | E-2 旧 chat 未被新 chat 污染
```

被测测试文件对同两条判据的断言（`test/acp-daemon.test.js:317-350` 区段）经阅读确认与上述独立结果同向，且断言强度不低于 PR 卡原文（额外加了 `out.meta.pid` 相等与"新 chat 自身仍可累积"的加固）。**结论不依赖被测测试**：自建装置得同一事实。

### V5 E-3（重启 web 后历史可读）：**pass**

```
PASS | E-3 重启 web 后 GET /api/chats 200
PASS | E-3 重启 web 后 GET /api/chats/:id 200
PASS | E-3 列表项重启前后逐字段一致 | {chat_id,title,agent_id,state,created_at,updated_at,message_count} 全等
PASS | E-3 chat 行重启前后一致
PASS | E-3 消息流重启前后一致
PASS | E-3 直连 SQLite 与 REST 文本一致 | ["请记住数字 42","已记录|42","数字是多少","回忆值=42"]
PASS | E-3 直连 SQLite direction 序列 in,out,in,out
```

双验成立：REST 读回（进程外）+ `node:sqlite` **直连库文件**读回（不经 web 读口，不经 `src/persist.js`）。重启 web 前后 Router/agent 未动，读回仍一致 → 证历史真源在库，不在进程内存。

### V6 E-4（终态前的实时增量）：**pass**

```
PASS | E-4 message(in) 先于过程增量 | in=0 firstUpdate=2
PASS | E-4 终态前 task_update ≥2 | n=3
PASS | E-4 全部 task_update 早于 message(out) | last=4 out=5
PASS | E-4 task_update 归属该 chat/task | ["chunk","chunk","chunk"]
PASS | E-4 增量文本严格递增 | acc="回忆值=42"
PASS | E-4 增量拼接 == 终态落盘文本 | {"acc":"回忆值=42","final":"回忆值=42"}
PASS | E-4 过程增量不入库（该轮仍恰 6 行）
```

自建 fake 切 3 片（被测 fake 切 2 片），断言 `≥2` 与"拼接 == 终态"均成立 → 判据不依赖被测 fake 的分片数。被测测试额外断言 `idxLastUpdate < idxOut`（"全部增量早于终态"），强度高于 PR 卡原文。

### V7 E-5（落库口径恰两类）：**pass**

- 一次含多片增量的问答后，**直连 SQLite** `SELECT direction, text FROM messages WHERE chat_id=?` → **恰 2 行**，`direction` 去重集合 = `["in","out"]`。
- 两轮后 → 恰 4 行（`in,out,in,out`）；E-4 那一轮结束后仍恰 6 行——**过程增量零行**（`task_update` 只走 SSE，不入库）。
- schema 层佐证：`src/persist.js:16-27` 的 `messages.direction` 仅有两个写口 `insertInput`/`insertOutput`，`direction` 在函数体内写死、不暴露参数（§4.7 的工程保障）。

### V8 F08 三形态不回归（F08-1/F08-2）：**pass**

```
PASS | F08-1 `!` 走 shell 且输出可见 | verify-shell-ok
PASS | F08-2 显式 one_shot 走 omp -p | VERIFY-ONESHOT-REPLY<请记住数字 7>
PASS | F08 一次性/shell 不进入常驻上下文（仍答 42） | 回忆值=42
PASS | F08 常驻实例标识未被两形态重建 | ctx-94501-1 vs ctx-94501-1
PASS | F08 两形态启动参数可观测 | [["acp",…],["acp",…],["-p","--no-session","--no-tools","请记住数字 7"]]
```

交叉轮次脚本（自建，非被测脚本）：常驻记住 42 → 插一轮 `one_shot`（记住 7）→ 插一轮 `!` 命令 → 再问常驻仍答 42 且 `context_id` 未变 → 一次性/shell 既不进入也不复用常驻上下文，且不重建常驻实例。启动参数日志证两条路径都被真实走到（`acp` 常驻 / `-p` 一次性），非"只看返回文本"。

补充 F08-4（既有交互保留，README 声明面）独立核查：`/api/agents` 返回 `dev-1 state=online`；`/` 返回 200 且含页面标识；缺 agent → 400；空白输入 → 400；未知 chat → 404。`preserved.mjs` 另核关闭语义：`POST /api/chats/:id/close` → 200，关闭后新输入 → **409**，历史只读仍 200 且 `state=closed` 保留 2 条记录。**8/8 pass**。

### V9 README 与边界卫生：**pass**

**失效表述已清除**：`grep -n "会话与消息存于 Router 内存|重启即清空|轮询" oamp/README.md` → 仅 1 处命中 `README.md:71`「任务与明细存于 Router 内存（Router 重启即清空——迭代 0010 N2 无持久化边界）」。该句描述 **Router 任务表**，与实现一致（§9.3「不动」；`preserved.mjs` 实测任务表确为内存且可用），属 MI-3 声明的"分面处置"，**不是**失效表述。

**新增能力文档 ↔ 实现逐项对照**（静态核对，无"文档里的接口不存在"）：

| README 声明 | 实现锚点 | 判定 |
|---|---|---|
| `oamp/config.json` 三键 `data.db`/`defaults.model`/`context.max`；相对路径基准 = 包根 | `src/config.js:14-16/70-95/:104-106`（`PKG_ROOT` + `path.resolve(PKG_ROOT, …)`） | ✓ |
| env `OAMP_CONFIG`/`OAMP_DB`/`OAMP_OMP_MODEL`/`OAMP_CTX_MAX`/`OAMP_WEB_PORT`/`OAMP_OMP_BIN` | `src/config.js:98/105/106/107`、`src/web.js:29/139`、`src/agent.js:22`（`OAMP_OMP_BIN`） | ✓ |
| 默认 db `<包根>/data/sql.db`、默认端口 7788、默认模型 `openai/gpt-5.6-luna`、`context.max` 默认 8 | `src/config.js:15-17`、`src/web.js:29`、`context-pool.js:23` | ✓ |
| 5 条 web API + SSE 端点 | `src/web.js:294/299/318/328/349/358`（路由逐条命中） | ✓ |
| SSE 四类事件 `message`/`task_update`/`chat_state`/`notice` | `src/web.js:10`、`:180 chat_state`、`:215 notice`、`:226 task_update`、`publishMessage` | ✓ |
| 一次问答恰两条记录、过程永不入库、启动扫尾置 failed | `src/persist.js:6-9`、`:219 startupSweep`；E-5 实测 | ✓ |
| 同键串行 / 队列上限 8 / `context_busy` / LRU `notice{context_reset}` | `src/context-pool.js:9/78/83/128-129` | ✓ |
| `omp-daemon` 默认 / `omp -p --no-session` 一次性 / `!` shell 三形态（判定顺序 `!` → 一次性 → 默认） | `src/agent.js:22-24`（300s/600s 上限）、`src/web.js:405-409`；启动参数实测 | ✓ |
| 关闭语义：释放上下文 + 只读 + 409 + 不重开 | `src/web.js:328-340`；`preserved.mjs` 实测 409/只读 | ✓ |
| 前端控件（New chat / Working / Completed 过滤 / 模型输入 / 一次性开关 / 关闭按钮） | `web/index.html:25/29/30/57/58`、`web/app.js:188/346/368` | ✓ |
| 状态集合 `working/completed/failed/closed`（无 `idle`） | `src/persist.js:31` | ✓ |

**边界卫生**：`git diff --name-status 514d299 HEAD` = 恰 5 项（`M oamp/README.md`、`M oamp/src/registry.js`、`M oamp/src/router.js`、`A oamp/test/acp-daemon.test.js`、`A docs/.../pr-005-tasks.md`），与 PR 卡文件范围一致；`oamp/package.json`、`oamp/.gitignore` **零改动**（`git diff` 空）→ 零新依赖；`test/hygiene.test.js:61-63` 静态断言 `dependencies` 为空且全绿。

**F08-3 口径核查**：`git diff --stat 514d299 HEAD -- oamp/test/` 仅 `A oamp/test/acp-daemon.test.js`；基线与现状测试文件数 15 → 16。PR 卡点名的 10 个 0010 基线测试文件（`agent-heartbeat`/`cli`/`delivery-contract`/`event-log`/`hygiene`/`omp-executor`/`reconnect`/`router-registry`/`status`/`task`）连同 pr-001~004 的 5 个文件**全部零修改**且全量 145/145 全绿（含 pr-004 等价重写的 `web.test.js`）。

---

## PR 卡 7 条验收 ↔ 判定映射

| PR 卡验收标准 | 判定 | 主证据 |
|---|---|---|
| 1 删除面 + 全仓 grep 无残留 | pass | V2（零命中 + 纯删除 134 行 + import 无孤儿） |
| 2 `messageId` 保留声明 | pass | V3（三处逐字未动 + 提交信息含残留事实声明；文案偏差见偏差 #2） |
| 3 其余 10 个既有测试文件未修改且全绿 | pass | V9 末段（diff 仅新增 1 文件 + 145/145） |
| 4 e2e E-1~E-5 断言通过 | pass | V4/V5/V6/V7（自建装置独立复现 38/38） |
| 5 两形态不回归（F08-1/F08-2） | pass | V8（交叉轮次 + 启动参数日志） |
| 6 `npm test` 全量通过 | pass | V1（145/145） |
| 7 README 失效描述清除 + 新能力说明 | pass | V9（逐项对照表 + 仅存的任务表内存描述属实） |

---

## 汇总

- pass: **9** 项（V1~V9）
- fail: **0** 项
- partial: **0** 项
- blocked: **0** 项

---

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| 1 | PR 卡验收 2 / `pr-005-tasks.md` T1 / 提交信息：保留 `message_id` 的理由之一写作「被 `oamp task` CLI 消费」 | `src/task.js` 的 `renderTask`（:79）/`renderList`（:113）**不渲染 `message_id`**（实测 `oamp task status/list` 输出无该字段）；该字段的真实约束是 `router.task_get`/`router.task_list` **原样返回任务对象**（`src/router.js:371-388`）这一 RPC 协议可见面，而非 CLI 渲染 | 下一迭代同步文档措辞：把理由改为「RPC 响应对象形状（协议可见面）」，勿写"CLI 消费"。保留决定本身正确，仅措辞不精确 |
| 2 | `src/router.js:296` 行内注释仍写 `// web 控制台：消息 ↔ 任务关联（chat 详情 join 用）`；`registry.js:185` 的 `message_id` 字段同理 | 本 PR 已删除 `tasksByMessage` 与 `chat_detail`，**该 join 不复存在**：字段/实参现无任何读者（残留事实已在提交信息声明，但行内注释仍指向已删除的消费者）。注释文本与 base `514d299` 逐字节相同 → 是"本 PR 使其失效"，非本 PR 新写 | 下一迭代（或删除该字段的那次）更新行内注释为「保留以维持 `router.task_get` 输出形状；当前无读者」。不阻塞本 PR |
| 3 | `src/registry.js:181` 任务条目 schema 注释：`{ task_id, from, to, state, label, created_at, updated_at, updates[], result\|null }` | 实际条目含 `message_id` 字段（:185），注释未列。**预先存在**（base `514d299` 同文），本 PR 未引入也未修复 | 与偏差 #2 同批处理；优先级最低 |

---

## 下一迭代候选

- **`messageId`/`message_id` 死参数的最终处置**：本迭代按 §9.3 边界保留，但字段已失去唯一读者（仅剩"协议形状"这一保守理由）。若下一迭代触及 Router 任务表（§16.4 明确排除项），应一并决策：删字段 + `router.task_get` 形状变更（需协议版本策略）或显式冻结。
- **e2e 端口分配无冲突回退**：`test/acp-daemon.test.js` 的 `pickPort()` 取 `47000 + rand(2000)`，无 `EADDRINUSE` 重试；验证过程中我在同机观测到 48999 被占（自建装置引发）。并发运行或本机端口占用时有一定 flake 概率，可加一次重试或改由内核分配后回读实际端口。
- **`docs/iterations/0011-*/prs/pr-005-tasks.md` 中 MI-1~MI-4 四条 model_inferred 未见主 agent 确认记录**：任务图内标注"需主 agent 确认"，但 worktree 中无对应确认痕迹（MI-1/MI-2 的加固断言已实际落地并被本次验证采纳，MI-3/MI-4 的处置也与实现一致）。属流程留痕问题，供主 agent 决定是否回填。
- **历史文档中的已删符号引用**：`docs/iterations/0010-*/architecture.md:602`、`0011/architecture.md:106/479-480` 等仍含 `chat_message`/`tasksByMessage` 等符号——均为描述"删除动作/历史基线"的正文，非代码残留；如需对外交付可加"（已删除）"标注。

---

## 结论

**PASS**（9 项全部 pass，无 fail、无 partial、无 blocked；偏差 3 条已记录，按契约不阻塞本迭代关闭）。

判据补充：本次验证的 E-1~E-5 结论**不依赖被测测试文件自身的断言**——自建 fake ACP + 自建驱动器独立复现 38/38，并通过直连 SQLite 二次取证。测试稳定性经连跑 3 次确认（5/5 ×3），全量 `npm test` 145/145。
