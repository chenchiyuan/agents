# verify-20260910-130941 — 0011-chat-context-protocol 阶段 4（PR 规划）独立验证

**验证者身份（反射）**：冲刺/交付计划审查者（Sprint & Release Plan Reviewer）——长期做提交单元切分与发布编排，熟悉 worktree/依赖解锁式调度、clean cutover 与"把顺序偏好误判成依赖"这类典型错误；对"PR 边界重叠""验收标准依赖下游 PR 才能判定""伪依赖"有专门的敏感度。

- **反射依据**：产出物类型 = PR 规划（提交单元划分 + 依赖图 + 逐 PR 验收标准）；标准来源 = `workflow-pb.md` §PR 文件格式规范 / §阶段 4 推进条件 / §验证目标（PR 粒度判断框架 + 依赖正确性验证）+ `prd.md`/`prd/F01~F08` + `architecture.md` v0.3.0 + 代码库现状。
- **产出物**：`docs/iterations/0011-chat-context-protocol/prs/` 下 5 个 PR 文件（pr-001 ~ pr-005）。
- **验证标准来源**：委托 brief 的四类来源（workflow-pb 规范、prd+prd/F01~F08、architecture v0.3.0、代码库现状），逐项对应见下表。
- **验证日期**：2026-09-10。
- **独立性声明**：未使用任何执行过程上下文。委托中夹带的过程性描述（"planner 的两处重切"）不作为判定依据——两处重切只以产出物文本与 `architecture.md` 文本对照为据核实。被验证文件全程只读，未修改。

## 判定项与标准来源

| 项 | 内容 | 标准来源 |
|---|---|---|
| S1 | 七字段齐备 | workflow-pb §PR 文件格式规范（1~7） |
| S2 | 命名 `pr-{NNN-描述}.md` 三位有序 | workflow-pb §文档路径协议 + 格式规范 |
| S3 | F01~F08 全被引用、无 prd 外新增范围 | 阶段 4 推进条件 + 委托标准 2 |
| S4 | PR 间文件范围零重叠、归属唯一 | 阶段 4 推进条件 + §验证目标"依赖正确性验证"（无重叠、无遗漏） |
| S5 | `depends_on` 每条证据可核实 | §验证目标"依赖正确性验证"（代码级证据） |
| S6 | 依赖图无环 + 首批可并行集正确 | §验证目标 + §阶段 5 依赖解锁式并发调度 |
| S7 | 验收标准可独立判断 | §PR 粒度判断框架"独立性" + 格式规范 §4 |
| S8 | 与 architecture §16 边界一致（含两处重切） | 委托标准 3（§9/§11/§12/§16/§17） |
| W1 | PR 粒度判断框架（逻辑原子性 / 可审查性 / 独立性） | workflow-pb §验证目标（工作流原生标准） |
| W2 | 依赖正确性验证汇总（证据 / 无环 / 无重叠 / 无遗漏功能点） | workflow-pb §验证目标（工作流原生标准） |

---

## 逐项判定

### S1 七字段齐备 — **pass**

对 5 个文件逐一抽取 `^## ` 标题，字段名与顺序与规范 §1~§7 完全一致，无缺项、无多余字段：

| 文件 | 七字段所在行（上下文摘要 / 涉及功能点 / 文件范围 / 验收标准 / 参考资料 / depends_on / batch） |
|---|---|
| pr-001-config-surface-and-persistence.md | 3 / 7 / 13 / 21 / 34 / 41 / 45 |
| pr-002-sse-transport-abstraction.md | 3 / 7 / 11 / 16 / 25 / 30 / 34 |
| pr-003-context-pool-acp-daemon.md | 3 / 7 / 13 / 20 / 34 / 40 / 44 |
| pr-004-web-api-and-console.md | 3 / 7 / 17 / 25 / 38 / 44 / 50 |
| pr-005-cleanup-e2e-and-docs.md | 3 / 7 / 16 / 23 / 32 / 40 / 47 |

- 空依赖写法符合规范：pr-001:43、pr-002:32 均为 `（无）`。
- 验收标准条目数：pr-001 = 10 条、pr-002 = 6 条、pr-003 = 11 条、pr-004 = 10 条、pr-005 = 6 条，均 ≥1。
- `batch` 取值 1/1/2/3/4，为人工速览分组；5 个文件均未把 `batch` 当调度依据（无"等 batch 1 全合并"之类的栅栏表述）。

### S2 命名与三位有序 — **pass**

`pr-001-config-surface-and-persistence.md`、`pr-002-sse-transport-abstraction.md`、`pr-003-context-pool-acp-daemon.md`、`pr-004-web-api-and-console.md`、`pr-005-cleanup-e2e-and-docs.md`——`pr-{NNN-描述}.md` 形态、编号 001~005 连续无缺号无重复（`read` 目录实测 5 个文件）。

### S3 F01~F08 全覆盖、无新增范围 — **pass**

**覆盖矩阵**（引用自各 PR "## 涉及功能点" 段）：

| 卡 | 引用它的 PR | 覆盖完整性抽查 |
|---|---|---|
| F01 | pr-004、pr-005 | pr-004 验收含新建即入列/标题 slice(0,40)/四态/时间戳/关闭 409/不删数据（F01-1~6） |
| F02 | pr-001、pr-004、pr-005 | pr-001 覆盖 E-5 结构面（仅两个写函数）+ 重启读回；pr-004 覆盖落盘时机/失败 out/派发失败 |
| F03 | pr-001、pr-004、pr-005 | pr-001 覆盖 SQL 侧（排序/分页/闭区间/LIKE 转义/agent 相关），pr-004 覆盖 HTTP 返回结构与 400 面 |
| F04 | pr-002、pr-004、pr-005 | pr-002 覆盖 F04-6 接口形状，pr-004 覆盖端点/四类事件/E-4/断线全量拉取 |
| F05 | pr-003、pr-004、pr-005 | pr-003 覆盖 F05-1~7（含多 agent 隔离、`context_busy`、LRU+notice、崩溃重建） |
| F06 | pr-003、pr-004 | pr-003 覆盖 F06-1~5（链/切换/model_unavailable 不回退/审计取自 currentValue） |
| F07 | pr-001 | pr-001 覆盖 F07-1~6（默认包根路径/`OAMP_CONFIG`/缺失不失败/非法抛错含 `OAMP 配置错误`/逐键优先级/无热重载口径） |
| F08 | pr-003、pr-004、pr-005 | pr-003 保 `executor:'omp'` 与 shell 不回归；pr-004 保既有交互 + `web.test.js` a~d 等价重写；pr-005 保 10 个既有测试文件原样全绿 |

**无 prd 外新增范围**：逐 PR 扫描"文件范围 + 验收标准"，未出现 `architecture.md` §16.4 明列的不在范围项（WebSocket、过程持久化/回放、跨 chat 共享、鉴权、历史迁移、chat 删除/归档/导出/重命名/重开、FTS5/语义检索、模型清单 UI、token 费用、ACP 权限应答、`session/load`、TTL 回收、Router 任务表改造、`/api/search` 独立端点）。pr-002 显式声明"不引入 WebSocket/额外依赖"（与 N-3/D-04 一致）；pr-005 的 README 更新对应 architecture §18 R-14，非新增功能。

### S4 PR 间文件范围零重叠、归属唯一 — **pass**

逐文件比对（脚本抽取 + 人工复核）：

| PR | 文件范围 |
|---|---|
| pr-001 | oamp/src/config.js、oamp/src/persist.js、oamp/.gitignore、oamp/test/config-file.test.js、oamp/test/persist.test.js |
| pr-002 | oamp/src/transport.js、oamp/test/transport.test.js |
| pr-003 | oamp/src/acp-client.js、oamp/src/context-pool.js、oamp/src/agent.js、oamp/test/context-pool.test.js |
| pr-004 | oamp/src/web.js、oamp/web/app.js、oamp/web/index.html、oamp/web/style.css、oamp/test/web.test.js |
| pr-005 | oamp/src/router.js、oamp/src/registry.js、oamp/test/acp-daemon.test.js、oamp/README.md |

- 重复计数 = 0（19 个文件条目两两互不重叠）；`config/web/agent/router/registry.js`、`oamp/web/*`、5 个新测试文件各有唯一属主。
- **无遗漏**：`architecture.md` §9.3 的 新增（persist/transport/context-pool/acp-client + 5 个新测试）、改造（web.js/agent.js/config.js/.gitignore/web 三文件）、删除（router.js 三 case/registry.js 会话面）之并集恰好等于上表文件集；`build`/`scripts`/`package.json`/其余 src 模块与 10 个既有测试文件未被任何 PR 列入范围（与 §9.3"不动（回归边界）"一致）。
- 与 architecture §16.1 的差异属**消除重叠**（§16.1 自身把 `src/web.js`/`web/app.js` 同时列进 PR-2 与 PR-4），见偏差 #1。

### S5 depends_on 证据可核实 — **pass**

逐条在**当前工作区**读文件核实（行号取 `cat -n` 实测）：

| PR 声明的依赖与证据 | 实测结果 | 判定 |
|---|---|---|
| pr-003→pr-001：`oamp/src/agent.js:8` 为 `import { loadConfig } from './config.js'` | agent.js:8 逐字一致 | ✓ |
| 同条：`oamp/src/config.js:30-44` 返回值无 `defaultModel`/`contextMax` | config.js 共 46 行；:30 `export function loadConfig`，:36-42 只返回 socketPath/heartbeat*/hbLogWindowMs/reconnect/reconnectMaxMs；无 defaultModel/contextMax（亦无 dbPath） | ✓ |
| pr-004→pr-001：`oamp/src/web.js:18` 为 `import { loadConfig }` | 一致 | ✓ |
| 同条：`oamp/src/web.js:174/180/209` 走 Router 会话 RPC | :174 `router.chat_list`、:180 `router.chat_get`、:209 `router.chat_message`（`queryOnce(config.socketPath, …)`） | ✓ |
| pr-004→pr-002：`oamp/src/transport.js` 不存在（`src/` 现为 agent/cli/config/log/node-client/registry/router/rpc/status/task/web.js） | 目录实测 11 个 js，恰为该列表，无 transport.js | ✓ |
| 同条：架构 §5.1 定义 `createSseTransport()` 的 handle/publish/closeAll | §5.1 代码块含 `kind:'sse'`、`handle(req,res,{chatId})`、`publish(chatId,event)`、`closeAll()` | ✓ |
| pr-004→pr-003：`oamp/src/agent.js:35-78` 只有 `executor:'omp'`(:50) 与 command(:67)，`omp-daemon` 落入 command 分支被拒 | parseTaskBody :35 起；:50 `if (body.executor === 'omp')`；:67 `if (typeof body.command !== 'string' …)` → :68 reason `缺少非空 command（或使用 executor:"omp" 提交 prompt）`；函数无 executor 白名单，`omp-daemon` 必落 :67 被拒（引述子串成立） | ✓ |
| pr-003 参考资料：agent.js:22 `OMP_BIN = () => process.env.OAMP_OMP_BIN \|\| 'omp'`；:35 parseTaskBody；:211 runTask 分派；:309 createTaskDeliverHandler；:384 SIGINT | 五处行号与符号逐一对上（:22/:35/:211/:309/:384） | ✓ |
| pr-001 参考资料：`config.js:9` PKG_ROOT、`:18` readPositiveInt、`:30` loadConfig | 逐行一致 | ✓ |
| pr-005→pr-004：`oamp/src/router.js:392/425/435` 为三个待删 case | :392 `case 'router.chat_message'`、:425 `case 'router.chat_get'`、:435 `case 'router.chat_list'` | ✓ |
| pr-005：`oamp/src/router.js:23` `VALID_TYPES` 已含 `notice` | :23 `new Set(['task.request','task.update','task.result','notice'])` | ✓ |
| pr-005：`router.js:290-297` createTask 的 `messageId` | :290-297 `registry.createTask({… messageId: m.message_id …})`（:296） | ✓ |
| pr-005：`registry.js:40/42` 两表、`:198` 写入、`:356-360` 导出 | :40 `const chats = new Map()`、:42 `const tasksByMessage = new Map()`、:198 `if (messageId) tasksByMessage.set(...)`、:356-360 五个会话函数导出 | ✓ |
| pr-005：`registry.js:265-320（5 个会话函数）` | :265 createChat / :279 appendChatMessage / :288 getChat / :293 chatDetail / :310 listChats（函数体延至 :335）——符号全部存在且可定位，但行区间未覆盖第 5 个函数尾部 | ✓（精确度见偏差 #8） |
| pr-005：`oamp/README.md:115` 含"会话与消息存于 Router 内存" | :115 原文含"会话与消息存于 Router 内存（重启即清空）" | ✓ |
| pr-003/pr-005 测试范式：`test/omp-executor.test.js` fake omp 注入 | :14-18 `makeFakeOmp()` 写 `mkdtemp` 临时 bin，经 `OAMP_OMP_BIN` 注入（:94/:148）——`OAMP_OMP_BIN` 注入面真实存在（agent.js:22 消费） | ✓ |
| pr-002/pr-003/pr-004 的参考资料锚点（§5.1/§5.2/§5.4/§6.1~§6.6/§7.1~§7.4/§9.1/§12/§14/§17） | 各节均存在且内容与该 PR 断言一致（抽查 §5.2 帧格式/`retry:1000`/15s 心跳、§6.2 队列上限 8、§6.5 `context_crashed`、§7.3 `model_unavailable`） | ✓ |
| pr-001 依赖"web/agent 入口既有 try/catch 退出码 1" | web.js:120-126 `loadConfig` 失败 `return 1`；agent.js:330-335 失败 `return 1` | ✓ |

**无"顺序偏好伪依赖"**：唯一两条非空依赖链（pr-003→pr-001、pr-004→{001,002,003}、pr-005→004）全部有接口/符号级证据，且方向均为"消费方依赖产出方"，无一条以文档叙述顺序为依据。

### S6 依赖图无环 + 首批可并行集 — **pass**

抽取结果（去重后）：

```
pr-001 → （无）
pr-002 → （无）
pr-003 → pr-001
pr-004 → pr-001, pr-002, pr-003
pr-005 → pr-004, pr-001, pr-002, pr-003
```

- **无环**：拓扑序 001 → 002 → 003 → 004 → 005 成立（003 的唯一入边来自 001；004 的三条入边均为更早层；005 的四条入边含 004/003/002/001）。
- **首批可并行集 = {pr-001, pr-002}** —— 与 S6 期望一致。解锁推进：pr-001 合并 → pr-003 解锁；pr-002 合并**不单独解锁任何 PR**（pr-004 仍需 pr-003），此时按 workflow「没有已解锁且排队中的 PR 时，释放出的槛位保持空置」不得提前派发 pr-004；pr-003 合并 → pr-004 解锁；pr-004 合并 → pr-005 解锁。
- 首批并发度 2 < 起始并发数默认 3：符合规则（无第 3 个已解锁 PR，槛位空置，不放宽解锁条件）。
- 未发现真实并发的 `depends_on` 缺失（对照 S5 逐条证据，004/005 的依赖集已闭合到其全部跨 PR 消费面：dbPath/persist、transport、daemon 分支与 notice、SSE 端点）。

### S7 验收标准可独立判断 — **pass**

逐 PR 检查"是否存在只有下游 PR 合并后才能判定的验收条目"：

- pr-001：全部断言落在 `test/config-file.test.js`/`test/persist.test.js`（临时库路径），自足；无跨 PR 断言。
- pr-002：全部断言落在 `test/transport.test.js`（`http` 起服务 + 裸 socket 读流），自足。
- pr-003：全部断言经 fake ACP（`OAMP_OMP_BIN`）在 agent 侧判定，自足；"模型分辨率链含 `config.defaultModel`"需 pr-001——属**已声明且已合并的上游**，符合规则。
- pr-004：E-4/SSE/payload 判定所需的上游（transport、daemon 分支）分别落在 pr-002/pr-003，均在其 `depends_on` 中；无一条依赖 pr-005。
- pr-005：E-1~E-5、两形态不回归、`npm test` 全量——所需上游为 pr-001~004，均已声明；无一条依赖尚不存在的 PR。
- 断言方式明确性抽查：pr-001/pr-002/pr-003 每条的判定手段（断言文件、fake 注入、命令）均写出；pr-005 的 E-1（`meta.context_id`/`pid` 相等）、E-5（恰 2 条 + `direction∈{in,out}`）可直接构造。
- 残留弱点（不影响本项判定，见偏差 #4/#5）：pr-004 的 F06 web 落点无显式断言；pr-001 未显式断言 `meta` 字段落盘（而 pr-005 的 E-1 依赖它）；pr-004 前端条目"可用"为定性表述（判定手段未写死）。

### S8 与 architecture §16 边界一致性（含两处重切） — **pass**

**（1）PR 切分与 §16.1 的差异**：架构给 4 个 PR，计划给 5 个——pr-002 = §16.1 PR-2 的 `transport.js`+`transport.test.js` 独立；pr-004 = §16.1 PR-2 的 web/前端部分 + §16.1 PR-4 的 `web.test.js` 重写与 `web.js` 接线部分；pr-005 = §16.1 PR-4 的删除面 + `acp-daemon.test.js` + README。**§16.1 的每一条"验收（可执行断言）"意图都能在新计划里找到唯一落点**（PR-1 验收 → pr-001 全含；PR-2 的 SSE/断线/E-4/关闭 409/列表含已关闭 → pr-002 或 pr-004；PR-3 验收 → pr-003 全含；PR-4 的 E-1~E-5/两形态/10 文件全绿/`npm test` → pr-005 全含），无验收意图丢失。

**（2）重切一：web 归 pr-004 单属** —— 与 §16.1 的差异是**修复架构文档自身重叠**：§16.1 PR-2 与 PR-4 都列了 `src/web.js` 与 `web/app.js`（PR-2 还列 `web/{index.html,style.css}`），直接照抄会产生 S4 无重叠失败。计划的归属（web.js + web/* 三文件 + web.test.js → pr-004；transport.js + transport.test.js → pr-002）使每个文件恰有唯一属主，且 `depends_on` 让 pr-004 在所有上游合并后才开工，与 §9.3/§16.4 的边界（不新增 WebSocket、不做模型清单 UI 等）一致。**判定：该重切成立，且优于文档原文**（文档需同步，见偏差 #1）。

**（3）重切二：pr-003 依赖 pr-001** —— 与 §16.2 "PR-1 与 PR-3 **互相独立**（不同进程、不同文件），可并行"文字冲突；计划给出的证据经核实成立：`agent.js:8` 已 import `loadConfig`，而现行 `config.js:30-44` 无 `defaultModel`/`contextMax`，pr-003 的 daemon 分支与"池上限"取值（§7.1 链、§6.3 `context.max`）确实消费 pr-001 新增键；pr-003 验收第 7 条要断言含 `config.defaultModel` 的分辨率链，无 pr-001 则该条不可判定。按 §验证目标"依赖正确性验证"，此依赖有代码级证据，**合规**；代价是首批并行集从 §16.2 设想的 {PR-1, PR-3} 变为 {pr-001, pr-002}，仍满足 S6 期望且不引入串行浪费（pr-002 本就与 pr-001 并行）。**判定：该依赖成立，architecture §16.2 文字需按证据修订**（见偏差 #2）。

**（4）其余 §16 边界逐条对照**：§16.3 开工前置（config 加载器 + 单测、`.gitignore` 加 `data/`、迭代分支）分别落在 pr-001（文件范围含 `config.js`/`test/config-file.test.js`/`.gitignore`）与已存在的分支（`git branch --list 'iteration/*'` 实测含 `iteration/0011-chat-context-protocol`，且为当前分支）；§16.4 不在范围清单无一项被夹带；§17 测试分层（单元 `persist`/`config`/`transport`、集成 fake ACP `context-pool`、端到端 `acp-daemon`）与计划的测试文件一一对应，且"一律不写真实 `oamp/data/sql.db`"在 pr-001 验收末条明文。

### W1 PR 粒度判断框架 — **pass**

- **逻辑原子性**：pr-002（仅传输抽象）、pr-003（仅 agent 侧上下文能力）、pr-004（仅 web HTTP 契约两端）、pr-005（仅收尾清理+端到端）各自"只做一件可描述的事"；pr-001 按 §16.1 PR-1 将"配置面 + 持久层"合为一件（两者以 `dbPath` 为接口、同属数据地基，回滚其一另一无意义），符合架构既定边界。
- **可审查性**：无跨模块心智切换——pr-004 虽含 5 个文件，但全部是"一次 HTTP 契约的服务端 + 客户端 + 该契约的测试"，属同一心智模型；pr-005 的"删除 + 端到端 + 文档"由同一解锁集（pr-004 合并后）绑定，理由已在文件内显式说明。
- **独立性**：见 S7。

### W2 依赖正确性验证汇总 — **pass**

四问四答：① 所有 `depends_on` 均有代码级证据（S5，0 条伪依赖）；② 依赖图无环（S6）；③ PR 间文件范围无重叠（S4，重复计数 0）；④ 无遗漏功能点（S3，F01~F08 全覆盖；§9.3 新增/改造/删除文件集全部有属主）。**通过**。

---

## 汇总

- pass: 10 项（S1~S8 + W1/W2）
- fail: 0 项（需返工）
- partial: 0 项（部分需返工）
- blocked: 0 项（缺信息无法判定）

## 偏差记录

> 本区块记录"文档规格 ↔ 本计划"的不一致。均不影响本次逐项判定（阶段 4 推进条件四条全部满足），但需要在阶段 5 开工前处理或不晚于阶段 6 同步。

| # | 规格/文档描述 | 实现（本计划）实际 | 建议处理 |
|---|---|---|---|
| 1 | `architecture.md` §16.1 给出 **4 个 PR**，且 **PR-2 与 PR-4 都列 `src/web.js`（PR-2 另列 `web/{app.js,index.html,style.css}`，PR-4 另列 `web/app.js`）**，存在文件范围重叠 | 计划 5 个 PR；`web.js` + `web/*` + `web.test.js` 单属 pr-004，`transport.js` 单属 pr-002（重叠消除） | 按计划更新 `architecture.md` §16.1 的 PR 划分与文件归属（保留"验收意图逐条可追溯"，S8(1) 已核）——否则阶段 6 复验 architecture 会报"文档 PR 划分与 `prs/` 不符" |
| 2 | `architecture.md` §16.2："PR-1 与 PR-3 **互相独立**（不同进程、不同文件），可并行" | pr-003 `depends_on` = pr-001（证据：`agent.js:8` 消费 `loadConfig`；现行 `config.js:30-44` 无 `defaultModel`/`contextMax`；pr-003 验收第 7 条要断言含 `config.defaultModel` 的链）；首批并行集为 {pr-001, pr-002} | 按证据修订 §16.2：改为"PR-1 ∥ PR-2；PR-3 依赖 PR-1（消费其新增配置键）" |
| 3 | 上下文提示的 kind 命名在架构内不一致：§5.2 事件表与 §6.3 写 `context_released`（SSE 侧释放提示），§6.4/§10.2/AR-12 写 `context_release`（web→agent 控制消息）；F05 卡"架构维度"两种写法混用 | pr-003 只断言 `context_reset`（崩溃/淘汰），pr-004 只断言 web→agent 的 `notice{kind:'context_release'}`；**SSE 侧"释放提示"事件的 kind 无任何 PR 断言** | 阶段 5 开工前在 `architecture.md` §5.2/§6.3 显式区分两个名字（建议：控制消息 `context_release`；SSE 事件 `context_released`），并同步 F05 卡；pr-004 的 SSE 事件断言按裁定值写死 |
| 4 | pr-004 "涉及功能点"含 **F06**（模型输入框、`model` 透传、每轮审计展示） | pr-004 的 10 条验收标准中无任何一条断言 `model` 进入 payload、或 out 记录/界面展示该轮实际模型；仅有"模型输入框…可用"（前端可用性） | 阶段 5 为 pr-004 增补 1 条可判定断言（如"请求带 `model` 时 payload 含该值且 out 记录/详情可读到生效模型"），否则 F06-2/F06-3 的 web 落点无验收 |
| 5 | pr-005 的 E-1 断言依赖 out 记录 `meta.context_id`/`pid`；`architecture.md` §4.1 的 `messages.meta` 字段由 pr-001 交付 | pr-001 的验收标准未包含 `meta`（`task_id`/`context_id`/`pid`）落盘/读回的断言 | 阶段 5 在 pr-001 的任务清单中显式列出 schema/meta 断言（或在 pr-005 明确"若 pr-001 未落 meta 则 e2e 无法成立"的前置检查） |
| 6 | `architecture.md` §9.3 删除清单 + D-12 "clean cutover 不留死代码"；§9.3 又列"Router 任务表不动" | pr-005 删除 `registry.tasksByMessage` 后，`registry.createTask` 的 `messageId` 参数（`registry.js:182/190`）与 `router.js:296` 的实参成为**死参数/死字段**（唯一消费方即 `tasksByMessage`），两条 PR 验收均未声明处理方式 | 阶段 5 在 pr-005 文件中显式声明"按 §9.3 边界保留 `message_id` 字段"的理由，或一并清理该参数与调用实参（清理会触碰 Router 任务表，需先确认边界） |
| 7 | `workflow-pb` v0.7.0「历史记录协议」："每次派发执行角色、收到执行角色报告、做出阶段推进核查决策时，同时追加 `history.md` 记录"；`status.md` 头部 `history: 开启` | `history.md` 最新记录停在阶段 1（"阶段 1 ✅ → 进入阶段 2"，05:00 条目），**阶段 2/3/4 的派发、报告、阶段推进决策记录缺失**；`status.md` 亦未初始化 `## 并发配置（阶段 5）` 区块（workflow §启动工作流 5：进入阶段 4→5 时、阶段 5 第一次并发派发之前必须初始化五字段），PR 实现子状态表仍为占位 | 阶段 5 首次派发**前**补齐 `## 并发配置（阶段 5）`（起始并发数/硬上限/当前有效上限/累计槛位释放次数/已派发总数）与 PR 实现子状态表；同时补记阶段 2~4 的 history 条目（否则阶段 6 按 §403-421 强制核查"并发调度真实执行证据"时，前置证据链缺失） |
| 8 | pr-005 参考资料写 `oamp/src/registry.js:265-320（5 个会话函数）` | 5 个函数实际覆盖 :265-:335（`listChats` 起于 :310、体终于 :335），引用区间未覆盖第 5 个函数尾部 | 引用精确度问题，不阻塞；建议改为 `:265-335` |

## 下一迭代候选

- **fake ACP 注入载体的归属未声明**：pr-003（`test/context-pool.test.js`）、pr-004（E-4 需要"可控节奏的增量"）、pr-005（`test/acp-daemon.test.js`）三处都要一个 fake ACP 脚本，但无任一 PR 声明共享 fixture 文件（如 `test/helpers/fake-acp.js`）；现有仓库范式是各测试文件自建临时脚本（`test/omp-executor.test.js:14-18` 的 `makeFakeOmp()` 写入 `mkdtemp`），因此不违反文件归属，但会造成 3 份重复脚本（约 50 行/份）。建议阶段 5 由主 agent 明确：自建（沿用范式，零归属冲突）或新增共享 helper（需指定属主 PR）。
- **pr-005 的 `depends_on` 存在传递冗余**：列出 pr-001/002/003 已被 pr-004 的依赖蕴含。不影响调度正确性（只需全部已合并），但会让依赖图噪声增加；下轮可约定"只列直接依赖 + 传递依赖不重复"。
- **定性验收措辞**：pr-004 前端条目（"EventSource 订阅生效""系统提示条可用"）、pr-005 README 条目（"不再含失效描述"）判定手段未写死断言方式；建议阶段 5 的任务清单把这类条目落成具体断言（如 grep 断言 + 事件断言），避免验收争议。
- **`oamp/test/` 计数口径已在架构/PRD 修正**（11 个，除 `web.test.js` 外 10 个）——本次实测一致；下一轮如新增 e2e 文件需同步该口径的所有出现处（PRD 疑问 8 已记录该风险）。
- **迭代目录中无 `deferred-demand-changes.md`**：本次验证无可透传的搭置内容（该文件不存在）。

## 结论

**PASS**（所有 10 项判定均为 pass，无 fail、无 partial、无 blocked）。

- 阶段 4 推进条件四条（每个 PR 文件满足格式规范 / 每个功能点被某 PR 引用 / PR 间文件范围无重叠 / 依赖图无环）**全部满足**，主 agent 可继续推进流程。
- **但阶段 5 首次并发派发前必须先处理偏差 #7**：`status.md` 的 `## 并发配置（阶段 5）` 区块与 PR 实现子状态表尚未初始化，`history.md` 的阶段 2~4 记录缺失。这不是阶段 4 的返工项（不阻塞阶段推进判定），而是阶段 5 入口的前置条件与阶段 6 强制核查项的证据链缺口。
- 偏差 #1~#6、#8 按"偏差被记录即满足交付条件"处理，建议在阶段 5 开工前随手更新 `architecture.md` §16.1/§16.2/§5.2（三处文字同步）与 pr-004/pr-001/pr-005 的验收补充，成本低、可避免阶段 6 复验时被判定为文档漂移。
