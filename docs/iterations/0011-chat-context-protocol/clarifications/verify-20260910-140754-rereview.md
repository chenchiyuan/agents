# verify-20260910-140754-rereview — 0011-chat-context-protocol 阶段 5 PR-003 复审（verify-20260910-140754 的 FAIL 项修复）

**复审对象**：worktree `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-003-context/` commit `5970a07`（fix(0011/pr-003): model 审计面取真实 ACP currentValue（configOptions 数组形态）+ 禁请求回显冒充（含失败轮）+ fake 对齐真实形态与审计锁定用例）；基线 = 被判定 FAIL 的 `e8ca50b`。

**验证者身份（反射）**：沿用初次验证的「ACP / stdio JSON-RPC 协议实现审查者 + 常驻子进程生命周期审查者」。复审焦点收窄为：修复是否**在真实 `omp acp` 上**消除了"审计值 = 请求回显"，而不是仅重排了代码。

- **复审范围**：初次报告的 1 项 fail 子项（V6 内「成功轮次的 `model` 取自 ACP `currentValue`」）+ 修复引入面（`readCurrentModel` / `agent.js` 两处 model 来源 / `context-pool.get model` / 测试 fake 形态 / 新增用例 17）+ 回归面（是否引入新 fail）。
- **验证标准来源**：`prs/pr-003-context-pool-acp-daemon.md` 验收标准 6（含「成功轮次的 `model` 取自 ACP `currentValue`（实际生效值，F06-3 审计面）」）+ `prd/F06` 卡（「由 agent 从 ACP session 的 `currentValue` 回读后上报（不是用户输入的回显）」）+ `architecture.md` §7.3/§7.4 + 主 agent 复审委托列出的 4 项修复内容与 3 项独立验证要求。
- **验证日期**：2026-09-10。
- **独立性声明**：未接收执行过程上下文（只接收修复内容清单与要求核实的点，未接收任何"为什么这么改"的解释）。被验证文件全程只读，复审前后 worktree `git status --porcelain` 为空。真实 omp 探针 `/tmp/v11/rereview-real.mjs` 自建，**不注入 `OAMP_OMP_BIN`**（走真实 `omp 18.0.11`）；fake 侧仍用我自己的 `fake-acp.cjs`（已改为真实数组形态 + sticky 模式），未 import 产出物的测试代码或其 fake。

---

## 逐项判定

### R1 修复实现审查（无回显兜底残留 + 形态兼容 + 失败轮来源）—— **pass**

- `oamp/src/acp-client.js:39-45`（`readCurrentModel`）现为数组优先 + 对象兼容：
  ```js
  const options = result && result.configOptions;
  const value = Array.isArray(options)
    ? options.find((option) => option && option.id === 'model')?.currentValue
    : options && options.model && options.model.currentValue;
  return typeof value === 'string' && value !== '' ? value : null;
  ```
  与真实帧形态（`{"id":"model","category":"model","currentValue":…}`，模块注释也已写明实测来源）一致；`id === 'model'` 与真实 `session/new` / `set_config_option` 两处响应均匹配（我在 R4 用同一选择器独立读出真值，两者一致）。
- `oamp/src/agent.js:294`：成功轮 `model: result.model` —— `|| model` 兜底已移除，注释明示"读不到即 null，不用请求参数冒充"。
- `oamp/src/agent.js:311`：失败轮 `model: session.model` —— 不再回显请求值，不可用模型只出现在 `text`（`:310` 的「模型不可用：<model>」）。
- `oamp/src/context-pool.js:118-120`：新增 `get model()` → `this.client.currentModel`；未建实例/未回读时为 `null`，与 agent 侧语义一致。
- 残留检查：`grep -rnF "|| model" oamp/src/` → **NONE**（无任何 `result.model || ...` 形式的回显兜底；初次验证定位的 `agent.js:294` 唯一处已消除）。
- 结论：三处来源全部指向 ACP 回读值，读不到时为 `null`（不再以请求值顶替）——与 F06「不是用户输入的回显」一致。

### R2 测试（新增计数 + 全量计数 + flake 归属）—— **pass**

- `node --test test/context-pool.test.js` → **`# tests 16 / # pass 16 / # fail 0`**（用例数由 15 增至 16，新增「§7.4：`task.result.model` 取自 ACP currentValue」）；复审期间共 5 次运行，均 16/16、0 not ok。
- `npm test`（`node --test test/*.test.js`）→ **`# tests 128 / # pass 128 / # fail 0`**，与委托期望 128/128 一致；复审期间共 9 次全量运行，其中 7 次 128/128、2 次 127/128。
- **flaky 归属（不构成本次 fail）**：2 次 127/128 中，1 次逐行定位到
  `not ok 126 - Web：发送消息（消息即命令）→ 会话入库 → 任务完成 → 明细回流可查` / `error: 'waitFor 超时（8000ms）: 任务完成并回流'`（`test/helpers/harness.js:39`）；另 1 次当时只采集了计数、未逐行定位（如实记录）。归属证据：
  1. `oamp/test/web.test.js` 本 PR **未修改**（`git diff --name-only 86d7a62..HEAD -- oamp/test/` → 仅 `context-pool.test.js`）；
  2. 该测试**不涉及 daemon 路径**：`test/web.test.js` 内 `omp-daemon` 出现 0 次；其驱动的 `src/web.js` 本 PR 未改，且只发 `{ executor: 'omp' }`（`src/web.js:206`）——即经 `runOmpTask`，该函数在 `86d7a62` 与 HEAD 上逐字节相同；
  3. 同一失败断言在**不含任何 PR 改动**的基线提交上已被复现并记录（初次验证链：`verify-20260910-133635.md` V1 附注，worktree 3/43、基线 1/50，同一 `waitFor 超时（8000ms）: 任务完成并回流`）；
  4. 单文件复跑 `test/web.test.js` 10 次 → 10 次 4/4 通过（非稳定失败，属全量并发下的计时 flake）。
- 结论：新增用例与既有 112 用例均无失败；出现的唯一失败为既有 `web.test.js` E2E 计时 flake，与本次修复无因果关系（且 `web.test.js` 已由 F08-3 裁定为 pr-004 等价重写对象）。

### R3 独立复现：自建 fake、双形态 + sticky 审计锁定 —— **pass**

- 池层（我自己的 `fake-acp.cjs`，默认**数组形态**=真实形态；`FAKE_CONFIG_SHAPE=object` 切对象形态；`FAKE_STICKY_MODEL=1` 模拟"接受但未生效"）：**73/73 pass、0 fail**（初次验证 67 项 + 复审新增 6 项）。关键断言：
  - `(复审/array) 首轮 model = ACP currentValue :: alpha/model-a`；
  - `(复审/array) ACP 接受但未生效 → model 必须为 ACP 实报值 ≠ 请求值 :: alpha/model-a`（请求 `beta/model-b`）；
  - `(复审/object) …` 同两条亦 pass → 对象形态兼容路径未被破坏；
  - 审计差异不导致实例重建/上下文丢失（`context_id`/`pid` 均不变）。
- agent 协议面（真实 Router + 真实 agent 进程 + 自建 fake + sticky）：**40/40 pass、0 fail**（初次 36 + 复审新增 4）。关键断言：`task.result.model === 'alpha/model-a'` 而请求为 `beta/model-b`，`≠ 请求值`，且该轮 `state:'completed'`、文本仍命中 `42`、`context_id`/`pid` 与首轮相同。
- 与我自己的独立实现交叉验证：上述断言均以我自建 fake 的 `currentValue` 为期望值，未使用产出物测试的任何期望常量。

### R4 真实 omp 复现（本次修复的核心判据）—— **pass**

探针 `/tmp/v11/rereview-real.mjs`，`OAMP_OMP_BIN` 已显式删除（断言 `undefined`），真实 `omp 18.0.11`，真实 Router + 真实 agent 子进程。**14/14 pass、0 fail**。关键证据：

1. **独立读 ACP 真值（不经过被验证代码）**：我用自己的 raw ACP 会话读 `session/new` 的数组项
   `ACP 真值（--model deepseek/deepseek-v4-flash）→ "deepseek/deepseek-v4-flash"`。
2. **成功轮 model = ACP 真值**：首轮 `{"model":"deepseek/deepseek-v4-flash"}`，与上式逐字一致；第二轮同值。
3. **E-1 同 chat 累积仍成立**：第二轮 `"42"`，两轮 `context_id`/`pid` 相等（`ctx-84556-1`）。
4. **A≠B 核心判据（真实 omp，失败轮）**：请求 `ghost/model-x` 而会话实际生效为 `deepseek/deepseek-v4-flash` →
   `{"state":"failed","error":"model_unavailable","text":"模型不可用：ghost/model-x","model":"deepseek/deepseek-v4-flash",…}`
   → `model` = ACP 实报值，**≠ 请求值**；断言 `model !== 'ghost/model-x'` 通过；不可用模型只出现在 `text`。
5. **与修复前同一构造的 A/B 对照**（同一本机、同一探针形状）：
   | 版本 | 同构造（新 chat/既有 chat + 请求 `ghost/model-x`）的 `task.result` |
   |---|---|
   | 修复前 `e8ca50b`（初次验证记录） | `{"state":"failed","error":"model_unavailable","text":"模型不可用：ghost/model-x","model":"ghost/model-x"}` ← 请求回显 |
   | 修复后 `5970a07` | `{"state":"failed","error":"model_unavailable","text":"模型不可用：ghost/model-x","model":"deepseek/deepseek-v4-flash"}` ← ACP 实报 |
   即修复**在真实协议上产生了可观察的行为变化**，非等价重写。
6. **新 chat 首轮未知模型仍被 model_unavailable 判定**（不是 `context_crashed`）：`model` = ACP 实报值（机器既有模型 `deepseek/deepseek-v4-flash`），非 ghost。
7. **所有轮次 `model` 均非 null**：`["deepseek/deepseek-v4-flash","deepseek/deepseek-v4-flash","deepseek/deepseek-v4-flash","deepseek/deepseek-v4-flash","openai/gpt-5.6-luna"]` → 回读链路对真实 omp 确实生效（初次验证时此处为 `null`）。
8. **判据可达边界（如实记录）**：真实 omp 对模型 id 做**精确匹配、无别名/规范化**——我用 raw ACP 逐项试了 6 个取值，被接受者 `currentValue` 恒等于请求值，未接受者返回 `Unknown ACP model`。因此"成功轮 A≠B（请求 A、生效 B）"在真实 omp 上**不可构造**；该语义由失败轮的真实 A≠B（第 4/5 条）+ sticky fake（R3）共同锁定。初次报告的 fail 子项由这两条证据判为已修复。

### R5 边界与卫生（变更范围 / 无新依赖 / 工作区干净）—— **pass**

- `git diff --name-status 86d7a62..HEAD` → 仍为 5 项（`A` tasks.md、`A` acp-client.js、`M` agent.js、`A` context-pool.js、`A` context-pool.test.js）；本次修复 commit `5970a07` 触及 5 个文件中的 4 个（含 tasks.md 修订记录与 T2-4/T5-2/T6-17），**未越出 PR-003 文件范围**，未触碰 `web.js`/`router.js`/`registry.js`/`web/*`。
- `git diff 86d7a62..HEAD -- oamp/package.json` → 无输出（零新依赖）；worktree 无 `node_modules`。
- `git status --porcelain` → 空（我未修改任何被验证文件）。
- 既有 112 用例文件未修改（同 R2 证据 1）。

---

## 汇总

- 复审判定条目 5 条：**pass 5**、fail 0、partial 0、blocked 0。
- 初次报告的 1 项 fail 子项（V6「成功轮次 `model` 取自 ACP `currentValue`」）：**已修复并通过真实 omp 复验**（R4 第 4/5/7 条）。
- 独立复现规模：池层 73 项断言（自建 fake，双形态 + sticky）全绿；agent 协议面 40 项断言全绿；真实 `omp` 14 项断言全绿。初次验证的其余 7 条判定条目（V1~V5、V7、V8）在本次复审的回归面（R2/R5）未出现反向证据。

## 偏差记录

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| 初次报告的 D1~D3（`configOptions` 对象形态的规格假设、队列上限 8 的口径、LRU 触碰点） | 代码侧已按真实数组形态修正并加了注释；`architecture.md` §6.2/§6.3/§6.6/§7.3 与 `prd/F05`/`F06` 卡文字仍为旧表述（本次修复只改 `prs/pr-003-tasks.md`） | 由 doc 同步责任方在阶段 6 前修订（本 PR 文件范围内无 architecture/prd 权限） |
| `task.result.model` 在"ACP 未回读成功"时为 `null`（`agent.js:294` 不再回显顶替） | 对真实 omp 不会发生（R4 第 7 条）；但若未来 omp 改字段名/形态，该字段会**静默**为 `null`，前端元信息行会空 | 开新迭代时评估：回读失败记一条事件（如 `MODEL_UNREADABLE`）以便可观测，而非只在 `TASK_RESULT` 里体现 `model=null` |
| `web.test.js` 间歇 flake（`waitFor 超时（8000ms）: 任务完成并回流`） | 全量并发下约 1/4~1/5 次运行触发；单文件复跑 10/10 通过；基线（无本 PR）亦可复现 | 归口 pr-004（该文件按 F08-3 等价重写）：加固等待窗口与失败诊断 |

## 下一迭代候选

- **回读失败的可观测性**：`readCurrentModel` 返回 `null` 时建议留一条事件（见偏差记录第 2 行），避免审计字段静默为空。
- **`web.test.js` flake（既有）**：pr-004 重写时把 8s 等待窗口改为带 `task` 最新状态的诊断式等待。
- **首次 SIGINT 之外的第二条退出路径**（二次 SIGINT → 130）仍未实测，复审未涉及；如需要可作 e2e 增补。
- **默认模型复测门禁（R-9）**：复审中默认链轮次 `openai/gpt-5.6-luna` 成功返回（本次 11.4~15s 量级，未挂起），上线前仍需按 §7.5 两条路径对照复测。

## 结论

**PASS**（复审 5 条判定全部 pass，0 fail / 0 partial / 0 blocked）。

初次报告的 fail 子项已修复并**在真实 `omp acp` 上复验**：`readCurrentModel` 按真实数组形态读取（对象形态兼容），成功轮 `model: result.model`、失败轮 `model: session.model`，`agent.js` 内不再有任何请求回显兜底（`grep -F "|| model"` 为空）；真实链路上 `task.result.model` 由初次验证的 `null`/`ghost/model-x`（回显）变为 ACP 实报值（`deepseek/deepseek-v4-flash`），同 chat 两轮 E-1 记忆与实例标识保持、未知模型仍 `model_unavailable` 且不回退；测试 128/128（单个 flaky 失败已归因为既有 `web.test.js` E2E 计时问题，与 PR-003 路径无交集）；变更范围与零新依赖未被本次修复破坏。

注：本报告只作结论与证据记录，未修改任何被验证文件；偏差记录不阻塞结论。
