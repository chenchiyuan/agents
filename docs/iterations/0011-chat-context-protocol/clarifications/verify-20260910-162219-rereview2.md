# 独立终审报告（第三轮）：PR-007 补丁 D-1/D-2 修复后的验收

**验证者身份**：分布式消息投递/幂等语义审查者（第三轮兼任"因果隔离"角色——用变异注入证明"低频续查"与"双故障可恢复"之间的因果链）
**产出物（worktree 工作区改动）**：
- `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-007-reconcile/oamp/src/web.js`（md5 `a0b3f14005c093624a9261b6b8b4bce2`）
- `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-007-reconcile/oamp/test/web.test.js`（md5 `677375f5b472826ec8c0db30baaa4224`）
  （两者 md5 与我本轮验证开始时的快照**完全一致**，验证期间产出物零改动）
**验证输入**：主 agent 给出的 3 条修复要点 + 指定重做的 V6'/V4'/V7/V2/V3/V5（**独立核实，不采信修复说明**）
**验证日期**：2026-09-10
**前序报告**：`verify-20260910-162219.md`（FAIL：上限分支删除投递凭据）、`verify-20260910-162219-rereview.md`（PASS；偏差 D-1 残余缺口、D-2 孤儿登记）
**验证方式**：只读产出物 + 运行命令 + 自建隔离拓扑实跑 + 对修复代码做**变异注入**（注入只发生在临时副本，被验证文件全程零修改）

---

## 逐项判定

### R1 快速预算用尽 → 低频续查（不放弃、不删登记）：**pass**

- 实现（`src/web.js:256-261`）：`attempts === RECONCILE_MAX_ATTEMPTS` 时置 `current.slow = true` + 写**一条** warn（`转入低频续查`），随后仍然 `scheduleReconcile`；`scheduleReconcile`（`web.js:266-271`）按 `entry.slow ? reconcileSlowMs : reconcileIntervalMs` 定时 → 轮询链路**不再终止**（除终态 / 软 TTL / shutdown）。常量：快速 5000ms、快速 6 次、低频 30000ms（`web.js:35-38`）。
- **删除路径全量审计**（`grep -n "tasks.delete\|tasks.clear" src/web.js`）：

  | 行 | 位置 | 判定 |
  |---|---|---|
  | 235 | `reconcileTask` 软 TTL 分支 | **预期内**（D-2 新增；见 R2） |
  | 250 | `reconcileTask` 终态分支 | 合法：`current.landed = true` 之后 |
  | 301 | `handleDeliver` 的 `task.result` 分支 | 合法：`entry.landed = true` 之后 |
  | 491 | 派发失败 `catch` | **既有语义**：与 HEAD 版本逐字节同形，该轮已落 `dispatch_failed` out，无"无 out 悬挂" |
  | 530 | SIGINT | 合法：紧跟 `clearTimeout(entry.timer)` 循环之后的 `tasks.clear()` |

  → 删除路径确为"**landed（2 处）/ TTL（1 处）/ SIGINT（1 处）+ 既有派发失败**"，**无"达上限即删"路径**；与主 agent 声明的三处判定一致。
- warn 恰一条：`attempts` 每轮 +1、判定用 `===`，故低频 warn 每任务恰触发一次；实测默认配置双故障场景 `slowWarns` 长度 = 1（见 V6'），且全场景 `abandonWarns = 0`（"放弃"语义已消失）。

### R2 登记软 TTL（防孤儿常驻）：**pass**

- 实现（`web.js:232-238`）：`reconcileTask` 入口先判 `Date.now() - entry.registeredAt >= reconcileTtlMs` → 删登记 + **一条** warn（`对账登记超时清理`）；TTL 默认 30 分钟（`web.js:38`，注释说明覆盖 agent 侧 300s 超时上限有余；`MAX_TIMEOUT_MS` = 600s，故 30min 有余量）。
- 实测（隔离拓扑，压缩 TTL）：**A 组**（任务永不终态）→ `warnCount = 1`（`对账登记超时清理 task=…（1s 未终态）`）、`outs = 0`、`chatState = working`；再等 900ms（数个低频 tick）后 warn 仍为 1 条（不刷屏）。
- **登记确被删除的外部证明（B 组对照）**：同一构造下，任务在 TTL 之后才把终态投给 web——
  - `TTL = 700ms`（先到期）→ `ttlWarn = 1`、`outs = 0`、`working`（登记已删 → 迟到投递不再被认领）；
  - `TTL = 600000ms`（未到期）→ `ttlWarn = 0`、`outs = 1`、`outText = 'TTL 后投递'`、`completed`。
  → TTL 生效为真删除（非仅停轮询），且在未到期时不影响认领。

### R3 测试全绿（22/22、151/151）：**pass**

```
cd .pb-agents/worktrees/0011-pr-007-reconcile/oamp
node --test test/web.test.js → 1..22  / # pass 22  / # fail 0  （36.8s）
npm test                     → 1..151 / # pass 151 / # fail 0  （37.0s）
```
新增 3 个 pr-007 用例（`任务时长超过快速预算后…`、`跨快速预算 + 投递丢失双故障…`、`登记软 TTL 到期清理…`），并适配既有 pr-007 用例到降频语义（含 `assert.doesNotMatch(web.stderr(), /对账放弃/)` 显式锁死"放弃"语义消失）。

### V6' 双故障（长任务 > 快速预算 + 投递丢失）——默认配置、不压缩时间轴：**pass**

构造：真实 Router + 真实 web（**不注入任何 `OAMP_WEB_RECONCILE_*`**，走代码内置默认 5s/30s/30min）；脚本化节点在 **35s** 才把终态发给**未注册 ghost**（Router 只记任务表、不投递 web）。

| 观测项 | 实测 |
|---|---|
| Router 任务表终态 | `completed`，`routerResultText = 双故障补落回复`，`routerTerminalAtMs = 35063` |
| 快速预算用尽 | `slowWarns` 恰 1 条：`对账转入低频续查 task=…（6 次未终态，转 30s/次）`（≈30s 处，即预算用尽点） |
| out 落库时刻 | **`outAtMs = 60117`**——即低频续查的第一个 tick（30s + 30s），与"快速预算用尽 → 等 30s 再查"完全吻合 |
| 落库形态 | `ins=1 / outs=1`、`outText = 双故障补落回复`（与 Router result 一致）、`chatState = completed` |
| 无副作用 | `ttlWarns = 0`、`abandonWarns = 0` |

**因果隔离（我自行注入，不采信说明）**：临时副本 `causal-oamp` 把"快速预算用尽"改回上一轮行为（写 `对账放弃` 并 `return`，即停轮询不续查），压缩时间轴（fast 100ms ×6 = 600ms、slow 3000ms、任务 1.2s 才终态、终态发 ghost）：

| 树 | 结果 |
|---|---|
| 注入版 | **红**：`verdict = NO_OUT:waitFor 超时(3000ms)`、`ins=1 / outs=0 / chatState=working`、`abandonWarns` 有值 —— 精确复现上一轮报告的 D-1 残余缺口 |
| 修复版（同配置） | **绿**：`outAtMs = 3659`（= 600ms 预算 + 3s 低频 tick）、`outs=1`、`completed`、`slowWarns` 恰 1 条 |

→ "低频续查"确实是双故障恢复的**唯一**因；上一轮的 D-1 缺口已闭合。

### V4' 短任务缺失率（≥8 次新建 chat 首发，投递丢失注入）：**pass**

拓扑同前（真实 Router + 真实 agent 真实 ACP 路径 + 真实 web；临时 socket/库/端口；丢失注入用真实代码路径：租约 300ms → web 被判 offline → Router `recorded` 不投递）。

| 运行 | 对账配置 | 次数 | 缺失 |
|---|---|---|---|
| 压缩预算（fast 200ms / slow 300ms） | 注入 env | 10 | **0/10（0%）** |
| **代码内置默认预算**（不注入任何 env） | 5s / 30s / 30min | 8 | **0/8（0%）** |

两组的 `webLog` 均为 `slowWarn=0 / ttlWarn=0 / abandon=0`；每轮 `router=completed`、`ins=1`、`outs=1`。
（首轮报告确立的对照仍在：修复前同拓扑 10/10 缺失；本轮不再重复。另注：上一轮"压缩预算下 1/12 缺失"的现象正是 D-1 缺口，本轮 D-1 修复后同条件已 0/10。）

### V2 幂等 / V3 文件范围与依赖 / V5 边界卫生：**不回退（pass）**

- **V2 幂等**（自建场景，全部实跑）：S1 竞争十档（投递时刻 0/30/60/85/95/100/110/130/180/250ms 扫过 100ms 对账 tick）→ 10/10 恰 1 条 out 且越过后 4 个间隔仍 1 条、文本逐档匹配；S2 同终态重复投递 3 次 → 1 条；S3 对账先补落 + 600ms 后迟到投递 → 1 条；S4 永不终态 → **`slowWarn=1`、`outs=0`、web 存活、全日志无"放弃"**；S5 挂 60s 快速 + 60s 低频定时器时 SIGINT → **exit code 0 / 4ms**。合计 **14/14 pass**。
- **V3 文件范围/依赖**：`git diff --stat HEAD` = `oamp/src/web.js (+79)`、`oamp/test/web.test.js (+230)`，**仅此 2 文件**；`git diff HEAD -- oamp/package.json` 为 0 行、`dependencies` 仍 `{}` → 零新依赖。
- **V5 边界卫生**：默认环境零影响——`oamp/data/sql.db` md5 `696eb7329a5746c9aab25f43a3e4ac55`、mtime `1789027481` 与验证前基线逐项一致；`oamp/.runtime/router.sock` mtime `1789025450` 不变；全部自建进程/临时 socket/库/端口已停止与删除（无残留）；产出物 md5 与验证开始时的快照一致。

---

## 汇总

- pass: **7 项**（R1、R2、R3、V6'、V4'、V2、V3/V5）
- fail: **0 项**
- partial: **0 项**
- blocked: 0 项

## 偏差记录

> 均为"记录即满足交付"级别；不影响结论。上一轮的 D-1（长任务 + 投递丢失不补拉）与本轮的 D-2（孤儿登记无回收）**已修复并实测确认**，不再列出。

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| D-3：`prd` F02-1「一次提问 = 恰一条 in + 一条 out」 | TTL 到期后登记被删，该任务的迟到投递不再被认领（本设计的有意边界：TTL 是"最终放弃"语义）。实害为 0——TTL 默认 30min > agent 侧最大任务超时 600s；实测仅在把 TTL 压到 700ms 时才观察到丢弃 | 保留（边界正确）；建议在 `architecture` §10.4 或配置说明中显式写明"TTL = 兜底上限，超过即不再认领" |
| D-4：终态兜底缺失（`architecture` §10.4 未覆盖） | 快速预算内的任务若永不终态 → 停摆期间 chat 停在 `working`，无 out、无 `chat_state`（TTL 清理后亦然）。S4/实测 A 组均为此形态 | 可评估"TTL/超时即落一条 `failed` out"，让前端不再永久"进行中"；不阻塞本次交付 |
| D-5：`architecture` §8 配置面「只有三个键」 | 现有 **3 个未登记 env**：`OAMP_WEB_RECONCILE_INTERVAL_MS`、`OAMP_WEB_RECONCILE_SLOW_MS`、`OAMP_WEB_RECONCILE_TTL_MS`（`web.js:43-45`，注释标明供测试压缩时间轴） | 按实现同步 §8 或显式标注"仅测试用" |
| D-6：注释/文案精度 | ① 登记形状注释（`web.js:184`）仍写 `{ chatId, agentId, lines, landed, attempts, timer }`，未含新增的 `slow` / `registeredAt`（行为无误，注释滞后）；② 低频 warn 文案 `转 ${Math.round(reconcileSlowMs/1000)}s/次` 在亚秒配置下显示 `转 0s/次`（仅压缩测试配置可见，默认 30s 正常） | 顺手修注释与文案（`Math.max(1, Math.round(...))`）；零行为影响 |
| D-7：`workflow-pb` 规则 B | worktree 改动**仍未提交**（`git status`: `M` 两文件）；`prs/` 无 pr-007 卡片；`status.md`/`history.md` 无 PR-007 记录 | 补 PR 卡 + 提交 + 迭代分支合并留痕（由主 agent 定） |

## 下一迭代候选

1. **停摆终态兜底**（D-4）：为"永不终态 + 投递丢失"的 chat 落一条失败 out（或复用 `db.startupSweep` 语义），避免前端永久"进行中"。
2. **低频续查参数入配置面**（D-5）：若保留三个 env，在 §8 正式登记并给出默认值语义与上限（如 TTL 必须 > 最大任务超时）。
3. **对账可观测性**：目前仅有 stderr 事件行；可评估把"转入低频续查 / 登记超时清理"作为运行时事件暴露（`/api/agents` 或状态面），便于运维观察兜底是否在工作。
4. **用例覆盖补强**：尚无"Router 不可达时对账顺延"（`queryOnce` 抛错分支）与"TTL 与终态同 tick 竞争"两条边界的用例；补 2 条可锁死降级路径。
5. **定时器粒度**：每在飞任务一个 `setTimeout`（低频下常驻）；若并发任务数上升，可评估单定时器 + 扫描表。

---

## 结论

**PASS**（0 fail / 0 partial；上一轮 D-1、D-2 均已闭合并实测确认）

- R1 独立核实成立：达快速预算只降频、不放弃、不删登记；全量删除路径审计后仅剩 landed（2 处）/ TTL / SIGINT + 既有派发失败，"放弃"语义已在代码与用例两侧消失（用例含 `doesNotMatch(/对账放弃/)`）。
- R2 独立核实成立：TTL 到期真删登记且 warn 恰一条（A 组），并以 B 组对照证明删除的实际效果（TTL 未到期时迟到投递仍被认领）。
- R3 独立核实成立：`web.test.js` 22/22、`npm test` 151/151。
- 关键验收重做：**V6'** 默认配置、不压缩时间轴下"35s 长任务 + 投递丢失"由低频续查在 60.1s 补落（`outs=1`、`completed`），并经变异注入证明注入旧行为即红、修复版即绿；**V4'** 压缩与内置默认两种预算下缺失率均为 0（10 + 8 次新建 chat 首发）；**V7** TTL 清理与 warn 恰一条经实测成立；**V2** 14/14（含 SIGINT 4ms 退出）；**V3/V5** 文件范围、零新依赖、默认环境零影响均不回退。
- 遗留 4 条偏差（D-3~D-7）均为记录级，其中 D-3/D-4 的边界在当前默认参数下无实害，按角色契约**不阻塞交付**。

---

### 附：本轮执行的命令与关键原始证据（临时树与产物已删除；被验证文件全程零修改）

```bash
cd .pb-agents/worktrees/0011-pr-007-reconcile/oamp
node --test test/web.test.js                       # 22/22
npm test                                           # 151/151
grep -n "tasks.delete|tasks.clear" src/web.js      # 235(TTL) / 250 / 301 / 491(既有) / 530

# V6'：默认配置（不注入任何 reconcile env）、35s 任务、终态发 ghost
node dual.mjs ./fixed-oamp dual-35s-ghost          # routerTerminalAtMs=35063 → outAtMs=60117, outs=1, completed
# 因果隔离（临时副本 causal-oamp 注入"达上限即停"，压缩时间轴）
VERIFY_FAST_MS=100 VERIFY_SLOW_MS=3000 VERIFY_WAIT_MS=3000 node dual.mjs ./causal-oamp causal-compressed 1200
                                                   # 红：NO_OUT、outs=0、working、含"放弃"
VERIFY_FAST_MS=100 VERIFY_SLOW_MS=3000 VERIFY_WAIT_MS=8000 node dual.mjs ./fixed-oamp fixed-compressed 1200
                                                   # 绿：outAtMs=3659、outs=1、completed、lowWarn=1

# V4'：投递丢失注入，隔离拓扑逐轮新建 chat 首发
VERIFY_FAST_MS=200 VERIFY_SLOW_MS=300 node runs.mjs ./fixed-oamp loss 10 v4-compressed-10   # 0/10 缺失
node runs.mjs ./fixed-oamp loss 8 v4-builtin-default-8                                      # 0/8 缺失（内置默认）

# V7：TTL（A 组孤儿清理 / B 组清理效果对照）
node ttl.mjs ./fixed-oamp fixed                     # A: warn=1, outs=0, working；B: ttl700→0 out / ttl600000→1 out

# V2：幂等与停摆/SIGINT
node scenarios.mjs ./fixed-oamp fixed               # 14/14（S4: slowWarn=1、无"放弃"；S5: exit 0 / 4ms）
```
