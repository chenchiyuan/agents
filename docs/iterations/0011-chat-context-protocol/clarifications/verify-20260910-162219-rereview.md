# 独立复验报告（复审）：PR-007 补丁修复后的第二次独立验收

**验证者身份**：分布式消息投递/幂等语义审查者（同首次报告；本次额外承担"因果隔离"角色——用注入法证明修复项与该修复消除的缺陷之间的因果链）
**产出物（修复后，worktree 工作区改动）**：
- `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-007-reconcile/oamp/src/web.js`（md5 `bc249ed7931c2b432aaf0dd6526600a5`）
- `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-007-reconcile/oamp/test/web.test.js`（md5 `e5d3dc049f4651e4d2ec70dca12f8d13`）
**复验输入**：主 agent 给出的 3 条修复要点 + 要求重做的 V4/V6/V2/V3/V5（**独立核实，不采信修复说明**）
**验证日期**：2026-09-10
**首次报告**：`clarifications/verify-20260910-162219.md`（结论 FAIL，1 fail：V6 上限分支删除投递凭据）
**验证方式**：只读产出物 + 运行命令 + 自建隔离拓扑实跑 + 对修复代码做**变异注入**（不修改被验证文件，注入在临时副本上）

---

## 逐项判定

### R1 上限分支不再删登记 + 全量 `tasks.delete` 审计：**pass**

- 上限分支（`src/web.js:244-251`）现为：`attempts >= RECONCILE_MAX_ATTEMPTS` → **只写 warn + return**，无删除、无重排定时器：
  ```js
  if (current.attempts >= RECONCILE_MAX_ATTEMPTS) {
    // 只停轮询、**不删登记**：登记同时是投递入口的认领凭据…
    process.stderr.write(`oamp web: 对账放弃 task=${taskId}（6 次未取得终态，改为等投递）\n`);
    return;
  }
  ```
- **全量出现处审计**（`grep -n "tasks.delete\|tasks.clear\|tasks.set\|tasks.get" src/web.js`）：

  | 行 | 位置 | 判定 |
  |---|---|---|
  | 240 | `reconcileTask` 终态分支 | **合法**：`current.landed = true` 之后才 `tasks.delete` → 已落库，登记使命完成 |
  | 291 | `handleDeliver` 的 `task.result` 分支 | **合法**：同样在 `entry.landed = true` 之后删除 → 已落库 |
  | 481 | 派发失败 `catch` 分支 | **既有语义，不受本次修改影响**：与 HEAD 版本逐字节同形（`git show HEAD:oamp/src/web.js` 同位置即 `tasks.delete(taskId)`），该轮落 `out(error='dispatch_failed')` 并已给用户可见终态，不产生"无 out 悬挂" |
  | 520 | SIGINT `tasks.clear()` | **合法**：紧跟在 `for (...) clearTimeout(entry.timer)` 之后，进程退出路径 |

  → 除"已落库"与"派发失败（既有）"两类外，**无其他提前删除路径**；R1 达成。
- `web.js:226/236/274` 三处 `tasks.get` 均保持"取到即校验 `landed`"的形态，未引入新分支绕过。

### R2 新增跨上限长任务回归用例 + 因果隔离复现：**pass**

- 新增用例：`test/web.test.js:808` `Web：任务时长超过对账上限后，晚到的 task.result 仍能落 out（对账不夺走投递凭据）`。构造：对账间隔 100ms（上限 ≈600ms）、假节点在 **1500ms** 才回终态、并断言 `web.stderr()` 含 `对账放弃`（证明本用例走的**必须**是投递路径而非对账补拉）→ 断言设计正确（若对账补拉替它落库，`对账放弃` 断言会因时序不同而敏感，且 text 来自终态 body）。
- **因果隔离（我自行做，不采信修复说明）**：把修复代码复制到临时树 `causal-oamp`，在**上限分支**注入回 `tasks.delete(taskId);`（仅此一行，模拟修复前行为），对同一用例 `node --test --test-name-pattern="超过对账上限" test/web.test.js`：
  - 注入版 → **红**：`not ok 1 … failureType: 'testCodeFailure'`、`error: 'waitFor 超时（8000ms）: 超上限任务仍落 out'`（`# pass 0 / # fail 1`）；
  - 修复版（同命令、同用例）→ **绿**：`# pass 1 / # fail 0`（1.73s）。
  → 修复项与缺陷消除之间的因果链成立，且该用例确实能失败（不是恒真断言）。

### R3 测试全绿（20/20、149/149）：**pass**

```
cd .pb-agents/worktrees/0011-pr-007-reconcile/oamp
node --test test/web.test.js   → 1..20 / # pass 20 / # fail 0   （33.8s）
npm test                       → 1..149 / # pass 149 / # fail 0 （34.0s）
```
与委托期望（20 / 149）一致；新增 1 个用例（19 → 20），全量由 148 → 149。既有用例未被削弱（`setup()` 仅新增默认空参 `webEnv`，`test/web.test.js:203/221`）。

### R4 真实缺失率（V4，隔离拓扑 ≥10 次新建 chat 首发，缺失率 0）：**pass**

拓扑同首次：真实 Router + 真实 agent（真实 ACP 路径，注入假 omp）+ 真实 web；临时 `OAMP_SOCKET`/`OAMP_DB`/随机端口；丢失注入用真实代码路径（租约压到 300ms → web 被判 offline → Router 按既有 `recorded` 语义只记任务表不投递）。

| 运行 | 对账预算 | 次数 | 缺失 | 证据 |
|---|---|---|---|---|
| 修复后 · 默认预算（间隔 5000ms = 产品默认值） | 30s | 12 | **0/12（0%）** | 每轮 `router=completed`、`ins=1`、`outs=1`、`outError=null`、`chatState=completed` |
| 修复后 · 压缩预算（间隔 200ms，仅用于压时间轴） | 1.2s | 12 | **1/12（8%）** | 唯一缺失轮 `run 1`：`router=completed`（result 完整）而 `outs=0`、`chatState=working`；web stderr 恰 1 条 `对账放弃 task=task-ef36e9a1…（6 次未取得终态，改为等投递）` → 见偏差 D-1，属**压缩预算下对账窗口（1.2s）短于任务时长（≈1s+）**的必然结果，非本项判据配置 |
| 修复前（对照，首次已做） | — | 10 | **10/10（100%）** | 同拓扑缺失注入 |

判定：**V4 pass**（产品默认预算下 12/12 全绿；压缩预算那一轮的缺失已被定位为"对账窗口 < 任务时长"的设计边界，且与首次报告同一失败机理，非新回归）。

### R5 V6 长任务反噬回归（> 上限预算的任务其 result 仍落 out、chat 终态 completed）：**pass**

| 运行 | 树/配置 | 结果 |
|---|---|---|
| 长任务 35s（默认对账 5s×6=30s，**上限已触发**） | 修复后 | `routerState=completed`、`terminalAtMs=35055`、`ins=1`、**`outs=1`**、`outText=长任务回复`、`chatState=completed`、warn=`…（6 次未取得终态，改为等投递）`、**pass** |
| 长任务 35s ×2（同场景） | 修复后 | 两次复跑同样 pass（含 `long-task` 用例内 100ms 压缩版） |
| 长任务 35s（对照，首次已做） | 修复前 | `outs=1`（既有行为，未被破坏） |

首次报告的 fail（修复后 `outs=0`/`working`）已消失，且**不是靠"对账又补上了"**——warn 明确显示对账已停摆，out 由迟到投递落库（用例内 `对账放弃` 断言 + 本例 `outText` 与 `routerResultText` 相等）。

### R6 V2 幂等 / V3 文件范围与依赖 / V5 边界卫生：**不回退（pass）**

- **V2 幂等**（自建场景，非产出物用例，全部实跑）：S1 竞争十档（投递时刻 0/30/60/85/95/100/110/130/180/250ms 扫过 100ms 对账 tick）→ 10/10 恰 1 条 out 且越过后 4 个间隔仍 1 条、文本逐档匹配；S2 同一终态重复投递 3 次 → 1 条；S3 对账先补落 + 600ms 后迟到投递 → 1 条（迟到投递未追加）；S4 任务永非终态 → 达上限 warn、out=0、web 存活；S5 挂 60s 对账定时器时 SIGINT → **exit code 0 / 5ms**（无悬挂定时器）。**合计 14/14 pass**。
- **V3 文件范围/依赖**：`git diff --stat HEAD` = `oamp/src/web.js (+69/-...)`、`oamp/test/web.test.js (+165)`，**仅此 2 文件**；`git diff HEAD -- oamp/package.json` 为 0 行、`dependencies` 仍为 `{}` → 零新依赖。
- **V5 边界卫生**：默认环境零影响——`oamp/data/sql.db` md5 `696eb7329a5746c9aab25f43a3e4ac55`、mtime `1789027481` 与验证前基线**逐项一致**；`oamp/.runtime/router.sock` mtime `1789025450` 不变；全部自建进程/临时 socket/库/端口均已停止与删除（无残留）。

---

## 汇总

- pass: **6 项**（R1~R6）
- fail: **0 项**
- partial: **0 项**
- blocked: 0 项

## 偏差记录

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| D-1：`prd` F02-1「一次提问 = 恰一条 in + 一条 out」 | **残余缺口（设计取舍，非本次回归）**：对账达上限后停止轮询，因此"**任务时长 > 上限预算** 且 投递丢失"仍不补拉。实测：35s 任务 + 终态发给未注册 ghost（投递必丢）+ 默认 30s 预算 → `router=completed`、`outs=0`、`chatState=working`；压缩预算（200ms）的 12 连跑中恰 1 轮复现同一机理 | 保留记录；若产品要求长任务也兜底，见候选 1（不阻塞本次交付） |
| D-2：内存/资源约束（`architecture` §10.4 未覆盖） | 上限停摆后 `task_id` 登记**保留在进程内 Map 直到该任务投递到达或 web 重启**（修复前会在上限处删除）→ 对"永不回终态"的任务（agent 崩溃/被杀）条目与 `lines`（≤200 行）留存，量与 Router 自身任务表同阶 | 属可接受取舍；如需收敛，见候选 2 |
| D-3：`architecture` §8「配置面只有三个键」 | 仍存在未登记的第四个 env `OAMP_WEB_RECONCILE_INTERVAL_MS`（`web.js:39-42`）与硬编码 `RECONCILE_MAX_ATTEMPTS` | 按实现同步 §8（标注"仅测试用"），或改为不经 env 的内部注入点 |
| D-4：`workflow-pb` 规则 B「变更须对应 `prs/pr-{NNN}.md`」 | worktree 分支 `feat/0011-pr-007-reconcile` 上改动**仍未提交**（`git status`: `M` 两文件）；`prs/` 无 pr-007 卡片；`status.md`/`history.md` 无 PR-007 记录 | 补 PR 卡 + 提交 + 迭代分支合并留痕（由主 agent 定） |

## 下一迭代候选

1. **长任务兜底策略**（D-1）：上限从"固定 6 次 × 间隔"改为与任务真实超时相关（agent 侧默认 omp 300s / shell 30s），或"停轮询后按低频（如 30s 一次）继续对账直到任务超时预算耗尽"——可闭合"长任务 + 投递丢失"这一残余缺口。
2. **停摆条目的回收**（D-2）：给上限停摆的登记加软 TTL（如按任务超时 + 余量清除），避免 agent 崩溃产生的孤儿条目常驻；或复用 `db.startupSweep` 的重启语义。
3. **终态兜底**：上限停摆后 chat 停在 `working`（既不落 out 也无 `chat_state`）——可评估"超时即落一条 `failed` out"，让前端不再永久"进行中"。
4. **对账参数入配置面**（D-3）：若保留 `OAMP_WEB_RECONCILE_INTERVAL_MS`，在 §8 登记为测试/运维可调项并给出默认值语义。
5. **用例覆盖**：现有 3 个 pr-007 用例覆盖"丢失补拉 / 顺延幂等 / 跨上限投递"，尚未覆盖"跨上限 **且** 丢失"（D-1）与"Router 不可达顺延"（`queryOnce` 抛错分支）两条边界；补 2 条可锁死残余缺口与降级路径。

---

## 结论

**PASS**（0 fail / 0 partial；首次报告的 V6 fail 已修复并经因果隔离证实）

- 修复要点 1 独立核实成立：上限分支只停轮询，全量 `tasks.delete` 审计后无其他提前删除路径（派发失败分支与 HEAD 逐字节同形，属既有语义）。
- 修复要点 2 独立核实成立：新增用例在注入旧行为时**红**、在修复版上**绿**（我自行注入复现，未采信说明）。
- 修复要点 3 独立核实成立：`web.test.js` 20/20、`npm test` 149/149。
- 关键验收重做：V4 默认预算下缺失 12→0；V6 长任务（35s，跨上限）`outs=1` + `chatState=completed`；V2 自建场景 14/14（含 SIGINT 5ms 退出）；V3/V5 文件范围、零新依赖、默认环境零影响均不回退。
- 遗留 1 项**残余缺口**（D-1：长任务 > 上限预算且投递丢失仍不补拉）与 3 项偏差已登记，按角色契约**不阻塞本次交付**，供主 agent 决定是否开启后续迭代。

---

### 附：本次复验执行的命令（临时树与产物已删除；被验证文件全程零修改）

```bash
cd .pb-agents/worktrees/0011-pr-007-reconcile/oamp
node --test test/web.test.js                       # 20/20
npm test                                           # 149/149
grep -n "tasks.delete|tasks.clear" src/web.js      # 审计：240/291/481/520

# 因果隔离（临时副本 causal-oamp 注入 tasks.delete 回上限分支）
cd <tmp>/causal-oamp && node --test --test-name-pattern="超过对账上限" test/web.test.js   # 红（超时）
cd <tmp>/fixed-oamp  && node --test --test-name-pattern="超过对账上限" test/web.test.js   # 绿

# 真实拓扑（隔离 OAMP_SOCKET/OAMP_DB/端口）
VERIFY_RECONCILE_MS=5000 node runs.mjs ./fixed-oamp loss 12 v4-fixed-loss-default   # 缺失 0/12
node runs.mjs ./fixed-oamp loss 12 v4-fixed-loss-12                                 # 缺失 1/12（200ms 压缩预算，见 D-1）
node late.mjs ./fixed-oamp fixed-35s                                                # 35s 任务：outs=1, completed
node late-ghost.mjs ./fixed-oamp fixed-35s-ghost                                    # 35s + 投递丢失：outs=0（D-1 探针）
node scenarios.mjs ./fixed-oamp fixed                                               # 14/14（S1 十档 / S2 / S3 / S4 / S5）
```
