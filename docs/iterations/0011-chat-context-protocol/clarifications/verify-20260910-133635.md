# verify-20260910-133635 — 0011-chat-context-protocol 阶段 5 PR-001（配置面 + 持久层）独立验证

**验证者身份（反射）**：同级代码审查者 + SQLite 持久层审查者（Peer Node.js Reviewer / SQLite Persistence Reviewer）——长期审 Node 内置 `node:sqlite` 同步 API、SQL 唯一出口模块、schema 与索引设计、LIKE 转义与闭区间查询、外键与状态哨兵这类具体坑；对"测试断言是否真的断言了行为（而非实现细节）"和"结构面约束是否真被结构保证"有专门敏感度。

- **反射依据**：产出物类型 = 代码实现（Node ESM 模块 + 其单测）；验证标准来源 = `prs/pr-001-config-surface-and-persistence.md` 验收标准 10 条 + `architecture.md` §4/§7/§8/§12/§16.1/§17 + `prd/F02`、`F03`、`F07` 卡 + 委托 brief 的判定条目 V1~V7。
- **产出物**：
  - `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-001-persist/oamp/src/config.js`（改造，121 行）
  - `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-001-persist/oamp/src/persist.js`（新建，220 行）
  - `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-001-persist/oamp/.gitignore`
  - `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-001-persist/oamp/test/config-file.test.js`（新建，9 用例）
  - `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-001-persist/oamp/test/persist.test.js`（新建，29 用例）
  - `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0011-pr-001-persist/docs/iterations/0011-chat-context-protocol/prs/pr-001-tasks.md`
- **验证标准来源**：`prs/pr-001-config-surface-and-persistence.md`「验收标准」10 条（含 Gate 补齐的 meta 断言）+ `architecture.md` §4.1~§4.7 / §7 / §8.1~§8.5 / §12（AR-02/AR-04/AR-05/AR-06/AR-07/AR-14）/ §16.1 / §17 + `prd/F02`（验收 1~7）、`prd/F03`（验收 1~9）、`prd/F07`（验收 1~6）+ 委托 brief 判定条目 V1~V7。
- **验证日期**：2026-09-10。
- **独立性声明**：未使用任何执行过程上下文。被验证的 5 个文件全程只读，未修改；独立复现脚本写在 worktree 之外的 `/tmp/verify-pr001/repro.mjs`；用于回归对照的基线来自 `git archive HEAD`（PR 落地前的提交 `ab9be12`）解到 `/tmp/oamp-base-check/`，不触碰 worktree 工作区。

---

## 逐项判定

### V1 测试全绿（含既有回归）—— **pass**

- 证据 1（新增面）：`cd oamp && node --test test/config-file.test.js test/persist.test.js` → `# tests 38 / # pass 38 / # fail 0`（`config-file.test.js` 9 用例、`persist.test.js` 29 用例，单独跑亦各为 9/9、29/29 全绿）。
- 证据 2（全量）：`npm test` → 8 次运行中 **7 次 `# tests 102 / # pass 102 / # fail 0`**；另 1 次为 `101/102`（唯一失败即下述既有 flake，见 V1 附注），其后 5 次连续全绿。
- 证据 3（回归基数）：基线 `/tmp/oamp-base-check/oamp`（`git archive HEAD oamp`）`npm test` → `# tests 64 / # pass 64`；64（既有）+ 38（新增）= 102，与 worktree 全量数一致，无既有用例被删改。
- 证据 4（既有文件零修改）：`git diff --name-only HEAD -- oamp/` → 仅 `oamp/.gitignore`、`oamp/src/config.js`；`test/` 下既有 11 个测试文件（含 `hygiene.test.js`）字节未变。

**V1 附注（既有 flake，非本 PR 引入，不构成 fail）**：`web.test.js`（本 PR 未触碰、与基线逐字节相同）单独循环运行存在间歇性失败：
- worktree：43 次独立运行中 3 次失败 —— `not ok 2 Web：发送消息（消息即命令）…`（`error: 'waitFor 超时（8000ms）: 任务完成并回流'`，web.test.js:80/100）与 `not ok 3 Web：追加消息到既有会话`（`waitFor 超时（8000ms）: 两条消息且第二条完成`，web.test.js:121/138）。
- 基线 `HEAD`（无本 PR 任何改动）：50 次独立运行中 1 次失败 —— 同样的 `not ok 2 … waitFor 超时（8000ms）`，同一断言行。
- 结论依据：同一失败模式在**未含本 PR 的基线提交**上可复现（`/private/tmp/oamp-base-check/oamp/test/web.test.js:100`），且 `web.test.js` 及其被测的 `web.js`/`agent.js`/`router.js` 本 PR 未修改；`config.js` 新增三键（`dbPath`/`defaultModel`/`contextMax`）不被 web/agent 读取，既有六键取值经 V2 逐字断言不变。故该失败为既有 E2E 计时 flake（8s 等待窗口内的路由器/agent/子进程往返），与本 PR 无因果关系；已记入「偏差记录 / 下一迭代候选」。

### V2 配置面（默认 / 优先级 / 非法处理 / 既有键不变）—— **pass**（覆盖 PR 验收 1、2、3）

- **默认值**（验收 1）：`src/config.js:14-16` 定义 `DB_DEFAULT='data/sql.db'`、`MODEL_DEFAULT='openai/gpt-5.6-luna'`、`CONTEXT_MAX_DEFAULT=8`；`:118` 用 `path.resolve(PKG_ROOT, …)` 解析 → 独立复现断言 `dbPath === <包根>/data/sql.db`、`defaultModel === 'openai/gpt-5.6-luna'`、`contextMax === 8`，且 `path.isAbsolute(dbPath) === true`（与 cwd 无关）。`PKG_ROOT` 推导沿用改造前 `config.js:9`（按模块位置）。
- **优先级逐键**（验收 2）：`:118-121` 三键各自独立解析；独立复现断言 `env > 文件 > 默认`（三键同时给 → 全取 env；只给 `OAMP_DB` → 另两键取文件值）、相对路径按包根、绝对路径原样。
- **文件缺失**（验收 3）：`src/config.js:75` `ENOENT → {}` → 不抛错；独立复现 `assert.doesNotThrow` 通过。
- **非法即失败**（验收 3）：`:46/:89-101`（段非对象、字段类型/空串）+ `:84`（JSON 语法）+ `:99`（`context.max` 非正整数）全部抛 `OAMP 配置错误: …`；独立复现枚举 11 种非法输入（语法错、顶层数组/字符串/null、`data` 非对象、`db` 非字符串、`defaults` 数组、`max` 为 0/-2/1.5/"8"）**全部**命中 `/OAMP 配置错误/`。
- **入口行为不变**（验收 3 后半）：`src/web.js:120-126` 保持既有 `try { loadConfig(process.env) } catch → stderr + return 1`；`src/agent.js:330-334` 同为 `catch → return 1`。两个入口文件本 PR 未修改。
- **既有六键回归**（验收 1）：`git diff HEAD -- oamp/src/config.js` 显示 `loadConfig` 返回对象中既有六行**逐字未改**（删除行仅为文件头注释 3 行），改动纯为新增；独立复现断言默认六键 `deepEqual` 逐字一致、`OAMP_SOCKET` 覆盖生效、`OAMP_HEARTBEAT_INTERVAL_MS='0'`/`OAMP_HEARTBEAT_TIMEOUT_MS='x'`/`OAMP_RECONNECT='2'`/`OAMP_HB_LOG_WINDOW_MS='-1'` 均抛 `OAMP 配置错误`。

### V3 持久层 schema 与写口结构面（两类 direction 不可产生第三类）—— **pass**（覆盖 PR 验收 4、5）

- **schema 与建库**（验收 4）：`src/persist.js:13-35` 两张表 + 两索引，逐列与 architecture §4.1 一致（独立复现 `sqlite_master` 断言为 `chats`/`messages`/`idx_chats_updated`/`idx_messages_chat_time`，`pragma_table_info('messages')` 列序逐字一致）；`:111` `mkdirSync(dirname, {recursive:true})` 多级目录自动创建；`:113` `PRAGMA foreign_keys = ON`（独立复现读回 `foreign_keys=1`，且向不存在的 `chat_id` 写 out 抛 `FOREIGN KEY`）。
- **幂等**（验收 4）：`:13/:22/:34/:35` 全部 `IF NOT EXISTS`；独立复现重复 `openDb` 同路径不抛错、`close()` 后可再开且数据仍在。
- **写口结构面**（验收 5 / E-5 / §4.7）：`persist.js` 全部 `export` 仅 `openDb` 一个（`:110`）；`:219` 返回句柄固定 8 个方法 `insertInput/insertOutput/upsertChat/closeChat/startupSweep/listChats/getChat/close`，内部 `stmts.insertMessage`（唯一写 `messages` 的语句，`:130-133`）**未暴露**；`direction` 在函数体内写死（输入 `:158` 常量 `'in'`，输出 `:173` 常量 `'out'`），签名无 `direction`/`state` 参数。
- **行为断言**（E-5 结构面）：独立复现 `insertInput({direction:'out'})` 后该行 `direction === 'in'`、`insertOutput({direction:'in'})` 后为 `'out'`；一次 `insertInput`+`insertOutput` 后 `COUNT(*)=2` 且 `SELECT DISTINCT direction = ['in','out']`。结构上无法产生第三类：无任何导出入口可携带 `direction` 写 `messages`。

### V4 meta 落盘与读回（task_id / context_id+pid）—— **pass**（覆盖 PR 验收 6）

- 写入 `src/persist.js:62-63`（`JSON.stringify`，未提供 → `NULL`），读回 `:66-67`（`JSON.parse`，`NULL` → `null`）。
- 独立复现：`insertInput({meta:{task_id:'task-9'}})`、`insertOutput({meta:{context_id:'ctx-77-2',pid:4242}})` → 裸连接直读 `meta` 列 `JSON.parse` 还原为**同一对象**；`getChat()` 读回 `messages[i].meta` `deepEqual` 相等；未传 meta → 读回 `null`。
- 校准：该契约按 tasks A.2/§4.3 定义为「调用方传入 meta，模块负责 JSON 往返」，`{task_id}` / `{context_id,pid}` 的**构造**在 pr-004/pr-003 消费侧；本 PR 交付的是可原样还原的往返能力，满足 F05-2/E-1（pr-005 端到端断言）所依赖的字段契约。

### V5 查询面（排序 / 分页 / 时间闭区间 / 关键词转义 / agent 相关性）—— **pass**（覆盖 PR 验收 7）

- **排序**：`:149` `ORDER BY c.updated_at DESC, c.chat_id DESC` —— 独立复现同毫秒两 chat 按 chat_id 倒序稳定输出。
- **分页与 total**：`:150` `LIMIT ? OFFSET ?`、`:152` 独立 `COUNT(*)`；`:191-207` 默认 `limit=50`、`offset=0`，`:74-86` 校验（`limit` 非 1..200、`offset` 负数 → 抛错）；独立复现两页拼接无重叠无遗漏、`total` 不受分页影响。
- **时间过滤**：`:54-55`（`updated_at >= from` / `<= to` 闭区间，`:198-202` 取参）；独立复现端点被包含、作用于 `updated_at`（写输入后 `updated_at` 前移 → 落入新窗口）、`from > to` 抛错。
- **关键词**：`:51` `title LIKE ? ESCAPE '\'` OR `EXISTS(messages.text LIKE ? ESCAPE '\')`、`:70-71` 对 `\ % _` 逐字符转义；独立复现 `q='%'`/`q='_'`/`q='\\'` 只命中字面量持有者、含已关闭 chat 在结果内。
- **agent 相关性**：`:52` `chats.agent_id = ?` OR `EXISTS(messages.agent_id = ?)`；独立复现「默认 agent 命中」与「参与过该 chat 的消息 agent 命中」两条分支均返回，无关 agent 为 0 条。
- **组合与状态**：`:51-55` 全条件 AND、`:194-197` 四态校验；独立复现 `q+agent+state+时间` 四条件组合结果正确、`state='idle'` 抛错。
- **详情**：`:143` `ORDER BY created_at ASC, id ASC`；独立复现同毫秒按自增 id 稳定升序、未知 chat 返回 `null`、空库返回 `{chats:[],total:0,limit:50,offset:0}`。

### V6 生命周期（哨兵 / 幂等 / 扫尾 / 重开可读）—— **pass**（覆盖 PR 验收 8、9）

- **closed 哨兵**：`:135-138` 三处状态写入与 upsert 均带 `AND state != 'closed'`（`:128`）；独立复现关闭后迟到 `insertInput`/`insertOutput` 不改写 `state`（仍 `closed`）与 `updated_at`。
- **关闭幂等**：`:138` 同一语句带哨兵 → 二次 `closeChat` 不改写 `closed_at`/`updated_at`；独立复现 `closed_at` 仍为首次值，返回值 `true`（未知 chat `false`，`:184`）。
- **startupSweep**：`:140` `UPDATE chats SET state='failed' WHERE state='working'`（不动 `updated_at`、不补记录，AR-05/§4.3）；独立复现仅遗留 working 被扫（返回影响行数 1），`completed` 不受影响。
- **重开可读**（E-3 落盘侧）：独立复现 `close()` → 同路径 `openDb` → `getChat()` 与关闭前 `deepEqual`（含两条消息与 meta），`listChats()` 仍可列出。

### V7 卫生与边界（零依赖 / 不写仓库 data/ / 文件范围）—— **pass**（覆盖 PR 验收 10）

- **.gitignore**：worktree `oamp/.gitignore` = `.runtime/` + `data/` 两行（`git diff HEAD` 为单行追加）；`oamp/test/hygiene.test.js` 3 个既有静态断言原样全绿（全量 102 的组成部分）。
- **不写仓库 data/**：跑完全量测试与独立复现后 `ls oamp/data` → `No such file or directory`；`persist.test.js` 自带 `createdPaths` 全量落在 `os.tmpdir()` 的卫生断言并通过。
- **零第三方依赖**：`package.json` `dependencies === {}`；`src/config.js`/`src/persist.js` 的 import 全部为 `node:` 内置（`:6-8` / `:8-10`）；两个新测试文件亦仅 import `node:` 内置。
- **无越权接线**：`grep -rn "persist" oamp/src oamp/web oamp/bin oamp/test`（排除 `test/persist.test.js` 自身）→ 仅 `persist.js` 注释行命中，无任何消费方 import；`web.js`/`agent.js` 未接 `dbPath`/`defaultModel`/`contextMax`（符合「本 PR 不接线」边界）。
- **文件范围**：`git status --porcelain`（worktree）恰为 6 项 —— `M oamp/.gitignore`、`M oamp/src/config.js`、`?? oamp/src/persist.js`、`?? oamp/test/config-file.test.js`、`?? oamp/test/persist.test.js`、`?? docs/iterations/0011-chat-context-protocol/prs/pr-001-tasks.md`；与 PR 卡「文件范围」5 项 + 阶段 5 产物 `pr-001-tasks.md` **严格一致**，无 Router/registry/前端/`bin/` 改动。

### PR 验收标准 10 条 → 判定追溯

| # | PR 卡验收标准（摘要） | 判定 | 证据 |
|---|---|---|---|
| 1 | 默认三键 + 既有键不变 | pass | V2；`config.js:14-16,118-121`；复现 C1/C4 |
| 2 | 三键逐键优先级 | pass | V2；`config.js:118-121`；复现 C2/C3 |
| 3 | 缺失不失败 / 非法抛 `OAMP 配置错误` / 入口退出 1 不变 | pass | V2；`config.js:75,84,99`；`web.js:120-126`、`agent.js:330-334`；复现 C5 |
| 4 | `mkdir -p` + 幂等建库 + FK | pass | V3；`persist.js:111,113,13-35`；复现 P1/P2 |
| 5 | 写接口仅 `insertInput`/`insertOutput`，`direction` 不由调用方传入 | pass | V3；`persist.js:158,173,219`；复现 P3/P4 |
| 6 | `meta` 落盘与读回（`task_id` / `context_id`+`pid`） | pass | V4；`persist.js:62-67`；复现 P5 |
| 7 | 排序/分页/时间闭区间/关键词转义/agent 相关性 | pass | V5；`persist.js:51,52,71,143,149,150,152`；复现 P6~P11 |
| 8 | `state!='closed'` 哨兵 + 关闭幂等 + 扫尾 | pass | V6；`persist.js:128,135-138,140`；复现 P12 |
| 9 | 重开库可读回（E-3 落盘侧） | pass | V6；复现 P12 后半 |
| 10 | `.gitignore data/` + 两测试全绿 + 不写 `oamp/data/` | pass | V1/V7；`.gitignore`；`hygiene.test.js` 全绿；复现 H1 |

**架构条款抽查**：AR-02（标题 = `text.trim().slice(0,40)`、空标题兜底「新对话」、后续输入不改标题 → `persist.js:42,156` + `ensureChat … DO NOTHING :121`）pass；AR-04（`node:sqlite`、两表、`mkdir -p`、幂等建表、`PRAGMA foreign_keys`、无迁移版本表）pass；AR-05（输入/输出/派发失败落盘口径、扫尾不补记录、过程永不入库）pass；AR-06（列表/详情接口形状、分页、排序、详情升序）pass；AR-07（`updated_at` 闭区间、`%_\` 转义、含 closed、agent 相关性）pass；AR-14（三键、env 名、包根基准、缺失/非法、`data/` 忽略、无热重载）pass。

---

## 汇总

- pass: 7 项（V1~V7，覆盖 PR 验收标准 10/10）
- fail: 0 项
- partial: 0 项
- blocked: 0 项

独立复现（worktree 之外）：`/tmp/verify-pr001/repro.mjs` → 19/19 检查项 PASS（C1~C5、P1~P12、H1~H2）。

## 偏差记录

> 实现与现有规格/文档不一致的地方。不影响本次验收判定，但应在下一迭代中同步文档或评估是否调整实现。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | architecture §4.3「输入」行只描述写 1 条 `in` 记录 + chat 置 `working`，未描述 chat 行由谁创建 | `insertInput` 对不存在的 `chat_id` 先做 `INSERT … ON CONFLICT DO NOTHING` 防御性建行（`persist.js:120-122,157`），title 按 AR-02 规则取输入前 40 字符 | 按实现回填 architecture §4.3（或在主 agent 处确认 `pr-001-tasks.md` 的 `[model_inferred]` I-2） |
| D-2 | architecture §8.2/§8.5 用 `??` 语义表达优先级，未定义 env 为空串时的行为 | `readNonEmptyString`（`config.js:37-40`）把空串/纯空白视为"未提供"而继续向下一级取值（`OAMP_DB='  '` → 取文件值，而非取空串后 `path.resolve` 到包根） | 同步写入 §8.2 空串语义（或确认 I-1）；否则另一读法（空串即字面值）会给出不同默认库路径 |
| D-3 | architecture §4.5/§4.6 对 `limit`/`state`/`from>to` 非法只写 HTTP 面「→ 400」 | SQL 层 `persist.js:74-102,195-202` 直接抛 JS `Error`，HTTP 400 映射不存在于本 PR | pr-004 必须捕获并映射为 400；建议在 §4.5/§4.6 补一句"校验落在 `persist` 层、HTTP 层负责映射"，避免下游误认为 persist 返回错误码（对应 I-5） |
| D-4 | architecture §4.7 字面："导出的写函数只有两个：`insertInput()`/`insertOutput()`" | 另导出 `upsertChat`/`closeChat`/`startupSweep`（`persist.js:219`），三者只写 `chats` 状态、不写 `messages` 且不接受 `direction`；"不可产生第三类"的实际语义未被削弱 | 把 §4.7 措辞收窄为"能写 `messages` 的函数只有两个"，使文档与结构面一一对应（PR 验收 5 的括号口径本就如此） |
| D-5 | AR-02「后续输入不改标题，不提供重命名」 | `insertInput` 路径满足（`ensureChat … DO NOTHING`）；但导出面 `upsertChat`（`:123-129,178-180`）允许改写 title/agent_id/updated_at | architecture 未定义 `upsertChat` 的调用点；建议在 §4.3 明确其仅用于新建 chat，或在 pr-004 实现时禁止用其改标题 |

## 下一迭代候选

- **既有 `web.test.js` 间歇性 flake（8s `waitFor` 超时）**：worktree 3/43、基线 1/50 复现同一失败（`not ok 2 … waitFor 超时（8000ms）: 任务完成并回流`）。非本 PR 引入，但阶段 5/6 的"全绿"判据会被它反复污染——建议 pr-004 等价重写 `web.test.js` 时把等待窗口与失败诊断（附 `task` 最新状态）一并加固。
- **`direction`/`state` 无 schema 级 `CHECK` 约束**：当前"仅两类"由模块 API 结构保证（满足 E-5），但裸 SQL 仍可写入第三类（`persist.js:25,21` 无 CHECK）。可选加 `CHECK (direction IN ('in','out'))` 作为纵深防御，本迭代未要求。
- **`insertOutput` 对不存在 chat 直接抛 FK 错误**（`:173` 无防御性建行）：pr-004 的「派发失败也落一条 out」路径必须先有 `insertInput` 建行；若 web 侧顺序有误会以 FK 异常暴露，建议 pr-004 单测覆盖该顺序。
- **`upsertChat` 与 `insertInput` 的重复建行能力**（`:120`/`:123`）：两个入口都能创建 chat 行，语义重叠（一个 DO NOTHING、一个 DO UPDATE）；pr-004 接线时需固定"谁负责建行"，否则可能出现标题被覆盖的时序差异。
- **`oamp/test/persist.test.js` 的 `[model_inferred]` I-3 影响面**：`insertOutput` 由 `error` 派生 `completed`/`failed`，若将来需要 `closed` 或其它终态的 out 记录需重新设计参数面——当前无此需求，仅登记。

## 结论

**PASS**（7/7 判定条目 pass，覆盖 PR-001 验收标准 10/10；fail 0、partial 0、blocked 0）

注：偏差记录不影响结论判定——D-1~D-5 均已在报告内显式记录并给出建议处理方式，由主 agent 决定是否回填文档或纳入下一迭代。
