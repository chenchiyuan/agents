# pr-004 独立验证报告（阶段 5 · PR 实现）

**验证者身份（反射结论）**：**双入口接缝审查员** —— 产出物是两个"只做装配 / 转发"的入口（可执行 `bin/hub.js` + 库面 `sdk/index.js`）与一件**跨 PR 测试基建**（`test/helpers/hub-harness.js`），外加 `package.json` 的一行登记。这类产物的价值不在"代码写得对不对"，而在**下游 pr-007~pr-010 能不能按它冻结的形状直接接上去**：我按三条线验证 ——（a）**真入口实测**：每个入口在真 Router + 真 web 上跑出可观察的退出码 / 输出 / 深比较结果；（b）**接缝成立性**：`runHub` 的签名、返回形状、`env` / `input` / `timeoutMs` 语义、进程组收口、`bin/hub.js` 的调用形态，是否**逐字**可被消费；（c）**改动面封闭**：本 PR 是否只碰它声明的 4 个文件、零触碰已合并产物。我不采信任何自述，全部判据来自本次自跑的脚本与进程快照。

## 0. 产出物与验证标准来源

| 项 | 内容 |
|---|---|
| 产出物 | `oamp/sdk/index.js`（新建，29 行，sha256 `7f220008…`）、`oamp/bin/hub.js`（新建，5 行，sha256 `9f6e47bc…`）、`oamp/test/helpers/hub-harness.js`（新建，76 行，sha256 `e5d615b0…`）、`oamp/package.json`（16 行，sha256 `1dab9e01…`，仅追加 `bin.hub`） |
| 提交 | head `72aac28841e8562b225450d712e8214e1391d360`；base `453326c31ed02fb98bf322e444b856cdd4316556`；`git log --oneline 453326c..HEAD` = `72aac28` / `f2e99c0` / `59c3e59` / `d00532f`；`git diff --stat` = **4 files / 112 insertions(+) / 1 deletion(-)** |
| 标准 1 | `docs/iterations/0025-hub-sdk-and-skill/prs/pr-004-sdk-dual-entry-and-hub-harness.md`「验收标准」**11 条** |
| 标准 2 | `docs/iterations/0025-hub-sdk-and-skill/prs/pr-004-tasks.md` T1~T5 逐任务验收标准（**42 条**：T1 7 + T2 6 + T3 9 + T4 11 + T5 9） |
| 标准 3 | `architecture.md` §4.1 **N-1 / N-2 / N-11**、§4.2 **M-1**、§5.2 库 API（规则 1~5）、§4.4 **顺序约束 6**、§4.3 Z-1~Z-7；`prd/{F01,F09,F13,G01}*.md` 验收标准（**29 项**） |
| 环境 | Node `v22.15.0`；真 Router（临时 socket）+ 真 `web start`（随机端口 + **绝对**临时 `OAMP_DB`）；验证脚本全部落 `/tmp/pr004verify/`（`e2e.mjs` / `mutate.mjs` / `extra.mjs` / `bytes2.mjs` / `ns.mjs` / `devprobe.mjs`）；工作区绝对路径 = `<worktree>/oamp` |
| 验证日期 | 2026-09-15 |
| 流程口径 | 按用户 2026-09-15 指令**不跑仓库级全量套件**：scoped = `node --test test/hygiene.test.js`（3/3 pass）与 `node --test test/sdk-skill.test.js`（8/8 pass）；另**加跑一条**与本 PR 真源关系最近的既有面 `node --test test/api-routes.test.js`（7/7 pass，见 §1-#11 证据） |

**独立性声明**：本报告全部断言由本次实测产出（真服务端到端 4 轮 + 14 组逐字节比对 + 变异注入 + 进程组快照 + 静态面扫描 + 散列比对）。**未修改 `oamp/**` 下任何文件**（变异注入只在 `mktemp -d` 副本内以**内存**方式施加，真源散列前后全等，见 §4-C）；本报告是本次验证的唯一写入（worktree 内绝对路径）。**未使用任何执行过程上下文，未采信任何自述**。

---

## 1. 逐项判定 · 标准 1（PR 文件「验收标准」11 条）

| # | 验收摘要 | 判定 | 证据（本次实测） |
|---|---|---|---|
| 1 | `bin` 只追加 `"hub": "./bin/hub.js"`；`dependencies` 仍 `{}`、`engines` 沿用、`type`/`scripts` 逐字不变 | **pass** | `JSON.parse` 后逐键与 base 原值 `deepEqual`：`{name,private,type,engines,scripts,dependencies}` **全等 true**；`bin` = `{"oamp":"./bin/oamp.js","hub":"./bin/hub.js"}`（`bin.oamp` 值不变）；`dependencies === '{}'`；`engines = {"node":">=22"}`；`git diff` 仅 2 行（新增 hub 键 + 原键补逗号），与"JSON 追加键必然产生尾逗号"一致 |
| 2 | `bin/hub.js` 与 `bin/oamp.js` 同形 | **pass** | 归一化 import 路径后逐行 diff：**唯一差异 = 第 2 行注释文案**；两文件同为 5 行 / 206 字节；`process.exit(` **零命中**、`try {` **零命中**；import 面恰 `['../sdk/cli.js']`；`bin/oamp.js` 逐行 diff 唯一差异为注释（`R/E 两行同形`） |
| 3 | 在 `oamp/` 之外执行 `--help` 与 `api docs`（hub 运行中）结果与仓库内一致、不需 `cd` | **pass** | `--help`：cwd=仓库根 / cwd=系统临时目录 → stdout **逐字节一致**、stderr 均空、退出码均 `0`；`api docs --port <运行端口>`：三处（仓库根 / 临时目录 / 临时目录第二进程）**stdout 与 stderr 逐字节一致**、`code=0`、stdout 单 JSON、`routes[]` 长度 21 |
| 4 | `bin/hub.js` 内不出现凭据类字段名 | **pass** | 7 词（词边界、大小写不敏感：`token`/`api_key`/`secret`/`password`/`credential`/`authorization`/`private_key`）**逐词零命中**；且 **`test/hygiene.test.js:26-39` 的扫描面确含 `bin/` 目录平铺 `*.js`** ⇒ 该用例不是空转：`node --test test/hygiene.test.js` = **3/3 pass** |
| 5 | `import` 得 `createHub`；四命名空间；`api.agents({state:'online'})` 返回服务端响应体原对象；`cli.run(['status'])` → `{exit_code,stdout,stderr}` | **pass** | `await import('<包根>/oamp/sdk/index.js')` 从临时目录成功，导出面恰 `['createHub']`（函数）；`createHub()` 键集合 `['api','cli','doctor','uds']`；`hub.api.agents({state:'online'})` 与①直连 `127.0.0.1:<p>/api/agents?state=online` 原始响应体 ②`hub api agents --state online` 的 stdout 解析结果 **`JSON.stringify` 全等**（无信封、键序同）；`Object.keys(hub.cli.run(['status']))` 排序 = `['exit_code','stderr','stdout']`，`exit_code=0` 且 stdout/stderr 与直跑 `oamp status` **逐字节一致** |
| 6 | 同一份入口表驱动两面：改一条入口定义 ⇒ CLI 面与库面**同时**体现 | **pass** | 在 `mktemp -d` 副本内**内存**改写 `ENTRIES` 的 `api docs` 条目（共享引用字段 `cmd[0]`→`docsx`、`run`→返回哨兵对象）：① 库面 `createHub({port:1}).api.docsx()` 返回**同一哨兵对象**（`===` 成立）、`api.docs === undefined`；② 同进程 CLI 面 `main(['api','docsx'])` stdout = `{"sentinel":"PR004-MUTATION-OK"}`、`main(['api','docs'])` 返回 `2`；③ 副本磁盘文件 sha256 不变、真源 4 文件 + `surface.js`/`cli.js` **散列前后全等**、`git status` 仍只有阶段产物 untracked |
| 7 | `createHub()` 每次返回新对象、无模块级可变状态、零本地写 | **pass** | `h1 !== h2`、`h1.api!==h2.api`、`h1.uds!==h2.uds`、`h1.cli!==h2.cli`、`h1.doctor!==h2.doctor`；就地改 `h1.api.agents='STUB'` 后 `h2.api.agents` 仍为函数（**无污染**）；模块导出面仅 `createHub`；`index.js` 内 `node:fs` **零命中**、模块级 `let` **零命中**（`grep -n 'let '` 空）、`.runtime`/`data/` 字面量 **零命中**；核验前后 `oamp/.runtime` 与 `oamp/data` **仍不存在**、`git status --porcelain` 仅 1 项 untracked（阶段产物 `pr-004-tasks.md`） |
| 8 | `hub-harness.js` 导出 `runHub(args,{env,input,timeoutMs}={})` → `{code,stdout,stderr}` | **pass** | 导出面恰 `['runHub']`；返回对象 `Object.keys` 排序 = `['code','stderr','stdout']`；被执行对象 = `path.join(PKG_ROOT,'bin','hub.js')`，`PKG_ROOT` 由 `import.meta.url` 上溯两级（`../..`）推导；从临时目录调用全部成功（14 组） |
| 9 | harness 及自测不写仓库内 `.runtime/` / `data/`、不依赖真实 omp / 外网 | **pass** | `hub-harness.js` 内 `writeFile`/`appendFile`/`mkdir`/`mkdtemp`/`createWriteStream`/`rm*` **逐项零命中**，`.runtime`/`data/` 字面量零命中，import 面 = 4 个 `node:*`；`cwd: os.tmpdir()` 为 spawn 缺省；核验后仓库内两目录仍不存在；全程只连本机环回与临时 UDS，无外网 / 无真实 omp |
| 10 | 零新增依赖 | **pass** | `dependencies === {}`；4 个文件的 import 面 = `node:child_process`/`node:os`/`node:path`/`node:url`（harness）+ 同目录 `./surface.js`/`./doctor.js`（index.js）+ `../sdk/cli.js`（hub.js）；零第三方 |
| 11 | diff 不含 `oamp/src/**`、`API.md`、`llms.txt`、`web/**`，不新增任何路由 | **pass** | `git diff --name-status 453326c..HEAD` = **恰 4 行**：`A oamp/bin/hub.js` / `M oamp/package.json` / `A oamp/sdk/index.js` / `A oamp/test/helpers/hub-harness.js`；对 `oamp/src` / `oamp/API.md` / `oamp/llms.txt` / `oamp/README.md` / `oamp/web` / `oamp/test/*.test.js` / `roles` / `tools` / `.claude` 的 **`git diff --stat` 全空**（退出码 0、零输出）；`oamp/test/*.test.js` 仍 **33** 个；既有漂移锁 `test/api-routes.test.js`（逐字节快照 + 路径级双向覆盖）**7/7 pass**（补充证据，见 §0 流程口径） |

**标准 1 汇总**：**pass 11 / fail 0 / partial 0 / blocked 0**。

---

## 2. 逐项判定 · 标准 2（`pr-004-tasks.md` T1~T5，42 条）

判定原则：同任务内凡有一条子项未达成即标 partial 并单列。逐条证据按主题归并（同一实测覆盖多条）。

| 任务 | 条数 | pass | partial | fail | 关键证据 |
|---|---|---|---|---|---|
| **T1** `sdk/index.js` 三层装配 | 7 | 7 | 0 | 0 | ①import 集合恰 `['./surface.js','./doctor.js']`，零 `node:*` 副作用 import、零 `../src/**`、零 `./http.js`/`./uds.js`/`./errors.js`/`./cli.js` 直接 import，加载即成功（无 TDZ/循环）；②从系统临时目录 import 成功，`typeof createHub==='function'`，返回键含 `api`/`uds`/`cli`；③**库面方法树 21 条与 `ENTRIES` 层 A 的 `cmd` 逐条等值**（`missing=[] extra=[]`，含二级 `chats.*`/`messages.send`/`stream.*`/`projects.*`/`calls.*`/`confirmations.*`），`uds={connect}`、`cli={run}`；`api.agents({state:'online'})` 与直连 HTTP 原始响应体 `deepEqual`；`cli.run(['status'])` → `{exit_code:0,stdout,stderr}` 与直跑逐字节一致；④`index.js` 内 `OAMP_WEB_PORT`/`7788` **零命中**；`createHub({port})` 打中该端口；`OAMP_WEB_PORT=<p>` 生效（routes 21）；无 env 时落 `7788`（实测 7788 上确有活 hub —— 主工作区 `oamp/bin/oamp.js web start --port 7788` 正监听，返回 21 条 routes ⇒ 缺省值确为 7788）；⑤显式 `socketPath` 与 `OAMP_SOCKET` 两态均连通临时 Router（`status()` 返回对象）；⑥`h1!==h2` 等 5 项引用全异 + 就地改动不污染 + 零 `let`/零 fs/零字面量 + 仓库内零新增文件；⑦`git diff --name-status` 仅 `A oamp/sdk/index.js` 一项涉及本任务 |
| **T2** `sdk/index.js` 的 `doctor` 组合 | 6 | 6 | 0 | 0 | ①键集合 `['api','cli','doctor','uds']`（**恰四键**），`doctor.check` 为函数，`doctor` 与 `api` 互不共享引用；②对运行中 hub：`check()` → `{pass:true, items:50}`，每项含 `id`/`ok`/`expected`/`actual` 四键、`items` 全 `ok`；对**未监听端口**：**抛** `HubError`，`code='HUB_UNREACHABLE'`、`exitCode=3`，无部分 `items`；③`apiDocPath` 注入生效：原文副本 ⇒ `pass=true`；人为漂移副本（`/api/agents`→`/api/agents_DRIFT`，落临时目录、**仓库文件零触碰**）⇒ `pass=false` 且失败项**点名端点**（`R1 GET /api/agents_DRIFT` + `R1 GET /api/agents`）；④`index.js` 内 `API.md`/`/api/docs`/端点名/flag 字面量**零命中**；`check()` 返回值与直调 `doctorCheck({port})` **`JSON.stringify` 全等**；⑤两次 `check()` 返回对象引用不同（无缓存）、零 fs、核验前后仓库零新增；⑥同文件串行（T1/T2 同文件，diff 只 1 项） |
| **T3** `bin/hub.js` + `package.json`（同一提交） | 9 | 9 | 0 | 0 | ①逐行 diff 唯二差异（注释文案 / import 路径）成立，零 argv 解析（除 `process.argv.slice(2)`）、零 try/catch、零 `process.exit()`、零 `../src/**`；②`git ls-files -s` = `100755 79190bd… 0	oamp/bin/hub.js`（与 `bin/oamp.js` 的 `100755` 一致）；③`package.json` 语义改动恰一处键（逐键 deepEqual 见 §1-#1）；④两 cwd（仓库根 / 系统临时目录）四条命令 stdout/stderr/退出码**逐字节一致**、无 `cd` 前置、除显式 `--port` 外无环境变量前置；⑤四态实测：`api docs --port <活>` ⇒ `0` + stdout 单 JSON + stderr 空；`api bogus` ⇒ `2` + stdout 空 + stderr **恰一行** JSON（`code='USAGE'`、无堆栈）；`api docs --port <未监听>` ⇒ `3` + `code='HUB_UNREACHABLE'` + stderr 恰一行（无堆栈）；`cli status` 与直跑 `oamp status` 逐字节一致（可达态两侧 `0`；**不可达态两侧同为 `1`**，见 §4-D）；⑥7 词零命中 + hygiene 用例覆盖 `bin/`；⑦`dependencies` 仍 `{}`、`bin/hub.js` 只 import `../sdk/cli.js`；⑧**同一提交**：`git show --name-only 59c3e59` 同时列出 `oamp/bin/hub.js` 与 `oamp/package.json`（另两提交各只含 1 文件）；⑨diff 只含该两文件 |
| **T4** `hub-harness.js` 的 `runHub` | 11 | 11 | 0 | 0 | ①签名逐字 + 返回键恰三（见 §1-#8）；②被执行对象 = `node <包根>/bin/hub.js`，包根按 `import.meta.url` 上溯 `../..`，本文件内零 `/Users/`、零 `/home/`、零硬编码 `/tmp`（`~` 仅出现在注释的 `pr-007~pr-010` 区段写法），从系统临时目录调用成功；③**14 组命令**（api/uds/cli/doctor/`--help`/用法错误/不可达）经 harness 与**直接 spawn** 的 stdout/stderr/退出码 **全部逐字节一致**；**未以换行结尾的尾部**用 `NODE_OPTIONS=--require` 只读注入子进程去尾换行后实测：直跑 stdout 1718 字节且**不以 `\n` 结尾**，harness 取回 **1718 字节且逐字节一致**（尾部 `..."hub doctor [--human]"`）⇒ 不丢不截断成立；`code` 为 number，信号终止时 `null`；④`input` 给出即写 stdin 并 `end()`、未给即 `'ignore'`：实测 `--help`（不给）正常返回、`cli status`（`input:''`）正常返回、**`--help`/`api bogus` 带 `input` 写早退子进程亦不抛**（无 EPIPE 逃逸）；两路径均不因等 stdin 挂起；⑤`timeoutMs`：长驻命令（`cli web start --port <p>`）`timeoutMs:2000` ⇒ 返回耗时 **2004ms**（区间 (1500,3000) 内）、`code===null`；**缺省上限**实测 **10004ms**（不给 `timeoutMs` 时同命令返回，`code===null`）；⑥`env` = `{...process.env, ...opts.env}`：本文件内 `OAMP_SOCKET`/`OAMP_DB`/`OAMP_WEB_PORT`/`7788`/`router.sock` **逐项零命中**（不补默认路径）；只给 `OAMP_SOCKET` 即连上临时 Router（`uds router.status --params {}` ⇒ `0` + 可解析 stdout）；⑦零 fs 写 API、零运行态字面量、核验前后仓库零新增；⑧import 面 ⊆ `node:*`，未引入测试框架 / CI 脚本，`scripts` 未改；⑨`oamp/test/*.test.js` 仍 **33**、`test/helpers/` 由 2→3、`helpers/{harness,fake-node}.js` **零 diff**、`hub-harness.js` 不被 `test/*.test.js` 拾取为用例；⑩四态冒烟 + 与直接 spawn 逐字节一致（见③）；⑪diff 只 1 文件 |
| **T5** 收口核验 | 9 | 9 | 0 | 0 | ①**两面同源**：结构面 —— `index.js` 内 `/api/`、端点名、`--` 选项名字面量**零命中**，两面同源由 `cli.js:matchEntry` 与 `surface.js:buildApiNamespace` 同取 `ENTRIES` 保证；行为面 —— 变体注入（§1-#6）库面 / CLI 面**同时**生效、旧名两面同时消失、`surface.js`/`cli.js` 零 diff（**全程内存改动，真源散列前后全等**）；端到端面 —— `hub api agents --state online --port <p>`（子进程）与 `createHub({port:p}).api.agents({state:'online'})`（库面）与直连 HTTP 三者 `deepEqual`；②**跨宿主一致**：①shell（仓库根 cwd）②Node `import`（库面）③另一进程经 shell（临时目录 cwd）⇒ ①与③**逐字节一致**、②与①的 JSON **结构全等**、三处退出码同；③**改动面封闭**：diff 4 文件；零命中清单全空；已合并产物 `sdk/{errors,http,uds,surface,cli,doctor}.js`、`skill/hub.md`、`test/sdk-skill.test.js`、`test/helpers/{harness,fake-node}.js` **全部零 diff**；测试面 33 不变、helpers 2→3；④`API.md` 与 `src/web.js` 零 diff（漂移锁 `api-routes.test.js` 7/7 pass 佐证）；⑤scoped 回归：`hygiene` **3/3 pass**、`sdk-skill` **8/8 pass**，两文件零修改（`git status` 无 `M`）；⑥零新增依赖 + 4 文件 import 面检索通过；⑦零状态 / 零本地写复跑：`.runtime`/`data` 仍不存在、`git status --porcelain` 仅阶段产物 untracked、同一命令连跑两次输出一致；**两个互不相干的新进程并发**同命令 ⇒ `codes=[0,0]` 且 stdout 与单跑逐字节一致（无污染）；⑧零自动性 / 零残留进程：harness 只 SIGKILL（无重试/重连/退避/心跳循环的代码面）；超时路径进程组快照 **before `[]` → during 2（leader+grandchild，pgid 均 = leader pid）→ after `[]`**，端口释放、800ms 后复检仍 `0`；⑨提交卫生：`git log` 四条提交语义清晰；**仓库无任何启用中的 hook**（`core.hooksPath` 未设、`hooks/` 下只有 `*.sample`）⇒ `--no-verify` 无对象可绕；`59c3e59` 同提交含 `bin/hub.js` + `package.json` |

**标准 2 汇总**：**pass 42 / fail 0 / partial 0 / blocked 0**。

---

## 3. 逐项判定 · 标准 3（architecture §4.1/§4.2/§5.2/§4.4 + PRD 四卡，29 项）

| 标准 | 判定 | 证据 |
|---|---|---|
| §4.1 **N-1** `bin/hub.js` 形态逐字沿用 | **pass** | 归一化后与 `bin/oamp.js` 逐行 diff 唯二差异；`import { main } from '../sdk/cli.js'` + `process.exitCode = await main(process.argv.slice(2))` 逐字一致；可执行位 100755（§5 MI-2）；实测四态退出码 / 输出契约成立（§2-T3-5） |
| §4.1 **N-2** `sdk/index.js` 库入口 | **pass** | `createHub({port,socketPath})` → 恰四键 `{api,uds,cli,doctor}`；三层**原样**取自 `createSurface`（引用即返回值，未包装：`api` 21 方法树与 `ENTRIES` 逐条等值）；`doctor` 由本文件组合（`doctorCheck({port: ctx.port, ...opts})`）；`index.js` 内零端口缺省链字面量 |
| §4.1 **N-11** `test/helpers/hub-harness.js` | **pass** | 文件存在、导出 `runHub`、行为逐条实测（§2-T4）；`test/helpers/` 3 文件的计数与 N-11 预期一致。**附条件登记**：N-11 的先决条件"仅在前项确有复用时才建"在**本 PR 时点只有计划面支撑**（消费者 pr-007~pr-010 尚未落盘；PR 文件明文"创建条件由本迭代的四个用例满足"）—— 见 §6 偏差 #4 |
| §4.2 **M-1** `package.json` `bin` 只加一行 | **pass** | 逐键 deepEqual + diff 2 行（§1-#1）；`dependencies`/`engines`/`type`/`scripts` 逐字不变 |
| §5.2 库 API **规则 1**（命名空间 = 层、两面同源） | **pass** | 结构面 + 内存变异注入的双面同源实测（§1-#6 / §2-T5-1） |
| §5.2 **规则 2**（返回值原样：层 A 响应体 / 层 B `result` / 层 C `{exit_code,stdout,stderr}`；4 条订阅为异步可迭代） | **pass** | `api.agents` 与直连 HTTP `deepEqual`；`api.docs` 与 CLI stdout 解析全等；`cli.run(['status'])` 三键且与直跑逐字节一致；**四条 `kind:'stream'` 入口**（`stream.chat`/`stream.events`/`stream.calls`/`stream.call`）经库面调用均返回带 `Symbol.asyncIterator` 的对象（未加信封） |
| §5.2 **规则 3**（失败抛 `HubError`：`.code` / `.exitCode` / `.httpStatus` / `.upstream`） | **pass** | 不可达端口 ⇒ `HubError{code:'HUB_UNREACHABLE', exitCode:3, httpStatus:null, upstream:null}`（库面）；CLI 面同源归类表 ⇒ 退出码 `3`（§1-#5 / §2-T2-2） |
| §5.2 **规则 4**（无跨调用状态） | **pass** | `createHub` 每次新对象、引用全异、就地改动不污染、无模块级可变状态、零本地写、两进程并发互不影响（§1-#7 / §2-T5-7） |
| §5.2 **规则 5**（三宿主等价物：库面 `HubError.exitCode` ↔ CLI 进程退出码） | **pass** | 同一失败场景两面同码：HTTP 面 `api docs --port <未监听>` ⇒ CLI `3` / 库面 `exitCode 3`；库面成功返回值 ↔ CLI stdout JSON 结构全等 |
| §4.4 **顺序约束 6**（`package.json` bin 一行与 `bin/hub.js` 同一提交） | **pass** | `git show --name-only 59c3e59` 同时含两文件；且该文件路径可解析（`bin.hub` → 存在文件） |
| §4.3 Z-1~Z-7 零改动面 | **pass** | 见 §1-#11；`src`/`API.md`/`llms.txt`/`README.md`/`web`/既有 33 个 `*.test.js`/`roles`/`tools`/`.claude/skills` 全零 diff；已合并 6 个 sdk 模块 + `skill/hub.md` + `sdk-skill.test.js` 全零 diff；`.gitignore` 未动（`.runtime/` 规则仍在，hygiene 用例 pass） |
| **F01 验收 1**（包存在、落点 `oamp/`、暴露可执行入口） | **pass** | `oamp/sdk/` + `oamp/bin/hub.js` + `package.json` 的 `bin`（`oamp` 与 `hub` 两条，均可解析） |
| **F01 验收 2**（同一份代码两种消费方式） | **pass** | 内存变异注入双面同源实测（§1-#6） |
| **F01 验收 3**（库可被 import 消费、不经 shell、无手写 HTTP） | **pass** | 临时目录 `await import` + `createHub({port}).api.agents({state:'online'})` 一次真实调用返回结构化结果（与直连 HTTP 一致），过程无 shell、无手写 HTTP |
| **F01 验收 4**（跨宿主一致：①shell ②Node import ③另一 agent 经 shell） | **pass** | ①与③ stdout/stderr/退出码**逐字节一致**；②与① JSON 结构全等、退出码同（脚本以三处并列方式实测） |
| **F01 验收 5**（零依赖与运行约束沿用） | **pass** | `dependencies` 空、`engines.node` 沿用 `>=22` |
| **F09 验收 1**（跨调用不留可复用本地状态） | **pass** | 同一只读命令连跑两次各自独立取得结果、输出逐字节一致（14 组 × 真服务）；核验前后仓库内零新增文件；`createHub` 无缓存（两次 `check()` 返回对象引用不同） |
| **F09 验收 2**（续查靠重新调用、独立新进程即可） | **pass（本 PR 可判面）** | 每条命令均在**全新进程**中执行并各自取得完整结果（harness 的子进程 + 临时目录 cwd）；完整"两端续查"闭环需任务派发能力（pr-006+ 判据面，本 PR 无对应实现，亦无从否定） |
| **F09 验收 3**（并发执行互不影响） | **pass** | 两个互不相干的进程并发同命令 ⇒ 两边输出均为完整结果且与单跑一致（无半截、无串扰） |
| **F09 验收 4**（无"续订"式隐式状态） | **n/a（越界面）** | 订阅断开后不补发 / 不落断点属层 A 订阅语义与既有 `API.md` §1.2，判据面在 pr-006（`sdk-cli-contract` / 流）与 pr-001 的 `http.js`；本 PR 交付物内**无订阅实现**，不构成可判项 |
| **F13 验收 1**（模板存在且形态正确、无本机路径字面量） | **pass（承载文件属已合并 pr-005，本 PR 零 diff 冻结）** | `oamp/skill/hub.md` 零 diff；本次扫描其正文：含"项目根"模板、`/Users/` 与 `/home/` 零命中、无 `~`、无 `cd` 前置 |
| **F13 验收 2**（不依赖 cwd） | **pass** | 仓库根 / 系统临时目录 / 第二进程三处执行同命令 ⇒ 逐字节一致、`--help` 与 `api docs` 均正常 |
| **F13 验收 3**（模板不要求前置切目录） | **pass** | 四条命令在 cwd 于仓库外时全部成功，无任何 `cd` 前置；harness 的 `cwd: os.tmpdir()` 是这一性质的结构性佐证（每条用例默认都在仓库外跑） |
| **F13 验收 4**（不写死机器相关值） | **pass** | 4 个文件内 `/Users/`/`/home/`/硬编码临时路径**零命中**；包内路径全按 `import.meta.url` 推导（`hub.js`→`../sdk/cli.js`、harness→`../..`）；`skill/hub.md` 定位段零 `~`、零环境变量前置 |
| **F13 验收 5**（三宿主同一模板均成立且结果一致） | **pass** | 同 F01 验收 4 的三处并列实测（退出码与 JSON 结构一致）；库面入口 `<项目根>/oamp/sdk/index.js` 亦经临时目录 import 实测成功 |
| **G01 验收 1**（接口清单零变更） | **pass** | `oamp/API.md` 零 diff（逐字节文件未动）；运行侧 21 条 `routes[]` 与既有登记一致（漂移锁 7/7 pass） |
| **G01 验收 2**（Router 方法面零变更） | **pass** | `oamp/src/**` 全量零 diff（`router.js` 在内） |
| **G01 验收 3**（既有接口行为不变、既有测试面仍全绿） | **pass** | `src/**` 零 diff + 既有测试面零改动；scoped 两条 **3/3 + 8/8** 全绿、补充一条 `api-routes` **7/7** 全绿（按用户口径未跑仓库级全量） |
| **G01 验收 4**（服务边界声明不变） | **pass** | `API.md` 零 diff ⇒ 无启停路径、仅本机回环、零鉴权声明未变；`src/**` 零 diff ⇒ 声明与实现一致 |
| **G01 验收 5**（真源关系成立） | **pass** | `doctor.check({apiDocPath})` 实测：原文副本 `pass=true`、漂移副本 `pass=false` 且点名端点 ⇒"改 `API.md` ⇒ SDK 自检结论随之变化"成立；SDK 侧无第二份接口定义（`index.js` 零端点字面量、`surface.js` 消费 `API.md` 行序由 pr-003 保证） |

**标准 3 汇总**：**pass 28 / fail 0 / partial 0 / blocked 0 / n/a 1（F09 验收 4，越界面）**。

---

## 4. 跨 PR 冻结契约与补充裁决的独立复核（本 PR 对 pr-007~pr-010 的接缝）

| # | 冻结项 | 独立复核结论 | 证据 |
|---|---|---|---|
| A | `oamp/bin/hub.js` 为可执行入口 | **成立** | 存在 + `100755`（`git ls-files -s`）+ 端到端四态可跑 + 从仓库外可跑 |
| B | `runHub(args,{env,input,timeoutMs}) → {code,stdout,stderr}` | **成立** | 导出面 / 返回键 / 包根推导 / 14 组逐字节一致 / 未以换行结尾的尾部保真 / `input` 两态 + 早退子进程不抛 / `timeoutMs` 缺省 10004ms 与显式 2000ms 两态 / `env` 只给增量 |
| C | **进程组收口**（主 agent 2026-09-15 补充裁决：超时与结束都须杀透整树，吞 ESRCH） | **成立（超时路径实测）** | 精确 marker（`--port <唯一端口>` + `bin/hub.js`/`bin/oamp.js`）快照：**before `[]` → during 2（leader `pid=pgid` 自成一组的证据：`pgid` 列 == leader pid；grandchild 同 pgid）→ runHub 返回 `code=null`、耗时 2004ms → after `[]`**，端口**释放**，再等 800ms 复检仍 `0`；`detached:true` + `process.kill(-pid,'SIGKILL')` + `catch{}` 吞 ESRCH 均在代码面确认。**正常路径的组收口**由 `settle()` 内同一 `killGroup()` 承担（代码面确认）；本次未构造"父退出而孙存活"的自然命令（既有命令无此形态），故仅超时路径有行为证据 —— 见 §6 偏差 #5/#6 |
| D | `createHub({port})` 委托 `surface.js` 端口缺省链 | **成立** | `index.js` 内 `OAMP_WEB_PORT`/`7788` 零命中；三态实测（显式 `port` 打中、`OAMP_WEB_PORT` 打中、无 env 落 7788 且 7788 上确有活 hub 应答 21 条 routes） |
| E | `doctor` 由 `index.js` 组合、绑定 `ctx.port`、透传 `apiDocPath` | **成立** | `deepEqual` 直调 `doctorCheck({port: ctx.port})`；`apiDocPath` 注入两副本对照（pass/false + 点名端点）；显式 `opts.port` 可覆盖（`createHub({port:<未监听>})` ⇒ 抛 3，证明 port 来自本次装配） |
| F | `createHub` 公开选项面恰 `{port, socketPath}` | **成立** | 签名逐字 `({ port, socketPath } = {})`；`index.js` 内无 `env`/`stdio` 透传面（`createSurface({port, socketPath})` 只传两项） |
| G | `oamp/test/helpers/hub-harness.js` 零仓库内运行态 | **成立** | 零 fs 写 API、零运行态字面量、`cwd=os.tmpdir()`；核验前后 `oamp/.runtime`、`oamp/data` 仍不存在（`git status --porcelain` 仅阶段产物 untracked） |

**变异注入的还原证据（真源零触碰）**：注入只在 `mktemp -d` 副本内、以**内存**方式改 `ENTRIES` 条目；副本磁盘文件 sha256 与注入前**相同**；真源 7 个受关注文件（`sdk/surface.js`、`sdk/cli.js`、`sdk/index.js`、`bin/hub.js`、`test/helpers/hub-harness.js`、`package.json`、`test/helpers/harness.js`）**注入前后 sha256 全等**：`e5e4b500c956401b…` / `2a2fa4ae81dd08b5…` / `7f22000886542555…` / `9f6e47bcf7defd07…` / `e5d615b0bfbbbb00…` / `1dab9e016c2b1dc0…` / `674ae84448280855…`；`git diff --stat 453326c..HEAD -- oamp/sdk/surface.js oamp/sdk/cli.js` 为空。

---

## 5. 独立验证方法与环境（复现口）

| 脚本 / 手段 | 覆盖 | 落点 |
|---|---|---|
| 真服务端到端（真 Router + 真 `web start` 随机端口 + **绝对**临时 `OAMP_DB`） | §1-#3/#5/#6/#7、§2-T1/T2/T3、§3 F01/F09/F13/G01 的行为面 | `/tmp/pr004verify/e2e.mjs`（14 组结果见 `results.json`） |
| 14 组命令 ×（直接 spawn ‖ `runHub`）逐字节三方比对 | §2-T4-3/#10、§3 F09-3、§1-#11 补充 | `/tmp/pr004verify/bytes2.mjs`（`all_same: true`） |
| `NODE_OPTIONS=--require` 只读注入子进程去尾换行 | §2-T4-3 的"未以换行结尾的尾部" | `/tmp/pr004verify/stripnl.cjs` + `bytes2.mjs`（1718 = 1718 且不以 `\n` 结尾） |
| 进程组快照（`ps -eo pid,ppid,pgid,command`）+ 端口占用复检 | §4-C | `/tmp/pr004verify/mutate.mjs` |
| 内存变异注入（`mktemp -d` 副本）+ 真源 sha256 前后比对 + `git status` | §1-#6、§2-T5-1、§4 还原证据 | `/tmp/pr004verify/mutate.mjs` |
| 静态面扫描（字面量 / import / fs 写 API / 凭据词 / 模块级 `let`） | §1-#4/#7/#10、§2-T1-1、T2-4、T3-1、T4-7/8、§3 §5.2 | 内联 node -e 扫描（命令见本文各证据列） |
| scoped 既有用例 | §2-T5-5、§3 G01-3 | `node --test test/hygiene.test.js`（3/3）、`node --test test/sdk-skill.test.js`（8/8）、补充 `node --test test/api-routes.test.js`（7/7） |
| 库面命名空间枚举对比 `ENTRIES` | §2-T1-3、§3 §5.2 规则 1 | `/tmp/pr004verify/ns.mjs`（21 vs 21，`missing=[] extra=[]`） |
| `socketPath` × `doctor` 偏差探查 | §6 偏差 #1 | `/tmp/pr004verify/devprobe.mjs` |

**未做的事（口径申明）**：未跑仓库级全量 `node --test test/*.test.js`（用户 2026-09-15 指令）；未跑格式化 / lint（非本 PR 面）；未对任何产出物做修改；未读取任何执行过程产物（`pr-004-tasks.md` 仅作为**验收标准来源**使用，不作"执行者解释"）。

---

## 6. 偏差记录（规格 vs 实现 / 文档 vs 事实；**不影响逐项判定**）

| # | 规格 / 文档描述 | 实现实际行为（本次实测） | 建议处理 |
|---|---|---|---|
| 1 | §5.2：`createHub({ port, socketPath })` 的 `socketPath` 是 UDS 层选项；§5.5 的 `doctor.check({apiDocPath, port})` | `createHub({port:P, socketPath:<活 Router socket>}).doctor.check()` 在进程 env 无 `OAMP_SOCKET` 时**抛 `HubError{HUB_UNREACHABLE, exitCode:3}`**（显式 `socketPath` 已确认存在）—— R3 探针 `connect({})`（`doctor.js:160`）无 `socketPath` 入参，只走 `src/config.js` 的 env 链 | pr-010 的库面 doctor 用例必须经**进程 env**（或 `runHub` 的 env）设 `OAMP_SOCKET`；若希望 `socketPath` 对 doctor 生效，需先扩 `check({ socketPath })` 契约（属架构外决策）。**本 PR 无一条验收标准要求该联动**，故不构成未达成项 |
| 2 | §5.2 库面无 `env` 选项（MI-4：公开选项面恰 `{port, socketPath}`） | 库面的层 C（`hub.cli.run`）与 `doctor` 的 socket 解析都读**调用方进程**的 env（`spawn(..., {stdio})` 不传 `env`；`connect` 走 `process.env`）⇒ `createHub({port})` **不携带**任何 env。实测：库进程 env 未设 `OAMP_SOCKET` 时 `hub.cli.run(['status'])` 返回 `exit_code:1` 且 stderr 为既有 CLI 的 ENOENT 文案；设入进程 env 后即为 `exit_code:0` 且与直跑逐字节一致 | pr-008 / pr-009 的库面用例必须在**调用进程**设 `OAMP_SOCKET`/`OAMP_DB`（或在测试内 `Object.assign(process.env, …)`）；本 PR 未改 `surface.js`，该现象继承自 pr-003（其验证报告偏差 #6/#7 同源） |
| 3 | architecture §4.3 **Z-7** 记"既有 **32** 个测试文件" | base `453326c` 实测 `oamp/test/*.test.js` 已是 **33** 个（含已合并 pr-005 的 `sdk-skill.test.js`）；tasks A16 记 33 | 同步 Z-7 计数为 33（本 PR 未改变该计数：33 → 33） |
| 4 | §4.1 **N-11**："**仅在前项确有复用时才建**" | 本 PR 落盘时点，四个声明的消费者（pr-007~pr-010）尚不存在 ⇒ "确有复用"目前**只有计划面支撑**（PR 文件明文"创建条件由本迭代的四个用例满足"） | pr-007~pr-010 落盘后回填"实际消费证据"（各文件 `import { runHub }` 的行号），使 N-11 的先决条件由计划面转为事实面 |
| 5 | §4.1 N-1 / N-2 / N-11 的规模估算（~10 行 / ~60 行 / ~40 行） | 实际 **5 行 / 29 行 / 76 行**（harness 中约 20 行为契约注释） | 估算非契约，无需动作；若后续以"规模"作为评审信号，建议按实际回填 |
| 6 | §5 MI-1：`timeoutMs` 到限 ⇒ SIGKILL + 等退出后返回 | 实现以 `exit`（超时路径）与 `close`（正常路径）**两条路径**各自调用同一个 `settle()` ⇒ 同一调用可能先后命中两者（`clearTimeout` 幂等、`killGroup` 幂等、`resolve` 二次为 no-op）—— 返回值不受影响 | 可选加固：`settle` 加一次性守卫（`if (settled) return;`），或在注释登记"双路径幂等"。**非验收面** |
| 7 | §5 MI-1 与 §10：harness 收口子进程 | `detached: true` 使子进程自成进程组（本次实测 `pgid == leader pid`，收口有效）——**副产品**：若"调用方进程"被强杀，内核不会向该独立进程组传播信号，只有 `runHub` 自身的 `settle()` 会收口 | pr-007~pr-010 对长驻命令（`web start` / `router start` / 订阅）务必显式传 `timeoutMs`，以免用例进程被中断时留下 hub 树 |

**偏差小结**：7 条中 **0 条**影响任何一条验收判定；#1 / #2 是下游 PR 的用例写法约束（须经 env），#3 / #5 是文档口径滞后，#4 是先决条件的时点差，#6 / #7 是可选的稳健性加固。

---

## 7. 下一迭代候选

1. **`socketPath` × `doctor` 的口径对齐**（最高优先，影响 pr-010 用例写法）：要么明确"doctor 只认 env 链"（在 §5.5 / F11 写明），要么把 `socketPath` 引入 `check()` 入参面（先改契约再落下游断言）。
2. **规格同步批次**：§4.3 Z-7 计数 32 → 33；§4.1 N-1/N-2/N-11 的规模；N-11 的"实际复用证据"回填。
3. **harness 收口纪律**（pr-007~pr-010 用例约定）：长驻命令显式 `timeoutMs`；可选给 `settle` 加一次性守卫。
4. **继承 pr-003 的收口项**：`--mode block` 的 `--wait` 上限语义（pr-011 收口修复 PR）。
5. **`runHub` 的消费者契约固化**：pr-007~pr-010 落盘后，把"实际 import / 调用点"回填到 N-11 与各 PR 文件，避免"跨 PR 契约"只有单向声明。

---

## 8. 汇总

| 层 | 判定项 | pass | fail | partial | blocked | n/a |
|---|---|---|---|---|---|---|
| 标准 1（PR 验收 11 条） | 11 | **11** | 0 | 0 | 0 | 0 |
| 标准 2（T1~T5，42 条） | 42 | **42** | 0 | 0 | 0 | 0 |
| 标准 3（architecture + PRD，29 项） | 29 | **28** | 0 | 0 | 0 | 1（F09-4 越界面） |
| **合计** | **82** | **81** | **0** | **0** | **0** | **1** |

- **偏差记录：7 条**（0 条影响判定；3 条为下游用例写法约束 / 文档口径，其余为时点差与稳健性登记）。
- **零回归证据**：scoped 两条 `hygiene 3/3` + `sdk-skill 8/8` 全绿且零修改；补充 `api-routes 7/7`；`oamp/test/*.test.js` 33 → 33；已合并全部产物零 diff。按用户口径**未跑仓库级全量套件**。
- **改动面**：`git diff --name-status 453326c..HEAD` = 恰 4 项（3 新建 + `package.json` 追加一键），112 insertions / 1 deletion。
- **真源零触碰**：变异注入只在 `mktemp -d` 副本内以内存方式施加，7 个受关注文件 sha256 前后全等，`git status` 仅阶段产物 untracked。
- **未修改任何产出物**；本报告为本次验证的唯一写入（worktree 内绝对路径）。

## 9. 结论

**PASS**

- 依据：**82 项判定中 81 项 pass、0 项 fail、0 项 partial、0 项 blocked**（1 项 `n/a` 为越界面的 F09 验收 4 —— 订阅续订语义，本 PR 交付物内无订阅实现）。三条验证标准（PR 文件 11 条 / tasks T1~T5 42 条 / architecture + PRD 29 项）**逐条有可复查证据**，无一项以"基本通过"收场。
- **跨 PR 冻结契约全部独立复核成立**：`bin/hub.js`（存在 + `100755` + 端到端四态）、`runHub` 的签名与返回键、`env` 只给增量、`input` 两态、`timeoutMs`（缺省实测 10004ms / 显式 2000ms 且 `code===null`）、**进程组收口（before `[]` → during 2 → after `[]`、端口释放）**、`createHub` 的四键与 `doctor` 组合（绑定 `ctx.port`、透传 `apiDocPath`）、`createHub` 公开选项面恰 `{port, socketPath}`。
- **"两面同源"不是自述而是实测**：在副本内对 `ENTRIES` 的一条共享字段做内存变异，CLI 面与库面**同时**改观、旧名两面同时消失，且真源零改动（sha256 前后全等）。
- **改动面封闭**：4 个文件、零命中 `src/**` / `API.md` / `llms.txt` / `web/**` / 既有 `*.test.js` / 已合并 6 个 sdk 模块 / `skill/hub.md` / `helpers/{harness,fake-node}.js`。
- 主 agent 可直接据此判断：**本 PR 无需返工**；§6 的偏差 #1 / #2 需在 **pr-008 / pr-009 / pr-010 的用例编写时**遵守（库面 env 与 `socketPath`↛doctor 两条接缝约束），不阻塞本迭代关闭。
