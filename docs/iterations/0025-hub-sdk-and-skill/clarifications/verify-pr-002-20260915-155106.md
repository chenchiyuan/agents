# verify-pr-002 — 独立验证报告（阶段 5 · pr-002 Router UDS 通道）

**验证者身份**：模块级协议契约实测审查者 —— 兼"UDS/JSON-RPC 客户端实现审查者"与"错误归类表消费面审查者"两种视角。
判据：本 PR 的产出物是**无 CLI 入口的单一模块**（`oamp/sdk/uds.js`），验证标准同时来自「接口/层级契约文档」（`architecture.md` §5.1 层 B 表 / §5.4 归类表 / §4.1 N-5 / §2.3 接缝）与「可观测行为」（`prd/F03·F08·F09·G01·G02`）。判定核心不是"代码写得好不好"，而是**8 个方法的帧级行为与 4 类错误的归类落点**是否与文档逐字一致，且**在真 Router 上与直连协议的结果逐字段相同**。因此按"懂 NDJSON 分帧/请求-通知-反向请求语义 + 懂退出码归类表该如何被消费"的评审者身份判定。

**产出物**：

- `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0025-hub-sdk-and-skill/.pb-agents/worktrees/0025-pr-002-sdk-router-uds-channel/oamp/sdk/uds.js`（179 行）
- 实现提交 `847899d`（base `4be4661`）；`git diff --name-status 4be4661..HEAD` = `A oamp/sdk/uds.js`（唯一改动文件）
- 消费面（pr-001 已合并，本次仅读）：`.../oamp/sdk/errors.js`（`HubError` / `classify` / `serializeError`）

**验证标准来源**（主 agent 委托指定三份，逐条判定）：

1. `docs/iterations/0025-hub-sdk-and-skill/prs/pr-002-sdk-router-uds-channel.md`「验收标准」段（10 条）
2. `docs/iterations/0025-hub-sdk-and-skill/prs/pr-002-tasks.md` 逐任务验收标准（T1~T5，共 45 条）
3. `docs/iterations/0025-hub-sdk-and-skill/architecture.md` §5.1 层 B 8 条 / §5.4 错误与退出码 / §4.1 N-5，以及 `prd/{F03,F08,F09,G01,G02}*.md` 验收标准
   另：主 agent 在委托中显式登记的两项裁决（① `message.deliver` 恒回传输应答、不做自动 ack、命令面仍 8 方法；② `loadConfig(env).socketPath` + `connect()` 内惰性动态 import `config.js`，失败归 `CONFIG_ERROR`）与跨 PR 契约（`errors.js` 零 diff）按**生效基准**判定。

**验证日期**：2026-09-15

**独立性声明**：本次验证未使用任何执行过程上下文——未询问实现者任何设计意图，未把提交信息/注释中的"为什么"作为判定依据。全部判定来自源码实读 + 自建探针 + 真 Router 实测。**产出物未被修改**（仅新建本报告，落在本任务工作区的 `docs/**/clarifications/`）。本 PR 的 `prs/pr-002-tasks.md` 是阶段产物（验证标准来源），不被当作"执行者解释"。

---

## 0. 环境与实测手段（可复查）

| 项 | 值 |
|---|---|
| Node | `v22.15.0` |
| 真服务 | `node <包根>/bin/oamp.js router start`（经既有 `oamp/test/helpers/harness.js` 的 `startRouter`：`fs.mkdtempSync` 临时 socket + `OAMP_SOCKET`） |
| SDK 调用 | 临时脚本 `import` `oamp/sdk/uds.js`；脚本全部落在 `/tmp/pr002/`，**未写入 worktree** |
| 自建服务 | 裸 `net.createServer` 逐行记录 NDJSON 帧（`{conn, method, id, params}`）、按 `mode` 编排应答（`ok`/`silent`/`notfound`/`unregistered`/`destroy`）、可反向发 `message.deliver` 请求 |
| 确定性 ECONNREFUSED 构造 | `net.createServer().listen(path, 1)` 的子进程 + 父进程 `SIGSTOP`（不再 accept）+ 灌满 backlog ⇒ 后续 `connect` 得 `ECONNREFUSED`（脚本 I0） |
| 残留句柄检测 | `setTimeout(...,4000).unref()` 看门狗：**仅在另有句柄让事件循环存活时**才触发 ⇒ 触发即证明有残留句柄 |

**未采信任何自述**：所有结论均有下述脚本的原始输出为据；未引用实现者提供的说明、注释或提交信息作为判定依据。

### 0.1 判据脚本与实跑结果（全部在 `/tmp/pr002/`）

| 脚本 | 覆盖面 | 检查数 | 结果 |
|---|---|---|---|
| `a-protocol.mjs` | 自建 UDS 服务端逐帧：会话形态 / 8 方法映射 / params 原样 / notify 语义 / 身份合成 / deliver 应答 / 无自动 ack / 4 类错误归类 / 超时 / 幂等 close | 30 | **30 pass / 0 fail** |
| `b-e2e.mjs` | 真 Router：注册闭环 + 拓扑三步 + 消息往返 + 任务盘点 + 直连 `RpcPeer` 对照 + 无自动心跳/重连 | 21 | **21 pass / 0 fail** |
| `c-failure.mjs` | ENOENT / 非 socket 路径 / CONFIG_ERROR（3 态）/ 显式路径优先 / 停服→恢复免清理 / 零本地写 | 11 | **11 pass / 0 fail** |
| `f-address.mjs` | 惰性 import（坏配置下仅 import 不抛错）/ `OAMP_SOCKET` 生效 / 默认路径 = `<包根>/.runtime/router.sock` | 4 | **4 pass / 0 fail** |
| `g-deliver-nohook.mjs` | 未给 `onDeliver` 仍回应答 / 钩子非函数容错 / 非法 deliver 参数 / 零自动 ack | 4 | **4 pass / 0 fail** |
| `h-isolation.mjs` | 心跳后静置 5600ms 零未处理 rejection / 两并发会话独立 / close 一个不影响另一个 / 自然退出 | 5 | **5 pass / 0 fail** |
| `i-blackhole.mjs` | 黑洞构造有效性尝试（结论：本平台不可构造）+ 确定性 ECONNREFUSED 经 uds.js 的归类 | 5 | **5 pass / 0 fail** |
| `d-nettimeout.mjs` / `d2-pending-timeout.mjs` / `e-natural-exit.mjs` / `j-exotic.mjs` | 机制探针（`net.connect` 的 `timeout` 选项装载与清零 / 连接态超时事件 / 自然退出 / 异构路径类型） | — | 见 §7.7、§7.5 |
| 静态面 | import 集合 / 禁写禁定时器检索 / diff 封闭 / 计数 | — | 见 §7.6 |

合计 **80 项动态检查，0 fail**。

---

## 1. 判定一：PR 文件「验收标准」10 条

| # | 验收摘要 | 判定 | 证据（可定位） |
|---|---|---|---|
| 1 | `connect()` 返回会话，8 方法 1:1 + `close()`；帧经 `RpcPeer`、地址经 `loadConfig` | **pass** | 会话自有可调用方法集 = `["ack","close","deregister","heartbeat","register","send","status","taskGet","taskList"]`（脚本 A1；命令面恰 8，A1b）。静态：`uds.js:11-13` import 仅 `node:net` / `../src/rpc.js` / `./errors.js`，`config.js` 走 `uds.js:35` 的 `await import('../src/config.js')`；全文件零 `JSON.parse` / 零 `socket.write` / 零按 `\n` 切帧（§7.6） |
| 2 | 真 Router 上 8 方法各成功一次，返回值 = `result` 原对象；入参原样透传 | **pass** | 脚本 B16：`called=["agent.deregister","agent.heartbeat","agent.register","message.ack","message.send","router.status","router.task_get","router.task_list"]`（8/8）；B11：SDK 的 `status()` 与直连 `RpcPeer.request('router.status',{})` 结果 `deepEqual`；A5：`send` 的 params 含未声明字段逐键原样，未裁未补 |
| 3 | 注册闭环（库面）全程可完成，拓扑侧可见上线/消息到达/下线 | **pass** | 脚本 B1→B10：`register`→`heartbeat`→（`send` 得 `status:'delivered'`）→收件会话 `onDeliver` 收到 → 显式 `ack` 得 `{acked:true,status:'accepted'}`→`deregister` 得 `{removed:true}`；拓扑三步：注册后 `nodes` 含 `sdk-a`/`sdk-b`（B2）→ 注销后仅 `sdk-a`（B10） |
| 4 | 身份由连接承载；不代替调用方做身份编排 | **pass** | B19：短租约（300ms）下注册后静置 1.5s，节点被 Router sweep 判 `offline` 且连接断开、后续调用 `AGENT_OFFLINE` ⇒ **无自动心跳**；B20/B21：再观察 2.5s 该实例未回到 `online`、会话未自愈；A2：连接后静置 2000ms 服务端**零请求帧** ⇒ 无自动注册/编排 |
| 5 | 不可达：`ENOENT` → 3；连接建立超 2000ms 归 3；立即结束、不挂起、不重试 | **pass** | C1：`ENOENT` ⇒ `HubError{HUB_UNREACHABLE, exitCode:3, httpStatus:null, upstream:null}`，耗时 **2ms**；I0b/I1/I2：确定性 `ECONNREFUSED`（SIGSTOP+backlog 满）⇒ 同一归类，文案含真实 errno 与目标路径，耗时 **0ms**；C5：非 socket 路径（`ENOTSOCK`）⇒ 同归 3。2000ms 上限见 §7.7（实现面 + 机制实测；黑洞态本平台不可构造，见偏差 D-6） |
| 6 | JSON-RPC 错误：`data.code` 原样进 `.code`、退出码归 1；`-32601` 不被语义改写 | **pass** | A19/A22/B12/B13/B14/B15：`UNREGISTERED`→`.code='UNREGISTERED'`/1（真 Router 亦同）；`INVALID_PARAMS`→1（真 Router 两例）；`-32601`→`.code='-32601'`、`.message='method not found: router.status'` 逐字、`.exitCode=1`，未落 `UNREGISTERED`/`HUB_UNREACHABLE`/`REQUEST_TIMEOUT` |
| 7 | 无自动性（心跳循环 / 重连 / 重试 / 自动重派） | **pass** | B19/B20/B21（真协议面无自动心跳、无重连）；A26（超时后连接数不变、请求帧恰 1）；静态零 `setInterval` / 零 `reconnect` / 零 `setTimeout`（§7.6）；A2（连接后零帧） |
| 8 | `close()` 释放连接；除会话外无句柄；不写任何本地文件 | **pass** | A28（两次 `close()` 零异常）；A29（关闭后调用以 `HubError{AGENT_OFFLINE}` 明确失败、不挂起）；`e-natural-exit.mjs` **退出码 0**（4s unref 看门狗未触发 ⇒ 零残留句柄）；`h-isolation.mjs` 亦自然退出 0；B17/C11（临时目录仅 Router 自建 `router.sock`，停服后为空）；静态零 `node:fs` / 零 `.runtime` 字面量（§7.6） |
| 9 | 零新增依赖：仅 `node:net`（经 `src/rpc.js`）与既有模块 | **pass** | `git diff 4be4661..HEAD -- oamp/package.json` = **0 字节**；`package.json` 的 `dependencies` 仍为 `{}`；import 集合 = `node:net` + `../src/rpc.js` + `./errors.js`（+ 动态 `../src/config.js`） |
| 10 | diff 不含 `oamp/src/**` / `API.md` / `llms.txt` / `web/**`，不新增路由与方法 | **pass** | `git diff --name-status 4be4661..HEAD` = `A oamp/sdk/uds.js`（唯一）；`oamp/src/**` 零 diff ⇒ 8 个 `dispatch` 分支方法名逐字不变、无新增路由/方法 |

---

## 2. 判定二：`pr-002-tasks.md` 逐任务验收标准（T1~T5，45 条）

### T1（`connect()` / `close()` + 地址解析 + 连接失败归类，12 条）

| # | 判据 | 判定 | 证据 |
|---|---|---|---|
| 1 | import 集合 ⊆ `node:*` + `src/rpc.js` + `src/config.js` + `./errors.js`；`package.json` 零 diff | **pass** | `uds.js:11-13` + `:35` 动态 import；**不 import `./http.js`**；`git diff` 0 字节 |
| 2 | `connect()` 返回 Promise，会话方法名集合恰 9 项 | **pass** | A1/A1b |
| 3 | 地址解析复用（无第二处定义）：显式路径优先 / 缺省 `loadConfig` | **pass** | F2（`OAMP_SOCKET` 生效连真 Router）、F3（默认路径逐字符 = `<包根>/.runtime/router.sock`）、F3b（缺省连接报错文案指向该路径）、C9（显式路径 + 坏配置仍成功）；全文件零 `.runtime` 字面量 |
| 4 | 帧编解码复用 `RpcPeer`（零自建帧编解码） | **pass** | 静态零 `JSON.parse`/`socket.write`（§7.6）；B4/B5/B6/B11 真协议往返成立；A 组全部帧级断言均以"服务端收到合法 NDJSON 帧"为前提 |
| 5 | 不可达两态 ⇒ `HUB_UNREACHABLE`/3，文案可读、无堆栈 | **pass** | C1（`ENOENT`）、I1/I2（`ECONNREFUSED`）、C5（`ENOTSOCK`）；`serializeError` 键集合 `["code","error","exit_code"]`（C3） |
| 6 | 连接建立上限 2000ms；连通后定时器清除 | **pass** | `uds.js:15` 常量 `= 2000`；`:57-61` `timeout` → `destroy()` + reject，`:105-108` 归同一 `unreachableError`；`d-nettimeout.mjs`：`net.connect({path,timeout:2000})` 后 `socket.timeout === 2000`（连接前装载），`:63 socket.setTimeout(0)` 后 `socket.timeout === 0`；A27：连通后静置 2600ms 会话仍可用。**黑洞态本平台不可构造**（D-6） |
| 7 | 不挂起：两态 ≤3000ms 结束 | **pass** | C4（2ms）、I3（0ms） |
| 8 | 配置错误 ⇒ `CONFIG_ERROR`/1，不逃逸未归类异常 | **pass** | C6（坏 JSON）、C8（坏数值键）、C7（与 `HUB_UNREACHABLE` 可区分）；F1：坏配置下**仅 import** `uds.js` 不抛错（惰性） |
| 9 | 零重试/零重连：连接尝试恰一次 | **pass** | 静态零重试/退避/reconnect（§7.6）；A26（超时后连接数不变、帧数 = 1）；B19-B21（无自动重连）；代码面 `openSocket` 单次调用、无循环 |
| 10 | `close()` 幂等；关闭后进程自然退出 | **pass** | A28、A29；`e-natural-exit.mjs` exit 0（无 `process.exit()`/`unref()` 兜底） |
| 11 | 零本地写（无 `node:fs` 写 API/无 `.runtime`/无 `.gitignore`） | **pass** | 静态检索零命中（§7.6）；B17/C11 目录观测；diff 面单文件 |
| 12 | 零越界：本任务 diff 只含 `uds.js` | **pass** | `git diff --name-status` 单文件 |

### T2（8 方法 1:1 + `result` 原样，8 条）

| # | 判据 | 判定 | 证据 |
|---|---|---|---|
| 1 | 8 名逐条 1:1、总数恰 8、无第 9 个方法名 | **pass** | A3/A5/A6/A7/A8/A9/A11/A12 逐条方法名断言；A14：服务端收到的 method 集合恰 8 个 |
| 2 | 返回值 = `result` 原对象（不加信封/不改名/不裁剪） | **pass** | A4（`register` 6 字段）、B7（`{task}`）、B8（`{tasks}`）、B9（`{removed:true}`）、B6（`{acked:true,status}`）、B4（`{accepted,message_id,status,task_id}`）、B11（与直连 `RpcPeer` deepEqual） |
| 3 | 入参原样透传（含未声明字段不剔除） | **pass** | A5：`{message:{…,extra:'keep-me'}, undeclared:42}` 逐键往返一致；A8 |
| 4 | 单参数便捷形态 | **pass** | A3（`register('x-1')`→`{instance_id:'x-1'}`）、A6（`status()`→`{}`）、A7（`taskGet('task-1')`→`{task_id:'task-1'}`）、A8（`taskList({state})`） |
| 5 | 真 Router 8 方法各成功一次 | **pass** | B16（8/8 逐条可达，输出见 §1-2/§1-3）。取证路径 = 登记⑤ 的**路径①**：双 SDK 会话（收件会话带 `onDeliver`）+ 发件会话 `send` + 显式 `ack` |
| 6 | 会话即身份、不代替调用方编排（零 `setTimeout`/`setInterval`） | **pass** | A2（连接后静置 2000ms 零请求帧）；静态零定时器（§7.6）；B19（无自动心跳） |
| 7 | 单向 1:1：每调用恰 1 帧 | **pass** | A13：帧数 8 = 调用数 8（无重发/补偿/探测帧） |
| 8 | 零越界 | **pass** | 单文件 diff |

### T3（身份载体 / `heartbeat` 通知语义 / 接收面与闭环，9 条）

| # | 判据 | 判定 | 证据 |
|---|---|---|---|
| 1 | 身份来源与合成；会话值优先；`send`/`router.*` 不合成 | **pass** | A10（`heartbeat` params 注入会话 `instance_id`/`session_id`）、A11（调用方自报 `SPOOF` 被会话值覆盖）、A12（`deregister` 仅身份二字段）、A5/A6/A7/A8（`send`/`status`/`taskGet`/`taskList` 零身份字段） |
| 2 | `heartbeat` 走 `notify`：帧无 `id`、同步返回、不建 pending | **pass** | A9：`s.heartbeat()` 返回 `undefined`（同步），帧 = `{jsonrpc:'2.0',method:'agent.heartbeat',params:{…}}` 且**无 `id` 键**；如误用 `request` 会挂 5000ms（A24 已显示该超时行为） |
| 3 | 不挂起：心跳后无待决状态、零未处理 rejection、可自然退出 | **pass** | `h-isolation.mjs` H1：心跳后静置 5600ms `unhandled=0`、H2 会话仍可用；进程自然退出（exit 0） |
| 4 | 接收面：`message.deliver` 必答 `{received:true,message_id}`、**不做自动 ack** | **pass** | A15（应答逐字）、A16（钩子收到消息）、A17（deliver 后 1.5s 零新增 `message.ack` 帧）；G1（未给钩子仍应答）、G2（钩子非函数容错）、G4（零自动 ack）；B5/B6（真 Router 侧消息到达 + 显式 ack） |
| 5 | 注册闭环全程可完成（含一次真实消息往返） | **pass** | B1→B10 逐步无 `HubError`；零手写 socket / 零自建帧（脚本仅用 SDK 的会话 API） |
| 6 | 拓扑侧可见上线 / 消息到达 / 下线 | **pass** | B2（上线两条目）、B4（`status:'delivered'`）、B5+B6（消息到达并受理）、B9+B10（注销后拓扑不含） |
| 7 | 无自动心跳循环（单次方法，帧数 = 显式调用次数） | **pass** | A13/A14（每调用恰 1 帧）；B3（`last_heartbeat` 前进 = 被 Router 接受，证明合成后的 `session_id` 匹配而非静默忽略）；B19（不心跳即被收割）；静态零调度 |
| 8 | 身份不跨会话：两会话独立、close 一个不影响另一个 | **pass** | H3（两会话各自注册成功，拓扑两条目）、H4（`close(A)` 后 `B.status()` 正常且 `iso-b` 仍 `online`）；B18（全部 close 后新会话可用） |
| 9 | 零越界 | **pass** | 单文件 diff |

### T4（JSON-RPC 错误 → `HubError`，8 条）

| # | 判据 | 判定 | 证据 |
|---|---|---|---|
| 1 | `data.code` 原样进 `.code`、退出码 1、`.message` 原文 | **pass** | A19（`UNREGISTERED`，真 Router B12 同）、B13/B14（`INVALID_PARAMS` 真 Router 两例）、A23（`AGENT_OFFLINE`）；共 4 个不同 `data.code` 逐例通过（≥4 例） |
| 2 | `.upstream` 带 `error` 键 ⇒ `serializeError().error` 为字符串、键集合不含 `stack` | **pass** | A20：`upstream={error:'sender not registered',code:'UNREGISTERED'}`、`serializeError={code,error:'sender not registered',exit_code:1}`（`error` 非 `undefined`、无 `stack`）；A22 同构通过。**反例可抓性**：A20 断言 `typeof ser.error === 'string'`，直接塞 `{code,message,data}` 时会失败 |
| 3 | `-32601` 不被语义改写 | **pass** | A22（`.code='-32601'`、文案逐字、exitCode 1、未落三类具名码）。**取证手段说明**：库面只暴露 8 个具名方法（无原始 method 通道，`--params` 通道属 pr-003），故以"自建服务端回 `-32601` + 代码面（`uds.js:80-86` 仅做 `dataCode ?? String(code)`）"取证，见偏差 D-4 |
| 4 | 连接断开不被误判为响应超时 | **pass** | A23：服务端收到请求后 `destroy()` ⇒ `{code:'AGENT_OFFLINE', exitCode:1}`，`code !== 'REQUEST_TIMEOUT'` |
| 5 | `REQUEST_TIMEOUT` 与 `HUB_UNREACHABLE` 可区分（5000ms） | **pass** | A24：服务端不回帧 ⇒ `REQUEST_TIMEOUT`/3，耗时 **5001ms**（∈ (5000,6200)）；A25/A17 并列表明 `code`/文案与连接失败态不同 |
| 6 | 无重试/无重连（连接数与帧数 = 1） | **pass** | A26（连接数 2→2、该请求帧恰 1） |
| 7 | 错误对象形态：`.exitCode ∈ {1,3}`、无堆栈、`errors.js`/`http.js` 零 diff | **pass** | 全部错误例 `.exitCode` ∈ {1,3}（C1/C5/I1=3；A19/A22/A23/B12-B15=1）；`serializeError` 不含 `stack`；`git diff -- oamp/sdk/errors.js` = **0 字节**、`-- oamp/sdk/http.js` = 0 字节 |
| 8 | 零越界 | **pass** | 单文件 diff |

### T5（收口核验，8 条）

| # | 判据 | 判定 | 证据 |
|---|---|---|---|
| 1 | 真 Router 端到端 + 直连对照 + 拓扑快照 | **pass** | B1-B11、B16（脚本输出已含 8 方法对照与拓扑快照） |
| 2 | 不可达与恢复（① ENOENT ② 停服后连接失败；≤3000ms；无堆栈；重启后同一调用成功） | **pass** | C1（ENOENT/2ms）、C5、I1/I2（ECONNREFUSED/0ms）、C10（上线→停服 `HUB_UNREACHABLE`/3→重启→**同一调用成功**，无本地状态清理） |
| 3 | 无自动性复跑（失败后零新增连接；心跳帧数 = 显式次数；零 `setInterval`/退避/reconnect） | **pass** | A26、A13/A14、B3、B19-B21、静态检索（§7.6） |
| 4 | 改动面封闭（源码面只含 `uds.js`；零命中清单；测试面计数；`dispatch` 方法名不变；`git status` 为空） | **partial** | 子项 a（源码面只含 `uds.js`）= **pass**；b（零命中 `oamp/src/**`/`bin/**`/`package.json`/`API.md`/`llms.txt`/`web/**`/`test/**`/`roles/**`/`.claude/skills/**`/`tools/**`）= **pass**；c（测试文件计数零改动）= **pass**（实测 `oamp/test/*.test.js` = **33**，base 与 HEAD 同；**但该条写的是"仍 32 个"⇒ 判据数字有误，见 D-1**）；d（`git status --porcelain` 为空）= **fail**（唯一项 = 未跟踪的 `docs/iterations/0025-hub-sdk-and-skill/prs/pr-002-tasks.md`，即本阶段产物，该文档 §0.1 自身声明不计入 PR 改动面，见 D-2） |
| 5 | 零新增依赖 | **pass** | `package.json` 零 diff、`dependencies === {}`、import 面命中清单同 T1-1 |
| 6 | 零状态残留（零本地写、句柄为零、双跑一致） | **pass** | B17/C11（零写入）、`e-natural-exit.mjs`/`h-isolation.mjs` 自然退出 0、B18（全部 close 后新会话可用）；A/B/C 脚本多轮续跑结果一致 |
| 7 | 错误面四类可区分且无堆栈 | **pass** | 四类并列：`HUB_UNREACHABLE`/3（C1/C5/I1）、`REQUEST_TIMEOUT`/3（A24）、`CONFIG_ERROR`/1（C6/C8）、`UNREGISTERED`/1（A19/B12）；四类 `serializeError` 均无 `stack`、`error` 均为可读字符串 |
| 8 | 提交卫生（未使用 `--no-verify`） | **pass** | `git log --format=%B -1` 无 `no-verify` 痕迹；`git rev-parse --git-common-dir` 的 `hooks/` 只有 `*.sample`（**无生效钩子** ⇒ "钩子全过"为空真，事后不可观测，属判据固有限制） |

---

## 3. 判定三：`architecture.md`（§5.1 层 B / §5.4 / §4.1 N-5）

### §5.1 层 B 8 条（库调用名 ↔ Router 方法）

| # | 层 B 表条目 | 判定 | 证据 |
|---|---|---|---|
| 1 | `s.register(id)` ↔ `agent.register` | **pass** | A3/A14/B16 |
| 2 | `s.heartbeat(params)` ↔ `agent.heartbeat`（通知语义） | **pass** | A9/A10/B3/B19 |
| 3 | `s.deregister(...)` ↔ `agent.deregister` | **pass** | A12/C14→B15/B9 |
| 4 | `s.send(params)` ↔ `message.send` | **pass** | A5/B4 |
| 5 | `s.ack(params)` ↔ `message.ack` | **pass** | A11/B6 |
| 6 | `s.status()` ↔ `router.status` | **pass** | A6/B2/B11 |
| 7 | `s.taskGet(id)` ↔ `router.task_get` | **pass** | A7/B7 |
| 8 | `s.taskList(q)` ↔ `router.task_list` | **pass** | A8/B8 |

铁律"`--params` 是唯一入参通道、对象原样透传、SDK 不做字段级搬运"：A5/A8 + B11 佐证（`--params` 的 argv 面归 pr-003；本 PR 判"入参原样"）。"注册闭环两条路径"中的**库面路径**已实测（B1-B10），shell 面属 pr-003。

### §5.4 错误与退出码（本模块适用行）

| # | 条款 | 判定 | 证据 |
|---|---|---|---|
| 1 | `3` 类 ① 连接建立失败/超 2000ms | **pass** | C1/C5/I1（失败态）；常量与分支见 §7.7 |
| 2 | `3` 类 ② 非阻塞条目 5000ms 未响应 | **pass** | A24（5001ms、`REQUEST_TIMEOUT`/3） |
| 3 | `1` 类 ① 上游业务错误原样透出 | **pass** | A19/A22/B12-B15 |
| 4 | `1` 类 ③ 本地配置错误（`loadConfig` 抛错） | **pass** | C6/C8/C7 |
| 5 | 不可达文案：单一明确错误、无堆栈 | **pass** | C2（含路径 + "未运行？"提示）、C3（`serializeError` 无 `stack`） |
| 6 | 要点：`REQUEST_TIMEOUT` 与 `WAIT_TIMEOUT` 语义不同；`REQUEST_TIMEOUT` 落 `3` 且不与其他混淆 | **pass** | A24/A25（`REQUEST_TIMEOUT`/3 与 `HUB_UNREACHABLE`/3 文案与 `code` 皆不同）；`WAIT_TIMEOUT` 属层 A/pr-001，本模块不产出 |

### §4.1 N-5（`sdk/uds.js` 内容清单）

| # | 条款 | 判定 | 证据 |
|---|---|---|---|
| 1 | `connect()` → 8 个方法 + `close()` | **pass** | A1/A1b |
| 2 | 复用 `src/rpc.js`（不重写协议） | **pass** | T1-4 |
| 3 | 复用 `src/config.js`（不新增 socket 配置项） | **pass** | F2/F3/F3b + 零 `.runtime` 字面量 |

---

## 4. 判定四：`prd/` 卡验收标准（F03/F08/F09/G01/G02，22 条）

| 卡 | # | 验收摘要 | 判定 | 证据 / 说明 |
|---|---|---|---|---|
| F03 | 1 | 8 方法逐条可达、无遗漏、无自发新增（8 行对照清单） | **pass** | A14/B16 |
| F03 | 2 | 请求与响应原样透传（与直连协议比对一致） | **pass** | B11（`deepEqual`）；A5/A8 |
| F03 | 3 | 注册闭环可完成（含一次消息往返）+ 拓扑可见上线/到达/下线 | **pass** | B1-B10 |
| F03 | 4 | 拓扑数据可被盘点 | **pass** | B2/B7/B8/B10（`status()` 的 `nodes` + `taskGet`/`taskList`） |
| F08 | 1 | 停服后单一明确错误、不输出堆栈 | **pass** | C2/C3/I2 |
| F08 | 2 | 退出码固定 3、与 1/2 可区分 | **pass** | C1/C5/I1（=3）vs A19/B12（=1）；`2`（用法）归 pr-003，本模块不产出 |
| F08 | 3 | 不挂起、立即结束 | **pass** | C4（2ms）/I3（0ms）；A24 的 5000ms 是响应上限（另一条语义），非挂起 |
| F08 | 4 | 两条接入路径（Web 面 / UDS 面）都成立 | **partial** | UDS 面子项 = **pass**（C1/C5/I1/I2）；Web 面子项不在本 PR 判定面（`sdk/http.js` 属 pr-001，本 PR 零 diff 不触碰） |
| F08 | 5 | 恢复后无需额外动作即可工作 | **pass** | C10 |
| F09 | 1 | 跨调用不留可复用本地状态 | **pass** | B17/C11（零写入）、`e-natural-exit.mjs`（零句柄）、B18（新会话独立）；层 B 的可判面按 tasks.md 登记⑧ |
| F09 | 2 | 续查靠重新调用（两个互不相干的新进程） | **blocked** | 该条的判定面是 CLI/层 A 侧（跨进程续查），**本 PR 无任何可执行入口**（`bin/hub.js` 在 pr-004、`cli.js` 在 pr-003）⇒ 缺 CLI 面无法判定；层 B 的等价可判面（身份不跨会话）已由 T3-8 pass |
| F09 | 3 | 并发执行互不影响 | **blocked** | 同上：并发**进程**面属 pr-003/pr-004；本 PR 可判的"并发会话互不影响"已由 H3/H4 pass |
| F09 | 4 | 无"续订"式隐式状态 | **blocked** | 该条关于订阅断点续订，属层 A 订阅面（pr-001/pr-003），本模块无订阅能力 |
| G01 | 1 | 接口清单零变更（21 行逐条不变） | **pass** | `git diff` 未触碰 `API.md`/`web.js`/`llms.txt` ⇒ 清单逐字不变（零 diff 即逐条不变） |
| G01 | 2 | Router 8 方法名与语义不变 | **pass** | `oamp/src/**` 零 diff |
| G01 | 3 | 既有接口行为不变；既有测试面仍全绿 | **partial** | 子项 a（不改既有断言/测试文件）= **pass**（`oamp/test/**` 零 diff）；子项 b（全量套件全绿实测）= **blocked**（本次验证授权明确不跑仓库级全量测试；本 PR 纯新增文件、无既有模块被引用改动，构造上不影响既有断言） |
| G01 | 4 | 服务边界声明不变（无启停路径、仅本机） | **pass** | 零服务端改动；`uds.js` 仅新增客户端模块，未新增监听/路由/鉴权面 |
| G01 | 5 | 真源关系成立（SDK 侧不出现第二份接口定义） | **partial** | 子项 a（`uds.js` 内无端点表/schema，仅 8 行方法名常量） = **pass**；子项 b（"改 `API.md` → 自检结论随之变化"）= **blocked**（该判定依赖 `doctor`，属 pr-003 的 `doctor.js`） |
| G02 | 1 | HTTP 面仍无启停路径 | **pass** | 零服务端/接口面改动 ⇒ 清单与改动前逐条一致（与 G01-1/4 交叉成立） |
| G02 | 2 | 封装既有 CLI 命令零语义变更 | **blocked** | 层 C 子进程透传归 pr-003/pr-004；本 PR 无 CLI 入口 |
| G02 | 3 | 不新增生命周期能力（守护/自愈/自动重试/自动重派） | **pass** | B19/B20/B21（无自动心跳、无重连、不自愈）、A26（无重试）、静态零自动性构造 |
| G02 | 4 | 能力不因接入而被隐式转移 | **pass** | 零服务端改动 + 未新增命令面/路由；启停仍只由既有 CLI 承担 |

---

## 5. 重点独立复核（主 agent 指定 6 项）

| # | 复核项 | 结论 | 独立证据 |
|---|---|---|---|
| ① | 命令面是否**恰为 8 个方法**（第 9 个命令即违反 PR 验收 1） | **恰 8** | `Object.getOwnPropertyNames(session)` 的可调用项 = 9 项，其中 `close` 非命令 ⇒ 命令 8；服务端收到的 `method` 集合恰 8 个（A1b/A14） |
| ② | `message.deliver` 的应答行为与"无自动 ack" | **符合裁决①** | A15（恒回 `{received:true,message_id}`）、A16（钩子取消息）、A17（deliver 后 1.5s 零自动 ack）、G1（未给钩子仍应答）、G2（容错）、G4（零自动 ack）；B5/B6 真 Router 侧以"显式 `ack` 成功"取证 |
| ③ | 惰性动态 import 的 `CONFIG_ERROR` 映射（制造坏配置） | **符合裁决②** | F1（坏配置下仅 import 不抛错 ⇒ 解析惰性）；C6/C8（坏 JSON / 坏数值键 ⇒ `HubError{CONFIG_ERROR,1}`，无裸异常）；C7（与 `HUB_UNREACHABLE` 可区分）；C9（显式路径不读配置） |
| ④ | 退出码归类（失败行 ∈ {1,3}）与 `REQUEST_TIMEOUT`/`HUB_UNREACHABLE` 可区分 | **成立** | 失败例 `.exitCode` 取值仅 {1,3}（§2/T4-7）；A24 `REQUEST_TIMEOUT`/3（5001ms）vs C1 `HUB_UNREACHABLE`/3（2ms），`code` 与文案皆不同 |
| ⑤ | 零重试 / 零重连 / 零本地写 | **成立** | 静态零 `reconnect`/`setInterval`/`setTimeout`/`node:fs`（§7.6）；A26（连接数与帧数不变）；B19-B21（收割后 2.5s 未重连、会话未自愈）；B17/C11（目录观测零写入） |
| ⑥ | 改动面封闭（`package.json` 与 `errors.js` 零 diff） | **成立** | `git diff --name-status 4be4661..HEAD` = 单文件 `A oamp/sdk/uds.js`；`package.json` / `sdk/errors.js` / `sdk/http.js` diff 均 **0 字节**（唯一非 diff 项 = 未跟踪的阶段产物 `prs/pr-002-tasks.md`，见 D-2） |

---

## 6. 汇总（判定计数）

| 组 | 条目数 | pass | fail | partial | blocked |
|---|---|---|---|---|---|
| PR 文件验收 10 条 | 10 | 10 | 0 | 0 | 0 |
| `pr-002-tasks.md` T1~T5 | 45 | 44 | 0 | 1 | 0 |
| `architecture.md` §5.1/§5.4/§4.1 N-5 | 17 | 17 | 0 | 0 | 0 |
| `prd/` F03·F08·F09·G01·G02 | 22 | 15 | 0 | 3 | 4 |
| **合计** | **94** | **86** | **0** | **4** | **4** |

- **fail = 0**。
- 4 项 partial 全部已拆到子项粒度：**属于本 PR 面的子项全部 pass**，不通过/不可判的子项分别是 `git status` 含阶段产物（T5-4d）、Web 面（F08-4）、全量测试实测（G01-3b）、doctor 判定面（G01-5b）。
- 4 项 blocked 全部是**条目跨 PR 划界**（层 C CLI、层 A Web/订阅、doctor），本 PR 无对应入口 ⇒ 缺可执行面，无法客观判定；每项都已给出层 B 的等价可判面并通过。

---

## 7. 关键证据原文（可复查）

### 7.1 改动面

```
$ git -C <worktree> diff --name-status 4be4661..HEAD
A	oamp/sdk/uds.js
$ git -C <worktree> diff --stat 4be4661..HEAD
 oamp/sdk/uds.js | 179 ++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 179 insertions(+)
$ git -C <worktree> diff 4be4661..HEAD -- oamp/package.json | wc -c      → 0
$ git -C <worktree> diff 4be4661..HEAD -- oamp/sdk/errors.js | wc -c     → 0
$ git -C <worktree> diff 4be4661..HEAD -- oamp/sdk/http.js   | wc -c     → 0
$ git -C <worktree> status --porcelain
?? docs/iterations/0025-hub-sdk-and-skill/prs/pr-002-tasks.md
```

### 7.2 import 面与静态检索（`oamp/sdk/uds.js`）

```
uds.js:11  import net from 'node:net';
uds.js:12  import { RpcPeer, RpcError, ERR } from '../src/rpc.js';
uds.js:13  import { HubError, classify } from './errors.js';
uds.js:35  const { loadConfig } = await import('../src/config.js');   // 惰性动态 import
```

检索 `setTimeout|setInterval|reconnect|writeFile|appendFile|createWriteStream|mkdir|\.runtime|JSON\.parse|\.write\(|process\.exit|unref|node:fs|readFile`
⇒ 仅命中 `uds.js:48/63` 的 `socket.setTimeout(...)`（**socket 自身的 idle timeout 设定，非 JS 定时器**）。`setInterval` / `reconnect` / `node:fs` / `.runtime` / `JSON.parse` / `socket.write` / `process.exit` / `unref` **零命中**。

### 7.3 8 方法逐条映射（自建服务端逐帧）

```
agent.register    {instance_id:"x-1"}
message.send      {message:{message_id:"m-1",extra:"keep-me"}, undeclared:42}
router.status     {}
router.task_get   {task_id:"task-1"}
router.task_list  {state:"completed"}
agent.heartbeat   {next_interval_ms:10000, instance_id:"sdk-1", session_id:"sess-1"}   ← 帧无 id（notify）
message.ack       {message_id:"m-1", status:"accepted", instance_id:"sdk-1", session_id:"sess-1"}  ← 自报 SPOOF 被覆盖
agent.deregister  {instance_id:"sdk-1", session_id:"sess-1"}
帧数 = 8 = 调用数；method 集合恰 8 个
```

### 7.4 错误归类四类（并列）

```
HUB_UNREACHABLE/3  message="无法连接 oamp router（socket=<tmp>/nope/router.sock，ENOENT；router 未运行？先执行 oamp router start）"  耗时 2ms
HUB_UNREACHABLE/3  message="…（socket=<tmp>/router.sock，ECONNREFUSED；…）"  ← SIGSTOP+backlog 满构造  耗时 0ms
REQUEST_TIMEOUT/3  message="request timeout (router.status)"  耗时 5001ms
CONFIG_ERROR/1     message="OAMP 配置错误: 配置文件非合法 JSON <tmp>/config.json（…）"
UNREGISTERED/1     message="sender not registered"  upstream={error:"sender not registered",code:"UNREGISTERED"}
'-32601'/1         message="method not found: router.status"（逐字未改写）
serializeError 键集合 = ["code","error","exit_code"]（无 stack）
```

### 7.5 接收面与零自动 ack

```
服务端发: {"id":"srv-deliver-1","method":"message.deliver","params":{"message":{"message_id":"m-d1",…}}}
客户端答: {"jsonrpc":"2.0","id":"srv-deliver-1","result":{"received":true,"message_id":"m-d1"}}
钩子收到: [{"message_id":"m-d1","type":"notice","payload":{…}}]
deliver 后 1.5s 新增帧 = 1（即该应答本身），message.ack 帧数 = 基线（0 新增）
未给 onDeliver: 仍应答 {"received":true,"message_id":"m-1"}
非法参数: {"error":{"code":-32602,"message":"invalid deliver params","data":{"code":"INVALID_MESSAGE"}}}
```

### 7.6 零自动性 / 零残留（真协议面）

```
B19 短租约(300ms) 注册后静置 1.5s：节点 online → offline，连接断开，后续调用 AGENT_OFFLINE
B20 再 1s：未回到 online，会话仍 AGENT_OFFLINE
B21 再 1.5s（总 2.5s）：同上 ⇒ 无自动心跳、无自动重连、无自愈
A26 请求超时场景：连接数 2→2（未新增连接），该请求帧恰 1
e-natural-exit.mjs：connect + 调用 + close + 服务端关停 ⇒ 进程自然退出（exit 0；4s unref 看门狗未触发）
h-isolation.mjs：心跳后静置 5600ms ⇒ unhandledRejection = 0，会话仍可用，进程自然退出 0
```

### 7.7 2000ms 连接上限（机制实测 + 平台限制）

```
$ node d-nettimeout.mjs
socket.timeout = 2000            ← net.connect({path,timeout:2000}) 在连接前装载上限
s2.timeout after setTimeout(0) = 0 / _idleTimeout = undefined
s2 idle timeout 触发次数 = 0      ← 连通后清零，不误杀空闲会话
$ node i-blackhole.mjs           ← macOS UDS 黑洞构造尝试（SIGSTOP + backlog=1）
灌队列连接状态 = connected
原生探针（第二条连接）状态 = error:ECONNREFUSED   ⇒ 内核直接拒绝而非挂起
$ node j-exotic.mjs              ← FIFO/目录/普通文件/dev/null 均为 ENOTSOCK|ENOENT（无挂起态）
```

⇒ 本平台**无法**构造"连接建立被黑洞"，因此该分支只能以"实现面逐条核对 + 机制实测"判定（与 tasks.md §5 登记⑨ 一致）：`uds.js:15` 常量 2000 / `:57-61` `timeout`→`destroy()`+reject / `:105-108` 统一归 `HUB_UNREACHABLE`/3 / `:63` `setTimeout(0)` 清零 / A27 空闲 2600ms 仍可用。

---

## 8. 偏差记录（规格/文档描述 ↔ 实现实际；**不影响判定**）

| # | 规格·文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | `pr-002-tasks.md` A12 与 T5 验收 4：「`oamp/test/*.test.js` 仍 **32** 个」 | 实测 **33** 个，且 base `4be4661` 亦为 33（`git ls-tree 4be4661 oamp/test/` = 33 个测试文件 + `helpers/`） | 修正 tasks.md 计数为 33；该条实质要求（本 PR 零测试面改动）已满足 |
| D-2 | T5 验收 4：「`git status --porcelain` 为空」 | 实测非空：`?? docs/iterations/0025-hub-sdk-and-skill/prs/pr-002-tasks.md`（本阶段产物，tasks.md §0.1 自身声明"不计入 PR 改动面"） | 把判据改为「除阶段产物外为空」或将该产物随提交入库 |
| D-3 | `architecture.md` §5.4 不可达文案样例：`无法连接 hub（<地址>；服务未运行？）` | 层 B 实际文案：`无法连接 oamp router（socket=<路径>，<errno>；router 未运行？先执行 oamp router start）`（含 errno，措辞与样例不同） | 由 pr-003 的渲染层统一层 A/层 B 文案，或在 §5.4 标注"样例仅示意"；不影响 F08 验收 1（单一明确错误 + 无堆栈） |
| D-4 | T4 验收 3 判据：「对真 Router 发**不存在的方法**」 | 库面只暴露 8 个具名方法，无原始 `method` 通道（`--params` 通道属 pr-003）⇒ 该判据在本 PR 无法按字面执行；本次以"自建服务端回 `-32601` + 代码面（`uds.js:80-86` 仅 `dataCode ?? String(code)`）"取证 | 把该条的取证面移到 pr-003 的 `hub uds` 通道，或在本 PR 明确取证替代手段 |
| D-5 | T1 验收 5：「socket 文件在但无人监听（`ECONNREFUSED`）」 | 实测该表述不唯一：普通文件在路径上得 **`ENOTSOCK`**；SIGSTOP+backlog 满得 **`ECONNREFUSED`** | 判据改为「任意 errno 的连接建立失败」；两态均被 `uds.js:105-112` 的无条件 catch 归 `HUB_UNREACHABLE`/3，判定不受影响 |
| D-6 | T1 验收 6：依赖"连接建立被黑洞"的可复现性 | 本平台**不可确定性构造**（SIGSTOP+backlog 满 ⇒ 内核回 `ECONNREFUSED` 不挂起；FIFO/目录/普通文件/`/dev/null` 亦快速失败）⇒ 2000ms 分支不可动态触发（与 tasks.md 登记⑨ 一致，非实现缺陷） | pr-008 用可复现手段固化（桩化 `net.connect` 或断言常量+分支路径），或正式登记"平台不可复现" |
| D-7 | tasks.md §0.1 规模估计：`oamp/sdk/uds.js` **~120 行** | 实际 **179 行**（+49%） | 更新规模估计（仅登记，非契约） |
| D-8 | tasks.md §0.4 契约 2：「**全部**失败路径构造 `HubError`」 | `toHubError`（`uds.js:75`）对非 `RpcError` **原样 rethrow**；经码读，`peer.request` 的全部拒绝路径（closed / timeout / error 响应 / 写失败）均产出 `RpcError` ⇒ 该分支实际不可达 | 把该句收敛为"全部**可达**失败路径"，或将该分支也包装为 `HubError`（低优先） |
| D-9 | 主 agent 裁决② 的 CONFIG_ERROR 面 | 实现满足；但 `resolveSocketPath` 只在**未给显式 `socketPath`** 时解析配置（C9 实测）⇒ 若 pr-003 恒传显式 `socketPath`，层 B 的 `CONFIG_ERROR` 将只在库面直连调用者处可达 | pr-003 接线时明确"谁负责解析地址"，避免该归类形同虚设 |

---

## 9. 下一迭代候选

1. **pr-008（`test/sdk-uds.test.js`）** 固化本次已实测的行为：8 方法逐条映射与 `params` 原样（自建 UDS 服务端逐帧断言）；`message.deliver` 恒回传输应答 + **零自动 ack** + 未给钩子时仍应答；`close()` 幂等 + 自然退出（unref 看门狗手法可直接复用）。
2. **pr-008** 为 `upstream.error` 加**反例断言**：把 `{code,message,data}` 原样塞进 `.upstream` 时必须被 `typeof serializeError(err).error === 'string'` 抓到（本次 A20 已证明该断言具备可抓性）。
3. **pr-008** 决定 2000ms 连接上限的固化方式（D-6）：桩化 `net.connect` / 只断言常量与分支，或正式登记平台不可复现。
4. **pr-003 / pr-004**：层 B 的 CLI 面（`hub uds <method> --params --as`）、`HubError.exitCode → process.exitCode` 落点、层 A/层 B 不可达文案统一（D-3）、`CONFIG_ERROR` 归属（D-9）。
5. **pr-003（doctor R3）**：按 §5.1 层 B 表逐行接线 8 方法探测面；注意 `agent.register` 的探针期望是"`INVALID_PARAMS` 先于副作用"，`agent.heartbeat` 是"静默"。
6. **迭代文档**：修正 tasks.md 的 32→33 计数（D-1）、`git status` 判据（D-2）、~120 行估计（D-7）、`ECONNREFUSED` 表述（D-5）。

---

## 10. 结论

**PASS**

- 三份验证标准共 **94 条**判定：**pass 86 ｜ fail 0 ｜ partial 4 ｜ blocked 4**。
- **fail = 0** ⇒ 无需返工。
- 4 项 partial **全部**是"条目跨 PR 划界或判据技术性"所致，逐条已拆到子项粒度并给出证据：**属于本 PR 面的子项全部 pass**（其中 T5-4d 的 `git status` 子项由本阶段未跟踪产物触发，非代码缺陷）。
- 4 项 blocked 全部是跨 PR 面（层 C CLI / 层 A Web 与订阅 / doctor），本 PR 无对应可执行入口，缺此无法客观判定；每项都给出了层 B 的等价可判面并通过。
- 偏差记录 **9 条**（均不阻塞交付：3 条文档数字/判据面、3 条平台可复现性、1 条文案样例、1 条规模估计、1 条不可达分支表述）。
- 下一迭代候选 **6 条**。
- 重点独立复核结论（不采信任何自述，80 项动态检查 0 fail）：
  - **命令面恰 8 个方法**（第 9 个命令不存在）；服务端收到的 `method` 集合恰 8 个。
  - **`message.deliver` 恒回传输应答 `{received:true,message_id}`，零自动 ack**（deliver 后 1.5s 无新增 `message.ack` 帧；未给钩子时仍应答）。
  - **惰性动态 import 的 `CONFIG_ERROR` 映射成立**：坏配置下仅 import `uds.js` 不抛错；`connect()` 归类为 `HubError{CONFIG_ERROR,1}`，无裸异常逃逸；显式 `socketPath` 时不读配置。
  - **退出码归类**：失败例 `.exitCode` 恒 ∈ {1,3}；`REQUEST_TIMEOUT`/3（实测 5001ms）与 `HUB_UNREACHABLE`/3（ENOENT 2ms / ECONNREFUSED 0ms）`code` 与文案皆不同。
  - **零重试 / 零重连 / 零本地写**：短租约下静置被收割后 2.5s 未重连、会话未自愈；超时场景连接数与帧数不变；临时目录零新增文件；进程自然退出（无 `process.exit`/`unref` 兜底）。
  - **改动面封闭**：唯一改动文件 = `oamp/sdk/uds.js`；`oamp/package.json`、`oamp/sdk/errors.js`、`oamp/sdk/http.js` 及 `oamp/src/**` 全部零 diff；真 Router 上 8 方法逐条可达，且 SDK 结果与直连 `RpcPeer` 结果 `deepEqual`。
