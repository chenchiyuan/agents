# pr-011 第 2 轮独立验收报告（verifier · 从零复核）

**报告路径（绝对）**: `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0025-hub-sdk-and-skill/.pb-agents/worktrees/0025-pr-011-closeout-wait-limit-and-assertion-strength/docs/iterations/0025-hub-sdk-and-skill/clarifications/verify-pr-011-r2-20260915-193303.md`
**日期**: 2026-09-15 19:33（本地）｜ **迭代**: 0025-hub-sdk-and-skill ｜ **阶段**: 5（PR 实现）· 收口 · pr-011 第 2 轮
**结论**: **PASS**（fail 0 ｜ partial 0 ｜ blocked 0 ｜ 偏差记录 3 条，均为低severity/流程项，无一与实现行为矛盾）

---

## 1. 验证者身份（反射）

产出物类型 = **修复类产出物**（一处功能缺陷 + 三处断言判别力）+ **记录一致性**。两层身份、两套判据：

| 层 | 判据 | 判定方式 |
|---|---|---|
| 行为层 | PR 文件「验收标准」+ tasks T1~T6 + `architecture §5.4/§3.3 P-1/§5.1` + `prd/{F10,F07,F02,F06,F09,F04}` | 自建桩实跑（CLI 面 + 库面）+ scoped 套件两轮 + 变异注入对照 |
| 记录层 | PR 文件（同步版）与 `prs/pr-011-tasks.md` 的范围/口径陈述 vs 代码实际行为 | 逐条文本↔符号/行为比对（不看断言即止，看口径陈述） |

**独立性声明**: 本报告只依据产出物与验证标准；未接收执行过程上下文（本任务简报未提供"如何修复"的信息，我亦未向 dev/planner 索取）。**未修改被验证产出物**：`oamp/**` 全程只读（§6 给出零触碰证据）。

## 2. 产出物与标准来源

| 项 | 值 |
|---|---|
| 工作区 | `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0025-hub-sdk-and-skill/.pb-agents/worktrees/0025-pr-011-closeout-wait-limit-and-assertion-strength` |
| 分支 / base / HEAD | `feat/0025-pr-011-closeout-wait-limit-and-assertion-strength` ｜ base `1d934b7` ｜ HEAD `62cac20`（实现 `eb2e845` 首轮 + `62cac20` 第 2 轮追加） |
| 环境 | Node `v22.15.0`；`oamp/package.json` `dependencies = {}`、`scripts.test = "node --test test/*.test.js"` |
| 产出文件（5，全为修改） | `oamp/sdk/http.js`、`oamp/sdk/surface.js`、`oamp/test/sdk-cli-contract.test.js`、`oamp/test/sdk-api.test.js`、`oamp/test/sdk-uds.test.js` |
| 文件 hash（实验后，未变） | `http.js ca508a53d57fbe99770af25e2d8f9d55` ｜ `surface.js cfa362cc3d94e0aabe3bb3114a9e82de` ｜ `sdk-cli-contract.test.js aa2e46c6c9df4271a48c466f23db4662` ｜ `sdk-api.test.js ad393d6785e4ebbe44e8a4236567bb43` ｜ `sdk-uds.test.js c8fc40000adae41dd31adb0bde3e82e1` |
| 标准 ① | PR 文件「验收标准」段 —— **同步版**在迭代分支提交 `4c5f597`（迭代主工作区 `.pb-agents/worktrees/0025-hub-sdk-and-skill/docs/.../prs/pr-011-closeout-wait-limit-and-assertion-strength.md`，`1d934b7..4c5f597` = 8 insertions / 8 deletions）；PR 工作区内的同名文件仍为 base 文本（见偏差 D-r2-02） |
| 标准 ② | `docs/.../prs/pr-011-tasks.md`（PR 工作区内，未跟踪；含 D1/D4/D8 裁决与本轮回写） |
| 标准 ③ | `docs/.../architecture.md` §3.3 P-1、§5.1、§5.4（四类归类表 +「可阻塞条目恒有上限（默认 1800000）／非可阻塞条目走 5000ms」+「三条上限语义不同、不混淆」）、`prd/{F10,F07,F02,F06,F09,F04}` |

**派生规则（判定基准，按主 agent 已生效裁决）**: `mode === 'block'` ⇒ 响应头上限 = `(waitMs ?? DEFAULT_WAIT_MS) + 1s 余量`（`DEFAULT_WAIT_MS = 1800000`）；其余 mode（含省略）⇒ 5000ms 不放大。

## 3. 验证动作（可复现命令）

```bash
# ① 本人独跑的两轮 scoped（>=2 次，均 96/96）
cd <worktree>/oamp && node --test test/sdk-cli-contract.test.js test/sdk-api.test.js \
  test/sdk-uds.test.js test/sdk-surface.test.js test/sdk-skill.test.js \
  test/sdk-doctor.test.js test/hygiene.test.js
# ② 补充锁（T6 验收 4 列出的另两个文件）
cd <worktree>/oamp && node --test test/api-routes.test.js ; node --test test/cli.test.js
# ③ 自建桩两宿主对照（独立于交付物用例，15 例；脚本落 /tmp/pr011-r2.wAN3NK/probe.mjs）
node /tmp/pr011-r2.wAN3NK/probe.mjs
# ④ 变异注入对照（全部在 /tmp/pr011-r2.wAN3NK/copies/* 副本内，真源零触碰）
```

## 4. 逐项判定（含证据）

### 4.1 行为面（12 条 · 全部 pass）—— 证据 = 自建桩实跑（`probe.mjs`，非交付物用例）

| # | 判据 | 实测 | 判定 |
|---|---|---|---|
| P-01 | `--mode block --wait 8000` × 黑障 | exit `1` ｜ `WAIT_TIMEOUT`/`exit_code 1` ｜ `error="等待超时（8000ms）：调用仍在进行"` ｜ `elapsedMs 8045` ｜ stdout `""` ｜ `requests 1` | **pass** |
| P-02 | `--mode block --wait 8000` × **6000ms 桩** | exit `0` ｜ stdout = 桩 body 原样 `{"calls":[{"call_id":"stub-6000",…}]}` ｜ `6049ms` ｜ `requests 1`（旧实现此处 `5042ms → 3/REQUEST_TIMEOUT`） | **pass** |
| P-03 | `--mode block` **不给** `--wait` × 6000ms 桩 | exit `0` ｜ body 原样 ｜ `6044ms`（缺省 1800000 的界内可测面） | **pass** |
| P-04 | `--wait 600` / `1500` × 黑障（<5000ms 段不回归） | 均 `WAIT_TIMEOUT`/`1`，`error` 分别含 `600` / `1500` ｜ `643ms` / `1543ms`（单调）｜ 各 `requests 1` | **pass** |
| P-05 | `--mode background --wait 8000` × 黑障（反向回归） | exit `3` ｜ `REQUEST_TIMEOUT` ｜ `error="等待响应超时（5000ms；…）"` ｜ `5043ms` ｜ `requests 1` | **pass** |
| P-06 | **省略** `mode` + `--wait 8000` × 黑障（反向回归） | exit `3` ｜ `REQUEST_TIMEOUT` ｜ 含 `5000` ｜ `5042ms` ｜ `requests 1` | **pass** |
| P-07 | `api docs` × 黑障（非 `--wait` 条目） | exit `3` ｜ `REQUEST_TIMEOUT` ｜ 含 `5000` ｜ `5043ms` | **pass** |
| P-08 | 上游业务错误优先（`--mode block --wait 8000` 下 404/400） | 404 ⇒ `NOT_FOUND`/`1`/`http_status 404`/`error` 上游原文/`44ms`；400 例见套件 ok 30 | **pass** |
| P-09 | **库面 D8**：`createHub({port}).api.calls.create({…, mode:'block'})` **只传 flags 槽、不传第二槽** × 6000ms 桩 | `exitCode 0` ｜ `body` 与桩深等 ｜ `6005ms`（**不落回 5000ms**；桩延时 6000 > 5000 故可判） | **pass** |
| P-10 | 库面显式 `{ waitMs: 8000 }`：6000ms 桩 / 黑障 | `0` / `6003ms`；`WAIT_TIMEOUT`/`1`/含 `8000`/`8003ms` | **pass** |
| P-11 | 库面反向回归：`mode:'background'`、省略 `mode`、`api.docs` × 黑障 | 三者均 `REQUEST_TIMEOUT`/`3`、`error` 含 `5000`、`5002ms` | **pass** |
| P-12 | 常量同值静态比对用例存在且**真能红** | `oamp/test/sdk-cli-contract.test.js:1302`「T9b · 常量同值…」：`readFileSync` + 正则取 `cli.js` 的 `DEFAULT_WAIT_MS` 与 `surface.js` 的 `BLOCK_WAIT_DEFAULT_MS`，**零 import、零运行时耦合**；注入 `1800000→1799999` ⇒ `not ok 1 - T9b …`「库面缺省 1799999 与 CLI 面缺省 1800000 必须同值」（`# tests 1 / # fail 1`）；`cli.js` 本 PR 零 diff | **pass** |

### 4.2 结构性判据（6 条 · 全部 pass）

| # | 判据 | 实测证据 | 判定 |
|---|---|---|---|
| S-01 | 单点派生 + mode 判定不下沉 | 全仓 `headerTimeoutMs` 的**派生点恰 1** = `oamp/sdk/surface.js:91-92`：`flags.mode === 'block' ? (waitMs ?? BLOCK_WAIT_DEFAULT_MS) + HEADER_TIMEOUT_MARGIN_MS : undefined`；`oamp/sdk/http.js` 只**读**：`:74 const headerTimeoutMs = spec.headerTimeoutMs ?? RESPONSE_TIMEOUT_MS`；**`grep -c mode oamp/sdk/http.js` = `0`** | **pass** |
| S-02 | 两个读取点同源（文案与实际生效值一致） | `http.js:98` 定时器回调 `responseTimeout(target, headerTimeoutMs)` 与 `:99` `}, headerTimeoutMs)` 同用局部值；实测 `--mode block --wait 8000` 的 `error` 含 `8000`（非 5000），非 block 形态含 `5000` | **pass** |
| S-03 | `ENTRIES` 40 条 + 字段集不变 | `ENTRIES.length = 40`；`kind === 'stream'` = `4`；`Object.keys().sort()` 的**唯一字段集 1 种、长度 10**；`test/sdk-surface.test.js` 在本轮两轮 scoped 内全绿 | **pass** |
| S-04 | 零新增依赖 | `dependencies = {}`；`scripts.test` 未变 | **pass** |
| S-05 | 改动面封闭 | `git diff --name-status 1d934b7..HEAD` ⇒ **恰 5 个 `M`**（§2 表列）；`oamp/src/**`、`oamp/bin/**`、`oamp/package.json`、`oamp/API.md`、`oamp/llms.txt`、`oamp/web/**`、`oamp/skill/hub.md`、`oamp/test/helpers/**`、其它用例文件**零 diff**（未出现在 name-status 即证） | **pass** |
| S-06 | 失效标记清零 | `grep -rn '已知偏差 · 归 pr-011' oamp/` ⇒ **0 命中**（`oamp/` 内 `已知偏差` 亦 0 命中）；三个受改用例文件 `skip`/`todo` ⇒ **0 命中**；两轮 TAP `# skipped 0 / # todo 0 / # cancelled 0` | **pass** |

### 4.3 scoped 零回归（2 pass + 1 口径外）

| # | 判据 | 实测 | 判定 |
|---|---|---|---|
| R-01 | 7 套件（cli-contract / api / uds / surface / skill / doctor / hygiene）**连跑两轮** | 两轮均 `# tests 96 / # pass 96 / # fail 0 / # skipped 0 / # todo 0`（含 T4-5、T9b、T7 三处新/改断言与 `ENTRY_FIELDS` 契约） | **pass** |
| R-02 | T6 验收 4 另列的两个锁 | `test/api-routes.test.js` `7/7 pass 0 fail`；`test/cli.test.js` `8/8 pass 0 fail` | **pass** |
| R-03 | 仓库级全量 `node --test test/*.test.js` | **未跑**（用户口径 + D7：归属主 agent 在合并时跑一次作为零回归证据） | **口径外·未判** |

### 4.4 变异注入判别实验（4 组 · 全部 pass；**均含未注入对照，真源零触碰**）

副本根 = `/tmp/pr011-r2.wAN3NK/copies/`（`cp -a <worktree>/oamp`；`mktemp -d` 派生）。

| # | 注入（副本内） | 对照（未注入，同副本环境） | 注入后实测 | 判定 |
|---|---|---|---|---|
| M-01 **B1** | `src/web.js` 路由表加 1 条 `kind:'sse'` 的 GET 路由（`/api/verify-r2-extra-stream`）⇒ `node --test test/sdk-api.test.js` **全文件跑** | T4-5 **`ok 9`**（该文件其余 10 项因副本环境性红，见 §7 注 A） | T4-5 **`not ok 9`**，失败原文：`运行侧订阅集合有 fixture 未覆盖的缺项: GET /api/verify-r2-extra-stream`（`missing` 非空，正是新增项） | **pass**（**全文件跑**必红，不再是"第 5 条 SSE 不会红"） |
| M-02 **B2** | `bin/hub.js` 注入 `<包根>/.runtime/verify-r2-injected.json` 写入 ⇒ `node --test test/sdk-uds.test.js` **全文件跑** | `19/19 pass 0 fail` | `18 pass / 1 fail`，唯一红 = `零仓库运行态…`，原文：`仓库运行态目录应与**模块基线**（早于本文件任何 hub 调用）一致：baseline={"runtime":null,"data":null}，after={"runtime":["verify-r2-injected.json"],"data":null}` | **pass**（DEV-01 的"全文件跑逃逸"已闭合：必红点 = 模块基线，非用例内基线） |
| M-03 **B3** | `src/task.js` 的 `renderList` 空态 `'（无任务）'` → `''`（stdout 变 `"\n"`，两侧仍逐字节相等）⇒ `node --test test/sdk-cli-contract.test.js` **全文件跑** | `18 pass / 1 fail`（唯一红 = 环境性 `T5`，`T7` = `ok 16`） | `17 pass / 2 fail`：**新增红 = `T7`**，原文 `task list（零任务）：stdout trim 后应为「（无任务）」单行（非空，防"空 vs 空"的虚假相等）`（另一红仍为环境性 `T5`） | **pass**（`trim()` 前置有牙齿；`stdout.length > 0` 下的逃逸形态不再成立） |
| M-04 **T9b** | `sdk/surface.js` 的 `BLOCK_WAIT_DEFAULT_MS 1800000 → 1799999` ⇒ 只跑 `--test-name-pattern='T9b'` | 两轮 scoped 中 `ok 36 - T9b …` | `not ok 1 - T9b · 常量同值…`（`# pass 0 / # fail 1`） | **pass** |

### 4.5 **记录与实现一致性面**（本轮显式判据；5 条 · 全部 pass，偏差另列 §5）

| # | 记录陈述 | 实现实际 | 判定 |
|---|---|---|---|
| C-01 | PR 文件（同步版 `4c5f597`）文件范围：`http.js`＝上限可按 `spec` 注入、缺省仍 5000ms、**两个读取点同源**；`surface.js`＝**按 `mode` 分支**派生 `(waitMs ?? DEFAULT_WAIT_MS)` + 1s 余量，其余 mode 不下发 | `http.js:74/98/99` 与 `http.js:16` 一致；`surface.js:50/55/91-92` 一致（余量 `HEADER_TIMEOUT_MARGIN_MS = 1000`、`BLOCK_WAIT_DEFAULT_MS = 1800000`） | **pass** |
| C-02 | PR 文件 A-非 block 形态上限不变（含 `--mode background`、省略 `mode`、`api docs` 三形态「即使给 `--wait 8000` 也不放大」）；A-单点派生括号内「原句『不按 `mode` 取值做语义分支』已于主 agent 裁决后取代」 | P-05/P-06/P-07 实测三形态均 5000ms；A-单点派生行内**保留了取代说明**（无残留矛盾陈述；`grep -c '不按 `mode` 取值做语义分支'` = 1，命中即该说明本身） | **pass** |
| C-03 | tasks T1 验收 2/7：闸门单一真值 `flags.mode === 'block'`（不再附加 `waitMs !== null`）、取值 `(waitMs ?? DEFAULT_WAIT_MS) + 余量`、`cli.js` 零改动；并点名 dev 实现名 `BLOCK_WAIT_DEFAULT_MS` / `HEADER_TIMEOUT_MARGIN_MS` | 与 `surface.js:50/55/91-92` 逐字一致；`cli.js` 不在 diff 面；库面缺省 → `1801000ms` 预算（结构性）＋ P-09 行为面（6000ms 桩可达） | **pass** |
| C-04 | tasks T3/T4/T5 落点：`sdk-api.test.js` T4 段 `kind === 'sse'` 4↔4（fixture 逐字、禁从运行侧反推）；`sdk-uds.test.js` 基线**模块作用域且早于任何 `runHub`**；`sdk-cli-contract.test.js` **三处**非空前置 `trim()`（两只读组 + 不可达态 stderr，第三处为 D4 追认） | `sdk-api.test.js:802-822` 逐字 fixture + `routeSet(… kind === 'sse')` + `missing/extra/条数/两两不同`；`sdk-uds.test.js:30-34` 模块级 `RUNTIME_BASELINE`（`OAMP_ROOT` 之后、任何 test 之前，`entriesOf` 为函数声明 ⇒ 前向引用成立）、`:888-905` 用例内改用该常量；`sdk-cli-contract.test.js:1195/1200/1209` 恰三处 `trim().length > 0`，其余 `length > 0`（`:582`/`:644`）未动 | **pass** |
| C-05 | PR 文件「A-偏差锁已翻转」＋ tasks T6 验收 7：用例名去标记、断言集 = `WAIT_TIMEOUT` + `exit_code 1` + `error` 含 `8000`、失效注释删除、`oamp/` 内旧标记零命中、非 `skip/todo` | 用例名 `T4 · --wait 8000 对黑障：\`--wait\` 上限先到（WAIT_TIMEOUT/1、耗时 ≈ 上限、恰 1 次请求）`；断言含 `code/exit_code/error 含 8000/stdout ''/耗时 (7600,9000)/requests 1`；`:793` 注释已改写为规格期望；S-06 零命中 | **pass** |

## 5. 偏差记录（3 条 · 均不构成判定失败）

| # | 严重度 | 偏差 | 证据 | 影响 |
|---|---|---|---|---|
| **D-r2-01** | 低（记录精度） | PR 文件两处把库面调用写作 `createHub().api.calls.create({ …, mode: 'block', waitMs: 8000 })`（flags 入参与 `waitMs` 同处一个对象字面量）。按 `surface.js:349-353` 的 `libParams`（`flags = callArgs[args.length]`；`options = callArgs[args.length + 1]`），`waitMs` 必须落在**第二槽**；tasks 文件 T2 验收 6 已显式写明「**不是**把 `waitMs` 混进 flags 对象 —— 那会变成请求体字段」 | 用例实际写法为两槽：`sdk-cli-contract.test.js:875` / `:896`；行为证据 P-09/P-10 亦为两槽 | 仅文档简写口径松，实现与用例均正确 ⇒ 不影响判定；建议下轮统一写法 |
| **D-r2-02** | 低（副本位置） | PR 工作区内的 `docs/.../pr-011-closeout-wait-limit-and-assertion-strength.md` 仍为 base 文本（`git diff --stat 1d934b7 -- <该文件>` 为空）；回写版本在迭代分支 `4c5f597`（迭代主工作区） | `git -C <迭代主工作区> log --oneline -1 = 4c5f597 docs(0025): pr-011 验收标准同步…`；`git -C <PR 工作区> diff --name-status 1d934b7..HEAD` 不含 docs ⇒ PR 分支不动 docs | 合并后记录以同步版为准（无回退风险：PR 分支不触 docs）；仅"把 PR 工作区副本当 PR 文件读"时会看到裁决前文本 ⇒ 不判 FAIL，登记以免下轮重复争议 |
| **D-r2-03** | 低（流程待办，非 T6 语义） | PR 工作区 `git status --porcelain` 有 2 个**未跟踪文档**（`prs/pr-011-tasks.md`、上轮报告 `clarifications/verify-pr-011-20260915-190800.md`），与 tasks T6 验收 1「无未跟踪文件」的**字面**不符 | 该条语义是"副本实验零残留"：`oamp/` 内零残留（无 `.runtime` / `data`、无未提交 ` M ` 改动）；对照 pr-009 的同类产物由归档 chore `7ab0d78` 提交入库 | 需在 worktree 清理前归档进迭代主工作区，否则产物随嵌套 worktree 删除而丢失（已列入 §8-2） |

> **说明（非偏差）**: PR 文件「验收标准」复选框保持 `- [ ]`（未勾选）符合仓库惯例 —— 已合并的 `pr-001` / `pr-007` / `pr-009` PR 文件同样 `[x]=0`（13/11/11 条全为 `[ ]`）⇒ **不得**据此判"记录未回写"。

## 6. 真源零触碰证据（变异实验边界）

| 检查 | 结果 |
|---|---|
| `git -C <worktree> diff --stat`（未提交改动） | **空**（无任何 ` M `） |
| `git -C <worktree> status --porcelain` | 仅 §5-D-r2-03 的 2 个未跟踪文档（**无** `oamp/**` 改动、无实验残留） |
| `oamp/.runtime` / `oamp/data` | **不存在**（`ls` 报 No such file） |
| 五个受改文件 md5（全部实验结束后采集） | 见 §2（与实验前一致；本次验证全程未对这 5 个文件写入） |
| 变异载体 | 只存在于 `/tmp/pr011-r2.wAN3NK/copies/{b1,b1ctl,b2,b3,t9b}`（`cp -a` 副本）；无 git 写命令（实验副本非 git 仓库） |

## 7. 判定前提与边界（透明登记）

- **注 A（副本环境性红，勿误读）**: 在 `/tmp` 副本内跑 `sdk-api.test.js` / `sdk-cli-contract.test.js` 时，凡依赖"假 ACP agent 注册"的用例会因 `agent 不可用: dev（无对应在线实例）` 大面积红（**未注入的对照副本完全同形**：`sdk-api` 4 pass/10 fail、`sdk-cli-contract` 18 pass/1 fail）。⇒ 该红为副本缺工程根上下文所致，**与注入无关**；判别实验因此采用"同环境对照 vs 注入"逐个比对，每组的**目标断言翻转可独立归因**（M-01 目标项/ M-03 目标项均只在注入侧转红，且失败原文点名注入物）。真源（worktree 内）7 套件两轮全绿，无此现象。
- **注 B**: 全量 `node --test test/*.test.js` **未跑**（R-03），零回归的仓库级结论归属主 agent；本报告不声称全量结果。
- **注 C**: `--mode block` 不给 `--wait` 的 **1800000ms 上限本身不可实测**（30 分钟）；本报告按 PR 文件口径接受"6000ms > 5000ms 仍走等待上限"的界内观测 + `BLOCK_WAIT_DEFAULT_MS`/`DEFAULT_WAIT_MS` 的**同值静态守卫（T9b，已证能红）**共同闭合，未做超时伪造。

## 8. 下一迭代候选

1. **PR 文件库面调用形态统一为两槽**：把 `createHub().api.calls.create({ …, mode: 'block', waitMs: 8000 })` 改写成 `createHub().api.calls.create({ …, mode: 'block' }, { waitMs: 8000 })`（涉及「A-上限内成功」「A-单点派生」两处），消除 D-r2-01 的读法歧义。
2. **产物归档（阻塞"worktree 清理"）**: 把 `prs/pr-011-tasks.md`、`clarifications/verify-pr-011-20260915-190800.md`、本报告从**嵌套 worktree** 搬到迭代主工作区并提交（对照 pr-009 的 `7ab0d78` chore），否则收口清理即丢失（D-r2-03）。
3. **判别实验的副本基线化**: 若后续仍需在副本内做"必红"实验，建议给副本补一个与 worktree 同形的工程根（或固定登记"环境性红清单"），避免"必红"被环境红稀释、削弱证据可归因性（注 A）。
4. **可选加固**: `T9b` 目前只守 `BLOCK_WAIT_DEFAULT_MS` 与 `DEFAULT_WAIT_MS` 的**同值**；若未来出现第二个消费面常量，可把守卫扩展为"取值来源唯一"（当前无此需求，登记为观察项）。

## 9. 汇总与结论

| 计数 | 值 |
|---|---|
| 判定条目 | **29**（行为 12 + 结构 6 + scoped 2 + 判别实验 4 + 记录一致性 5），另 1 条口径外未判（R-03 全量） |
| pass | **29** |
| fail | **0** |
| partial | **0** |
| blocked | **0** |
| 偏差记录 | **3**（D-r2-01 / D-r2-02 / D-r2-03，均低severity，无一与实现行为矛盾） |

**结论: PASS** —— 行为面（CLI 面 / 库面 / 反向回归 / 上游错误优先 / 常量同值）与判别力面（B1/B2/B3/T9b 四组注入在全文件跑下必红，且各有未注入对照）均自证成立；记录面（同步版 PR 文件 + tasks 文件 T1~T6）与实现逐条一致，无遗留的"裁决未回写"形态。上轮 FAIL 的根因（记录不一致）在本轮**已解**，且**未**因记录一致而放松断言判定（本轮判定全部来自自建桩实跑与变异对照，未依赖交付物用例的自证）。
