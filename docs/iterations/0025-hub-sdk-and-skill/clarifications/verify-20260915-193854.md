# 独立验证报告 · 0025-hub-sdk-and-skill（阶段 6）

**验证者身份（反射结果）**：**交付单元与调度记录审计者（commit-unit & scheduling-record auditor）** —— 从产出物类型（11 个 PR 定义 + 一个 SDK 交付物 + 一份阶段 5 调度记录视图）反射出的身份是：既审"提交单元切得对不对"（粒度/依赖），也审"调度声明有没有被真实执行过"（以 git 与磁盘一手记录为准）。不采信任何叙述性自陈。

**产出物**（工作区地址 = `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0025-hub-sdk-and-skill`，下称 `<WS>`）：

1. `<WS>/docs/iterations/0025-hub-sdk-and-skill/` —— 全量阶段产物（`demand.md` / `prd.md` + `prd/` 17 卡 / `architecture.md` / `prs/` 11 个 PR 文件 + 11 个 tasks 文件 / `status.md` / `history.md` / `progress.md` / `clarifications/`）
2. `<WS>/oamp/` —— 代码交付物
3. `<WS>` 的 git 记录（`4d162d7..HEAD`、各 `feat/0025-*` 分支 reflog、`git worktree list`）

**验证标准来源**：`roles/workflow-pb/workflow-pb.md` §「验证目标」（PR 粒度判断框架 / 依赖正确性验证 / 并发调度真实执行证据三项强制核查）+ 本委托 inline 的甲/乙/丙/丁四项。

**验证日期**：2026-09-15（本机 19:38–19:55）

**验证基线（实测）**：
- `<WS>` HEAD = **`62954ea`**（`docs(0025): 阶段 5 完成——11/11 合并 + 最终全量 480/480，进入阶段 6`，19:36:09）。委托给的 tip `833eeb1` 与当前 HEAD 的差异经实测**只在 docs 面**：`git diff --stat 833eeb1 62954ea` = `history.md` +20 / `status.md` +47−28 → `oamp/` 零字节差异。两基线对本报告结论等价。
- `git status --short` = 空（工作区干净）。
- `git diff --shortstat 4d162d7..HEAD -- oamp/` = **17 files changed, 6456 insertions(+), 1 deletion(-)** —— 与委托声明的改动面逐值吻合（独立复现）。

**取证纪律**：本报告未使用任何执行过程上下文（委托只给路径与标准）；未修改任何既有文件（唯一写入 = 本文件）；全部证据由我自行以只读命令与文件读取取得（`git log/reflog/diff/show/worktree list/branch`、`stat`、`read`、`grep`、一次只读的 `node -e "import('./sdk/surface.js')"`）；未执行任何 git 写命令。

---

## 甲 · PR 粒度判断框架 —— 判定：**partial**

三条判断依据逐 PR 应用（行数为事实陈列，按框架要求不作判据；判据是逻辑原子性 / 可审查性 / 独立性）：

| PR | 交付文件（新建 A / 修改 M） | 代码行（新增） | 功能点 | 逻辑原子性 | 可审查性 | 独立性 |
|---|---|---|---|---|---|---|
| pr-001 | `sdk/errors.js` A(84) + `sdk/http.js` A(199) | 283 | 7 | 通过（一条可描述的事：层 A 请求原语 + 错误契约；两文件间为真实 import 边 `http.js:12 → errors.js`） | 通过 | 通过（PR 文件自陈"无可执行入口，验收以模块级实测判定"） |
| pr-002 | `sdk/uds.js` A(179) | 179 | 5 | 通过（单文件单事） | 通过 | 通过（仅依赖已声明的 pr-001） |
| pr-003 | `sdk/surface.js` A(395) + `sdk/cli.js` A(327) + `sdk/doctor.js` A(203) | **925** | **15** | **不通过**（三个模块、三类可分别描述的事，见下证据） | **存疑**（reviewer 需在「入口表定义 / argv 解析渲染 / 探针式自检」三种心智模型间切换，属框架点名的"跨模块混合"信号） | 通过 |
| pr-004 | `sdk/index.js` A(29) + `bin/hub.js` A(5) + `package.json` M + `test/helpers/hub-harness.js` A(76) | 113 | 4 | 通过（一件可描述的事：hub 第二消费入口及其子进程助手；harness 与 `bin/hub.js` 同批是 N-11 的条件性创建结论，且下游 4 个测试 PR 以 pr-004 为前置） | 通过 | 通过 |
| pr-005 | `skill/hub.md` A(129) + `test/sdk-skill.test.js` A(259) | 388 | 3 | 通过（文本 + 其机械核对，单一体） | 通过 | 通过（`depends_on` 为空，且文件间无引用边——我核到用例只读文本、不 import SDK） |
| pr-006 | `test/sdk-surface.test.js` A(579) | 579 | 4 | 通过（单文件单事：入口表 ↔ 三个只读面双向比对） | 通过 | 通过 |
| pr-007 | `test/sdk-api.test.js` A(956) | 956 | 4 | 通过（单文件单事：层 A 行为） | 通过 | 通过 |
| pr-008 | `test/sdk-uds.test.js` A(1005) | 1005 | 4 | 通过（单文件单事：层 B 行为） | 通过 | 通过 |
| pr-009 | `test/sdk-cli-contract.test.js` A(1177) | 1177 | 9 | 通过（单文件单事：统一调用契约的四类退出码 + 契约面；9 个功能点同属一个心智模型） | 通过 | 通过 |
| pr-010 | `test/sdk-doctor.test.js` A(659) | 659 | 2 | 通过（单文件单事：doctor 两态自检） | 通过 | 通过 |
| pr-011 | `sdk/http.js` M + `sdk/surface.js` M + 3 个测试文件 M | 见右列 | 6 | **不通过**（PR 文件 `:25` 自述"后三个各含一类**独立修复**，彼此无引用边" ⇒ 一个 PR 承载 4 类互不相关的事：1 处生产行为修复 + 3 处断言判别力修复） | **存疑**（生产代码修复与测试断言强度两类心智模型；12 条验收标准跨两类） | 通过 |

**甲 判定为 partial 的具体证据**

1. **pr-003 的分组理由被代码证伪**（逻辑原子性不通过的直接依据）。
   - 声明（`prs/pr-003-sdk-entry-surface-cli-and-doctor.md:5`；同一声明见于 `history.md:56` / `:190`，登记为"分组形状的**唯一来源**"）："三者同 PR 是结构性要求：…`cli.js` 分派须引用 `doctor.js`，**拆开则互为对方产出、依赖图成环**"。
   - 我实测的**已交付** import 边（`grep -n "^import"` 于 `oamp/sdk/*.js`）：
     - `sdk/cli.js:14,15,16` → `./surface.js` / `./doctor.js` / `./errors.js`
     - `sdk/surface.js:25-30` → 仅 `node:*` + `./http.js` + `./uds.js`（**不 import `doctor.js`，也不 import `cli.js`**；`:7` / `:390` 注记"doctor 命名空间由 sdk/index.js 组合，本模块不装配"）
     - `sdk/doctor.js:14-19` → 仅 `node:*` + `./http.js` + `./uds.js` + `./errors.js`（`:132` 明写"本模块 **不 import surface.js**：§2.1 无 DOC --> SUR 边"）
   - 即三个模块构成**无环 DAG**：`cli → {surface, doctor}`、`surface → {http, uds}`、`doctor → {http, uds}`。按此形状切成 3 个 PR（`surface` ← pr-001/pr-002；`doctor` ← pr-001/pr-002；`cli` ← surface + doctor）**不产生任何环**。`architecture.md` §2.1 的组件图（`:145-160`）同样只有 `CLI --> SUR`、`DOC --> HTTP/UDS/APIDOC`，**没有**任何 `SUR --> CLI` / `SUR --> DOC` 反向边。
   - 结论：以已交付形状为证，"拆开则成环"不成立；该 PR 把三个模块合并是**取舍**而非结构必然（我不能排除某种未写出的切法会成环，但 PR 文件给出的就是这条理由，而它与代码矛盾）。故 pr-003 同时承担 3 个可描述交付物 + 15/17 张功能卡 + 925 行新代码，逻辑原子性与可审查性均不满足框架要求。
2. **pr-011 承载 4 类独立修复**（同上第二处依据）。其自身文本即承认三处修复"彼此无引用边"，而第 4 项（等待上限可达性）是生产行为修复——回滚 pr-011 会同时撤掉功能修复与三处断言强化，违反"回滚它不影响无关功能"。**减轻情节（事实陈列，不改判定）**：该形状源于用户裁定"全量集中到最后跑一次并驱动收口修复 PR"（`status.md`「用户决策点记录」），且 PR 文件已把四类修复逐条开列，可审查性损失有限。
3. 其余 9 个 PR 三条判断依据**全部满足**（单文件或单一体交付；回滚影响面封闭在其自身交付物；验收可在自身声明的依赖已合并的前提下独立判定）。

---

## 乙 · 依赖正确性验证 —— 判定：**partial**（子项 1、2、4 通过；子项 3 为 partial）

### 乙-1 `depends_on` 逐条代码级证据 —— **pass**

11 个 PR 共 **16 条** `depends_on` 边（`grep -o '^\- pr-[0-9]*'` 于各 PR 文件），逐条核实（"声明理由"栏为 PR 文件原文要点，"我核到的代码级证据"栏为我独立读到的事实）：

| # | 边 | 我核到的代码级证据（文件:行） | 判定 |
|---|---|---|---|
| 1 | pr-002 → pr-001 | `sdk/uds.js:13` `import { HubError, classify } from './errors.js'`；`errors.js` 由 pr-001 新建（merge `f9327ad`，`git diff --stat f9327ad^1 f9327ad` 含 `oamp/sdk/errors.js \| 84 +`） | pass |
| 2 | pr-003 → pr-001 | `sdk/surface.js:29` `import { request, stream } from './http.js'`；`sdk/cli.js:16` `import { HubError, classify, serializeError } from './errors.js'`；`sdk/doctor.js:17` `import { request } from './http.js'` | pass |
| 3 | pr-003 → pr-002 | `sdk/surface.js:30` `import { connect as connectUds } from './uds.js'`；`sdk/doctor.js:18` `import { connect } from './uds.js'` | pass |
| 4 | pr-004 → pr-003 | `bin/hub.js:3` `import { main } from '../sdk/cli.js'`；`sdk/index.js:11,12` `import { createSurface } from './surface.js'` + `import { check as doctorCheck } from './doctor.js'` | pass |
| 5 | pr-006 → pr-003 | `test/sdk-surface.test.js:16` `import { ENTRIES, LAYERS } from '../sdk/surface.js'` | pass |
| 6 | pr-007 → pr-004 | `test/sdk-api.test.js:25` `import { runHub } from './helpers/hub-harness.js'`；`test/helpers/hub-harness.js:20-21` `HUB_BIN = path.join(PKG_ROOT,'bin','hub.js')` + `spawn(process.execPath, [HUB_BIN, ...])` | pass |
| 7 | pr-008 → pr-004 | `test/sdk-uds.test.js:22,23` `runHub` + `import { createHub } from '../sdk/index.js'` | pass |
| 8 | pr-009 → pr-004 | `test/sdk-cli-contract.test.js:22` `import { runHub } from './helpers/hub-harness.js'`（+ 同文件对 `bin/hub.js` 的进程面调用） | pass |
| 9 | pr-010 → pr-004 | `test/sdk-doctor.test.js:24,25` `runHub` + `createHub` | pass |
| 10 | pr-010 → pr-003 | `sdk/doctor.js:193` `export async function check({ apiDocPath = API_DOC_PATH, port } = {})`（即声明的跨 PR 入参契约）；`test/sdk-doctor.test.js:418,422` 以注入副本调用 `doctor.check({ apiDocPath })` | pass |
| 11 | pr-011 → pr-001 | `sdk/http.js:16` `RESPONSE_TIMEOUT_MS`、`:74` `spec.headerTimeoutMs ?? RESPONSE_TIMEOUT_MS`、`:98-99` 定时器同源；pr-011 diff 确实修改该文件（`git diff --stat 833eeb1^1 833eeb1` → `oamp/sdk/http.js \| 22 +-`） | pass |
| 12 | pr-011 → pr-003 | `sdk/surface.js:91-92` `headerTimeoutMs: flags.mode === 'block' ? (waitMs ?? BLOCK_WAIT_DEFAULT_MS) + HEADER_TIMEOUT_MARGIN_MS : undefined`（声明所指的"单点派生"）；`sdk/cli.js:18` `DEFAULT_WAIT_MS = 1800000`、`:255` 授予点 | pass |
| 13 | pr-011 → pr-006 | `test/sdk-surface.test.js:473` `ENTRY_FIELDS` 十项 + `:480` 逐条字段集比对（读取对象 = pr-011 所改的 `surface.js` 的 `ENTRIES`）；我实测 `ENTRIES` 条目字段集 = `acceptsAs,args,cmd,flags,id,kind,layer,method,path,run`（与之逐项一致） | pass |
| 14 | pr-011 → pr-007 | pr-011 diff 修改 `oamp/test/sdk-api.test.js`（`+24`）；`:354` `routeSet`、`:812` 新增 `route.kind === 'sse'` 核对 | pass |
| 15 | pr-011 → pr-008 | pr-011 diff 修改 `oamp/test/sdk-uds.test.js`（`+19/-?`）；`:35` 模块作用域 `RUNTIME_BASELINE`、`:276` `entriesOf`、`:903-907` 前后判据 | pass |
| 16 | pr-011 → pr-009 | pr-011 diff 修改 `oamp/test/sdk-cli-contract.test.js`（`+179/-?`）；`:722/:790/:862` T4 三态、`:1195/:1200/:1209` 三处 `.trim().length > 0` | pass |

**16/16 均有真实代码级耦合证据**，无一条属于"把顺序偏好当依赖"。

### 乙-2 依赖图无环 —— **pass**

以各 PR 文件的 `## depends_on` 段为唯一输入独立复算（不读任何 `batch`/叙述）：

```
pr-001 -> (无)          pr-005 -> (无)
pr-002 -> pr-001        pr-003 -> pr-001, pr-002      pr-004 -> pr-003
pr-006 -> pr-003        pr-007 -> pr-004              pr-008 -> pr-004
pr-009 -> pr-004        pr-010 -> pr-004, pr-003
pr-011 -> pr-001, pr-003, pr-006, pr-007, pr-008, pr-009
```

DFS 三色标记：`any cycle: False`；被引用节点全部有定义（`undefined refs: set()`）。根节点 = `pr-001` / `pr-005`，末端节点 = `pr-011`。11 个 PR 均能在迭代分支上找到对应 merge（`git log --merges 4d162d7..HEAD | wc -l` = **11**）。

### 乙-3 PR 间文件范围无重叠 —— **partial**（10 个阶段 4 PR 满足；11-PR 全集不满足）

- **阶段 4 产物（pr-001~pr-010）满足**：17 条声明路径**两两互异**（2+1+3+4+2+1+1+1+1+1 = 17），且与 `git diff --name-status 4d162d7..HEAD -- oamp/` 的 17 个新增/修改文件**一一对应、无一文件被两个 PR 触及**（`oamp/package.json` 仅 3 行改动、`dependencies` 仍为 `{}`、`bin` 只加 `hub` 一键——与 pr-004 声明一致）。
- **11-PR 全集不满足**：`pr-011` 的文件范围与 **5 个** PR 重叠 —— `sdk/http.js`(pr-001)、`sdk/surface.js`(pr-003)、`test/sdk-api.test.js`(pr-007)、`test/sdk-uds.test.js`(pr-008)、`test/sdk-cli-contract.test.js`(pr-009)。**性质**：全部 5 个所有者都写在 pr-011 的 `depends_on` 里，故重叠是**严格有序**的（pr-011 最后合并、只做修改，无并发写冲突风险）；但"无重叠"这一条在全集口径下**字面上不成立**，且该 PR 是迭代中途新增（18:38）而非阶段 4 产物。

### 乙-4 无遗漏功能点 —— **pass**

`prd/` 实测 17 个卡文件（`F01~F14` + `G01~G03`）；逐 PR 抽取「涉及功能点」段后独立复算覆盖矩阵：

```
F01 pr-003,004        F08 pr-001,002,003,008,009     G01 pr-001,002,003,004,009,010
F02 pr-001,003,006,007,011   F09 pr-001..004,007,008,009,011   G02 pr-002,003,008,009
F03 pr-002,003,006,008       F10 pr-001,003,009,011          G03 pr-005
F04 pr-003,006,009,011       F11 pr-003,010
F05 pr-003,007,009           F12 pr-005
F06 pr-001,003,007,009,011   F13 pr-003,004,005
F07 pr-001,003,009,011       F14 pr-003,006
```

**17/17 均有 ≥1 个 PR 引用，零遗漏，且无 `prd/` 之外的功能点被引入**（PR 文件的功能点清单全部落在上述 17 个代号内）。

---

## 丙 · 并发调度真实执行证据（三项强制核查）—— 判定：**pass**（3/3 通过）

触发条件成立：`prs/` 产出 11 个 PR（>2），依赖图存在真实并发分支（`pr-001 ‖ pr-005`；`pr-004 ‖ pr-006`；`pr-007 ‖ pr-008 ‖ pr-009 ‖ pr-010`）。

### 丙-1 worktree 时间窗口重叠 —— **pass**

**基础事实（磁盘）**：11 个 PR worktree 目录**全部仍存在**（`git worktree list` 12 行 = 主工作区 + 本迭代工作区 + 11 个 PR worktree），11 个 `feat/0025-*` 分支全部存活（`git branch -a`），**从未发生过清理**（无 `worktree remove`/`prune` 痕迹；`status.md` 另注记 pr-009 的 worktree 按协议"原样保留"）。就"同时存在过"这一命题，任意两个 PR 的 worktree 存在区间都重叠。

**决定性证据（时间线交错，排除"串行后误报并发"）**：worktree 目录创建时刻（`stat -f "%SB"`）+ 各分支 reflog（`branch: Created from …`）+ worktree 内 tasks 文件写入时刻（`stat %Sm`）+ 各分支提交时刻（`git reflog --date=iso`）：

| wave | PR | 分支创建 | 该 PR tasks 写入 | 实现提交 | 合并 |
|---|---|---|---|---|---|
| 1 | pr-001 | 14:59:19 | 15:03:59 | `8389330` 15:11:46 | `f9327ad` 15:24:49 |
| 1 | pr-005 | 14:59:19 | 15:03:00 | `2d1849d` 15:12:13 | `4be4661` 15:24:49 |
| 4 | pr-006 | 17:02:49 | 17:13:25 | `c4e1487` 17:20:59 | `b93fead` 17:43:36 |
| 4 | pr-004 | 17:02:48 | 17:17:21 | `d00532f` 17:43:08 / `59c3e59` 17:43:17 / `f2e99c0` 17:43:17 | `3ba06dd` 17:53:07 |
| 5 | pr-010 | 17:53:08 | 17:59:12 | `7a025e0` 18:08:12 | `ec7fc84` 18:18:25 |
| 5 | pr-007 | 17:53:07 | 18:00:02 | `dbbb898` 18:08:17 | `f4b6aa7` 18:18:25 |
| 5 | pr-009 | 17:53:07 | 18:00:18 | `9266740` 18:09:46→18:10:50 | `5dc04c4` 18:33:35 |
| 5 | pr-008 | 17:53:07 | 18:01:05 | `3fa4eab` 18:10:02→18:10:26 | `46969a0` 18:18:25 |

- wave 1：两分支**同一秒**创建（14:59:19），两份 tasks 与两次实现提交交错（15:03:00/15:03:59、15:11:46/15:12:13），**两分支工作窗口 [14:59:19, 15:24:49] 完全重叠**。
- wave 5（最强证据）：四个分支在 **1 秒内**创建（17:53:07 / 17:53:07 / 17:53:07 / 17:53:08），四份 tasks 文件在 17:59:12–18:01:05 内交错落盘，**四条独立实现提交流全部落在 18:08:12–18:10:50，早于本 wave 首个合并 18:18:25**。若为串行执行，任一 PR 的窗口必须晚于前一个 PR 的合并——实测不成立 ⇒ **并发确为真实发生**。
- wave 4：`pr-004` 与 `pr-006` 同一秒（17:02:48/17:02:49）解锁并建树，两个工作窗口 [17:02:48, 17:53:07] / [17:02:49, 17:43:36] 重叠。

（另：`progress.md`（15:27:45 由独立进度核实者产出，早于本报告）曾记录当时 3 个 worktree 并存；本报告的结论不依赖该文件，仅记录其一致性。）

### 丙-2 并发配置区块真实初始化与更新 —— **pass**

**区块存在且五字段均有值**（实测 `## 并发配置（阶段 5）`）：`起始并发数` / `硬上限` / `当前有效上限` / `累计槛位释放次数` / `已派发总数`。

**真实更新（不是初始化后再未变化）**：该区块在迭代分支上有 **4 个不同的已提交版本**，每个字段都随 PR 合并/派发推进变化，且变化值与 git 一手事实逐条吻合：

| status.md 版本（提交时刻） | 起始 | 硬上限 | 当前有效上限 | 累计槛位释放 | 已派发 | 该 rev 可核实事实（我独立复算） |
|---|---|---|---|---|---|---|
| `17f64eb`（15:31:32） | 3 | 5 | 5（"已因 2 次槛位释放爬升至硬上限"） | 2 | 3 | merge `4d162d7..17f64eb` = **2**；分支/建树 3 个（pr-001/002/005）；worktree 目录 = 3 |
| `1d653b3`（15:54:05） | 3 | 5 | 5 | 3 | 4 | merge = **3**；worktree = 4 |
| `85d10f7`（18:20:52） | 3 | 5 | 5 | **8** | 10 | merge = **9**；`feat/0025-*` 分支 = 10（见「偏差记录」#3） |
| `62954ea`（19:36:09，现 HEAD） | 3 | 5 | 5 | 11 | 11 | merge = **11**；worktree = 11；PR 表 `✅` 行 = 11 |

**证据限度（如实登记）**：阶段 4 落盘基线 `4d162d7` 的 `status.md`（`git show 4d162d7:…/status.md`）**没有**该区块，其内容仍为"阶段 4 完成（阶段 5 待授权）"；区块的**最早可观测状态**是 `17f64eb`（15:31，已含 2 次释放），而该提交正是 `history.md` 记载的"修正进度视图漂移"提交。故"**在阶段 4→5 入口（≈14:59）被初始化**"这一点我只能间接支持：① `history.md:68-72` 的 15:00:00 派发记录写有"当前有效上限 **3**，已派发总数 2"（= 规格的初始值，且该条目属事后补记）；② `progress.md`（15:27:45，独立第三者快照）逐字引用当时工作区副本的区块为"起始并发数 3 / 硬上限 5 / 累计槛位释放 2 / 当前有效上限 5"，即**阶段 5 进行中该区块已带全部五字段存在**。"初始化时点精确到入口秒"在 git 面不可观测——这是取证限度，不改变"区块真实存在并被真实更新"的判定。

**失败判定亦被真实记账**：`85d10f7` 的 PR 表把 pr-009 标为 `**❌FAIL(现场保留)**`、`槛位状态 = 已释放（返工中）`，并注明其 worktree/分支不得清理——与 `5dc04c4`（pr-009 第 2 轮返工后合并）的 git 事实一致。

### 丙-3 爬升公式真实触发 —— **pass**

- **公式与初值**（`roles/workflow-pb/workflow-pb.md:189-190`）：`硬上限 = 2 × 起始并发数 − 1` = 2×3−1 = **5**；`当前有效上限 = min(起始并发数 + 累计槛位释放次数 × 起始并发数, 硬上限)`；`当前有效上限` 初值 = 起始并发数 = **3**；**首次**释放即 `min(3 + 1×3, 5) = 5` ⇒ 爬升条件在本迭代**必然在同一次释放内触发**。
- **「当前有效上限」确实被重算，而非维持初值**：
  1. 记录面：`history.md:72`（15:00 记录）"当前有效上限 3"（= 初值）→ `17f64eb` status.md "5（已因 **2** 次槛位释放爬升至硬上限）"；`history.md:134` 给出算式 `min(3+2×3,5) = 5`。此后三次修订维持 5（= 硬上限，与"达到硬上限后维持不变"的规则一致）。
  2. **运行面（决定性）**：wave 5 需要 **4 个 PR 同时在执行**（丙-1 表：17:53:07–08 建树、四条提交流并行、18:18:25 才首次合并）。若 `当前有效上限` 维持初值 3，该 wave 在调度上不可能成立 ⇒ 上限在 17:53 之前**已被实际抬高到 ≥4**，而这只能来自公式重算（`min(3+6×3, 5) = 5`；当时累计释放 = 6）。`status.md` 记录的"实测并发波形 **2 → 1 → 1 → 2 → 4 → 1**"与 git/磁盘实测的 wave 规模（2/1/1/2/4/1）逐项吻合。

**丙 三项全部通过** ⇒ 本验证目标不判 partial。

---

## 丁 · 本迭代特定项：`deferred-demand-changes.md` —— **不适用**

依据：`test -e <WS>/docs/iterations/0025-hub-sdk-and-skill/deferred-demand-changes.md` → **不存在（NOT EXISTS）**（实测 2026-09-15 19:38）。故本项记"不适用"，报告中**不作任何原文摘录**（无内容可摘录），亦不存在需要置顶的搭置需求变更/错误。

---

## 汇总

**验证目标级（4 项）**

| 目标 | 判定 |
|---|---|
| 甲 PR 粒度判断框架 | partial（11 个 PR 中 9 个三条全满足；pr-003、pr-011 逻辑原子性不通过） |
| 乙 依赖正确性验证 | partial（乙-1 pass / 乙-2 pass / 乙-3 partial / 乙-4 pass） |
| 丙 并发调度真实执行证据 | **pass**（3/3 子项通过，均附 git/磁盘一手证据） |
| 丁 `deferred-demand-changes.md` | 不适用（文件不存在） |

**子项级计数**：pass = **6**（乙-1、乙-2、乙-4、丙-1、丙-2、丙-3）、fail = **0**、partial = **2**（甲、乙-3）、blocked = **0**、不适用 = **1**（丁）。

**无 fail 条目**——全部 16 条依赖边均有代码级证据、依赖图无环、17/17 功能点无遗漏、deliverable 改动面（17 文件 / +6456 / −1）独立复现一致、`oamp/src/**` 与 `API.md`/`web/**`/`llms.txt` 零改动、`dependencies` 仍为 `{}`、入口表实测 40 条 = 21/8/11 且条目字段集与 `ENTRY_FIELDS` 一致、工作区干净。两项 partial 均落在**分组理由 / 记录口径 / 文件范围口径**层面，不指向交付物的功能面缺失。

---

## 偏差记录

（逐项判定针对"实现是否满足规格"；本表针对"规格/文档描述是否准确描述当前事实"。**不影响上述判定结论**。）

| # | 规格 / 文档描述 | 实现 / 实际事实 | 建议处理 |
|---|---|---|---|
| 1 | `prs/pr-003-…md:5`（并见 `history.md:56`"分组形状的唯一来源"）：三模块合并是因"**拆开则互为对方产出、依赖图成环**" | 已交付 import 图**无环**：`cli → {surface, doctor}`、`surface → {http, uds}`、`doctor → {http, uds}`；`doctor.js:132` 明写不 import surface.js；`architecture.md` §2.1（`:145-160`）亦无 DOC→SUR / SUR→CLI 反向边 ⇒ 按 surface / doctor / cli 切分不产生环 | 订正 pr-003 的分组理由（或补出确会成环的切法证据）；本迭代已交付形状不回改 |
| 2 | 阶段 4 出口条件"PR 间文件范围无重叠"（`workflow-pb.md` 阶段定义表）与 `§验证目标` 的同名通过条件 | pr-011（迭代中途新增的收口 PR，18:38）与 **5** 个 PR 的文件范围重叠（`sdk/http.js`、`sdk/surface.js`、3 个 `test/sdk-*.test.js`）；重叠均为声明式且严格有序（5 个所有者全在 pr-011 的 `depends_on` 内，pr-011 最后合并） | 在协议层明确"收口/收尾 PR 的受控重叠"如何登记与豁免（当前口径下该条件字面不成立，实际无并发冲突） |
| 3 | `85d10f7` 版 `status.md`：`累计槛位释放次数 = 8` | 同一 rev 上可核实事实 = **9 次合并**（`git log --oneline --merges 4d162d7..85d10f7 \| wc -l` = 9），同文件 PR 表亦为 9 行 `✅`（仅 pr-009 为 `❌FAIL`）；规格 `workflow-pb.md:190` 的"失败/阻塞判定同样释放槛位"口径下应为 10。终版 `62954ea` 的 11 与 11 次合并一致 | 在 `data/formats.md` 明确 `累计槛位释放次数` 的计数口径（是否含失败/阻塞判定），并按同一口径每次合并后累加 |
| 4 | `§验证目标`：`depends_on` 理由须有**代码级**证据（共享符号/接口/文件引用） | 16 条边的**耦合证据实际存在**（乙-1 全部核实）；但证据的**书写强度**不均——pr-002/pr-007/pr-008/pr-009 的理由段以 `architecture.md` 章节（如 §2.1 边、§10 T2/T3/T4）为主要依据，未给文件行号级证据；仅 pr-011 的理由段给出了 `file:line` | 把"理由段须给文件/符号级证据（可含行号）"写成显式要求，避免架构章节叙述被当作代码证据 |
| 5 | `architecture.md` §2.1 组件图 | 图中有 `BIN --> CLI`、`CLI --> SUR`、`DOC --> HTTP/UDS/APIDOC`，但**缺**实现必需的 `CLI --> DOC` 边（`sdk/cli.js:15` `import { check as doctorCheck } from './doctor.js'`，`:297` 调用 `doctorCheck({ port: ctx.port })`） | 补 `CLI --> DOC` 边（实现已存在该 import，图与实现不一致） |

---

## 下一迭代候选

（仅登记，不影响本迭代结论）

1. **pr-003 的分组理由订正**：给出确实成环的切法证据，或承认 `doctor.js` 具备独立成 PR 的条件（`check({ apiDocPath })` 已是显式入参、与 surface/cli 无引用边）。
2. **收口类 PR 的承载上限**：一次收口 PR 是否可以同时承载"生产行为修复 + 测试断言判别力修复"两类；若可以，其验收标准是否需要按类别分卡，以便回滚粒度可判。
3. **文件范围口径补全**：阶段 4 出口条件"PR 间文件范围无重叠"对**迭代中途追加的收口 PR**如何适用（显式豁免 + 登记，或要求新增 PR 只触碰自建文件）。
4. **`depends_on` 证据标准显式化**：理由段要求文件/符号级证据（本迭代仅 pr-011 达标）。
5. **`累计槛位释放次数` 计数口径显式化**：明确是否包含"失败/阻塞判定"，消除偏差 #3 一类的计数漂移。
6. **架构图与实现的边一致性**：`architecture.md` §2.1 补 `CLI --> DOC`。

---

## 结论

**FAIL**（严格口径：甲、乙 两项验证目标判定为 **partial**，未达"全部 pass"；**fail 条目 = 0**，丙 三项全部 pass，丁 不适用）

判定要点（供主 agent 直接决策）：
- **不存在**功能面/交付面缺失：交付物 17 文件 / +6456 / −1 独立复现一致，17/17 功能点无遗漏，16/16 依赖边有代码级证据，依赖图无环，40 条入口表实测 21/8/11 且字段集不变，`dependencies` 仍为 `{}`，既有面零改动。
- **两项 partial 的内容**：甲 = pr-003（3 模块 / 15 卡 / 925 行，且其"成环"理由被代码 DAG 证伪）与 pr-011（4 类独立修复同 PR）的逻辑原子性不通过；乙 = "PR 间文件范围无重叠"在 11-PR 全集口径下因 pr-011 与 5 个 PR 的受控重叠而字面不成立（阶段 4 的 10 个 PR 之间满足）。
- **并发调度真实执行证据（丙）成立**：worktree 重叠、并发配置区块真实初始化与四次真实更新、爬升公式真实生效（4 路并发 > 初值 3）三项均有 git/磁盘一手证据。
