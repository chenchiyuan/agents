# 独立验证报告 · pr-006（三层覆盖面对照用例 · F02/F03/F04/F14）

| 项 | 值 |
|---|---|
| 迭代 | 0025-hub-sdk-and-skill |
| 阶段 | 5（PR 实现）· 中间 PR 的独立验收 |
| PR 文件 | `docs/iterations/0025-hub-sdk-and-skill/prs/pr-006-sdk-surface-coverage-test.md` |
| worktree | `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0025-hub-sdk-and-skill/.pb-agents/worktrees/0025-pr-006-sdk-surface-coverage-test` |
| 分支 | `feat/0025-pr-006-sdk-surface-coverage-test` |
| base / HEAD | `453326c` / `c4e1487`（唯一提交：`test: 三层覆盖面对照用例锁定 surface.js 入口表（F02/F03/F04/F14）`） |
| 验证日期 | 2026-09-15 |
| 结论 | **PASS** |

## 验证者身份（反射结果）

**独立测试用例审计人（守门用例的漂移拦截能力审计）**。

反射依据：产出物不是产品代码，而是**一个"覆盖面守门"式测试用例**；这类产物的价值不在"自身通过"（它天然通过与实现同源的表），而在**真能拦住三方真源的漂移**。因此身份取"独立测试用例审计人"，手段取两件：① 逐条静态核对用例的判据是否锚在标准要求的对象上；② **变异注入**——在临时副本上制造真源漂移，观察用例是否转红并点名。

验证标准三份（本次逐条判定 56 项）：

| 组 | 标准 | 条目数 |
|---|---|---|
| A | `prs/pr-006-sdk-surface-coverage-test.md`「验收标准」9 条 | 9 |
| B | `prs/pr-006-tasks.md` T1~T5 逐任务验收标准 | 29 |
| C | `architecture.md` §5.1 三层入口表 40 条 / §7 F02·F03·F04·F14 行 / §10 T-07（T1 行）；`prd/{F02,F03,F04,F14}*.md` | 18 |

**已生效基准**（按简报，作为标准的一部分使用，不重新裁决）：跨 PR 契约 = `surface.js` 导出 `LAYERS` / `ENTRIES`(40) / `createSurface`；40 条 cmd 名面须与已合并 `oamp/skill/hub.md` 逐字一致；主 agent 已 ratify 4 项（严格解析+点名失败 / 层 C 只断集合 / `#9`·`#16` 形式不要求统一 / 中间 PR 只做 scoped 验证）。

## 产出物

- `oamp/test/sdk-surface.test.js`（**新建**，579 行，commit `c4e1487`）

## 独立执行的原始命令与结果

```sh
# 1) scoped 跑（用户口径：中间 PR 只做 scoped 验证，不跑仓库级全量套件）
cd <worktree>/oamp && node --test test/sdk-surface.test.js
# => # tests 12 / # pass 12 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0

# 2) cwd 独立性（A10）
cd <worktree> && node --test oamp/test/sdk-surface.test.js   # => 12 pass / 0 fail
cd / && node --test <worktree>/oamp/test/sdk-surface.test.js # => 12 pass / 0 fail

# 3) 改动面封闭
git -C <worktree> diff --name-only 453326c..HEAD -- oamp/    # => oamp/test/sdk-surface.test.js（唯一）
git -C <worktree> diff --name-only 453326c -- oamp/          # => 同上（含工作区，无额外改动）
git -C <worktree> diff --name-only 453326c..HEAD -- oamp/sdk oamp/API.md oamp/skill oamp/src oamp/bin oamp/package.json oamp/test/sdk-skill.test.js oamp/web
# => 空（0 行）
git -C <worktree> status --porcelain -- oamp/                # => 空（`oamp/**` 零未跟踪 / 零改动）

# 4) 冻结契约（40 条名面 vs 已合并 skill/hub.md）
#    逐段抽取 hub.md 三层清单的 `- \`<名面>\`` 项，与 ENTRIES 逐条同序比较
# => api 21/21、uds 8/8、cli 11/11，三层均为「逐条同序完全相等」（verbatim(list+order)=True）

# 5) §5.1 三层入口表（architecture.md）逐条同序比较
# => 层 A 21/21、层 B 8/8、层 C 11/11，均逐条同序相等；21+8+11 = 40

# 6) 零副作用（写盘）
#    在 mktemp 副本内跑用例，跑前/跑后对 85 个文件做 sha256 快照
# => ADDED: none / REMOVED: none / CHANGED: none；仓库内 .runtime/ 与 data/ 零创建
```

## 逐项判定

### 组 A · PR 文件「验收标准」9 条

| # | 标准（摘要） | 判定 | 证据 |
|---|---|---|---|
| A1 | 层 A 双向比对：`api` 条目 ↔ `API.md` §3 解析结果（21 行，`<param>`/`:param` 归一），无缺项无多出项 | **pass** | 用例 `层 A 双向比对…`（test #1）断言 `missing` / `extra` 双向为空；独立复算：`§3` 表 21 行 ↔ ENTRIES 层 A 21 条，双向零差集。变异 M1/M4/M6/M7' 全部转红并点名 |
| A2 | 机械产出 21 行对照清单 | **pass** | `t.diagnostic` 输出 21 行，形如 `#3\tGET /api/chats/<chat_id>\t↔\thub api chats get`；该 21 行由 `doc.map(...)` × `entries[i]` **同一循环现算**（非手抄常量：40 条名面中仅 9 条以字面出现，且全部是 §5.1 三处跨层样例，见 B-T4-4）；实测 stdout 可见（标题行「层 A 对照清单（API.md §3 ↔ hub api 入口表） —— 共 21 行」） |
| A3 | 层 B 双向比对：`uds` 条目 ↔ `router.js` 8 个 `dispatch` 分支，无缺项无多出项 | **pass** | test #4；独立复算：`dispatch` 区段 `case '<方法>':` 恰 8 条，与 ENTRIES 层 B 8 条双向零差集且同序。变异 M2（分支名漂移）转红并点名 `router.tasklist_x` |
| A4 | 层 C 双向比对：`cli` 条目 ↔ `cli.js` `main(argv)` 展开的 11 个叶子（集合口径） | **pass** | test #5 只断言集合（无顺序断言，符合 C9）；独立复算：分块展开恰 11 个叶子，与 ENTRIES 层 C 11 条集合相等。变异 M3a（+purge）/ M3b（−watch）/ M5（新顶层分支）/ M9（`cluster up`→`launch`）全部转红并点名 |
| A5 | 分层可判定 + `doctor` 单列不入 40 条（P-4） | **pass** | test #6 / #7：`LAYERS` deepEqual `['api','uds','cli']`；`ENTRIES.length === 40`；逐条 `layer ∈ LAYERS` 且 = 完整名面首 token 且 `id` 以 `${layer}.` 开头；三层计数 21/8/11；零条 `cmd[0]==='doctor'` / `id` 含 `doctor`；`40 === 21+8+11` |
| A6 | 无同名同形 + 跨层近名三处样例无歧义 | **pass** | test #8 / #9：40 名面与 40 id 均无重复；同一 `cmd` 名面不跨层（`byCmd` 层集大小 ≤ 1）；单条内 `args` / `flags` 名不重复；含 §5.1 三处样例（实例拓扑 / 任务清单 / 单任务终态）九名面存在性 + 首 token = `api`/`uds`/`cli` |
| A7 | 无编排条目：40 条逐条对应单一端点/单一方法/单一条既有命令 | **pass** | test #10：字段集恰 10 项；层 A `method ∈ {GET,POST}` 且 `path` 以 `/api/` 开头且不含空白/`\|`/`?`/`#`；层 B `method` 形如 `ns.name` 且 `path === null`；层 C `method === null && path === null`；`kind ∈ {result,stream}`；配合 A1/A3/A4 的三个双射 ⇒ 无一条覆盖两个目标。test #3 另核层 A 位置参数必须属该端点（防跨端点入参混入） |
| A8 | 用例零依赖（只读四文件；不起服务/不联网络/不写 `.runtime/`·`data/`） | **pass** | test #11 静态守门（import 白名单恰 6 项 / 零禁止模块 / 零写盘 API / `readFileSync` 调用点恰 2 处）；独立复算：跑前跑后 85 文件 sha256 全等（零新增/零改动）；仓库内无 `.runtime`/`data` 创建。守门非空已由变异 M10/M11/M12 证伪 |
| A9 | 经 `node --test test/*.test.js` 被拾取并通过 | **pass**（按已 ratify 的 scoped 口径） | `package.json` `scripts.test = node --test test/*.test.js`（本 PR 零 diff）；文件名落在 glob 内；scoped 跑 12/12 通过、零 `skipped`/`todo`。全量拾取按用户口径留待收口 PR |

### 组 B · `pr-006-tasks.md` T1~T5 逐任务验收

#### T1（8 项）

| # | 标准（摘要） | 判定 | 证据 |
|---|---|---|---|
| T1-1 | 落点与体例：只 import `node:test`/`node:assert/strict`/`node:fs`/`node:url`/`node:path` + `../sdk/surface.js`；包根按 `import.meta.url` 推导；不起服务/不连端口/不 spawn/不写盘 | **pass** | 源文本 import 恰 6 条说明符（test #11 断言）；`OAMP_ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..')`；从 `/` 跑仍 12/12（cwd 无关） |
| T1-2 | 读取面恰四个文件；读不到即 `assert.fail` 并点名路径 | **pass** | `readSource` 抛内 `assert.fail('期望可读的 <rel>（未命中或不可读：…）')`；变异 M12（把读取路径改成 `file+'.bak'`）→ 6 个用例转红，消息逐字为 `期望可读的 API.md（未命中或不可读：ENOENT）` |
| T1-3 | §3 表区段解析恰 21 行、编号 1..21 齐全；≠21 点名实际行数 | **pass** | `parseApiSection` 先断 `rows.length === 21`，再逐行断 `row.no === i+1`。变异 M6（删整行）→ `API.md §3 表区段应解析出 21 行，实际 20 行`；M7'（增整行）→ `…实际 22 行` |
| T1-4 | 层 A 双向 + 顺序一致 | **pass** | 见 A1；顺序断言 `orderFindings`（表序 ↔ §3 行序），变异 M15（行序互换并同步改编号）→ 转红且**未**被编号断言抢先（编号断言亦为独立锁） |
| T1-5 | 21 行对照清单机械产出，格式 `#<n>\t<METHOD> <路径原文>\t↔\thub api <子命令名面>` | **pass** | 见 A2（实测首行 `#1\tGET /api/agents\t↔\thub api agents`、末行 `#21\tPOST /api/confirmations/<confirmation_id>/decision\t↔\thub api confirmations decide`） |
| T1-6 | 订阅面 `kind` 与 §3 用途列 SSE 行一致（4 / 17） | **pass** | test #2；变异 M8（删 #9 行的 `（SSE）`）→ 转红，消息点名 `#9 GET /api/stream?chat_id=<id>：文档 result ↔ 入口表 stream` |
| T1-7 | 位置参数 ↔ 端点参数（含 `#9` 查询参数形态 / `#16` 空 args） | **pass** | test #3：`args[i].name ∈ 路径参 ∪ 查询参`，且路径参数按出现序是 `args` 名序子序列；基线全绿 ⇒ `api stream chat` 的 `chat_id`（查询参形态）与 `api stream calls`（args 空、`--chat-id`）均未被误判 |
| T1-8 | scoped 跑绿、无 `skipped`/`todo`、不触发全量套件 | **pass** | `# tests 12 / pass 12 / fail 0 / skipped 0 / todo 0`；命令为单文件 scoped 调用 |

#### T2（5 项）

| # | 标准（摘要） | 判定 | 证据 |
|---|---|---|---|
| T2-1 | `dispatch` 区段解析恰 8 条 `case`；≠8 点名实际条数 | **pass** | `parseDispatchMethods` 以 `async function dispatch(` + `\n  }\n` 取区段，抽 `^\s+case '…':`；变异 M2 为「条数不变、名字漂移」——证明**条数断言不是唯一防线**，双向比对独立生效 |
| T2-2 | 层 B 双向 + 顺序一致 + `method`/`cmd` 名面 | **pass** | test #4：`missing`/`extra` 双向为空、`orderFindings` 为空、`cmd.length===1 && cmd[0]===method`。变异 M16（`task_get`↔`task_list` 分支对调）→ 转红 |
| T2-3 | 单方法形态与 `--as` 接受面（`acceptsAs` 恰 4 条且为指定四方法） | **pass** | 独立复算 `acceptsAs === true` 的恰为 `uds agent.heartbeat` / `uds agent.deregister` / `uds message.send` / `uds message.ack`（与 §5.1 层 B 表 ✗/✓ 列一致）；`path === null`、`args.length === 0` 全 8 条成立 |
| T2-4 | 8 行对照清单机械产出 | **pass** | `uds <方法名>\t↔\toamp/src/router.js dispatch 分支 <方法名>`；实测标题行「层 B 对照清单（router.js dispatch 分支 ↔ hub uds 入口表） —— 共 8 行」 |
| T2-5 | scoped 跑绿 | **pass** | 同 T1-8 |

#### T3（5 项）

| # | 标准（摘要） | 判定 | 证据 |
|---|---|---|---|
| T3-1 | `main(argv)` 分块展开恰 11 叶子；≠11 点名实际条数与分支数 | **pass** | `parseCliLeaves` 以 `export async function main(` + `\n}\n` 取体，按 `if (cmd === '…') {` 分块，`validSubs` / `sub !== '…'` / 单 token 三态展开；变异 M3a/M3b/M5/M9 的消息均含「应得 11 个叶子命令，实际 N 个（顶层分支 M 个）：[完整清单]」 |
| T3-2 | 层 C 双向（**集合口径**，顺序不断言） | **pass** | test #5 仅有 `missing`/`extra` 断言，**无顺序断言**（与 C9 一致；反查源码确认）；变异 M9（`up`→`launch`）→ 点名 `层 C 入口表缺项（cli.js 有、ENTRIES 无）：cluster launch` |
| T3-3 | 名面与条目形态（11 叶子；`method`/`path`/`args`/`flags` 全空） | **pass** | 独立复算：层 C 11 条 `method===null && path===null && args.length===0 && flags.length===0` 全部成立 |
| T3-4 | 11 行对照清单机械产出 | **pass** | `cli <叶子命令>\t↔\toamp <叶子命令>`；实测标题行「层 C 对照清单（cli.js 叶子命令 ↔ hub cli 入口表） —— 共 11 行」 |
| T3-5 | scoped 跑绿 | **pass** | 同 T1-8 |

#### T4（6 项）

| # | 标准（摘要） | 判定 | 证据 |
|---|---|---|---|
| T4-1 | 分层可判定（`LAYERS`、40 条、层归属 = 首 token、`id` 前缀、三层计数） | **pass** | test #6（见 A5） |
| T4-2 | `doctor` 单列，不进 40 条 | **pass** | test #7：零条 `cmd[0]==='doctor'`、零条 `id` 含 `doctor`，且 `40 === 21+8+11` |
| T4-3 | 无同名同形（名面/id 唯一、同 `cmd` 不跨层、单条内不重名） | **pass** | test #8（见 A6） |
| T4-4 | 跨层近名三处样例逐行核 | **pass** | test #9；`CROSS_LAYER_SAMPLES` 的 9 个名面与 `architecture.md:399-407` 三样例逐字一致；三组各自「三名面都存在 + 组内两两不同 + 首 token 分属 api/uds/cli」 |
| T4-5 | 无编排条目（判据 ①~⑤，含字段集恰 10 项） | **pass** | test #10（见 A7）；字段集独立复算：40 条 `Object.keys(e).sort()` 全等且 = `[acceptsAs, args, cmd, flags, id, kind, layer, method, path, run]`（与 `surface.js:8-17` 的封闭声明一致，未被并行 PR 扩张） |
| T4-6 | scoped 跑绿 | **pass** | 同 T1-8 |

#### T5（5 项）

| # | 标准（摘要） | 判定 | 证据 |
|---|---|---|---|
| T5-1 | 零依赖守门（静态白名单扫描自身；读取点收敛） | **pass** | test #11；守门非空已证伪：M10（注入 `writeFileSync`）→ `命中写盘 API writeFileSync（1 处）`；M11（注入 `node:http`）→ import 白名单断言点名实际列表；M12（读取点漂移）→ 转红。`readFileSync` 调用点恰 2 处（`file, 'utf8'` / `SELF_FILE, 'utf8'`），并断言 `const file = path.join(OAMP_ROOT, rel)` 存在 + `SOURCES` 取值面恰三个路径 |
| T5-2 | 拾取性（文件名落 glob + scoped 可执行） | **pass** | 见 A9；`test/sdk-surface.test.js`，`scripts.test = node --test test/*.test.js` 零 diff |
| T5-3 | 改动面恰一条新增路径 | **pass** | `git diff --name-only 453326c..HEAD -- oamp/` = `oamp/test/sdk-surface.test.js`（A，579 插入）；对 `oamp/sdk/**`、`API.md`、`skill/hub.md`、`src/**`、`package.json`、`test/sdk-skill.test.js`、`test/helpers/**` 零 diff；`git status --porcelain -- oamp/` 为空 |
| T5-4 | 失败可定位（缺项/多出项分列点名；行数异常点名；清单不因失败丢失） | **pass** | 断言消息中 `missing`/`extra` 分列并点名签名；解析面消息含「实际 N 行 / N 条」；层 A 的三处失败消息追加 `detail`（21 行完整对照清单），对标 `C8` 的「对齐信息随失败消息一并给出」 |
| T5-5 | PR 9 条验收逐条对位 + 可复现证据 | **pass** | `pr-006-tasks.md` §3 已给出 9→任务对位表且自查「无遗漏」；本次由**独立执行**逐条复现（组 A 9/9 pass，命令与输出摘要见本报告「独立执行的原始命令与结果」）。注：执行者未在 worktree 留 PR 级证据文件，证据由本报告承载（见偏差 D2） |

### 组 C · architecture §5.1 / §7 / §10 T-07 与 prd

| # | 标准 | 判定 | 证据 |
|---|---|---|---|
| C1 | §5.1 层 A 表 21 条 | **pass** | 逐段抽取架构表「SDK 子命令」列 vs ENTRIES 层 A：21/21，**逐条同序完全相等** |
| C2 | §5.1 层 B 表 8 条 | **pass** | 同上：8/8，逐条同序完全相等 |
| C3 | §5.1 层 C 表 11 条 | **pass** | 同上（按未转义 `\|` 正确切分含 `'<json>'\|@file` 的第 4 行）：11/11，逐条同序完全相等 |
| C4 | §5.1 三层合计 40 条 | **pass** | `21 + 8 + 11 = 40`，且 `ENTRIES.length === 40`；`doctor` 零条（P-4） |
| C5 | §7 F02 行（验收 1「21 行对照清单 ✓ 表可机械导出」） | **pass** | 见 A1/A2（清单由用例机械产出，非手抄） |
| C6 | §7 F03 行（验收 1「8 行清单 ✓」） | **pass** | 见 A3 / T2-4 |
| C7 | §7 F04 行（验收 1「11 行清单 ✓」） | **pass** | 见 A4 / T3-4 |
| C8 | §7 F14 行（验收 1 分层可判定 ✓ 前缀即层；2 跨层无歧义 ✓ 三样例；3 无编排 ✓；4 无同名同形 ✓） | **pass** | 见 A5/A6/A7 与 T4-1~T4-5 |
| C9 | §10 T-07（T1 行）：三层入口表 ↔ 覆盖面双向比对（层 A 21 ↔ API.md §3 解析结果 / 层 B 8 ↔ 方法名清单 / 层 C 11 ↔ `src/cli.js` 命令分发表）；对照清单由本用例机械产出；无同名同形（F14 验收 4） | **pass** | 层 B 的实现以 `router.js` `dispatch` 分支解析落定（比「方法名清单」更硬的可判形态）；其余逐项见 A1~A7 |
| C10 | `prd/F02` 验收 1（21 条逐条可达、无遗漏、无自发新增；一张 21 行对照清单，双向无缺项） | **pass** | 见 A1/A2。行为面（验收 2/3/5）属 pr-007 用例面，不在本 PR 判定范围 |
| C11 | `prd/F02` 验收 4 的"各有可寻址入口"面（T1 追溯引用项） | **pass** | 4 条 SSE 端点各有唯一条目（`api stream chat/events/calls/call`），`kind === 'stream'` 恰 4 条；见 A5/T1-6 |
| C12 | `prd/F03` 验收 1（8 个方法逐条可达、无遗漏、无自发新增；一张 8 行对照清单） | **pass** | 见 A3 / T2-4 |
| C13 | `prd/F03` 验收 4 的拓扑盘点面（§7 F03 行「`uds router.status` + `api agents`」） | **pass** | 两处名面均存在于 ENTRIES 且层归属分别 = `uds` / `api` |
| C14 | `prd/F04` 验收 1（既有命令逐条可达；"既有命令 → SDK 入口"清单；双向无缺项） | **pass** | 见 A4 / T3-4；CLI 真源解析（`main(argv)` 展开 11 叶子）与 ENTRIES 层 C 集合相等 |
| C15 | `prd/F14` 验收 1（分层可判定；判定面 = 40 条，P-4） | **pass** | 见 A5。人判协议（"找一位未参与实现的人"）的机械投影 = "层归属 = 首个 token"，用例逐条断言 |
| C16 | `prd/F14` 验收 2（跨层无歧义；同资源两层可达时依结构可判） | **pass** | 见 A6 / T4-4（三处样例） |
| C17 | `prd/F14` 验收 3（清单中不含编排命令） | **pass** | 见 A7（每条单一目标 + 三个双射） |
| C18 | `prd/F14` 验收 4（两层之间无同名同形条目；近名条目归属可由结构唯一确定） | **pass** | 见 A6（名面/id 唯一 + 同 `cmd` 不跨层 + 单条内不重名） |

## 变异注入矩阵（守门能力的独立证伪）

全部变异在 **`mktemp -d /tmp/pr006-*` 的 `oamp/` 副本**内执行；仓库真源零触碰（还原后 hash 证据见下）。

| # | 注入点 | 期望 | 实测 | 判定 |
|---|---|---|---|---|
| M1 | `API.md` §3 #1 路径 `GET /api/agents` → `/api/instancez` | 红 + 点名端点 | `not ok 1`；`层 A 入口表缺项（§3 有、ENTRIES 无）：GET /api/instancez` | ✅ 拦住 |
| M2 | `src/router.js` `case 'router.task_list':` → `'router.tasklist_x':`（**条数不变**） | 红 + 点名方法 | `not ok 4`；`层 B 入口表缺项（dispatch 有、ENTRIES 无）：router.tasklist_x` | ✅ 拦住 |
| M3a | `src/cli.js` `task` 的 `validSubs` 增 `'purge'` | 红 + 点名 | `not ok 5`；`…应得 11 个叶子命令，实际 12 个（顶层分支 6 个）：[完整 12 条]` | ✅ 拦住 |
| M3b | 同上去掉 `'watch'` | 红 + 点名 | `not ok 5`；`…实际 10 个（顶层分支 6 个）：[完整 10 条]` | ✅ 拦住 |
| M4 | `API.md` §3 #21 路径改为不存在的端点（行数仍 21） | 红 + 点名 | `not ok 1` + `not ok 3`（层 A 双向 + 位置参数）；点名 `POST /api/confirmations_nope` | ✅ 拦住 |
| M5 | `src/cli.js` 增一个顶层分支 `if (cmd === 'doctor2') {` | 红 + 点名 | `not ok 5`；`…实际 12 个（顶层分支 7 个）：[… | doctor2]` | ✅ 拦住 |
| M6 | `API.md` §3 真删一整表行（#20） | 红 + 实际行数 | `not ok 1/2/3`；`API.md §3 表区段应解析出 21 行，实际 20 行` | ✅ 拦住 |
| M7' | `API.md` §3 真增一整表行（#22 ghost） | 红 + 实际行数 | `not ok 1/2/3`；`…应解析出 21 行，实际 22 行`（`22 !== 21`，expected/actual 结构化） | ✅ 拦住 |
| M7a | 畸形插入（把 #21 行拦腰截断） | 红 + 点名异常行 | `not ok 1`；`API.md §3 第 21 行的编号应为 21，实际 22（GET /api/ghost）` —— 严格解析把畸形行暴露为编号失败并点名该行 | ✅ 拦住（附带证据：解析面不静默吞行） |
| M8 | `API.md` §3 #9 用途列去掉 `（SSE）` | 红 + 点名行 | `not ok 2`；`层 A kind 与文档 SSE 面不一致：\n#9 GET /api/stream?chat_id=<id>：文档 result ↔ 入口表 stream` | ✅ 拦住 |
| M9 | `src/cli.js` `cluster` 的 `validSubs` 把 `'up'` → `'launch'` | 红 + 点名 | `not ok 5`；`层 C 入口表缺项（cli.js 有、ENTRIES 无）：cluster launch` | ✅ 拦住 |
| M10 | 往**用例自身**注入 `writeFileSync('/tmp/x','y')` | 守门应红 | `not ok 11`；`命中写盘 API writeFileSync（1 处）` | ✅ 守门非空 |
| M11 | 往**用例自身**注入 `import http from 'node:http'` | 守门应红 | `not ok 11`；白名单断言点名实际列表（含 `node:http`） | ✅ 守门非空 |
| M12 | 把 `readFileSync(file, 'utf8')` 改成 `file + '.bak'` | 红 + 点名路径 | `not ok 1/2/3/4/5/11`；`期望可读的 API.md（未命中或不可读：ENOENT）` | ✅ 拦住 |
| M15 | `API.md` §3 #1↔#2 行序互换（编号同步改） | 顺序锁应红 | `not ok 1`（顺序断言） | ✅ 拦住 |
| M16 | `src/router.js` `dispatch` 分支 `task_get`↔`task_list` 对调 | 顺序锁应红 | `not ok 4`（`orderFindings` 断言） | ✅ 拦住 |
| M13 | `sdk/surface.js` 层 A flag 名 `state` → `states` | —— | **12/12 全绿（未拦住）** | ⚠️ 覆盖边界，见偏差 D1 |

**结论**：15/15 个"应拦住"的变异全部转红，其中 12 个**点名**到具体端点/方法/叶子/行号；M10/M11/M12 证明零依赖守门本身非空。M13 揭示一处**规格未要求**的覆盖边界（见 D1），不影响任何判定。

### 还原后 hash 证据（仓库真源零留痕）

变异前 = 变异后 = 最终态，逐字节一致：

```
df3839f0d7cb60ca70041e3fe77832cf75017481a5b5e8a19acaa1c64e24b563  oamp/API.md
b4c588b99a0eb8cc6f57cabd91ca291594ff6c8e84be026b6dbcbdb05aa2fdd3  oamp/src/router.js
09473d41dbdf40e456ff8b78c90685855bd435e429ddfe20074406e08c671f0b  oamp/src/cli.js
e5e4b500c956401bcad29ec61a554d933a862747eda3306ffecf9f7aab6de910  oamp/sdk/surface.js
74c854a0f49e15877a6bb3a2976200cae8e0cd6ea1fff84855f6a932fe9b4952  oamp/skill/hub.md
c3a942e8d86a6c403db4fb6fe1cd058d9af114a302dc7e89aff4704cd9ba6d9f  oamp/test/sdk-surface.test.js
```

配套证据：`git -C <worktree> status --porcelain -- oamp/` 为空；`git diff --name-only 453326c..HEAD -- oamp/` 仍恰为 `oamp/test/sdk-surface.test.js`。

## 汇总计数

| 维度 | 值 |
|---|---|
| 判定条目总数 | **56**（组 A 9 + 组 B 29 + 组 C 18） |
| pass | **56** |
| fail | **0** |
| partial | **0** |
| blocked | **0** |
| 变异注入 | **17 次**（16 次致红并点名 / 1 次揭示覆盖边界） |
| 偏差记录 | **3** |
| 下一迭代候选 | **3** |

## 偏差记录

> 逐项判定针对「实现是否满足规格」；偏差记录针对「规格 / 文档是否准确描述当前实现」。**不影响上面的 pass/fail 判定与结论。**

| # | 规格·文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D1 | `architecture.md` §5.1 规则 3：「query / body 字段 → `--<字段名>`（`_` → `-`）」——即 flag 名由 `API.md` 字段名机械推导 | 本用例不核 flag 名与 `API.md` 字段名的对应：变异 M13（`sdk/surface.js` 层 A `--state` → `--states`）用例 **12/12 全绿**。用例的三个对照面只覆盖 `method`/`path`/`args`/`kind`/层归属/名面 | 不属 pr-006 任何验收条目（PR 验收 1~9 与 T1~T5 均未要求 flag 面对照）⇒ **不返工**。登记为下一迭代候选 N1 |
| D2 | `pr-006-tasks.md` T5 验收 5：PR 9 条验收逐条对位「并有可复现证据（命令 + 输出摘要）」 | worktree 的 `prs/` 下只有 `pr-006-tasks.md`（含 §3 的 9→任务对位表），**无执行者自己的 PR 级证据文件**；可复现证据由本独立验证报告承载 | 无需返工：pr-006 的文件范围只允许写 `oamp/test/sdk-surface.test.js`，PR 级证据属流程产物。收口 PR 汇总时从本报告回填证据（见 N2） |
| D3 | PR 文件「代码锚点」写 `oamp/test/api-routes.test.js:250`（`shapePath` 归一手法） | 实测该文件 `function shapePath` 在 **`:292`**（`:297` 为 `docSignatures`）。其余锚点全部自验通过：`API.md` §3 标题 `:157` / 首行 `:161` / 末行 `:181` / `### 3.1` `:187`；`router.js` `dispatch` `:104` + 8 分支 `:109/161/186/201/348/379/384/395` + `default` `:405`；`cli.js` `main` `:62` + `validSubs` `:107`/`:117` | 无需返工：`C6` 明确禁止把行号写进断言，用例不含任何行号锚；该过期行号仅存在于 PR 文件（流程产物）。`pr-006-tasks.md` §5-8 已登记同一事实更正 |

## 下一迭代候选

| # | 候选 | 来源 | 代价 / 位置 |
|---|---|---|---|
| N1 | 把「层 A `flags[].name` ↔ `API.md` 对应行字段名的机械推导（`_`→`-`）」纳入漂移锁 | 偏差 D1（变异 M13 未被拦住） | 小：在层 A 循环内解析该行「用途」单元格或 §3.1+ 小节即可现算；但需先确认 flag 面是否已由 `test/sdk-api.test.js`（pr-007）的行为面覆盖，避免重复锁 |
| N2 | 收口 PR 汇总时，以本报告的 56 项判定 + 17 次变异矩阵作为 pr-006 的验收证据存档 | 偏差 D2 | 小：仅文档回填 |
| N3 | 把「三处跨层样例的 9 个名面」纳入 `sdk-skill.test.js` 侧的同等断言（当前仅 pr-006 侧核） | 观察到：`skill/hub.md` 与 `surface.js` 的 40 条名面已由 pr-005 锁死，但「样例组内两两不同 + 首 token = 层前缀」这一 F14 验收 2 的判定样本只在 pr-006 侧 | 小；属可选加固（F14 验收 2 已由本 PR 覆盖，非缺口） |

## 结论

**PASS**

- 56 项判定全部 pass，0 fail / 0 partial / 0 blocked。
- 产出物真实存在且改动面严格封闭：`oamp/test/sdk-surface.test.js` 为唯一新增路径，`oamp/sdk/**` / `API.md` / `skill/hub.md` / `src/**` / `package.json` / `test/sdk-skill.test.js` 零 diff。
- 用例**真能拦住漂移**（这是本 PR 的核心价值）：15/15 应拦住的变异全部转红，12 个点名到具体端点/方法/叶子/行号；零依赖守门经 3 次注入证伪（非空）。
- 用例**零依赖**：import 白名单恰 6 项、无网络/子进程/写盘、跑前跑后 85 文件 sha256 全等、仓库内无 `.runtime`/`data` 创建、cwd 无关（从 `/` 跑仍全绿）。
- 冻结跨 PR 契约成立：40 条 cmd 名面与已合并 `oamp/skill/hub.md` 三层清单**逐条同序完全相等**，且与 `architecture.md` §5.1 三层表逐条同序完全相等。
- 3 条偏差均**不阻塞**（D1 属规格未要求的覆盖边界、D2 属流程产物、D3 仅 PR 文件里的过期锚点且用例不含行号断言），已分别落到下一迭代候选。
- 按用户 2026-09-15 口径，本 PR 只做 scoped 验证；仓库级全量套件留待全部开发完成后统一跑。
