# verify-pr-009 — 独立验证报告（pr-009 · 统一调用契约用例）

**验证者身份（反射）**：本产出物 = 一个**测试用例文件**（`oamp/test/sdk-cli-contract.test.js`，1082 行，新建）。对测试产物的"通过"判定不能只看"用例自己跑绿"，必须判**它是否真的在约束被测系统** ⇒ 反射出的验证者 = **独立测试审查者 + 变异注入审计者**：①逐条对位验收标准与用例断言；②脱离用例源码自行复跑被测行为（自建桩服务/自己的探针）；③在 `mktemp -d` 副本内注入变异，确认用例**能转红**（证明非空转）。

| 项 | 值 |
|---|---|
| 产出物 | `oamp/test/sdk-cli-contract.test.js`（新建，1082 行，`+1082 -0`） |
| worktree | `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0025-hub-sdk-and-skill/.pb-agents/worktrees/0025-pr-009-sdk-cli-contract-test` |
| 实现提交 | `9266740 test(hub): 补齐统一调用契约的进程面用例（F04~F10 / G01 / G02）`，base `3ba06dd` |
| 验证标准来源 | ① `docs/iterations/0025-hub-sdk-and-skill/prs/pr-009-sdk-cli-contract-test.md`「验收标准」段；② 同目录 `prs/pr-009-tasks.md`（T1~T9 / C1~C12 / MI-1~MI-10）；③ `architecture.md` §3.3(P-1/P-2/P-3) · §5.3 · §5.4 · §10 T4；`prd/{F04~F10,G01,G02}`；④ 主 agent 两项已生效裁决（简报注入） |
| 日期 | 2026-09-15 |
| 环境 | Node `v22.15.0`；`oamp/` `dependencies = {}`（T9 用例断言 + 逐字复核） |
| 执行纪律 | 只跑本 PR 自己的 scoped 用例（`cd oamp && node --test test/sdk-cli-contract.test.js`），**未跑仓库级全量**；未修改 `oamp/**` 任何文件；未执行任何 git 写命令 |

---

## 1. 改动面封闭（先验事实）

```
$ git -C <worktree> diff --name-status 3ba06dd..HEAD
A       oamp/test/sdk-cli-contract.test.js
$ git -C <worktree> diff --name-only 3ba06dd..HEAD -- oamp/sdk oamp/bin oamp/package.json \
    oamp/API.md oamp/llms.txt oamp/src oamp/web oamp/test/helpers \
    oamp/test/api-routes.test.js oamp/test/hygiene.test.js oamp/test/cli.test.js \
    oamp/test/sdk-skill.test.js oamp/test/sdk-surface.test.js
（空）
$ git -C <worktree> status --porcelain
?? docs/iterations/0025-hub-sdk-and-skill/prs/pr-009-tasks.md      ← 工作流产物，非代码面
```

`oamp/sdk/**` 七文件的 md5（验证全程前后一致，未被本 PR 也未被我触碰）：

| 文件 | md5 |
|---|---|
| `sdk/http.js` | `2ced0a3204b27ddcaecfd1008ab65fed` |
| `sdk/cli.js` | `c0524ee64c5c3199d56d8477b6e9fe8c` |
| `sdk/surface.js` | `b7087cccd010f2c34db9c1a099dc549f` |
| `sdk/index.js` | `c8643c3d6158352da8e561a8c46f3d9d` |
| `sdk/errors.js` | `98aeb12c0634d418738fd913ca657e95` |
| `sdk/uds.js` | `d24ea0094e69cfcc18f064485003eb84` |
| `sdk/doctor.js` | `49b3fdbb52d75e1312a1ff22466ca482` |

**判定：pass**（改动面恰一条新增；`oamp/sdk/**` 零 diff ⇒ 已知偏差锁"只登记不修"成立）。

---

## 2. scoped 用例执行（必须自己跑，且 ≥2 次）

| # | 命令 | 结果 | 耗时 |
|---|---|---|---|
| 1 | `cd <worktree>/oamp && node --test test/sdk-cli-contract.test.js` | `# pass 12 # fail 0 # skipped 0 # todo 0`（exit 0） | 22323 ms |
| 2 | 同上（连跑） | 12/12 pass，`skipped 0 / todo 0` | 22205 ms |
| 3 | 同上（带端口采样） | 12/12 pass（exit 0） | 22206 ms |
| 4 | 同上（带端口/进程归属采样 + 残留复核） | 12/12 pass（exit 0） | 22205 ms |
| 5 | `mktemp -d` 副本内同用例（副本保真度基线） | 12/12 pass，`skipped 0 / todo 0` | ~22 s |

**判定：pass**（四次独立运行全绿、零 skip/todo；用例确实被 `test/*.test.js` 拾取面覆盖 —— T9 用例断言 `pkg.scripts.test === 'node --test test/*.test.js'` 且 `path.dirname(THIS_FILE) === <ROOT>/test`）。

---

## 3. 逐项判定 A —— PR 文件「验收标准」10 条

| # | 验收条 | 判定 | 证据（可定位） |
|---|---|---|---|
| 1 | 退出码四类各一例且固定；仅凭退出码可区分 | **pass** | 用例 `T1 · 退出码四类矩阵`（表驱动 5 行：`0`/`1`/`2`/`3`×2，Google: 每行连跑两次同码）+ 码集合 `[0,1,2,3]` 断言。**我的独立探针**（自建桩，见 §7-E1）：`0→0 / 404→1 / 用法→2 / Web 不可达→3 / UDS 不可达→3`，`distinctCodes=[0,1,2,3]` |
| 2 | 用法错误样本覆盖 7 类（含 `--port abc` / `--wait` 给非阻塞条目 / `--as` 给非身份方法）且零连接 | **pass** | 用例 `T2` 13 行样本；**我的探针**同 13 行样本 ⇒ 全部 `code 2`，计数服务器 `{connections:0, requests:0}`；同 env 下合法命令 `api docs` ⇒ `code 0`（**证明计数面不是空转**：若用例真的连了，码不会是 2） |
| 3 | `--human` 两态：形态不同 + 事实逐项一致 + 默认仍 JSON | **pass** | 用例 `T3`（`routes.length+1` 行、表头含 `method/path`、逐条 method/path 出现、单一数组载荷无 `键: 值` 段、`chats list` 有 `键: 值` 段）；**我的探针**：默认 `{"routes":[…]}` 可 parse / `--human` 为 3 行表格且不可 parse、两态均 code 0、事实条目一致 |
| 4 | stdout/stderr 分离；失败为 stderr 单行 JSON（层 A 带 `http_status`） | **pass** | `parseErrorLine()`（恰一行 / 无 `\n    at ` / 不以 `Error:` 开头 / 键集合精确匹配 / `error` 非空）；我的探针实测层 1 失败 `{"code":"NOT_FOUND","error":"chat 不存在: ghost","exit_code":1,"http_status":404}`，层 2/3 无 `http_status` |
| 5 | `--wait` 超时 → `WAIT_TIMEOUT`/1；同码不同 `code`；新进程取回终态（P-1 background） | **pass** | `T4`（1000/600/1500/8000ms 四组 + 与 `NOT_FOUND`/`HUB_UNREACHABLE`/`USAGE` 的四类互异断言）+ `T5`（background 立刻返回 `call_id` + 新进程 `calls get` 取回 + `transcript` 第二面）。我的探针：`--wait 1000` ⇒ `WAIT_TIMEOUT/1 @1043ms` |
| 6 | 管道截断 `\| head -1` ⇒ 自行退出 / 码 0 / 无残留进程 | **pass** | 用例 `T6`（`set -o pipefail` + 进程组 `ESRCH` + NDJSON 单行 + 端口释放）。**我的探针**：SSE 桩发 2 帧 ⇒ 退出码 `0`（非 SIGPIPE）、1 行、帧键 `{event,data}`、进程组 `ESRCH`、246ms 自退出（未靠超时强杀） |
| 7 | 层 C 零语义变更（逐字节）+ 启停类命令对照现场一致 | **pass** | 用例 `T7` 四组逐字节（`status` 零节点 / `task list` / `status` 不可达态 / `agent start` 用法面）+ `queryStatus` 前后相等。**我的探针独立 4 组**：code/stdout/stderr 三者逐字节相等（含不可达态 `1` 双侧相同 ⇒ **既有 `1` 未被重分类为 `3`**） |
| 8 | 跨进程无状态：并发两进程各自完整；执行前后不新增状态文件 | **pass** | 用例 `T8`（`Promise.all` 两跑逐字节相等 ×2 组 + 新进程 `chats get` 续查 ×2 间隔 + `oamp/` 递归路径集合前后 `deepEqual` + 临时绝对路径结构性断言）。变异 M6（§8）证明"零仓库写"断言**非空转** |
| 9 | 经 `runHub()` 起子进程；不写仓库内 `.runtime/` / `data/` | **pass** | 全文件 `runHub` 为唯一进程入口（唯一例外 = T6 的 shell 管道，已在 tasks §5.2-2 登记）；`OAMP_DB`/`OAMP_SOCKET` 均为 `os.tmpdir()` 绝对路径；实测 `oamp/.runtime`、`oamp/data` 两个目录**全程不存在** |
| 10 | 经 `node --test test/*.test.js` 被拾取并通过 | **pass** | 文件名 + 位置断言（T9）；scoped 四连跑全绿；按用户 2026-09-15 口径**未**触发仓库级全量 |

**A 汇总：10 / 10 pass。**

---

## 4. 逐项判定 B —— tasks T1~T9

| 任务 | 判定 | 证据 |
|---|---|---|
| T1 骨架 + 基建原语 + 退出码四类矩阵 | **fail（仅"主 agent 修订条"一处，见 §6-基准 #1）** | 字面条款全部成立：import 面恰 `node:` + 三个既有 helper（零第三方）；`PORT_SEGMENT=[53000,53999]` + 与 `[51000,51999]/[52000,52999]/[54000,54999]` 的零交集断言（第 30/33/34/35 行）+ `pickFreePort()` 运行时探测（40 次重试，失败点名）；`setupHub(t)`/`startWeb()`/`runHubJson()`/`describeByteDiff()` 齐备；矩阵 5 行 + 两次同码 + 码集合；不可达态"单行 / 无堆栈 / 含目标地址 / ≤3000ms / 恢复后重跑得 0"。**但**主 agent 修订要求"T1 应含一组 `createHub()` 对照且该组内设进程 env" ⇒ `createHub` 在用例文件内 **0 命中**，**未落** |
| T2 用法错误样本集 + 零连接零副作用 + stdout/stderr 分离 | **pass** | 13 行样本覆盖 7 类（未知层 / 未知子命令×2 / 缺必填位置 / 位置参数多余 / 缺必填选项 / 选项缺值 / 取值非法×3 / 不接受选项×2 / 层 C 缺命令）；子串逐字命中实现文案（我的探针逐条复核一致）；计数服务器 `connections/requests === 0` 且同 env 合法命令码 0（反证非空转）；对照样本 `--port <已释放>` ⇒ 3 |
| T3 默认 JSON 单文档 + `--human` 两态 + 事实一致 | **pass** | 三条只读命令：`JSON.parse` 成功 + 单个尾换行 + `trim().split('\n').length===1` + 顶层键 `{routes}` / `{agents}` / `{chats,total,limit,offset}`；`--human` 形态不同、不可 parse、行数 = 表头+N、事实逐项出现；第三渲染分支（对象含额外标量键）显式覆盖 |
| T4 `--wait` 超时语义 + 5000ms 偏差锁 | **pass** | 超时形状 / 上限有序（600 < 1500 实测）/ 上限内成功（原样透传服务端体）/ 四类互异（4 个 `code` 互异 + 3 个退出码 `{1,2,3}`）/ 缺省上限界内观察（<2000ms，`1800000` 显式登记不可实测）/ 服务端 `requests === 1`（不重发）。偏差锁见 §6-基准 #2 |
| T5 P-1 background 取回终态 | **pass（附夹具名口径偏差 D-6）** | 夹具 = 本文件内自建 fake ACP（`chmod 0o755`，`acp` 常驻 + `-p` 一次式两形态），`OAMP_OMP_BIN` 注入 ⇒ 零真实 omp / 零外网；受理即返回 `call_id` + `state==='submitted'` + <3000ms；新进程取回逐字相同 + `waitFor` 有界轮询至终态 + `text/duration_ms/truncated` 形状；roster 恰 2 行（无自动重派）+ `transcript` 第二面 |
| T6 订阅管道截断 | **pass（附端口释放读法偏差 D-2）** | `bash -c 'set -o pipefail; … \| head -1'` 自建 + `detached` 进程组；"管道自行退出"用 `waitFor(exit!==null, 15000)`（超时即失败，不靠强杀）；退出码 `0`；NDJSON 恰 1 行 + 尾换行 + 键 `{event,data}`；进程组 `ESRCH`；≥2 帧前提由"探针节点注册 → 停止产生 offline"构造（我实测该构造确实产生 2 帧且 EPIPE 路径收尾） |
| T7 层 C 逐字节 + 启停面 + 零现场变化 + 无自动拉起 | **pass（附投影收窄偏差 D-3 / 非空断言缺口 D-4）** | 四组逐字节（同 bin / 同 env / 同 cwd / 同 stdio 形态，`Buffer.compare` + 退出码），失败消息带首个差异**字节**偏移 + 两侧上下文；只读组先断言 stdout 非空（防"空 vs 空"虚假相等）；不可达态证"不重分类"；`agent start` 用法面（code 2）；两组 500ms 观察窗 + `queryStatus` 投影相等（无守护/无自动拉起）；agent 不在线 ⇒ `NOT_FOUND`/1 + 同窗零新增 |
| T8 跨进程无状态 + 仓库零状态文件 | **pass（附计数硬编码偏差 D-7）** | 并发两跑逐字节相等（2 组）+ 失败消息打印两跑 `code` 与 stdout 前 200 字节；新进程续查（两种间隔）；`repoSnapshot` 递归集合前后 `deepEqual` + `.runtime`/`data` 存在性不变；`OAMP_DB`/`OAMP_SOCKET` 绝对路径且不在包根下 |
| T9 收口 | **pass** | 改动面恰一条新增（§1）；`package.json.scripts.test` 与 `dependencies` 断言；文件名/位置断言；零 `skip`/`todo`（全文件正则 `\.(skip\|todo\|only)\(` **零命中**）；零 flaky 结构（关键路径 `waitFor`，裸 `setTimeout` 仅 3 处且均为 tasks 已登记的固定观察窗/非关键间隔）；零 `Object.assign(process.env, …)`（全文件零命中） |

**B 汇总：8 pass / 1 fail（T1 修订条）。**

### C1~C12 与 MI-1~MI-10

C1 落点唯一 **pass**；C2 零新框架（import 面逐条复核）**pass**；C3 端口段（文件内 5 位字面量仅出现在第 8 行注释与第 30/33/34/35 行的段常量；零 `7788`）**pass**；C4 进程/临时态收口（长驻命令无 `runHub` 调用 ⇒ 无未给 `timeoutMs` 的长驻面；自建 spawn 均 `detached` + SIGKILL 整组；`t.after` 顺序 web→router→临时目录）**pass**；C5 断言不锚行号（全文件零 `sdk/*.js:NNN` 形态）**pass**；C6 逐字节口径 + 差异定位 **pass**（附 D-4）；C7 失败可定位（容器启动失败打印 stderr：`startWeb` 抛错带 `stderr=`）**pass**；C8 零上游改动 **pass**（§1）；C9 偏差锁写法 **pass**（§6-基准 #2）；C10 不重复其它 PR 用例面（不 import `sdk/index.js`、不写 doctor、不断言 pr-007/008/010 文件）**pass**（字面成立，但其与"基准 #1"的关系见 §6）；C11 零 flaky **pass**；C12 收口纪律（scoped-only）**pass**。

MI-1 端口段落形态 **pass**；MI-2 `--human` 第三分支 **pass**；MI-3 零连接判据 = 计数服务器 **pass**（我独立复核有效）；MI-4 UDS 不可达样本构造 **pass**；MI-5 不重试观测面 = 服务端 `requests === 1` **pass**（我独立复核：黑障 `requests` 恰等于调用次数）；MI-6 偏差锁可执行形态 **pass**；MI-7 P-1 夹具（fake ACP + `OAMP_OMP_BIN`）**pass**（夹具实例名口径偏差见 D-6/§9-1）；MI-8 启停类取样 = `agent start` 用法面 **pass**；MI-9 仓库零状态判据形态（`oamp/` 递归集合）**pass**（变异 M6 证明有判别力）；MI-10 "不补发"未写成 flaky/`skip` **pass**（用例内无该断言，符合"稳定才写、否则登记"）。

**C + MI 汇总：22 / 22 pass。**

---

## 5. 逐项判定 C —— architecture / prd

| 判据 | 判定 | 证据 |
|---|---|---|
| §5.3 默认输出 = 单结果一个 JSON 文档 + 换行；失败 stdout 空 + stderr 单行 JSON | **pass** | T1/T2/T3；探针同形 |
| §5.3 `--human` 渲染（数组⇒表头+数据行 / 对象含单数组键⇒该数组走表格+其余键逐行） | **pass** | T3 两分支 + `chats list` 标量键分支 |
| §5.4 四类归类表（首条命中者生效）可区分 | **pass** | T1/T2/T4 + 探针 `distinctCodes=[0,1,2,3]` |
| §5.4 层 C 退出码透传、既有 `1` 不重分类 | **pass** | T7 不可达态组（两侧码均 1 + stderr 逐字节相同） |
| §5.4 `0` 类"订阅被下游管道截断" | **pass** | T6（退出码 0）+ 探针复现 |
| §3.3 P-1（background 形态取回终态；block 不保证标识；不反查 roster） | **pass** | T5 §4 注释逐字登记该边界；实现面以 background 取回闭合，未做 roster 反查 |
| §3.3 P-2（层 C 原样透传，不 JSON 信封化） | **pass** | T7 逐字节四组 |
| §3.3 P-3（本地只判"构造请求所必需"的取值非法 ⇒ 2；服务端可判 ⇒ 1） | **pass** | T2 取值非法三例（整数 / JSON / JSON 对象）全 `2`；T1 业务失败（404）`1` |
| §10 T4 行（退出码四类 / `--human` / 分离 / `--wait` / `管道截断后无残留` / 层 C 逐字节） | **pass** | T1/T3/T2/T4/T6/T7 逐条对位；T6 的"无残留"由进程组 `ESRCH` + 探针复核 |
| prd F04-2（零语义变更）/ F04-3（只读零副作用） | **pass** | T7；只读组前后拓扑投影相等 |
| prd F05-1（默认 JSON）含"跨宿主 ①②③" | **partial（拆分）** | ①shell（`runHub`→`bin/hub.js`）**pass**；③另一 agent 经 shell（T6 的 `bash -c` 管道）**pass**；②Node `import` **fail** —— 本 PR 面无 `createHub()` 判据（根因同 §6-基准 #1） |
| prd F05-2/3/4 | **pass** | T3（形态切换 / 事实一致）+ T2（失败不污染 stdout） |
| prd F06-3（可截断、无残留） | **pass** | T6 |
| prd F07-1~5 | **pass**（验收 1 的"三宿主"面同 F05-1，见上） | T1/T2 + 探针 |
| prd F08-1~5（单行无堆栈 / 3 / 不挂起 / 两条路径 / 恢复无残留） | **pass** | T1 不可达态两路径（`ECONNREFUSED` + `ENOENT`），≤3000ms；恢复后同命令 ⇒ 0 |
| prd F09-1/2/3 | **pass** | T8（并发两跑 / 新进程续查 / 零仓库写）；F09-4（不补发）按 tasks MI-10 登记为不在本 PR 写（无 flaky、无 skip） |
| prd F10-1~5 | **pass** | T4（上限显式且有序 / 界内返回终态 / 超时单独一类 / 上限内成功）+ T5（不丢任务，background 取回） |
| prd G01-3/4（既有测试面不需改断言；服务边界不变） | **pass** | `oamp/**` 代码面零 diff ⇒ 两把漂移锁的输入（`API.md`/`llms.txt`/`src/web.js` 登记）与 `hygiene` 扫描面零变化；用例内亦无"改既有断言" |
| prd G02-2/3（启停类零语义变更；不自动拉起/重派） | **pass** | T7（用法面逐字节 + 两处 500ms 窗口零新增 + agent 离线调用明确 `NOT_FOUND`）+ T5（roster 无额外行） |

**C 汇总：19 pass / 1 partial（F05-1，其 ② 面即 §6-基准 #1 的根因）。**

---

## 6. 主 agent 两项已生效基准的核验

### 基准 #1 —— 要求本 PR 覆盖 Node import 侧（E4 跳宿主一致）：T1 应含一组 `createHub()` 对照且该组内设进程 env
**判定：fail（未落地）**
- 证据 1：`grep -c createHub oamp/test/sdk-cli-contract.test.js` ⇒ **0 命中**（全文件无 `createHub`，亦无 `await import('…/sdk/index.js')` 形态）。
- 证据 2：import 面逐条复核 = `node:test` / `node:assert/strict` / `node:child_process` / `node:net` / `node:http` / `node:fs` / `node:os` / `node:path` / `node:url` + `./helpers/hub-harness.js` + `./helpers/harness.js` + `./helpers/fake-node.js` —— **无 `sdk/index.js`**。
- 证据 3：`git diff 3ba06dd..HEAD -- oamp/sdk oamp/test` 仅 `A oamp/test/sdk-cli-contract.test.js` ⇒ 该组不可能落在其它文件。
- 影响：`prd/F05-1` 与 `prd/F07-1` 的判定明文含"三种宿主"，其 ② Node `import` 一路在本 PR 面**无判据**（①shell 与 ③另一 agent 经 shell 已覆盖）。
- 反差说明：tasks `C10` 字面写"不 import `sdk/index.js`（…若追加库面对照，**必须**按 A18 设进程 env）"，tasks §5.2-1 已把"是否要求 pr-009 覆盖 import 面"显式提请主 agent 裁决 —— 裁决为"要求"，但**未落**。dev 的交付严格贴合了 C10 的字面（而不是裁决后的口径）。

### 基准 #2 —— 已知偏差锁（`--wait ≥5000` 当前返回 `REQUEST_TIMEOUT`/3，归 pr-011 修）
**判定：pass（断言的是当前行为、无 skip/todo、无当下必失败断言、未改 `oamp/sdk/**`）**
- 用例名逐字含 `T4 [已知偏差 · 归 pr-011 收口修复]`（第 697 行）；注释逐字登记"规格期望 = `WAIT_TIMEOUT`/1；修复点 = `oamp/sdk/http.js` 的响应头上限与 `--wait` 的先后关系；修复后本断言应改为 `WAIT_TIMEOUT`/1"。
- 断言集合 = `code===3` + `code==='REQUEST_TIMEOUT'` + `exit_code===3` + `error` 含 `5000` + `stdout===''` + 耗时 ∈(4900,5400) + 黑障 `requests===1`。
- **我独立复现同一行为**（自建黑障，不经用例源码）：`--wait 8000 --mode block` ⇒ `{"code":"REQUEST_TIMEOUT","error":"等待响应超时（5000ms；127.0.0.1:53919）","exit_code":3}`，退出码 `3`，耗时 **5045 ms**；对照 `--wait 1000` ⇒ `{"code":"WAIT_TIMEOUT","error":"等待超时（1000ms）：调用仍在进行","exit_code":1}`，耗时 1043 ms ⇒ **锁与我观测的当前行为一致**。
- `grep` 零命中 `skip`/`todo`/`only`；`oamp/sdk/**` 零 diff（md5 表见 §1）。

---

## 7. 重点独立复核（脱离用例源码的自证）

| # | 复核项 | 方法与结果 | 判定 |
|---|---|---|---|
| E1 | 退出码四类可达且**仅凭退出码**可区分 | 自建桩（`/api/docs`→200 `{routes}`、其余→404 `{error,code:'NOT_FOUND'}`）+ 无监听端口 + 不存在的 socket：`0→0`、`404→1`、`bogus→2`、`--port abc→2`、Web 不可达→`3`、UDS 不可达→`3`；`distinctCodes=[0,1,2,3]`；`3` 类耗时 40~41 ms（不挂起）；每一类的 stderr 为单行 JSON 且键集合精确 | **pass** |
| E2 | 层 C 逐字节透传 | 4 组（`status` 不可达态 / `task list` 不可达态 / `agent start` 用法面 / 另一不存在 socket）：`code` 相等、`stdout`/`stderr` `Buffer.compare===0` **全部成立**（如 `status` 双侧 `code=1` 且 stderr 逐字节相同：`oamp: status 失败: 无法连接 oamp router（socket=…）`） | **pass** |
| E3 | 管道截断自退出 / 码 0 / 无残留 | SSE 桩发 2 帧后保持连接；`bash -c 'set -o pipefail; exec node bin/hub.js api stream events --port P \| head -1'` ⇒ 未超时、**246 ms** 自退出、退出码 **0**、`signal=null`、stdout 恰 1 行且 `JSON.parse` 得键 `{event,data}`、`process.kill(-pgid,0)` ⇒ **ESRCH** | **pass** |
| E4 | `--human` 两态事实一致 | 同一桩：默认 = 单 JSON 文档（顶层 `routes`）、stderr 空、code 0；`--human` = 3 行（`method  path` 表头 + 2 数据行）、不可 `JSON.parse`、含全部 method/path、code 0 | **pass** |
| E5 | 用法错误**零连接**（记录型服务端） | 计数服务器**会正常应答**（`200 {routes}`）⇒ 若真连通则码必为 0/1；13 行用法样本同 env（`OAMP_WEB_PORT=计数端口`, `OAMP_SOCKET=不存在 socket`）⇒ 全部 `code 2` 且 `{connections:0, requests:0}`；**对照**：同 env 下 `hub api docs` ⇒ `code 0` 且计数服务器被命中 ⇒ 判据非空转 | **pass** |
| E6 | 端口均在 `53000-53999` | ①静态：全文件 5 位数字字面量仅 `53000/53999`（第 8 行注释、第 30 行常量）与其余三段常量 `51000/51999/52000/52999/54000/54999`（第 33~35 行）；零 `7788`、零硬编码 `/tmp`、零 `/Users/`；②运行期采样（第 4 次运行，每 0.2 s `lsof -iTCP -sTCP:LISTEN`）：采样到 **13 个** 53xxx 监听端口 `53228 53274 53539 53632 53654 53708 53782 53821 53850 53916 53923 53926 53985` ⇒ **全部 ∈ 段内** | **pass** |
| E7 | 改动面封闭 | §1（git 三连验证） | **pass** |
| E8 | 测试后无残留子进程 / 无端口残留 | 第 4 次运行结束 1.5 s 后：`ps -ww -eo pid,command \| grep 0025-pr-009` ⇒ **空**；`lsof` 中 53xxx 监听 ⇒ **空**；`oamp/.runtime`、`oamp/data` ⇒ **不存在**；`git status --porcelain` 仅工作流产物 `?? docs/…/pr-009-tasks.md` | **pass** |

（探针脚本：`/tmp/pr009-probe.mjs`，位于仓库外，只读消费 `oamp` 包；产物 JSON 快照 `/tmp/pr009-probe-out.json`。）

---

## 8. 变异注入（`mktemp -d` 副本内；真源零触碰）

副本：`/var/folders/ss/pyt_516n0tsgb3dcqvn5zw900000gn/T/pr009-mut-i_anbwsv/repo`（`oamp/` + `roles/` 两份拷贝；仓库真源未参与任何注入）。
**真源零触碰证据（sha256 前 16 位，注入前后全等）**：

```
before: errors.js e75490f62719eb5b | cli.js 2a2fa4ae81dd08b5 | surface.js e5e4b500c956401b
        http.js aef7613905fdf6ec | sdk-cli-contract.test.js 6033503d46c41bd6 | hub-harness.js e5d615b0bfbbbb00
after : 同上，逐项相同（注入与还原只作用于副本）
```

| 变异 | 注入点（副本内） | 受限运行 | 结果 |
|---|---|---|---|
| — 基线 | 无 | 全量 | `pass 12 / fail 0`（副本保真） |
| M1 | `sdk/errors.js`：`EXIT_CONNECTION = 3` → `1` | `T1` | **fail 2**（`expected: 3 / actual: 1`，两条 T1 用例） |
| M2 | `sdk/errors.js`：`EXIT_USAGE = 2` → `1` | `T2` | **fail 1** |
| M3 | `sdk/cli.js`：EPIPE 分支 `process.exit(0)` → `exit(4)` | `T6` | **fail 1**（捕获到管道退出码变化） |
| M4 | `sdk/cli.js`：层 C `return typeof result.exit_code === 'number' ? … : 1` → `return 0` | `T7` | **fail 1** |
| M5 | `sdk/cli.js`：`--human` 置位 → `out.human = false` | `T3` | **fail 1** |
| M6 | `sdk/surface.js` 模块级写入仓库内文件 | `T8` | **fail 1**，消息逐字：`oamp/ 递归路径集合在调用前后必须完全相等（零新增、零删除）` + diff 行 `+ '.runtime-probe'` |
| M7 | 反事实：**用例副本**的夹具名 `pb-dev` → `dev-1` | `T5` | **fail 1**（`expected: 0 / actual: 1` ⇒ `calls create` 转 404） |

**结论：7 类变异全部被捕获 ⇒ 用例对该 PR 的判据面（归类表 / 用法码 / EPIPE 收尾 / 层 C 码 / `--human` / 零仓库写 / 角色反解夹具）**均有真实判别力**，不存在"跑绿但什么都没约束"的空转。

（旁证：M6 第二次运行**通过**，原因是我未清理副本内上一轮遗留的 `.runtime-probe` —— 这恰好符合该判据语义"只比对**观测窗内**的集合变化"；清理后立刻转红。）

---

## 9. dev 自报三处口径的独立判定

### 9-1 T5 夹具名取 `pb-dev` 而非 `dev-1`（理由：`/api/calls --agent <role>` 经角色反解要求 `pb-` 前缀）
**判定：成立。**
- 机制（读源码，非采信自述）：`src/web.js:995-999` —— `const agentId = instanceIdForRole(role); if (roleOfInstance(agentId) !== role) sendError(404, 'NOT_FOUND', 'agent 不可用: …')`；`src/web.js:277-279` 的 `roleOfInstance` = `roleFromInstanceId`；`src/role-binding.js`：`instanceIdForRole(role) = 'pb-' + role`，`roleFromInstanceId` 要求 `^pb-(.+)$` **且** `<roleRoot>/roles/<role>/<role>.md` 存在（`roleRoot` = 包根上级 = worktree 根）。
- 事实核对：`roles/dev/dev.md` **存在**（9941 B）；`roles/dev-1` **不存在**。
- 反事实实测（M7）：把副本用例的 `AGENT_ID` 改成 `dev-1` ⇒ T5 **转红**（`calls create` 得到 404 ⇒ 退出码 1 而非 0）。
- 结论：`pb-dev` 是**载荷所需**的选择（不是装饰），且与 tasks MI-7 字面 `dev-1` 的差异属合理修正（偏差 D-6）。

### 9-2 T7「零现场变化」投影为排序后的 `{instance_id, state}` 集合（排除 `last_heartbeat` / `session_id`）
**判定：成立，但属有意收窄，应显式登记（偏差 D-3）。**
- 成立面：`last_heartbeat` 在 `OAMP_HEARTBEAT_INTERVAL_MS`（`SHORT_ENV`）下高频变化 ⇒ 字面逐字段 `deepEqual` 在观测窗内**确实不可满足**（会假红）；投影保留 `instance_id` + `state` ⇒ 对"出现/消失注册、状态变化"仍有判别力，而这两点正是 F04-3 / G02-3"零副作用 / 无自动拉起"的观测对象（M6 类反事实亦证明同类快照判据非空转）。
- 收窄面：**在 T7 的零节点拓扑下**，`before/after` 恒为 `[]` ⇒ 对"只读组零变化"该断言是**平凡真**；且投影无法察觉"会话接管 / 心跳异常"（`session_id` 被排除）。
- 是否掩盖真问题：**否**。本 PR 的 T7 判据面是"零语义变更 + 无自动拉起"，不是"会话稳定性"；`session_id` 变化不在其判据内。但其"零现场变化"的措辞比实际判别力强 ⇒ 建议在用例注释中写明收窄范围（已有一条简短注释，未写明"零节点下平凡真"）。

### 9-3 T6 的「hub 端点端口 ≤1s 内不可连」读法
**判定：读法自洽且已在用例内注明，但该条**并不度量 hub 残留**（偏差 D-2）。**
- 用例第 942-944 行：先 `await hub.web.stop()`，再 `waitFor(() => isPortFree(hub.port), 1000ms)`。因此它度量的是"web 进程停机后端口释放"（合理但与本 PR 判据弱相关）。
- hub 的"无残留"由**同用例的进程组判据**承担（`process.kill(-child.pid, 0)` ⇒ `ESRCH`），该判据在管道成员（bash + hub + head）层面确证无残留 —— 我独立复现为 `ESRCH`（§7-E3）。
- 未发现"看起来通过但实则空转"的误导：注释逐字说明"hub 是客户端（不监听端口）⇒ 可判形态 = …"，没有把该条伪称为 hub 残留判据。

---

## 10. 偏差记录

| # | 偏差 | 位置 / 证据 | 影响 | 处置建议 |
|---|---|---|---|---|
| **D-1** | T1 矩阵末尾的 `classOf` 循环是**恒真断言**（`classOf[row.expectClass].includes(row.expectClass)` 对 `{0:[0],1:[1],2:[2],3:[3]}` 永远为真）⇒ 零判别力 | 用例 `:463-470` | 判据仍由"逐行期望码 + 码集合 `[0,1,2,3]`"覆盖 ⇒ 不影响 F07-4 的成立；属**死断言** | 删除，或改为真判据（样本→码 反查表：码 `n` 只允许由该类样本产生） |
| **D-2** | T6 的"端口 ≤1s 释放"判据在用例内先自停 web ⇒ 不度量 hub 残留（hub 从不监听） | 用例 `:942-944` | 低（hub 残留由进程组 `ESRCH` 判据独立覆盖） | 改为对订阅进程组/子进程的存活判据，或声明"本判据只覆盖 web 停机" |
| **D-3** | T7 的现场快照投影 `{instance_id, state}` 排除 `last_heartbeat`/`session_id`；零节点拓扑下 `before/after` 恒 `[]` ⇒ 只读组的"零变化"断言平凡真 | 用例 `:69-73`、`:972`、`:1002`、`:1013` | 中低（对"无自动拉起"仍有判别力；对心跳/会话面零判别力） | 注释写明收窄范围与"零节点下平凡真"；如需会话面判据另立 |
| **D-4** | C6 的"只读组须先断言 stdout 非空"仅在 `status` 组显式；`task list` 组仅断两侧相等 | 用例 `:979-985` | 低（零任务拓扑下实测 stdout 为表头单行非空） | 为 `task list` 组补非空断言 |
| **D-5** | 偏差锁耗时窗 `(4900, 5400)` 上界仅 400ms 余量 | 用例 `:713` | 低（4 次运行全绿；实测 5045ms） | 放宽至 `(4900, 6000)` 以抗并行负载；pr-011 修复后翻转期望 |
| **D-6** | 夹具实例名 `pb-dev` 与 tasks MI-7 字面（`dev-1`）不一致 | 用例 `:736`；判定见 §9-1 | 无（修正了 tasks 的字面错误） | 回填 tasks MI-7 的字面 |
| **D-7** | T5 断言 `roster.calls.length === 2` 硬编码既有实现的计数语义（messages send 亦登记一行调用） | 用例 `:779` | 低（同用例已先断言目标 `call_id` 行数 = 1） | 改为相对判据（先取基线计数，再断言 `+1`） |
| **D-8** | A18（库面 env 约束）在本 PR 面**未触发**（不 import `sdk/index.js`）—— 这同时是"基准 #1 未落"的另一面 | `grep createHub` 零命中 | 见 §6-基准 #1 | 随基准 #1 一并处置 |

---

## 11. 下一迭代候选

1. **补 Node import 侧（最高优先）**：在 `test/sdk-cli-contract.test.js` 增一组 `createHub()` 对照（类 `0`/`1`/`2`/`3` 各一 + `--human` 等价面），**组内设进程 env `OAMP_SOCKET`**（A18：`createHub` 无 `env` 选项，层 C 与 `doctor` 读调用方进程 env），或作为独立文件在 pr-011 收口。落地后 `prd/F05-1`、`prd/F07-1` 的"三宿主"②面才有判据。
2. **删除 T1 恒真断言**（D-1），换成"码→类"反查判据。
3. **T6 端口判据重述或加固**（D-2）：把"hub 无残留"与"web 端口释放"分成两条明示判据。
4. **T7 快照注释标明收窄范围**（D-3）；如后续需要会话面判据，另立"零新增 `session_id`"的条件断言（非零节点拓扑下）。
5. **T4 偏差锁耗时窗放宽**（D-5），并在 pr-011 修复后把期望翻转为 `WAIT_TIMEOUT`/1（用例内已预登记翻转点）。
6. **T5 roster 判据相对化**（D-7）。
7. **T2 计数面加固**：把"若连通则码必为 0/1"的对照（我在探针里做的）固化进用例，使"零连接"断言自带反空转证明。
8. **口径回填**：tasks MI-7 的 `dev-1` → `pb-dev`（D-6）；并在 tasks `C10` 注明"若主 agent 裁决覆盖 import 面，则 `C10` 的『不 import』条按 A18 例外面执行"（本次裁决与 C10 字面的冲突应显式记录）。

---

## 12. 结论

**判定：FAIL（1 条根因、2 个判据落点 + 1 个拆分项）**

- **判据项 71：pass 68 / fail 2 / partial 1 / blocked 0**（fail 2 项同根因 —— "主 agent 基准 #1：Node import 侧 `createHub()` 对照组"未落地；partial 1 项 = `prd/F05-1`、`F07-1` 的"三宿主"条目，其 ①shell / ③另一 agent 经 shell pass、②Node import fail）。
- **偏差记录 8 条**（D-1~D-8，均不阻塞；其中 D-1 为死断言、D-5 为时间窗余量）。
- **强证据面（可供主 agent 直接采信）**：
  1. scoped 用例**四连跑 12/12 全绿、零 skip/todo**，且**副本基线**同绿；
  2. **7 类变异注入全部被捕获**（归类表 / 用法码 / EPIPE 收尾 / 层 C 码透传 / `--human` / 零仓库写 / 角色反解夹具）⇒ 用例**有真实判别力**，非空转；真源 sha256 前后全等；
  3. **脱离用例源码的独立探针**复现了全部关键契约：退出码 `{0,1,2,3}` 可达且仅凭码可区分、13 行用法样本零连接（计数服务器 + 合法命令对照反证）、层 C 4 组逐字节相等、管道截断 246ms 自退出且码 `0`、进程组 `ESRCH`、`--human` 两态事实一致、已知偏差锁当前行为 `REQUEST_TIMEOUT/3 @5045ms`（与锁的断言一致）；
  4. 端口：静态零额外字面量 + 运行期 13 个 53xxx 监听端口全部落段内；测试后**无残留进程 / 无端口残留 / 无仓库写**；
  5. 改动面恰 `A oamp/test/sdk-cli-contract.test.js`，`oamp/sdk/**` 与全部上游面零 diff（含 md5 逐文件证据）。
- **所需返工的最小改动**：在 T1 追加一组 `createHub()` 对照（进程 env 设 `OAMP_SOCKET`），即可把它从 FAIL 翻为 PASS；其余 68 项判定与 8 条偏差均不需要返工。

**回报**：报告路径 = `docs/iterations/0025-hub-sdk-and-skill/clarifications/verify-pr-009-20260915-181653.md`；结论 = **FAIL**；fail 条目数 = **2（同根因 1 条）**；偏差记录条数 = **8**。
