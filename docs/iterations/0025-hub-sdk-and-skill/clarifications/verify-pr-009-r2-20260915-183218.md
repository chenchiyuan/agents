# verify-pr-009-r2 — 独立验证报告（第 2 轮 · 统一调用契约用例）

**验证者身份（反射）**：产出物 = 一个**测试用例文件**（`oamp/test/sdk-cli-contract.test.js`，1177 行，新建）。判"通过"不能只看"用例自己跑绿"——必须判它**是否真的在约束被测系统**。反射出的验证者 = **独立测试审查者 + 变异注入审计者**：①逐条对位验收标准与断言；②**脱离用例源码**自建桩服务 / 自建探针复跑被测行为；③在 `mktemp -d` 副本内注入变异，确认用例**能转红**（证明非空转）。本轮**不接收执行过程上下文**：只读产出物与三份标准。

| 项 | 值 |
|---|---|
| 产出物 | `oamp/test/sdk-cli-contract.test.js`（新建，1177 行，`+1177 -0`） |
| worktree | `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0025-hub-sdk-and-skill/.pb-agents/worktrees/0025-pr-009-sdk-cli-contract-test` |
| base / 实现提交 | base `3ba06dd`；`9266740`（首版 1082 行）+ `905bdc7`（返工 `+104 -9`，仅同一文件） |
| 验证标准来源 | ① `docs/iterations/0025-hub-sdk-and-skill/prs/pr-009-sdk-cli-contract-test.md`「验收标准」段；② 同目录 `prs/pr-009-tasks.md`（T1~T9 / C1~C12 / MI-1~MI-10）；③ `architecture.md` §3.3(P-1/P-2/P-3)·§5.3·§5.4·§10 T4；`prd/{F04~F10,G01,G02}` |
| 日期 | 2026-09-15 |
| 环境 | Node `v22.15.0`；`oamp/` `dependencies = {}`（用例断言 + 逐字复核）；**宿主事实**：`127.0.0.1:7788` 有常驻同版本 oamp web（PID 62417，主仓库 cluster）——见 §8-E11、§11-N-02 |
| 执行纪律 | 只跑本 PR 自己的 scoped 用例；**未跑仓库级全量**；**未修改 `oamp/**` 任何文件**；未执行任何 git 写命令；变异注入只在 `mktemp -d` 副本内（§9 有还原后 hash 证据） |

---

## 1. 改动面封闭与真源完整性

```
$ git -C <worktree> diff --name-status 3ba06dd..HEAD
A       oamp/test/sdk-cli-contract.test.js
$ git -C <worktree> diff --name-only 3ba06dd..HEAD -- oamp/sdk oamp/bin oamp/package.json oamp/API.md \
      oamp/llms.txt oamp/src oamp/web oamp/test/helpers oamp/test/api-routes.test.js oamp/test/hygiene.test.js \
      oamp/test/cli.test.js oamp/test/sdk-skill.test.js oamp/test/sdk-surface.test.js
（空）
$ git -C <worktree> status --porcelain
?? docs/iterations/0025-hub-sdk-and-skill/prs/pr-009-tasks.md      ← 工作流产物
?? docs/iterations/0025-hub-sdk-and-skill/clarifications/verify-pr-009-20260915-181653.md  ← 上轮报告
```
`905bdc7` 的 `--stat` = 同一文件 `+104 -9`（无第二个文件）。

**真源 sha256（本验证全程前后一致；我未触碰任何被验证产出物）**：

| 文件 | sha256（前 16 位） |
|---|---|
| `oamp/test/sdk-cli-contract.test.js` | `94ad6cd761062d7a` |
| `oamp/sdk/http.js` | `aef7613905fdf6ec` |
| `oamp/sdk/cli.js` | `2a2fa4ae81dd08b5` |
| `oamp/sdk/surface.js` | `e5e4b500c956401b` |
| `oamp/sdk/index.js` | `7f22000886542555` |
| `oamp/sdk/errors.js` | `e75490f62719eb5b` |
| `oamp/sdk/uds.js` | `a013979c0b3a2cf6` |
| `oamp/sdk/doctor.js` | `0904e929bb3ae8b8` |
| `oamp/src/task.js` | `11b6eb7d301b41db` |

**判定：pass**（改动面恰一条新增；`oamp/sdk/**`、`oamp/src/**`、既有 `test/*.test.js`、三个 helper 零 diff ⇒ 已知偏差锁"只登记不修"成立）。

---

## 2. scoped 执行（我自己跑，共 5 次）

| # | 命令（`cd <worktree>/oamp`） | 结果 | 耗时 |
|---|---|---|---|
| 1 | `node --test test/sdk-cli-contract.test.js` | `# tests 13 # pass 13 # fail 0 # skipped 0 # todo 0`（exit 0） | 22704 ms |
| 2 | 同上（连跑） | 13/13，`skipped 0 / todo 0`（exit 0） | 22465 ms |
| 3 | 同上（**带运行期端口采样 + 事后残留检查**，见 §8-E10/E12） | 13/13，`skipped 0 / todo 0`（exit 0） | 23011 ms |
| 4 | 同上，但父进程环境注入 `OAMP_WEB_PORT=<计数服务端>`（见 §8-E11） | 13/13；计数服务端 `{connections: 0, requests: 0}` | 22750 ms |
| 5 | `mktemp -d` 副本内同用例（副本保真度基线，用于 §9 变异） | 13/13 | ~23 s |

**稳定性判定：pass**（3 次独立全量运行 + 1 次环境面运行 + 1 次副本基线，全绿、零 skip/todo、零 flaky 迹象）。

---

## 3. 本轮两项重点复核（上轮痛点）

### 3.1 重点 1 —— 三宿主②Node import 面是否真落地

**判定：pass（真落地、真调用、非自证式）**

| 证据面 | 内容 |
|---|---|
| 源码真实调用 | `:525` `const { createHub } = await import('../sdk/index.js');`；**四处**库面真实调用：`:529` `createHub().api.docs()`、`:543` `createHub().api.chats.get('chat-does-not-exist')`、`:561` `createHub({port: releasedPort}).api.agents()`、`:578` `createHub().cli.run(['status'])`。不再是"零命中"。 |
| 组内设进程 env + 用后还原 | `:515-517` 捕获原值后设 `process.env.OAMP_SOCKET = hub.socketPath`、`process.env.OAMP_WEB_PORT = String(hub.port)`；`:518-523` `t.after` 逐键还原（原值为 `undefined` ⇒ `delete`）。**机制面复核**：`sdk/surface.js:378-380` `const env = opts.env ?? process.env; const port = opts.port ?? Number(env.OAMP_WEB_PORT \|\| DEFAULT_PORT)` ⇒ 库面确读**调用方进程 env**（A18 成立），故"组内设 env"是**必要而非装饰**。 |
| 窗口无重叠 | 13 条顶层用例 `duration_ms` 之和 = **22654 ms** ≈ 总时长 22704 ms ⇒ node:test **顶层串行**，`process.env` 改写窗口不与其他用例重叠。 |
| **我自己实测（脱离用例源码）** | 自建探针（`/tmp/pr009-r2-probe.mjs`，自起 router+web，不经用例任何断言）：<br>① 成功面 `api docs`：进程面 `code=0 / keys=[routes] / routes=21`，库面 `keys=[routes] / routes=21`，**JSON 深等 true**；<br>② 业务失败：进程面 `code=1 + {code:NOT_FOUND, http_status:404, error:"chat 不存在: ghost-chat-x"}`；库面 `exitCode=1 / code=NOT_FOUND / httpStatus=404 / message` **逐字相同（true/true/true）**；<br>③ 连接失败（已释放端口 53689）：进程面 `code=3 / HUB_UNREACHABLE (39ms)`；库面 `exitCode=3 / HUB_UNREACHABLE`，message **逐字相同**；<br>④ 层 C：进程面 `cli status` stdout = `"instance_id  session_id  state  last_heartbeat\n"`；库面 `cli.run(['status'])` = `{exit_code:0, stdout:同串, stderr:""}` ⇒ **逐字节相等 true、stdout 非空 true**。 |
| 结论 | ② 面**真覆盖**：F05-1 / F07-1 明文要求的"①shell ②Node `import` ③另一 agent 经 shell"三宿主，② 一路本轮有**可执行判据**且我已独立复现（不依赖用例的断言消息）。 |

### 3.2 重点 2 —— 两处判别力问题（D-1 / D-4）是真修还是掩盖

#### D-1（T1 末尾 `classOf` 恒真断言）→ **真修**
- 事实：`905bdc7` 删除了 `classOf = {0:[0],1:[1],2:[2],3:[3]}` 的 `assert.ok(classOf[row.expectClass].includes(row.expectClass))` 循环，原位改为注释说明（`:477-482`）。
- 该断言**结构上不可能失败**（`row.expectClass ∈ {0,1,2,3}`，映射恒等）⇒ 删除**未损失任何判别力**。
- **等价强度验证（我自己注入）**：副本内 `sdk/errors.js` 的 `EXIT_CONNECTION = 3 → 1`（这正是"一码两义 / 类码错位"的形态）⇒ T1 矩阵**转红**，消息逐字 `类 3 · 连接失败（Web 面：已释放端口）（第一次）：期望退出码 3，实际 1`。⇒ 剩余断言（逐行 `r.code === row.expectClass` + 四值齐全集合断言）**确实承担了**"仅凭退出码可区分四类"的语义。
- 结论：删除是**真修**（去死断言），不是掩盖；注释对"由何闭合"的描述与代码一致。

#### D-4（只读组 `task list` 缺 stdout 非空前置）→ **部分真修（强度不足）**
- 事实：`:1077` 新增 `assert.ok(taskList.viaHub.stdout.length > 0, 'task list（零任务）：stdout 应为「（无任务）」单行（非空，防"空 vs 空"的虚假相等）')`，`:1078` 补 `stderr === ''`。**是新增加固，不是删断言**。
- 该断言**确实为真**：`oamp/src/task.js:197` `process.stdout.write(\`${renderList(tasks)}\n\`)`，零任务时 `renderList` 返回 `'（无任务）'` ⇒ stdout = `"（无任务）\n"`（我实测 6 字节）。
- **但它挡不住它要挡的那一类回归（我自己注入）**：副本内 `src/task.js:114` `'（无任务）' → ''` ⇒ 我直接实测该形态 `EXIT 0, stdoutBytes 1, stdoutJSON "\n", trimEmpty true`；此时两侧 stdout 都是 `"\n"`（**实质仍为"空 vs 空"**），`length > 0` 成立 ⇒ **T7 全绿（未捕获）**。
- 结论：**literal 合 C6**（"先断言 stdout 非空"确实写了、两个只读组都写了），但**未达成 C6 的意图**（"防空 vs 空的虚假相等"）——因为 oamp 侧"载荷丢失"的自然形态恰是 `"\n"` 而非 `""`。判为 **partial**，并登记偏差 N-01（不阻塞：PR 验收第 7 条要求的是"层 C 逐字节一致"，该形态下 hub 仍忠实透传，判据未被违反）。

---

## 4. 逐项判定 A —— PR 文件「验收标准」10 条

| # | 验收条 | 判定 | 证据（可定位） |
|---|---|---|---|
| 1 | 退出码四类各一例且固定；仅凭退出码可区分 | **pass** | 用例 `T1 · 退出码四类矩阵`（5 行表驱动：`0`/`1`/`2`/`3`×2；每行连跑两次同码 `:437-444`；四值齐全 `:478`）。**我的独立探针**：`ok=0 / business=1 / usage=2 / connWeb=3 / connUds=3`，`distinct=[0,1,2,3]`；`3` 类 39ms（不挂起） |
| 2 | 用法错误样本覆盖（含 `--port abc` / `--wait` 给非阻塞条目 / `--as` 给非身份方法）且零连接 | **pass** | 用例 `T2` 12 行样本（`:603-628`）+ 计数服务器 `{connections:0,requests:0}` + 合法命令对照。**我的独立探针**：12 行样本全 `code 2` + 计数服务端 `0/0`，同 env 合法命令 `api docs` ⇒ `code 0` 且**计数服务端被命中 1 次**（证明判据非空转） |
| 3 | `--human` 两态：形态不同 + 事实逐项一致 + 默认仍 JSON | **pass** | 用例 `T3`（`:649-702`）。**我的独立探针**（自建桩）：默认 = 单 JSON（2 条 routes）、stderr 空、code 0；`--human` = `["method  path","GET     /api/alpha","POST    /api/beta"]`、不可 parse、两条事实**逐项出现**、code 0 |
| 4 | stdout/stderr 分离；失败 stderr 单行 JSON（层 A 带 `http_status`） | **pass** | `parseErrorLine()`（恰一行 / 无 `\n    at ` / 不以 `Error:` 开头 / 键集合精确 / error 非空）；我的探针实测层 A 失败 `{"code":"NOT_FOUND","error":"chat 不存在: ghost-chat-x","exit_code":1,"http_status":404}`、层 2/3 无 `http_status` |
| 5 | `--wait` 超时 → `WAIT_TIMEOUT`/1；同码不同 `code`；新进程取回终态（P-1 background） | **pass** | `T4`（1000/600/1500/8000ms 四组 + 四类 `code` 互异 + 三类退出码 `{1,2,3}`）+ `T5`（background 立即返回 `call_id`/`submitted` + 新进程 `calls get` 取回 + `transcript` 第二面）。我独立实测 `--wait 8000` 的当前行为见 §10 |
| 6 | 管道截断 `\| head -1` ⇒ 自行退出 / 码 0 / 无残留进程 | **pass** | 用例 `T6`（`set -o pipefail` + 进程组 `ESRCH` + NDJSON 单行）。**我的独立探针（真 hub + 真 agent 造 2 帧）**：`code=0 / signal=null / timedOut=false / 5953ms`、stdout 恰 1 行 `{"event":"agent_online",...}`、键 `{data,event}`、`groupAlive=false`、stderr 空 |
| 7 | 层 C 零语义变更（逐字节）+ 启停类用法面对照 | **pass** | 用例 `T7` 四组逐字节（`:1043-1091`）+ `queryStatus` 前后投影相等。**我的独立探针 4 组**：`status`(live)、`task list`(live，stdout=`（无任务）\n`)、`status`(不可达 ⇒ 双侧 `code 1`、stderr 逐字节 ⇒ **既有 `1` 未被重分类为 3**)、`agent start`(用法面 ⇒ 双侧 `code 2`)：**三面（code/stdout/stderr）全部逐字节相等** |
| 8 | 跨进程无状态：并发两进程各自完整；执行前后不新增状态文件 | **pass** | 用例 `T8`（`Promise.all` 两跑逐字节相等 ×2 组 + 新进程续查 ×2 间隔 + `oamp/` 递归路径集合前后 `deepEqual` + `.runtime`/`data` 存在性 + 临时绝对路径结构性断言）；变异 J4 类反证见 §9（同类快照判据在上一轮已被证明非空转） |
| 9 | 经 `runHub()` 起子进程；不写仓库内 `.runtime/` / `data/` | **pass** | 全文件唯一进程入口 = `runHub`（例外 = T6 的 shell 管道 + T1 库面组，均已在文件头 `:6-7` 显式登记）；`OAMP_DB`/`OAMP_SOCKET` 均为 `os.tmpdir()` 绝对路径；实测运行前后 `oamp/.runtime`、`oamp/data` **均不存在** |
| 10 | 经 `node --test test/*.test.js` 被拾取并通过 | **pass** | 用例 `T9`（断言 `pkg.scripts.test`、`dependencies === {}`、文件名/位置）；scoped 5 次全绿；按用户 2026-09-15 口径**未**触发仓库级全量 |

**A 汇总：10 / 10 pass。**

---

## 5. 逐项判定 B —— tasks T1~T9

| 任务 | 判定 | 证据 |
|---|---|---|
| T1 骨架 + 基建原语 + 退出码四类矩阵 **+ 三宿主②组** | **pass** | import 面恰 `node:` + 三个既有 helper（零第三方）；`PORT_SEGMENT=[53000,53999]` + 与其余三段零交集断言（`:33-38`、`:390-393`）+ `pickFreePort()` 运行时探测（40 次重试）；`setupHub(t)`/`startWeb()`/`runHubJson()`/`describeByteDiff()` 齐备；矩阵 5 行 + 两次同码 + 四值齐全；不可达态"单行 / 无堆栈 / 含目标地址 / ≤3000ms / 恢复后得 0"。**本轮新增的三宿主②组**见 §3.1（真落地 + 我独立复现） |
| T2 用法错误样本集 + 零连接零副作用 + 分离 | **pass** | 12 行样本覆盖九类（未知层 / 未知子命令×2 / 缺必填位置 / 位置参数多余 / 缺必填选项 / 选项缺值 / 取值非法×3 / 不接受选项×2 / 层 C 缺命令）；计数服务器 `connections/requests === 0` + 合法对照命中（反证非空转）；对照样本 `--port <已释放>` ⇒ 3 |
| T3 默认 JSON 单文档 + `--human` 两态 + 事实一致 | **pass** | 三条只读命令：`JSON.parse` + 单个尾换行 + `trim().split('\n').length===1` + 顶层键 `{routes}`/`{agents}`/`{chats,total,limit,offset}`；`--human` 形态不同、不可 parse、行数 = 表头+N、事实逐项出现；第三渲染分支（含额外标量键）显式覆盖 |
| T4 `--wait` 超时语义 + 5000ms 偏差锁 | **pass** | 超时形状 / 上限有序（600 < 1500）/ 上限内成功（原样透传服务端体）/ 四类互异（4 个 `code` 互异 + 3 个退出码）/ 缺省上限界内观察（<2000ms；`1800000` 显式登记不可实测）/ 黑障 `requests === 1`（不重发）。偏差锁见 §10 |
| T5 P-1 background 取回终态 | **pass（附夹具名口径偏差 N-08）** | 本文件内自建 fake ACP（`chmod 0o755`，`acp` 常驻 + `-p` 一次式），`OAMP_OMP_BIN` 注入 ⇒ 零真实 omp / 零外网；受理即返回 `call_id` + `submitted` + <3000ms；新进程取回逐字相同 + `waitFor` 有界轮询至终态 + `text/duration_ms/truncated` 形状；roster 无额外行 + `transcript` 第二面。**机制复核**：`instanceIdForRole('dev')='pb-dev'` 且 `roleFromInstanceId` 要求 `roles/dev/dev.md` 存在（`src/role-binding.js:30/44`）—— 该文件在本 worktree 存在（9941 B）⇒ `pb-dev` 是**载荷所需**的选择 |
| T6 订阅管道截断 | **pass（附端口读法偏差 N-04）** | 自建 `bash -c 'set -o pipefail; … \| head -1'` + `detached` 进程组；"管道自行退出"用 `waitFor(exit!==null, 15000)`（超时即失败，不靠强杀）；退出码 `0`；NDJSON 恰 1 行 + 尾换行 + 键 `{event,data}`；进程组 `ESRCH`；≥2 帧前提由"注册探针节点 → 停止产生 offline"构造 |
| T7 层 C 逐字节 + 启停面 + 零现场变化 + 无自动拉起 | **pass（附强度偏差 N-01 / 遗留 N-04·N-05·N-06）** | 四组逐字节（同 bin / 同 env / 同 cwd / 同 stdio，`Buffer.compare` + 退出码），失败消息带首个差异**字节**偏移 + 两侧上下文；**本轮补齐** `task list` 组非空 + stderr 空（`:1077-1078`）；不可达态证"不重分类"；两组 500ms 观察窗 + `queryStatus` 投影相等；agent 不在线 ⇒ `NOT_FOUND`/1 + 同窗零新增 |
| T8 跨进程无状态 + 仓库零状态文件 | **pass（附 N-07）** | 并发两跑逐字节相等（2 组）+ 失败消息打印两跑 `code` 与 stdout 前 200 字节；新进程续查（两种间隔）；`repoSnapshot` 递归集合前后 `deepEqual` + `.runtime`/`data` 存在性不变；`OAMP_DB`/`OAMP_SOCKET` 绝对路径且不在包根下 |
| T9 收口 | **pass** | 改动面恰一条新增（§1）；`package.json.scripts.test` 与 `dependencies` 断言；文件名/位置断言；零 `skip`/`todo`/`only`（全文件正则零命中）；零 `Object.assign(process.env, …)`（零命中）；关键路径 `waitFor`（裸 `setTimeout` 仅 3 处 = 桩服务延迟 + 两处 tasks 已登记的固定观察窗 + T8 非关键间隔） |

**B 汇总：9 / 9 pass。**

---

## 6. 逐项判定 C —— C1~C12 与 MI-1~MI-10

| 项 | 判定 | 证据 |
|---|---|---|
| C1 落点唯一 | **pass** | §1（`git diff --name-status` 恰一条新增；`905bdc7` 也只在同一文件） |
| C2 零新框架 / 零新依赖 | **pass** | import 面逐条复核（`:13-23`）；`pkg.dependencies === {}`（用例断言 + 我复核 `package.json`） |
| C3 端口段（A13） | **pass** | 全文件 5 位数字字面量**仅** `53000/53999`（`:32`）与另三段端点（`:35-37`）；零 `7788`；零硬编码 `/tmp`、零 `/Users/`；运行期采样见 §8-E10 |
| C4 进程 / 临时态收口 | **pass** | 无"长驻命令经 `runHub` 且未给 `timeoutMs`"的面（长驻 = T6 自建 spawn，`detached` + `waitFor` 有界 + `t.after` 整组 SIGKILL）；临时态一律 `mkdtemp(os.tmpdir())` 绝对路径；`t.after` 顺序（web.stop → stopAll → 删目录） |
| C5 断言不锚行号 | **pass** | 全文件零 `sdk/*.js:NNN` 形态；期望值取自既有 `code` 名与逐字文案 |
| C6 逐字节口径 + 只读组非空前置 | **pass（附偏差 N-01）** | 逐字节 + 首差异字节偏移；`status` 组与 `task list` 组**均已**加非空前置 —— 字面满足；**但** `length > 0` 挡不住 `"\n"`（§3.2 D-4） |
| C7 失败可定位 | **pass** | `startWeb` 提前退出抛 `stderr=` 摘要；缺项/多出项/解析异常/逐字节差异各自点名；无静默跳过 |
| C8 零上游改动 | **pass** | §1（含 sha256 表） |
| C9 偏差锁写法 | **pass** | 用例名逐字含 `[已知偏差 · 归 pr-011 收口修复]`（`:807`）；注释逐字登记"规格期望 = `WAIT_TIMEOUT`/1；修复点 = `oamp/sdk/http.js`；修复后本断言应改为…"；断言集合锁**当前**行为；零 `skip`/`todo`/条件跳过 |
| C10 不重复其它 PR 用例面 | **pass** | 不断言 `hub doctor` 行为（全文件 `doctor` 零命中）；不断言 pr-007/008/010 文件存在性/内容（零命中）；**新增** `import('../sdk/index.js')` 属 C10 明文允许的"库面对照"，且按 A18 设进程 env |
| C11 零 flaky | **pass（附 N-03）** | 关键路径 `waitFor` 有界；负向断言只用于"固定 500ms 观察窗"；端口/临时目录按用例独立；零 `Object.assign(process.env,…)`；库面组直接赋值 `process.env` 是 C10 授权面（窗口不重叠见 §3.1） |
| C12 收口纪律（scoped-only） | **pass** | §2（未跑仓库级全量） |
| MI-1 端口段形态 | **pass** | 段常量 + `net` 探测空闲 + 段内断言 + 零交集 + 零额外端口字面量 |
| MI-2 `--human` 第三分支 | **pass** | `chats list`（含额外标量键）分支显式覆盖（`:686-695`） |
| MI-3 零连接判据 = 计数服务器 | **pass** | 我独立复核有效（含合法命令对照命中 ⇒ 非空转） |
| MI-4 UDS 不可达样本构造 | **pass** | `OAMP_SOCKET=<tmp>/nonexistent.sock` ⇒ `HUB_UNREACHABLE/3`；我的探针实测同形 |
| MI-5 不重试观测面 = 服务端 `requests` | **pass** | `blackhole.counts().requests === 1`（T4 两处）；我独立复现（§10） |
| MI-6 偏差锁可执行形态 | **pass** | 正常通过 + 用例名标注 + 注释写明翻转点；无 skip/todo |
| MI-7 P-1 夹具（fake ACP + `OAMP_OMP_BIN`） | **pass（附 N-08）** | 夹具真实可用；实例名 `pb-dev` 与 tasks 字面 `dev-1` 的差异经机制复核判定**成立**（§5 T5 行） |
| MI-8 启停类取样 = `agent start` 用法面 | **pass** | 零现场副作用 + 双侧 `code 2` + stderr 逐字节（我独立复现） |
| MI-9 仓库零状态判据形态（`oamp/` 递归集合） | **pass** | 上一轮变异 M6 已证该判据非空转；本 PR 面未变 |
| MI-10 "不补发"未写成 flaky/`skip` | **pass** | 用例内无该断言，符合"稳定才写、否则登记"；无 `skip`/`todo` |

**C + MI 汇总：22 / 22 pass。**

---

## 7. 逐项判定 D —— architecture / prd

| 判据 | 判定 | 证据 |
|---|---|---|
| §5.3 默认输出 = 单结果一个 JSON 文档 + 换行 | **pass** | T3；我的探针同形 |
| §5.3 `--human` 渲染（数组 ⇒ 表头+数据行；对象含单数组键 ⇒ 该数组走表格 + 其余键逐行） | **pass** | T3 两分支 + `chats list` 标量键分支；探针复现表头/数据行形态 |
| §5.4 四类归类表（首条命中者生效）可区分 | **pass** | T1/T2/T4 + 探针 `distinct=[0,1,2,3]` |
| §5.4 层 C 退出码透传、既有 `1` 不重分类 | **pass** | T7 不可达态组（双侧码均 1 + stderr 逐字节相同）；探针独立复现 |
| §5.4 `0` 类"订阅被下游管道截断" | **pass** | T6（码 0）+ 探针（真 hub，5953ms 自退出、非强杀） |
| §3.3 P-1（background 形态取回终态；block 不保证标识；不反查 roster） | **pass** | T5 `:783-786` 注释逐字登记该边界；实现面以 background 取回闭合，未做 roster 反查 |
| §3.3 P-2（层 C 原样透传，不 JSON 信封化） | **pass** | T7 逐字节四组 + 探针 4 组 |
| §3.3 P-3（本地只判"构造请求所必需"的取值非法 ⇒ 2；服务端可判 ⇒ 1） | **pass** | T2 取值非法三例全 `2`；T1 业务失败（404）`1` |
| §10 T4 行（四类码 / `--human` / 分离 / `--wait` / 管道截断后无残留 / 层 C 逐字节） | **pass** | T1/T3/T2/T4/T6/T7 逐条对位；"无残留"由进程组 `ESRCH` + 探针复核 |
| prd F04-2（零语义变更）/ F04-3（只读零副作用） | **pass** | T7；只读组前后拓扑投影相等 |
| **prd F05-1（默认 JSON + 跨宿主 ①②③）/ F05-2·3·4** | **pass（本轮翻正）** | ①shell = `runHub`→`bin/hub.js`（全部用例）；**②Node `import` = 新增 `createHub()` 组（§3.1，我已独立复现）**；③另一 agent 经 shell = 同形入口（T6 的 `bash -c` 管道为其独立证据） |
| prd F06-3（可截断、无残留） | **pass** | T6 + 探针 |
| **prd F07-1~5（含 F07-1 的三宿主面）** | **pass（本轮翻正）** | T1/T2 + 探针；F07-1 的成功面三宿主由 `createHub()` 组的"库面 `exitCode` === 进程面 `code`"承接 |
| prd F08-1~5（单行无堆栈 / 3 / 不挂起 / 两条路径 / 恢复无残留） | **pass** | T1 不可达态两路径（`ECONNREFUSED` + `ENOENT`）≤3000ms；恢复后同命令 ⇒ 0 |
| prd F09-1/2/3 | **pass** | T8；F09-4（不补发）按 MI-10 登记为不在本 PR 写（无 flaky、无 skip） |
| prd F10-1~5 | **pass** | T4（上限显式且有序 / 界内返回终态 / 超时单独一类 / 上限内成功）+ T5（不丢任务，background 取回） |
| prd G01-3/4（既有测试面不需改断言；服务边界不变） | **pass** | `oamp/**` 代码面零 diff ⇒ 两把漂移锁的输入与 `hygiene` 扫描面零变化 |
| prd G02-2/3（启停类零语义变更；不自动拉起/重派） | **pass** | T7（用法面逐字节 + 两处 500ms 窗口零新增 + agent 离线调用明确 `NOT_FOUND`）+ T5（roster 无额外行） |

**D 汇总：18 / 18 pass。**

---

## 8. 独立复核（脱离用例源码；全部由我自建探针 / 采样完成）

| # | 复核项 | 方法与结果 | 判定 |
|---|---|---|---|
| E1 | 退出码四类可达且仅凭码可区分 | 自建 router+web + 已释放端口 + 不存在 socket：`ok 0 / business 1 / usage 2 / connWeb 3 / connUds 3`，`distinct=[0,1,2,3]`；`3` 类 39ms | **pass** |
| E2 | 12 行用法样本零连接 | 自建记录型服务端（**会正常应答** ⇒ 若真连通则码必为 0/1）：12 行样本同 env 全 `code 2`、`{connections:0, requests:0}`；**对照**：同 env 合法命令 ⇒ `code 0` 且服务端 `requests` 由 0 → 1（判据非空转） | **pass** |
| E3 | 层 C 逐字节（进程 vs 直跑） | 4 组（`status` live / `task list` live / `status` 不可达 / `agent start` 用法面）：三面（`code`/`stdout`/`stderr`）**全部逐字节相等**；不可达态双侧 `code 1`（未被重分类为 3）；`task list` stdout = `"（无任务）\n"`（6 字节，非空） | **pass** |
| E4 | 管道截断（真 hub + 真 agent 造 2 帧） | `bash -c 'set -o pipefail; exec node bin/hub.js api stream events --port P \| head -1'`：`code=0`、`signal=null`、`timedOut=false`、5953ms **自退出**、stdout 恰 1 行 NDJSON、键 `{data,event}`、`process.kill(-pgid,0)` ⇒ `ESRCH`、stderr 空 | **pass** |
| E5 | `--human` 两态事实一致 | 自建桩：默认 = 单 JSON（`routes` 2 条）/ stderr 空 / code 0；`--human` = 3 行（表头 + 2 数据行）/ 不可 parse / 两条事实逐项出现 / code 0 | **pass** |
| E6 | 库面 vs 进程面（三宿主②） | 四处对位全部一致（详见 §3.1 表） | **pass** |
| E7 | 库面**确读调用方进程 env** | 机制：`surface.js:378-380`；现象：删 `OAMP_WEB_PORT` 后 `createHub().api.docs()` 仍成功 ⇒ 命中宿主 `127.0.0.1:7788` 的常驻 oamp web（`http_status=200 bytes=14414`，`lsof` 显示 PID 62417 LISTEN） | **pass** |
| E8 | 端口段 | ①静态：5 位字面量仅段常量与三段端点；零 `7788`/`/tmp`/`/Users/`。②运行期采样（第 3 次运行，每 150ms `lsof -nP -iTCP -sTCP:LISTEN`）：**14 个** 5 位数监听端口 `53064 53241 53242 53287 53345 53357 53377 53691 53715 53747 53752 53799 53905 53948` ⇒ **全部 ∈ 53000-53999** | **pass** |
| E9 | **禁走 7788（环境面实证）** | 父进程注入 `OAMP_WEB_PORT=<计数服务端 53850>` 跑完整用例：13/13 通过，计数服务端 **`{connections: 0, requests: 0}`**。推理闭合：若存在任何"既不给 `--port` 也不给 env 覆盖"的 HTTP 调用路径，它会先落到**环境变量**端口 ⇒ 计数必然 > 0；实测为 0 ⇒ 该路径不存在 ⇒ 默认 `7788` 在结构上不可达 | **pass（强证据）** |
| E10 | 改动面封闭 | §1（git 三连 + sha256 表） | **pass** |
| E11 | 测试后无残留 | 运行结束 2s 后：`ps` 中 `0025-pr-009` **零命中**（注意：`ps \| grep -c` 的自匹配会给出假阳性 2，需改用 `ps -eo pid,command \| grep`）；`lsof` 中无任何 53xxx 测试残留（仅 `rapportd:59548`、Chrome:51620、Electron:56304/50551 等非测试服务）；`oamp/.runtime`、`oamp/data` **均不存在** | **pass** |
| E12 | 稳定性 | 3 次独立全量 + 1 次环境面 + 1 次副本基线，13/13 全绿、零 skip/todo | **pass** |
| E13 | 已知缺陷锁（§10） | 断言只锁**当前**行为、无 skip/todo、无当下必失败断言；我独立实测当前行为 `--wait 8000 --mode block` + 黑障 ⇒ `{"code":"REQUEST_TIMEOUT","error":"等待响应超时（5000ms；127.0.0.1:53268）","exit_code":3}`、退出码 `3`、**5044 ms** | **pass** |
| E14 | D-1 真修 | §3.2（含注入反证） | **pass** |
| E15 | D-4 真修强度 | §3.2（注入未捕获） | **partial** |

**E 汇总：14 pass / 1 partial。**

---

## 9. 变异注入（`mktemp -d` 副本内；真源零触碰）

副本：`/tmp/pr009-r2-mut-ervi0g`（`oamp/` + `roles/` 两份拷贝）。

**还原后保真证据**：
```
$ diff -r --brief /tmp/pr009-r2-mut-ervi0g/oamp <worktree>/oamp
（空）⇒ COPY IDENTICAL TO REAL SOURCE
$ sha256sum <worktree>/oamp/test/sdk-cli-contract.test.js <worktree>/oamp/sdk/*.js <worktree>/oamp/src/task.js
94ad6cd761062d7a  test/sdk-cli-contract.test.js      ← 注入前后全等
e75490f62719eb5b  sdk/errors.js                      ← 注入前后全等
7f22000886542555  sdk/index.js                       ← 注入前后全等
11b6eb7d301b41db  src/task.js                        ← 注入前后全等
2a2fa4ae81dd08b5  sdk/cli.js / aef7613905fdf6ec sdk/http.js / e5e4b500c956401b sdk/surface.js / a013979c0b3a2cf6 sdk/uds.js
```
（全部注入只作用于副本；每条注入后逐条还原，末次 `diff -r` 全等 + 真源 sha256 与注入前一致。）

| # | 变异（副本内） | 受限运行（`--test-name-pattern`） | 结果 |
|---|---|---|---|
| J1 | `sdk/index.js`：`createSurface({ port, … })` → `{ port: 7788, … }`（库面端口被钉死） | `三宿主` | **捕获**（③：`api agents：库面退出码 0 与进程面 3 不一致`） |
| J2 | `sdk/index.js`：→ `{ port: 53217, … }`（钉到一个空闲错端口） | `三宿主` | **捕获**（①：`api docs：库面退出码 3 与进程面 0 不一致`） |
| J3 | `sdk/index.js`：`cli` → `{ run: async () => ({stdout:'',stderr:'',exit_code:0}) }` | `三宿主` | **捕获**（④：`层 C：库面 stdout 不得为空（防空 vs 空的虚假相等）`） |
| J4 | `src/task.js`：`renderList` 空态 `'（无任务）'` → `''` | `层 C 逐字节` | **未捕获**（T7 全绿）⇒ 偏差 **N-01**（实测该形态 stdout = `"\n"`，1 字节） |
| J5 | `sdk/errors.js`：`EXIT_CONNECTION = 3` → `1` | `退出码四类` | **捕获**（`类 3 · 连接失败（Web 面…）：期望退出码 3，实际 1`）⇒ 证明删掉 D-1 死断言后**语义仍被承担** |
| J6 | `sdk/index.js`：`{ port }` → `{ port: port ?? 7788 }`（**只忽略 env**，显式 port 仍生效） | `三宿主` | **未捕获**（组内 4 面全绿）⇒ 偏差 **N-02** |

**结论**：6 类注入 **4 捕获 / 2 未捕获**；未捕获的两条均已作为偏差登记（N-01 为断言强度、N-02 为环境耦合）。用例对**归类表 / 库面端口来源（非 7788 情形）/ 库面 cli 面 / 层 C 码透传**均有真实判别力，不存在"跑绿但什么都没约束"的空转。

---

## 10. 已知缺陷锁（A17，归 pr-011）

- 用例名 `:807` = `T4 [已知偏差 · 归 pr-011 收口修复] · --wait 8000 时 5000ms 响应头上限先行（当前行为锁 + 翻转点已登记）`；注释逐字登记"规格期望 = `WAIT_TIMEOUT`/1；修复点 = `oamp/sdk/http.js` 的响应头上限与 `--wait` 的先后关系；修复后本断言应改为…"。
- 断言集合 = `code===3` + `code==='REQUEST_TIMEOUT'` + `exit_code===3` + `error` 含 `5000` + `stdout===''` + 耗时 ∈ `(4900, 5400)` + 黑障 `requests===1`。
- **我独立实测当前行为**（自建黑障，不经用例源码）：退出码 `3`、`REQUEST_TIMEOUT`、error 含 `5000`、耗时 **5044 ms** ⇒ 与锁**一致**；用例侧 3 次运行均绿（该用例单次 5043.68 ms）。
- **判定：pass** —— 只断言当前行为、零 `skip`/`todo`、无当下必然失败断言、未改 `oamp/sdk/**`。（耗时窗余量见偏差 N-06。）

---

## 11. 偏差记录

| # | 偏差 | 位置 / 证据 | 影响 | 处置建议 |
|---|---|---|---|---|
| **N-01**（新） | `task list` 组的"非空前置"强度不足：`stdout.length > 0` 被 `"\n"` 满足 ⇒ "载荷丢失"这一**它要挡的**形态不会被捕获 | 用例 `:1077`；注入 J4（`src/task.js:114` → `''`）⇒ T7 **全绿**；直接实测该形态 `stdoutBytes 1 / stdoutJSON "\n" / trimEmpty true` | 中低：PR 验收第 7 条（层 C 逐字节）在该形态下**未被违反**，故非阻塞；但 C6 的意图未达成 | 改成 `stdout.trim().length > 0` 或断言具体行内容；`status` 组同法加固 |
| **N-02**（新） | 三宿主②组的 ①② 面对"库面端口来源"不敏感**当宿主存在 7788 常驻同版本 oamp web** 时 | 注入 J6（库面忽略 `OAMP_WEB_PORT` 回落 7788）⇒ 组内 4 面全绿；宿主事实：`127.0.0.1:7788` PID 62417 LISTEN、`/api/docs` 200 / 14414 B（与本 hub 同形） | 低：③（显式 port）与 ④（socket env）仍有效；J2 证明换空闲错端口时 ① 立即转红 ⇒ 仅在"错端口恰好是同版本服务"时失效 | 给 ①② 加"端点同一性"锚（例：先向本 hub 建唯一项目再读 `chats list`，其内容在 7788 上必然不同） |
| **N-03**（新） | 库面组直接改写 `process.env`（C11 字面禁 `Object.assign(process.env, …)`；C10 明确授权 A18 的进程 env 面） | 用例 `:515-523`；实证：13 条用时之和 22654 ms ≈ 总 22704 ms ⇒ 顶层串行、窗口不重叠；`t.after` 逐键还原（`undefined` ⇒ `delete`） | 低：合规且窗口已界定 | 在注释中点明"依赖 node:test 顶层串行"，防后续加 `concurrency` 时静默失效 |
| **N-04**（遗留 D-2） | T6 的"端口 ≤1s 释放"在用例内**先** `hub.web.stop()` ⇒ 度量的是 web 停机释放，**不是** hub 残留（hub 是客户端、不监听） | 用例 `:1024-1025` | 低：hub 无残留由同用例进程组 `ESRCH` 独立承担（我已复现 `ESRCH`）；注释已说明，不构成误导 | 改为对订阅进程组的存活判据，或声明只覆盖 web 停机 |
| **N-05**（遗留 D-3） | T7 现场快照投影 `{instance_id, state}`（排除 `last_heartbeat`/`session_id`）；**零节点拓扑下 `before/after` 恒 `[]`** ⇒ 只读组的"零变化"断言平凡真 | 用例 `:63-68`、`:1043`、`:1097`、`:1108` | 中低：对"无自动拉起"仍有判别力；对会话/心跳面零判别力 | 注释写明收窄范围与"零节点下平凡真" |
| **N-06**（遗留 D-5） | 偏差锁耗时窗 `(4900, 5400)`：实测 5043.68 ms（3 次一致）⇒ 上界余量 ~356 ms | 用例 `:829` | 低（并行负载下可能 flaky） | 放宽上界（如 `(4900, 6000)`）；pr-011 修复后翻转期望值 |
| **N-07**（遗留 D-7） | T5 `roster.calls.length === 2` 硬编码既有实现的计数语义（`messages send` 亦登记一行） | 用例 `:895` | 低（同用例已先断言目标 `call_id` 行数 = 1） | 改为相对判据（先取基线，再断言 `+1`） |
| **N-08**（遗留 D-6 的另一半） | 夹具实例名 `pb-dev`（经机制复核**正确**：`roles/dev/dev.md` 存在 + `pb-` 前缀反解）与 tasks MI-7 字面 `dev-1` 仍不一致 | 用例 `:905`；`src/role-binding.js:30/44` | 无（代码侧正确，文档侧滞后） | 回填 tasks MI-7 字面为 `pb-dev` |
| **N-09**（新） | T5 夹具依赖仓库级 `roles/dev/dev.md`（`roleRoot` = 包根上级） | 用例 `:905`；存在性实测（9941 B） | 低：在"只有 `oamp/` 的裸包"环境下夹具不可用（属产品角色绑定设计使然） | 在注释登记该前置，或在缺失时给出可定位失败消息 |

**偏差合计：9 条（新增 4 条 N-01/N-02/N-03/N-09；上轮 5 条 N-04~N-08 仍成立）。无阻塞项。**

---

## 12. 下一迭代候选

1. **N-01（最优先）**：把 T7 两个只读组的非空前置改为 `trim().length > 0`（或直接断言行内容），使"载荷丢失 ⇒ `"\n"`"可被捕获。
2. **N-02**：给三宿主②组的 ①② 面加"端点同一性"锚（唯一标记内容），消除"宿主 7788 常驻同版本服务"造成的敏感度盲区。
3. **N-06**：放宽偏差锁耗时上界；pr-011 修复后按已登记的翻转点把期望改为 `WAIT_TIMEOUT`/1（用例内已预登记）。
4. **N-07**：T5 roster 判据相对化（基线 + `+1`）。
5. **N-05**：注释写明 T7 投影收窄范围与"零节点下平凡真"。
6. **N-03**：库面组注释登记"依赖顶层串行"的前置。
7. **N-08**：tasks MI-7 字面回填 `pb-dev`。

---

## 13. 结论

**判定：PASS**

- **判定项：74 项** —— **pass 73 / partial 1 / fail 0 / blocked 0**。
  - A（PR 10 条）= 10/10 pass；B（T1~T9）= 9/9 pass；C+MI（C1~C12 / MI-1~MI-10）= 22/22 pass；D（architecture §3.3·§5.3·§5.4·§10 T4 + prd F04~F10·G01·G02）= 18/18 pass；E（本轮独立复核 15 项）= 14 pass / **1 partial（D-4 断言强度，见 §3.2 / N-01）**。
- **本轮两项痛点均已处置**：① **三宿主②Node import 面真落地**（源码四处真实 `createHub()` 调用 + 组内设并还原进程 env + 我独立复现四处库面/进程面一致的退出码与 JSON 结构 ⇒ 非自证式）；② **D-1 = 真修**（删死断言，注入 J5 证明语义仍被承担）、**D-4 = 部分真修**（新增前置字面合 C6，但注入 J4 证明挡不住 `"\n"` ⇒ partial + N-01）。
- **强证据面（可直接采信）**：
  1. scoped 用例 **3 次独立全量 + 1 次环境面 + 1 次副本基线全绿，13/13、零 skip/todo**；
  2. **6 类变异注入：4 捕获 / 2 未捕获**（未捕获两条已成偏差 N-01/N-02），副本 `diff -r` 全等、真源 sha256 前后一致；
  3. **脱离用例源码的独立探针**复现全部关键契约：四类退出码 `{0,1,2,3}` 仅凭码可区分、12 行用法样本零连接（记录型服务端 + 合法命令对照反证）、层 C 4 组三面逐字节相等、管道截断 5953 ms 自退出且码 `0` + `ESRCH`、`--human` 两态事实一致、已知偏差锁当前行为 `REQUEST_TIMEOUT/3 @5044ms`；
  4. **端口**：静态零额外字面量 + 运行期 14 个 53xxx 监听全部落段内；**"禁走 7788" 环境面实证**（父进程注入环境端口跑完整用例 ⇒ 计数服务端 `0 连接 0 请求` ⇒ 结构上不存在回落到默认端口的调用路径）；
  5. 测试后**无残留进程 / 无测试端口残留 / 无仓库写**；改动面恰 `A oamp/test/sdk-cli-contract.test.js`，`oamp/sdk/**` 与全部上游面零 diff（sha256 逐文件）。
- **无需返工**：未捕获的两条注入均为**测试产物自身的强度/灵敏度**问题（N-01/N-02），不违反 PR 文件任何验收条；其余 7 条偏差为遗留或低危登记项。

**回报**：报告路径 = `docs/iterations/0025-hub-sdk-and-skill/clarifications/verify-pr-009-r2-20260915-183218.md`；结论 = **PASS**；fail 条目数 = **0**（partial 1）；偏差记录条数 = **9**。
