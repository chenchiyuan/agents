# 独立验证报告 · pr-011（收口修复：block 等待上限可达性 + 三处断言判别力）

## 0. 验证者身份（反射结果）

**产出物类型** = **修复类产出物**（缺陷修复 + 交付物自身判别力修复）。
⇒ 验证者身份 = **修复验证者**，核心问题四问：**① 它真的修好了吗（前后对照）② 修法对不对、有没有越界 ③ 有没有引入回归 ④ 三处断言真的长牙了吗、偏差锁真的翻转了吗**。

本报告不接受任何执行过程上下文；全部判定证据来自**产出物本体**（worktree `eb2e845` 的文件）＋**标准文本**（PR 文件 / `pr-011-tasks.md` / `architecture.md` / `prd/*`）＋**本 verifier 自建的独立取证**（自建桩、`mktemp` 副本变异注入）。

---

## 1. 基本信息

| 项 | 值 |
|---|---|
| 迭代 / PR | `0025-hub-sdk-and-skill` / `pr-011-closeout-wait-limit-and-assertion-strength` |
| 工作区（判定基准根） | `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0025-hub-sdk-and-skill/.pb-agents/worktrees/0025-pr-011-closeout-wait-limit-and-assertion-strength` |
| base / 实现提交 | `1d934b7` / `eb2e845`（HEAD 实测 = `eb2e845`） |
| 产出物 | `oamp/sdk/http.js`、`oamp/sdk/surface.js`、`oamp/test/sdk-cli-contract.test.js`、`oamp/test/sdk-api.test.js`、`oamp/test/sdk-uds.test.js`（`git diff --name-status 1d934b7 eb2e845` ⇒ **恰 5 条 `M`，零新增/零删除**） |
| 标准来源 | ① PR 文件「验收标准」13 条；② `prs/pr-011-tasks.md` T1~T6 与 §5.1 的 4 条 `[model_inferred]`（MI-1~MI-4）；③ 主 agent 已 ratify 的派生规则（`mode === 'block'` 才派生）＝ tasks §0.3 **D1**；④ `architecture.md` §3.3 P-1 / §5.4 / §5.1；`prd/{F10,F07,F02,F06,F09,F04}` |
| 环境 | Node `v22.15.0`；`oamp/` 零依赖（`dependencies === {}`） |
| 日期 | 2026-09-15 |
| 口径 | scoped 验证（相关 7 套件 + 独立对照）；**仓库级全量未跑**，按口径由主 agent 在合并时跑一次 |

**写入门禁自证**：本 verifier 全程未修改 `oamp/**`——5 个受改文件的工作区字节与本提交 blob 逐一相等（§6.4 hash 表）；变异注入全部落在 `/tmp/pr011-verify/**` 副本内；未执行任何 git 写命令。

---

## 2. 取证明细（本 verifier 自建，不依赖被测仓库的 `test/helpers`）

### 2.1 自建桩（黑障 / 延迟）＋ 双版本 CLI 对照

- 桩脚本：`/tmp/pr011-verify/stub-harness.mjs`（自建 `node:http` 桩：`delayMs === null` ⇒ 接受 TCP、永不写响应头 = 黑障；否则 `delayMs` 后回 `status`/`body`；计数 `requests`）。
- base 侧副本：`/tmp/pr011-verify/base/oamp`（`git show 1d934b7:<file>` 还原 5 个文件；`surface.js` 与 `1d934b7` 逐字 `diff` 为空，已自证）。
- 两次运行各 11 个形态，原始 JSON 落 `/tmp/pr011-verify/{base,fixed}.json`。

| # | 形态 | base `1d934b7` | fixed `eb2e845` | 判定 |
|---|---|---|---|---|
| 1 | 黑障 + `--mode block --wait 8000` | `code=3` `REQUEST_TIMEOUT` `5044ms` `req=1` | **`code=1` `WAIT_TIMEOUT` `8046ms` `req=1`**，stderr `{"code":"WAIT_TIMEOUT","error":"等待超时（8000ms）：调用仍在进行","exit_code":1}`，stdout `""` | ✅ 修复可见 |
| 2 | 6000ms 桩 + `--mode block --wait 8000` | `code=3` `5042ms` | **`code=0` `6045ms`**，stdout `{"calls":[{"id":"x"}]}` 逐字原样 | ✅ 修复可见 |
| 3 | 6000ms 桩 + `--mode block`（不给 `--wait`） | `code=3` `5042ms` | **`code=0` `6046ms`**，响应体原样 | ✅ 缺省上限可达（有界观测） |
| 4 | 黑障 + `--mode background --wait 8000` | `code=3` `5042ms` | `code=3` `5044ms` `error` 含 `5000` | ✅ **未放大**（反向回归） |
| 5 | 黑障 + 省略 `--mode` + `--wait 8000` | `code=3` `5043ms` | `code=3` `5041ms` `error` 含 `5000` | ✅ **未放大**（反向回归） |
| 6 | 黑障 + `api docs`（非 `calls create`） | `code=3` `5044ms` | `code=3` `5042ms` `error` 含 `5000` | ✅ **未放大** |
| 7 | 黑障 + `block` + `--wait 1000` / `600` / `1500` | `1/1042ms`、`1/641ms`、`1/1541ms` | `1/1041ms`、`1/642ms`、`1/1543ms` | ✅ 恒 `WAIT_TIMEOUT`/1、耗时单调、每次 `req=1` |
| 8 | 404（0ms） + `block` + `wait 8000` | `1/NOT_FOUND/http_status=404` `43ms` | `1/NOT_FOUND/http_status=404` `44ms`，stdout `""` | ✅ 上游错误即时归类 |
| 9 | 400（0ms） + `block` + `wait 8000` | `1/INVALID_PARAM/http_status=400` `42ms` | `1/INVALID_PARAM/http_status=400` `45ms` | ✅ 同上 |

**主判据结论：缺陷真实存在且已被修复**——base 侧 6 个「>5000ms 段 / 黑障 + block」形态全部退化为 `REQUEST_TIMEOUT/3 @ ~5.04s`；fixed 侧按规格收场（`WAIT_TIMEOUT/1 @ 8.05s`、6000ms 桩成功）。

### 2.2 库面（`createHub`）两宿主同码对照（`/tmp/pr011-verify/lib-probe.mjs`）

```
FIXED  LIB block/wait8000  code=WAIT_TIMEOUT exitCode=1 8002ms req=1 text="等待超时（8000ms）：调用仍在进行"
FIXED  LIB no-mode        code=REQUEST_TIMEOUT exitCode=3 5000ms req=1 text="等待响应超时（5000ms；…）"
BASE   LIB block/wait8000  code=REQUEST_TIMEOUT exitCode=3 5003ms req=1 text="等待响应超时（5000ms；…）"
BASE   LIB no-mode        code=REQUEST_TIMEOUT exitCode=3 5001ms req=1
```

⇒ 库面与 CLI 面对同一黑障桩同 `code`/`exitCode`（`WAIT_TIMEOUT`/`1`）；且库面同样按 `mode` 分支（`mode` 缺省 ⇒ 5000ms 未放大）。**单点派生在两条宿主上均生效**。

### 2.3 变异注入（全部在 `/tmp/pr011-verify/m{1,2,3}-{base,fixed}/` 副本内；注入脚本 `inject.mjs`）

| 编号 | 注入对象 | 注入内容 | 改动前（base） | 改动后（fixed） |
|---|---|---|---|---|
| **B1** | `oamp/src/web.js` | 运行侧多注册第 5 条 SSE 路由（`GET /api/verify-injected-sse`, `kind: 'sse'`） | base 文件内 `kind === 'sse'` 集合断言命中数 = **0**（结构性零判别力） | **隔离跑 T4-5 红**：`运行侧订阅集合有 fixture 未覆盖的缺项: GET /api/verify-injected-sse`（`m1-fixed-iso.txt`）；未注入时全绿 |
| **B2** | `oamp/bin/hub.js` | 入口注入一次 `<包根>/.runtime/state.json` 写入 | **全文件跑 19 pass / 0 fail = 逃逸**；隔离跑 `--test-name-pattern='零仓库运行态'` **红**（DEV-01 / M12 逐字复现） | **全文件跑 18 pass / 1 fail = 检出**：`仓库运行态目录应与**模块基线**（早于本文件任何 hub 调用）一致：baseline={"runtime":null,"data":null}，after={"runtime":["state.json"],"data":null}` |
| **B3** | `oamp/src/task.js` | `renderList` 空态 `'（无任务）'` → `''`（stdout 变 `"\n"`） | **T7 绿 = 逃逸**（`stdout.length > 0` 被 1 字节 `"\n"` 满足） | **T7 红**：`task list（零任务）：stdout trim 后应为「（无任务）」单行（非空，防"空 vs 空"的虚假相等）` |

> B2 的对照口径注记：首次跑 m2-base 隔离跑曾得「绿」，经排查为**我的实验卫生问题**（`.runtime/state.json` 被前一次全文件跑遗留，使 `before` 也含该文件）。`rm -rf .runtime data` 后重跑，得到与 DEV-01 一致的 **base 隔离跑红 / base 全文件跑绿（逃逸）/ fixed 全文件跑红** 三态。报告采用清理后的结果。
> B1 的对照口径注记：宽注入同时打红了 base 与 fixed 的 T2/T3（集合双射类断言必然察觉路由表变化），因此 B1 的判别以**隔离跑 T4-5** 为判据；其判别力证据是「base 文件内 SSE 集合断言命中数 = 0 ⇒ 结构上不可能检出」。

### 2.4 scoped 零回归（本 verifier 亲跑）

| 运行 | 命令 | 结果 |
|---|---|---|
| 合并跑 ① | `node --test test/sdk-cli-contract.test.js test/sdk-api.test.js test/sdk-uds.test.js test/sdk-surface.test.js test/sdk-skill.test.js test/sdk-doctor.test.js test/hygiene.test.js` | `tests 94 / pass 94 / fail 0 / skipped 0 / todo 0`（`/tmp/pr011-verify/run1.txt`） |
| 合并跑 ② | 同上（连跑两次） | `tests 94 / pass 94 / fail 0 / skipped 0 / todo 0`（`run2.txt`） |
| 逐文件 | 7 个套件各跑一次 | `sdk-cli-contract 17/17`、`sdk-api 14/14`、`sdk-uds 19/19`、`sdk-surface 12/12`、`sdk-skill 8/8`、`sdk-doctor 21/21`、`hygiene 3/3`（合计 94 = 合并跑总数，一致） |
| T6-4 追加 | `node --test test/api-routes.test.js` / `test/cli.test.js` | `7/7` / `8/8`，`0 fail` |

新增的 5 条判据用例在两次运行中均为 `ok`：`T4-5 SSE 集合核对`、`T4 · --wait 8000 对黑障`、`T4 · >5000ms 段`、`T4 · 非 block 形态不放大`、`T4 · 单点派生的两宿主同码`、`T4 · 上游业务错误优先于本地超时`。

---

## 3. 逐项判定

### 3.1 PR 文件「验收标准」13 条 —— **13/13 pass**

| # | 验收摘要 | 判定 | 证据 |
|---|---|---|---|
| 1 | A-主判据（block 上限可达） | **pass** | §2.1 #1：`WAIT_TIMEOUT`/`1`/`error` 含 `8000`/stdout 空/`8046ms ∈ (7600, 9000)`/`requests === 1`；base 对照 `3 @ 5044ms` |
| 2 | A-上限内成功（>5000ms 段） | **pass** | §2.1 #2：`code 0`、`6045ms ∈ (6000, 7000)`、stdout 与桩 body 深等（`{"calls":[{"id":"x"}]}`，无信封/无改字段名） |
| 3 | A-缺省上限可达（有界观测） | **pass** | §2.1 #3：不给 `--wait` ⇒ `code 0`、`6046ms`；`DEFAULT_WAIT_MS` 常量面闭合 |
| 4 | A-<5000ms 原有路径不回归 | **pass** | §2.1 #7：`1000/600/1500` 全部 `WAIT_TIMEOUT`/`1`，耗时单调（642 < 1041 < 1543），每次 `req=1` |
| 5 | A-非 `--wait` 条目上限不变 | **pass** | §2.1 #6：`api docs` ⇒ `REQUEST_TIMEOUT`/`3`、`5042ms`、`error` 含 `5000` |
| 6 | A-上游业务错误优先于本地超时 | **pass** | §2.1 #8/#9：404/400 ⇒ `NOT_FOUND`/`INVALID_PARAM`、`exit 1`、`http_status` = 上游真实码、stdout 无残片、`43~45ms`（未被本地超时抢先） |
| 7 | A-单点派生、两宿主同享 | **pass** | 赋值点全仓恰 1（`oamp/sdk/surface.js:85`；`grep -rn headerTimeoutMs oamp/` 无第二处赋值）；§2.2 库面与 CLI 面同码；`oamp/sdk/http.js` 内 `mode` **零命中**（判定未下沉传输层）；lib 侧 `mode` 缺省 ⇒ 5000ms |
| 8 | A-偏差锁已翻转 | **pass** | 用例名现为 ``T4 · --wait 8000 对黑障：`--wait` 上限先到（WAIT_TIMEOUT/1、耗时 ≈ 上限、恰 1 次请求）``；`grep -rn '已知偏差 · 归 pr-011' oamp/` **零命中**；`grep -rn '本 PR 不改' oamp/` **零命中**；5 个受改文件内 `skip`/`todo` **零命中** |
| 9 | B1-SSE 集合有牙齿 | **pass** | §2.3 B1：新增 `T4-5`（`routeSet(hub, r => r.method === 'GET' && r.kind === 'sse')` ↔ 4 条逐字 fixture），`missing`/`extra`/`条数`/`签名互异` 四项断言；注入第 5 条 ⇒ 红并点名缺项 |
| 10 | B2-运行态判据无观测盲区 | **pass** | §2.3 B2：基线在模块作用域（`RUNTIME_BASELINE`，任何 `runHub` 之前）；注入 ⇒ **全文件跑必红**；base 的「全文件跑绿」逃逸形态已复现并被闭合 |
| 11 | B3-载荷丢失可捕获 | **pass** | §2.3 B3：两个只读组前置改为 `stdout.trim().length > 0`；注入空态 `''` ⇒ T7 红（base 同注入为绿） |
| 12 | C-零回归 | **pass**（scoped） | §2.4：7 套件 ×2 = 94/94 全绿，另 `api-routes 7/7`、`cli 8/8`。**仓库级全量未跑**，明示归属主 agent |
| 13 | C-零新增依赖 / 零越界 | **pass** | `dependencies === {}`；diff 恰 5 个文件；`oamp/src/**`、`oamp/bin/**`、`oamp/package.json`、`API.md`、`llms.txt`、`web/**`、`skill/hub.md`、`test/helpers/**`、其它用例文件**零 diff**；`ENTRIES === 40` 且 `sdk-surface.test.js` 的 `ENTRY_FIELDS` 契约套件全绿 |

### 3.2 tasks 文件 T1~T6 —— **fail 2 / pass 40**（T1 9 项、T2 9 项、T3 5 项、T4 6 项、T5 5 项、T6 8 项，合计 42）

| 任务 | 判定 | 证据 / 说明 |
|---|---|---|
| T1（1~9） | **pass** | 1：`RESPONSE_TIMEOUT_MS` 语义降为缺省、新增可选 `headerTimeoutMs`、未给时行为逐字一致（§2.1 #4/#5/#6 恒 5000ms）；模块头三条上限并列未合并。2：派生条件 = `params.waitMs !== null && flags.mode === 'block'`（`surface.js:85`），`mode` 判定未入 `http.js`，`cli.js` 零 diff。3：两个读取点同源（`http.js:98` 文案构造与 `:99` 定时器同用 `headerTimeoutMs`）。4：`headerTimeoutMs = waitMs + 1000 > waitMs`，`--wait 8000` 实测 `8046ms < 9000`。5：§2.1 #6 + 4 条 `kind:'stream'` 条目 `waitMs` 恒 `null`（订阅面既有断言全绿）。6：赋值点恰 1；归类仍只由 `http.js` 的 `waited` 决定，`errors.js` 零 diff。7：`ENTRIES 40` / `ENTRY_FIELDS` 契约绿 / `libParams` 取值链零改。8：两半边同处一个提交 `eb2e845`。9：`sdk-cli-contract` 整文件 `17/17`、零 skip/todo |
| T2（1~9） | **fail 1（T2-9）**，其余 pass | 1：§3.1 #8。2：既有五组零回归（套件全绿 + 未改期望值）。3：§2.1 #2。4：§2.1 #3 + 用例内注释登记「不可实测，由 `DEFAULT_WAIT_MS` 常量面 + 6000ms>5000ms 共同闭合」。5：三形态逐条断言（§2.1 #4/#5/#6）。6：§2.2（两槽调用形态、未把 `waitMs` 混进 flags）。7：§2.1 #8/#9（404+400 双例）。8：`--test-name-pattern='T4'` 与整文件均绿，失败消息含 `elapsedMs`/`stderr`。**9 fail**：`:1086`（现 `:1185`）的 `stderr.length > 0` 被改为 `.trim().length > 0`，与该条「`:1086` 的 stderr 非空断言不动」直接冲突（见 §5 偏差 D-1） |
| T3（1~5） | **pass** | 1：四项断言齐备（`missing`/`extra`/签名互异/`length` 双射），失败消息点名缺项。2：`t.diagnostic` 输出「用例号 ↔ 命令 ↔ 签名」4 行。3：§2.3 B1 注入必红 + base 零 SSE 集合断言对照。4：实验仅在 `/tmp` 副本，worktree 无残留。5：`sdk-api` 全绿；`sdk-api.test.js` 的 diff 为**纯追加**（+24/-0）；`routeSet` 原语未改 |
| T4（1~6） | **pass** | 1：基线落模块作用域（`OAMP_ROOT` 之后的 `RUNTIME_BASELINE`），`entriesOf` 前向引用成立。2：§2.3 B2 全文件跑必红。3：base 全文件跑绿（逃逸）vs 隔离跑红 + fixed 全文件跑红，三态齐备。4：`sdk-uds.test.js` 为唯一改动文件；`entriesOf`/socket 目录断言未改。5：隔离跑与全文件跑均绿。6：无新增等待/时序依赖（加载期一次只读快照） |
| T5（1~5） | **fail 1（T5-2）**，其余 pass | 1：两个只读组前置均 `trim()`（`:1171`/`:1176`），注释同步。**2 fail**：同 §5 偏差 D-1（`:1086` 被改，而该条明示「保持原状」）。3：§2.3 B3 注入必红 + base 绿（逃逸）。4：实验仅在副本。5：`--test-name-pattern='T7'` 与整文件均绿 |
| T6（1~8） | **pass** | 1：diff 恰 5 条 `M`，零实验残留（`src/**`、`bin/**`、`task.js` 与 base 零 diff）。2：`dependencies {}`、非范围面零 diff。3：`ENTRIES 40`（独立复核）+ `sdk-surface` 契约套件绿。4：7 套件 + `api-routes` + `cli` 全 `0 fail`。5：本 PR 未自行声称仓库级全量结果；**未跑**并明示归属主 agent（执行者回执未作为判据，本项按产出物面判定）。6：13 条验收由本报告独立重建证据链（§3.1 / §2.1~§2.4）。7：§3.1 #8。8：失败消息均含命令/期望/实际/耗时/stderr/两侧快照 |

> T6-1 附注（不改变判定）：worktree 内存在未跟踪文件 `docs/iterations/0025-hub-sdk-and-skill/prs/pr-011-tasks.md`。本 verifier 判定其**非本 PR 实现残留**：该文件的引入时间早于实现提交（`git log --oneline -- <path>` 为空 ⇒ 从未被提交；本会话在读取任何产出物之前首次 `git status` 即已见 `??`），且它是本 PR 的**标准来源之一**（任务图），非实现产物。`oamp/**` 侧零未跟踪残留。

### 3.3 §5.1 的 4 条 `[model_inferred]` —— **4/4 pass**

| MI | 判定 | 证据 |
|---|---|---|
| MI-1：两处 6000ms 用例需显式 `--mode block` | **pass** | 两处用例均带 `--mode block`（`callArgs()` 固定 `['…','--mode','block',…]`）；与 D1 一致。**该 MI 登记的「与 PR 原文冲突」风险已由 D1 承接**，见偏差 D-3 |
| MI-2：派生余量取值不指定 | **pass** | 实现取 `HEADER_TIMEOUT_MARGIN_MS = 1000`；两侧可观测结果满足（严格大于 `waitMs`，且 `--wait 8000` 耗时 `8046ms < 9000`） |
| MI-3：集合核对 fixture 形态与落点 | **pass** | 新增 `T4-5` 落 `sdk-api.test.js` 的 T4 段，`rows` 为 4 条**逐字**签名，未从运行侧反推 declared；复用 `routeSet` |
| MI-4：6000ms 桩响应体形状与耗时窗 | **pass** | body = `{ calls: [{ call_id: 'stub-call-6000', state: 'completed', text: '' }] }`（`{"calls":[…]}` 信封形态）；耗时窗逐字 `(6000, 7000)`，未收紧 |

### 3.4 关键实现约束 C1~C12（§4 硬约束）

C1 落点唯一 ✅｜C2 零新依赖/零新框架 ✅（无 helper 改动，diff 不含 `test/helpers/**`）｜C3 两半边同批 ✅｜C4 顺序不变量 ✅（`+1000`，实测 `8046 ∈ (7600, 9000)`）｜C5 `mode` 判定只在 `surface.js` ✅（`http.js` 内 `mode` 零命中；`cli.js` 零 diff）｜C6 上限语义不合并 ✅（三条并列仍在；`errors.js` 零 diff）｜C7 断言锚稳定符号 ✅（断言文本未出现行号）｜C8 判别实验在副本 ✅｜C9 零 flaky/不用假时钟 ✅（真实 `delayMs`；两次 94/94 无抖动；耗时窗取原文区间未收紧）｜C10 **✗（D-1）**｜C11 scoped 口径 ✅｜C12 失败可定位 ✅

---

## 4. 汇总计数

| 类别 | 计数 |
|---|---|
| 判定条目总数 | **59**（PR 13 + T1~T6 42 + MI 4） |
| **pass** | **57** |
| **fail** | **2**（T2-9、T5-2 —— 同一处越界的两个表述面） |
| partial / blocked | 0 / 0 |
| 偏差记录 | **5 条**（D-1 fail 级；D-2~D-5 记录级） |
| 下一迭代候选 | 5 条 |

---

## 5. 偏差记录

| # | 级别 | 位置 | 事实 | 判定基准（原文） |
|---|---|---|---|---|
| **D-1** | **fail** | `oamp/test/sdk-cli-contract.test.js`（base `:1086` → 现 `:1185`） | `assert.ok(unreachable.viaHub.stderr.length > 0, …)` → `assert.ok(unreachable.viaHub.stderr.trim().length > 0, …)`；该文件 T7 内被改的非空前置共 **3** 处（`:1171`/`:1176`/`:1185`），登记面只含**前 2 处** | PR 文件「文件范围」③ 只列「两个只读组的非空前置」；tasks **T2-9**「`:1086` 的 stderr 非空断言不动（§5.2-1）」、**T5-2**「其余 `length > 0` 断言（…`:1086`）不在本 PR 范围、**保持原状**」、**C10**「`:1086` 的 stderr 非空断言不动」、§5.2-1「本图**不改**该处…请裁决后再扩 T5 的范围」 |
| D-2 | 记录 | PR 文件「A-单点派生、两宿主同享」括号内 | PR 原文要求「**不按 `mode` 取值做语义分支**」；实现按 `flags.mode === 'block'` 分支 | 该句已被主 agent 裁决 **D1** 明确取代（tasks §0.3 D1），且本件判定基准（派生规则）亦写明「按 `mode` 分支派生」⇒ 判 **pass**；但 PR 文件原文未同步，后续 verifier 若按 PR 原文判会误判 fail。**建议同步 PR 文件该句** |
| D-3 | 记录 | PR 文件「A-上限内成功」「A-缺省上限可达」 | 两条原文未写 `mode`；在 D1 口径下**只有显式 `--mode block` 才可能为绿**（缺省 = 非可阻塞 ⇒ 5000ms ⇒ 6000ms 桩必红） | tasks §5.1-1（MI-1）已登记该冲突并给出裁决路径；本次按 D1 判定 ⇒ **pass**。**建议在 PR 文件补 `--mode block`** |
| D-4 | 记录 | tasks **T2-7** 末句 | 「两例的耗时均应 **>> `--wait` 上限**（说明未被本地超时抢先）」——与「上游错误**即时**归类」自相矛盾（0ms 桩不可能耗时 > 8000ms） | 按 PR 文件原文（「`--wait` 上限内收到 404/400 ⇒ 仍 `NOT_FOUND`/`INVALID_PARAM`…」）判定 ⇒ **pass**；实测 `43~45ms`，且若本地超时抢先则必为 `REQUEST_TIMEOUT`/`WAIT_TIMEOUT`，判别力充分。**建议改写该句为「耗时 << 4900ms」** |
| D-5 | 记录 | worktree 根 | 未跟踪文件 `docs/iterations/0025-hub-sdk-and-skill/prs/pr-011-tasks.md` | 非本 PR 实现残留（从未被提交；会话初始即存在；系标准来源）。仅登记，不影响 T6-1 判定 |

---

## 6. 还原与零触碰证据

1. **副本实验隔离**：三组变异注入的副本根为 `/tmp/pr011-verify/{m1,m2,m3}-{base,fixed}`；注入脚本 `/tmp/pr011-verify/inject.mjs`（每处注入前先断言锚点唯一，命中数 ≠ 1 即报错中止）。
2. **注入对象零触碰**：`oamp/src/web.js`、`oamp/bin/hub.js`、`oamp/src/task.js` 在 `1d934b7 → eb2e845` 之间**零 diff**（`git diff --quiet` 逐一通过）。
3. **git 写操作**：零；仅使用 `git log / show / diff / status`（只读）。
4. **工作区文件 vs 提交 blob（逐字相等）**：

```
SAME  oamp/sdk/http.js                     4adb32c63e6415066107e7a61958a403b59b012f007f20bb73e91eddad4e73db
SAME  oamp/sdk/surface.js                  0db035b84aaffd1d2514c88754d0a1c68121ac83ec831931275c05b7f08a2d8f
SAME  oamp/test/sdk-cli-contract.test.js   dfee1ea520f655cba5a9367b89ee277c547befa46a133fd7198fbde7c638073a
SAME  oamp/test/sdk-api.test.js            8abcaa07a8e308332a158ec27a4889efadcc4523cf872143b39248bb153ed7b9
SAME  oamp/test/sdk-uds.test.js            b2165d564992e6fcbe3b69dbd96cf0d598722114341905c1a1c2367f9ce320ca
base 副本 oamp/sdk/http.js                 aef7613905fdf6ec36e513e502aba4f7190e558b65b94d0d374c0b14833682c1  （= 1d934b7 blob）
base 副本 oamp/sdk/surface.js              e5e4b500c956401bcad29ec61a554d933a862747eda3306ffecf9f7aab6de910  （= 1d934b7 blob）
```

5. **工作区状态**：`git status --porcelain` = 仅 `?? docs/iterations/0025-hub-sdk-and-skill/prs/pr-011-tasks.md`（见 D-5）；HEAD = `eb2e845`。

---

## 7. 下一迭代候选

1. **D-1 二选一收口**：把 `sdk-cli-contract.test.js:1185` 回退为原状（遵 C10/T5-2），或由主 agent 追认扩范围（§5.2-1 已预留裁决口）。若不收口，本 PR 在「范围由登记面界定」这一纪律上留下先例。
2. **同步 PR 文件与裁决**：把「A-单点派生」括号内「不按 `mode` 取值做语义分支」改写成 D1 口径的表述；在「A-上限内成功」「A-缺省上限可达」补 `--mode block`。否则每一轮独立 verifier 都要重新裁决同一处文档-实现表面冲突（D-2/D-3）。
3. **修正 tasks T2-7 的耗时判据措辞**（D-4）：「耗时 >> `--wait` 上限」→「耗时 << 本地超时级别（≤4900ms）」。当前措辞可被机械解读为不可满足项。
4. **余量语义登记**：`HEADER_TIMEOUT_MARGIN_MS = 1000` 使「响应头在 `[waitMs, waitMs+1000)` 到达」的调用被归为 `WAIT_TIMEOUT`。这与 F10「可阻塞条目恒有上限」相容，但该 1s 窗口未在任何文档登记；建议在 §5.4 落一行（避免下次把它当作 off-by-one 缺陷重新报）。
5. **`--mode` 取值归一化**：`--mode BLOCK` / 别名不派生（撞 C5 的「不做大小写/别名归一」⇒ 非缺陷）。建议在 `API.md` 或入口表登记 `mode` 取值的字面量集合，把这一边界显式化。
6. **判据用例时长**：新增长等待使 `sdk-cli-contract.test.js` 单文件跑时长升至 ~40s（整文件 `17/17` 实测约 40s）。若后续迭代继续叠加真实等待用例，建议评估是否拆分文件（纯观察，非缺陷）。

---

## 8. 结论

# 结论：**FAIL**（1 类越界；功能面无缺陷）

**据以判定的核心事实**：

1. **功能缺陷已真实修复**——自建黑障/延迟桩在 `1d934b7` 与 `eb2e845` 上的 9 组对照（§2.1）＋库面 2 组对照（§2.2）全部指向修复生效：`--mode block --wait 8000` 由 `REQUEST_TIMEOUT/3 @ 5044ms` 变为 `WAIT_TIMEOUT/1 @ 8046ms`；6000ms 段由失败变为成功且响应体原样；**非 block 形态（background / 省略 mode / 非 `calls create` 条目）仍恒为 5000ms 级 `REQUEST_TIMEOUT/3`，未随缺省 `--wait` 放大**——本 PR 最易搞错的反向回归点未被搞错。
2. **修法正确、单点、未越界到传输层**——`headerTimeoutMs` 全仓赋值点恰 1（`surface.js:85`），`http.js` 只取缺省、不判 `mode`；`src/**`、`cli.js`、`errors.js`、`package.json` 零 diff；`ENTRIES` 仍 40 条、字段集不变；`dependencies` 仍 `{}`。
3. **三处断言确实长牙**——B1/B2/B3 各自的变异注入均在全文件跑（或隔离跑 T4-5）下必红，且 B2/B3 的「改动前逃逸」对照（全文件跑绿 / T7 绿）逐字复现，随后被闭合。
4. **偏差锁确实翻转**——用例名去标记、断言集改为规格期望、`oamp/` 内 `已知偏差 · 归 pr-011` 与 `本 PR 不改` 零命中、零 `skip`/`todo`。
5. **但**：实现改动超出登记范围 **1 处**（`sdk-cli-contract.test.js:1185` 的 stderr 前置被 `trim()`），而这处被 tasks 的 T2-9 / T5-2 / C10 / §5.2-1 **四处明文要求「不动」「保持原状」**，且 §5.2-1 明确把「是否纳入」留给主 agent 裁决而**未获裁决**。该改动本身是同类加固、无功能风险，但它使两个登记验收条目（T2-9、T5-2）不成立 ⇒ 本 verifier 按 literal 判定为 **fail**，而非「基本通过」。

**返工建议（最小代价、一行）**：将 `oamp/test/sdk-cli-contract.test.js:1185` 回退为 `assert.ok(unreachable.viaHub.stderr.length > 0, '不可达态 status：stderr 应明确报错（不静默空结果）');`，则 59 项全 pass、偏差仅余 4 条记录级。若主 agent 认为该加固应当保留，则应以「追认 §5.2-1 的扩范围请求」的形式落在本 PR，并在 PR 文件「文件范围」③ 补记该行。

**未覆盖/未跑的项（明示）**：仓库级全量 `node --test test/*.test.js` **未跑**（按口径归属主 agent 在合并时执行一次作为零回归证据）；本 PR 登记的 `473/473` 基线未由本 verifier 复核（继承自迭代分支 tip `5dc04c4`，与本 PR base `1d934b7` 代码面等价）。
