# pr-005 独立验收报告（`hub` skill 文本与内容核对）

**验证者身份**：独立技术鉴定人 → 反射为 **文档契约审阅者（技术写作 + 机械可核性双背景）**。
本 PR 的产出物是"仅到子命令名的接口 skill 文本"＋"对该文本的机械核对用例"，其验收面几乎全部是**可机械判定的文本契约**（标题字面量、条目数、名面逐条对齐、禁写字面量零命中）与**改动面边界**（零 diff）。因此最合适的验证者是：能逐条把文本与上游契约表做**字面比对**、并能独立复跑断言的人，而不是"读起来顺不顺"的评审者。

**产出物**
- `/Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0025-hub-sdk-and-skill/.pb-agents/worktrees/0025-pr-005-hub-skill-and-content-check/oamp/skill/hub.md`（新建，129 行，sha256 `74c854a0f49e15877a6bb3a2976200cae8e0cd6ea1fff84855f6a932fe9b4952`）
- `.../oamp/test/sdk-skill.test.js`（新建，259 行，sha256 `6c7efd4a8497a207cc1789a50f496697425414002546d182ec00184befcf6ebc`）
- 实现提交：`2d1849d`（`git -C <worktree> log --oneline 4d162d7..HEAD` 仅此一条）

**验证标准来源**（主 agent 指定，三份）
1. `docs/iterations/0025-hub-sdk-and-skill/prs/pr-005-hub-skill-and-content-check.md`「验收标准」段（14 条）
2. `docs/iterations/0025-hub-sdk-and-skill/prs/pr-005-tasks.md` 的 T1~T3 逐任务验收标准（该文件在 worktree 中未跟踪，仅作标准来源使用）
3. `docs/iterations/0025-hub-sdk-and-skill/architecture.md` §5.6（T-05 / T-06，含 §5.1 P-4 / §5.4）；`prd/F12-…md`、`prd/F13-…md`、`prd/G03-…md` 的验收标准

**验证日期**：2026-09-15

**独立复跑记录（我自己执行，未采信任何自述）**

```
$ cd <worktree>/oamp && node --test test/sdk-skill.test.js
1..8  # tests 8  # pass 8  # fail 0  # skipped 0  # todo 0

$ cd <worktree>/oamp && node --test test/*.test.js        # 全量拾取面
1..395  # tests 395  # pass 395  # fail 0  # skipped 0  # todo 0
其中 sdk-skill.test.js 的 8 条 = TAP #295~#302，全 ok

$ grep -c '<禁用字面量>' oamp/skill/hub.md   # /Users/ /home/ curl http:// https:// -- /api/ ~ $
全部 = 0；cd 前置（^cd / && cd / ; cd 三种形态）零命中；表格分隔行零命中；字段类型标注零命中

$ awk 分层独立计数  → A=21  B=8  C=11
$ grep -nE '^## ' → 恰 3 行（何时用 18 行区 / 怎么用 / 红线）；'^### ' → 9 行，无第四段顶层标题
$ git diff --name-status 4d162d7..HEAD -- oamp/  → A oamp/skill/hub.md ; A oamp/test/sdk-skill.test.js
$ git diff --name-only 4d162d7..HEAD -- roles/ tools/ .claude/skills/  → 空
$ git diff --name-only 4d162d7..HEAD -- oamp/src/ oamp/package.json oamp/bin/ oamp/sdk/ oamp/API.md → 空
$ git status --porcelain  → 仅 ?? docs/.../pr-005-tasks.md（本简报已声明不计入产出物）
```

---

## A. PR 文件「验收标准」14 条逐项判定

**A-1 落点 `oamp/skill/hub.md`、单文件自包含**：**pass**
证据：`git diff --name-status` 显示 `A oamp/skill/hub.md`；`ls oamp/skill/` 该目录下**只有 `hub.md` 一个文件**（无随附资产）。全文 129 行；开篇第 5 行自陈"读完本文件即可照做，不需要先打开别的文件"，且指向外部文件的**唯一**指路是 `oamp/API.md`（用于参数/字段），与 T1 验收 1 允许的唯一外部指路一致。三段（何时用 / 怎么用 / 红线）内容本身完整，不需要外部文件作为必读前置。

**A-2 三段式标题齐备且各自成段**：**pass**
证据：`grep -nE '^## '` 输出恰 3 行 —— `7:## 何时用` / `18:## 怎么用` / `125:## 红线`；`^### ` 9 行全部为 H3（入口定位、层 A/B/C、doctor、序列 1~4），**无第四段顶层标题**。三段各自成段（区间不交叉）。

**A-3 "何时用"给出可判定触发条件**：**pass**
证据：`## 何时用`（7~16 行）逐条列出 6 条：派发角色任务 / 取调用终态 / 取过程记录（转录，含截断场景）/ 订阅事件 / 盘点实例状态 / 走既有运维命令，与要求 6 类一一对应。`grep '需要与 hub 交互'` 零命中（无循环表述）。

**A-4 三层清单 21/8/11 逐条与 §5.1 名面一致；`doctor` 单列并标注**：**pass**
证据：独立 awk 分层计数 = `A=21 B=8 C=11`（合计 40）；三小节 H3 字面量为 `### 层 A · api（21 条）` / `### 层 B · uds（8 条）` / `### 层 C · cli（11 条）`。
名面逐条比对（我按 `architecture.md:328-408` 三张表的「SDK 子命令」列 / 层 C 表「SDK 入口」列逐字对齐，剥掉位置参数占位符）：
- 层 A：21/21 命中且**顺序与 §5.1 表序完全一致**（含 `api calls create/list/stream calls/stream call/transcript/get` 这一非字典序片段也一致），无缺项、无多出项。
- 层 B：8/8 命中，顺序一致。
- 层 C：11/11 命中（`hub cli …` 前缀按 §5.1 列名剥为 `cli …`），顺序一致。
- `doctor` 在三个层小节**之外**单列于 `### doctor（自检）`（92 行），且同行含标注字面量 `自检（不属于三层封装）`，与 §5.1 P-4（`architecture.md:361`）逐字一致。三个层小节体内均无 `doctor` 字样。

**A-5 4 条序列齐备且各给可照做的命令顺序**：**pass**
证据：4 个 H3 字面量齐备（96 / 101 / 109 / 117 行），与钉死的 `### 序列 1：派发 → 取终态` / `### 序列 2：截断恢复` / `### 序列 3：事件订阅` / `### 序列 4：状态盘点` 逐字一致。每条给出**有序步骤**，每步 = `node "<项目根>/oamp/bin/hub.js" <层> <子命令> [位置参数占位符]` 形态，步骤面覆盖 T1 验收 7 的骨架（序列 1：`api calls create`→`api calls get <call_id>`；序列 2：`api calls get`→`api calls transcript`；序列 3：`api stream events`/`api stream chat <chat_id>`/`api stream call <call_id>`；序列 4：`api agents`＋`uds router.status`＋`cli status`）。序列步骤**不写选项**（符合 C3），并在末尾 123 行明示"每一步的选项与字段以 `oamp/API.md` 为准"。
边界（不算 fail，另见 C-F12-4）：**实跑可照做性属运行面**，本 worktree 无 `oamp/bin/hub.js`（`ls oamp/bin/` 仅 `oamp.js`），该面归 pr-004 与 pr-007~pr-010。

**A-6 "截断恢复"是序列不是命令**：**pass**
证据：序列 2 的步骤**恰为两条已有子命令的组合**（105 行 `api calls get <call_id>` + 106 行 `api calls transcript <call_id>`），并显式写"**由消费方自行拼接**"（103 行）；106 行步骤 3 亦为"消费方自行拼接过程条目与终态体，重建全文"。清单 21+8+11+`doctor` 中无任何"一步恢复全文"专用命令；正文 107 行明确"清单里没有'一步恢复全文'的专用命令，SDK 也不代做正文重建"（**负向断言**，不宣称 SDK 代做）。与 F12 验收 5 / W3·C-A 口径一致。

**A-7 红线三条齐备**：**pass**
证据：`## 红线`（125~129 行）三条齐备 —— ① 不裸写 HTTP（128 行，明写"一律经 `hub` 子命令或 SDK 方法…不要手写 socket、自建 HTTP 客户端"）；② 不复制 schema（129 行，"选项、参数与字段以 `oamp/API.md` 为准，不要在别处再记一份"）；③ 退出码语义（129 行末条）：`0` 成功 / `1` 业务失败 / `2` 用法错误（本地参数解析失败，未发出请求）/ `3` 连接失败（连不上、超时、上游不可达）——四值语义与 `architecture.md:473-500` §5.4 归类表**逐字一致**，无第五类、无归类改写。

**A-8 正文不含参数表、字段定义清单、端点路径表**：**pass**
证据（四类机械判据全部独立复跑，零命中）：markdown 表格分隔行 = 0（`grep -nE '^[[:space:]]*\|?[[:space:]]*:?-{3,}'`）；`--` 选项字面量 = 0；`/api/` 端点路径字面量 = 0；字段类型标注（`[:：]\s*(string|number|boolean|integer|object|array)`）= 0。全文无 markdown 表格。

**A-9 入口定位模板形态正确**：**pass**
证据：`### 入口定位` 小节（20 行）位于 `## 怎么用` 段内。shell 面字面量逐字命中（25 行）：`node "<项目根>/oamp/bin/hub.js" <层> <子命令> [选项]`；库面字面量逐字命中（31 行）：`const { createHub } = await import('<项目根>/oamp/sdk/index.js');`（含钉死的 `await import('<项目根>/oamp/sdk/index.js')`）。22 行含钉死句"**项目根由调用方自行解析**（不由 cwd 推导）"。

**A-10 模板约束自检**：**pass**
证据：`/Users/` = 0、`/home/` = 0、`cd` 前置三种形态（行首 / `&&` 后 / `;` 后）= 0、`~` = 0、`$`（环境变量引用体）= 0。正文 35 行另以散文声明"不需要先切到某个目录，也不依赖本机特有值"，与 F13 验收 3/4 正向一致。

**A-11 4 条序列全程零手写 HTTP**：**pass**
证据：`curl` = 0、`http://` = 0、`https://` = 0；无 socket 手写、无自建 HTTP 客户端式示例。4 条序列的每一步（98/99/105/106/113/114/115/119/120/121 行）全部是 `hub` 子命令形态，逐条 ∈ §5.1 的 40 条入口（见 A-12 / T1-12），与 E1 裸判据一致。

**A-12 改动面不含 `roles/**`、`tools/**`、`.claude/skills/**`**：**pass**
证据：`git diff --name-only 4d162d7..HEAD -- roles/ tools/ .claude/skills/` = **空**；`git status --porcelain` 仅一条 `?? docs/.../pr-005-tasks.md`（本简报声明不计入产出物，且属 `docs/**`）。`oamp/package.json`、`oamp/API.md`、`oamp/src/**`、`oamp/bin/`、`oamp/sdk/` 亦零 diff（`oamp/sdk/` 本就不存在）。全量 diff 恰为 2 个新增文件（`A` 状态）。

**A-13 `oamp/test/sdk-skill.test.js` 机械核对（四项）＋只读文本不 import SDK**：**pass**
证据：文件内仅 5 条 import —— `node:test` / `node:assert/strict` / `node:fs` / `node:url` / `node:path`；无指向 `sdk/` 的 import，无 `child_process`，无 `net.` / `fetch(`，无 `writeFileSync` / `mkdirSync`（零写盘、零进程、零端口）。`^test` 恰 8 条，无 `skip` / `todo`。四项核心机械核对对应 4 条独立 `test(...)`：
- 三段标题齐备（每条 H2 字面量命中恰 1 次 + 顶层 H2 集合 deepEqual 三段）
- 4 条序列标题齐备（各恰 1 次）
- 无参数表与字段清单特征（表格分隔行 / `--` / `/api/` / 字段类型标注 四类零命中）
- 无本机路径字面量与 `cd` 前置（`/Users/` `/home/` `cd 前置三形态` `~` `$VAR` 各零命中）

**A-14 用例经 `node --test test/*.test.js` 被拾取并通过**：**pass**
证据：<worktree>/oamp 下 `node --test test/*.test.js` → `1..395`、`# pass 395`、`# fail 0`、`# skipped 0`、`# todo 0`；TAP 输出中出现 `F12-2：…` `F12-4：…` `F12-7 / N8：…` `F13-1/3/4：…` `F12-4 / E1：…` `P-4：…` `§5.1 / F14-1·3：…` `§5.4 / F12-6：…` 共 8 条用例名（#295~#302，全 `ok`），证明其落在既有 `scripts.test`（`package.json:11-13`）拾取面内且全绿；全量无一条 `not ok`，故无需做 T3 验收 4 的 stash 回归对照。

**PR 文件 14 条 → 14 pass / 0 fail / 0 partial。**

---

## B. `pr-005-tasks.md` 逐任务验收标准（T1~T3）

### T1（`oamp/skill/hub.md`）12 条

- **T1-1 落点与自包含**：pass。证据见 A-1；`oamp/skill/` 目录下恰一个文件，无随附资产；唯一外部指路 = `oamp/API.md`（T1-1 明文允许）。
- **T1-2 三段标题齐备且各自成段（H2 各恰一次、下层一律 H3）**：pass。`^## ` 恰 3 行，`^### ` 9 行，无第四段顶层标题。
- **T1-3 "何时用"6 条可判定触发条件、无循环表述**：pass。见 A-3；6 条逐条可与场景对答"算不算"（如"只读本地文件/只看代码"被明示为不命中）。
- **T1-4 入口定位模板在 `## 怎么用` 段内（`### 入口定位`）＋两处字面量＋"项目根由调用方自行解析"句＋不新增第四段**：pass。20 行 H3、25 行 shell 字面量、31 行库面字面量、22 行钉死句，四者齐备。
- **T1-5 模板约束自检（零本机字面量 / 零 `cd` 前置 / 零 `~` / 零环境变量前置）**：pass。五类独立复跑全零命中。
- **T1-6 三层清单：只到子命令名、条目数 21/8/11、名面逐字一致、`doctor` 单列且标注**：pass。见 A-4；条目**一行一条**、条目内无选项 / 无位置参数占位符 / 无字段（清单条目为纯子命令名，位置参数占位符只出现在序列步骤中）。
- **T1-7 4 条典型序列齐备且各给可照做的命令顺序**：pass（文本面）。见 A-5；四步骨架逐条覆盖，步骤只用 §5.1 的 40 条入口组合、不写选项。
- **T1-8 "截断恢复"是序列不是命令**：pass。见 A-6；步骤恰两条已有子命令组合、清单无专用命令、明文"由消费方自行拼接"且不宣称 SDK 代做。
- **T1-9 红线三条齐备且可核（退出码四值逐字对齐 §5.4）**：pass。见 A-7。
- **T1-10 不成为第二份文档（四类特征零命中）**：pass。见 A-8。
- **T1-11 4 条序列全程零手写 HTTP**：pass。见 A-11。
- **T1-12 蓝本同源性（正文中每条子命令名都能在 §5.1 的 40 条中找到，无自造 / 无编排命令）**：pass。正文出现的全部子命令实例：清单 40 条（21+8+11 已逐条对齐）＋ 序列中 `api calls create` / `api calls get` / `api calls transcript` / `api stream events` / `api stream chat` / `api stream call` / `api agents` / `uds router.status` / `cli status` —— 逐条 ∈ 上述 40 条；`doctor` ∈ §5.1 P-4 的第四个顶层入口。`\bdispatch\b` / 编排动词零命中（`\bawait\b` 仅 2 处命中，均为库面模板的 `await import(...)` / `await createHub()`，非子命令）。

### T2（`oamp/test/sdk-skill.test.js`）6 条

- **T2-1 只读文本、零 SDK 依赖**：pass。见 A-13：import 集仅 Node 内置 5 个；零 `sdk/` 引用、零 `child_process`、零网络、零写盘；被核文件路径按 `import.meta.url` 推导（`path.resolve(dirname(fileURLToPath(import.meta.url)), '..')` → 包根），不依赖 cwd。
- **T2-2 核心机械核对 4 项，各一条 `test(...)`，断言以字面量集合表述**：pass。4 条 `test` 分别覆盖三段标题 / 4 条序列标题 / 无参数表与字段清单特征 / 无本机路径与 `cd` 前置；断言对象为字面量数组（`H2_TITLES` / `SEQUENCE_TITLES` / `NOT_A_SECOND_DOC` / `LOCAL_COUPLINGS`）。
- **T2-3 追加机械检查 4 项**：pass。`curl`+`http://`+`https://` 零命中（`HAND_WRITTEN_HTTP`）；`doctor` 标注字面量存在且不出现在三个层小节内（`P-4` 条）；三层条目数 == 21/8/11 且逐条名面一致（`§5.1 / F14-1·3` 条，含 40 条合计断言）；`## 红线` 段内含退出码四值语义（`§5.4 / F12-6` 条，逐值正则）。合计 8 条 `test` = 4 核心 + 4 追加。
- **T2-4 断言落在稳定字面量上，不依赖实现措辞**：pass。断言面仅为 T1 已钉死的标题字面量、三层名面、四类禁写模式与退出码值；**未**出现行号断言、段落长度断言、段落字节数断言、命令示例措辞断言（序列骨架命令名除外）。`sectionBody()` 以"标题行 → 下一个 ≤ 同级标题"界定小节，改写非钉死散文不影响通过。
- **T2-5 失败时可定位**：pass。每条断言的 message 均点名期望字面量与实测次数/缺项（如 `小节「…」条目数应为 21，实际 N`、`期望本机路径字面量 /Users/ 零命中，实际 N 次`、`{missing, extra}` 结构化差异），失败即可直接回答"缺哪一项"。
- **T2-6 `node --test test/sdk-skill.test.js` 全绿，无 skipped / 无 todo**：pass。8/8 pass，`# skipped 0`、`# todo 0`；文件内无 `skip`/`todo` 字样。

### T3（收口核对与边界审计）5 条

- **T3-1 改动面恰为本 PR 文件范围的两条新增路径（判据面 `-- oamp/`）**：pass。`git diff --name-status 4d162d7..HEAD -- oamp/` → `A oamp/skill/hub.md`、`A oamp/test/sdk-skill.test.js`（恰两条、均为新增；`git diff --stat` 显示 388 insertions / 0 deletions）。`docs/**` 未纳入该判据面。
- **T3-2 G03 三处零 diff ＋ `package.json` 不在 diff**：pass。`-- roles/ tools/ .claude/skills/` 输出为空（命令输出紧接 `(end)` 标记，确认为空）；`-- oamp/package.json` 为空；未新增任何指向 `oamp/skill/hub.md` 的副本 / 转发 / 软链（全量 diff 仅 2 文件，`.claude/skills/` 无新目录）。
- **T3-3 用例被既有拾取面拾取**：pass。`node --test test/*.test.js` 的 TAP 中出现 `sdk-skill.test.js` 的 8 条用例名（#295~#302）且全 `ok`。
- **T3-4 无因本 PR 引起的新增失败**：pass。全量 395/395 全绿、`# fail 0`，不存在需要 stash 对照的失败项，该条的对照前提自然满足。
- **T3-5 PR 14 条验收逐条对位 + 未实测条目显式标注边界**：pass。我在本报告 §A 逐条独立复现了 14 条（全部 pass，含命令与输出摘要）；未实测的运行面条目（F13 验收 2/5 的"仓库外目录执行"与三宿主一致、F12 验收 4 的"照做走通"）在任务的边界段被**显式标注**为"本 PR 只能保证文本面…不主张本 PR 独力闭合 F13 验收 2/5"（该文件 §5 边界与疑问 6），未以"看起来没问题"结案；我在 §C 亦按此边界分别判定。

**T1~T3 → 23 pass / 0 fail / 0 partial。**

---

## C. `architecture.md` §5.6 与 F12 / F13 / G03 卡片验收标准

### C-1 `architecture.md` §5.6（T-05 / T-06 落定，含 §5.1 P-4、§5.4）

- **T-05 具体路径 = `oamp/skill/hub.md`，单一文件、自包含、不进 `.claude/skills/`、不建副本**：**pass**。落点命中；目录下仅此一文件；`roles/**` / `tools/**` / `.claude/skills/**` 零 diff。
- **T-05 内容面（① 何时用 ② 只到子命令名的三层清单 + 4 条序列 ③ 红线；不出现参数表 / 字段定义清单 / 端点路径表）**：**pass**。三段齐备且结构为一 H2×3 + H3×9；四类禁写特征零命中。
- **T-06 跨 skill 引用机制 = 路径引用模板（项目根 + 绝对路径），零注册表 / 索引 / 加载器 / 软链 / 副本 / 转发**：**pass**。全量 diff 仅 2 文件且均在 `oamp/` 内，无任何登记实体或转发实体；模板以 `<项目根>` 占位 + 绝对路径表达，与 §5.6 的机制描述一致。
- **§5.6 入口定位模板 + 约束自检（不含 `/Users/` `/home/`、不含 `cd` 前置、不含 `~`、不含环境变量前置）**：**pass**。两处模板字面量齐备；五类禁写全零命中（见 A-10）。
- **§5.1 P-4 口径（`doctor` 单列并标注"自检（不属于三层封装）"）**：**pass**。`### doctor（自检）` 位于三层小节之外，标注字面量逐字一致。

### C-2 `prd/F12-hub-skill-three-sections.md` 验收 1~7

- **F12-1 与 SDK 同包落 `oamp/` 内、可被路径寻址消费、仅读该文件即可得到完整三段式**：pass。文件在 `oamp/` 包内（非 `.claude/skills/`）；通读全文即得完整三段式；参数/字段按 N8 明确不在内容面内，唯一外部指路 `oamp/API.md` 由 T1-1 允许。
- **F12-2 三段式结构齐备，各段有明确无歧义标题**：pass。`## 何时用` / `## 怎么用` / `## 红线` 各恰一次。
- **F12-3 "何时用"给出可判定的触发条件（非"需要与 hub 交互时用它"式）**：pass。6 条逐条可判定；循环表述零命中。
- **F12-4 "怎么用"含只到子命令名的清单 + 4 条序列齐备、各给可照做的命令顺序，全程零手写 HTTP**：**partial**
  - 文本面 **pass**：清单 21/8/11 + `doctor` 单列（只到子命令名、零 `--`）；4 条序列标题齐备、步骤为可照做的**命令顺序**（每步一条 §5.1 入口）；`curl` / `http://` / `https://` / socket 手写零命中。
  - 运行面 **blocked**：该条判定语要求"按 4 条序列逐条照做 → 每条都能走通"，实跑需要 `oamp/bin/hub.js`；本 worktree `ls oamp/bin/` 仅 `oamp.js`，`oamp/sdk/` 不存在、`package.json` 无 `bin.hub` ⇒ 本 PR 产出物面**无法**执行序列。缺少的信息 = pr-004 的入口实现（用例面归 pr-007~pr-010）。
- **F12-5 "截断恢复"是序列不是命令（组合已有子命令 + 消费方自行拼接、无专用命令、不宣称 SDK 代做）**：pass。见 A-6。
- **F12-6 "红线"三条齐备且可核（含退出码 0/1/2/3 约定）**：pass。见 A-7；"不复制 schema"以正文无参数表 / 字段清单核对（A-8）亦成立。
- **F12-7 不复制 schema、不成为第二份文档**：pass。四类特征零命中。

### C-3 `prd/F13-skill-sdk-entry-addressing.md` 验收 1~5

- **F13-1 模板存在且形态正确、不含本机绝对路径字面量**：pass。见 A-9 / A-10。
- **F13-2 不依赖 cwd（从仓库外任意目录执行模板实例化后的命令，SDK 被找到且结果与仓库内一致）**：**blocked**。缺少 `oamp/bin/hub.js` 与 `oamp/sdk/index.js`（本 worktree 均不存在）⇒ 无法实例化执行。该条属 pr-004 的交付面与 pr-007~pr-010 的用例面；本 PR 只提供文本形态，且其 `depends_on` 为空、文件面不含 `bin/` / `sdk/`。此 blocked **不阻塞本 PR 验收**（见结论）。
- **F13-3 模板不要求前置切目录**：pass。`cd` 前置三形态零命中；正文 35 行正向声明"不需要先切到某个目录"。
- **F13-4 不写死机器相关值（不依赖 `~` 展开、不依赖环境变量前置）**：pass。`~` = 0、`$` = 0；定位只用"项目根 + 相对位置"（22 行明示）；正文未写死端口或其它本机特有值。
- **F13-5 三种宿主下同一模板都成立且结果一致**：**partial**
  - 文本面 **pass**：三种宿主写法齐备 —— ①shell = `node "<项目根>/oamp/bin/hub.js" …`（25 行）②Node = `await import('<项目根>/oamp/sdk/index.js')`（31 行）③另一 agent 经 shell = 与 ①同形。
  - 运行面 **blocked**："定位后得到一致的结果（退出码与 JSON 结构一致）"需可执行入口，同上缺 `bin/hub.js` / `sdk/index.js`。

### C-4 `prd/G03-roles-and-distribution-untouched.md` 验收 1~4

- **G03-1 `roles/**` 零改动**：pass。`-- roles/` diff 为空。
- **G03-2 不进分发脚本的分发面（既有分发脚本未被修改）**：pass。`-- tools/` diff 为空。
- **G03-3 角色接入留待独立迭代（角色定义与工作流规范零引用）**：pass。本 PR 未触碰 `roles/**` 与工作流规范，改动面仅 2 个 `oamp/` 内文件，不存在对 SDK / skill 的新引用。
- **G03-4 不改 `.claude/skills/**`、不新增指向它的转发 / 副本**：pass。`-- .claude/skills/` diff 为空；全量 diff 无副本 / 转发 / 软链实体。

**§5.6 + 卡片 → 5 pass / 4 pass / 3 pass + 1 partial / 4 pass。**

---

## 汇总

- **pass：55 项**
  - PR 文件 14 条：14 pass
  - T1~T3：23 pass（T1 12 / T2 6 / T3 5）
  - §5.6：5 pass｜F12：6 pass｜F13：3 pass｜G03：4 pass
- **fail：0 项**（无需返工）
- **partial：2 项**（F12 验收 4、F13 验收 5 —— 均为"文本面通过 / 运行面不可执行"，非本 PR 产出物面所致）
- **blocked：1 项**（F13 验收 2 —— 缺 `oamp/bin/hub.js` / `oamp/sdk/index.js`，属 pr-004 交付面）

---

## 偏差记录

> 实现与现有规格/文档不一致之处。不影响本次验收判定，但应在下一迭代同步文档或评估是否调整实现。

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| `architecture.md` §5.6「入口定位模板」示例块为 `node "<项目根>/oamp/bin/hub.js" api agents --state online`（**含选项 `--state online`**） | `oamp/skill/hub.md` 全文 `--` 选项字面量零命中（25 行模板为 `… <层> <子命令> [选项]`，序列步骤一律不带选项），遵循 N8 + F12 验收 4「只到子命令名」 | 两处口径并存会误导后来者：建议在 `architecture.md` §5.6 的模板块去掉 `--state online`（或补一句"示例中的选项由消费方按 `API.md` 补充，skill 正文不复制选项"） |
| `architecture.md` §5.2 T-03「库 API」落定为同步构造：`const hub = createHub();` | skill 31~32 行为 `const { createHub } = await import(...)` ＋ `const hub = await createHub();`（把构造写成 await 形态） | 建议把 skill 第 32 行与 §5.2 对齐（`const hub = createHub();`），或在 §5.2 明确 `createHub()` 允许 async；`await` 作用于非 Promise 值不报错，故当前不影响可照做性 |
| `prd/F12` 验收 4 判定语要求"按 4 条序列逐条照做 → 每条都能走通"，而 `architecture.md` §5.1 规则 4 规定"缺必填 flag ⇒ 用法错误 `2`（不发请求）"，且层 A #14 `api calls create` 必有 `--agent` 等必填项 | 序列 1 首步仅写 `api calls create`（无选项），照原文直接执行必然退出 `2`；需消费方按 skill 末尾指引去 `oamp/API.md` 补齐必填项后方可走通 | 这是"只到子命令名（N8）"与"照做走通"两条口径的固有张力（任务图 §5-2 已按保守侧裁决并留待主 agent）：建议在 `prd/F12` 验收 4 或其判定语中显式写明"照做 = 按 `API.md` 补齐必填选项后的顺序"，避免下游把"只给子命令名"读成"可直接运行" |

（偏差 1、2 为跨文档口径不一致；偏差 3 为规格内部张力。三条均不改变本 PR 的 pass 判定。）

---

## 下一迭代候选

- **运行面闭合（F13 验收 2/5、F12 验收 4 的实跑）**：待 pr-004 的 `oamp/bin/hub.js` / `oamp/sdk/index.js` 落盘后，需补一条"从仓库外目录（如系统临时目录）执行模板实例化命令"的用例，以及三宿主一致性用例；本次在 pr-005 的 worktree 内无法闭合，属 pr-007~pr-010 用例面。
- **序列示例是否允许携带必填选项**：当前收紧口径（全文零 `--`）导致序列 1 不能直接执行。若下一迭代认为"可照做"优先于"零选项"，放宽点 = 判据集改为"**清单条目内**零 `--`、表格分隔行零命中"（序列允许写必填选项），T1-6 / T2-2 的机械断言需同步调整。
- **skill 未点出"缺必填选项 ⇒ 退出码 2"这一常见诊断场景**：红线 ③ 已含"本地参数解析失败（未发出请求）"，但未点出必填缺失。可评估在红线 ③ 内补半句，帮助消费方自行诊断（不新增 schema 知识，仍在 N8 面内）。
- **跨 skill 引用实体仍为空**：T-06 明确本迭代不实施任何引用实体（零注册表 / 零副本 / 零转发），`roles/**` 与 `.claude/skills/**` 对新 tool 保持零引用；角色接入留独立迭代（W8 / A-5）——下一迭代若启动接入，需同时评估 F13 验收 2/5 的运行面验收方式。
- **`oamp/skill/` 目前只有一个文件且无版本/更新锚点**：skill 文本与被描述的 40 条入口表之间无机械绑定（本 PR 的用例只做名面集合比对，能发现清单漂移但不能发现"子命令语义变化而名面不变"）。属可选增强，未在本次验收标准内。

---

## 结论

**PASS**

- 所有条目为 pass，或 partial（文本面已核、运行面不在本 PR 产出物面）；**无 fail 条目** ⇒ 不需返工。
- 唯一 blocked 条目（F13 验收 2）的缺信息对象是 pr-004 的 `oamp/bin/hub.js` / `oamp/sdk/index.js`，本 PR 文件面与 `depends_on` 均不含该面，且任务图 §5-6 已显式标注该边界 ⇒ **可绕过**，不改变结论。
- 偏差记录 3 条，均属跨文档口径不一致或规格内部张力，被显式记录即满足交付条件。
