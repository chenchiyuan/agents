# F04：提问上浮（agent 主动提问登记进收件箱，不再被自动取消）

**功能 ID**: F04
**来源**: `demand.md` 第二段 §2 **W3**（`user_confirmed`：U-3 + Q2 + A1 + A3-P7）；§4 **M6**（登记待答、不代答）；§5 **E3**（可见性面）、**E5**（放弃兜底的对照面）；§3 **N6** / **N7** / **N12**；事实 **F2**（RPC 非门交互自动回 `{cancelled:true}`）/ **F4**（ACP 非 `Approve|Deny` 的 elicitation 一律 `decline`）
**迭代**: 0023-yolo-approval-and-question-inbox
**产品对象（引用 `demand.md` 只读实测，非本阶段决策）**: F2 / F4（两条链路现状都把提问当场取消）；0021 既有交付物 = 待确认收件箱（第三栏）+ 既有通知事件集合

---

## 用户价值

agent 说不清要什么的时候，终于是它在等我——而不是它自己猜一个、或者整轮直接死掉。

## 验收标准

1. **提问类请求登记进收件箱**（W3 / E3）：agent 发起提问时，控制台**待确认栏出现该条目**（**无需刷新页面**），条目内容等于提问内容。
   *判定*：构造一次提问 → 盯控制台看条目是否出现、内容是否等于提问内容。 〔判定方式逐字取自 E3 的裸判据〕
2. **不再被自动取消 / 拒绝**（W3 / N12 / M6）：提问请求**不再**被自动回 `{cancelled:true}` / `decline`；该轮**未在无人介入下被中止**。
   *判定*：同一构造动作 → 条目出现 + 该轮**不以 cancelled 收尾**（对照现状：提问必被取消）。
3. **通知沿用既有事件类型**（**N6** / A4-P11）：提问条目产生时的通知沿用既有事件类型（`confirmation_required`），**不新增事件类型**。
   *判定*：提问期间通知事件类型 ∈ 既有集合（`chat_completed` / `chat_failed` / `confirmation_required`）→ 无新类型。 〔措辞例外：既有事件类型名〕
4. **上浮不随档位变**（W3 / A4-P10）：提问类上浮与档位无关——**两个档位下提问均上浮**。
   *判定*：分别以 `yolo` 与 `always-ask` 各触发一次提问 → 两次都出现条目。
5. **人的注意力只花在有歧义的地方**（U-3 / U-4）：上浮的判据是"方案执行有歧义"（= agent 主动提问），不是"某种工具 / 某种帧"。
   *判定*：读上浮来源 → 指向 agent 的主动提问；不含"因工具类型而上浮"的通路（工具门是另一类条目 → F10）。

## 边界（不包含）

- 不含提问的**形状**（问题文本 / 选项 / 多选 / 自由文本 → F05）、**多问题**（→ F06）、**作答回传**（→ F07）、**未作答语义**（→ F08）、**两条链路的承载**（→ F09）。
- 不含 `kind:'permission'` 类条目（工具门 → F10）。
- **不含通知分级 / 免打扰 / 其它入口载体**（**N7** 封死，沿用 0021 既有裁决）；不含通知服务本身的改造（0021 既有）。
- 不含一次性 `omp -p` 路径与 `!` shell 路径（**N5**：无反向请求通道，维持现状、不上浮）。
- 不含**代答拒绝 / 自动作答**的任何形式（**N12** → F08 验收 3）。
- 不含信箱的信封字段名与形状承载（→ T-02）、不含两条链路的提问工具形态（→ T-05）。

## 架构待填（阶段 3 · **已回填**）

> **回填说明**：本节原为留白（阶段 2 体例）。阶段 3（技术架构）按 `docs/iterations/0023-yolo-approval-and-question-inbox/architecture.md` 落定，结论均在该文件的对应小节内可核（本轮另含 6 条真实探针证据，脚本与原始输出落 `clarifications/probes/`）。上方「越界自查」是**阶段 2 的历史自查记录**，其中「已落 T-xx 留白」一类表述由本节取代；**本卡的产品维度（用户价值 / 验收标准 / 边界）未被阶段 3 触碰**。

| 编号 | 原待填内容 | **架构落定（阶段 3）** |
|---|---|---|
| T-02 | 信封与提问形状的最终字段名（`request_kind` 取值表达 + 问题文本 / 选项含是否多选 / 是否允许自由文本 / 无选项纯自由文本 的字段承载） | 信封在既有 7 字段上**新增 `request_kind`（`'permission' | 'question'`，两类条目都带）与 `multiple`（boolean）**；四项可表达性逐项有承载：**问题文本 = 既有 `title`**（与既有"给人看的请求正文"同一展示位 ⇒ 前端零改动）、**选项集合 = 既有 `options`**（question 类 `option_id` = `label`）、**是否多选 = `multiple`**、**是否允许自由文本由 `request_kind:'question'` 蕴含**（question 类恒允许：带选项可附文本 + 纯自由文本两种形态都是验收要求，故不设恒真字段）、**无选项纯自由文本 = `options: []`**。`permission` 类字段集合与语义逐字不变。（architecture §5.2 / §7 T-02 / L1-3） |
| T-05 | 宿主工具的注册时机 / 命名 / 参数 schema + `acp` 表单到信封的映射细节（含"每题一个自由文本"与"多选"如何落进形状） | **默认链路（rpc）**：宿主工具名 **`ask_user`**，schema `{ question:string, options?:string[], multiple?:boolean }`（`required: [question]`，一调用 = 一问）；**注册时机 = 握手完成后、返回会话对象之前，恰一次**（实测：ready 前/后发送均被受理；重复注册为**替换**语义 ⇒ 不得重复；`--no-tools` 下仍可注册并调用 ⇒ 提问能力不依赖工具开关）。承接：`host_tool_call` ⇒ 冻结轮次计时 ⇒ 上浮 ⇒ `host_tool_result{result:{content:[{type:'text',text}]}}`；`host_tool_cancel` ⇒ 撤条目、不回包。**acp 链路**：`elicitation/create` 的非门形状按映射表拆问 —— askDialog（`q{i}` + `q{i}__other`）⇒ N 条（数组型 ⇒ `multiple:true`）；`value` 单值形状 ⇒ 1 条（`enum` ⇒ 选项 / `boolean` ⇒ 是·否 / `string` 无 enum ⇒ 纯自由文本）；**其余未知形状维持既有 `decline`**。（architecture §5.4 / §5.5 / §7 T-05 / L1-6） |
## 口径更替落点（`demand.md` 登记⑧ ②保留）

- 本卡承载 **②保留** 的通知面：0021 `W4`（通知服务）/ `W5`（三事件集合）/ `N4`（不做通知分级）/ `N7`（不做其它入口载体）**不退化**——提问类沿用既有事件类型与既有通知服务。

## 越界自查

- 本卡未做技术选型 / 数据模型 / 接口设计；信封字段名与提问工具形态已落 T-02 / T-05 留白。
- 验收 1 的判定方式**逐字来自** E3；验收 3 逐字来自 N6（"沿用 `confirmation_required`"）；验收 4 逐字来自 W3 与 A4-P10。
- **未派生**通知分级 / 免打扰 / 新事件类型 / 其它入口载体（N6 / N7）。
