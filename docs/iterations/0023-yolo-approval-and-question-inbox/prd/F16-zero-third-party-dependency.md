# F16：不引入第三方依赖（保持零依赖）〔保证项〕

**功能 ID**: F16
**来源**: `demand.md` 第二段 §3 **N10**（`user_confirmed`：A6 保留，沿用 0022 `N10`）；0022 `N10` / `F12` 既有边界（oamp 零依赖）
**迭代**: 0023-yolo-approval-and-question-inbox
**产品对象（引用 `demand.md` 只读实测，非本阶段决策）**: 0022 `F12` 既有结论（不引入第三方依赖、保持零依赖）

---

## 用户价值

不为这次改动引入新的外部包——依赖面不扩大。

## 验收标准

1. **零新增第三方依赖**（N10）：本迭代**不引入**任何新的外部包 / 运行时依赖。
   *判定*：复核依赖清单与包管理面 → **无新增外部包**。
2. **新增通路不引入依赖**（N10 的适用面）：档位配置面、提问上浮 / 作答回传、两条链路承载的实现**均不依赖新引入的外部包**。
   *判定*：同法逐面复核 → 无新增外部包。

## 边界（不包含）

- 不含既有依赖面的改动（0022 既有；本迭代不改）。
- 不含"为方便实现而临时引入"的任何形式（N10 封死）。
- 不含构建 / 打包 / 分发面的改造（不做）。

## 架构待填（阶段 3）

> **本卡无 `[架构待填]` 项**——产品维度为机械可复核的断言。

## 越界自查

- 本卡为**保证项卡**，只承载 `demand.md` 已确认的 N10；体例先例：0022 `F12`。
- **未派生**任何实现便利类需求（如"引入表单渲染库"）。
