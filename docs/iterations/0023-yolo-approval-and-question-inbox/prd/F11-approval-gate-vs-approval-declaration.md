# F11：能力位与档位声明分离（`approvalGate` 不随档位变）〔形态约束〕

**功能 ID**: F11
**来源**: `demand.md` 第二段 §4 **M8**（`user_confirmed`：**A4-P10** + 0022 `M6` / `A3-P3`）；0022 既有交付物 = 能力位显式声明机制
**迭代**: 0023-yolo-approval-and-question-inbox
**产品对象（引用 `demand.md` 只读实测，非本阶段决策）**: 0022 `M6` / `A3-P3`（缺能力必须显式降级；能力位逐协议声明）；0022 `W8` / `D-1`（"指定即生效"判据的单一含义）

---

## 用户价值

"这个协议有没有这道门"和"这次开不开这道门"是两件事——不会因为档位一改，能力说明就跟着变。

## 验收标准

1. **能力位不随档位变**（M8 / A4-P10）：描述"该协议**是否有工具门这一机制**"的能力位取值，**不随档位（`always-ask` / `yolo`）变化**。
   *判定*：以两个档位分别起同一协议的会话 → 对照该能力位取值 → **相同**。
2. **档位由档位声明单独表达**（M8 / A4-P10）："**本次生效形态**"由 profile 的**档位声明**（`approval` 字段）单独表达。
   *判定*：读该协议的 profile 档位声明 → 与本次实际档位一致；档位变化时只有该声明变、能力位不变。
3. **两件事不混写**（M8 的理由面 / 0022 `D-1` 体例）：能力位语义 = "**协议是否有该机制**"，**不含**"本次是否生效"。
   *判定*：读能力位定义 → 只有机制有无，无档位取值；不存在"档位 = `yolo` ⇒ 该协议不生效"的表述。

## 边界（不包含）

- 不含能力位的**最终集合与签名**（→ T-06）。
- 不含其它能力位的定义（0022 既有；本迭代不改）。
- 不含档位的取值域与默认值（→ F01 / F02）、不含档位解析落点（→ T-01）。
- 不含控制台内的能力展示形态（阶段 3；本卡只判"两件事分开表达"）。

## 架构待填（阶段 3 · **已回填**）

> **回填说明**：本节原为留白（阶段 2 体例）。阶段 3（技术架构）按 `docs/iterations/0023-yolo-approval-and-question-inbox/architecture.md` 落定，结论均在该文件的对应小节内可核（本轮另含 6 条真实探针证据，脚本与原始输出落 `clarifications/probes/`）。上方「越界自查」是**阶段 2 的历史自查记录**，其中「已落 T-xx 留白」一类表述由本节取代；**本卡的产品维度（用户价值 / 验收标准 / 边界）未被阶段 3 触碰**。

| 编号 | 原待填内容 | **架构落定（阶段 3）** |
|---|---|---|
| T-06 | 能力位的最终集合与签名 | **能力位集合与签名不变**（`CAPABILITY_KEYS` 六键原样；`capabilities()` / `capabilityNotes()` 签名不变）；`approvalGate` 恒按协议声明（rpc / acp = `'yes'`、oneshot = `'no'` + 非空 note），**与档位无关**——档位取值**不进入任何能力位表**（代码结构保证，非约定）。"本次生效形态"由**档位声明**独立表达：`spec.approval`（运行值）+ `AGENT_START` 事件新增的 `approval` 字段 + argv 的 `--approval-mode <值>`；三处同源于唯一汇聚点的同一输出 ⇒ F11 验收 2 的"一致"不可能破。（architecture §5.1 能力位面 / §5.6 / §7 T-06 / L1-7） |
## 越界自查

- 本卡为**形态约束卡**（体例先例：0022 `F09`），只钉"两件事分开表达"这一需求级形态；**未做技术选型 / 数据模型 / 接口设计**，集合与签名已落 T-06 留白。
- 验收 1~3 逐条来自 M8 与 A4-P10；**未新增能力位、未改 0022 既有能力位定义**。
