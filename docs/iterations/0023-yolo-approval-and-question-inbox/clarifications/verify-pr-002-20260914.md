# verify-pr-002-20260914.md — pr-002 独立验收报告

**验证者身份**：同级代码审查者（web 进程路由 / 服务端校验与回传契约）× 接口面审查者（确认项信封与 HTTP 接口契约）
**产出物**：`git -C /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0023-yolo-approval-and-question-inbox/.pb-agents/worktrees/0023-pr-002-web-envelope-and-decision-routing diff 4101e0b..00e9b58`（HEAD `00e9b58`，基线 `4101e0b`）
**验证标准来源**：`prs/pr-002-web-envelope-and-decision-routing.md`（验收标准 + 文件范围）、`prs/pr-002-tasks.md`（T1~T4）、`architecture.md` v0.3.0（§5.2 / §5.2.1 / §5.3）、`clarifications/2026-09-14-stage5-wave1-verdicts.md`（MI-a~MI-e + Q1~Q3）+ 主 agent 委托的 7 条验证标准
**验证日期**：2026-09-14

> 独立性声明：本报告只依据上面两份路径下的产出物与规格作出判定，未向执行者求证、未读取执行过程叙述；未修改任何被测产物（唯一写入 = 本报告）；未执行任何 git 写操作（`status --porcelain` 为空）。

---

## 逐项判定

### 标准 1：PR 文件验收标准全部满足 —— **partial**

10 条验收逐条判定（`prs/pr-002-web-envelope-and-decision-routing.md` §验收标准）：

| # | 验收摘要 | 判定 | 证据（可定位） |
|---|---|---|---|
| 1 | 信封字段白名单（9 键逐字段）+ 缺 `request_kind` ⇒ `'permission'` | pass | `oamp/src/web.js:1617-1627` 逐字段白名单重建：`confirmation_id` / `request_kind: body.request_kind === 'question' ? 'question' : 'permission'`（:1619）/ `chat_id` / `agent_id` / `tool` / `title` / `options` / `multiple: body.multiple === true`（:1625）/ `created_at`，无 spread；实跑断言 `oamp/test/confirmation-inbox.test.js:434-435`（`deepEqual` 9 键 + `Object.keys(...).sort()` 恰为 9 键清单）、`:455-457`（缺字段 / 非字符串 `42` / 域外串 `'nonsense'` ⇒ `permission`；非布尔 ⇒ `false`；`options: []` 原样）——该用例在实跑中 `ok 12`（`/tmp/verify0023-targeted.txt:72-73`） |
| 2 | 全局帧形状一致（恰一帧 / 重复不发帧 / 重建不发帧） | pass | `oamp/src/web.js:1628`（`if (inbox.add(entry)) transport.publishGlobal({ type: 'confirmation', data: entry })` 单一发布点）；实跑断言 `oamp/test/confirmation-inbox.test.js:436-440`（帧数 = 1、`deepEqual(frame.data, entryOf(q1))`、重复投递后仍 = 1）、既有 T3 用例覆盖「重建零帧 / 取消零帧 / 无定向帧」（实跑 `ok 10`） |
| 3 | 回传载荷按 `request_kind` 分化 | pass | `oamp/src/web.js:1317`（question ⇒ `{kind, confirmation_id, option_ids, text, chat_id}`，无 `option_id`）/ `:1325`（permission ⇒ `{kind, confirmation_id, option_id, text, chat_id}`，无 `option_ids`）；实跑断言 `oamp/test/confirmation-inbox.test.js:473`（`['B','A']` 同值同序）、`:382`（permission 体 `deepEqual` 逐字，含键集） |
| 4 | 旁路停掉（question 零 `in` / 零派发；permission 文本落地不变） | pass | `oamp/src/web.js:1333`（`const projectRow = !question && text !== '' && entry.chat_id !== null ? … : null`）为唯一闸门；实跑断言 `oamp/test/confirmation-inbox.test.js:496-497`（`in` 计数不变、`task.request` 计数不变）；permission 侧落地由既有用例断言（`oamp/test/confirmation-inbox.test.js` T2 用例实跑 `ok 11`，`:379-406` 区域） |
| 5 | 必填性与错误契约（三种输入 / 域外 / 非字符串数组 / 404 同码） | pass | `oamp/src/web.js:1311-1316`（question：缺失 ⇒ `[]`；非数组 / 域外 / 全空 ⇒ `inbox.add(entry)` 回填 + 400，回填与 `take()` 间无 `await`）；`:1297-1300`（404 同码）；实跑 10 类非法 body 断言 `oamp/test/confirmation-inbox.test.js:460-465`（每类 400 + 条目仍在表内）；404 同码 `:474-476`；「仅 text 非空白可提交」`:483-486`（含空选项态下非空 `option_ids` ⇒ 400 且保留 与 `:479-482`）；「仅 `option_ids` 非空可提交」`:488-492` |
| 6 | permission 类零回归（既有用例零削弱） | pass | 既有 7 字段口径未动（`oamp/src/web.js:1620-1624`、`1626` 与基线逐字相同）；permission 分支错误文案 `oamp/src/web.js:1322` 与基线 `4101e0b:oamp/src/web.js:1304` 逐字相同；既有用例断言形态未降级（`oamp/test/confirmation-inbox.test.js:193-203` 的 `entryOf` 由 7 字段扩为 9 字段、`:284-285` 的「帧 data 与列表元素同形状 + `Object.keys` 逐键清单」断言仍为逐键清单）；实跑 `ok 11`（T2 裁决契约）、`ok 10/11/12` 均绿 |
| 7 | 服务边界不变（F14） | pass | 见标准 6 |
| 8 | 无历史台账（F15） | pass | 见标准 6 |
| 9 | 派生面同步且漂移锁未削弱 | pass | 见标准 5 |
| 10 | 命令全绿且改动面封闭 | **partial** | 命令全绿（见标准 7，两命令退出码 0）；`oamp/` 改动面恰 4 文件 pass；但**分支整体 diff 含第 5 个文件** `docs/iterations/0023-…/prs/pr-002-tasks.md`（`+187`，由 `415d82e` 引入），不在 PR 文件「文件范围」清单内 ⇒ 「`git diff --stat` 只含本 PR 列出的文件」字面判据不成立（详见标准 2 与偏差 D-3） |

判定依据：9 条 pass、1 条（#10 的「只含本 PR 列出的文件」子句）不通过 ⇒ 本条标准为 partial。

### 标准 2：改动面恰为 4 个文件；`call-protocol.test.js` 零改动且行号窗口判据仍绿；`web/app.js`、`inbox.js`、`persist.js` 零改动 —— **partial**

- **`oamp/` 业务/产物面 = 恰 4 文件：pass**
  - 证据：`git diff --name-status 4101e0b..00e9b58 -- oamp/` ⇒ `M oamp/API.md`、`M oamp/llms.txt`、`M oamp/src/web.js`、`M oamp/test/confirmation-inbox.test.js`（`--numstat`：`16/9`、`1/1`、`38/18`、`100/3`）。
- **零改动清单逐条不出现：pass**
  - 证据：`git diff --numstat 4101e0b..00e9b58 -- oamp/src/inbox.js oamp/src/persist.js oamp/src/transport.js oamp/test/call-protocol.test.js oamp/web/app.js oamp/test/web.test.js oamp/test/api-routes.test.js oamp/test/project-workspace.test.js` ⇒ 输出为空；全量 `--name-status` 中除 4 个文件外仅 `docs/.../prs/pr-002-tasks.md`（见下）。
- **`call-protocol.test.js` 零改动且窗口判据仍绿：pass**
  - 证据：该文件 `--numstat` 为空（零改动）；判据位于 `oamp/test/call-protocol.test.js:1068-1081`（`SCOPE` 绝对行号窗口 `[1025,1050]` ∉ `ALLOWED`）；实跑 `node --test oamp/test/*.test.js` ⇒ `ok 44 - 0018 组 M（F15）：路由表零禁用词 / API.md 调用面章节命中 ⊆ 允许集合 / llms.txt 零命中 …`（`/tmp/verify0023-full.txt:266-267`），整轮 `# tests 362 / # pass 362 / # fail 0`，退出码 0。
- **`oamp/web/app.js` / `src/inbox.js` / `src/persist.js` 零改动：pass**
  - 证据：同上 `--numstat` 为空；`oamp/src/inbox.js:13,22,27,35,40` 导出面仍恰为 `add/list/take/remove/size`，`take()` 实现（`oamp/src/inbox.js:27-32`）为「取到即 `delete`」。
- **「改动面恰为 4 个文件」在分支整体 diff 上不成立：不通过**
  - 证据：`git diff --name-status 4101e0b..00e9b58` 第 5 行为 `A docs/iterations/0023-yolo-approval-and-question-inbox/prs/pr-002-tasks.md`（`415d82e docs(0023): pr-002 内部任务图…`），该文件不在 PR 文件「文件范围」段，也不在 `pr-002-tasks.md` §0.1「唯一可写面（`git diff --stat` 超出即 PR 验收 10 不通过）」的 4 行清单内。

⇒ 四个子项中「oamp/ 面 4 文件」「零改动清单」「call-protocol 零改动 + 窗口绿」「app.js/inbox.js/persist.js 零改动」均通过，仅「分支 diff 恰 4 文件」的字面读数不通过 ⇒ partial（记入偏差 D-3，非返工级：该第 5 文件是阶段 5 任务图，由独立规划提交引入，非业务代码夹带）。

### 标准 3：信封消费面 —— **pass**

- **`request_kind` 白名单（缺 / 非字符串 / 域外 ⇒ 兜底 `permission`）**：`oamp/src/web.js:1619`（`body.request_kind === 'question' ? 'question' : 'permission'`）——三态同归 `permission`，值域闭合为 `'permission' | 'question'`（architecture §5.2 类型列 / MI-e）；实跑断言 `oamp/test/confirmation-inbox.test.js:455`（`['question','permission','permission','permission','question','question']`，覆盖缺失、`42`、`'nonsense'`）。
- **`multiple`（非 `true` ⇒ false）**：`oamp/src/web.js:1625`（`body.multiple === true`）；实跑断言 `oamp/test/confirmation-inbox.test.js:456`（缺失与非布尔 `'yes'` 均为 `false`）。
- **条目键集符合 architecture §5.2**：`oamp/src/web.js:1617-1627` 键序 = `confirmation_id, request_kind, chat_id, agent_id, tool, title, options, multiple, created_at`，与 §5.2 表（「沿用既有 7 字段，新增 2 个」）9 键一致；实跑键集合断言 `oamp/test/confirmation-inbox.test.js:435`。
- 路由元数据 `response` 串亦为同一 9 键同序：`oamp/src/web.js:1261`。

### 标准 4：裁决分化 —— **pass**

- **question 类 `option_ids` 校验**：`oamp/src/web.js:1305`（`inOptions` = 字符串 + 命中 `entry.options[].option_id`）、`:1311-1316`（缺失 ⇒ `[]`；非数组 / 域外 / 与空 `text` 同空 ⇒ `inbox.add(entry)` 回填 + `400 INVALID_PARAM`）。校验前无 `await`（`take()` 在 `:1297`，回填在 `:1313`，中间仅纯计算）⇒ 保留在途的原子性与既有体例同源。
- **10 类非法 body ⇒ 400 且条目回填保留在途**：实跑 `oamp/test/confirmation-inbox.test.js:460`（`{}`、`{text:'   '}`、`{option_ids:[]}`、`{option_ids:['C']}`、`{option_ids:'A'}`、`{option_ids:42}`、`{option_ids:{}}`、`{option_ids:null}`、`{option_ids:['A',42]}`、`{option_ids:['C'],text:'理由'}` 恰 10 类），逐类断言 `:462-464`（status 400 + code + 条目仍在表内，含「域外取值不因 text 非空而放行」）。
- **回传**：`oamp/src/web.js:1317`（`option_ids` 同值同序透传 / `text` 取 `:1302` 的 trim 值 / `chat_id` 同值 / **不含** `option_id`，符合 MI-a）；实跑 `oamp/test/confirmation-inbox.test.js:473`（`{kind, confirmation_id, option_ids:['B','A'], text:'补充', chat_id}` 逐键 `deepEqual`）。
- **permission 类逐字不变**：错误文案 `oamp/src/web.js:1322` 与基线 `4101e0b:oamp/src/web.js:1304` 逐字相同；回传体 `oamp/src/web.js:1325` 与基线键集/键序/取值完全相同（`git diff` 中该行仅位置移动）；实跑 `oamp/test/confirmation-inbox.test.js:382` 断言 `deepEqual` 全对象（显式反证「不含 `option_ids` 键」）。
- **question 类停掉「文本 → chat 输入」旁路**：`oamp/src/web.js:1333`（`!question && text !== '' && …`）；实跑 `oamp/test/confirmation-inbox.test.js:496-497`（提交 3 条 question 后 `in` 计数与 `task.request` 计数均不变）、`:499-500`（已裁决项不在表、未裁决 3 条仍在途）。
- **404 同码**：`oamp/src/web.js:1298-1300`；实跑 `oamp/test/confirmation-inbox.test.js:474-476`（已裁决 ⇒ 404 `NOT_FOUND`）。

### 标准 5：派生面一致 —— **pass**

- **路由元数据（summary / params / response）与 `API.md` 同步**：
  - 列表路由 `response` 9 键同序（`oamp/src/web.js:1261`）↔ `oamp/API.md:918,929`（JSON 示例含 `request_kind` / `multiple`）+ `:939,945`（字段表 9 行）。
  - 裁决路由 `summary`（`oamp/src/web.js:1277`）↔ `oamp/API.md:955`（`permission` 类 `option_id` 必填 / `question` 类 `option_ids` 与 `text` 至少一个非空）；顶部路由注释 `oamp/src/web.js:23` 与 `:1277` 表述同源。
  - `params`：`oamp/src/web.js:1279-1282`（`option_id` `required:false` + 分类 desc、新增 `option_ids`（`type:'json'`，与既有 `tasks` / `output_schema` 同编码，见 `:958,960`）、`text`）↔ `oamp/API.md:967-970`（`confirmation_id` 路径参数 + 必填列「按类」/「否」，与 MI-c、Q1 一致；`type` 文档写 `array` 属既有约定，先例 `oamp/API.md:641` 的 `tasks`，非偏差）。
  - `response` `'对象 { confirmation_id, accepted: true }'`（`oamp/src/web.js:1284`）与 `oamp/API.md:974-979` 成功示例一致；错误表 `oamp/API.md:985` 已补 question 类触发条件。
- **`llms.txt` 由生成脚本重生成且与投影逐字节相等**：`renderLlmsTxt(projectRoutes(createApiRoutes({})))`（脚本 `oamp/scripts/gen-llms-txt.mjs` 仅调用该纯函数）在临时副本中执行 `node <tmp>/oamp/scripts/gen-llms-txt.mjs` ⇒ 生成物与仓库 `oamp/llms.txt` **BYTE_IDENTICAL**，`sha256 = 66d29ba3b00a3b5088ea5a5bdb01f9bd963cda97d584e069cbad7fe4a3390170`（两侧同值）；标题 `## 接口（21 条）`（`oamp/llms.txt:11`）与基线（`git show 4101e0b:oamp/llms.txt` 第 11 行）一致 ⇒ 零新增路由。
  - 说明：验证未就地运行写文件的 CLI 包装，改用同一纯函数在 `/tmp` 副本内执行真实脚本（受「唯一写入 = 报告」约束，避免触碰被测产物）；判据等价性来自脚本自身只做 `fs.writeFileSync(OUT, renderLlmsTxt(...))`（`oamp/scripts/gen-llms-txt.mjs:13-14`）。
- **漂移锁未削弱**：实跑 `node --test oamp/test/confirmation-inbox.test.js oamp/test/api-routes.test.js oamp/test/project-workspace.test.js oamp/test/web.test.js` ⇒ `ok`：`漂移锁①`、`漂移锁②：llms.txt 快照逐字节`、`漂移锁③：API.md 路径级双向覆盖`、`派生面 HTTP：/api/docs 的 21 条投影`、`API.md 同步：… §3 标题 21 条`、`F10：登记面（进程内）两条新表项元数据逐字 + 追加末位 + 三条锁的静默真源`（含 `project-workspace.test.js:1283-1305` 的快照逐字节 + 路径级双向覆盖），合计 `# pass 74 / # fail 0`（`/tmp/verify0023-targeted.txt`）。

### 标准 6：F14 与 F15 有依据 —— **pass**

- **F14（服务边界不变）**：`grep -n "listen\|createServer" oamp/src/web.js` ⇒ `1725 / 1729 / 1757`；基线 `git show 4101e0b:oamp/src/web.js` ⇒ `1705 / 1709 / 1737` —— 命中集合同为 3 处 `createServer`×1 + `listen`×1 + 注释 `createServer`×1（仅行号平移，无新增监听/端口/鉴权面）；新增对外接口为零（标准 5 的 `llms.txt` 逐字节 + `## 接口（21 条）` 不变）。
- **F15（无历史台账）**：
  - `oamp/src/inbox.js` 导出面恰 `add/list/take/remove/size`（`:13,22,27,35,40`），`take()` 即删（`:27-32`）；`oamp/src/persist.js` 零改动（`--numstat` 为空）。
  - 无历史/查询/导出入口：`oamp/src/web.js:1260`（`params: []`）、`:1265`（列表 handler 形参仅 `{ res }`，不读 query）⇒ `GET /api/confirmations?confirmation_id=…` 无参数读取路径，已裁决项因 `take()` 即删而不可能出现在返回体；实跑 `oamp/test/confirmation-inbox.test.js:500`（裁决后仅剩未裁决 3 条）。
  - 未裁决项刷新后仍在：列表读取进程内表（`oamp/src/web.js:1266`），既有 T3 用例断言重建面零帧且条目在场（实跑 `ok 11`）。

### 标准 7：目标测试命令全绿 —— **pass**

| 命令 | 结果 |
|---|---|
| `node --test oamp/test/confirmation-inbox.test.js oamp/test/api-routes.test.js oamp/test/project-workspace.test.js oamp/test/web.test.js`（cwd = PR worktree） | 退出码 **0**；`# tests 74 / # pass 74 / # fail 0 / # skipped 0`；新增用例 `ok 12 - T1/T2（0023 pr-002）：question 类入表（9 键深等 / 兜底 / 空选项态）+ 裁决分化（option_ids 回传 / 旁路停掉 / 错误契约）` |
| `node --test oamp/test/*.test.js`（cwd = PR worktree） | 退出码 **0**；`# tests 362 / # pass 362 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0`（无 `not ok` 行） |

（原始输出：`/tmp/verify0023-targeted.txt`、`/tmp/verify0023-full.txt`；重跑时间 2026-09-14，工作区 `git status --porcelain` 为空。）

---

## 汇总

- pass：5 项（标准 3、4、5、6、7）
- fail：0 项
- partial：2 项（标准 1 —— 其子项 #10 的「只含本 PR 列出的文件」；标准 2 —— 其子项「改动面恰为 4 个文件」在分支整体 diff 上不成立）
- blocked：0 项

---

## 偏差记录

| 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|
| D-1：`oamp/API.md:1011`（§4.2 全局事件表 `confirmation` 行）仍列 **7 字段** `{confirmation_id, chat_id, agent_id, tool, title, options, created_at}`，同时断言「`data` 与 §3.20 的列表元素**同形状**」 | §3.20 条目已是 **9 字段**（`oamp/API.md:918-945`），实现发帧亦为 9 字段（`oamp/src/web.js:1628` 直接发 `entry` 本体）。⇒ `API.md` 自相矛盾：同一文件内「同形状」的两处字段面不一致 | 按实现更新文档：下一迭代（或 pr-003 文档面）把该行字段面改为 9 字段。**不影响本次判定**（PR 验收 2「全局帧与列表元素同形状」按实现判定为 pass；`/api/events` 路由元数据不枚举字段 ⇒ 与「API.md ↔ 元数据一致」不冲突） |
| D-2：`oamp/API.md:945` 写「`multiple` … 缺失 / 非布尔 ⇒ `false`，**`permission` 类恒 `false`**」；`architecture.md` §5.2 字段表同列 permission 类 `multiple = false` | `oamp/src/web.js:1625` 为 `multiple: body.multiple === true`，**不按类别钳制** ⇒ 若上游对 `request_kind:'permission'` 的信封投 `multiple:true`，条目将持 `true`（与文档口径相左）。当前生产面不触发（pr-001 分支 `feat/0023-pr-001-approval-resolution-and-question-channel:oamp/src/agent.js:288,294` = `request_kind: isQuestion ? 'question' : 'permission'`、`multiple: isQuestion ? info.multiple === true : false`） | 二者择一（请主 agent 裁定）：① 文档按实现口径改写为「`multiple` = 投递值严格等于 `true`」；② 实现按类钳制（`request_kind === 'question' && body.multiple === true`）。**不影响本次判定**（委托标准 3 的口径即「非 `true` ⇒ `false`」，实现满足） |
| D-3：PR 文件「文件范围」列 4 个文件；`pr-002-tasks.md` §0.1「唯一可写面（`git diff --stat` 超出即 PR 验收 10 不通过）」 | 分支 `4101e0b..00e9b58` 含第 5 个文件 `docs/iterations/0023-…/prs/pr-002-tasks.md`（`+187`，独立提交 `415d82e`，非业务代码） | 主 agent 明确阶段 5 任务图是否计入「改动面」：若计入 ⇒ PR 文件「文件范围」应补列规划产物；若不计入（建议）⇒ 判据表述改为「`oamp/` 改动面恰 4 文件 + 零改动清单逐条不出现」 |

偏差计数：**3 条**（均不阻塞交付，不构成 fail）。

> 报告自检：7 条委托标准（含 PR 10 条验收的子项）逐项有判定与可定位证据；无「感觉 / 基本通过」类模糊判定；未使用执行过程上下文（未读执行者叙述、未向执行者求证）；未修改被测产物（唯一写入 = 本报告）。

---

## 下一迭代候选

1. **`call-protocol.test.js` 的 API.md 绝对行号窗口是脆弱耦合**：`SCOPE` 窗口 `[1025,1050]` ∉ `ALLOWED`（`oamp/test/call-protocol.test.js:1068-1081`），而 `oamp/API.md:988` 的 `best-effort` 行含禁用词 `effort`；本 PR 净增 **+7 行**（`16/9`）恰好把该行留在窗口外。任何一次 ±10 行以上的文档改动都可能让这个零改动文件转红。建议把判据换成语义锚点（按章节标题切片）而非绝对行号。
2. **pr-003 必须同步前端提交体**：`oamp/web/app.js:725` 恒提交 `{option_id, text}`（并被 `oamp/test/inbox-console.test.js:342` 逐字锁定），本 PR 落地后 question 条目从界面提交将命中 `oamp/src/web.js:1312-1315` 的 400 分支 ⇒ 界面上的 question 条目在 pr-003 合入前**不可裁决**（本 PR 已按边界声明不判前端面）。
3. **D-1 的可执行化**：`API.md` §4.2 事件表的 `confirmation` 行字段面同步为 9 字段（与 §3.20 对齐），属零风险文档修正。
4. **测试助手即第二实现**：`oamp/test/confirmation-inbox.test.js:193-201` 的 `entryOf` 复刻了服务端兜底口径（`request_kind` / `multiple`）；当前与 `oamp/src/web.js:1619,1625` 等价，但兜底规则若改动会被静默同步而掩盖回归。建议在断言侧显式标注口径来源，或对兜底规则另设一条直击服务端的断言。

---

## 结论

**PASS**

（无 fail 条目；2 项 partial 均为「分支整体 diff 多一个规划产物文件」这一同一事实的两处投影，`oamp/` 业务面严格为 4 文件、零改动清单逐条满足；3 条偏差已记录并给出建议处理。）
