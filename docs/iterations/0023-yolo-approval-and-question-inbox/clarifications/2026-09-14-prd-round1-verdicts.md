# 0023 prd 第 1 轮 · 用户裁决记录

**日期**: 2026-09-14（主 agent 阻塞式取得）
**转呈来源**: `prd.md` §「MI 判定口径」+ `clarifications/2026-09-14-prd-round1.md`（3 项 `[model_inferred]`）
**性质**: `user_confirmed`

---

## MI-01 · 非法档位取值的处置 —— **拒绝启动并报错**

- 非法 `--approval-mode` / 配置值 ⇒ **拒绝启动并报错**（体例同 0022 的配置校验：非法值响亮失败，不静默回落）。
- 已否决：告警后按默认 yolo 启动；静默用内置默认。

## MI-02 · `deny` 档的拒绝观测面 —— **acp 看审计行 + rpc 看轮次中止**

- **acp 链路**：观测 `TOOL_DENIED` 审计行（该链路本就有审批审计面）。
- **rpc 链路**：观测「该轮以 `permission_denied` 中止 **且** 收件箱零新增条目」——rpc 链路本就零审批审计行（K6，存量状态），故不新增审计面（不违 N4）。
- 已否决：要求 rpc 也产出拒绝痕迹（撞 N4）；只在 acp 链路验收（rpc 的 deny 能力将无验收面）。

## MI-03 · 提问类作答的必填性 —— **至少填其一**

- 选项与自由文本**至少一个非空**；纯自由文本提问下**文本必填**；空提交不被当成"作答"（条目保持挂起）。

---

## prd 第 2 轮的处理清单

1. 回收 3 项 `[model_inferred]` → `[user_confirmed]`，按上述裁决改写对应验收表述（F02 验收 4 / F03 验收 3 / F05 验收 5 + F07 验收 1）。
2. 保持 `[架构待填]` T-01~T-08 留白不变；不新增功能点、不改卡片编号与文件范围集合。
3. 收敛后 `prd.md` 版本升至 v0.2.0，`model_inferred` 归零，满足阶段 2 三项推进条件。
