# 0023 阶段 2（功能规格）· 第 2 轮记录

**日期**: 2026-09-14
**阶段**: 2（功能规格）
**角色**: `prd`（功能规格拆解者）
**性质**: **收敛轮** —— 回收 3 项 `[model_inferred]` ⇒ `prd.md` 收敛为 **v0.2.0**（`model_inferred` 归零）
**本轮产出**: `prd.md`（**v0.2.0**）+ 受影响卡 4 张（`F02` / `F03` / `F05` / `F07`）+ 本文件
**本轮主输入**: `clarifications/2026-09-14-prd-round1-verdicts.md`（**用户裁决原件**，MI-01~MI-03）
**只读输入**: `prd.md`（v0.1.0 全文）+ `prd/F01~F16*.md`（16 张全文）+ `demand.md`（v1.0.0，**未修改**）+ `clarifications/2026-09-14-prd-round1.md`（第 1 轮转呈件）
**未读取**: 产品代码 / `oamp/**` / omp 安装包（本轮为**判定口径回收**，无事实缺口，不需要重跑侦察）

---

## 一、回收对照（3 项 · 逐项照录裁决口径 + 落卡）

**总原则**：裁决口径**原文照录**（不改写、不加严、不放宽），落点为第 1 轮已登记的对应验收项；**未新增验收条目、未新增卡、未改编号**。

### MI-01 · 非法档位取值的处置 → **F02 验收 4**

- **裁决口径（原文）**：非法 `--approval-mode` / 配置值 ⇒ **拒绝启动并报错**（体例同 0022 的配置校验：非法值响亮失败，不静默回落）。**已否决**：告警后按默认 `yolo` 启动；静默用内置默认。
- **改写前（v0.1.0）**：`[model_inferred MI-01]` 未生效——只给首轮推导值 + 三种备选读法 + "口径待裁决后落字"。
- **改写后（v0.2.0）**：`[user_confirmed]`，验收 4 定为**拒绝启动并报错**；判定改写为可裸判定（见 §二）。
- **交叉确认（本轮处理清单 1）**：**F01 / F02 的「默认 `yolo`」不受影响**——未配置档位 ⇒ 档位 = `yolo`（合法值、零门档），"非法值拒绝启动"只作用于**非法取值**；F01 验收 1~3 与 F02 验收 1~3 的语义**未改一字**。该确认已写入 F02「越界自查」。

### MI-02 · `deny` 档的拒绝观测面 → **F03 验收 3**

- **裁决口径（原文）**：**acp 链路**观 `TOOL_DENIED` 审计行（该链路本就有审批审计面）；**rpc 链路**观「该轮以 `permission_denied` 中止 **且** 收件箱零新增条目」——rpc 本就零审批审计行（K6，存量状态），**不因此新增审计面**（不违 N4）。**已否决**：要求 rpc 也产出拒绝痕迹（撞 N4）；只在 acp 链路验收。
- **改写前（v0.1.0）**：`[model_inferred MI-02]` 未生效——只写"观测链路 / 观测面待主 agent 转呈用户"。
- **改写后（v0.2.0）**：`[user_confirmed]`，**两条链路的判定方式各自写成可裸判定**（见 §二）；E2 的"agent 侧有拒绝痕迹（`TOOL_DENIED`）"半句据此**按链路分工**落字。
- **N4 一致性**：rpc 侧只用「轮次以 `permission_denied` 中止 + 收件箱零新增条目」判定，**未为 rpc 补任何痕迹载体**；F03 边界行同步补注（"不为它补任何痕迹"）。**未新增审计面**。

### MI-03 · 提问类作答的必填性 → **F05 验收 5 + F07 验收 1**

- **裁决口径（原文）**：选项与自由文本**至少一个非空**；**纯自由文本提问下文本必填**；**空提交不被当成"作答"**（条目保持挂起）。**已否决**：带选项提问必须选一个选项（文本仅附加）；两者皆可空。
- **改写前（v0.1.0）**：`[model_inferred MI-03]` 未生效——F05 验收 5 只给首轮推导值 + 三种备选读法；F07 验收 1 无必填性口径。
- **改写后（v0.2.0）**：F05 验收 5 定为**至少填其一 + 纯自由文本必填 + 空提交不作答**，判定写为三种输入的裸判定；F07 验收 1 补入**作答成立条件**（空提交不解锁回传）并把"空提交 ⇒ 该轮仍等待、条目仍在栏内"写进判定（复用 F08 验收 1 的既有挂起语义，**未派生新功能点**）。

**回收完备性**：第 1 轮 3 项 `[model_inferred]`（MI-01~MI-03）**全部闭环**；`prd.md` 与 16 张卡的 `model_inferred` 计数 = **0**。本轮**未产生新的推断结论**。

---

## 二、卡件改写清单（本轮写入的 4 张卡 + `prd.md`）

| 文件 | 改写位置 | 改写性质 |
|---|---|---|
| `prd.md` | 版本 / 状态行；「澄清依据」新增本轮两件；「MI 判定口径」整节改写；「本阶段验证记录」标题 + V7 修订 + 新增 V8 / V9 + 声明；「疑问与越界」4、6；「未越界声明」 | 版本升 v0.2.0；MI 段由"未生效转呈"改为"已回收 + 口径原文照录" |
| `prd/F02-approval-mode-config.md` | 验收 4 + 越界自查第 3 条 | `[model_inferred MI-01]` → `[user_confirmed]`；判定改为裸判定；确认默认 `yolo` 不受影响 |
| `prd/F03-deny-forces-always-ask.md` | 验收 3 + 边界（N4 行）+ 越界自查第 2、3 条 | `[model_inferred MI-02]` → `[user_confirmed]`；两条链路判定各自裸判定；补 N4 一致性注 |
| `prd/F05-question-shape.md` | 验收 5 + 越界自查第 3 条 | `[model_inferred MI-03]` → `[user_confirmed]`；三种输入的裸判定 |
| `prd/F07-answer-roundtrip.md` | 验收 1（含判定）+ 越界自查新增 1 条 | 补入作答成立条件与空提交判定；口径来源标注 |

### 三条回收后的判定（**可裸判定自检**：每条都写成"谁、在什么条件下、看到什么"）

1. **F02 验收 4**：填入一个非法取值（参数面 / 配置面各一次）→ 该实例**起不来**且报错信息点名该非法取值；**不出现**"启动成功且档位落到 `yolo`"或"启动成功且无任何提示"（出现即失败）。
2. **F03 验收 3**：同一动作在**两条链路各跑一次** → ① `acp` 链路：审批审计面出现 `TOOL_DENIED` 审计行；② `rpc` 链路：该轮以 `permission_denied` 中止 **且** 收件箱零新增条目；两条链路都不得出现新增的拒绝痕迹载体（出现即失败）。
3. **F05 验收 5 / F07 验收 1**：三种输入试提交 → "只填文本"可提交、"只选选项"可提交、"两者皆空"不构成作答（**零回传 + 条目仍在栏内 + 轮次仍等待**）；纯自由文本提问下空文本同判。

---

## 三、本轮自检（**阶段 2 推进条件 3 项 + 可裸判定 + 无架构决策泄漏**）

**判据**：逐项核对 `prd.md` v0.2.0 与 `prd/F01~F16*.md`（16 张）的实际文本（非采信第 1 轮报告）。

| # | 检查项 | 结果 | 依据 |
|---|---|---|---|
| S1 | **所有功能点有独立卡片** | ✅ | 索引 16 行 ↔ `prd/` 下 16 个文件**一一对应**；本轮**未新增 / 删除卡片**，F01~F16 编号与文件名**未改**，文件范围集合**不变**（11 个需求功能点 + 5 个保证项） |
| S2 | **无 `demand.md` 之外新增功能点** | ✅ | 六张追溯表（U / W / N / M / E / R）逐条有落点、**无未承载项**；本轮**只改判定口径**，未新增验收条目 / 条款 / 卡；F12~F16 仍只承载 N3 / N4 / N8 / N9 / N10 |
| S3 | **架构待定项已标注 `[架构待填]`** | ✅ | T-01~T-08 在索引与各卡「架构待填（阶段 3）」段**全部保持留白、未回填**（逐项核查见 §四）；`[model_inferred]` 与 `[架构待填]` **未混用** |
| S4 | **三条回收判定可裸判定** | ✅ | 见 §二末三条：均为"谁、在什么条件下、看到什么"，且给出**失败面**（"出现即失败" / "零回传" / "仍在栏内"） |
| S5 | **无架构决策泄漏** | ✅ | 本轮改写的四处文本**零**技术选型 / 数据模型 / 接口设计 / 模块与字段名；出现的标识均为 `demand.md` 既有的产品对象与裸判据原文（`TOOL_DENIED` / `permission_denied` / 收件箱 / 审计行 / `--approval-mode`），措辞例外范围见 `prd.md` 顶部「术语口径」 |
| S6 | **`model_inferred` 归零** | ✅ | MI-01~MI-03 全部转 `[user_confirmed]`，卡内标记已改写；全文件残留的 `model_inferred` 字样均为**回收说明或归零声明**（无生效待确认项） |
| S7 | **零自问自答 / 无新缺口** | ✅ | 本轮**未跑出新缺口**：3 项回收后卡内判定全部可裸判定、无新多解项；故**无新增 `[model_inferred]`、待转呈清单为空**（若存在新缺口，本 agent 只能列出待转呈、不得自答） |
| S8 | **派发约束遵守** | ✅ | 只写 `prd.md` + 4 张受影响卡 + 本文件；**未改 `demand.md`**；**未触碰** `status.md` / `history.md` / `clarifications/` 下已有文件；**未执行** git 写操作；**未跑**测试 / 构建 / 格式化（一切写入以工作区地址为根、以绝对路径） |

**结论**：**S1 / S2 / S3 三项推进条件全部满足 ✅**，且 `model_inferred` **归零**、判定**可裸判定**、**无架构决策泄漏** ⇒ `prd.md` **v0.2.0 为本轮收敛产物**，阶段 2 可据此判定通过。

---

## 四、`[架构待填]` 留白状态核查（**逐项未回填**）

| 编号 | 待填内容（摘要） | 本轮状态 |
|---|---|---|
| T-01 | 档位解析的归属与"唯一汇聚点"落点（`deny ⇒ always-ask` 由谁算、算在哪一处） | 留白（F01 / F02 / F03 三卡的 T 表未动） |
| T-02 | 信封与提问形状的**最终字段名**（四项可表达性的字段承载） | 留白（F04 / F05 / F10） |
| T-03 | `acp` 多问题的**组内暂存形态**（逐问登记、齐答后一次性回包的最小状态） | 留白（F06） |
| T-04 | 裁决回传载荷的**字段名**（选项 + 自由文本如何随载荷到达提问方） | 留白（F07） |
| T-05 | 宿主工具的**注册时机 / 命名 / 参数 schema** + `acp` 表单到信封的映射细节 | 留白（F09 / F05 / F07） |
| T-06 | 能力位的**最终集合与签名** | 留白（F11） |
| T-07 | 档位配置面的**落点与键名形态**（派生留白） | 留白（F01 / F02 / F03） |
| T-08 | 提问类挂起期的**计时 / 超时归属**（派生留白） | 留白（F08） |

**声明**：8 项**均未在本轮回填**，也未据此新增验收条目；MI 回收**未产生**任何新的形态待定项。**移交阶段 3 的承接项**（`acp` 多问题表单端到端帧面端到端实测 1 项 + 派生 1 项）**原样保持**，未增未减。

---

## 五、疑问与越界（交主 agent）

1. **本轮无需用户裁决的事项**：3 项 MI 已闭环、无新缺口，**待转呈清单为空**。
2. **供主 agent 复核的两点（非阻塞）**：
   - **F07 验收 1 的"作答成立条件"**（空提交不作答）与 **F08 验收 1~3 的挂起语义**是同一行为的两个面——本轮按"F05/F07 钉必填性、F08 钉搁置状态"切分，**未新增卡、未改编号**（体例与第 1 轮 W3 的四卡切分一致）；
   - **F03 验收 3 的 rpc 观测面**只用"轮次以 `permission_denied` 中止 + 收件箱零新增条目"判定，**未为 rpc 补痕迹**——这是 MI-02 裁决与 N4（F13）的一致落点，阶段 6 核基准时请按此口径比对 E2。
3. **`demand.md` 无需变更**：本轮裁决均为**判定口径**层面（验收标准怎么判），不触及需求合同的 W / N / M / E / R 任何条款；**本阶段未发现 `demand.md` 内部矛盾**，故**未写 `deferred-demand-changes.md`**。
4. **未越界**：本轮唯一写入为 `prd.md`、`prd/F02,F03,F05,F07*.md`、本文件——均为本迭代工作区内、以**绝对路径**写入；`demand.md` / `status.md` / `history.md` / `clarifications/` 下已有文件（含 `2026-09-14-prd-round1-verdicts.md`）**只读**；`oamp/**`、`roles/**`、0021 / 0022 / 0020 迭代目录**只读**；**未跑**测试 / 构建 / 格式化 / 服务；**未执行**任何 git 写命令。
