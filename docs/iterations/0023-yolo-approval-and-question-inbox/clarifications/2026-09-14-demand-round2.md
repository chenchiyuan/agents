# 0023 demand 第 2 轮收敛记录（裁决回收 + 闭合 + 最终六维诊断）

**迭代**: 0023-yolo-approval-and-question-inbox
**阶段**: 1（需求收敛）· **轮次**: 第 2 轮
**产出者**: demand
**日期**: 2026-09-14
**对应合同**: `demand.md`（**v1.0.0 收敛版**；前一版 v0.1.0 = 第 1 轮草稿）
**输入**: `clarifications/2026-09-14-kickoff-discussion.md`（起点材料）、`clarifications/2026-09-14-demand-round1.md`（第 1 轮产出）、**`clarifications/2026-09-14-demand-round1-verdicts.md`（用户裁决，本轮主输入）**、`demand.md`（v0.1.0）、`clarifications/probes/probe-r1-ask-tool.mjs` + `probe-r1b-host-tool-ask.mjs` + `r1b-host-tool-ask-output.txt`（主 agent 本会话两条探针证据）

---

## 0. 本轮性质与硬性限制

- 本 agent 运行在**无实时对话通道**的子 agent 中，**无法直接与用户对话**；本轮**只做回收与闭合**，不产生新问点。
- 用户裁决（A1~A6）与本轮引用的用户原话 / 开工前裁决（Q1~Q3）是 `user_confirmed`：**不重问、不推翻、不把被改写的旧措辞当结论**。
- 本轮对最终草稿重跑六维诊断（§3）：**未发现**未被 A1~A6 覆盖的新缺口 ⇒ **转呈件清单为空**（§6）。
- 本轮**未新增功能点**；`[架构待填]` 项（解析归属、提问形状的最终字段名、host tool 的注册时机与命名等）**保持留白**交阶段 3。
- 写入范围：仅 `demand.md` 与本文件（以工作区地址为根、绝对路径寻址）；不写 `data/`、不改 `status.md` / `history.md` / `oamp/**`、不做 git 写操作、不跑测试 / 构建 / 格式化。

---

## 1. 裁决 → 合同的逐项回收（对照第 1 轮转呈件 15 行）

> 裁决原件：`clarifications/2026-09-14-demand-round1-verdicts.md`。裁决原件的小节标题为 `P1` / `P2` / `P3（提问形状与作答）` / `P4（组）（阻塞与通知）` / `P5（组）（审计与口径）` / `N 系边界`；本文件统一编号 **A1~A6**（体例同 0022 的 J / A 两批）并逐行注明对应的原始提案号。

| 转呈件（第 1 轮 §7） | 裁决 | 本轮落笔（`demand.md` v1.0.0） |
|---|---|---|
| 1 · **P1** 默认链路是否改为具备 UI 的 RPC 会话 | **A1**：**默认链路保持 `--mode rpc`**；hub 经 `set_host_tools` 注册宿主工具（host tool）承载提问；acp 用原生 `ask` → `elicitation/create`；两者同收件箱、同 `kind:'question'` 信封。已否决 `rpc-ui` 与"仅 acp 可提问" | **W5 重写**（默认链路具备提问能力，且**不改会话形态**）；**M7**（提问通道按协议能力位表达 + host tool 形态 `[架构待填]`）；**W3/M3** 的提问通路；**E3** 改为"两条链路各一次"；**R2** 改为"提问承载的定义权移到 hub 侧"；**登记⑨** + §2（含已否决记录） |
| 2 · **P2** 档位取值域 / `tier` 命名 | **A2**：只落 `{always-ask, yolo}`（默认 `yolo`）；`tier` 不入域（omp 侧叫 `write`）；已否决三档 | **W1 / M1 / M2 / E8** 按两值落定；**N1** 增补"`tier` 不入取值域、本迭代不落该档"；登记④ |
| 3 · **P5** 多问题表单的栏内形态 | **A3/P5**：**一问一条**（复用现有裁决面，**不引入 `questions[]` 大信封**） | **M4**（一问一条 + acp 逐问登记、齐答后回包 + 组内暂存 `[架构待填]`）；新增 **E9**（可裸判定）；登记⑧ ③ 条件化行 |
| 4 · **P6** 回答是否必须回传自由文本 | **A3/P6**：**必须回传**，并**停掉「文本 → 当成一条 chat 输入」旁路** | **W4**（作答含自由文本回传 + 停旁路）+ **M5**（回传答案本体；载荷字段名 `[架构待填]`）+ **E4**（判定含"对话记录零新增用户消息"）；登记⑧ ④ 改写行 |
| 5 · **P4** 是否支持"无选项的纯自由文本提问" | **A3/P4**：**支持** | **M3**（提问类必须能表达：问题文本 / 选项集合含多选 / 是否允许自由文本 / **允许无选项纯自由文本**）+ **E3** 的判定面；登记⑧ ③ 条件化行 |
| 6 · **P7** 提问请求是否允许被代答拒绝 | **A3/P7**：**不允许**（一律登记待答；未作答则一直等） | **W3**（不代答拒绝、不自动作答）+ **M6** + 新增边界 **N12** + **E5** 的判定面（该轮不以 cancelled 收尾） |
| 7 · **P12** deny 与显式 `--approval-mode` 的优先级 | **A5/P12**：**deny 优先** | **M1**（"与显式档位冲突时以 deny 为准"）+ **W2**（"该约束优先于显式 `--approval-mode`"）+ **E8** 的判定面 |
| 8 · **P3** deny ⇒ always-ask 的解析归属 | **A5/P3**：**需求只钉行为 + 「唯一汇聚点」硬约束**，解析归属留 `[架构待填]`；备选（明确指定 L1 / L2）**未被采纳为需求级指定** | **M1**（唯一汇聚点硬约束 + 落点 `[架构待填]`）；§2 已否决记录第 15 条 |
| 9 · **P9** 是否补偿 yolo 后消失的审批审计行 | **A5/P9**：**不补偿**（登记为已知代价；`TOOL_CALL` 保留）——用户显式接受该代价 | **N4**（含"用户显式接受"）+ **R1**（已 知 代 价）+ 登记②（范围收窄）+ 登记⑧ ⑤ 适用范围收窄行 |
| 10 · **P10** 能力位 `approvalGate` 是否随档位变 | **A4/P10**：**不随档位变**（档位由 profile 声明表达） | **M8**（能力位描述机制、档位声明表达生效形态；最终集合与签名 `[架构待填]`） |
| 11 · **P11** 是否新增提问类通知事件 | **A4/P11**：**不新增**（沿用 `confirmation_required`） | **N6** 原文保留并转为 `user_confirmed`（来源改标 A4/P11） |
| 12 · **P13** 提问类是否同样"阻塞无上限、无自动裁决" | **A4/P13**：**同样适用** | **M6**（提问类同样阻塞无上限、不超时、不自动选）+ **N2**（扩写为"提问类同样"）+ **E5** |
| 13 · **P8** 0021/0022 口径更替清单是否按提案登记 | **A5/P8**：**按提案登记**（五类处置） | **登记⑧ 明细**（五类处置，逐条列出条款号；标题由第 1 轮的"四类"更正为**五类**） |
| 14 · **Q3′** `always-ask` 档"必须验收"还是"仅保留路径" | **A5/Q3′**：**必须验收**（deny 能力的唯一载体） | **W6**（"必须验收"）+ **E6**（"必须实际跑通，不是仅保留代码路径"）+ 登记⑦ |
| 15 · **N 系边界**（N1~N11） | **A6**：**全部保留** | N1~N11 全部由 `[model_inferred]` 转为 `user_confirmed`，**编号与内容不变**，每条保留「为什么这次不做」；A3/P7 另产生 **N12**（登记⑩） |

### 1.1 派发处理清单（8 项）→ 落笔对照

| 清单项 | 落笔 |
|---|---|
| 1) P1 双链路承载 + 两条探针证据登记为 `实测` | W5 / M7 / E3；**K11（r1 否定面）+ K12（r1b 肯定面）** 写入第一段 §4.3，并在 K1 行注明"结论由 K11 + K12 双侧支撑"；§4.4 后果第 1、2 条 |
| 2) P2 取值域只落两值；`tier` 不入域 | W1 / M1 / M2 / N1 / E8；登记④「`tier` 不在域内、omp 侧叫 `write`、本迭代不落该档」 |
| 3) P4/P5/P6/P7 四项 | M3（纯自由文本）/ M4 + E9（一问一条）/ W4 + M5 + E4（回传 + 停旁路）/ W3 + M6 + N12 + E5（不代答不兜底） |
| 4) P13/P11/P10 三项 | M6 + N2 + E5（提问类同样阻塞）/ N6（不新增事件类型）/ M8（能力位不随档位变） |
| 5) P9/P8/Q3′/P12/P3 五项 | N4 + R1 + 登记②（不补偿）/ 登记⑧ 五类处置（逐条条款号）/ W6 + E6（必须验收）/ M1 + W2 + E8（deny 优先）/ M1 + `[架构待填]`（解析归属留白） |
| 6) N1~N11 转 `user_confirmed` 并保留理由 | 第二段 §3 全 11 条（含"为什么这次不做"列） |
| 7) 对最终草稿再跑六维诊断；如有新缺口列入转呈件 | §3（六维全部"无新缺口"）⇒ §6 **转呈件为空** |
| 8) 保持 `[架构待填]` 留白 | 第一段 §7 统计（6 处）+ 第二段 §4 各条内联标注；不在本阶段拍板 |

**回收完备性**：第 1 轮 **13 项 `[model_inferred]`（P1~P13）+ 1 项 Q3′ + 11 条 AI 推导边界（N1~N11）= 全部闭环**；`model_inferred` 归零。另有 **2 项待实测**（1 项解除 / 1 项保留）与 **5 处 `[架构待填]`**（1 处解除 / 4 处保留 / 1 处新增 ⇒ 本版共 6 处），见 §7。

---

## 2. A1 定稿：默认链路提问承载（本轮核心改写）

**用户裁决（A1，裁决原件 §P1）**：

- **默认链路保持 `--mode rpc`**；hub 经 RPC 的 **`set_host_tools`** 为每个会话注册一个提问工具（如 `ask_user`），agent 主动调用 ⇒ `host_tool_call` 帧 ⇒ hub 上浮到收件箱 ⇒ 用户作答 ⇒ `host_tool_result` 回传 ⇒ 该轮继续。
- **acp 链路**的提问承载 = omp **原生 `ask` → `elicitation/create`**（demand 的 K3 事实；oamp 今天只认 `Approve|Deny` ⇒ 需扩展）。
- **两者都必须落进同一收件箱与同一 `kind:'question'` 信封形状**。

**写入 `demand.md` 的形态（W5 / M7）**：

| 项 | 内容 | 来源 |
|---|---|---|
| 默认链路形态 | **不改**（`--mode rpc` 保持） | `user_confirmed`（A1） |
| 默认链路提问承载 | hub 经 `set_host_tools` 注册的**宿主工具（host tool）** | `user_confirmed`（A1）；可行性 = `实测` K12 |
| acp 链路提问承载 | omp **原生 `ask` → `elicitation/create`** | 通路形态 = `实测` K3（主 agent 补充口径，**非用户决策点**）；"同一收件箱、同一 `kind` 信封、不要求逐字等价" = `user_confirmed`（A1） |
| 收件箱 / 信封 | 两条链路**同一收件箱、同一 `kind:'question'` 信封**；**不要求跨协议逐字等价**（体例沿 0022 A3/P5） | `user_confirmed`（A1） |
| 留白项 | host tool 的**注册时机 / 命名 / 参数 schema** 与 acp 表单到信封的映射细节 | `[架构待填]`（本阶段不拍板） |

**未采纳口径的显式记录（防误引）** —— 以下读法**已由用户否决或未被采纳为需求级结论**，不得作为本迭代的启动依据：

| # | 未采纳读法 | 否决来源 |
|---|---|---|
| 1 | 改用 **`--mode rpc-ui`**（改所有实例启动模式） | A1（否决；理由含 `PI_NO_PTY` 副作用未实测） |
| 2 | 提问**仅在 acp 链路可用**（默认链路无该能力） | A1（否决；与用户"默认链路也要能提问"的要求不符） |
| 3 | 默认协议**改为 `acp`** | 第 1 轮 P1 备选②；A1 亦未采纳（与 0022 A2 的"内置默认 `rpc`"相撞） |
| 4 | 档位**三档全落**（含 `write`）／用 `tier` 作为取值名 | A2（否决） |
| 5 | 多问题**一条条目承载整表单**（信封加 `questions[]`、裁决面加 `answers[]`） | A3/P5（否决；选"一问一条"） |
| 6 | 提问类**只回选项**（自由文本仅作附加说明；RPC 的 `editor` 帧按取消处理） | A3/P6（否决） |
| 7 | 保留"**无人在场时可拒绝**"的兜底 | A3/P7（否决） |
| 8 | 提问类**带超时**（超时后按 `recommended` 自动选） | A4/P13（否决） |
| 9 | 新增 **`question_required`** 通知事件类型 | A4/P11（否决） |
| 10 | 能力位**随生效档位求值**（`yolo` ⇒ `approvalGate=false`） | A4/P10（否决） |
| 11 | **补偿审计面**（把 RPC 的 `tool_execution_*` 映射为 `TOOL_CALL` 行） | A5/P9（否决） |
| 12 | **不登记**口径更替清单、交阶段 2 自行发现冲突 | A5/P8（否决） |
| 13 | `always-ask` 档**仅保留代码路径**、本迭代不专门验收 | A5/Q3′（否决） |
| 14 | 显式 `--approval-mode` **优先于** deny（deny 在 yolo 下退化为"无门可拒"） | A5/P12（否决） |
| 15 | 在需求层**明确指定解析落点**（L1 解析函数 / L2 起会话前统一算） | A5/P3（未被采纳为需求级指定；只钉行为 + 唯一汇聚点） |

**顺带闭合的一致性问题（本轮新发现并当场闭合，共 3 处）**：

1. **提问上浮 × 档位互不影响**：提问类上浮**不随档位变化**（档位只决定工具门通路；写作 W3/M7）——否则"always-ask 档下 agent 不能提问"会成为一处隐性缺口。依据：Q2 的触发面未以档位为条件 + A2 的取值域只管工具门档位。
2. **host tool 通路 × N3（不改 omp）**：`set_host_tools` / `host_tool_call` / `host_tool_result` 是 omp **既有** RPC 能力（K12 实测完整跑通）⇒ A1 的选择**不要求改上游**，N3 成立。
3. **A1 的 host tool × 0022 M6 能力位候选含 `hostTools`**：一致——该位在上一迭代已预留，本轮只是让它有了真实来源；最终集合与签名仍 `[架构待填]`（M8）。

---

## 3. 最终六维诊断（对 v1.0.0 草稿再跑一遍）

| 维度 | 结论 | 依据 / 本轮闭合点 |
|---|---|---|
| 1 · 明确性 | **无新缺口** | 第 1 轮 2 处缺口闭合：① 档位解析归属 → 钉行为 + 唯一汇聚点，落点 `[架构待填]`（A5/P3），优先级已裁（A5/P12）；② "agent 主动提问"的载体宽窄两读 → A1 定为两条协议通路 + 同一收件箱同一信封。提问形状 4 项读法由 A3 一次裁清。残留未定项均为**形态性**（解析落点、字段名、host tool 注册时机与命名、组内暂存形态），不构成需求歧义 |
| 2 · 逻辑闭环 | **无新缺口** | why → what 逐条可反推（V1→W1/W2；V2→W3/W4/W5；V3→W2/W6；V4→W3/N 系），无多余项；**第 1 轮唯一脱节点闭合**：默认链路提问不可达（K1）由 A1 的 host tool 通路解决，且有运行期实证（K12：`host_tool_call` → `host_tool_result` → `agent_end{isTerminal:true}`）。方案雏形可达效果：用户给的 3 条雏形与 A1 的通路选择互不冲突，共同覆盖 E1~E9 |
| 3 · 边界完整性 | **无新缺口** | N1~N11 用户逐条保留（A6）+ N12（A3/P7 直接产生，登记⑩）⇒ **AI 推导且未获确认的边界 = 0**；每条保留「为什么这次不做」 |
| 4 · 内部一致性 | **无新缺口**（本轮新闭合 3 处 + 复核 4 处） | 新闭合：① 提问上浮 × 档位（互不影响，见 §2 末）；② host tool × N3（无需改上游）；③ A1 × 0022 M6 的 `hostTools` 位（一致，已预留）。复核：④ 停掉文本旁路 × 0021 `F03` 既有口径 = **有序替代**（登记⑧ ④，仅 `question` 类改口径，`permission` 类不变，N11 不受影响）；⑤ 一问一条 × 现裁决面（复用 `option_id` + `text`，不引入大信封）；⑥ 不补偿审计行 × Q3′ always-ask 必须验收（验收面 = `permission` 类条目上浮 + 裁决，不依赖审批审计行）；⑦ 取值域两档 × 起点材料 §四.5 的 `tier` 之问（已裁） |
| 5 · 可验证性 | **无新缺口** | E1~E9 全部过裸判定；第 1 轮两处"尚不可判"回收：**E4** 的判定面 = "提问方收到的文本与输入一致 **且** 对话记录零新增用户消息"；**默认链路可提问**有真实样本（K12）。新增 **E9** 使 A3/P5 亦可裸判定 |
| 6 · 方案雏形 | **无新缺口** | 第 1 轮 4 组未问条目（默认链路形态 / 多问题形态 / 审计面 / 纯自由文本）+ Q3′ + 回传形态**均由用户在 A1~A5 中明确取舍**（登记⑥）⇒ 该维不再跑出问点；第 2 轮的口径变化**全部由用户裁决给出**，无 AI 单方面补写。用户未给机制的形态项一律 `[架构待填]` |

**停止条件达成**：连续一轮六维诊断跑不出新缺口，且无活跃冲突（登记①~⑩ 均为派生修订、事实澄清或裁决落地）⇒ 收敛完成，`demand.md` v1.0.0 为可交付版本。

---

## 4. 第 2 轮新增登记（相对第 1 轮登记①~⑧）

| # | 登记内容 | 依据 |
|---|---|---|
| ⑨ | **提问承载的链路分化登记（A1）**：默认 rpc = hub 经 `set_host_tools` 注册的宿主工具；acp = 原生 `ask` → `elicitation/create`；两者同收件箱、同 `kind:'question'` 信封，不要求逐字等价。**同时作废两条旧读法**（`--mode rpc-ui`、"提问仅 acp 链路可用"），连带**解除**第 1 轮 P1 的附带登记项「`rpc-ui` 的 `PI_NO_PTY=1` 副作用」（不再成为阶段 3 首步实测项）与 `status.md` 记的阶段 3 首步实测项"`ask` 在 RPC 是否可用"（后者已由 K11/K12 闭合） | A1 + K2/K11/K12 + 登记③ |
| ⑩ | **边界编号顺延登记**：A3/P7 直接产生 **N12**（不做提问类的"无人时自动拒绝 / 自动作答"兜底）；**N1~N11 原编号与内容不变**，仅来源标注转 `user_confirmed`（A6） | A3/P7 + A6 |

> 第 1 轮登记①~⑧ 在本版中的状态：① 补写为"已最终闭合"（K11/K12 + A1）；② 依 A5/P9 冻结为"不补偿"；③ 依 A1 收窄待实测项为 1 项（登记⑨）；④ ~ ⑦ 原样保留（④ 依 A2 落定"不入域"；⑦ 依 A5/Q3′ 落定"必须验收"）；⑧ 依 A5/P8 冻结（标题更正为**五类**处置）。

---

## 5. 参考视角处置（不进入结论段）

第 1 轮 §8 的 3 条参考视角**不写入** `demand.md` 任何一段（角色文件：参考视角只用于讨论、不进入结论）。本轮处置：

| 第 1 轮参考视角 | 本轮状态 |
|---|---|
| ① 若选 `rpc-ui`，它不是"另一个协议"而是同一协议的 UI 变体；能力位里可能需要 `hostUI` 之类 | **入口已被 A1 否决**（不改会话形态）；其实质（用**协议能力位**表达承载差异）由 **M7** 以需求级要求承接，且 0022 `M6` 的既有候选位 **`hostTools`** 恰是该位的落点（最终集合与签名 `[架构待填]`） |
| ② "一问一条 + 齐答后回包"在 acp 侧需要一个组内暂存状态；若一次 4~5 问，栏内会一次涌出多条 | **已被 A3/P5 吸收**为需求口径（M4：逐问登记 + 齐答后回包 + 组内暂存 `[架构待填]`）；体感取舍由用户裁决完成（选一问一条） |
| ③ `ask` 的 `ToolAbortError` 语义 ⇒ "取消提问 = 中止轮次"；将来若要"忽略提问并继续"需上游把"取消"与"拒绝回答"分开 | 仅讨论留档；A3/P7 已排除"代答拒绝"兜底，该后续方向**不进本迭代边界**（登记的后续输入） |

---

## 6. 剩余转呈件清单

**空。** 本轮未发现未被 A1~A6 覆盖的新缺口：六维诊断（§3）全部判为"无新缺口"；`model_inferred` 已归零；无活跃冲突。

---

## 7. 来源标注统计 / 未生效项账目（本轮记录）

| 类别 | 数量 | 说明 |
|---|---|---|
| `user_confirmed`（本轮消费） | 第 2 批 **6 组（A1~A6）**，覆盖原 15 个问点（P1~P13 + Q3′ + N1~N11） | 见 §1 逐行对照 |
| 回收的第 1 轮未生效项 | `[model_inferred]` **13 项**（P1~P13）+ Q3′ + AI 推导边界 **11 条**（N1~N11） | 全部闭环；`model_inferred` 归零 |
| 本轮新增 `实测` | **2 条**（K11 否定面 / K12 肯定面，均有脚本 + 输出存档、可复跑） | 双侧支撑 K1 的结论（§2） |
| 本轮新产出 | **N12**（边界）、**E9**（效果）、登记 **⑨⑩**、`M4/M5/M6/M7` 的形态落定 | 均由 A1 / A3 / A4 直接推导；**未新增功能点** |
| 参考视角新增 | 0 | §5 |
| 转呈件 | 0 | §6 |
| **待实测（交阶段 3 首步）** | 第 1 轮 2 项 → **本版 1 项** | 解除：`rpc-ui` 的 `PI_NO_PTY` 副作用（A1 否决该路径）；保留：ACP 多问题表单的端到端帧面（一问一条 × 齐答后回包） |
| **`[架构待填]`** | 第 1 轮 5 处 → **本版 6 处** | 保留：档位解析归属（M1）、acp 多问组内暂存形态（M4）、裁决回传载荷字段名（M5）；由 1 处拆为 2 处：信封形状字段名（M3）+ 载荷字段名（M5）；**新增**：host tool 的注册时机 / 命名 / 参数 schema 与 acp 表单到信封的映射（M7）；**沿用**：能力位最终集合与签名（M8，承 0022 M6）。**解除**：`rpc-ui` 副作用 |

---

## 8. 未闭合声明

- `demand.md` v1.0.0：**两段完整 ✅ / `model_inferred` 归零 ✅（全部经用户裁决确认）/ 无活跃冲突 ✅ / 转呈件为空 ✅** ⇒ 阶段 1 三项推进条件全部满足，可进入阶段 2（功能规格）。
- 本轮**只写** `demand.md` 与本文件，未触碰其他任何文件；未执行 git 写操作；未跑测试 / 构建 / 格式化。
- `[架构待填]` 6 处（§7）为**需求已定「必须有什么」、形态留阶段 3**，**不是未闭合项**。
- **交主 agent 的一条观察**（属主 agent 的写入面，本角色未改）：`status.md` 的"阶段 3 首步必须实测「`ask` 工具在 RPC 模式是否可用」"已由 K11/K12 闭合（该风险不再存在），阶段 3 首步的实测项应改为 **ACP 多问题表单的端到端帧面**；另 `status.md` 的"待确认项"（阶段 1 的 `[model_inferred]`）已可勾除。
