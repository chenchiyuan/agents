# pr-001 独立验收报告

**验证者身份**：协议实现审查者（rpc / acp 双链路协议消费面 + argv 契约 + 注入点分层）——产出物是「协议门面 + 三条链路消费 + 宿主工具通路」的代码改动，故反射为「看过 ESM 循环导入 TDZ、argv 收窄迁移、钩子挂起语义这类坑的协议实现审查者」，而非通用检查员。

**产出物**：`git -C /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0023-yolo-approval-and-question-inbox/.pb-agents/worktrees/0023-pr-001-approval-resolution-and-question-channel diff 4101e0b..2182b8b`
（PR worktree 分支 `feat/0023-pr-001-approval-resolution-and-question-channel`，HEAD `2182b8b`，基线 `4101e0b`；`git status --porcelain` 为空 ⇒ 产出物 = 该提交，无未提交残留）

**验证标准来源**：
1. `prs/pr-001-approval-resolution-and-question-channel.md`（验收标准 + 文件范围 + 择一判定声明）
2. `prs/pr-001-tasks.md`（T1~T15 + §0.4 冻结契约 1~12 + §0.3 读码事实锚点）
3. `architecture.md` **v0.3.0**（§4.2 L2-1/L2-2、§5.1~§5.7、§6 F01~F16、§7 T-01~T-08、§9.1/§9.3/§9.4、§11 N-1~N-5）
4. `clarifications/2026-09-14-stage5-wave1-verdicts.md`（用户裁决 MI-1~MI-5 + 主 agent 三条判据口径澄清 + Gate 义务 G2-D1/D2/D4）
5. 主 agent 简报的 7 条验证标准

**验证日期**：2026-09-14

---

## 逐项判定

### 标准 1：PR 文件验收标准全部满足 —— **pass**

逐条对照 PR 文件「验收标准」14 项：

| # | 验收项 | 判定 | 可定位证据 |
|---|---|---|---|
| 1 | 唯一汇聚点（F03-4 / L1-1） | pass | `protocol.js:65` `resolveApproval` 定义（全仓唯一实现）、`protocol.js:110` 唯一调用/赋值点；`grep -rn "resolveApproval" oamp/src` 命中集合 = `{protocol.js}`；`grep -rn "spec\.approval\s*=\|\.approval =" oamp/src` 命中恰 `protocol.js:110` 一处 |
| 2 | 默认档 = `yolo`（F01-1/3/4） | pass | `protocol.js:72` `return configured === undefined ? DEFAULT_APPROVAL`（`DEFAULT_APPROVAL='yolo'`，`:44`）；默认 argv = yolo：`acp-daemon.test.js:560`（常驻）/`:582`（一次性）/`confirmation-roundtrip.test.js:832`；**运行时半面**（yolo 零门 + 工具产生后果）由阶段 3 探针实测支撑 —— `clarifications/probes/r4-yolo-gate-output.txt` Y1：`门数=3 ["setWidget:","setWidget:","setWidget:"]`（三帧全为展示类 `setWidget`，零审批门）+ `工具=["write","bash"]` + `out.txt = "hello-0023"` |
| 3 | 档位可配 + 取值域两值（F02-1/2/3） | pass | `config.js:63-70` `readApproval` + `:21-22` 值域集合；`config-file.test.js:148+`（缺省 yolo / 显式 always-ask / 域外拒收）；`agent.js:612-615` `--approval-mode` 解析；全仓无 `write`/`tier` 入域 |
| 4 | 非法值响亮失败（F02-4 / MI-01） | pass | **实跑**：`node oamp/bin/oamp.js agent start x --approval-mode bogus` → stderr `oamp: agent start: --approval-mode 仅支持 always-ask|yolo: "bogus"`，`exit=2`；`OAMP_CONFIG=<{"approval":"bogus"}> node oamp/bin/oamp.js agent start x` → `OAMP 配置错误: approval 仅支持 always-ask/yolo（当前值 "bogus"）`，`exit=1`；`{"approval":"write"}` 同形。**不出现**「启动成功且档位落到 yolo」 |
| 5 | `deny` 强制 `always-ask` 且优先（F03-1/2/3 / MI-02） | pass | 解析链首档 `protocol.js:67` `if (source.permission === 'deny') return 'always-ask'`（在显式档位之前）；`approval-resolution.test.js:11-13`（deny 压显式 yolo）；argv 面 `acp-daemon.test.js:594-598`（tools on + deny ⇒ `always-ask`）；**rpc 三步实跑**：`confirmation-roundtrip.test.js:866-890`（E2E：拒绝回执 + 无参 `abort` 恰一帧 + `permission_denied` 结算 + 收件箱零新增）与 `protocol-layer.test.js:531-544`（单元）；acp 侧拒绝三步逐字未改（`acp-client.js` diff 无该区 hunk，见标准 4） |
| 6 | 门通路保留（F10-1~5） | pass | permission 类结算路径字面不变（`agent.js:312` 只认 `body.option_id`）；`confirmation_roundtrip` 既有 T1~T5 全绿；`deny` 实例零条目（`confirmation-roundtrip.test.js:811`）；yolo 偶发门（R4 Y2 形态）由 `r4-yolo-gate-output.txt` Y2 帧 + 既有钩子零改动承载 |
| 7 | 提问上浮（F04-1~5） | pass | rpc：`protocol-layer.test.js:549-575`（`set_host_tools` 恰一次 + 描述符逐字）、`:576-604`（`host_tool_call` → 上浮 → 回包 → 该轮继续）；acp：`tool-permission.test.js:1085-1138`（四形状拆问）；信封面 E2E：`confirmation-roundtrip.test.js:892-924`（rpc `request_kind:'question'`、`title` 逐字、`options` 不增不减、`multiple`）+ `:926-963`（acp 一帧 N 问）；通知类型零新增（`agent.js:291` `kind: 'confirmation_request'` 未被覆盖） |
| 8 | 提问形状与回传（F05 / F07 / MI-03） | pass | 信封 9 字段 `agent.js:284-295`；结算分型 `agent.js:319-326`（question ⇒ `{optionIds, text}`，**不含** `optionId`）；回包模板 `rpc-client.js:104-109`；E2E 逐字断言回包文本 `'选项：高\n文本：尽快'`（`confirmation-roundtrip.test.js:915-919`） |
| 9 | 一问一条与组内暂存（F06 / L1-5） | pass | `acp-client.js:687-712`（本帧局部聚合 + `Promise.all` + 齐答后单次 `_respond`）；`tool-permission.test.js:1140-1172`（齐答前不回包 / 齐答后恰一次）；E2E `confirmation-roundtrip.test.js:926-963`（N=2 条独立条目 + 齐答后 `replyFrames().length === 1`） |
| 10 | 挂起无上限（F08 / T-08） | pass | 复用既有冻结原语（`rpc-client.js:209-230` `freezeTurnTimer`/`thawTurnTimer`；`acp-client.js:715-727` `_askQuestion` ⇒ `_pauseTurnTimer`）；**零新增计时器**：`git diff 4101e0b..2182b8b -- oamp/src \| grep -c "^+.*setTimeout"` = 0；无 `ask.timeout` 配置键；超时远小于挂起时长的用例：`tool-permission.test.js:1140`（`timeoutMs:200` + `sleep(500)` 不 cancel/kill）、`confirmation-roundtrip.test.js:850`（`timeout_ms:300`）、`:926`（E2E 同形） |
| 11 | 两条链路承载与零上游改动（F09 / F12） | pass | argv 首段不变（`launcher.js:16` `modeArgs: ['--mode','rpc']`）；`--no-tools` 下仍注册并可调用：`protocol-layer.test.js:606-614`（断言 `argv.includes('--no-tools')` + `set_host_tools` 恰一次 + `hostToolResults.length === 3` + `capabilities.hostTools === 'yes'`）；`git diff --name-only` 不含 `omp/**` |
| 12 | 能力位与档位声明分离（F11 / T-06） | pass | `CAPABILITY_KEYS` 未改（`git diff` 无该符号）；`approval-resolution.test.js:60-79`（rpc 两档 `deepEqual`、`approvalGate==='yes'`、acp 两档同为 `null`、oneshot 两档 `deepEqual` 且 `approvalGate==='no'`）；三处同源：`agent.js:714` `approval: residentSpec.approval`（AGENT_START）↔ `protocol.js:110` 写回值 ↔ argv（三实现均传 `spec.approval`：`rpc-client.js:145`、`acp-client.js:271`、`oneshot-client.js:96`） |
| 13 | 不新增审计面与零依赖（F13 / F16） | pass | `git diff -- oamp/src \| grep -E "^\+.*(logger\.event\|\.event\()"` 无命中（零新增审计发射）；`TOOL_CALL`/`TOOL_APPROVED`/`TOOL_DENIED` 行类型零改动（diff 无命中）；rpc 拒绝路径复用既有码值 `permission_denied`（`rpc-client.js:344-347`，无新码值）；`oamp/package.json` `dependencies = {}`；`hygiene.test.js` 在全量套件内绿 |
| 14 | 命令全绿且改动面封闭 | pass | 见标准 2 与 7 |

**非本 PR 归属项（择一判定声明）复核**：`web.js`（F04-1 栏内可见 / F07-3 旁路停止）、`web/app.js`（F05-2 多选控件）、F14/F15 判定面均**零改动**且本报告未据其判定 —— pass（越界即失败的反面证据见标准 2）。

---

### 标准 2：改动面恰为该 PR 文件范围 —— **pass**

`git diff --name-only 4101e0b..2182b8b`（19 项，逐字）：

```
docs/iterations/0023-yolo-approval-and-question-inbox/prs/pr-001-tasks.md   ← 阶段 5 自身产物（不计入改动面）
oamp/README.md                                  ← 文档面 1
oamp/src/{protocol,config,launcher,oneshot-client,acp-client,rpc-client,agent,context-pool}.js   ← 生产面 8
oamp/test/{approval-resolution(new),config-file,protocol-layer,tool-permission,acp-daemon,confirmation-roundtrip,context-pool,project-workspace,web}.test.js   ← 测试面 1 新建 + 8 迁移
```

= **18 个本 PR 文件 + 1 个阶段 5 产物**，与简报「应含 18 个文件」及 `pr-001-tasks.md §0.1` 的 18 行清单**逐项一致，零多零少**。

**pr-002 / pr-003 面零改动核验**（`git diff --name-only 4101e0b..2182b8b | grep -E "web\.js|web/|call-protocol|confirmation-inbox|omp/|hygiene|zero-intrusion|inbox-console|api-routes|helpers|package\.json"` → **无命中**）：
`oamp/src/web.js`、`oamp/web/**`、`oamp/test/call-protocol.test.js`、`oamp/test/confirmation-inbox.test.js`、`oamp/API.md`、`oamp/llms.txt`、`oamp/bin/**`、`oamp/package.json`、`omp/**`、`oamp/test/{zero-intrusion,hygiene,inbox-console,api-routes}.test.js`、`oamp/test/helpers/**` 全部零改动。

**零改动声明复核（本 PR 文件面内部）**：`config.js` 不 import `src/` 内模块（仅 `node:url` / `node:fs` / `node:path`，`:6-8`）⇒ 叶子约束保持；`oneshot-client` 行流上限 200 / 超时三拍 / `-p` 形态未动（diff 仅 17 行增删，全在档位合成→消费）；`agent.js` 的 `cancelPending` / `runShellTask` / 心跳两档无命中改动。

---

### 标准 3：档位唯一汇聚点 —— **pass**

- **已解析档位值 `spec.approval` 的赋值面恰一处**：`protocol.js:110` `spec.approval = resolveApproval(spec);`（`createProtocolLayer` 装配期求值一次，就地写回同一对象）。`approval-resolution.test.js:44-57` 以 getter 计数断言「装配期只读一次」（`configReads === 1`）且写回同一 `spec`。
- **`resolveApproval` 实现/调用各恰一处**：定义 `protocol.js:65`，调用 `protocol.js:110`；`grep -rn "resolveApproval" oamp/src` 仅命中 `protocol.js`（测试文件另计）。
- **三实现零档位字面**：`grep -rn "always-ask\|yolo" oamp/src/{rpc-client,acp-client,oneshot-client}.js` → **无命中**。三实现只读：`rpc-client.js:145` `approval: toolsOn ? spec.approval : null`、`acp-client.js:271` `approval: this.tools ? this.approvalArg : null`、`oneshot-client.js:96` `const approval = toolsOn ? spec.approval : null` —— 均为「给值即落段」，无判定。
- **输入面字面（判据口径澄清 ②允许）**：`config.js:21-22`（`APPROVAL_VALUES`）与 `agent.js:612-613`（`value !== 'always-ask' && value !== 'yolo'`）为输入面校验，**不违反**唯一汇聚点。
- **解析链顺序**：`protocol.js:67-72` —— ① `permission === 'deny'` ⇒ `always-ask` → ② 显式 `--approval-mode` → ③ `configApproval`（config 第 5 键）→ ④ 内置 `DEFAULT_APPROVAL='yolo'`。逐档用例 `approval-resolution.test.js:11-34`。
- **无静默回落通路**：`launcher.js:113-115` —— `approval === undefined` ⇒ `throw new Error('OAMP 配置错误: buildArgv 需要已解析档位…')`；旧「`undefined ⇒ 取 profile.approval`」已删除（diff 移除 `if (approvalSpec !== null && (approvalSpec.appliesWhen === 'always' || toolsOn))`）。

---

### 标准 4：G2 义务已落 —— **pass**

| 义务 | 判定 | 证据 |
|---|---|---|
| **G2-D1** `config.js` 锚点/行为：非法值 ⇒ 响亮失败点名该值 | pass | `config.js:63-70` `readApproval`：`typeof value !== 'string' \|\| !APPROVAL_VALUES.has(value)` ⇒ `throw new Error(\`OAMP 配置错误: ${source} 仅支持 always-ask/yolo（当前值 ${JSON.stringify(value)}）\`)`（`:68`）；折叠位 `:132`，缺省 `:162` `approval: file.approval \|\| APPROVAL_DEFAULT`。实跑退出 1 + 点名，见标准 1-4。**锚点口径**：实测 `readProtocol` 基线行号 = `:49`（非 PR 文件所写 `:41`）；`readApproval` 在 HEAD 为 `:63` —— 该锚点裁定已落入 `pr-001-tasks.md §0.4` 契约（以 `:49` 为准），实现以行为满足；PR 文件正文未同步（记入偏差 D-3） |
| **G2-D2** `protocol-layer.test.js:535` 的 `{mode,appliesWhen}` 合成写法**彻底消失** | pass | 全仓 grep `appliesWhen` 仅命中 `launcher.js:22/37/52/68/83`（profile 静态数据）+ `:134`（注释）；`grep -rn "appliesWhen\|approval\.mode" oamp/test` **零命中**；该调用点改为 `rpcSession`/`buildArgv` 传已解析档位（`protocol-layer.test.js` 相关调用点） |
| **G2-D4** `rpc-client.js` 的 `hostTools` 转 `'yes'` | pass | `rpc-client.js:51` `hostTools: 'yes'`（diff 显示 `-  hostTools: 'no'`）；note 表 `RPC_CAPABILITY_NOTES` 整体删除，`rpcCapabilityNotes()` 现返回 `{}`（`:64-66`，rpc 六键恒 `'yes'` ⇒ 符合 MI-5「不为 yes 键新增 note」）；证书面 `protocol-layer.test.js:613` 断言 `session.capabilities.hostTools === 'yes'`。**acp 侧 `hostTools:'no'`（`acp-client.js:35`）未动** —— 与 G2-D4「不改 acp」一致 |
| **G2-D4** `approvalGate` 不随档位变 | pass | `RPC_CAPABILITY_VALUES.approvalGate = 'yes'` 为常量表（`rpc-client.js:50`）；`approval-resolution.test.js:60-72` 对 rpc 两档做 `deepEqual` 全表比较 ⇒ 结构保证 |

---

### 标准 5：提问通路 —— **pass**

**rpc 宿主工具 `ask_user`：**
- **握手后恰一次注册**：`rpc-client.js:271-276`（`negotiate_protocol` 回包成功分支内 `trySend({type:'set_host_tools', tools:[ASK_USER_TOOL]})`，早于 `settle.resolve()`）；测试 `protocol-layer.test.js:549-566` 断言 `set_host_tools` 帧数 === 1、`negotiate_protocol < set_host_tools < prompt` 的次序，以及 `--no-tools` 下仍注册（`:606-614`）。
- **描述符逐字**：`rpc-client.js:26-38` 与 architecture §5.4 逐项一致 —— `name: 'ask_user'`、`label: 'Ask User'`、`parameters` = `{type:'object', properties:{question:{type:'string'}, options:{type:'array',items:{type:'string'}}, multiple:{type:'boolean'}}, required:['question'], additionalProperties:false}`；`protocol-layer.test.js:568-583` 对 `descriptor.parameters` 做 `deepEqual` 逐字断言。
- **承接**：`handleHostToolCall`（`:386-424`）+ `hostToolResult`（`:375-379`，帧形 = A18 探针 `{type:'host_tool_result', id, result:{content:[{type:'text',text}]}}`）+ `handleHostToolCancel`（`:426-429`，撤条目不回包）+ `handleFrame` 分派（`:497-503`）。

**acp 非门 elicitation 四形状拆问：**
- 门/非门分流：`acp-client.js:669-677`（`enum ⊇ {Approve,Deny}` ⇒ 既有门路径；否则 `_handleQuestionForm`）。
- 拆问实现：`readQuestionForm`（`:98-146`）覆盖 askDialog（`q{i}` 多问，`:106-126`）/ select（enum）/ confirm（boolean ⇒ 选项「是/否」，`CONFIRM_TRUE/FALSE` = `:26-28`）/ input（string）；未知形状 ⇒ `null`。
- 一问一条 + 齐答恰一次：`_handleQuestionForm`（`:687-712`）逐问 `_askQuestion` → `Promise.all` → **单次** `_respond({action:'accept', content})`。
- 证据：`tool-permission.test.js:1085-1135`（六次上浮逐条形状 `deepEqual` + 五帧各回包恰一次 + 单值形状选项优先 + 未知形状 `decline`）、`:1140-1172`（齐答前不回包 / 齐答后恰一次 / 挂起无超时）、E2E `confirmation-roundtrip.test.js:926-963`（N=2 条独立条目，`replyFrames().length === 1`，content `{q0,q0__other,q1,q1__other}` 逐字）。

**两条链路同一信封：**
- `request_kind:'question'` 由 `agent.js:288-293` 生成（钩子入参 `info.requestKind === 'question'` ⇒ `request_kind: 'question'`，否则 `'permission'` 兜底）；两链路入参见 `rpc-client.js:394`（`requestKind:'question'`）与 `acp-client.js:695`（`requestKind:'question'`）。
- 同一信封 E2E：rpc `confirmation-roundtrip.test.js:898-906`（`kind === 'confirmation_request'` + `request_kind === 'question'` + `title` 逐字 + `options` 不增不减 + `multiple` + `chat_id`/`agent_id`）；acp `:934-938`。
- **钩子恒注入（deny 实例同样上浮）**：`context-pool.js:202-205` 与 `:213`（`onQuestion` 不参与 `permission === 'allow'` 判定）；门钩子判定逐字保留（`:193-200`）；测试 `context-pool.test.js:551-608`（deny 档：`onPermissionRequest === null` / `onApproval === null` / `typeof onQuestionRequest === 'function'`，且可调用并附加身份；allow 档对照两型同时在位）。

---

### 标准 6：rpc 无收件人三步 —— **pass**（附实跑证据）

实现：`rpc-client.js:336-348` —— 无 `hooks.onApproval` 时 ① `trySend({type:'extension_ui_response', id, cancelled:true})` → ② `if (!abortSent) { abortSent = true; trySend({ type:'abort' }); }`（**无参**，幂等位 ⇒ 恰一帧）→ ③ `if (turn) failTurn(turn, new ProtocolError('permission_denied', …))`。

实跑证据（两次均在本 PR worktree 内实跑，非静态阅读）：
1. **单元实跑**：`protocol-layer.test.js:531-544`「④b 门无收件人（hooks=null）：回执拒绝 + 无参 abort 恰一帧 + 以 permission_denied 结算该轮」——`assert.rejects(... err.code === 'permission_denied')` + `assert.deepEqual(log.replies, [{type:'extension_ui_response', id:'g-1', cancelled:true}])` + `assert.deepEqual(log.aborts, [{type:'abort'}])` + 第二轮可再起且 `aborts.length === 2`（轮次级幂等位按轮重置）。该用例在目标命令运行中报 `ok`。
2. **端到端实跑**：`confirmation-roundtrip.test.js:866-890`「T5④/PR-6（rpc）：deny 档受门禁调用 ⇒ 收件箱零新增条目 + 该轮以 permission_denied 中止（三步俱在）」——`result.state === 'failed'`、`result.error === 'permission_denied'`、`notices('confirmation_request').length === 0`、帧级断言 `extension_ui_response` 恰一条 `{cancelled:true}`、`abort` 恰一条、且 `!frames.some(f => f.type === 'host_tool_result')`。
3. **目标命令输出**：9 文件集合 `# tests 179 / # pass 179 / # fail 0 / # cancelled 0`。

---

### 标准 7：目标测试命令全绿 + 零第三方依赖 —— **pass**

**命令 1（PR 文件指定集合，实跑）**：
```
node --test oamp/test/approval-resolution.test.js oamp/test/config-file.test.js oamp/test/protocol-layer.test.js \
  oamp/test/tool-permission.test.js oamp/test/acp-daemon.test.js oamp/test/confirmation-roundtrip.test.js \
  oamp/test/context-pool.test.js oamp/test/project-workspace.test.js oamp/test/web.test.js
```
输出尾部：`1..179` / `# tests 179` / `# pass 179` / `# fail 0` / `# cancelled 0` / `# skipped 0` / `# todo 0`（duration_ms 61231.52）。

**命令 2（全量套件，实跑）**：`node --test oamp/test/*.test.js` → `1..379` / `# tests 379` / `# pass 379` / `# fail 0` / `# cancelled 0`，含 `ok 378 - B-17②：生产消费层不出现按协议取值的字面`、`ok 379 - B-17③：切换协议只改注入配置 ⇒ 生产消费层逐字节不变`（`zero-intrusion.test.js` 保持绿且**零改动**）。

**命令 3（新建测试单跑，实跑）**：`node --test oamp/test/approval-resolution.test.js` → `# tests 5 / # pass 5 / # fail 0`。

**零第三方依赖**：`node -e "require('./oamp/package.json').dependencies"` → `{}`（无 `devDependencies` 键）；`hygiene.test.js` 在全量套件内 pass。

---

## 汇总

- pass: **7 项**（主标准全项）
- fail: **0 项**
- partial: **0 项**
- blocked: **0 项**

---

## 偏差记录

> 实现与规格/文档不一致之处。**不影响本次验收判定**，供下一迭代同步文档或评估实现。

| # | 规格/文档描述 | 实现实际 | 严重度 | 建议处理 |
|---|---|---|---|---|
| D-1 | `architecture.md` §5.4「承接」行写 `host_tool_result{id, result:{content:[{type:'text',text}], details:{}}}`（含 `details` 键） | `rpc-client.js:375-379` 回包 `{ type:'host_tool_result', id, result:{ content:[{type:'text', text}] } }`，**不发 `details`**；与 A18 探针实测帧形（`prs/pr-001-…md` 参考资料）及 `prd/F04` T-05 行（无 `details`）一致；实测 omp 侧亦无需该键（`tool-permission` / `protocol-layer` 全部回包用例绿） | 低 | 按实测更新 `architecture.md` §5.4 承接行（去掉 `details:{}`，或注明该键非必需） |
| D-2 | `prs/pr-001-…md` 文件范围写 `launcher.js`：「五行的 `approval.mode` 删除、**保留 `appliesWhen`**」 | `launcher.js:52`（`omp:oneshot` 行）的 `appliesWhen` 由 `'always'` 改为 `'tools-on'`；`buildArgv` 追加条件由 `appliesWhen === 'always' \|\| toolsOn` 收窄为纯 `toolsOn`（`:135-137`）。**行为等价**（一次性实现工具关时传 `null` ⇒ argv 逐字不变，`confirmation-roundtrip.test.js:824-833` / `acp-daemon.test.js:582` 断言 argv 仍 `yolo`）；该统一形态见 `pr-001-tasks.md §0.4` L2-2 与 `tasks.md §0.3` A2，但**未回写 PR 文件范围描述** | 低 | 在 PR 文件范围行或 `architecture.md` §4.2 L2-2 补一句「五行的 `appliesWhen` 统一为 `'tools-on'`（一次性路径 argv 逐字不变）」，使规格描述与实现同形 |
| D-3 | `prs/pr-001-…md` 代码基线锚点写 `oamp/src/config.js:41`（`function readProtocol`） | 基线 `4101e0b` 实测 `readProtocol` 在 **`:49`**；HEAD 因新增常量下移为 `:52`，新增 `readApproval` 在 `:63`。该锚点已由 Gate 裁定（`pr-001-tasks.md §0.4` G2-D1「一律以 `:49` 为准」，`§0.3` A1 留档），**PR 文件正文锚点未同步** | 低 | 阶段 6 收口或下一迭代同步 PR 文件锚点（`:41` → `:49`），避免后续读者按错行号定位 |
| D-4 | `prd/F04` 验收 2 字面「提问请求**不再**被自动回 `{cancelled:true}` / `decline`」（未限定来源帧型） | rpc 的 `INTERACTIVE_METHODS`（`select`/`confirm`/`input`/`editor`，`rpc-client.js:24`）仍回 `{cancelled:true}`（`:437-439`）；acp 未知形状 / 无提问收件人仍回 `{action:'decline'}`（`acp-client.js:691` / `:705`）。两处均为 **PR 文件与 architecture 明文列出的零改动 / 保守回落面**（§4.2 L2-6「不代答产品外提问」、§5.5「其余未知形状 ⇒ 既有 `decline`」），且 PRD F04 验收 5 把上浮源限定为「agent 主动提问（`ask_user` / 非门 elicitation）」⇒ 本 PR 的两条提问通路**均不产生**这两类回执（rpc 走 `host_tool_result`，acp 四形状走 `accept`） | 低 | 在 `prd/F04` 验收 2 补适用面限定（「提问通路 = `ask_user` / 非门 elicitation 四形状」），或按 architecture 已定口径在迭代收口时登记——不改产品维度语义 |

---

## 下一迭代候选

- **acp `select` + 自由文本的「文本胜出」为上游硬约束**（`architecture.md` §11 R4 / §12.1-Q-1）：hub 不可解（改上游违反 F12）。若未来 omp 侧放开单值形状的双值承载，此处映射表（`acp-client.js:166-171` `readQuestionContent`）是唯一需要回改的点，建议在下一迭代跟踪 omp 版本变化。
- **`ask_user` 描述符 `description` 无规格真源**：`rpc-client.js:29` 的描述文本来自 hub 自定（architecture §12.1 R2），测试只断言其为非空字符串（`protocol-layer.test.js:570`）。若下游前端要据描述做展示，建议把该文本纳入 architecture 或固化为契约常量。
- **`launcher.js` 的 `appliesWhen` 已成单值常量**：五行统一为 `'tools-on'` 后，`approvalSpec` 形状退化为「非 null 即落段」（`:135`），`appliesWhen` 字段本身不再承载分支。若后续无第二取值计划，可考虑在下一迭代把它降级为文档注释（奥卡姆）；本次不动（属超出本 PR 范围的重构）。
- **`buildArgv` 未给档位即抛错的响亮面**（`launcher.js:113-115`）目前无直接单测覆盖（由 `approval-resolution` / `protocol-layer` 的装配面间接覆盖）。下一迭代若新增链路实现，建议补一条断言防止回退为静默回落。

---

## 结论

**PASS**

7 条主标准全部 pass，0 fail / 0 partial / 0 blocked；18 个本 PR 文件改动面零多零少，pr-002 / pr-003 面与 `omp/**` 零改动；档位解析唯一汇聚点、G2-D1/D2/D4 三项义务、提问通路两链路、rpc 无收件人三步均有可定位代码证据 + 实跑证据（目标集合 179/179、全量 379/379、CLI 非法值实跑退出码 2 / 1）。

4 条偏差（D-1~D-4）全部为低严重度的「文档滞后于实现」或「PRD 字面未限定适用面」，**不阻塞本 PR 合入**，建议随阶段 6 收口或下一迭代同步文档。
