# verify-pr-003-20260914.md — pr-003 独立验收报告

**验证者身份**：同级前端审查者（零依赖原生 DOM/字符串渲染的控制台第三栏）× 前端测试工程师（node:vm 沙箱函数体抽取 + 产出字符串/请求体断言体例）
**产出物**：`git -C /Users/chenchiyuan/projects/agents/.pb-agents/worktrees/0023-yolo-approval-and-question-inbox/.pb-agents/worktrees/0023-pr-003-inbox-question-item-frontend diff d90e6e0..4162fff`（HEAD `4162fff`，基线 `d90e6e0`；`--numstat`：`320/1` + `19/0` + `58/1`，共 3 文件 / 397 插入 / 2 删除）
**验证标准来源**：主 agent 委托的 7 条验证标准 + `prs/pr-003-inbox-question-item-frontend.md`（验收 1~10 + 文件范围 + 择一判定声明）+ `architecture.md` v0.3.0（§5.2 / §5.2.1 / §5.3 / §4.2 L2-7·L2-8 / §9.2 / §9.3）+ `clarifications/2026-09-14-stage5-wave2-verdicts.md`（MI-1~MI-7 + Q3 + Q1/Q2/Q4/Q5）
**验证日期**：2026-09-14

> **独立性声明**：本报告只依据上述产出物与规格作出判定；未向执行者求证执行过程，未读取任何执行过程叙述（本报告不含"设计意图 / 过程背景"类依据）；未修改任何被测产物（唯一写入 = 本报告）；未执行任何 git 写操作（只读命令仅 `diff` / `show` / `log` / `rev-parse` / `status`，验收结束时 PR worktree `status --porcelain` 为空 ⇒ 工作区零残留）。
>
> **规格文件可得性说明**：委托指定的 `prs/pr-003-tasks.md` 在迭代工作区**不存在**（`glob docs/iterations/0023-…/**/*tasks*.md` ⇒ 仅 `pr-001-tasks.md` / `pr-002-tasks.md`）。其内容已按 `clarifications/2026-09-14-stage5-wave2-verdicts.md` 首行自述（"**转呈来源**: `prs/pr-003-tasks.md`（7 项 MI + Q1~Q5）"，性质 `user_confirmed`）由该裁决记录承载，本报告以裁判记录（MI-1~MI-7 / Q3）为等效任务图标准，未据此降低任何判据。此事实记入偏差 D-2。

---

## 逐项判定

### 标准 1：PR 文件验收 1~10 全部满足 —— **pass**

逐条判定（`prs/pr-003-inbox-question-item-frontend.md` §验收标准）。运行期证据由**独立于本 PR 自带测试的复现 harness**（`/tmp/vfy003.js`：从 head `oamp/web/app.js` 抽取函数体在独立 `vm` 上下文执行，自写桩）给出，命令行 `node /tmp/vfy003.js`；静态证据为 diff 与 head 源码行号。

| # | 验收摘要 | 判定 | 证据（可定位） |
|---|---|---|---|
| 1 | question 类条目形状：三行 + 单选即取舍控件 + 自由文本 + 提交按钮；`multiple:true` ⇒ 可多选 | pass | 渲染产出（harness 实跑 `multiple:false`）：`<div class="inbox-source">pb-dev · chat-1</div>` / `<div class="inbox-request">ask_user · 优先保证哪一点？</div>` / 两个 `<input type="radio" class="inbox-choice-box" name="inbox-choice-cfm-q1" data-option=…>`（**同名 ⇒ 组内至多单值**）/ `<input class="inbox-text" … placeholder="补充说明或作答（可不填）">` / `<button class="inbox-submit">提交</button>`；`multiple:true` ⇒ 2 × `type="checkbox"`、0 × radio、产出串与单选态不相等。实现锚点 `oamp/web/app.js:653-659`（`renderInboxChoice`）、`:684-696`（question 分支） |
| 2 | `options: []` 仍渲染（选项区空）、文本可提交、无「选项必选」阻塞 | pass | harness `options: []` 与 `options: undefined` 两态：0 个勾选控件、无残留 `class="inbox-actions"` 空容器、`class="inbox-text"` 与 `<button class="inbox-submit">提交</button>` 均在、条目容器在；`submitQuestion` 内**零校验分支**（`oamp/web/app.js:769-789` 全文无「必填」判定） |
| 3 | 提交载荷分化：question ⇒ `{option_ids:[…], text}`（同值同序）；仅文本 / 仅选项同路径；permission ⇒ `{option_id, text}` | pass | harness 三态实跑：勾选 A、C（B 未勾）⇒ body `{"option_ids":["A","C"],"text":"  都要  "}`（**未排序未去重、文本未 trim**）；仅文本 ⇒ `{"option_ids":[],"text":"优先保证工具调用可审批"}`（空数组而非缺键）；仅勾选 ⇒ `{"option_ids":["A"],"text":""}`；三者 path 均为 `/api/confirmations/cfm-q1/decision`、method `POST`。permission 侧 `decide` 实跑 body `{"option_id":"allow","text":"理由"}`（`oamp/web/app.js:749-764` 与基线逐字相同，见标准 3） |
| 4 | 一次提交即一次裁决：控件 disabled / 200 移出 / 404 移出 / 其余保留重绘 | pass | harness：提交中 `boxes.disabled=[true,true]`、`textInput.disabled=true`、`submit.disabled=true` 且 `textContent='提交中…'`；200 ⇒ `drops=['cfm-q1']`、`redraws=0`；404 ⇒ `drops=['cfm-q1']`、`redraws=0`、`posts=1`（不重放）；500 ⇒ `drops=[]`、`redraws=1`。实现锚点 `oamp/web/app.js:774-778`（disabled 三处）、`:785-788`（200 / 404 / 其余三分支） |
| 5 | permission 类零退化：三行 + 每选项一按钮 + 文本，点选即裁决，与 0021 逐字一致 | pass | **字节级对照**：把 head 的 permission 分支尾段（自 `const options = …` 至函数尾）与基线 `d90e6e0:oamp/web/app.js` 同段比较 ⇒ 完全相同（`True`）；把 head `renderInboxItem` 与基线函数分别在沙箱内对同一条目执行 ⇒ 产出字符串**完全相等**（`permission output byte-equal to 0021 baseline: true`）；`request_kind` 缺失 / `'PENDING'` 域外值 ⇒ 与 `'permission'` 产出同一串（harness 三态 `true`）。接线面：`oamp/web/app.js:727` 的 `for (const btn of el.querySelectorAll('.inbox-option')) btn.onclick = () => decide(entry, btn, text);` 逐字在场。详见标准 6 |
| 6 | 多问题独立：N 条 question 只答一条 ⇒ 该条移出、其余仍在且可独立提交 | pass | harness 以两条 question 条目跑 `dropInboxItem('cfm-q1')` ⇒ `state.inbox.items.map(confirmation_id) = ['cfm-q2']`、长度 1、余项 `request_kind` 仍为 `'question'`；`dropInboxItem` 为 `oamp/web/app.js:741-744`（**未改动**，见标准 3 的逐函数比对）；提交按 `entry.confirmation_id` 逐条闭包绑定（`:724`） |
| 7 | 数据来源与载体不变（`GET /api/confirmations` + 全局 `confirmation` 帧；零存储 / 零轮询 / 零新页面） | pass | `loadConfirmations`（`oamp/web/app.js:663-671`，逐字未变）、`api('/api/confirmations')`、`es.addEventListener('open', loadConfirmations)`、boot 首屏调用均在场（本 PR 的 app.js diff 4 个 hunk 均不触及）；`confirmation` 帧分支与 `onopen` 重建不在 diff 内（diff 仅 `@@ -648 / -667 / -692 / -731` 四处）；harness/静态：`grep -n "POLL_MS\|setTimeout(tick" oamp/web/app.js` ⇒ 无匹配；`localStorage|sessionStorage` 零命中（既有断言 `oamp/test/inbox-console.test.js:407`，实跑 `ok 8`） |
| 8 | 界面无历史面：栏内只在途条目，无已裁决列表 / 入口 | pass | 本 PR 新增代码仅 `renderInboxChoice` / question 渲染分支 / `renderInbox` 类型分派 / `submitQuestion`（`git diff` 全文，`artifact:1428`），无任何终态条目列表、无历史视图、无新入口；移出仍唯一经 `dropInboxItem`（`:741-744`，未变） |
| 9 | 样式只追加：既有声明原样在场，新增规则为 question 控件样式 | pass | `git diff --numstat d90e6e0..4162fff -- oamp/web/style.css` ⇒ **`19 0`（零删除）**；新增规则逐名命中控件类名 `oamp/web/style.css:479`（`.inbox-choice`）、`:480`（`.inbox-choice-box`）、`:481`（`.inbox-choice-text`）、`:482`（`:checked +` 选中态）、`:484-495`（`.inbox-submit` / `:hover:not(:disabled)` / `:disabled`）；引用的变量均在既有根变量表内（`--green:8` / `--line:5` / `--radius:13` / `--bg:3` / `--text:6`）；零新 CSS 文件（`--diff-filter=A` 为空）；`@import|@font-face|url(` 零命中 |
| 10 | 命令全绿且改动面封闭 | pass | `node --test oamp/test/inbox-console.test.js` ⇒ `1..16 / # pass 16 / # fail 0`，**exit=0**；`node --test oamp/test/*.test.js` ⇒ `1..387 / # tests 387 / # pass 387 / # fail 0`，**exit=0**（输出落 `/tmp/verify003-targeted.txt` / `/tmp/verify003-full.txt`）；`git diff --name-status d90e6e0..4162fff` ⇒ 恰 3 行（见标准 2） |

**择一判定声明核对**：① F05 验收 2 主面在本 PR（栏内控件）——本报告按栏内控件判定，pass；② F07 验收 1 本 PR 只判"提交载荷形态正确"——按形态判定，pass；③ 端到端不在本 PR 判定面——未纳入。

### 标准 2：改动面恰为 3 个代码文件；零改动清单逐条不出现 —— **pass**

- **恰 3 文件**：`git diff --name-status d90e6e0..4162fff` ⇒ 三行且无新增文件（`--diff-filter=A` 为空）：
  - `M oamp/test/inbox-console.test.js`（`320/1`）
  - `M oamp/web/app.js`（`58/1`）
  - `M oamp/web/style.css`（`19/0`）
- **零改动清单逐条不出现（输出为空 ⇒ 零改动）**：`git diff --stat d90e6e0..4162fff -- oamp/src oamp/web/index.html oamp/web/notify.js oamp/API.md oamp/llms.txt oamp/package.json oamp/test/api-pages.test.js oamp/test/confirmation-inbox.test.js oamp/test/web.test.js oamp/test/project-workspace.test.js oamp/test/hygiene.test.js oamp/test/helpers omp` ⇒ 无输出。交叉验证：全量 `--name-status` 仅 3 行 ⇒ `oamp/src/**`、`index.html`、`notify.js`、`API.md`、`llms.txt`、其余测试文件与 `omp/**` 均零改动。
- **MI-7 代码文件集合 = 3** ⇒ 成立（任务图 docs 文件不在本 PR diff 内：`pr-003-tasks.md` 由基线提交 `d90e6e0` 承载于 diff 之外）。
- **基线上下文**：`git log --oneline -6 d90e6e0` ⇒ `d90e6e0`（pr-003 任务图）叠在 `6dedd23`（pr-002 合入）与 `0123dcb`（pr-001 合入）之上 ⇒ 本 worktree 的全量套件结果已覆盖 pr-001/pr-002 面。

### 标准 3：渲染分化（两态 / `options: []` / permission 逐字未动）—— **pass**

- **question 分支两态可区分**（`oamp/web/app.js:683-696`）：`multiple === true` ⇒ 原生 checkbox；否则 ⇒ **同 `name` 原生 radio**（`oamp/web/app.js:653-659` 的 `name="inbox-choice-${escapeHtml(entry.confirmation_id)}"`）。harness 逐 `multiple` 取值实跑：`false / undefined / null / 'true' / 1 / 0` ⇒ **全部 radio×2 / checkbox×0**（仅字面 `true` 走多选 ⇒ A8 兜底与 PR 文件 `entry.multiple === true` 判据一致）；`true` ⇒ checkbox×2 / radio×0；两态产出串不相等。
- **`options: []` 完整渲染**：条目容器 + 来源行 + 请求行 + 文本 + 提交按钮全在，勾选控件区**不产出空容器**（`${choices === '' ? '' : …}`，`:692`）。
- **permission 分支逐字未动（diff 证据）**：`git diff d90e6e0..4162fff -- oamp/web/app.js` 中 permission 分支的全部行（`const options = …` / `.map((o) => \`<button class="inbox-option" …\`` / `.join('')` / 四处模板行）均为**未标记的上下文行**（无 `-` / `+` 前缀）；逐函数字节比对（脚本抽取函数体）：`escapeHtml` / `inboxSource` / `inboxRequest` / `syncInboxLabels` / `dropInboxItem` / `decide` / `focusFromNotify` / `loadConfirmations` / `api` ⇒ **全部 `same`**；`renderInboxItem` / `renderInbox` 标 `CHANGED`（仅新增 question 分支与类型分派，permission 尾段字节包含关系成立 ⇒ `perm branch bytes present in head: True`、`head perm tail == base perm: True`）。`decide` 函数体与基线**完全相同**（`:749-764` ↔ `d90e6e0:oamp/web/app.js:717-732`）。
- **`request_kind` 引用面封闭**：head 全文中 `request_kind` 仅 4 处（`:680` 注释 / `:684` 渲染分支 / `:698` 注释 / `:723` 接线分派），基线为 0 处 ⇒ 消费面改动点恰为「渲染分支 + 接线分派」两处，`loadConfirmations` / SSE `confirmation` 分支 / `inboxSource` / `inboxRequest` 均**不按类型分化**（与验收 7「不区分条目类型地统一消费」一致）。

### 标准 4：提交体分化 —— **pass**

- **question**（`oamp/web/app.js:769-789`）：`body: { option_ids: optionIds, text }`（`:782`），`optionIds = [...item.querySelectorAll('.inbox-choice-box:checked')].map((box) => box.dataset.option)`（`:772` 一行为唯一构造点 ⇒ **DOM 顺序即原序，无 sort / 无去重 / 无筛选**）；`text = textInput.value`（`:773`，**不 trim**）。harness 实测三态见标准 1#3，与规格「同值同序 / 未勾选=空数组 / 文本原样」逐条对齐。
- **permission**：`decide` body `{ option_id: button.dataset.option, text: textInput.value }`（`:757`）与基线 `d90e6e0` 同句**逐字相同**（函数体字节相等）；harness 实跑 body `{"option_id":"allow","text":"理由"}`；`assert.doesNotMatch(fnBody(appJs,'decide'), /option_ids/)` 在实跑中通过。
- **两类互不串形**：`submitQuestion` 内无 `option_id:`（既有断言实跑通过）；`decide` 内无 `option_ids`。

### 标准 5：Q3 落地 —— 前端不短路空提交 —— **pass**

- `submitQuestion` 内**零前置校验**：`:770-780` 依次为 `requestPermission()` → 收集勾选 → 取值 → disabled → 直接进入 `await api(...)`；无「若全空则 return」形态。
- harness 实跑全空条目（无勾选、`text=''`）：`posts.length === 1`、body `{"option_ids":[],"text":""}` —— 请求确实发出；服务端 400 面由 pr-002 承载（本 PR 不判服务端）。同时实跑 400 响应 ⇒ `drops=[]`、`redraws=1`（条目保留 + 重绘复位）⇒ L2-7「前端不作权威判定」在 UI 上可见。
- 与 architecture §5.3「空提交不构成作答（服务端 400）」+ Q3 裁决一致；前端亦未新增弹窗（`alert|confirm` 零命中，既有 `doesNotMatch` 断言实跑通过）。

### 标准 6：既有断言强度未被削弱 —— **pass**

- **唯一删除的既有断言**：`git diff -- oamp/test/inbox-console.test.js` 的全部删除行恰 **1 行**（`grep -n '^-[^-]'` ⇒ `120:- assert.equal(…requestPermission…).length, 2, …`）；其替换为 **3 + 落点归属断言**（`oamp/test/inbox-console.test.js:471` 计数 = 3，`:472-475` 以 `toggle` / `decide` / `submitQuestion` 三作用域求和 = 3）⇒ 强度**提升**（MI-6 满足）。
- **decide 手势断言仍在**：`oamp/test/inbox-console.test.js:476` `assert.match(fnBody(appJs, 'decide'), /oampNotify\.requestPermission\(\)/, '点选选项同样触发手势入口')`；新增 `:477` 对 `submitQuestion` 的同型断言。实现面独立核对：`grep -n -F "oampNotify.requestPermission()" oamp/web/app.js` ⇒ 3 处（`:750` decide / `:770` submitQuestion / `:1213` 第三栏开合块内 `$('btn-inbox-toggle').onclick`），**全部落在第三栏交互回调内**，零落在加载期 / 派发面。
- **permission 静态契约断言逐字保留**：基线 `d90e6e0:oamp/test/inbox-console.test.js:331-336`（`class="inbox-option"` / `label || o.option_id` / `placeholder="拒绝理由 / 补充说明（可不填）"`）在 head 中同句在场（`:414-416`）；基线 `:379` 手势计数断言（文件范围点名锚点）确认为本 PR 唯一改写的既有行。
- **body 逐字锁双路径（MI-5 / Q4）**：`decide` 的 `body: { option_id: button.dataset.option, text: textInput.value }` 锁仍在（head `:432`），question 侧新增 `body: { option_ids: optionIds, text }` 锁（`:481`）⇒ 强度未降，两路径各有逐字锁。
- 其余既有断言零删改（320 插入 / 1 删除的 numstat 已闭合）。

### 标准 7：全量套件绿 + 零新依赖 + 无新增面板/页面/SSE 事件类型 —— **pass**

- **命令与输出**（PR worktree 内执行，工作区干净）：
  - `node --test oamp/test/inbox-console.test.js` ⇒ `# tests 16 / # pass 16 / # fail 0`，exit 0
  - `node --test oamp/test/*.test.js` ⇒ `# tests 387 / # pass 387 / # fail 0`，exit 0（`oamp/package.json` 的 `scripts.test` 即为此命令）
- **零新依赖**：`oamp/package.json` 零改动；全量套件中 `ok 162 - package.json dependencies 为空（F08 载体 / 零依赖声明）`（`/tmp/verify003-full.txt:977`）通过；`oamp/test/hygiene.test.js:61-63` 判据为 `deepEqual(dependencies, {})`。
- **无新增面板 / 页面**：`oamp/web/index.html` 零改动（标准 2）；diff 无新 HTML/CSS 文件。
- **无新增 SSE 事件类型**：`oamp/web/notify.js` 零改动；app.js diff 内 `addEventListener|EventSource|=== 'confirmation'` 零新增命中（既有 4 类会话事件与 `agent_online/offline` 断言原样在场）。
- **无新增存储 / 轮询**：`localStorage|sessionStorage` 零命中；`POLL_MS` / `setTimeout(tick` 零命中。

---

## 汇总

- **pass: 7 项**（标准 1~7，含标准 1 内 10 条验收全 pass）
- **fail: 0 项**
- **partial: 0 项**
- **blocked: 0 项**
- **偏差记录: 3 条**（D-1~D-3，均非返工级，不影响结论）

---

## 偏差记录

> 逐项判定针对"实现是否满足规格"；本区块针对"规格/文档是否准确描述当前实现"。不影响上述判定。

| # | 规格/文档描述 | 实现实际行为 | 建议处理 |
|---|---|---|---|
| D-1 | `architecture.md` §9.2 前端变更点表：`web/app.js`「`renderInboxItem` 按 `request_kind` 分化…；**`decide()` body 按 `request_kind` 提交 `{option_ids, text}` / `{option_id, text}`**」 | 实现取**两条独立路径**：`decide()` **逐字未动**（仍恒为 `{option_id, text}`），question 侧新增 `submitQuestion()`（`:769-789`）+ 新增 `renderInboxChoice()`（`:653-659`），接线在 `renderInbox` 内分派（`:723-726`）。该形态为 PR 文件 §文件范围（"`decide`（permission 类 `{option_id, text}`）**逐字不变**"）与 MI-5 / Q4（"两条路径相容"）**明文许可** | 按实现更新 `architecture.md` §9.2 的措辞（把「`decide()` body 按 `request_kind` 分化」改为「`decide()` 逐字不变 + 新增 `submitQuestion()` 承担 question 提交」），或在下轮架构收口登记该措辞为 L3 呈现细节 |
| D-2 | MI-3：「控件类名清单与提交体构造签名须写入**交付说明**」；本文件范围内 `prs/pr-003-tasks.md` 作为标准来源 | ① 迭代工作区内**无独立交付说明文件**（`docs/iterations/0023-…/` 下无 `deliveries/` 或交接文档；PR 分支 `--diff-filter=A` 为空）；② 但 MI-3 的**实质接口面已由产物自身承载**：类名清单落在 `oamp/test/inbox-console.test.js:374-376`（`.inbox-choice` / `.inbox-choice-box` / `.inbox-choice-text` / `.inbox-submit` 逐名断言，T3 可直接消费为 CSS 选择器接口）、提交体签名落在 `:481`（`body: { option_ids: optionIds, text }` 逐字锁，T3 可直接消费为沙箱判据接口）。验证者按红线不接收执行过程上下文 ⇒ 无法判定"交付说明"是否以非落盘形式（执行者回报）存在 | 若"交付说明"要求落盘 ⇒ 在 `prs/pr-003-tasks.md`（或等效裁决记录）补一行控件类名清单 + 提交体签名；若其定义为执行者回报主 agent 的口头面 ⇒ 在迭代 `status.md` 登记该定义，避免下轮验收再次出现"标准来源文件不存在" |
| D-3 | 委托标准来源列出的 `prs/pr-003-tasks.md` | 文件**不存在**（`glob docs/iterations/0023-…/**/*tasks*.md` ⇒ 仅 `pr-001-tasks.md` / `pr-002-tasks.md`）；其 7 项 MI + Q1~Q5 由 `clarifications/2026-09-14-stage5-wave2-verdicts.md`（自述"转呈来源 = `prs/pr-003-tasks.md`"、性质 `user_confirmed`）承载。本报告以该裁判记录为等效任务图标准判定，未降低判据 | 二选一：补落 `prs/pr-003-tasks.md`（与 pr-001/pr-002 一致的文件形态），或在 `status.md` 标注"pr-003 任务图以 wave2 裁决记录为唯一载体" |

---

## 下一迭代候选

> 未在本轮验收标准范围内，供下一迭代决策。

- **N-1 · question 类通知正文模板不贴切（与 Q1 同源，附本报告独立证据）**：`oamp/web/notify.js` 零改动 ⇒ question 类条目的浏览器通知正文仍是 permission 口径模板（`请求执行 ask_user：…`），而 question 类在栏内的交互是「取舍 + 提交」，通知点击仅展开高亮条目（`focusFromNotify`，本 PR 未改）。影响面：仅文案贴切度，无功能阻断；证据：`oamp/web/app.js:170-182`（`confirmation` 帧分支零 `request_kind` 分化）+ notify.js 未改动。
- **N-2 · question 提交失败无任何用户可见反馈**：按 Q2/Q3，400/500 一律「保留条目 + 重绘」，用户看不到失败原因（`submitQuestion` 的 `catch` 无提示路径，`alert|confirm` 零新增属有意为之）。当服务端因 MI-03 必填校验回 400 时，UI 表现为"点提交后按钮闪回" ⇒ 可选增强：在条目内加一行静默错误文案（不引组件、不加弹窗）。
- **N-3 · question 提交路径对缺失控件无保护**：`submitQuestion` 直接 `item.querySelector('.inbox-text').value` 与 `item.querySelector('.inbox-submit')`（`:771-778`），若未来 question 渲染分支去掉文本框 / 提交按钮，将抛 TypeError 而非静默保留条目。当前渲染模板恒产出两者 ⇒ 非现实缺陷，属防御性增强候选。
- **N-4 · `options: []` 时 HTML 残留纯空白行**：`:692` 的三元产出空串后，模板中该行仅剩缩进空白（harness 实跑可见空行）。零功能影响，可在下一轮顺手收敛。
- **N-5 · radio 组名未做属性级转义测试**：`name="inbox-choice-${escapeHtml(entry.confirmation_id)}"` 依赖 `escapeHtml` 转义 `"`（`oamp/web/app.js:1134-1136` 已含 `"` ⇒ 注入面闭合），但 `confirmation_id` 含空格等字符时的分组行为无断言（同名恒成一组，实际无风险）。属测试面补强候选。

---

## 结论

**PASS**

依据：7 条标准全部 pass、无 fail / partial / blocked；PR 文件验收 1~10 逐条 pass；改动面严格封闭为 3 个代码文件、零改动清单逐项无命中；question 渲染两态与 `options: []` 经独立 harness 复现；permission 分支与原 `decide()` 经**字节级对照**证明逐字未动；Q3（前端不短路空提交）经实跑证明请求确实发出；既有断言强度未削弱（唯一删除行以更强断言替换）；全量套件 387/387 与定向套件 16/16 全绿（exit 0）；零新依赖、零新面板/页面/SSE 事件类型。3 条偏差均为文档/文件形态同步问题，按角色契约不阻塞本迭代关闭，交由主 agent 决定是否同步。
