# 0023 阶段 5 · 末波任务图 `[model_inferred]` 与 Q3 裁决记录

**日期**: 2026-09-14（主 agent 阻塞式取得）
**转呈来源**: `prs/pr-003-tasks.md`（7 项 MI + Q1~Q5）
**性质**: `user_confirmed`

---

## 7 项 `[model_inferred]` —— **全部采纳**

| # | 采纳 |
|---|---|
| MI-1 | question 控件形态判据取「两态产物可区分 + 单值/多值语义可判，优先原生控件（checkbox / 单值控件）」；**不替实现钉 tag**，等价形态须交付等价可执行判据 |
| MI-2 | question 分支文本输入可复用 `.inbox-text` 类名，但文案可不同于 permission 的既有 placeholder（**后者逐字保留**） |
| MI-3 | 控件类名清单与提交体构造签名须写入交付说明（作为 T3 的 CSS 选择器接口与沙箱判据接口） |
| MI-4 | 多问题独立的可执行判据取「沙箱内以两条 question 条目执行移出路径（`renderInbox` 以桩替代）」或等价判据 |
| MI-5 | 既有 body 逐字锁（`test :342`）适配允许两条路径（`decide` 逐字不变 + 新增 question body 锁；或两类 body 合一后锁两类），**强度不得降** |
| MI-6 | 手势入口计数断言（`:379`）2→3 时须保留「落点在第三栏交互回调内」的断言强度 |
| MI-7 | 「改动面封闭」的代码文件集合 = **3 个**（`web/app.js` / `web/style.css` / `test/inbox-console.test.js`）；任务图 docs 文件不计入代码面 |

## Q3 · L2-7「前端只做可用性提示」的强度 —— **不短路，仅可选提示**

- 前端**不短路**空提交（允许视觉提示但**不作验收要求**）——保证「其余非法提交 ⇒ 保留在途」分支在 UI 上可达（与后端 400 路径呼应）。

## planner 自行登记的 4 项（非用户决策点，已确认）

| # | 登记 |
|---|---|
| Q1 | 通知正文模板对 question 不贴切（`notify.js`「请求执行 ask_user：…」）⇒ PR 文件把 `web/notify.js` 列零改动且无验收覆盖 ⇒ 本任务图不承接，登记为**下一迭代候选** |
| Q2 | question 控件 tag 未被 PR/架构钉死 ⇒ 由 MI-1 给最小可执行判据 |
| Q4 | `decide` 逐字不变 × 既有 body 锁「需相应适配」的表面张力 ⇒ 两条路径相容（各有逐字锁），由实现者按落地形态定 |
| Q5 | 阶段 6 浏览器实测面 vs 本任务图判据面 ⇒ 本图运行期判据一律在 vm 沙箱内；**真实交互链归阶段 6**，不虚设 E2E 任务 |
