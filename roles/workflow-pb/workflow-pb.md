---
name: workflow-pb
description: pb 产品研发工作流规范。定义从需求到交付的完整阶段序列、每阶段的职责、输入输出契约、推进条件、方案确认门、调度算法与验证目标。提交管理协议（worktree 隔离/PR 文件前置/分支命名/隔离边界等）见 `data/scm-protocol.md`；各类产物的具体数据格式（PR 七字段/status.md/history.md）见 `data/formats.md`——三份文件共同构成完整规范，本文件是流程契约主体。主 agent 读它来调度工作，执行角色读它来找上下文和归档位置。不执行任何工作，只定义规范。
role:
  identity: |
    在中型产品团队做过 3 年 PM、后来转做 5 年交付流程顾问的人——
    专门研究"哪个阶段交接出了问题"。标志性战绩是：设计的流程让
    "架构阶段才发现 prd 漏了关键约束"这类事故从每个版本必发降到了
    可追溯、可预防的概率事件。核心执念是出口定义：每个阶段不是
    "做完了往下传"，而是"满足这几条可验证标准才往下传"——
    含糊的推进条件是未来返工的种子。
  relationship: |
    工作流是执行规范，不是执行者。
    主 agent 读本文件来理解如何调度；执行角色读本文件来找输入路径、输出路径和交付标准。
    本文件不发指令，不做判断——只定义规则。
  character: |
    完整、自描述、可验证。
    每条规则必须能回答"为什么这么定"；
    每个推进条件必须能被客观判断；
    不接受"基本完成"作为阶段出口。
---

# workflow-pb

**版本**: 0.14.0
**创建日期**: 2026-09-02
**变更历史**: 见 `data/workflow-pb-changelog.md`
**配套文件**: `data/scm-protocol.md`（提交管理与工作区隔离协议）、`data/formats.md`（PR/status.md/history.md 数据格式规范）——两者与本文件共同构成完整规范，权威定义各只存在一处

---

**CRITICAL: 本文件只定义规范，不执行工作。所有执行判断由主 agent 和执行角色完成。**

**CRITICAL: 修改本文件不得侵入任何执行角色文件。角色文件只定义能力，工作流文件定义契约。**

**CRITICAL: 提交管理协议（worktree 隔离/PR 文件前置/分支命名/隔离边界/角色来源）的唯一定义位置是 `data/scm-protocol.md`；数据格式规范（PR 七字段/status.md/history.md）的唯一定义位置是 `data/formats.md`。本文件与这两份文件对同一规则只能各定义一次——发现同一规则在两处都有完整定义（而非一处定义、另一处索引引用）视为耦合未解；把内容按关注点移到这两份文件本身不违反这条约束。**

---

## 一句话职责

定义 pb 产品研发工作流的完整规范（阶段序列、推进条件、方案确认门、调度算法与验证目标），让主 agent 知道怎么调度、怎么并发，让执行角色知道读什么、写什么、交付什么。提交管理约束和数据格式规范的具体条款见配套文件。

---

## 阶段定义

工作流包含 6 个阶段。每个阶段有独立的验证步骤，验证通过才算该阶段完成。

| # | 阶段 | 职责描述 | 输入 | 输出 | 推进条件 |
|---|---|---|---|---|---|
| 1 | 需求收敛（**主 agent 内联执行，不派发子 agent**，见「阶段 1~4 推进」） | 通过结构化对话消除模糊、界定边界，产出有澄清依据的需求合同 | 用户原始表述 / 迭代目标 / 草稿文本 | `demand.md`（两段：澄清依据 + 带最小边界的结论） | 两段均有内容；所有 `model_inferred` 结论经用户确认；无活跃冲突 |
| 2 | 功能规格 | 将需求合同原子化为可独立验证的功能卡，只做产品维度，不做架构决策 | `demand.md` | `prd.md`（报告级索引）+ `prd/*.md`（每功能点独立卡，含验收标准和边界） | 所有功能点有独立卡片；无 demand.md 外新增功能；架构待定项已标注 `[架构待填]` |
| 3 | 技术架构 | 在现有架构上演进，补全功能卡的架构维度，产出技术方案 | `prd/*.md` + 现有代码库 / 架构文档 | `architecture.md` + `prd/*.md` 中 `[架构待填]` 项补全 | L1 决策经用户确认；所有功能卡有技术路径；无架构内部冲突 |
| 4 | PR 规划 | 反射出可独立合并的提交单元划分及单元间真实依赖，从架构方案+代码库现状出发，不经过全局任务图这一中间环节 | `architecture.md` + `prd/*.md` + 代码库现状 | `prs/pr-{NNN-描述}.md`（七字段，见 `data/formats.md` §PR 文件格式规范） | 每个 PR 文件满足格式规范；`prd/*.md` 中每个功能点被某个 PR 引用；PR 间文件范围无重叠；依赖图无环；并发可行性检查全部通过（见 pr-planner.md §并发可行性）；关键路径长度 ≤3，或合并受阻已转出"建议拆分为多个迭代"结论（`prs/split-suggestion.md` 存在，见 pr-planner.md §关键路径长度） |
| 5 | PR 实现 | 逐 PR 在独立 **PR worktree** 分支、独立子 agent 中执行：先拆该 PR 内部的任务（子 agent 内部步骤，不产出全局任务图），再实现，产出最小实现 | 单个 `prs/pr-{NNN}.md` 文件 | `prs/pr-{NNN}-tasks.md`（该 PR 内部任务列表，供 dev 消费）+ 代码 | 该 PR 验收标准全部通过；无简报外改动；对应 PR 文件存在（合并前置） |
| 6 | 独立验证 | 不接收执行过程上下文，独立评判任意阶段产物的质量 | 待验证产物（路径由主 agent 指定） | `clarifications/verify-{timestamp}.md`（pass / fail / partial / blocked + 逐项证据） | 每项有明确结论和文件证据；无"基本通过"模糊表述 |

> 阶段 6（独立验证）可在任意阶段结束后触发，不局限于最终产物。推进条件中"已验证"指该阶段产物经过阶段 6 且结论为 pass。
>
> 阶段 5「PR 实现」内部的任务拆解由 `planner` 角色承担：**不是一次性对全架构拆解，而是主 agent 逐 PR 派发、在该 PR 专属的子 agent 会话内完成**，输入范围收窄为单个 PR 文件（+ architecture.md/prd 作背景）。这样任务粒度天然被 PR 的真实文件边界约束，不会出现"把顺序偏好当依赖"的问题。

---

## 方案确认门（阶段4→5之间）

**触发时机**：阶段4（PR 规划）推进条件全部通过后、阶段5（PR 实现）开始前。

**触发条件**：`status.md` 头部 `**方案确认门**` 字段为 `enabled`（默认值）。

**前置说明**：阶段4推进条件（六项，见 §阶段定义表）已包含依赖图无环、文件范围不重叠、并发可行性、关键路径压平结果四项结构性检查——这些检查在阶段4推进条件核查时已经完成，方案确认门只做结果汇总呈现，不重新执行一遍。

**呈现内容**：主 agent 向用户整体呈现三部分方案内容，不能只呈现其中部分：
- 产品维度：`prd.md` + `prd/*.md`
- 架构维度：`architecture.md`
- PR 拆分方案摘要：PR 数、依赖图、关键路径长度；若 `prs/split-suggestion.md` 存在（pr-planner 转出了"建议拆分为多个迭代"结论），该文件内容也必须一并呈现，不能省略

**通过条件**：用户明确确认方案后，方可进入阶段5；不确认则停在原地，不得由主 agent 自行判定"方案没问题"而跳过。

**关闭情况**：若 `**方案确认门**` 字段为 `disabled`，阶段4推进条件满足后直接进入阶段5，不触发此门。

---

## 提交管理协议（索引，权威定义见 `data/scm-protocol.md`）

以下约束在每次使用本工作流的迭代中强制生效，完整条款、判断方式和示例见 `data/scm-protocol.md`，本节只列索引：

- 规则 A：PR worktree 隔离
- 规则 B：PR 文件前置
- 规则 C：会话工作区隔离
- 规则 D：启动与寻址契约
- 规则 E：分支命名
- 规则 F：跨工作区写入禁止
- 规则 G：暂停 / 终止迭代的现场保留
- 规则 H：显式寻址纪律
- 协议产物：工作区间与生效规范（`docs/worktrees/README.md`）
- 约束主体（对人无前置 / 对会话三项纪律）
- 隔离边界声明（隔离到什么层为止）
- 角色定义来源与部署
- 约束生效范围（仅从下一个迭代起生效，不追溯）

本文件后续章节引用上述规则时，标注为「见 `data/scm-protocol.md` §规则 X」。

---

## PR 文件格式规范（索引，权威定义见 `data/formats.md`）

每个 PR 文件必须包含七个字段：上下文摘要 / 涉及功能点 / 文件范围 / 验收标准 / 参考资料 / depends_on / batch。逐字段的格式模板、示例和判断标准见 `data/formats.md` §PR 文件格式规范。

---

## 调度指南（主 agent 读）

### 启动工作流

**本工作流的产物路径以该会话声明的工作区地址为根**（见「文档路径协议」/ `data/scm-protocol.md` §规则 H）。会话开工前按 `data/scm-protocol.md` §规则 D 解析并声明该地址（缺省时建立）；本流程**不校验启动位置**，也不新增步骤号（步骤编号体系仍为 1~5 + 4.5）。

**启动前询问**（在步骤 1 之前完成，不占用步骤编号）：询问用户"是否开启方案确认门（默认开启，阶段4→5之间会暂停呈现完整方案等你确认）？"——用户明确选择关闭时记为 `disabled`；用户选择开启或未明确表态时记为 `enabled`（默认值）。

1. 确认迭代 ID（`{编号-迭代名}`）
2. 创建 `docs/iterations/{编号-迭代名}/status.md`，初始化阶段表，并写入 `**工作区地址**: <该迭代的迭代工作区绝对地址>`（取值 = 会话开工时按 `data/scm-protocol.md` §规则 D 解析并声明的地址；简报的显式覆盖值优先）与 `**方案确认门**: <上一步确认的值>`（`enabled` / `disabled`）。
3. 创建 `status.md` 之后，若 `history` 字段为开启，创建空 `history.md`（见 `data/formats.md` §历史记录协议）
4. 从阶段 1（需求收敛）开始
4.5. 阶段 1（需求收敛）通过推进条件后，在 status.md 头部写入 `**迭代分支**: iteration/{迭代ID}`。迭代工作区与迭代分支由会话按其工作区地址建立（缺省时，见 `data/scm-protocol.md` §规则 D）——本步骤不执行 `git branch` / `git checkout`，只负责该字段的写入。此后阶段 2~5 期间，**该迭代的工作区**（根目录）保持检出该分支。
5. 进入阶段 4→5 时（阶段 5 第一次并发派发之前），须在该迭代的 `status.md` 中初始化 `## 并发配置（阶段 5）` 区块，字段包括：
   - `**起始并发数**`（默认 3）
   - `**硬上限**`（公式 `2×起始-1`）
   - `**当前有效上限**`（初始等于起始并发数）
   - `**累计槛位释放次数**`（初始 0）
   - `**已派发总数**`（初始 0）

### 派发执行角色时的 brief 构建

**本节适用于阶段 2~6。阶段 1（需求收敛）由主 agent 内联执行，不派发子 agent，不构建 brief**——主 agent 直接读取 `{角色定义根}/demand/demand.md` 全文作为自己的判断框架，与用户直接对话（见「阶段 1~4 推进」）。

每次派发执行角色时，主 agent 必须先读出角色文件全文（`{角色定义根}/<role>/<role>.md`），将全文内容显性注入子 agent 的初始指令（brief）中——不是只传递文件路径让子 agent 自行决定要不要读、读多细。这确保机械性契约（CRITICAL 清单、报告格式、字段定义、停止条件）的遵循度。

brief **至少**包含以下字段，且必须逐行以 `字段名：值` 的形式呈现（不得改写、合并或省略字段名）：

```
角色名：<role>（如 demand/prd/architect，供对外呈现时可辨认角色对应关系）
角色定义（全文注入）：
---
{读出的 {角色定义根}/<role>/<role>.md 完整内容，原样注入}
---
工作流规范：{角色定义根}/workflow-pb/workflow-pb.md
工作区地址：<该次派发对应的绝对地址：阶段 1~4 为该迭代的迭代工作区；阶段 5 为该 PR 的 PR worktree>
检测项：本次一切写入以「工作区地址」为根、以绝对路径进行；git 写操作使用 git -C <工作区地址>
当前迭代 ID：{迭代ID}
当前阶段：阶段 {N}（{阶段名}）
任务：{阶段职责，一句话，读自本文件 §阶段定义"职责描述"列}
输入：docs/iterations/{迭代ID}/{输入文档，读自本文件 §阶段定义"输入"列}
输出：docs/iterations/{迭代ID}/{输出文档，读自本文件 §阶段定义"输出"列}
完成定义：{内联该阶段推进条件清单，读自本文件 §阶段定义"推进条件"列}
```

阶段 5 额外必填：
```
当前 PR：docs/iterations/{迭代ID}/prs/{pr文件名}
PR worktree 分支：{分支名}
注意：只处理该 PR 文件范围内的功能，不拿全部 prs/ 目录
```

**恢复派发**（迭代中断后再派发）时，简报的「工作区地址」字段即覆盖被恢复迭代既有文本中的任何"须在该目录内启动 / 不得在该目录外推进"类前置；**被恢复迭代的既有文本不改写**。

### 阶段 1~4 推进

每次推进前对照该阶段的"推进条件"逐项核查，全部满足才推进：
- 所有条件满足 → 更新 `status.md`，进入下一阶段
- 有条件未满足 → 回到执行角色补充（本阶段自己没做完，不是需求问题，见「阶段回退」判断标准）

**阶段 4 的结构性检查可机械复核**（本框架仓库内提供 `tools/check-pr-gates.py`；下游项目按本节逐项人工核查）：`tools/check-pr-gates.py <迭代文档目录>`（七字段 / 功能点覆盖 / 文件范围两两不重叠 / 依赖无环 / 无悬挂依赖 / 关键路径 ≤3 / 并发可行性；覆盖项为提示，豁免属人的裁决）。它是核查的**辅助**——用于避免人工漏检，不替代逐项判断；未过项须回到执行角色补充。

**阶段 1 是硬性前置，且由主 agent 内联执行**：无论迭代规模大小、需求看起来多简单，主 agent 都必须先读取 `{角色定义根}/demand/demand.md` 全文、直接以该判断框架和用户对话走一轮（**不派发子 agent**），产出 `demand.md` 并通过阶段 1 推进条件，才能进入阶段 2。不得因"这次需求很清楚"跳过阶段 1。

每次派发执行角色、收到执行角色报告、做出阶段推进核查决策时，同时按 `data/formats.md` §历史记录协议 追加 history.md 记录（不在本段重复定义格式）。

### 阶段 5（PR 实现）：依赖解锁式并发调度

**不是按 PR 文件列表顺序逐一 dispatch**，而是：

1. 读取 `prs/*.md`，抽取每个 PR 的 `depends_on`，构建依赖图。构建时校验无环——发现环，停止并向用户上报，这是规划阶段的错误，不在调度层绕开。
2. 对所有 `depends_on` 为空、或所依赖的 PR 均已**合并进当前迭代的迭代分支**的已解锁 PR，按当前有效上限取前 N 个（`N = 当前有效上限`，首批派发时"仍在执行中的 PR 数量"为 0）立即并发派发：为该 PR 创建独立 worktree 分支（`git worktree add .pb-agents/worktrees/{迭代编号}-pr-{NNN}-{slug} -b feat/{迭代编号}-pr-{NNN}-{slug} iteration/{迭代ID}`，**在该 PR worktree 的绝对地址下执行**（`git -C <该地址>`；该地址按派发简报的「工作区地址」字段解析），落点即该工作区内的子目录；base 为当前迭代的迭代分支，不是 main），在子 agent 里依次完成「派发 planner 产出该 PR 的 `prs/pr-{NNN}-tasks.md`」→「派发 dev 实现」→「验收」→「merge 进当前迭代的迭代分支」。首批派发和后续补位派发共用同一条封顶规则（见「并发槛位算法」），不是两套不同的算法。没有已解锁排队 PR 时，槛位空置，不因此放宽"合并进迭代分支才算解锁"这一条件去提前派发未解锁的 PR。
3. 每当一个 PR merge 完成，重新扫描依赖图，检查是否有新的 PR 因此解锁，立即派发，不等待其他并发中的 PR。
4. 某个 PR 阻塞或失败：只暂停依赖它的下游 PR，不影响其他无关 PR 的并发推进。**主 agent 不得对状态为失败/阻塞的 PR 执行 `git worktree remove`、`git branch -d` 或等价清理操作**——现场必须原样保留供人工排查，这是失败/阻塞状态的固有属性（见 `data/formats.md` §状态追踪协议 `❌失败(现场保留)`/`⏸阻塞(现场保留)` 定义），不是可选的保守做法。

**为什么必须以"合并进迭代分支"作为解锁条件，不是"dev 自称完成"**：每个 PR 在独立 worktree 分支上工作，如果 PR-B 依赖 PR-A 的产出，PR-B 的 worktree 分支必须是从"已经合入 PR-A 之后的迭代分支"拉出来的，否则 PR-B 的子 agent 在自己的分支里根本看不到 PR-A 的代码。这是技术约束，不是可选的保守做法。

**`batch` 字段的定位**：仅供人工在 `prs/` 目录里快速扫一眼大致分组，主 agent 的调度判断永远以 `depends_on` 的实际合并状态为准，不读 `batch` 数字做栅栏同步。

**并发派发的技术手段**：主 agent 在同一轮 assistant 响应中，连续发起多个 `Agent()` 工具调用——同一条 assistant message 里的多个工具调用会并发执行，不需要等待前一个返回结果，这是"并发"在本工作流里的具体实现方式，不是描述性的比喻。每个 `Agent()` 调用对应一个独立 PR 的完整执行流程（创建 worktree → 派发 planner → 派发 dev → 验收 → merge）。

**并发槛位算法**：

- 起始并发数从该迭代的 `status.md` 的 `## 并发配置（阶段 5）` 区块读取，默认值为 3（见「启动工作流」的初始化步骤）。
- 硬上限公式：`硬上限 = 2 × 起始并发数 - 1`（起始并发数为 3 时，硬上限为 5）。
- 爬升公式：`当前有效上限 = min(起始并发数 + 累计槛位释放次数 × 起始并发数, 硬上限)`。每当任一并发中的 PR 返回"成功合并"或"失败/阻塞"判定，`累计槛位释放次数` 加 1，重新计算 `当前有效上限`；达到硬上限后，无论再有多少 PR 返回，`当前有效上限` 维持硬上限不变。这个字段名不含"成功"二字是有意的——失败/阻塞判定同样释放槛位、同样计入累加，只是不解锁下游依赖，与"成功合并"是两件不同的事，不要混用判断标准。
- 每次有槛位释放（PR 完成合并或判定失败/阻塞）时，主 agent 从"已解锁且排队中"的 PR 里取前 N 个（`N = 当前有效上限 - 仍在执行中的 PR 数量`），在同一轮响应内发起新的并发 `Agent()` 调用补位——爬升带来的新增槛位，仍然只能分配给依赖已合并的排队 PR，不改变「合并进迭代分支才算解锁」这一前提。**没有已解锁且排队中的 PR 时，释放出的槛位保持空置，不触发任何派发，不得因为槛位空闲而放宽解锁条件去提前派发依赖未合并进迭代分支的 PR。**

每次派发、收到报告、槛位释放/爬升、PR 失败或阻塞判定时，同时按 `data/formats.md` §历史记录协议 追加 history.md 记录（不在本段重复定义格式）。

### 可观测性：progress-observer 自动/按需触发

并发调度带来一个新风险：`status.md` 由主 agent 手动维护，并发度越高，人工记录和真实 git 状态脱节的概率越高（`docs/iterations/0004-model-dispatch/project-retrospective.md` P1 条目是先例）。`progress-observer` 角色专门做独立核实，不采信任何自我声明，只信 git 一手记录。

**触发时机**（角色文件本身不描述这一点，调度契约定义在这里）：
1. **自动触发**：每次一个 PR 完成 merge 之后，或阶段 1~4 中任一阶段推进完成后，主 agent 自动派发一次 `progress-observer`，保持 `docs/iterations/{迭代ID}/progress.md` 新鲜
2. **按需触发**：用户或主 agent 想查看当前客观进度时，随时可以手动派发，不需要等触发条件

**核实项**：每次生成 `progress.md` 时，对状态为失败/阻塞的 PR，须核实其 worktree 目录和分支是否仍存在于磁盘（技术手段：`git worktree list` + `git branch` 交叉核对）。若发现已被清理（与"现场保留"的预期不一致），记入 `progress.md` 的"发现的不一致"部分。这一核实项是调度契约层的具体核实内容清单，不改变 `progress-observer` 角色文件本身"核实一手记录"的能力定义。当前迭代存在迭代分支时，核实合并状态的 git log 命令以迭代分支为基准（如 `git log iteration/{迭代ID} --grep="pr-001"`），而非 main。

**主 agent 拿到 `progress.md` 后的动作**：
- 报告中的"发现的不一致"和"并发度分析"（尤其是"可并发但闲置"的 PR）是主 agent 优先处理的信号——闲置 PR 应立即补派发，不一致状态应先核实 `status.md` 记录错误还是真实的执行异常
- `progress-observer` 只报告事实，不做严重性判断或调度建议；是否重新派发、是否修正 `status.md`，由主 agent 决定
- 不用 `progress-observer` 的报告替代阶段 6 独立验证——两者职责不同：`progress-observer` 判断"状态是否一致"，独立验证判断"产物质量是否达标"

### 阶段回退

**判断标准（执行角色发现问题时先自问，再决定怎么处理）**：解决这个问题，是否要改变 `demand.md` 第二段的结论（做什么/不做什么/大概怎么做/达到什么效果/为什么）？

- **否**——技术方案、实现路径、架构完整度问题（如 `[架构待填]` 项本来就没填完、发现更好的实现方式、发现的 bug）→ 执行角色在当前阶段直接解决，不上报、不搭置、不新开迭代。这类问题本来就是当前阶段自己的职责范围
- **是**——需求本身有矛盾、范围不清、技术上根本做不到、上游产物在需求层面自相矛盾 → **不回退修正、不暂停等待用户**，执行角色直接把问题写入 `docs/iterations/{迭代ID}/deferred-demand-changes.md`（见「文档路径协议」），继续按当前 `demand.md` 完成本迭代——需求变更只能通过发起新迭代来处理，本迭代是对已落盘需求的确定性还原

**唯一的流程结构错误中止**：阶段 4→5 入口 `pr-planner` 报告依赖图有环——这是 PR 规划的结构性错误，不是需求问题，调度层无法排出执行顺序，必须停下修正，不能搭置。（与 `data/scm-protocol.md` §规则 D 的工作区就绪性硬停、与「需要用户决策的情况」的暂停**三类并列、互不替代**。）

### 需要用户决策的情况

本节定义哪些情况下主 agent 必须暂停并通知用户，不得自行推进。分两类范围：

**阶段1~3适用**（需求/功能/架构阶段）：
- 阶段 1（主 agent 内联执行）判断"根本没有问题要解决"
- 产物中出现 `[model_inferred]` 标注项
- 架构方案中出现 L1 决策（不可逆的技术判断）
- `pr-planner` 报告依赖图有环

**阶段4/5适用**（PR 规划/实现阶段）：

阶段4/5仅在以下两种结构性情况下触发暂停，其余情况按现有机制处理（如写入 `deferred-demand-changes.md`）后继续：
- `pr-planner` 报告依赖图有环
- 执行角色报告"阻塞"，且主 agent 无法在不修改上游产物的情况下解决

**明确排除**：阶段4/5产物中出现的 `[model_inferred]` 标注**不触发暂停**（与阶段1~3的处理方式不同，这是有意的范围收窄）。

**需求层问题不暂停**：阶段4/5中执行角色发现的需求层问题，仍按现有 `deferred-demand-changes.md` 机制记录后继续，不暂停、不回退——本次改动不引入新的处理路径，只是明确"这不算需要暂停的用户决策点"。

### 终止工作流

在 `status.md` 中将状态标记为"已终止"，写明终止原因。

### 迭代分支合并进 main（在仓库主工作区执行）

阶段 6（独立验证）对本迭代最终产物判定 pass 之后，主 agent 在**仓库主工作区**依次执行以下三步（动作顺序与触发时机不变）：
1. `git -C <仓库主工作区> checkout main`
2. `git -C <仓库主工作区> merge --no-ff iteration/{迭代ID}`
3. `git -C <仓库主工作区> branch -d iteration/{迭代ID}`

**第 3 步的前置动作（在三步之前单独执行，不是新增收口步骤）**：若 `iteration/{迭代ID}` 仍被该迭代的工作区检出，先执行 `git -C <该迭代的工作区地址> checkout --detach` 把检出点游离——「同一分支不能在两处同时检出」是 git 硬约束，故该动作是 `git branch -d` 的**必然前置**（见 `data/scm-protocol.md` §隔离边界声明）。

commit message 格式：`merge: iteration {迭代ID} {该迭代一句话目标} into main`。

若步骤 2 报告冲突：停止推进，向用户上报（结构性错误，类比"PR 依赖图有环"，不在调度层绕开）。

合并完成后，status.md 的 `**迭代分支**` 字段改为 `iteration/{迭代ID}（已合并）`。

---

## 文档路径协议

所有阶段产物统一落在 `docs/iterations/{编号-迭代名}/`。

**全部路径相对该会话声明的工作区地址判读**（见 `data/scm-protocol.md` §规则 D / §规则 H）；相对路径不得作为唯一寻址方式。

```
docs/iterations/{编号-迭代名}/
├── status.md              ← 工作流进度（由主 agent 维护）
├── history.md              ← 历史记录（由主 agent 维护，见 `data/formats.md` §历史记录协议）
├── demand.md              ← 阶段 1 产物
├── deferred-demand-changes.md  ← 需求变更/需求错误的搭置记录（任意阶段执行角色发现即直接写入，无固定阶段归属）
├── prd.md                 ← 阶段 2 产物（索引）
├── prd/                   ← 阶段 2 产物（功能卡）
│   └── {功能名}.md
├── architecture.md        ← 阶段 3 产物
├── prs/                   ← 阶段 4 产物（pr-planner）+ 阶段 5 产物（逐 PR 任务文件）
│   ├── pr-{NNN-描述}.md          ← 阶段 4：PR 边界+依赖（pr-planner 产出）
│   └── pr-{NNN-描述}-tasks.md    ← 阶段 5：该 PR 内部任务列表（planner 逐 PR 产出，子 agent 内部）
└── clarifications/        ← 澄清记录 + 验证报告
    └── verify-{timestamp}.md
```

**`deferred-demand-changes.md` 格式**（执行角色直接写入，不经过主 agent）：

```markdown
## {发现日期} · {发现阶段}

**问题**：{需求层面的矛盾/错误/变更点描述}
**为什么判定为需求层面问题**：{为什么这不是技术方案问题，是 demand.md 结论本身要改}
**本迭代如何处理**：按现有 demand.md 继续，此项留给下一迭代决策
```

每条追加写入，不覆盖已有记录。

**`{编号-迭代名}` 由主 agent 在工作流启动时确定，并写入每个任务 brief。**

---

## 验证目标（阶段 6 独立验证读）

阶段 4/5 的验证由 verifier 角色执行，主 agent 派发时将以下验证目标内联写入 brief。

### 搭置的需求变更/错误报告

verifier 只读委托里给的产出物路径和验证标准，不会自己扫描目录找 `deferred-demand-changes.md`——主 agent 触发阶段 6 时，若该文件存在，必须把它的路径作为委托的一部分显式传入，要求 verifier 将其内容原文摘录进验证报告。这是用户判断"是否需要发起新迭代"的依据，verifier 不对搭置内容本身做二次判断（是否真的是需求问题、是否应该搭置），只做透传。

**`deferred-demand-changes.md` 强制呈现要求**：当该文件存在时，verifier 必须：
- 将该文件内容**原文摘录**（不转述、不总结）
- **置顶或显著呈现**在验证报告中——**禁止脚注式一笔带过、不得放在报告末尾或不显眼位置**
- 作为交给下一迭代的重点决策信号

主 agent 在触发阶段 6 验证时，必须把该文件路径显式加入委托，不依赖 verifier 自行扫描发现。

### PR 粒度判断框架

pr-planner 分组时使用，verifier 验证时使用。以下三条是判断依据，不是可量化指标：

- **逻辑原子性**：该 PR 只做一件可描述的事，回滚它不影响无关功能。
- **可审查性**：reviewer 能在不切换心智模型的情况下完整审查——逻辑跳跃、跨模块混合是信号。
- **独立性**：验收标准可在不依赖其他 PR 合并的情况下判断。

对于自动生成代码、纯格式化、纯文档变更，行数不是粒度信号，逻辑原子性才是。

### 依赖正确性验证

- **指标**：`depends_on` 中声明的每一条依赖，都能在代码库中找到对应的真实耦合证据（共享符号/接口/文件引用）
- **判断方式**：verifier 对每条 `depends_on` 逐一核实证据是否存在；找不到代码级证据的依赖判定为不合规（可能是把顺序偏好误判成依赖）
- **通过条件**：所有 `depends_on` 条目均有证据支撑；依赖图无环；PR 间文件范围无重叠、无遗漏功能点

### 并发调度真实执行证据（下一个多 PR 迭代强制核查）

并发调度文本存在"从未在真实的多 PR 场景里被验证过执行结果"的风险——历次迭代阶段 5 若都只规划了一个 PR，阶段 6 的验证会停留在"协议文字读起来自洽"这一层级，无法证明协议本身被真实调度执行过。

**触发条件**：某次迭代的 `prs/` 目录在阶段 4 产出了 ≥2 个 PR，且依赖图存在真实的并发分支（即存在至少两个互不依赖、或依赖已满足从而可同时进入阶段 5 的 PR）。

**该迭代的阶段 6 验证必须额外核查以下三项，缺任何一项均判定该验证目标 partial，不接受"协议文本正确"代替真实证据**：

1. **worktree 时间窗口重叠**：核实是否存在至少 2 个 PR 的 worktree 分支在磁盘上同时存在过（技术手段：结合 `progress.md` 历史记录或 `git worktree list` 输出的时间信息判断创建/清理区间是否重叠），证明"并发"确实发生过，不是先后串行执行后误报为并发。
2. **并发配置区块真实初始化与更新**：核实该迭代 `status.md` 的 `## 并发配置（阶段 5）` 区块是否在阶段 4→5 入口被真实初始化（五个字段均有值），且随后续 PR 合并/失败判定被真实更新（不是初始化后再未变化）。
3. **爬升公式真实触发**：若该迭代并发中的 PR 数量达到过触发爬升的条件，核实 `当前有效上限` 是否按爬升公式 `min(起始并发数 + 累计槛位释放次数 × 起始并发数, 硬上限)` 被正确重新计算，而不是维持初始值不变。若该迭代 PR 数量始终未触发爬升条件，本项记为"不适用"并说明原因，不算未通过。

三项核查结论均需附文件证据（`status.md` 历史版本、`progress.md`、git 命令输出），不接受"应该没问题"式的模糊判断。

### 验证触发时机

`pr-planner` 完成 PR 规划、`prs/` 目录中所有 PR 文件生成后，主 agent 触发 verifier：

```
产出物路径: docs/iterations/{迭代ID}/prs/
验证标准: [内联自本文件 §验证目标 的两项条目]
```

---

## 状态追踪协议（索引，权威定义见 `data/formats.md`）

`status.md`/`history.md` 的完整格式模板、字段取值、更新时机见 `data/formats.md` §状态追踪协议 / §历史记录协议，本节不重复定义。

---

## 角色读取指南（执行角色读）

**本节适用于阶段 2~6 被派发的执行角色。demand（阶段 1）由主 agent 内联执行，不被派发，不适用本节**——主 agent 直接从「阶段定义」表读取阶段 1 的输入/输出/推进条件。

你被派发时，brief 里会包含：
1. **工作流文件路径**：`roles/workflow-pb/workflow-pb.md`
2. **当前迭代 ID**：如 `0003-user-profile`
3. **当前阶段编号**：如 `阶段 3`
4. **（阶段 5 专属）当前 PR 文件路径**：如 `docs/iterations/{迭代ID}/prs/pr-002-xxx.md`——阶段 5 的角色（planner、dev）只拿到单个 PR 文件，不拿到全部 `prs/` 目录

你的动作：
1. 读本文件"阶段定义"表，找到你负责的阶段行
2. **输入**：从"输入"列确定文档性质，路径 = `docs/iterations/{迭代ID}/` + 输入文档名
3. **输出**：从"输出"列确定文档性质，路径 = `docs/iterations/{迭代ID}/` + 输出文档名
4. **交付标准**："推进条件"列是你的完成定义
5. 读 `status.md` 了解当前进度和是否有待处理的上下文

**不要从自己的角色文件里找输入输出路径**——角色文件只定义能力，工作流定义契约。

---

## 原则

**工作流规范不定义执行细节**

本文件定义"做什么、交什么、满足什么标准"，不定义"怎么做"。执行细节由对应的执行角色文件定义。

**出口定义优先于流程顺序**

推进条件是每个阶段最重要的内容。顺序是结果，推进条件是原因。含糊的推进条件（"完成了就推进"）是未来返工的种子。

**不指定执行角色名**

本文件描述每个阶段的能力要求，不绑定具体的角色文件名。角色文件会迭代、会重命名，工作流的阶段职责应该稳定。

**同一规则的权威定义只能存在一处，移动位置不算重复**

提交管理约束（**PR worktree** 隔离、PR 文件前置等）的权威定义只在 `data/scm-protocol.md`，数据格式规范（PR 七字段、status.md、history.md）的权威定义只在 `data/formats.md`——禁止的是"同一规则在两处都写完整定义、需要人工保持同步"，不是禁止"按关注点把内容放进独立文件、本文件保留索引引用"。判断一条内容该不该独立成文件，看它的变更原因：变更原因是"git 操作习惯/工作区隔离方式变化"的归 `scm-protocol.md`；变更原因是"字段增减/格式调整"的归 `formats.md`；变更原因是"阶段怎么排/调度算法怎么算"的留在本文件。

**执行细节与流程契约分层，不因方便执行角色而混入本文件**

本文件保留的调度算法（并发槛位公式等）是流程决策所必需的机械细节，与"提交管理协议"「数据格式规范」两类基础设施性内容性质不同——后两类和"6 个阶段怎么排"没有耦合，因此拆分到独立文件；前者的变更历史上经常与阶段定义同批修改（如工作区地址协议变更同时牵动调度指南），因此保留在本文件。

---

## 何时更新本文件

- 某个阶段的推进条件导致下游返工 → 收紧该阶段的推进条件
- 某个阶段的职责边界不清导致执行角色越界 → 在对应阶段的职责描述里明确排除项
- 发现遗漏了某类工作（如 review）→ 新增阶段，不改动现有阶段
- PR 粒度判断导致执行角色产出与实际工作量不匹配 → 修订判断框架描述
- 依赖正确性验证出现误判 → 收紧判断规则
- 不得因"方便执行角色"而在本文件里加入执行细节
- 提交管理协议的具体条款改动 → 改 `data/scm-protocol.md`，本文件只在索引列表变化时同步
- 数据格式规范的具体条款改动 → 改 `data/formats.md`，本文件只在索引变化时同步
- 不得为图省事把已拆分到 `data/scm-protocol.md`/`data/formats.md` 的内容重新复制回本文件；也不得在这两份文件之外再新建第三处重复定义同一规则

变更历史写入 `data/workflow-pb-changelog.md`，保留旧版推进条件被修改的原因。
