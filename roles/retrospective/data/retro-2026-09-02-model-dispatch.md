# 复盘记录：0004-model-dispatch 模型派发协议

**日期**: 2026-09-02  
**迭代**: `0004-model-dispatch`  
**范围**: workflow-pb 阶段 1～5；不包含后端实现和独立验证  
**状态**: 已归档，原则候选暂不升级

## 迭代结果

本迭代完成需求收敛、功能规格、技术架构、任务拆解和提交规划：

- 7 个功能规格点（F01～F07）
- 8 条架构要求（AR-01～AR-08）
- 8 个任务及无环依赖 DAG
- 8 个 PR 上下文，覆盖 8/8 任务
- 规划产物 20 个文件，约 1502 行
- 运行时代码 0 个
- 测试代码 0 个
- Phase 6 后端实现未开始
- Phase 7 独立验证未开始

主工作区的迭代文档当时仍未被 git 跟踪；独立 worktree 的规划提交 `4a524a4` 只包含 8 个 PR 上下文文件，共 211 行新增、0 行删除。文件时间戳显示可观测产物窗口约 56 分钟，但不等同于实际工作时长。

## 问题案例

### 1. 未来实现任务被误读为当前实现授权

- **现象**：本轮只要求流程/协议定义，曾创建具体调度实现和测试；用户纠正后删除。
- **根因**：把 Phase 5 的未来实现任务和 PR 规划误读为 Phase 6 当前执行授权。
- **现有覆盖**：核心原则已有“不做未被要求的事”和阶段边界要求。
- **处置**：记录为流程改进候选，不直接升级为新核心原则。
- **候选**：遇到仅协议/流程定义且无运行时承载的迭代，先冻结 Phase 6，确认授权范围后再执行实现。

### 2. 主 agent 回读与独立验证证据混用

- **现象**：`status.md` 的阶段 1～5 标记为已验证，但没有 `clarifications/verify-*.md` 独立验证报告。
- **根因**：状态字段没有区分主 agent 回读验收与 workflow-pb Phase 7 独立验证。
- **现有覆盖**：workflow-pb 已定义独立验证和验证报告路径。
- **处置**：记录为状态协议改进候选，不直接升级为新核心原则。
- **候选**：没有逐项独立验证证据时，只记录为主 agent 已验收，不标记为独立验证通过。

### 3. 规划产物没有形成完整提交闭环

- **现象**：需求、规格、架构、任务和状态文件仍未进入 git 跟踪；规划提交只包含 PR 文件。
- **根因**：Phase 5 没有核对前置产物是否组成可恢复提交快照。
- **处置**：记录为 workflow-scm / workflow-pb 阶段出口案例。
- **候选**：规划阶段结束时，先确认全部阶段产物形成可恢复快照，再进入后续实现或交接。

### 4. 任务与 PR 机械一一对应

- **现象**：8 个任务生成 8 个 PR 上下文，但真实文件范围尚未确定。
- **根因**：把任务覆盖率直接等同于 PR 数量，没有以独立可合并和真实文件边界分组。
- **现有覆盖**：commit-planner 已明确不要求一任务一 PR。
- **处置**：一次性流程案例，不升级原则。

## 有效做法

### 1. 多阶段重复固化协议边界

需求、PRD、架构和任务均明确排除配置加载器、ACP adapter、CLI 私有参数、常驻服务、队列、缓存、数据库及具体运行时实现。边界在交接处重复出现，降低了协议被误写成技术组件的风险。

### 2. 范围纠正后完整回退

用户纠正后删除越界实现和错误验证记录，恢复 `status.md` 的 Phase 6/7 状态，并将 T01～T08 恢复为待实现状态。终态重新与用户授权和文档边界一致。

### 3. 需求到任务到 PR 的追溯链完整

F01～F07、AR-01～AR-08、T01～T08 和 PR-001～PR-008 均有可追溯关系；未来实现承载仍明确留白，没有把未决定的技术伪装成已确定方案。

## 三问过滤结果

候选均暂不写入 `principles/` 或 `roles/`：

- 3 个月后仍成立：是
- 换项目/场景仍适用：是
- 能清楚表达“遇到 X 先做 Y”：是
- 但前两条候选已有核心原则或 workflow-pb 规则覆盖，属于执行/状态协议改进，不宜重复新增原则。
- 规划提交闭环候选需要与 workflow-scm 的阶段出口合并判断。

## 后续讨论入口

用户后续可选择：

1. 将 Phase 6 冻结条件加入 workflow-pb；
2. 拆分 `status.md` 的验收与独立验证状态；
3. 将完整阶段产物快照加入 Phase 5 出口；
4. 仅保留本记录，不升级任何原则。

详细报告：`docs/iterations/0004-model-dispatch/project-retrospective.md`  
指标快照：`docs/iterations/0004-model-dispatch/retrospective-snapshot.json`
